Index: git/drivers/pinctrl/renesas/pinctrl-rzg2l.c
===================================================================
--- git.orig/drivers/pinctrl/renesas/pinctrl-rzg2l.c
+++ git/drivers/pinctrl/renesas/pinctrl-rzg2l.c
@@ -150,6 +150,7 @@
 #define HIGH_LEVEL	2
 #define LOW_LEVEL	3
 #define IRQ_MASK	0x3
+#define BOTH_EDGE	8
 
 #define TINT_MAX       32	/* Maximum 32 Interrupts can be supported */
 
@@ -433,6 +434,7 @@ struct rzg2l_pinctrl {
 	struct irq_chip			irq_chip;
 	unsigned int			irq_start;
 	atomic_t			wakeup_path;
+	unsigned int			irq_type;
 
 	/* This array will store GPIO IDs for TINT[0-32] with value:
 	 * - [15-0] bits: store GPIO IDs (ID = port * 8 + bit).
@@ -1589,7 +1591,11 @@ static void rzg2l_gpio_irq_unmask(struct
 	} else {
 		u32 irq_type;
 
-		if (tint_slot > 15) {
+		if (pctrl->irq_type == BOTH_EDGE) {
+			u8 reg8 = readb(pctrl->base + PIN(port));
+
+			irq_type = (reg8 & BIT(bit)) ? FALLING_EDGE : RISING_EDGE;
+		} else if (tint_slot > 15) {
 			reg32 = readl(pctrl->base_tint + TITSR1);
 			reg32 = reg32 >> ((tint_slot - 16) * 2);
 			irq_type = reg32 & IRQ_MASK;
@@ -1648,6 +1654,9 @@ static int rzg2l_gpio_irq_set_type(struc
 	 * About level type, we do not support because we can not clear
 	 * after triggering.
 	 */
+	case IRQ_TYPE_EDGE_BOTH:
+		irq_type = BOTH_EDGE;
+		break;
 	case IRQ_TYPE_EDGE_RISING:
 		irq_type = RISING_EDGE;
 		break;
@@ -1663,6 +1672,7 @@ static int rzg2l_gpio_irq_set_type(struc
 	default:
 		return -EINVAL;
 	}
+	pctrl->irq_type = irq_type;
 
 	spin_lock_irqsave(&pctrl->lock, flags);
 
@@ -1678,6 +1688,11 @@ static int rzg2l_gpio_irq_set_type(struc
 
 	pctrl->tint[tint_slot] = BIT(16) | hw_irq;
 
+	if (irq_type == BOTH_EDGE) {
+		u8 reg8 = readb(pctrl->base + PIN(port));
+
+		irq_type = (reg8 & BIT(bit)) ? FALLING_EDGE : RISING_EDGE;
+	}
 	if (tint_slot > 15) {
 		reg32 = readl(pctrl->base_tint + TITSR1);
 		reg32 &= ~(IRQ_MASK << ((tint_slot - 16) * 2));
