Index: optee-txmp-2550/core/arch/arm/plat-stm32mp2/main.c
===================================================================
--- optee-txmp-2550.orig/core/arch/arm/plat-stm32mp2/main.c
+++ optee-txmp-2550/core/arch/arm/plat-stm32mp2/main.c
@@ -243,13 +243,18 @@ bool stm32mp_supports_cpu_opp(uint32_t o
 	size_t bit_len = 0;
 	uint32_t id = 0;
 
-	if (stm32_bsec_find_otp_in_nvmem_layout("part_number_otp",
-						&otp, NULL, &bit_len))
-		return false;
-
-	if (stm32_bsec_read_otp(&part_number, otp))
-		return false;
+	if (!part_number) {
+		TEE_Result res;
 
+		res = stm32_bsec_find_otp_in_nvmem_layout("part-number-otp",
+							  &otp, NULL, &bit_len);
+		if (res) {
+			EMSG("'part-number-otp' not found in DTB");
+			return false;
+		}
+		if (stm32_bsec_read_otp(&part_number, otp))
+			return false;
+	}
 	/* The bit 31 indicate support of 1.5GHz in RPN (variant d/f) */
 	if (part_number & BIT(31))
 		id = BIT(1);
Index: optee-txmp-2550/core/drivers/stm32_iwdg.c
===================================================================
--- optee-txmp-2550.orig/core/drivers/stm32_iwdg.c
+++ optee-txmp-2550/core/drivers/stm32_iwdg.c
@@ -479,7 +479,7 @@ static TEE_Result stm32_iwdg_parse_fdt(s
 	/* Get and check timeout value */
 	cuint = fdt_getprop(fdt, node, "timeout-sec", NULL);
 	if (!cuint)
-		return TEE_ERROR_BAD_PARAMETERS;
+		return TEE_SUCCESS;
 
 	iwdg->timeout = (int)fdt32_to_cpu(*cuint);
 	if (!iwdg->timeout)
@@ -543,7 +543,7 @@ static TEE_Result stm32_iwdg_setup(struc
 
 	iwdg_wdt_get_version_and_status(iwdg);
 
-	if (iwdg_wdt_is_enabled(iwdg)) {
+	if (iwdg_wdt_is_enabled(iwdg) && iwdg->timeout) {
 		/* Configure timeout if watchdog is already enabled */
 		res = configure_timeout(iwdg);
 		if (res)
Index: optee-txmp-2550/core/arch/arm/dts/stm32mp251.dtsi
===================================================================
--- optee-txmp-2550.orig/core/arch/arm/dts/stm32mp251.dtsi
+++ optee-txmp-2550/core/arch/arm/dts/stm32mp251.dtsi
@@ -37,6 +37,12 @@
 	cpu0_opp_table: cpu0-opp-table {
 		compatible = "operating-points-v2";
 
+		opp-1000000000 {
+			opp-hz = /bits/ 64 <1000000000>;
+			opp-microvolt = <800000>;
+			opp-supported-hw = <0x3>;
+		};
+
 		opp-1200000000 {
 			opp-hz = /bits/ 64 <1200000000>;
 			opp-microvolt = <800000>;
@@ -1168,27 +1174,27 @@
 					#address-cells = <1>;
 					#size-cells = <0>;
 
-					voltd-vddio1 {
+					voltd-vddio1@0 { /* vddio_sdcard */
 						reg = <VOLTD_SCMI_VDDIO1>;
 						voltd-supply = <&vddio1>;
 					};
 
-					voltd-vddio2 {
+					voltd-vddio2@1 { /* v1v8 */
 						reg = <VOLTD_SCMI_VDDIO2>;
 						voltd-supply = <&vddio2>;
 					};
 
-					voltd-vddio3 {
+					voltd-vddio3@2 { /* vddio_pmic */
 						reg = <VOLTD_SCMI_VDDIO3>;
 						voltd-supply = <&vddio3>;
 					};
 
-					voltd-vddio4 {
+					voltd-vddio4@3 { /* vddio_pmic */
 						reg = <VOLTD_SCMI_VDDIO4>;
 						voltd-supply = <&vddio4>;
 					};
 
-					voltd-vdda18adc {
+					voltd-vdda18adc@7 { /* v1v8 */
 						reg = <VOLTD_SCMI_ADC>;
 						voltd-supply = <&vdda18adc>;
 					};
@@ -2209,7 +2215,7 @@
 					#address-cells = <1>;
 					#size-cells = <0>;
 
-					voltd-vdd33ucpd {
+					voltd-vdd33ucpd@5 {
 						reg = <VOLTD_SCMI_UCPD>;
 						voltd-supply = <&vdd33ucpd>;
 					};
@@ -2902,11 +2908,11 @@
 			#address-cells = <1>;
 			#size-cells = <1>;
 
-			part_number_otp@24 {
+			part-number-otp@24 {
 				reg = <0x24 0x4>;
 			};
 
-			package_otp@1e8 {
+			package-otp@1e8 {
 				reg = <0x1e8 0x1>;
 			};
 
Index: optee-txmp-2550/core/drivers/stm32_bsec3.c
===================================================================
--- optee-txmp-2550.orig/core/drivers/stm32_bsec3.c
+++ optee-txmp-2550/core/drivers/stm32_bsec3.c
@@ -987,13 +987,27 @@ TEE_Result stm32_bsec_find_otp_in_nvmem_
 					       size_t *otp_bit_len)
 {
 	size_t i = 0U;
+	char *alt_name = NULL;
 
 	if (!name)
 		return TEE_ERROR_BAD_PARAMETERS;
 
-	for (i = 0U; i < bsec_dev.cell_count; i++) {
-		if (!bsec_dev.cells[i].name ||
-		    strcmp(name, bsec_dev.cells[i].name))
+	if (strchr(name, '-')) {
+		char *p;
+
+		alt_name = strdup(name);
+		p = alt_name;
+		while ((p = strchr(p, '-')))
+			*p = '_';
+	}
+	for (i = 0; i < bsec_dev.cell_count; i++) {
+		if (!bsec_dev.cells[i].name)
+			continue;
+		if (alt_name && !strcmp(alt_name, bsec_dev.cells[i].name))
+			IMSG("WARNING: obsolete property '%s' should be replaced with '%s'",
+			     alt_name, name);
+		if (strcmp(name, bsec_dev.cells[i].name) &&
+		    (alt_name && strcmp(alt_name, bsec_dev.cells[i].name)))
 			continue;
 
 		if (otp_id)
@@ -1009,8 +1023,10 @@ TEE_Result stm32_bsec_find_otp_in_nvmem_
 		     name, i, bsec_dev.cells[i].otp_id,
 		     bsec_dev.cells[i].bit_offset, bsec_dev.cells[i].bit_len);
 
+		free(alt_name);
 		return TEE_SUCCESS;
 	}
+	free(alt_name);
 
 	return TEE_ERROR_ITEM_NOT_FOUND;
 }
Index: optee-txmp-2550/core/drivers/stm32_cpu_opp.c
===================================================================
--- optee-txmp-2550.orig/core/drivers/stm32_cpu_opp.c
+++ optee-txmp-2550/core/drivers/stm32_cpu_opp.c
@@ -115,7 +115,6 @@ bool opp_voltage_is_supported(struct reg
 	int new_volt_uv = 0;
 	int min_uv = 0;
 	int max_uv = 0;
-	unsigned int i = 0;
 	struct regulator_voltages_desc *desc = NULL;
 	const int *levels = NULL;
 	TEE_Result res = TEE_ERROR_GENERIC;
@@ -131,6 +130,8 @@ bool opp_voltage_is_supported(struct reg
 	}
 
 	if (desc->type == VOLTAGE_TYPE_FULL_LIST) {
+		unsigned int i = 0;
+
 		for (i = 0 ; i < desc->num_levels; i++) {
 			if (levels[i] >= target_volt_uv) {
 				new_volt_uv = levels[i];
@@ -357,26 +358,26 @@ static int cmp_cpu_opp_by_freq(const voi
 
 static int min_cpu_voltage(struct cpu_dvfs *dvfs, size_t count)
 {
-	int min_mv = INT_MAX;
+	int min_uv = INT_MAX;
 	size_t n = 0;
 
 	for (n = 0; n < count; n++)
-		min_mv = MIN(min_mv, dvfs[n].volt_uv);
+		min_uv = MIN(min_uv, dvfs[n].volt_uv);
 
-	assert(min_mv < INT_MAX);
-	return min_mv;
+	assert(min_uv < INT_MAX);
+	return min_uv;
 }
 
 static int max_cpu_voltage(struct cpu_dvfs *dvfs, size_t count)
 {
-	int max_mv = INT_MIN;
+	int max_uv = INT_MIN;
 	size_t n = 0;
 
 	for (n = 0; n < count; n++)
-		max_mv = MAX(max_mv, dvfs[n].volt_uv);
+		max_uv = MAX(max_uv, dvfs[n].volt_uv);
 
-	assert(max_mv > 0);
-	return max_mv;
+	assert(max_uv > 0);
+	return max_uv;
 }
 
 TEE_Result optee_scmi_server_init_dvfs(const void *fdt __unused,
@@ -386,9 +387,9 @@ TEE_Result optee_scmi_server_init_dvfs(c
 {
 	TEE_Result res = TEE_ERROR_GENERIC;
 	unsigned int *dvfs_khz = NULL;
-	unsigned int *dvfs_mv = NULL;
+	unsigned int *dvfs_uv = NULL;
 	unsigned int *dvfs_opp_khz = NULL;
-	unsigned int *dvfs_opp_mv = NULL;
+	unsigned int *dvfs_opp_uv = NULL;
 	size_t opp = 0;
 	struct cpu_dvfs *sorted_dvfs = NULL;
 	unsigned int scpfw_default_opp_index = UINT_MAX;
@@ -455,12 +456,12 @@ TEE_Result optee_scmi_server_init_dvfs(c
 
 	/* Feed SCP-firmware with CPU DVFS configuration data */
 	dvfs_khz = calloc(cpu_opp.opp_count, sizeof(*dvfs_khz));
-	dvfs_mv = calloc(cpu_opp.opp_count, sizeof(*dvfs_mv));
-	assert(dvfs_khz && dvfs_mv);
+	dvfs_uv = calloc(cpu_opp.opp_count, sizeof(*dvfs_uv));
+	assert(dvfs_khz && dvfs_uv);
 
 	for (opp = 0; opp < cpu_opp.opp_count; opp++) {
 		dvfs_khz[opp] = sorted_dvfs[opp].freq_khz;
-		dvfs_mv[opp] = sorted_dvfs[opp].volt_uv / U(1000);
+		dvfs_uv[opp] = sorted_dvfs[opp].volt_uv;
 		if (cpu_opp.default_opp_freq == dvfs_khz[opp])
 			scpfw_default_opp_index = opp;
 	}
@@ -475,21 +476,21 @@ TEE_Result optee_scmi_server_init_dvfs(c
 	 * Only perfd[0] will be filled
 	 */
 	dvfs_opp_khz = calloc(cpu_opp.opp_count, sizeof(*dvfs_opp_khz));
-	dvfs_opp_mv = calloc(cpu_opp.opp_count, sizeof(*dvfs_opp_mv));
+	dvfs_opp_uv = calloc(cpu_opp.opp_count, sizeof(*dvfs_opp_uv));
 
 	channel_cfg->perfd_count = 1;
 	channel_cfg->perfd = calloc(channel_cfg->perfd_count,
 				    sizeof(*channel_cfg->perfd));
 
-	if (!dvfs_opp_khz || !dvfs_opp_mv || !channel_cfg->perfd) {
-		free(dvfs_opp_mv);
+	if (!dvfs_opp_khz || !dvfs_opp_uv || !channel_cfg->perfd) {
+		free(dvfs_opp_uv);
 		free(dvfs_opp_khz);
 		free(cpu_opp.scp_cpu_opp_levels_uv);
 		free(channel_cfg->perfd);
 
 		return TEE_ERROR_OUT_OF_MEMORY;
 	}
-	memcpy(dvfs_opp_mv, dvfs_mv, cpu_opp.opp_count * sizeof(*dvfs_opp_mv));
+	memcpy(dvfs_opp_uv, dvfs_uv, cpu_opp.opp_count * sizeof(*dvfs_opp_uv));
 	memcpy(dvfs_opp_khz, dvfs_khz,
 	       cpu_opp.opp_count * sizeof(*dvfs_opp_khz));
 
@@ -498,13 +499,13 @@ TEE_Result optee_scmi_server_init_dvfs(c
 		.initial_opp = scpfw_default_opp_index,
 		.dvfs_opp_count = cpu_opp.opp_count,
 		.dvfs_opp_khz = dvfs_opp_khz,
-		.dvfs_opp_mv = dvfs_opp_mv,
+		.dvfs_opp_uv = dvfs_opp_uv,
 		.clk = &cpu_opp.scp_clock,
 		.regulator = &cpu_opp.scp_regulator,
 	};
 
 	free(dvfs_khz);
-	free(dvfs_mv);
+	free(dvfs_uv);
 
 	return TEE_SUCCESS;
 }
@@ -561,7 +562,6 @@ TEE_Result stm32_cpu_opp_read_level(unsi
 static TEE_Result cpu_opp_pm(enum pm_op op, unsigned int pm_hint __unused,
 			     const struct pm_callback_handle *hdl __unused)
 {
-	unsigned long clk_cpu = 0;
 	unsigned int opp = 0;
 
 	assert(op == PM_OP_SUSPEND || op == PM_OP_RESUME);
@@ -616,6 +616,8 @@ static TEE_Result cpu_opp_pm(enum pm_op
 #endif
 
 	if (op == PM_OP_RESUME) {
+		unsigned long clk_cpu = 0;
+
 		opp = cpu_opp.current_opp;
 
 		DMSG("Resume to OPP %u", opp);
@@ -640,13 +642,13 @@ static TEE_Result stm32_cpu_opp_is_suppo
 	cuint32 = fdt_getprop(fdt, subnode, "opp-supported-hw", NULL);
 
 	if (!cuint32) {
-		DMSG("Can't find property opp-supported-hw");
+		EMSG("Can't find property opp-supported-hw");
 		return TEE_ERROR_GENERIC;
 	}
 
 	opp = fdt32_to_cpu(*cuint32);
 	if (!stm32mp_supports_cpu_opp(opp)) {
-		DMSG("Not supported opp-supported-hw %#"PRIx32, opp);
+		EMSG("Unsupported opp-supported-hw %#"PRIx32, opp);
 		return TEE_ERROR_GENERIC;
 	}
 
@@ -733,9 +735,11 @@ static TEE_Result stm32_cpu_opp_get_dt_s
 		i++;
 	}
 
-	/* Erreur when "st,opp-default" is not present */
-	if (!opp_default)
+	/* Error when "st,opp-default" is not present */
+	if (!opp_default) {
+		EMSG("No 'st,opp-default' property found");
 		return TEE_ERROR_GENERIC;
+	}
 
 	/* Select the max "st,opp-default" node as current OPP */
 #ifdef CFG_SCPFW_MOD_DVFS
@@ -761,7 +765,7 @@ stm32_cpu_opp_init(const void *fdt, int
 		   const void *compat_data __unused)
 {
 	TEE_Result res = TEE_SUCCESS;
-	uint16_t __maybe_unused cpu_voltage = 0;
+	uint32_t __maybe_unused cpu_voltage = 0;
 
 	res = clk_dt_get_by_index(fdt, cpu_node, 0, &cpu_opp.clock);
 	if (res)
Index: optee-txmp-2550/core/drivers/stm32_i2c.c
===================================================================
--- optee-txmp-2550.orig/core/drivers/stm32_i2c.c
+++ optee-txmp-2550/core/drivers/stm32_i2c.c
@@ -290,16 +290,13 @@ static const struct i2c_spec_s i2c_specs
  * @timeout_ms: Timeout in millisenconds for the request
  */
 struct i2c_request {
-	uint32_t dev_addr;
 	enum i2c_mode_e mode;
-	uint32_t mem_addr;
-	uint32_t mem_addr_size;
 	unsigned int timeout_ms;
+	uint16_t dev_addr;
+	uint16_t mem_addr;
+	uint16_t mem_addr_size;
 };
 
-/* Non-null reference for compat data */
-static const int secure_i2c;
-
 static vaddr_t get_base(struct i2c_handle_s *hi2c)
 {
 	return io_pa_or_va_secure(&hi2c->base, hi2c->reg_size);
@@ -400,11 +397,8 @@ static int i2c_compute_timing(struct stm
 	uint32_t tsync = 0;
 	uint32_t clk_min = 0;
 	uint32_t clk_max = 0;
-	int clk_error_prev = 0;
+	int clk_error_prev = INT_MAX;
 	uint16_t p = 0;
-	uint16_t l = 0;
-	uint16_t a = 0;
-	uint16_t h = 0;
 	unsigned int sdadel_min = 0;
 	unsigned int sdadel_max = 0;
 	unsigned int scldel_min = 0;
@@ -420,7 +414,6 @@ static int i2c_compute_timing(struct stm
 
 	speed_freq = specs->rate;
 	i2cbus = UDIV_ROUND_NEAREST(I2C_NSEC_PER_SEC, speed_freq);
-	clk_error_prev = INT_MAX;
 
 	if (init->rise_time > specs->rise_max ||
 	    init->fall_time > specs->fall_max) {
@@ -460,8 +453,11 @@ static int i2c_compute_timing(struct stm
 
 	/* Compute possible values for PRESC, SCLDEL and SDADEL */
 	for (p = 0; p < I2C_TIMINGR_PRESC_MAX; p++) {
+		uint16_t l = 0;
+
 		for (l = 0; l < I2C_TIMINGR_SCLDEL_MAX; l++) {
 			uint32_t scldel = (l + 1) * (p + 1) * i2cclk;
+			uint16_t a = 0;
 
 			if (scldel < scldel_min)
 				continue;
@@ -506,12 +502,14 @@ static int i2c_compute_timing(struct stm
 	 */
 	for (p = 0; p < I2C_TIMINGR_PRESC_MAX; p++) {
 		uint32_t prescaler = (p + 1) * i2cclk;
+		uint16_t l = 0;
 
 		if (!solutions[p].is_saved)
 			continue;
 
 		for (l = 0; l < I2C_TIMINGR_SCLL_MAX; l++) {
 			uint32_t tscl_l = ((l + 1) * prescaler) + tsync;
+			uint16_t h = 0;
 
 			if (tscl_l < specs->l_min ||
 			    i2cclk >= ((tscl_l - af_delay_min - dnf_delay) / 4))
@@ -542,7 +540,7 @@ static int i2c_compute_timing(struct stm
 	}
 
 	if (s < 0) {
-		DMSG("I2C no solution at all");
+		EMSG("Cannot calculate I2C timing");
 		return -1;
 	}
 
@@ -733,7 +731,7 @@ TEE_Result stm32_i2c_get_setup_from_fdt(
 		init->bus_rate = fdt32_to_cpu(*cuint);
 
 		if (init->bus_rate > I2C_FAST_PLUS_RATE) {
-			DMSG("Invalid bus speed (%"PRIu32" > %i)",
+			EMSG("Invalid bus speed (%"PRIu32" > %i)",
 			     init->bus_rate, I2C_FAST_PLUS_RATE);
 			return TEE_ERROR_GENERIC;
 		}
@@ -971,8 +969,8 @@ static int i2c_wait_stop(struct i2c_hand
  * @startstop: One of I2C_NO_STARTSTOP, I2C_GENERATE_STOP,
  *		I2C_GENERATE_START_{READ|WRITE}
  */
-static void i2c_transfer_config(struct i2c_handle_s *hi2c, uint32_t dev_addr,
-				uint32_t size, uint32_t i2c_mode,
+static void i2c_transfer_config(struct i2c_handle_s *hi2c, uint16_t dev_addr,
+				uint16_t size, enum i2c_mode_e i2c_mode,
 				uint32_t startstop)
 {
 	uint32_t clr_value = I2C_CR2_SADD | I2C_CR2_NBYTES | I2C_CR2_RELOAD |
@@ -1186,8 +1184,8 @@ bail:
 	return rc;
 }
 
-int stm32_i2c_mem_write(struct i2c_handle_s *hi2c, uint32_t dev_addr,
-			uint32_t mem_addr, uint32_t mem_addr_size,
+int stm32_i2c_mem_write(struct i2c_handle_s *hi2c, uint16_t dev_addr,
+			uint16_t mem_addr, uint16_t mem_addr_size,
 			uint8_t *p_data, size_t size, unsigned int timeout_ms)
 {
 	struct i2c_request request = {
@@ -1201,7 +1199,7 @@ int stm32_i2c_mem_write(struct i2c_handl
 	return do_write(hi2c, &request, p_data, size);
 }
 
-int stm32_i2c_master_transmit(struct i2c_handle_s *hi2c, uint32_t dev_addr,
+int stm32_i2c_master_transmit(struct i2c_handle_s *hi2c, uint16_t dev_addr,
 			      uint8_t *p_data, size_t size,
 			      unsigned int timeout_ms)
 {
@@ -1414,8 +1412,8 @@ bail:
 	return rc;
 }
 
-int stm32_i2c_mem_read(struct i2c_handle_s *hi2c, uint32_t dev_addr,
-		       uint32_t mem_addr, uint32_t mem_addr_size,
+int stm32_i2c_mem_read(struct i2c_handle_s *hi2c, uint16_t dev_addr,
+		       uint16_t mem_addr, uint16_t mem_addr_size,
 		       uint8_t *p_data, size_t size, unsigned int timeout_ms)
 {
 	struct i2c_request request = {
@@ -1429,7 +1427,7 @@ int stm32_i2c_mem_read(struct i2c_handle
 	return do_read(hi2c, &request, p_data, size);
 }
 
-int stm32_i2c_master_receive(struct i2c_handle_s *hi2c, uint32_t dev_addr,
+int stm32_i2c_master_receive(struct i2c_handle_s *hi2c, uint16_t dev_addr,
 			     uint8_t *p_data, size_t size,
 			     unsigned int timeout_ms)
 {
@@ -1484,7 +1482,7 @@ static const struct i2c_ctrl_ops stm32_i
 	.write = stm32_i2c_write_data,
 };
 
-bool stm32_i2c_is_device_ready(struct i2c_handle_s *hi2c, uint32_t dev_addr,
+bool stm32_i2c_is_device_ready(struct i2c_handle_s *hi2c, uint16_t dev_addr,
 			       unsigned int trials, unsigned int timeout_ms)
 {
 	vaddr_t base = get_base(hi2c);
@@ -1653,7 +1651,7 @@ static TEE_Result stm32_i2c_probe(const
 	res = stm32_i2c_get_setup_from_fdt((void *)fdt, node, &init_data,
 					   &pinctrl_active, &pinctrl_idle);
 	if (res)
-		return res;
+		goto out;
 
 	i2c_handle_p = calloc(1, sizeof(struct i2c_handle_s));
 	if (!i2c_handle_p)
@@ -1674,11 +1672,13 @@ static TEE_Result stm32_i2c_probe(const
 	init_data.analog_filter = true;
 	init_data.digital_filter_coef = 0;
 
-	if (compat_data == &secure_i2c)
+	if (compat_data)
 		i2c_handle_p->i2c_secure = true;
 
-	if (pinctrl_apply_state(i2c_handle_p->pinctrl))
-		panic();
+	if (pinctrl_apply_state(i2c_handle_p->pinctrl)) {
+		res = TEE_ERROR_GENERIC;
+		goto out;
+	}
 
 	res = stm32_i2c_init(i2c_handle_p, &init_data);
 	if (res)
@@ -1696,15 +1696,17 @@ static TEE_Result stm32_i2c_probe(const
 			panic();
 		}
 	}
-
+out:
+	if (res && res != TEE_ERROR_DEFER_DRIVER_INIT)
+		EMSG("I2C probe FAILED: %d\n", res);
 	return res;
 }
 
 static const struct dt_device_match stm32_i2c_match_table[] = {
-	{ .compatible = "st,stm32mp15-i2c", .compat_data = &secure_i2c },
-	{ .compatible = "st,stm32mp13-i2c", .compat_data = &secure_i2c },
-	{ .compatible = "st,stm32mp15-i2c-non-secure" },
-	{ .compatible = "st,stm32mp25-i2c", .compat_data = &secure_i2c },
+	{ .compatible = "st,stm32mp15-i2c", .compat_data = (void *)1, },
+	{ .compatible = "st,stm32mp13-i2c", .compat_data = (void *)1, },
+	{ .compatible = "st,stm32mp15-i2c-non-secure", },
+	{ .compatible = "st,stm32mp25-i2c", .compat_data = (void *)1, },
 	{ }
 };
 
Index: optee-txmp-2550/core/drivers/stm32_ipcc.c
===================================================================
--- optee-txmp-2550.orig/core/drivers/stm32_ipcc.c
+++ optee-txmp-2550/core/drivers/stm32_ipcc.c
@@ -690,7 +690,8 @@ static TEE_Result parse_dt(const void *f
 
 	cuint = fdt_getprop(fdt, node, "st,protreg", &lenp);
 	if (!cuint) {
-		DMSG("No RIF configuration available");
+		EMSG("st,protreg missing on node %s\n",
+		     fdt_get_name(fdt, node, NULL));
 		return TEE_SUCCESS;
 	}
 
Index: optee-txmp-2550/core/kernel/dt_driver.c
===================================================================
--- optee-txmp-2550.orig/core/kernel/dt_driver.c
+++ optee-txmp-2550/core/kernel/dt_driver.c
@@ -136,13 +136,14 @@ TEE_Result dt_driver_register_provider(c
 
 	provider_cells = fdt_get_dt_driver_cells(fdt, nodeoffset, type);
 	if (provider_cells < 0) {
-		DMSG("Failed to find provider cells: %d", provider_cells);
+		EMSG("Failed to find provider cells: %d", provider_cells);
 		return TEE_ERROR_GENERIC;
 	}
 
 	phandle = fdt_get_phandle(fdt, nodeoffset);
 	if (phandle == (uint32_t)-1) {
-		DMSG("Failed to find provide phandle");
+		EMSG("Invalid provider phandle for %s",
+		     fdt_get_name(fdt, nodeoffset, NULL));
 		return TEE_ERROR_GENERIC;
 	}
 
@@ -325,7 +326,7 @@ TEE_Result dt_driver_device_from_node_id
 	prop = fdt_getprop(fdt, nodeoffs, prop_name, &len);
 	if (!prop) {
 		if (len != -FDT_ERR_NOTFOUND) {
-			DMSG("Corrupted node %s", prop_name);
+			EMSG("Corrupted node %s", prop_name);
 			return TEE_ERROR_GENERIC;
 		} else {
 			DMSG("Property %s missing in node %s", prop_name,
@@ -358,7 +359,6 @@ TEE_Result dt_driver_device_from_node_id
 	int len = 0;
 	int idx = 0;
 	int idx32 = 0;
-	int prv_cells = 0;
 	int phandle_node = -1;
 	uint32_t phandle = 0;
 	const uint32_t *prop = NULL;
@@ -373,6 +373,8 @@ TEE_Result dt_driver_device_from_node_id
 	}
 
 	while (idx < len) {
+		int prv_cells = 0;
+
 		idx32 = idx / sizeof(uint32_t);
 		phandle = fdt32_to_cpu(prop[idx32]);
 		if (!phandle) {
@@ -416,7 +418,7 @@ TEE_Result dt_driver_device_from_node_id
 			 */
 			phandle_node = fdt_node_offset_by_phandle(fdt, phandle);
 			if (phandle_node < 0) {
-				DMSG("Can't find node for phandle %"PRIu32,
+				EMSG("Can't find node for phandle %"PRIu32,
 				     phandle);
 				return TEE_ERROR_GENERIC;
 			}
@@ -424,7 +426,7 @@ TEE_Result dt_driver_device_from_node_id
 			prv_cells = fdt_get_dt_driver_cells(fdt, phandle_node,
 							    type);
 			if (prv_cells < 0) {
-				DMSG("Can't find cells count on node %s: %d",
+				EMSG("Can't find cells count on node %s: %d",
 				     fdt_get_name(fdt, phandle_node, NULL),
 				     prv_cells);
 				return TEE_ERROR_GENERIC;
@@ -457,11 +459,7 @@ TEE_Result dt_driver_count_devices(const
 {
 	int len = 0;
 	int idx = 0;
-	int idx32 = 0;
-	int prv_cells = 0;
-	uint32_t phandle = 0;
 	const uint32_t *prop = NULL;
-	struct dt_driver_provider *prv = NULL;
 
 	assert(nb_element);
 
@@ -475,8 +473,11 @@ TEE_Result dt_driver_count_devices(const
 	}
 
 	while (idx < len) {
-		idx32 = idx / sizeof(uint32_t);
-		phandle = fdt32_to_cpu(prop[idx32]);
+		int idx32 = idx / sizeof(uint32_t);
+		uint32_t phandle = fdt32_to_cpu(prop[idx32]);
+		int prv_cells = 0;
+		struct dt_driver_provider *prv = NULL;
+
 		if (!phandle)
 			return TEE_ERROR_BAD_PARAMETERS;
 
@@ -617,10 +618,7 @@ TEE_Result dt_driver_probe_device_by_nod
 					  enum dt_driver_type type)
 {
 	int idx = 0;
-	int len = 0;
 	int count = 0;
-	const char *compat = NULL;
-	TEE_Result res = TEE_ERROR_GENERIC;
 
 	assert_type_is_valid(type);
 
@@ -629,6 +627,10 @@ TEE_Result dt_driver_probe_device_by_nod
 		return TEE_ERROR_ITEM_NOT_FOUND;
 
 	for (idx = 0; idx < count; idx++) {
+		TEE_Result res = TEE_ERROR_GENERIC;
+		int len = 0;
+		const char *compat = NULL;
+
 		compat = fdt_stringlist_get(fdt, nodeoffset, "compatible",
 					    idx, &len);
 		if (!compat)
@@ -870,9 +872,8 @@ static void parse_node(const void *fdt,
 static TEE_Result probe_dt_drivers_early(void)
 {
 	TEE_Result res = TEE_ERROR_GENERIC;
-	const void *fdt = NULL;
+	const void *fdt = get_secure_dt();
 
-	fdt = get_secure_dt();
 	if (!fdt)
 		return TEE_SUCCESS;
 
@@ -891,9 +892,8 @@ static TEE_Result probe_dt_drivers_early
 static TEE_Result probe_dt_drivers(void)
 {
 	TEE_Result res = TEE_ERROR_GENERIC;
-	const void *fdt = NULL;
+	const void *fdt = get_secure_dt();
 
-	fdt = get_secure_dt();
 	if (!fdt)
 		return TEE_SUCCESS;
 
@@ -916,9 +916,8 @@ static TEE_Result release_probe_lists(vo
 	struct dt_driver_probe *next = NULL;
 	struct dt_driver_provider *prov = NULL;
 	struct dt_driver_provider *next_prov = NULL;
-	const void *fdt = NULL;
+	const void *fdt = get_secure_dt();
 
-	fdt = get_secure_dt();
 	if (!fdt)
 		return TEE_SUCCESS;
 
@@ -944,10 +943,11 @@ release_init_resource(release_probe_list
 static TEE_Result simple_bus_probe(const void *fdt, int node,
 				   const void *compat_data __unused)
 {
-	TEE_Result res = TEE_ERROR_GENERIC;
 	int subnode = 0;
 
 	fdt_for_each_subnode(subnode, fdt, node) {
+		TEE_Result res = TEE_ERROR_GENERIC;
+
 		res = dt_driver_maybe_add_probe_node(fdt, subnode);
 		if (res) {
 			EMSG("Failed on node %s with %#"PRIx32,
Index: optee-txmp-2550/mk/compile.mk
===================================================================
--- optee-txmp-2550.orig/mk/compile.mk
+++ optee-txmp-2550/mk/compile.mk
@@ -35,8 +35,8 @@ comp-cflags-warns-high = \
 	-Wmissing-prototypes -Wnested-externs -Wpointer-arith \
 	-Wshadow -Wstrict-prototypes -Wswitch-default \
 	-Wwrite-strings \
-	-Wno-missing-field-initializers -Wno-format-zero-length \
-	-Wno-c2x-extensions
+	-Wno-missing-field-initializers -Wno-format-zero-length
+comp-cflags-warns-high += $(call cc-option,-Wno-c2x-extensions)
 comp-cflags-warns-high += $(call cc-option,-Wpacked-not-aligned)
 comp-cflags-warns-high += $(call cc-option,-Waddress-of-packed-member)
 ifeq ($(CFG_WARN_DECL_AFTER_STATEMENT),y)
Index: optee-txmp-2550/core/arch/arm/plat-stm32mp2/drivers/stm32mp25_pwr_irq.c
===================================================================
--- optee-txmp-2550.orig/core/arch/arm/plat-stm32mp2/drivers/stm32mp25_pwr_irq.c
+++ optee-txmp-2550/core/arch/arm/plat-stm32mp2/drivers/stm32mp25_pwr_irq.c
@@ -107,7 +107,7 @@ static enum itr_return pwr_it_call_handl
 static enum itr_return pwr_it_threaded_handler(void)
 {
 	struct pwr_wkup_data *priv = pwr_wkup_d;
-	uint32_t i = 0;
+	int i = 0;
 
 	VERBOSE_PWR("threaded_handler");
 
@@ -146,7 +146,7 @@ struct notif_driver stm32_pwr_notif = {
 static enum itr_return pwr_it_handler(struct itr_handler *handler)
 {
 	struct pwr_wkup_data *priv = (struct pwr_wkup_data *)handler->data;
-	uint32_t i = 0;
+	int i = 0;
 
 	VERBOSE_PWR("it_handler");
 
Index: optee-txmp-2550/core/drivers/stm32_tamp.c
===================================================================
--- optee-txmp-2550.orig/core/drivers/stm32_tamp.c
+++ optee-txmp-2550/core/drivers/stm32_tamp.c
@@ -755,7 +755,7 @@ static bool is_ext_tamp_id_valid(enum st
 	       _TAMP_HWCFGR1_TAMPER_SHIFT;
 }
 
-static TEE_Result stm32_tamp_set_int_config(struct stm32_tamp_compat *tcompat,
+static TEE_Result stm32_tamp_set_int_config(const struct stm32_tamp_compat *tcompat,
 					    uint32_t itamp_index, uint32_t *cr1,
 					    uint32_t *cr3, uint32_t *ier)
 {
@@ -806,7 +806,7 @@ static TEE_Result stm32_tamp_set_int_con
 	return TEE_SUCCESS;
 }
 
-static TEE_Result stm32_tamp_set_ext_config(struct stm32_tamp_compat *tcompat,
+static TEE_Result stm32_tamp_set_ext_config(const struct stm32_tamp_compat *tcompat,
 					    uint32_t etamp_index, uint32_t *cr1,
 					    uint32_t *cr2, uint32_t *atcr1,
 					    uint32_t *atcr2, uint32_t *ier)
@@ -933,10 +933,10 @@ static void parse_bkpregs_dt_conf(struct
 
 	/*
 	 * When TAMP does not support RIF, the backup registers can
-	 * be splited in 3 zones. These zones have specific read/write
-	 * access permissions based on the secure status of the accesser.
-	 * When RIF is supported, these zones can additionally be splited
-	 * in subzones that have CID filtering. Zones/Subzones can be empty and
+	 * be split into 3 zones. These zones have specific read/write
+	 * access permissions based on the secure status of the accessor.
+	 * When RIF is supported, these zones can additionally be split
+	 * into subzones that have CID filtering. Zones/Subzones can be empty and
 	 * are contiguous.
 	 */
 	if (!(pdata->compat->tags & TAMP_HAS_RIF_SUPPORT)) {
@@ -1529,7 +1529,7 @@ DECLARE_KEEP_PAGER(stm32_tamp_it_handler
 static void stm32_tamp_configure_pin(uint32_t id, struct gpio *gpio, bool out,
 				     struct stm32_tamp_platdata *pdata)
 {
-	struct stm32_tamp_compat *compat = pdata->compat;
+	const struct stm32_tamp_compat *compat = pdata->compat;
 	unsigned int bank = stm32_gpio_chip_bank_id(gpio->chip);
 	unsigned int pin = gpio->pin;
 	size_t i = 0;
@@ -1571,10 +1571,12 @@ stm32_tamp_configure_pin_from_dt(const v
 	 * Else, it is an active tamper.
 	 */
 	res = gpio_dt_get_by_index(fdt, node, 1, "tamper", &gpio_out);
-	if (res && res != TEE_ERROR_ITEM_NOT_FOUND)
-		return res;
-
-	if (res != TEE_ERROR_ITEM_NOT_FOUND) {
+	if (res) {
+		if (res != TEE_ERROR_ITEM_NOT_FOUND)
+			return res;
+		active = false;
+		out_id = INVALID_OUT_TAMP;
+	} else {
 		active = true;
 		out_id = OUT_TAMP1 + out_tamp_id - 1;
 		if (out_tamp_id > pdata->compat->ext_tamp_size)
@@ -1940,7 +1942,7 @@ static TEE_Result stm32_tamp_parse_fdt(s
 		return TEE_ERROR_BAD_PARAMETERS;
 	}
 
-	pdata->compat = (struct stm32_tamp_compat *)compat;
+	pdata->compat = compat;
 
 	if (stm32_tamp.pdata.compat->tags & TAMP_HAS_RIF_SUPPORT) {
 		res = stm32_rifsc_check_tdcid(&pdata->is_tdcid);
@@ -2128,7 +2130,7 @@ static TEE_Result stm32_tamp_probe(const
 		goto err;
 
 	if (stm32_tamp.pdata.is_wakeup_source) {
-		struct stm32_tamp_compat *compat = stm32_tamp.pdata.compat;
+		const struct stm32_tamp_compat *compat = stm32_tamp.pdata.compat;
 
 		if (IS_ENABLED(CFG_STM32_EXTI))
 			stm32_exti_enable_wake(stm32_tamp.pdata.exti,
Index: optee-txmp-2550/core/include/drivers/stm32_tamp.h
===================================================================
--- optee-txmp-2550.orig/core/include/drivers/stm32_tamp.h
+++ optee-txmp-2550/core/include/drivers/stm32_tamp.h
@@ -115,7 +115,7 @@ struct stm32_tamp_compat {
 struct stm32_tamp_platdata {
 	struct io_pa_va base;
 	struct clk *clock;
-	struct stm32_tamp_compat *compat;
+	const struct stm32_tamp_compat *compat;
 	struct rif_conf_data *conf_data;
 	struct stm32_bkpregs_conf *bkpregs_conf;
 	struct stm32_exti_pdata *exti;
Index: optee-txmp-2550/core/arch/arm/dts/stm32mp25xxak-pinctrl.dtsi
===================================================================
--- optee-txmp-2550.orig/core/arch/arm/dts/stm32mp25xxak-pinctrl.dtsi
+++ optee-txmp-2550/core/arch/arm/dts/stm32mp25xxak-pinctrl.dtsi
@@ -58,6 +58,9 @@
 		ngpios = <12>;
 		gpio-ranges = <&pinctrl 0 128 12>;
 	};
+
+	/delete-node/ gpio@442d0000; /* GPIOJ */
+	/delete-node/ gpio@442e0000; /* GPIOK */
 };
 
 &pinctrl_z {
Index: optee-txmp-2550/core/arch/arm/dts/stm32mp25xxal-pinctrl.dtsi
===================================================================
--- optee-txmp-2550.orig/core/arch/arm/dts/stm32mp25xxal-pinctrl.dtsi
+++ optee-txmp-2550/core/arch/arm/dts/stm32mp25xxal-pinctrl.dtsi
@@ -58,6 +58,9 @@
 		ngpios = <12>;
 		gpio-ranges = <&pinctrl 0 128 12>;
 	};
+
+	/delete-node/ gpio@442d0000; /* GPIOJ */
+	/delete-node/ gpio@442e0000; /* GPIOK */
 };
 
 &pinctrl_z {
Index: optee-txmp-2550/core/drivers/firewall/stm32_risab.c
===================================================================
--- optee-txmp-2550.orig/core/drivers/firewall/stm32_risab.c
+++ optee-txmp-2550/core/drivers/firewall/stm32_risab.c
@@ -500,7 +500,7 @@ static TEE_Result parse_dt(const void *f
 		size_t length = 0;
 
 		mem_reg_node =
-		fdt_node_offset_by_phandle(fdt, fdt32_to_cpu(mem_regions[i]));
+			fdt_node_offset_by_phandle(fdt, fdt32_to_cpu(mem_regions[i]));
 		if (mem_reg_node < 0)
 			return TEE_ERROR_ITEM_NOT_FOUND;
 
@@ -511,6 +511,12 @@ static TEE_Result parse_dt(const void *f
 		address = (uintptr_t)fdt_reg_base_address(fdt, mem_reg_node);
 		length = fdt_reg_size(fdt, mem_reg_node);
 
+		if (!IS_ALIGNED(address, _RISAB_PAGE_SIZE))
+			EMSG("address %08lx is not aligned to %08x",
+			     address, _RISAB_PAGE_SIZE);
+		if (!IS_ALIGNED(length, _RISAB_PAGE_SIZE))
+			EMSG("length %08lx is not aligned to %08x",
+			     length, _RISAB_PAGE_SIZE);
 		assert(IS_ALIGNED(address, _RISAB_PAGE_SIZE) &&
 		       IS_ALIGNED(length, _RISAB_PAGE_SIZE));
 
Index: optee-txmp-2550/core/drivers/stm32mp_pmic2.c
===================================================================
--- optee-txmp-2550.orig/core/drivers/stm32mp_pmic2.c
+++ optee-txmp-2550/core/drivers/stm32mp_pmic2.c
@@ -193,7 +193,7 @@ static TEE_Result pmic_get_voltage(struc
 	if (res)
 		goto out;
 
-	*level_uv = (int)val * 1000;
+	*level_uv = val * 1000;
 
 	FMSG("%s: get voltage: %d uV", regulator_name(regulator), *level_uv);
 out:
@@ -207,7 +207,7 @@ static TEE_Result pmic_set_voltage(struc
 	struct pmic_regu *regu = regulator->priv;
 	TEE_Result res = TEE_ERROR_GENERIC;
 
-	FMSG("%s: set volt to %d mV", regulator_name(regulator), level_uv);
+	FMSG("%s: set volt to %d uV", regulator_name(regulator), level_uv);
 
 	lock_pmic_access();
 
@@ -241,10 +241,12 @@ static size_t refine_levels_array(size_t
 	size_t n = 0;
 	size_t m = 0;
 
+	FMSG("refining %zu voltages between %u and %u\n",
+	     count, min_uv, max_uv);
 	qsort_int(levels_uv, count);
 
 	/* Remove duplicates and return optimized count */
-	for (n = 1; n < count; n++) {
+	for (m = 0, n = 1; n < count; n++) {
 		if (levels_uv[m] != levels_uv[n]) {
 			if (m + 1 != n)
 				levels_uv[m + 1] = levels_uv[n];
@@ -266,6 +268,8 @@ static size_t refine_levels_array(size_t
 
 	memmove(levels_uv, levels_uv + n, count * sizeof(*levels_uv));
 
+	FMSG("returning %zu voltages between %u and %u\n",
+	     count, levels_uv[0], levels_uv[count - 1]);
 	return count;
 }
 
@@ -287,7 +291,7 @@ static TEE_Result pmic_list_voltages(str
 		size_t n = 0;
 
 		/*
-		 * Allocate and build a consise and ordered voltage list
+		 * Allocate and build a concise and ordered voltage list
 		 * based on the voltage list provided by stpmic2 driver.
 		 * Also add bypass voltage to the list.
 		 */
@@ -674,20 +678,21 @@ static TEE_Result initialize_pmic2_irq(c
 	SLIST_INIT(&pmic->it_list);
 
 	cuint = fdt_getprop(fdt, node, "wakeup-parent", &len);
-	if (!cuint || len != sizeof(uint32_t))
-		panic("Missing wakeup-parent");
-
-	phandle = fdt32_to_cpu(*cuint);
-
-	wakeup_parent_node = fdt_node_offset_by_phandle(fdt, phandle);
+	if (cuint) {
+		phandle = fdt32_to_cpu(*cuint);
+		wakeup_parent_node = fdt_node_offset_by_phandle(fdt, phandle);
+	} else {
+		wakeup_parent_node = -1;
+	}
 
 	cuint = fdt_getprop(fdt, node, "st,wakeup-pin-number", NULL);
-	if (cuint) {
-		TEE_Result res = TEE_ERROR_GENERIC;
-		size_t it = 0;
 
-		it = fdt32_to_cpu(*cuint) - 1;
+	if (cuint) {
+		TEE_Result res;
+		size_t it = fdt32_to_cpu(*cuint) - 1;
 
+		if (wakeup_parent_node < 0)
+			panic("Missing wakeup-parent");
 #if defined(CFG_STM32MP25) || defined(CFG_STM32MP23) || defined(CFG_STM32MP21)
 		res = stm32mp25_pwr_itr_alloc_add(fdt, wakeup_parent_node, it,
 						  stpmic2_irq_handler,
@@ -717,7 +722,6 @@ static TEE_Result initialize_pmic2_irq(c
 		return TEE_ERROR_ITEM_NOT_FOUND;
 
 	if (nb_notif > 0) {
-		struct pmic_it_handle_s *prv = NULL;
 		unsigned int i = 0;
 		const uint32_t *pmic_its = NULL;
 		int nb_it = 0;
@@ -731,8 +735,8 @@ static TEE_Result initialize_pmic2_irq(c
 
 		for (i = 0; i < (nb_notif / sizeof(uint32_t)); i++) {
 			uint8_t pmic_it = 0;
+			struct pmic_it_handle_s *prv = calloc(1, sizeof(*prv));
 
-			prv = calloc(1, sizeof(*prv));
 			if (!prv)
 				panic("pmic: Could not allocate pmic it");
 
Index: optee-txmp-2550/core/include/drivers/rstctrl.h
===================================================================
--- optee-txmp-2550.orig/core/include/drivers/rstctrl.h
+++ optee-txmp-2550/core/include/drivers/rstctrl.h
@@ -160,16 +160,9 @@ static inline TEE_Result rstctrl_dt_get_
 						 unsigned int index,
 						 struct rstctrl **out_rstctrl)
 {
-	TEE_Result res = TEE_ERROR_GENERIC;
-	void *rstctrl = NULL;
-
-	res = dt_driver_device_from_node_idx_prop("resets", fdt, nodeoffset,
+	return dt_driver_device_from_node_idx_prop("resets", fdt, nodeoffset,
 						  index, DT_DRIVER_RSTCTRL,
-						  &rstctrl);
-	if (!res)
-		*out_rstctrl = rstctrl;
-
-	return res;
+						  out_rstctrl);
 }
 #else
 static inline TEE_Result rstctrl_dt_get_by_index(const void *fdt __unused,
Index: optee-txmp-2550/core/lib/scmi-server/scmi_reset_consumer.c
===================================================================
--- optee-txmp-2550.orig/core/lib/scmi-server/scmi_reset_consumer.c
+++ optee-txmp-2550/core/lib/scmi-server/scmi_reset_consumer.c
@@ -79,14 +79,14 @@ TEE_Result optee_scmi_server_init_resets
 
 		res = rstctrl_dt_get_by_index(fdt, subnode, 0, &reset);
 		if (res == TEE_ERROR_DEFER_DRIVER_INIT) {
-			panic("Unexpected init deferral");
+			DMSG("Unexpected init deferral for %s", fdt_get_name(fdt, subnode, NULL));
 		} else if (res) {
 			EMSG("Can't get reset %s (%#"PRIx32"), skipped",
 			     fdt_get_name(fdt, subnode, NULL), res);
 			continue;
 		}
 
-		s_reset = s_resets + domain_id;
+		s_reset = &s_resets[domain_id];
 		s_reset->domain_id = domain_id;
 
 		cuint = fdt_getprop(fdt, subnode, "domain-name", NULL);
Index: optee-txmp-2550/core/include/drivers/stm32_i2c.h
===================================================================
--- optee-txmp-2550.orig/core/include/drivers/stm32_i2c.h
+++ optee-txmp-2550/core/include/drivers/stm32_i2c.h
@@ -190,8 +190,8 @@ int stm32_i2c_init(struct i2c_handle_s *
  * @timeout_ms: Timeout value in milliseconds
  * Return 0 on success else a negative value
  */
-int stm32_i2c_mem_write(struct i2c_handle_s *hi2c, uint32_t dev_addr,
-			uint32_t mem_addr, uint32_t mem_addr_size,
+int stm32_i2c_mem_write(struct i2c_handle_s *hi2c, uint16_t dev_addr,
+			uint16_t mem_addr, uint16_t mem_addr_size,
 			uint8_t *p_data, size_t size, unsigned int timeout_ms);
 
 /*
@@ -206,8 +206,8 @@ int stm32_i2c_mem_write(struct i2c_handl
  * @timeout_ms: Timeout value in milliseconds
  * Return 0 on success else a negative value
  */
-int stm32_i2c_mem_read(struct i2c_handle_s *hi2c, uint32_t dev_addr,
-		       uint32_t mem_addr, uint32_t mem_addr_size,
+int stm32_i2c_mem_read(struct i2c_handle_s *hi2c, uint16_t dev_addr,
+		       uint16_t mem_addr, uint16_t mem_addr_size,
 		       uint8_t *p_data, size_t size, unsigned int timeout_ms);
 
 /*
@@ -220,7 +220,7 @@ int stm32_i2c_mem_read(struct i2c_handle
  * @timeout_ms: Timeout value in milliseconds
  * Return 0 on success else a negative value
  */
-int stm32_i2c_master_transmit(struct i2c_handle_s *hi2c, uint32_t dev_addr,
+int stm32_i2c_master_transmit(struct i2c_handle_s *hi2c, uint16_t dev_addr,
 			      uint8_t *p_data, size_t size,
 			      unsigned int timeout_ms);
 
@@ -234,7 +234,7 @@ int stm32_i2c_master_transmit(struct i2c
  * @timeout_ms: Timeout value in milliseconds
  * Return 0 on success else a negative value
  */
-int stm32_i2c_master_receive(struct i2c_handle_s *hi2c, uint32_t dev_addr,
+int stm32_i2c_master_receive(struct i2c_handle_s *hi2c, uint16_t dev_addr,
 			     uint8_t *p_data, size_t size,
 			     unsigned int timeout_ms);
 
@@ -256,7 +256,7 @@ int stm32_i2c_read_write_membyte(struct
  * @timeout_ms: Timeout value in milliseconds for each I2C request
  * Return 0 on success else a negative value
  */
-bool stm32_i2c_is_device_ready(struct i2c_handle_s *hi2c, uint32_t dev_addr,
+bool stm32_i2c_is_device_ready(struct i2c_handle_s *hi2c, uint16_t dev_addr,
 			       unsigned int trials, unsigned int timeout_ms);
 
 /*
Index: optee-txmp-2550/core/drivers/regulator/regulator.c
===================================================================
--- optee-txmp-2550.orig/core/drivers/regulator/regulator.c
+++ optee-txmp-2550/core/drivers/regulator/regulator.c
@@ -191,8 +191,12 @@ TEE_Result regulator_set_voltage(struct
 	assert(regulator);
 	FMSG("%s %duV", regulator_name(regulator), level_uv);
 
-	if (level_uv < regulator->min_uv || level_uv > regulator->max_uv)
+	if (level_uv < regulator->min_uv || level_uv > regulator->max_uv) {
+		EMSG("regulator %s designated voltage %u is out of range: %u..%u",
+		     regulator_name(regulator), level_uv,
+		     regulator->min_uv, regulator->max_uv);
 		return TEE_ERROR_BAD_PARAMETERS;
+	}
 
 	cur_uv = regulator_get_voltage(regulator);
 	if (level_uv == cur_uv)
@@ -206,8 +210,8 @@ TEE_Result regulator_set_voltage(struct
 	unlock_regulator(regulator);
 
 	if (res) {
-		EMSG("regul %s set volt failed with %#"PRIx32,
-		     regulator_name(regulator), res);
+		EMSG("regul %s failed to set voltage to %uuV: %#"PRIx32,
+		     regulator_name(regulator), level_uv, res);
 		return res;
 	}
 
@@ -228,11 +232,63 @@ TEE_Result regulator_set_voltage(struct
 	return TEE_SUCCESS;
 }
 
+static inline void dump_voltages(struct regulator *regulator,
+				 struct regulator_voltages_desc *desc,
+				 const int *levels)
+{
+	size_t i;
+
+	printf("%s: %zu voltages: %u..%u uV\n", regulator_name(regulator),
+	       desc->num_levels, regulator->min_uv, regulator->max_uv);
+	for (i = 0; i < desc->num_levels; i++) {
+		printf("\t[%2zu]=%uuV\n", i, levels[i]);
+	}
+}
+
+static size_t refine_levels_array(size_t count, int *levels_uv,
+				  int min_uv, int max_uv)
+{
+	size_t n;
+	size_t m;
+
+	FMSG("refining %zu voltages between %u and %u\n",
+	     count, min_uv, max_uv);
+	qsort_int(levels_uv, count);
+
+	/* Remove duplicates and return optimized count */
+	for (m = 0, n = 1; n < count; n++) {
+		if (levels_uv[m] != levels_uv[n]) {
+			if (m + 1 != n)
+				levels_uv[m + 1] = levels_uv[n];
+			m++;
+		}
+	}
+	count = m + 1;
+
+	/* Find max voltage index */
+	for (n = count; n; n--)
+		if (levels_uv[n - 1] <= max_uv)
+			break;
+	count = n;
+
+	for (n = 0; n < count; n++)
+		if (levels_uv[n] >= min_uv)
+			break;
+	count -= n;
+
+	memmove(levels_uv, levels_uv + n, count * sizeof(*levels_uv));
+
+	FMSG("returning %zu voltages between %u and %u\n",
+	     count, levels_uv[0], levels_uv[count - 1]);
+	return count;
+}
+
 TEE_Result regulator_supported_voltages(struct regulator *regulator,
 					struct regulator_voltages_desc **desc,
 					const int **levels)
 {
 	TEE_Result res = TEE_ERROR_NOT_SUPPORTED;
+	size_t count = 0;
 
 	assert(regulator && desc && levels);
 
@@ -245,7 +301,15 @@ TEE_Result regulator_supported_voltages(
 	} else if (res) {
 		return res;
 	}
-
+	//dump_voltages(regulator, *desc, *levels);
+	count = refine_levels_array((*desc)->num_levels, (int *)(*levels),
+				    regulator->min_uv, regulator->max_uv);
+	if ((*desc)->num_levels != count) {
+		FMSG("%s: number of voltages reduced to %zu (%u..%u)",
+		     regulator_name(regulator), count, (*levels)[0],
+		     (*levels)[(*desc)->num_levels - 1]);
+		(*desc)->num_levels = count;
+	}
 	if ((*desc)->type == VOLTAGE_TYPE_FULL_LIST) {
 		assert((*desc)->num_levels);
 		assert((*levels)[0] >= regulator->min_uv);
Index: optee-txmp-2550/core/drivers/stm32_gpu_opp.c
===================================================================
--- optee-txmp-2550.orig/core/drivers/stm32_gpu_opp.c
+++ optee-txmp-2550/core/drivers/stm32_gpu_opp.c
@@ -67,12 +67,7 @@ static TEE_Result stm32_gpu_opp_is_suppo
 
 static TEE_Result stm32_gpu_opp_get_dt_subnode(const void *fdt, int node)
 {
-	const fdt64_t *cuint64 = NULL;
-	const fdt32_t *cuint32 = NULL;
-	uint64_t freq_hz = 0;
-	uint64_t freq_khz = 0;
 	uint64_t freq_khz_opp_def = 0;
-	uint32_t volt_uv = 0;
 	unsigned int i = 0;
 	int subnode = -1;
 
@@ -93,6 +88,12 @@ static TEE_Result stm32_gpu_opp_get_dt_s
 	 * Also retrieve the default OPP with the highest rate
 	 */
 	fdt_for_each_subnode(subnode, fdt, node) {
+		const fdt64_t *cuint64 = NULL;
+		const fdt32_t *cuint32 = NULL;
+		uint64_t freq_hz = 0;
+		uint64_t freq_khz = 0;
+		uint32_t volt_uv = 0;
+
 		if (stm32_gpu_opp_is_supported(fdt, subnode) != TEE_SUCCESS) {
 			DMSG("Skip OPP %"PRIu64"kHz/%"PRIu32"uV by SOC",
 			     freq_khz, volt_uv);
@@ -151,7 +152,7 @@ static TEE_Result stm32_gpu_opp_get_dt_s
 		i++;
 	}
 
-	/* Erreur when "st,opp-default" is not present */
+	/* Error when "st,opp-default" is not present */
 	if (gpu_opp.current_opp == gpu_opp.opp_count) {
 		EMSG("no st,opp-default found");
 		return TEE_ERROR_BAD_PARAMETERS;
Index: optee-txmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp2/module/psu_optee_regulator/src/mod_psu_optee_regulator.c
===================================================================
--- optee-txmp-2550.orig/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp2/module/psu_optee_regulator/src/mod_psu_optee_regulator.c
+++ optee-txmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp2/module/psu_optee_regulator/src/mod_psu_optee_regulator.c
@@ -100,17 +100,16 @@ static int psu_optee_regulator_get_enabl
 static int psu_optee_regulator_set_voltage(fwk_id_t id, uint32_t voltage)
 {
     TEE_Result res = TEE_ERROR_GENERIC;
-    struct regulator *regulator;
+    struct regulator *regulator = get_regulator(id);
 
-    regulator = get_regulator(id);
     if (!regulator) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
-    DEBUG_MSG("PSU set regulator %s level: %u mV",
+    DEBUG_MSG("PSU set regulator %s level: %u uV",
               psu_regulator_name(regulator), voltage);
 
-    res = regulator_set_voltage(regulator, (int)voltage * 1000);
+    res = regulator_set_voltage(regulator, voltage);
 
     if (res) {
         return FWK_E_HANDLER;
@@ -123,19 +122,19 @@ static int psu_optee_regulator_set_volta
 static int psu_optee_regulator_get_voltage(fwk_id_t id, uint32_t *voltage)
 {
     struct regulator *regulator;
-    int level_mv;
+    int level_uv;
 
     regulator = get_regulator(id);
     if (!regulator || (voltage == NULL)) {
         return FWK_E_PARAM;
     }
 
-    level_mv = regulator_get_voltage(regulator) / 1000;
+    level_uv = regulator_get_voltage(regulator);
 
-    DEBUG_MSG("PSU get regulator %s level: %d mV",
-              psu_regulator_name(regulator), level_mv);
+    DEBUG_MSG("PSU get regulator %s level: %d uV",
+              psu_regulator_name(regulator), level_uv);
 
-    *voltage = (uint32_t)level_mv;
+    *voltage = level_uv;
 
     return FWK_SUCCESS;
 }
Index: optee-txmp-2550/core/arch/arm/dts/stm32mp131.dtsi
===================================================================
--- optee-txmp-2550.orig/core/arch/arm/dts/stm32mp131.dtsi
+++ optee-txmp-2550/core/arch/arm/dts/stm32mp131.dtsi
@@ -380,23 +380,23 @@
 			#address-cells = <1>;
 			#size-cells = <1>;
 
-			cfg0_otp: cfg0_otp@0 {
+			cfg0_otp: cfg0-otp@0 {
 				reg = <0x0 0x2>;
 			};
-			part_number_otp: part_number_otp@4 {
+			part_number_otp: part-number-otp@4 {
 				reg = <0x4 0x2>;
 				bits = <0 12>;
 			};
-			monotonic_otp: monotonic_otp@10 {
+			monotonic_otp: monotonic-otp@10 {
 				reg = <0x10 0x4>;
 			};
-			nand_otp: cfg9_otp@24 {
+			nand_otp: cfg9-otp@24 {
 				reg = <0x24 0x4>;
 			};
-			uid_otp: uid_otp@34 {
+			uid_otp: uid-otp@34 {
 				reg = <0x34 0xc>;
 			};
-			hw2_otp: hw2_otp@48 {
+			hw2_otp: hw2-otp@48 {
 				reg = <0x48 0x4>;
 			};
 			ts_cal1: calib@5c {
@@ -405,14 +405,14 @@
 			ts_cal2: calib@5e {
 				reg = <0x5e 0x2>;
 			};
-			pkh_otp: pkh_otp@60 {
+			pkh_otp: pkh-otp@60 {
 				reg = <0x60 0x20>;
 			};
 			ethernet_mac1_address: mac1@e4 {
 				reg = <0xe4 0xc>;
 				st,non-secure-otp;
 			};
-			oem_enc_key: oem_enc_key@170 {
+			oem_enc_key: oem-enc-key@170 {
 				reg = <0x170 0x10>;
 			};
 		};
Index: optee-txmp-2550/core/arch/arm/dts/stm32mp151.dtsi
===================================================================
--- optee-txmp-2550.orig/core/arch/arm/dts/stm32mp151.dtsi
+++ optee-txmp-2550/core/arch/arm/dts/stm32mp151.dtsi
@@ -374,25 +374,25 @@
 			#address-cells = <1>;
 			#size-cells = <1>;
 
-			cfg0_otp: cfg0_otp@0 {
+			cfg0_otp: cfg0-otp@0 {
 				reg = <0x0 0x1>;
 			};
-			part_number_otp: part_number_otp@4 {
+			part_number_otp: part-number-otp@4 {
 				reg = <0x4 0x1>;
 			};
-			monotonic_otp: monotonic_otp@10 {
+			monotonic_otp: monotonic-otp@10 {
 				reg = <0x10 0x4>;
 			};
-			nand_otp: nand_otp@24 {
+			nand_otp: nand-otp@24 {
 				reg = <0x24 0x4>;
 			};
-			uid_otp: uid_otp@34 {
+			uid_otp: uid-otp@34 {
 				reg = <0x34 0xc>;
 			};
-			package_otp: package_otp@40 {
+			package_otp: package-otp@40 {
 				reg = <0x40 0x4>;
 			};
-			hw2_otp: hw2_otp@48 {
+			hw2_otp: hw2-otp@48 {
 				reg = <0x48 0x4>;
 			};
 			ts_cal1: calib@5c {
@@ -401,7 +401,7 @@
 			ts_cal2: calib@5e {
 				reg = <0x5e 0x2>;
 			};
-			pkh_otp: pkh_otp@60 {
+			pkh_otp: pkh-otp@60 {
 				reg = <0x60 0x20>;
 			};
 			ethernet_mac_address: mac@e4 {
Index: optee-txmp-2550/core/arch/arm/dts/stm32mp211.dtsi
===================================================================
--- optee-txmp-2550.orig/core/arch/arm/dts/stm32mp211.dtsi
+++ optee-txmp-2550/core/arch/arm/dts/stm32mp211.dtsi
@@ -2416,11 +2416,11 @@
 			#address-cells = <1>;
 			#size-cells = <1>;
 
-			part_number_otp@24 {
+			part-number-otp@24 {
 				reg = <0x24 0x4>;
 			};
 
-			package_otp@1e8 {
+			package-otp@1e8 {
 				reg = <0x1e8 0x1>;
 			};
 
Index: optee-txmp-2550/core/arch/arm/dts/stm32mp231.dtsi
===================================================================
--- optee-txmp-2550.orig/core/arch/arm/dts/stm32mp231.dtsi
+++ optee-txmp-2550/core/arch/arm/dts/stm32mp231.dtsi
@@ -2759,11 +2759,11 @@
 			#address-cells = <1>;
 			#size-cells = <1>;
 
-			part_number_otp@24 {
+			part-number-otp@24 {
 				reg = <0x24 0x4>;
 			};
 
-			package_otp@1e8 {
+			package-otp@1e8 {
 				reg = <0x1e8 0x1>;
 			};
 
Index: optee-txmp-2550/core/drivers/stm32_bsec.c
===================================================================
--- optee-txmp-2550.orig/core/drivers/stm32_bsec.c
+++ optee-txmp-2550/core/drivers/stm32_bsec.c
@@ -186,7 +186,7 @@ static bool state_is_closed_mode(void)
 	if (IS_ENABLED(CFG_STM32MP13))
 		return bsec_status() & BSEC_MODE_CLOSED;
 
-	res = stm32_bsec_find_otp_in_nvmem_layout("cfg0_otp", &otp_cfg,
+	res = stm32_bsec_find_otp_in_nvmem_layout("cfg0-otp", &otp_cfg,
 						  &offset, &sz);
 	if (res || sz != 8 || offset)
 		panic("CFG0 OTP not found or invalid");
@@ -657,17 +657,34 @@ TEE_Result stm32_bsec_find_otp_in_nvmem_
 					       size_t *otp_bit_len)
 {
 	size_t i = 0;
+	char *alt_name = NULL;
 
 	if (!name)
 		return TEE_ERROR_BAD_PARAMETERS;
 
+	if (strchr(name, '-')) {
+		char *p;
+
+		alt_name = strdup(name);
+		p = alt_name;
+		while ((p = strchr(p, '-')))
+			*p = '_';
+	}
 	for (i = 0; i < nvmem_layout_count; i++) {
-		if (!nvmem_layout[i].name || strcmp(name, nvmem_layout[i].name))
+		if (!nvmem_layout[i].name)
+			continue;
+		if (alt_name && !strcmp(alt_name, nvmem_layout[i].name))
+			IMSG("WARNING: obsolete property '%s' should be replaced with '%s'",
+			     alt_name, name);
+		if (strcmp(name, nvmem_layout[i].name) &&
+		    (alt_name && strcmp(alt_name, nvmem_layout[i].name)))
 			continue;
 
+		free(alt_name);
 		return stm32_bsec_otp_setting(i, otp_id, otp_bit_offset,
 					      otp_bit_len);
 	}
+	free(alt_name);
 
 	DMSG("nvmem %s failed", name);
 
Index: optee-txmp-2550/documentation/devicetree/bindings/nvmem/st,stm32-romem.yaml
===================================================================
--- optee-txmp-2550.orig/documentation/devicetree/bindings/nvmem/st,stm32-romem.yaml
+++ optee-txmp-2550/documentation/devicetree/bindings/nvmem/st,stm32-romem.yaml
@@ -81,12 +81,12 @@ examples:
         reg = <0x22c 0x2>;
       };
 
-      mac_addr@e4 {
+      mac-addr@e4 {
         reg = <0xe4 0x8>;
         st,non-secure-otp;
       };
 
-      oem_enc_key@170 {
+      oem-enc-key@170 {
         reg = <0x170 0x10>;
         st,non-secure-otp-provisioning;
       };
Index: optee-txmp-2550/core/arch/arm/plat-stm32mp1/main.c
===================================================================
--- optee-txmp-2550.orig/core/arch/arm/plat-stm32mp1/main.c
+++ optee-txmp-2550/core/arch/arm/plat-stm32mp1/main.c
@@ -579,7 +579,7 @@ static int get_part_number(uint32_t *par
 	if (get_chip_dev_id(&dev_id) < 0)
 		return -1;
 
-	if (stm32_bsec_find_otp_in_nvmem_layout("part_number_otp",
+	if (stm32_bsec_find_otp_in_nvmem_layout("part-number-otp",
 						&otp, NULL, &bit_len))
 		return -1;
 
Index: optee-txmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp1/module/psu_optee_regulator/src/mod_psu_optee_regulator.c
===================================================================
--- optee-txmp-2550.orig/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp1/module/psu_optee_regulator/src/mod_psu_optee_regulator.c
+++ optee-txmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp1/module/psu_optee_regulator/src/mod_psu_optee_regulator.c
@@ -100,17 +100,17 @@ static int psu_optee_regulator_set_volta
 {
     TEE_Result res = TEE_ERROR_GENERIC;
     struct rdev *regulator;
-    uint16_t level_mv = voltage;
+    uint32_t level_uv = voltage;
 
     regulator = get_regulator(id);
     if (!regulator) {
         return FWK_E_PARAM;
     }
 
-    DEBUG_MSG("PSU set regulator %s level: %"PRIu32"mV",
-              regulator_name(regulator), (uint32_t)level_mv);
+    DEBUG_MSG("PSU set regulator %s level: %"PRIu32"uV",
+              regulator_name(regulator), level_uv);
 
-    res = regulator_set_voltage(regulator, level_mv);
+    res = regulator_set_voltage(regulator, level_uv);
 
     if (res) {
         return FWK_E_HANDLER;
@@ -124,23 +124,22 @@ static int psu_optee_regulator_get_volta
 {
     TEE_Result res = TEE_ERROR_GENERIC;
     struct rdev *regulator;
-    uint16_t level_mv;
+    uint32_t level_uv;
 
     regulator = get_regulator(id);
     if (!regulator || (voltage == NULL)) {
         return FWK_E_PARAM;
     }
 
-    res = regulator_get_voltage(regulator, &level_mv);
+    res = regulator_get_voltage(regulator, &level_uv);
 
-    DEBUG_MSG("PSU get regulator %s level: %"PRIu32"mV (res %#"PRIx32")",
-              regulator_name(regulator), (uint32_t)level_mv, res);
+    DEBUG_MSG("PSU get regulator %s level: %"PRIu32"uV (res %#"PRIx32")",
+              regulator_name(regulator), level_uv, res);
 
-    if (res) {
+    if (res)
         return FWK_E_HANDLER;
-    }
 
-    *voltage = level_mv;
+    *voltage = level_uv;
 
     return FWK_SUCCESS;
 }
Index: optee-txmp-2550/core/include/drivers/stm32_cpu_opp.h
===================================================================
--- optee-txmp-2550.orig/core/include/drivers/stm32_cpu_opp.h
+++ optee-txmp-2550/core/include/drivers/stm32_cpu_opp.h
@@ -28,7 +28,7 @@ unsigned int stm32_cpu_opp_level(size_t
 /* Provide CPU clock and regulator reference for CPU DVFS with SCMI */
 void scmi_server_set_cpu_resources(struct regulator *regulator, struct clk *clk,
 				   unsigned int *dvfs_khz,
-				   unsigned int *dvfs_mv, size_t dvfs_count);
+				   unsigned int *dvfs_uv, size_t dvfs_count);
 
 /*
  * Initialize SCMI DVFS from DT.
Index: optee-txmp-2550/core/arch/arm/plat-stm32mp1/scmi_server_scpfw.c
===================================================================
--- optee-txmp-2550.orig/core/arch/arm/plat-stm32mp1/scmi_server_scpfw.c
+++ optee-txmp-2550/core/arch/arm/plat-stm32mp1/scmi_server_scpfw.c
@@ -472,7 +472,7 @@ void scmi_scpfw_release_configuration(vo
 
 			for (k = 0; k < channel_cfg->perfd_count; k++) {
 				free(channel_cfg->perfd[k].dvfs_opp_khz);
-				free(channel_cfg->perfd[k].dvfs_opp_mv);
+				free(channel_cfg->perfd[k].dvfs_opp_uv);
 			}
 
 			free(channel_cfg->perfd);
@@ -555,7 +555,7 @@ TEE_Result scmi_scpfw_cfg_add_dvfs(unsig
 	struct scpfw_channel_config *channel_cfg = NULL;
 	struct scpfw_agent_config *agent_cfg = NULL;
 	unsigned int *dvfs_opp_khz = NULL;
-	unsigned int *dvfs_opp_mv = NULL;
+	unsigned int *dvfs_opp_uv = NULL;
 
 	agent_cfg = scpfw_cfg.agent_config + agent_id;
 	channel_cfg = agent_cfg->channel_config + channel_id;
@@ -565,15 +565,15 @@ TEE_Result scmi_scpfw_cfg_add_dvfs(unsig
 		return TEE_ERROR_BAD_PARAMETERS;
 
 	dvfs_opp_khz = calloc(dvfs_count, sizeof(*dvfs_opp_khz));
-	dvfs_opp_mv = calloc(dvfs_count, sizeof(*dvfs_opp_mv));
-	if (!dvfs_opp_khz || !dvfs_opp_mv) {
+	dvfs_opp_uv = calloc(dvfs_count, sizeof(*dvfs_opp_uv));
+	if (!dvfs_opp_khz || !dvfs_opp_uv) {
 		free(dvfs_opp_khz);
-		free(dvfs_opp_mv);
+		free(dvfs_opp_uv);
 
 		return TEE_ERROR_OUT_OF_MEMORY;
 	}
 	memcpy(dvfs_opp_khz, dvfs_khz, dvfs_count * sizeof(*dvfs_opp_khz));
-	memcpy(dvfs_opp_mv, dvfs_mv, dvfs_count * sizeof(*dvfs_opp_mv));
+	memcpy(dvfs_opp_uv, dvfs_mv, dvfs_count * sizeof(*dvfs_opp_uv));
 
 	if (channel_cfg->perfd_count <= domain_id) {
 		void *ptr = realloc(channel_cfg->perfd,
@@ -582,7 +582,7 @@ TEE_Result scmi_scpfw_cfg_add_dvfs(unsig
 
 		if (!ptr) {
 			free(dvfs_opp_khz);
-			free(dvfs_opp_mv);
+			free(dvfs_opp_uv);
 			return TEE_ERROR_OUT_OF_MEMORY;
 		}
 
@@ -601,7 +601,7 @@ TEE_Result scmi_scpfw_cfg_add_dvfs(unsig
 		.initial_opp = initial_index,
 		.dvfs_opp_count = dvfs_count,
 		.dvfs_opp_khz = dvfs_opp_khz,
-		.dvfs_opp_mv = dvfs_opp_mv,
+		.dvfs_opp_uv = dvfs_opp_uv,
 		.clk = clk,
 		.regulator = regulator,
 	};
Index: optee-txmp-2550/core/arch/arm/plat-stm32mp2/scmi_server_scpfw.c
===================================================================
--- optee-txmp-2550.orig/core/arch/arm/plat-stm32mp2/scmi_server_scpfw.c
+++ optee-txmp-2550/core/arch/arm/plat-stm32mp2/scmi_server_scpfw.c
@@ -100,7 +100,7 @@ static void scmi_scpfw_free_agent(struct
 			struct scmi_perfd *perfd = channel_cfg->perfd + k;
 
 			free(perfd->dvfs_opp_khz);
-			free(perfd->dvfs_opp_mv);
+			free(perfd->dvfs_opp_uv);
 		}
 		free(channel_cfg->perfd);
 
Index: optee-txmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp1/fw/config_all.c
===================================================================
--- optee-txmp-2550.orig/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp1/fw/config_all.c
+++ optee-txmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp1/fw/config_all.c
@@ -745,16 +745,17 @@ static void set_resources(struct scpfw_c
                     for (l = 0; l < perfd_cfg->dvfs_opp_count; l++) {
                         uint64_t power = 0;
 
-                        /* Quick estimation of power consumpsion */
+                        /* Quick estimation of power consumption */
                         power = perfd_cfg->dvfs_opp_khz[l];
-                        power *= perfd_cfg->dvfs_opp_mv[l];
-                        power *= perfd_cfg->dvfs_opp_mv[l];
+                        power *= perfd_cfg->dvfs_opp_uv[l];
+                        power *= perfd_cfg->dvfs_opp_uv[l];
                         power /= 100 * 1000 * 1000;
+                        power /= 1000 * 1000; /* µV -> mV */
 
                         dvfs_data[dvfs_index].opps[l] = (struct mod_dvfs_opp){
                             .level = perfd_cfg->dvfs_opp_khz[l] * 1000UL,
                             .frequency = perfd_cfg->dvfs_opp_khz[l],
-                            .voltage = perfd_cfg->dvfs_opp_mv[l],
+                            .voltage = perfd_cfg->dvfs_opp_uv[l],
                             .power = (uint32_t)power,
                         };
                     }
Index: optee-txmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp2/fw/config_all.c
===================================================================
--- optee-txmp-2550.orig/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp2/fw/config_all.c
+++ optee-txmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp2/fw/config_all.c
@@ -932,16 +932,17 @@ static void set_resources(struct scpfw_c
                     for (l = 0; l < perfd_cfg->dvfs_opp_count; l++) {
                         uint64_t power = 0;
 
-                        /* Quick estimation of power consumpsion */
+                        /* Quick estimation of power consumption */
                         power = perfd_cfg->dvfs_opp_khz[l];
-                        power *= perfd_cfg->dvfs_opp_mv[l];
-                        power *= perfd_cfg->dvfs_opp_mv[l];
+                        power *= perfd_cfg->dvfs_opp_uv[l];
+                        power *= perfd_cfg->dvfs_opp_uv[l];
                         power /= 100 * 1000 * 1000;
+                        power /= 1000 * 1000; /* µV -> mV */
 
                         dvfs_data[dvfs_index].opps[l] = (struct mod_dvfs_opp){
                             .level = perfd_cfg->dvfs_opp_khz[l] * 1000UL,
                             .frequency = perfd_cfg->dvfs_opp_khz[l],
-                            .voltage = perfd_cfg->dvfs_opp_mv[l],
+                            .voltage = perfd_cfg->dvfs_opp_uv[l],
                             .power = (uint32_t)power,
                         };
                     }
Index: optee-txmp-2550/core/lib/scmi-server/include/scmi_agent_configuration.h
===================================================================
--- optee-txmp-2550.orig/core/lib/scmi-server/include/scmi_agent_configuration.h
+++ optee-txmp-2550/core/lib/scmi-server/include/scmi_agent_configuration.h
@@ -50,22 +50,22 @@ struct scmi_voltd {
 /*
  * struct scmi_perfd - DVFS/performance domaindescription
  * @name: DVFS name
- * @dvfs_opp_count: Number of cells in @dvfs_opp_khz and @dvfs_opp_mv
+ * @dvfs_opp_count: Number of cells in @dvfs_opp_khz and @dvfs_opp_uv
  * @initial_opp: Initial operating point
  * @dvfs_opp_khz: Operating point frequencies in Hertz
- * @dvfs_opp_mv: Operating point voltage levels in millivolts
+ * @dvfs_opp_uv: Operating point voltage levels in millivolts
  * @clk: Clock used by the DVFS service
  * @regulator: Regulator used by the DVFS service
  *
  * SCP firmware configuration expects array couple @dvfs_opp_khz and
- * @dvfs_opp_mv values are ordered by increasing performance operating points.
+ * @dvfs_opp_uv values are ordered by increasing performance operating points.
  */
 struct scmi_perfd {
 	const char *name;
 	size_t dvfs_opp_count;
 	size_t initial_opp;
 	unsigned int *dvfs_opp_khz;
-	unsigned int *dvfs_opp_mv;
+	unsigned int *dvfs_opp_uv;
 	struct clk *clk;
 	struct regulator *regulator;
 };
Index: optee-txmp-2550/core/arch/arm/plat-stm32mp2/drivers/stm32mp25_pwr_regul.c
===================================================================
--- optee-txmp-2550.orig/core/arch/arm/plat-stm32mp2/drivers/stm32mp25_pwr_regul.c
+++ optee-txmp-2550/core/arch/arm/plat-stm32mp2/drivers/stm32mp25_pwr_regul.c
@@ -240,7 +240,6 @@ static TEE_Result pwr_set_low_volt(struc
 			if (!(io_read32(reg) & pwr_regu->vrsel_mask)) {
 				EMSG("%s: set VRSEL failed",
 				     regulator_name(regulator));
-				panic();
 			}
 		} else {
 			io_clrbits32(reg, pwr_regu->vrsel_mask);
