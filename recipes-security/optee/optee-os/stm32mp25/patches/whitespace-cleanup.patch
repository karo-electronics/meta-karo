Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/CMakeLists.txt
@@ -20,40 +20,40 @@ list(APPEND CMAKE_MODULE_PATH "${CMAKE_S
 
 if(SCP_FIRMWARE_SOURCE_DIR)
     get_filename_component(SCP_FIRMWARE_SOURCE_DIR "${SCP_FIRMWARE_SOURCE_DIR}"
-                           ABSOLUTE BASE_DIR "${CMAKE_SOURCE_DIR}/product")
+			   ABSOLUTE BASE_DIR "${CMAKE_SOURCE_DIR}/product")
 
     include("${SCP_FIRMWARE_SOURCE_DIR}/Firmware.cmake")
 
     if((NOT SCP_FIRMWARE) OR (NOT SCP_FIRMWARE_TARGET))
-        # cmake-format: off
-        message(FATAL_ERROR
-            "Insufficient firmware metadata provided.\n"
-
-            "Please ensure your 'Firmware.cmake' has set both `SCP_FIRMWARE` "
-            "and `SCP_FIRMWARE_TARGET` to the name of your firmware and the "
-            "firmware's CMake target respectively.")
-        # cmake-format: on
+	# cmake-format: off
+	message(FATAL_ERROR
+	    "Insufficient firmware metadata provided.\n"
+
+	    "Please ensure your 'Firmware.cmake' has set both `SCP_FIRMWARE` "
+	    "and `SCP_FIRMWARE_TARGET` to the name of your firmware and the "
+	    "firmware's CMake target respectively.")
+	# cmake-format: on
     endif()
 
     if(NOT SCP_FIRMWARE_BINARY_DIR)
-        #
-        # Derive the binary directory from the location of the source directory
-        # if it's in-tree.
-        #
+	#
+	# Derive the binary directory from the location of the source directory
+	# if it's in-tree.
+	#
 
-        file(RELATIVE_PATH SCP_FIRMWARE_BINARY_DIR "${CMAKE_SOURCE_DIR}"
-             "${SCP_FIRMWARE_SOURCE_DIR}")
+	file(RELATIVE_PATH SCP_FIRMWARE_BINARY_DIR "${CMAKE_SOURCE_DIR}"
+	     "${SCP_FIRMWARE_SOURCE_DIR}")
     endif()
 
     if(SCP_FIRMWARE_BINARY_DIR MATCHES "\\.\\.")
-        # cmake-format: off
-        message(FATAL_ERROR
-            "Invalid firmware binary directory.\n"
-
-            "Please ensure your firmware binary directory is a relative path. "
-            "This path is used as the location within the project binary "
-            "directory that will be used for firmware artifacts.")
-        # cmake-format: on
+	# cmake-format: off
+	message(FATAL_ERROR
+	    "Invalid firmware binary directory.\n"
+
+	    "Please ensure your firmware binary directory is a relative path. "
+	    "This path is used as the location within the project binary "
+	    "directory that will be used for firmware artifacts.")
+	# cmake-format: on
     endif()
 endif()
 
@@ -70,17 +70,17 @@ if(SCP_TOOLCHAIN_INIT AND (NOT CMAKE_TOO
     #
 
     if(NOT SCP_TOOLCHAIN)
-        set(SCP_TOOLCHAIN "${SCP_TOOLCHAIN_INIT}")
+	set(SCP_TOOLCHAIN "${SCP_TOOLCHAIN_INIT}")
     endif()
 
     set(CMAKE_TOOLCHAIN_FILE
-        "${SCP_FIRMWARE_SOURCE_DIR}/Toolchain-${SCP_TOOLCHAIN}.cmake")
+	"${SCP_FIRMWARE_SOURCE_DIR}/Toolchain-${SCP_TOOLCHAIN}.cmake")
 endif()
 
 if(SCP_TOOLCHAIN STREQUAL "Clang" AND NOT SCP_LLVM_SYSROOT_CC)
     message(
-        FATAL_ERROR
-            "When Clang is set as toolchain SCP_LLVM_SYSROOT_CC must be defined"
+	FATAL_ERROR
+	    "When Clang is set as toolchain SCP_LLVM_SYSROOT_CC must be defined"
     )
 endif()
 
@@ -89,7 +89,7 @@ project(
     VERSION 2.12.0
     DESCRIPTION "Arm SCP/MCP Software"
     HOMEPAGE_URL
-        "https://developer.arm.com/tools-and-software/open-source-software/firmware/scp-firmware"
+	"https://developer.arm.com/tools-and-software/open-source-software/firmware/scp-firmware"
     LANGUAGES C ASM)
 
 #
@@ -101,12 +101,12 @@ project(
 
 if((NOT CMAKE_BUILD_TYPE) AND (NOT CMAKE_CONFIGURATION_TYPES))
     set(CMAKE_BUILD_TYPE
-        "Release"
-        CACHE STRING "Build type." FORCE)
+	"Release"
+	CACHE STRING "Build type." FORCE)
 
     set_property(
-        CACHE CMAKE_BUILD_TYPE PROPERTY STRINGS "Debug" "Release" "MinSizeRel"
-                                                "RelWithDebInfo")
+	CACHE CMAKE_BUILD_TYPE PROPERTY STRINGS "Debug" "Release" "MinSizeRel"
+						"RelWithDebInfo")
 endif()
 
 #
@@ -166,40 +166,40 @@ if(NOT DISABLE_CPPCHECK)
     find_program(CMAKE_C_CPPCHECK NAMES "cppcheck")
 
     if(NOT CMAKE_C_CPPCHECK)
-        unset(CMAKE_C_CPPCHECK CACHE)
+	unset(CMAKE_C_CPPCHECK CACHE)
     endif()
 
     if(CMAKE_C_CPPCHECK)
-        # Prepend cppcheck_wrapper.py before binary to perform pre and post
-        # cppcheck operations.
-        set(CPPCHECK_WRAPPER ${CMAKE_SOURCE_DIR}/tools/cppcheck_wrapper.py)
-        # cmake-format: off
-
-        list(PREPEND CMAKE_C_CPPCHECK  ${CPPCHECK_WRAPPER})
-        list(APPEND CMAKE_C_CPPCHECK "--quiet")
-        list(APPEND CMAKE_C_CPPCHECK "--suppressions-list=${CMAKE_CURRENT_SOURCE_DIR}/tools/cppcheck_suppress_list.txt")
-        list(APPEND CMAKE_C_CPPCHECK "--enable=all")
-        list(APPEND CMAKE_C_CPPCHECK "--error-exitcode=1")
-
-        if(SCP_ARCHITECTURE STREQUAL "arm-m")
-            if(CMAKE_SYSTEM_PROCESSOR MATCHES "cortex-m(33|55)")
-                list(APPEND CMAKE_C_CPPCHECK "--library=${CMAKE_CURRENT_SOURCE_DIR}/.armv8m.cppcheck.cfg")
-            elseif(CMAKE_SYSTEM_PROCESSOR MATCHES "cortex-m(3|7)")
-                list(APPEND CMAKE_C_CPPCHECK "--library=${CMAKE_CURRENT_SOURCE_DIR}/.armv7m.cppcheck.cfg")
-            endif()
-        endif()
-
-        if(CMAKE_C_COMPILER_ID STREQUAL "ARMClang")
-            list(APPEND CMAKE_C_CPPCHECK "--library=${CMAKE_CURRENT_SOURCE_DIR}/.armclang.cppcheck.cfg")
-        else()
-            list(APPEND CMAKE_C_CPPCHECK "--library=${CMAKE_CURRENT_SOURCE_DIR}/.gnu.cppcheck.cfg")
-        endif()
-
-        if(COMMAND_OUTPUT_VERBOSE)
-             list(APPEND CMAKE_C_CPPCHECK "verbose")
-        endif()
+	# Prepend cppcheck_wrapper.py before binary to perform pre and post
+	# cppcheck operations.
+	set(CPPCHECK_WRAPPER ${CMAKE_SOURCE_DIR}/tools/cppcheck_wrapper.py)
+	# cmake-format: off
+
+	list(PREPEND CMAKE_C_CPPCHECK  ${CPPCHECK_WRAPPER})
+	list(APPEND CMAKE_C_CPPCHECK "--quiet")
+	list(APPEND CMAKE_C_CPPCHECK "--suppressions-list=${CMAKE_CURRENT_SOURCE_DIR}/tools/cppcheck_suppress_list.txt")
+	list(APPEND CMAKE_C_CPPCHECK "--enable=all")
+	list(APPEND CMAKE_C_CPPCHECK "--error-exitcode=1")
+
+	if(SCP_ARCHITECTURE STREQUAL "arm-m")
+	    if(CMAKE_SYSTEM_PROCESSOR MATCHES "cortex-m(33|55)")
+		list(APPEND CMAKE_C_CPPCHECK "--library=${CMAKE_CURRENT_SOURCE_DIR}/.armv8m.cppcheck.cfg")
+	    elseif(CMAKE_SYSTEM_PROCESSOR MATCHES "cortex-m(3|7)")
+		list(APPEND CMAKE_C_CPPCHECK "--library=${CMAKE_CURRENT_SOURCE_DIR}/.armv7m.cppcheck.cfg")
+	    endif()
+	endif()
+
+	if(CMAKE_C_COMPILER_ID STREQUAL "ARMClang")
+	    list(APPEND CMAKE_C_CPPCHECK "--library=${CMAKE_CURRENT_SOURCE_DIR}/.armclang.cppcheck.cfg")
+	else()
+	    list(APPEND CMAKE_C_CPPCHECK "--library=${CMAKE_CURRENT_SOURCE_DIR}/.gnu.cppcheck.cfg")
+	endif()
+
+	if(COMMAND_OUTPUT_VERBOSE)
+	     list(APPEND CMAKE_C_CPPCHECK "verbose")
+	endif()
 
-        # cmake-format: on
+	# cmake-format: on
     endif()
 endif()
 
@@ -209,47 +209,47 @@ endif()
 if(ENABLE_CLANG_TIDY)
 
     find_program(
-        CMAKE_C_CLANG_TIDY
-        NAMES "clang-tidy"
-              "clang-tidy-3.6"
-              "clang-tidy-3.7"
-              "clang-tidy-3.8"
-              "clang-tidy-3.9"
-              "clang-tidy-4.0"
-              "clang-tidy-5.0"
-              "clang-tidy-6.0"
-              "clang-tidy-7"
-              "clang-tidy-8"
-              "clang-tidy-9"
-              "clang-tidy-10"
-              "clang-tidy-11")
+	CMAKE_C_CLANG_TIDY
+	NAMES "clang-tidy"
+	      "clang-tidy-3.6"
+	      "clang-tidy-3.7"
+	      "clang-tidy-3.8"
+	      "clang-tidy-3.9"
+	      "clang-tidy-4.0"
+	      "clang-tidy-5.0"
+	      "clang-tidy-6.0"
+	      "clang-tidy-7"
+	      "clang-tidy-8"
+	      "clang-tidy-9"
+	      "clang-tidy-10"
+	      "clang-tidy-11")
 
     if(NOT CMAKE_C_CLANG_TIDY)
-        unset(CMAKE_C_CLANG_TIDY CACHE)
+	unset(CMAKE_C_CLANG_TIDY CACHE)
     endif()
 
     if(CMAKE_C_CLANG_TIDY)
-        # cmake-format: off
+	# cmake-format: off
 
-        if(CMAKE_C_COMPILER_TARGET)
-            list(APPEND CMAKE_C_CLANG_TIDY "--extra-arg=--target=${CMAKE_C_COMPILER_TARGET}")
-        endif()
-
-        foreach(dir IN LISTS CMAKE_C_IMPLICIT_INCLUDE_DIRECTORIES)
-            list(APPEND CMAKE_C_CLANG_TIDY "--extra-arg=-isystem")
-            list(APPEND CMAKE_C_CLANG_TIDY "--extra-arg=${dir}")
-            list(APPEND CMAKE_C_CLANG_TIDY "--extra-arg=-fms-extensions")
-        endforeach()
-
-        if(CMAKE_C_COMPILER_ID STREQUAL "ARMClang")
-            list(APPEND CMAKE_C_CLANG_TIDY "--extra-arg=-D__ARM_PROMISE=__builtin_assume")
-            list(APPEND CMAKE_C_CLANG_TIDY "--extra-arg=-D__ARMCC_VERSION=600000")
-            list(APPEND CMAKE_C_CLANG_TIDY "--extra-arg=-D__ESCAPE__(x)=(x)")
-        endif()
+	if(CMAKE_C_COMPILER_TARGET)
+	    list(APPEND CMAKE_C_CLANG_TIDY "--extra-arg=--target=${CMAKE_C_COMPILER_TARGET}")
+	endif()
+
+	foreach(dir IN LISTS CMAKE_C_IMPLICIT_INCLUDE_DIRECTORIES)
+	    list(APPEND CMAKE_C_CLANG_TIDY "--extra-arg=-isystem")
+	    list(APPEND CMAKE_C_CLANG_TIDY "--extra-arg=${dir}")
+	    list(APPEND CMAKE_C_CLANG_TIDY "--extra-arg=-fms-extensions")
+	endforeach()
+
+	if(CMAKE_C_COMPILER_ID STREQUAL "ARMClang")
+	    list(APPEND CMAKE_C_CLANG_TIDY "--extra-arg=-D__ARM_PROMISE=__builtin_assume")
+	    list(APPEND CMAKE_C_CLANG_TIDY "--extra-arg=-D__ARMCC_VERSION=600000")
+	    list(APPEND CMAKE_C_CLANG_TIDY "--extra-arg=-D__ESCAPE__(x)=(x)")
+	endif()
 
-        list(APPEND CMAKE_C_CLANG_TIDY "--quiet")
+	list(APPEND CMAKE_C_CLANG_TIDY "--quiet")
 
-        # cmake-format: on
+	# cmake-format: on
     endif()
 
 endif()
@@ -262,28 +262,28 @@ if(ENABLE_IWYU)
     find_program(CMAKE_C_INCLUDE_WHAT_YOU_USE NAMES iwyu include-what-you-use)
 
     if(NOT CMAKE_C_INCLUDE_WHAT_YOU_USE)
-        unset(CMAKE_C_INCLUDE_WHAT_YOU_USE CACHE)
+	unset(CMAKE_C_INCLUDE_WHAT_YOU_USE CACHE)
     endif()
 
     if(CMAKE_C_INCLUDE_WHAT_YOU_USE)
-        # cmake-format: off
+	# cmake-format: off
 
-        if(CMAKE_C_COMPILER_TARGET)
-            list(APPEND CMAKE_C_INCLUDE_WHAT_YOU_USE "--target=${CMAKE_C_COMPILER_TARGET}")
-        endif()
-
-        foreach(dir IN LISTS CMAKE_C_IMPLICIT_INCLUDE_DIRECTORIES)
-            list(APPEND CMAKE_C_INCLUDE_WHAT_YOU_USE "-isystem")
-            list(APPEND CMAKE_C_INCLUDE_WHAT_YOU_USE "${dir}")
-        endforeach()
-
-        if(CMAKE_C_COMPILER_ID STREQUAL "ARMClang")
-            list(APPEND CMAKE_C_INCLUDE_WHAT_YOU_USE "-D__ARM_PROMISE=__builtin_assume")
-            list(APPEND CMAKE_C_INCLUDE_WHAT_YOU_USE "-D__ARMCC_VERSION=600000")
-            list(APPEND CMAKE_C_INCLUDE_WHAT_YOU_USE "-D__ESCAPE__(x)=(x)")
-        endif()
+	if(CMAKE_C_COMPILER_TARGET)
+	    list(APPEND CMAKE_C_INCLUDE_WHAT_YOU_USE "--target=${CMAKE_C_COMPILER_TARGET}")
+	endif()
+
+	foreach(dir IN LISTS CMAKE_C_IMPLICIT_INCLUDE_DIRECTORIES)
+	    list(APPEND CMAKE_C_INCLUDE_WHAT_YOU_USE "-isystem")
+	    list(APPEND CMAKE_C_INCLUDE_WHAT_YOU_USE "${dir}")
+	endforeach()
+
+	if(CMAKE_C_COMPILER_ID STREQUAL "ARMClang")
+	    list(APPEND CMAKE_C_INCLUDE_WHAT_YOU_USE "-D__ARM_PROMISE=__builtin_assume")
+	    list(APPEND CMAKE_C_INCLUDE_WHAT_YOU_USE "-D__ARMCC_VERSION=600000")
+	    list(APPEND CMAKE_C_INCLUDE_WHAT_YOU_USE "-D__ESCAPE__(x)=(x)")
+	endif()
 
-        # cmake-format: on
+	# cmake-format: on
     endif()
 
 endif()
@@ -298,7 +298,7 @@ if(CMAKE_C_COMPILER_ID MATCHES "ARMClang
     find_program(SCP_FROMELF fromelf ${base})
 
     if(SCP_FROMELF)
-        mark_as_advanced(SCP_FROMELF)
+	mark_as_advanced(SCP_FROMELF)
     endif()
 endif()
 
@@ -394,36 +394,36 @@ macro(add_executable target)
     _add_executable(${target} ${ARGN})
 
     if("${target}" STREQUAL "${SCP_FIRMWARE_TARGET}")
-        if(SCP_GENERATE_FLAT_BINARY)
-            #
-            # Invoke 'objcopy' or 'fromelf', which we use to generate a flat
-            # binary for the generated firmware image. We make this optional
-            # because some binaries consist of disjoint sections far apart from
-            # one another, which can generate huge binaries.
-            #
-
-            if(SCP_FROMELF)
-                # cmake-format: off
-                add_custom_command(
-                    TARGET ${target} POST_BUILD
-                    COMMAND ${SCP_FROMELF}
-                    ARGS
-                        --bin "$<TARGET_FILE:${target}>"
-                        --output "$<TARGET_FILE_DIR:${target}>/$<TARGET_PROPERTY:${target},OUTPUT_NAME>.bin"
-                    COMMENT "Generating flat binary ${target}.bin")
-                # cmake-format: on
-            elseif(CMAKE_OBJCOPY)
-                # cmake-format: off
-                add_custom_command(
-                    TARGET ${target} POST_BUILD
-                    COMMAND ${CMAKE_OBJCOPY}
-                    ARGS -O binary
-                        "$<TARGET_FILE:${target}>"
-                        "$<TARGET_FILE_DIR:${target}>/$<TARGET_FILE_BASE_NAME:${target}>.bin"
-                    COMMENT "Generating flat binary ${target}.bin")
-                # cmake-format: on
-            endif()
-        endif()
+	if(SCP_GENERATE_FLAT_BINARY)
+	    #
+	    # Invoke 'objcopy' or 'fromelf', which we use to generate a flat
+	    # binary for the generated firmware image. We make this optional
+	    # because some binaries consist of disjoint sections far apart from
+	    # one another, which can generate huge binaries.
+	    #
+
+	    if(SCP_FROMELF)
+		# cmake-format: off
+		add_custom_command(
+		    TARGET ${target} POST_BUILD
+		    COMMAND ${SCP_FROMELF}
+		    ARGS
+			--bin "$<TARGET_FILE:${target}>"
+			--output "$<TARGET_FILE_DIR:${target}>/$<TARGET_PROPERTY:${target},OUTPUT_NAME>.bin"
+		    COMMENT "Generating flat binary ${target}.bin")
+		# cmake-format: on
+	    elseif(CMAKE_OBJCOPY)
+		# cmake-format: off
+		add_custom_command(
+		    TARGET ${target} POST_BUILD
+		    COMMAND ${CMAKE_OBJCOPY}
+		    ARGS -O binary
+			"$<TARGET_FILE:${target}>"
+			"$<TARGET_FILE_DIR:${target}>/$<TARGET_FILE_BASE_NAME:${target}>.bin"
+		    COMMENT "Generating flat binary ${target}.bin")
+		# cmake-format: on
+	    endif()
+	endif()
     endif()
 endmacro()
 
@@ -441,22 +441,22 @@ if(SCP_FIRMWARE)
     #
 
     add_subdirectory("${SCP_FIRMWARE_SOURCE_DIR}" "${SCP_FIRMWARE_BINARY_DIR}"
-                     EXCLUDE_FROM_ALL)
+		     EXCLUDE_FROM_ALL)
 
     #
     # Generate a map file for the firmware.
     #
 
     if(CMAKE_C_COMPILER_ID STREQUAL "ARMClang")
-        target_link_options(
-            ${SCP_FIRMWARE_TARGET}
-            PRIVATE "LINKER:--map"
-                    "LINKER:--list=$<TARGET_FILE_DIR:${SCP_FIRMWARE_TARGET}>/$<TARGET_FILE_BASE_NAME:${SCP_FIRMWARE_TARGET}>.map")
+	target_link_options(
+	    ${SCP_FIRMWARE_TARGET}
+	    PRIVATE "LINKER:--map"
+		    "LINKER:--list=$<TARGET_FILE_DIR:${SCP_FIRMWARE_TARGET}>/$<TARGET_FILE_BASE_NAME:${SCP_FIRMWARE_TARGET}>.map")
     else()
-        target_link_options(
-            ${SCP_FIRMWARE_TARGET}
-            PRIVATE "LINKER:--cref"
-                    "LINKER:-Map=$<TARGET_FILE_DIR:${SCP_FIRMWARE_TARGET}>/$<TARGET_FILE_BASE_NAME:${SCP_FIRMWARE_TARGET}>.map")
+	target_link_options(
+	    ${SCP_FIRMWARE_TARGET}
+	    PRIVATE "LINKER:--cref"
+		    "LINKER:-Map=$<TARGET_FILE_DIR:${SCP_FIRMWARE_TARGET}>/$<TARGET_FILE_BASE_NAME:${SCP_FIRMWARE_TARGET}>.map")
     endif()
 
     #
@@ -467,32 +467,32 @@ if(SCP_FIRMWARE)
     #
 
     if("${SCP_ENABLE_OVERRIDE_FIRMWARE_NAME}" STREQUAL "")
-        set(SCP_FIRMWARE_NAME ${SCP_FIRMWARE_TARGET})
+	set(SCP_FIRMWARE_NAME ${SCP_FIRMWARE_TARGET})
     else()
-        set(SCP_FIRMWARE_NAME ${SCP_ENABLE_OVERRIDE_FIRMWARE_NAME})
+	set(SCP_FIRMWARE_NAME ${SCP_ENABLE_OVERRIDE_FIRMWARE_NAME})
     endif()
 
     set_target_properties(
-        ${SCP_FIRMWARE_TARGET}
-        PROPERTIES EXCLUDE_FROM_ALL FALSE
-                   OUTPUT_NAME "${SCP_FIRMWARE_NAME}"
-                   RUNTIME_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/bin")
+	${SCP_FIRMWARE_TARGET}
+	PROPERTIES EXCLUDE_FROM_ALL FALSE
+		   OUTPUT_NAME "${SCP_FIRMWARE_NAME}"
+		   RUNTIME_OUTPUT_DIRECTORY "${CMAKE_BINARY_DIR}/bin")
 
     set_target_properties(
-        ${SCP_FIRMWARE_TARGET}
-        PROPERTIES EXCLUDE_FROM_ALL FALSE
-        SUFFIX ".elf")
+	${SCP_FIRMWARE_TARGET}
+	PROPERTIES EXCLUDE_FROM_ALL FALSE
+	SUFFIX ".elf")
     #
     # Load in the CLI debugger if it was requested.
     #
 
     cmake_dependent_option(
-        SCP_ENABLE_DEBUGGER "Enable the debugger-cli subsystem?"
-        "${SCP_ENABLE_DEBUGGER_INIT}" "DEFINED SCP_ENABLE_DEBUGGER_INIT"
-        "${SCP_ENABLE_DEBUGGER}")
+	SCP_ENABLE_DEBUGGER "Enable the debugger-cli subsystem?"
+	"${SCP_ENABLE_DEBUGGER_INIT}" "DEFINED SCP_ENABLE_DEBUGGER_INIT"
+	"${SCP_ENABLE_DEBUGGER}")
 
     if(SCP_ENABLE_DEBUGGER)
-        add_subdirectory("debugger" EXCLUDE_FROM_ALL)
+	add_subdirectory("debugger" EXCLUDE_FROM_ALL)
     endif()
 
     #
@@ -547,16 +547,16 @@ if(SCP_FIRMWARE)
     list(APPEND scp_libraries "$<TARGET_FILE:${SCP_FIRMWARE_TARGET}>")
 
     foreach(module IN LISTS SCP_MODULE_TARGETS)
-        string(APPEND scp_sources "$<TARGET_PROPERTY:${module},INTERFACE_SOURCES>$<SEMICOLON>")
-        string(APPEND scp_sources "$<TARGET_PROPERTY:${module},SOURCES>$<SEMICOLON>")
+	string(APPEND scp_sources "$<TARGET_PROPERTY:${module},INTERFACE_SOURCES>$<SEMICOLON>")
+	string(APPEND scp_sources "$<TARGET_PROPERTY:${module},SOURCES>$<SEMICOLON>")
 
-        string(APPEND scp_includes "$<TARGET_PROPERTY:${module},INTERFACE_INCLUDE_DIRECTORIES>$<SEMICOLON>")
-        string(APPEND scp_includes "$<TARGET_PROPERTY:${module},INCLUDE_DIRECTORIES>$<SEMICOLON>")
+	string(APPEND scp_includes "$<TARGET_PROPERTY:${module},INTERFACE_INCLUDE_DIRECTORIES>$<SEMICOLON>")
+	string(APPEND scp_includes "$<TARGET_PROPERTY:${module},INCLUDE_DIRECTORIES>$<SEMICOLON>")
 
-        string(APPEND scp_defines "$<TARGET_PROPERTY:${module},INTERFACE_COMPILE_DEFINITIONS>$<SEMICOLON>")
-        string(APPEND scp_defines "$<TARGET_PROPERTY:${module},COMPILE_DEFINITIONS>$<SEMICOLON>")
+	string(APPEND scp_defines "$<TARGET_PROPERTY:${module},INTERFACE_COMPILE_DEFINITIONS>$<SEMICOLON>")
+	string(APPEND scp_defines "$<TARGET_PROPERTY:${module},COMPILE_DEFINITIONS>$<SEMICOLON>")
 
-        list(APPEND scp_libraries "$<TARGET_FILE:${module}>")
+	list(APPEND scp_libraries "$<TARGET_FILE:${module}>")
     endforeach()
 
     set(scp_sources $<REMOVE_DUPLICATES:${scp_sources}>)
@@ -577,8 +577,8 @@ set(SCP_FIRMWARE_LIB "${SCP_FIRMWARE_TAR
 # merged libraries is only built when `make ${SCP_FIRMWARE_LIB}` is issued
 add_custom_target(${SCP_FIRMWARE_LIB}
     # COMMAND/COMMENT must be upper case
-        COMMAND ${CMAKE_AR} rcT $<TARGET_FILE_DIR:${SCP_FIRMWARE_TARGET}>/lib${SCP_FIRMWARE_LIB}.a ${scp_libraries}
-        COMMENT " Merging all libraries in a single lib${SCP_FIRMWARE_LIB}.a"
+	COMMAND ${CMAKE_AR} rcT $<TARGET_FILE_DIR:${SCP_FIRMWARE_TARGET}>/lib${SCP_FIRMWARE_LIB}.a ${scp_libraries}
+	COMMENT " Merging all libraries in a single lib${SCP_FIRMWARE_LIB}.a"
 )
 
 add_dependencies(${SCP_FIRMWARE_LIB} ${SCP_FIRMWARE_TARGET})
@@ -618,14 +618,14 @@ foreach(type cmake markdown yaml c)
     unset(sources)
 
     foreach(include IN LISTS glob_includes)
-        foreach(glob IN LISTS ${type}_globs)
-            file(GLOB_RECURSE _sources "${include}/${glob}")
-            list(APPEND sources ${_sources})
-        endforeach()
+	foreach(glob IN LISTS ${type}_globs)
+	    file(GLOB_RECURSE _sources "${include}/${glob}")
+	    list(APPEND sources ${_sources})
+	endforeach()
     endforeach()
 
     foreach(exclude IN LISTS glob_excludes)
-        list(FILTER sources EXCLUDE REGEX "${exclude}")
+	list(FILTER sources EXCLUDE REGEX "${exclude}")
     endforeach()
 
     set(${type}_sources ${sources})
@@ -639,31 +639,31 @@ find_package(CMakeFormat OPTIONAL_COMPON
 
 if(TARGET CMakeFormat::Format)
     add_custom_target(
-        format-cmake
-        COMMAND CMakeFormat::Format -i "${cmake_sources}"
-        WORKING_DIRECTORY "${SCP_SOURCE_DIR}"
-        COMMENT "Formatting CMake sources..."
-        COMMAND_EXPAND_LISTS)
+	format-cmake
+	COMMAND CMakeFormat::Format -i "${cmake_sources}"
+	WORKING_DIRECTORY "${SCP_SOURCE_DIR}"
+	COMMENT "Formatting CMake sources..."
+	COMMAND_EXPAND_LISTS)
 
     list(APPEND format_targets "format-cmake")
 
     add_custom_target(
-        check-cmake
-        COMMAND CMakeFormat::Format --check "${cmake_sources}"
-        WORKING_DIRECTORY "${SCP_SOURCE_DIR}"
-        COMMENT "Checking CMake sources..."
-        COMMAND_EXPAND_LISTS)
+	check-cmake
+	COMMAND CMakeFormat::Format --check "${cmake_sources}"
+	WORKING_DIRECTORY "${SCP_SOURCE_DIR}"
+	COMMENT "Checking CMake sources..."
+	COMMAND_EXPAND_LISTS)
 
     list(APPEND check_targets "check-cmake")
 endif()
 
 if(TARGET CMakeFormat::Lint)
     add_custom_target(
-        lint-cmake
-        COMMAND CMakeFormat::Lint --suppress-decorations "${cmake_sources}"
-        WORKING_DIRECTORY "${SCP_SOURCE_DIR}"
-        COMMENT "Linting CMake sources..."
-        COMMAND_EXPAND_LISTS)
+	lint-cmake
+	COMMAND CMakeFormat::Lint --suppress-decorations "${cmake_sources}"
+	WORKING_DIRECTORY "${SCP_SOURCE_DIR}"
+	COMMENT "Linting CMake sources..."
+	COMMAND_EXPAND_LISTS)
 
     list(APPEND lint_targets "lint-cmake")
 endif()
@@ -676,11 +676,11 @@ find_package(Markdownlint)
 
 if(Markdownlint_FOUND)
     add_custom_target(
-        lint-markdown
-        COMMAND Markdownlint "${markdown_sources}"
-        WORKING_DIRECTORY "${SCP_SOURCE_DIR}"
-        COMMENT "Running Markdown sources..."
-        COMMAND_EXPAND_LISTS)
+	lint-markdown
+	COMMAND Markdownlint "${markdown_sources}"
+	WORKING_DIRECTORY "${SCP_SOURCE_DIR}"
+	COMMENT "Running Markdown sources..."
+	COMMAND_EXPAND_LISTS)
 
     list(APPEND lint_targets "lint-markdown")
 endif()
@@ -693,22 +693,22 @@ find_package(Python3 COMPONENTS Interpre
 
 if(Python3_Interpreter_FOUND)
     add_custom_target(
-        format-yaml
-        COMMAND Python3::Interpreter "${SCP_SOURCE_DIR}/tools/yaml-format.py"
-                format -i "${yaml_sources}"
-        WORKING_DIRECTORY "${SCP_SOURCE_DIR}"
-        COMMENT "Formatting YAML sources..."
-        COMMAND_EXPAND_LISTS)
+	format-yaml
+	COMMAND Python3::Interpreter "${SCP_SOURCE_DIR}/tools/yaml-format.py"
+		format -i "${yaml_sources}"
+	WORKING_DIRECTORY "${SCP_SOURCE_DIR}"
+	COMMENT "Formatting YAML sources..."
+	COMMAND_EXPAND_LISTS)
 
     list(APPEND format_targets "format-yaml")
 
     add_custom_target(
-        check-yaml
-        COMMAND Python3::Interpreter "${SCP_SOURCE_DIR}/tools/yaml-format.py"
-                diff --check "${yaml_sources}"
-        WORKING_DIRECTORY "${SCP_SOURCE_DIR}"
-        COMMENT "Checking YAML sources..."
-        COMMAND_EXPAND_LISTS)
+	check-yaml
+	COMMAND Python3::Interpreter "${SCP_SOURCE_DIR}/tools/yaml-format.py"
+		diff --check "${yaml_sources}"
+	WORKING_DIRECTORY "${SCP_SOURCE_DIR}"
+	COMMENT "Checking YAML sources..."
+	COMMAND_EXPAND_LISTS)
 
     list(APPEND check_targets "check-yaml")
 endif()
@@ -721,11 +721,11 @@ find_package(Yamllint)
 
 if(Yamllint_FOUND)
     add_custom_target(
-        lint-yaml
-        COMMAND Yamllint -s "${yaml_sources}"
-        WORKING_DIRECTORY "${SCP_SOURCE_DIR}"
-        COMMENT "Linting YAML sources..."
-        COMMAND_EXPAND_LISTS)
+	lint-yaml
+	COMMAND Yamllint -s "${yaml_sources}"
+	WORKING_DIRECTORY "${SCP_SOURCE_DIR}"
+	COMMENT "Linting YAML sources..."
+	COMMAND_EXPAND_LISTS)
 
     list(APPEND lint_targets "lint-yaml")
 endif()
@@ -738,22 +738,22 @@ find_package(Clang OPTIONAL_COMPONENTS F
 
 if(Clang_FormatGit_FOUND)
     add_custom_target(
-        format-diff-c
-        COMMAND Clang::FormatGit HEAD
-        WORKING_DIRECTORY "${SCP_SOURCE_DIR}"
-        COMMENT "Formatting modified C/C++ sources..."
-        COMMAND_EXPAND_LISTS)
+	format-diff-c
+	COMMAND Clang::FormatGit HEAD
+	WORKING_DIRECTORY "${SCP_SOURCE_DIR}"
+	COMMENT "Formatting modified C/C++ sources..."
+	COMMAND_EXPAND_LISTS)
 
     list(APPEND format_diff_targets "format-diff-c")
 endif()
 
 if(Clang_Format_FOUND)
     add_custom_target(
-        format-c
-        COMMAND Clang::Format -i "${c_sources}"
-        WORKING_DIRECTORY "${SCP_SOURCE_DIR}"
-        COMMENT "Formatting C/C++ sources..."
-        COMMAND_EXPAND_LISTS)
+	format-c
+	COMMAND Clang::Format -i "${c_sources}"
+	WORKING_DIRECTORY "${SCP_SOURCE_DIR}"
+	COMMENT "Formatting C/C++ sources..."
+	COMMAND_EXPAND_LISTS)
 
     list(APPEND format_targets "format-c")
 endif()
@@ -766,15 +766,15 @@ find_package(Python3 COMPONENTS Interpre
 
 if(Python3_Interpreter_FOUND)
     add_custom_target(
-        check-c
-        COMMAND
-            Python3::Interpreter
-            "${SCP_SOURCE_DIR}/contrib/run-clang-format/git/run-clang-format.py"
-            --clang-format-executable "${Clang_Format_EXECUTABLE}"
-            "${c_sources}"
-        WORKING_DIRECTORY "${SCP_SOURCE_DIR}"
-        COMMENT "Checking C sources..."
-        COMMAND_EXPAND_LISTS)
+	check-c
+	COMMAND
+	    Python3::Interpreter
+	    "${SCP_SOURCE_DIR}/contrib/run-clang-format/git/run-clang-format.py"
+	    --clang-format-executable "${Clang_Format_EXECUTABLE}"
+	    "${c_sources}"
+	WORKING_DIRECTORY "${SCP_SOURCE_DIR}"
+	COMMENT "Checking C sources..."
+	COMMAND_EXPAND_LISTS)
 
     list(APPEND check_targets "check-c")
 endif()
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/Jenkinsfile
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/Jenkinsfile
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/Jenkinsfile
@@ -3,13 +3,13 @@ List generateMatrix(Map matrixAxes) {
     List axes = []
 
     matrixAxes.each { axis, values ->
-        List axisList = []
+	List axisList = []
 
-        values.each { value ->
-            axisList << [(axis): value]
-        }
+	values.each { value ->
+	    axisList << [(axis): value]
+	}
 
-        axes << axisList
+	axes << axisList
     }
 
     axes.combinations()*.sum()
@@ -17,153 +17,153 @@ List generateMatrix(Map matrixAxes) {
 
 pipeline {
     agent {
-        label 'docker'
+	label 'docker'
     }
 
     stages {
-        stage('Build container') {
-            steps {
-                /*
-                 * Using `--target` with the Dockerfile agent currently bugs out
-                 * on Jenkins, producing an error along the lines of:
-                 *
-                 *     Cannot retrieve .Id from 'docker inspect ...'
-                 *
-                 * Apparently it doesn't like multi-stage builds, so until
-                 * we're on a version of the Docker workflow plugin that
-                 * doesn't exhibit this bug, we just have to build the image
-                 * ourselves.
-                 *
-                 * See:
-                 *   - https://github.com/jenkinsci/docker-workflow-plugin/pull/149
-                 *   - https://github.com/jenkinsci/docker-workflow-plugin/pull/162
-                 *   - https://github.com/jenkinsci/docker-workflow-plugin/pull/180
-                 *
-                 * Once the Docker workflow plugin has been updated, we should
-                 * be able to get rid of this stage and use:
-                 *
-                 *     agent {
-                 *         dockerfile {
-                 *             dir 'docker'
-                 *             filename 'Dockerfile'
-                 *             additionalBuildArgs '--target ci'
-                 *         }
-                 *     }
-                 */
-
-                sh """ \
-                    docker build -t scp-firmware:build-${currentBuild.number} \
-                        --build-arg JENKINS_UID=1000 \
-                        --build-arg JENKINS_GID=36293 \
-                        --target=jenkins docker
-                """
-            }
-        }
-
-        stage('Run tests') {
-            parallel {
-                stage('Run legacy tests') {
-                    agent {
-                        docker {
-                            image "scp-firmware:build-${currentBuild.number}"
-                            args '-e ARMLMD_LICENSE_FILE'
-                        }
-                    }
-
-                    steps {
-                        sh '/usr/local/bin/init'
-                        sh 'python3 ./tools/ci.py'
-                    }
-                }
-
-                stage('Build and test') {
-                    /*
-                     * We are on an old enough version of the Jenkins pipeline
-                     * workflow plugin that we do not have support for matrices.
-                     *
-                     * Without this support, we need to generate the stages as
-                     * part of a scripted stage.
-                     *
-                     * See:
-                     *  - https://www.jenkins.io/blog/2019/11/22/welcome-to-the-matrix/
-                     *  - https://www.jenkins.io/blog/2019/12/02/matrix-building-with-scripted-pipeline/
-                     *  - https://stackoverflow.com/questions/60829465/jenkins-unknown-stage-section-matrix-in-declarative-pipeline
-                     *
-                     * Once the plugin has been updated, we can adopt a proper
-                     * matrix.
-                     */
-
-                    steps {
-                        script {
-                            /*
-                             * This is, admittedly, not particularly clean, but
-                             * should require very little adjustment. The
-                             * premise is relatively simple: generate a list of
-                             * all the possible matrix combinations, create a
-                             * a list of closures returning a pipeline stage for
-                             * each combination, then execute them in parallel.
-                             */
-
-                            def axes = [
-                                generator: [ 'Ninja', 'Unix Makefiles' ],
-                                buildType: [ 'Debug', 'Release', 'MinSizeRel',
-                                             'RelWithDebInfo' ]
-                            ]
-
-                            def tasks = generateMatrix(axes).collectEntries { config ->
-                                ["Build and test (${config})", {
-                                    node('docker') {
-                                        docker
-                                            .image("scp-firmware:build-${currentBuild.number}")
-                                            .inside('-e ARMLMD_LICENSE_FILE')
-                                        {
-                                            /*
-                                             * If you need to adjust the
-                                             * behaviour of the generated stages
-                                             * then this is probably where you
-                                             * want to do it.
-                                             */
-
-                                            stage("Build ${config}") {
-                                                checkout scm
-
-                                                /*
-                                                 * Unfortunately, we don't have
-                                                 * the CMake build plugin
-                                                 * available to us, so we'll
-                                                 * have to make do with shell
-                                                 * scripts for now.
-                                                 */
-
-                                                sh '/usr/local/bin/init'
-
-                                                sh """ \
-                                                    cmake \
-                                                        -G "${config.generator}" \
-                                                        -DCMAKE_BUILD_TYPE="${config.buildType}" \
-                                                        .
-                                                """
-
-                                                sh 'cmake --build .'
-                                            }
-
-                                            stage("Check ${config}") {
-                                                sh """
-                                                    cmake
-                                                        --build . \
-                                                        --target check
-                                                """
-                                            }
-                                        }
-                                    }
-                                }]
-                            }
-
-                            parallel tasks
-                        }
-                    }
-                }
-            }
-        }
+	stage('Build container') {
+	    steps {
+		/*
+		 * Using `--target` with the Dockerfile agent currently bugs out
+		 * on Jenkins, producing an error along the lines of:
+		 *
+		 *     Cannot retrieve .Id from 'docker inspect ...'
+		 *
+		 * Apparently it doesn't like multi-stage builds, so until
+		 * we're on a version of the Docker workflow plugin that
+		 * doesn't exhibit this bug, we just have to build the image
+		 * ourselves.
+		 *
+		 * See:
+		 *   - https://github.com/jenkinsci/docker-workflow-plugin/pull/149
+		 *   - https://github.com/jenkinsci/docker-workflow-plugin/pull/162
+		 *   - https://github.com/jenkinsci/docker-workflow-plugin/pull/180
+		 *
+		 * Once the Docker workflow plugin has been updated, we should
+		 * be able to get rid of this stage and use:
+		 *
+		 *     agent {
+		 *         dockerfile {
+		 *             dir 'docker'
+		 *             filename 'Dockerfile'
+		 *             additionalBuildArgs '--target ci'
+		 *         }
+		 *     }
+		 */
+
+		sh """ \
+		    docker build -t scp-firmware:build-${currentBuild.number} \
+			--build-arg JENKINS_UID=1000 \
+			--build-arg JENKINS_GID=36293 \
+			--target=jenkins docker
+		"""
+	    }
+	}
+
+	stage('Run tests') {
+	    parallel {
+		stage('Run legacy tests') {
+		    agent {
+			docker {
+			    image "scp-firmware:build-${currentBuild.number}"
+			    args '-e ARMLMD_LICENSE_FILE'
+			}
+		    }
+
+		    steps {
+			sh '/usr/local/bin/init'
+			sh 'python3 ./tools/ci.py'
+		    }
+		}
+
+		stage('Build and test') {
+		    /*
+		     * We are on an old enough version of the Jenkins pipeline
+		     * workflow plugin that we do not have support for matrices.
+		     *
+		     * Without this support, we need to generate the stages as
+		     * part of a scripted stage.
+		     *
+		     * See:
+		     *  - https://www.jenkins.io/blog/2019/11/22/welcome-to-the-matrix/
+		     *  - https://www.jenkins.io/blog/2019/12/02/matrix-building-with-scripted-pipeline/
+		     *  - https://stackoverflow.com/questions/60829465/jenkins-unknown-stage-section-matrix-in-declarative-pipeline
+		     *
+		     * Once the plugin has been updated, we can adopt a proper
+		     * matrix.
+		     */
+
+		    steps {
+			script {
+			    /*
+			     * This is, admittedly, not particularly clean, but
+			     * should require very little adjustment. The
+			     * premise is relatively simple: generate a list of
+			     * all the possible matrix combinations, create a
+			     * a list of closures returning a pipeline stage for
+			     * each combination, then execute them in parallel.
+			     */
+
+			    def axes = [
+				generator: [ 'Ninja', 'Unix Makefiles' ],
+				buildType: [ 'Debug', 'Release', 'MinSizeRel',
+					     'RelWithDebInfo' ]
+			    ]
+
+			    def tasks = generateMatrix(axes).collectEntries { config ->
+				["Build and test (${config})", {
+				    node('docker') {
+					docker
+					    .image("scp-firmware:build-${currentBuild.number}")
+					    .inside('-e ARMLMD_LICENSE_FILE')
+					{
+					    /*
+					     * If you need to adjust the
+					     * behaviour of the generated stages
+					     * then this is probably where you
+					     * want to do it.
+					     */
+
+					    stage("Build ${config}") {
+						checkout scm
+
+						/*
+						 * Unfortunately, we don't have
+						 * the CMake build plugin
+						 * available to us, so we'll
+						 * have to make do with shell
+						 * scripts for now.
+						 */
+
+						sh '/usr/local/bin/init'
+
+						sh """ \
+						    cmake \
+							-G "${config.generator}" \
+							-DCMAKE_BUILD_TYPE="${config.buildType}" \
+							.
+						"""
+
+						sh 'cmake --build .'
+					    }
+
+					    stage("Check ${config}") {
+						sh """
+						    cmake
+							--build . \
+							--target check
+						"""
+					    }
+					}
+				    }
+				}]
+			    }
+
+			    parallel tasks
+			}
+		    }
+		}
+	    }
+	}
     }
 }
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/Makefile.cmake
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/Makefile.cmake
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/Makefile.cmake
@@ -125,25 +125,25 @@ PRODUCT_INDEPENDENT_GOALS := clean help
 
 ifneq ($(filter-out $(PRODUCT_INDEPENDENT_GOALS), $(MAKECMDGOALS)),)
     ifeq ($(PRODUCT),)
-        $(error "You must define PRODUCT. Aborting...")
+	$(error "You must define PRODUCT. Aborting...")
     endif
 
     PRODUCT_DIR := $(PRODUCTS_DIR)/$(PRODUCT)
 
     ifeq ($(wildcard $(PRODUCT_DIR)/product.mk),)
-        $(error "Missing product.mk in $(PRODUCT_DIR)")
+	$(error "Missing product.mk in $(PRODUCT_DIR)")
     endif
 
     include $(PRODUCT_DIR)/product.mk
 
     ifeq ($(BS_FIRMWARE_LIST),)
-        $(error "You must define BS_FIRMWARE_LIST in product.mk. Aborting...")
+	$(error "You must define BS_FIRMWARE_LIST in product.mk. Aborting...")
     endif
 
     # Terminate the build if the chosen platform is in the list of deprecated
     # ones.
     ifeq ($(filter-out $(DEPRECATED_PLATFORMS), $(PRODUCT)),)
-        $(error "$(PRODUCT) has been deprecated! Build terminated")
+	$(error "$(PRODUCT) has been deprecated! Build terminated")
     endif
 
     FIRMWARE_TARGETS := $(addprefix firmware-, $(BS_FIRMWARE_LIST))
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/Vagrantfile
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/Vagrantfile
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/Vagrantfile
@@ -8,14 +8,14 @@ Vagrant.configure("2") do |config|
     config.vm.provision "file", source: "~/.gitconfig", destination: "${HOME}/.gitconfig"
 
     config.vm.provider "docker" do |d|
-        d.build_dir = "docker"
+	d.build_dir = "docker"
 
-        d.build_args = [ "--target", "vagrant" ]
-        d.cmd = [ "sudo", "/usr/sbin/sshd", "-D" ]
-        d.env = { "ARMLMD_LICENSE_FILE" => ENV["ARMLMD_LICENSE_FILE"] }
+	d.build_args = [ "--target", "vagrant" ]
+	d.cmd = [ "sudo", "/usr/sbin/sshd", "-D" ]
+	d.env = { "ARMLMD_LICENSE_FILE" => ENV["ARMLMD_LICENSE_FILE"] }
 
-        d.has_ssh = true
+	d.has_ssh = true
 
-        ENV["LC_ALL"] = "C.UTF-8"
+	ENV["LC_ALL"] = "C.UTF-8"
     end
 end
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/arch/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/CMakeLists.txt
@@ -16,46 +16,46 @@ foreach(path IN LISTS SCP_ARCHITECTURE_P
     unset(SCP_ARCHITECTURE)
 
     include("${path}/Architecture.cmake" OPTIONAL
-            RESULT_VARIABLE SCP_ARCHITECTURE_LIST_FILE)
+	    RESULT_VARIABLE SCP_ARCHITECTURE_LIST_FILE)
 
     if(NOT SCP_ARCHITECTURE_LIST_FILE)
-        # cmake-format: off
-        message(WARNING
-            "No architecture list file found!\n"
-
-            "${path}/Architecture.cmake\n"
-
-            "This architecture path was provided as part of a firmware "
-            "initial-cache file ('Firmware.cmake'), but its "
-            "'Architecture.cmake' file could not be located.")
-        # cmake-format: on
+	# cmake-format: off
+	message(WARNING
+	    "No architecture list file found!\n"
+
+	    "${path}/Architecture.cmake\n"
+
+	    "This architecture path was provided as part of a firmware "
+	    "initial-cache file ('Firmware.cmake'), but its "
+	    "'Architecture.cmake' file could not be located.")
+	# cmake-format: on
 
-        continue()
+	continue()
     elseif(NOT DEFINED SCP_ARCHITECTURE)
-        # cmake-format: off
-        message(WARNING
-            "No name given by architecture metadata file!\n"
-
-            "${path}/Architecture.cmake\n"
-
-            "This architecture metadata file has not yielded an architecture "
-            "name and as such cannot be identified. please ensure that you have "
-            "set `SCP_ARCHITECTURE` in your 'Architecture.cmake' file.")
-        # cmake-format: on
+	# cmake-format: off
+	message(WARNING
+	    "No name given by architecture metadata file!\n"
+
+	    "${path}/Architecture.cmake\n"
+
+	    "This architecture metadata file has not yielded an architecture "
+	    "name and as such cannot be identified. please ensure that you have "
+	    "set `SCP_ARCHITECTURE` in your 'Architecture.cmake' file.")
+	# cmake-format: on
 
-        continue()
+	continue()
     elseif(NOT DEFINED SCP_ARCHITECTURE_TARGET)
-        # cmake-format: off
-        message(WARNING
-            "No target given by architecture: ${SCP_ARCHITECTURE}\n"
-
-            "This architecture has not yielded a target name, and as such "
-            "cannot be used as a dependency for any other targets. "
-            "please ensure that you have set `SCP_ARCHITECTURE_TARGET` in your "
-            "'Architecture.cmake' file.")
-        # cmake-format: on
+	# cmake-format: off
+	message(WARNING
+	    "No target given by architecture: ${SCP_ARCHITECTURE}\n"
+
+	    "This architecture has not yielded a target name, and as such "
+	    "cannot be used as a dependency for any other targets. "
+	    "please ensure that you have set `SCP_ARCHITECTURE_TARGET` in your "
+	    "'Architecture.cmake' file.")
+	# cmake-format: on
 
-        continue()
+	continue()
     endif()
 
     #
@@ -73,12 +73,12 @@ set(SCP_ARCHITECTURE "${SCP_ARCHITECTURE
 if(NOT "${SCP_ARCHITECTURE}" IN_LIST SCP_VALID_ARCHITECTURES)
     # cmake-format: off
     message(FATAL_ERROR
-        "No architecture found: ${SCP_ARCHITECTURE}\n"
+	"No architecture found: ${SCP_ARCHITECTURE}\n"
 
-        "The architecture requested by the firmware you are trying to build "
-        "has not been found. This is probably because the firmware "
-        "initial-cache file you provided has requested a custom architecture "
-        "without providing its path.")
+	"The architecture requested by the firmware you are trying to build "
+	"has not been found. This is probably because the firmware "
+	"initial-cache file you provided has requested a custom architecture "
+	"without providing its path.")
     # cmake-format: on
 endif()
 
@@ -96,7 +96,7 @@ set(SCP_ARCHITECTURE_BINARY_DIR
     "${CMAKE_CURRENT_BINARY_DIR}/${SCP_ARCHITECTURE}")
 
 add_subdirectory("${SCP_ARCHITECTURE_SOURCE_DIR}"
-                 "${SCP_ARCHITECTURE_BINARY_DIR}" EXCLUDE_FROM_ALL)
+		 "${SCP_ARCHITECTURE_BINARY_DIR}" EXCLUDE_FROM_ALL)
 
 #
 # Ensure the firmware links to the architecture library, and that the
@@ -111,12 +111,12 @@ target_link_libraries(${SCP_ARCHITECTURE
 target_include_directories(
     ${SCP_ARCHITECTURE_TARGET}
     PRIVATE
-        $<TARGET_PROPERTY:${SCP_FIRMWARE_TARGET},INTERFACE_INCLUDE_DIRECTORIES>)
+	$<TARGET_PROPERTY:${SCP_FIRMWARE_TARGET},INTERFACE_INCLUDE_DIRECTORIES>)
 
 target_compile_definitions(
     ${SCP_ARCHITECTURE_TARGET}
     PRIVATE
-        $<TARGET_PROPERTY:${SCP_FIRMWARE_TARGET},INTERFACE_COMPILE_DEFINITIONS>)
+	$<TARGET_PROPERTY:${SCP_FIRMWARE_TARGET},INTERFACE_COMPILE_DEFINITIONS>)
 
 set(SCP_ARCHITECTURE_TARGET
     "${SCP_ARCHITECTURE_TARGET}"
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/arm/arm-m/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/arch/arm/arm-m/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/arm/arm-m/CMakeLists.txt
@@ -13,23 +13,23 @@ add_library(arch-arm-m)
 #
 
 if((CMAKE_SYSTEM_PROCESSOR MATCHES "cortex-m(33|55)")
-                           OR (CMAKE_SYSTEM_ARCH MATCHES "armv8(\.1)?-m"))
+			   OR (CMAKE_SYSTEM_ARCH MATCHES "armv8(\.1)?-m"))
     target_compile_definitions(arch-arm-m PUBLIC "ARMV8M")
 else()
     target_compile_definitions(arch-arm-m PUBLIC "ARMV7M")
 endif()
 
 target_include_directories(arch-arm-m
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 # cmake-lint: disable=E1122
 
 target_sources(
     arch-arm-m
     PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/arch_exceptions.c"
-            "${CMAKE_CURRENT_SOURCE_DIR}/src/arch_handlers.c"
-            "${CMAKE_CURRENT_SOURCE_DIR}/src/arch_main.c"
-            "${CMAKE_CURRENT_SOURCE_DIR}/src/arch_nvic.c"
+	    "${CMAKE_CURRENT_SOURCE_DIR}/src/arch_handlers.c"
+	    "${CMAKE_CURRENT_SOURCE_DIR}/src/arch_main.c"
+	    "${CMAKE_CURRENT_SOURCE_DIR}/src/arch_nvic.c"
     PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/../src/arch_mm.c")
 
 target_link_libraries(arch-arm-m PUBLIC cmsis::core-m)
@@ -48,15 +48,15 @@ cmake_dependent_option(
 
 if(SCP_ENABLE_NEWLIB_NANO)
     if(CMAKE_C_COMPILER_ID STREQUAL "GNU")
-        target_link_options(arch-arm-m PUBLIC "--specs=nano.specs")
+	target_link_options(arch-arm-m PUBLIC "--specs=nano.specs")
     elseif(CMAKE_C_COMPILER_ID STREQUAL "Clang")
-        target_link_libraries(arch-arm-m PUBLIC c_nano)
+	target_link_libraries(arch-arm-m PUBLIC c_nano)
     endif()
 else()
     if(CMAKE_C_COMPILER_ID STREQUAL "GNU")
-        target_link_options(arch-arm-m PUBLIC "--specs=nosys.specs")
+	target_link_options(arch-arm-m PUBLIC "--specs=nosys.specs")
     elseif(CMAKE_C_COMPILER_ID STREQUAL "Clang")
-        target_link_libraries(arch-arm-m PUBLIC c)
+	target_link_libraries(arch-arm-m PUBLIC c)
     endif()
 endif()
 
@@ -69,12 +69,12 @@ target_link_options(arch-arm-m PUBLIC "L
 
 if(CMAKE_C_COMPILER_ID STREQUAL "ARMClang")
     target_link_options(arch-arm-m
-                        PUBLIC "LINKER:--entry=arch_exception_reset")
+			PUBLIC "LINKER:--entry=arch_exception_reset")
 endif()
 
 if(SCP_HAVE_NEWLIB)
     target_link_options(arch-arm-m PUBLIC "LINKER:--undefined=posix_memalign"
-                                           "LINKER:--undefined=_sbrk")
+					   "LINKER:--undefined=_sbrk")
 endif()
 
 #
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/arm/arm-m/include/arch_helpers.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/arch/arm/arm-m/include/arch_helpers.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/arm/arm-m/include/arch_helpers.h
@@ -26,12 +26,12 @@ inline static void arch_interrupts_enabl
 {
     /* Decrement critical_section_nest_level only if in critical section */
     if (critical_section_nest_level > 0) {
-        critical_section_nest_level--;
+	critical_section_nest_level--;
     }
 
     /* Enable interrupts globally if now outside critical section */
     if (critical_section_nest_level == 0) {
-        __enable_irq();
+	__enable_irq();
     }
 }
 
@@ -47,7 +47,7 @@ inline static unsigned int arch_interrup
 
     /* If now in outer-most critical section, disable interrupts globally */
     if (critical_section_nest_level == 1) {
-        __disable_irq();
+	__disable_irq();
     }
 
     return 0;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/arm/arm-m/src/arch.ld.S
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/arch/arm/arm-m/src/arch.ld.S
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/arm/arm-m/src/arch.ld.S
@@ -80,20 +80,20 @@ SECTIONS {
      */
 
     .exceptions : {
-        KEEP(*(.exceptions))
+	KEEP(*(.exceptions))
     } > x
 
     .text : {
-        *(.text .text.*)
+	*(.text .text.*)
     } > x
 
 #ifdef __clang__
     .eh_frame : {
-        (.eh_frame)
+	(.eh_frame)
     } > x
 
     .ARM.exidx : {
-        (.ARM.exidx)
+	(.ARM.exidx)
     } > x
 #endif
 
@@ -106,48 +106,48 @@ SECTIONS {
      */
 
     .init : {
-        KEEP(*(.init))
+	KEEP(*(.init))
     } > x
 
     .fini : {
-        KEEP(*(.fini))
+	KEEP(*(.fini))
     } > x
 
     .init_array : {
-        __init_array_start = .;
-        KEEP(*(.init_array*))
-        __init_array_end = .;
+	__init_array_start = .;
+	KEEP(*(.init_array*))
+	__init_array_end = .;
     } > r
 
     .fini_array : {
-        __fini_array_start = .;
-        KEEP(*(.fini_array))
-        __fini_array_end = .;
+	__fini_array_start = .;
+	KEEP(*(.fini_array))
+	__fini_array_end = .;
     } > r
 
     .rodata : {
-        *(.rodata .rodata.*)
+	*(.rodata .rodata.*)
     } > r
 
     .data : {
-        __data_load__ = LOADADDR(.data);
-        __data_start__ = ABSOLUTE(.);
+	__data_load__ = LOADADDR(.data);
+	__data_start__ = ABSOLUTE(.);
 
-        *(.data .data.*)
+	*(.data .data.*)
     } > w AT> r
 
     .bss (NOLOAD) : {
-        __bss_start__ = ABSOLUTE(.);
+	__bss_start__ = ABSOLUTE(.);
 
-        *(.bss .bss.*)
+	*(.bss .bss.*)
     } > w
 
     .stackheap (NOLOAD) : {
-        __stackheap_start__ = ABSOLUTE(.);
+	__stackheap_start__ = ABSOLUTE(.);
 
-        . = ORIGIN(w) + LENGTH(w);
+	. = ORIGIN(w) + LENGTH(w);
 
-        __stackheap_end__ = ABSOLUTE(.);
+	__stackheap_end__ = ABSOLUTE(.);
     } > w
 
     /*
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/arm/arm-m/src/arch.scatter.S
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/arch/arm/arm-m/src/arch.scatter.S
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/arm/arm-m/src/arch.scatter.S
@@ -62,23 +62,23 @@
 
 LR_FIRMWARE FMW_MEM0_BASE {
     ER_EXCEPTIONS ARCH_X_BASE {
-        *(.exceptions)
+	*(.exceptions)
     }
 
     ER_TEXT +0 {
-        *(+CODE)
+	*(+CODE)
     }
 
     ER_RODATA ARCH_R_BASE {
-        *(+CONST)
+	*(+CONST)
     }
 
     ER_DATA ARCH_W_BASE {
-        *(+DATA)
+	*(+DATA)
     }
 
     ER_BSS +0 {
-        *(+BSS)
+	*(+BSS)
     }
 
     ARM_LIB_STACKHEAP +0 EMPTY (ARCH_W_LIMIT - +0) { }
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/arm/arm-m/src/arch_exceptions.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/arch/arm/arm-m/src/arch_exceptions.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/arm/arm-m/src/arch_exceptions.c
@@ -33,7 +33,7 @@ void software_init_hook(void)
     char *end = &__data_end__;
 
     if (load != start) {
-        (void)memcpy(start, load, (size_t)(end - start));
+	(void)memcpy(start, load, (size_t)(end - start));
     }
 }
 #endif
@@ -60,30 +60,30 @@ const struct {
 } arch_exceptions FWK_SECTION(".exceptions") = {
     .stack = (uintptr_t)(arch_exception_stack),
     .exceptions = {
-        [NVIC_USER_IRQ_OFFSET + Reset_IRQn - 1] =
-            (uintptr_t)(arch_exception_reset),
-        [NonMaskableInt_IRQn +  (NVIC_USER_IRQ_OFFSET - 1)] =
-            (uintptr_t)(arch_exception_invalid),
-        [NVIC_USER_IRQ_OFFSET + HardFault_IRQn - 1] =
-            (uintptr_t)(arch_exception_invalid),
-        [NVIC_USER_IRQ_OFFSET + MemoryManagement_IRQn - 1] =
-            (uintptr_t)(arch_exception_invalid),
-        [NVIC_USER_IRQ_OFFSET + BusFault_IRQn - 1] =
-            (uintptr_t)(arch_exception_invalid),
-        [NVIC_USER_IRQ_OFFSET + UsageFault_IRQn - 1] =
-            (uintptr_t)(arch_exception_invalid),
+	[NVIC_USER_IRQ_OFFSET + Reset_IRQn - 1] =
+	    (uintptr_t)(arch_exception_reset),
+	[NonMaskableInt_IRQn +  (NVIC_USER_IRQ_OFFSET - 1)] =
+	    (uintptr_t)(arch_exception_invalid),
+	[NVIC_USER_IRQ_OFFSET + HardFault_IRQn - 1] =
+	    (uintptr_t)(arch_exception_invalid),
+	[NVIC_USER_IRQ_OFFSET + MemoryManagement_IRQn - 1] =
+	    (uintptr_t)(arch_exception_invalid),
+	[NVIC_USER_IRQ_OFFSET + BusFault_IRQn - 1] =
+	    (uintptr_t)(arch_exception_invalid),
+	[NVIC_USER_IRQ_OFFSET + UsageFault_IRQn - 1] =
+	    (uintptr_t)(arch_exception_invalid),
 #ifdef ARMV8M
-        [NVIC_USER_IRQ_OFFSET + SecureFault_IRQn - 1] =
-            (uintptr_t)(arch_exception_invalid),
+	[NVIC_USER_IRQ_OFFSET + SecureFault_IRQn - 1] =
+	    (uintptr_t)(arch_exception_invalid),
 #endif
-        [NVIC_USER_IRQ_OFFSET + DebugMonitor_IRQn - 1] =
-            (uintptr_t)(arch_exception_invalid),
+	[NVIC_USER_IRQ_OFFSET + DebugMonitor_IRQn - 1] =
+	    (uintptr_t)(arch_exception_invalid),
 
-        [NVIC_USER_IRQ_OFFSET + SVCall_IRQn - 1] =
-            (uintptr_t)(arch_exception_invalid),
-        [NVIC_USER_IRQ_OFFSET + PendSV_IRQn - 1] =
-            (uintptr_t)(arch_exception_invalid),
-        [NVIC_USER_IRQ_OFFSET + SysTick_IRQn - 1] =
-            (uintptr_t)(arch_exception_invalid),
+	[NVIC_USER_IRQ_OFFSET + SVCall_IRQn - 1] =
+	    (uintptr_t)(arch_exception_invalid),
+	[NVIC_USER_IRQ_OFFSET + PendSV_IRQn - 1] =
+	    (uintptr_t)(arch_exception_invalid),
+	[NVIC_USER_IRQ_OFFSET + SysTick_IRQn - 1] =
+	    (uintptr_t)(arch_exception_invalid),
     },
 };
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/arm/arm-m/src/arch_handlers.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/arch/arm/arm-m/src/arch_handlers.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/arm/arm-m/src/arch_handlers.c
@@ -44,6 +44,6 @@ noreturn void arch_exception_reset(void)
 noreturn FWK_WEAK void arch_exception_invalid(void)
 {
     while (true) {
-        __WFI();
+	__WFI();
     }
 }
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/arm/arm-m/src/arch_main.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/arch/arm/arm-m/src/arch_main.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/arm/arm-m/src/arch_main.c
@@ -30,7 +30,7 @@ unsigned int critical_section_nest_level
 void _exit(int status)
 {
     while (true) {
-        __WFI();
+	__WFI();
     }
 }
 #endif
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/arm/arm-m/src/arch_nvic.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/arch/arm/arm-m/src/arch_nvic.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/arm/arm-m/src/arch_nvic.c
@@ -85,7 +85,7 @@ static int global_disable(void)
 static int is_enabled(unsigned int interrupt, bool *enabled)
 {
     if (interrupt >= irq_count) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     *enabled = NVIC_GetEnableIRQ((enum IRQn)interrupt) != 0;
@@ -96,7 +96,7 @@ static int is_enabled(unsigned int inter
 static int enable(unsigned int interrupt)
 {
     if (interrupt >= irq_count) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     NVIC_EnableIRQ((enum IRQn)interrupt);
@@ -107,7 +107,7 @@ static int enable(unsigned int interrupt
 static int disable(unsigned int interrupt)
 {
     if (interrupt >= irq_count) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     NVIC_DisableIRQ((enum IRQn)interrupt);
@@ -118,7 +118,7 @@ static int disable(unsigned int interrup
 static int is_pending(unsigned int interrupt, bool *pending)
 {
     if (interrupt >= irq_count) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     *pending = NVIC_GetPendingIRQ((enum IRQn)interrupt) != 0;
@@ -129,7 +129,7 @@ static int is_pending(unsigned int inter
 static int set_pending(unsigned int interrupt)
 {
     if (interrupt >= irq_count) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     NVIC_SetPendingIRQ((enum IRQn)interrupt);
@@ -140,7 +140,7 @@ static int set_pending(unsigned int inte
 static int clear_pending(unsigned int interrupt)
 {
     if (interrupt >= irq_count) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     NVIC_ClearPendingIRQ((enum IRQn)interrupt);
@@ -151,7 +151,7 @@ static int clear_pending(unsigned int in
 static int set_isr_irq(unsigned int interrupt, void (*isr)(void))
 {
     if (interrupt >= irq_count) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     NVIC_SetVector((enum IRQn)interrupt, (uint32_t)isr);
@@ -166,7 +166,7 @@ static int set_isr_irq_param(
 {
     struct irq_callback *entry;
     if (interrupt >= irq_count) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     entry = &callback[NVIC_USER_IRQ_OFFSET + interrupt - 1];
@@ -214,15 +214,15 @@ static int get_current(unsigned int *int
 
     /* Not an interrupt */
     if (*interrupt == 0) {
-        return FWK_E_STATE;
+	return FWK_E_STATE;
     }
 
     if (*interrupt == (NVIC_USER_IRQ_OFFSET + (int)NonMaskableInt_IRQn)) {
-        *interrupt = FWK_INTERRUPT_NMI;
+	*interrupt = FWK_INTERRUPT_NMI;
     } else if (*interrupt < NVIC_USER_IRQ_OFFSET) {
-        *interrupt = FWK_INTERRUPT_EXCEPTION;
+	*interrupt = FWK_INTERRUPT_EXCEPTION;
     } else {
-        *interrupt -= NVIC_USER_IRQ_OFFSET;
+	*interrupt -= NVIC_USER_IRQ_OFFSET;
     }
 
     return FWK_SUCCESS;
@@ -232,7 +232,7 @@ static bool is_interrupt_context(void)
 {
     /* Not an interrupt */
     if (__get_IPSR() == 0) {
-        return false;
+	return false;
     }
 
     return true;
@@ -270,7 +270,7 @@ int arch_nvic_init(const struct fwk_arch
     uint32_t irq;
 
     if (driver == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     /* Find the number of interrupt lines implemented in hardware */
@@ -311,9 +311,9 @@ int arch_nvic_init(const struct fwk_arch
 
     /* Copy the processor exception table over to the new vector table */
     (void)memcpy(
-        vector,
-        (const void *)SCB->VTOR,
-        NVIC_USER_IRQ_OFFSET * sizeof(vector[0]));
+	vector,
+	(const void *)SCB->VTOR,
+	NVIC_USER_IRQ_OFFSET * sizeof(vector[0]));
 
     __DMB();
 
@@ -324,19 +324,19 @@ int arch_nvic_init(const struct fwk_arch
 
     /* Initialize IRQs */
     for (irq = 0; irq < irq_count; irq++) {
-        /* Ensure IRQs are disabled during boot sequence */
-        NVIC_DisableIRQ((IRQn_Type)irq);
-        NVIC_ClearPendingIRQ((IRQn_Type)irq);
+	/* Ensure IRQs are disabled during boot sequence */
+	NVIC_DisableIRQ((IRQn_Type)irq);
+	NVIC_ClearPendingIRQ((IRQn_Type)irq);
 
-        /* Initialize all IRQ entries to point to the irq_invalid() handler */
-        NVIC_SetVector((IRQn_Type)irq, (uint32_t)irq_invalid);
+	/* Initialize all IRQ entries to point to the irq_invalid() handler */
+	NVIC_SetVector((IRQn_Type)irq, (uint32_t)irq_invalid);
     }
 
     __enable_irq();
 
     /* Enable the Usage, Bus and Memory faults which are disabled by default */
     SCB->SHCSR |= SCB_SHCSR_MEMFAULTENA_Msk | SCB_SHCSR_BUSFAULTENA_Msk |
-        SCB_SHCSR_USGFAULTENA_Msk;
+	SCB_SHCSR_USGFAULTENA_Msk;
 
     *driver = &arch_nvic_driver;
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/arm/armv8-a/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/arch/arm/armv8-a/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/arm/armv8-a/CMakeLists.txt
@@ -17,12 +17,12 @@ target_include_directories(
 target_sources(
     arch-armv8a
     PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/arch_cache_helpers.S"
-            "${CMAKE_CURRENT_SOURCE_DIR}/src/arch_crt0.S"
-            "${CMAKE_CURRENT_SOURCE_DIR}/src/arch_exceptions.S"
-            "${CMAKE_CURRENT_SOURCE_DIR}/src/arch_gic.c"
-            "${CMAKE_CURRENT_SOURCE_DIR}/src/arch_main.c"
-            "${CMAKE_CURRENT_SOURCE_DIR}/src/arch_misc_helpers.S"
-            "${CMAKE_CURRENT_SOURCE_DIR}/src/arch_mm.c")
+	    "${CMAKE_CURRENT_SOURCE_DIR}/src/arch_crt0.S"
+	    "${CMAKE_CURRENT_SOURCE_DIR}/src/arch_exceptions.S"
+	    "${CMAKE_CURRENT_SOURCE_DIR}/src/arch_gic.c"
+	    "${CMAKE_CURRENT_SOURCE_DIR}/src/arch_main.c"
+	    "${CMAKE_CURRENT_SOURCE_DIR}/src/arch_misc_helpers.S"
+	    "${CMAKE_CURRENT_SOURCE_DIR}/src/arch_mm.c")
 
 # Force the linker to include any object files containing our implementations of
 # functions that the standard library relies on.
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/arm/armv8-a/include/arch_helpers.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/arch/arm/armv8-a/include/arch_helpers.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/arm/armv8-a/include/arch_helpers.h
@@ -24,15 +24,15 @@
 #define _DEFINE_SYSREG_READ_FUNC(_name, _reg_name) \
     static inline unsigned long read_##_name(void) \
     { \
-        unsigned long v; \
-        __asm__ volatile("mrs %0, " #_reg_name : "=r"(v)); \
-        return v; \
+	unsigned long v; \
+	__asm__ volatile("mrs %0, " #_reg_name : "=r"(v)); \
+	return v; \
     }
 
 #define _DEFINE_SYSREG_WRITE_FUNC(_name, _reg_name) \
     static inline void write_##_name(unsigned long v) \
     { \
-        __asm__ volatile("msr " #_reg_name ", %0" : : "r"(v)); \
+	__asm__ volatile("msr " #_reg_name ", %0" : : "r"(v)); \
     }
 
 #define SYSREG_WRITE_CONST(reg_name, v) \
@@ -67,21 +67,21 @@
 #define DEFINE_SYSOP_FUNC(_op) \
     static inline void _op(void) \
     { \
-        __asm__(#_op); \
+	__asm__(#_op); \
     }
 
 /* Define function for system instruction with type specifier */
 #define DEFINE_SYSOP_TYPE_FUNC(_op, _type) \
     static inline void _op##_type(void) \
     { \
-        __asm__(#_op " " #_type); \
+	__asm__(#_op " " #_type); \
     }
 
 /* Define function for system instruction with register parameter */
 #define DEFINE_SYSOP_TYPE_PARAM_FUNC(_op, _type) \
     static inline void _op##_type(uint64_t v) \
     { \
-        __asm__(#_op " " #_type ", %0" : : "r"(v)); \
+	__asm__(#_op " " #_type ", %0" : : "r"(v)); \
     }
 
 /*******************************************************************************
@@ -94,28 +94,28 @@
  * the workaround for errata 813419 of Cortex-A57.
  */
 #    define DEFINE_TLBIOP_ERRATA_A57_813419_TYPE_FUNC(_type) \
-        static inline void tlbi##_type(void) \
-        { \
-            __asm__("tlbi " #_type \
-                    "\n" \
-                    "dsb ish\n" \
-                    "tlbi " #_type); \
-        }
+	static inline void tlbi##_type(void) \
+	{ \
+	    __asm__("tlbi " #_type \
+		    "\n" \
+		    "dsb ish\n" \
+		    "tlbi " #_type); \
+	}
 
 /*
  * Define function for TLBI instruction with register parameter that implements
  * the workaround for errata 813419 of Cortex-A57.
  */
 #    define DEFINE_TLBIOP_ERRATA_A57_813419_TYPE_PARAM_FUNC(_type) \
-        static inline void tlbi##_type(uint64_t v) \
-        { \
-            __asm__("tlbi " #_type \
-                    ", %0\n" \
-                    "dsb ish\n" \
-                    "tlbi " #_type ", %0" \
-                    : \
-                    : "r"(v)); \
-        }
+	static inline void tlbi##_type(uint64_t v) \
+	{ \
+	    __asm__("tlbi " #_type \
+		    ", %0\n" \
+		    "dsb ish\n" \
+		    "tlbi " #_type ", %0" \
+		    : \
+		    : "r"(v)); \
+	}
 #endif /* ERRATA_A57_813419 */
 
 #if ERRATA_A53_819472 || ERRATA_A53_824069 || ERRATA_A53_827319
@@ -124,10 +124,10 @@
  * the workaround for errata 819472, 824069 and 827319 of Cortex-A53.
  */
 #    define DEFINE_DCOP_ERRATA_A53_TYPE_PARAM_FUNC(_name, _type) \
-        static inline void dc##_name(uint64_t v) \
-        { \
-            __asm__("dc " #_type ", %0" : : "r"(v)); \
-        }
+	static inline void dc##_name(uint64_t v) \
+	{ \
+	    __asm__("dc " #_type ", %0" : : "r"(v)); \
+	}
 #endif /* ERRATA_A53_819472 || ERRATA_A53_824069 || ERRATA_A53_827319 */
 
 DEFINE_SYSOP_TYPE_FUNC(tlbi, alle1)
@@ -518,11 +518,11 @@ static inline unsigned int get_current_e
 static inline uint64_t el_implemented(unsigned int el)
 {
     if (el > 3U) {
-        return EL_IMPL_NONE;
+	return EL_IMPL_NONE;
     } else {
-        unsigned int shift = ID_AA64PFR0_EL1_SHIFT * el;
+	unsigned int shift = ID_AA64PFR0_EL1_SHIFT * el;
 
-        return (read_id_aa64pfr0_el1() >> shift) & ID_AA64PFR0_ELX_MASK;
+	return (read_id_aa64pfr0_el1() >> shift) & ID_AA64PFR0_ELX_MASK;
     }
 }
 
@@ -565,12 +565,12 @@ inline static void arch_interrupts_enabl
 {
     /* Decrement critical_section_nest_level only if in critical section */
     if (critical_section_nest_level > 0) {
-        critical_section_nest_level--;
+	critical_section_nest_level--;
     }
 
     /* Enable interrupts globally if now outside critical section */
     if (critical_section_nest_level == 0) {
-        enable_irq();
+	enable_irq();
     }
 }
 
@@ -586,7 +586,7 @@ inline static unsigned int arch_interrup
 
     /* If now in outer-most critical section, disable interrupts globally */
     if (critical_section_nest_level == 1) {
-        disable_irq();
+	disable_irq();
     }
 
     return 0;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/arm/armv8-a/include/asm_macros.S
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/arch/arm/armv8-a/include/asm_macros.S
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/arm/armv8-a/include/asm_macros.S
@@ -128,13 +128,13 @@
      * other bits.
      */
     .macro _mov_imm16 _reg, _val, _shift
-        .if (\_val >> \_shift) & 0xffff
-            .if (\_val & (1 << \_shift - 1))
-                movk    \_reg, (\_val >> \_shift) & 0xffff, LSL \_shift
-            .else
-                mov \_reg, \_val & (0xffff << \_shift)
-            .endif
-        .endif
+	.if (\_val >> \_shift) & 0xffff
+	    .if (\_val & (1 << \_shift - 1))
+		movk    \_reg, (\_val >> \_shift) & 0xffff, LSL \_shift
+	    .else
+		mov \_reg, \_val & (0xffff << \_shift)
+	    .endif
+	.endif
     .endm
 
     /*
@@ -144,14 +144,14 @@
      * that case
      */
     .macro mov_imm _reg, _val
-        .if (\_val) == 0
-            mov \_reg, #0
-        .else
-            _mov_imm16  \_reg, (\_val), 0
-            _mov_imm16  \_reg, (\_val), 16
-            _mov_imm16  \_reg, (\_val), 32
-            _mov_imm16  \_reg, (\_val), 48
-        .endif
+	.if (\_val) == 0
+	    mov \_reg, #0
+	.else
+	    _mov_imm16  \_reg, (\_val), 0
+	    _mov_imm16  \_reg, (\_val), 16
+	    _mov_imm16  \_reg, (\_val), 32
+	    _mov_imm16  \_reg, (\_val), 48
+	.endif
     .endm
 
     /*
@@ -159,7 +159,7 @@
      * expect a return. To provide the function being jumped to with
      * additional information, we use 'bl' instruction to jump rather than
      * 'b'.
-         *
+	 *
      * Debuggers infer the location of a call from where LR points to, which
      * is usually the instruction after 'bl'. If this macro expansion
      * happens to be the last location in a function, that'll cause the LR
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/arm/armv8-a/include/assert_macros.S
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/arch/arm/armv8-a/include/assert_macros.S
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/arm/armv8-a/include/assert_macros.S
@@ -18,7 +18,7 @@
 .ifndef .L_assert_filename ;\
     .pushsection .rodata.str1.1, "aS" ;\
     .L_assert_filename: ;\
-            .string __FILE__ ;\
+	    .string __FILE__ ;\
     .popsection ;\
 .endif ;\
     b._cc   300f ;\
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/arm/armv8-a/include/lib/utils_def.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/arch/arm/armv8-a/include/lib/utils_def.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/arm/armv8-a/include/lib/utils_def.h
@@ -31,15 +31,15 @@
  */
 #if defined(__LINKER__) || defined(__ASSEMBLY__)
 #    define GENMASK_32(h, l) \
-        (((0xFFFFFFFF) << (l)) & (0xFFFFFFFF >> (32 - 1 - (h))))
+	(((0xFFFFFFFF) << (l)) & (0xFFFFFFFF >> (32 - 1 - (h))))
 
 #    define GENMASK_64(h, l) ((~0 << (l)) & (~0 >> (64 - 1 - (h))))
 #else
 #    define GENMASK_32(h, l) \
-        (((~UINT32_C(0)) << (l)) & (~UINT32_C(0) >> (32 - 1 - (h))))
+	(((~UINT32_C(0)) << (l)) & (~UINT32_C(0) >> (32 - 1 - (h))))
 
 #    define GENMASK_64(h, l) \
-        (((~UINT64_C(0)) << (l)) & (~UINT64_C(0) >> (64 - 1 - (h))))
+	(((~UINT64_C(0)) << (l)) & (~UINT64_C(0) >> (64 - 1 - (h))))
 #endif
 
 #ifdef AARCH32
@@ -56,24 +56,24 @@
 
 #define div_round_up(val, div) \
     __extension__({ \
-        __typeof__(div) _div = (div); \
-        ((val) + _div - (__typeof__(div))1) / _div; \
+	__typeof__(div) _div = (div); \
+	((val) + _div - (__typeof__(div))1) / _div; \
     })
 
 #define MIN(x, y) \
     __extension__({ \
-        __typeof__(x) _x = (x); \
-        __typeof__(y) _y = (y); \
-        (void)(&_x == &_y); \
-        _x < _y ? _x : _y; \
+	__typeof__(x) _x = (x); \
+	__typeof__(y) _y = (y); \
+	(void)(&_x == &_y); \
+	_x < _y ? _x : _y; \
     })
 
 #define MAX(x, y) \
     __extension__({ \
-        __typeof__(x) _x = (x); \
-        __typeof__(y) _y = (y); \
-        (void)(&_x == &_y); \
-        _x > _y ? _x : _y; \
+	__typeof__(x) _x = (x); \
+	__typeof__(y) _y = (y); \
+	(void)(&_x == &_y); \
+	_x > _y ? _x : _y; \
     })
 
 /*
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/arm/armv8-a/src/arch.ld.S
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/arch/arm/armv8-a/src/arch.ld.S
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/arm/armv8-a/src/arch.ld.S
@@ -77,11 +77,11 @@
 
 #if FMW_MEM_MODE != ARCH_MEM_MODE_SINGLE_REGION
     #ifndef FIRMWARE_MEM1_BASE
-        #error "FIRMWARE_MEM1_BASE has not been configured"
+	#error "FIRMWARE_MEM1_BASE has not been configured"
     #endif
 
     #ifndef FIRMWARE_MEM1_SIZE
-        #error "FIRMWARE_MEM1_SIZE has not been configured"
+	#error "FIRMWARE_MEM1_SIZE has not been configured"
     #endif
 #endif
 
@@ -91,26 +91,26 @@
 
 #if FMW_MEM_MODE == ARCH_MEM_MODE_SINGLE_REGION
     ASSERT(FMW_STACK_SIZE < FMW_MEM0_SIZE,
-        "FMW_STACK_SIZE does not fit in MEM0")
+	"FMW_STACK_SIZE does not fit in MEM0")
     #define UNALIGNED_STACK_BASE \
-        (FMW_MEM0_BASE + FMW_MEM0_SIZE - FMW_STACK_SIZE)
+	(FMW_MEM0_BASE + FMW_MEM0_SIZE - FMW_STACK_SIZE)
 #else
     ASSERT(FMW_STACK_SIZE < FIRMWARE_MEM1_SIZE,
-        "FMW_STACK_SIZE does not fit in MEM1")
+	"FMW_STACK_SIZE does not fit in MEM1")
     #define UNALIGNED_STACK_BASE \
-        (FIRMWARE_MEM1_BASE + FIRMWARE_MEM1_SIZE - FMW_STACK_SIZE)
+	(FIRMWARE_MEM1_BASE + FIRMWARE_MEM1_SIZE - FMW_STACK_SIZE)
 #endif
 
 #define STACK_BASE \
     ( \
-        ((UNALIGNED_STACK_BASE + STACK_ALIGNMENT - 1) / STACK_ALIGNMENT) \
-            * STACK_ALIGNMENT \
+	((UNALIGNED_STACK_BASE + STACK_ALIGNMENT - 1) / STACK_ALIGNMENT) \
+	    * STACK_ALIGNMENT \
     )
 
 #define STACK_SIZE \
     (( \
-        ((STACK_BASE + FMW_STACK_SIZE) / STACK_ALIGNMENT) \
-            * STACK_ALIGNMENT \
+	((STACK_BASE + FMW_STACK_SIZE) / STACK_ALIGNMENT) \
+	    * STACK_ALIGNMENT \
     ) - STACK_BASE)
 
 ASSERT(STACK_SIZE > 0, "FMW_STACK_SIZE is too small")
@@ -121,11 +121,11 @@ MEMORY {
 #if FMW_MEM_MODE == ARCH_MEM_MODE_SINGLE_REGION
     /* Only one memory region with read, execute and write attributes */
     mem0 (rxw): ORIGIN = FMW_MEM0_BASE, LENGTH = FMW_MEM0_SIZE - \
-                                                      FMW_STACK_SIZE
+						      FMW_STACK_SIZE
 #else
     mem0 (rx):  ORIGIN = FMW_MEM0_BASE, LENGTH = FMW_MEM0_SIZE
     mem1 (rxw): ORIGIN = FIRMWARE_MEM1_BASE, LENGTH = FIRMWARE_MEM1_SIZE - \
-                                                      FMW_STACK_SIZE
+						      FMW_STACK_SIZE
 #endif
     stack (rw):  ORIGIN = STACK_BASE, LENGTH = STACK_SIZE
     sram (rxw): ORIGIN = ((0xE6302000)), LENGTH = (0x00001000)
@@ -133,21 +133,21 @@ MEMORY {
 
 SECTIONS {
     .text : {
-        *(.vectors)
-        *(.entrypoint)
-        *(.text*)
-        *(.rodata*)
-        *(.note.gnu.build-id)
+	*(.vectors)
+	*(.entrypoint)
+	*(.text*)
+	*(.rodata*)
+	*(.note.gnu.build-id)
     } > mem0
 
     __text__ = .;
 
     __sram_copy_start__ = .;
     .system_ram : {
-        __system_ram_start__ = .;
-        *(.system_ram*)
-        *iic_dvfs.o(.rodata)
-        __system_ram_end__ = .;
+	__system_ram_start__ = .;
+	*(.system_ram*)
+	*iic_dvfs.o(.rodata)
+	__system_ram_end__ = .;
     } > sram AT> mem0
 
     . = __text__ + SIZEOF(.system_ram);
@@ -159,9 +159,9 @@ SECTIONS {
     __RW_START__ = . ;
 
     .data : {
-        . = ALIGN(4);
-        *(.data*)
-        . = ALIGN(4);
+	. = ALIGN(4);
+	*(.data*)
+	. = ALIGN(4);
 #if FMW_MEM_MODE == ARCH_MEM_MODE_SINGLE_REGION
     } > mem0 /* .data follows .text in mem0 */
 #elif FMW_MEM_MODE == ARCH_MEM_MODE_DUAL_REGION_NO_RELOCATION
@@ -173,9 +173,9 @@ SECTIONS {
 #endif
 
     .bss : {
-        . = ALIGN(4);
-        *(.bss*)
-        . = ALIGN(4);
+	. = ALIGN(4);
+	*(.bss*)
+	. = ALIGN(4);
 #if FMW_MEM_MODE == ARCH_MEM_MODE_SINGLE_REGION
     } > mem0  /* Run-time image is at mem1, but loaded from mem0 */
 #else
@@ -183,7 +183,7 @@ SECTIONS {
 #endif
 
     .stack (NOLOAD) : {
-        . = . + STACK_SIZE;
+	. = . + STACK_SIZE;
     } > stack
     __RW_END__ = .;
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/arm/armv8-a/src/arch_gic.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/arch/arm/armv8-a/src/arch_gic.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/arm/armv8-a/src/arch_gic.c
@@ -39,8 +39,8 @@
  */
 struct callback {
     union {
-        void (*func)(uintptr_t param);
-        void (*funcn)(void);
+	void (*func)(uintptr_t param);
+	void (*funcn)(void);
     };
     uintptr_t param;
 };
@@ -63,13 +63,13 @@ static struct r_tree *radix;
 struct r_tree *init_entry(struct r_tree *rt)
 {
     if (NULL == rt) {
-        rt = fwk_mm_calloc(1, sizeof(struct r_tree));
-        if (NULL == rt) {
-            return NULL;
-        }
-        rt->_allocated = 1;
+	rt = fwk_mm_calloc(1, sizeof(struct r_tree));
+	if (NULL == rt) {
+	    return NULL;
+	}
+	rt->_allocated = 1;
     } else {
-        rt->_allocated = 0;
+	rt->_allocated = 0;
     }
     rt->root = NULL;
 
@@ -83,17 +83,17 @@ static void *_lookup(
     int bitno)
 {
     if (NULL == cur) {
-        return NULL != cand ? cand->entry : NULL;
+	return NULL != cand ? cand->entry : NULL;
     }
 
     if (cur->valid) {
-        cand = cur;
+	cand = cur;
     }
 
     if (CHECK_BIT(iid, bitno)) {
-        return _lookup(cur->right, cand, iid, bitno + 1);
+	return _lookup(cur->right, cand, iid, bitno + 1);
     } else {
-        return _lookup(cur->left, cand, iid, bitno + 1);
+	return _lookup(cur->left, cand, iid, bitno + 1);
     }
 }
 
@@ -112,27 +112,27 @@ static int _add(
     struct r_node *new;
 
     if (NULL == *cur) {
-        new = fwk_mm_calloc(1, sizeof(struct r_node));
-        if (NULL == new) {
-            return -1;
-        }
-        memset(new, 0, sizeof(struct r_node));
-        *cur = new;
+	new = fwk_mm_calloc(1, sizeof(struct r_node));
+	if (NULL == new) {
+	    return -1;
+	}
+	memset(new, 0, sizeof(struct r_node));
+	*cur = new;
     }
 
     if (bitsize == bitno) {
-        if ((*cur)->valid) {
-            return -1;
-        }
-        (*cur)->valid = 1;
-        (*cur)->entry = entry;
-        return 0;
+	if ((*cur)->valid) {
+	    return -1;
+	}
+	(*cur)->valid = 1;
+	(*cur)->entry = entry;
+	return 0;
     } else {
-        if (CHECK_BIT(iid, bitno)) {
-            return _add(&(*cur)->right, iid, entry, bitsize, bitno + 1);
-        } else {
-            return _add(&(*cur)->left, iid, entry, bitsize, bitno + 1);
-        }
+	if (CHECK_BIT(iid, bitno)) {
+	    return _add(&(*cur)->right, iid, entry, bitsize, bitno + 1);
+	} else {
+	    return _add(&(*cur)->left, iid, entry, bitsize, bitno + 1);
+	}
     }
 }
 
@@ -149,16 +149,16 @@ void irq_global(uint32_t iid)
 
     entry = (struct callback *)lookup_entry(radix, iid);
     if (entry != NULL) {
-        if (entry->func) {
-            /* Available callback Function */
-            if (entry->param) {
-                entry->func(entry->param);
-            } else {
-                entry->funcn();
-            }
-        }
+	if (entry->func) {
+	    /* Available callback Function */
+	    if (entry->param) {
+		entry->func(entry->param);
+	    } else {
+		entry->funcn();
+	    }
+	}
     } else {
-        /* No interrupt entry */
+	/* No interrupt entry */
     }
     c_interrupt = 0;
 }
@@ -343,14 +343,14 @@ void gic_cpuif_disable(void)
 void gic_init(void)
 {
     gicd_set_ipriorityr(
-        RCAR_GICD_BASE,
-        (unsigned int)VIRTUAL_TIMER_IRQ,
-        GIC_HIGHEST_SEC_PRIORITY);
+	RCAR_GICD_BASE,
+	(unsigned int)VIRTUAL_TIMER_IRQ,
+	GIC_HIGHEST_SEC_PRIORITY);
     gicd_set_isenabler(RCAR_GICD_BASE, (unsigned int)VIRTUAL_TIMER_IRQ);
     gicd_set_ipriorityr(
-        RCAR_GICD_BASE,
-        (unsigned int)NS_PHYSICAL_TIMER_IRQ,
-        GIC_HIGHEST_SEC_PRIORITY);
+	RCAR_GICD_BASE,
+	(unsigned int)NS_PHYSICAL_TIMER_IRQ,
+	GIC_HIGHEST_SEC_PRIORITY);
     gicd_set_isenabler(RCAR_GICD_BASE, (unsigned int)NS_PHYSICAL_TIMER_IRQ);
     gic_cpuif_enable();
 }
@@ -372,7 +372,7 @@ static int global_disable(void)
 static int is_enabled(unsigned int interrupt, bool *enabled)
 {
     if (!IS_SUPPORT_INT(interrupt))
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     *enabled = (bool)gicd_get_isenabler(RCAR_GICD_BASE, interrupt);
 
@@ -382,7 +382,7 @@ static int is_enabled(unsigned int inter
 static int enable(unsigned int interrupt)
 {
     if (!IS_SUPPORT_INT(interrupt))
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     gicd_set_isenabler(RCAR_GICD_BASE, interrupt);
 
@@ -392,7 +392,7 @@ static int enable(unsigned int interrupt
 static int disable(unsigned int interrupt)
 {
     if (!IS_SUPPORT_INT(interrupt))
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     gicd_set_icenabler(RCAR_GICD_BASE, interrupt);
 
@@ -404,12 +404,12 @@ static int is_pending(unsigned int inter
     unsigned int bit;
 
     if (!IS_SUPPORT_INT(interrupt))
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     bit = interrupt % 32;
     *pending =
-        ((gicd_read_ispendr(RCAR_GICD_BASE, interrupt) & (1 << bit)) ? true :
-                                                                       false);
+	((gicd_read_ispendr(RCAR_GICD_BASE, interrupt) & (1 << bit)) ? true :
+								       false);
 
     return FWK_SUCCESS;
 }
@@ -419,7 +419,7 @@ static int set_pending(unsigned int inte
     unsigned int bit;
 
     if (!IS_SUPPORT_INT(interrupt))
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     bit = interrupt % 32;
     gicd_write_ispendr(RCAR_GICD_BASE, interrupt, 1U << bit);
@@ -432,7 +432,7 @@ static int clear_pending(unsigned int in
     unsigned int bit;
 
     if (!IS_SUPPORT_INT(interrupt))
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     bit = interrupt % 32;
     gicd_write_icpendr(RCAR_GICD_BASE, interrupt, 1U << bit);
@@ -490,17 +490,17 @@ static int set_isr_irq(unsigned int inte
     int ret;
 
     if ((MIN_IRQ > interrupt) || (MAX_IRQ <= interrupt))
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     entry = fwk_mm_calloc(1, sizeof(struct callback));
     if (NULL == entry)
-        return FWK_E_PANIC;
+	return FWK_E_PANIC;
 
     entry->funcn = isr;
     entry->param = (uintptr_t)NULL;
     ret = add_entry(radix, interrupt, (void *)entry, IID_LEN);
     if (ret)
-        return FWK_E_PANIC;
+	return FWK_E_PANIC;
 
     return FWK_SUCCESS;
 }
@@ -514,17 +514,17 @@ static int set_isr_irq_param(
     int ret;
 
     if ((MIN_IRQ > interrupt) || (MAX_IRQ <= interrupt))
-        return FWK_E_PANIC;
+	return FWK_E_PANIC;
 
     entry = fwk_mm_calloc(1, sizeof(struct callback));
     if (NULL == entry)
-        return FWK_E_PANIC;
+	return FWK_E_PANIC;
 
     entry->func = isr;
     entry->param = parameter;
     ret = add_entry(radix, interrupt, (void *)entry, IID_LEN);
     if (ret)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     return FWK_SUCCESS;
 }
@@ -547,7 +547,7 @@ static int get_current(unsigned int *int
 
     /* Not an interrupt */
     if (0 == *interrupt)
-        return FWK_E_STATE;
+	return FWK_E_STATE;
 
     return FWK_SUCCESS;
 }
@@ -556,7 +556,7 @@ static bool is_interrupt_context(void)
 {
     /* Not an interrupt */
     if (c_interrupt == 0) {
-        return false;
+	return false;
     }
 
     return true;
@@ -588,7 +588,7 @@ int arm_gic_init(const struct fwk_arch_i
      */
     radix = init_entry(NULL);
     if (radix == NULL)
-        return FWK_E_NOMEM;
+	return FWK_E_NOMEM;
 
     gic_init();
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/arm/armv8-a/src/arch_libc.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/arch/arm/armv8-a/src/arch_libc.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/arm/armv8-a/src/arch_libc.c
@@ -18,7 +18,7 @@ void *memset(void *s, int c, size_t coun
 {
     char *xs = s;
     while (count--)
-        *xs++ = (char)c;
+	*xs++ = (char)c;
     return s;
 }
 
@@ -29,7 +29,7 @@ void *memcpy(void *dst, const void *src,
     char *q = dst;
 
     while (n--) {
-        *q++ = *p++;
+	*q++ = *p++;
     }
 
     return dst;
@@ -40,9 +40,9 @@ char *strncpy(char *dest, const char *sr
     size_t i;
 
     for (i = 0; i < n && src[i] != 0; i++)
-        dest[i] = src[i];
+	dest[i] = src[i];
     for (; i < n; i++)
-        dest[i] = '\0';
+	dest[i] = '\0';
 
     return dest;
 }
@@ -50,9 +50,9 @@ char *strncpy(char *dest, const char *sr
 char *strchr(const char *str, int c)
 {
     do {
-        if (*str == (char)c)
-            return (char *)str;
-        str++;
+	if (*str == (char)c)
+	    return (char *)str;
+	str++;
     } while (*str);
 
     return NULL;
@@ -63,7 +63,7 @@ size_t strlen(const char *str)
     char *tmp = (char *)str;
     size_t counter = 0;
     while (*tmp++)
-        ++counter;
+	++counter;
     return counter;
 }
 
@@ -75,20 +75,20 @@ static void uint_to_str(unsigned int i,
     char const *digit;
 
     if (base == 10)
-        digit = digit_10;
+	digit = digit_10;
     else
-        digit = digit_16;
+	digit = digit_16;
 
     do {
-        ++buf;
-        shifter = shifter / base;
+	++buf;
+	shifter = shifter / base;
     } while (shifter);
 
     *buf = '\0';
 
     do {
-        *--buf = digit[i % base];
-        i = i / base;
+	*--buf = digit[i % base];
+	i = i / base;
     } while (i);
 }
 
@@ -97,14 +97,14 @@ static void int_to_str(int i, char *buf,
     int sign = i;
 
     if (i < 0) {
-        i = -i;
-        buf++;
+	i = -i;
+	buf++;
     }
 
     uint_to_str((unsigned int)i, buf, base);
 
     if (sign < 0)
-        *--buf = '-';
+	*--buf = '-';
 }
 
 static int isdigit(char c)
@@ -119,20 +119,20 @@ static int handle_num(char type, char *b
 
     switch (type) {
     case 'u':
-        uint_num = va_arg(*args, unsigned int);
-        uint_to_str(uint_num, buf, 10);
-        break;
+	uint_num = va_arg(*args, unsigned int);
+	uint_to_str(uint_num, buf, 10);
+	break;
     case 'd':
-        int_num = va_arg(*args, int);
-        int_to_str(int_num, buf, 10);
-        break;
+	int_num = va_arg(*args, int);
+	int_to_str(int_num, buf, 10);
+	break;
     case 'x':
-        uint_num = va_arg(*args, unsigned int);
-        uint_to_str(uint_num, buf, 16);
-        break;
+	uint_num = va_arg(*args, unsigned int);
+	uint_to_str(uint_num, buf, 16);
+	break;
     default:
-        return 1;
-        break;
+	return 1;
+	break;
     }
 
     return 0;
@@ -149,110 +149,110 @@ int vsnprintf(char *str, size_t n, const
     int not_implemented;
 
     for (pos = (char *)format; *pos != '\0'; pos++) {
-        while ((*pos != '%') && (*pos != '\0') && (length < n)) {
-            *tmp++ = *pos++;
-            length++;
-        }
-
-        if (length == n)
-            break;
-
-        if (*pos == '\0') {
-            *tmp = '\0';
-            break;
-        }
-
-        pos++;
-
-        not_implemented = 0;
-
-        switch (*pos) {
-        case 's':
-            s = va_arg(args, char *);
-            strncpy(tmp, s, n - length);
-            break;
-        case '0':
-            if (isdigit(*(pos + 1)) && (*(pos + 1) > '0')) {
-                pos++;
-            } else {
-                not_implemented = 1;
-                break;
-            }
-        case '1':
-        case '2':
-        case '3':
-        case '4':
-        case '5':
-        case '6':
-        case '7':
-        case '8':
-        case '9':
-            min_length = (int)(*pos - '0');
-
-            if (handle_num(*(pos + 1), num_buf, &args)) {
-                if (*(pos - 1) == '0')
-                    pos--;
-
-                not_implemented = 1;
-                break;
-            }
-
-            num_length = (int)strlen(num_buf);
-
-            if (num_length < min_length) {
-                while (num_length >= 0)
-                    num_buf[min_length--] = num_buf[num_length--];
-
-                if (*(pos - 1) == '0') {
-                    if (num_buf[0] == '-') {
-                        min_length++;
-                        while (min_length > 0)
-                            num_buf[min_length--] = '0';
-                    } else {
-                        while (min_length >= 0)
-                            num_buf[min_length--] = '0';
-                    }
-                } else {
-                    while (min_length >= 0)
-                        num_buf[min_length--] = ' ';
-                }
-            }
-            strncpy(tmp, num_buf, n - length);
-            pos++;
-            break;
-        default:
-            if (handle_num(*pos, num_buf, &args))
-                not_implemented = 1;
-            else
-                strncpy(tmp, num_buf, n - length);
-            break;
-        }
-
-        if (not_implemented) {
-            va_arg(args, unsigned int);
-            *tmp++ = '%';
-            length++;
-            pos--;
-        } else {
-            while ((*tmp != '\0') && (length < n)) {
-                tmp++;
-                length++;
-            }
-        }
+	while ((*pos != '%') && (*pos != '\0') && (length < n)) {
+	    *tmp++ = *pos++;
+	    length++;
+	}
+
+	if (length == n)
+	    break;
+
+	if (*pos == '\0') {
+	    *tmp = '\0';
+	    break;
+	}
+
+	pos++;
+
+	not_implemented = 0;
+
+	switch (*pos) {
+	case 's':
+	    s = va_arg(args, char *);
+	    strncpy(tmp, s, n - length);
+	    break;
+	case '0':
+	    if (isdigit(*(pos + 1)) && (*(pos + 1) > '0')) {
+		pos++;
+	    } else {
+		not_implemented = 1;
+		break;
+	    }
+	case '1':
+	case '2':
+	case '3':
+	case '4':
+	case '5':
+	case '6':
+	case '7':
+	case '8':
+	case '9':
+	    min_length = (int)(*pos - '0');
+
+	    if (handle_num(*(pos + 1), num_buf, &args)) {
+		if (*(pos - 1) == '0')
+		    pos--;
+
+		not_implemented = 1;
+		break;
+	    }
+
+	    num_length = (int)strlen(num_buf);
+
+	    if (num_length < min_length) {
+		while (num_length >= 0)
+		    num_buf[min_length--] = num_buf[num_length--];
+
+		if (*(pos - 1) == '0') {
+		    if (num_buf[0] == '-') {
+			min_length++;
+			while (min_length > 0)
+			    num_buf[min_length--] = '0';
+		    } else {
+			while (min_length >= 0)
+			    num_buf[min_length--] = '0';
+		    }
+		} else {
+		    while (min_length >= 0)
+			num_buf[min_length--] = ' ';
+		}
+	    }
+	    strncpy(tmp, num_buf, n - length);
+	    pos++;
+	    break;
+	default:
+	    if (handle_num(*pos, num_buf, &args))
+		not_implemented = 1;
+	    else
+		strncpy(tmp, num_buf, n - length);
+	    break;
+	}
+
+	if (not_implemented) {
+	    va_arg(args, unsigned int);
+	    *tmp++ = '%';
+	    length++;
+	    pos--;
+	} else {
+	    while ((*tmp != '\0') && (length < n)) {
+		tmp++;
+		length++;
+	    }
+	}
     }
 
     if (tmp == str) {
-        *tmp = '\0';
+	*tmp = '\0';
     } else if (length == n) {
-        tmp--;
-        if (*tmp != '\0')
-            *tmp = '\0';
-        else
-            length--;
+	tmp--;
+	if (*tmp != '\0')
+	    *tmp = '\0';
+	else
+	    length--;
     } else if (*(tmp - 1) != '\0') {
-        *tmp = '\0';
+	*tmp = '\0';
     } else {
-        length--;
+	length--;
     }
 
     return (int)length;
@@ -275,5 +275,5 @@ void __assert_fail(
     const char *function)
 {
     while (1)
-        continue;
+	continue;
 }
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/arm/armv8-a/src/arch_main.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/arch/arm/armv8-a/src/arch_main.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/arm/armv8-a/src/arch_main.c
@@ -34,7 +34,7 @@ FWK_WEAK int _platform_init(void *params
 void panic(void)
 {
     while (true)
-        wfi();
+	wfi();
 }
 
 static struct fwk_arch_init_driver arch_init_driver = {
@@ -54,5 +54,5 @@ void arm_main(void)
 
     status = fwk_arch_init(&arch_init_driver);
     if (status != FWK_SUCCESS)
-        panic();
+	panic();
 }
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/arm/armv8-a/src/arch_mm.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/arch/arm/armv8-a/src/arch_mm.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/arm/armv8-a/src/arch_mm.c
@@ -35,18 +35,18 @@ static struct arch_mm_ctx {
 void *_sbrk(intptr_t increment)
 {
     if (increment == 0) {
-        return (void *)arch_mm_ctx.heap_break;
+	return (void *)arch_mm_ctx.heap_break;
     } else {
-        uintptr_t heap_old = FWK_ALIGN_NEXT(arch_mm_ctx.heap_break, 16);
-        uintptr_t heap_new = heap_old + increment;
+	uintptr_t heap_old = FWK_ALIGN_NEXT(arch_mm_ctx.heap_break, 16);
+	uintptr_t heap_new = heap_old + increment;
 
-        if (heap_new > arch_mm_ctx.heap_end) {
-            return (void *)-1;
-        } else {
-            arch_mm_ctx.heap_break = heap_new;
+	if (heap_new > arch_mm_ctx.heap_end) {
+	    return (void *)-1;
+	} else {
+	    arch_mm_ctx.heap_break = heap_new;
 
-            return (void *)heap_old;
-        }
+	    return (void *)heap_old;
+	}
     }
 }
 
@@ -56,7 +56,7 @@ void *malloc(size_t size)
     void *mem = _sbrk((intptr_t)size);
 
     if (mem == ((void *)-1))
-        mem = NULL;
+	mem = NULL;
 
     return mem;
 }
@@ -66,7 +66,7 @@ void *calloc(size_t nmemb, size_t size)
     void *mem = malloc(nmemb * size);
 
     if (mem)
-        memset(mem, 0, nmemb * size);
+	memset(mem, 0, nmemb * size);
 
     return mem;
 }
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/arm/src/arch_mm.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/arch/arm/src/arch_mm.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/arm/src/arch_mm.c
@@ -39,26 +39,26 @@ static struct arch_mm_ctx_str {
 int posix_memalign(void **memptr, size_t alignment, size_t size)
 {
     if (alignment == 0) {
-        return EINVAL;
+	return EINVAL;
     }
 
     /* Enforce power-of-two alignment */
     if ((alignment & (alignment - 1)) != 0) {
-        return EINVAL;
+	return EINVAL;
     }
 
     if ((alignment % sizeof(void *)) != 0) {
-        return EINVAL;
+	return EINVAL;
     }
 
     if (size == 0) {
-        *memptr = NULL;
+	*memptr = NULL;
     } else {
-        *memptr = _memalign_r(_REENT, alignment, size);
+	*memptr = _memalign_r(_REENT, alignment, size);
 
-        if (*memptr == NULL) {
-            return ENOMEM;
-        }
+	if (*memptr == NULL) {
+	    return ENOMEM;
+	}
     }
 
     return 0;
@@ -67,20 +67,20 @@ int posix_memalign(void **memptr, size_t
 void *_sbrk(intptr_t increment)
 {
     if (increment == 0) {
-        return (void *)arch_mm_ctx.heap_break;
+	return (void *)arch_mm_ctx.heap_break;
     } else {
-        uintptr_t heap_old = arch_mm_ctx.heap_break;
-        uintptr_t heap_new = arch_mm_ctx.heap_break + increment;
+	uintptr_t heap_old = arch_mm_ctx.heap_break;
+	uintptr_t heap_new = arch_mm_ctx.heap_break + increment;
 
-        if (heap_new > ((uintptr_t)&__stackheap_end__)) {
-            errno = ENOMEM;
+	if (heap_new > ((uintptr_t)&__stackheap_end__)) {
+	    errno = ENOMEM;
 
-            return (void *)-1;
-        } else {
-            arch_mm_ctx.heap_break = heap_new;
+	    return (void *)-1;
+	} else {
+	    arch_mm_ctx.heap_break = heap_new;
 
-            return (void *)heap_old;
-        }
+	    return (void *)heap_old;
+	}
     }
 }
 #endif
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/none/host/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/arch/none/host/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/none/host/CMakeLists.txt
@@ -8,8 +8,8 @@
 add_library(arch-none)
 
 target_include_directories(arch-none
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(
     arch-none PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/arch_interrupt.c"
-                      "${CMAKE_CURRENT_SOURCE_DIR}/src/arch_main.c")
+		      "${CMAKE_CURRENT_SOURCE_DIR}/src/arch_main.c")
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/none/host/src/arch_interrupt.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/arch/none/host/src/arch_interrupt.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/none/host/src/arch_interrupt.c
@@ -113,7 +113,7 @@ static const struct fwk_arch_interrupt_d
 int arch_interrupt_init(const struct fwk_arch_interrupt_driver **_driver)
 {
     if (_driver == NULL)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     *_driver = &driver;
     return FWK_SUCCESS;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/none/host/src/arch_main.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/arch/none/host/src/arch_main.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/none/host/src/arch_main.c
@@ -33,5 +33,5 @@ int main(void)
 
     status = fwk_arch_init(&arch_init_driver);
     if (status != FWK_SUCCESS)
-        panic();
+	panic();
 }
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/none/optee/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/arch/none/optee/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/none/optee/CMakeLists.txt
@@ -8,25 +8,25 @@
 add_library(arch-optee)
 
 if(NOT CFG_NUM_THREADS)
-        message(FATAL_ERROR
-            "Insufficient firmware metadata provided.\n"
-            "For optee arch, you must set CFG_NUM_THREADS")
+	message(FATAL_ERROR
+	    "Insufficient firmware metadata provided.\n"
+	    "For optee arch, you must set CFG_NUM_THREADS")
 endif()
 
 if(NOT SCP_OPTEE_DIR)
-        message(FATAL_ERROR
-            "Insufficient firmware metadata provided.\n"
-            "For optee arch, you must set SCP_OPTEE_DIR")
+	message(FATAL_ERROR
+	    "Insufficient firmware metadata provided.\n"
+	    "For optee arch, you must set SCP_OPTEE_DIR")
 endif()
 
 target_compile_definitions(arch-optee PUBLIC -DCFG_NUM_THREADS=${CFG_NUM_THREADS})
 
 target_include_directories(arch-optee
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_link_libraries(arch-optee
-                      PUBLIC module-optee-mbx)
+		      PUBLIC module-optee-mbx)
 
 target_sources(
     arch-optee PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/arch_interrupt.c"
-                       "${CMAKE_CURRENT_SOURCE_DIR}/src/arch_main.c")
+		       "${CMAKE_CURRENT_SOURCE_DIR}/src/arch_main.c")
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/none/optee/doc/arch_optee_design.md
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/arch/none/optee/doc/arch_optee_design.md
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/none/optee/doc/arch_optee_design.md
@@ -23,25 +23,25 @@ of a SCMI server as a OP-TEE Pseudo Trus
 included in the OP-TEE os.
 
 ```
-                   +----------------------------------------------------------------+
-                   | +---------------------+    +--------------------------------+  |
-                   | | OPTEE/shared memory |--->| SCMI                           |  |
-                   | +---------------------+    +--------------------------------+  |
-                   |            ^                      |                  |         |
-                   |            |                      v                  v         |
-                   | +---------------------+    +-------------+    +--------------+ |
-                   | | OPTEE/Mailbox       |    | SCMI clock  |    | SCMI reset   | |
-                   | +---------------------+    +-------------+    +--------------+ |
-                   |            ^                      |                  |         |
-                   |            | SCP-Firmware         v                  v         |
-                   |            |               +-------------+    +--------------+ |
-                   |            |               | clock       |    | reset domain | |
-                   |            |               +-------------+    +--------------+ |
-                   |            |                      |                  |         |
-                   |            |                      v                  v         |
-                   | +---------------------+    +-------------+    +--------------+ |
-                   | |  optee architecture |    | OPTEE/clock |    | OPTEE/reset  | |
-                   | +---------------------+    +-------------+    +--------------+ |
+		   +----------------------------------------------------------------+
+		   | +---------------------+    +--------------------------------+  |
+		   | | OPTEE/shared memory |--->| SCMI                           |  |
+		   | +---------------------+    +--------------------------------+  |
+		   |            ^                      |                  |         |
+		   |            |                      v                  v         |
+		   | +---------------------+    +-------------+    +--------------+ |
+		   | | OPTEE/Mailbox       |    | SCMI clock  |    | SCMI reset   | |
+		   | +---------------------+    +-------------+    +--------------+ |
+		   |            ^                      |                  |         |
+		   |            | SCP-Firmware         v                  v         |
+		   |            |               +-------------+    +--------------+ |
+		   |            |               | clock       |    | reset domain | |
+		   |            |               +-------------+    +--------------+ |
+		   |            |                      |                  |         |
+		   |            |                      v                  v         |
+		   | +---------------------+    +-------------+    +--------------+ |
+		   | |  optee architecture |    | OPTEE/clock |    | OPTEE/reset  | |
+		   | +---------------------+    +-------------+    +--------------+ |
 +---------------+  +------------|----------------------|------------------|---------+
 |  Linux kernel |               |                      |                  |
 |               |  +------------|----------------------v------------------v---------+
@@ -54,7 +54,7 @@ included in the OP-TEE os.
 | | OPTEE fwk | |  |            |                          OPTEE os                 |
 | +-----------+ |  |            |                                                   |
 +-------|-------+  +------------|---------------------------------------------------+
-        v                       |
+	v                       |
       +---------------------------+
       | SMC   EL3                 |
       +---------------------------+
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/none/optee/include/arch_main.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/arch/none/optee/include/arch_main.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/none/optee/include/arch_main.h
@@ -43,6 +43,6 @@ extern int scmi_get_device(unsigned int
 extern void scmi_process_mbx_smt(unsigned int id);
 
 extern void scmi_process_mbx_msg(unsigned int id, void *in_buf, size_t in_size,
-                                 void *out_buf, size_t *out_size);
+				 void *out_buf, size_t *out_size);
 
 #endif /* ARCH_MAIN_H */
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/none/optee/src/arch_interrupt.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/arch/none/optee/src/arch_interrupt.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/none/optee/src/arch_interrupt.c
@@ -115,7 +115,7 @@ static const struct fwk_arch_interrupt_d
 int arch_interrupt_init(const struct fwk_arch_interrupt_driver **_driver)
 {
     if (_driver == NULL)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     *_driver = &driver;
     return FWK_SUCCESS;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/none/optee/src/arch_main.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/arch/none/optee/src/arch_main.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/arch/none/optee/src/arch_main.c
@@ -56,7 +56,7 @@ int scmi_get_device(unsigned int id)
     device_id = optee_mbx_get_device(id);
 
     if (fwk_id_is_type(device_id, FWK_ID_TYPE_NONE)) {
-        return -1;
+	return -1;
     }
 
     return (int)device_id.value;
@@ -78,7 +78,7 @@ void scmi_process_mbx_smt(unsigned int f
 }
 
 void scmi_process_mbx_msg(unsigned int fwk_id, void *in_buf, size_t in_size,
-                          void *out_buf, size_t *out_size)
+			  void *out_buf, size_t *out_size)
 {
 #ifdef BUILD_HAS_MOD_MSG_SMT
     fwk_id_t device_id;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/cmake/FindCMakeFormat.cmake
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/cmake/FindCMakeFormat.cmake
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/cmake/FindCMakeFormat.cmake
@@ -64,8 +64,8 @@ if(CMakeFormat_Format_EXECUTABLE)
     add_executable(CMakeFormat::Format IMPORTED)
 
     set_target_properties(
-        CMakeFormat::Format
-        PROPERTIES IMPORTED_LOCATION "${CMakeFormat_Format_EXECUTABLE}")
+	CMakeFormat::Format
+	PROPERTIES IMPORTED_LOCATION "${CMakeFormat_Format_EXECUTABLE}")
 endif()
 
 if(CMakeFormat_Lint_EXECUTABLE)
@@ -76,8 +76,8 @@ if(CMakeFormat_Lint_EXECUTABLE)
     add_executable(CMakeFormat::Lint IMPORTED)
 
     set_target_properties(
-        CMakeFormat::Lint
-        PROPERTIES IMPORTED_LOCATION "${CMakeFormat_Lint_EXECUTABLE}")
+	CMakeFormat::Lint
+	PROPERTIES IMPORTED_LOCATION "${CMakeFormat_Lint_EXECUTABLE}")
 endif()
 
 include(FindPackageHandleStandardArgs)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/cmake/FindClang.cmake
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/cmake/FindClang.cmake
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/cmake/FindClang.cmake
@@ -57,34 +57,34 @@ Result Variables
 find_program(
     Clang_Format_EXECUTABLE
     NAMES "clang-format-11"
-          "clang-format-10"
-          "clang-format-9"
-          "clang-format-8"
-          "clang-format-7"
-          "clang-format-6.0"
-          "clang-format-5.0"
-          "clang-format-4.0"
-          "clang-format-3.9"
-          "clang-format-3.8"
-          "clang-format-3.7"
-          "clang-format-3.6"
-          "clang-format")
+	  "clang-format-10"
+	  "clang-format-9"
+	  "clang-format-8"
+	  "clang-format-7"
+	  "clang-format-6.0"
+	  "clang-format-5.0"
+	  "clang-format-4.0"
+	  "clang-format-3.9"
+	  "clang-format-3.8"
+	  "clang-format-3.7"
+	  "clang-format-3.6"
+	  "clang-format")
 
 find_program(
     Clang_FormatGit_EXECUTABLE
     NAMES "git-clang-format-11"
-          "git-clang-format-10"
-          "git-clang-format-9"
-          "git-clang-format-8"
-          "git-clang-format-7"
-          "git-clang-format-6.0"
-          "git-clang-format-5.0"
-          "git-clang-format-4.0"
-          "git-clang-format-3.9"
-          "git-clang-format-3.8"
-          "git-clang-format-3.7"
-          "git-clang-format-3.6"
-          "git-clang-format")
+	  "git-clang-format-10"
+	  "git-clang-format-9"
+	  "git-clang-format-8"
+	  "git-clang-format-7"
+	  "git-clang-format-6.0"
+	  "git-clang-format-5.0"
+	  "git-clang-format-4.0"
+	  "git-clang-format-3.9"
+	  "git-clang-format-3.8"
+	  "git-clang-format-3.7"
+	  "git-clang-format-3.6"
+	  "git-clang-format")
 
 if(Clang_Format_EXECUTABLE)
     set(Clang_Format_FOUND TRUE)
@@ -94,7 +94,7 @@ if(Clang_Format_EXECUTABLE)
     add_executable(Clang::Format IMPORTED)
 
     set_target_properties(
-        Clang::Format PROPERTIES IMPORTED_LOCATION "${Clang_Format_EXECUTABLE}")
+	Clang::Format PROPERTIES IMPORTED_LOCATION "${Clang_Format_EXECUTABLE}")
 endif()
 
 if(Clang_FormatGit_EXECUTABLE)
@@ -105,8 +105,8 @@ if(Clang_FormatGit_EXECUTABLE)
     add_executable(Clang::FormatGit IMPORTED)
 
     set_target_properties(
-        Clang::FormatGit
-        PROPERTIES IMPORTED_LOCATION "${Clang_FormatGit_EXECUTABLE}")
+	Clang::FormatGit
+	PROPERTIES IMPORTED_LOCATION "${Clang_FormatGit_EXECUTABLE}")
 endif()
 
 include(FindPackageHandleStandardArgs)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/cmake/FindMarkdownlint.cmake
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/cmake/FindMarkdownlint.cmake
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/cmake/FindMarkdownlint.cmake
@@ -43,10 +43,10 @@ if(Markdownlint_EXECUTABLE)
     add_executable(Markdownlint IMPORTED)
 
     set_target_properties(
-        Markdownlint PROPERTIES IMPORTED_LOCATION "${Markdownlint_EXECUTABLE}")
+	Markdownlint PROPERTIES IMPORTED_LOCATION "${Markdownlint_EXECUTABLE}")
 endif()
 
 include(FindPackageHandleStandardArgs)
 
 find_package_handle_standard_args(Markdownlint DEFAULT_MSG
-                                  Markdownlint_EXECUTABLE)
+				  Markdownlint_EXECUTABLE)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/cmake/FindYamllint.cmake
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/cmake/FindYamllint.cmake
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/cmake/FindYamllint.cmake
@@ -43,7 +43,7 @@ if(Yamllint_EXECUTABLE)
     add_executable(Yamllint IMPORTED)
 
     set_target_properties(Yamllint
-                          PROPERTIES IMPORTED_LOCATION "${Yamllint_EXECUTABLE}")
+			  PROPERTIES IMPORTED_LOCATION "${Yamllint_EXECUTABLE}")
 endif()
 
 include(FindPackageHandleStandardArgs)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/cmake/SCPModuleTrace.cmake
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/cmake/SCPModuleTrace.cmake
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/cmake/SCPModuleTrace.cmake
@@ -26,6 +26,6 @@ macro(scp_module_trace module_name)
 
     # Checks if the enable flag is set.
     if(${MOD_TRACE_ENABLE_FLAG})
-        add_compile_definitions(PUBLIC FWK_TRACE_ENABLE)
+	add_compile_definitions(PUBLIC FWK_TRACE_ENABLE)
     endif()
 endmacro()
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/cmake/SCPPreprocessSource.cmake
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/cmake/SCPPreprocessSource.cmake
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/cmake/SCPPreprocessSource.cmake
@@ -62,7 +62,7 @@ macro(scp_preprocess_source target sourc
     # cmake-format: off
 
     separate_arguments(cpp_FLAGS
-        UNIX_COMMAND "${CMAKE_C_FLAGS} ${CMAKE_C_FLAGS_${config}} -P")
+	UNIX_COMMAND "${CMAKE_C_FLAGS} ${CMAKE_C_FLAGS_${config}} -P")
 
     list(APPEND cpp_FLAGS "$<TARGET_PROPERTY:${target},COMPILE_OPTIONS>")
     list(APPEND cpp_DEFINES "$<TARGET_PROPERTY:${target},COMPILE_DEFINITIONS>")
@@ -77,16 +77,16 @@ macro(scp_preprocess_source target sourc
     string(REGEX MATCH "\\\${[^}]*}" match "${command}")
 
     while(match)
-        string(REGEX REPLACE "\\\${(.*)}" "\\1" variable "${match}")
-        string(REPLACE "\${${variable}}" "${${variable}}" command "${command}")
-        string(REGEX MATCH "\\\${[^}]*}" match "${command}")
+	string(REGEX REPLACE "\\\${(.*)}" "\\1" variable "${match}")
+	string(REPLACE "\${${variable}}" "${${variable}}" command "${command}")
+	string(REGEX MATCH "\\\${[^}]*}" match "${command}")
     endwhile()
 
     add_custom_command(
-        OUTPUT ${output}
-        MAIN_DEPENDENCY ${source}
-        COMMAND "${command}"
-        VERBATIM COMMAND_EXPAND_LISTS)
+	OUTPUT ${output}
+	MAIN_DEPENDENCY ${source}
+	COMMAND "${command}"
+	VERBATIM COMMAND_EXPAND_LISTS)
 
     add_custom_target(${target} DEPENDS ${output})
 endmacro()
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/cmake/SCPTargetLinkerScript.cmake
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/cmake/SCPTargetLinkerScript.cmake
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/cmake/SCPTargetLinkerScript.cmake
@@ -28,13 +28,13 @@ macro(scp_target_linker_script target so
     scp_preprocess_source(${subtarget} "${source}" "${output}")
 
     set_target_properties(
-        ${subtarget}
-        PROPERTIES
-            INCLUDE_DIRECTORIES
-                "$<TARGET_PROPERTY:${target},INCLUDE_DIRECTORIES>"
-            COMPILE_DEFINITIONS
-                "$<TARGET_PROPERTY:${target},COMPILE_DEFINITIONS>"
-            COMPILE_OPTIONS "$<TARGET_PROPERTY:${target},COMPILE_OPTIONS>")
+	${subtarget}
+	PROPERTIES
+	    INCLUDE_DIRECTORIES
+		"$<TARGET_PROPERTY:${target},INCLUDE_DIRECTORIES>"
+	    COMPILE_DEFINITIONS
+		"$<TARGET_PROPERTY:${target},COMPILE_DEFINITIONS>"
+	    COMPILE_OPTIONS "$<TARGET_PROPERTY:${target},COMPILE_OPTIONS>")
 
     #
     # Add the linker script to the dependencies of the target.
@@ -43,12 +43,12 @@ macro(scp_target_linker_script target so
     add_dependencies(${target} ${subtarget})
 
     set_target_properties(${target}
-                          PROPERTIES INTERFACE_LINK_DEPENDS "${output}")
+			  PROPERTIES INTERFACE_LINK_DEPENDS "${output}")
 
     if(CMAKE_C_COMPILER_ID STREQUAL "ARMClang")
-        target_link_options(${target} PUBLIC "LINKER:--scatter"
-                                             "LINKER:${output}")
+	target_link_options(${target} PUBLIC "LINKER:--scatter"
+					     "LINKER:${output}")
     else()
-        target_link_options(${target} PUBLIC "LINKER:-T" "LINKER:${output}")
+	target_link_options(${target} PUBLIC "LINKER:-T" "LINKER:${output}")
     endif()
 endmacro()
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/cmake/Toolchain/ArmClang-Base.cmake
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/cmake/Toolchain/ArmClang-Base.cmake
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/cmake/Toolchain/ArmClang-Base.cmake
@@ -17,8 +17,8 @@ set(CMAKE_CXX_COMPILER
 
 foreach(language IN ITEMS ASM C CXX)
     if(CMAKE_${language}_COMPILER_TARGET)
-        string(APPEND CMAKE_${language}_FLAGS_INIT
-               "--target=${CMAKE_${language}_COMPILER_TARGET} ")
+	string(APPEND CMAKE_${language}_FLAGS_INIT
+	       "--target=${CMAKE_${language}_COMPILER_TARGET} ")
     endif()
 endforeach()
 
@@ -44,8 +44,8 @@ if(CMAKE_VERSION VERSION_LESS "3.18.3")
     # cmake-lint: disable=C0111
 
     macro(scp_reset_linker_wrapper_flag)
-        set(CMAKE_C_LINKER_WRAPPER_FLAG "")
-        set(CMAKE_CXX_LINKER_WRAPPER_FLAG "")
+	set(CMAKE_C_LINKER_WRAPPER_FLAG "")
+	set(CMAKE_CXX_LINKER_WRAPPER_FLAG "")
     endmacro()
 
     variable_watch(CMAKE_C_LINKER_WRAPPER_FLAG scp_reset_linker_wrapper_flag)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/cmake/Toolchain/Clang-Baremetal.cmake
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/cmake/Toolchain/Clang-Baremetal.cmake
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/cmake/Toolchain/Clang-Baremetal.cmake
@@ -10,65 +10,65 @@ include("${CMAKE_CURRENT_LIST_DIR}/Gener
 
 if(CMAKE_SYSTEM_PROCESSOR STREQUAL "aarch64")
     execute_process(
-        COMMAND "which" "${SCP_LLVM_SYSROOT_CC}"
-        OUTPUT_VARIABLE LLVM_SYSROOT_CC_PATH
-        OUTPUT_STRIP_TRAILING_WHITESPACE)
+	COMMAND "which" "${SCP_LLVM_SYSROOT_CC}"
+	OUTPUT_VARIABLE LLVM_SYSROOT_CC_PATH
+	OUTPUT_STRIP_TRAILING_WHITESPACE)
     execute_process(
-        COMMAND "dirname" "${LLVM_SYSROOT_CC_PATH}"
-        OUTPUT_VARIABLE LLVM_SYSROOT_PATH
-        OUTPUT_STRIP_TRAILING_WHITESPACE)
+	COMMAND "dirname" "${LLVM_SYSROOT_CC_PATH}"
+	OUTPUT_VARIABLE LLVM_SYSROOT_PATH
+	OUTPUT_STRIP_TRAILING_WHITESPACE)
     string(APPEND LLVM_SYSROOT_PATH "/../aarch64-none-elf")
     cmake_path(NORMAL_PATH LLVM_SYSROOT_PATH OUTPUT_VARIABLE LLVM_SYSROOT_PATH)
     set(gcc-multi-dir "")
 else()
     execute_process(
-        COMMAND "${SCP_LLVM_SYSROOT_CC}" "-print-sysroot"
-        OUTPUT_VARIABLE LLVM_SYSROOT_PATH
-        OUTPUT_STRIP_TRAILING_WHITESPACE)
+	COMMAND "${SCP_LLVM_SYSROOT_CC}" "-print-sysroot"
+	OUTPUT_VARIABLE LLVM_SYSROOT_PATH
+	OUTPUT_STRIP_TRAILING_WHITESPACE)
     execute_process(
-        COMMAND "${SCP_LLVM_SYSROOT_CC}" "-print-multi-directory"
-                "-mcpu=${CMAKE_SYSTEM_PROCESSOR}" "-mthumb"
-        OUTPUT_VARIABLE gcc-multi-dir
-        OUTPUT_STRIP_TRAILING_WHITESPACE)
+	COMMAND "${SCP_LLVM_SYSROOT_CC}" "-print-multi-directory"
+		"-mcpu=${CMAKE_SYSTEM_PROCESSOR}" "-mthumb"
+	OUTPUT_VARIABLE gcc-multi-dir
+	OUTPUT_STRIP_TRAILING_WHITESPACE)
 endif()
 
 foreach(language IN ITEMS ASM C CXX)
 
     if(CMAKE_SYSTEM_PROCESSOR STREQUAL "aarch64")
-        string(APPEND CMAKE_${language}_FLAGS_INIT
-               "-mstrict-align -fno-builtin -DAARCH64 -D__ASSEMBLY__ ")
-        string(APPEND CMAKE_${language}_FLAGS_INIT
-               "-I\"${LLVM_SYSROOT_PATH}/include\" ")
+	string(APPEND CMAKE_${language}_FLAGS_INIT
+	       "-mstrict-align -fno-builtin -DAARCH64 -D__ASSEMBLY__ ")
+	string(APPEND CMAKE_${language}_FLAGS_INIT
+	       "-I\"${LLVM_SYSROOT_PATH}/include\" ")
     endif()
 
     if(CMAKE_SYSTEM_PROCESSOR MATCHES "cortex-m(3|7|33|55)")
-        set(BUILD_TARGET "-mcpu=${CMAKE_SYSTEM_PROCESSOR} -mthumb ")
-        string(APPEND CMAKE_${language}_FLAGS_INIT "${BUILD_TARGET}")
-        if(CMAKE_SYSTEM_PROCESSOR MATCHES "cortex-m7")
-            set(CLANG_BUILTINS_ARCH "armv7em")
-        elseif(CMAKE_SYSTEM_PROCESSOR MATCHES "cortex-m33")
-            set(CLANG_BUILTINS_ARCH "armv8m.main")
-        elseif(CMAKE_SYSTEM_PROCESSOR MATCHES "cortex-m3")
-            set(CLANG_BUILTINS_ARCH "armv7m")
-        elseif(CMAKE_SYSTEM_PROCESSOR MATCHES "cortex-m55")
-            set(CLANG_BUILTINS_ARCH "armv8.1m.main")
-        endif()
+	set(BUILD_TARGET "-mcpu=${CMAKE_SYSTEM_PROCESSOR} -mthumb ")
+	string(APPEND CMAKE_${language}_FLAGS_INIT "${BUILD_TARGET}")
+	if(CMAKE_SYSTEM_PROCESSOR MATCHES "cortex-m7")
+	    set(CLANG_BUILTINS_ARCH "armv7em")
+	elseif(CMAKE_SYSTEM_PROCESSOR MATCHES "cortex-m33")
+	    set(CLANG_BUILTINS_ARCH "armv8m.main")
+	elseif(CMAKE_SYSTEM_PROCESSOR MATCHES "cortex-m3")
+	    set(CLANG_BUILTINS_ARCH "armv7m")
+	elseif(CMAKE_SYSTEM_PROCESSOR MATCHES "cortex-m55")
+	    set(CLANG_BUILTINS_ARCH "armv8.1m.main")
+	endif()
     endif()
 
     string(APPEND CMAKE_${language}_FLAGS_INIT
-           "--target=${CMAKE_${language}_COMPILER_TARGET} ")
+	   "--target=${CMAKE_${language}_COMPILER_TARGET} ")
 
     string(APPEND CMAKE_${language}_FLAGS_INIT
-           "--sysroot=${LLVM_SYSROOT_PATH} ")
+	   "--sysroot=${LLVM_SYSROOT_PATH} ")
     string(APPEND CMAKE_${language}_FLAGS_INIT "-ffunction-sections ")
     string(APPEND CMAKE_${language}_FLAGS_INIT "-fdata-sections ")
     string(APPEND CMAKE_${language}_FLAGS_INIT "-fshort-enums ")
     string(APPEND CMAKE_${language}_FLAGS_INIT "-Wall -Werror -Wextra ")
     string(APPEND CMAKE_${language}_FLAGS_INIT
-           "-Wno-error=deprecated-declarations ")
+	   "-Wno-error=deprecated-declarations ")
     string(APPEND CMAKE_${language}_FLAGS_INIT "-Wno-unused-parameter ")
     string(APPEND CMAKE_${language}_FLAGS_INIT
-           "-Wno-missing-field-initializers ")
+	   "-Wno-missing-field-initializers ")
 
     string(APPEND CMAKE_${language}_FLAGS_DEBUG_INIT "-Og")
 endforeach()
@@ -81,14 +81,14 @@ string(APPEND CMAKE_EXE_LINKER_FLAGS_INI
 if(NOT CMAKE_SYSTEM_PROCESSOR STREQUAL "aarch64")
     string(APPEND CMAKE_EXE_LINKER_FLAGS_INIT "-fuse-ld=lld ")
     string(APPEND CMAKE_EXE_LINKER_FLAGS_INIT
-           "-nostdlib -lclang_rt.builtins-${CLANG_BUILTINS_ARCH} ")
+	   "-nostdlib -lclang_rt.builtins-${CLANG_BUILTINS_ARCH} ")
     foreach(crtobj IN ITEMS crti crtbegin crt0 crtend crtn)
-        execute_process(
-            COMMAND "${SCP_LLVM_SYSROOT_CC}" "--print-file-name=${crtobj}.o"
-                    "-mcpu=${CMAKE_SYSTEM_PROCESSOR}" "-mthumb"
-            OUTPUT_VARIABLE gcc-${crtobj}
-            OUTPUT_STRIP_TRAILING_WHITESPACE)
-        cmake_path(NORMAL_PATH gcc-${crtobj} OUTPUT_VARIABLE gcc-${crtobj})
-        string(APPEND CMAKE_EXE_LINKER_FLAGS_INIT "${gcc-${crtobj}} ")
+	execute_process(
+	    COMMAND "${SCP_LLVM_SYSROOT_CC}" "--print-file-name=${crtobj}.o"
+		    "-mcpu=${CMAKE_SYSTEM_PROCESSOR}" "-mthumb"
+	    OUTPUT_VARIABLE gcc-${crtobj}
+	    OUTPUT_STRIP_TRAILING_WHITESPACE)
+	cmake_path(NORMAL_PATH gcc-${crtobj} OUTPUT_VARIABLE gcc-${crtobj})
+	string(APPEND CMAKE_EXE_LINKER_FLAGS_INIT "${gcc-${crtobj}} ")
     endforeach()
 endif()
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/cmake/Toolchain/GNU-Baremetal.cmake
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/cmake/Toolchain/GNU-Baremetal.cmake
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/cmake/Toolchain/GNU-Baremetal.cmake
@@ -11,18 +11,18 @@ include("${CMAKE_CURRENT_LIST_DIR}/Gener
 foreach(language IN ITEMS ASM C CXX)
 
     if(CMAKE_SYSTEM_PROCESSOR STREQUAL "aarch64")
-        if(DEFINED SCP_AARCH64_PROCESSOR_TARGET)
-            string(APPEND CMAKE_${language}_FLAGS_INIT
-               "-mcpu=${SCP_AARCH64_PROCESSOR_TARGET} ")
-        endif()
-        string(APPEND CMAKE_${language}_FLAGS_INIT
-            "-mstrict-align -fno-builtin -DAARCH64 -D__ASSEMBLY__ ")
+	if(DEFINED SCP_AARCH64_PROCESSOR_TARGET)
+	    string(APPEND CMAKE_${language}_FLAGS_INIT
+	       "-mcpu=${SCP_AARCH64_PROCESSOR_TARGET} ")
+	endif()
+	string(APPEND CMAKE_${language}_FLAGS_INIT
+	    "-mstrict-align -fno-builtin -DAARCH64 -D__ASSEMBLY__ ")
     endif()
 
     if(CMAKE_SYSTEM_PROCESSOR MATCHES "cortex-m(3|7|33|55)")
-        string(APPEND CMAKE_${language}_FLAGS_INIT "-mthumb ")
-        string(APPEND CMAKE_${language}_FLAGS_INIT
-            "-mcpu=${CMAKE_SYSTEM_PROCESSOR} ")
+	string(APPEND CMAKE_${language}_FLAGS_INIT "-mthumb ")
+	string(APPEND CMAKE_${language}_FLAGS_INIT
+	    "-mcpu=${CMAKE_SYSTEM_PROCESSOR} ")
     endif()
 
     string(APPEND CMAKE_${language}_FLAGS_INIT "-ffunction-sections ")
@@ -30,10 +30,10 @@ foreach(language IN ITEMS ASM C CXX)
     string(APPEND CMAKE_${language}_FLAGS_INIT "-fshort-enums ")
     string(APPEND CMAKE_${language}_FLAGS_INIT "-Wall -Werror -Wextra ")
     string(APPEND CMAKE_${language}_FLAGS_INIT
-           "-Wno-error=deprecated-declarations ")
+	   "-Wno-error=deprecated-declarations ")
     string(APPEND CMAKE_${language}_FLAGS_INIT "-Wno-unused-parameter ")
     string(APPEND CMAKE_${language}_FLAGS_INIT
-           "-Wno-missing-field-initializers ")
+	   "-Wno-missing-field-initializers ")
 
     string(APPEND CMAKE_${language}_FLAGS_DEBUG_INIT "-Og")
 endforeach()
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/contrib/cmsis/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/contrib/cmsis/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/contrib/cmsis/CMakeLists.txt
@@ -47,35 +47,35 @@ add_library(rtos2-rtx INTERFACE)
 target_include_directories(
     rtos2-rtx
     INTERFACE "${CMAKE_CURRENT_SOURCE_DIR}/include"
-              "${CMAKE_CURRENT_SOURCE_DIR}/git/CMSIS/RTOS2/RTX/Include"
-              "${CMAKE_CURRENT_SOURCE_DIR}/git/CMSIS/RTOS2/RTX/Source")
+	      "${CMAKE_CURRENT_SOURCE_DIR}/git/CMSIS/RTOS2/RTX/Include"
+	      "${CMAKE_CURRENT_SOURCE_DIR}/git/CMSIS/RTOS2/RTX/Source")
 
 # cmake-lint: disable=E1122
 target_sources(
     rtos2-rtx
     INTERFACE
-        "${CMAKE_CURRENT_SOURCE_DIR}/git/CMSIS/RTOS2/RTX/Source/rtx_delay.c"
-        "${CMAKE_CURRENT_SOURCE_DIR}/git/CMSIS/RTOS2/RTX/Source/rtx_evflags.c"
-        "${CMAKE_CURRENT_SOURCE_DIR}/git/CMSIS/RTOS2/RTX/Source/rtx_evr.c"
-        "${CMAKE_CURRENT_SOURCE_DIR}/git/CMSIS/RTOS2/RTX/Source/rtx_kernel.c"
-        "${CMAKE_CURRENT_SOURCE_DIR}/git/CMSIS/RTOS2/RTX/Source/rtx_memory.c"
-        "${CMAKE_CURRENT_SOURCE_DIR}/git/CMSIS/RTOS2/RTX/Source/rtx_mempool.c"
-        "${CMAKE_CURRENT_SOURCE_DIR}/git/CMSIS/RTOS2/RTX/Source/rtx_msgqueue.c"
-        "${CMAKE_CURRENT_SOURCE_DIR}/git/CMSIS/RTOS2/RTX/Source/rtx_mutex.c"
-        "${CMAKE_CURRENT_SOURCE_DIR}/git/CMSIS/RTOS2/RTX/Source/rtx_semaphore.c"
-        "${CMAKE_CURRENT_SOURCE_DIR}/git/CMSIS/RTOS2/RTX/Source/rtx_system.c"
-        "${CMAKE_CURRENT_SOURCE_DIR}/git/CMSIS/RTOS2/RTX/Source/rtx_thread.c"
-        "${CMAKE_CURRENT_SOURCE_DIR}/git/CMSIS/RTOS2/RTX/Source/rtx_timer.c"
-        "${CMAKE_CURRENT_SOURCE_DIR}/git/CMSIS/RTOS2/Source/os_systick.c")
+	"${CMAKE_CURRENT_SOURCE_DIR}/git/CMSIS/RTOS2/RTX/Source/rtx_delay.c"
+	"${CMAKE_CURRENT_SOURCE_DIR}/git/CMSIS/RTOS2/RTX/Source/rtx_evflags.c"
+	"${CMAKE_CURRENT_SOURCE_DIR}/git/CMSIS/RTOS2/RTX/Source/rtx_evr.c"
+	"${CMAKE_CURRENT_SOURCE_DIR}/git/CMSIS/RTOS2/RTX/Source/rtx_kernel.c"
+	"${CMAKE_CURRENT_SOURCE_DIR}/git/CMSIS/RTOS2/RTX/Source/rtx_memory.c"
+	"${CMAKE_CURRENT_SOURCE_DIR}/git/CMSIS/RTOS2/RTX/Source/rtx_mempool.c"
+	"${CMAKE_CURRENT_SOURCE_DIR}/git/CMSIS/RTOS2/RTX/Source/rtx_msgqueue.c"
+	"${CMAKE_CURRENT_SOURCE_DIR}/git/CMSIS/RTOS2/RTX/Source/rtx_mutex.c"
+	"${CMAKE_CURRENT_SOURCE_DIR}/git/CMSIS/RTOS2/RTX/Source/rtx_semaphore.c"
+	"${CMAKE_CURRENT_SOURCE_DIR}/git/CMSIS/RTOS2/RTX/Source/rtx_system.c"
+	"${CMAKE_CURRENT_SOURCE_DIR}/git/CMSIS/RTOS2/RTX/Source/rtx_thread.c"
+	"${CMAKE_CURRENT_SOURCE_DIR}/git/CMSIS/RTOS2/RTX/Source/rtx_timer.c"
+	"${CMAKE_CURRENT_SOURCE_DIR}/git/CMSIS/RTOS2/Source/os_systick.c")
 
 target_compile_definitions(rtos2-rtx INTERFACE RTX_NO_MULTITHREAD_CLIB)
 target_compile_options(rtos2-rtx INTERFACE -Wno-array-bounds)
 
 if(CMAKE_SYSTEM_PROCESSOR MATCHES "cortex-m[37]")
     target_sources(
-        rtos2-rtx
-        INTERFACE
-            "${CMAKE_CURRENT_SOURCE_DIR}/git/CMSIS/RTOS2/RTX/Source/GCC/irq_cm3.S"
+	rtos2-rtx
+	INTERFACE
+	    "${CMAKE_CURRENT_SOURCE_DIR}/git/CMSIS/RTOS2/RTX/Source/GCC/irq_cm3.S"
     )
 endif()
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/debugger/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/debugger/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/debugger/CMakeLists.txt
@@ -14,15 +14,15 @@ add_library(debugger)
 target_compile_definitions(debugger PUBLIC "BUILD_HAS_DEBUGGER")
 
 target_include_directories(debugger
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(
     debugger
     PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/checkpoint.c"
-            "${CMAKE_CURRENT_SOURCE_DIR}/src/cli/cli.c"
-            "${CMAKE_CURRENT_SOURCE_DIR}/src/cli/cli_commands_checkpoint.c"
-            "${CMAKE_CURRENT_SOURCE_DIR}/src/cli/cli_commands_core.c"
-            "${CMAKE_CURRENT_SOURCE_DIR}/src/cli/cli_fifo.c"
-            "${CMAKE_CURRENT_SOURCE_DIR}/src/cli/cli_platform_time.c")
+	    "${CMAKE_CURRENT_SOURCE_DIR}/src/cli/cli.c"
+	    "${CMAKE_CURRENT_SOURCE_DIR}/src/cli/cli_commands_checkpoint.c"
+	    "${CMAKE_CURRENT_SOURCE_DIR}/src/cli/cli_commands_core.c"
+	    "${CMAKE_CURRENT_SOURCE_DIR}/src/cli/cli_fifo.c"
+	    "${CMAKE_CURRENT_SOURCE_DIR}/src/cli/cli_platform_time.c")
 
 target_link_libraries(debugger PUBLIC framework)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/debugger/include/checkpoint.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/debugger/include/checkpoint.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/debugger/include/checkpoint.h
@@ -129,11 +129,11 @@ void checkpoint(checkpoint_st *c, char *
  *
  */
 #    define checkpoint_register(c, name) \
-        ({ \
-            (void)c; \
-            (void)name; \
-            FWK_SUCCESS; \
-        })
+	({ \
+	    (void)c; \
+	    (void)name; \
+	    FWK_SUCCESS; \
+	})
 
 /*!
  * \brief Insert a checkpoint
@@ -152,12 +152,12 @@ void checkpoint(checkpoint_st *c, char *
  * \param tag Checkpoint tag string.
  */
 #    define checkpoint(c, file, line, tag) \
-        do { \
-            (void)c; \
-            (void)file; \
-            (void)line; \
-            (void)tag; \
-        } while (0)
+	do { \
+	    (void)c; \
+	    (void)file; \
+	    (void)line; \
+	    (void)tag; \
+	} while (0)
 
 #endif
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/debugger/include/cli.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/debugger/include/cli.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/debugger/include/cli.h
@@ -295,10 +295,10 @@ int cli_command_register(cli_command_st
  * \retval CLI_ERR_MEM Not enough memory.
  */
 #    define cli_command_register(new_cmd) \
-        ({ \
-            (void)new_cmd; \
-            FWK_SUCCESS; \
-        })
+	({ \
+	    (void)new_cmd; \
+	    FWK_SUCCESS; \
+	})
 #endif
 
 #endif /* _CLI_H_ */
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/debugger/src/checkpoint.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/debugger/src/checkpoint.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/debugger/src/checkpoint.c
@@ -24,12 +24,12 @@ void checkpoint_enable_all(void)
     uint32_t i = 0;
 
     for (i = 0; i < CHECKPOINT_NUM; i++) {
-        /*
-         * Bypass set to 0 to ensure thread stops at the next checkpoint. Tag
-         * is set to empty string to keep things clean.
-         */
-        checkpoint_table[i].bypass = CHECKPOINT_ENABLED;
-        checkpoint_table[i].tag[0] = 0;
+	/*
+	 * Bypass set to 0 to ensure thread stops at the next checkpoint. Tag
+	 * is set to empty string to keep things clean.
+	 */
+	checkpoint_table[i].bypass = CHECKPOINT_ENABLED;
+	checkpoint_table[i].tag[0] = 0;
     }
 
     /* Changing reset value to enabled by default. */
@@ -41,12 +41,12 @@ void checkpoint_disable_all(void)
     uint32_t i = 0;
 
     for (i = 0; i < CHECKPOINT_NUM; i++) {
-        /*
-         * Bypass is set to -1 and tag is set to an empty string so checkpoint
-         * will be skipped.
-         */
-        checkpoint_table[i].bypass = CHECKPOINT_DISABLED;
-        checkpoint_table[i].tag[0] = 0;
+	/*
+	 * Bypass is set to -1 and tag is set to an empty string so checkpoint
+	 * will be skipped.
+	 */
+	checkpoint_table[i].bypass = CHECKPOINT_DISABLED;
+	checkpoint_table[i].tag[0] = 0;
     }
 }
 
@@ -55,15 +55,15 @@ int32_t checkpoint_register(checkpoint_s
     uint32_t i = 0;
 
     for (i = 0; i < CHECKPOINT_NUM; i++) {
-        if (checkpoint_table[i].in_use == false) {
-            checkpoint_table[i].in_use = true;
-            checkpoint_table[i].index = i;
-            strncpy(checkpoint_table[i].name, name, CHECKPOINT_NAME_LEN);
-            checkpoint_table[i].name[CHECKPOINT_NAME_LEN - 1] = 0;
-            checkpoint_table[i].bypass = bypass_reset;
-            *c = &checkpoint_table[i];
-            return FWK_SUCCESS;
-        }
+	if (checkpoint_table[i].in_use == false) {
+	    checkpoint_table[i].in_use = true;
+	    checkpoint_table[i].index = i;
+	    strncpy(checkpoint_table[i].name, name, CHECKPOINT_NAME_LEN);
+	    checkpoint_table[i].name[CHECKPOINT_NAME_LEN - 1] = 0;
+	    checkpoint_table[i].bypass = bypass_reset;
+	    *c = &checkpoint_table[i];
+	    return FWK_SUCCESS;
+	}
     }
 
     return FWK_E_NOMEM;
@@ -73,35 +73,35 @@ void checkpoint(checkpoint_st *c, char *
 {
     /* If tags match or if bypass == 0, stop here. */
     if ((c->bypass == CHECKPOINT_ENABLED) ||
-        ((tag != NULL) && (strncmp(tag, c->tag, CHECKPOINT_TAG_LEN) == 0))) {
-        cli_printf(NONE,
-            "SCP Debugger has stopped at a checkpoint.\n"
-            "Tag: %s\n  File: %s\n  Line: %d\n",
-            tag,
-            file,
-            line);
-
-        c->tag[0] = 0;
-        c->bypass = CHECKPOINT_ENABLED;
-
-        cli_start();
-
-        cli_printf(NONE,
-            "SCP execution resume at a checkpoint.\n"
-            "Tag: %s\n  File: %s\n  Line: %d\n",
-            tag,
-            file,
-            line);
+	((tag != NULL) && (strncmp(tag, c->tag, CHECKPOINT_TAG_LEN) == 0))) {
+	cli_printf(NONE,
+	    "SCP Debugger has stopped at a checkpoint.\n"
+	    "Tag: %s\n  File: %s\n  Line: %d\n",
+	    tag,
+	    file,
+	    line);
+
+	c->tag[0] = 0;
+	c->bypass = CHECKPOINT_ENABLED;
+
+	cli_start();
+
+	cli_printf(NONE,
+	    "SCP execution resume at a checkpoint.\n"
+	    "Tag: %s\n  File: %s\n  Line: %d\n",
+	    tag,
+	    file,
+	    line);
 
-        return;
+	return;
     }
 
     /* If bypass is less than 0 (-1), checkpoint is disabled so return. */
     if (c->bypass == CHECKPOINT_DISABLED)
-        return;
+	return;
     else {
-        /* If bypass is greater than 0, decrement and return. */
-        c->bypass = c->bypass - 1;
-        return;
+	/* If bypass is greater than 0, decrement and return. */
+	c->bypass = c->bypass - 1;
+	return;
     }
 }
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/debugger/src/cli/cli.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/debugger/src/cli/cli.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/debugger/src/cli/cli.c
@@ -59,7 +59,7 @@ size_t maxlen;
     size_t n;
 
     for (e = s, n = 0; *e && n < maxlen; e++, n++)
-        ;
+	;
     return n;
 }
 
@@ -80,15 +80,15 @@ int cli_command_register(cli_command_st
     struct command_ctx *c = NULL;
 
     FWK_LIST_FOR_EACH(&cli_commands_list, node,
-        struct command_ctx, list_node, c) {
-        if (strcmp(new_cmd.command, c->cmd.command) == 0)
-            /* Command exist. Skip registration */
-            return FWK_SUCCESS;
+	struct command_ctx, list_node, c) {
+	if (strcmp(new_cmd.command, c->cmd.command) == 0)
+	    /* Command exist. Skip registration */
+	    return FWK_SUCCESS;
     }
 
     c = fwk_mm_calloc(1, sizeof(struct command_ctx));
     if (c == NULL)
-        return FWK_E_NOMEM;
+	return FWK_E_NOMEM;
 
     c->cmd = new_cmd;
     fwk_list_push_tail(&cli_commands_list, &c->list_node);
@@ -105,9 +105,9 @@ int cli_command_init(void)
 
     /* Add the common commands to the commands linked list */
     for (index = 0; cli_commands[index].command != 0; index++) {
-        status = cli_command_register(cli_commands[index]);
-        if (status != FWK_SUCCESS)
-            return status;
+	status = cli_command_register(cli_commands[index]);
+	if (status != FWK_SUCCESS)
+	    return status;
     }
     return FWK_SUCCESS;
 }
@@ -121,18 +121,18 @@ int cli_command_init(void)
 int32_t cli_strncmp(const char *s1, const char *s2, uint32_t limit)
 {
     for (uint32_t i = 0; i < limit; i++) {
-        if (s1[i] != s2[i])
-            /*
-             * If first string greater than second, return positive, if second
-             * string greater than first, return negative.
-             */
-            return s1[i] - s2[i];
-        else if (s1[i] == 0)
-            /*
-             * We know characters are equal, so if one of them is equal to zero
-             * both are, so return zero to indicate a match.
-             */
-            return 0;
+	if (s1[i] != s2[i])
+	    /*
+	     * If first string greater than second, return positive, if second
+	     * string greater than first, return negative.
+	     */
+	    return s1[i] - s2[i];
+	else if (s1[i] == 0)
+	    /*
+	     * We know characters are equal, so if one of them is equal to zero
+	     * both are, so return zero to indicate a match.
+	     */
+	    return 0;
     }
 
     /* Limit reached, return 0 as first string and second string match. */
@@ -346,24 +346,24 @@ uint32_t cli_init(void)
 
     /* This function can only run when current state is CLI_NOT_READY. */
     if (cli_state != CLI_NOT_READY)
-        return FWK_E_STATE;
+	return FWK_E_STATE;
 
     /* Store CLI prompt text. */
     if (strlen(prompt) > (CLI_CONFIG_PROMPT_BUF_SIZE - 1))
-        return FWK_E_NOMEM;
+	return FWK_E_NOMEM;
 
     strncpy(cli_prompt, prompt, CLI_CONFIG_PROMPT_BUF_SIZE);
     cli_prompt_size = strlen(prompt);
 
     /* Initialize print buffer FIFO. */
     status = fifo_init(
-        &cli_print_fifo, cli_print_fifo_buffer, CLI_CONFIG_PRINT_BUFFER_SIZE);
+	&cli_print_fifo, cli_print_fifo_buffer, CLI_CONFIG_PRINT_BUFFER_SIZE);
     if (status != FWK_SUCCESS)
-        return status;
+	return status;
 
     status = cli_command_init();
     if (status != FWK_SUCCESS)
-        return status;
+	return status;
 
     /* Setting state to CLI_READY. */
     cli_state = CLI_READY;
@@ -375,7 +375,7 @@ uint32_t cli_start(void)
 {
     /* This function can only run when current state is CLI_READY. */
     if (cli_state != CLI_READY)
-        return FWK_E_STATE;
+	return FWK_E_STATE;
 
     /* Attempt to start thread. */
     cli_main(NULL);
@@ -391,41 +391,41 @@ uint32_t cli_bprintf(cli_option_et optio
 
     /* Check parameters. */
     if (format == NULL)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     /* Check CLI state. */
     if (cli_state == CLI_NOT_READY)
-        return FWK_E_STATE;
+	return FWK_E_STATE;
 
     /* Check if we need any escape sequence formatting. */
     if (options != 0) {
-        cli_format(options, scratch_buffer, CLI_CONFIG_SCRATCH_BUFFER_SIZE);
-        buffer_index = strnlen(scratch_buffer, CLI_CONFIG_SCRATCH_BUFFER_SIZE);
+	cli_format(options, scratch_buffer, CLI_CONFIG_SCRATCH_BUFFER_SIZE);
+	buffer_index = strnlen(scratch_buffer, CLI_CONFIG_SCRATCH_BUFFER_SIZE);
     }
 
     /* Generated formatted print string. */
     va_start(arg, format);
     cli_snprintf_arg(
-        (scratch_buffer + buffer_index),
-        (scratch_buffer + CLI_CONFIG_SCRATCH_BUFFER_SIZE),
-        format,
-        &arg);
+	(scratch_buffer + buffer_index),
+	(scratch_buffer + CLI_CONFIG_SCRATCH_BUFFER_SIZE),
+	format,
+	&arg);
     va_end(arg);
     buffer_index = strnlen(scratch_buffer, CLI_CONFIG_SCRATCH_BUFFER_SIZE);
 
     /* Make sure to return formatting to normal after escape sequences are used.
      */
     if (options != NONE) {
-        strncat(
-            scratch_buffer,
-            "\x1B[0m",
-            (CLI_CONFIG_SCRATCH_BUFFER_SIZE - buffer_index));
-        buffer_index = strnlen(scratch_buffer, CLI_CONFIG_SCRATCH_BUFFER_SIZE);
+	strncat(
+	    scratch_buffer,
+	    "\x1B[0m",
+	    (CLI_CONFIG_SCRATCH_BUFFER_SIZE - buffer_index));
+	buffer_index = strnlen(scratch_buffer, CLI_CONFIG_SCRATCH_BUFFER_SIZE);
     }
 
     /* Check to make sure we didn't fill up our scratch buffer. */
     if (buffer_index >= CLI_CONFIG_SCRATCH_BUFFER_SIZE)
-        return FWK_E_NOMEM;
+	return FWK_E_NOMEM;
 
     /* Send formatted print data to print FIFO. */
     return cli_bprint(scratch_buffer);
@@ -438,29 +438,29 @@ uint32_t cli_bprint(const char *string)
 
     /* Check parameters. */
     if (string == NULL)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     /* If not ready, return an error. */
     if (cli_state == CLI_NOT_READY)
-        return FWK_E_STATE;
+	return FWK_E_STATE;
 
     /* If ready but not running, print directly to UART. */
     if (cli_state == CLI_READY)
-        return cli_print(string);
+	return cli_print(string);
 
     /* Put data into FIFO. */
     for (i = 0; ; i++) {
-        if ((string[i] == 0) &&
-            (i >= CLI_CONFIG_SCRATCH_BUFFER_SIZE) &&
-            (status != FWK_SUCCESS))
-            break;
-        status = fifo_put(&cli_print_fifo, (char *)&string[i]);
+	if ((string[i] == 0) &&
+	    (i >= CLI_CONFIG_SCRATCH_BUFFER_SIZE) &&
+	    (status != FWK_SUCCESS))
+	    break;
+	status = fifo_put(&cli_print_fifo, (char *)&string[i]);
     }
 
     /* Print an error message if the print buffer is full. */
     if ((status != FWK_SUCCESS) && (overflow == false)) {
-        overflow = true;
-        cli_print("\x1B[31mCONSOLE ERROR:\x1B[0m Print buffer overflow.\n");
+	overflow = true;
+	cli_print("\x1B[31mCONSOLE ERROR:\x1B[0m Print buffer overflow.\n");
     }
 
     return status;
@@ -476,43 +476,43 @@ uint32_t cli_printf(cli_option_et option
 
     /* Checking pointers. */
     if (format == 0)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     /* Applying style options. */
     if (options != NONE) {
-        status = cli_format(options, 0, 0);
-        if (status != FWK_SUCCESS)
-            return status;
+	status = cli_format(options, 0, 0);
+	if (status != FWK_SUCCESS)
+	    return status;
     }
 
     /* Printing formatted text. */
     va_start(arg, format);
     cli_snprintf_arg(
-        sCLIbuffer,
-        sCLIbuffer + CLI_CONFIG_SCRATCH_BUFFER_SIZE,
-        (char *)format,
-        &arg);
+	sCLIbuffer,
+	sCLIbuffer + CLI_CONFIG_SCRATCH_BUFFER_SIZE,
+	(char *)format,
+	&arg);
     va_end(arg);
 
     /* Print format string. */
     status = cli_print((const char *)sCLIbuffer);
     if (status != FWK_SUCCESS)
-        return status;
+	return status;
 
     /* If style options were used, reset everything to default. */
     if (options != NONE) {
-        cli_format(NONE, 0, 0);
-        if (status != FWK_SUCCESS)
-            return status;
+	cli_format(NONE, 0, 0);
+	if (status != FWK_SUCCESS)
+	    return status;
     }
 
     /* Making sure we didn't try to print too much. */
     if (strnlen((char *)sCLIbuffer, CLI_CONFIG_SCRATCH_BUFFER_SIZE) >=
-        CLI_CONFIG_SCRATCH_BUFFER_SIZE - 1) {
-        status = cli_print(str_format_buf_too_small);
-        if (status != FWK_SUCCESS)
-            return status;
-        return FWK_E_NOMEM;
+	CLI_CONFIG_SCRATCH_BUFFER_SIZE - 1) {
+	status = cli_print(str_format_buf_too_small);
+	if (status != FWK_SUCCESS)
+	    return status;
+	return FWK_E_NOMEM;
     }
 
     return status;
@@ -524,16 +524,16 @@ uint32_t cli_print(const char *string)
     int32_t status = FWK_SUCCESS;
 
     for (index = 0; string[index] != 0; index++) {
-        if (string[index] == '\n') {
-            status = fwk_io_putch(fwk_io_stdout, '\r');
-            if (status != FWK_SUCCESS) {
-                return status;
-            }
-        }
-        status = fwk_io_putch(fwk_io_stdout, string[index]);
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
+	if (string[index] == '\n') {
+	    status = fwk_io_putch(fwk_io_stdout, '\r');
+	    if (status != FWK_SUCCESS) {
+		return status;
+	    }
+	}
+	status = fwk_io_putch(fwk_io_stdout, string[index]);
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
     }
     return status;
 }
@@ -562,11 +562,11 @@ uint32_t cli_getline(
 
     /* Validate parameters. */
     if (buffer == NULL ||
-        buffer_size == 0 ||
-        cursor_position >= cli_terminal_width ||
-        argbuf == NULL ||
-        argbuf_size == 0)
-        return FWK_E_PARAM;
+	buffer_size == 0 ||
+	cursor_position >= cli_terminal_width ||
+	argbuf == NULL ||
+	argbuf_size == 0)
+	return FWK_E_PARAM;
 
     /* Zero out buffer and argbuf. */
     memset(buffer, 0, buffer_size);
@@ -575,7 +575,7 @@ uint32_t cli_getline(
     /* Print prompt arrow. */
     status = cli_print(CLI_PROMPT);
     if (status != FWK_SUCCESS)
-        return status;
+	return status;
 
     /* Increment cursor position since we just printed an arrow and space. */
     cursor_position = (cursor_position + 2) % cli_terminal_width;
@@ -585,83 +585,83 @@ uint32_t cli_getline(
      * generated. This loop will have negligible impact on system performance.
      */
     while (1) {
-        /* Grab a character from the UART. */
-        do {
-            status = fwk_io_getch(fwk_io_stdin, &c);
-        } while (status == FWK_PENDING);
-
-        if (status != FWK_SUCCESS)
-            return status;
-
-        /*
-         * Ignore non-printing characters except for a few we care about.
-         * 0x00 - 0x1F are non-printing characters.
-         * 0x7F - 0xFF are non-printing characters.
-         * Carriage return: \r, 0x0D
-         * Newline:         \n, 0x0C
-         * Backspace:       \b, 0x08
-         * Delete:              0x7F
-         */
-        if ((c <= 0x1F || c >= 0x7F) && c != '\r' && c != '\n' && c != '\b' &&
-            c != 0x7F)
-            continue;
-
-
-        /* If backspace (0x08) or delete (0x7F) character received. */
-        if (c == '\b' || c == 0x7F) {
-            /* Only accept backspace presses if we're not at the beginning of
-             * the string. */
-            if (index != 0) {
-                status = cli_backspace(&cursor_position, cli_terminal_width);
-                if (status != FWK_SUCCESS)
-                    return status;
-
-                /* Decrement index and set old last character to null. */
-                index = index - 1;
-                buffer[index] = 0;
-            }
-            continue;
-        }
-
-        /* If newline received. */
-        if (c == '\n' || c == '\r') {
-            /* Making sure the rest of the buffer is zero. */
-            memset(&(buffer[index]), 0, buffer_size - index);
-            status = cli_print("\n");
-            if (status != FWK_SUCCESS)
-                return status;
-            break;
-        }
-
-        /* Echo received character to console. */
-        do {
-            status = fwk_io_putch(fwk_io_stdout, c);
-        } while (status == FWK_PENDING);
-
-        if (status != FWK_SUCCESS)
-            return status;
-
-        cursor_position = (cursor_position + 1) % cli_terminal_width;
-
-        /* Incrementing indices. */
-        buffer[index] = c;
-        index = index + 1;
-        if (index >= buffer_size) {
-            /*
-             * Add null termination in case the user doesn't check return codes
-             * and tries to use the buffer.
-             */
-            buffer[index - 1] = 0;
-            return FWK_E_NOMEM;
-        }
+	/* Grab a character from the UART. */
+	do {
+	    status = fwk_io_getch(fwk_io_stdin, &c);
+	} while (status == FWK_PENDING);
+
+	if (status != FWK_SUCCESS)
+	    return status;
+
+	/*
+	 * Ignore non-printing characters except for a few we care about.
+	 * 0x00 - 0x1F are non-printing characters.
+	 * 0x7F - 0xFF are non-printing characters.
+	 * Carriage return: \r, 0x0D
+	 * Newline:         \n, 0x0C
+	 * Backspace:       \b, 0x08
+	 * Delete:              0x7F
+	 */
+	if ((c <= 0x1F || c >= 0x7F) && c != '\r' && c != '\n' && c != '\b' &&
+	    c != 0x7F)
+	    continue;
+
+
+	/* If backspace (0x08) or delete (0x7F) character received. */
+	if (c == '\b' || c == 0x7F) {
+	    /* Only accept backspace presses if we're not at the beginning of
+	     * the string. */
+	    if (index != 0) {
+		status = cli_backspace(&cursor_position, cli_terminal_width);
+		if (status != FWK_SUCCESS)
+		    return status;
+
+		/* Decrement index and set old last character to null. */
+		index = index - 1;
+		buffer[index] = 0;
+	    }
+	    continue;
+	}
+
+	/* If newline received. */
+	if (c == '\n' || c == '\r') {
+	    /* Making sure the rest of the buffer is zero. */
+	    memset(&(buffer[index]), 0, buffer_size - index);
+	    status = cli_print("\n");
+	    if (status != FWK_SUCCESS)
+		return status;
+	    break;
+	}
+
+	/* Echo received character to console. */
+	do {
+	    status = fwk_io_putch(fwk_io_stdout, c);
+	} while (status == FWK_PENDING);
+
+	if (status != FWK_SUCCESS)
+	    return status;
+
+	cursor_position = (cursor_position + 1) % cli_terminal_width;
+
+	/* Incrementing indices. */
+	buffer[index] = c;
+	index = index + 1;
+	if (index >= buffer_size) {
+	    /*
+	     * Add null termination in case the user doesn't check return codes
+	     * and tries to use the buffer.
+	     */
+	    buffer[index - 1] = 0;
+	    return FWK_E_NOMEM;
+	}
 
-        /* Add new null terminator. */
-        buffer[index] = 0;
+	/* Add new null terminator. */
+	buffer[index] = 0;
     }
 
     status = cli_split(argbuf, argbuf_size, buffer, index, " ");
     if (status != FWK_SUCCESS)
-        return status;
+	return status;
 
     return FWK_SUCCESS;
 }
@@ -682,75 +682,75 @@ static void cli_main(void const *argumen
 
     /* Initialize command history buffer pointers. */
     for (uint32_t i = 0; i < CLI_CONFIG_HISTORY_LENGTH; i++)
-        cli_history[i] = &cli_history_buffer[i * CLI_CONFIG_COMMAND_BUF_SIZE];
+	cli_history[i] = &cli_history_buffer[i * CLI_CONFIG_COMMAND_BUF_SIZE];
 
     /* Loop forever. */
     while (1) {
-        /* Printing prompt text. */
-        cli_printf(NONE, cli_prompt);
+	/* Printing prompt text. */
+	cli_printf(NONE, cli_prompt);
 
-        /* Zero out input buffer. */
-        memset(cli_input_buffer, 0, CLI_CONFIG_COMMAND_BUF_SIZE);
+	/* Zero out input buffer. */
+	memset(cli_input_buffer, 0, CLI_CONFIG_COMMAND_BUF_SIZE);
 
-        /* Get command from terminal UART. */
-        status = cli_get_command(
-            cli_input_buffer,
-            CLI_CONFIG_COMMAND_BUF_SIZE - 1,
-            &command_length,
-            cli_history,
-            history_index,
-            CLI_CONFIG_HISTORY_LENGTH,
-            &cli_exit);
-        if (status != FWK_SUCCESS) {
-            cli_error_handler(status);
-            continue;
-        }
-
-        /* Ctrl+d was pressed to exit cli */
-        if (cli_exit) {
-            cli_state = CLI_READY;
-            return;
-        }
-
-        /* Make sure command string is not empty. */
-        if (cli_input_buffer[0] == 0)
-            continue;
-
-
-        /* Update history buffer if command is different than the last one. */
-        if (cli_strncmp(
-                cli_input_buffer,
-                cli_history[last_history_index],
-                CLI_CONFIG_COMMAND_BUF_SIZE) != 0) {
-            strncpy(
-                cli_history[history_index],
-                cli_input_buffer,
-                CLI_CONFIG_COMMAND_BUF_SIZE);
-            history_index = (history_index + 1) % CLI_CONFIG_HISTORY_LENGTH;
-            last_history_index =
-                (last_history_index + 1) % CLI_CONFIG_HISTORY_LENGTH;
-        }
-
-        /* Splitting up command into individual argument strings. */
-        status = cli_split(
-            cli_args,
-            CLI_CONFIG_MAX_NUM_ARGUMENTS,
-            cli_input_buffer,
-            command_length,
-            " ");
-        if (status != FWK_SUCCESS) {
-            cli_error_handler(status);
-            continue;
-        }
-
-        /* If the user didn't type any valid arguments, don't process it. */
-        if (cli_args[0] == 0)
-            continue;
-
-        /* Dispatching command for processing. */
-        status = cli_command_dispatch(cli_args);
-        if (status != FWK_SUCCESS)
-            cli_error_handler(status);
+	/* Get command from terminal UART. */
+	status = cli_get_command(
+	    cli_input_buffer,
+	    CLI_CONFIG_COMMAND_BUF_SIZE - 1,
+	    &command_length,
+	    cli_history,
+	    history_index,
+	    CLI_CONFIG_HISTORY_LENGTH,
+	    &cli_exit);
+	if (status != FWK_SUCCESS) {
+	    cli_error_handler(status);
+	    continue;
+	}
+
+	/* Ctrl+d was pressed to exit cli */
+	if (cli_exit) {
+	    cli_state = CLI_READY;
+	    return;
+	}
+
+	/* Make sure command string is not empty. */
+	if (cli_input_buffer[0] == 0)
+	    continue;
+
+
+	/* Update history buffer if command is different than the last one. */
+	if (cli_strncmp(
+		cli_input_buffer,
+		cli_history[last_history_index],
+		CLI_CONFIG_COMMAND_BUF_SIZE) != 0) {
+	    strncpy(
+		cli_history[history_index],
+		cli_input_buffer,
+		CLI_CONFIG_COMMAND_BUF_SIZE);
+	    history_index = (history_index + 1) % CLI_CONFIG_HISTORY_LENGTH;
+	    last_history_index =
+		(last_history_index + 1) % CLI_CONFIG_HISTORY_LENGTH;
+	}
+
+	/* Splitting up command into individual argument strings. */
+	status = cli_split(
+	    cli_args,
+	    CLI_CONFIG_MAX_NUM_ARGUMENTS,
+	    cli_input_buffer,
+	    command_length,
+	    " ");
+	if (status != FWK_SUCCESS) {
+	    cli_error_handler(status);
+	    continue;
+	}
+
+	/* If the user didn't type any valid arguments, don't process it. */
+	if (cli_args[0] == 0)
+	    continue;
+
+	/* Dispatching command for processing. */
+	status = cli_command_dispatch(cli_args);
+	if (status != FWK_SUCCESS)
+	    cli_error_handler(status);
     }
 }
 
@@ -760,152 +760,152 @@ uint32_t cli_format(cli_option_et option
     static char tmp_buf[10] = { 0 };
 
     if (buffer != NULL)
-        /* Add a null terminator before we do anything. */
-        buffer[0] = '\0';
+	/* Add a null terminator before we do anything. */
+	buffer[0] = '\0';
 
     if (automation_mode)
-        return status;
+	return status;
 
     /* If no options given, send SGR default sequence to remove formatting. */
     if (options == NONE) {
-        if (buffer != NULL) {
-            if (size < 5)
-                return FWK_E_NOMEM;
-            memcpy(buffer, "\x1B[0m", 5);
-            buffer = &(buffer[4]);
-            size = size - 4;
-        } else
-            return cli_print("\x1B[0m");
+	if (buffer != NULL) {
+	    if (size < 5)
+		return FWK_E_NOMEM;
+	    memcpy(buffer, "\x1B[0m", 5);
+	    buffer = &(buffer[4]);
+	    size = size - 4;
+	} else
+	    return cli_print("\x1B[0m");
     }
     /* Clear terminal window. */
     if (options & CLEAR_DISPLAY) {
-        if (buffer != NULL) {
-            if (size < 5)
-                return FWK_E_NOMEM;
-            memcpy(buffer, "\x1B[2J", 5);
-            buffer = &(buffer[4]);
-            size = size - 4;
-        } else
-            status = cli_print("\x1B[2J");
+	if (buffer != NULL) {
+	    if (size < 5)
+		return FWK_E_NOMEM;
+	    memcpy(buffer, "\x1B[2J", 5);
+	    buffer = &(buffer[4]);
+	    size = size - 4;
+	} else
+	    status = cli_print("\x1B[2J");
 
-        if (status != FWK_SUCCESS)
-            return status;
+	if (status != FWK_SUCCESS)
+	    return status;
     }
     /* Reset cursor. */
     if (options & RESET_CURSOR) {
-        if (buffer != NULL) {
-            if (size < 7)
-                return FWK_E_NOMEM;
-
-            memcpy(buffer, "\x1B[0;0f", 7);
-            buffer = &(buffer[6]);
-            size = size - 6;
-        } else
-            status = cli_print("\x1B[0;0f");
+	if (buffer != NULL) {
+	    if (size < 7)
+		return FWK_E_NOMEM;
+
+	    memcpy(buffer, "\x1B[0;0f", 7);
+	    buffer = &(buffer[6]);
+	    size = size - 6;
+	} else
+	    status = cli_print("\x1B[0;0f");
 
-        if (status != FWK_SUCCESS)
-            return status;
+	if (status != FWK_SUCCESS)
+	    return status;
     }
     /* SGR settings. */
     if (options & CLI_ALL_MASK) {
-        if (buffer != NULL) {
-            if (size < 3)
-                return FWK_E_NOMEM;
-            memcpy(buffer, "\x1B[", 3);
-            buffer = &(buffer[2]);
-            size = size - 2;
-        } else {
-            status = cli_print("\x1B[");
-            if (status != FWK_SUCCESS)
-                return status;
-        }
-        /* Bold/bright. */
-        if (options & BOLD) {
-            if (buffer != NULL) {
-                if (size < 3)
-                    return FWK_E_NOMEM;
-
-                memcpy(buffer, ";1", 3);
-                buffer = &(buffer[2]);
-                size = size - 2;
-            } else {
-                status = cli_print(";1");
-                if (status != FWK_SUCCESS)
-                    return status;
-            }
-        }
-        /* Underlining. */
-        if (options & UNDERLINE) {
-            if (buffer != NULL) {
-                if (size < 3)
-                    return FWK_E_NOMEM;
-                memcpy(buffer, ";4", 3);
-                buffer = &(buffer[2]);
-                size = size - 2;
-            } else {
-                status = cli_print(";4");
-                if (status != FWK_SUCCESS)
-                    return status;
-            }
-        }
-        /* Background color. */
-        if (options & CLI_BG_COLOR_MASK) {
-            if (buffer != NULL) {
-                if (size < 4)
-                    return FWK_E_NOMEM;
-
-                cli_snprintf(
-                    tmp_buf,
-                    tmp_buf + 10,
-                    (const char *)";%d",
-                    (uint16_t)(
-                        (options & CLI_BG_COLOR_MASK) >> CLI_BG_COLOR_SHIFT));
-                memcpy(buffer, tmp_buf, 4);
-                buffer = &(buffer[3]);
-                size = size - 3;
-            } else {
-                status = cli_printf(
-                    0,
-                    ";%d",
-                    (uint16_t)(
-                        (options & CLI_BG_COLOR_MASK) >> CLI_BG_COLOR_SHIFT));
-                if (status != FWK_SUCCESS)
-                    return status;
-            }
-        }
-        /* Foreground color. */
-        if (options & CLI_TEXT_COLOR_MASK) {
-            if (buffer != NULL) {
-                if (size < 4)
-                    return FWK_E_NOMEM;
-                cli_snprintf(
-                    tmp_buf,
-                    tmp_buf + 10,
-                    (const char *)";%d",
-                    (char)((options & CLI_TEXT_COLOR_MASK) >>
-                        CLI_TEXT_COLOR_SHIFT));
-                memcpy(buffer, tmp_buf, 4);
-                buffer = &(buffer[3]);
-                size = size - 3;
-            } else {
-                status = cli_printf(
-                    0,
-                    ";%d",
-                    (char)((options & CLI_TEXT_COLOR_MASK) >>
-                        CLI_TEXT_COLOR_SHIFT));
-                if (status != FWK_SUCCESS)
-                    return status;
-            }
-        }
-        if (buffer != NULL) {
-            if (size < 2)
-                return FWK_E_NOMEM;
-            memcpy(buffer, "m", 2);
-        } else {
-            status = cli_print("m");
-            if (status != FWK_SUCCESS)
-                return status;
-        }
+	if (buffer != NULL) {
+	    if (size < 3)
+		return FWK_E_NOMEM;
+	    memcpy(buffer, "\x1B[", 3);
+	    buffer = &(buffer[2]);
+	    size = size - 2;
+	} else {
+	    status = cli_print("\x1B[");
+	    if (status != FWK_SUCCESS)
+		return status;
+	}
+	/* Bold/bright. */
+	if (options & BOLD) {
+	    if (buffer != NULL) {
+		if (size < 3)
+		    return FWK_E_NOMEM;
+
+		memcpy(buffer, ";1", 3);
+		buffer = &(buffer[2]);
+		size = size - 2;
+	    } else {
+		status = cli_print(";1");
+		if (status != FWK_SUCCESS)
+		    return status;
+	    }
+	}
+	/* Underlining. */
+	if (options & UNDERLINE) {
+	    if (buffer != NULL) {
+		if (size < 3)
+		    return FWK_E_NOMEM;
+		memcpy(buffer, ";4", 3);
+		buffer = &(buffer[2]);
+		size = size - 2;
+	    } else {
+		status = cli_print(";4");
+		if (status != FWK_SUCCESS)
+		    return status;
+	    }
+	}
+	/* Background color. */
+	if (options & CLI_BG_COLOR_MASK) {
+	    if (buffer != NULL) {
+		if (size < 4)
+		    return FWK_E_NOMEM;
+
+		cli_snprintf(
+		    tmp_buf,
+		    tmp_buf + 10,
+		    (const char *)";%d",
+		    (uint16_t)(
+			(options & CLI_BG_COLOR_MASK) >> CLI_BG_COLOR_SHIFT));
+		memcpy(buffer, tmp_buf, 4);
+		buffer = &(buffer[3]);
+		size = size - 3;
+	    } else {
+		status = cli_printf(
+		    0,
+		    ";%d",
+		    (uint16_t)(
+			(options & CLI_BG_COLOR_MASK) >> CLI_BG_COLOR_SHIFT));
+		if (status != FWK_SUCCESS)
+		    return status;
+	    }
+	}
+	/* Foreground color. */
+	if (options & CLI_TEXT_COLOR_MASK) {
+	    if (buffer != NULL) {
+		if (size < 4)
+		    return FWK_E_NOMEM;
+		cli_snprintf(
+		    tmp_buf,
+		    tmp_buf + 10,
+		    (const char *)";%d",
+		    (char)((options & CLI_TEXT_COLOR_MASK) >>
+			CLI_TEXT_COLOR_SHIFT));
+		memcpy(buffer, tmp_buf, 4);
+		buffer = &(buffer[3]);
+		size = size - 3;
+	    } else {
+		status = cli_printf(
+		    0,
+		    ";%d",
+		    (char)((options & CLI_TEXT_COLOR_MASK) >>
+			CLI_TEXT_COLOR_SHIFT));
+		if (status != FWK_SUCCESS)
+		    return status;
+	    }
+	}
+	if (buffer != NULL) {
+	    if (size < 2)
+		return FWK_E_NOMEM;
+	    memcpy(buffer, "m", 2);
+	} else {
+	    status = cli_print("m");
+	    if (status != FWK_SUCCESS)
+		return status;
+	}
     }
 
     return FWK_SUCCESS;
@@ -933,190 +933,190 @@ static uint32_t cli_get_command(
 
     /* Checking parameters. */
     if (buffer == 0 || buffer_size == 0 || history == 0 ||
-        command_length == 0)
-        return FWK_E_PARAM;
+	command_length == 0)
+	return FWK_E_PARAM;
 
     if (!automation_mode) {
-        /* Getting terminal window size. */
-        /* Saving cursor position. */
-        cli_print("\x1B[s");
-        /* Moving cursor to bottom right position. */
-        cli_print("\x1B[999;999f");
-        /* Requesting new cursor position. */
-        cli_print("\x1B[6n");
-        /* Restoring old cursor position. */
-        cli_print("\x1B[u");
+	/* Getting terminal window size. */
+	/* Saving cursor position. */
+	cli_print("\x1B[s");
+	/* Moving cursor to bottom right position. */
+	cli_print("\x1B[999;999f");
+	/* Requesting new cursor position. */
+	cli_print("\x1B[6n");
+	/* Restoring old cursor position. */
+	cli_print("\x1B[u");
     }
 
     while (1) {
-        /* Get character from UART. */
-        do {
-            status = fwk_io_getch(fwk_io_stdin, &c);
-        } while (status == FWK_PENDING);
-
-        if (status != FWK_SUCCESS)
-            return status;
-
-        /* If we received a Ctrl+C press, go to debug mode. */
-        if (c == '\x03') {
-            *command_length = 0;
-            return cli_debug_output();
-        }
-
-        /* If we received a Ctrl+d press, exit cli. */
-        if (c == '\x04') {
-            *exit = true;
-            return FWK_SUCCESS;
-        }
-
-        /* Ignoring non-printing characters except for a few we care about. */
-        if (c < 0x20 && c != '\r' && c != '\n' && c != '\b') {
-            if (c == 0x1B)
-                flag_escape_sequence = true;
-            continue;
-        }
-
-        /* Dealing with escape sequences. */
-        if (flag_escape_sequence == true) {
-            escape[escape_index] = c;
-            escape[escape_index + 1] = 0;
-            escape_index = escape_index + 1;
-
-            /* Escape sequences end with a letter. */
-            if ((c > 0x40 && c < 0x5B) || (c > 0x60 && c < 0x7B)) {
-                flag_escape_sequence = false;
-                escape_index = 0;
-
-                /* Up arrow press. */
-                if (c == 'A') {
-                    if (((history_oldest + 1) % history_size) !=
-                        history_index) {
-                        /* Rewind history index by 1. */
-                        if (history_index == 0)
-                            history_index = history_size - 1;
-                        else
-                            history_index = history_index - 1;
-
-                        /* If command entry is empty then stop and restore index
-                         * value. */
-                        if (history[history_index][0] == 0) {
-                            history_index = (history_index + 1) % history_size;
-                        } else {
-                            /* Erasing currently entered command. */
-                            for (; index > 0; index--) {
-                                status = cli_backspace(
-                                    &cursor_index, cli_terminal_width);
-                                if (status != FWK_SUCCESS)
-                                    return status;
-                            }
-
-                            /* Copying history command from history buffer. */
-                            strncpy(
-                                buffer, history[history_index], buffer_size);
-
-                            /* Printing history command to screen. */
-                            while (buffer[index] != 0) {
-                                status =
-                                    fwk_io_putch(fwk_io_stdout, buffer[index]);
-                                if (status != FWK_SUCCESS)
-                                    return status;
-                                index = index + 1;
-                                cursor_index =
-                                    (cursor_index + 1) % cli_terminal_width;
-                            }
-                        }
-                    }
-                }
-
-                /* Down arrow press. */
-                if (c == 'B') {
-                    if (history_index != history_oldest) {
-                        /* Getting index of history item to load. */
-                        history_index = (history_index + 1) % history_size;
-
-                        /* Erasing everything in the currently entered command.
-                         */
-                        for (; index > 0; index--) {
-                            status = cli_backspace(
-                                &cursor_index, cli_terminal_width);
-                            if (status != FWK_SUCCESS)
-                                return status;
-                        }
-
-                        /* If we're back to the current command start fresh and
-                         * zero buffer. */
-                        if (history_index == history_oldest)
-                            memset(buffer, 0, buffer_size);
-                        else {
-                            /* Copying history command from history buffer. */
-                            strncpy(
-                                buffer, history[history_index], buffer_size);
-
-                            /* Printing history command to screen. */
-                            while (buffer[index] != 0) {
-                                status =
-                                    fwk_io_putch(fwk_io_stdout, buffer[index]);
-                                if (status != FWK_SUCCESS)
-                                    return status;
-
-                                index = index + 1;
-                                cursor_index =
-                                    (cursor_index + 1) % cli_terminal_width;
-                            }
-                        }
-                    }
-                }
-
-                /* Handling cursor position response sequence. */
-                if (c == 'R') {
-                    uint32_t i = 0;
-                    for (i = 0; escape[i] != '['; i++)
-                        ;
-                    i++;
-                    cli_terminal_height = strtoul(&escape[i], NULL, 0);
-                    for (; escape[i] != ';'; i++)
-                        ;
-                    i++;
-                    cli_terminal_width = strtoul(&escape[i], NULL, 0);
-                }
-            }
-            continue;
-        }
-
-        /* If backspace (0x08) or delete (0x7F) character received. */
-        if (c == 0x08 || c == 0x7F) {
-            /* Only accept backspace presses if we're not at the beginning of
-             * the string. */
-            if (index != 0) {
-                status = cli_backspace(&cursor_index, cli_terminal_width);
-                if (status != FWK_SUCCESS)
-                    return status;
-                index = index - 1;
-                buffer[index] = 0;
-            }
-            continue;
-        }
-
-        /* If newline received. */
-        if (c == '\n' || c == '\r') {
-            /* Making sure the rest of the buffer is zero. */
-            memset(&(buffer[index]), 0, buffer_size - index);
-            *command_length = index;
-            return cli_print("\n");
-        }
-
-        /* Printing received character to console. */
-        status = fwk_io_putch(fwk_io_stdout, c);
-        if (status != FWK_SUCCESS)
-            return status;
-
-        /* Incrementing indices. */
-        buffer[index] = c;
-        index = index + 1;
-        if (index >= buffer_size)
-            return FWK_E_NOMEM;
-        buffer[index] = 0;
-        cursor_index = (cursor_index + 1) % cli_terminal_width;
+	/* Get character from UART. */
+	do {
+	    status = fwk_io_getch(fwk_io_stdin, &c);
+	} while (status == FWK_PENDING);
+
+	if (status != FWK_SUCCESS)
+	    return status;
+
+	/* If we received a Ctrl+C press, go to debug mode. */
+	if (c == '\x03') {
+	    *command_length = 0;
+	    return cli_debug_output();
+	}
+
+	/* If we received a Ctrl+d press, exit cli. */
+	if (c == '\x04') {
+	    *exit = true;
+	    return FWK_SUCCESS;
+	}
+
+	/* Ignoring non-printing characters except for a few we care about. */
+	if (c < 0x20 && c != '\r' && c != '\n' && c != '\b') {
+	    if (c == 0x1B)
+		flag_escape_sequence = true;
+	    continue;
+	}
+
+	/* Dealing with escape sequences. */
+	if (flag_escape_sequence == true) {
+	    escape[escape_index] = c;
+	    escape[escape_index + 1] = 0;
+	    escape_index = escape_index + 1;
+
+	    /* Escape sequences end with a letter. */
+	    if ((c > 0x40 && c < 0x5B) || (c > 0x60 && c < 0x7B)) {
+		flag_escape_sequence = false;
+		escape_index = 0;
+
+		/* Up arrow press. */
+		if (c == 'A') {
+		    if (((history_oldest + 1) % history_size) !=
+			history_index) {
+			/* Rewind history index by 1. */
+			if (history_index == 0)
+			    history_index = history_size - 1;
+			else
+			    history_index = history_index - 1;
+
+			/* If command entry is empty then stop and restore index
+			 * value. */
+			if (history[history_index][0] == 0) {
+			    history_index = (history_index + 1) % history_size;
+			} else {
+			    /* Erasing currently entered command. */
+			    for (; index > 0; index--) {
+				status = cli_backspace(
+				    &cursor_index, cli_terminal_width);
+				if (status != FWK_SUCCESS)
+				    return status;
+			    }
+
+			    /* Copying history command from history buffer. */
+			    strncpy(
+				buffer, history[history_index], buffer_size);
+
+			    /* Printing history command to screen. */
+			    while (buffer[index] != 0) {
+				status =
+				    fwk_io_putch(fwk_io_stdout, buffer[index]);
+				if (status != FWK_SUCCESS)
+				    return status;
+				index = index + 1;
+				cursor_index =
+				    (cursor_index + 1) % cli_terminal_width;
+			    }
+			}
+		    }
+		}
+
+		/* Down arrow press. */
+		if (c == 'B') {
+		    if (history_index != history_oldest) {
+			/* Getting index of history item to load. */
+			history_index = (history_index + 1) % history_size;
+
+			/* Erasing everything in the currently entered command.
+			 */
+			for (; index > 0; index--) {
+			    status = cli_backspace(
+				&cursor_index, cli_terminal_width);
+			    if (status != FWK_SUCCESS)
+				return status;
+			}
+
+			/* If we're back to the current command start fresh and
+			 * zero buffer. */
+			if (history_index == history_oldest)
+			    memset(buffer, 0, buffer_size);
+			else {
+			    /* Copying history command from history buffer. */
+			    strncpy(
+				buffer, history[history_index], buffer_size);
+
+			    /* Printing history command to screen. */
+			    while (buffer[index] != 0) {
+				status =
+				    fwk_io_putch(fwk_io_stdout, buffer[index]);
+				if (status != FWK_SUCCESS)
+				    return status;
+
+				index = index + 1;
+				cursor_index =
+				    (cursor_index + 1) % cli_terminal_width;
+			    }
+			}
+		    }
+		}
+
+		/* Handling cursor position response sequence. */
+		if (c == 'R') {
+		    uint32_t i = 0;
+		    for (i = 0; escape[i] != '['; i++)
+			;
+		    i++;
+		    cli_terminal_height = strtoul(&escape[i], NULL, 0);
+		    for (; escape[i] != ';'; i++)
+			;
+		    i++;
+		    cli_terminal_width = strtoul(&escape[i], NULL, 0);
+		}
+	    }
+	    continue;
+	}
+
+	/* If backspace (0x08) or delete (0x7F) character received. */
+	if (c == 0x08 || c == 0x7F) {
+	    /* Only accept backspace presses if we're not at the beginning of
+	     * the string. */
+	    if (index != 0) {
+		status = cli_backspace(&cursor_index, cli_terminal_width);
+		if (status != FWK_SUCCESS)
+		    return status;
+		index = index - 1;
+		buffer[index] = 0;
+	    }
+	    continue;
+	}
+
+	/* If newline received. */
+	if (c == '\n' || c == '\r') {
+	    /* Making sure the rest of the buffer is zero. */
+	    memset(&(buffer[index]), 0, buffer_size - index);
+	    *command_length = index;
+	    return cli_print("\n");
+	}
+
+	/* Printing received character to console. */
+	status = fwk_io_putch(fwk_io_stdout, c);
+	if (status != FWK_SUCCESS)
+	    return status;
+
+	/* Incrementing indices. */
+	buffer[index] = c;
+	index = index + 1;
+	if (index >= buffer_size)
+	    return FWK_E_NOMEM;
+	buffer[index] = 0;
+	cursor_index = (cursor_index + 1) % cli_terminal_width;
     }
 }
 
@@ -1126,16 +1126,16 @@ static uint32_t cli_backspace(uint32_t *
 
     /* If cursor is at the first position of a line. */
     if (*cursor == 0) {
-        status = cli_printf(0, "\x1B[A\x1B[%dC ", width - 1);
-        if (status != FWK_SUCCESS)
-            return status;
-        *cursor = width - 1;
+	status = cli_printf(0, "\x1B[A\x1B[%dC ", width - 1);
+	if (status != FWK_SUCCESS)
+	    return status;
+	*cursor = width - 1;
     } else {
-        /* For compatibility, print back, space, back. */
-        status = cli_print("\x1B[D \x1B[D");
-        if (status != FWK_SUCCESS)
-            return status;
-        *cursor = *cursor - 1;
+	/* For compatibility, print back, space, back. */
+	status = cli_print("\x1B[D \x1B[D");
+	if (status != FWK_SUCCESS)
+	    return status;
+	*cursor = *cursor - 1;
     }
 
     return FWK_SUCCESS;
@@ -1155,53 +1155,53 @@ static uint32_t cli_split(
 
     /* Checking pointers. */
     if (argbuf == 0 || command == 0 || argbuf_size == 0 || whitespace == 0)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     argbuf[0] = 0;
 
     for (index = 0; index < command_length; index++) {
-        /* If whitespace is encountered outside of parenthesis, change it to
-         * null and set flag. */
-        if (strchr(whitespace, command[index]) != 0 && flag_paren == false) {
-            command[index] = 0;
-            flag_last_was_whitespace = true;
-            continue;
-        }
-
-        /* Handle parenthesis. */
-        if (command[index] == '\"') {
-            /* If we've reached the end of an arg in parenthesis, reset flag. */
-            if (flag_paren == true) {
-                flag_paren = false;
-                command[index] = 0;
-                flag_last_was_whitespace = true;
-                continue;
-            }
-
-            /* If we receive an opening parenthesis preceded by a whitespace
-             * character, mark null and enter parenthesis processing. */
-            if (flag_paren == false && flag_last_was_whitespace == true) {
-                flag_paren = true;
-                command[index] = 0;
-                continue;
-            }
-        }
-
-        /* First regular character after whitespace encountered. */
-        if ((strchr(whitespace, command[index]) == 0 || flag_paren == true) &&
-            flag_last_was_whitespace == true) {
-            flag_last_was_whitespace = false;
-            if (argbuf_ctr + 1 >= argbuf_size)
-                return FWK_E_NOMEM;
-            argbuf[argbuf_ctr] = &command[index];
-            argbuf[argbuf_ctr + 1] = 0;
-            argbuf_ctr = argbuf_ctr + 1;
-        }
+	/* If whitespace is encountered outside of parenthesis, change it to
+	 * null and set flag. */
+	if (strchr(whitespace, command[index]) != 0 && flag_paren == false) {
+	    command[index] = 0;
+	    flag_last_was_whitespace = true;
+	    continue;
+	}
+
+	/* Handle parenthesis. */
+	if (command[index] == '\"') {
+	    /* If we've reached the end of an arg in parenthesis, reset flag. */
+	    if (flag_paren == true) {
+		flag_paren = false;
+		command[index] = 0;
+		flag_last_was_whitespace = true;
+		continue;
+	    }
+
+	    /* If we receive an opening parenthesis preceded by a whitespace
+	     * character, mark null and enter parenthesis processing. */
+	    if (flag_paren == false && flag_last_was_whitespace == true) {
+		flag_paren = true;
+		command[index] = 0;
+		continue;
+	    }
+	}
+
+	/* First regular character after whitespace encountered. */
+	if ((strchr(whitespace, command[index]) == 0 || flag_paren == true) &&
+	    flag_last_was_whitespace == true) {
+	    flag_last_was_whitespace = false;
+	    if (argbuf_ctr + 1 >= argbuf_size)
+		return FWK_E_NOMEM;
+	    argbuf[argbuf_ctr] = &command[index];
+	    argbuf[argbuf_ctr + 1] = 0;
+	    argbuf_ctr = argbuf_ctr + 1;
+	}
     }
 
     /* If the user forgot to close their parenthesis, return an error. */
     if (flag_paren == true)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     return FWK_SUCCESS;
 }
@@ -1217,36 +1217,36 @@ static uint32_t cli_command_dispatch(cha
 
     /* Checking pointer. */
     if (args == 0)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     /* Special case command: help. */
     if (cli_strncmp(args[0], "help", 5) == 0) {
-        FWK_LIST_FOR_EACH(&cli_commands_list, node,
-            struct command_ctx, list_node, cc) {
-                cli_printf(NONE, "%s\n", cc->cmd.command);
-                cli_print(cc->cmd.help);
-                cli_print("\n");
-        }
-
-        cli_printf(NONE, "help\n");
-        cli_print("  Displays this information.\n");
-        cli_printf(NONE, "Ctrl+C\n");
-        cli_print("  Displays debug output from running threads.\n");
-        cli_printf(NONE, "Ctrl+d\n");
-        cli_print("  Exit the CLI.\n");
-        return FWK_SUCCESS;
+	FWK_LIST_FOR_EACH(&cli_commands_list, node,
+	    struct command_ctx, list_node, cc) {
+		cli_printf(NONE, "%s\n", cc->cmd.command);
+		cli_print(cc->cmd.help);
+		cli_print("\n");
+	}
+
+	cli_printf(NONE, "help\n");
+	cli_print("  Displays this information.\n");
+	cli_printf(NONE, "Ctrl+C\n");
+	cli_print("  Displays debug output from running threads.\n");
+	cli_printf(NONE, "Ctrl+d\n");
+	cli_print("  Exit the CLI.\n");
+	return FWK_SUCCESS;
     }
 
     if (cli_strncmp(args[0], "AUTO", 4) == 0) {
-        cli_printf(NONE, "AUTO Mode ON\n");
-        automation_mode = 1;
-        return FWK_SUCCESS;
+	cli_printf(NONE, "AUTO Mode ON\n");
+	automation_mode = 1;
+	return FWK_SUCCESS;
     }
 
     if (cli_strncmp(args[0], "OTUA", 4) == 0) {
-        cli_printf(NONE, "AUTO Mode OFF\n");
-        automation_mode = 0;
-        return FWK_SUCCESS;
+	cli_printf(NONE, "AUTO Mode OFF\n");
+	automation_mode = 0;
+	return FWK_SUCCESS;
     }
 
     /* Searching for command handler. */
@@ -1254,48 +1254,48 @@ static uint32_t cli_command_dispatch(cha
      * terminated by cli_split. */
     command_found = false;
     FWK_LIST_FOR_EACH(&cli_commands_list, node,
-        struct command_ctx, list_node, cc) {
-        if (strcmp(args[0], cc->cmd.command) == 0) {
-            command_found = true;
-            break;
-        }
+	struct command_ctx, list_node, cc) {
+	if (strcmp(args[0], cc->cmd.command) == 0) {
+	    command_found = true;
+	    break;
+	}
     }
     if (!command_found)
-        return FWK_E_SUPPORT;
+	return FWK_E_SUPPORT;
 
     /* Handler found, if -h or --help is given just print it's help string and
      * return. */
     if (cc->cmd.ignore_help_flag == false) {
-        for (index = 1;
-             (args[index] != 0) && (cli_strncmp(args[index], "-h", 3) != 0) &&
-             (cli_strncmp(args[index], "--help", 7) != 0);
-             index++)
-            ;
-
-        if ((args[index] != 0 || cc->cmd.handler == 0) &&
-            cc->cmd.help != 0) {
-            status = cli_print(cc->cmd.help);
-            if (status != FWK_SUCCESS)
-                return status;
-
-            /* Print a newline since help strings shouldn't have newlines at the
-             * end. */
-            cli_print("\n");
-            return status;
-        }
+	for (index = 1;
+	     (args[index] != 0) && (cli_strncmp(args[index], "-h", 3) != 0) &&
+	     (cli_strncmp(args[index], "--help", 7) != 0);
+	     index++)
+	    ;
+
+	if ((args[index] != 0 || cc->cmd.handler == 0) &&
+	    cc->cmd.help != 0) {
+	    status = cli_print(cc->cmd.help);
+	    if (status != FWK_SUCCESS)
+		return status;
+
+	    /* Print a newline since help strings shouldn't have newlines at the
+	     * end. */
+	    cli_print("\n");
+	    return status;
+	}
     }
 
     /* Counting arguments. */
     for (num_args = 0; args[num_args] != 0; num_args++)
-        ;
+	;
 
     /* Calling command handler. */
     /* args is incremented so the command just gets the arguments and not the
      * name of itself. */
     if (cc->cmd.handler != 0)
-        return cc->cmd.handler(num_args, args);
+	return cc->cmd.handler(num_args, args);
     else
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 }
 
 static uint32_t cli_debug_output(void)
@@ -1304,36 +1304,36 @@ static uint32_t cli_debug_output(void)
     char c = 0;
 
     cli_printf(
-        0,
-        "\nNow showing debug console output, to return to the command line, "
-        "press Ctrl+C.\n");
+	0,
+	"\nNow showing debug console output, to return to the command line, "
+	"press Ctrl+C.\n");
     while (1) {
-        /* Looking for Ctrl+C press. */
-        if (fwk_io_getch(fwk_io_stdin, &c) == FWK_SUCCESS) {
-            if (c == '\x03') {
-                cli_printf(
-                    0,
-                    "\nNow showing command line, to return to debug output, "
-                    "press Ctrl+C.\n");
-                return FWK_SUCCESS;
-            }
-        }
-
-        /* Read from print FIFO. */
-        fifo_status = fifo_get(&cli_print_fifo, &c);
-        if (fifo_status == FWK_SUCCESS) {
-            overflow = false;
-            fwk_io_putch(fwk_io_stdout, c);
-        } else
-            /* If no characters are available, let other stuff run. */
-            cli_platform_delay_ms(0);
+	/* Looking for Ctrl+C press. */
+	if (fwk_io_getch(fwk_io_stdin, &c) == FWK_SUCCESS) {
+	    if (c == '\x03') {
+		cli_printf(
+		    0,
+		    "\nNow showing command line, to return to debug output, "
+		    "press Ctrl+C.\n");
+		return FWK_SUCCESS;
+	    }
+	}
+
+	/* Read from print FIFO. */
+	fifo_status = fifo_get(&cli_print_fifo, &c);
+	if (fifo_status == FWK_SUCCESS) {
+	    overflow = false;
+	    fwk_io_putch(fwk_io_stdout, c);
+	} else
+	    /* If no characters are available, let other stuff run. */
+	    cli_platform_delay_ms(0);
     }
 }
 
 static void cli_error_handler(uint32_t status)
 {
     if (status == FWK_SUCCESS)
-        return;
+	return;
 
     cli_printf(NONE, "CONSOLE ERROR: %s\n", fwk_status_str(status));
 }
@@ -1350,21 +1350,21 @@ static void cli_val2str(
     int i = 0;
 
     do {
-        str[i++] = "0123456789ABCDEF"[value % base];
+	str[i++] = "0123456789ABCDEF"[value % base];
     } while (value /= base);
 
     while (--fill >= i) {
-        **outstr = '0';
-        *outstr = *outstr + 1;
-        if (*outstr >= smax)
-            return;
+	**outstr = '0';
+	*outstr = *outstr + 1;
+	if (*outstr >= smax)
+	    return;
     }
 
     while (--i >= 0) {
-        **outstr = str[i];
-        *outstr = *outstr + 1;
-        if (*outstr >= smax)
-            return;
+	**outstr = str[i];
+	*outstr = *outstr + 1;
+	if (*outstr >= smax)
+	    return;
     }
 }
 
@@ -1383,135 +1383,135 @@ static void cli_snprintf_arg(
     char c = 0;
 
     while (*fmt) {
-        if (*fmt == '%') {
-            fmt++;
-            bit64 = 0;
-            fill = 0;
-        /* Check the format specifier */
+	if (*fmt == '%') {
+	    fmt++;
+	    bit64 = 0;
+	    fill = 0;
+	/* Check the format specifier */
 loop:
-            switch (*fmt) {
-            case 'c':
-                c = (char)va_arg(*args, int32_t);
-                *s++ = c;
-                if (s >= smax) {
-                    s[-1] = 0;
-                    return;
-                }
-                break;
-            case 'i': /* Specifiers i and d do the same thing. */
-            case 'd':
-                if (bit64) {
-                    *s = 0;
-                    return;
-                }
-
-                num = va_arg(*args, int32_t);
-
-                if (num < 0) {
-                    *s++ = '-';
-                    if (s >= smax) {
-                        s[-1] = 0;
-                        return;
-                    }
-                    unum = (unsigned long int)-num;
-                } else {
-                    unum = (unsigned long int)num;
-                }
-
-                cli_val2str(&s, smax, unum, 10, fill);
-                if (s >= smax) {
-                    s[-1] = 0;
-                    return;
-                }
-                break;
-            case 's':
-                str = va_arg(*args, char *);
-                while (*str) {
-                    *s++ = *str++;
-                    if (s >= smax) {
-                        s[-1] = 0;
-                        return;
-                    }
-                }
-                break;
-            case 'x': /* All hex prints use uppercase hex digits. */
-            case 'X':
-                if (bit64) {
-                    unum = va_arg(*args, uint64_t);
-                    most_significant = (uint32_t)(unum >> 32);
-                    if (most_significant) {
-                        cli_val2str(
-                            &s,
-                            smax,
-                            most_significant,
-                            16,
-                            (fill >= 8) ? fill - 8 : 0);
-                        if (s >= smax) {
-                            s[-1] = 0;
-                            return;
-                        }
-                        cli_val2str(&s, smax, unum, 16, 8);
-                        if (s >= smax) {
-                            s[-1] = 0;
-                            return;
-                        }
-                    } else {
-                        cli_val2str(&s, smax, unum, 16, fill);
-                        if (s >= smax) {
-                            s[-1] = 0;
-                            return;
-                        }
-                    }
-                } else {
-                    unum = va_arg(*args, uint32_t);
-                    cli_val2str(&s, smax, unum, 16, fill);
-                    if (s >= smax) {
-                        s[-1] = 0;
-                        return;
-                    }
-                }
-                break;
-            case 'l':
-                bit64 = 1;
-                fmt++;
-                goto loop;
-            case 'u':
-                if (bit64) {
-                    *s = 0;
-                    return;
-                }
-
-                unum = va_arg(*args, uint32_t);
-
-                cli_val2str(&s, smax, unum, 10, fill);
-                if (s >= smax) {
-                    s[-1] = 0;
-                    return;
-                }
-                break;
-            case '0':
-                fmt++;
-                /* Make sure we have a number for fill length. */
-                if (((*fmt) < '0') || ((*fmt) > '9')) {
-                    *s = 0;
-                    return;
-                }
-                fill = strtoul((char *)fmt, (char **)&fmt, 0);
-                goto loop;
-            default:
-                /* Exit on any other format specifier */
-                *s = 0;
-                return;
-            }
-            fmt++;
-            continue;
-        }
-
-        *s++ = *fmt++;
-        if (s == smax) {
-            s[-1] = 0;
-            return;
-        }
+	    switch (*fmt) {
+	    case 'c':
+		c = (char)va_arg(*args, int32_t);
+		*s++ = c;
+		if (s >= smax) {
+		    s[-1] = 0;
+		    return;
+		}
+		break;
+	    case 'i': /* Specifiers i and d do the same thing. */
+	    case 'd':
+		if (bit64) {
+		    *s = 0;
+		    return;
+		}
+
+		num = va_arg(*args, int32_t);
+
+		if (num < 0) {
+		    *s++ = '-';
+		    if (s >= smax) {
+			s[-1] = 0;
+			return;
+		    }
+		    unum = (unsigned long int)-num;
+		} else {
+		    unum = (unsigned long int)num;
+		}
+
+		cli_val2str(&s, smax, unum, 10, fill);
+		if (s >= smax) {
+		    s[-1] = 0;
+		    return;
+		}
+		break;
+	    case 's':
+		str = va_arg(*args, char *);
+		while (*str) {
+		    *s++ = *str++;
+		    if (s >= smax) {
+			s[-1] = 0;
+			return;
+		    }
+		}
+		break;
+	    case 'x': /* All hex prints use uppercase hex digits. */
+	    case 'X':
+		if (bit64) {
+		    unum = va_arg(*args, uint64_t);
+		    most_significant = (uint32_t)(unum >> 32);
+		    if (most_significant) {
+			cli_val2str(
+			    &s,
+			    smax,
+			    most_significant,
+			    16,
+			    (fill >= 8) ? fill - 8 : 0);
+			if (s >= smax) {
+			    s[-1] = 0;
+			    return;
+			}
+			cli_val2str(&s, smax, unum, 16, 8);
+			if (s >= smax) {
+			    s[-1] = 0;
+			    return;
+			}
+		    } else {
+			cli_val2str(&s, smax, unum, 16, fill);
+			if (s >= smax) {
+			    s[-1] = 0;
+			    return;
+			}
+		    }
+		} else {
+		    unum = va_arg(*args, uint32_t);
+		    cli_val2str(&s, smax, unum, 16, fill);
+		    if (s >= smax) {
+			s[-1] = 0;
+			return;
+		    }
+		}
+		break;
+	    case 'l':
+		bit64 = 1;
+		fmt++;
+		goto loop;
+	    case 'u':
+		if (bit64) {
+		    *s = 0;
+		    return;
+		}
+
+		unum = va_arg(*args, uint32_t);
+
+		cli_val2str(&s, smax, unum, 10, fill);
+		if (s >= smax) {
+		    s[-1] = 0;
+		    return;
+		}
+		break;
+	    case '0':
+		fmt++;
+		/* Make sure we have a number for fill length. */
+		if (((*fmt) < '0') || ((*fmt) > '9')) {
+		    *s = 0;
+		    return;
+		}
+		fill = strtoul((char *)fmt, (char **)&fmt, 0);
+		goto loop;
+	    default:
+		/* Exit on any other format specifier */
+		*s = 0;
+		return;
+	    }
+	    fmt++;
+	    continue;
+	}
+
+	*s++ = *fmt++;
+	if (s == smax) {
+	    s[-1] = 0;
+	    return;
+	}
     }
     *s = 0;
 }
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/debugger/src/cli/cli_commands_checkpoint.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/debugger/src/cli/cli_commands_checkpoint.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/debugger/src/cli/cli_commands_checkpoint.c
@@ -38,69 +38,69 @@ int32_t checkpoint_f(int32_t argc, char
     uint32_t i = 0;
 
     if ((argc == 2) && (cli_strncmp(argv[1], "list", 4) == 0)) {
-        for (i = 0; i < CHECKPOINT_NUM; i++) {
-            if (checkpoint_table[i].in_use == true)
-                cli_printf(NONE, "%d: %s\n", i, checkpoint_table[i].name);
-        }
-        return FWK_SUCCESS;
+	for (i = 0; i < CHECKPOINT_NUM; i++) {
+	    if (checkpoint_table[i].in_use == true)
+		cli_printf(NONE, "%d: %s\n", i, checkpoint_table[i].name);
+	}
+	return FWK_SUCCESS;
     }
 
     else if ((argc == 4) && (cli_strncmp(argv[2], "skip", 4) == 0)) {
-        id = strtoul(argv[1], NULL, 0);
-        run_cnt = strtoul(argv[3], NULL, 0);
-        if (id >= CHECKPOINT_NUM) {
-            cli_print("CP ID out of range.\n");
-            return FWK_E_PARAM;
-        }
-        checkpoint_table[id].tag[0] = 0;
-        checkpoint_table[id].bypass = run_cnt;
-        return FWK_SUCCESS;
+	id = strtoul(argv[1], NULL, 0);
+	run_cnt = strtoul(argv[3], NULL, 0);
+	if (id >= CHECKPOINT_NUM) {
+	    cli_print("CP ID out of range.\n");
+	    return FWK_E_PARAM;
+	}
+	checkpoint_table[id].tag[0] = 0;
+	checkpoint_table[id].bypass = run_cnt;
+	return FWK_SUCCESS;
     }
 
     else if ((argc == 4) && (cli_strncmp(argv[2], "tag", 3) == 0)) {
-        id = strtoul(argv[1], NULL, 0);
-        if (id >= CHECKPOINT_NUM) {
-            cli_print("Checkpoint number out of range.\n");
-            return FWK_E_PARAM;
-        }
-        strncpy(checkpoint_table[id].tag, argv[3], CHECKPOINT_TAG_LEN);
-        checkpoint_table[id].bypass = CHECKPOINT_DISABLED;
-        return FWK_SUCCESS;
+	id = strtoul(argv[1], NULL, 0);
+	if (id >= CHECKPOINT_NUM) {
+	    cli_print("Checkpoint number out of range.\n");
+	    return FWK_E_PARAM;
+	}
+	strncpy(checkpoint_table[id].tag, argv[3], CHECKPOINT_TAG_LEN);
+	checkpoint_table[id].bypass = CHECKPOINT_DISABLED;
+	return FWK_SUCCESS;
     }
 
     else if ((argc == 3) && (cli_strncmp(argv[2], "enable", 6) == 0)) {
-        id = strtoul(argv[1], NULL, 0);
-        if (id >= CHECKPOINT_NUM) {
-            cli_print("CP ID out of range.\n");
-            return FWK_E_PARAM;
-        }
-        checkpoint_table[id].bypass = CHECKPOINT_ENABLED;
-        return FWK_SUCCESS;
+	id = strtoul(argv[1], NULL, 0);
+	if (id >= CHECKPOINT_NUM) {
+	    cli_print("CP ID out of range.\n");
+	    return FWK_E_PARAM;
+	}
+	checkpoint_table[id].bypass = CHECKPOINT_ENABLED;
+	return FWK_SUCCESS;
     }
 
     else if ((argc == 3) && (cli_strncmp(argv[2], "disable", 7) == 0)) {
-        id = strtoul(argv[1], NULL, 0);
-        if (id >= CHECKPOINT_NUM) {
-            cli_print("CP ID out of range.\n");
-            return FWK_E_PARAM;
-        }
-        checkpoint_table[id].bypass = CHECKPOINT_DISABLED;
-        return FWK_SUCCESS;
+	id = strtoul(argv[1], NULL, 0);
+	if (id >= CHECKPOINT_NUM) {
+	    cli_print("CP ID out of range.\n");
+	    return FWK_E_PARAM;
+	}
+	checkpoint_table[id].bypass = CHECKPOINT_DISABLED;
+	return FWK_SUCCESS;
     }
 
     else if ((argc == 2) && (cli_strncmp(argv[1], "enableall", 9) == 0)) {
-        checkpoint_enable_all();
-        return FWK_SUCCESS;
+	checkpoint_enable_all();
+	return FWK_SUCCESS;
     }
 
     else if ((argc == 2) && (cli_strncmp(argv[1], "disableall", 10) == 0)) {
-        checkpoint_disable_all();
-        return FWK_SUCCESS;
+	checkpoint_disable_all();
+	return FWK_SUCCESS;
     }
 
     cli_print("CLI: Invalid command received:\n");
     for (i = 0; i < (uint32_t)argc; i++)
-        cli_printf(NONE, "Parameter %d is %s\n", i, argv[i]);
+	cli_printf(NONE, "Parameter %d is %s\n", i, argv[i]);
 
     return FWK_E_PARAM;
 }
@@ -111,17 +111,17 @@ void checkpoint_boot_state(uint32_t time
 
     /* Make sure nothing is waiting in UART buffer. */
     while (fwk_io_getch(fwk_io_stdin, &c) == FWK_SUCCESS)
-        ;
+	;
 
     cli_print("Press any key to enable checkpoints before boot... ");
     while (timeout_s != 0) {
-        if (fwk_io_getch(fwk_io_stdin, &c) == FWK_SUCCESS) {
-            cli_print("\nCheckpoints enabled.\n");
-            checkpoint_enable_all();
-            return;
-        }
-        cli_printf(NONE, "%c%d", 0x8, timeout_s);
-        timeout_s = timeout_s - 1;
+	if (fwk_io_getch(fwk_io_stdin, &c) == FWK_SUCCESS) {
+	    cli_print("\nCheckpoints enabled.\n");
+	    checkpoint_enable_all();
+	    return;
+	}
+	cli_printf(NONE, "%c%d", 0x8, timeout_s);
+	timeout_s = timeout_s - 1;
     }
     cli_printf(NONE, "%c0\n", 0x8);
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/debugger/src/cli/cli_commands_core.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/debugger/src/cli/cli_commands_core.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/debugger/src/cli/cli_commands_core.c
@@ -87,33 +87,33 @@ static int32_t dump_memory_f(int32_t arg
 
     /* Sanity check. */
     if (size == 0)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     cli_printf(NONE, "Reading %d bytes from 0x%08x.\n", size, addr);
 
     /* Loop through all bytes. */
     for (i = 0; i < size; i = i + NUM_BYTES_PER_LINE) {
-        /* Read line worth of bytes from EEPROM. */
-        memcpy((void *)bytes, (void *)(addr + i), NUM_BYTES_PER_LINE);
+	/* Read line worth of bytes from EEPROM. */
+	memcpy((void *)bytes, (void *)(addr + i), NUM_BYTES_PER_LINE);
 
-        /* Print line base address. */
-        cli_printf(NONE, "0x%08x", addr + i);
+	/* Print line base address. */
+	cli_printf(NONE, "0x%08x", addr + i);
 
-        /* Print hex bytes. */
-        for (j = 0; j < NUM_BYTES_PER_LINE; j++)
-            cli_printf(NONE, " %02x", bytes[j]);
-
-        /* Print ASCII representation. */
-        cli_print(" \"");
-        for (j = 0; j < NUM_BYTES_PER_LINE; j++) {
-            if ((bytes[j] >= 0x20) && (bytes[j] <= 0x7E))
-                /* Character is printing. */
-                fwk_io_putch(fwk_io_stdout, bytes[j]);
-            else
-                /* Character is non-printing so put a period. */
-                fwk_io_putch(fwk_io_stdout, '.');
-        }
-        cli_print("\"\n");
+	/* Print hex bytes. */
+	for (j = 0; j < NUM_BYTES_PER_LINE; j++)
+	    cli_printf(NONE, " %02x", bytes[j]);
+
+	/* Print ASCII representation. */
+	cli_print(" \"");
+	for (j = 0; j < NUM_BYTES_PER_LINE; j++) {
+	    if ((bytes[j] >= 0x20) && (bytes[j] <= 0x7E))
+		/* Character is printing. */
+		fwk_io_putch(fwk_io_stdout, bytes[j]);
+	    else
+		/* Character is non-printing so put a period. */
+		fwk_io_putch(fwk_io_stdout, '.');
+	}
+	cli_print("\"\n");
     }
 
     return FWK_SUCCESS;
@@ -144,30 +144,30 @@ static int32_t cycle_memory_f(int32_t ar
 
     /* Sanity check. */
     if ((size == 0) || (size > MAX_CYCLE_BUFFER_SZ))
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     memset((void *)cycle_buffer, 0, sizeof(uint32_t) * MAX_CYCLE_BUFFER_SZ);
 
     cli_printf(
-        NONE,
-        "Cycle Compare Starts for 0x%08x for Length 0x%08x for iterations "
-        "%d.\n",
-        addr,
-        size,
-        iterations);
+	NONE,
+	"Cycle Compare Starts for 0x%08x for Length 0x%08x for iterations "
+	"%d.\n",
+	addr,
+	size,
+	iterations);
 
     /* Snapshot the current state */
     for (index = 0; index < size; index++)
-        cycle_buffer[index] = tmp_address[index];
+	cycle_buffer[index] = tmp_address[index];
 
     /* Loop through all bytes. */
     for (cycle_count = 0; cycle_count < iterations; cycle_count++) {
-        for (index = 0; index < size; index++) {
-            if (tmp_address[index] != cycle_buffer[index]) {
-                cycle_buffer[index] = tmp_address[index];
-                deviation_count++;
-            }
-        }
+	for (index = 0; index < size; index++) {
+	    if (tmp_address[index] != cycle_buffer[index]) {
+		cycle_buffer[index] = tmp_address[index];
+		deviation_count++;
+	    }
+	}
     }
 
     cli_printf(NONE, "Cycle Compare Deviation Count %d \r\n", deviation_count);
@@ -188,7 +188,7 @@ static int32_t read_memory_f(int32_t arg
 {
     /* Checking for the correct number of arguments. */
     if (argc != 3)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     /* Getting address of access. */
     uintptr_t address = (uintptr_t)strtoul(argv[1], 0, 0);
@@ -196,36 +196,36 @@ static int32_t read_memory_f(int32_t arg
     /* Getting width of access and making sure it is valid. */
     uint32_t width = strtoul(argv[2], 0, 0);
     if (width != 1 && width != 2 && width != 4 && width != 8)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     /* Switching based on width. */
     cli_print("Value: 0x");
     switch (width) {
     case 1:
-        /* No boundary restrictions on single byte accesses. */
-        cli_printf(NONE, "%02x", *((uint8_t *)address));
-        break;
+	/* No boundary restrictions on single byte accesses. */
+	cli_printf(NONE, "%02x", *((uint8_t *)address));
+	break;
     case 2:
-        if (address % 2) {
-            /* 16 bit accesses need to be aligned on 2 byte boundaries. */
-            return FWK_E_PARAM;
-        }
-        cli_printf(NONE, "%04x", *((uint16_t *)address));
-        break;
+	if (address % 2) {
+	    /* 16 bit accesses need to be aligned on 2 byte boundaries. */
+	    return FWK_E_PARAM;
+	}
+	cli_printf(NONE, "%04x", *((uint16_t *)address));
+	break;
     case 4:
-        if (address % 4) {
-            /* 32 bit accesses need to be aligned to 4 byte boundaries. */
-            return FWK_E_PARAM;
-        }
-        cli_printf(NONE, "%08x", *((uint32_t *)address));
-        break;
+	if (address % 4) {
+	    /* 32 bit accesses need to be aligned to 4 byte boundaries. */
+	    return FWK_E_PARAM;
+	}
+	cli_printf(NONE, "%08x", *((uint32_t *)address));
+	break;
     case 8:
-        if (address % 4) {
-            /* 64 bit accesses need to be aligned on 4 byte boundaries. */
-            return FWK_E_PARAM;
-        }
-        cli_printf(NONE, "%016lx", *((uint64_t *)address));
-        break;
+	if (address % 4) {
+	    /* 64 bit accesses need to be aligned on 4 byte boundaries. */
+	    return FWK_E_PARAM;
+	}
+	cli_printf(NONE, "%016lx", *((uint64_t *)address));
+	break;
     }
 
     cli_print("\n");
@@ -246,7 +246,7 @@ static int32_t write_memory_f(int32_t ar
 {
     /* Checking for the correct number of arguments. */
     if (argc != 4)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     /* Getting address of access. */
     uintptr_t address = (uintptr_t)strtoul(argv[1], 0, 0);
@@ -254,35 +254,35 @@ static int32_t write_memory_f(int32_t ar
     /* Getting width of access and making sure it is valid. */
     uint32_t width = strtoul(argv[2], 0, 0);
     if (width != 1 && width != 2 && width != 4 && width != 8)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     /* Switching based on width. */
     switch (width) {
     case 1:
-        /* No boundary restrictions on single byte accesses. */
-        *((uint8_t *)address) = (uint8_t)strtoul(argv[3], 0, 0);
-        break;
+	/* No boundary restrictions on single byte accesses. */
+	*((uint8_t *)address) = (uint8_t)strtoul(argv[3], 0, 0);
+	break;
     case 2:
-        if (address % 2) {
-            /* 16 bit accesses need to be aligned on 2 byte boundaries. */
-            return FWK_E_PARAM;
-        }
-        *((uint16_t *)address) = (uint16_t)strtoul(argv[3], 0, 0);
-        break;
+	if (address % 2) {
+	    /* 16 bit accesses need to be aligned on 2 byte boundaries. */
+	    return FWK_E_PARAM;
+	}
+	*((uint16_t *)address) = (uint16_t)strtoul(argv[3], 0, 0);
+	break;
     case 4:
-        if (address % 4) {
-            /* 32 bit accesses need to be aligned to 4 byte boundaries. */
-            return FWK_E_PARAM;
-        }
-        *((uint32_t *)address) = (uint32_t)strtoul(argv[3], 0, 0);
-        break;
+	if (address % 4) {
+	    /* 32 bit accesses need to be aligned to 4 byte boundaries. */
+	    return FWK_E_PARAM;
+	}
+	*((uint32_t *)address) = (uint32_t)strtoul(argv[3], 0, 0);
+	break;
     case 8:
-        if (address % 4) {
-            /* 64 bit accesses need to be aligned on 4 byte boundaries. */
-            return FWK_E_PARAM;
-        }
-        *((uint64_t *)address) = (uint64_t)strtoull(argv[3], 0, 0);
-        break;
+	if (address % 4) {
+	    /* 64 bit accesses need to be aligned on 4 byte boundaries. */
+	    return FWK_E_PARAM;
+	}
+	*((uint64_t *)address) = (uint64_t)strtoull(argv[3], 0, 0);
+	break;
     }
 
     return FWK_SUCCESS;
@@ -299,13 +299,13 @@ static int32_t uptime_f(int32_t argc, ch
     cli_timestamp_t t = { 0 };
     cli_platform_get_time(&t);
     cli_printf(
-        NONE,
-        "System Uptime: %02d:%02d:%02d:%02d.%02d\n",
-        t.days,
-        t.hours,
-        t.minutes,
-        t.seconds,
-        t.fraction);
+	NONE,
+	"System Uptime: %02d:%02d:%02d:%02d.%02d\n",
+	t.days,
+	t.hours,
+	t.minutes,
+	t.seconds,
+	t.fraction);
     return FWK_SUCCESS;
 }
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/debugger/src/cli/cli_fifo.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/debugger/src/cli/cli_fifo.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/debugger/src/cli/cli_fifo.c
@@ -14,7 +14,7 @@
 uint32_t fifo_init(fifo_st *fifo, char *buf, uint32_t buf_size)
 {
     if (fifo == 0 || buf == 0 || buf_size == 0)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     fifo->get_ptr = 0;
     fifo->put_ptr = 0;
@@ -30,19 +30,19 @@ uint32_t fifo_get(fifo_st *fifo, char *v
 {
     /* Checking parameters. */
     if (fifo == 0 || val == 0)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     /* Making sure FIFO isn't empty. */
     if (fifo->get_ptr != fifo->put_ptr) {
-        *val = fifo->buf[fifo->get_ptr];
-        fifo->get_ptr = (fifo->get_ptr + 1) % fifo->buf_size;
-        fifo->count = fifo->count - 1;
-        /* Tracking FIFO high-water mark. */
-        if ((fifo->reset_high_water == true) && (fifo->count == 0)) {
-            fifo->high_water = 0;
-            fifo->reset_high_water = false;
-        }
-        return FWK_SUCCESS;
+	*val = fifo->buf[fifo->get_ptr];
+	fifo->get_ptr = (fifo->get_ptr + 1) % fifo->buf_size;
+	fifo->count = fifo->count - 1;
+	/* Tracking FIFO high-water mark. */
+	if ((fifo->reset_high_water == true) && (fifo->count == 0)) {
+	    fifo->high_water = 0;
+	    fifo->reset_high_water = false;
+	}
+	return FWK_SUCCESS;
     }
 
     return FWK_E_DATA;
@@ -52,20 +52,20 @@ uint32_t fifo_put(fifo_st *fifo, char *v
 {
     /* Checking parameters. */
     if (fifo == 0 || val == 0)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     uint32_t newPutPtr = (fifo->put_ptr + 1) % fifo->buf_size;
 
     /* Making sure FIFO isn't full. */
     if (newPutPtr != fifo->get_ptr) {
-        fifo->buf[fifo->put_ptr] = *val;
-        fifo->put_ptr = newPutPtr;
-        fifo->count = fifo->count + 1;
-        /* Tracking FIFO high-water mark. */
-        if (fifo->count > fifo->high_water && fifo->reset_high_water == false)
-            fifo->high_water = fifo->count;
+	fifo->buf[fifo->put_ptr] = *val;
+	fifo->put_ptr = newPutPtr;
+	fifo->count = fifo->count + 1;
+	/* Tracking FIFO high-water mark. */
+	if (fifo->count > fifo->high_water && fifo->reset_high_water == false)
+	    fifo->high_water = fifo->count;
 
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     return FWK_E_NOMEM;
@@ -75,7 +75,7 @@ uint32_t fifo_free_space(fifo_st *fifo)
 {
     /* Checking parameters. */
     if (fifo == 0)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     return fifo->buf_size - fifo->count - 1;
 }
@@ -84,7 +84,7 @@ uint32_t fifo_count(fifo_st *fifo)
 {
     /* Checking parameters. */
     if (fifo == 0)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     return fifo->count;
 }
@@ -93,7 +93,7 @@ uint32_t fifo_capacity(fifo_st *fifo)
 {
     /* Checking parameters. */
     if (fifo == 0)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     return fifo->buf_size - 1;
 }
@@ -102,7 +102,7 @@ uint32_t fifo_high_water(fifo_st *fifo)
 {
     /* Checking parameters. */
     if (fifo == 0)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     return fifo->high_water;
 }
@@ -111,7 +111,7 @@ uint32_t fifo_high_water_reset(fifo_st *
 {
     /* Checking parameters. */
     if (fifo == 0)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     fifo->reset_high_water = true;
     return FWK_SUCCESS;
@@ -121,7 +121,7 @@ uint32_t fifo_empty(fifo_st *fifo)
 {
     /* Check for valid pointer. */
     if (fifo == 0)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     fifo->get_ptr = 0;
     fifo->put_ptr = 0;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/doc/Doxyfile
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/doc/Doxyfile
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/doc/Doxyfile
@@ -119,16 +119,16 @@ REPEAT_BRIEF           = YES
 # specifies, contains, represents, a, an and the.
 
 ABBREVIATE_BRIEF       = "The $name class" \
-                         "The $name widget" \
-                         "The $name file" \
-                         is \
-                         provides \
-                         specifies \
-                         contains \
-                         represents \
-                         a \
-                         an \
-                         the
+			 "The $name widget" \
+			 "The $name file" \
+			 is \
+			 provides \
+			 specifies \
+			 contains \
+			 represents \
+			 a \
+			 an \
+			 the
 
 # If the ALWAYS_DETAILED_SEC and REPEAT_BRIEF tags are both set to YES then
 # doxygen will generate a detailed section even if there is only a brief
@@ -791,22 +791,22 @@ WARN_LOGFILE           =
 # Note: If this tag is empty the current directory is searched.
 
 INPUT                  = $(TOP_DIR)/readme.md \
-                         $(TOP_DIR)/license.md \
-                         $(TOP_DIR)/user_guide.md \
-                         $(TOP_DIR)/change_log.md \
-                         $(DOC_DIR)/build_system.md \
-                         $(DOC_DIR)/cmake_readme.md \
-                         $(DOC_DIR)/code_style.md \
-                         $(DOC_DIR)/code_rules.md \
-                         $(DOC_DIR)/framework.md \
-                         $(DOC_DIR)/cmsis.md \
-                         $(DOC_DIR)/glossary.md \
-                         $(DOC_DIR)/deferred_response_architecture.md \
-                         $(DOC_DIR)/architecture_support.md \
-                         $(FWK_DIR)/include \
-                         $(FWK_DIR)/test/fwk_test.h \
-                         $(MODULE_INCLUDES) \
-                         $(MODULE_DOCS)
+			 $(TOP_DIR)/license.md \
+			 $(TOP_DIR)/user_guide.md \
+			 $(TOP_DIR)/change_log.md \
+			 $(DOC_DIR)/build_system.md \
+			 $(DOC_DIR)/cmake_readme.md \
+			 $(DOC_DIR)/code_style.md \
+			 $(DOC_DIR)/code_rules.md \
+			 $(DOC_DIR)/framework.md \
+			 $(DOC_DIR)/cmsis.md \
+			 $(DOC_DIR)/glossary.md \
+			 $(DOC_DIR)/deferred_response_architecture.md \
+			 $(DOC_DIR)/architecture_support.md \
+			 $(FWK_DIR)/include \
+			 $(FWK_DIR)/test/fwk_test.h \
+			 $(MODULE_INCLUDES) \
+			 $(MODULE_DOCS)
 
 # This tag can be used to specify the character encoding of the source files
 # that doxygen parses. Internally doxygen uses the UTF-8 encoding. Doxygen uses
@@ -2054,9 +2054,9 @@ INCLUDE_FILE_PATTERNS  =
 # This tag requires that the tag ENABLE_PREPROCESSING is set to YES.
 
 PREDEFINED             = __attribute__(x)= \
-                         __declspec(x)= \
-                         __has_attribute(x)=1 \
-                         __has_declspec_attribute(x)=1
+			 __declspec(x)= \
+			 __has_attribute(x)=1 \
+			 __has_declspec_attribute(x)=1
 
 # If the MACRO_EXPANSION and EXPAND_ONLY_PREDEF tags are set to YES then this
 # tag can be used to specify a list of macro names that should be expanded. The
@@ -2066,16 +2066,16 @@ PREDEFINED             = __attribute__(x
 # This tag requires that the tag ENABLE_PREPROCESSING is set to YES.
 
 EXPAND_AS_DEFINED      = FWK_ALLOC \
-                         FWK_ALLOC_SIZE1 \
-                         FWK_ALLOC_SIZE2 \
-                         FWK_ALLOC_ALIGN \
-                         FWK_WEAK \
-                         FWK_PRINTF \
-                         FWK_CONSTRUCTOR \
-                         FWK_PACKED \
-                         FWK_SECTION \
-                         FWK_DEPRECATED \
-                         FWK_NOINLINE
+			 FWK_ALLOC_SIZE1 \
+			 FWK_ALLOC_SIZE2 \
+			 FWK_ALLOC_ALIGN \
+			 FWK_WEAK \
+			 FWK_PRINTF \
+			 FWK_CONSTRUCTOR \
+			 FWK_PACKED \
+			 FWK_SECTION \
+			 FWK_DEPRECATED \
+			 FWK_NOINLINE
 
 # If the SKIP_FUNCTION_MACROS tag is set to YES then doxygen's preprocessor will
 # remove all references to function-like macros that are alone on a line, have
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/doc/build_system.md
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/doc/build_system.md
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/doc/build_system.md
@@ -23,10 +23,10 @@ __product__ directory and must adhere to
      └─ product
         └── <product>
             ├── product.mk
-            │   ├── include
-            │   │   └── <product level header files...>
-            │   └── src
-            │       └── <product level source files...>
+	    │   ├── include
+	    │   │   └── <product level header files...>
+	    │   └── src
+	    │       └── <product level source files...>
             ├── <firmware_1>
             │   └── <firmware 1 level configuration files...>
             └── <firmware_2>
@@ -58,19 +58,19 @@ directory. In either case, modules have
 
     <module root>
      └── <module>
-          ├── include
-          │   └── <header files...>
-          ├── src
-          │    ├── Makefile
-          │    └── <source files...>
-          ├── lib
-          │    └── mod_<module>.a
-          ├── test
-          │    └── <unit test files...>
-          ├── doc
-          │    └── <documentation files...>
-          ├── CMakeLists.txt
-          └── Module.cmake
+	  ├── include
+	  │   └── <header files...>
+	  ├── src
+	  │    ├── Makefile
+	  │    └── <source files...>
+	  ├── lib
+	  │    └── mod_<module>.a
+	  ├── test
+	  │    └── <unit test files...>
+	  ├── doc
+	  │    └── <documentation files...>
+	  ├── CMakeLists.txt
+	  └── Module.cmake
 
 Only one of the 'src' or 'lib' directories is required. When building a
 firmware, if the 'src' directory is present then the module library is built
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/doc/cmsis.md
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/doc/cmsis.md
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/doc/cmsis.md
@@ -30,14 +30,14 @@ CMSIS, and are described in the CMSIS 5
     #define __Vendor_SysTickConfig 0
 
     typedef enum IRQn {
-        NonMaskableInt_IRQn   = -14,
-        MemoryManagement_IRQn = -12,
-        BusFault_IRQn         = -11,
-        UsageFault_IRQn       = -10,
-        SVCall_IRQn           = -5,
-        DebugMonitor_IRQn     = -4,
-        PendSV_IRQn           = -2,
-        SysTick_IRQn          = -1,
+	NonMaskableInt_IRQn   = -14,
+	MemoryManagement_IRQn = -12,
+	BusFault_IRQn         = -11,
+	UsageFault_IRQn       = -10,
+	SVCall_IRQn           = -5,
+	DebugMonitor_IRQn     = -4,
+	PendSV_IRQn           = -2,
+	SysTick_IRQn          = -1,
     } IRQn_Type;
 
     #include <core_cm3.h>
@@ -54,19 +54,19 @@ sharing a configuration, but the suggest
     <root>
      └─ product
         └── <product>
-            ├── include
+	    ├── include
             │   ├── fmw_cmsis_a.h
             │   ├── fmw_cmsis_b.h
-            │   └── ...
+	    │   └── ...
             ├── a1
             │   ├── fmw_cmsis.h (includes <fmw_cmsis_a.h>)
-            │   └── ...
+	    │   └── ...
             ├── a2
             │   ├── fmw_cmsis.h (includes <fmw_cmsis_a.h>)
-            │   └── ...
+	    │   └── ...
             └── b
                 ├── fmw_cmsis.h (includes <fmw_cmsis_b.h>)
-                └── ...
+		└── ...
 
 Alternatively, for products with only one CMSIS configuration, it is reasonable
 to move `<fmw_cmsis.h>` to the product include directory:
@@ -74,12 +74,12 @@ to move `<fmw_cmsis.h>` to the product i
     <root>
      └─ product
         └── <product>
-            ├── include
+	    ├── include
             │   ├── fmw_cmsis.h
-            │   └── ...
+	    │   └── ...
             ├── a
-            │   └── ...
+	    │   └── ...
             ├── b
-            │   └── ...
+	    │   └── ...
             └── c
-                └── ...
+		└── ...
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/doc/code_rules.md
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/doc/code_rules.md
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/doc/code_rules.md
@@ -29,14 +29,14 @@ To invoke `cppcheck` manually, use:
 
 ``` bash
 cppcheck --xml \
-         --enable=all \
-         --force \
-         --suppressions-list="${WORKDIR}/tools/cppcheck_suppress_list.txt" \
-         --includes-file="${WORKDIR}/framework/include/" \
-         -I "${WORKDIR}/CMSIS" \
-         -i cmsis \
-         -U__CSMC__ \
-         ${WORKDIR}
+	 --enable=all \
+	 --force \
+	 --suppressions-list="${WORKDIR}/tools/cppcheck_suppress_list.txt" \
+	 --includes-file="${WORKDIR}/framework/include/" \
+	 -I "${WORKDIR}/CMSIS" \
+	 -i cmsis \
+	 -U__CSMC__ \
+	 ${WORKDIR}
 ```
 
 The CMSIS submodule is not checked. Errors are printed in XML format on the
@@ -61,10 +61,10 @@ cmake -DCMAKE_EXPORT_COMPILE_COMMANDS=1
 cmake --build ${BUILD_DIR} -- -n
 
 cppcheck --xml \
-         --enable=all \
-         --force \
-         --suppressions-list="${WORKDIR}/tools/cppcheck_suppress_list.txt" \
-         --project="${BUILD_DIR}/compile_commands.json"
+	 --enable=all \
+	 --force \
+	 --suppressions-list="${WORKDIR}/tools/cppcheck_suppress_list.txt" \
+	 --project="${BUILD_DIR}/compile_commands.json"
 ```
 
 MISRAC-2012
@@ -347,14 +347,14 @@ possible and a comment must explicit sho
 struct devicename_reg {
     /* Readable and writable register */
     FWK_RW uint32_t CONFIG;
-           uint32_t RESERVED1;
+	   uint32_t RESERVED1;
 
     /* Write-only register */
     FWK_W  uint32_t IRQ_CLEAR;
 
     /* Read-only register */
     FWK_R  uint32_t IRQ_STATUS;
-           uint32_t RESERVED2[0x40];
+	   uint32_t RESERVED2[0x40];
 };
 
 /* Register bit definitions */
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/doc/code_style.md
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/doc/code_style.md
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/doc/code_style.md
@@ -40,7 +40,7 @@ example:
 
 ```c
 struct some_reg {
-             uint8_t   RESERVED0[0x820 - 0x818];
+	     uint8_t   RESERVED0[0x820 - 0x818];
      FWK_RW  uint32_t  REGISTER0;
      FWK_RW  uint32_t  REGIRTER1;
 };
@@ -53,7 +53,7 @@ definitions may be preceded and followed
 ```c
 // clang-format off
 struct some_reg {
-             uint8_t   RESERVED0[0x820 - 0x818];
+	     uint8_t   RESERVED0[0x820 - 0x818];
      FWK_RW  uint32_t  REGISTER0;
      FWK_RW  uint32_t  REGIRTER1;
 };
@@ -68,9 +68,9 @@ multiple lines of code must be surrounde
 ```c
 if (condition) {
     function_call(
-        long_variable_name_x,
-        long_variable_name_y,
-        long_variable_name_z);
+	long_variable_name_x,
+	long_variable_name_y,
+	long_variable_name_z);
 }
 ```
 
@@ -79,9 +79,9 @@ This rule applies also to an if-else cha
 ```c
 if (condition) {
     function_call(
-        long_variable_name_x,
-        long_variable_name_y,
-        long_variable_name_z);
+	long_variable_name_x,
+	long_variable_name_y,
+	long_variable_name_z);
 } else {
     function_call_b();
 }
@@ -326,10 +326,10 @@ standardized:
 
 struct my_timer_reg {
     FWK_RW uint32_t CONFIG;
-           uint32_t RESERVED1;
+	   uint32_t RESERVED1;
     FWK_W  uint32_t IRQ_CLEAR;
     FWK_R  uint32_t IRQ_STATUS;
-           uint8_t  RESERVED2[0x40];
+	   uint8_t  RESERVED2[0x40];
 };
 
 #define MY_TIMER_CONFIG_ENABLE UINT32_C(0x00000001)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/doc/deferred_response_architecture.md
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/doc/deferred_response_architecture.md
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/doc/deferred_response_architecture.md
@@ -53,46 +53,46 @@ the requested operation.
 
        Client            HAL Module           Driver           Driver ISR
 
-          |                  |                  |                  |
-          |                  |                  |                  |
-         +++     get/set     |                  |                  |
-         | +--------------->+++                 |                  |
-         | |                | |     get/set     |                  |
-         | |                | +--------------->+++                 |
-         | |                | |                | |                 |
-         | |                | +<---------------+++                 |
-         | |                | |       PENDING   |                  |
-         | |                | +-----+           |                  |
-         | |                | |     |           |                  |
-         | |                | +<----+ PE1       |                  |
-         | |                | |                 |                  |
-         | +<---------------+++                 |                  |
-         +++        PENDING  |                  |                  |
-          |                  |                  |                  |
-          |         E1>>>>>>+++                 |                  |
-          |                 | |                 |                  |
-          |                 +++                 |                  |
-          |                  |                  |    DRV_RESP     +++
-          |                 +++<----------------------------------+ |
-          |                 | |                 |                 | |
-          |                 | +-----+           |                 | |
-          |                 | |     |           |                 | |
-          |                 | +<----+ PE2       |                 | |
-          |                 | |                 |                 | |
-          |                 +++---------------------------------->+ |
-          |                  |                  |                 +++
-          |                  |                  |                  |
-          |         E2>>>>>>+++                 |                  |
-          |                 | +-----+           |                  |
-          |                 | |     |           |                  |
-          |                 | +<----+ PE3       |                  |
-          |                 +++                 |                  |
-          |                  |                  |                  |
-         +++<<<<<<RE         |                  |                  |
-         | |                 |                  |                  |
-         | |                 |                  |                  |
-         +++                 |                  |                  |
-          |                  |                  |                  |
+	  |                  |                  |                  |
+	  |                  |                  |                  |
+	 +++     get/set     |                  |                  |
+	 | +--------------->+++                 |                  |
+	 | |                | |     get/set     |                  |
+	 | |                | +--------------->+++                 |
+	 | |                | |                | |                 |
+	 | |                | +<---------------+++                 |
+	 | |                | |       PENDING   |                  |
+	 | |                | +-----+           |                  |
+	 | |                | |     |           |                  |
+	 | |                | +<----+ PE1       |                  |
+	 | |                | |                 |                  |
+	 | +<---------------+++                 |                  |
+	 +++        PENDING  |                  |                  |
+	  |                  |                  |                  |
+	  |         E1>>>>>>+++                 |                  |
+	  |                 | |                 |                  |
+	  |                 +++                 |                  |
+	  |                  |                  |    DRV_RESP     +++
+	  |                 +++<----------------------------------+ |
+	  |                 | |                 |                 | |
+	  |                 | +-----+           |                 | |
+	  |                 | |     |           |                 | |
+	  |                 | +<----+ PE2       |                 | |
+	  |                 | |                 |                 | |
+	  |                 +++---------------------------------->+ |
+	  |                  |                  |                 +++
+	  |                  |                  |                  |
+	  |         E2>>>>>>+++                 |                  |
+	  |                 | +-----+           |                  |
+	  |                 | |     |           |                  |
+	  |                 | +<----+ PE3       |                  |
+	  |                 +++                 |                  |
+	  |                  |                  |                  |
+	 +++<<<<<<RE         |                  |                  |
+	 | |                 |                  |                  |
+	 | |                 |                  |                  |
+	 +++                 |                  |                  |
+	  |                  |                  |                  |
 
     LEGEND
     get/set  : A request to the HAL interface
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/doc/framework.md
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/doc/framework.md
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/doc/framework.md
@@ -50,13 +50,13 @@ directory per product under the product
 ```
 root/
     product/
-        productname/
-            include/
-            src/
-            module/
-            firmware_a/
-            firmware_b/
-            product.mk
+	productname/
+	    include/
+	    src/
+	    module/
+	    firmware_a/
+	    firmware_b/
+	    product.mk
 ```
 
 ### Firmware
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/doc/media/header.tex
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/doc/media/header.tex
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/doc/media/header.tex
@@ -14,18 +14,18 @@
 \usepackage{ifpdf}
 \ifpdf
 \usepackage[pdftex,
-            pagebackref=true,
-            colorlinks=true,
-            linkcolor=blue,
-            unicode
-           ]{hyperref}
+	    pagebackref=true,
+	    colorlinks=true,
+	    linkcolor=blue,
+	    unicode
+	   ]{hyperref}
 \else
 \usepackage[ps2pdf,
-            pagebackref=true,
-            colorlinks=true,
-            linkcolor=blue,
-            unicode
-           ]{hyperref}
+	    pagebackref=true,
+	    colorlinks=true,
+	    linkcolor=blue,
+	    unicode
+	   ]{hyperref}
 \usepackage{pspicture}
 \fi
 \usepackage[utf8]{inputenc}
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/doc/media/style.css
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/doc/media/style.css
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/doc/media/style.css
@@ -78,13 +78,13 @@ caption {
 }
 
 span.legend {
-        font-size: 70%;
-        text-align: center;
+	font-size: 70%;
+	text-align: center;
 }
 
 h3.version {
-        font-size: 90%;
-        text-align: center;
+	font-size: 90%;
+	text-align: center;
 }
 
 div.qindex, div.navtab{
@@ -130,7 +130,7 @@ a.qindexHL {
 }
 
 .contents a.qindexHL:visited {
-        color: #ffffff;
+	color: #ffffff;
 }
 
 a.el {
@@ -155,28 +155,28 @@ dl.el {
 }
 
 pre.fragment {
-        border: 1px solid #C4CFE5;
-        background-color: #FBFCFD;
-        padding: 4px 6px;
-        margin: 4px 8px 4px 2px;
-        overflow: auto;
-        word-wrap: break-word;
-        font-size:  9pt;
-        line-height: 125%;
-        font-family: monospace, fixed;
-        font-size: 105%;
+	border: 1px solid #C4CFE5;
+	background-color: #FBFCFD;
+	padding: 4px 6px;
+	margin: 4px 8px 4px 2px;
+	overflow: auto;
+	word-wrap: break-word;
+	font-size:  9pt;
+	line-height: 125%;
+	font-family: monospace, fixed;
+	font-size: 105%;
 }
 
 div.fragment {
-        padding: 4px;
-        margin: 4px;
+	padding: 4px;
+	margin: 4px;
 	background-color: #FBFCFD;
 	border: 1px solid #C4CFE5;
 }
 
 div.line {
 	font-family: monospace, fixed;
-        font-size: 13px;
+	font-size: 13px;
 	min-height: 13px;
 	line-height: 1.0;
 	text-wrap: unrestricted;
@@ -212,7 +212,7 @@ span.lineno {
 	text-align: right;
 	border-right: 2px solid #0F0;
 	background-color: #E8E8E8;
-        white-space: pre;
+	white-space: pre;
 }
 span.lineno a {
 	background-color: #D8D8D8;
@@ -254,7 +254,7 @@ div.groupText {
 body {
 	background-color: white;
 	color: black;
-        margin: 0;
+	margin: 0;
 }
 
 div.contents {
@@ -269,8 +269,8 @@ td.indexkey {
 	border: 1px solid #C4CFE5;
 	margin: 2px 0px 2px 0;
 	padding: 2px 10px;
-        white-space: nowrap;
-        vertical-align: top;
+	white-space: nowrap;
+	vertical-align: top;
 }
 
 td.indexvalue {
@@ -298,9 +298,9 @@ img.formulaInl {
 
 div.center {
 	text-align: center;
-        margin-top: 0px;
-        margin-bottom: 0px;
-        padding: 0px;
+	margin-top: 0px;
+	margin-bottom: 0px;
+	padding: 0px;
 }
 
 div.center img {
@@ -364,10 +364,10 @@ span.vhdllogic {
 }
 
 blockquote {
-        background-color: #F7F8FB;
-        border-left: 2px solid #9CAFD4;
-        margin: 0 24px 0 4px;
-        padding: 0 12px 0 16px;
+	background-color: #F7F8FB;
+	border-left: 2px solid #9CAFD4;
+	margin: 0 24px 0 4px;
+	padding: 0 12px 0 16px;
 }
 
 /* @end */
@@ -456,14 +456,14 @@ table.memberdecls {
 }
 
 .memSeparator {
-        border-bottom: 1px solid #DEE4F0;
-        line-height: 1px;
-        margin: 0px;
-        padding: 0px;
+	border-bottom: 1px solid #DEE4F0;
+	line-height: 1px;
+	margin: 0px;
+	padding: 0px;
 }
 
 .memItemLeft, .memTemplItemLeft {
-        white-space: nowrap;
+	white-space: nowrap;
 }
 
 .memItemRight {
@@ -472,7 +472,7 @@ table.memberdecls {
 
 .memTemplParams {
 	color: #4665A2;
-        white-space: nowrap;
+	white-space: nowrap;
 	font-size: 80%;
 }
 
@@ -506,22 +506,22 @@ table.memberdecls {
 	padding: 0;
 	margin-bottom: 10px;
 	margin-right: 5px;
-        -webkit-transition: box-shadow 0.5s linear;
-        -moz-transition: box-shadow 0.5s linear;
-        -ms-transition: box-shadow 0.5s linear;
-        -o-transition: box-shadow 0.5s linear;
-        transition: box-shadow 0.5s linear;
-        display: table !important;
-        width: 100%;
+	-webkit-transition: box-shadow 0.5s linear;
+	-moz-transition: box-shadow 0.5s linear;
+	-ms-transition: box-shadow 0.5s linear;
+	-o-transition: box-shadow 0.5s linear;
+	transition: box-shadow 0.5s linear;
+	display: table !important;
+	width: 100%;
 }
 
 .memitem.glow {
-         box-shadow: 0 0 15px cyan;
+	 box-shadow: 0 0 15px cyan;
 }
 
 .memname {
-        font-weight: bold;
-        margin-left: 6px;
+	font-weight: bold;
+	margin-left: 6px;
 }
 
 .memname td {
@@ -529,62 +529,62 @@ table.memberdecls {
 }
 
 .memproto, dl.reflist dt {
-        border-top: 1px solid #A8B8D9;
-        border-left: 1px solid #A8B8D9;
-        border-right: 1px solid #A8B8D9;
-        padding: 6px 0px 6px 0px;
-        color: #253555;
-        font-weight: bold;
-        text-shadow: 0px 1px 1px rgba(255, 255, 255, 0.9);
-        background-image:url('nav_f.png');
-        background-repeat:repeat-x;
-        background-color: #E2E8F2;
-        /* opera specific markup */
-        box-shadow: 5px 5px 5px rgba(0, 0, 0, 0.15);
-        border-top-right-radius: 4px;
-        border-top-left-radius: 4px;
-        /* firefox specific markup */
-        -moz-box-shadow: rgba(0, 0, 0, 0.15) 5px 5px 5px;
-        -moz-border-radius-topright: 4px;
-        -moz-border-radius-topleft: 4px;
-        /* webkit specific markup */
-        -webkit-box-shadow: 5px 5px 5px rgba(0, 0, 0, 0.15);
-        -webkit-border-top-right-radius: 4px;
-        -webkit-border-top-left-radius: 4px;
+	border-top: 1px solid #A8B8D9;
+	border-left: 1px solid #A8B8D9;
+	border-right: 1px solid #A8B8D9;
+	padding: 6px 0px 6px 0px;
+	color: #253555;
+	font-weight: bold;
+	text-shadow: 0px 1px 1px rgba(255, 255, 255, 0.9);
+	background-image:url('nav_f.png');
+	background-repeat:repeat-x;
+	background-color: #E2E8F2;
+	/* opera specific markup */
+	box-shadow: 5px 5px 5px rgba(0, 0, 0, 0.15);
+	border-top-right-radius: 4px;
+	border-top-left-radius: 4px;
+	/* firefox specific markup */
+	-moz-box-shadow: rgba(0, 0, 0, 0.15) 5px 5px 5px;
+	-moz-border-radius-topright: 4px;
+	-moz-border-radius-topleft: 4px;
+	/* webkit specific markup */
+	-webkit-box-shadow: 5px 5px 5px rgba(0, 0, 0, 0.15);
+	-webkit-border-top-right-radius: 4px;
+	-webkit-border-top-left-radius: 4px;
 
 }
 
 .memdoc, dl.reflist dd {
-        border-bottom: 1px solid #A8B8D9;
-        border-left: 1px solid #A8B8D9;
-        border-right: 1px solid #A8B8D9;
-        padding: 6px 10px 2px 10px;
-        background-color: #FBFCFD;
-        border-top-width: 0;
-        background-image:url('nav_g.png');
-        background-repeat:repeat-x;
-        background-color: #FFFFFF;
-        /* opera specific markup */
-        border-bottom-left-radius: 4px;
-        border-bottom-right-radius: 4px;
-        box-shadow: 5px 5px 5px rgba(0, 0, 0, 0.15);
-        /* firefox specific markup */
-        -moz-border-radius-bottomleft: 4px;
-        -moz-border-radius-bottomright: 4px;
-        -moz-box-shadow: rgba(0, 0, 0, 0.15) 5px 5px 5px;
-        /* webkit specific markup */
-        -webkit-border-bottom-left-radius: 4px;
-        -webkit-border-bottom-right-radius: 4px;
-        -webkit-box-shadow: 5px 5px 5px rgba(0, 0, 0, 0.15);
+	border-bottom: 1px solid #A8B8D9;
+	border-left: 1px solid #A8B8D9;
+	border-right: 1px solid #A8B8D9;
+	padding: 6px 10px 2px 10px;
+	background-color: #FBFCFD;
+	border-top-width: 0;
+	background-image:url('nav_g.png');
+	background-repeat:repeat-x;
+	background-color: #FFFFFF;
+	/* opera specific markup */
+	border-bottom-left-radius: 4px;
+	border-bottom-right-radius: 4px;
+	box-shadow: 5px 5px 5px rgba(0, 0, 0, 0.15);
+	/* firefox specific markup */
+	-moz-border-radius-bottomleft: 4px;
+	-moz-border-radius-bottomright: 4px;
+	-moz-box-shadow: rgba(0, 0, 0, 0.15) 5px 5px 5px;
+	/* webkit specific markup */
+	-webkit-border-bottom-left-radius: 4px;
+	-webkit-border-bottom-right-radius: 4px;
+	-webkit-box-shadow: 5px 5px 5px rgba(0, 0, 0, 0.15);
 }
 
 dl.reflist dt {
-        padding: 5px;
+	padding: 5px;
 }
 
 dl.reflist dd {
-        margin: 0px 0px 10px 0px;
-        padding: 5px;
+	margin: 0px 0px 10px 0px;
+	padding: 5px;
 }
 
 .paramkey {
@@ -603,27 +603,27 @@ dl.reflist dd {
 	font-style: normal;
 }
 .paramname code {
-        line-height: 14px;
+	line-height: 14px;
 }
 
 .params, .retval, .exception, .tparams {
-        margin-left: 0px;
-        padding-left: 0px;
+	margin-left: 0px;
+	padding-left: 0px;
 }
 
 .params .paramname, .retval .paramname {
-        font-weight: bold;
-        vertical-align: top;
+	font-weight: bold;
+	vertical-align: top;
 }
 
 .params .paramtype {
-        font-style: italic;
-        vertical-align: top;
+	font-style: italic;
+	vertical-align: top;
 }
 
 .params .paramdir {
-        font-family: "courier new",courier,monospace;
-        vertical-align: top;
+	font-family: "courier new",courier,monospace;
+	vertical-align: top;
 }
 
 table.mlabels {
@@ -642,15 +642,15 @@ td.mlabels-right {
 }
 
 span.mlabels {
-        margin-left: 8px;
+	margin-left: 8px;
 }
 
 span.mlabel {
-        background-color: #728DC1;
-        border-top:1px solid #5373B4;
-        border-left:1px solid #5373B4;
-        border-right:1px solid #C4CFE5;
-        border-bottom:1px solid #C4CFE5;
+	background-color: #728DC1;
+	border-top:1px solid #5373B4;
+	border-left:1px solid #5373B4;
+	border-right:1px solid #C4CFE5;
+	border-bottom:1px solid #C4CFE5;
 	text-shadow: none;
 	color: white;
 	margin-right: 4px;
@@ -668,38 +668,38 @@ span.mlabel {
 /* these are for tree view when not used as main index */
 
 div.directory {
-        margin: 10px 0px;
-        border-top: 1px solid #A8B8D9;
-        border-bottom: 1px solid #A8B8D9;
-        width: 100%;
+	margin: 10px 0px;
+	border-top: 1px solid #A8B8D9;
+	border-bottom: 1px solid #A8B8D9;
+	width: 100%;
 }
 
 .directory table {
-        border-collapse:collapse;
+	border-collapse:collapse;
 }
 
 .directory td {
-        margin: 0px;
-        padding: 0px;
+	margin: 0px;
+	padding: 0px;
 	vertical-align: top;
 }
 
 .directory td.entry {
-        white-space: nowrap;
-        padding-right: 6px;
+	white-space: nowrap;
+	padding-right: 6px;
 }
 
 .directory td.entry a {
-        outline:none;
+	outline:none;
 }
 
 .directory td.entry a img {
-        border: none;
+	border: none;
 }
 
 .directory td.desc {
-        width: 100%;
-        padding-left: 6px;
+	width: 100%;
+	padding-left: 6px;
 	padding-right: 6px;
 	padding-top: 3px;
 	border-left: 1px solid rgba(0,0,0,0.05);
@@ -715,21 +715,21 @@ div.directory {
 }
 
 .directory .levels {
-        white-space: nowrap;
-        width: 100%;
-        text-align: right;
-        font-size: 9pt;
+	white-space: nowrap;
+	width: 100%;
+	text-align: right;
+	font-size: 9pt;
 }
 
 .directory .levels span {
-        cursor: pointer;
-        padding-left: 2px;
-        padding-right: 2px;
+	cursor: pointer;
+	padding-left: 2px;
+	padding-right: 2px;
 	color: #3D578C;
 }
 
 div.dynheader {
-        margin-top: 8px;
+	margin-top: 8px;
 	-webkit-touch-callout: none;
 	-webkit-user-select: none;
 	-khtml-user-select: none;
@@ -745,8 +745,8 @@ address {
 
 table.doxtable {
 	border-collapse:collapse;
-        margin-top: 4px;
-        margin-bottom: 4px;
+	margin-top: 4px;
+	margin-bottom: 4px;
 }
 
 table.doxtable td, table.doxtable th {
@@ -763,54 +763,54 @@ table.doxtable th {
 }
 
 table.fieldtable {
-        width: 100%;
-        margin-bottom: 10px;
-        border: 1px solid #A8B8D9;
-        border-spacing: 0px;
-        -moz-border-radius: 4px;
-        -webkit-border-radius: 4px;
-        border-radius: 4px;
-        -moz-box-shadow: rgba(0, 0, 0, 0.15) 2px 2px 2px;
-        -webkit-box-shadow: 2px 2px 2px rgba(0, 0, 0, 0.15);
-        box-shadow: 2px 2px 2px rgba(0, 0, 0, 0.15);
+	width: 100%;
+	margin-bottom: 10px;
+	border: 1px solid #A8B8D9;
+	border-spacing: 0px;
+	-moz-border-radius: 4px;
+	-webkit-border-radius: 4px;
+	border-radius: 4px;
+	-moz-box-shadow: rgba(0, 0, 0, 0.15) 2px 2px 2px;
+	-webkit-box-shadow: 2px 2px 2px rgba(0, 0, 0, 0.15);
+	box-shadow: 2px 2px 2px rgba(0, 0, 0, 0.15);
 }
 
 .fieldtable td, .fieldtable th {
-        padding: 3px 7px 2px;
+	padding: 3px 7px 2px;
 }
 
 .fieldtable td.fieldtype, .fieldtable td.fieldname {
-        white-space: nowrap;
-        border-right: 1px solid #A8B8D9;
-        border-bottom: 1px solid #A8B8D9;
-        vertical-align: top;
+	white-space: nowrap;
+	border-right: 1px solid #A8B8D9;
+	border-bottom: 1px solid #A8B8D9;
+	vertical-align: top;
 }
 
 .fieldtable td.fielddoc {
-        border-bottom: 1px solid #A8B8D9;
-        width: 100%;
+	border-bottom: 1px solid #A8B8D9;
+	width: 100%;
 }
 
 .fieldtable tr:last-child td {
-        border-bottom: none;
+	border-bottom: none;
 }
 
 .fieldtable th {
-        background-image:url('nav_f.png');
-        background-repeat:repeat-x;
-        background-color: #E2E8F2;
-        font-size: 90%;
-        color: #253555;
-        padding-bottom: 4px;
-        padding-top: 5px;
-        text-align:left;
-        -moz-border-radius-topleft: 4px;
-        -moz-border-radius-topright: 4px;
-        -webkit-border-top-left-radius: 4px;
-        -webkit-border-top-right-radius: 4px;
-        border-top-left-radius: 4px;
-        border-top-right-radius: 4px;
-        border-bottom: 1px solid #A8B8D9;
+	background-image:url('nav_f.png');
+	background-repeat:repeat-x;
+	background-color: #E2E8F2;
+	font-size: 90%;
+	color: #253555;
+	padding-bottom: 4px;
+	padding-top: 5px;
+	text-align:left;
+	-moz-border-radius-topleft: 4px;
+	-moz-border-radius-topright: 4px;
+	-webkit-border-top-left-radius: 4px;
+	-webkit-border-top-right-radius: 4px;
+	border-top-left-radius: 4px;
+	border-top-right-radius: 4px;
+	border-bottom: 1px solid #A8B8D9;
 }
 
 
@@ -870,15 +870,15 @@ table.fieldtable {
 
 .navpath li.footer
 {
-        list-style-type:none;
-        float:right;
-        padding-left:10px;
-        padding-right:15px;
-        background-image:none;
-        background-repeat:no-repeat;
-        background-position:right;
-        color:#364D7C;
-        font-size: 8pt;
+	list-style-type:none;
+	float:right;
+	padding-left:10px;
+	padding-right:15px;
+	background-image:none;
+	background-repeat:no-repeat;
+	background-position:right;
+	color:#364D7C;
+	font-size: 8pt;
 }
 
 
@@ -910,8 +910,8 @@ div.ingroups a
 
 div.header
 {
-        background-image:url('nav_h.png');
-        background-repeat:repeat-x;
+	background-image:url('nav_h.png');
+	background-repeat:repeat-x;
 	background-color: #F9FAFC;
 	margin:  0px;
 	border-bottom: 1px solid #C4CFE5;
@@ -924,7 +924,7 @@ div.headertitle
 
 dl
 {
-        padding: 0 0 0 10px;
+	padding: 0 0 0 10px;
 }
 
 /* dl.note, dl.warning, dl.attention, dl.pre, dl.post, dl.invariant, dl.deprecated, dl.todo, dl.test, dl.bug */
@@ -936,58 +936,58 @@ dl.section
 
 dl.note
 {
-        margin-left:-7px;
-        padding-left: 3px;
-        border-left:4px solid;
-        border-color: #D0C000;
+	margin-left:-7px;
+	padding-left: 3px;
+	border-left:4px solid;
+	border-color: #D0C000;
 }
 
 dl.warning, dl.attention
 {
-        margin-left:-7px;
-        padding-left: 3px;
-        border-left:4px solid;
-        border-color: #FF0000;
+	margin-left:-7px;
+	padding-left: 3px;
+	border-left:4px solid;
+	border-color: #FF0000;
 }
 
 dl.pre, dl.post, dl.invariant
 {
-        margin-left:-7px;
-        padding-left: 3px;
-        border-left:4px solid;
-        border-color: #00D000;
+	margin-left:-7px;
+	padding-left: 3px;
+	border-left:4px solid;
+	border-color: #00D000;
 }
 
 dl.deprecated
 {
-        margin-left:-7px;
-        padding-left: 3px;
-        border-left:4px solid;
-        border-color: #505050;
+	margin-left:-7px;
+	padding-left: 3px;
+	border-left:4px solid;
+	border-color: #505050;
 }
 
 dl.todo
 {
-        margin-left:-7px;
-        padding-left: 3px;
-        border-left:4px solid;
-        border-color: #00C0E0;
+	margin-left:-7px;
+	padding-left: 3px;
+	border-left:4px solid;
+	border-color: #00C0E0;
 }
 
 dl.test
 {
-        margin-left:-7px;
-        padding-left: 3px;
-        border-left:4px solid;
-        border-color: #3030E0;
+	margin-left:-7px;
+	padding-left: 3px;
+	border-left:4px solid;
+	border-color: #3030E0;
 }
 
 dl.bug
 {
-        margin-left:-7px;
-        padding-left: 3px;
-        border-left:4px solid;
-        border-color: #C08050;
+	margin-left:-7px;
+	padding-left: 3px;
+	border-left:4px solid;
+	border-color: #C08050;
 }
 
 dl.section dd {
@@ -1040,17 +1040,17 @@ dl.section dd {
 
 .image
 {
-        text-align: center;
+	text-align: center;
 }
 
 .dotgraph
 {
-        text-align: center;
+	text-align: center;
 }
 
 .mscgraph
 {
-        text-align: center;
+	text-align: center;
 }
 
 .caption
@@ -1064,74 +1064,74 @@ div.zoom
 }
 
 dl.citelist {
-        margin-bottom:50px;
+	margin-bottom:50px;
 }
 
 dl.citelist dt {
-        color:#334975;
-        float:left;
-        font-weight:bold;
-        margin-right:10px;
-        padding:5px;
+	color:#334975;
+	float:left;
+	font-weight:bold;
+	margin-right:10px;
+	padding:5px;
 }
 
 dl.citelist dd {
-        margin:2px 0;
-        padding:5px 0;
+	margin:2px 0;
+	padding:5px 0;
 }
 
 div.toc {
-        padding: 14px 25px;
-        background-color: #F4F6FA;
-        border: 1px solid #D8DFEE;
-        border-radius: 7px 7px 7px 7px;
-        float: right;
-        height: auto;
-        margin: 0 20px 10px 10px;
-        width: 200px;
+	padding: 14px 25px;
+	background-color: #F4F6FA;
+	border: 1px solid #D8DFEE;
+	border-radius: 7px 7px 7px 7px;
+	float: right;
+	height: auto;
+	margin: 0 20px 10px 10px;
+	width: 200px;
 }
 
 div.toc li {
-        background: url("bdwn.png") no-repeat scroll 0 5px transparent;
-        font: 10px/1.2 Verdana,DejaVu Sans,Geneva,sans-serif;
-        margin-top: 5px;
-        padding-left: 10px;
-        padding-top: 2px;
+	background: url("bdwn.png") no-repeat scroll 0 5px transparent;
+	font: 10px/1.2 Verdana,DejaVu Sans,Geneva,sans-serif;
+	margin-top: 5px;
+	padding-left: 10px;
+	padding-top: 2px;
 }
 
 div.toc h3 {
-        font: bold 12px/1.2 Arial,FreeSans,sans-serif;
+	font: bold 12px/1.2 Arial,FreeSans,sans-serif;
 	color: #4665A2;
-        border-bottom: 0 none;
-        margin: 0;
+	border-bottom: 0 none;
+	margin: 0;
 }
 
 div.toc ul {
-        list-style: none outside none;
-        border: medium none;
-        padding: 0px;
+	list-style: none outside none;
+	border: medium none;
+	padding: 0px;
 }
 
 div.toc li.level1 {
-        margin-left: 0px;
+	margin-left: 0px;
 }
 
 div.toc li.level2 {
-        margin-left: 15px;
+	margin-left: 15px;
 }
 
 div.toc li.level3 {
-        margin-left: 30px;
+	margin-left: 30px;
 }
 
 div.toc li.level4 {
-        margin-left: 45px;
+	margin-left: 45px;
 }
 
 .inherit_header {
-        font-weight: bold;
-        color: gray;
-        cursor: pointer;
+	font-weight: bold;
+	color: gray;
+	cursor: pointer;
 	-webkit-touch-callout: none;
 	-webkit-user-select: none;
 	-khtml-user-select: none;
@@ -1141,16 +1141,16 @@ div.toc li.level4 {
 }
 
 .inherit_header td {
-        padding: 6px 0px 2px 5px;
+	padding: 6px 0px 2px 5px;
 }
 
 .inherit {
-        display: none;
+	display: none;
 }
 
 tr.heading h2 {
-        margin-top: 12px;
-        margin-bottom: 4px;
+	margin-top: 12px;
+	margin-bottom: 4px;
 }
 
 @media print
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/doc/template/device.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/doc/template/device.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/doc/template/device.h
@@ -18,14 +18,14 @@
 struct device_reg {
     /* Readable and writable register */
     FWK_RW uint32_t <REGISTER NAME>;
-           uint32_t RESERVED1;
+	   uint32_t RESERVED1;
 
     /* Write-only register */
     FWK_W  uint32_t <REGISTER NAME>;
 
     /* Read-only register */
     FWK_R  uint32_t <REGISTER NAME>;
-           uint32_t RESERVED2[0x40];
+	   uint32_t RESERVED2[0x40];
 };
 
 /* Register bit definitions */
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/docker/Dockerfile
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/docker/Dockerfile
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/docker/Dockerfile
@@ -23,8 +23,8 @@ RUN apt-get update && \
     apt-get dist-upgrade -y && \
     apt-get update && \
     apt-get install -y --no-install-recommends \
-        bundler gcc g++ gpg-agent git gosu lsb-release make ninja-build \
-        xz-utils python3 python3-pip software-properties-common wget lcov && \
+	bundler gcc g++ gpg-agent git gosu lsb-release make ninja-build \
+	xz-utils python3 python3-pip software-properties-common wget lcov && \
     apt-get update && \
     python3 -m pip install --upgrade pip && \
     gem install bundler
@@ -35,7 +35,7 @@ RUN mkdir "/opt/arm-gnu-rm" && \
     wget -nv -O - -c "${ARM_GNU_RM_URL}" | \
     tar -jxf - -C "/opt/arm-gnu-rm" --strip-components=1 && \
     echo 'export PATH=/opt/arm-gnu-rm/bin:${PATH}' >> \
-        "/etc/profile.d/50-scp-firmware-env.sh"
+	"/etc/profile.d/50-scp-firmware-env.sh"
 
 ENV PATH="/opt/arm-gnu-rm/bin:${PATH}"
 
@@ -45,11 +45,11 @@ ENV PATH="/opt/arm-compiler-6/bin:${PATH
 RUN mkdir "/opt/doxygen" && \
     apt-get update && \
     apt-get install -y --no-install-recommends \
-        libclang1-9 libclang-9-dev libclang-cpp9 && \
+	libclang1-9 libclang-9-dev libclang-cpp9 && \
     wget -nv -O - -c "${DOXYGEN_URL}" | \
     tar -zxf - -C "/opt/doxygen" --strip-components=1 && \
     echo 'export PATH=/opt/doxygen/bin:${PATH}' >> \
-        "/etc/profile.d/50-scp-firmware-env.sh"
+	"/etc/profile.d/50-scp-firmware-env.sh"
 
 ENV PATH="/opt/doxygen/bin:${PATH}"
 
@@ -57,18 +57,18 @@ RUN mkdir "/opt/cmake" && \
     wget -nv -O - -c "${CMAKE_URL}" | \
     tar -zxf - -C "/opt/cmake" --strip-components=1 && \
     echo 'export PATH=/opt/cmake/bin:${PATH}' >> \
-        "/etc/profile.d/50-scp-firmware-env.sh"
+	"/etc/profile.d/50-scp-firmware-env.sh"
 
 ENV PATH="/opt/cmake/bin:${PATH}"
 
 RUN apt-get update && \
     wget --no-check-certificate -O - \
-        https://apt.llvm.org/llvm-snapshot.gpg.key | apt-key add - && \
+	https://apt.llvm.org/llvm-snapshot.gpg.key | apt-key add - && \
     add-apt-repository \
-        'deb http://apt.llvm.org/focal/   llvm-toolchain-focal-13  main' && \
+	'deb http://apt.llvm.org/focal/   llvm-toolchain-focal-13  main' && \
     apt-get update && \
     apt-get install -y --no-install-recommends \
-        clang-tidy-13 clang-13 llvm-13 libclang-13-dev llvm-13-dev
+	clang-tidy-13 clang-13 llvm-13 libclang-13-dev llvm-13-dev
 
 COPY rootfs/usr/local/bin/prepare_llvm /usr/local/bin/prepare_llvm
 RUN  chmod 755 "/usr/local/bin/prepare_llvm"
@@ -78,7 +78,7 @@ RUN mkdir "/opt/aarch64-gcc" && \
     wget -nv -O - -c "${AARCH64_GCC_URL}" | \
     tar -Jxf - -C "/opt/aarch64-gcc" --strip-components=1 && \
     echo 'export PATH=/opt/aarch64-gcc/bin:${PATH}' >> \
-        "/etc/profile.d/50-scp-firmware-env.sh"
+	"/etc/profile.d/50-scp-firmware-env.sh"
 
 ENV PATH="/opt/aarch64-gcc/bin:${PATH}"
 
@@ -86,10 +86,10 @@ RUN cwd=$PWD && mkdir "/opt/cppcheck" &&
     git clone --depth 1 --branch "${CPPCHECK_CHECKOUT_TAG}" \
     "${CPPCHECK_SRC_URL}" source && \
     cmake -G "Ninja" -DCMAKE_INSTALL_PREFIX=/opt/cppcheck \
-        -DFILESDIR=/opt/cppcheck ./source && \
+	-DFILESDIR=/opt/cppcheck ./source && \
     cmake --build . -- install && cd $cwd && \
     echo 'export PATH=/opt/cppcheck/bin:${PATH}' >> \
-        "/etc/profile.d/50-scp-firmware-env.sh"
+	"/etc/profile.d/50-scp-firmware-env.sh"
 
 ENV PATH="/opt/cppcheck/bin:${PATH}"
 
@@ -99,7 +99,7 @@ RUN cwd=$PWD && mkdir "/opt/iwyu" && cd
     ./iwyu-13 && cmake --build . && \
     cd bin && ln -s include-what-you-use iwyu && cd $cwd && \
     echo 'export PATH=/opt/iwyu/bin:${PATH}' >> \
-        "/etc/profile.d/50-scp-firmware-env.sh"
+	"/etc/profile.d/50-scp-firmware-env.sh"
 
 ENV PATH="/opt/iwyu/bin:${PATH}"
 
@@ -118,7 +118,7 @@ ARG JENKINS_GID=1000
 
 RUN addgroup --gid "${JENKINS_GID}" jenkins && \
     adduser --uid "${JENKINS_UID}" --gid "${JENKINS_GID}" --disabled-password \
-        --gecos "" jenkins
+	--gecos "" jenkins
 
 ENTRYPOINT [ "sh", "-c" ]
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/docker/rootfs/usr/local/bin/dependencies
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/docker/rootfs/usr/local/bin/dependencies
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/docker/rootfs/usr/local/bin/dependencies
@@ -13,7 +13,7 @@ fi
 
 if [ -f "Gemfile" ]; then
     if [ "$(id -u)" -ne 0 ]; then
-        bundle config set --local path "/home/${USER}/.local/bin"
+	bundle config set --local path "/home/${USER}/.local/bin"
     fi
     bundle install > /dev/null
 fi
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/docker/rootfs/usr/local/bin/init
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/docker/rootfs/usr/local/bin/init
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/docker/rootfs/usr/local/bin/init
@@ -23,8 +23,8 @@ else
     groupmod -g ${gid} ${user}
 
     if [ $# -eq 0 ]; then
-        exec su ${user}
+	exec su ${user}
     else
-        exec gosu ${user} $@
+	exec gosu ${user} $@
     fi
 fi
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/docker/rootfs/usr/local/bin/prepare_llvm
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/docker/rootfs/usr/local/bin/prepare_llvm
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/docker/rootfs/usr/local/bin/prepare_llvm
@@ -97,25 +97,25 @@ cmd mkdir -p $BAREMETAL_PATH
 
 for ARCH in armv7m armv7em armv8m.main armv8.1m.main; do
     cmdp "Preparing Compiler-RT builtins for target ${ARCH}" cmake -GNinja \
-        -DLLVM_CONFIG_PATH=$(command -v llvm-config-13) \
-        -DCMAKE_TRY_COMPILE_TARGET_TYPE=STATIC_LIBRARY \
-        -DCOMPILER_RT_OS_DIR="baremetal" \
-        -DCOMPILER_RT_BUILD_BUILTINS=ON \
-        -DCOMPILER_RT_BUILD_SANITIZERS=OFF \
-        -DCOMPILER_RT_BUILD_XRAY=OFF \
-        -DCOMPILER_RT_BUILD_LIBFUZZER=OFF \
-        -DCOMPILER_RT_BUILD_PROFILE=OFF \
-        -DCMAKE_C_COMPILER=$(command -v clang-13) \
-        -DCMAKE_C_COMPILER_TARGET="${ARCH}-none-eabi" \
-        -DCMAKE_ASM_COMPILER_TARGET="${ARCH}-none-eabi" \
-        -DCMAKE_AR=$(command -v llvm-ar-13) \
-        -DCMAKE_NM=$(command -v llvm-nm-13) \
-        -DCMAKE_RANLIB=$(command -v llvm-ranlib-13) \
-        -DCOMPILER_RT_BAREMETAL_BUILD=ON \
-        -DCOMPILER_RT_DEFAULT_TARGET_ONLY=ON \
-        -DCOMPILER_RT_INCLUDE_TESTS=OFF \
-        -DCMAKE_C_FLAGS=\"-march=${ARCH} -mthumb -mfpu=none -mfloat-abi=soft -I${SYSROOT}/include\" \
-        -DCMAKE_ASM_FLAGS=\"-march=${ARCH} -mthumb -mfpu=none -mfloat-abi=soft -I${SYSROOT}/include\"
+	-DLLVM_CONFIG_PATH=$(command -v llvm-config-13) \
+	-DCMAKE_TRY_COMPILE_TARGET_TYPE=STATIC_LIBRARY \
+	-DCOMPILER_RT_OS_DIR="baremetal" \
+	-DCOMPILER_RT_BUILD_BUILTINS=ON \
+	-DCOMPILER_RT_BUILD_SANITIZERS=OFF \
+	-DCOMPILER_RT_BUILD_XRAY=OFF \
+	-DCOMPILER_RT_BUILD_LIBFUZZER=OFF \
+	-DCOMPILER_RT_BUILD_PROFILE=OFF \
+	-DCMAKE_C_COMPILER=$(command -v clang-13) \
+	-DCMAKE_C_COMPILER_TARGET="${ARCH}-none-eabi" \
+	-DCMAKE_ASM_COMPILER_TARGET="${ARCH}-none-eabi" \
+	-DCMAKE_AR=$(command -v llvm-ar-13) \
+	-DCMAKE_NM=$(command -v llvm-nm-13) \
+	-DCMAKE_RANLIB=$(command -v llvm-ranlib-13) \
+	-DCOMPILER_RT_BAREMETAL_BUILD=ON \
+	-DCOMPILER_RT_DEFAULT_TARGET_ONLY=ON \
+	-DCOMPILER_RT_INCLUDE_TESTS=OFF \
+	-DCMAKE_C_FLAGS=\"-march=${ARCH} -mthumb -mfpu=none -mfloat-abi=soft -I${SYSROOT}/include\" \
+	-DCMAKE_ASM_FLAGS=\"-march=${ARCH} -mthumb -mfpu=none -mfloat-abi=soft -I${SYSROOT}/include\"
     cmdp "Building builtins for target ${ARCH}" ninja builtins
     cmdp "Installing the builtins" cp ./lib/baremetal/libclang_rt.builtins-arm.a ${BAREMETAL_PATH}/libclang_rt.builtins-${ARCH}.a
 done
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/framework/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/CMakeLists.txt
@@ -18,27 +18,27 @@
 add_library(framework)
 
 target_include_directories(framework
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(
     framework
     PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/assert.c"
-            "${CMAKE_CURRENT_SOURCE_DIR}/src/fwk_arch.c"
-            "${CMAKE_CURRENT_SOURCE_DIR}/src/fwk_dlist.c"
-            "${CMAKE_CURRENT_SOURCE_DIR}/src/fwk_id.c"
-            "${CMAKE_CURRENT_SOURCE_DIR}/src/fwk_interrupt.c"
-            "${CMAKE_CURRENT_SOURCE_DIR}/src/fwk_io.c"
-            "${CMAKE_CURRENT_SOURCE_DIR}/src/fwk_log.c"
-            "${CMAKE_CURRENT_SOURCE_DIR}/src/fwk_mm.c"
-            "${CMAKE_CURRENT_SOURCE_DIR}/src/fwk_module.c"
-            "${CMAKE_CURRENT_SOURCE_DIR}/src/fwk_ring.c"
-            "${CMAKE_CURRENT_SOURCE_DIR}/src/fwk_slist.c"
-            "${CMAKE_CURRENT_SOURCE_DIR}/src/fwk_status.c"
-            "${CMAKE_CURRENT_SOURCE_DIR}/src/fwk_string.c"
-            "${CMAKE_CURRENT_SOURCE_DIR}/src/fwk_delayed_resp.c"
-            "${CMAKE_CURRENT_SOURCE_DIR}/src/fwk_time.c"
-            "${CMAKE_CURRENT_SOURCE_DIR}/src/stdlib.c"
-            "${CMAKE_CURRENT_SOURCE_DIR}/src/fwk_core.c")
+	    "${CMAKE_CURRENT_SOURCE_DIR}/src/fwk_arch.c"
+	    "${CMAKE_CURRENT_SOURCE_DIR}/src/fwk_dlist.c"
+	    "${CMAKE_CURRENT_SOURCE_DIR}/src/fwk_id.c"
+	    "${CMAKE_CURRENT_SOURCE_DIR}/src/fwk_interrupt.c"
+	    "${CMAKE_CURRENT_SOURCE_DIR}/src/fwk_io.c"
+	    "${CMAKE_CURRENT_SOURCE_DIR}/src/fwk_log.c"
+	    "${CMAKE_CURRENT_SOURCE_DIR}/src/fwk_mm.c"
+	    "${CMAKE_CURRENT_SOURCE_DIR}/src/fwk_module.c"
+	    "${CMAKE_CURRENT_SOURCE_DIR}/src/fwk_ring.c"
+	    "${CMAKE_CURRENT_SOURCE_DIR}/src/fwk_slist.c"
+	    "${CMAKE_CURRENT_SOURCE_DIR}/src/fwk_status.c"
+	    "${CMAKE_CURRENT_SOURCE_DIR}/src/fwk_string.c"
+	    "${CMAKE_CURRENT_SOURCE_DIR}/src/fwk_delayed_resp.c"
+	    "${CMAKE_CURRENT_SOURCE_DIR}/src/fwk_time.c"
+	    "${CMAKE_CURRENT_SOURCE_DIR}/src/stdlib.c"
+	    "${CMAKE_CURRENT_SOURCE_DIR}/src/fwk_core.c")
 
 
 if(SCP_ENABLE_SUB_SYSTEM_MODE)
@@ -47,7 +47,7 @@ endif()
 
 if(SCP_ENABLE_NOTIFICATIONS)
     target_sources(framework
-                   PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/fwk_notification.c")
+		   PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/fwk_notification.c")
 
     target_compile_definitions(framework PUBLIC "BUILD_HAS_NOTIFICATION")
 endif()
@@ -55,7 +55,7 @@ endif()
 if(SCP_ENABLE_SCMI_NOTIFICATIONS)
     target_compile_definitions(framework PUBLIC "BUILD_HAS_SCMI_NOTIFICATIONS")
     if(SCP_ENABLE_SCMI_SENSOR_EVENTS)
-        target_compile_definitions(framework PUBLIC "BUILD_HAS_SCMI_SENSOR_EVENTS")
+	target_compile_definitions(framework PUBLIC "BUILD_HAS_SCMI_SENSOR_EVENTS")
     endif()
 endif()
 
@@ -116,15 +116,15 @@ endif()
 
 if(NOT DEFINED SCP_LOG_LEVEL)
     set(SCP_LOG_LEVEL
-        "${SCP_LOG_LEVEL_DEFAULT}"
-        CACHE STRING "Minimum logging level.")
+	"${SCP_LOG_LEVEL_DEFAULT}"
+	CACHE STRING "Minimum logging level.")
 
     set_property(CACHE SCP_LOG_LEVEL PROPERTY STRINGS "DEBUG" "INFO" "WARN"
-                                                      "ERROR" "CRIT")
+						      "ERROR" "CRIT")
 endif()
 
 target_compile_definitions(framework
-                           PUBLIC FWK_LOG_LEVEL=FWK_LOG_LEVEL_${SCP_LOG_LEVEL})
+			   PUBLIC FWK_LOG_LEVEL=FWK_LOG_LEVEL_${SCP_LOG_LEVEL})
 
 #
 # Generate `<fwk_module_idx.h>` and `<fwk_module_list.c>` based on the list of
@@ -138,9 +138,9 @@ list(LENGTH SCP_MODULES SCP_MODULE_IDX_M
 
 foreach(idx RANGE ${SCP_MODULE_IDX_MAX})
     if(idx EQUAL SCP_MODULE_IDX_MAX)
-        string(APPEND SCP_MODULE_IDX_GEN "    FWK_MODULE_IDX_COUNT = ${idx},\n")
+	string(APPEND SCP_MODULE_IDX_GEN "    FWK_MODULE_IDX_COUNT = ${idx},\n")
 
-        break()
+	break()
     endif()
 
     list(GET SCP_MODULES ${idx} SCP_MODULE)
@@ -167,20 +167,20 @@ foreach(idx RANGE ${SCP_MODULE_IDX_MAX})
     #
 
     target_compile_definitions(framework
-                               PUBLIC "BUILD_HAS_MOD_${SCP_MODULE_UPPER}=1")
+			       PUBLIC "BUILD_HAS_MOD_${SCP_MODULE_UPPER}=1")
 endforeach()
 
 configure_file("${CMAKE_CURRENT_SOURCE_DIR}/include/fwk_module_idx.h.in"
-               "${CMAKE_CURRENT_BINARY_DIR}/include/fwk_module_idx.h")
+	       "${CMAKE_CURRENT_BINARY_DIR}/include/fwk_module_idx.h")
 
 configure_file("${CMAKE_CURRENT_SOURCE_DIR}/src/fwk_module_list.c.in"
-               "${CMAKE_CURRENT_BINARY_DIR}/src/fwk_module_list.c")
+	       "${CMAKE_CURRENT_BINARY_DIR}/src/fwk_module_list.c")
 
 target_include_directories(framework
-                           PUBLIC "${CMAKE_CURRENT_BINARY_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_BINARY_DIR}/include")
 
 target_sources(framework
-               PRIVATE "${CMAKE_CURRENT_BINARY_DIR}/src/fwk_module_list.c")
+	       PRIVATE "${CMAKE_CURRENT_BINARY_DIR}/src/fwk_module_list.c")
 
 #
 # Build framework version string
@@ -194,10 +194,10 @@ find_package(Git)
 
 if(GIT_FOUND)
     execute_process(
-        COMMAND "${GIT_EXECUTABLE}" describe --tags --dirty --always
-        WORKING_DIRECTORY "${SCP_SOURCE_DIR}"
-        OUTPUT_VARIABLE SCP_DESCRIBE
-        OUTPUT_STRIP_TRAILING_WHITESPACE)
+	COMMAND "${GIT_EXECUTABLE}" describe --tags --dirty --always
+	WORKING_DIRECTORY "${SCP_SOURCE_DIR}"
+	OUTPUT_VARIABLE SCP_DESCRIBE
+	OUTPUT_STRIP_TRAILING_WHITESPACE)
 endif()
 
 if(NOT SCP_DESCRIBE)
@@ -207,9 +207,9 @@ endif()
 target_compile_definitions(
     framework
     PUBLIC "BUILD_VERSION_DESCRIBE_STRING=\"${SCP_DESCRIBE}\""
-           "BUILD_VERSION_MAJOR=${SCP_VERSION_MAJOR}"
-           "BUILD_VERSION_MINOR=${SCP_VERSION_MINOR}"
-           "BUILD_VERSION_PATCH=${SCP_VERSION_PATCH}")
+	   "BUILD_VERSION_MAJOR=${SCP_VERSION_MAJOR}"
+	   "BUILD_VERSION_MINOR=${SCP_VERSION_MINOR}"
+	   "BUILD_VERSION_PATCH=${SCP_VERSION_PATCH}")
 
 #
 # Pull in any include directories explicitly exposed by the firmware. We need
@@ -219,7 +219,7 @@ target_compile_definitions(
 target_include_directories(
     framework
     PRIVATE
-        $<TARGET_PROPERTY:${SCP_FIRMWARE_TARGET},INTERFACE_INCLUDE_DIRECTORIES>)
+	$<TARGET_PROPERTY:${SCP_FIRMWARE_TARGET},INTERFACE_INCLUDE_DIRECTORIES>)
 
 #
 # Make sure the framework links privately to all of the modules, as it depends
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/include/fwk_arch.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/framework/include/fwk_arch.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/include/fwk_arch.h
@@ -131,8 +131,8 @@ struct fwk_arch_interrupt_driver {
      * \retval ::FWK_E_PARAM One or more parameters were invalid.
      */
     int (*set_isr_irq_param)(unsigned int interrupt,
-                             void (*isr)(uintptr_t param),
-                             uintptr_t parameter);
+			     void (*isr)(uintptr_t param),
+			     uintptr_t parameter);
 
     /*!
      * \brief Set the interrupt service routine for the non-maskable interrupt
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/include/fwk_assert.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/framework/include/fwk_assert.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/include/fwk_assert.h
@@ -136,10 +136,10 @@
 #ifdef NDEBUG
 #    if FWK_HAS_BUILTIN(__builtin_assume)
 #        define fwk_assert(condition) \
-            do { \
-                bool c = (condition); \
-                __builtin_assume(c); \
-            } while (0)
+	    do { \
+		bool c = (condition); \
+		__builtin_assume(c); \
+	    } while (0)
 #    else
 #        define fwk_assert(condition) ((void)(condition))
 #    endif
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/include/fwk_attributes.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/framework/include/fwk_attributes.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/include/fwk_attributes.h
@@ -151,7 +151,7 @@
 #if FWK_HAS_GNU_ATTRIBUTE(__alloc_size__)
 #    define FWK_ALLOC_SIZE1(SIZE_POS) __attribute__((__alloc_size__(SIZE_POS)))
 #    define FWK_ALLOC_SIZE2(SIZE_POS, COUNT_POS) \
-        __attribute__((__alloc_size__(SIZE_POS, COUNT_POS)))
+	__attribute__((__alloc_size__(SIZE_POS, COUNT_POS)))
 #else
 #    define FWK_ALLOC_SIZE1(SIZE_POS)
 #    define FWK_ALLOC_SIZE2(SIZE_POS, COUNT_POS)
@@ -172,7 +172,7 @@
 
 #if FWK_HAS_GNU_ATTRIBUTE(__alloc_align__)
 #    define FWK_ALLOC_ALIGN(ALIGN_POS) \
-        __attribute__((__alloc_align__(ALIGN_POS)))
+	__attribute__((__alloc_align__(ALIGN_POS)))
 #else
 #    define FWK_ALLOC_ALIGN(ALIGN_POS)
 #endif
@@ -208,7 +208,7 @@
 
 #if FWK_HAS_GNU_ATTRIBUTE(__format__)
 #    define FWK_PRINTF(STR_POS, VA_POS) \
-        __attribute__((__format__(printf, STR_POS, VA_POS)))
+	__attribute__((__format__(printf, STR_POS, VA_POS)))
 #else
 #    define FWK_PRINTF(STR_POS, VA_POS)
 #endif
@@ -416,9 +416,9 @@
 
 #if FWK_HAS_GNU_ATTRIBUTE(__access__)
 #    define FWK_READ_ONLY1(REF_POS) \
-        __attribute__((__access__(read_only, REF_POS)))
+	__attribute__((__access__(read_only, REF_POS)))
 #    define FWK_READ_ONLY2(REF_POS, SIZE_POS) \
-        __attribute__((__access__(read_only, REF_POS, SIZE_POS)))
+	__attribute__((__access__(read_only, REF_POS, SIZE_POS)))
 #else
 #    define FWK_READ_ONLY1(REF_POS)
 #    define FWK_READ_ONLY2(REF_POS, SIZE_POS)
@@ -455,9 +455,9 @@
 
 #if FWK_HAS_GNU_ATTRIBUTE(__access__)
 #    define FWK_WRITE_ONLY1(REF_POS) \
-        __attribute__((__access__(write_only, REF_POS)))
+	__attribute__((__access__(write_only, REF_POS)))
 #    define FWK_WRITE_ONLY2(REF_POS, SIZE_POS) \
-        __attribute__((__access__(write_only, REF_POS, SIZE_POS)))
+	__attribute__((__access__(write_only, REF_POS, SIZE_POS)))
 #else
 #    define FWK_WRITE_ONLY1(REF_POS)
 #    define FWK_WRITE_ONLY2(REF_POS, SIZE_POS)
@@ -494,9 +494,9 @@
 
 #if FWK_HAS_GNU_ATTRIBUTE(__access__)
 #    define FWK_READ_WRITE1(REF_POS) \
-        __attribute__((__access__(read_write, REF_POS)))
+	__attribute__((__access__(read_write, REF_POS)))
 #    define FWK_READ_WRITE2(REF_POS, SIZE_POS) \
-        __attribute__((__access__(read_write, REF_POS, SIZE_POS)))
+	__attribute__((__access__(read_write, REF_POS, SIZE_POS)))
 #else
 #    define FWK_READ_WRITE1(REF_POS)
 #    define FWK_READ_WRITE2(REF_POS, SIZE_POS)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/include/fwk_core.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/framework/include/fwk_core.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/include/fwk_core.h
@@ -64,8 +64,8 @@
  */
 #define fwk_put_event(event) \
     _Generic((event), struct fwk_event * \
-             : __fwk_put_event, struct fwk_event_light * \
-             : __fwk_put_event_light)(event)
+	     : __fwk_put_event, struct fwk_event_light * \
+	     : __fwk_put_event_light)(event)
 
 /*!
  * \brief Processing events already raised by modules and interrupt handlers.
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/include/fwk_dlist.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/framework/include/fwk_dlist.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/include/fwk_dlist.h
@@ -64,7 +64,7 @@ struct fwk_dlist_node {
  */
 void __fwk_dlist_push_head(struct fwk_dlist *list, struct fwk_dlist_node *new)
     FWK_LEAF FWK_NOTHROW FWK_NONNULL(1) FWK_NONNULL(2) FWK_READ_WRITE1(1)
-        FWK_READ_WRITE1(2);
+	FWK_READ_WRITE1(2);
 
 /*
  * Add a new node to the end of a doubly-linked list.
@@ -74,7 +74,7 @@ void __fwk_dlist_push_head(struct fwk_dl
  */
 void __fwk_dlist_push_tail(struct fwk_dlist *list, struct fwk_dlist_node *new)
     FWK_LEAF FWK_NOTHROW FWK_NONNULL(1) FWK_NONNULL(2) FWK_READ_WRITE1(1)
-        FWK_READ_WRITE1(2);
+	FWK_READ_WRITE1(2);
 
 /*
  * Remove and return the head node from a doubly-linked list.
@@ -93,7 +93,7 @@ struct fwk_dlist_node *__fwk_dlist_pop_h
  */
 void __fwk_dlist_remove(struct fwk_dlist *list, struct fwk_dlist_node *node)
     FWK_LEAF FWK_NOTHROW FWK_NONNULL(1) FWK_NONNULL(2) FWK_READ_ONLY1(1)
-        FWK_READ_WRITE1(2);
+	FWK_READ_WRITE1(2);
 
 /*
  * Insert a node into a doubly-linked list.
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/include/fwk_id.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/framework/include/fwk_id.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/include/fwk_id.h
@@ -66,9 +66,9 @@ enum fwk_id_type {
  */
 #define FWK_ID_NONE_INIT \
     { \
-        .common = { \
-            .type = (uint32_t)__FWK_ID_TYPE_NONE, \
-        }, \
+	.common = { \
+	    .type = (uint32_t)__FWK_ID_TYPE_NONE, \
+	}, \
     }
 
 /*!
@@ -109,10 +109,10 @@ enum fwk_id_type {
  */
 #define FWK_ID_MODULE_INIT(MODULE_IDX) \
     { \
-        .common = { \
-            .type = (uint32_t)__FWK_ID_TYPE_MODULE, \
-            .module_idx = (uint32_t)MODULE_IDX, \
-        }, \
+	.common = { \
+	    .type = (uint32_t)__FWK_ID_TYPE_MODULE, \
+	    .module_idx = (uint32_t)MODULE_IDX, \
+	}, \
     }
 
 /*!
@@ -153,11 +153,11 @@ enum fwk_id_type {
  */
 #define FWK_ID_ELEMENT_INIT(MODULE_IDX, ELEMENT_IDX) \
     { \
-        .element = { \
-            .type = (uint32_t)__FWK_ID_TYPE_ELEMENT, \
-            .module_idx = (uint32_t)MODULE_IDX, \
-            .element_idx = (uint32_t)ELEMENT_IDX, \
-        }, \
+	.element = { \
+	    .type = (uint32_t)__FWK_ID_TYPE_ELEMENT, \
+	    .module_idx = (uint32_t)MODULE_IDX, \
+	    .element_idx = (uint32_t)ELEMENT_IDX, \
+	}, \
     }
 
 /*!
@@ -192,7 +192,7 @@ enum fwk_id_type {
  * \details Example usage:
  *      \code{.c}
  *      static const fwk_id_t my_sub_element =
-            FWK_ID_SUB_ELEMENT_INIT(42, 58, 1);
+	    FWK_ID_SUB_ELEMENT_INIT(42, 58, 1);
  *      \endcode
  *
  * \param MODULE_IDX Module index.
@@ -203,12 +203,12 @@ enum fwk_id_type {
  */
 #define FWK_ID_SUB_ELEMENT_INIT(MODULE_IDX, ELEMENT_IDX, SUB_ELEMENT_IDX) \
     { \
-        .sub_element = { \
-            .type = (uint32_t)__FWK_ID_TYPE_SUB_ELEMENT, \
-            .module_idx = (uint32_t)MODULE_IDX, \
-            .element_idx = (uint32_t)ELEMENT_IDX, \
-            .sub_element_idx = (uint32_t)SUB_ELEMENT_IDX, \
-        }, \
+	.sub_element = { \
+	    .type = (uint32_t)__FWK_ID_TYPE_SUB_ELEMENT, \
+	    .module_idx = (uint32_t)MODULE_IDX, \
+	    .element_idx = (uint32_t)ELEMENT_IDX, \
+	    .sub_element_idx = (uint32_t)SUB_ELEMENT_IDX, \
+	}, \
     }
 
 /*!
@@ -234,7 +234,7 @@ enum fwk_id_type {
  */
 #define FWK_ID_SUB_ELEMENT(MODULE_IDX, ELEMENT_IDX, SUB_ELEMENT_IDX) \
     ((fwk_id_t)FWK_ID_SUB_ELEMENT_INIT( \
-        MODULE_IDX, ELEMENT_IDX, SUB_ELEMENT_IDX))
+	MODULE_IDX, ELEMENT_IDX, SUB_ELEMENT_IDX))
 
 /*!
  * \brief Build an API identifier from a module index and an API index.
@@ -254,11 +254,11 @@ enum fwk_id_type {
  */
 #define FWK_ID_API_INIT(MODULE_IDX, API_IDX) \
     { \
-        .api = { \
-            .type = (uint32_t)__FWK_ID_TYPE_API, \
-            .module_idx = (uint32_t)MODULE_IDX, \
-            .api_idx = (uint32_t)API_IDX, \
-        }, \
+	.api = { \
+	    .type = (uint32_t)__FWK_ID_TYPE_API, \
+	    .module_idx = (uint32_t)MODULE_IDX, \
+	    .api_idx = (uint32_t)API_IDX, \
+	}, \
     }
 
 /*!
@@ -301,11 +301,11 @@ enum fwk_id_type {
  */
 #define FWK_ID_EVENT_INIT(MODULE_IDX, EVENT_IDX) \
     { \
-        .event = { \
-            .type = (uint32_t)__FWK_ID_TYPE_EVENT, \
-            .module_idx = (uint32_t)MODULE_IDX, \
-            .event_idx = (uint32_t)EVENT_IDX, \
-        }, \
+	.event = { \
+	    .type = (uint32_t)__FWK_ID_TYPE_EVENT, \
+	    .module_idx = (uint32_t)MODULE_IDX, \
+	    .event_idx = (uint32_t)EVENT_IDX, \
+	}, \
     }
 
 /*!
@@ -340,7 +340,7 @@ enum fwk_id_type {
  * \details Example usage:
  *      \code{.c}
  *      static const fwk_id_t my_notification =
-            FWK_ID_NOTIFICATION_INIT(42, 56);
+	    FWK_ID_NOTIFICATION_INIT(42, 56);
  *      \endcode
  *
  * \param MODULE_IDX Module index.
@@ -350,11 +350,11 @@ enum fwk_id_type {
  */
 #define FWK_ID_NOTIFICATION_INIT(MODULE_IDX, NOTIFICATION_IDX) \
     { \
-        .notification = { \
-            .type = (uint32_t)__FWK_ID_TYPE_NOTIFICATION, \
-            .module_idx = (uint32_t)MODULE_IDX, \
-            .notification_idx = (uint32_t)NOTIFICATION_IDX, \
-        }, \
+	.notification = { \
+	    .type = (uint32_t)__FWK_ID_TYPE_NOTIFICATION, \
+	    .module_idx = (uint32_t)MODULE_IDX, \
+	    .notification_idx = (uint32_t)NOTIFICATION_IDX, \
+	}, \
     }
 
 /*!
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/include/fwk_interrupt.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/framework/include/fwk_interrupt.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/include/fwk_interrupt.h
@@ -189,8 +189,8 @@ int fwk_interrupt_set_isr(unsigned int i
  * \retval ::FWK_E_INIT The component has not been initialized.
  */
 int fwk_interrupt_set_isr_param(unsigned int interrupt,
-                                void (*isr)(uintptr_t param),
-                                uintptr_t param);
+				void (*isr)(uintptr_t param),
+				uintptr_t param);
 
 /*!
  * \brief Get the interrupt number for the interrupt service routine being
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/include/fwk_list.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/framework/include/fwk_list.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/include/fwk_list.h
@@ -48,8 +48,8 @@
  */
 #define fwk_list_init(list) \
     _Generic((list), \
-        struct fwk_slist * : __fwk_slist_init, \
-        struct fwk_dlist * : __fwk_slist_init \
+	struct fwk_slist * : __fwk_slist_init, \
+	struct fwk_dlist * : __fwk_slist_init \
     )((struct fwk_slist *)list)
 
 /*!
@@ -62,10 +62,10 @@
  */
 #define fwk_list_head(list) \
     ((void *)_Generic((list), \
-        const struct fwk_slist * : __fwk_slist_head, \
-        const struct fwk_dlist * : __fwk_slist_head, \
-        struct fwk_slist * : __fwk_slist_head, \
-        struct fwk_dlist * : __fwk_slist_head \
+	const struct fwk_slist * : __fwk_slist_head, \
+	const struct fwk_dlist * : __fwk_slist_head, \
+	struct fwk_slist * : __fwk_slist_head, \
+	struct fwk_dlist * : __fwk_slist_head \
     )((const struct fwk_slist *)list))
 
 /*!
@@ -78,10 +78,10 @@
  */
 #define fwk_list_is_empty(list) \
     _Generic((list), \
-        const struct fwk_slist * : __fwk_slist_is_empty, \
-        const struct fwk_dlist * : __fwk_slist_is_empty, \
-        struct fwk_slist * : __fwk_slist_is_empty, \
-        struct fwk_dlist * : __fwk_slist_is_empty \
+	const struct fwk_slist * : __fwk_slist_is_empty, \
+	const struct fwk_dlist * : __fwk_slist_is_empty, \
+	struct fwk_slist * : __fwk_slist_is_empty, \
+	struct fwk_dlist * : __fwk_slist_is_empty \
     )((const struct fwk_slist *)list)
 
 /*!
@@ -96,8 +96,8 @@
  */
 #define fwk_list_push_head(list, new) \
     _Generic((list), \
-        struct fwk_slist * : __fwk_slist_push_head, \
-        struct fwk_dlist * : __fwk_dlist_push_head \
+	struct fwk_slist * : __fwk_slist_push_head, \
+	struct fwk_dlist * : __fwk_dlist_push_head \
     )(list, new)
 
 /*!
@@ -112,8 +112,8 @@
  */
 #define fwk_list_push_tail(list, new) \
     _Generic((list), \
-        struct fwk_slist * : __fwk_slist_push_tail, \
-        struct fwk_dlist * : __fwk_dlist_push_tail \
+	struct fwk_slist * : __fwk_slist_push_tail, \
+	struct fwk_dlist * : __fwk_dlist_push_tail \
     )(list, new)
 
 /*!
@@ -128,8 +128,8 @@
  */
 #define fwk_list_pop_head(list) \
     _Generic((list), \
-        struct fwk_slist * : __fwk_slist_pop_head, \
-        struct fwk_dlist * : __fwk_dlist_pop_head \
+	struct fwk_slist * : __fwk_slist_pop_head, \
+	struct fwk_dlist * : __fwk_dlist_pop_head \
     )(list)
 
 /*!
@@ -144,10 +144,10 @@
  */
 #define fwk_list_next(list, node) \
     ((void *) _Generic((list), \
-        const struct fwk_slist * : __fwk_slist_next, \
-        const struct fwk_dlist * : __fwk_slist_next, \
-        struct fwk_slist * : __fwk_slist_next, \
-        struct fwk_dlist * : __fwk_slist_next \
+	const struct fwk_slist * : __fwk_slist_next, \
+	const struct fwk_dlist * : __fwk_slist_next, \
+	struct fwk_slist * : __fwk_slist_next, \
+	struct fwk_dlist * : __fwk_slist_next \
     )((const struct fwk_slist *)list, (const struct fwk_slist_node *)node))
 
 /*!
@@ -165,8 +165,8 @@
  */
 #define fwk_list_remove(list, node) \
     _Generic((list), \
-        struct fwk_slist * : __fwk_slist_remove, \
-        struct fwk_dlist * : __fwk_dlist_remove \
+	struct fwk_slist * : __fwk_slist_remove, \
+	struct fwk_dlist * : __fwk_dlist_remove \
     )(list, node)
 
 /*!
@@ -183,7 +183,7 @@
  */
 #define fwk_list_insert(list, new, node) \
     _Generic((list), \
-        struct fwk_dlist * : __fwk_dlist_insert \
+	struct fwk_dlist * : __fwk_dlist_insert \
     )(list, new, node)
 
 /*!
@@ -197,10 +197,10 @@
  */
 #define fwk_list_contains(list, node) \
     _Generic((list), \
-        const struct fwk_slist * : __fwk_slist_contains, \
-        const struct fwk_dlist * : __fwk_slist_contains, \
-        struct fwk_slist * : __fwk_slist_contains, \
-        struct fwk_dlist * : __fwk_slist_contains \
+	const struct fwk_slist * : __fwk_slist_contains, \
+	const struct fwk_dlist * : __fwk_slist_contains, \
+	struct fwk_slist * : __fwk_slist_contains, \
+	struct fwk_dlist * : __fwk_slist_contains \
     )((const struct fwk_slist *)list, (const struct fwk_slist_node *)node)
 
 /*!
@@ -214,10 +214,10 @@
  */
 #define FWK_LIST_FOR_EACH(list, node, type, member, elem) \
     for (node = fwk_list_head(list), \
-        elem = FWK_LIST_GET(node, type, member); \
-        node != NULL; \
-        node = fwk_list_next(list, node), \
-        elem = FWK_LIST_GET(node, type, member))
+	elem = FWK_LIST_GET(node, type, member); \
+	node != NULL; \
+	node = fwk_list_next(list, node), \
+	elem = FWK_LIST_GET(node, type, member))
 
 /*!
  * \}
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/include/fwk_macros.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/framework/include/fwk_macros.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/include/fwk_macros.h
@@ -151,9 +151,9 @@
  */
 #define FWK_MIN(a, b) \
     ({ \
-        __auto_type _a = (a); \
-        __auto_type _b = (b); \
-        _a < _b ? _a : _b; \
+	__auto_type _a = (a); \
+	__auto_type _b = (b); \
+	_a < _b ? _a : _b; \
     })
 
 /*!
@@ -170,9 +170,9 @@
  */
 #define FWK_MAX(a, b) \
     ({ \
-        __auto_type _a = (a); \
-        __auto_type _b = (b); \
-        _a > _b ? _a : _b; \
+	__auto_type _a = (a); \
+	__auto_type _b = (b); \
+	_a > _b ? _a : _b; \
     })
 
 /*!
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/include/fwk_module.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/framework/include/fwk_module.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/include/fwk_module.h
@@ -120,7 +120,7 @@ struct fwk_module {
      * \return One of the other module-defined error codes.
      */
     int (*init)(fwk_id_t module_id, unsigned int element_count,
-                const void *data);
+		const void *data);
 
     /*!
      * \brief Pointer to the module element initialization function.
@@ -146,7 +146,7 @@ struct fwk_module {
      * \return One of the other module-defined error codes.
      */
     int (*element_init)(fwk_id_t element_id, unsigned int sub_element_count,
-         const void *data);
+	 const void *data);
 
     /*!
      * \brief Pointer to the module post-initialization function.
@@ -275,7 +275,7 @@ struct fwk_module {
      * \return One of the other module-defined error codes.
      */
     int (*process_bind_request)(fwk_id_t source_id, fwk_id_t target_id,
-                                fwk_id_t api_id, const void **api);
+				fwk_id_t api_id, const void **api);
 
     /*!
      * \brief Process an event.
@@ -300,7 +300,7 @@ struct fwk_module {
      * \return One of the other module-defined error codes.
      */
     int (*process_event)(const struct fwk_event *event,
-                         struct fwk_event *resp_event);
+			 struct fwk_event *resp_event);
 
     /*!
      * \brief Process a notification.
@@ -318,7 +318,7 @@ struct fwk_module {
      * \return One of the other module-defined error codes.
      */
     int (*process_notification)(const struct fwk_event *event,
-                                struct fwk_event *resp_event);
+				struct fwk_event *resp_event);
 
 };
 
@@ -331,8 +331,8 @@ struct fwk_module {
  */
 #define FWK_MODULE_STATIC_ELEMENTS(...) \
     { \
-        .type = FWK_MODULE_ELEMENTS_TYPE_STATIC, \
-        .table = (const struct fwk_element[])__VA_ARGS__, \
+	.type = FWK_MODULE_ELEMENTS_TYPE_STATIC, \
+	.table = (const struct fwk_element[])__VA_ARGS__, \
     }
 
 /*!
@@ -349,8 +349,8 @@ struct fwk_module {
  */
 #define FWK_MODULE_STATIC_ELEMENTS_PTR(TABLE) \
     { \
-        .type = FWK_MODULE_ELEMENTS_TYPE_STATIC, \
-        .table = TABLE, \
+	.type = FWK_MODULE_ELEMENTS_TYPE_STATIC, \
+	.table = TABLE, \
     }
 
 /*!
@@ -366,8 +366,8 @@ struct fwk_module {
  */
 #define FWK_MODULE_DYNAMIC_ELEMENTS(GENERATOR) \
     { \
-        .type = FWK_MODULE_ELEMENTS_TYPE_DYNAMIC, \
-        .generator = (GENERATOR), \
+	.type = FWK_MODULE_ELEMENTS_TYPE_DYNAMIC, \
+	.generator = (GENERATOR), \
     }
 
 /*!
@@ -395,34 +395,34 @@ struct fwk_module_elements {
      * \brief Element table data.
      */
     union {
-        /*!
-         * \brief Pointer to the function to get the table of element
-         *      descriptions.
-         *
-         * \param[in] module_id Identifier of the module.
-         *
-         * \details The table of module element descriptions ends with an
-         *      invalid element description where the pointer to the element
-         *      name is equal to `NULL`.
-         *
-         * \warning The framework does not copy the element description data
-         *      and keep a pointer to the ones returned by this function.
-         *      Pointers returned by this function must thus points to data
-         *      with static storage or data stored in memory allocated from
-         *      the memory management component.
-         *
-         * \return Pointer to table of element descriptions.
-         */
-        const struct fwk_element *(*generator)(fwk_id_t module_id);
-
-        /*!
-         * \brief Table of element descriptions.
-         *
-         * \details The table of module element descriptions ends with an
-         *      invalid element description where the pointer to the element
-         *      name is equal to `NULL`.
-         */
-        const struct fwk_element *table;
+	/*!
+	 * \brief Pointer to the function to get the table of element
+	 *      descriptions.
+	 *
+	 * \param[in] module_id Identifier of the module.
+	 *
+	 * \details The table of module element descriptions ends with an
+	 *      invalid element description where the pointer to the element
+	 *      name is equal to `NULL`.
+	 *
+	 * \warning The framework does not copy the element description data
+	 *      and keep a pointer to the ones returned by this function.
+	 *      Pointers returned by this function must thus points to data
+	 *      with static storage or data stored in memory allocated from
+	 *      the memory management component.
+	 *
+	 * \return Pointer to table of element descriptions.
+	 */
+	const struct fwk_element *(*generator)(fwk_id_t module_id);
+
+	/*!
+	 * \brief Table of element descriptions.
+	 *
+	 * \details The table of module element descriptions ends with an
+	 *      invalid element description where the pointer to the element
+	 *      name is equal to `NULL`.
+	 */
+	const struct fwk_element *table;
     };
 };
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/include/fwk_notification.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/framework/include/fwk_notification.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/include/fwk_notification.h
@@ -53,7 +53,7 @@
  * \retval ::FWK_E_NOMEM The maximum number of subscriptions has been reached.
  */
 int fwk_notification_subscribe(fwk_id_t notification_id, fwk_id_t source_id,
-                               fwk_id_t target_id);
+			       fwk_id_t target_id);
 
 /*!
  * \brief Unsubscribe from a notification.
@@ -70,7 +70,7 @@ int fwk_notification_subscribe(fwk_id_t
  *      notification \p notification_id from the entity \p source_id.
  */
 int fwk_notification_unsubscribe(fwk_id_t notification_id, fwk_id_t source_id,
-                                 fwk_id_t target_id);
+				 fwk_id_t target_id);
 
 /*!
  * \brief Send a notification to all entities that are subscribed to it.
@@ -90,7 +90,7 @@ int fwk_notification_unsubscribe(fwk_id_
  * \retval ::FWK_E_PARAM One of more parameters were invalid.
  */
 int fwk_notification_notify(struct fwk_event *notification_event,
-                            unsigned int *count);
+			    unsigned int *count);
 
 /*!
  * \}
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/include/fwk_slist.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/framework/include/fwk_slist.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/include/fwk_slist.h
@@ -86,7 +86,7 @@ bool __fwk_slist_is_empty(const struct f
  */
 void __fwk_slist_push_head(struct fwk_slist *list, struct fwk_slist_node *new)
     FWK_LEAF FWK_NOTHROW FWK_NONNULL(1) FWK_NONNULL(2) FWK_READ_WRITE1(1)
-        FWK_READ_WRITE1(2);
+	FWK_READ_WRITE1(2);
 
 /*
  * Add a new node to the end of a singly-linked list.
@@ -96,7 +96,7 @@ void __fwk_slist_push_head(struct fwk_sl
  */
 void __fwk_slist_push_tail(struct fwk_slist *list, struct fwk_slist_node *new)
     FWK_LEAF FWK_NOTHROW FWK_NONNULL(1) FWK_NONNULL(2) FWK_READ_WRITE1(1)
-        FWK_READ_WRITE1(2);
+	FWK_READ_WRITE1(2);
 
 /*
  * Remove and return the head node from a singly-linked list.
@@ -126,7 +126,7 @@ struct fwk_slist_node *__fwk_slist_next(
  */
 void __fwk_slist_remove(struct fwk_slist *list, struct fwk_slist_node *node)
     FWK_LEAF FWK_NOTHROW FWK_NONNULL(1) FWK_NONNULL(2) FWK_READ_WRITE1(1)
-        FWK_READ_ONLY1(2);
+	FWK_READ_ONLY1(2);
 
 /*
  * Test if a node is in a singly-linked list.
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/include/internal/fwk_id.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/framework/include/internal/fwk_id.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/include/internal/fwk_id.h
@@ -65,44 +65,44 @@ union __fwk_id {
     uint32_t value; /* Integer value */
 
     struct {
-        uint32_t type : 4; /* Identifier type */
-        uint32_t module_idx : 8; /* Module index */
-        uint32_t reserved : 20; /* Reserved */
+	uint32_t type : 4; /* Identifier type */
+	uint32_t module_idx : 8; /* Module index */
+	uint32_t reserved : 20; /* Reserved */
     } common; /* Common fields */
 
     struct {
-        uint32_t type : 4; /* Identifier type */
-        uint32_t module_idx : 8; /* Module index */
-        uint32_t element_idx : 12; /* Element index */
-        uint32_t reserved : 8; /* Reserved */
+	uint32_t type : 4; /* Identifier type */
+	uint32_t module_idx : 8; /* Module index */
+	uint32_t element_idx : 12; /* Element index */
+	uint32_t reserved : 8; /* Reserved */
     } element; /* Element variant */
 
     struct {
-        uint32_t type : 4; /* Identifier type */
-        uint32_t module_idx : 8; /* Module index */
-        uint32_t element_idx : 12; /* Element index */
-        uint32_t sub_element_idx : 8; /* Sub-element index */
+	uint32_t type : 4; /* Identifier type */
+	uint32_t module_idx : 8; /* Module index */
+	uint32_t element_idx : 12; /* Element index */
+	uint32_t sub_element_idx : 8; /* Sub-element index */
     } sub_element; /* Sub-element variant */
 
     struct {
-        uint32_t type : 4; /* Identifier type */
-        uint32_t module_idx : 8; /* Module index */
-        uint32_t api_idx : 4; /* API index */
-        uint32_t reserved : 16; /* Reserved */
+	uint32_t type : 4; /* Identifier type */
+	uint32_t module_idx : 8; /* Module index */
+	uint32_t api_idx : 4; /* API index */
+	uint32_t reserved : 16; /* Reserved */
     } api; /* API variant */
 
     struct {
-        uint32_t type : 4; /* Identifier type */
-        uint32_t module_idx : 8; /* Module index */
-        uint32_t event_idx : 6; /* Event index */
-        uint32_t reserved : 14; /* Reserved */
+	uint32_t type : 4; /* Identifier type */
+	uint32_t module_idx : 8; /* Module index */
+	uint32_t event_idx : 6; /* Event index */
+	uint32_t reserved : 14; /* Reserved */
     } event; /* Event variant */
 
     struct {
-        uint32_t type : 4; /* Identifier type */
-        uint32_t module_idx : 8; /* Module index */
-        uint32_t notification_idx : 6; /* Notification index */
-        uint32_t reserved : 14; /* Reserved */
+	uint32_t type : 4; /* Identifier type */
+	uint32_t module_idx : 8; /* Module index */
+	uint32_t notification_idx : 6; /* Notification index */
+	uint32_t reserved : 14; /* Reserved */
     } notification; /* Notification variant */
 };
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/src/assert.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/framework/src/assert.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/src/assert.c
@@ -11,7 +11,7 @@
 void __aeabi_assert(const char *expr, const char *file, int line)
 {
     while (1) {
-        continue;
+	continue;
     }
 }
 #elif defined(__NEWLIB__)
@@ -22,7 +22,7 @@ void __assert_func(
     const char *assertion)
 {
     while (1) {
-        continue;
+	continue;
     }
 }
 #endif
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/src/fwk_arch.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/framework/src/fwk_arch.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/src/fwk_arch.c
@@ -43,13 +43,13 @@ static int fwk_arch_interrupt_init(int (
      */
     status = interrupt_init_handler(&driver);
     if (status != FWK_SUCCESS) {
-        return FWK_E_PANIC;
+	return FWK_E_PANIC;
     }
 
     /* Initialize the interrupt management component */
     status = fwk_interrupt_init(driver);
     if (status != FWK_SUCCESS) {
-        return FWK_E_PANIC;
+	return FWK_E_PANIC;
     }
 
     return FWK_SUCCESS;
@@ -60,34 +60,34 @@ int fwk_arch_init(const struct fwk_arch_
     int status;
 
     if (driver == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     if (driver->interrupt == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     fwk_module_init();
 
     status = fwk_io_init();
     if (!fwk_expect(status == FWK_SUCCESS)) {
-        return FWK_E_PANIC;
+	return FWK_E_PANIC;
     }
 
     status = fwk_log_init();
     if (!fwk_expect(status == FWK_SUCCESS)) {
-        return FWK_E_PANIC;
+	return FWK_E_PANIC;
     }
 
     /* Initialize interrupt management */
     status = fwk_arch_interrupt_init(driver->interrupt);
     if (!fwk_expect(status == FWK_SUCCESS)) {
-        return FWK_E_PANIC;
+	return FWK_E_PANIC;
     }
 
     status = fwk_module_start();
     if (!fwk_expect(status == FWK_SUCCESS)) {
-        return FWK_E_PANIC;
+	return FWK_E_PANIC;
     }
 
     /*
@@ -111,7 +111,7 @@ int fwk_arch_deinit(void)
 
     status = fwk_module_stop();
     if (!fwk_expect(status == FWK_SUCCESS)) {
-        return FWK_E_PANIC;
+	return FWK_E_PANIC;
     }
 
     return FWK_SUCCESS;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/src/fwk_core.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/framework/src/fwk_core.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/src/fwk_core.c
@@ -67,26 +67,26 @@ static struct fwk_event *duplicate_event
 
     flags = fwk_interrupt_global_disable();
     allocated_event = FWK_LIST_GET(
-        fwk_list_pop_head(&ctx.free_event_queue), struct fwk_event, slist_node);
+	fwk_list_pop_head(&ctx.free_event_queue), struct fwk_event, slist_node);
     (void)fwk_interrupt_global_enable(flags);
 
     if (allocated_event == NULL) {
-        FWK_LOG_CRIT(err_msg_func, FWK_E_NOMEM, __func__);
-        fwk_unexpected();
+	FWK_LOG_CRIT(err_msg_func, FWK_E_NOMEM, __func__);
+	fwk_unexpected();
 
-        return NULL;
+	return NULL;
     }
     if (event_type == FWK_EVENT_TYPE_LIGHT) {
-        struct fwk_event_light *light_event = (struct fwk_event_light *)event;
-        allocated_event->id = light_event->id;
-        allocated_event->source_id = light_event->source_id;
-        allocated_event->target_id = light_event->target_id;
-        allocated_event->is_notification = false;
-        allocated_event->response_requested = light_event->response_requested;
-        allocated_event->is_delayed_response = false;
-        allocated_event->is_response = false;
+	struct fwk_event_light *light_event = (struct fwk_event_light *)event;
+	allocated_event->id = light_event->id;
+	allocated_event->source_id = light_event->source_id;
+	allocated_event->target_id = light_event->target_id;
+	allocated_event->is_notification = false;
+	allocated_event->response_requested = light_event->response_requested;
+	allocated_event->is_delayed_response = false;
+	allocated_event->is_response = false;
     } else {
-        *allocated_event = *((struct fwk_event *)event);
+	*allocated_event = *((struct fwk_event *)event);
     }
 
     allocated_event->slist_node = (struct fwk_slist_node){ 0 };
@@ -104,58 +104,58 @@ static int put_event(
     struct fwk_event *std_event = NULL;
 
     if (event_type == FWK_EVENT_TYPE_STD) {
-        std_event = (struct fwk_event *)event;
+	std_event = (struct fwk_event *)event;
     }
 
     if (std_event != NULL && std_event->is_delayed_response) {
-        allocated_event = __fwk_search_delayed_response(
-            std_event->source_id, std_event->cookie);
-        if (allocated_event == NULL) {
-            FWK_LOG_CRIT(err_msg_func, FWK_E_NOMEM, __func__);
-            return FWK_E_PARAM;
-        }
-
-        fwk_list_remove(
-            __fwk_get_delayed_response_list(std_event->source_id),
-            &allocated_event->slist_node);
-
-        (void)memcpy(
-            allocated_event->params,
-            std_event->params,
-            sizeof(allocated_event->params));
+	allocated_event = __fwk_search_delayed_response(
+	    std_event->source_id, std_event->cookie);
+	if (allocated_event == NULL) {
+	    FWK_LOG_CRIT(err_msg_func, FWK_E_NOMEM, __func__);
+	    return FWK_E_PARAM;
+	}
+
+	fwk_list_remove(
+	    __fwk_get_delayed_response_list(std_event->source_id),
+	    &allocated_event->slist_node);
+
+	(void)memcpy(
+	    allocated_event->params,
+	    std_event->params,
+	    sizeof(allocated_event->params));
 
     } else {
-        allocated_event = duplicate_event(event, event_type);
-        if (allocated_event == NULL) {
-            return FWK_E_NOMEM;
-        }
+	allocated_event = duplicate_event(event, event_type);
+	if (allocated_event == NULL) {
+	    return FWK_E_NOMEM;
+	}
     }
 
     if (std_event != NULL) {
-        allocated_event->cookie = ctx.event_cookie_counter++;
-        std_event->cookie = allocated_event->cookie;
+	allocated_event->cookie = ctx.event_cookie_counter++;
+	std_event->cookie = allocated_event->cookie;
     }
 
     if (intr_state == UNKNOWN_STATE) {
-        if (fwk_is_interrupt_context()) {
-            intr_state = INTERRUPT_STATE;
-        } else {
-            intr_state = NOT_INTERRUPT_STATE;
-        }
+	if (fwk_is_interrupt_context()) {
+	    intr_state = INTERRUPT_STATE;
+	} else {
+	    intr_state = NOT_INTERRUPT_STATE;
+	}
     }
     if (intr_state == NOT_INTERRUPT_STATE) {
-        fwk_list_push_tail(&ctx.event_queue, &allocated_event->slist_node);
+	fwk_list_push_tail(&ctx.event_queue, &allocated_event->slist_node);
     } else {
-        fwk_list_push_tail(&ctx.isr_event_queue, &allocated_event->slist_node);
+	fwk_list_push_tail(&ctx.isr_event_queue, &allocated_event->slist_node);
     }
 
 #if FWK_LOG_LEVEL <= FWK_LOG_LEVEL_DEBUG
     FWK_LOG_DEBUG(
-        "[FWK] Sent %" PRIu32 ": %s @ %s -> %s",
-        std_event != NULL ? std_event->cookie : 0,
-        FWK_ID_STR(allocated_event->id),
-        FWK_ID_STR(allocated_event->source_id),
-        FWK_ID_STR(allocated_event->target_id));
+	"[FWK] Sent %" PRIu32 ": %s @ %s -> %s",
+	std_event != NULL ? std_event->cookie : 0,
+	FWK_ID_STR(allocated_event->id),
+	FWK_ID_STR(allocated_event->source_id),
+	FWK_ID_STR(allocated_event->target_id));
 #endif
 
     return FWK_SUCCESS;
@@ -176,61 +176,61 @@ static void process_next_event(void)
     struct fwk_event *event, *allocated_event, async_response_event;
     const struct fwk_module *module;
     int (*process_event)(
-        const struct fwk_event *event, struct fwk_event *resp_event);
+	const struct fwk_event *event, struct fwk_event *resp_event);
 
     ctx.current_event = event = FWK_LIST_GET(
-        fwk_list_pop_head(&ctx.event_queue), struct fwk_event, slist_node);
+	fwk_list_pop_head(&ctx.event_queue), struct fwk_event, slist_node);
 
 #if FWK_LOG_LEVEL <= FWK_LOG_LEVEL_DEBUG
     FWK_LOG_DEBUG(
-        "[FWK] Processing %" PRIu32 ": %s @ %s -> %s\n",
-        event->cookie,
-        FWK_ID_STR(event->id),
-        FWK_ID_STR(event->source_id),
-        FWK_ID_STR(event->target_id));
+	"[FWK] Processing %" PRIu32 ": %s @ %s -> %s\n",
+	event->cookie,
+	FWK_ID_STR(event->id),
+	FWK_ID_STR(event->source_id),
+	FWK_ID_STR(event->target_id));
 #endif
 
     module = fwk_module_get_ctx(event->target_id)->desc;
     process_event = event->is_notification ? module->process_notification :
-                                             module->process_event;
+					     module->process_event;
 
     if (event->response_requested) {
-        fwk_str_memset(&async_response_event, 0, sizeof(async_response_event));
-        async_response_event = *event;
-        async_response_event.source_id = event->target_id;
-        async_response_event.target_id = event->source_id;
-        async_response_event.is_delayed_response = false;
-
-        status = process_event(event, &async_response_event);
-        if (status != FWK_SUCCESS) {
-            FWK_LOG_CRIT(err_msg_line, status, __func__, __LINE__);
-        }
-
-        async_response_event.is_response = true;
-        async_response_event.response_requested = false;
-        if (!async_response_event.is_delayed_response) {
-            (void)put_event(
-                &async_response_event, UNKNOWN_STATE, FWK_EVENT_TYPE_STD);
-        } else {
-            allocated_event =
-                duplicate_event(&async_response_event, FWK_EVENT_TYPE_STD);
-            if (allocated_event != NULL) {
-                fwk_list_push_tail(
-                    __fwk_get_delayed_response_list(
-                        async_response_event.source_id),
-                    &allocated_event->slist_node);
-            }
-        }
+	fwk_str_memset(&async_response_event, 0, sizeof(async_response_event));
+	async_response_event = *event;
+	async_response_event.source_id = event->target_id;
+	async_response_event.target_id = event->source_id;
+	async_response_event.is_delayed_response = false;
+
+	status = process_event(event, &async_response_event);
+	if (status != FWK_SUCCESS) {
+	    FWK_LOG_CRIT(err_msg_line, status, __func__, __LINE__);
+	}
+
+	async_response_event.is_response = true;
+	async_response_event.response_requested = false;
+	if (!async_response_event.is_delayed_response) {
+	    (void)put_event(
+		&async_response_event, UNKNOWN_STATE, FWK_EVENT_TYPE_STD);
+	} else {
+	    allocated_event =
+		duplicate_event(&async_response_event, FWK_EVENT_TYPE_STD);
+	    if (allocated_event != NULL) {
+		fwk_list_push_tail(
+		    __fwk_get_delayed_response_list(
+			async_response_event.source_id),
+		    &allocated_event->slist_node);
+	    }
+	}
     } else {
-        status = process_event(event, &async_response_event);
-        if ((status != FWK_SUCCESS) && (status != FWK_PENDING)) {
-            FWK_LOG_CRIT(
-                "[FWK] Process event (%s: %s -> %s) (%d)\n",
-                FWK_ID_STR(event->id),
-                FWK_ID_STR(event->source_id),
-                FWK_ID_STR(event->target_id),
-                status);
-        }
+	status = process_event(event, &async_response_event);
+	if ((status != FWK_SUCCESS) && (status != FWK_PENDING)) {
+	    FWK_LOG_CRIT(
+		"[FWK] Process event (%s: %s -> %s) (%d)\n",
+		FWK_ID_STR(event->id),
+		FWK_ID_STR(event->source_id),
+		FWK_ID_STR(event->target_id),
+		status);
+	}
     }
 
     ctx.current_event = NULL;
@@ -245,19 +245,19 @@ static bool process_isr(void)
 
     flags = fwk_interrupt_global_disable();
     isr_event = FWK_LIST_GET(
-        fwk_list_pop_head(&ctx.isr_event_queue), struct fwk_event, slist_node);
+	fwk_list_pop_head(&ctx.isr_event_queue), struct fwk_event, slist_node);
     (void)fwk_interrupt_global_enable(flags);
 
     if (isr_event == NULL) {
-        return false;
+	return false;
     }
 
 #if FWK_LOG_LEVEL <= FWK_LOG_LEVEL_DEBUG
     FWK_LOG_DEBUG(
-        "[FWK] Pulled ISR event (%s: %s -> %s)\n",
-        FWK_ID_STR(isr_event->id),
-        FWK_ID_STR(isr_event->source_id),
-        FWK_ID_STR(isr_event->target_id));
+	"[FWK] Pulled ISR event (%s: %s -> %s)\n",
+	FWK_ID_STR(isr_event->id),
+	FWK_ID_STR(isr_event->source_id),
+	FWK_ID_STR(isr_event->target_id));
 #endif
 
     fwk_list_push_tail(&ctx.event_queue, &isr_event->slist_node);
@@ -281,7 +281,7 @@ int __fwk_init(size_t event_count)
     fwk_list_init(&ctx.isr_event_queue);
 
     for (event = event_table; event < (event_table + event_count); event++) {
-        fwk_list_push_tail(&ctx.free_event_queue, &event->slist_node);
+	fwk_list_push_tail(&ctx.free_event_queue, &event->slist_node);
     }
 
     ctx.initialized = true;
@@ -292,23 +292,23 @@ int __fwk_init(size_t event_count)
 void fwk_process_event_queue(void)
 {
     for (;;) {
-        while (!fwk_list_is_empty(&ctx.event_queue)) {
-            process_next_event();
-        }
-
-        if (!process_isr()) {
-            break;
-        }
+	while (!fwk_list_is_empty(&ctx.event_queue)) {
+	    process_next_event();
+	}
+
+	if (!process_isr()) {
+	    break;
+	}
     }
 }
 
 noreturn void __fwk_run_main_loop(void)
 {
     for (;;) {
-        fwk_process_event_queue();
-        if (fwk_log_unbuffer() == FWK_SUCCESS) {
-            fwk_arch_suspend();
-        }
+	fwk_process_event_queue();
+	if (fwk_log_unbuffer() == FWK_SUCCESS) {
+	    fwk_arch_suspend();
+	}
     }
 }
 
@@ -343,61 +343,61 @@ int __fwk_put_event(struct fwk_event *ev
 
 #ifdef BUILD_MODE_DEBUG
     if (!ctx.initialized) {
-        status = FWK_E_INIT;
-        goto error;
+	status = FWK_E_INIT;
+	goto error;
     }
 
     if (event == NULL) {
-        goto error;
+	goto error;
     }
 #endif
 
     if (fwk_is_interrupt_context()) {
-        intr_state = INTERRUPT_STATE;
+	intr_state = INTERRUPT_STATE;
     } else {
-        intr_state = NOT_INTERRUPT_STATE;
+	intr_state = NOT_INTERRUPT_STATE;
     }
 
     if ((intr_state == NOT_INTERRUPT_STATE) && (ctx.current_event != NULL)) {
-        event->source_id = ctx.current_event->target_id;
+	event->source_id = ctx.current_event->target_id;
     } else if (
-        !fwk_id_type_is_valid(event->source_id) ||
-        !fwk_module_is_valid_entity_id(event->source_id)) {
-        status = FWK_E_PARAM;
-        goto error;
+	!fwk_id_type_is_valid(event->source_id) ||
+	!fwk_module_is_valid_entity_id(event->source_id)) {
+	status = FWK_E_PARAM;
+	goto error;
     }
 
 #ifdef BUILD_MODE_DEBUG
     status = FWK_E_PARAM;
     if (event->is_notification) {
-        if (!fwk_module_is_valid_notification_id(event->id)) {
-            goto error;
-        }
-        if ((!event->is_response) || (event->response_requested)) {
-            goto error;
-        }
-        if (fwk_id_get_module_idx(event->target_id) !=
-            fwk_id_get_module_idx(event->id)) {
-            goto error;
-        }
+	if (!fwk_module_is_valid_notification_id(event->id)) {
+	    goto error;
+	}
+	if ((!event->is_response) || (event->response_requested)) {
+	    goto error;
+	}
+	if (fwk_id_get_module_idx(event->target_id) !=
+	    fwk_id_get_module_idx(event->id)) {
+	    goto error;
+	}
     } else {
-        if (!fwk_module_is_valid_event_id(event->id)) {
-            goto error;
-        }
-        if (event->is_response) {
-            if (fwk_id_get_module_idx(event->source_id) !=
-                fwk_id_get_module_idx(event->id)) {
-                goto error;
-            }
-            if (event->response_requested) {
-                goto error;
-            }
-        } else {
-            if (fwk_id_get_module_idx(event->target_id) !=
-                fwk_id_get_module_idx(event->id)) {
-                goto error;
-            }
-        }
+	if (!fwk_module_is_valid_event_id(event->id)) {
+	    goto error;
+	}
+	if (event->is_response) {
+	    if (fwk_id_get_module_idx(event->source_id) !=
+		fwk_id_get_module_idx(event->id)) {
+		goto error;
+	    }
+	    if (event->response_requested) {
+		goto error;
+	    }
+	} else {
+	    if (fwk_id_get_module_idx(event->target_id) !=
+		fwk_id_get_module_idx(event->id)) {
+		goto error;
+	    }
+	}
     }
 #endif
 
@@ -415,40 +415,40 @@ int __fwk_put_event_light(struct fwk_eve
 
 #ifdef BUILD_MODE_DEBUG
     if (!ctx.initialized) {
-        status = FWK_E_INIT;
-        goto error;
+	status = FWK_E_INIT;
+	goto error;
     }
 
     if (event == NULL) {
-        goto error;
+	goto error;
     }
 #endif
 
     if (fwk_is_interrupt_context()) {
-        intr_state = INTERRUPT_STATE;
+	intr_state = INTERRUPT_STATE;
     } else {
-        intr_state = NOT_INTERRUPT_STATE;
+	intr_state = NOT_INTERRUPT_STATE;
     }
 
     if ((intr_state == NOT_INTERRUPT_STATE) && (ctx.current_event != NULL)) {
-        event->source_id = ctx.current_event->target_id;
+	event->source_id = ctx.current_event->target_id;
     } else if (
-        !fwk_id_type_is_valid(event->source_id) ||
-        !fwk_module_is_valid_entity_id(event->source_id)) {
-        status = FWK_E_PARAM;
-        goto error;
+	!fwk_id_type_is_valid(event->source_id) ||
+	!fwk_module_is_valid_entity_id(event->source_id)) {
+	status = FWK_E_PARAM;
+	goto error;
     }
 
 #ifdef BUILD_MODE_DEBUG
     status = FWK_E_PARAM;
 
     if (!fwk_module_is_valid_event_id(event->id)) {
-        goto error;
+	goto error;
     }
 
     if (fwk_id_get_module_idx(event->target_id) !=
-        fwk_id_get_module_idx(event->id)) {
-        goto error;
+	fwk_id_get_module_idx(event->id)) {
+	goto error;
     }
 
 #endif
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/src/fwk_delayed_resp.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/framework/src/fwk_delayed_resp.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/src/fwk_delayed_resp.c
@@ -27,11 +27,11 @@ static const char err_msg_func[] = "[FWK
 static int check_api_call(fwk_id_t id, void *data)
 {
     if (fwk_is_interrupt_context()) {
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 
     if ((!fwk_module_is_valid_entity_id(id)) || (data == NULL)) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     return FWK_SUCCESS;
@@ -43,7 +43,7 @@ static int check_api_call(fwk_id_t id, v
 struct fwk_slist *__fwk_get_delayed_response_list(fwk_id_t id)
 {
     if (fwk_id_is_type(id, FWK_ID_TYPE_MODULE)) {
-        return &fwk_module_get_ctx(id)->delayed_response_list;
+	return &fwk_module_get_ctx(id)->delayed_response_list;
     }
 
     return &fwk_module_get_element_ctx(id)->delayed_response_list;
@@ -58,16 +58,16 @@ struct fwk_event *__fwk_search_delayed_r
     delayed_response_node = fwk_list_head(delayed_response_list);
 
     while (delayed_response_node != NULL) {
-        struct fwk_event *delayed_response;
+	struct fwk_event *delayed_response;
 
-        delayed_response =
-            FWK_LIST_GET(delayed_response_node, struct fwk_event, slist_node);
-        if (delayed_response->cookie == cookie) {
-            return delayed_response;
-        }
+	delayed_response =
+	    FWK_LIST_GET(delayed_response_node, struct fwk_event, slist_node);
+	if (delayed_response->cookie == cookie) {
+	    return delayed_response;
+	}
 
-        delayed_response_node =
-            fwk_list_next(delayed_response_list, delayed_response_node);
+	delayed_response_node =
+	    fwk_list_next(delayed_response_list, delayed_response_node);
     }
 
     return NULL;
@@ -86,13 +86,13 @@ int fwk_get_delayed_response(
 
     status = check_api_call(id, event);
     if (status != FWK_SUCCESS) {
-        goto error;
+	goto error;
     }
 
     delayed_response = __fwk_search_delayed_response(id, cookie);
     if (delayed_response == NULL) {
-        status = FWK_E_PARAM;
-        goto error;
+	status = FWK_E_PARAM;
+	goto error;
     }
 
     *event = *delayed_response;
@@ -112,7 +112,7 @@ int fwk_is_delayed_response_list_empty(f
 
     status = check_api_call(id, is_empty);
     if (status != FWK_SUCCESS) {
-        goto error;
+	goto error;
     }
 
     delayed_response_list = __fwk_get_delayed_response_list(id);
@@ -135,17 +135,17 @@ int fwk_get_first_delayed_response(fwk_i
 
     status = check_api_call(id, event);
     if (status != FWK_SUCCESS) {
-        goto error;
+	goto error;
     }
 
     delayed_response_list = __fwk_get_delayed_response_list(id);
     delayed_response_node = fwk_list_head(delayed_response_list);
 
     if (delayed_response_node != NULL) {
-        *event = *(
-            FWK_LIST_GET(delayed_response_node, struct fwk_event, slist_node));
+	*event = *(
+	    FWK_LIST_GET(delayed_response_node, struct fwk_event, slist_node));
     } else {
-        return FWK_E_STATE;
+	return FWK_E_STATE;
     }
 
     return FWK_SUCCESS;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/src/fwk_dlist.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/framework/src/fwk_dlist.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/src/fwk_dlist.c
@@ -26,8 +26,8 @@ void __fwk_dlist_push_head(
     list->head->prev = new;
 
     __fwk_slist_push_head(
-        (struct fwk_slist *)list,
-        (struct fwk_slist_node *)new);
+	(struct fwk_slist *)list,
+	(struct fwk_slist_node *)new);
 }
 
 void __fwk_dlist_push_tail(
@@ -41,8 +41,8 @@ void __fwk_dlist_push_tail(
     new->prev = list->tail;
 
     __fwk_slist_push_tail(
-        (struct fwk_slist *)list,
-        (struct fwk_slist_node *)new);
+	(struct fwk_slist *)list,
+	(struct fwk_slist_node *)new);
 }
 
 struct fwk_dlist_node *__fwk_dlist_pop_head(struct fwk_dlist *list)
@@ -52,12 +52,12 @@ struct fwk_dlist_node *__fwk_dlist_pop_h
     fwk_assert(list != NULL);
 
     popped = (struct fwk_dlist_node *)__fwk_slist_pop_head(
-        (struct fwk_slist *)list);
+	(struct fwk_slist *)list);
 
     list->head->prev = (struct fwk_dlist_node *)list;
 
     if (popped != NULL) {
-        popped->prev = NULL;
+	popped->prev = NULL;
     }
 
     return popped;
@@ -74,8 +74,8 @@ void __fwk_dlist_remove(
     fwk_assert(node->next != NULL);
 
     fwk_assert(__fwk_slist_contains(
-        (struct fwk_slist *)list,
-        (struct fwk_slist_node *)node));
+	(struct fwk_slist *)list,
+	(struct fwk_slist_node *)node));
 
     node->prev->next = node->next;
     node->next->prev = node->prev;
@@ -96,17 +96,17 @@ void __fwk_dlist_insert(
     fwk_check(new->prev == NULL);
 
     if (node == NULL) {
-        __fwk_dlist_push_tail(list, new);
+	__fwk_dlist_push_tail(list, new);
 
-        return;
+	return;
     }
 
     fwk_assert(node->prev != NULL);
     fwk_assert(node->next != NULL);
 
     fwk_assert(__fwk_slist_contains(
-        (struct fwk_slist *)list,
-        (struct fwk_slist_node *)node));
+	(struct fwk_slist *)list,
+	(struct fwk_slist_node *)node));
 
     node->prev->next = new;
     new->prev = node->prev;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/src/fwk_id.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/framework/src/fwk_id.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/src/fwk_id.c
@@ -23,14 +23,14 @@
 static void fwk_id_format(char *buffer, size_t buffer_size, fwk_id_t id)
 {
     static const char *types[__FWK_ID_TYPE_COUNT] = {
-        [__FWK_ID_TYPE_INVALID] = "INV",
-        [__FWK_ID_TYPE_NONE] = "NON",
-        [__FWK_ID_TYPE_MODULE] = "MOD",
-        [__FWK_ID_TYPE_ELEMENT] = "ELM",
-        [__FWK_ID_TYPE_SUB_ELEMENT] = "SUB",
-        [__FWK_ID_TYPE_API] = "API",
-        [__FWK_ID_TYPE_EVENT] = "EVT",
-        [__FWK_ID_TYPE_NOTIFICATION] = "NOT",
+	[__FWK_ID_TYPE_INVALID] = "INV",
+	[__FWK_ID_TYPE_NONE] = "NON",
+	[__FWK_ID_TYPE_MODULE] = "MOD",
+	[__FWK_ID_TYPE_ELEMENT] = "ELM",
+	[__FWK_ID_TYPE_SUB_ELEMENT] = "SUB",
+	[__FWK_ID_TYPE_API] = "API",
+	[__FWK_ID_TYPE_EVENT] = "EVT",
+	[__FWK_ID_TYPE_NOTIFICATION] = "NOT",
     };
 
     size_t length = 0;
@@ -42,53 +42,53 @@ static void fwk_id_format(char *buffer,
     fwk_assert(buffer_size > 0);
 
     if (id.common.type >= FWK_ARRAY_SIZE(types) ||
-        id.common.type == (uint32_t)__FWK_ID_TYPE_INVALID) {
-        (void)snprintf(
-            buffer,
-            buffer_size,
-            "[%s]",
-            types[(uint32_t)__FWK_ID_TYPE_INVALID]);
-        return;
+	id.common.type == (uint32_t)__FWK_ID_TYPE_INVALID) {
+	(void)snprintf(
+	    buffer,
+	    buffer_size,
+	    "[%s]",
+	    types[(uint32_t)__FWK_ID_TYPE_INVALID]);
+	return;
     }
 
     indices[0] = id.common.module_idx;
 
     switch (com_id_type) {
     case __FWK_ID_TYPE_SUB_ELEMENT:
-        indices[2] = id.sub_element.sub_element_idx;
+	indices[2] = id.sub_element.sub_element_idx;
 
-        FWK_FALLTHROUGH;
+	FWK_FALLTHROUGH;
 
     case __FWK_ID_TYPE_ELEMENT:
-        indices[1] = id.element.element_idx;
+	indices[1] = id.element.element_idx;
 
-        break;
+	break;
 
     case __FWK_ID_TYPE_API:
-        indices[1] = id.api.api_idx;
+	indices[1] = id.api.api_idx;
 
-        break;
+	break;
 
     case __FWK_ID_TYPE_EVENT:
-        indices[1] = id.event.event_idx;
+	indices[1] = id.event.event_idx;
 
-        break;
+	break;
 
     case __FWK_ID_TYPE_NOTIFICATION:
-        indices[1] = id.notification.notification_idx;
+	indices[1] = id.notification.notification_idx;
 
-        break;
+	break;
 
     default:
-        break;
+	break;
     }
 
     length += snprintf(
-        buffer + length,
-        buffer_size - length,
-        "[%s %u",
-        types[id.common.type],
-        indices[0]);
+	buffer + length,
+	buffer_size - length,
+	"[%s %u",
+	types[id.common.type],
+	indices[0]);
 
     switch (com_id_type) {
     case __FWK_ID_TYPE_ELEMENT:
@@ -96,24 +96,24 @@ static void fwk_id_format(char *buffer,
     case __FWK_ID_TYPE_API:
     case __FWK_ID_TYPE_EVENT:
     case __FWK_ID_TYPE_NOTIFICATION:
-        length +=
-            snprintf(buffer + length, buffer_size - length, ":%u", indices[1]);
+	length +=
+	    snprintf(buffer + length, buffer_size - length, ":%u", indices[1]);
 
-        break;
+	break;
 
     default:
-        break;
+	break;
     }
 
     switch (com_id_type) {
     case __FWK_ID_TYPE_SUB_ELEMENT:
-        length +=
-            snprintf(buffer + length, buffer_size - length, ":%u", indices[2]);
+	length +=
+	    snprintf(buffer + length, buffer_size - length, ":%u", indices[2]);
 
-        break;
+	break;
 
     default:
-        break;
+	break;
     }
 
     (void)snprintf(buffer + length, buffer_size - length, "]");
@@ -139,8 +139,8 @@ bool fwk_id_is_type(fwk_id_t id, enum fw
 bool fwk_id_type_is_valid(fwk_id_t id)
 {
     if ((id.common.type != __FWK_ID_TYPE_INVALID) &&
-        (id.common.type < __FWK_ID_TYPE_COUNT)) {
-        return true;
+	(id.common.type < __FWK_ID_TYPE_COUNT)) {
+	return true;
     }
 
     return false;
@@ -189,7 +189,7 @@ fwk_id_t fwk_id_build_sub_element_id(fwk
     fwk_assert(id.common.type == __FWK_ID_TYPE_ELEMENT);
 
     return FWK_ID_SUB_ELEMENT(
-        id.common.module_idx, id.element.element_idx, sub_element_idx);
+	id.common.module_idx, id.element.element_idx, sub_element_idx);
 }
 
 fwk_id_t fwk_id_build_api_id(fwk_id_t id, unsigned int api_idx)
@@ -216,8 +216,8 @@ unsigned int fwk_id_get_module_idx(fwk_i
 unsigned int fwk_id_get_element_idx(fwk_id_t element_id)
 {
     fwk_assert(
-        (element_id.common.type == __FWK_ID_TYPE_ELEMENT) ||
-        (element_id.common.type == __FWK_ID_TYPE_SUB_ELEMENT));
+	(element_id.common.type == __FWK_ID_TYPE_ELEMENT) ||
+	(element_id.common.type == __FWK_ID_TYPE_SUB_ELEMENT));
 
     return element_id.element.element_idx;
 }
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/src/fwk_interrupt.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/framework/src/fwk_interrupt.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/src/fwk_interrupt.c
@@ -24,49 +24,49 @@ int fwk_interrupt_init(const struct fwk_
 {
     /* Validate driver by checking that all function pointers are non-null */
     if (driver == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
     if (driver->global_enable == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
     if (driver->global_disable == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
     if (driver->is_enabled == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
     if (driver->enable == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
     if (driver->disable == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
     if (driver->is_pending == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
     if (driver->set_pending == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
     if (driver->clear_pending == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
     if (driver->set_isr_irq == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
     if (driver->set_isr_irq_param == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
     if (driver->set_isr_nmi == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
     if (driver->set_isr_nmi_param == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
     if (driver->set_isr_fault == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
     if (driver->get_current == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     fwk_interrupt_driver = driver;
@@ -78,11 +78,11 @@ int fwk_interrupt_init(const struct fwk_
 int fwk_interrupt_is_enabled(unsigned int interrupt, bool *enabled)
 {
     if (!initialized) {
-        return FWK_E_INIT;
+	return FWK_E_INIT;
     }
 
     if (enabled == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     return fwk_interrupt_driver->is_enabled(interrupt, enabled);
@@ -91,7 +91,7 @@ int fwk_interrupt_is_enabled(unsigned in
 int fwk_interrupt_enable(unsigned int interrupt)
 {
     if (!initialized) {
-        return FWK_E_INIT;
+	return FWK_E_INIT;
     }
 
     return fwk_interrupt_driver->enable(interrupt);
@@ -100,7 +100,7 @@ int fwk_interrupt_enable(unsigned int in
 int fwk_interrupt_disable(unsigned int interrupt)
 {
     if (!initialized) {
-        return FWK_E_INIT;
+	return FWK_E_INIT;
     }
 
     return fwk_interrupt_driver->disable(interrupt);
@@ -109,11 +109,11 @@ int fwk_interrupt_disable(unsigned int i
 int fwk_interrupt_is_pending(unsigned int interrupt, bool *pending)
 {
     if (!initialized) {
-        return FWK_E_INIT;
+	return FWK_E_INIT;
     }
 
     if (pending == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     return fwk_interrupt_driver->is_pending(interrupt, pending);
@@ -122,7 +122,7 @@ int fwk_interrupt_is_pending(unsigned in
 int fwk_interrupt_set_pending(unsigned int interrupt)
 {
     if (!initialized) {
-        return FWK_E_INIT;
+	return FWK_E_INIT;
     }
 
     return fwk_interrupt_driver->set_pending(interrupt);
@@ -131,7 +131,7 @@ int fwk_interrupt_set_pending(unsigned i
 int fwk_interrupt_clear_pending(unsigned int interrupt)
 {
     if (!initialized) {
-        return FWK_E_INIT;
+	return FWK_E_INIT;
     }
 
     return fwk_interrupt_driver->clear_pending(interrupt);
@@ -140,47 +140,47 @@ int fwk_interrupt_clear_pending(unsigned
 int fwk_interrupt_set_isr(unsigned int interrupt, void (*isr)(void))
 {
     if (!initialized) {
-        return FWK_E_INIT;
+	return FWK_E_INIT;
     }
 
     if (isr == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     if (interrupt == FWK_INTERRUPT_NMI) {
-        return fwk_interrupt_driver->set_isr_nmi(isr);
+	return fwk_interrupt_driver->set_isr_nmi(isr);
     } else {
-        return fwk_interrupt_driver->set_isr_irq(interrupt, isr);
+	return fwk_interrupt_driver->set_isr_irq(interrupt, isr);
     }
 }
 
 int fwk_interrupt_set_isr_param(unsigned int interrupt,
-                                void (*isr)(uintptr_t param),
-                                uintptr_t param)
+				void (*isr)(uintptr_t param),
+				uintptr_t param)
 {
     if (!initialized) {
-        return FWK_E_INIT;
+	return FWK_E_INIT;
     }
 
     if (isr == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     if (interrupt == FWK_INTERRUPT_NMI) {
-        return fwk_interrupt_driver->set_isr_nmi_param(isr, param);
+	return fwk_interrupt_driver->set_isr_nmi_param(isr, param);
     } else {
-        return fwk_interrupt_driver->set_isr_irq_param(interrupt, isr, param);
+	return fwk_interrupt_driver->set_isr_irq_param(interrupt, isr, param);
     }
 }
 
 int fwk_interrupt_get_current(unsigned int *interrupt)
 {
     if (!initialized) {
-        return FWK_E_INIT;
+	return FWK_E_INIT;
     }
 
     if (interrupt == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     return fwk_interrupt_driver->get_current(interrupt);
@@ -189,7 +189,7 @@ int fwk_interrupt_get_current(unsigned i
 bool fwk_is_interrupt_context(void)
 {
     if (!initialized) {
-        return false;
+	return false;
     }
 
     return fwk_interrupt_driver->is_interrupt_context();
@@ -199,11 +199,11 @@ bool fwk_is_interrupt_context(void)
 int fwk_interrupt_set_isr_fault(void (*isr)(void))
 {
     if (!initialized) {
-        return FWK_E_INIT;
+	return FWK_E_INIT;
     }
 
     if (isr == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     return fwk_interrupt_driver->set_isr_fault(isr);
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/src/fwk_io.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/framework/src/fwk_io.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/src/fwk_io.c
@@ -35,16 +35,16 @@ int fwk_io_null_close(const struct fwk_i
 
 static struct fwk_io_stream fwk_io_null = {
     .adapter =
-        &(const struct fwk_io_adapter){
-            .open = fwk_io_null_open,
-            .getch = fwk_io_null_getch,
-            .putch = fwk_io_null_putch,
-            .close = fwk_io_null_close,
-        },
+	&(const struct fwk_io_adapter){
+	    .open = fwk_io_null_open,
+	    .getch = fwk_io_null_getch,
+	    .putch = fwk_io_null_putch,
+	    .close = fwk_io_null_close,
+	},
 
     .id = FWK_ID_NONE,
     .mode = (enum fwk_io_mode)(
-        FWK_IO_MODE_READ | FWK_IO_MODE_WRITE | FWK_IO_MODE_BINARY),
+	FWK_IO_MODE_READ | FWK_IO_MODE_WRITE | FWK_IO_MODE_BINARY),
 };
 
 struct fwk_io_stream *fwk_io_stdin = &fwk_io_null;
@@ -63,43 +63,43 @@ int fwk_io_init(void)
     bool stdout_is_stdin = fwk_id_is_equal(FMW_IO_STDIN_ID, FMW_IO_STDOUT_ID);
 
     if (configure_stdin) {
-        if (stdout_is_stdin) {
-            /*
-             * If stdin and stdout share the same entity, we only want to open
-             * it once and we want to do it with both read and write modes
-             * enabled, rather than opening the same entity twice with different
-             * modes.
-             */
-
-            status = fwk_io_open(
-                &stdin_stream,
-                FMW_IO_STDIN_ID,
-                (enum fwk_io_mode)(
-                    ((unsigned int)FWK_IO_MODE_READ) |
-                    ((unsigned int)FWK_IO_MODE_WRITE)));
-            if (fwk_expect(status == FWK_SUCCESS)) {
-                fwk_io_stdin = &stdin_stream;
-                fwk_io_stdout = &stdin_stream;
-            }
-        } else {
-            status =
-                fwk_io_open(&stdin_stream, FMW_IO_STDIN_ID, FWK_IO_MODE_READ);
-            if (fwk_expect(status == FWK_SUCCESS)) {
-                fwk_io_stdin = &stdin_stream;
-            }
-        }
+	if (stdout_is_stdin) {
+	    /*
+	     * If stdin and stdout share the same entity, we only want to open
+	     * it once and we want to do it with both read and write modes
+	     * enabled, rather than opening the same entity twice with different
+	     * modes.
+	     */
+
+	    status = fwk_io_open(
+		&stdin_stream,
+		FMW_IO_STDIN_ID,
+		(enum fwk_io_mode)(
+		    ((unsigned int)FWK_IO_MODE_READ) |
+		    ((unsigned int)FWK_IO_MODE_WRITE)));
+	    if (fwk_expect(status == FWK_SUCCESS)) {
+		fwk_io_stdin = &stdin_stream;
+		fwk_io_stdout = &stdin_stream;
+	    }
+	} else {
+	    status =
+		fwk_io_open(&stdin_stream, FMW_IO_STDIN_ID, FWK_IO_MODE_READ);
+	    if (fwk_expect(status == FWK_SUCCESS)) {
+		fwk_io_stdin = &stdin_stream;
+	    }
+	}
     }
 
     if (configure_stdout && !stdout_is_stdin) {
-        status =
-            fwk_io_open(&stdout_stream, FMW_IO_STDOUT_ID, FWK_IO_MODE_WRITE);
-        if (fwk_expect(status == FWK_SUCCESS)) {
-            fwk_io_stdout = &stdout_stream;
-        }
+	status =
+	    fwk_io_open(&stdout_stream, FMW_IO_STDOUT_ID, FWK_IO_MODE_WRITE);
+	if (fwk_expect(status == FWK_SUCCESS)) {
+	    fwk_io_stdout = &stdout_stream;
+	}
     }
 
     if (status != FWK_SUCCESS) {
-        status = FWK_E_DEVICE;
+	status = FWK_E_DEVICE;
     }
 
     return status;
@@ -116,41 +116,41 @@ int fwk_io_open(
     bool write = (((unsigned int)mode & (unsigned int)FWK_IO_MODE_WRITE) != 0U);
 
     if (stream == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     *stream = (struct fwk_io_stream){
-        .adapter = NULL,
-        .id = id,
-        .mode = mode,
+	.adapter = NULL,
+	.id = id,
+	.mode = mode,
     };
 
     if (!read && !write) {
-        return FWK_E_PARAM; /* Neither reading nor writing requested */
+	return FWK_E_PARAM; /* Neither reading nor writing requested */
     }
 
     status = fwk_module_adapter(&stream->adapter, id);
     if (status != FWK_SUCCESS) {
-        return FWK_E_PARAM; /* System entity doesn't exist */
+	return FWK_E_PARAM; /* System entity doesn't exist */
     }
 
     fwk_assert(stream->adapter != NULL);
 
     if (stream->adapter->open == NULL) {
-        return FWK_E_SUPPORT; /* Stream adapter is not implemented */
+	return FWK_E_SUPPORT; /* Stream adapter is not implemented */
     }
 
     if ((read && (stream->adapter->getch == NULL))) {
-        return FWK_E_SUPPORT; /* Reads requested but no read interface */
+	return FWK_E_SUPPORT; /* Reads requested but no read interface */
     }
 
     if (write && (stream->adapter->putch == NULL)) {
-        return FWK_E_SUPPORT; /* Writes requested but no write interface */
+	return FWK_E_SUPPORT; /* Writes requested but no write interface */
     }
 
     status = stream->adapter->open(stream);
     if (status != FWK_SUCCESS) {
-        return FWK_E_HANDLER;
+	return FWK_E_HANDLER;
     }
 
     return FWK_SUCCESS;
@@ -161,31 +161,31 @@ int fwk_io_getch(const struct fwk_io_str
     int status;
 
     if (stream == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     if (ch == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     *ch = '\0';
 
     if (stream->adapter == NULL) {
-        return FWK_E_STATE; /* The stream is not open */
+	return FWK_E_STATE; /* The stream is not open */
     }
 
     if ((((unsigned int)stream->mode) & ((unsigned int)FWK_IO_MODE_READ)) ==
-        0U) {
-        return FWK_E_SUPPORT; /* Stream not open for read operations */
+	0U) {
+	return FWK_E_SUPPORT; /* Stream not open for read operations */
     }
 
     fwk_assert(stream->adapter->getch != NULL);
 
     status = stream->adapter->getch(stream, ch);
     if (status == FWK_PENDING) {
-        return FWK_PENDING;
+	return FWK_PENDING;
     } else if (status != FWK_SUCCESS) {
-        return FWK_E_HANDLER;
+	return FWK_E_HANDLER;
     }
 
     return FWK_SUCCESS;
@@ -196,23 +196,23 @@ int fwk_io_putch(const struct fwk_io_str
     int status;
 
     if (stream == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     if (stream->adapter == NULL) {
-        return FWK_E_STATE; /* The stream is not open */
+	return FWK_E_STATE; /* The stream is not open */
     }
 
     if ((((unsigned int)stream->mode) & ((unsigned int)FWK_IO_MODE_WRITE)) ==
-        0U) {
-        return FWK_E_SUPPORT; /* Stream not open for write operations */
+	0U) {
+	return FWK_E_SUPPORT; /* Stream not open for write operations */
     }
 
     fwk_assert(stream->adapter->putch != NULL);
 
     status = stream->adapter->putch(stream, ch);
     if (status != FWK_SUCCESS) {
-        return FWK_E_HANDLER;
+	return FWK_E_HANDLER;
     }
 
     return FWK_SUCCESS;
@@ -230,21 +230,21 @@ int fwk_io_read(
     char *cbuffer = buffer;
 
     if (read != NULL) {
-        *read = 0;
+	*read = 0;
     }
 
     for (size_t i = 0; (i < count) && (status == FWK_SUCCESS); i++) {
-        for (size_t j = 0; (j < size) && (status == FWK_SUCCESS); j++) {
-            status = fwk_io_getch(stream, cbuffer++);
-        }
-
-        if ((status == FWK_SUCCESS) && (read != NULL)) {
-            *read += 1;
-        }
+	for (size_t j = 0; (j < size) && (status == FWK_SUCCESS); j++) {
+	    status = fwk_io_getch(stream, cbuffer++);
+	}
+
+	if ((status == FWK_SUCCESS) && (read != NULL)) {
+	    *read += 1;
+	}
     }
 
     if ((status == FWK_PENDING) && (read == NULL)) {
-        return FWK_E_DATA; /* Reached end-of-stream */
+	return FWK_E_DATA; /* Reached end-of-stream */
     }
 
     return status;
@@ -262,21 +262,21 @@ int fwk_io_write(
     const char *cbuffer = buffer;
 
     if (cbuffer == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     if (written != NULL) {
-        *written = 0;
+	*written = 0;
     }
 
     for (size_t i = 0; (i < count) && (status == FWK_SUCCESS); i++) {
-        for (size_t j = 0; (j < size) && (status == FWK_SUCCESS); j++) {
-            status = fwk_io_putch(stream, *cbuffer++);
-        }
-
-        if ((status == FWK_SUCCESS) && (written != NULL)) {
-            *written += 1;
-        }
+	for (size_t j = 0; (j < size) && (status == FWK_SUCCESS); j++) {
+	    status = fwk_io_putch(stream, *cbuffer++);
+	}
+
+	if ((status == FWK_SUCCESS) && (written != NULL)) {
+	    *written += 1;
+	}
     }
 
     return status;
@@ -287,27 +287,27 @@ int fwk_io_close(struct fwk_io_stream *s
     int status;
 
     if (stream == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     if (stream->adapter == NULL) {
-        return FWK_SUCCESS; /* The stream is not open */
+	return FWK_SUCCESS; /* The stream is not open */
     }
 
     if (stream->adapter->close == NULL) {
-        return FWK_SUCCESS; /* Nothing else to do */
+	return FWK_SUCCESS; /* Nothing else to do */
     }
 
     status = stream->adapter->close(stream);
 
     *stream = (struct fwk_io_stream){
-        .adapter = NULL,
-        .id = FWK_ID_NONE,
-        .mode = 0,
+	.adapter = NULL,
+	.id = FWK_ID_NONE,
+	.mode = 0,
     };
 
     if (status != FWK_SUCCESS) {
-        return FWK_E_HANDLER;
+	return FWK_E_HANDLER;
     }
 
     return FWK_SUCCESS;
@@ -318,7 +318,7 @@ int fwk_io_puts(
     const char *restrict str)
 {
     if (str == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     return fwk_io_write(stream, NULL, str, sizeof(char), strlen(str));
@@ -337,7 +337,7 @@ int fwk_io_vprintf(
     char *buffer;
 
     if (format == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     va_copy(length_args, args);
@@ -345,19 +345,19 @@ int fwk_io_vprintf(
     va_end(length_args);
 
     if (length < 0) { /* Possibly invalid format string? */
-        return FWK_E_STATE;
+	return FWK_E_STATE;
     }
 
     buffer = fwk_mm_alloc_notrap(sizeof(buffer[0]), (size_t)(length + 1));
     if (buffer == NULL) { /* Not enough memory for the string buffer */
-        return FWK_E_NOMEM;
+	return FWK_E_NOMEM;
     }
 
     length = vsnprintf(buffer, (size_t)(length + 1), format, args);
     if (length >= 0) {
-        status = fwk_io_puts(stream, buffer); /* Write out the buffer */
+	status = fwk_io_puts(stream, buffer); /* Write out the buffer */
     } else {
-        status = FWK_E_STATE;
+	status = FWK_E_STATE;
     }
 
     fwk_mm_free(buffer);
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/src/fwk_log.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/framework/src/fwk_log.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/src/fwk_log.c
@@ -52,12 +52,12 @@ int fwk_log_init(void)
     int status = FWK_SUCCESS;
 
     if (fwk_id_is_equal(FMW_LOG_DRAIN_ID, FMW_IO_STDOUT_ID)) {
-        fwk_log_stream = fwk_io_stdout;
+	fwk_log_stream = fwk_io_stdout;
     } else if (!fwk_id_is_equal(FMW_LOG_DRAIN_ID, FWK_ID_NONE)) {
-        status = fwk_io_open(&stream, FMW_LOG_DRAIN_ID, FWK_IO_MODE_WRITE);
-        if (fwk_expect(status == FWK_SUCCESS)) {
-            fwk_log_stream = &stream;
-        }
+	status = fwk_io_open(&stream, FMW_LOG_DRAIN_ID, FWK_IO_MODE_WRITE);
+	if (fwk_expect(status == FWK_SUCCESS)) {
+	    fwk_log_stream = &stream;
+	}
     }
 
     return status;
@@ -75,7 +75,7 @@ static bool fwk_log_buffer(struct fwk_ri
      */
 
     if ((sizeof(length) + length) > fwk_ring_get_free(ring)) {
-        return false; /* Not enough buffer space */
+	return false; /* Not enough buffer space */
     }
 
     fwk_ring_push(ring, (char *)&length, sizeof(length));
@@ -126,11 +126,11 @@ static void fwk_log_vsnprintf(
 
     /* Generate the timestamp at the beginning of the buffer */
     length = (size_t)snprintf(
-        buffer,
-        buffer_size,
-        "[%5" PRIu32 ".%06" PRIu32 "] ",
-        duration_s,
-        duration_us);
+	buffer,
+	buffer_size,
+	"[%5" PRIu32 ".%06" PRIu32 "] ",
+	duration_s,
+	duration_us);
     fwk_assert(length < buffer_size);
 
     /*
@@ -150,7 +150,7 @@ static void fwk_log_vsnprintf(
 
     newline = strstr(buffer, FWK_LOG_TERMINATOR);
     if (newline == NULL) {
-        newline = buffer + length;
+	newline = buffer + length;
     }
 
     /*
@@ -182,24 +182,24 @@ static bool fwk_log_banner(void)
 
 #ifndef FMW_LOG_MINIMAL_BANNER
     const char *banner[] = {
-        " ___  ___ ___      __ _",
-        "/ __|/ __| _ \\___ / _(_)_ _ _ ____ __ ____ _ _ _ ___",
-        "\\__ | (__|  _|___|  _| | '_| '  \\ V  V / _` | '_/ -_)",
-        "|___/\\___|_|     |_| |_|_| |_|_|_\\_/\\_/\\__,_|_| \\___|",
-        "",
-        BUILD_VERSION_DESCRIBE_STRING,
-        "",
+	" ___  ___ ___      __ _",
+	"/ __|/ __| _ \\___ / _(_)_ _ _ ____ __ ____ _ _ _ ___",
+	"\\__ | (__|  _|___|  _| | '_| '  \\ V  V / _` | '_/ -_)",
+	"|___/\\___|_|     |_| |_|_| |_|_|_\\_/\\_/\\__,_|_| \\___|",
+	"",
+	BUILD_VERSION_DESCRIBE_STRING,
+	"",
     };
 #else
     const char *banner[] = { "SCP-firmware " BUILD_VERSION_DESCRIBE_STRING,
-                             "" };
+			     "" };
 #endif
     for (unsigned int i = 0; i < FWK_ARRAY_SIZE(banner); i++) {
-        fwk_log_snprintf(
-            sizeof(buffer), buffer, "%s%s", banner[i], FWK_LOG_TERMINATOR);
-        if (fwk_io_puts(fwk_log_stream, buffer) != FWK_SUCCESS) {
-            return false;
-        }
+	fwk_log_snprintf(
+	    sizeof(buffer), buffer, "%s%s", banner[i], FWK_LOG_TERMINATOR);
+	if (fwk_io_puts(fwk_log_stream, buffer) != FWK_SUCCESS) {
+	    return false;
+	}
     }
 
     return true;
@@ -224,7 +224,7 @@ void fwk_log_printf(const char *format,
      */
 
     if (!banner) {
-        banner = fwk_log_banner();
+	banner = fwk_log_banner();
     }
 
     va_start(args, format);
@@ -239,19 +239,19 @@ void fwk_log_printf(const char *format,
 
     bool dropped = !fwk_log_buffer(&fwk_log_ctx.ring, buffer);
     if (dropped) {
-        /*
-         * If we don't have enough room left in the buffer, then we're out of
-         * luck. We don't want to spend what are likely to be precious cycles
-         * printing on the always-on backend, so our best option is simply to
-         * mark the message as dropped and move on.
-         */
+	/*
+	 * If we don't have enough room left in the buffer, then we're out of
+	 * luck. We don't want to spend what are likely to be precious cycles
+	 * printing on the always-on backend, so our best option is simply to
+	 * mark the message as dropped and move on.
+	 */
 
-        fwk_log_ctx.dropped++;
+	fwk_log_ctx.dropped++;
     }
 #else
     int status = fwk_io_puts(fwk_log_stream, buffer);
     if (status != FWK_SUCCESS) {
-        fwk_log_ctx.dropped++;
+	fwk_log_ctx.dropped++;
     }
 #endif
 
@@ -270,34 +270,34 @@ int fwk_log_unbuffer(void)
     flags = fwk_interrupt_global_disable();
 
     if (fwk_log_ctx.remaining == 0) {
-        /*
-         * We've finished printing whatever message we were previously on, so we
-         * need to try and fetch the next one.
-         */
-
-        bool empty = !fwk_ring_pop(
-            &fwk_log_ctx.ring,
-            (char *)&fwk_log_ctx.remaining,
-            sizeof(fwk_log_ctx.remaining));
-
-        if (empty) {
-            /*
-             * At this point we've cleared the buffer of any remaining messages.
-             * If we were forced to drop any messages prior to this point, now
-             * is a good time to let the user know.
-             */
-
-            if (fwk_log_ctx.dropped > 0) {
-                fwk_log_printf(
-                    "[FWK] ... and %u more messages...", fwk_log_ctx.dropped);
+	/*
+	 * We've finished printing whatever message we were previously on, so we
+	 * need to try and fetch the next one.
+	 */
+
+	bool empty = !fwk_ring_pop(
+	    &fwk_log_ctx.ring,
+	    (char *)&fwk_log_ctx.remaining,
+	    sizeof(fwk_log_ctx.remaining));
+
+	if (empty) {
+	    /*
+	     * At this point we've cleared the buffer of any remaining messages.
+	     * If we were forced to drop any messages prior to this point, now
+	     * is a good time to let the user know.
+	     */
+
+	    if (fwk_log_ctx.dropped > 0) {
+		fwk_log_printf(
+		    "[FWK] ... and %u more messages...", fwk_log_ctx.dropped);
 
-                fwk_log_ctx.dropped = 0;
+		fwk_log_ctx.dropped = 0;
 
-                status = FWK_PENDING;
-            }
+		status = FWK_PENDING;
+	    }
 
-            goto exit;
-        }
+	    goto exit;
+	}
     }
 
     /*
@@ -312,9 +312,9 @@ int fwk_log_unbuffer(void)
 
     status = fwk_io_putch(fwk_log_stream, ch);
     if (status == FWK_SUCCESS) {
-        fwk_log_ctx.remaining--;
+	fwk_log_ctx.remaining--;
 
-        status = FWK_PENDING;
+	status = FWK_PENDING;
     }
 
 exit:
@@ -333,7 +333,7 @@ void fwk_log_flush(void)
     flags = fwk_interrupt_global_disable();
 
     do {
-        status = fwk_log_unbuffer();
+	status = fwk_log_unbuffer();
     } while (status == FWK_PENDING);
 
     fwk_interrupt_global_enable(flags);
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/src/fwk_mm.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/framework/src/fwk_mm.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/src/fwk_mm.c
@@ -18,7 +18,7 @@ void *fwk_mm_alloc(size_t num, size_t si
     void *ptr = malloc(num * size);
 
     if (ptr == NULL) {
-        fwk_trap();
+	fwk_trap();
     }
 
     return ptr;
@@ -34,7 +34,7 @@ void *fwk_mm_alloc_aligned(size_t alignm
     void *ptr = aligned_alloc(alignment, num * size);
 
     if (ptr == NULL) {
-        fwk_trap();
+	fwk_trap();
     }
 
     return ptr;
@@ -44,7 +44,7 @@ void *fwk_mm_calloc(size_t num, size_t s
 {
     void *ptr = calloc(num, size);
     if (ptr == NULL) {
-        fwk_trap();
+	fwk_trap();
     }
 
     return ptr;
@@ -55,9 +55,9 @@ void *fwk_mm_calloc_aligned(size_t align
     void *ptr = fwk_mm_alloc_aligned(alignment, num, size);
 
     if (ptr != NULL) {
-        (void)memset(ptr, 0, num * size);
+	(void)memset(ptr, 0, num * size);
 
-        return ptr;
+	return ptr;
     }
 
     fwk_trap();
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/src/fwk_module.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/framework/src/fwk_module.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/src/fwk_module.c
@@ -69,7 +69,7 @@ static size_t fwk_module_count_elements(
     size_t count = 0;
 
     for (; elements[count].name != NULL; count++) {
-        continue;
+	continue;
     }
 
     return count;
@@ -80,11 +80,11 @@ static void fwk_module_init_subscription
 {
     *list = fwk_mm_calloc(count, sizeof((*list)[0]));
     if (*list == NULL) {
-        fwk_trap();
+	fwk_trap();
     }
 
     for (size_t i = 0; i < count; i++) {
-        fwk_list_init(&((*list)[i]));
+	fwk_list_init(&((*list)[i]));
     }
 }
 #endif
@@ -95,17 +95,17 @@ static void fwk_module_init_element_ctx(
     size_t notification_count)
 {
     *ctx = (struct fwk_element_ctx){
-        .state = FWK_MODULE_STATE_UNINITIALIZED,
-        .desc = element,
-        .sub_element_count = element->sub_element_count,
+	.state = FWK_MODULE_STATE_UNINITIALIZED,
+	.desc = element,
+	.sub_element_count = element->sub_element_count,
     };
 
     fwk_list_init(&ctx->delayed_response_list);
 
 #ifdef BUILD_HAS_NOTIFICATION
     if (notification_count > 0) {
-        fwk_module_init_subscriptions(
-            &ctx->subscription_dlist_table, notification_count);
+	fwk_module_init_subscriptions(
+	    &ctx->subscription_dlist_table, notification_count);
     }
 #endif
 }
@@ -118,14 +118,14 @@ static void fwk_module_init_element_ctxs
     ctx->element_count = fwk_module_count_elements(elements);
 
     ctx->element_ctx_table =
-        fwk_mm_calloc(ctx->element_count, sizeof(ctx->element_ctx_table[0]));
+	fwk_mm_calloc(ctx->element_count, sizeof(ctx->element_ctx_table[0]));
     if (ctx->element_ctx_table == NULL) {
-        fwk_trap();
+	fwk_trap();
     }
 
     for (size_t i = 0; i < ctx->element_count; i++) {
-        fwk_module_init_element_ctx(
-            &ctx->element_ctx_table[i], &elements[i], notification_count);
+	fwk_module_init_element_ctx(
+	    &ctx->element_ctx_table[i], &elements[i], notification_count);
     }
 }
 
@@ -136,41 +136,41 @@ void fwk_module_init(void)
      * enum fwk_module_idx
      */
     for (uint32_t i = 0U; i < (uint32_t)FWK_MODULE_IDX_COUNT; i++) {
-        struct fwk_module_context *ctx = &fwk_module_ctx.module_ctx_table[i];
+	struct fwk_module_context *ctx = &fwk_module_ctx.module_ctx_table[i];
 
-        fwk_id_t id = FWK_ID_MODULE(i);
+	fwk_id_t id = FWK_ID_MODULE(i);
 
-        const struct fwk_module *desc = module_table[i];
-        const struct fwk_module_config *config = module_config_table[i];
+	const struct fwk_module *desc = module_table[i];
+	const struct fwk_module_config *config = module_config_table[i];
 
-        *ctx = (struct fwk_module_context){
-            .id = id,
+	*ctx = (struct fwk_module_context){
+	    .id = id,
 
-            .desc = desc,
-            .config = config,
-        };
+	    .desc = desc,
+	    .config = config,
+	};
 
-        fwk_assert(ctx->desc != NULL);
-        fwk_assert(ctx->config != NULL);
+	fwk_assert(ctx->desc != NULL);
+	fwk_assert(ctx->config != NULL);
 
-        fwk_list_init(&ctx->delayed_response_list);
+	fwk_list_init(&ctx->delayed_response_list);
 
-        if (config->elements.type == FWK_MODULE_ELEMENTS_TYPE_STATIC) {
-            size_t notification_count = 0;
+	if (config->elements.type == FWK_MODULE_ELEMENTS_TYPE_STATIC) {
+	    size_t notification_count = 0;
 
 #ifdef BUILD_HAS_NOTIFICATION
-            notification_count = desc->notification_count;
+	    notification_count = desc->notification_count;
 #endif
 
-            fwk_module_init_element_ctxs(
-                ctx, config->elements.table, notification_count);
-        }
+	    fwk_module_init_element_ctxs(
+		ctx, config->elements.table, notification_count);
+	}
 
 #ifdef BUILD_HAS_NOTIFICATION
-        if (desc->notification_count > 0) {
-            fwk_module_init_subscriptions(
-                &ctx->subscription_dlist_table, desc->notification_count);
-        }
+	if (desc->notification_count > 0) {
+	    fwk_module_init_subscriptions(
+		&ctx->subscription_dlist_table, desc->notification_count);
+	}
 #endif
     }
 }
@@ -182,28 +182,28 @@ static void fwk_module_init_elements(str
     const struct fwk_module *desc = ctx->desc;
 
     if (desc->element_init == NULL) {
-        fwk_trap();
+	fwk_trap();
     }
 
     for (size_t i = 0; i < ctx->element_count; i++) {
-        fwk_id_t element_id = fwk_id_build_element_id(ctx->id, (unsigned int)i);
+	fwk_id_t element_id = fwk_id_build_element_id(ctx->id, (unsigned int)i);
 
-        const struct fwk_element *element = ctx->element_ctx_table[i].desc;
+	const struct fwk_element *element = ctx->element_ctx_table[i].desc;
 
-        fwk_module_ctx.bind_id = element_id;
+	fwk_module_ctx.bind_id = element_id;
 
-        /* Each element must have a valid pointer to specific data */
-        if (element->data == NULL) {
-            fwk_trap();
-        }
+	/* Each element must have a valid pointer to specific data */
+	if (element->data == NULL) {
+	    fwk_trap();
+	}
 
-        status = desc->element_init(
-            element_id, element->sub_element_count, element->data);
-        if (status != FWK_SUCCESS) {
-            fwk_trap();
-        }
+	status = desc->element_init(
+	    element_id, element->sub_element_count, element->data);
+	if (status != FWK_SUCCESS) {
+	    fwk_trap();
+	}
 
-        ctx->state = FWK_MODULE_STATE_INITIALIZED;
+	ctx->state = FWK_MODULE_STATE_INITIALIZED;
     }
 }
 
@@ -215,52 +215,52 @@ static void fwk_module_init_module(struc
     const struct fwk_module_config *config = ctx->config;
 
     if (desc->type >= FWK_MODULE_TYPE_COUNT) {
-        fwk_trap();
+	fwk_trap();
     }
 
     if (desc->init == NULL) {
-        fwk_trap();
+	fwk_trap();
     }
 
     if ((desc->api_count == 0) != (desc->process_bind_request == NULL)) {
-        fwk_trap();
+	fwk_trap();
     }
 
     if (config->elements.type == FWK_MODULE_ELEMENTS_TYPE_DYNAMIC) {
-        size_t notification_count = 0;
+	size_t notification_count = 0;
 
-        const struct fwk_element *elements = NULL;
+	const struct fwk_element *elements = NULL;
 
-        if (config->elements.generator == NULL) {
-            fwk_trap();
-        }
-
-        elements = config->elements.generator(ctx->id);
-        if (elements == NULL) {
-            fwk_trap();
-        }
+	if (config->elements.generator == NULL) {
+	    fwk_trap();
+	}
+
+	elements = config->elements.generator(ctx->id);
+	if (elements == NULL) {
+	    fwk_trap();
+	}
 
 #ifdef BUILD_HAS_NOTIFICATION
-        notification_count = desc->notification_count;
+	notification_count = desc->notification_count;
 #endif
 
-        fwk_module_init_element_ctxs(ctx, elements, notification_count);
+	fwk_module_init_element_ctxs(ctx, elements, notification_count);
     }
 
     status = desc->init(ctx->id, ctx->element_count, config->data);
     if (status != FWK_SUCCESS) {
-        fwk_trap();
+	fwk_trap();
     }
 
     if (ctx->element_count > 0) {
-        fwk_module_init_elements(ctx);
+	fwk_module_init_elements(ctx);
     }
 
     if (desc->post_init != NULL) {
-        status = desc->post_init(ctx->id);
-        if (status != FWK_SUCCESS) {
-            fwk_trap();
-        }
+	status = desc->post_init(ctx->id);
+	if (status != FWK_SUCCESS) {
+	    fwk_trap();
+	}
     }
 
     ctx->state = FWK_MODULE_STATE_INITIALIZED;
@@ -269,7 +269,7 @@ static void fwk_module_init_module(struc
 static void fwk_module_init_modules(void)
 {
     for (unsigned int i = 0U; i < (unsigned int)FWK_MODULE_IDX_COUNT; i++) {
-        fwk_module_init_module(&fwk_module_ctx.module_ctx_table[i]);
+	fwk_module_init_module(&fwk_module_ctx.module_ctx_table[i]);
     }
 }
 
@@ -284,19 +284,19 @@ static int fwk_module_bind_elements(
     module = fwk_mod_ctx->desc;
 
     for (element_idx = 0; element_idx < fwk_mod_ctx->element_count;
-         element_idx++) {
-        fwk_module_ctx.bind_id =
-            fwk_id_build_element_id(fwk_mod_ctx->id, element_idx);
-        status = module->bind(fwk_module_ctx.bind_id, round);
-        if (!fwk_expect(status == FWK_SUCCESS)) {
-            FWK_LOG_CRIT(fwk_module_err_msg_func, status, __func__);
-            return status;
-        }
-
-        if (round == FWK_MODULE_BIND_ROUND_MAX) {
-            fwk_mod_ctx->element_ctx_table[element_idx].state =
-                FWK_MODULE_STATE_BOUND;
-        }
+	 element_idx++) {
+	fwk_module_ctx.bind_id =
+	    fwk_id_build_element_id(fwk_mod_ctx->id, element_idx);
+	status = module->bind(fwk_module_ctx.bind_id, round);
+	if (!fwk_expect(status == FWK_SUCCESS)) {
+	    FWK_LOG_CRIT(fwk_module_err_msg_func, status, __func__);
+	    return status;
+	}
+
+	if (round == FWK_MODULE_BIND_ROUND_MAX) {
+	    fwk_mod_ctx->element_ctx_table[element_idx].state =
+		FWK_MODULE_STATE_BOUND;
+	}
     }
 
     return FWK_SUCCESS;
@@ -311,19 +311,19 @@ static int fwk_module_bind_module(
 
     module = fwk_mod_ctx->desc;
     if (module->bind == NULL) {
-        fwk_mod_ctx->state = FWK_MODULE_STATE_BOUND;
-        return FWK_SUCCESS;
+	fwk_mod_ctx->state = FWK_MODULE_STATE_BOUND;
+	return FWK_SUCCESS;
     }
 
     fwk_module_ctx.bind_id = fwk_mod_ctx->id;
     status = module->bind(fwk_mod_ctx->id, round);
     if (!fwk_expect(status == FWK_SUCCESS)) {
-        FWK_LOG_CRIT(fwk_module_err_msg_func, status, __func__);
-        return status;
+	FWK_LOG_CRIT(fwk_module_err_msg_func, status, __func__);
+	return status;
     }
 
     if (round == FWK_MODULE_BIND_ROUND_MAX) {
-        fwk_mod_ctx->state = FWK_MODULE_STATE_BOUND;
+	fwk_mod_ctx->state = FWK_MODULE_STATE_BOUND;
     }
 
     return fwk_module_bind_elements(fwk_mod_ctx, round);
@@ -336,11 +336,11 @@ static int fwk_module_bind_modules(unsig
     struct fwk_module_context *fwk_mod_ctx;
 
     for (module_idx = 0; module_idx < FWK_MODULE_IDX_COUNT; module_idx++) {
-        fwk_mod_ctx = &fwk_module_ctx.module_ctx_table[module_idx];
-        status = fwk_module_bind_module(fwk_mod_ctx, round);
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
+	fwk_mod_ctx = &fwk_module_ctx.module_ctx_table[module_idx];
+	status = fwk_module_bind_module(fwk_mod_ctx, round);
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
     }
 
     return FWK_SUCCESS;
@@ -354,18 +354,18 @@ static int fwk_module_start_elements(str
 
     module = fwk_mod_ctx->desc;
     for (element_idx = 0; element_idx < fwk_mod_ctx->element_count;
-         element_idx++) {
-        if (module->start != NULL) {
-            status = module->start(
-                fwk_id_build_element_id(fwk_mod_ctx->id, element_idx));
-            if (!fwk_expect(status == FWK_SUCCESS)) {
-                FWK_LOG_CRIT(fwk_module_err_msg_func, status, __func__);
-                return status;
-            }
-        }
+	 element_idx++) {
+	if (module->start != NULL) {
+	    status = module->start(
+		fwk_id_build_element_id(fwk_mod_ctx->id, element_idx));
+	    if (!fwk_expect(status == FWK_SUCCESS)) {
+		FWK_LOG_CRIT(fwk_module_err_msg_func, status, __func__);
+		return status;
+	    }
+	}
 
-        fwk_mod_ctx->element_ctx_table[element_idx].state =
-            FWK_MODULE_STATE_STARTED;
+	fwk_mod_ctx->element_ctx_table[element_idx].state =
+	    FWK_MODULE_STATE_STARTED;
     }
 
     return FWK_SUCCESS;
@@ -379,11 +379,11 @@ static int fwk_module_start_module(struc
     module = fwk_mod_ctx->desc;
 
     if (module->start != NULL) {
-        status = module->start(fwk_mod_ctx->id);
-        if (!fwk_expect(status == FWK_SUCCESS)) {
-            FWK_LOG_CRIT(fwk_module_err_msg_func, status, __func__);
-            return status;
-        }
+	status = module->start(fwk_mod_ctx->id);
+	if (!fwk_expect(status == FWK_SUCCESS)) {
+	    FWK_LOG_CRIT(fwk_module_err_msg_func, status, __func__);
+	    return status;
+	}
     }
 
     fwk_mod_ctx->state = FWK_MODULE_STATE_STARTED;
@@ -398,11 +398,11 @@ static int start_modules(void)
     struct fwk_module_context *fwk_mod_ctx;
 
     for (module_idx = 0; module_idx < FWK_MODULE_IDX_COUNT; module_idx++) {
-        fwk_mod_ctx = &fwk_module_ctx.module_ctx_table[module_idx];
-        status = fwk_module_start_module(fwk_mod_ctx);
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
+	fwk_mod_ctx = &fwk_module_ctx.module_ctx_table[module_idx];
+	status = fwk_module_start_module(fwk_mod_ctx);
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
     }
 
     return FWK_SUCCESS;
@@ -414,15 +414,15 @@ int fwk_module_start(void)
     unsigned int bind_round;
 
     if (fwk_module_ctx.initialized) {
-        FWK_LOG_CRIT(fwk_module_err_msg_func, FWK_E_STATE, __func__);
-        return FWK_E_STATE;
+	FWK_LOG_CRIT(fwk_module_err_msg_func, FWK_E_STATE, __func__);
+	return FWK_E_STATE;
     }
 
     CLI_DEBUGGER();
 
     status = __fwk_init(FWK_MODULE_EVENT_COUNT);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     fwk_module_ctx.stage = MODULE_STAGE_INITIALIZE;
@@ -430,17 +430,17 @@ int fwk_module_start(void)
 
     fwk_module_ctx.stage = MODULE_STAGE_BIND;
     for (bind_round = 0; bind_round <= FWK_MODULE_BIND_ROUND_MAX;
-         bind_round++) {
-        status = fwk_module_bind_modules(bind_round);
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
+	 bind_round++) {
+	status = fwk_module_bind_modules(bind_round);
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
     }
 
     fwk_module_ctx.stage = MODULE_STAGE_START;
     status = start_modules();
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     fwk_module_ctx.initialized = true;
@@ -459,18 +459,18 @@ static int fwk_module_stop_elements(stru
     module = fwk_mod_ctx->desc;
 
     for (element_idx = 0; element_idx < fwk_mod_ctx->element_count;
-         element_idx++) {
-        if (module->stop != NULL) {
-            status = module->stop(
-                fwk_id_build_element_id(fwk_mod_ctx->id, element_idx));
-            if (!fwk_expect(status == FWK_SUCCESS)) {
-                FWK_LOG_CRIT(fwk_module_err_msg_func, status, __func__);
-                return status;
-            }
-        }
+	 element_idx++) {
+	if (module->stop != NULL) {
+	    status = module->stop(
+		fwk_id_build_element_id(fwk_mod_ctx->id, element_idx));
+	    if (!fwk_expect(status == FWK_SUCCESS)) {
+		FWK_LOG_CRIT(fwk_module_err_msg_func, status, __func__);
+		return status;
+	    }
+	}
 
-        fwk_mod_ctx->element_ctx_table[element_idx].state =
-            FWK_MODULE_STATE_SUSPENDED;
+	fwk_mod_ctx->element_ctx_table[element_idx].state =
+	    FWK_MODULE_STATE_SUSPENDED;
     }
 
     return FWK_SUCCESS;
@@ -484,11 +484,11 @@ static int fwk_module_stop_module(struct
     module = fwk_mod_ctx->desc;
 
     if (module->stop != NULL) {
-        status = module->stop(fwk_mod_ctx->id);
-        if (!fwk_expect(status == FWK_SUCCESS)) {
-            FWK_LOG_CRIT(fwk_module_err_msg_func, status, __func__);
-            return status;
-        }
+	status = module->stop(fwk_mod_ctx->id);
+	if (!fwk_expect(status == FWK_SUCCESS)) {
+	    FWK_LOG_CRIT(fwk_module_err_msg_func, status, __func__);
+	    return status;
+	}
     }
 
     fwk_mod_ctx->state = FWK_MODULE_STATE_SUSPENDED;
@@ -503,11 +503,11 @@ static int stop_modules(void)
     struct fwk_module_context *fwk_mod_ctx;
 
     for (module_idx = 0; module_idx < FWK_MODULE_IDX_COUNT; module_idx++) {
-        fwk_mod_ctx = &fwk_module_ctx.module_ctx_table[module_idx];
-        status = fwk_module_stop_module(fwk_mod_ctx);
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
+	fwk_mod_ctx = &fwk_module_ctx.module_ctx_table[module_idx];
+	status = fwk_module_stop_module(fwk_mod_ctx);
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
     }
 
     return FWK_SUCCESS;
@@ -518,14 +518,14 @@ int fwk_module_stop(void)
     int status;
 
     if (!fwk_module_ctx.initialized) {
-        FWK_LOG_CRIT(fwk_module_err_msg_func, FWK_E_STATE, __func__);
-        return FWK_E_STATE;
+	FWK_LOG_CRIT(fwk_module_err_msg_func, FWK_E_STATE, __func__);
+	return FWK_E_STATE;
     }
 
     fwk_module_ctx.stage = MODULE_STAGE_STOP;
     status = stop_modules();
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     return FWK_SUCCESS;
@@ -546,18 +546,18 @@ struct fwk_element_ctx *fwk_module_get_e
 int fwk_module_get_state(fwk_id_t id, enum fwk_module_state *state)
 {
     if (state == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     if (fwk_module_is_valid_element_id(id) ||
-        fwk_module_is_valid_sub_element_id(id)) {
-        *state = fwk_module_get_element_ctx(id)->state;
+	fwk_module_is_valid_sub_element_id(id)) {
+	*state = fwk_module_get_element_ctx(id)->state;
     } else {
-        if (fwk_module_is_valid_module_id(id)) {
-            *state = fwk_module_get_ctx(id)->state;
-        } else {
-            return FWK_E_PARAM;
-        }
+	if (fwk_module_is_valid_module_id(id)) {
+	    *state = fwk_module_get_ctx(id)->state;
+	} else {
+	    return FWK_E_PARAM;
+	}
     }
 
     return FWK_SUCCESS;
@@ -571,11 +571,11 @@ void fwk_module_reset(void)
 bool fwk_module_is_valid_module_id(fwk_id_t id)
 {
     if (!fwk_id_is_type(id, FWK_ID_TYPE_MODULE)) {
-        return false;
+	return false;
     }
 
     if (fwk_id_get_module_idx(id) >= FWK_MODULE_IDX_COUNT) {
-        return false;
+	return false;
     }
 
     return true;
@@ -586,17 +586,17 @@ bool fwk_module_is_valid_element_id(fwk_
     unsigned int module_idx;
 
     if (!fwk_id_is_type(id, FWK_ID_TYPE_ELEMENT)) {
-        return false;
+	return false;
     }
 
     module_idx = fwk_id_get_module_idx(id);
     if (module_idx >= FWK_MODULE_IDX_COUNT) {
-        return false;
+	return false;
     }
 
     return (
-        fwk_id_get_element_idx(id) <
-        fwk_module_ctx.module_ctx_table[module_idx].element_count);
+	fwk_id_get_element_idx(id) <
+	fwk_module_ctx.module_ctx_table[module_idx].element_count);
 }
 
 bool fwk_module_is_valid_sub_element_id(fwk_id_t id)
@@ -606,23 +606,23 @@ bool fwk_module_is_valid_sub_element_id(
     unsigned int element_idx;
 
     if (!fwk_id_is_type(id, FWK_ID_TYPE_SUB_ELEMENT)) {
-        return false;
+	return false;
     }
 
     module_idx = fwk_id_get_module_idx(id);
     if (module_idx >= FWK_MODULE_IDX_COUNT) {
-        return false;
+	return false;
     }
     fwk_mod_ctx = &fwk_module_ctx.module_ctx_table[module_idx];
 
     element_idx = fwk_id_get_element_idx(id);
     if (element_idx >= fwk_mod_ctx->element_count) {
-        return false;
+	return false;
     }
 
     return (
-        fwk_id_get_sub_element_idx(id) <
-        fwk_mod_ctx->element_ctx_table[element_idx].sub_element_count);
+	fwk_id_get_sub_element_idx(id) <
+	fwk_mod_ctx->element_ctx_table[element_idx].sub_element_count);
 }
 
 bool fwk_module_is_valid_entity_id(fwk_id_t id)
@@ -631,19 +631,19 @@ bool fwk_module_is_valid_entity_id(fwk_i
 
     switch (fwk_id_get_type(id)) {
     case FWK_ID_TYPE_MODULE:
-        return_status = fwk_module_is_valid_module_id(id);
-        break;
+	return_status = fwk_module_is_valid_module_id(id);
+	break;
 
     case FWK_ID_TYPE_ELEMENT:
-        return_status = fwk_module_is_valid_element_id(id);
-        break;
+	return_status = fwk_module_is_valid_element_id(id);
+	break;
 
     case FWK_ID_TYPE_SUB_ELEMENT:
-        return_status = fwk_module_is_valid_sub_element_id(id);
-        break;
+	return_status = fwk_module_is_valid_sub_element_id(id);
+	break;
 
     default:
-        break;
+	break;
     }
 
     return return_status;
@@ -654,17 +654,17 @@ bool fwk_module_is_valid_api_id(fwk_id_t
     unsigned int module_idx;
 
     if (!fwk_id_is_type(id, FWK_ID_TYPE_API)) {
-        return false;
+	return false;
     }
 
     module_idx = fwk_id_get_module_idx(id);
     if (module_idx >= FWK_MODULE_IDX_COUNT) {
-        return false;
+	return false;
     }
 
     return (
-        fwk_id_get_api_idx(id) <
-        fwk_module_ctx.module_ctx_table[module_idx].desc->api_count);
+	fwk_id_get_api_idx(id) <
+	fwk_module_ctx.module_ctx_table[module_idx].desc->api_count);
 }
 
 bool fwk_module_is_valid_event_id(fwk_id_t id)
@@ -672,17 +672,17 @@ bool fwk_module_is_valid_event_id(fwk_id
     unsigned int module_idx;
 
     if (!fwk_id_is_type(id, FWK_ID_TYPE_EVENT)) {
-        return false;
+	return false;
     }
 
     module_idx = fwk_id_get_module_idx(id);
     if (module_idx >= FWK_MODULE_IDX_COUNT) {
-        return false;
+	return false;
     }
 
     return (
-        fwk_id_get_event_idx(id) <
-        fwk_module_ctx.module_ctx_table[module_idx].desc->event_count);
+	fwk_id_get_event_idx(id) <
+	fwk_module_ctx.module_ctx_table[module_idx].desc->event_count);
 }
 
 bool fwk_module_is_valid_notification_id(fwk_id_t id)
@@ -691,17 +691,17 @@ bool fwk_module_is_valid_notification_id
     unsigned int module_idx;
 
     if (!fwk_id_is_type(id, FWK_ID_TYPE_NOTIFICATION)) {
-        return false;
+	return false;
     }
 
     module_idx = fwk_id_get_module_idx(id);
     if (module_idx >= FWK_MODULE_IDX_COUNT) {
-        return false;
+	return false;
     }
 
     return (
-        fwk_id_get_notification_idx(id) <
-        fwk_module_ctx.module_ctx_table[module_idx].desc->notification_count);
+	fwk_id_get_notification_idx(id) <
+	fwk_module_ctx.module_ctx_table[module_idx].desc->notification_count);
 #else
     return false;
 #endif
@@ -710,25 +710,25 @@ bool fwk_module_is_valid_notification_id
 int fwk_module_get_element_count(fwk_id_t id)
 {
     if (fwk_module_is_valid_module_id(id)) {
-        return (int)fwk_module_get_ctx(id)->element_count;
+	return (int)fwk_module_get_ctx(id)->element_count;
     } else {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 }
 
 int fwk_module_get_sub_element_count(fwk_id_t element_id)
 {
     if (fwk_module_is_valid_element_id(element_id)) {
-        return (int)fwk_module_get_element_ctx(element_id)->sub_element_count;
+	return (int)fwk_module_get_element_ctx(element_id)->sub_element_count;
     } else {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 }
 
 const char *fwk_module_get_element_name(fwk_id_t id)
 {
     if (fwk_module_is_valid_element_id(id)) {
-        return fwk_module_get_element_ctx(id)->desc->name;
+	return fwk_module_get_element_ctx(id)->desc->name;
     }
 
     fwk_unexpected();
@@ -738,10 +738,10 @@ const char *fwk_module_get_element_name(
 const void *fwk_module_get_data(fwk_id_t id)
 {
     if (fwk_module_is_valid_element_id(id) ||
-        fwk_module_is_valid_sub_element_id(id)) {
-        return fwk_module_get_element_ctx(id)->desc->data;
+	fwk_module_is_valid_sub_element_id(id)) {
+	return fwk_module_get_element_ctx(id)->desc->data;
     } else if (fwk_module_is_valid_module_id(id)) {
-        return fwk_module_get_ctx(id)->config->data;
+	return fwk_module_get_ctx(id)->config->data;
     }
 
     fwk_unexpected();
@@ -754,40 +754,40 @@ int fwk_module_bind(fwk_id_t target_id,
     struct fwk_module_context *fwk_mod_ctx;
 
     if (!fwk_module_is_valid_entity_id(target_id)) {
-        goto error;
+	goto error;
     }
 
     if (!fwk_module_is_valid_api_id(api_id)) {
-        goto error;
+	goto error;
     }
 
     if (fwk_id_get_module_idx(target_id) != fwk_id_get_module_idx(api_id)) {
-        goto error;
+	goto error;
     }
 
     if (api == NULL) {
-        goto error;
+	goto error;
     }
 
     fwk_mod_ctx = fwk_module_get_ctx(target_id);
 
     if (((fwk_module_ctx.stage != MODULE_STAGE_INITIALIZE) ||
-         (fwk_mod_ctx->state != FWK_MODULE_STATE_INITIALIZED)) &&
-        (fwk_module_ctx.stage != MODULE_STAGE_BIND)) {
-        status = FWK_E_STATE;
-        goto error;
+	 (fwk_mod_ctx->state != FWK_MODULE_STATE_INITIALIZED)) &&
+	(fwk_module_ctx.stage != MODULE_STAGE_BIND)) {
+	status = FWK_E_STATE;
+	goto error;
     }
 
     status = fwk_mod_ctx->desc->process_bind_request(
-        fwk_module_ctx.bind_id, target_id, api_id, (const void **)api);
+	fwk_module_ctx.bind_id, target_id, api_id, (const void **)api);
     if (!fwk_expect(status == FWK_SUCCESS)) {
-        FWK_LOG_CRIT(fwk_module_err_msg_line, status, __func__, __LINE__);
-        return status;
+	FWK_LOG_CRIT(fwk_module_err_msg_line, status, __func__, __LINE__);
+	return status;
     }
 
     if (*(void **)api == NULL) {
-        status = FWK_E_HANDLER;
-        goto error;
+	status = FWK_E_HANDLER;
+	goto error;
     }
 
     return FWK_SUCCESS;
@@ -803,13 +803,13 @@ int fwk_module_adapter(const struct fwk_
     unsigned int idx;
 
     if (adapter == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     *adapter = NULL;
 
     if (!fwk_module_is_valid_entity_id(id)) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     idx = fwk_id_get_module_idx(id);
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/src/fwk_notification.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/framework/src/fwk_notification.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/src/fwk_notification.c
@@ -59,15 +59,15 @@ static struct fwk_dlist *get_subscriptio
     struct fwk_dlist *subscription_dlist_table;
 
     if (fwk_id_is_type(source_id, FWK_ID_TYPE_MODULE)) {
-        subscription_dlist_table =
-            fwk_module_get_ctx(source_id)->subscription_dlist_table;
+	subscription_dlist_table =
+	    fwk_module_get_ctx(source_id)->subscription_dlist_table;
     } else {
-        subscription_dlist_table =
-            fwk_module_get_element_ctx(source_id)->subscription_dlist_table;
+	subscription_dlist_table =
+	    fwk_module_get_element_ctx(source_id)->subscription_dlist_table;
     }
 
     return &subscription_dlist_table[
-               fwk_id_get_notification_idx(notification_id)];
+	       fwk_id_get_notification_idx(notification_id)];
 }
 
 /*
@@ -92,14 +92,14 @@ static struct __fwk_notification_subscri
     struct __fwk_notification_subscription *subscription;
 
     for (node = fwk_list_head(subscription_dlist); node != NULL;
-         node = fwk_list_next(subscription_dlist, node)) {
-        subscription = FWK_LIST_GET(node,
-            struct __fwk_notification_subscription, dlist_node);
-
-        if (fwk_id_is_equal(subscription->source_id, source_id) &&
-            fwk_id_is_equal(subscription->target_id, target_id)) {
-            return subscription;
-        }
+	 node = fwk_list_next(subscription_dlist, node)) {
+	subscription = FWK_LIST_GET(node,
+	    struct __fwk_notification_subscription, dlist_node);
+
+	if (fwk_id_is_equal(subscription->source_id, source_id) &&
+	    fwk_id_is_equal(subscription->target_id, target_id)) {
+	    return subscription;
+	}
     }
 
     return NULL;
@@ -115,7 +115,7 @@ static struct __fwk_notification_subscri
  * \param[out] count The number of notifications being sent.
  */
 static void send_notifications(struct fwk_event *notification_event,
-                               unsigned int *count)
+			       unsigned int *count)
 {
     int status;
     struct fwk_dlist *subscription_dlist;
@@ -123,26 +123,26 @@ static void send_notifications(struct fw
     struct __fwk_notification_subscription *subscription;
 
     subscription_dlist = get_subscription_dlist(notification_event->id,
-                                                notification_event->source_id);
+						notification_event->source_id);
     notification_event->is_response = false;
     notification_event->is_notification = true;
 
     for (node = fwk_list_head(subscription_dlist); node != NULL;
-         node = fwk_list_next(subscription_dlist, node)) {
-        subscription = FWK_LIST_GET(node,
-            struct __fwk_notification_subscription, dlist_node);
-
-        if (!fwk_id_is_equal(
-                subscription->source_id, notification_event->source_id)) {
-            continue;
-        }
-
-        notification_event->target_id = subscription->target_id;
-
-        status = __fwk_put_notification(notification_event);
-        if (status == FWK_SUCCESS) {
-            (*count)++;
-        }
+	 node = fwk_list_next(subscription_dlist, node)) {
+	subscription = FWK_LIST_GET(node,
+	    struct __fwk_notification_subscription, dlist_node);
+
+	if (!fwk_id_is_equal(
+		subscription->source_id, notification_event->source_id)) {
+	    continue;
+	}
+
+	notification_event->target_id = subscription->target_id;
+
+	status = __fwk_put_notification(notification_event);
+	if (status == FWK_SUCCESS) {
+	    (*count)++;
+	}
     }
 }
 
@@ -153,7 +153,7 @@ static void send_notifications(struct fw
 static FWK_CONSTRUCTOR void fwk_notification_init(void)
 {
     static struct __fwk_notification_subscription
-        subscriptions[FMW_NOTIFICATION_MAX];
+	subscriptions[FMW_NOTIFICATION_MAX];
 
     unsigned int i;
 
@@ -161,8 +161,8 @@ static FWK_CONSTRUCTOR void fwk_notifica
     fwk_list_init(&ctx.free_subscription_dlist);
 
     for (i = 0; i < FMW_NOTIFICATION_MAX; i++) {
-        fwk_list_push_tail(
-            &ctx.free_subscription_dlist, &subscriptions[i].dlist_node);
+	fwk_list_push_tail(
+	    &ctx.free_subscription_dlist, &subscriptions[i].dlist_node);
     }
 }
 
@@ -176,7 +176,7 @@ void __fwk_notification_reset(void)
  */
 
 int fwk_notification_subscribe(fwk_id_t notification_id, fwk_id_t source_id,
-                               fwk_id_t target_id)
+			       fwk_id_t target_id)
 {
     int status;
     unsigned int flags;
@@ -184,33 +184,33 @@ int fwk_notification_subscribe(fwk_id_t
     struct __fwk_notification_subscription *subscription;
 
     if (fwk_is_interrupt_context()) {
-        status = FWK_E_HANDLER;
-        goto error;
+	status = FWK_E_HANDLER;
+	goto error;
     }
 
     if (!fwk_module_is_valid_notification_id(notification_id) ||
-        !fwk_module_is_valid_entity_id(source_id) ||
-        !fwk_module_is_valid_entity_id(target_id) ||
-        !fwk_id_is_equal(fwk_id_build_module_id(notification_id),
-                         fwk_id_build_module_id(source_id))) {
-        status = FWK_E_PARAM;
-        goto error;
+	!fwk_module_is_valid_entity_id(source_id) ||
+	!fwk_module_is_valid_entity_id(target_id) ||
+	!fwk_id_is_equal(fwk_id_build_module_id(notification_id),
+			 fwk_id_build_module_id(source_id))) {
+	status = FWK_E_PARAM;
+	goto error;
     }
 
     subscription_dlist = get_subscription_dlist(notification_id, source_id);
     if (search_subscription(subscription_dlist, source_id, target_id) != NULL) {
-        status = FWK_E_STATE;
-        goto error;
+	status = FWK_E_STATE;
+	goto error;
     }
 
     subscription = FWK_LIST_GET(
-        fwk_list_pop_head(&ctx.free_subscription_dlist),
-        struct __fwk_notification_subscription, dlist_node);
+	fwk_list_pop_head(&ctx.free_subscription_dlist),
+	struct __fwk_notification_subscription, dlist_node);
 
     if (subscription == NULL) {
-        status = FWK_E_NOMEM;
-        fwk_unexpected();
-        goto error;
+	status = FWK_E_NOMEM;
+	fwk_unexpected();
+	goto error;
     }
 
     subscription->source_id = source_id;
@@ -228,7 +228,7 @@ error:
 }
 
 int fwk_notification_unsubscribe(fwk_id_t notification_id, fwk_id_t source_id,
-                                 fwk_id_t target_id)
+				 fwk_id_t target_id)
 {
     int status;
     unsigned int flags;
@@ -236,25 +236,25 @@ int fwk_notification_unsubscribe(fwk_id_
     struct __fwk_notification_subscription *subscription;
 
     if (fwk_is_interrupt_context()) {
-        status = FWK_E_HANDLER;
-        goto error;
+	status = FWK_E_HANDLER;
+	goto error;
     }
 
     if (!(fwk_module_is_valid_notification_id(notification_id)) ||
-        !(fwk_module_is_valid_entity_id(source_id)) ||
-        !(fwk_module_is_valid_entity_id(target_id)) ||
-        !fwk_id_is_equal(fwk_id_build_module_id(notification_id),
-                         fwk_id_build_module_id(source_id))) {
-        status = FWK_E_PARAM;
-        goto error;
+	!(fwk_module_is_valid_entity_id(source_id)) ||
+	!(fwk_module_is_valid_entity_id(target_id)) ||
+	!fwk_id_is_equal(fwk_id_build_module_id(notification_id),
+			 fwk_id_build_module_id(source_id))) {
+	status = FWK_E_PARAM;
+	goto error;
     }
 
     subscription_dlist = get_subscription_dlist(notification_id, source_id);
     subscription = search_subscription(subscription_dlist,
-                                       source_id, target_id);
+				       source_id, target_id);
     if (subscription == NULL) {
-        status = FWK_E_STATE;
-        goto error;
+	status = FWK_E_STATE;
+	goto error;
     }
 
     flags = fwk_interrupt_global_disable();
@@ -270,38 +270,38 @@ error:
 }
 
 int fwk_notification_notify(struct fwk_event *notification_event,
-                            unsigned int *count)
+			    unsigned int *count)
 {
     int status;
     const struct fwk_event *current_event;
 
     if ((notification_event == NULL) || (count == NULL)) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     if (fwk_is_interrupt_context()) {
-        if (!fwk_module_is_valid_entity_id(notification_event->source_id)) {
-            status = FWK_E_PARAM;
-            goto error;
-        }
+	if (!fwk_module_is_valid_entity_id(notification_event->source_id)) {
+	    status = FWK_E_PARAM;
+	    goto error;
+	}
     } else {
-        current_event = __fwk_get_current_event();
+	current_event = __fwk_get_current_event();
 
-        if ((current_event != NULL) &&
-            (!fwk_module_is_valid_entity_id(notification_event->source_id))) {
-            /*
-             * The source_id provided is not valid, use the identifier of the
-             * target for the current event.
-             */
-            notification_event->source_id = current_event->target_id;
-        }
+	if ((current_event != NULL) &&
+	    (!fwk_module_is_valid_entity_id(notification_event->source_id))) {
+	    /*
+	     * The source_id provided is not valid, use the identifier of the
+	     * target for the current event.
+	     */
+	    notification_event->source_id = current_event->target_id;
+	}
     }
 
     if (!fwk_module_is_valid_notification_id(notification_event->id) ||
-        (fwk_id_get_module_idx(notification_event->id) !=
-         fwk_id_get_module_idx(notification_event->source_id))) {
-        status = FWK_E_PARAM;
-        goto error;
+	(fwk_id_get_module_idx(notification_event->id) !=
+	 fwk_id_get_module_idx(notification_event->source_id))) {
+	status = FWK_E_PARAM;
+	goto error;
     }
 
     *count = 0;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/src/fwk_ring.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/framework/src/fwk_ring.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/src/fwk_ring.c
@@ -25,8 +25,8 @@ void fwk_ring_init(struct fwk_ring *ring
     fwk_assert(storage_size > 0);
 
     *ring = (struct fwk_ring){
-        .storage = storage,
-        .capacity = storage_size,
+	.storage = storage,
+	.capacity = storage_size,
     };
 
     fwk_ring_clear(ring);
@@ -44,9 +44,9 @@ size_t fwk_ring_get_length(const struct
     fwk_assert(ring != NULL);
 
     if ((ring->tail >= ring->head) && !ring->full) {
-        return (ring->tail - ring->head);
+	return (ring->tail - ring->head);
     } else {
-        return ((ring->capacity - ring->head) + ring->tail);
+	return ((ring->capacity - ring->head) + ring->tail);
     }
 }
 
@@ -76,13 +76,13 @@ size_t fwk_ring_pop(struct fwk_ring *rin
     fwk_assert(ring != NULL);
 
     buffer_size = (buffer == NULL) ?
-        FWK_MIN(buffer_size, fwk_ring_get_length(ring)) :
-        fwk_ring_peek(ring, buffer, buffer_size);
+	FWK_MIN(buffer_size, fwk_ring_get_length(ring)) :
+	fwk_ring_peek(ring, buffer, buffer_size);
 
     if (buffer_size > 0) {
-        ring->head = fwk_ring_offset(ring, ring->head + buffer_size);
+	ring->head = fwk_ring_offset(ring, ring->head + buffer_size);
 
-        ring->full = false;
+	ring->full = false;
     }
 
     return buffer_size;
@@ -98,17 +98,17 @@ size_t fwk_ring_peek(
 
     buffer_size = FWK_MIN(buffer_size, fwk_ring_get_length(ring));
     if (buffer_size == 0) {
-        return buffer_size;
+	return buffer_size;
     }
 
     if (fwk_ring_offset(ring, ring->head + buffer_size) > ring->head) {
-        (void)memcpy(buffer, ring->storage + ring->head, buffer_size);
+	(void)memcpy(buffer, ring->storage + ring->head, buffer_size);
     } else {
-        size_t chunk_size = ring->capacity - ring->head;
+	size_t chunk_size = ring->capacity - ring->head;
 
-        (void)memcpy(buffer, ring->storage + ring->head, chunk_size);
-        (void)memcpy(
-            buffer + chunk_size, ring->storage, buffer_size - chunk_size);
+	(void)memcpy(buffer, ring->storage + ring->head, chunk_size);
+	(void)memcpy(
+	    buffer + chunk_size, ring->storage, buffer_size - chunk_size);
     }
 
     return buffer_size;
@@ -125,36 +125,36 @@ size_t fwk_ring_push(
     fwk_assert(buffer != NULL);
 
     if (buffer_size == 0) {
-        return buffer_size;
+	return buffer_size;
     }
 
     if (buffer_size > ring->capacity) {
-        /*
-         * If the user has requested that we write more bytes than the ring
-         * buffer can hold, capture only the trailing bytes.
-         */
+	/*
+	 * If the user has requested that we write more bytes than the ring
+	 * buffer can hold, capture only the trailing bytes.
+	 */
 
-        buffer += (buffer_size - ring->capacity);
-        buffer_size = ring->capacity;
+	buffer += (buffer_size - ring->capacity);
+	buffer_size = ring->capacity;
     }
 
     remaining = fwk_ring_get_free(ring);
 
     if (fwk_ring_offset(ring, ring->tail + buffer_size) > ring->tail) {
-        (void)memcpy(ring->storage + ring->tail, buffer, buffer_size);
+	(void)memcpy(ring->storage + ring->tail, buffer, buffer_size);
     } else {
-        size_t chunk_size = ring->capacity - ring->tail;
+	size_t chunk_size = ring->capacity - ring->tail;
 
-        (void)memcpy(ring->storage + ring->tail, buffer, chunk_size);
-        (void)memcpy(
-            ring->storage, buffer + chunk_size, buffer_size - chunk_size);
+	(void)memcpy(ring->storage + ring->tail, buffer, chunk_size);
+	(void)memcpy(
+	    ring->storage, buffer + chunk_size, buffer_size - chunk_size);
     }
 
     ring->tail = fwk_ring_offset(ring, ring->tail + buffer_size);
 
     if (buffer_size >= remaining) {
-        ring->head = ring->tail;
-        ring->full = true;
+	ring->head = ring->tail;
+	ring->full = true;
     }
 
     /*
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/src/fwk_slist.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/framework/src/fwk_slist.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/src/fwk_slist.c
@@ -28,7 +28,7 @@ struct fwk_slist_node *__fwk_slist_head(
     fwk_assert(list != NULL);
 
     if (__fwk_slist_is_empty(list)) {
-        return NULL;
+	return NULL;
     }
 
     return list->head;
@@ -43,7 +43,7 @@ bool __fwk_slist_is_empty(const struct f
     is_empty = list->head == (struct fwk_slist_node *)list;
 
     if (is_empty) {
-        fwk_assert(list->tail == list->head);
+	fwk_assert(list->tail == list->head);
     }
 
     return is_empty;
@@ -61,7 +61,7 @@ void __fwk_slist_push_head(
 
     list->head = new;
     if (list->tail == (struct fwk_slist_node *)list) {
-        list->tail = new;
+	list->tail = new;
     }
 }
 
@@ -86,12 +86,12 @@ struct fwk_slist_node *__fwk_slist_pop_h
     fwk_assert(list != NULL);
 
     if (__fwk_slist_is_empty(list)) {
-        return NULL;
+	return NULL;
     }
 
     popped = list->head;
     if (popped->next == (struct fwk_slist_node *)list) {
-        list->tail = (struct fwk_slist_node *)list;
+	list->tail = (struct fwk_slist_node *)list;
     }
 
     list->head = popped->next;
@@ -124,18 +124,18 @@ void __fwk_slist_remove(
     struct fwk_slist_node *node_iter = (struct fwk_slist_node *)list;
 
     while (node_iter->next != (struct fwk_slist_node *)list) {
-        if (node_iter->next == node) {
-            node_iter->next = node->next;
+	if (node_iter->next == node) {
+	    node_iter->next = node->next;
 
-            if (node->next == (struct fwk_slist_node *)list) {
-                list->tail = (struct fwk_slist_node *)node_iter;
-            }
-
-            node->next = NULL;
-
-            return;
-        }
-        node_iter = node_iter->next;
+	    if (node->next == (struct fwk_slist_node *)list) {
+		list->tail = (struct fwk_slist_node *)node_iter;
+	    }
+
+	    node->next = NULL;
+
+	    return;
+	}
+	node_iter = node_iter->next;
     }
 
     fwk_unexpected();
@@ -154,11 +154,11 @@ bool __fwk_slist_contains(
     node_iter = (struct fwk_slist_node *)list;
 
     while (node_iter->next != (struct fwk_slist_node *)list) {
-        if (node_iter->next == node) {
-            return true;
-        }
+	if (node_iter->next == node) {
+	    return true;
+	}
 
-        node_iter = node_iter->next;
+	node_iter = node_iter->next;
     }
 
     return false;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/src/fwk_status.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/framework/src/fwk_status.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/src/fwk_status.c
@@ -44,9 +44,9 @@ const char *fwk_status_str(int status)
     unsigned int error_idx = (unsigned int)(-status);
 
     if ((status < 0) && (error_idx < FWK_ARRAY_SIZE(error_string))) {
-        return error_string[error_idx];
+	return error_string[error_idx];
     } else if ((status >= 0) && (status < (int)FWK_ARRAY_SIZE(status_string))) {
-        return status_string[status];
+	return status_string[status];
     }
 
     return unknown;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/src/fwk_string.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/framework/src/fwk_string.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/src/fwk_string.c
@@ -18,12 +18,12 @@ void fwk_str_memset(void *dest, int ch,
     void *ret;
 
     if (dest == NULL) {
-        fwk_trap();
+	fwk_trap();
     }
 
     ret = memset(dest, ch, count);
     if (ret != dest) {
-        fwk_trap();
+	fwk_trap();
     }
 }
 
@@ -32,12 +32,12 @@ void fwk_str_memcpy(void *dest, const vo
     void *ret;
 
     if ((dest == NULL) || (src == NULL)) {
-        fwk_trap();
+	fwk_trap();
     }
 
     ret = memcpy(dest, src, count);
     if (ret != dest) {
-        fwk_trap();
+	fwk_trap();
     }
 }
 
@@ -46,11 +46,11 @@ void fwk_str_strncpy(char *dest, const c
     char *ch;
 
     if ((dest == NULL) || (src == NULL)) {
-        fwk_trap();
+	fwk_trap();
     }
 
     ch = strncpy(dest, src, count);
     if (ch != dest) {
-        fwk_trap();
+	fwk_trap();
     }
 }
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/src/fwk_time.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/framework/src/fwk_time.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/src/fwk_time.c
@@ -27,7 +27,7 @@ FWK_CONSTRUCTOR void fwk_time_init(void)
 fwk_timestamp_t fwk_time_current(void)
 {
     if (fwk_time_ctx.driver.timestamp == NULL) {
-        return FWK_NS(0);
+	return FWK_NS(0);
     }
 
     return fwk_time_ctx.driver.timestamp(fwk_time_ctx.driver_ctx);
@@ -73,6 +73,6 @@ fwk_duration_h_t fwk_time_duration_h(fwk
 FWK_WEAK struct fwk_time_driver fmw_time_driver(const void **ctx)
 {
     return (struct fwk_time_driver){
-        .timestamp = NULL,
+	.timestamp = NULL,
     };
 }
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/test/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/framework/test/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/test/CMakeLists.txt
@@ -12,7 +12,7 @@ project(
     VERSION 2.12.0
     DESCRIPTION "Arm SCP/MCP Software Framework tests"
     HOMEPAGE_URL
-        "https://developer.arm.com/tools-and-software/open-source-software/firmware/scp-firmware"
+	"https://developer.arm.com/tools-and-software/open-source-software/firmware/scp-firmware"
     LANGUAGES C ASM)
 
 set(SCP_FWK_TEST_DIR "${CMAKE_CURRENT_SOURCE_DIR}")
@@ -21,10 +21,10 @@ set(SCP_FWK_TEST_DIR "${CMAKE_CURRENT_SO
 find_package(Git)
 if(GIT_FOUND)
     execute_process(
-        COMMAND "${GIT_EXECUTABLE}" describe --tags --dirty --always
-        WORKING_DIRECTORY "${SCP_FWK_TEST_DIR}"
-        OUTPUT_VARIABLE SCP_FWK_TEST_DESCRIBE
-        OUTPUT_STRIP_TRAILING_WHITESPACE)
+	COMMAND "${GIT_EXECUTABLE}" describe --tags --dirty --always
+	WORKING_DIRECTORY "${SCP_FWK_TEST_DIR}"
+	OUTPUT_VARIABLE SCP_FWK_TEST_DESCRIBE
+	OUTPUT_STRIP_TRAILING_WHITESPACE)
 endif()
 
 if(NOT SCP_FWK_TEST_DESCRIBE)
@@ -136,7 +136,7 @@ enable_testing()
 # cmake-lint: disable=E1120
 foreach(idx RANGE ${SCP_FWK_TEST_MAX})
     if(idx EQUAL SCP_FWK_TEST_MAX)
-        break()
+	break()
     endif()
 
     list(GET SCP_FWK_TEST_TARGETS ${idx} TEST_TARGET)
@@ -144,20 +144,20 @@ foreach(idx RANGE ${SCP_FWK_TEST_MAX})
     add_executable(${TEST_TARGET} ${TEST_TARGET}.c)
 
     target_compile_definitions(
-        ${TEST_TARGET}
-        PUBLIC "BUILD_VERSION_DESCRIBE_STRING=\"${SCP_FWK_TEST_DESCRIBE}\""
-               "BUILD_VERSION_MAJOR=${SCP_FWK_TEST_VERSION_MAJOR}"
-               "BUILD_VERSION_MINOR=${SCP_FWK_TEST_VERSION_MINOR}")
+	${TEST_TARGET}
+	PUBLIC "BUILD_VERSION_DESCRIBE_STRING=\"${SCP_FWK_TEST_DESCRIBE}\""
+	       "BUILD_VERSION_MAJOR=${SCP_FWK_TEST_VERSION_MAJOR}"
+	       "BUILD_VERSION_MINOR=${SCP_FWK_TEST_VERSION_MINOR}")
 
     foreach(COMPILE_FLAG IN LISTS EXTRA_COMPILE_FLAGS)
-        target_compile_options(${TEST_TARGET} PRIVATE "${COMPILE_FLAG}")
+	target_compile_options(${TEST_TARGET} PRIVATE "${COMPILE_FLAG}")
     endforeach()
 
     target_include_directories(
-        ${TEST_TARGET}
-        PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}"
-        PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include"
-        PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/../include")
+	${TEST_TARGET}
+	PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}"
+	PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include"
+	PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/../include")
 
     target_sources(${TEST_TARGET} PRIVATE ${COMMON_SRC})
 
@@ -167,26 +167,26 @@ foreach(idx RANGE ${SCP_FWK_TEST_MAX})
     # Check whether this test need notification support
     list(FIND NOTIFICATION_ENABLED_TEST ${TEST_TARGET} NOTIFICATIONS)
     if(NOT NOTIFICATIONS EQUAL -1)
-        target_sources(${TEST_TARGET}
-                       PRIVATE ${FWK_SRC_ROOT}/fwk_notification.c)
-        target_compile_definitions(${TEST_TARGET}
-                                   PUBLIC "BUILD_HAS_NOTIFICATION")
+	target_sources(${TEST_TARGET}
+		       PRIVATE ${FWK_SRC_ROOT}/fwk_notification.c)
+	target_compile_definitions(${TEST_TARGET}
+				   PUBLIC "BUILD_HAS_NOTIFICATION")
     endif()
 
     # Check if this test requires any custom module_idx_h file
     list(FIND TEST_MODULE_IDX_H ${TEST_TARGET} MODULE_IDX_H)
     if(NOT MODULE_IDX_H EQUAL -1)
-        target_compile_definitions(
-            ${TEST_TARGET}
-            PRIVATE "FWK_TEST_MODULE_IDX_H=\"${${TEST_TARGET}_MODULE_IDX_H}\"")
+	target_compile_definitions(
+	    ${TEST_TARGET}
+	    PRIVATE "FWK_TEST_MODULE_IDX_H=\"${${TEST_TARGET}_MODULE_IDX_H}\"")
     endif()
 
     # Use linker option to provide wrap functions
     if(${TEST_TARGET}_WRAP)
-        foreach(link_option IN LISTS ${TEST_TARGET}_WRAP)
-            target_link_options(${TEST_TARGET}
-                                PRIVATE "LINKER:-wrap=${link_option}")
-        endforeach()
+	foreach(link_option IN LISTS ${TEST_TARGET}_WRAP)
+	    target_link_options(${TEST_TARGET}
+				PRIVATE "LINKER:-wrap=${link_option}")
+	endforeach()
     endif()
 
     add_test(NAME ${TEST_TARGET} COMMAND ${TEST_TARGET})
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/test/arch_helpers.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/framework/test/arch_helpers.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/test/arch_helpers.h
@@ -21,7 +21,7 @@ inline static void arch_interrupts_enabl
 {
     /* Decrement critical_section_nest_level only if in critical section */
     if (critical_section_nest_level > 0) {
-        critical_section_nest_level--;
+	critical_section_nest_level--;
     }
 }
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/test/fwk_test.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/framework/test/fwk_test.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/test/fwk_test.c
@@ -65,7 +65,7 @@ static void print_epilogue(unsigned int
 
     print_separator();
     printf("%u / %u passed (%d%% pass rate)\n\n", successful_tests,
-        test_suite.test_case_count, pass_rate);
+	test_suite.test_case_count, pass_rate);
 }
 
 static void print_result(const char *name, bool success)
@@ -82,46 +82,46 @@ static unsigned int run_tests(void)
     const struct fwk_test_case_desc *test_case;
 
     if (test_suite.test_suite_setup != NULL) {
-        if (test_suite.test_suite_setup() != FWK_SUCCESS)
-            return 0;
+	if (test_suite.test_suite_setup() != FWK_SUCCESS)
+	    return 0;
     }
 
     for (i = 0; i < test_suite.test_case_count; i++) {
-        test_case = &test_suite.test_case_table[i];
+	test_case = &test_suite.test_case_table[i];
 
-        if ((test_case->test_execute == NULL) || (test_case->name == NULL)) {
-            print_result("Test case undefined!", false);
+	if ((test_case->test_execute == NULL) || (test_case->name == NULL)) {
+	    print_result("Test case undefined!", false);
 
-            continue;
-        }
+	    continue;
+	}
 
-        if (test_suite.test_case_setup != NULL)
-            test_suite.test_case_setup();
-
-        /*
-         * The setjmp function stores the execution context of the processor at
-         * that point in time. When called, 0 is returned by default.
-         * If an assertion fails in the test case following, execution returns
-         * to an undefined point within setjmp() which then returns a non-zero
-         * value. See __assert_fail() for exactly how assertion failure is
-         * handled.
-         */
-        if (setjmp(test_buf_context) == FWK_SUCCESS) {
-            test_case->test_execute();
-
-            success = true;
-            successful_tests++;
-        } else
-            success = false;
+	if (test_suite.test_case_setup != NULL)
+	    test_suite.test_case_setup();
+
+	/*
+	 * The setjmp function stores the execution context of the processor at
+	 * that point in time. When called, 0 is returned by default.
+	 * If an assertion fails in the test case following, execution returns
+	 * to an undefined point within setjmp() which then returns a non-zero
+	 * value. See __assert_fail() for exactly how assertion failure is
+	 * handled.
+	 */
+	if (setjmp(test_buf_context) == FWK_SUCCESS) {
+	    test_case->test_execute();
+
+	    success = true;
+	    successful_tests++;
+	} else
+	    success = false;
 
-        if (test_suite.test_case_teardown != NULL)
-            test_suite.test_case_teardown();
+	if (test_suite.test_case_teardown != NULL)
+	    test_suite.test_case_teardown();
 
-        print_result(test_case->name, success);
+	print_result(test_case->name, success);
     }
 
     if (test_suite.test_suite_teardown != NULL)
-        test_suite.test_suite_teardown();
+	test_suite.test_suite_teardown();
 
     return successful_tests;
 }
@@ -132,29 +132,29 @@ int main(void)
 
     for (enum fwk_module_idx i = 0; i < FWK_MODULE_IDX_COUNT; i++) {
 
-        struct fwk_module *module = malloc(sizeof(*module));
-        struct fwk_module_config *config = malloc(sizeof(*config));
+	struct fwk_module *module = malloc(sizeof(*module));
+	struct fwk_module_config *config = malloc(sizeof(*config));
 
-        *module = (struct fwk_module){
-            .type = FWK_MODULE_TYPE_SERVICE,
-            .init = fake_init,
-        };
+	*module = (struct fwk_module){
+	    .type = FWK_MODULE_TYPE_SERVICE,
+	    .init = fake_init,
+	};
 
-        *config = (struct fwk_module_config){ 0 };
+	*config = (struct fwk_module_config){ 0 };
 
-        module_table[i] = module;
-        module_config_table[i] = config;
+	module_table[i] = module;
+	module_config_table[i] = config;
     }
 
     fwk_module_init();
 
     if (test_suite.test_case_count != 0) {
-        print_prologue();
-        successful_tests = run_tests();
-        print_epilogue(successful_tests);
+	print_prologue();
+	successful_tests = run_tests();
+	print_epilogue(successful_tests);
 
-        if (successful_tests != test_suite.test_case_count)
-            return EXIT_FAILURE;
+	if (successful_tests != test_suite.test_case_count)
+	    return EXIT_FAILURE;
     }
 
     return EXIT_SUCCESS;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/test/test_fwk_core.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/framework/test/test_fwk_core.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/test/test_fwk_core.c
@@ -31,8 +31,8 @@ static int fwk_mm_calloc_return_val;
 void *__wrap_fwk_mm_calloc(size_t num, size_t size)
 {
     if (fwk_mm_calloc_return_val) {
-        fwk_mm_calloc_val = (void *)calloc(num, size);
-        return fwk_mm_calloc_val;
+	fwk_mm_calloc_val = (void *)calloc(num, size);
+	return fwk_mm_calloc_val;
     }
     return NULL;
 }
@@ -54,7 +54,7 @@ void __wrap___fwk_slist_push_tail(
 {
     __real___fwk_slist_push_tail(list, new);
     if (free_event_queue_break && (list == &(ctx->free_event_queue)))
-        longjmp(test_context, FWK_SUCCESS);
+	longjmp(test_context, FWK_SUCCESS);
 }
 
 static bool is_valid_entity_id_return_val;
@@ -156,11 +156,11 @@ static void test___fwk_init(void)
     result = __fwk_init(event_count);
     assert(result == FWK_SUCCESS);
     assert(
-        ctx->free_event_queue.head ==
-        &(((struct fwk_event *)fwk_mm_calloc_val)->slist_node));
+	ctx->free_event_queue.head ==
+	&(((struct fwk_event *)fwk_mm_calloc_val)->slist_node));
     assert(
-        ctx->free_event_queue.tail ==
-        &((((struct fwk_event *)(fwk_mm_calloc_val)) + 1)->slist_node));
+	ctx->free_event_queue.tail ==
+	&((((struct fwk_event *)(fwk_mm_calloc_val)) + 1)->slist_node));
 }
 
 static void test___fwk_run_main_loop(void)
@@ -169,65 +169,65 @@ static void test___fwk_run_main_loop(voi
     struct fwk_event *free_event, *allocated_event;
 
     struct fwk_event event1 = {
-        .source_id = FWK_ID_MODULE(0x1),
-        .target_id = FWK_ID_MODULE(0x2),
-        .is_response = false,
-        .response_requested = true,
-        .is_notification = false,
-        .id = FWK_ID_EVENT(0x2, 0x7),
+	.source_id = FWK_ID_MODULE(0x1),
+	.target_id = FWK_ID_MODULE(0x2),
+	.is_response = false,
+	.response_requested = true,
+	.is_notification = false,
+	.id = FWK_ID_EVENT(0x2, 0x7),
     };
 
     struct fwk_event event2 = {
-        .source_id = FWK_ID_MODULE(0x3),
-        .target_id = FWK_ID_MODULE(0x4),
-        .is_response = false,
-        .response_requested = false,
-        .is_notification = true,
-        .id = FWK_ID_NOTIFICATION(0x4, 0x8),
+	.source_id = FWK_ID_MODULE(0x3),
+	.target_id = FWK_ID_MODULE(0x4),
+	.is_response = false,
+	.response_requested = false,
+	.is_notification = true,
+	.id = FWK_ID_NOTIFICATION(0x4, 0x8),
     };
 
     struct fwk_event event3 = {
-        .source_id = FWK_ID_MODULE(0x5),
-        .target_id = FWK_ID_MODULE(0x6),
-        .is_response = false,
-        .response_requested = false,
-        .is_notification = false,
-        .id = FWK_ID_EVENT(0x6, 0x9),
+	.source_id = FWK_ID_MODULE(0x5),
+	.target_id = FWK_ID_MODULE(0x6),
+	.is_response = false,
+	.response_requested = false,
+	.is_notification = false,
+	.id = FWK_ID_EVENT(0x6, 0x9),
     };
 
     struct fwk_event notification1 = {
-        .source_id = FWK_ID_MODULE(0x5),
-        .target_id = FWK_ID_MODULE(0x6),
-        .is_response = false,
-        .response_requested = true,
-        .is_notification = true,
-        .id = FWK_ID_NOTIFICATION(0x5, 0x9),
+	.source_id = FWK_ID_MODULE(0x5),
+	.target_id = FWK_ID_MODULE(0x6),
+	.is_response = false,
+	.response_requested = true,
+	.is_notification = true,
+	.id = FWK_ID_NOTIFICATION(0x5, 0x9),
     };
 
     result = __fwk_init(1);
     assert(result == FWK_SUCCESS);
     free_event_queue_break = true;
     allocated_event = FWK_LIST_GET(
-        fwk_list_head(&ctx->free_event_queue), struct fwk_event, slist_node);
+	fwk_list_head(&ctx->free_event_queue), struct fwk_event, slist_node);
 
     __real___fwk_slist_push_tail(&ctx->event_queue, &(event1.slist_node));
     __real___fwk_slist_push_tail(&ctx->event_queue, &(event2.slist_node));
     __real___fwk_slist_push_tail(&ctx->isr_event_queue, &(event3.slist_node));
     __real___fwk_slist_push_tail(
-        &ctx->isr_event_queue, &(notification1.slist_node));
+	&ctx->isr_event_queue, &(notification1.slist_node));
 
     /* Event1 processing */
     if (setjmp(test_context) == FWK_SUCCESS)
-        __fwk_run_main_loop();
+	__fwk_run_main_loop();
     assert(ctx->isr_event_queue.head == &(event3.slist_node));
     assert(ctx->isr_event_queue.tail == &(notification1.slist_node));
     assert(ctx->event_queue.head == &(event2.slist_node));
     assert(ctx->event_queue.tail == &(allocated_event->slist_node));
 
     free_event = FWK_LIST_GET(
-        fwk_list_pop_head(&ctx->free_event_queue),
-        struct fwk_event,
-        slist_node);
+	fwk_list_pop_head(&ctx->free_event_queue),
+	struct fwk_event,
+	slist_node);
     assert(fwk_list_is_empty(&ctx->free_event_queue));
     assert(free_event == &event1);
     assert(processed_event == &event1);
@@ -237,16 +237,16 @@ static void test___fwk_run_main_loop(voi
 
     /* Event2 processing */
     if (setjmp(test_context) == FWK_SUCCESS)
-        __fwk_run_main_loop();
+	__fwk_run_main_loop();
     assert(ctx->isr_event_queue.head == &(event3.slist_node));
     assert(ctx->isr_event_queue.tail == &(notification1.slist_node));
     assert(ctx->event_queue.head == &(allocated_event->slist_node));
     assert(ctx->event_queue.tail == &(allocated_event->slist_node));
 
     free_event = FWK_LIST_GET(
-        fwk_list_pop_head(&ctx->free_event_queue),
-        struct fwk_event,
-        slist_node);
+	fwk_list_pop_head(&ctx->free_event_queue),
+	struct fwk_event,
+	slist_node);
     assert(fwk_list_is_empty(&ctx->free_event_queue));
     assert(free_event == &event2);
     assert(processed_notification == &event2);
@@ -256,15 +256,15 @@ static void test___fwk_run_main_loop(voi
 
     /* Response to Event1 processing */
     if (setjmp(test_context) == FWK_SUCCESS)
-        __fwk_run_main_loop();
+	__fwk_run_main_loop();
     assert(ctx->isr_event_queue.head == &(event3.slist_node));
     assert(ctx->isr_event_queue.tail == &(notification1.slist_node));
     assert(fwk_list_is_empty(&ctx->event_queue));
 
     free_event = FWK_LIST_GET(
-        fwk_list_pop_head(&ctx->free_event_queue),
-        struct fwk_event,
-        slist_node);
+	fwk_list_pop_head(&ctx->free_event_queue),
+	struct fwk_event,
+	slist_node);
     assert(free_event == allocated_event);
     assert(processed_event == allocated_event);
     assert(processed_event->is_response == true);
@@ -276,15 +276,15 @@ static void test___fwk_run_main_loop(voi
 
     /* Extract ISR Event3 and process it */
     if (setjmp(test_context) == FWK_SUCCESS)
-        __fwk_run_main_loop();
+	__fwk_run_main_loop();
     assert(ctx->isr_event_queue.head == &(notification1.slist_node));
     assert(ctx->isr_event_queue.tail == &(notification1.slist_node));
     assert(fwk_list_is_empty(&ctx->event_queue));
 
     free_event = FWK_LIST_GET(
-        fwk_list_pop_head(&ctx->free_event_queue),
-        struct fwk_event,
-        slist_node);
+	fwk_list_pop_head(&ctx->free_event_queue),
+	struct fwk_event,
+	slist_node);
     assert(free_event == &event3);
     assert(processed_event == &event3);
     assert(processed_event->is_response == false);
@@ -296,15 +296,15 @@ static void test___fwk_run_main_loop(voi
     fwk_list_push_tail(&ctx->free_event_queue, &(allocated_event->slist_node));
     free_event_queue_break = true;
     if (setjmp(test_context) == FWK_SUCCESS)
-        __fwk_run_main_loop();
+	__fwk_run_main_loop();
     assert(fwk_list_is_empty(&ctx->isr_event_queue));
     assert(ctx->event_queue.head == &(allocated_event->slist_node));
     assert(ctx->event_queue.tail == &(allocated_event->slist_node));
 
     free_event = FWK_LIST_GET(
-        fwk_list_pop_head(&ctx->free_event_queue),
-        struct fwk_event,
-        slist_node);
+	fwk_list_pop_head(&ctx->free_event_queue),
+	struct fwk_event,
+	slist_node);
     assert(free_event == &notification1);
     assert(processed_notification == &notification1);
     assert(processed_notification->is_response == false);
@@ -313,25 +313,25 @@ static void test___fwk_run_main_loop(voi
 
     /* Process response to Notification1 */
     if (setjmp(test_context) == FWK_SUCCESS)
-        __fwk_run_main_loop();
+	__fwk_run_main_loop();
     assert(fwk_list_is_empty(&ctx->isr_event_queue));
     assert(fwk_list_is_empty(&ctx->event_queue));
 
     free_event = FWK_LIST_GET(
-        fwk_list_pop_head(&ctx->free_event_queue),
-        struct fwk_event,
-        slist_node);
+	fwk_list_pop_head(&ctx->free_event_queue),
+	struct fwk_event,
+	slist_node);
     assert(free_event == allocated_event);
     assert(processed_notification == allocated_event);
     assert(processed_notification->is_response == true);
     assert(processed_notification->response_requested == false);
     assert(processed_notification->is_notification == true);
     assert(
-        fwk_id_is_equal(processed_notification->source_id, FWK_ID_MODULE(0x6)));
+	fwk_id_is_equal(processed_notification->source_id, FWK_ID_MODULE(0x6)));
     assert(
-        fwk_id_is_equal(processed_notification->target_id, FWK_ID_MODULE(0x5)));
+	fwk_id_is_equal(processed_notification->target_id, FWK_ID_MODULE(0x5)));
     assert(fwk_id_is_equal(
-        processed_notification->id, FWK_ID_NOTIFICATION(0x5, 0x9)));
+	processed_notification->id, FWK_ID_NOTIFICATION(0x5, 0x9)));
 }
 
 static void test_fwk_put_event(void)
@@ -340,18 +340,18 @@ static void test_fwk_put_event(void)
     struct fwk_event *result_event;
 
     struct fwk_event event1 = {
-        .source_id = FWK_ID_MODULE(0x1),
-        .target_id = FWK_ID_MODULE(0x2),
-        .is_response = false,
-        .response_requested = true,
-        .id = FWK_ID_EVENT(0x2, 7),
+	.source_id = FWK_ID_MODULE(0x1),
+	.target_id = FWK_ID_MODULE(0x2),
+	.is_response = false,
+	.response_requested = true,
+	.id = FWK_ID_EVENT(0x2, 7),
     };
 
     struct fwk_event event2 = {
-        .source_id = FWK_ID_MODULE(0x3),
-        .target_id = FWK_ID_MODULE(0x4),
-        .is_response = true,
-        .response_requested = false,
+	.source_id = FWK_ID_MODULE(0x3),
+	.target_id = FWK_ID_MODULE(0x4),
+	.is_response = true,
+	.response_requested = false,
     };
 
     /* Framework core not initialized */
@@ -381,7 +381,7 @@ static void test_fwk_put_event(void)
     result = fwk_put_event(&event1);
     assert(result == FWK_SUCCESS);
     result_event = FWK_LIST_GET(
-        fwk_list_pop_head(&ctx->event_queue), struct fwk_event, slist_node);
+	fwk_list_pop_head(&ctx->event_queue), struct fwk_event, slist_node);
     assert(fwk_id_is_equal(result_event->source_id, event1.source_id));
     assert(fwk_id_is_equal(result_event->target_id, event1.target_id));
     assert(result_event->is_response == event1.is_response);
@@ -394,7 +394,7 @@ static void test_fwk_put_event(void)
     assert(result == FWK_SUCCESS);
     assert(fwk_list_is_empty(&ctx->free_event_queue));
     result_event = FWK_LIST_GET(
-        fwk_list_pop_head(&ctx->isr_event_queue), struct fwk_event, slist_node);
+	fwk_list_pop_head(&ctx->isr_event_queue), struct fwk_event, slist_node);
     assert(fwk_id_is_equal(result_event->source_id, event2.source_id));
     assert(fwk_id_is_equal(result_event->target_id, event2.target_id));
     assert(result_event->is_response == true);
@@ -408,17 +408,17 @@ static void test_fwk_put_event_light(voi
     struct fwk_event *result_event;
 
     struct fwk_event_light event1 = {
-        .source_id = FWK_ID_MODULE(0x1),
-        .target_id = FWK_ID_MODULE(0x2),
-        .response_requested = true,
-        .id = FWK_ID_EVENT(0x2, 7),
+	.source_id = FWK_ID_MODULE(0x1),
+	.target_id = FWK_ID_MODULE(0x2),
+	.response_requested = true,
+	.id = FWK_ID_EVENT(0x2, 7),
     };
 
     struct fwk_event_light event2 = {
-        .source_id = FWK_ID_MODULE(0x3),
-        .target_id = FWK_ID_MODULE(0x4),
-        .response_requested = false,
-        .id = FWK_ID_EVENT(0x4, 7),
+	.source_id = FWK_ID_MODULE(0x3),
+	.target_id = FWK_ID_MODULE(0x4),
+	.response_requested = false,
+	.id = FWK_ID_EVENT(0x4, 7),
     };
 
     /* Framework core not initialized */
@@ -450,7 +450,7 @@ static void test_fwk_put_event_light(voi
     assert(result == FWK_SUCCESS);
     /* Framework always queue light event by converting in a standard event */
     result_event = FWK_LIST_GET(
-        fwk_list_pop_head(&ctx->event_queue), struct fwk_event, slist_node);
+	fwk_list_pop_head(&ctx->event_queue), struct fwk_event, slist_node);
     assert(fwk_id_is_equal(result_event->source_id, event1.source_id));
     assert(fwk_id_is_equal(result_event->target_id, event1.target_id));
     assert(result_event->is_response == false);
@@ -465,7 +465,7 @@ static void test_fwk_put_event_light(voi
 
     /* Framework always queue light event by converting in a standard event */
     result_event = FWK_LIST_GET(
-        fwk_list_pop_head(&ctx->isr_event_queue), struct fwk_event, slist_node);
+	fwk_list_pop_head(&ctx->isr_event_queue), struct fwk_event, slist_node);
     assert(fwk_id_is_equal(result_event->source_id, event2.source_id));
     assert(fwk_id_is_equal(result_event->target_id, event2.target_id));
     assert(result_event->is_response == false);
@@ -479,18 +479,18 @@ static void test___fwk_put_notification(
     struct fwk_event *result_event;
 
     struct fwk_event event1 = {
-        .source_id = FWK_ID_MODULE(0x1),
-        .target_id = FWK_ID_MODULE(0x2),
-        .is_response = false,
-        .response_requested = true,
-        .id = FWK_ID_EVENT(0x2, 7),
+	.source_id = FWK_ID_MODULE(0x1),
+	.target_id = FWK_ID_MODULE(0x2),
+	.is_response = false,
+	.response_requested = true,
+	.id = FWK_ID_EVENT(0x2, 7),
     };
 
     struct fwk_event event2 = {
-        .source_id = FWK_ID_MODULE(0x3),
-        .target_id = FWK_ID_MODULE(0x4),
-        .is_response = true,
-        .response_requested = false,
+	.source_id = FWK_ID_MODULE(0x3),
+	.target_id = FWK_ID_MODULE(0x4),
+	.is_response = true,
+	.response_requested = false,
     };
 
     result = __fwk_init(2);
@@ -499,7 +499,7 @@ static void test___fwk_put_notification(
     result = __fwk_put_notification(&event1);
     assert(result == FWK_SUCCESS);
     result_event = FWK_LIST_GET(
-        fwk_list_pop_head(&ctx->event_queue), struct fwk_event, slist_node);
+	fwk_list_pop_head(&ctx->event_queue), struct fwk_event, slist_node);
     assert(fwk_id_is_equal(result_event->source_id, event1.source_id));
     assert(fwk_id_is_equal(result_event->target_id, event1.target_id));
     assert(result_event->is_response == false);
@@ -512,7 +512,7 @@ static void test___fwk_put_notification(
     assert(result == FWK_SUCCESS);
     assert(fwk_list_is_empty(&ctx->free_event_queue));
     result_event = FWK_LIST_GET(
-        fwk_list_pop_head(&ctx->isr_event_queue), struct fwk_event, slist_node);
+	fwk_list_pop_head(&ctx->isr_event_queue), struct fwk_event, slist_node);
     assert(fwk_id_is_equal(result_event->source_id, event2.source_id));
     assert(fwk_id_is_equal(result_event->target_id, event2.target_id));
     assert(result_event->is_response == false);
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/test/test_fwk_interrupt.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/framework/test/test_fwk_interrupt.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/test/test_fwk_interrupt.c
@@ -90,8 +90,8 @@ static int set_isr(unsigned int interrup
 }
 
 static int set_isr_param(unsigned int interrupt,
-                         void (*isr)(uintptr_t p),
-                         uintptr_t p)
+			 void (*isr)(uintptr_t p),
+			 uintptr_t p)
 {
     return set_isr_param_return_val;
 }
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/test/test_fwk_list_get.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/framework/test/test_fwk_list_get.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/test/test_fwk_list_get.c
@@ -14,8 +14,8 @@ struct container {
     unsigned int value;
 
     union {
-        struct fwk_slist_node slink;
-        struct fwk_dlist_node dlink;
+	struct fwk_slist_node slink;
+	struct fwk_dlist_node dlink;
     };
 };
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/test/test_fwk_module.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/framework/test/test_fwk_module.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/test/test_fwk_module.c
@@ -48,9 +48,9 @@
 /* Fake API */
 struct fake_api {
     int (*init)(fwk_id_t module_id, unsigned int element_count,
-                const void *data);
+		const void *data);
     int (*element_init)(fwk_id_t element_id, unsigned int sub_element_count,
-                        const void *data);
+			const void *data);
 };
 
 /* Fake element data */
@@ -148,7 +148,7 @@ static int process_bind_request(fwk_id_t
     (void) target_id;
     (void) api_id;
     if (process_bind_request_return_api)
-        *api = &fake_api;
+	*api = &fake_api;
 
     return process_bind_request_return_val;
 }
@@ -157,18 +157,18 @@ static const struct fwk_element *get_ele
 {
     (void) module_id;
     if (get_element_table0_return_val)
-        return fake_element_desc_table0;
+	return fake_element_desc_table0;
     else
-        return NULL;
+	return NULL;
 }
 
 static const struct fwk_element *get_element_table1(fwk_id_t module_id)
 {
     (void) module_id;
     if (get_element_table1_return_val)
-        return fake_element_desc_table1;
+	return fake_element_desc_table1;
     else
-        return NULL;
+	return NULL;
 }
 
 /* Wrapped functions */
@@ -184,12 +184,12 @@ static int fwk_mm_calloc_return;
 void * __wrap_fwk_mm_calloc(size_t num, size_t size)
 {
     if (num == 0)
-        return NULL;
+	return NULL;
     if (fwk_mm_calloc_return == 0)
-        return calloc(num, size);
+	return calloc(num, size);
     else if (fwk_mm_calloc_return <= 4) {
-        fwk_mm_calloc_return++;
-        return calloc(num, size);
+	fwk_mm_calloc_return++;
+	return calloc(num, size);
     }
     return NULL;
 }
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/test/test_fwk_notification.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/framework/test/test_fwk_notification.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/test/test_fwk_notification.c
@@ -29,10 +29,10 @@ static int fwk_mm_calloc_return_val;
 void *__wrap_fwk_mm_calloc(size_t num, size_t size)
 {
     if (fwk_mm_calloc_return_val) {
-        fwk_mm_calloc_num = num;
-        fwk_mm_calloc_size = size;
-        fwk_mm_calloc_val = (void *)calloc(num, size);
-        return fwk_mm_calloc_val;
+	fwk_mm_calloc_num = num;
+	fwk_mm_calloc_size = size;
+	fwk_mm_calloc_val = (void *)calloc(num, size);
+	return fwk_mm_calloc_val;
     }
 
     fwk_mm_calloc_num = 0;
@@ -114,10 +114,10 @@ static void test_case_setup(void)
     notification_event_count = 0;
 
     for (i = 0; i < FWK_ARRAY_SIZE(fake_module_dlist_table); i++)
-        fwk_list_init(&fake_module_dlist_table[i]);
+	fwk_list_init(&fake_module_dlist_table[i]);
 
     for (i = 0; i < FWK_ARRAY_SIZE(fake_element_dlist_table); i++)
-        fwk_list_init(&fake_element_dlist_table[i]);
+	fwk_list_init(&fake_element_dlist_table[i]);
 }
 
 static void test_case_teardown(void)
@@ -132,55 +132,55 @@ static void test_fwk_notification_subscr
     /* Call from an ISR */
     interrupt_get_current_return_val = true;
     result = fwk_notification_subscribe(FWK_ID_NOTIFICATION(0x2, 0x3),
-                                        FWK_ID_ELEMENT(0x2, 0x9),
-                                        FWK_ID_MODULE(0x4));
+					FWK_ID_ELEMENT(0x2, 0x9),
+					FWK_ID_MODULE(0x4));
     assert(result == FWK_E_HANDLER);
     interrupt_get_current_return_val = false;
 
     /* Invalid entity ID */
     is_valid_entity_id_return_val = false;
     result = fwk_notification_subscribe(FWK_ID_NOTIFICATION(0x2, 0x3),
-                                        FWK_ID_ELEMENT(0x2, 0x9),
-                                        FWK_ID_MODULE(0x4));
+					FWK_ID_ELEMENT(0x2, 0x9),
+					FWK_ID_MODULE(0x4));
     assert(result == FWK_E_PARAM);
     is_valid_entity_id_return_val = true;
 
     /* Invalid notification ID */
     is_valid_notification_id_return_val = false;
     result = fwk_notification_subscribe(FWK_ID_NOTIFICATION(0x2, 0x3),
-                                        FWK_ID_ELEMENT(0x2, 0x9),
-                                        FWK_ID_MODULE(0x4));
+					FWK_ID_ELEMENT(0x2, 0x9),
+					FWK_ID_MODULE(0x4));
     assert(result == FWK_E_PARAM);
     is_valid_notification_id_return_val = true;
 
     /* Incompatible source and event identifier */
     result = fwk_notification_subscribe(FWK_ID_NOTIFICATION(0x3, 0x3),
-                                        FWK_ID_ELEMENT(0x2, 0x9),
-                                        FWK_ID_MODULE(0x4));
+					FWK_ID_ELEMENT(0x2, 0x9),
+					FWK_ID_MODULE(0x4));
     assert(result == FWK_E_PARAM);
 
     /* Subscribe successfully to an element notification */
     result = fwk_notification_subscribe(FWK_ID_NOTIFICATION(0x2, 0x3),
-                                        FWK_ID_ELEMENT(0x2, 0x9),
-                                        FWK_ID_MODULE(0x4));
+					FWK_ID_ELEMENT(0x2, 0x9),
+					FWK_ID_MODULE(0x4));
     assert(result == FWK_SUCCESS);
 
     /* Try to subscribe to the same element notification */
     result = fwk_notification_subscribe(FWK_ID_NOTIFICATION(0x2, 0x3),
-                                        FWK_ID_ELEMENT(0x2, 0x9),
-                                        FWK_ID_MODULE(0x4));
+					FWK_ID_ELEMENT(0x2, 0x9),
+					FWK_ID_MODULE(0x4));
     assert(result == FWK_E_STATE);
 
     /* Subscribe successfully to a module notification */
     result = fwk_notification_subscribe(FWK_ID_NOTIFICATION(0x2, 0x1),
-                                        FWK_ID_MODULE(0x2),
-                                        FWK_ID_ELEMENT(0x4, 0x15));
+					FWK_ID_MODULE(0x2),
+					FWK_ID_ELEMENT(0x4, 0x15));
     assert(result == FWK_SUCCESS);
 
     /* Try to subscribe to the same module notification */
     result = fwk_notification_subscribe(FWK_ID_NOTIFICATION(0x2, 0x1),
-                                        FWK_ID_MODULE(0x2),
-                                        FWK_ID_ELEMENT(0x4, 0x15));
+					FWK_ID_MODULE(0x2),
+					FWK_ID_ELEMENT(0x4, 0x15));
     assert(result == FWK_E_STATE);
 }
 
@@ -191,55 +191,55 @@ static void test_fwk_notification_unsubs
     /* Call from an ISR */
     interrupt_get_current_return_val = true;
     result = fwk_notification_unsubscribe(FWK_ID_NOTIFICATION(0x2, 0x3),
-                                          FWK_ID_ELEMENT(0x2, 0x9),
-                                          FWK_ID_MODULE(0x4));
+					  FWK_ID_ELEMENT(0x2, 0x9),
+					  FWK_ID_MODULE(0x4));
     assert(result == FWK_E_HANDLER);
     interrupt_get_current_return_val = false;
 
     /* Invalid entity ID */
     is_valid_entity_id_return_val = false;
     result = fwk_notification_unsubscribe(FWK_ID_NOTIFICATION(0x2, 0x3),
-                                          FWK_ID_ELEMENT(0x2, 0x9),
-                                          FWK_ID_MODULE(0x4));
+					  FWK_ID_ELEMENT(0x2, 0x9),
+					  FWK_ID_MODULE(0x4));
     assert(result == FWK_E_PARAM);
     is_valid_entity_id_return_val = true;
 
     /* Invalid notification ID */
     is_valid_notification_id_return_val = false;
     result = fwk_notification_unsubscribe(FWK_ID_NOTIFICATION(0x2, 0x3),
-                                          FWK_ID_ELEMENT(0x2, 0x9),
-                                          FWK_ID_MODULE(0x4));
+					  FWK_ID_ELEMENT(0x2, 0x9),
+					  FWK_ID_MODULE(0x4));
     assert(result == FWK_E_PARAM);
     is_valid_notification_id_return_val = true;
 
     /* Incompatible source and event identifier */
     result = fwk_notification_unsubscribe(FWK_ID_NOTIFICATION(0x3, 0x3),
-                                          FWK_ID_ELEMENT(0x2, 0x9),
-                                          FWK_ID_MODULE(0x4));
+					  FWK_ID_ELEMENT(0x2, 0x9),
+					  FWK_ID_MODULE(0x4));
     assert(result == FWK_E_PARAM);
 
     /* Subscribe to an element notification */
     result = fwk_notification_subscribe(FWK_ID_NOTIFICATION(0x2, 0x1),
-                                        FWK_ID_ELEMENT(0x2, 0x9),
-                                        FWK_ID_MODULE(0x4));
+					FWK_ID_ELEMENT(0x2, 0x9),
+					FWK_ID_MODULE(0x4));
     assert(result == FWK_SUCCESS);
 
     /* Subscribe to the same element notification for another target */
     result = fwk_notification_subscribe(FWK_ID_NOTIFICATION(0x2, 0x1),
-                                        FWK_ID_ELEMENT(0x2, 0x9),
-                                        FWK_ID_ELEMENT(0x6, 0x1));
+					FWK_ID_ELEMENT(0x2, 0x9),
+					FWK_ID_ELEMENT(0x6, 0x1));
     assert(result == FWK_SUCCESS);
 
     /* Subscribe to a module notification */
     result = fwk_notification_subscribe(FWK_ID_NOTIFICATION(0x2, 0x1),
-                                        FWK_ID_MODULE(0x2),
-                                        FWK_ID_ELEMENT(0x4, 0x15));
+					FWK_ID_MODULE(0x2),
+					FWK_ID_ELEMENT(0x4, 0x15));
     assert(result == FWK_SUCCESS);
 
     /* Unsubscribe to a notification that has not been subscribed to */
     result = fwk_notification_unsubscribe(FWK_ID_NOTIFICATION(0x2, 0x1),
-                                          FWK_ID_ELEMENT(0x2, 0x9),
-                                          FWK_ID_MODULE(0x6));
+					  FWK_ID_ELEMENT(0x2, 0x9),
+					  FWK_ID_MODULE(0x6));
     assert(result == FWK_E_STATE);
 
     /*
@@ -247,15 +247,15 @@ static void test_fwk_notification_unsubs
      * first
      */
     result = fwk_notification_unsubscribe(FWK_ID_NOTIFICATION(0x2, 0x1),
-                                          FWK_ID_ELEMENT(0x2, 0x9),
-                                          FWK_ID_MODULE(0x4));
+					  FWK_ID_ELEMENT(0x2, 0x9),
+					  FWK_ID_MODULE(0x4));
     assert(result == FWK_SUCCESS);
 
     /* Subscribe to another element notification, the subscription freed in the
        previous step is re-used */
     result = fwk_notification_subscribe(FWK_ID_NOTIFICATION(0x2, 0x3),
-                                        FWK_ID_MODULE(0x2),
-                                        FWK_ID_ELEMENT(0xC, 0xC));
+					FWK_ID_MODULE(0x2),
+					FWK_ID_ELEMENT(0xC, 0xC));
     assert(result == FWK_SUCCESS);
 }
 
@@ -297,20 +297,20 @@ static void test_fwk_notification_notify
 
     /* Subscribe to an element notification */
     result = fwk_notification_subscribe(FWK_ID_NOTIFICATION(0x2, 0x1),
-                                        FWK_ID_ELEMENT(0x2, 0x9),
-                                        FWK_ID_MODULE(0x4));
+					FWK_ID_ELEMENT(0x2, 0x9),
+					FWK_ID_MODULE(0x4));
     assert(result == FWK_SUCCESS);
 
     /* Subscribe to the same element notification for another target */
     result = fwk_notification_subscribe(FWK_ID_NOTIFICATION(0x2, 0x1),
-                                        FWK_ID_ELEMENT(0x2, 0x9),
-                                        FWK_ID_ELEMENT(0x6, 0x1));
+					FWK_ID_ELEMENT(0x2, 0x9),
+					FWK_ID_ELEMENT(0x6, 0x1));
     assert(result == FWK_SUCCESS);
 
     /* Subscribe to a module notification */
     result = fwk_notification_subscribe(FWK_ID_NOTIFICATION(0x2, 0x1),
-                                        FWK_ID_MODULE(0x2),
-                                        FWK_ID_ELEMENT(0x4, 0x15));
+					FWK_ID_MODULE(0x2),
+					FWK_ID_ELEMENT(0x4, 0x15));
     assert(result == FWK_SUCCESS);
 
     /* Send the notifications FWK_ID_NOTIFICATION(0x2, 0x1) from
@@ -322,17 +322,17 @@ static void test_fwk_notification_notify
     assert(count == 2);
     assert(notification_event_count == 2);
     assert(fwk_id_is_equal(notification_event_table[0].source_id,
-                           FWK_ID_ELEMENT(0x2, 0x9)));
+			   FWK_ID_ELEMENT(0x2, 0x9)));
     assert(fwk_id_is_equal(notification_event_table[0].target_id,
-                           FWK_ID_MODULE(0x4)));
+			   FWK_ID_MODULE(0x4)));
     assert(fwk_id_is_equal(notification_event_table[0].id,
-                           FWK_ID_NOTIFICATION(0x2, 0x1)));
+			   FWK_ID_NOTIFICATION(0x2, 0x1)));
     assert(fwk_id_is_equal(notification_event_table[1].source_id,
-                           FWK_ID_ELEMENT(0x2, 0x9)));
+			   FWK_ID_ELEMENT(0x2, 0x9)));
     assert(fwk_id_is_equal(notification_event_table[1].target_id,
-                           FWK_ID_ELEMENT(0x6, 0x1)));
+			   FWK_ID_ELEMENT(0x6, 0x1)));
     assert(fwk_id_is_equal(notification_event_table[1].id,
-                           FWK_ID_NOTIFICATION(0x2, 0x1)));
+			   FWK_ID_NOTIFICATION(0x2, 0x1)));
     notification_event_count = 0;
 
     /* Send the notification FWK_ID_NOTIFICATION(0x2, 0x1) from
@@ -347,11 +347,11 @@ static void test_fwk_notification_notify
     assert(count == 1);
     assert(notification_event_count == 1);
     assert(fwk_id_is_equal(notification_event_table[0].source_id,
-                           FWK_ID_MODULE(0x2)));
+			   FWK_ID_MODULE(0x2)));
     assert(fwk_id_is_equal(notification_event_table[0].target_id,
-                           FWK_ID_ELEMENT(0x4, 0x15)));
+			   FWK_ID_ELEMENT(0x4, 0x15)));
     assert(fwk_id_is_equal(notification_event_table[0].id,
-                           FWK_ID_NOTIFICATION(0x2, 0x1)));
+			   FWK_ID_NOTIFICATION(0x2, 0x1)));
     get_current_event_return_val = NULL;
     notification_event_count = 0;
 }
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/test/test_fwk_string.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/framework/test/test_fwk_string.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/framework/test/test_fwk_string.c
@@ -22,7 +22,7 @@ void test_fwk_str_memset(void)
     fwk_str_memset((void *)&dest[0], ch, count);
 
     for (size_t i = 0; i < count; i++) {
-        assert(dest[i] == ch);
+	assert(dest[i] == ch);
     }
 }
 
@@ -35,7 +35,7 @@ static void test_fwk_str_memcpy(void)
     fwk_str_memcpy((void *)&dest[0], (const void *)&src[0], count);
 
     for (size_t i = 0; i < count; i++) {
-        assert(dest[i] == src[i]);
+	assert(dest[i] == src[i]);
     }
 }
 
@@ -48,7 +48,7 @@ static void test_fwk_str_strncpy(void)
     fwk_str_strncpy(&dest[0], &src[0], count);
 
     for (size_t i = 0; i < count; i++) {
-        assert(dest[i] == src[i]);
+	assert(dest[i] == src[i]);
     }
 }
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/CMakeLists.txt
@@ -98,35 +98,35 @@ foreach(source_dir IN LISTS SCP_MODULE_P
     unset(SCP_MODULE)
 
     include("${source_dir}/Module.cmake" OPTIONAL
-            RESULT_VARIABLE SCP_MODULE_LIST_FILE)
+	    RESULT_VARIABLE SCP_MODULE_LIST_FILE)
 
     if(NOT SCP_MODULE_LIST_FILE)
-        # cmake-format: off
+	# cmake-format: off
 
-        message(FATAL_ERROR
-            "No module list file found!\n"
+	message(FATAL_ERROR
+	    "No module list file found!\n"
 
-            "${source_dir}/Module.cmake\n"
+	    "${source_dir}/Module.cmake\n"
 
-            "This module path was provided as part of a firmware initial-cache "
-            "file ('Firmware.cmake'), but its 'Module.cmake' file could not be "
-            "located.")
+	    "This module path was provided as part of a firmware initial-cache "
+	    "file ('Firmware.cmake'), but its 'Module.cmake' file could not be "
+	    "located.")
 
-        # cmake-format: on
+	# cmake-format: on
     elseif(NOT DEFINED SCP_MODULE)
-        # cmake-format: off
+	# cmake-format: off
 
-        message(FATAL_ERROR
-            "No module name given for module!\n"
+	message(FATAL_ERROR
+	    "No module name given for module!\n"
 
-            "${source_dir}/Module.cmake\n"
+	    "${source_dir}/Module.cmake\n"
 
-            "This module metadata file has not yielded a module name and as "
-            "such cannot be identified. "
-            "please ensure that you have set `SCP_MODULE` in your "
-            "'Module.cmake' file.")
+	    "This module metadata file has not yielded a module name and as "
+	    "such cannot be identified. "
+	    "please ensure that you have set `SCP_MODULE` in your "
+	    "'Module.cmake' file.")
 
-        # cmake-format: on
+	# cmake-format: on
     endif()
 
     #
@@ -154,11 +154,11 @@ list(REMOVE_ITEM SCP_MISSING_MODULES ${S
 foreach(module IN LISTS SCP_MISSING_MODULES)
     # cmake-format: off
     message(WARNING
-        "Module requested, but not found: ${module}\n"
+	"Module requested, but not found: ${module}\n"
 
-        "The firmware requested this module, but we were unable to locate it. "
-        "If you are the firmware developer, please ensure that you have added "
-        "the module path to `SCP_MODULE_PATHS` in your 'Firmware.cmake' file.")
+	"The firmware requested this module, but we were unable to locate it. "
+	"If you are the firmware developer, please ensure that you have added "
+	"the module path to `SCP_MODULE_PATHS` in your 'Firmware.cmake' file.")
     # cmake-format: on
 endforeach()
 
@@ -182,25 +182,25 @@ macro(add_library target)
     set(is_module FALSE)
 
     if("SCP_MODULE" IN_LIST args)
-        if(SCP_MODULE_HEADER_ONLY)
-            #
-            # If this module is being loaded in as a header-only module, then we
-            # silently replace any `add_library` calls using the `SCP_MODULE`
-            # type with `INTERFACE`.
-            #
-
-            string(REPLACE "SCP_MODULE" "INTERFACE" args "${args}")
-        else()
-            string(REPLACE "SCP_MODULE" "" args "${args}")
-        endif()
+	if(SCP_MODULE_HEADER_ONLY)
+	    #
+	    # If this module is being loaded in as a header-only module, then we
+	    # silently replace any `add_library` calls using the `SCP_MODULE`
+	    # type with `INTERFACE`.
+	    #
+
+	    string(REPLACE "SCP_MODULE" "INTERFACE" args "${args}")
+	else()
+	    string(REPLACE "SCP_MODULE" "" args "${args}")
+	endif()
 
-        set(is_module TRUE)
+	set(is_module TRUE)
     endif()
 
     _add_library(${target} ${args})
 
     if(is_module)
-        set_target_properties("${target}" PROPERTIES _SCP_IS_MODULE TRUE)
+	set_target_properties("${target}" PROPERTIES _SCP_IS_MODULE TRUE)
     endif()
 endmacro()
 
@@ -208,39 +208,39 @@ endmacro()
 
 function(scp_generate_builtin_module_wrapper builtin)
     function(${builtin} target)
-        set(args "${ARGN}")
+	set(args "${ARGN}")
 
-        get_target_property(is_module "${target}" _SCP_IS_MODULE)
-        get_target_property(type "${target}" TYPE)
-        get_target_property(is_imported "${target}" IMPORTED)
-
-        set(is_interface FALSE)
-
-        if(type STREQUAL "INTERFACE_LIBRARY")
-            set(is_interface TRUE)
-        endif()
-
-        if(is_imported)
-            set(is_interface TRUE)
-        endif()
-
-        if(is_module AND is_interface)
-            #
-            # If this module is being pulled in as an interface library target,
-            # we need to strip any `PRIVATE` arguments and convert any `PUBLIC`
-            # ones to `INTERFACE`.
-            #
-
-            cmake_parse_arguments("scp_${CMAKE_CURRENT_FUNCTION}" "" ""
-                                  "PRIVATE" ${ARGN})
-
-            set(args "${scp_${CMAKE_CURRENT_FUNCTION}_UNPARSED_ARGUMENTS}")
-            string(REPLACE "PUBLIC" "INTERFACE" args "${args}")
-        endif()
-
-        if(args)
-            cmake_language(CALL _${CMAKE_CURRENT_FUNCTION} ${target} ${args})
-        endif()
+	get_target_property(is_module "${target}" _SCP_IS_MODULE)
+	get_target_property(type "${target}" TYPE)
+	get_target_property(is_imported "${target}" IMPORTED)
+
+	set(is_interface FALSE)
+
+	if(type STREQUAL "INTERFACE_LIBRARY")
+	    set(is_interface TRUE)
+	endif()
+
+	if(is_imported)
+	    set(is_interface TRUE)
+	endif()
+
+	if(is_module AND is_interface)
+	    #
+	    # If this module is being pulled in as an interface library target,
+	    # we need to strip any `PRIVATE` arguments and convert any `PUBLIC`
+	    # ones to `INTERFACE`.
+	    #
+
+	    cmake_parse_arguments("scp_${CMAKE_CURRENT_FUNCTION}" "" ""
+				  "PRIVATE" ${ARGN})
+
+	    set(args "${scp_${CMAKE_CURRENT_FUNCTION}_UNPARSED_ARGUMENTS}")
+	    string(REPLACE "PUBLIC" "INTERFACE" args "${args}")
+	endif()
+
+	if(args)
+	    cmake_language(CALL _${CMAKE_CURRENT_FUNCTION} ${target} ${args})
+	endif()
     endfunction()
 endfunction()
 
@@ -264,7 +264,7 @@ foreach(SCP_MODULE IN LISTS SCP_VALID_MO
     list(FIND SCP_VALID_MODULES "${SCP_MODULE}" SCP_MODULE_IDX)
     list(GET SCP_VALID_MODULE_TARGETS ${SCP_MODULE_IDX} SCP_MODULE_TARGET)
     list(GET SCP_VALID_MODULE_SOURCE_DIRS ${SCP_MODULE_IDX}
-         SCP_MODULE_SOURCE_DIR)
+	 SCP_MODULE_SOURCE_DIR)
 
     #
     # Pull the module in, creating the target. Modules that we do not intend on
@@ -273,16 +273,16 @@ foreach(SCP_MODULE IN LISTS SCP_VALID_MO
     #
 
     if(SCP_MODULE IN_LIST SCP_MODULES)
-        set(SCP_MODULE_HEADER_ONLY FALSE)
-        set(SCP_MODULE_PUBLIC "PUBLIC")
+	set(SCP_MODULE_HEADER_ONLY FALSE)
+	set(SCP_MODULE_PUBLIC "PUBLIC")
     else()
-        set(SCP_MODULE_HEADER_ONLY TRUE)
-        set(SCP_MODULE_PUBLIC "INTERFACE")
+	set(SCP_MODULE_HEADER_ONLY TRUE)
+	set(SCP_MODULE_PUBLIC "INTERFACE")
     endif()
 
     add_subdirectory(
-        "${SCP_MODULE_SOURCE_DIR}"
-        "${CMAKE_CURRENT_BINARY_DIR}/modules/${SCP_MODULE}" EXCLUDE_FROM_ALL)
+	"${SCP_MODULE_SOURCE_DIR}"
+	"${CMAKE_CURRENT_BINARY_DIR}/modules/${SCP_MODULE}" EXCLUDE_FROM_ALL)
 
     #
     # Link the firmware to the module, and the module to the framework.
@@ -296,21 +296,21 @@ foreach(SCP_MODULE IN LISTS SCP_VALID_MO
     #
 
     if(SCP_MODULE IN_LIST SCP_MODULES)
-        #
-        # Expose any firmware interface headers to the module.
-        #
-
-        target_include_directories(
-            ${SCP_MODULE_TARGET}
-            PRIVATE
-                $<TARGET_PROPERTY:${SCP_FIRMWARE_TARGET},INTERFACE_INCLUDE_DIRECTORIES>
-        )
-
-        #
-        # Make sure this module is linked.
-        #
+	#
+	# Expose any firmware interface headers to the module.
+	#
+
+	target_include_directories(
+	    ${SCP_MODULE_TARGET}
+	    PRIVATE
+		$<TARGET_PROPERTY:${SCP_FIRMWARE_TARGET},INTERFACE_INCLUDE_DIRECTORIES>
+	)
+
+	#
+	# Make sure this module is linked.
+	#
 
-        list(APPEND SCP_MODULE_TARGETS "${SCP_MODULE_TARGET}")
+	list(APPEND SCP_MODULE_TARGETS "${SCP_MODULE_TARGET}")
     endif()
 endforeach()
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/apcontext/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/apcontext/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/apcontext/CMakeLists.txt
@@ -8,9 +8,9 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_apcontext.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_apcontext.c")
 
 target_link_libraries(${SCP_MODULE_TARGET} PRIVATE module-clock)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/apcontext/src/mod_apcontext.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/apcontext/src/mod_apcontext.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/apcontext/src/mod_apcontext.c
@@ -29,10 +29,10 @@ static void apcontext_zero(void)
     config = fwk_module_get_data(fwk_module_id_apcontext);
 
     FWK_LOG_INFO(
-        MODULE_NAME " Zeroing AP context area [0x%" PRIxPTR " - 0x%" PRIxPTR
-                    "]",
-        config->base,
-        config->base + config->size);
+	MODULE_NAME " Zeroing AP context area [0x%" PRIxPTR " - 0x%" PRIxPTR
+		    "]",
+	config->base,
+	config->base + config->size);
 
     memset((void *)config->base, 0, config->size);
 }
@@ -48,13 +48,13 @@ static int apcontext_init(fwk_id_t modul
 
     /* This module does not support elements */
     if (element_count != 0)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     if (config->base == 0)
-        return FWK_E_DATA;
+	return FWK_E_DATA;
 
     if (config->size == 0)
-        return FWK_E_DATA;
+	return FWK_E_DATA;
 
     return FWK_SUCCESS;
 }
@@ -62,18 +62,18 @@ static int apcontext_init(fwk_id_t modul
 static int apcontext_start(fwk_id_t id)
 {
     const struct mod_apcontext_config *config =
-        fwk_module_get_data(fwk_module_id_apcontext);
+	fwk_module_get_data(fwk_module_id_apcontext);
 
     if (fwk_id_is_equal(config->clock_id, FWK_ID_NONE)) {
-        apcontext_zero();
-        return FWK_SUCCESS;
+	apcontext_zero();
+	return FWK_SUCCESS;
     }
 
     /* Register the module for clock state notifications */
     return fwk_notification_subscribe(
-        mod_clock_notification_id_state_changed,
-        config->clock_id,
-        id);
+	mod_clock_notification_id_state_changed,
+	config->clock_id,
+	id);
 }
 
 static int apcontext_process_notification(const struct fwk_event *event,
@@ -82,7 +82,7 @@ static int apcontext_process_notificatio
     struct clock_notification_params *params;
 
     fwk_assert(
-        fwk_id_is_equal(event->id, mod_clock_notification_id_state_changed));
+	fwk_id_is_equal(event->id, mod_clock_notification_id_state_changed));
     fwk_assert(fwk_id_is_type(event->target_id, FWK_ID_TYPE_MODULE));
 
     params = (struct clock_notification_params *)event->params;
@@ -92,11 +92,11 @@ static int apcontext_process_notificatio
      * only
      */
     if (params->new_state == MOD_CLOCK_STATE_RUNNING) {
-        apcontext_zero();
+	apcontext_zero();
 
-        /* Unsubscribe to the notification */
-        return fwk_notification_unsubscribe(event->id, event->source_id,
-                                            event->target_id);
+	/* Unsubscribe to the notification */
+	return fwk_notification_unsubscribe(event->id, event->source_id,
+					    event->target_id);
     }
 
     return FWK_SUCCESS;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/apremap/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/apremap/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/apremap/CMakeLists.txt
@@ -8,7 +8,7 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_apremap.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_apremap.c")
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/apremap/doc/module_apremap_design.md
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/apremap/doc/module_apremap_design.md
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/apremap/doc/module_apremap_design.md
@@ -22,63 +22,63 @@ For all memory accesses generated by MSC
 Port window, the memory remapping applied to that address is illustrated below.
 
 ```
-                                                   +------------------------------+
-                                                   |          Remap to            |
-                                                   | {ADDR_TRANS[47:20 bits],     +---------------------------->+
-                                                   | Incoming address[19:0 bits]} |                             |
-                                                   +--------------+---------------+                             |
-                                                                  ^                                             |
-                                                                  | Yes                                         |
-                                                                  |                                             |
-                                                       +----------+------------+                                |
-                                                       |  Address in range     |    No                          |
-                                                       |  {0x0_2B00_0000       +------------------------------->+
-                                                       |  to 0x0_2B0F_ FFFF} + |                                |
-                                                       |  4TB * CHIP_ID ?      |                                |
-                                                       +---------+-------------+                                |
-                                                                 ^                                              |
-                     MSCP Memory Space                           |                                              |
-                                                                 |                                              |
+						   +------------------------------+
+						   |          Remap to            |
+						   | {ADDR_TRANS[47:20 bits],     +---------------------------->+
+						   | Incoming address[19:0 bits]} |                             |
+						   +--------------+---------------+                             |
+								  ^                                             |
+								  | Yes                                         |
+								  |                                             |
+						       +----------+------------+                                |
+						       |  Address in range     |    No                          |
+						       |  {0x0_2B00_0000       +------------------------------->+
+						       |  to 0x0_2B0F_ FFFF} + |                                |
+						       |  4TB * CHIP_ID ?      |                                |
+						       +---------+-------------+                                |
+								 ^                                              |
+		     MSCP Memory Space                           |                                              |
+								 |                                              |
    0xFFFF_FFFF +--------------------------+                +-----+--------+                                     |
-               |                          |                |  Address     |   No                                |
-               |                          |                |  Translation +------------------------------------>+
-               |                          |                |  enabled?    |                                     |
-               |                          |                +-----+--------+                                     |
-               |                          |                      ^                                              |
-               |                          |                      |                                              |
-               |                          |                      |                                              |
-               |                          |        +-------------+-------------+ 0x3FFF_FFFF + 4TB * CHIP_ID    |
+	       |                          |                |  Address     |   No                                |
+	       |                          |                |  Translation +------------------------------------>+
+	       |                          |                |  enabled?    |                                     |
+	       |                          |                +-----+--------+                                     |
+	       |                          |                      ^                                              |
+	       |                          |                      |                                              |
+	       |                          |                      |                                              |
+	       |                          |        +-------------+-------------+ 0x3FFF_FFFF + 4TB * CHIP_ID    |
    0xE000_0000 +--------------------------+        |                           |                                |
-               |                          |        | remap to 4TB * CHIP_ID +  |                                |
-               |                          |        |       0GB to 1GB of       |                                |
-               |   System Access Port 1   +------->+   Application Processor   |                                |
-               |                          |        |       Memory Space        |                                |
-               |                          |        +---------------------------+ 0x0000_0000 + 4TB * CHIP_ID    |
+	       |                          |        | remap to 4TB * CHIP_ID +  |                                |
+	       |                          |        |       0GB to 1GB of       |                                |
+	       |   System Access Port 1   +------->+   Application Processor   |                                |
+	       |                          |        |       Memory Space        |                                |
+	       |                          |        +---------------------------+ 0x0000_0000 + 4TB * CHIP_ID    |
    0xA000_0000 +--------------------------+                                                                     |
-               |                          |         +-----+--------+        +---------------------------+ 0x01_7FFF_FFFF + 4TB * CHIP_ID
-               |                          |         |  CMN Addr    |  Yes   |                           |       |
-               |   System Access Port 0   +-------->+  Translation +------->+ remap to 4TB * CHIP_ID +  |       |
-               |                          |         |  enabled?    |        |       CMN Configuration   |       |
-               |                          |         +-----+--------+        |       region in AP        |       |
+	       |                          |         +-----+--------+        +---------------------------+ 0x01_7FFF_FFFF + 4TB * CHIP_ID
+	       |                          |         |  CMN Addr    |  Yes   |                           |       |
+	       |   System Access Port 0   +-------->+  Translation +------->+ remap to 4TB * CHIP_ID +  |       |
+	       |                          |         |  enabled?    |        |       CMN Configuration   |       |
+	       |                          |         +-----+--------+        |       region in AP        |       |
    0x6000_0000 +--------------------------+               |                 |       Memory Space        |       |
-               |                          |               |  No             +-------------+-------------+ 0x01_4000_0000 + 4TB * CHIP_ID
-               |                          |               |                               |                     |
-               |                          |               v                               |                     |
-               |                          |         +-----+---------------------+ 0x7FFF_FFFF + 4TB * CHIP_ID   |
-               |                          |         |                           |         |                     |
-               |                          |         + remap to 4TB * CHIP_ID +  |         |                     |
-               |                          |         |       1GB to 2GB of       |         |                     |
-               |                          |         |   Application Processor   |         |                     |
-               |                          |         |       Memory Space        |         |                     |
-               |                          |         +-------------+-------------+ 0x4000_0000 + 4TB * CHIP_ID   |
-               |                          |                       |                       |                     |
-               |                          |                       |                       |                     |
+	       |                          |               |  No             +-------------+-------------+ 0x01_4000_0000 + 4TB * CHIP_ID
+	       |                          |               |                               |                     |
+	       |                          |               v                               |                     |
+	       |                          |         +-----+---------------------+ 0x7FFF_FFFF + 4TB * CHIP_ID   |
+	       |                          |         |                           |         |                     |
+	       |                          |         + remap to 4TB * CHIP_ID +  |         |                     |
+	       |                          |         |       1GB to 2GB of       |         |                     |
+	       |                          |         |   Application Processor   |         |                     |
+	       |                          |         |       Memory Space        |         |                     |
+	       |                          |         +-------------+-------------+ 0x4000_0000 + 4TB * CHIP_ID   |
+	       |                          |                       |                       |                     |
+	       |                          |                       |                       |                     |
    0x0000_0000 +--------------------------+                       |                       v                     |
-                                                                  +-----------------------+---------------------+
-                                                                  |
-                                                                  |
-                                                                  v
-                                                            To Interconnect
+								  +-----------------------+---------------------+
+								  |
+								  |
+								  v
+							    To Interconnect
 ```
 
     - MSCP to access the lower 2GB of the application processor memory space
@@ -86,10 +86,10 @@ Port window, the memory remapping applie
       memory map of System Access Port window memory to the application
       processor memory map is shown below.
 
-          System Access Port             Application Processor Memory Map
-          ------------------             --------------------------------
-          0x6000_0000 - 0x9FFF_FFFF  ->  0x4000_0000 - 0x7FFF_FFFF
-          0xA000_0000 - 0xFFFF_FFFF  ->  0x0000_0000 - 0x3FFF_FFFF
+	  System Access Port             Application Processor Memory Map
+	  ------------------             --------------------------------
+	  0x6000_0000 - 0x9FFF_FFFF  ->  0x4000_0000 - 0x7FFF_FFFF
+	  0xA000_0000 - 0xFFFF_FFFF  ->  0x0000_0000 - 0x3FFF_FFFF
 
     - MSCP to access 2GB and above address space of the local chip's application
       processor memory space or remote chip's application processor memory space.
@@ -127,31 +127,31 @@ Setting this bit will translate MSCP's `
 ```c
 
 access_ap_memory_1mb_window(uint64t addr):
-        disable_interrupts();
-        DSB()
-        if(cmn_addr_trans_bit_set):
-            disable_cmn_addr_trans();
-        enable_addr_trans( with addr_47_20 )
-        read(0x0_CB00_0000 + addr_19_0)
-        //
-        // or access_ap_memory(0x0_2B00_0000 + addr_19_0) but avoiding recursion
-        // under critical section
-        //
-        DSB()
-        if(cmn_addr_trans_bit_set):
-            enable_cmn_addr_trans();
-        enable_interrupts()
+	disable_interrupts();
+	DSB()
+	if(cmn_addr_trans_bit_set):
+	    disable_cmn_addr_trans();
+	enable_addr_trans( with addr_47_20 )
+	read(0x0_CB00_0000 + addr_19_0)
+	//
+	// or access_ap_memory(0x0_2B00_0000 + addr_19_0) but avoiding recursion
+	// under critical section
+	//
+	DSB()
+	if(cmn_addr_trans_bit_set):
+	    enable_cmn_addr_trans();
+	enable_interrupts()
 
 
 access_ap_memory(uint64t addr):
     if (addr >= 0x0_0000_0000 <= 0x0_3FFF_FFFF):           // first 1GB block
-        read(addr + 0x0_A000_0000);
+	read(addr + 0x0_A000_0000);
     else if (addr >= 0x0_4000_0000 <= 0x0_7FFF_FFFF):      // second 1GB block
-        if(cmn_addr_trans_bit_set):
-            disable_cmn_addr_trans();
-        read(addr + 0x0_2000_0000)
-        if(cmn_addr_trans_bit_set):
-            enable_cmn_addr_trans();
+	if(cmn_addr_trans_bit_set):
+	    disable_cmn_addr_trans();
+	read(addr + 0x0_2000_0000)
+	if(cmn_addr_trans_bit_set):
+	    enable_cmn_addr_trans();
     elseif (addr > 0x0_7FFF_FFFF):
-        access_ap_memory_1mb_window(addr)
+	access_ap_memory_1mb_window(addr)
 ```
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/apremap/src/mod_apremap.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/apremap/src/mod_apremap.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/apremap/src/mod_apremap.c
@@ -72,7 +72,7 @@ static inline void enable_addr_trans(uin
 
     __DSB();
     ctx.apremap_reg->ADDR_TRANS =
-        (ap_mem_1mb_window_addr << 1) | APREMAP_ADDR_TRANS_EN;
+	(ap_mem_1mb_window_addr << 1) | APREMAP_ADDR_TRANS_EN;
     __DSB();
 }
 
@@ -121,37 +121,37 @@ static inline void read_ap_memory_1mb_wi
     bool cmn_addr_trans_active = false;
 
     if (ctx.cmn_addr_trans_enabled) {
-        cmn_addr_trans_active = true;
-        disable_cmn_addr_trans();
+	cmn_addr_trans_active = true;
+	disable_cmn_addr_trans();
     }
 
     enable_addr_trans(addr);
 
     switch (data_type) {
     case TYPE_UINT8:
-        *(uint8_t *)value = *(volatile uint8_t *)(APREMAP_1MB_ADDR(addr));
-        break;
+	*(uint8_t *)value = *(volatile uint8_t *)(APREMAP_1MB_ADDR(addr));
+	break;
 
     case TYPE_UINT16:
-        *(uint16_t *)value = *(volatile uint16_t *)(APREMAP_1MB_ADDR(addr));
-        break;
+	*(uint16_t *)value = *(volatile uint16_t *)(APREMAP_1MB_ADDR(addr));
+	break;
 
     case TYPE_UINT32:
-        *(uint32_t *)value = *(volatile uint32_t *)(APREMAP_1MB_ADDR(addr));
-        break;
+	*(uint32_t *)value = *(volatile uint32_t *)(APREMAP_1MB_ADDR(addr));
+	break;
 
     case TYPE_UINT64:
-        *(uint64_t *)value = *(volatile uint64_t *)(APREMAP_1MB_ADDR(addr));
-        break;
+	*(uint64_t *)value = *(volatile uint64_t *)(APREMAP_1MB_ADDR(addr));
+	break;
 
     default:
-        break;
+	break;
     }
 
     disable_addr_trans();
 
     if (cmn_addr_trans_active)
-        enable_cmn_addr_trans();
+	enable_cmn_addr_trans();
 }
 
 /*
@@ -166,73 +166,73 @@ static void mmio_ap_mem_read(
     bool cmn_addr_trans_active = false;
 
     if (is_addr_first_1gb_block(addr)) {
-        switch (data_type) {
-        case TYPE_UINT8:
-            *(uint8_t *)value =
-                *(volatile uint8_t *)(ADDR_OFFSET_SYSTEM_ACCESS_PORT_1(addr));
-            break;
-
-        case TYPE_UINT16:
-            *(uint16_t *)value =
-                *(volatile uint16_t *)(ADDR_OFFSET_SYSTEM_ACCESS_PORT_1(addr));
-            break;
-
-        case TYPE_UINT32:
-            *(uint32_t *)value =
-                *(volatile uint32_t *)(ADDR_OFFSET_SYSTEM_ACCESS_PORT_1(addr));
-            break;
-
-        case TYPE_UINT64:
-            *(uint64_t *)value =
-                *(volatile uint64_t *)(ADDR_OFFSET_SYSTEM_ACCESS_PORT_1(addr));
-            break;
-
-        default:
-            break;
-        }
+	switch (data_type) {
+	case TYPE_UINT8:
+	    *(uint8_t *)value =
+		*(volatile uint8_t *)(ADDR_OFFSET_SYSTEM_ACCESS_PORT_1(addr));
+	    break;
+
+	case TYPE_UINT16:
+	    *(uint16_t *)value =
+		*(volatile uint16_t *)(ADDR_OFFSET_SYSTEM_ACCESS_PORT_1(addr));
+	    break;
+
+	case TYPE_UINT32:
+	    *(uint32_t *)value =
+		*(volatile uint32_t *)(ADDR_OFFSET_SYSTEM_ACCESS_PORT_1(addr));
+	    break;
+
+	case TYPE_UINT64:
+	    *(uint64_t *)value =
+		*(volatile uint64_t *)(ADDR_OFFSET_SYSTEM_ACCESS_PORT_1(addr));
+	    break;
+
+	default:
+	    break;
+	}
 
-        return;
+	return;
     } else if (is_addr_second_1gb_block(addr)) {
-        if (ctx.cmn_addr_trans_enabled) {
-            cmn_addr_trans_active = true;
-            disable_cmn_addr_trans();
-        }
-
-        switch (data_type) {
-        case TYPE_UINT8:
-            *(uint8_t *)value =
-                *(volatile uint8_t *)(ADDR_OFFSET_SYSTEM_ACCESS_PORT_0(addr));
-            break;
-
-        case TYPE_UINT16:
-            *(uint16_t *)value =
-                *(volatile uint16_t *)(ADDR_OFFSET_SYSTEM_ACCESS_PORT_0(addr));
-            break;
-
-        case TYPE_UINT32:
-            *(uint32_t *)value =
-                *(volatile uint32_t *)(ADDR_OFFSET_SYSTEM_ACCESS_PORT_0(addr));
-            break;
-
-        case TYPE_UINT64:
-            *(uint64_t *)value =
-                *(volatile uint64_t *)(ADDR_OFFSET_SYSTEM_ACCESS_PORT_0(addr));
-            break;
-
-        default:
-            break;
-        }
+	if (ctx.cmn_addr_trans_enabled) {
+	    cmn_addr_trans_active = true;
+	    disable_cmn_addr_trans();
+	}
+
+	switch (data_type) {
+	case TYPE_UINT8:
+	    *(uint8_t *)value =
+		*(volatile uint8_t *)(ADDR_OFFSET_SYSTEM_ACCESS_PORT_0(addr));
+	    break;
+
+	case TYPE_UINT16:
+	    *(uint16_t *)value =
+		*(volatile uint16_t *)(ADDR_OFFSET_SYSTEM_ACCESS_PORT_0(addr));
+	    break;
+
+	case TYPE_UINT32:
+	    *(uint32_t *)value =
+		*(volatile uint32_t *)(ADDR_OFFSET_SYSTEM_ACCESS_PORT_0(addr));
+	    break;
+
+	case TYPE_UINT64:
+	    *(uint64_t *)value =
+		*(volatile uint64_t *)(ADDR_OFFSET_SYSTEM_ACCESS_PORT_0(addr));
+	    break;
+
+	default:
+	    break;
+	}
 
-        if (cmn_addr_trans_active)
-            enable_cmn_addr_trans();
+	if (cmn_addr_trans_active)
+	    enable_cmn_addr_trans();
 
-        return;
+	return;
     } else if (is_addr_above_2gb(addr)) {
-        read_ap_memory_1mb_window(addr, value, data_type);
+	read_ap_memory_1mb_window(addr, value, data_type);
 
-        return;
+	return;
     } else
-        fwk_unexpected();
+	fwk_unexpected();
 }
 
 /*
@@ -246,37 +246,37 @@ static inline void write_ap_memory_1mb_w
     bool cmn_addr_trans_active = false;
 
     if (ctx.cmn_addr_trans_enabled) {
-        cmn_addr_trans_active = true;
-        disable_cmn_addr_trans();
+	cmn_addr_trans_active = true;
+	disable_cmn_addr_trans();
     }
 
     enable_addr_trans(addr);
 
     switch (data_type) {
     case TYPE_UINT8:
-        *(volatile uint8_t *)(APREMAP_1MB_ADDR(addr)) = *(uint8_t *)value;
-        break;
+	*(volatile uint8_t *)(APREMAP_1MB_ADDR(addr)) = *(uint8_t *)value;
+	break;
 
     case TYPE_UINT16:
-        *(volatile uint16_t *)(APREMAP_1MB_ADDR(addr)) = *(uint16_t *)value;
-        break;
+	*(volatile uint16_t *)(APREMAP_1MB_ADDR(addr)) = *(uint16_t *)value;
+	break;
 
     case TYPE_UINT32:
-        *(volatile uint32_t *)(APREMAP_1MB_ADDR(addr)) = *(uint32_t *)value;
-        break;
+	*(volatile uint32_t *)(APREMAP_1MB_ADDR(addr)) = *(uint32_t *)value;
+	break;
 
     case TYPE_UINT64:
-        *(volatile uint64_t *)(APREMAP_1MB_ADDR(addr)) = *(uint64_t *)value;
-        break;
+	*(volatile uint64_t *)(APREMAP_1MB_ADDR(addr)) = *(uint64_t *)value;
+	break;
 
     default:
-        break;
+	break;
     }
 
     disable_addr_trans();
 
     if (cmn_addr_trans_active)
-        enable_cmn_addr_trans();
+	enable_cmn_addr_trans();
 }
 
 /*
@@ -291,73 +291,73 @@ static void mmio_ap_mem_write(
     bool cmn_addr_trans_active = false;
 
     if (is_addr_first_1gb_block(addr)) {
-        switch (data_type) {
-        case TYPE_UINT8:
-            *(volatile uint8_t *)(ADDR_OFFSET_SYSTEM_ACCESS_PORT_1(addr)) =
-                *(uint8_t *)value;
-            break;
-
-        case TYPE_UINT16:
-            *(volatile uint16_t *)(ADDR_OFFSET_SYSTEM_ACCESS_PORT_1(addr)) =
-                *(uint16_t *)value;
-            break;
-
-        case TYPE_UINT32:
-            *(volatile uint32_t *)(ADDR_OFFSET_SYSTEM_ACCESS_PORT_1(addr)) =
-                *(uint32_t *)value;
-            break;
-
-        case TYPE_UINT64:
-            *(volatile uint64_t *)(ADDR_OFFSET_SYSTEM_ACCESS_PORT_1(addr)) =
-                *(uint64_t *)value;
-            break;
-
-        default:
-            break;
-        }
+	switch (data_type) {
+	case TYPE_UINT8:
+	    *(volatile uint8_t *)(ADDR_OFFSET_SYSTEM_ACCESS_PORT_1(addr)) =
+		*(uint8_t *)value;
+	    break;
+
+	case TYPE_UINT16:
+	    *(volatile uint16_t *)(ADDR_OFFSET_SYSTEM_ACCESS_PORT_1(addr)) =
+		*(uint16_t *)value;
+	    break;
+
+	case TYPE_UINT32:
+	    *(volatile uint32_t *)(ADDR_OFFSET_SYSTEM_ACCESS_PORT_1(addr)) =
+		*(uint32_t *)value;
+	    break;
+
+	case TYPE_UINT64:
+	    *(volatile uint64_t *)(ADDR_OFFSET_SYSTEM_ACCESS_PORT_1(addr)) =
+		*(uint64_t *)value;
+	    break;
+
+	default:
+	    break;
+	}
 
-        return;
+	return;
     } else if (is_addr_second_1gb_block(addr)) {
-        if (ctx.cmn_addr_trans_enabled) {
-            cmn_addr_trans_active = true;
-            disable_cmn_addr_trans();
-        }
-
-        switch (data_type) {
-        case TYPE_UINT8:
-            *(volatile uint8_t *)(ADDR_OFFSET_SYSTEM_ACCESS_PORT_0(addr)) =
-                *(uint8_t *)value;
-            break;
-
-        case TYPE_UINT16:
-            *(volatile uint16_t *)(ADDR_OFFSET_SYSTEM_ACCESS_PORT_0(addr)) =
-                *(uint16_t *)value;
-            break;
-
-        case TYPE_UINT32:
-            *(volatile uint32_t *)(ADDR_OFFSET_SYSTEM_ACCESS_PORT_0(addr)) =
-                *(uint32_t *)value;
-            break;
-
-        case TYPE_UINT64:
-            *(volatile uint64_t *)(ADDR_OFFSET_SYSTEM_ACCESS_PORT_0(addr)) =
-                *(uint64_t *)value;
-            break;
-
-        default:
-            break;
-        }
+	if (ctx.cmn_addr_trans_enabled) {
+	    cmn_addr_trans_active = true;
+	    disable_cmn_addr_trans();
+	}
+
+	switch (data_type) {
+	case TYPE_UINT8:
+	    *(volatile uint8_t *)(ADDR_OFFSET_SYSTEM_ACCESS_PORT_0(addr)) =
+		*(uint8_t *)value;
+	    break;
+
+	case TYPE_UINT16:
+	    *(volatile uint16_t *)(ADDR_OFFSET_SYSTEM_ACCESS_PORT_0(addr)) =
+		*(uint16_t *)value;
+	    break;
+
+	case TYPE_UINT32:
+	    *(volatile uint32_t *)(ADDR_OFFSET_SYSTEM_ACCESS_PORT_0(addr)) =
+		*(uint32_t *)value;
+	    break;
+
+	case TYPE_UINT64:
+	    *(volatile uint64_t *)(ADDR_OFFSET_SYSTEM_ACCESS_PORT_0(addr)) =
+		*(uint64_t *)value;
+	    break;
+
+	default:
+	    break;
+	}
 
-        if (cmn_addr_trans_active)
-            enable_cmn_addr_trans();
+	if (cmn_addr_trans_active)
+	    enable_cmn_addr_trans();
 
-        return;
+	return;
     } else if (is_addr_above_2gb(addr)) {
-        write_ap_memory_1mb_window(addr, value, data_type);
+	write_ap_memory_1mb_window(addr, value, data_type);
 
-        return;
+	return;
     } else
-        fwk_unexpected();
+	fwk_unexpected();
 }
 
 /*
@@ -458,15 +458,15 @@ static int apremap_process_bind_request(
 {
     switch (fwk_id_get_api_idx(api_id)) {
     case MOD_APREMAP_API_IDX_AP_MEM_RW:
-        *api = &apremap_mem_api;
-        break;
+	*api = &apremap_mem_api;
+	break;
 
     case MOD_APREMAP_API_IDX_CMN_ATRANS:
-        *api = &cmn_atrans_api;
-        break;
+	*api = &cmn_atrans_api;
+	break;
 
     default:
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
     return FWK_SUCCESS;
 }
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/armv7m_mpu/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/armv7m_mpu/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/armv7m_mpu/CMakeLists.txt
@@ -8,7 +8,7 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_armv7m_mpu.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_armv7m_mpu.c")
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/armv8m_mpu/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/armv8m_mpu/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/armv8m_mpu/CMakeLists.txt
@@ -8,7 +8,7 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_armv8m_mpu.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_armv8m_mpu.c")
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/armv8m_mpu/src/mod_armv8m_mpu.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/armv8m_mpu/src/mod_armv8m_mpu.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/armv8m_mpu/src/mod_armv8m_mpu.c
@@ -29,19 +29,19 @@ static int armv8m_mpu_init(
     config = data;
 
     if (config->attributes_count <= (uint8_t)MPU_MAX_ATTR_COUNT) {
-        ARM_MPU_Disable();
+	ARM_MPU_Disable();
 
-        for (attr_index = 0U; attr_index < config->attributes_count;
-             attr_index++) {
-            ARM_MPU_SetMemAttr(attr_index, config->attributes[attr_index]);
-        }
-        ARM_MPU_Load(
-            config->first_region_number, config->regions, config->region_count);
-        ARM_MPU_Enable(MPU_CTRL_HFNMIENA_Msk);
+	for (attr_index = 0U; attr_index < config->attributes_count;
+	     attr_index++) {
+	    ARM_MPU_SetMemAttr(attr_index, config->attributes[attr_index]);
+	}
+	ARM_MPU_Load(
+	    config->first_region_number, config->regions, config->region_count);
+	ARM_MPU_Enable(MPU_CTRL_HFNMIENA_Msk);
 
-        status = FWK_SUCCESS;
+	status = FWK_SUCCESS;
     } else {
-        status = FWK_E_RANGE;
+	status = FWK_E_RANGE;
     }
 
     return status;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/bootloader/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/bootloader/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/bootloader/CMakeLists.txt
@@ -8,11 +8,11 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(
     ${SCP_MODULE_TARGET}
     PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_bootloader.c"
-            "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_bootloader_boot.S")
+	    "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_bootloader_boot.S")
 
 target_link_libraries(${SCP_MODULE_TARGET} PRIVATE module-sds)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/bootloader/src/mod_bootloader.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/bootloader/src/mod_bootloader.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/bootloader/src/mod_bootloader.c
@@ -54,10 +54,10 @@ static struct bootloader_ctx mod_bootloa
 static int load_image(void)
 {
     extern noreturn void mod_bootloader_boot(
-        uint8_t * destination,
-        const uint8_t *source,
-        size_t size,
-        volatile uint32_t *vtor);
+	uint8_t * destination,
+	const uint8_t *source,
+	size_t size,
+	volatile uint32_t *vtor);
 
 #ifdef BUILD_HAS_MOD_SDS
     int status;
@@ -73,16 +73,16 @@ static int load_image(void)
     bool sds = false;
 
     if (mod_bootloader_ctx.module_config == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
     if (mod_bootloader_ctx.module_config->source_base == 0) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
     if (mod_bootloader_ctx.module_config->destination_base == 0) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
     if (mod_bootloader_ctx.module_config->source_size == 0) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
 #ifdef BUILD_HAS_MOD_SDS
@@ -91,99 +91,99 @@ static int load_image(void)
 
     if (sds) {
 #ifdef BUILD_HAS_MOD_SDS
-        /*
-         * Wait until Trusted Firmware writes the image metadata and sets the
-         * data valid flag.
-         */
-        while (true) {
-            status = mod_bootloader_ctx.sds_api->struct_read(
-                mod_bootloader_ctx.module_config->sds_struct_id,
-                BOOTLOADER_STRUCT_VALID_POS,
-                &image_flags,
-                sizeof(image_flags));
-
-            if (status != FWK_SUCCESS) {
-                return status;
-            }
-            if ((image_flags & (uint32_t)IMAGE_FLAGS_VALID_MASK) !=
-                (uint32_t)0) {
-                break;
-            }
-        }
-        /*
-         * Clear the image flag, so that at reboot if the RAM contents are
-         * retained, then it would need to be set again by AP.
-         */
-        status = mod_bootloader_ctx.sds_api->struct_write(
-            mod_bootloader_ctx.module_config->sds_struct_id,
-            BOOTLOADER_STRUCT_VALID_POS,
-            &zero_flag,
-            sizeof(zero_flag));
-
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
-        /*
-         * The image metadata from Trusted Firmware can now be read and
-         * validated.
-         */
-        status = mod_bootloader_ctx.sds_api->struct_read(
-            mod_bootloader_ctx.module_config->sds_struct_id,
-            BOOTLOADER_STRUCT_IMAGE_OFFSET_POS,
-            &image_offset,
-            sizeof(image_offset));
-
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
-        status = mod_bootloader_ctx.sds_api->struct_read(
-            mod_bootloader_ctx.module_config->sds_struct_id,
-            BOOTLOADER_STRUCT_IMAGE_SIZE_POS,
-            &image_size,
-            sizeof(image_size));
-
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
-
-        if (image_size == 0) {
-            return FWK_E_SIZE;
-        }
-        if ((image_offset % 4) != 0) {
-            return FWK_E_ALIGN;
-        }
-        if (image_offset > mod_bootloader_ctx.module_config->source_size) {
-            return FWK_E_SIZE;
-        }
-
-        /* Read the image header now that its base address is known */
-        image_base =
-            (const uint8_t *)mod_bootloader_ctx.module_config->source_base +
-            image_offset;
+	/*
+	 * Wait until Trusted Firmware writes the image metadata and sets the
+	 * data valid flag.
+	 */
+	while (true) {
+	    status = mod_bootloader_ctx.sds_api->struct_read(
+		mod_bootloader_ctx.module_config->sds_struct_id,
+		BOOTLOADER_STRUCT_VALID_POS,
+		&image_flags,
+		sizeof(image_flags));
+
+	    if (status != FWK_SUCCESS) {
+		return status;
+	    }
+	    if ((image_flags & (uint32_t)IMAGE_FLAGS_VALID_MASK) !=
+		(uint32_t)0) {
+		break;
+	    }
+	}
+	/*
+	 * Clear the image flag, so that at reboot if the RAM contents are
+	 * retained, then it would need to be set again by AP.
+	 */
+	status = mod_bootloader_ctx.sds_api->struct_write(
+	    mod_bootloader_ctx.module_config->sds_struct_id,
+	    BOOTLOADER_STRUCT_VALID_POS,
+	    &zero_flag,
+	    sizeof(zero_flag));
+
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
+	/*
+	 * The image metadata from Trusted Firmware can now be read and
+	 * validated.
+	 */
+	status = mod_bootloader_ctx.sds_api->struct_read(
+	    mod_bootloader_ctx.module_config->sds_struct_id,
+	    BOOTLOADER_STRUCT_IMAGE_OFFSET_POS,
+	    &image_offset,
+	    sizeof(image_offset));
+
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
+	status = mod_bootloader_ctx.sds_api->struct_read(
+	    mod_bootloader_ctx.module_config->sds_struct_id,
+	    BOOTLOADER_STRUCT_IMAGE_SIZE_POS,
+	    &image_size,
+	    sizeof(image_size));
+
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
+
+	if (image_size == 0) {
+	    return FWK_E_SIZE;
+	}
+	if ((image_offset % 4) != 0) {
+	    return FWK_E_ALIGN;
+	}
+	if (image_offset > mod_bootloader_ctx.module_config->source_size) {
+	    return FWK_E_SIZE;
+	}
+
+	/* Read the image header now that its base address is known */
+	image_base =
+	    (const uint8_t *)mod_bootloader_ctx.module_config->source_base +
+	    image_offset;
 #endif
     } else {
-        image_base =
-            (const uint8_t *)mod_bootloader_ctx.module_config->source_base;
-        image_size = mod_bootloader_ctx.module_config->source_size;
+	image_base =
+	    (const uint8_t *)mod_bootloader_ctx.module_config->source_base;
+	image_size = mod_bootloader_ctx.module_config->source_size;
     }
 
     if (mod_bootloader_ctx.module_config->destination_size > 0) {
-        if (image_size > mod_bootloader_ctx.module_config->destination_size) {
-            return FWK_E_SIZE;
-        }
+	if (image_size > mod_bootloader_ctx.module_config->destination_size) {
+	    return FWK_E_SIZE;
+	}
     }
 
     (void)
-        fwk_interrupt_global_disable(); /* We are relocating the vector table */
+	fwk_interrupt_global_disable(); /* We are relocating the vector table */
 
     FWK_LOG_INFO("[BOOTLOADER] Booting RAM firmware...");
     FWK_LOG_FLUSH();
 
     mod_bootloader_boot(
-        (uint8_t *)mod_bootloader_ctx.module_config->destination_base,
-        image_base,
-        image_size,
-        &SCB->VTOR);
+	(uint8_t *)mod_bootloader_ctx.module_config->destination_base,
+	image_base,
+	image_size,
+	&SCB->VTOR);
 }
 
 static const struct mod_bootloader_api bootloader_api = {
@@ -210,18 +210,18 @@ static int bootloader_bind(fwk_id_t id,
 
     /* Only the first round of binding is used (round number is zero-indexed) */
     if (call_number == 1) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     if (fwk_module_is_valid_element_id(id)) {
-        /* No element-level binding required */
-        return FWK_SUCCESS;
+	/* No element-level binding required */
+	return FWK_SUCCESS;
     }
 
     status = fwk_module_bind(
-        FWK_ID_MODULE(FWK_MODULE_IDX_SDS),
-        FWK_ID_API(FWK_MODULE_IDX_SDS, 0),
-        &mod_bootloader_ctx.sds_api);
+	FWK_ID_MODULE(FWK_MODULE_IDX_SDS),
+	FWK_ID_API(FWK_MODULE_IDX_SDS, 0),
+	&mod_bootloader_ctx.sds_api);
 
     return status;
 }
@@ -231,11 +231,11 @@ static int bootloader_process_bind_reque
     fwk_id_t api_id, const void **api)
 {
     if (api == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     if (!fwk_module_is_valid_module_id(id)) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     *api = &bootloader_api;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cdns_i2c/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/cdns_i2c/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cdns_i2c/CMakeLists.txt
@@ -7,7 +7,7 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_cdns_i2c.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_cdns_i2c.c")
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cdns_i2c/include/mod_cdns_i2c.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/cdns_i2c/include/mod_cdns_i2c.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cdns_i2c/include/mod_cdns_i2c.h
@@ -166,10 +166,10 @@ struct mod_cdns_i2c_controller_api_polle
      * \return One of the other specific error codes described by the framework.
      */
     int (*read)(
-        fwk_id_t device_id,
-        uint16_t address,
-        char *data,
-        uint16_t length);
+	fwk_id_t device_id,
+	uint16_t address,
+	char *data,
+	uint16_t length);
 
     /*!
      * \brief I2C write function.
@@ -185,11 +185,11 @@ struct mod_cdns_i2c_controller_api_polle
      * \return One of the other specific error codes described by the framework.
      */
     int (*write)(
-        fwk_id_t device_id,
-        uint16_t address,
-        const char *data,
-        uint16_t length,
-        bool stop);
+	fwk_id_t device_id,
+	uint16_t address,
+	const char *data,
+	uint16_t length,
+	bool stop);
 };
 
 /*!
@@ -238,24 +238,24 @@ enum mod_cdns_i2c_notifications {
  */
 static const fwk_id_t mod_cdns_i2c_notification_id_target_rx =
     FWK_ID_NOTIFICATION_INIT(
-        FWK_MODULE_IDX_CDNS_I2C,
-        MOD_CDNS_I2C_NOTIFICATION_IDX_RX);
+	FWK_MODULE_IDX_CDNS_I2C,
+	MOD_CDNS_I2C_NOTIFICATION_IDX_RX);
 
 /*!
  * \brief Identifier for I2C target transmit callback notification.
  */
 static const fwk_id_t mod_cdns_i2c_notification_id_target_tx =
     FWK_ID_NOTIFICATION_INIT(
-        FWK_MODULE_IDX_CDNS_I2C,
-        MOD_CDNS_I2C_NOTIFICATION_IDX_TX);
+	FWK_MODULE_IDX_CDNS_I2C,
+	MOD_CDNS_I2C_NOTIFICATION_IDX_TX);
 
 /*!
  * \brief Identifier for I2C target error callback notification.
  */
 static const fwk_id_t mod_cdns_i2c_notification_id_target_error =
     FWK_ID_NOTIFICATION_INIT(
-        FWK_MODULE_IDX_CDNS_I2C,
-        MOD_CDNS_I2C_NOTIFICATION_IDX_ERROR);
+	FWK_MODULE_IDX_CDNS_I2C,
+	MOD_CDNS_I2C_NOTIFICATION_IDX_ERROR);
 
 /*!
  * \}
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cdns_i2c/src/mod_cdns_i2c.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/cdns_i2c/src/mod_cdns_i2c.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cdns_i2c/src/mod_cdns_i2c.c
@@ -67,8 +67,8 @@ struct cdns_i2c_dev_ctx {
 
     /* I2C callback pointer */
     void (*i2c_callback)(
-        struct cdns_i2c_dev_ctx *ctx,
-        enum mod_cdns_i2c_notifications cb_type);
+	struct cdns_i2c_dev_ctx *ctx,
+	enum mod_cdns_i2c_notifications cb_type);
 };
 
 /* Module context */
@@ -90,26 +90,26 @@ static void i2c_callback_fn(
     fwk_assert(ctx != NULL);
 
     if (!fwk_id_is_equal(ctx->config->callback_mod_id, FWK_ID_NONE)) {
-        memset((void *)&transfer_event, 0, sizeof(struct fwk_event));
-        switch (cb_type) {
-        case MOD_CDNS_I2C_NOTIFICATION_IDX_TX:
-            transfer_event.id = mod_cdns_i2c_notification_id_target_tx;
-            break;
-        case MOD_CDNS_I2C_NOTIFICATION_IDX_RX:
-            transfer_event.id = mod_cdns_i2c_notification_id_target_rx;
-            break;
-        case MOD_CDNS_I2C_NOTIFICATION_IDX_ERROR:
-            transfer_event.id = mod_cdns_i2c_notification_id_target_error;
-            break;
-        default:
-            fwk_unexpected();
-            return;
-        }
-        transfer_event.response_requested = false;
-        transfer_event.source_id =
-            FWK_ID_ELEMENT(FWK_MODULE_IDX_CDNS_I2C, ctx->i2c_id);
-        transfer_event.target_id = ctx->config->callback_mod_id;
-        fwk_notification_notify(&transfer_event, &i);
+	memset((void *)&transfer_event, 0, sizeof(struct fwk_event));
+	switch (cb_type) {
+	case MOD_CDNS_I2C_NOTIFICATION_IDX_TX:
+	    transfer_event.id = mod_cdns_i2c_notification_id_target_tx;
+	    break;
+	case MOD_CDNS_I2C_NOTIFICATION_IDX_RX:
+	    transfer_event.id = mod_cdns_i2c_notification_id_target_rx;
+	    break;
+	case MOD_CDNS_I2C_NOTIFICATION_IDX_ERROR:
+	    transfer_event.id = mod_cdns_i2c_notification_id_target_error;
+	    break;
+	default:
+	    fwk_unexpected();
+	    return;
+	}
+	transfer_event.response_requested = false;
+	transfer_event.source_id =
+	    FWK_ID_ELEMENT(FWK_MODULE_IDX_CDNS_I2C, ctx->i2c_id);
+	transfer_event.target_id = ctx->config->callback_mod_id;
+	fwk_notification_notify(&transfer_event, &i);
     }
 }
 
@@ -134,99 +134,99 @@ static void i2c_isr(uintptr_t data)
     ctx->reg->ISR = isr;
 
     if (isr == 0) {
-        return;
+	return;
     }
 
     if (isr & I2C_ISR_ERROR_MASK) {
-        goto i2c_error;
+	goto i2c_error;
     }
 
     switch (ctx->irq_data_ctx.state) {
     case MOD_CDNS_I2C_STATE_TX:
-        /* Check for data interrupt */
-        if (isr & I2C_ISR_DATA_MASK) {
-            for (i = 0; (i < (ctx->config->max_xfr_size - 2U)) &&
-                 (ctx->irq_data_ctx.index < ctx->irq_data_ctx.size);
-                 i++) {
-                ctx->reg->DR = ctx->irq_data_ctx.data[ctx->irq_data_ctx.index];
-                ctx->irq_data_ctx.index++;
-            }
-        }
-        break;
+	/* Check for data interrupt */
+	if (isr & I2C_ISR_DATA_MASK) {
+	    for (i = 0; (i < (ctx->config->max_xfr_size - 2U)) &&
+		 (ctx->irq_data_ctx.index < ctx->irq_data_ctx.size);
+		 i++) {
+		ctx->reg->DR = ctx->irq_data_ctx.data[ctx->irq_data_ctx.index];
+		ctx->irq_data_ctx.index++;
+	    }
+	}
+	break;
 
     case MOD_CDNS_I2C_STATE_RX:
-        /* Read new data from FIFO. */
-        for (; (ctx->reg->SR & I2C_SR_RXDV_MASK) &&
-             (ctx->irq_data_ctx.index < ctx->irq_data_ctx.size);
-             ctx->irq_data_ctx.index++) {
-            ctx->irq_data_ctx.data[ctx->irq_data_ctx.index] =
-                (uint8_t)ctx->reg->DR;
-        }
-
-        /* Make sure transaction sizes align. */
-        if (ctx->reg->SR & I2C_SR_RXDV_MASK) {
-            goto i2c_error;
-        }
-
-        /* If controller, reload TSR if more data expected. */
-        if ((ctx->config->mode == MOD_CDNS_I2C_CONTROLLER_MODE) &&
-            (ctx->irq_data_ctx.index < ctx->irq_data_ctx.size)) {
-            if ((ctx->irq_data_ctx.size - ctx->irq_data_ctx.index) >
-                ctx->config->max_xfr_size) {
-                ctx->reg->TSR = ctx->config->max_xfr_size;
-            } else {
-                ctx->reg->TSR =
-                    (ctx->irq_data_ctx.size - ctx->irq_data_ctx.index);
-            }
-        }
-        break;
+	/* Read new data from FIFO. */
+	for (; (ctx->reg->SR & I2C_SR_RXDV_MASK) &&
+	     (ctx->irq_data_ctx.index < ctx->irq_data_ctx.size);
+	     ctx->irq_data_ctx.index++) {
+	    ctx->irq_data_ctx.data[ctx->irq_data_ctx.index] =
+		(uint8_t)ctx->reg->DR;
+	}
+
+	/* Make sure transaction sizes align. */
+	if (ctx->reg->SR & I2C_SR_RXDV_MASK) {
+	    goto i2c_error;
+	}
+
+	/* If controller, reload TSR if more data expected. */
+	if ((ctx->config->mode == MOD_CDNS_I2C_CONTROLLER_MODE) &&
+	    (ctx->irq_data_ctx.index < ctx->irq_data_ctx.size)) {
+	    if ((ctx->irq_data_ctx.size - ctx->irq_data_ctx.index) >
+		ctx->config->max_xfr_size) {
+		ctx->reg->TSR = ctx->config->max_xfr_size;
+	    } else {
+		ctx->reg->TSR =
+		    (ctx->irq_data_ctx.size - ctx->irq_data_ctx.index);
+	    }
+	}
+	break;
 
     default:
-        goto i2c_error;
+	goto i2c_error;
     }
 
     /* Check for completion interrupt */
     if (isr & I2C_ISR_COMP_MASK) {
-        /* Make sure the correct amount of data was transferred */
-        if (ctx->irq_data_ctx.index != ctx->irq_data_ctx.size) {
-            goto i2c_error;
-        }
-
-        ctx->irq_data_ctx.busy = false;
-        I2C_REG_W(ctx->reg->AR, I2C_AR_ADD7_MASK, I2C_AR_ADD7_SHIFT, 0);
-
-        /* Run completion callback */
-        if ((ctx->irq_data_ctx.state == MOD_CDNS_I2C_STATE_TX) &&
-            (ctx->i2c_callback)) {
-            ctx->i2c_callback(ctx, MOD_CDNS_I2C_NOTIFICATION_IDX_TX);
-        } else if (
-            (ctx->irq_data_ctx.state == MOD_CDNS_I2C_STATE_RX) &&
-            (ctx->i2c_callback)) {
-            ctx->i2c_callback(ctx, MOD_CDNS_I2C_NOTIFICATION_IDX_RX);
-        }
-
-        /* Reset I2C driver state */
-        ctx->reg->IDR = 0xFFFF;
-        I2C_REG_RMW(
-            ctx->reg->CR,
-            I2C_CR_CLRFIFO_MASK,
-            I2C_CR_CLRFIFO_SHIFT,
-            I2C_CLRFIFO_ON);
-        ctx->irq_data_ctx.data = NULL;
+	/* Make sure the correct amount of data was transferred */
+	if (ctx->irq_data_ctx.index != ctx->irq_data_ctx.size) {
+	    goto i2c_error;
+	}
+
+	ctx->irq_data_ctx.busy = false;
+	I2C_REG_W(ctx->reg->AR, I2C_AR_ADD7_MASK, I2C_AR_ADD7_SHIFT, 0);
+
+	/* Run completion callback */
+	if ((ctx->irq_data_ctx.state == MOD_CDNS_I2C_STATE_TX) &&
+	    (ctx->i2c_callback)) {
+	    ctx->i2c_callback(ctx, MOD_CDNS_I2C_NOTIFICATION_IDX_TX);
+	} else if (
+	    (ctx->irq_data_ctx.state == MOD_CDNS_I2C_STATE_RX) &&
+	    (ctx->i2c_callback)) {
+	    ctx->i2c_callback(ctx, MOD_CDNS_I2C_NOTIFICATION_IDX_RX);
+	}
+
+	/* Reset I2C driver state */
+	ctx->reg->IDR = 0xFFFF;
+	I2C_REG_RMW(
+	    ctx->reg->CR,
+	    I2C_CR_CLRFIFO_MASK,
+	    I2C_CR_CLRFIFO_SHIFT,
+	    I2C_CLRFIFO_ON);
+	ctx->irq_data_ctx.data = NULL;
     }
     return;
 
 i2c_error:
     if (ctx->i2c_callback) {
-        ctx->i2c_callback(ctx, MOD_CDNS_I2C_NOTIFICATION_IDX_ERROR);
+	ctx->i2c_callback(ctx, MOD_CDNS_I2C_NOTIFICATION_IDX_ERROR);
     }
 
     ctx->reg->IDR = 0xFFFF;
     I2C_REG_RMW(
-        ctx->reg->CR,
-        I2C_CR_CLRFIFO_MASK,
-        I2C_CR_CLRFIFO_SHIFT,
-        I2C_CLRFIFO_ON);
+	ctx->reg->CR,
+	I2C_CR_CLRFIFO_MASK,
+	I2C_CR_CLRFIFO_SHIFT,
+	I2C_CLRFIFO_ON);
     ctx->irq_data_ctx.data = NULL;
 }
 
@@ -258,27 +258,27 @@ static int i2c_controller_read_polled(
     device_ctx = &i2c_ctx.device_ctx_table[fwk_id_get_element_idx(device_id)];
 
     I2C_REG_RMW(
-        device_ctx->reg->CR, I2C_CR_HOLD_MASK, I2C_CR_HOLD_SHIFT, I2C_HOLD_ON);
+	device_ctx->reg->CR, I2C_CR_HOLD_MASK, I2C_CR_HOLD_SHIFT, I2C_HOLD_ON);
 
     fifo_depth = device_ctx->config->fifo_depth;
 
     /* Make sure any previous error state is cleared */
     I2C_REG_RMW(
-        device_ctx->reg->CR,
-        I2C_CR_CLRFIFO_MASK,
-        I2C_CR_CLRFIFO_SHIFT,
-        I2C_CLRFIFO_ON);
+	device_ctx->reg->CR,
+	I2C_CR_CLRFIFO_MASK,
+	I2C_CR_CLRFIFO_SHIFT,
+	I2C_CLRFIFO_ON);
 
     /* Set the direction */
     I2C_REG_RMW(
-        device_ctx->reg->CR, I2C_CR_RW_MASK, I2C_CR_RW_SHIFT, I2C_RW_READ);
+	device_ctx->reg->CR, I2C_CR_RW_MASK, I2C_CR_RW_SHIFT, I2C_RW_READ);
 
     if (!device_ctx->perform_repeat_start) {
-        I2C_REG_RMW(
-            device_ctx->reg->CR,
-            I2C_CR_CLRFIFO_MASK,
-            I2C_CR_CLRFIFO_SHIFT,
-            I2C_CLRFIFO_ON);
+	I2C_REG_RMW(
+	    device_ctx->reg->CR,
+	    I2C_CR_CLRFIFO_MASK,
+	    I2C_CR_CLRFIFO_SHIFT,
+	    I2C_CLRFIFO_ON);
     }
 
     clear_isr(device_ctx);
@@ -296,110 +296,110 @@ static int i2c_controller_read_polled(
      */
     tsr_reload_size = (length > fifo_depth) ? (fifo_depth + 1) : length;
     I2C_REG_RMW(
-        device_ctx->reg->TSR,
-        I2C_TSR_SIZE_MASK,
-        I2C_TSR_SIZE_SHIFT,
-        tsr_reload_size);
+	device_ctx->reg->TSR,
+	I2C_TSR_SIZE_MASK,
+	I2C_TSR_SIZE_SHIFT,
+	tsr_reload_size);
 
     I2C_REG_RMW(
-        device_ctx->reg->AR, I2C_AR_ADD7_MASK, I2C_AR_ADD7_SHIFT, address);
+	device_ctx->reg->AR, I2C_AR_ADD7_MASK, I2C_AR_ADD7_SHIFT, address);
 
     while (!complete && !timeout && !xfr_error) {
-        isr = I2C_REG_R(device_ctx->reg->ISR, I2C_ISR_MASK, I2C_ISR_SHIFT);
-        /*
-         * If receiving more than the configured TSR size, don't let the TSR
-         * drop to 0 as it would terminate the transfer.
-         */
-        if ((length > fifo_depth) &&
-            (I2C_REG_R(
-                 device_ctx->reg->TSR, I2C_TSR_SIZE_MASK, I2C_TSR_SIZE_SHIFT) ==
-             1)) {
-            length -= fifo_depth;
-
-            /* Set the expected number of bytes to be received */
-            tsr_reload_size = (length > fifo_depth) ? (fifo_depth + 1) : length;
-            I2C_REG_RMW(
-                device_ctx->reg->TSR,
-                I2C_TSR_SIZE_MASK,
-                I2C_TSR_SIZE_SHIFT,
-                tsr_reload_size);
-
-            for (i = 0; i < fifo_depth; i++) {
-                if (I2C_REG_R(
-                        device_ctx->reg->SR,
-                        I2C_SR_RXDV_MASK,
-                        I2C_SR_RXDV_SHIFT)) {
-                    *data++ = I2C_REG_R(
-                        device_ctx->reg->DR,
-                        I2C_DR_DATA_MASK,
-                        I2C_DR_DATA_SHIFT);
-                }
-            }
-        }
-
-        /* Check if we are done with the transfer */
-        if ((isr & I2C_ISR_COMP_MASK) &&
-            I2C_REG_R(
-                device_ctx->reg->TSR, I2C_TSR_SIZE_MASK, I2C_TSR_SIZE_SHIFT) ==
-                0) {
-            /*
-             *  Bytes remaining in the FIFO are the final bytes left to be read
-             *  off the FIFO.
-             */
-            for (i = 0; i < fifo_depth; i++) {
-                if (I2C_REG_R(
-                        device_ctx->reg->SR,
-                        I2C_SR_RXDV_MASK,
-                        I2C_SR_RXDV_SHIFT)) {
-                    *data++ = I2C_REG_R(
-                        device_ctx->reg->DR,
-                        I2C_DR_DATA_MASK,
-                        I2C_DR_DATA_SHIFT);
-                }
-            }
-
-            /* Clear the interrupt status bit */
-            I2C_REG_W(
-                device_ctx->reg->ISR, I2C_ISR_COMP_MASK, I2C_ISR_COMP_SHIFT, 1);
-
-            complete = true;
-        }
-
-        /* Check for any I2C interface errors */
-        if (isr & I2C_ISR_ERROR_MASK) {
-            xfr_error = true;
-        }
-
-        /* Check for transfer timeout */
-        if (I2C_REG_R(
-                device_ctx->reg->ISR, I2C_ISR_TO_MASK, I2C_ISR_TO_SHIFT)) {
-            timeout = true;
-        }
+	isr = I2C_REG_R(device_ctx->reg->ISR, I2C_ISR_MASK, I2C_ISR_SHIFT);
+	/*
+	 * If receiving more than the configured TSR size, don't let the TSR
+	 * drop to 0 as it would terminate the transfer.
+	 */
+	if ((length > fifo_depth) &&
+	    (I2C_REG_R(
+		 device_ctx->reg->TSR, I2C_TSR_SIZE_MASK, I2C_TSR_SIZE_SHIFT) ==
+	     1)) {
+	    length -= fifo_depth;
+
+	    /* Set the expected number of bytes to be received */
+	    tsr_reload_size = (length > fifo_depth) ? (fifo_depth + 1) : length;
+	    I2C_REG_RMW(
+		device_ctx->reg->TSR,
+		I2C_TSR_SIZE_MASK,
+		I2C_TSR_SIZE_SHIFT,
+		tsr_reload_size);
+
+	    for (i = 0; i < fifo_depth; i++) {
+		if (I2C_REG_R(
+			device_ctx->reg->SR,
+			I2C_SR_RXDV_MASK,
+			I2C_SR_RXDV_SHIFT)) {
+		    *data++ = I2C_REG_R(
+			device_ctx->reg->DR,
+			I2C_DR_DATA_MASK,
+			I2C_DR_DATA_SHIFT);
+		}
+	    }
+	}
+
+	/* Check if we are done with the transfer */
+	if ((isr & I2C_ISR_COMP_MASK) &&
+	    I2C_REG_R(
+		device_ctx->reg->TSR, I2C_TSR_SIZE_MASK, I2C_TSR_SIZE_SHIFT) ==
+		0) {
+	    /*
+	     *  Bytes remaining in the FIFO are the final bytes left to be read
+	     *  off the FIFO.
+	     */
+	    for (i = 0; i < fifo_depth; i++) {
+		if (I2C_REG_R(
+			device_ctx->reg->SR,
+			I2C_SR_RXDV_MASK,
+			I2C_SR_RXDV_SHIFT)) {
+		    *data++ = I2C_REG_R(
+			device_ctx->reg->DR,
+			I2C_DR_DATA_MASK,
+			I2C_DR_DATA_SHIFT);
+		}
+	    }
+
+	    /* Clear the interrupt status bit */
+	    I2C_REG_W(
+		device_ctx->reg->ISR, I2C_ISR_COMP_MASK, I2C_ISR_COMP_SHIFT, 1);
+
+	    complete = true;
+	}
+
+	/* Check for any I2C interface errors */
+	if (isr & I2C_ISR_ERROR_MASK) {
+	    xfr_error = true;
+	}
+
+	/* Check for transfer timeout */
+	if (I2C_REG_R(
+		device_ctx->reg->ISR, I2C_ISR_TO_MASK, I2C_ISR_TO_SHIFT)) {
+	    timeout = true;
+	}
     }
 
     if (xfr_error == true) {
-        I2C_REG_RMW(
-            device_ctx->reg->CR,
-            I2C_CR_HOLD_MASK,
-            I2C_CR_HOLD_SHIFT,
-            I2C_HOLD_OFF);
-        clear_isr(device_ctx);
-        return FWK_E_DEVICE;
+	I2C_REG_RMW(
+	    device_ctx->reg->CR,
+	    I2C_CR_HOLD_MASK,
+	    I2C_CR_HOLD_SHIFT,
+	    I2C_HOLD_OFF);
+	clear_isr(device_ctx);
+	return FWK_E_DEVICE;
     }
 
     if (timeout == true) {
-        I2C_REG_RMW(
-            device_ctx->reg->CR,
-            I2C_CR_HOLD_MASK,
-            I2C_CR_HOLD_SHIFT,
-            I2C_HOLD_OFF);
-        clear_isr(device_ctx);
-        return FWK_E_TIMEOUT;
+	I2C_REG_RMW(
+	    device_ctx->reg->CR,
+	    I2C_CR_HOLD_MASK,
+	    I2C_CR_HOLD_SHIFT,
+	    I2C_HOLD_OFF);
+	clear_isr(device_ctx);
+	return FWK_E_TIMEOUT;
     }
 
     /* Clear the hold bit to signify the end of the read sequence */
     I2C_REG_RMW(
-        device_ctx->reg->CR, I2C_CR_HOLD_MASK, I2C_CR_HOLD_SHIFT, I2C_HOLD_OFF);
+	device_ctx->reg->CR, I2C_CR_HOLD_MASK, I2C_CR_HOLD_SHIFT, I2C_HOLD_OFF);
 
     clear_isr(device_ctx);
 
@@ -431,166 +431,166 @@ static int i2c_controller_write_polled(
     fifo_depth = device_ctx->config->fifo_depth;
 
     I2C_REG_RMW(
-        device_ctx->reg->CR, I2C_CR_HOLD_MASK, I2C_CR_HOLD_SHIFT, I2C_HOLD_ON);
+	device_ctx->reg->CR, I2C_CR_HOLD_MASK, I2C_CR_HOLD_SHIFT, I2C_HOLD_ON);
 
     /* Make sure any previous error state is cleared */
     I2C_REG_RMW(
-        device_ctx->reg->CR,
-        I2C_CR_CLRFIFO_MASK,
-        I2C_CR_CLRFIFO_SHIFT,
-        I2C_CLRFIFO_ON);
+	device_ctx->reg->CR,
+	I2C_CR_CLRFIFO_MASK,
+	I2C_CR_CLRFIFO_SHIFT,
+	I2C_CLRFIFO_ON);
 
     /* Set the direction */
     I2C_REG_RMW(
-        device_ctx->reg->CR, I2C_CR_RW_MASK, I2C_CR_RW_SHIFT, I2C_RW_WRITE);
+	device_ctx->reg->CR, I2C_CR_RW_MASK, I2C_CR_RW_SHIFT, I2C_RW_WRITE);
 
     clear_isr(device_ctx);
 
     /* Set the expected number of bytes to be transmitted */
     if (length > fifo_depth) {
-        I2C_REG_RMW(
-            device_ctx->reg->TSR,
-            I2C_TSR_SIZE_MASK,
-            I2C_TSR_SIZE_SHIFT,
-            fifo_depth);
+	I2C_REG_RMW(
+	    device_ctx->reg->TSR,
+	    I2C_TSR_SIZE_MASK,
+	    I2C_TSR_SIZE_SHIFT,
+	    fifo_depth);
     } else {
-        I2C_REG_RMW(
-            device_ctx->reg->TSR,
-            I2C_TSR_SIZE_MASK,
-            I2C_TSR_SIZE_SHIFT,
-            length);
+	I2C_REG_RMW(
+	    device_ctx->reg->TSR,
+	    I2C_TSR_SIZE_MASK,
+	    I2C_TSR_SIZE_SHIFT,
+	    length);
     }
 
     if (!device_ctx->perform_repeat_start) {
-        /*
-         * Preload the FIFO with a single data byte. If this write is to
-         * generate a repeat start then don't write any data until the
-         * address has been set.
-         */
-        I2C_REG_RMW(
-            device_ctx->reg->DR, I2C_DR_DATA_MASK, I2C_DR_DATA_SHIFT, *data++);
-        length--;
+	/*
+	 * Preload the FIFO with a single data byte. If this write is to
+	 * generate a repeat start then don't write any data until the
+	 * address has been set.
+	 */
+	I2C_REG_RMW(
+	    device_ctx->reg->DR, I2C_DR_DATA_MASK, I2C_DR_DATA_SHIFT, *data++);
+	length--;
     }
 
     /* Write the address, triggering the start of the transfer */
     I2C_REG_RMW(
-        device_ctx->reg->AR, I2C_AR_ADD7_MASK, I2C_AR_ADD7_SHIFT, address);
+	device_ctx->reg->AR, I2C_AR_ADD7_MASK, I2C_AR_ADD7_SHIFT, address);
 
     for (i = 0; (i < (fifo_depth - 1U)) && length; i++) {
-        I2C_REG_RMW(
-            device_ctx->reg->DR, I2C_DR_DATA_MASK, I2C_DR_DATA_SHIFT, *data++);
-        length--;
+	I2C_REG_RMW(
+	    device_ctx->reg->DR, I2C_DR_DATA_MASK, I2C_DR_DATA_SHIFT, *data++);
+	length--;
     }
 
     /* Wait for transfer completion */
     while (!complete && !timeout && !xfr_error) {
-        isr = I2C_REG_R(device_ctx->reg->ISR, I2C_ISR_MASK, I2C_ISR_SHIFT);
-        /*
-         * Check if FIFO is nearing empty and there is more data to be
-         * transferred
-         */
-        if (isr & I2C_ISR_DATA_MASK) {
-            /*
-             * The FIFO may still contain 2 outstanding bytes that it needs to
-             * flush out. Reload the FIFO sparing two bytes to avoid the risk of
-             * overrunning the FIFO with the remaining data. Number of bytes
-             * reloaded in the FIFO will be the new TSR value once it reaches 0.
-             *
-             */
-            for (i = 0; (i < (fifo_depth - 2U)) && length; i++) {
-                I2C_REG_RMW(
-                    device_ctx->reg->DR,
-                    I2C_DR_DATA_MASK,
-                    I2C_DR_DATA_SHIFT,
-                    *data++);
-                length--;
-            }
-
-            tsr_reload_size = i;
-
-            /* Clear DATA bit so we don't overflow the FIFO */
-            I2C_REG_W(
-                device_ctx->reg->ISR, I2C_ISR_DATA_MASK, I2C_ISR_DATA_SHIFT, 1);
-        }
-
-        /* Check for transfer complete */
-
-        if (isr & I2C_ISR_COMP_MASK) {
-            /*
-             * TSR reload size is calculated based on how many bytes were
-             * actually written to the FIFO. If the TSR reload size is 0, it
-             * either means that the the data length to be transferred was less
-             * than the I2C_TSR_SIZE_CONFIGURED, or that all the outstanding
-             * data has been transferred.
-             */
-            if (tsr_reload_size) {
-                I2C_REG_RMW(
-                    device_ctx->reg->TSR,
-                    I2C_TSR_SIZE_MASK,
-                    I2C_TSR_SIZE_SHIFT,
-                    tsr_reload_size);
-            } else {
-                complete = true;
-            }
-
-            /* Clear the interrupt status bit */
-            I2C_REG_W(
-                device_ctx->reg->ISR, I2C_ISR_COMP_MASK, I2C_ISR_COMP_SHIFT, 1);
-        }
-
-        /* Check for any I2C interface errors */
-        if (isr & I2C_ISR_ERROR_MASK) {
-            xfr_error = true;
-        }
-
-        /* Check for transfer timeout */
-        if (isr & I2C_ISR_TO_MASK) {
-            timeout = true;
-        }
+	isr = I2C_REG_R(device_ctx->reg->ISR, I2C_ISR_MASK, I2C_ISR_SHIFT);
+	/*
+	 * Check if FIFO is nearing empty and there is more data to be
+	 * transferred
+	 */
+	if (isr & I2C_ISR_DATA_MASK) {
+	    /*
+	     * The FIFO may still contain 2 outstanding bytes that it needs to
+	     * flush out. Reload the FIFO sparing two bytes to avoid the risk of
+	     * overrunning the FIFO with the remaining data. Number of bytes
+	     * reloaded in the FIFO will be the new TSR value once it reaches 0.
+	     *
+	     */
+	    for (i = 0; (i < (fifo_depth - 2U)) && length; i++) {
+		I2C_REG_RMW(
+		    device_ctx->reg->DR,
+		    I2C_DR_DATA_MASK,
+		    I2C_DR_DATA_SHIFT,
+		    *data++);
+		length--;
+	    }
+
+	    tsr_reload_size = i;
+
+	    /* Clear DATA bit so we don't overflow the FIFO */
+	    I2C_REG_W(
+		device_ctx->reg->ISR, I2C_ISR_DATA_MASK, I2C_ISR_DATA_SHIFT, 1);
+	}
+
+	/* Check for transfer complete */
+
+	if (isr & I2C_ISR_COMP_MASK) {
+	    /*
+	     * TSR reload size is calculated based on how many bytes were
+	     * actually written to the FIFO. If the TSR reload size is 0, it
+	     * either means that the the data length to be transferred was less
+	     * than the I2C_TSR_SIZE_CONFIGURED, or that all the outstanding
+	     * data has been transferred.
+	     */
+	    if (tsr_reload_size) {
+		I2C_REG_RMW(
+		    device_ctx->reg->TSR,
+		    I2C_TSR_SIZE_MASK,
+		    I2C_TSR_SIZE_SHIFT,
+		    tsr_reload_size);
+	    } else {
+		complete = true;
+	    }
+
+	    /* Clear the interrupt status bit */
+	    I2C_REG_W(
+		device_ctx->reg->ISR, I2C_ISR_COMP_MASK, I2C_ISR_COMP_SHIFT, 1);
+	}
+
+	/* Check for any I2C interface errors */
+	if (isr & I2C_ISR_ERROR_MASK) {
+	    xfr_error = true;
+	}
+
+	/* Check for transfer timeout */
+	if (isr & I2C_ISR_TO_MASK) {
+	    timeout = true;
+	}
     }
 
     if (xfr_error == true) {
-        I2C_REG_RMW(
-            device_ctx->reg->CR,
-            I2C_CR_HOLD_MASK,
-            I2C_CR_HOLD_SHIFT,
-            I2C_HOLD_OFF);
-        clear_isr(device_ctx);
-        return FWK_E_DEVICE;
+	I2C_REG_RMW(
+	    device_ctx->reg->CR,
+	    I2C_CR_HOLD_MASK,
+	    I2C_CR_HOLD_SHIFT,
+	    I2C_HOLD_OFF);
+	clear_isr(device_ctx);
+	return FWK_E_DEVICE;
     }
 
     if (timeout == true) {
-        I2C_REG_RMW(
-            device_ctx->reg->CR,
-            I2C_CR_HOLD_MASK,
-            I2C_CR_HOLD_SHIFT,
-            I2C_HOLD_OFF);
-        clear_isr(device_ctx);
-        return FWK_E_TIMEOUT;
+	I2C_REG_RMW(
+	    device_ctx->reg->CR,
+	    I2C_CR_HOLD_MASK,
+	    I2C_CR_HOLD_SHIFT,
+	    I2C_HOLD_OFF);
+	clear_isr(device_ctx);
+	return FWK_E_TIMEOUT;
     }
 
     clear_isr(device_ctx);
 
     if (stop) {
-        /* Clear the hold bit to signify the end of the write sequence */
-        I2C_REG_RMW(
-            device_ctx->reg->CR,
-            I2C_CR_HOLD_MASK,
-            I2C_CR_HOLD_SHIFT,
-            I2C_HOLD_OFF);
-        clear_isr(device_ctx);
-        device_ctx->perform_repeat_start = false;
+	/* Clear the hold bit to signify the end of the write sequence */
+	I2C_REG_RMW(
+	    device_ctx->reg->CR,
+	    I2C_CR_HOLD_MASK,
+	    I2C_CR_HOLD_SHIFT,
+	    I2C_HOLD_OFF);
+	clear_isr(device_ctx);
+	device_ctx->perform_repeat_start = false;
     } else {
-        /* Track repeated start so that the following read or write is aware */
-        device_ctx->perform_repeat_start = true;
+	/* Track repeated start so that the following read or write is aware */
+	device_ctx->perform_repeat_start = true;
     }
     return FWK_SUCCESS;
 }
 
 static const struct mod_cdns_i2c_controller_api_polled
     i2c_controller_api_polled = {
-        .read = i2c_controller_read_polled,
-        .write = i2c_controller_write_polled,
+	.read = i2c_controller_read_polled,
+	.write = i2c_controller_write_polled,
     };
 
 /*
@@ -608,7 +608,7 @@ static int i2c_target_write_irq(
     fwk_assert((length != 0) && (data != NULL));
 
     if (device_ctx->irq_data_ctx.busy) {
-        return FWK_E_BUSY;
+	return FWK_E_BUSY;
     }
 
     device_ctx->irq_data_ctx.state = MOD_CDNS_I2C_STATE_TX;
@@ -619,23 +619,23 @@ static int i2c_target_write_irq(
 
     /* Clear FIFO */
     I2C_REG_RMW(
-        device_ctx->reg->CR,
-        I2C_CR_CLRFIFO_MASK,
-        I2C_CR_CLRFIFO_SHIFT,
-        I2C_CLRFIFO_ON);
+	device_ctx->reg->CR,
+	I2C_CR_CLRFIFO_MASK,
+	I2C_CR_CLRFIFO_SHIFT,
+	I2C_CLRFIFO_ON);
 
     /* Set address in AR register */
     I2C_REG_W(
-        device_ctx->reg->AR,
-        I2C_AR_ADD7_MASK,
-        I2C_AR_ADD7_SHIFT,
-        device_ctx->config->target_addr);
+	device_ctx->reg->AR,
+	I2C_AR_ADD7_MASK,
+	I2C_AR_ADD7_SHIFT,
+	device_ctx->config->target_addr);
 
     /* Clear any pending IRQs and re-enable interrupts */
     device_ctx->reg->ISR = I2C_ISR_MASK;
     device_ctx->reg->IER =
-        (I2C_IER_COMP_MASK | I2C_IER_DATA_MASK | I2C_IER_NACK_MASK |
-         I2C_IER_RXOVF_MASK | I2C_IER_TXOVF_MASK | I2C_IER_RXUNF_MASK);
+	(I2C_IER_COMP_MASK | I2C_IER_DATA_MASK | I2C_IER_NACK_MASK |
+	 I2C_IER_RXOVF_MASK | I2C_IER_TXOVF_MASK | I2C_IER_RXUNF_MASK);
 
     /* ISR will take over from here */
     return FWK_SUCCESS;
@@ -653,7 +653,7 @@ static int i2c_target_read_irq(
     fwk_assert((length != 0) && (data != NULL));
 
     if (device_ctx->irq_data_ctx.busy) {
-        return FWK_E_BUSY;
+	return FWK_E_BUSY;
     }
 
     device_ctx->irq_data_ctx.state = MOD_CDNS_I2C_STATE_RX;
@@ -664,27 +664,27 @@ static int i2c_target_read_irq(
 
     /* Clear FIFO */
     I2C_REG_RMW(
-        device_ctx->reg->CR,
-        I2C_CR_CLRFIFO_MASK,
-        I2C_CR_CLRFIFO_SHIFT,
-        I2C_CLRFIFO_ON);
+	device_ctx->reg->CR,
+	I2C_CR_CLRFIFO_MASK,
+	I2C_CR_CLRFIFO_SHIFT,
+	I2C_CLRFIFO_ON);
 
     /* Set address in AR register */
     I2C_REG_W(
-        device_ctx->reg->AR,
-        I2C_AR_ADD7_MASK,
-        I2C_AR_ADD7_SHIFT,
-        device_ctx->config->target_addr);
+	device_ctx->reg->AR,
+	I2C_AR_ADD7_MASK,
+	I2C_AR_ADD7_SHIFT,
+	device_ctx->config->target_addr);
 
     if (length < (device_ctx->config->max_xfr_size - 2)) {
-        device_ctx->reg->TSR = length;
+	device_ctx->reg->TSR = length;
     }
 
     /* Clear any pending IRQs and re-enable interrupts */
     device_ctx->reg->ISR = I2C_ISR_MASK;
     device_ctx->reg->IER =
-        (I2C_IER_COMP_MASK | I2C_IER_DATA_MASK | I2C_IER_NACK_MASK |
-         I2C_IER_RXOVF_MASK | I2C_IER_TXOVF_MASK | I2C_IER_RXUNF_MASK);
+	(I2C_IER_COMP_MASK | I2C_IER_DATA_MASK | I2C_IER_NACK_MASK |
+	 I2C_IER_RXOVF_MASK | I2C_IER_TXOVF_MASK | I2C_IER_RXUNF_MASK);
 
     /* ISR will take over from here */
     return FWK_SUCCESS;
@@ -707,11 +707,11 @@ static void i2c_init(
     divider = (aclk / (I2C_HZ * hz)) - I2C_DIV;
 
     if (divider > I2C_DIV_MAX) {
-        divider = (divider / I2C_DIV_VAL) | (I2C_DIV_VAL1 << I2C_DIV_SHIFT);
+	divider = (divider / I2C_DIV_VAL) | (I2C_DIV_VAL1 << I2C_DIV_SHIFT);
     }
 
     I2C_REG_RMW(
-        device_ctx->reg->CR, I2C_CR_DIV_MASK, I2C_CR_DIV_SHIFT, divider);
+	device_ctx->reg->CR, I2C_CR_DIV_MASK, I2C_CR_DIV_SHIFT, divider);
 }
 
 /*
@@ -723,11 +723,11 @@ static int cdns_i2c_init(
     const void *data)
 {
     if (element_count == 0) {
-        return FWK_E_DATA;
+	return FWK_E_DATA;
     }
 
     i2c_ctx.device_ctx_table =
-        fwk_mm_calloc(element_count, sizeof(i2c_ctx.device_ctx_table[0]));
+	fwk_mm_calloc(element_count, sizeof(i2c_ctx.device_ctx_table[0]));
 
     return FWK_SUCCESS;
 }
@@ -741,58 +741,58 @@ static int cdns_i2c_element_init(
     struct cdns_i2c_dev_ctx *device_ctx;
 
     if (data == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     config = (struct mod_cdns_i2c_device_config *)data;
 
     if ((config->addr_size != MOD_CDNS_I2C_ADDRESS_7_BIT) ||
-        (config->ack_en != MOD_CDNS_I2C_ACK_ENABLE)) {
-        return FWK_E_SUPPORT;
+	(config->ack_en != MOD_CDNS_I2C_ACK_ENABLE)) {
+	return FWK_E_SUPPORT;
     }
 
     device_ctx = &i2c_ctx.device_ctx_table[fwk_id_get_element_idx(element_id)];
     if (device_ctx == NULL) {
-        return FWK_E_DATA;
+	return FWK_E_DATA;
     }
 
     device_ctx->config = config;
     device_ctx->reg = (struct cdns_i2c_reg *)config->reg_base;
 
     I2C_REG_RMW(
-        device_ctx->reg->CR, I2C_CR_MS_MASK, I2C_CR_MS_SHIFT, config->mode);
+	device_ctx->reg->CR, I2C_CR_MS_MASK, I2C_CR_MS_SHIFT, config->mode);
 
     if (config->mode == MOD_CDNS_I2C_TARGET_MODE) {
-        I2C_REG_W(
-            device_ctx->reg->AR,
-            I2C_AR_ADD7_MASK,
-            I2C_AR_ADD7_SHIFT,
-            config->target_addr);
+	I2C_REG_W(
+	    device_ctx->reg->AR,
+	    I2C_AR_ADD7_MASK,
+	    I2C_AR_ADD7_SHIFT,
+	    config->target_addr);
     }
 
     I2C_REG_RMW(
-        device_ctx->reg->CR,
-        I2C_CR_NEA_MASK,
-        I2C_CR_NEA_SHIFT,
-        config->addr_size);
+	device_ctx->reg->CR,
+	I2C_CR_NEA_MASK,
+	I2C_CR_NEA_SHIFT,
+	config->addr_size);
 
     I2C_REG_RMW(
-        device_ctx->reg->CR,
-        I2C_CR_ACKEN_MASK,
-        I2C_CR_ACKEN_SHIFT,
-        config->ack_en);
+	device_ctx->reg->CR,
+	I2C_CR_ACKEN_MASK,
+	I2C_CR_ACKEN_SHIFT,
+	config->ack_en);
 
     I2C_REG_RMW(
-        device_ctx->reg->CR,
-        I2C_CR_HOLD_MASK,
-        I2C_CR_HOLD_SHIFT,
-        config->hold_mode);
+	device_ctx->reg->CR,
+	I2C_CR_HOLD_MASK,
+	I2C_CR_HOLD_SHIFT,
+	config->hold_mode);
 
     I2C_REG_RMW(
-        device_ctx->reg->TOR,
-        I2C_TOR_TIMEOUT_MASK,
-        I2C_TOR_TIMEOUT_SHIFT,
-        I2C_TOR_TIMEOUT_VALUE);
+	device_ctx->reg->TOR,
+	I2C_TOR_TIMEOUT_MASK,
+	I2C_TOR_TIMEOUT_SHIFT,
+	I2C_TOR_TIMEOUT_VALUE);
 
     device_ctx->perform_repeat_start = false;
 
@@ -809,13 +809,13 @@ static int cdns_i2c_process_bind_request
 {
     switch (fwk_id_get_api_idx(api_id)) {
     case MOD_CDNS_I2C_API_CONTROLLER_POLLED:
-        *api = &i2c_controller_api_polled;
-        break;
+	*api = &i2c_controller_api_polled;
+	break;
     case MOD_CDNS_I2C_API_TARGET_IRQ:
-        *api = &i2c_target_api_irq;
-        break;
+	*api = &i2c_target_api_irq;
+	break;
     default:
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     return FWK_SUCCESS;
@@ -830,36 +830,36 @@ static int cdns_i2c_start(fwk_id_t id)
 
     /* Nothing to do for module */
     if (!fwk_module_is_valid_element_id(id)) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     i2c_element_id = fwk_id_get_element_idx(id);
     device_ctx = &i2c_ctx.device_ctx_table[i2c_element_id];
     if (device_ctx == NULL) {
-        return FWK_E_DATA;
+	return FWK_E_DATA;
     }
 
     device_ctx->i2c_id = i2c_element_id;
 
     if (device_ctx->config->irq) {
-        i2c_irq = device_ctx->config->irq;
-        status = fwk_interrupt_set_isr_param(
-            i2c_irq, i2c_isr, (uintptr_t)device_ctx);
-        if (status != FWK_SUCCESS) {
-            return FWK_E_DEVICE;
-        }
-
-        status = fwk_interrupt_clear_pending(i2c_irq);
-        if (status != FWK_SUCCESS) {
-            return FWK_E_DEVICE;
-        }
-
-        status = fwk_interrupt_enable(i2c_irq);
-        if (status != FWK_SUCCESS) {
-            return FWK_E_DEVICE;
-        }
+	i2c_irq = device_ctx->config->irq;
+	status = fwk_interrupt_set_isr_param(
+	    i2c_irq, i2c_isr, (uintptr_t)device_ctx);
+	if (status != FWK_SUCCESS) {
+	    return FWK_E_DEVICE;
+	}
+
+	status = fwk_interrupt_clear_pending(i2c_irq);
+	if (status != FWK_SUCCESS) {
+	    return FWK_E_DEVICE;
+	}
+
+	status = fwk_interrupt_enable(i2c_irq);
+	if (status != FWK_SUCCESS) {
+	    return FWK_E_DEVICE;
+	}
 
-        device_ctx->i2c_callback = i2c_callback_fn;
+	device_ctx->i2c_callback = i2c_callback_fn;
     }
 
     return FWK_SUCCESS;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/clock/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/clock/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/clock/CMakeLists.txt
@@ -8,11 +8,11 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(
     ${SCP_MODULE_TARGET}
     PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_clock.c"
-            "${CMAKE_CURRENT_SOURCE_DIR}/src/clock_tree_management.c")
+	    "${CMAKE_CURRENT_SOURCE_DIR}/src/clock_tree_management.c")
 
 target_link_libraries(${SCP_MODULE_TARGET} PRIVATE module-power-domain)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/clock/doc/clock_management_framework.md
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/clock/doc/clock_management_framework.md
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/clock/doc/clock_management_framework.md
@@ -60,7 +60,7 @@ In the following example we consider ena
        |
        |   +------------+
        +--->    CLOCK-3 |
-           +------------+
+	   +------------+
 ```
 
 #### Enable __Clock-2__
@@ -213,7 +213,7 @@ every child clock can decide how to beha
        |
        |   +------------+
        +--->    CLOCK-2 |
-           +------------+
+	   +------------+
 ```
 #### Initial rates
 
@@ -233,7 +233,7 @@ every child clock can decide how to beha
        |
        |   +------------+
        +--->    CLOCK-2 | @ 50MHz
-           +------------+
+	   +------------+
 ```
 1. OSPM Agent sends SCMI request to set Clock-0 @ 180MHz.
 2. Clock-0 responds to original SCMI request.
@@ -262,39 +262,39 @@ should be defined.
 static const struct fwk_element clock_dev_desc_table[] = {
 ...
     [CLOCK_IDX_MOCK_0] =
-        {
-            .name = "MOCK_CLOCK_0",
-            .data = &((struct mod_clock_dev_config){
-                ...
-            }),
-        },
+	{
+	    .name = "MOCK_CLOCK_0",
+	    .data = &((struct mod_clock_dev_config){
+		...
+	    }),
+	},
     [CLOCK_IDX_MOCK_1] =
-        {
-            .name = "MOCK_CLOCK_1",
-            .data = &((struct mod_clock_dev_config){
-                ...
-                .parent_id =
-                    FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_CLOCK, CLOCK_IDX_MOCK_0),
-            }),
-        },
+	{
+	    .name = "MOCK_CLOCK_1",
+	    .data = &((struct mod_clock_dev_config){
+		...
+		.parent_id =
+		    FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_CLOCK, CLOCK_IDX_MOCK_0),
+	    }),
+	},
     [CLOCK_IDX_MOCK_2] =
-        {
-            .name = "MOCK_CLOCK_2",
-            .data = &((struct mod_clock_dev_config){
-                ...
-                .parent_id =
-                    FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_CLOCK, CLOCK_IDX_MOCK_1),
-            }),
-        },
+	{
+	    .name = "MOCK_CLOCK_2",
+	    .data = &((struct mod_clock_dev_config){
+		...
+		.parent_id =
+		    FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_CLOCK, CLOCK_IDX_MOCK_1),
+	    }),
+	},
     [CLOCK_IDX_MOCK_3] =
-        {
-            .name = "MOCK_CLOCK_3",
-            .data = &((struct mod_clock_dev_config){
-                ...
-                .parent_id =
-                    FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_CLOCK, CLOCK_IDX_MOCK_1),
-            }),
-        },
+	{
+	    .name = "MOCK_CLOCK_3",
+	    .data = &((struct mod_clock_dev_config){
+		...
+		.parent_id =
+		    FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_CLOCK, CLOCK_IDX_MOCK_1),
+	    }),
+	},
 ...
 };
 ```
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/clock/include/mod_clock.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/clock/include/mod_clock.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/clock/include/mod_clock.h
@@ -62,8 +62,8 @@ enum mod_clock_notification_idx {
  */
 static const fwk_id_t mod_clock_notification_id_state_changed =
     FWK_ID_NOTIFICATION_INIT(
-        FWK_MODULE_IDX_CLOCK,
-        MOD_CLOCK_NOTIFICATION_IDX_STATE_CHANGED);
+	FWK_MODULE_IDX_CLOCK,
+	MOD_CLOCK_NOTIFICATION_IDX_STATE_CHANGED);
 
 /*!
  * \brief Identifier for the ::MOD_CLOCK_NOTIFICATION_IDX_STATE_CHANGE_PENDING
@@ -71,8 +71,8 @@ static const fwk_id_t mod_clock_notifica
  */
 static const fwk_id_t mod_clock_notification_id_state_change_pending =
     FWK_ID_NOTIFICATION_INIT(
-        FWK_MODULE_IDX_CLOCK,
-        MOD_CLOCK_NOTIFICATION_IDX_STATE_CHANGE_PENDING);
+	FWK_MODULE_IDX_CLOCK,
+	MOD_CLOCK_NOTIFICATION_IDX_STATE_CHANGE_PENDING);
 #endif
 
 /*!
@@ -271,7 +271,7 @@ struct mod_clock_drv_api {
      * \return One of the standard framework error codes.
      */
     int (*set_rate)(fwk_id_t clock_id, uint64_t rate,
-                    enum mod_clock_round_mode round_mode);
+		    enum mod_clock_round_mode round_mode);
 
     /*!
      * \brief Get the current rate of a clock in Hertz (Hz).
@@ -300,7 +300,7 @@ struct mod_clock_drv_api {
      * \return One of the standard framework error codes.
      */
     int (*get_rate_from_index)(fwk_id_t clock_id, unsigned int rate_index,
-                               uint64_t *rate);
+			       uint64_t *rate);
 
     /*!
      * \brief Set the running state of a clock.
@@ -366,9 +366,9 @@ struct mod_clock_drv_api {
      * \return One of the standard framework error codes.
      */
     int (*process_pending_power_transition)(
-        fwk_id_t clock_id,
-        unsigned int current_state,
-        unsigned int new_state);
+	fwk_id_t clock_id,
+	unsigned int current_state,
+	unsigned int new_state);
 
     /*!
      * \brief Handle the condition where the state of a clock's power domain
@@ -421,9 +421,9 @@ struct mod_clock_drv_api {
      * \return One of the standard framework error codes.
      */
     int (*update_input_rate)(
-        fwk_id_t clock_id,
-        uint64_t in_rate,
-        uint64_t *out_rate);
+	fwk_id_t clock_id,
+	uint64_t in_rate,
+	uint64_t *out_rate);
 
     /*!
      * \brief Get the clock duty cycle.
@@ -477,7 +477,7 @@ struct mod_clock_api {
      * \return One of the standard framework error codes.
      */
     int (*set_rate)(fwk_id_t clock_id, uint64_t rate,
-                    enum mod_clock_round_mode round_mode);
+		    enum mod_clock_round_mode round_mode);
 
     /*!
      * \brief Get the current rate of a clock in Hertz (Hz).
@@ -514,7 +514,7 @@ struct mod_clock_api {
      * \return One of the standard framework error codes.
      */
     int (*get_rate_from_index)(fwk_id_t clock_id, unsigned int rate_index,
-                               uint64_t *rate);
+			       uint64_t *rate);
 
     /*!
      * \brief Set the running state of a clock.
@@ -632,7 +632,7 @@ struct mod_clock_driver_response_api {
      * \param resp_values Pointer to the values requested.
      */
     void (*request_complete)(fwk_id_t dev_id,
-                             struct mod_clock_driver_resp_params *resp_values);
+			     struct mod_clock_driver_resp_params *resp_values);
 };
 
 /*!
@@ -667,19 +667,19 @@ enum mod_clock_event_idx {
  */
 static const fwk_id_t mod_clock_event_id_set_rate_request =
     FWK_ID_EVENT_INIT(FWK_MODULE_IDX_CLOCK,
-                      MOD_CLOCK_EVENT_IDX_SET_RATE_REQUEST);
+		      MOD_CLOCK_EVENT_IDX_SET_RATE_REQUEST);
 
 static const fwk_id_t mod_clock_event_id_get_rate_request =
     FWK_ID_EVENT_INIT(FWK_MODULE_IDX_CLOCK,
-                      MOD_CLOCK_EVENT_IDX_GET_RATE_REQUEST);
+		      MOD_CLOCK_EVENT_IDX_GET_RATE_REQUEST);
 
 static const fwk_id_t mod_clock_event_id_set_state_request =
     FWK_ID_EVENT_INIT(FWK_MODULE_IDX_CLOCK,
-                      MOD_CLOCK_EVENT_IDX_SET_STATE_REQUEST);
+		      MOD_CLOCK_EVENT_IDX_SET_STATE_REQUEST);
 
 static const fwk_id_t mod_clock_event_id_get_state_request =
     FWK_ID_EVENT_INIT(FWK_MODULE_IDX_CLOCK,
-                      MOD_CLOCK_EVENT_IDX_GET_STATE_REQUEST);
+		      MOD_CLOCK_EVENT_IDX_GET_STATE_REQUEST);
 
 /*!
  * \}
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/clock/src/clock.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/clock/src/clock.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/clock/src/clock.h
@@ -23,22 +23,22 @@ struct clock_dev_ctx {
     struct mod_clock_drv_api *api;
 
     struct {
-        /* Cookie for the pre-transition notification response */
-        unsigned int pre_power_transition_cookie;
+	/* Cookie for the pre-transition notification response */
+	unsigned int pre_power_transition_cookie;
 
-        /* Counter of notifications sent */
-        unsigned int transition_pending_sent;
+	/* Counter of notifications sent */
+	unsigned int transition_pending_sent;
 
-        /* Status of the pending transition */
-        unsigned int transition_pending_response_status;
+	/* Status of the pending transition */
+	unsigned int transition_pending_response_status;
     } pd_notif;
 
     struct {
-        /* A request is on-going */
-        bool is_ongoing;
+	/* A request is on-going */
+	bool is_ongoing;
 
-        /* Cookie for the response event */
-        uint32_t cookie;
+	/* Cookie for the response event */
+	uint32_t cookie;
     } request;
 
 #ifdef BUILD_HAS_CLOCK_TREE_MGMT
@@ -58,14 +58,14 @@ struct clock_dev_ctx {
 
     /* Clock state transition */
     struct {
-        /* Number of pending responses */
-        uint32_t pending_responses;
-        /* Clock state transition */
-        uint32_t state;
-        /* Clock id caller to transition */
-        fwk_id_t caller_id;
-        /* Flag indicating if this clock is the starter of transition */
-        bool is_transition_initiator;
+	/* Number of pending responses */
+	uint32_t pending_responses;
+	/* Clock state transition */
+	uint32_t state;
+	/* Clock id caller to transition */
+	fwk_id_t caller_id;
+	/* Flag indicating if this clock is the starter of transition */
+	bool is_transition_initiator;
     } state_transition;
 
     /* Reference count */
@@ -158,14 +158,14 @@ static const fwk_id_t mod_clock_event_id
 /* Identifier of the set state pre-request */
 static const fwk_id_t mod_clock_event_id_set_state_pre_request =
     FWK_ID_EVENT_INIT(
-        FWK_MODULE_IDX_CLOCK,
-        CLOCK_EVENT_IDX_SET_STATE_PRE_REQUEST);
+	FWK_MODULE_IDX_CLOCK,
+	CLOCK_EVENT_IDX_SET_STATE_PRE_REQUEST);
 
 /* Identifier of the set rate pre-request */
 static const fwk_id_t mod_clock_event_id_set_rate_pre_request =
     FWK_ID_EVENT_INIT(
-        FWK_MODULE_IDX_CLOCK,
-        CLOCK_EVENT_IDX_SET_RATE_PRE_REQUEST);
+	FWK_MODULE_IDX_CLOCK,
+	CLOCK_EVENT_IDX_SET_RATE_PRE_REQUEST);
 #endif
 
 #endif /* CLOCK_H */
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/clock/src/clock_tree_management.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/clock/src/clock_tree_management.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/clock/src/clock_tree_management.c
@@ -32,8 +32,8 @@ static int clk_mgmt_send_event_set(
     struct fwk_event event;
 
     event = (struct fwk_event){
-        .target_id = target,
-        .id = mod_clock_event_id_set_state_pre_request,
+	.target_id = target,
+	.id = mod_clock_event_id_set_state_pre_request,
     };
     memcpy(event.params, params, sizeof(struct clock_set_state_params));
 
@@ -47,8 +47,8 @@ static int clk_mgmt_send_event_rate(
     struct fwk_event event;
 
     event = (struct fwk_event){
-        .target_id = target,
-        .id = mod_clock_event_id_set_rate_pre_request,
+	.target_id = target,
+	.id = mod_clock_event_id_set_rate_pre_request,
     };
     memcpy(event.params, params, sizeof(struct clock_set_rate_params));
 
@@ -67,22 +67,22 @@ static int clk_mgmt_complete_response(
      * if it is the transition initiator.
      */
     if (!ctx->state_transition.is_transition_initiator) {
-        /*
-         * If requested_state is STOPPED there is no need to send a event to the
-         * caller.
-         */
-        if (event_params->target_state != MOD_CLOCK_STATE_STOPPED) {
-            return clk_mgmt_send_event_set(
-                event_params, ctx->state_transition.caller_id);
-        }
+	/*
+	 * If requested_state is STOPPED there is no need to send a event to the
+	 * caller.
+	 */
+	if (event_params->target_state != MOD_CLOCK_STATE_STOPPED) {
+	    return clk_mgmt_send_event_set(
+		event_params, ctx->state_transition.caller_id);
+	}
 
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     ctx->state_transition.is_transition_initiator = false;
     response_params.status = event_params->caller_status;
     response_params.value.state =
-        (enum mod_clock_state)event_params->target_state;
+	(enum mod_clock_state)event_params->target_state;
 
     /*
      * This is the last clock dev in the tree, respond to the caller.
@@ -98,14 +98,14 @@ static int clk_mgmt_complete_transition(
 {
     int status;
     status = ctx->api->set_state(
-        ctx->config->driver_id,
-        (enum mod_clock_state)event_params->target_state);
+	ctx->config->driver_id,
+	(enum mod_clock_state)event_params->target_state);
 
     /* Async drivers not supported with clock tree management */
     if (status == FWK_PENDING) {
-        FWK_LOG_WARN(
-            "[CLOCK] Async drivers not supported with clock tree mgmt");
-        return FWK_E_SUPPORT;
+	FWK_LOG_WARN(
+	    "[CLOCK] Async drivers not supported with clock tree mgmt");
+	return FWK_E_SUPPORT;
     }
     event_params->caller_status = status;
     return status;
@@ -114,7 +114,7 @@ static int clk_mgmt_complete_transition(
 int clock_management_process_state(const struct fwk_event *event)
 {
     struct clock_set_state_params *event_params =
-        (struct clock_set_state_params *)event->params;
+	(struct clock_set_state_params *)event->params;
     struct clock_dev_ctx *ctx;
     enum mod_clock_state current_state;
     fwk_id_t target_id;
@@ -131,141 +131,141 @@ int clock_management_process_state(const
      * or children updating the state to the correspondent value.
      */
     case CLOCK_STATE_TRANSITION_IDLE:
-        ctx->state_transition.caller_id = event->source_id;
-        ctx->state_transition.pending_responses = 0;
-        status = ctx->api->get_state(ctx->config->driver_id, &current_state);
-        /* Async drivers not supported with clock tree management */
-        if (status == FWK_PENDING) {
-            FWK_LOG_WARN(
-                "[CLOCK] Async drivers not supported with clock tree mgmt");
-            return FWK_E_SUPPORT;
-        }
-        if (status != FWK_SUCCESS) {
-            event_params->caller_status = status;
-            return clk_mgmt_complete_response(ctx, event_params);
-        }
-
-        if (event_params->target_state == MOD_CLOCK_STATE_RUNNING) {
-            if (current_state == event_params->target_state) {
-                ctx->ref_count++;
-                return clk_mgmt_complete_response(ctx, event_params);
-            }
-
-            if (fwk_id_is_equal(ctx->parent_id, FWK_ID_NONE)) {
-                status = clk_mgmt_complete_transition(ctx, event_params);
-
-                /*
-                 * This should not be reached because. asynchronous drivers
-                 * are not supported.
-                 */
-                fwk_assert(status != FWK_E_SUPPORT);
-
-                if (status == FWK_E_SUPPORT) {
-                    return status;
-                } else if (status == FWK_SUCCESS) {
-                    ctx->ref_count++;
-                }
-
-                return clk_mgmt_complete_response(ctx, event_params);
-            }
-
-            *state = CLOCK_STATE_TRANSITION_WAIT_PARENT;
-            target_id = ctx->parent_id;
-
-            /* Raise event */
-            return clk_mgmt_send_event_set(event_params, target_id);
-
-        } else if (event_params->target_state == MOD_CLOCK_STATE_STOPPED) {
-            ctx->ref_count--;
-
-            if (ctx->ref_count == 0) {
-                status = clk_mgmt_complete_transition(ctx, event_params);
-
-                /*
-                 * This should not be reached because. asynchronous drivers
-                 * are not supported.
-                 */
-                fwk_assert(status != FWK_E_SUPPORT);
-
-                if (status == FWK_E_SUPPORT) {
-                    return status;
-                }
-
-                status = clk_mgmt_complete_response(ctx, event_params);
-                if (status != FWK_SUCCESS) {
-                    return status;
-                }
-                /*
-                 * A sanity check is performed and turn off every unneeded
-                 * clock. If ref_count == 0 it will send an event to its parent
-                 * to decrement their ref_count and turn it off in case it is
-                 * not longer used by an external agent or one of its child.
-                 */
-                if (!fwk_id_is_equal(ctx->parent_id, FWK_ID_NONE)) {
-                    return clk_mgmt_send_event_set(
-                        event_params, ctx->parent_id);
-                }
-
-                return status;
-            }
-
-            return clk_mgmt_complete_response(ctx, event_params);
-        } else {
-            return FWK_E_PARAM;
-        }
-        break;
+	ctx->state_transition.caller_id = event->source_id;
+	ctx->state_transition.pending_responses = 0;
+	status = ctx->api->get_state(ctx->config->driver_id, &current_state);
+	/* Async drivers not supported with clock tree management */
+	if (status == FWK_PENDING) {
+	    FWK_LOG_WARN(
+		"[CLOCK] Async drivers not supported with clock tree mgmt");
+	    return FWK_E_SUPPORT;
+	}
+	if (status != FWK_SUCCESS) {
+	    event_params->caller_status = status;
+	    return clk_mgmt_complete_response(ctx, event_params);
+	}
+
+	if (event_params->target_state == MOD_CLOCK_STATE_RUNNING) {
+	    if (current_state == event_params->target_state) {
+		ctx->ref_count++;
+		return clk_mgmt_complete_response(ctx, event_params);
+	    }
+
+	    if (fwk_id_is_equal(ctx->parent_id, FWK_ID_NONE)) {
+		status = clk_mgmt_complete_transition(ctx, event_params);
+
+		/*
+		 * This should not be reached because. asynchronous drivers
+		 * are not supported.
+		 */
+		fwk_assert(status != FWK_E_SUPPORT);
+
+		if (status == FWK_E_SUPPORT) {
+		    return status;
+		} else if (status == FWK_SUCCESS) {
+		    ctx->ref_count++;
+		}
+
+		return clk_mgmt_complete_response(ctx, event_params);
+	    }
+
+	    *state = CLOCK_STATE_TRANSITION_WAIT_PARENT;
+	    target_id = ctx->parent_id;
+
+	    /* Raise event */
+	    return clk_mgmt_send_event_set(event_params, target_id);
+
+	} else if (event_params->target_state == MOD_CLOCK_STATE_STOPPED) {
+	    ctx->ref_count--;
+
+	    if (ctx->ref_count == 0) {
+		status = clk_mgmt_complete_transition(ctx, event_params);
+
+		/*
+		 * This should not be reached because. asynchronous drivers
+		 * are not supported.
+		 */
+		fwk_assert(status != FWK_E_SUPPORT);
+
+		if (status == FWK_E_SUPPORT) {
+		    return status;
+		}
+
+		status = clk_mgmt_complete_response(ctx, event_params);
+		if (status != FWK_SUCCESS) {
+		    return status;
+		}
+		/*
+		 * A sanity check is performed and turn off every unneeded
+		 * clock. If ref_count == 0 it will send an event to its parent
+		 * to decrement their ref_count and turn it off in case it is
+		 * not longer used by an external agent or one of its child.
+		 */
+		if (!fwk_id_is_equal(ctx->parent_id, FWK_ID_NONE)) {
+		    return clk_mgmt_send_event_set(
+			event_params, ctx->parent_id);
+		}
+
+		return status;
+	    }
+
+	    return clk_mgmt_complete_response(ctx, event_params);
+	} else {
+	    return FWK_E_PARAM;
+	}
+	break;
 
     /*
      * This state waits until parent clock completes running state.
      */
     case CLOCK_STATE_TRANSITION_WAIT_PARENT:
 
-        /*
-         * The parent has sent back an event to its child once their clock has
-         * changed state successfully.
-         */
-        if (!fwk_id_is_equal(event->source_id, ctx->parent_id)) {
-            /*
-             * Assume this node is waiting for its parent to switch ON. Before
-             * the parent respond, the same node received a request from one
-             * of its children. This statement is going to return error to the
-             * caller
-             */
-            event_params->caller_status = FWK_E_BUSY;
-            return clk_mgmt_send_event_set(event_params, event->source_id);
-        }
-        if (event_params->caller_status == FWK_SUCCESS) {
-            status = clk_mgmt_complete_transition(ctx, event_params);
-
-            /*
-             * This should not be reached because. asynchronous drivers
-             * are not supported.
-             */
-            fwk_assert(status != FWK_E_SUPPORT);
-
-            if (status == FWK_E_SUPPORT) {
-                return status;
-            } else if (status == FWK_SUCCESS) {
-                ctx->ref_count++;
-            } else {
-                /*
-                 * When transition can not be completed it is returned
-                 * immediately to the parent the status and a STOPPED state
-                 * to revert the state request.
-                 */
-                event_params->target_state = MOD_CLOCK_STATE_STOPPED;
-                status = clk_mgmt_send_event_set(event_params, ctx->parent_id);
-                if (status != FWK_SUCCESS) {
-                    return status;
-                }
-                event_params->target_state = MOD_CLOCK_STATE_RUNNING;
-            }
-        }
+	/*
+	 * The parent has sent back an event to its child once their clock has
+	 * changed state successfully.
+	 */
+	if (!fwk_id_is_equal(event->source_id, ctx->parent_id)) {
+	    /*
+	     * Assume this node is waiting for its parent to switch ON. Before
+	     * the parent respond, the same node received a request from one
+	     * of its children. This statement is going to return error to the
+	     * caller
+	     */
+	    event_params->caller_status = FWK_E_BUSY;
+	    return clk_mgmt_send_event_set(event_params, event->source_id);
+	}
+	if (event_params->caller_status == FWK_SUCCESS) {
+	    status = clk_mgmt_complete_transition(ctx, event_params);
+
+	    /*
+	     * This should not be reached because. asynchronous drivers
+	     * are not supported.
+	     */
+	    fwk_assert(status != FWK_E_SUPPORT);
+
+	    if (status == FWK_E_SUPPORT) {
+		return status;
+	    } else if (status == FWK_SUCCESS) {
+		ctx->ref_count++;
+	    } else {
+		/*
+		 * When transition can not be completed it is returned
+		 * immediately to the parent the status and a STOPPED state
+		 * to revert the state request.
+		 */
+		event_params->target_state = MOD_CLOCK_STATE_STOPPED;
+		status = clk_mgmt_send_event_set(event_params, ctx->parent_id);
+		if (status != FWK_SUCCESS) {
+		    return status;
+		}
+		event_params->target_state = MOD_CLOCK_STATE_RUNNING;
+	    }
+	}
 
-        return clk_mgmt_complete_response(ctx, event_params);
+	return clk_mgmt_complete_response(ctx, event_params);
 
     default:
-        return FWK_E_STATE;
+	return FWK_E_STATE;
     }
 
     return FWK_SUCCESS;
@@ -274,7 +274,7 @@ int clock_management_process_state(const
 int clock_management_process_rate(const struct fwk_event *event)
 {
     struct clock_set_rate_params *event_params =
-        (struct clock_set_rate_params *)event->params;
+	(struct clock_set_rate_params *)event->params;
     struct clock_dev_ctx *ctx;
     struct clock_dev_ctx *child = NULL;
     struct fwk_slist *c_node = NULL;
@@ -289,18 +289,18 @@ int clock_management_process_rate(const
      * output rate to match with its internal settings.
      */
     FWK_LIST_FOR_EACH(
-        &ctx->children_list, c_node, struct clock_dev_ctx, child_node, child)
+	&ctx->children_list, c_node, struct clock_dev_ctx, child_node, child)
     {
-        if (child->api->update_input_rate != NULL) {
-            status = child->api->update_input_rate(
-                child->config->driver_id, in_rate, &out_rate);
-            if (status != FWK_SUCCESS) {
-                return status;
-            }
-
-            event_params->input_rate = out_rate;
-            clk_mgmt_send_event_rate(event_params, child->id);
-        }
+	if (child->api->update_input_rate != NULL) {
+	    status = child->api->update_input_rate(
+		child->config->driver_id, in_rate, &out_rate);
+	    if (status != FWK_SUCCESS) {
+		return status;
+	    }
+
+	    event_params->input_rate = out_rate;
+	    clk_mgmt_send_event_rate(event_params, child->id);
+	}
     }
     return FWK_SUCCESS;
 }
@@ -308,7 +308,7 @@ int clock_management_process_rate(const
 bool clock_is_single_node(struct clock_dev_ctx *ctx)
 {
     return fwk_id_is_equal(ctx->parent_id, FWK_ID_NONE) &&
-        fwk_list_is_empty(&ctx->children_list);
+	fwk_list_is_empty(&ctx->children_list);
 }
 
 /* Sub-routine of 'clock_start()' Build the clock tree */
@@ -320,35 +320,35 @@ int clock_connect_tree(struct clock_ctx
     struct clock_dev_ctx *clk, *parent;
 
     for (clk_idx = 0; clk_idx < module_ctx->dev_count; clk_idx++) {
-        clk = &(module_ctx->dev_ctx_table[clk_idx]);
+	clk = &(module_ctx->dev_ctx_table[clk_idx]);
 
-        if (!fwk_optional_id_is_defined(clk->config->parent_id)) {
-            clk->parent_id = FWK_ID_NONE;
-            continue;
-        }
-
-        clk->parent_id = clk->config->parent_id;
-        parent_idx = fwk_id_get_element_idx(clk->parent_id);
-
-        parent = &(module_ctx->dev_ctx_table[parent_idx]);
-        if (parent == NULL) {
-            return FWK_E_DATA;
-        }
-
-        fwk_list_push_tail(&parent->children_list, &clk->child_node);
-
-        status = clk->api->get_state(clk->config->driver_id, &current_state);
-        if (status == FWK_PENDING) {
-            FWK_LOG_WARN(
-                "[CLOCK] Async drivers not supported with clock tree mgmt");
-            return FWK_E_SUPPORT;
-        } else if (status != FWK_SUCCESS) {
-            return status;
-        }
-
-        if (current_state != MOD_CLOCK_STATE_STOPPED) {
-            parent->ref_count++;
-        }
+	if (!fwk_optional_id_is_defined(clk->config->parent_id)) {
+	    clk->parent_id = FWK_ID_NONE;
+	    continue;
+	}
+
+	clk->parent_id = clk->config->parent_id;
+	parent_idx = fwk_id_get_element_idx(clk->parent_id);
+
+	parent = &(module_ctx->dev_ctx_table[parent_idx]);
+	if (parent == NULL) {
+	    return FWK_E_DATA;
+	}
+
+	fwk_list_push_tail(&parent->children_list, &clk->child_node);
+
+	status = clk->api->get_state(clk->config->driver_id, &current_state);
+	if (status == FWK_PENDING) {
+	    FWK_LOG_WARN(
+		"[CLOCK] Async drivers not supported with clock tree mgmt");
+	    return FWK_E_SUPPORT;
+	} else if (status != FWK_SUCCESS) {
+	    return status;
+	}
+
+	if (current_state != MOD_CLOCK_STATE_STOPPED) {
+	    parent->ref_count++;
+	}
     }
 
     return FWK_SUCCESS;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/clock/src/mod_clock.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/clock/src/mod_clock.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/clock/src/mod_clock.c
@@ -36,17 +36,17 @@ static int process_response_event(const
     struct fwk_event resp_event;
     struct clock_dev_ctx *ctx;
     struct mod_clock_driver_resp_params *event_params =
-        (struct mod_clock_driver_resp_params *)event->params;
+	(struct mod_clock_driver_resp_params *)event->params;
     struct mod_clock_resp_params *resp_params =
-        (struct mod_clock_resp_params *)resp_event.params;
+	(struct mod_clock_resp_params *)resp_event.params;
 
     ctx =
-        &mod_clock_ctx.dev_ctx_table[fwk_id_get_element_idx(event->target_id)];
+	&mod_clock_ctx.dev_ctx_table[fwk_id_get_element_idx(event->target_id)];
 
     status = fwk_get_delayed_response(
-        event->target_id, ctx->request.cookie, &resp_event);
+	event->target_id, ctx->request.cookie, &resp_event);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     resp_params->status = event_params->status;
@@ -57,12 +57,12 @@ static int process_response_event(const
 }
 
 static int process_request_event(const struct fwk_event *event,
-                                 struct fwk_event *resp_event)
+				 struct fwk_event *resp_event)
 {
     struct clock_dev_ctx *ctx;
 
     ctx =
-        &mod_clock_ctx.dev_ctx_table[fwk_id_get_element_idx(event->target_id)];
+	&mod_clock_ctx.dev_ctx_table[fwk_id_get_element_idx(event->target_id)];
 
     ctx->request.cookie = event->cookie;
     resp_event->is_delayed_response = true;
@@ -79,14 +79,14 @@ static int create_async_request(
     struct fwk_event request_event;
 
     request_event = (struct fwk_event) {
-        .target_id = clock_id,
-        .id = event_id,
-        .response_requested = true,
+	.target_id = clock_id,
+	.id = event_id,
+	.response_requested = true,
     };
 
     status = fwk_put_event(&request_event);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     ctx->request.is_ongoing = true;
@@ -117,23 +117,23 @@ void clock_request_complete(
     struct fwk_event event;
     struct clock_dev_ctx *ctx;
     struct mod_clock_driver_resp_params *event_params =
-        (struct mod_clock_driver_resp_params *)event.params;
+	(struct mod_clock_driver_resp_params *)event.params;
 
     fwk_assert(fwk_module_is_valid_element_id(dev_id));
 
     ctx = &mod_clock_ctx.dev_ctx_table[fwk_id_get_element_idx(dev_id)];
 
     event = (struct fwk_event) {
-        .id = mod_clock_event_id_response,
-        .source_id = ctx->config->driver_id,
-        .target_id = dev_id,
+	.id = mod_clock_event_id_response,
+	.source_id = ctx->config->driver_id,
+	.target_id = dev_id,
     };
 
     if (response != NULL) {
-        event_params->status = response->status;
-        event_params->value = response->value;
+	event_params->status = response->status;
+	event_params->value = response->value;
     } else {
-        event_params->status = FWK_E_PARAM;
+	event_params->status = FWK_E_PARAM;
     }
 
     status = fwk_put_event(&event);
@@ -149,7 +149,7 @@ static struct mod_clock_driver_response_
  */
 
 static int clock_set_rate(fwk_id_t clock_id, uint64_t rate,
-                          enum mod_clock_round_mode round_mode)
+			  enum mod_clock_round_mode round_mode)
 {
     int status;
     struct clock_dev_ctx *ctx;
@@ -163,23 +163,23 @@ static int clock_set_rate(fwk_id_t clock
 
     /* Concurrency is not supported */
     if (ctx->request.is_ongoing) {
-        return FWK_E_BUSY;
+	return FWK_E_BUSY;
     }
 
 #ifdef BUILD_HAS_CLOCK_TREE_MGMT
 
     status = ctx->api->set_rate(ctx->config->driver_id, rate, round_mode);
     if (status == FWK_PENDING) {
-        return create_async_request(
-            ctx, clock_id, mod_clock_event_id_set_rate_request);
+	return create_async_request(
+	    ctx, clock_id, mod_clock_event_id_set_rate_request);
     }
     if (clock_is_single_node(ctx)) {
-        return status;
+	return status;
     }
 
     event = (struct fwk_event){
-        .target_id = clock_id,
-        .id = mod_clock_event_id_set_rate_pre_request,
+	.target_id = clock_id,
+	.id = mod_clock_event_id_set_rate_pre_request,
     };
     event_params = (struct clock_set_rate_params *)event.params;
     event_params->input_rate = rate;
@@ -188,10 +188,10 @@ static int clock_set_rate(fwk_id_t clock
 #else
     status = ctx->api->set_rate(ctx->config->driver_id, rate, round_mode);
     if (status == FWK_PENDING) {
-        return create_async_request(
-            ctx,
-            clock_id,
-            mod_clock_event_id_set_rate_request);
+	return create_async_request(
+	    ctx,
+	    clock_id,
+	    mod_clock_event_id_set_rate_request);
     }
     return status;
 #endif
@@ -205,38 +205,38 @@ static int clock_get_rate(fwk_id_t clock
     clock_get_ctx(clock_id, &ctx);
 
     if (rate == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     /* Concurrency is not supported */
     if (ctx->request.is_ongoing) {
-        return FWK_E_BUSY;
+	return FWK_E_BUSY;
     }
 
     status = ctx->api->get_rate(ctx->config->driver_id, rate);
     if (status == FWK_PENDING) {
-        return create_async_request(
-            ctx,
-            clock_id,
-            mod_clock_event_id_get_rate_request);
+	return create_async_request(
+	    ctx,
+	    clock_id,
+	    mod_clock_event_id_get_rate_request);
     } else {
-        return status;
+	return status;
     }
 }
 
 static int clock_get_rate_from_index(fwk_id_t clock_id, unsigned int rate_index,
-                                     uint64_t *rate)
+				     uint64_t *rate)
 {
     struct clock_dev_ctx *ctx;
 
     clock_get_ctx(clock_id, &ctx);
 
     if (rate == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     return ctx->api->get_rate_from_index(ctx->config->driver_id, rate_index,
-                                         rate);
+					 rate);
 }
 
 static int clock_set_state(fwk_id_t clock_id, enum mod_clock_state state)
@@ -252,46 +252,46 @@ static int clock_set_state(fwk_id_t cloc
     clock_get_ctx(clock_id, &ctx);
     /* Concurrency is not supported */
     if (ctx->request.is_ongoing) {
-        return FWK_E_BUSY;
+	return FWK_E_BUSY;
     }
 
 #ifdef BUILD_HAS_CLOCK_TREE_MGMT
     if (ctx->state_transition.state != CLOCK_STATE_TRANSITION_IDLE) {
-        return FWK_E_BUSY;
+	return FWK_E_BUSY;
     }
 
     if (clock_is_single_node(ctx)) {
-        status = ctx->api->set_state(ctx->config->driver_id, state);
-        if (status == FWK_PENDING) {
-            return create_async_request(
-                ctx, clock_id, mod_clock_event_id_set_state_request);
-        }
-        return status;
+	status = ctx->api->set_state(ctx->config->driver_id, state);
+	if (status == FWK_PENDING) {
+	    return create_async_request(
+		ctx, clock_id, mod_clock_event_id_set_state_request);
+	}
+	return status;
     }
 
     status = create_async_request(
-        ctx, clock_id, mod_clock_event_id_set_state_request);
+	ctx, clock_id, mod_clock_event_id_set_state_request);
     if (status != FWK_PENDING) {
-        return status;
+	return status;
     }
 
     event = (struct fwk_event){
-        .target_id = clock_id,
-        .id = mod_clock_event_id_set_state_pre_request,
+	.target_id = clock_id,
+	.id = mod_clock_event_id_set_state_pre_request,
     };
     event_params = (struct clock_set_state_params *)event.params;
     event_params->target_state = state;
     ctx->state_transition.is_transition_initiator = true;
     status = fwk_put_event(&event);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
     return FWK_PENDING;
 #else
     status = ctx->api->set_state(ctx->config->driver_id, state);
     if (status == FWK_PENDING) {
-        return create_async_request(
-            ctx, clock_id, mod_clock_event_id_set_state_request);
+	return create_async_request(
+	    ctx, clock_id, mod_clock_event_id_set_state_request);
     }
     return status;
 #endif
@@ -305,22 +305,22 @@ static int clock_get_state(fwk_id_t cloc
     clock_get_ctx(clock_id, &ctx);
 
     if (state == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     /* Concurrency is not supported */
     if (ctx->request.is_ongoing) {
-        return FWK_E_BUSY;
+	return FWK_E_BUSY;
     }
 
     status = ctx->api->get_state(ctx->config->driver_id, state);
     if (status == FWK_PENDING) {
-        return create_async_request(
-            ctx,
-            clock_id,
-            mod_clock_event_id_get_state_request);
+	return create_async_request(
+	    ctx,
+	    clock_id,
+	    mod_clock_event_id_get_state_request);
     } else {
-        return status;
+	return status;
     }
 }
 
@@ -332,12 +332,12 @@ static int clock_get_info(fwk_id_t clock
     clock_get_ctx(clock_id, &ctx);
 
     if (info == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     status = ctx->api->get_range(ctx->config->driver_id, &info->range);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     info->name = fwk_module_get_element_name(clock_id);
@@ -352,25 +352,25 @@ static int clock_get_duty_cycle(fwk_id_t
     clock_get_ctx(clock_id, &ctx);
 
     if (!ctx->api->get_duty_cycle) {
-        return FWK_E_SUPPORT;
+	return FWK_E_SUPPORT;
     }
 
     return ctx->api->get_duty_cycle(ctx->config->driver_id, num, den);
 }
 
 static int clock_round_rate(fwk_id_t clock_id, uint64_t rate,
-                            uint64_t *rounded_rate)
+			    uint64_t *rounded_rate)
 {
     struct clock_dev_ctx *ctx;
 
     clock_get_ctx(clock_id, &ctx);
 
     if (rounded_rate == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     if (!ctx->api->round_rate) {
-        return FWK_E_SUPPORT;
+	return FWK_E_SUPPORT;
     }
 
     return ctx->api->round_rate(ctx->config->driver_id, rate, rounded_rate);
@@ -392,30 +392,30 @@ static const struct mod_clock_api clock_
  */
 
 static int clock_init(fwk_id_t module_id, unsigned int element_count,
-                      const void *data)
+		      const void *data)
 {
     const struct mod_clock_config *config = data;
 
     if (element_count == 0) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
 #ifdef BUILD_HAS_NOTIFICATION
     if (config == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 #endif
 
     mod_clock_ctx.config = config;
     mod_clock_ctx.dev_ctx_table =
-        fwk_mm_calloc(element_count, sizeof(struct clock_dev_ctx));
+	fwk_mm_calloc(element_count, sizeof(struct clock_dev_ctx));
     mod_clock_ctx.dev_count = element_count;
 
     return FWK_SUCCESS;
 }
 
 static int clock_dev_init(fwk_id_t element_id, unsigned int sub_element_count,
-                          const void *data)
+			  const void *data)
 {
     struct clock_dev_ctx *ctx;
     const struct mod_clock_dev_config *dev_config = data;
@@ -436,19 +436,19 @@ static int clock_bind(fwk_id_t id, unsig
     struct clock_dev_ctx *ctx;
 
     if (round == 1) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     if (!fwk_id_is_type(id, FWK_ID_TYPE_ELEMENT)) {
-        /* Only element binding is supported */
-        return FWK_SUCCESS;
+	/* Only element binding is supported */
+	return FWK_SUCCESS;
     }
 
     ctx = &mod_clock_ctx.dev_ctx_table[fwk_id_get_element_idx(id)];
 
     return fwk_module_bind(ctx->config->driver_id,
-                           ctx->config->api_id,
-                           &ctx->api);
+			   ctx->config->api_id,
+			   &ctx->api);
 }
 
 static int clock_start(fwk_id_t id)
@@ -461,43 +461,43 @@ static int clock_start(fwk_id_t id)
     /* Clock tree is initialized */
     if (!fwk_id_is_type(id, FWK_ID_TYPE_ELEMENT)) {
 #ifdef BUILD_HAS_CLOCK_TREE_MGMT
-        return clock_connect_tree(&mod_clock_ctx);
+	return clock_connect_tree(&mod_clock_ctx);
 #else
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 #endif
     }
 
     ctx = &mod_clock_ctx.dev_ctx_table[fwk_id_get_element_idx(id)];
 
     if (fwk_id_is_type(ctx->config->pd_source_id, FWK_ID_TYPE_NONE)) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
 #ifdef BUILD_HAS_NOTIFICATION
     if ((ctx->api->process_power_transition != NULL) &&
-        (fwk_id_is_type(
-            mod_clock_ctx.config->pd_transition_notification_id,
-            FWK_ID_TYPE_NOTIFICATION))) {
-        status = fwk_notification_subscribe(
-            mod_clock_ctx.config->pd_transition_notification_id,
-            ctx->config->pd_source_id,
-            id);
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
+	(fwk_id_is_type(
+	    mod_clock_ctx.config->pd_transition_notification_id,
+	    FWK_ID_TYPE_NOTIFICATION))) {
+	status = fwk_notification_subscribe(
+	    mod_clock_ctx.config->pd_transition_notification_id,
+	    ctx->config->pd_source_id,
+	    id);
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
     }
 
     if ((ctx->api->process_pending_power_transition != NULL) &&
-        (fwk_id_is_type(
-            mod_clock_ctx.config->pd_pre_transition_notification_id,
-            FWK_ID_TYPE_NOTIFICATION))) {
-        status = fwk_notification_subscribe(
-            mod_clock_ctx.config->pd_pre_transition_notification_id,
-            ctx->config->pd_source_id,
-            id);
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
+	(fwk_id_is_type(
+	    mod_clock_ctx.config->pd_pre_transition_notification_id,
+	    FWK_ID_TYPE_NOTIFICATION))) {
+	status = fwk_notification_subscribe(
+	    mod_clock_ctx.config->pd_pre_transition_notification_id,
+	    ctx->config->pd_source_id,
+	    id);
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
     }
 #endif
 
@@ -505,34 +505,34 @@ static int clock_start(fwk_id_t id)
 }
 
 static int clock_process_bind_request(fwk_id_t source_id, fwk_id_t target_id,
-                                      fwk_id_t api_id, const void **api)
+				      fwk_id_t api_id, const void **api)
 {
     enum mod_clock_api_type api_type = fwk_id_get_api_idx(api_id);
     struct clock_dev_ctx *ctx;
 
     switch (api_type) {
     case MOD_CLOCK_API_TYPE_HAL:
-        *api = &clock_api;
+	*api = &clock_api;
 
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 
     case MOD_CLOCK_API_TYPE_DRIVER_RESPONSE:
-        if (!fwk_id_is_type(target_id, FWK_ID_TYPE_ELEMENT)) {
-            return FWK_E_PARAM;
-        }
-
-        ctx = &mod_clock_ctx.dev_ctx_table[fwk_id_get_element_idx(target_id)];
-
-        if (fwk_id_is_equal(source_id, ctx->config->driver_id)) {
-            *api = &clock_driver_response_api;
-        } else {
-            return FWK_E_ACCESS;
-        }
+	if (!fwk_id_is_type(target_id, FWK_ID_TYPE_ELEMENT)) {
+	    return FWK_E_PARAM;
+	}
+
+	ctx = &mod_clock_ctx.dev_ctx_table[fwk_id_get_element_idx(target_id)];
+
+	if (fwk_id_is_equal(source_id, ctx->config->driver_id)) {
+	    *api = &clock_driver_response_api;
+	} else {
+	    return FWK_E_ACCESS;
+	}
 
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 
     default:
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 }
 
@@ -546,25 +546,25 @@ static int clock_process_pd_pre_transiti
     int status;
     struct mod_pd_power_state_pre_transition_notification_params *pd_params;
     struct mod_pd_power_state_pre_transition_notification_resp_params
-        *pd_resp_params;
+	*pd_resp_params;
     struct clock_notification_params *out_params;
     struct fwk_event outbound_event = {
-        .response_requested = true,
-        .id = mod_clock_notification_id_state_change_pending,
-        .source_id = FWK_ID_NONE
+	.response_requested = true,
+	.id = mod_clock_notification_id_state_change_pending,
+	.source_id = FWK_ID_NONE
     };
 
     pd_params = (struct mod_pd_power_state_pre_transition_notification_params *)
-        event->params;
+	event->params;
     pd_resp_params =
-        (struct mod_pd_power_state_pre_transition_notification_resp_params *)
-            resp_event->params;
+	(struct mod_pd_power_state_pre_transition_notification_resp_params *)
+	    resp_event->params;
 
     fwk_assert(ctx->api->process_pending_power_transition != NULL);
     status = ctx->api->process_pending_power_transition(
-        ctx->config->driver_id,
-        pd_params->current_state,
-        pd_params->target_state);
+	ctx->config->driver_id,
+	pd_params->current_state,
+	pd_params->target_state);
 
     /*
      * The response to the notification should initially be the overall result
@@ -573,13 +573,13 @@ static int clock_process_pd_pre_transiti
     pd_resp_params->status = status;
 
     if (status != FWK_SUCCESS) {
-        ctx->pd_notif.transition_pending_sent = 0;
-        return status;
+	ctx->pd_notif.transition_pending_sent = 0;
+	return status;
     }
 
     ctx->pd_notif.transition_pending_response_status = FWK_SUCCESS;
     out_params =
-        (struct clock_notification_params *)outbound_event.params;
+	(struct clock_notification_params *)outbound_event.params;
 
     /*
      * For now it is sufficient to assume that a PD ON state implies that
@@ -589,21 +589,21 @@ static int clock_process_pd_pre_transiti
      * outgoing notification.
      */
     out_params->new_state = (pd_params->target_state == MOD_PD_STATE_ON)
-        ? MOD_CLOCK_STATE_RUNNING
-        : MOD_CLOCK_STATE_STOPPED;
+	? MOD_CLOCK_STATE_RUNNING
+	: MOD_CLOCK_STATE_STOPPED;
 
     /* Notify subscribers of the pending clock state change */
     status = fwk_notification_notify(
-        &outbound_event, &(ctx->pd_notif.transition_pending_sent));
+	&outbound_event, &(ctx->pd_notif.transition_pending_sent));
     if (status != FWK_SUCCESS) {
-        pd_resp_params->status = status;
-        return status;
+	pd_resp_params->status = status;
+	return status;
     }
 
     if (ctx->pd_notif.transition_pending_sent > 0) {
-        /* There are one or more subscribers that must respond */
-        resp_event->is_delayed_response = true;
-        ctx->pd_notif.pre_power_transition_cookie = event->cookie;
+	/* There are one or more subscribers that must respond */
+	resp_event->is_delayed_response = true;
+	ctx->pd_notif.pre_power_transition_cookie = event->cookie;
     }
 
     return status;
@@ -618,33 +618,33 @@ static int clock_process_pd_transition_n
     struct mod_pd_power_state_transition_notification_params *pd_params;
     struct clock_notification_params* out_params;
     struct fwk_event outbound_event = {
-        .response_requested = false,
-        .id = mod_clock_notification_id_state_changed,
-        .source_id = FWK_ID_NONE
+	.response_requested = false,
+	.id = mod_clock_notification_id_state_changed,
+	.source_id = FWK_ID_NONE
     };
 
     pd_params =
-        (struct mod_pd_power_state_transition_notification_params *)event
-            ->params;
+	(struct mod_pd_power_state_transition_notification_params *)event
+	    ->params;
 
     fwk_assert(ctx->api->process_power_transition != NULL);
     status = ctx->api->process_power_transition(
-        ctx->config->driver_id, pd_params->state);
+	ctx->config->driver_id, pd_params->state);
 
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     /* Notify subscribers of the clock state change */
     out_params = (struct clock_notification_params *)outbound_event.params;
     if (pd_params->state == MOD_PD_STATE_ON) {
-        out_params->new_state = MOD_CLOCK_STATE_RUNNING;
+	out_params->new_state = MOD_CLOCK_STATE_RUNNING;
     } else {
-        out_params->new_state = MOD_CLOCK_STATE_STOPPED;
+	out_params->new_state = MOD_CLOCK_STATE_STOPPED;
     }
 
     status = fwk_notification_notify(
-        &outbound_event, &(transition_notifications_sent));
+	&outbound_event, &(transition_notifications_sent));
 
     return status;
 }
@@ -655,26 +655,26 @@ static int clock_process_notification_re
 {
     struct clock_state_change_pending_resp_params *resp_params;
     struct mod_pd_power_state_pre_transition_notification_resp_params
-        *pd_resp_params;
+	*pd_resp_params;
     struct fwk_event pd_response_event = {
-        .id = mod_clock_ctx.config->pd_pre_transition_notification_id,
-        .target_id = ctx->config->pd_source_id,
-        .cookie = ctx->pd_notif.pre_power_transition_cookie,
-        .is_notification = true,
-        .is_response = true,
-        .is_delayed_response = true,
+	.id = mod_clock_ctx.config->pd_pre_transition_notification_id,
+	.target_id = ctx->config->pd_source_id,
+	.cookie = ctx->pd_notif.pre_power_transition_cookie,
+	.is_notification = true,
+	.is_response = true,
+	.is_delayed_response = true,
     };
 
     fwk_assert(fwk_id_is_equal(event->id,
-                               mod_clock_notification_id_state_change_pending));
+			       mod_clock_notification_id_state_change_pending));
 
     /* At least one notification response must be outstanding */
     if (!fwk_expect(ctx->pd_notif.transition_pending_sent != 0)) {
-        return FWK_E_PANIC;
+	return FWK_E_PANIC;
     }
 
     resp_params =
-        (struct clock_state_change_pending_resp_params *)event->params;
+	(struct clock_state_change_pending_resp_params *)event->params;
 
     /*
      * Change the status used in the response to the power domain notification
@@ -684,21 +684,21 @@ static int clock_process_notification_re
      * that is pending.
      */
     if (resp_params->status != FWK_SUCCESS) {
-        ctx->pd_notif.transition_pending_response_status =
-            (unsigned int)resp_params->status;
+	ctx->pd_notif.transition_pending_response_status =
+	    (unsigned int)resp_params->status;
     }
 
     if ((--(ctx->pd_notif.transition_pending_sent)) == 0) {
-        /*
-         * If this is the final response then the response to the power domain
-         * notification can be sent.
-         */
-        pd_resp_params =
-            (struct mod_pd_power_state_pre_transition_notification_resp_params
-                 *)pd_response_event.params;
-        pd_resp_params->status =
-            (int)ctx->pd_notif.transition_pending_response_status;
-        return fwk_put_event(&pd_response_event);
+	/*
+	 * If this is the final response then the response to the power domain
+	 * notification can be sent.
+	 */
+	pd_resp_params =
+	    (struct mod_pd_power_state_pre_transition_notification_resp_params
+		 *)pd_response_event.params;
+	pd_resp_params->status =
+	    (int)ctx->pd_notif.transition_pending_response_status;
+	return fwk_put_event(&pd_response_event);
     }
 
     return FWK_SUCCESS;
@@ -711,36 +711,36 @@ static int clock_process_notification(
     struct clock_dev_ctx *ctx;
 
     if (!fwk_id_is_type(event->target_id, FWK_ID_TYPE_ELEMENT)) {
-        /* Only elements should be registered for notifications */
-        return FWK_E_PARAM;
+	/* Only elements should be registered for notifications */
+	return FWK_E_PARAM;
     }
 
     ctx =
-        &mod_clock_ctx.dev_ctx_table[fwk_id_get_element_idx(event->target_id)];
+	&mod_clock_ctx.dev_ctx_table[fwk_id_get_element_idx(event->target_id)];
 
     if (event->is_response) {
-        return clock_process_notification_response(ctx, event);
+	return clock_process_notification_response(ctx, event);
     }
 
     if (fwk_id_is_equal(
-            event->id, mod_clock_ctx.config->pd_transition_notification_id)) {
-        return clock_process_pd_transition_notification(ctx, event);
+	    event->id, mod_clock_ctx.config->pd_transition_notification_id)) {
+	return clock_process_pd_transition_notification(ctx, event);
     } else if (fwk_id_is_equal(
-                   event->id,
-                   mod_clock_ctx.config->pd_pre_transition_notification_id)) {
-        return clock_process_pd_pre_transition_notification(
-            ctx, event, resp_event);
+		   event->id,
+		   mod_clock_ctx.config->pd_pre_transition_notification_id)) {
+	return clock_process_pd_pre_transition_notification(
+	    ctx, event, resp_event);
     } else {
-        return FWK_E_HANDLER;
+	return FWK_E_HANDLER;
     }
 }
 #endif /* BUILD_HAS_NOTIFICATION */
 
 static int clock_process_event(const struct fwk_event *event,
-                               struct fwk_event *resp_event)
+			       struct fwk_event *resp_event)
 {
     if (!fwk_module_is_valid_element_id(event->target_id)) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     switch (fwk_id_get_event_idx(event->id)) {
@@ -748,21 +748,21 @@ static int clock_process_event(const str
     case (unsigned int)MOD_CLOCK_EVENT_IDX_GET_RATE_REQUEST:
     case (unsigned int)MOD_CLOCK_EVENT_IDX_SET_STATE_REQUEST:
     case (unsigned int)MOD_CLOCK_EVENT_IDX_GET_STATE_REQUEST:
-        return process_request_event(event, resp_event);
+	return process_request_event(event, resp_event);
 
 #ifdef BUILD_HAS_CLOCK_TREE_MGMT
     case (unsigned int)CLOCK_EVENT_IDX_SET_STATE_PRE_REQUEST:
-        return clock_management_process_state(event);
+	return clock_management_process_state(event);
 
     case (unsigned int)CLOCK_EVENT_IDX_SET_RATE_PRE_REQUEST:
-        return clock_management_process_rate(event);
+	return clock_management_process_rate(event);
 #endif
 
     case (unsigned int)CLOCK_EVENT_IDX_RESPONSE:
-        return process_response_event(event);
+	return process_response_event(event);
 
     default:
-        return FWK_E_PANIC;
+	return FWK_E_PANIC;
     }
 }
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cmn600/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/cmn600/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cmn600/CMakeLists.txt
@@ -15,9 +15,9 @@ target_include_directories(
 target_sources(
     ${SCP_MODULE_TARGET}
     PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_cmn600.c"
-            "${CMAKE_CURRENT_SOURCE_DIR}/src/cmn600.c"
-            "${CMAKE_CURRENT_SOURCE_DIR}/src/cmn600_ccix.c")
+	    "${CMAKE_CURRENT_SOURCE_DIR}/src/cmn600.c"
+	    "${CMAKE_CURRENT_SOURCE_DIR}/src/cmn600_ccix.c")
 
 target_link_libraries(
     ${SCP_MODULE_TARGET} PRIVATE module-clock module-ppu-v1 module-system-info
-                                 module-timer)
+				 module-timer)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cmn600/include/internal/cmn600_ccix.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/cmn600/include/internal/cmn600_ccix.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cmn600/include/internal/cmn600_ccix.h
@@ -58,23 +58,23 @@ struct cxg_link_regs {
 struct cmn600_cxg_ha_reg {
     FWK_R  uint64_t     CXG_HA_NODE_INFO;
     FWK_RW uint64_t     CXG_HA_ID;
-           uint8_t      RESERVED0[0x80-0x10];
+	   uint8_t      RESERVED0[0x80-0x10];
     FWK_R  uint64_t     CXG_HA_CHILD_INFO;
-           uint8_t      RESERVED1[0x900-0x88];
+	   uint8_t      RESERVED1[0x900-0x88];
     FWK_R  uint64_t     CXG_HA_UNIT_INFO;
-           uint8_t      RESERVED2[0x980-0x908];
+	   uint8_t      RESERVED2[0x980-0x908];
     FWK_RW uint64_t     CXG_HA_SEC_REG_GRP_OVERRIDE;
-           uint8_t      RESERVED3[0xA08-0x988];
+	   uint8_t      RESERVED3[0xA08-0x988];
     FWK_RW uint64_t     CXG_HA_AUX_CTRL;
-           uint8_t      RESERVED4[0xC00-0xA10];
+	   uint8_t      RESERVED4[0xC00-0xA10];
     FWK_RW uint64_t     CXG_HA_RNF_RAID_TO_LDID_REG[8];
     FWK_RW uint64_t     CXG_HA_AGENTID_TO_LINKID_REG[8];
-           uint8_t      RESERVED5[0xD00-0xC80];
+	   uint8_t      RESERVED5[0xD00-0xC80];
     FWK_RW uint64_t     CXG_HA_AGENTID_TO_LINKID_VAL;
     FWK_RW uint64_t     CXG_HA_RNF_RAID_TO_LDID_VAL;
-           uint8_t      RESERVED6[0x1000-0xD10];
-           struct cxg_link_regs LINK_REGS[3];
-           uint8_t      RESERVED7[0x2000-0x1030];
+	   uint8_t      RESERVED6[0x1000-0xD10];
+	   struct cxg_link_regs LINK_REGS[3];
+	   uint8_t      RESERVED7[0x2000-0x1030];
     FWK_RW uint64_t     CXG_HA_PMU_EVENT_SEL;
 };
 
@@ -83,20 +83,20 @@ struct cmn600_cxg_ha_reg {
  */
 struct cmn600_cxg_ra_reg {
     FWK_R  uint64_t     CXG_RA_NODE_INFO;
-           uint8_t      RESERVED0[0x80-0x8];
+	   uint8_t      RESERVED0[0x80-0x8];
     FWK_R  uint64_t     CXG_RA_CHILD_INFO;
-           uint8_t      RESERVED1[0x900-0x88];
+	   uint8_t      RESERVED1[0x900-0x88];
     FWK_R  uint64_t     CXG_RA_UNIT_INFO;
-           uint8_t      RESERVED2[0x980-0x908];
+	   uint8_t      RESERVED2[0x980-0x908];
     FWK_RW uint64_t     CXG_RA_SEC_REG_GRP_OVERRIDE;
-           uint8_t      RESERVED3[0xA00-0x988];
+	   uint8_t      RESERVED3[0xA00-0x988];
     FWK_RW uint64_t     CXG_RA_CFG_CTRL;
     FWK_RW uint64_t     CXG_RA_AUX_CTRL;
-           uint8_t      RESERVED4[0xDA8-0xA10];
+	   uint8_t      RESERVED4[0xDA8-0xA10];
     FWK_RW uint64_t     CXG_RA_SAM_ADDR_REGION_REG[8];
-           uint8_t      RESERVED5[0xE00-0xDE8];
+	   uint8_t      RESERVED5[0xE00-0xDE8];
     FWK_RW uint64_t     CXG_RA_SAM_MEM_REGION_LIMIT_REG[8];
-           uint8_t      RESERVED6[0xE60-0xE40];
+	   uint8_t      RESERVED6[0xE60-0xE40];
     FWK_RW uint64_t     CXG_RA_AGENTID_TO_LINKID_REG[8];
     FWK_RW uint64_t     CXG_RA_RNF_LDID_TO_RAID_REG[8];
     FWK_RW uint64_t     CXG_RA_RNI_LDID_TO_RAID_REG[4];
@@ -105,9 +105,9 @@ struct cmn600_cxg_ra_reg {
     FWK_RW uint64_t     CXG_RA_RNF_LDID_TO_RAID_VAL;
     FWK_RW uint64_t     CXG_RA_RNI_LDID_TO_RAID_VAL;
     FWK_RW uint64_t     CXG_RA_RND_LDID_TO_RAID_VAL;
-           uint8_t      RESERVED7[0x1000-0xF40];
-           struct cxg_link_regs LINK_REGS[3];
-           uint8_t      RESERVED8[0x2000-0x1030];
+	   uint8_t      RESERVED7[0x1000-0xF40];
+	   struct cxg_link_regs LINK_REGS[3];
+	   uint8_t      RESERVED8[0x2000-0x1030];
     FWK_RW uint64_t     CXG_RA_PMU_EVENT_SEL;
 };
 
@@ -116,20 +116,20 @@ struct cmn600_cxg_ra_reg {
  */
 struct cmn600_cxla_reg {
     FWK_R  uint64_t     CXLA_NODE_INFO;
-           uint8_t      RESERVED0[0x80-0x8];
+	   uint8_t      RESERVED0[0x80-0x8];
     FWK_R  uint64_t     CXLA_CHILD_INFO;
-           uint8_t      RESERVED1[0x900-0x88];
+	   uint8_t      RESERVED1[0x900-0x88];
     FWK_R  uint64_t     CXLA_UNIT_INFO;
-           uint8_t      RESERVED2[0x980-0x908];
+	   uint8_t      RESERVED2[0x980-0x908];
     FWK_RW uint64_t     CXLA_SEC_REG_GRP_OVERRIDE;
-           uint8_t      RESERVED3[0xA08-0x988];
+	   uint8_t      RESERVED3[0xA08-0x988];
     FWK_RW uint64_t     CXLA_AUX_CTRL;
-           uint8_t      RESERVED4[0xC00-0xA10];
+	   uint8_t      RESERVED4[0xC00-0xA10];
     FWK_R  uint64_t     CXLA_CCIX_PROP_CAPABILITIES;
     FWK_RW uint64_t     CXLA_CCIX_PROP_CONFIGURED;
     FWK_R  uint64_t     CXLA_TX_CXS_ATTR_CAPABILITIES;
     FWK_R  uint64_t     CXLA_RX_CXS_ATTR_CAPABILITIES;
-           uint8_t      RESERVED5[0xC30-0xC20];
+	   uint8_t      RESERVED5[0xC30-0xC20];
     FWK_RW uint64_t     CXLA_AGENTID_TO_LINKID_REG[8];
     FWK_RW uint64_t     CXLA_AGENTID_TO_LINKID_VAL;
     FWK_RW uint64_t     CXLA_LINKID_TO_PCIE_BUS_NUM;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cmn600/src/cmn600.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/cmn600/src/cmn600.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cmn600/src/cmn600.c
@@ -49,7 +49,7 @@ unsigned int get_node_logical_id(void *n
 {
     struct node_header *node = node_base;
     return (node->NODE_INFO & CMN600_NODE_INFO_LOGICAL_ID) >>
-            CMN600_NODE_INFO_LOGICAL_ID_POS;
+	    CMN600_NODE_INFO_LOGICAL_ID_POS;
 }
 
 void *get_child_node(uintptr_t base, void *node_base, unsigned int child_index)
@@ -74,12 +74,12 @@ unsigned int get_child_node_id(void *nod
     unsigned int device_id;
 
     node_pointer = (node->CHILD_POINTER[child_index] &
-                    CMN600_CHILD_POINTER_EXT_NODE_POINTER) >>
-                    CMN600_CHILD_POINTER_EXT_NODE_POINTER_POS;
+		    CMN600_CHILD_POINTER_EXT_NODE_POINTER) >>
+		    CMN600_CHILD_POINTER_EXT_NODE_POINTER_POS;
 
     device_id = (((node_pointer >> 6) & 0xff) << 3) |
-                ((node_pointer & 0x1) << 2) |
-                ((node_pointer >> 2) & 0x3);
+		((node_pointer & 0x1) << 2) |
+		((node_pointer >> 2) & 0x3);
 
     return device_id;
 }
@@ -87,8 +87,8 @@ unsigned int get_child_node_id(void *nod
 unsigned int get_cmn600_revision(struct cmn600_cfgm_reg *root)
 {
     return (
-        (root->PERIPH_ID[1] & CMN600_PERIPH_ID_2_MASK) >>
-        CMN600_PERIPH_ID_2_REV_POS);
+	(root->PERIPH_ID[1] & CMN600_PERIPH_ID_2_MASK) >>
+	CMN600_PERIPH_ID_2_REV_POS);
 }
 
 const char *get_cmn600_revision_name(struct cmn600_cfgm_reg *root)
@@ -97,8 +97,8 @@ const char *get_cmn600_revision_name(str
 
     revision = get_cmn600_revision(root);
     if (revision > CMN600_PERIPH_ID_UNKNOWN_REV) {
-        revision = CMN600_PERIPH_ID_UNKNOWN_REV;
-        fwk_unexpected();
+	revision = CMN600_PERIPH_ID_UNKNOWN_REV;
+	fwk_unexpected();
     }
 
     return cmn600_rev_to_name[revision];
@@ -107,7 +107,7 @@ const char *get_cmn600_revision_name(str
 bool is_cal_mode_supported(struct cmn600_cfgm_reg *root)
 {
     return (get_cmn600_revision(root) >= CMN600_PERIPH_ID_2_REV_R2_P0) ?
-            true : false;
+	    true : false;
 }
 
 bool is_child_external(void *node_base, unsigned int child_index)
@@ -119,14 +119,14 @@ bool is_child_external(void *node_base,
 bool get_port_number(unsigned int child_node_id)
 {
     return (child_node_id >> CMN600_NODE_ID_PORT_POS) &
-        CMN600_NODE_ID_PORT_MASK;
+	CMN600_NODE_ID_PORT_MASK;
 }
 
 unsigned int get_device_type(void *mxp_base, bool port)
 {
     struct cmn600_mxp_reg *mxp = mxp_base;
     return mxp->PORT_CONNECT_INFO[port] &
-        CMN600_MXP_PORT_CONNECT_INFO_DEVICE_TYPE_MASK;
+	CMN600_MXP_PORT_CONNECT_INFO_DEVICE_TYPE_MASK;
 }
 
 uint64_t sam_encode_region_size(uint64_t size)
@@ -188,12 +188,12 @@ const char *get_node_type_name(enum node
 {
     /* Base node IDs */
     if (node_type <= NODE_TYPE_RN_SAM)
-        return type_to_name[node_type];
+	return type_to_name[node_type];
 
     /* CML node IDs */
     if ((node_type >= NODE_TYPE_CML_BASE) &&
-        (node_type <= NODE_TYPE_CXLA))
-        return type_to_name_cml[node_type - NODE_TYPE_CML_BASE];
+	(node_type <= NODE_TYPE_CXLA))
+	return type_to_name_cml[node_type - NODE_TYPE_CML_BASE];
 
     /* Invalid node IDs */
     return type_to_name[NODE_TYPE_INVALID];
@@ -203,7 +203,7 @@ unsigned int get_node_pos_x(void *node_b
 {
     struct node_header *node = node_base;
     return (get_node_id(node) >> (CMN600_NODE_ID_Y_POS + encoding_bits)) &
-            mask_bits;
+	    mask_bits;
 }
 
 unsigned int get_node_pos_y(void *node_base)
@@ -230,14 +230,14 @@ struct cmn600_cfgm_reg *get_root_node(ui
     mask_bits = (1 << encoding_bits) - 1;
     node_pos_y = (hnd_node_id >> CMN600_NODE_ID_Y_POS) & mask_bits;
     node_pos_x = (hnd_node_id >> (CMN600_NODE_ID_Y_POS + encoding_bits)) &
-                    mask_bits;
+		    mask_bits;
     node_port = (hnd_node_id >> CMN600_NODE_ID_PORT_POS) &
-                CMN600_NODE_ID_PORT_MASK;
+		CMN600_NODE_ID_PORT_MASK;
 
     /* Calculate node address offset */
     offset = (node_pos_y << CMN600_ROOT_NODE_OFFSET_Y_POS) |
-             (node_pos_x << (CMN600_ROOT_NODE_OFFSET_Y_POS + encoding_bits)) |
-             (node_port << CMN600_ROOT_NODE_OFFSET_PORT_POS);
+	     (node_pos_x << (CMN600_ROOT_NODE_OFFSET_Y_POS + encoding_bits)) |
+	     (node_port << CMN600_ROOT_NODE_OFFSET_PORT_POS);
 
     return (struct cmn600_cfgm_reg *)(base + offset);
 }
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cmn600/src/cmn600.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/cmn600/src/cmn600.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cmn600/src/cmn600.h
@@ -57,9 +57,9 @@ enum device_type {
 /* Common node header */
 struct node_header {
     FWK_R uint64_t NODE_INFO;
-          uint8_t  RESERVED0[0x80 - 0x8];
+	  uint8_t  RESERVED0[0x80 - 0x8];
     FWK_R uint64_t CHILD_INFO;
-          uint8_t  RESERVED1[0x100 - 0x88];
+	  uint8_t  RESERVED1[0x100 - 0x88];
     FWK_R uint64_t CHILD_POINTER[256];
 };
 
@@ -80,26 +80,26 @@ enum sam_node_type {
  */
 struct cmn600_rnsam_reg {
     FWK_R  uint64_t NODE_INFO;
-           uint8_t  RESERVED0[0x80 - 0x8];
+	   uint8_t  RESERVED0[0x80 - 0x8];
     FWK_R  uint64_t CHILD_INFO;
-           uint8_t  RESERVED1[0x900 - 0x88];
+	   uint8_t  RESERVED1[0x900 - 0x88];
     FWK_R  uint64_t UNIT_INFO;
-           uint8_t  RESERVED2[0x980 - 0x908];
+	   uint8_t  RESERVED2[0x980 - 0x908];
     FWK_RW uint64_t SECURE_REGISTER_GROUPS_OVERRIDE;
-           uint8_t  RESERVED3[0xC00 - 0x988];
+	   uint8_t  RESERVED3[0xC00 - 0x988];
     FWK_RW uint64_t STATUS;
     FWK_RW uint64_t NON_HASH_MEM_REGION[5];
     FWK_RW uint64_t NON_HASH_TGT_NODEID[3];
     FWK_RW uint64_t SYS_CACHE_GRP_REGION[2];
     FWK_RW uint64_t SYS_CACHE_GRP_HN_NODEID[8];
     FWK_RW uint64_t SYS_CACHE_GRP_NOHASH_NODEID;
-           uint8_t  RESERVED4[0xCE0 - 0xCA0];
+	   uint8_t  RESERVED4[0xCE0 - 0xCA0];
     FWK_RW uint64_t NON_HASH_TGT_NODEID_GRP2[2];
-           uint8_t  RESERVED5[0xD00 - 0xCF0];
+	   uint8_t  RESERVED5[0xD00 - 0xCF0];
     FWK_RW uint64_t SYS_CACHE_GRP_HN_COUNT;
     FWK_RW uint64_t SYS_CACHE_GRP_SN_NODEID[8];
     FWK_RW uint64_t SYS_CACHE_GRP_SN_SAM_CFG[2];
-           uint8_t  RESERVED6[0xF10 - 0xD58];
+	   uint8_t  RESERVED6[0xF10 - 0xD58];
     FWK_RW uint64_t SYS_CACHE_GRP_CAL_MODE;
 };
 
@@ -108,37 +108,37 @@ struct cmn600_rnsam_reg {
  */
 struct cmn600_dt_reg {
     FWK_R  uint64_t NODE_INFO;
-           uint8_t  RESERVED0[0x80 - 0x8];
+	   uint8_t  RESERVED0[0x80 - 0x8];
     FWK_R  uint64_t CHILD_INFO;
-           uint8_t  RESERVED1[0x980 - 0x88];
+	   uint8_t  RESERVED1[0x980 - 0x88];
     FWK_RW uint64_t SECURE_ACCESS;
-           uint8_t  RESERVED2[0xA00 - 0x988];
+	   uint8_t  RESERVED2[0xA00 - 0x988];
     FWK_RW uint64_t DTC_CTL;
-           uint8_t  RESERVED3[0xA10 - 0xA08];
+	   uint8_t  RESERVED3[0xA10 - 0xA08];
     FWK_R  uint64_t TRIGGER_STATUS;
-           uint8_t  RESERVED4[0xA20 - 0xA18];
+	   uint8_t  RESERVED4[0xA20 - 0xA18];
     FWK_W  uint64_t TRIGGER_STATUS_CLR;
-           uint8_t  RESERVED5[0xA30 - 0xA28];
+	   uint8_t  RESERVED5[0xA30 - 0xA28];
     FWK_RW uint64_t TRACE_CONTROL;
-           uint8_t  RESERVED6[0xA40 - 0xA38];
+	   uint8_t  RESERVED6[0xA40 - 0xA38];
     FWK_R  uint64_t DBG_ID;
     FWK_RW uint64_t TRACE_ID;
-           uint8_t  RESERVED7[0x2000 - 0xA50];
+	   uint8_t  RESERVED7[0x2000 - 0xA50];
     FWK_RW uint64_t PMEVCNT[8];
     FWK_RW uint64_t PMCCNTR;
-           uint8_t  RESERVED8[0x2050 - 0x2048];
+	   uint8_t  RESERVED8[0x2050 - 0x2048];
     FWK_RW uint64_t PMEVCNTSR[8];
     FWK_RW uint64_t PMCCNTRSR;
-           uint8_t  RESERVED9[0x2100 - 0x2098];
+	   uint8_t  RESERVED9[0x2100 - 0x2098];
     FWK_RW uint64_t PMCR;
     FWK_RW uint64_t PMEVCNT_LOCALNUM;
-           uint8_t  RESERVED10[0x2118 - 0x2110];
+	   uint8_t  RESERVED10[0x2118 - 0x2110];
     FWK_R  uint64_t PMOVSR;
     FWK_W  uint64_t PMOVSR_CLR;
     FWK_R  uint64_t PMSR;
     FWK_W  uint64_t PMSR_REQ;
     FWK_W  uint64_t PMSR_CLR;
-           uint8_t  RESERVED11[0x2DA0 - 0x2140];
+	   uint8_t  RESERVED11[0x2DA0 - 0x2140];
     FWK_RW uint64_t CLAIM;
     FWK_R  uint64_t DEVAFF;
     FWK_R  uint64_t LSR;
@@ -158,43 +158,43 @@ struct cmn600_dt_reg {
  */
 struct cmn600_dn_reg {
     FWK_R  uint64_t NODE_INFO;
-           uint8_t  RESERVED0[0x80 - 0x8];
+	   uint8_t  RESERVED0[0x80 - 0x8];
     FWK_R  uint64_t CHILD_INFO;
-           uint8_t  RESERVED1[0x900 - 0x88];
+	   uint8_t  RESERVED1[0x900 - 0x88];
     FWK_R  uint64_t BUILD_INFO;
-           uint8_t  RESERVED2[0x980 - 0x908];
+	   uint8_t  RESERVED2[0x980 - 0x908];
     FWK_RW uint64_t SECURE_REGISTER_GROUPS_OVERRIDE;
-           uint8_t  RESERVED3[0xA00 - 0x988];
+	   uint8_t  RESERVED3[0xA00 - 0x988];
     FWK_RW uint64_t AUX_CTL;
-           uint8_t  RESERVED4[0xC00 - 0xA08];
+	   uint8_t  RESERVED4[0xC00 - 0xA08];
     FWK_RW uint64_t VMF0_CTRL;
     FWK_RW uint64_t VMF0_RNF[4];
-           uint8_t  RESERVED5[0xC40 - 0xC28];
+	   uint8_t  RESERVED5[0xC40 - 0xC28];
     FWK_RW uint64_t VMF0_RND;
-           uint8_t  RESERVED6[0xC60 - 0xC48];
+	   uint8_t  RESERVED6[0xC60 - 0xC48];
     FWK_RW uint64_t VMF0_RNC;
-           uint8_t  RESERVED7[0xC80 - 0xC68];
+	   uint8_t  RESERVED7[0xC80 - 0xC68];
     FWK_RW uint64_t VMF1_CTRL;
     FWK_RW uint64_t VMF1_RNF[4];
-           uint8_t  RESERVED8[0xCC0 - 0xCA8];
+	   uint8_t  RESERVED8[0xCC0 - 0xCA8];
     FWK_RW uint64_t VMF1_RND;
-           uint8_t  RESERVED9[0xCE0 - 0xCC8];
+	   uint8_t  RESERVED9[0xCE0 - 0xCC8];
     FWK_RW uint64_t VMF1_RNC;
-           uint8_t  RESERVED10[0xD00 - 0xCE8];
+	   uint8_t  RESERVED10[0xD00 - 0xCE8];
     FWK_RW uint64_t VMF2_CTRL;
     FWK_RW uint64_t VMF2_RNF[4];
-           uint8_t  RESERVED11[0xD40 - 0xD28];
+	   uint8_t  RESERVED11[0xD40 - 0xD28];
     FWK_RW uint64_t VMF2_RND;
-           uint8_t  RESERVED12[0xD60 - 0xD48];
+	   uint8_t  RESERVED12[0xD60 - 0xD48];
     FWK_RW uint64_t VMF2_RNC;
-           uint8_t  RESERVED13[0xD80 - 0xD68];
+	   uint8_t  RESERVED13[0xD80 - 0xD68];
     FWK_RW uint64_t VMF3_CTRL;
     FWK_RW uint64_t VMF3_RNF[4];
-           uint8_t  RESERVED14[0xDC0 - 0xDA8];
+	   uint8_t  RESERVED14[0xDC0 - 0xDA8];
     FWK_RW uint64_t VMF3_RND;
-           uint8_t  RESERVED15[0xDE0 - 0xDC8];
+	   uint8_t  RESERVED15[0xDE0 - 0xDC8];
     FWK_RW uint64_t VMF3_RNC;
-           uint8_t  RESERVED16[0x2000 - 0xDE8];
+	   uint8_t  RESERVED16[0x2000 - 0xDE8];
     FWK_RW uint64_t PMU_EVEN_SEL;
 };
 
@@ -214,22 +214,22 @@ struct cmn600_qos_reg {
  */
 struct cmn600_rnd_reg {
     FWK_R  uint64_t NODE_INFO;
-           uint8_t  RESERVED0[0x80 - 0x8];
+	   uint8_t  RESERVED0[0x80 - 0x8];
     FWK_R  uint64_t CHILD_INFO;
-           uint8_t  RESERVED1[0x900 - 0x88];
+	   uint8_t  RESERVED1[0x900 - 0x88];
     FWK_R  uint64_t UNIT_INFO;
-           uint8_t  RESERVED2[0x980 - 0x908];
+	   uint8_t  RESERVED2[0x980 - 0x908];
     FWK_RW uint64_t SECURE_REGISTER_GROUPS_OVERRIDE;
-           uint8_t  RESERVED3[0xA00 - 0x988];
+	   uint8_t  RESERVED3[0xA00 - 0x988];
     FWK_RW uint64_t CFG_CTL;
     FWK_RW uint64_t AUX_CTL;
     FWK_RW uint64_t S_PORT_CONTROL[3];
-           uint8_t  RESERVED4[0xA80 - 0xA28];
+	   uint8_t  RESERVED4[0xA80 - 0xA28];
     struct cmn600_qos_reg S_QOS[3];
-           uint8_t  RESERVED5[0x1000 - 0xAE0];
+	   uint8_t  RESERVED5[0x1000 - 0xAE0];
     FWK_RW uint64_t SYSCOREQ_CTL;
     FWK_R  uint64_t SYSCOACK_STATUS;
-           uint8_t  RESERVED6[0x2000 - 0x1010];
+	   uint8_t  RESERVED6[0x2000 - 0x1010];
     FWK_RW uint64_t PMU_EVENT_SEL;
 };
 
@@ -238,66 +238,66 @@ struct cmn600_rnd_reg {
  */
 struct cmn600_hnf_reg {
     FWK_R  uint64_t NODE_INFO;
-           uint8_t  RESERVED0[0x80 - 0x8];
+	   uint8_t  RESERVED0[0x80 - 0x8];
     FWK_R  uint64_t CHILD_INFO;
-           uint8_t  RESERVED1[0x900 - 0x88];
+	   uint8_t  RESERVED1[0x900 - 0x88];
     FWK_R  uint64_t UNIT_INFO;
-           uint8_t  RESERVED2[0x980 - 0x908];
+	   uint8_t  RESERVED2[0x980 - 0x908];
     FWK_RW uint64_t SECURE_REGISTER_GROUPS_OVERRIDE;
-           uint8_t  RESERVED3[0xA00 - 0x988];
+	   uint8_t  RESERVED3[0xA00 - 0x988];
     FWK_RW uint64_t CFG_CTL;
     FWK_RW uint64_t AUX_CTL;
-           uint8_t  RESERVED4[0xA80 - 0xA10];
+	   uint8_t  RESERVED4[0xA80 - 0xA10];
     FWK_R  uint64_t QOS_BAND;
     FWK_RW uint64_t QOS_RESERVATION;
     FWK_RW uint64_t RN_STARVATION;
-           uint8_t  RESERVED5[0xB80 - 0xA98];
+	   uint8_t  RESERVED5[0xB80 - 0xA98];
     FWK_W  uint64_t CFG_L3SF_DBGRD;
     FWK_R  uint64_t L3_CACHE_ACCESS_L3_TAG;
     FWK_R  uint64_t L3_CACHE_ACCESS_L3_DATA;
     FWK_R  uint64_t L3_CACHE_ACCESS_SF_DATA;
-           uint8_t  RESERVED6[0xC00 - 0xBA0];
+	   uint8_t  RESERVED6[0xC00 - 0xBA0];
     FWK_RW uint64_t L3_LOCK_WAYS;
     FWK_RW uint64_t L3_LOCK_BASE[4];
-           uint8_t  RESERVED7[0xC30 - 0xC28];
+	   uint8_t  RESERVED7[0xC30 - 0xC28];
     FWK_RW uint64_t RNID_REGION_VEC;
     FWK_RW uint64_t RNF_REGION_VEC;
-           uint8_t  RESERVED8[0xD00 - 0xC40];
+	   uint8_t  RESERVED8[0xD00 - 0xC40];
     FWK_RW uint64_t SAM_CONTROL;
     FWK_RW uint64_t SAM_MEMREGION[2];
     FWK_RW uint64_t SAM_SN_PROPERTIES;
     FWK_RW uint64_t SAM_6SN_NODEID;
     FWK_RW uint64_t RN_PHYS_ID[32];
-           uint8_t  RESERVED9[0xF00 - 0xE28];
+	   uint8_t  RESERVED9[0xF00 - 0xE28];
     FWK_RW uint64_t SF_CXG_BLOCKED_WAYS;
-           uint8_t  RESERVED10[0xF10 - 0xF08];
+	   uint8_t  RESERVED10[0xF10 - 0xF08];
     FWK_RW uint64_t CML_PORT_AGGR_GRP0_ADD_MASK;
-           uint8_t  RESERVED11[0xF28 - 0xF18];
+	   uint8_t  RESERVED11[0xF28 - 0xF18];
     FWK_RW uint64_t CML_PORT_AGGR_GRP0_ADD_REG;
-           uint8_t  RESERVED12[0x1000 - 0xF30];
+	   uint8_t  RESERVED12[0x1000 - 0xF30];
     FWK_RW uint64_t PPU_PWPR;
     FWK_R  uint64_t PPU_PWSR;
-           uint8_t  RESERVED13[0x1014 - 0x1010];
+	   uint8_t  RESERVED13[0x1014 - 0x1010];
     FWK_R  uint32_t PPU_MISR;
-           uint8_t  RESERVED14[0x1100 - 0x1018];
+	   uint8_t  RESERVED14[0x1100 - 0x1018];
     FWK_RW uint64_t PPU_DYN_RET_THRESHOLD;
-           uint8_t  RESERVED15[0x1FB0 - 0x1108];
+	   uint8_t  RESERVED15[0x1FB0 - 0x1108];
     FWK_R  uint32_t PPU_IDR[2];
-           uint8_t  RESERVED16[0x1FC8 - 0x1FB8];
+	   uint8_t  RESERVED16[0x1FC8 - 0x1FB8];
     FWK_R  uint32_t PPU_IIDR;
     FWK_R  uint32_t PPU_AIDR;
-           uint8_t  RESERVED17[0x2000 - 0x1FD0];
+	   uint8_t  RESERVED17[0x2000 - 0x1FD0];
     FWK_RW uint64_t PMU_EVENT_SEL;
-           uint8_t  RESERVED18[0x3000 - 0x2008];
+	   uint8_t  RESERVED18[0x3000 - 0x2008];
     FWK_R  uint64_t ERRFR;
     FWK_RW uint64_t ERRCTLR;
     FWK_RW uint64_t ERRSTATUS;
     FWK_RW uint64_t ERRADDR;
     FWK_RW uint64_t ERRMISC;
-           uint8_t  RESERVED19[0x3030 - 0x3028];
+	   uint8_t  RESERVED19[0x3030 - 0x3028];
     FWK_RW uint64_t RN_ERR_INJ;
     FWK_W  uint64_t RN_BYTE_PAR_ERR_INJ;
-           uint8_t  RESERVED20[0x3100 - 0x3040];
+	   uint8_t  RESERVED20[0x3100 - 0x3040];
     FWK_R  uint64_t ERRFR_NS;
     FWK_RW uint64_t ERRCTLR_NS;
     FWK_RW uint64_t ERRSTATUS_NS;
@@ -310,21 +310,21 @@ struct cmn600_hnf_reg {
  */
 struct cmn600_sbsx_reg {
     FWK_R  uint64_t NODE_INFO;
-           uint8_t  RESERVED0[0x80 - 0x8];
+	   uint8_t  RESERVED0[0x80 - 0x8];
     FWK_R  uint64_t CHILD_INFO;
-           uint8_t  RESERVED1[0x900 - 0x88];
+	   uint8_t  RESERVED1[0x900 - 0x88];
     FWK_R  uint64_t UNIT_INFO;
-           uint8_t  RESERVED2[0xA00 - 0x908];
+	   uint8_t  RESERVED2[0xA00 - 0x908];
     FWK_RW uint64_t AUX_CTL;
-           uint8_t  RESERVED3[0x2000 - 0xA08];
+	   uint8_t  RESERVED3[0x2000 - 0xA08];
     FWK_RW uint64_t PMU_EVENT_SEL;
-           uint8_t  RESERVED4[0x3000 - 0x2008];
+	   uint8_t  RESERVED4[0x3000 - 0x2008];
     FWK_R  uint64_t ERRFR;
     FWK_RW uint64_t ERRCTL;
     FWK_RW uint64_t ERRSTATUS;
     FWK_RW uint64_t ERRADDR;
     FWK_RW uint64_t ERRMISC;
-           uint8_t  RESERVED5[0x3100 - 0x3028];
+	   uint8_t  RESERVED5[0x3100 - 0x3028];
     FWK_R  uint64_t ERRFR_NS;
     FWK_RW uint64_t ERRCTL_NS;
     FWK_RW uint64_t ERRSTATUS_NS;
@@ -339,26 +339,26 @@ struct cmn600_cfgm_reg {
     FWK_R  uint64_t NODE_INFO;
     FWK_RW uint64_t PERIPH_ID[4];
     FWK_RW uint64_t COMPONENT_ID[2];
-           uint8_t  RESERVED0[0x80 - 0x38];
+	   uint8_t  RESERVED0[0x80 - 0x38];
     FWK_R  uint64_t CHILD_INFO;
-           uint8_t  RESERVED1[0x100 - 0x88];
+	   uint8_t  RESERVED1[0x100 - 0x88];
     FWK_R  uint64_t CHILD_POINTER[256];
     FWK_R  uint64_t INFO_GLOBAL;
-           uint8_t  RESERVED2[0x980 - 0x908];
+	   uint8_t  RESERVED2[0x980 - 0x908];
     FWK_RW uint64_t SECURE_ACCESS;
     FWK_RW uint64_t SECURE_REGISTER_GROUPS_OVERRIDE;
-           uint8_t  RESERVED3[0x1000 - 0x990];
+	   uint8_t  RESERVED3[0x1000 - 0x990];
     FWK_RW uint64_t PPU_INT_MASK;
     FWK_RW uint64_t PPU_INT_STATUS;
-           uint8_t  RESERVED4[0x3000 - 0x1010];
+	   uint8_t  RESERVED4[0x3000 - 0x1010];
     FWK_R  uint64_t ERRGSR[8];
-           uint8_t  RESERVED5[0x3100 - 0x3040];
+	   uint8_t  RESERVED5[0x3100 - 0x3040];
     FWK_R  uint64_t ERRGSR_NS[8];
-           uint8_t  RESERVED6[0x3FA8 - 0x3140];
+	   uint8_t  RESERVED6[0x3FA8 - 0x3140];
     FWK_R  uint64_t ERRDEVAFF;
-           uint8_t  RESERVED7[0x3FB8 - 0x3FB0];
+	   uint8_t  RESERVED7[0x3FB8 - 0x3FB0];
     FWK_R  uint64_t ERRDEVARCH;
-           uint8_t  RESERVED8[0x3FC8 - 0x3FC0];
+	   uint8_t  RESERVED8[0x3FC8 - 0x3FC0];
     FWK_R  uint64_t ERRIDR;
     FWK_R  uint64_t ERRPIDR45;
     FWK_R  uint64_t ERRPIDR67;
@@ -374,33 +374,33 @@ struct cmn600_cfgm_reg {
 struct cmn600_mxp_reg {
     FWK_R  uint64_t NODE_INFO;
     FWK_R  uint64_t PORT_CONNECT_INFO[2];
-           uint8_t  RESERVED0[0x80 - 0x18];
+	   uint8_t  RESERVED0[0x80 - 0x18];
     FWK_R  uint64_t CHILD_INFO;
-           uint8_t  RESERVED1[0x100 - 0x88];
+	   uint8_t  RESERVED1[0x100 - 0x88];
     FWK_R  uint64_t CHILD_POINTER[16];
-           uint8_t  RESERVED2[0x900 - 0x180];
+	   uint8_t  RESERVED2[0x900 - 0x180];
     FWK_R  uint64_t P0_INFO;
     FWK_R  uint64_t P1_INFO;
-           uint8_t  RESERVED3[0x980 - 0x910];
+	   uint8_t  RESERVED3[0x980 - 0x910];
     FWK_RW uint64_t SECURE_REGISTER_GROUPS_OVERRIDE;
-           uint8_t  RESERVED4[0xA00 - 0x988];
+	   uint8_t  RESERVED4[0xA00 - 0x988];
     FWK_RW uint64_t AUX_CTL;
-           uint8_t  RESERVED5[0xA80 - 0xA08];
+	   uint8_t  RESERVED5[0xA80 - 0xA08];
     struct cmn600_qos_reg P_QOS[2];
-           uint8_t  RESERVED6[0x1000 - 0xAC0];
+	   uint8_t  RESERVED6[0x1000 - 0xAC0];
     FWK_RW uint64_t P_SYSCOREQ_CTL[2];
     FWK_R  uint64_t P_SYSCOACK_STATUS[2];
-           uint8_t  RESERVED7[0x2000 - 0x1020];
+	   uint8_t  RESERVED7[0x2000 - 0x1020];
     FWK_RW uint64_t PMU_EVENT_SEL;
-           uint8_t  RESERVED8[0x2100 - 0x2008];
+	   uint8_t  RESERVED8[0x2100 - 0x2008];
     FWK_RW uint64_t DTM_CONTROL;
-           uint8_t  RESERVED9[0x2118 - 0x2108];
+	   uint8_t  RESERVED9[0x2118 - 0x2108];
     FWK_R  uint64_t DTM_FIFO_ENTRY_READY;
     FWK_R  uint64_t DTM_FIFO_ENTRY0[3];
     FWK_R  uint64_t DTM_FIFO_ENTRY1[3];
     FWK_R  uint64_t DTM_FIFO_ENTRY2[3];
     FWK_R  uint64_t DTM_FIFO_ENTRY3[3];
-           uint8_t  RESERVED10[0x21A0 - 0x2180];
+	   uint8_t  RESERVED10[0x21A0 - 0x2180];
     FWK_RW uint64_t DTM_WP0_CONFIG;
     FWK_RW uint64_t DTM_WP0_VAL;
     FWK_RW uint64_t DTM_WP0_MASK;
@@ -416,21 +416,21 @@ struct cmn600_mxp_reg {
     FWK_RW uint64_t PMSICR;
     FWK_RW uint64_t PMSIRR;
     FWK_RW uint64_t DTM_PMU_CONFIG;
-           uint8_t  RESERVED11[0x2220 - 0x2218];
+	   uint8_t  RESERVED11[0x2220 - 0x2218];
     FWK_RW uint64_t PMEVCNT[4];
     FWK_RW uint64_t PMEVCNTSR[4];
-           uint8_t  RESERVED12[0x3000 - 0x2260];
+	   uint8_t  RESERVED12[0x3000 - 0x2260];
     FWK_R  uint64_t ERRFR;
     FWK_RW uint64_t ERRCTL;
     FWK_RW uint64_t ERRSTATUS;
-           uint8_t  RESERVED13[0x3028 - 0x3018];
+	   uint8_t  RESERVED13[0x3028 - 0x3018];
     FWK_RW uint64_t ERRMISC;
     FWK_W  uint64_t P_BYTE_PAR_ERR_INJ[2];
-           uint8_t  RESERVED14[0x3100 - 0x3040];
+	   uint8_t  RESERVED14[0x3100 - 0x3040];
     FWK_R  uint64_t ERRFR_NS;
     FWK_RW uint64_t ERRCTL_NS;
     FWK_RW uint64_t ERRSTATUS_NS;
-           uint8_t  RESERVED15[0x3128 - 0x3118];
+	   uint8_t  RESERVED15[0x3128 - 0x3118];
     FWK_RW uint64_t ERRMISC_NS;
 };
 
@@ -439,21 +439,21 @@ struct cmn600_mxp_reg {
  */
 struct cmn600_rni_reg {
     FWK_R  uint64_t NODE_INFO;
-           uint8_t  RESERVED0[0x80 - 0x8];
+	   uint8_t  RESERVED0[0x80 - 0x8];
     FWK_R  uint64_t CHILD_INFO;
-           uint8_t  RESERVED1[0x900 - 0x88];
+	   uint8_t  RESERVED1[0x900 - 0x88];
     FWK_R  uint64_t UNIT_INFO;
-           uint8_t  RESERVED2[0x980 - 0x908];
+	   uint8_t  RESERVED2[0x980 - 0x908];
     FWK_RW uint64_t SECURE_REGISTER_GROUPS_OVERRIDE;
-           uint8_t  RESERVED3[0xA00 - 0x988];
+	   uint8_t  RESERVED3[0xA00 - 0x988];
     FWK_RW uint64_t CFG_CTL;
     FWK_RW uint64_t AUX_CTL;
     FWK_RW uint64_t S0_PORT_CONTRL;
     FWK_RW uint64_t S1_PORT_CONTRL;
     FWK_RW uint64_t S2_PORT_CONTRL;
-           uint8_t  RESERVED4[0xA80 - 0xA28];
+	   uint8_t  RESERVED4[0xA80 - 0xA28];
     struct cmn600_qos_reg S_QOS[3];
-           uint8_t  RESERVED5[0x2000 - 0xAE0];
+	   uint8_t  RESERVED5[0x2000 - 0xAE0];
     FWK_RW uint64_t PMU_EVENT_SEL;
 };
 
@@ -462,26 +462,26 @@ struct cmn600_rni_reg {
  */
 struct cmn600_hni_reg {
     FWK_R  uint64_t NODE_INFO;
-           uint8_t  RESERVED0[0x80 - 0x8];
+	   uint8_t  RESERVED0[0x80 - 0x8];
     FWK_R  uint64_t CHILD_INFO;
-           uint8_t  RESERVED1[0x900 - 0x88];
+	   uint8_t  RESERVED1[0x900 - 0x88];
     FWK_R  uint64_t UNIT_INFO;
-           uint8_t  RESERVED2[0x980 - 0x908];
+	   uint8_t  RESERVED2[0x980 - 0x908];
     FWK_RW uint64_t SECURE_REGISTER_GROUPS_OVERRIDE;
-           uint8_t  RESERVED3[0xA00 - 0x988];
+	   uint8_t  RESERVED3[0xA00 - 0x988];
     FWK_RW uint64_t CFG_CTL;
     FWK_RW uint64_t AUX_CTL;
-           uint8_t  RESERVED4[0xC00 - 0xA10];
+	   uint8_t  RESERVED4[0xC00 - 0xA10];
     FWK_RW uint64_t SAM_ADDRREGION_CFG[4];
-           uint8_t  RESERVED5[0x2000 - 0xC20];
+	   uint8_t  RESERVED5[0x2000 - 0xC20];
     FWK_RW uint64_t PMU_EVENT_SEL;
-           uint8_t  RESERVED6[0x3000 - 0x2008];
+	   uint8_t  RESERVED6[0x3000 - 0x2008];
     FWK_R  uint64_t ERRFR;
     FWK_RW uint64_t ERRCTL;
     FWK_RW uint64_t ERRSTATUS;
     FWK_RW uint64_t ERRADDR;
     FWK_RW uint64_t ERRMISC;
-           uint8_t  RESERVED7[0x3100 - 0x3028];
+	   uint8_t  RESERVED7[0x3100 - 0x3028];
     FWK_R  uint64_t ERRFR_NS;
     FWK_RW uint64_t ERRCTL_NS;
     FWK_RW uint64_t ERRSTATUS_NS;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cmn600/src/cmn600_ccix.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/cmn600/src/cmn600_ccix.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cmn600/src/cmn600_ccix.c
@@ -36,93 +36,93 @@ static bool cxg_link_wait_condition(void
     fwk_assert(data != NULL);
 
     struct cxg_wait_condition_data *wait_data =
-        (struct cxg_wait_condition_data *)data;
+	(struct cxg_wait_condition_data *)data;
     struct cmn600_ctx *ctx = wait_data->ctx;
     link_id = wait_data->link_id;
 
     switch (wait_data->cond) {
     case CXG_LINK_CTRL_EN_BIT_SET:
-        val1 = ctx->cxg_ra_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_CTRL;
-        val2 = ctx->cxg_ha_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_CTRL;
-        return (((val1 & CXG_LINK_CTRL_EN_MASK) &&
-                  (val2 & CXG_LINK_CTRL_EN_MASK)) != 0);
+	val1 = ctx->cxg_ra_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_CTRL;
+	val2 = ctx->cxg_ha_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_CTRL;
+	return (((val1 & CXG_LINK_CTRL_EN_MASK) &&
+		  (val2 & CXG_LINK_CTRL_EN_MASK)) != 0);
 
     case CXG_LINK_CTRL_UP_BIT_CLR:
-        val1 = ctx->cxg_ra_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_CTRL;
-        val2 = ctx->cxg_ha_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_CTRL;
-        return ((((val1 & CXG_LINK_CTRL_UP_MASK) == 0) &&
-                  ((val2 & CXG_LINK_CTRL_UP_MASK) == 0)) != 0);
+	val1 = ctx->cxg_ra_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_CTRL;
+	val2 = ctx->cxg_ha_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_CTRL;
+	return ((((val1 & CXG_LINK_CTRL_UP_MASK) == 0) &&
+		  ((val2 & CXG_LINK_CTRL_UP_MASK) == 0)) != 0);
 
     case CXG_LINK_STATUS_DWN_BIT_SET:
-        val1 = ctx->cxg_ra_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_STATUS;
-        val2 = ctx->cxg_ha_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_STATUS;
-        return (((val1 & CXG_LINK_STATUS_DOWN_MASK) &&
-                  (val2 & CXG_LINK_STATUS_DOWN_MASK)) != 0);
+	val1 = ctx->cxg_ra_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_STATUS;
+	val2 = ctx->cxg_ha_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_STATUS;
+	return (((val1 & CXG_LINK_STATUS_DOWN_MASK) &&
+		  (val2 & CXG_LINK_STATUS_DOWN_MASK)) != 0);
 
     case CXG_LINK_STATUS_DWN_BIT_CLR:
-        val1 = ctx->cxg_ra_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_STATUS;
-        val2 = ctx->cxg_ha_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_STATUS;
-        return ((((val1 & CXG_LINK_STATUS_DOWN_MASK) == 0) &&
-                  ((val2 & CXG_LINK_STATUS_DOWN_MASK) == 0)) != 0);
+	val1 = ctx->cxg_ra_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_STATUS;
+	val2 = ctx->cxg_ha_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_STATUS;
+	return ((((val1 & CXG_LINK_STATUS_DOWN_MASK) == 0) &&
+		  ((val2 & CXG_LINK_STATUS_DOWN_MASK) == 0)) != 0);
 
     case CXG_LINK_STATUS_ACK_BIT_SET:
-        val1 = ctx->cxg_ra_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_STATUS;
-        val2 = ctx->cxg_ha_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_STATUS;
-        return (((val1 & CXG_LINK_STATUS_ACK_MASK) &&
-                (val2 & CXG_LINK_STATUS_ACK_MASK)) != 0);
+	val1 = ctx->cxg_ra_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_STATUS;
+	val2 = ctx->cxg_ha_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_STATUS;
+	return (((val1 & CXG_LINK_STATUS_ACK_MASK) &&
+		(val2 & CXG_LINK_STATUS_ACK_MASK)) != 0);
 
     case CXG_LINK_STATUS_ACK_BIT_CLR:
-        val1 = ctx->cxg_ra_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_STATUS;
-        val2 = ctx->cxg_ha_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_STATUS;
-        return ((((val1 & CXG_LINK_STATUS_ACK_MASK) == 0) &&
-                  ((val2 & CXG_LINK_STATUS_ACK_MASK) == 0)) != 0);
+	val1 = ctx->cxg_ra_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_STATUS;
+	val2 = ctx->cxg_ha_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_STATUS;
+	return ((((val1 & CXG_LINK_STATUS_ACK_MASK) == 0) &&
+		  ((val2 & CXG_LINK_STATUS_ACK_MASK) == 0)) != 0);
 
     case CXG_LINK_STATUS_HA_DVMDOMAIN_ACK_BIT_SET:
-        val1 = ctx->cxg_ha_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_STATUS;
-        return (((val1 & CXG_LINK_STATUS_DVMDOMAIN_ACK_MASK)) != 0);
+	val1 = ctx->cxg_ha_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_STATUS;
+	return (((val1 & CXG_LINK_STATUS_DVMDOMAIN_ACK_MASK)) != 0);
 
     case CXG_LINK_STATUS_RA_DVMDOMAIN_ACK_BIT_SET:
-        val1 = ctx->cxg_ra_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_STATUS;
-        return (((val1 & CXG_LINK_STATUS_DVMDOMAIN_ACK_MASK)) != 0);
+	val1 = ctx->cxg_ra_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_STATUS;
+	return (((val1 & CXG_LINK_STATUS_DVMDOMAIN_ACK_MASK)) != 0);
 
     default:
-        fwk_unexpected();
-        return false;
+	fwk_unexpected();
+	return false;
     }
 }
 
 static int enable_smp_mode(struct cmn600_ctx *ctx)
 {
     if (get_cmn600_revision(ctx->root) == CMN600_PERIPH_ID_2_REV_R2_P0) {
-        /*
-         * CMN-600 revision r2p0 uses read-only bits from the CXG_RA and CXG_HA
-         * registers to determine whether the SMP Mode is enabled in hardware.
-         */
-        if (((ctx->cxg_ra_reg->CXG_RA_UNIT_INFO &
-              CXG_RA_UNIT_INFO_SMP_MODE_RO_MASK) !=
-             CXG_RA_UNIT_INFO_SMP_MODE_RO_MASK) ||
-            ((ctx->cxg_ha_reg->CXG_HA_UNIT_INFO &
-              CXG_HA_UNIT_INFO_SMP_MODE_RO_MASK) !=
-             CXG_HA_UNIT_INFO_SMP_MODE_RO_MASK)) {
-            FWK_LOG_INFO(MOD_NAME "SMP Mode not supported");
-            return FWK_E_SUPPORT;
-        }
+	/*
+	 * CMN-600 revision r2p0 uses read-only bits from the CXG_RA and CXG_HA
+	 * registers to determine whether the SMP Mode is enabled in hardware.
+	 */
+	if (((ctx->cxg_ra_reg->CXG_RA_UNIT_INFO &
+	      CXG_RA_UNIT_INFO_SMP_MODE_RO_MASK) !=
+	     CXG_RA_UNIT_INFO_SMP_MODE_RO_MASK) ||
+	    ((ctx->cxg_ha_reg->CXG_HA_UNIT_INFO &
+	      CXG_HA_UNIT_INFO_SMP_MODE_RO_MASK) !=
+	     CXG_HA_UNIT_INFO_SMP_MODE_RO_MASK)) {
+	    FWK_LOG_INFO(MOD_NAME "SMP Mode not supported");
+	    return FWK_E_SUPPORT;
+	}
 
-        FWK_LOG_INFO(MOD_NAME "SMP Mode supported");
-        return FWK_SUCCESS;
+	FWK_LOG_INFO(MOD_NAME "SMP Mode supported");
+	return FWK_SUCCESS;
     } else if (get_cmn600_revision(ctx->root) >= CMN600_PERIPH_ID_2_REV_R3_P0) {
-        /*
-         * CMN-600 revision r3p0 and above allows the software to configure the
-         * SMP mode
-         */
-        ctx->cxg_ra_reg->CXG_RA_AUX_CTRL |=
-            (1 << CXG_RA_AUX_CTRL_SMP_MODE_RW_SHIFT_VAL);
-        ctx->cxg_ha_reg->CXG_HA_AUX_CTRL |=
-            (1 << CXG_HA_AUX_CTRL_SMP_MODE_RW_SHIFT_VAL);
-        ctx->cxla_reg->CXLA_AUX_CTRL |=
-            ((uint64_t)0x1 << CXLA_AUX_CTRL_SMP_MODE_SHIFT_VAL);
-        FWK_LOG_INFO(MOD_NAME "SMP MODE enabled");
-        return FWK_SUCCESS;
+	/*
+	 * CMN-600 revision r3p0 and above allows the software to configure the
+	 * SMP mode
+	 */
+	ctx->cxg_ra_reg->CXG_RA_AUX_CTRL |=
+	    (1 << CXG_RA_AUX_CTRL_SMP_MODE_RW_SHIFT_VAL);
+	ctx->cxg_ha_reg->CXG_HA_AUX_CTRL |=
+	    (1 << CXG_HA_AUX_CTRL_SMP_MODE_RW_SHIFT_VAL);
+	ctx->cxla_reg->CXLA_AUX_CTRL |=
+	    ((uint64_t)0x1 << CXLA_AUX_CTRL_SMP_MODE_SHIFT_VAL);
+	FWK_LOG_INFO(MOD_NAME "SMP MODE enabled");
+	return FWK_SUCCESS;
     }
 
     /*
@@ -144,12 +144,12 @@ static void program_cxg_ra_rnf_ldid_to_r
 
     /* Adding raid_value into LDID-to-RAID register */
     ctx->cxg_ra_reg->CXG_RA_RNF_LDID_TO_RAID_REG[reg_offset] |=
-        ((uint64_t)raid <<
-            (ldid_value_offset * NUM_BITS_RESERVED_FOR_RAID));
+	((uint64_t)raid <<
+	    (ldid_value_offset * NUM_BITS_RESERVED_FOR_RAID));
 
     /* Set corresponding valid bit */
     ctx->cxg_ra_reg->CXG_RA_RNF_LDID_TO_RAID_VAL |=
-        ((uint64_t)0x1 << ldid_value);
+	((uint64_t)0x1 << ldid_value);
 
     /* Increment raid_value */
     ctx->raid_value++;
@@ -167,12 +167,12 @@ static void program_cxg_ra_rni_ldid_to_r
 
     /* Adding raid_value into LDID-to-RAID register */
     ctx->cxg_ra_reg->CXG_RA_RNI_LDID_TO_RAID_REG[reg_offset] |=
-        ((uint64_t)raid <<
-            (ldid_value_offset * NUM_BITS_RESERVED_FOR_RAID));
+	((uint64_t)raid <<
+	    (ldid_value_offset * NUM_BITS_RESERVED_FOR_RAID));
 
     /* Set corresponding valid bit */
     ctx->cxg_ra_reg->CXG_RA_RNI_LDID_TO_RAID_VAL |=
-        ((uint64_t)0x1 << ldid_value);
+	((uint64_t)0x1 << ldid_value);
 
     /* Increment raid_value */
     ctx->raid_value++;
@@ -190,12 +190,12 @@ static void program_cxg_ra_rnd_ldid_to_r
 
     /* Adding raid_value into LDID-to-RAID register */
     ctx->cxg_ra_reg->CXG_RA_RND_LDID_TO_RAID_REG[reg_offset] |=
-        ((uint64_t)raid <<
-            (ldid_value_offset * NUM_BITS_RESERVED_FOR_RAID));
+	((uint64_t)raid <<
+	    (ldid_value_offset * NUM_BITS_RESERVED_FOR_RAID));
 
     /* Set corresponding valid bit */
     ctx->cxg_ra_reg->CXG_RA_RND_LDID_TO_RAID_VAL |=
-        ((uint64_t)0x1 << ldid_value);
+	((uint64_t)0x1 << ldid_value);
 
     /* Increment raid_value */
     ctx->raid_value++;
@@ -213,20 +213,20 @@ static void program_agentid_to_linkid_re
 
     /* Writing AgentID-to-LinkID mappings */
     ctx->cxg_ra_reg->CXG_RA_AGENTID_TO_LINKID_REG[reg_index] |=
-        ((uint64_t)link_id <<
-            (agentid_bit_offset * NUM_BITS_RESERVED_FOR_LINK_ID));
+	((uint64_t)link_id <<
+	    (agentid_bit_offset * NUM_BITS_RESERVED_FOR_LINK_ID));
     ctx->cxg_ha_reg->CXG_HA_AGENTID_TO_LINKID_REG[reg_index] |=
-        ((uint64_t)link_id <<
-            (agentid_bit_offset * NUM_BITS_RESERVED_FOR_LINK_ID));
+	((uint64_t)link_id <<
+	    (agentid_bit_offset * NUM_BITS_RESERVED_FOR_LINK_ID));
     ctx->cxla_reg->CXLA_AGENTID_TO_LINKID_REG[reg_index] |=
-        ((uint64_t)link_id <<
-            (agentid_bit_offset * NUM_BITS_RESERVED_FOR_LINK_ID));
+	((uint64_t)link_id <<
+	    (agentid_bit_offset * NUM_BITS_RESERVED_FOR_LINK_ID));
 
     /* Setting corresponding valid bits */
     ctx->cxg_ra_reg->CXG_RA_AGENTID_TO_LINKID_VAL |=
-        ((uint64_t)0x1 << agent_id);
+	((uint64_t)0x1 << agent_id);
     ctx->cxg_ha_reg->CXG_HA_AGENTID_TO_LINKID_VAL |=
-        ((uint64_t)0x1 << agent_id);
+	((uint64_t)0x1 << agent_id);
     ctx->cxla_reg->CXLA_AGENTID_TO_LINKID_VAL |= ((uint64_t)0x1 << agent_id);
 }
 
@@ -251,8 +251,8 @@ static void program_cxg_ha_raid_to_ldid_
 
     /* Write RAID-to-LDID mapping (with RNF bit set) */
     ctx->cxg_ha_reg->CXG_HA_RNF_RAID_TO_LDID_REG[reg_index] |=
-        ((uint64_t)(ldid | CXG_HA_RAID_TO_LDID_RNF_MASK) <<
-            (raid_bit_offset * NUM_BITS_RESERVED_FOR_LDID));
+	((uint64_t)(ldid | CXG_HA_RAID_TO_LDID_RNF_MASK) <<
+	    (raid_bit_offset * NUM_BITS_RESERVED_FOR_LDID));
 
     /* Set corresponding valid bit */
     ctx->cxg_ha_reg->CXG_HA_RNF_RAID_TO_LDID_VAL |= ((uint64_t)0x1 << raid_id);
@@ -271,14 +271,14 @@ static void program_hnf_ldid_to_chi_node
     reg_bit_offset = ldid % 2;
 
     for (i = 0; i < ctx->hnf_count; i++) {
-        hnf_reg = (struct cmn600_hnf_reg *)ctx->hnf_offset[i];
-        /* Write NodeID, local/remote and valid bit */
-        hnf_reg->RN_PHYS_ID[reg_index] |=
-            ((uint64_t)(node_id | (local_remote <<
-                                   HNF_RN_PHYS_RN_LOCAL_REMOTE_SHIFT_VAL) |
-                                   ((uint64_t)0x1 <<
-                                   HNF_RN_PHYS_RN_ID_VALID_SHIFT_VAL)) <<
-                        (reg_bit_offset * NUM_BITS_RESERVED_FOR_PHYS_ID));
+	hnf_reg = (struct cmn600_hnf_reg *)ctx->hnf_offset[i];
+	/* Write NodeID, local/remote and valid bit */
+	hnf_reg->RN_PHYS_ID[reg_index] |=
+	    ((uint64_t)(node_id | (local_remote <<
+				   HNF_RN_PHYS_RN_LOCAL_REMOTE_SHIFT_VAL) |
+				   ((uint64_t)0x1 <<
+				   HNF_RN_PHYS_RN_ID_VALID_SHIFT_VAL)) <<
+			(reg_bit_offset * NUM_BITS_RESERVED_FOR_PHYS_ID));
     }
 }
 
@@ -290,23 +290,23 @@ static void program_cxg_ra_sam_addr_regi
     uint64_t sz;
 
     for (i = 0; i < config->remote_ha_mmap_count; i++) {
-        /* Size must be a multiple of SAM_GRANULARITY */
-        fwk_assert((config->remote_ha_mmap[i].size % (64 * 1024)) == 0);
+	/* Size must be a multiple of SAM_GRANULARITY */
+	fwk_assert((config->remote_ha_mmap[i].size % (64 * 1024)) == 0);
 
-        /* Size also must be a power of two */
-        fwk_assert((config->remote_ha_mmap[i].size &
-                    (config->remote_ha_mmap[i].size - 1)) == 0);
-
-        /* Region base should be naturally aligned to the region size */
-        fwk_assert(config->remote_ha_mmap[i].base %
-                   config->remote_ha_mmap[i].size == 0);
-
-        blocks = config->remote_ha_mmap[i].size / (64 * 1024);
-        sz = fwk_math_log2(blocks);
-        ctx->cxg_ra_reg->CXG_RA_SAM_ADDR_REGION_REG[i] =
-            sz | (config->remote_ha_mmap[i].base) |
-            ((uint64_t)ctx->cxg_ha_id_remote << SAM_ADDR_HOME_AGENT_ID_SHIFT) |
-            (SAM_ADDR_REG_VALID_MASK);
+	/* Size also must be a power of two */
+	fwk_assert((config->remote_ha_mmap[i].size &
+		    (config->remote_ha_mmap[i].size - 1)) == 0);
+
+	/* Region base should be naturally aligned to the region size */
+	fwk_assert(config->remote_ha_mmap[i].base %
+		   config->remote_ha_mmap[i].size == 0);
+
+	blocks = config->remote_ha_mmap[i].size / (64 * 1024);
+	sz = fwk_math_log2(blocks);
+	ctx->cxg_ra_reg->CXG_RA_SAM_ADDR_REGION_REG[i] =
+	    sz | (config->remote_ha_mmap[i].base) |
+	    ((uint64_t)ctx->cxg_ha_id_remote << SAM_ADDR_HOME_AGENT_ID_SHIFT) |
+	    (SAM_ADDR_REG_VALID_MASK);
     }
 }
 
@@ -318,66 +318,66 @@ static int enable_and_start_ccix_link_up
     struct cxg_wait_condition_data wait_data;
 
     if (link_id > 2)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     wait_data.ctx = ctx;
     wait_data.link_id = link_id;
 
     if (config->ccix_opt_tlp)
-        ctx->cxla_reg->CXLA_CCIX_PROP_CONFIGURED |= PCIE_OPT_HDR_MASK;
+	ctx->cxla_reg->CXLA_CCIX_PROP_CONFIGURED |= PCIE_OPT_HDR_MASK;
     else
-        ctx->cxla_reg->CXLA_CCIX_PROP_CONFIGURED &= ~PCIE_OPT_HDR_MASK;
+	ctx->cxla_reg->CXLA_CCIX_PROP_CONFIGURED &= ~PCIE_OPT_HDR_MASK;
 
     if (config->ccix_msg_pack_enable) {
-        ctx->cxla_reg->CXLA_CCIX_PROP_CONFIGURED &=
-            ~(1U << CXLA_CCIX_PROP_MSG_PACK_SHIFT_VAL);
+	ctx->cxla_reg->CXLA_CCIX_PROP_CONFIGURED &=
+	    ~(1U << CXLA_CCIX_PROP_MSG_PACK_SHIFT_VAL);
     }
 
     ctx->cxla_reg->CXLA_CCIX_PROP_CONFIGURED &=
-        ~CXLA_CCIX_PROP_MAX_PACK_SIZE_MASK;
+	~CXLA_CCIX_PROP_MAX_PACK_SIZE_MASK;
 
     ctx->cxla_reg->CXLA_CCIX_PROP_CONFIGURED |=
-        (config->ccix_max_packet_size <<
-        CXLA_CCIX_PROP_MAX_PACK_SIZE_SHIFT_VAL);
+	(config->ccix_max_packet_size <<
+	CXLA_CCIX_PROP_MAX_PACK_SIZE_SHIFT_VAL);
 
     ctx->cxla_reg->CXLA_LINKID_TO_PCIE_BUS_NUM =
-        ((uint64_t)config->pcie_bus_num << (link_id * 16));
+	((uint64_t)config->pcie_bus_num << (link_id * 16));
 
     /* Set up TC1 for PCIe so CCIx uses VC1 */
     val1 = ctx->cxla_reg->CXLA_PCIE_HDR_FIELDS &
-           ~CXLA_PCIE_HDR_TRAFFIC_CLASS_MASK;
+	   ~CXLA_PCIE_HDR_TRAFFIC_CLASS_MASK;
 
     ctx->cxla_reg->CXLA_PCIE_HDR_FIELDS =
-        val1 | ((uint64_t)config->ccix_tc <<
-        CXLA_PCIE_HDR_TRAFFIC_CLASS_SHIFT_VAL);
+	val1 | ((uint64_t)config->ccix_tc <<
+	CXLA_PCIE_HDR_TRAFFIC_CLASS_SHIFT_VAL);
 
     val1 = ctx->cxla_reg->CXLA_PCIE_HDR_FIELDS &
-           ~CXLA_PCIE_HDR_VENDOR_ID_MASK;
+	   ~CXLA_PCIE_HDR_VENDOR_ID_MASK;
 
     ctx->cxla_reg->CXLA_PCIE_HDR_FIELDS =
-        val1 | ((uint64_t)CCIX_VENDER_ID <<
-        CXLA_PCIE_HDR_VENDOR_ID_SHIFT_VAL);
+	val1 | ((uint64_t)CCIX_VENDER_ID <<
+	CXLA_PCIE_HDR_VENDOR_ID_SHIFT_VAL);
 
     FWK_LOG_INFO(
-        MOD_NAME "CXLA_PCIE_HDR_FIELDS: 0x%" PRIx64,
-        ctx->cxla_reg->CXLA_PCIE_HDR_FIELDS);
+	MOD_NAME "CXLA_PCIE_HDR_FIELDS: 0x%" PRIx64,
+	ctx->cxla_reg->CXLA_PCIE_HDR_FIELDS);
 
     FWK_LOG_INFO(MOD_NAME "Enabling CCIX link %d...", link_id);
     /* Set link enable bit to enable the CCIX link */
     ctx->cxg_ra_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_CTRL =
-        CXG_LINK_CTRL_EN_MASK;
+	CXG_LINK_CTRL_EN_MASK;
     ctx->cxg_ha_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_CTRL =
-        CXG_LINK_CTRL_EN_MASK;
+	CXG_LINK_CTRL_EN_MASK;
 
     /* Wait until link enable bits are set */
     wait_data.cond = CXG_LINK_CTRL_EN_BIT_SET;
     status = ctx->timer_api->wait(FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
-                                  CXG_PRTCL_LINK_CTRL_TIMEOUT,
-                                  cxg_link_wait_condition,
-                                  &wait_data);
+				  CXG_PRTCL_LINK_CTRL_TIMEOUT,
+				  cxg_link_wait_condition,
+				  &wait_data);
     if (status != FWK_SUCCESS) {
-        FWK_LOG_INFO(MOD_NAME "Failed");
-        return status;
+	FWK_LOG_INFO(MOD_NAME "Failed");
+	return status;
     }
     FWK_LOG_INFO(MOD_NAME "Done");
 
@@ -385,34 +385,34 @@ static int enable_and_start_ccix_link_up
     /* Wait till link up bits are cleared in control register */
     wait_data.cond = CXG_LINK_CTRL_UP_BIT_CLR;
     status = ctx->timer_api->wait(FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
-                                  CXG_PRTCL_LINK_CTRL_TIMEOUT,
-                                  cxg_link_wait_condition,
-                                  &wait_data);
+				  CXG_PRTCL_LINK_CTRL_TIMEOUT,
+				  cxg_link_wait_condition,
+				  &wait_data);
     if (status != FWK_SUCCESS) {
-        FWK_LOG_INFO(MOD_NAME "Failed");
-        return status;
+	FWK_LOG_INFO(MOD_NAME "Failed");
+	return status;
     }
 
     /* Wait till link down bits are set in status register */
     wait_data.cond = CXG_LINK_STATUS_DWN_BIT_SET;
     status = ctx->timer_api->wait(FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
-                                  CXG_PRTCL_LINK_CTRL_TIMEOUT,
-                                  cxg_link_wait_condition,
-                                  &wait_data);
+				  CXG_PRTCL_LINK_CTRL_TIMEOUT,
+				  cxg_link_wait_condition,
+				  &wait_data);
     if (status != FWK_SUCCESS) {
-        FWK_LOG_INFO(MOD_NAME "Failed");
-        return status;
+	FWK_LOG_INFO(MOD_NAME "Failed");
+	return status;
     }
 
     /* Wait till link ACK bits are cleared in status register */
     wait_data.cond = CXG_LINK_STATUS_ACK_BIT_CLR;
     status = ctx->timer_api->wait(FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
-                                   CXG_PRTCL_LINK_CTRL_TIMEOUT,
-                                   cxg_link_wait_condition,
-                                   &wait_data);
+				   CXG_PRTCL_LINK_CTRL_TIMEOUT,
+				   cxg_link_wait_condition,
+				   &wait_data);
     if (status != FWK_SUCCESS) {
-        FWK_LOG_INFO(MOD_NAME "Failed");
-        return status;
+	FWK_LOG_INFO(MOD_NAME "Failed");
+	return status;
     }
 
     FWK_LOG_INFO(MOD_NAME "Done");
@@ -421,31 +421,31 @@ static int enable_and_start_ccix_link_up
 
     /* Bring up link using link request bit */
     ctx->cxg_ra_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_CTRL |=
-        CXG_LINK_CTRL_REQ_MASK;
+	CXG_LINK_CTRL_REQ_MASK;
     ctx->cxg_ha_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_CTRL |=
-        CXG_LINK_CTRL_REQ_MASK;
+	CXG_LINK_CTRL_REQ_MASK;
 
     /* Wait till link ACK bits are set in status register */
     wait_data.cond = CXG_LINK_STATUS_ACK_BIT_SET;
     status = ctx->timer_api->wait(FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
-                                   CXG_PRTCL_LINK_CTRL_TIMEOUT,
-                                   cxg_link_wait_condition,
-                                   &wait_data);
+				   CXG_PRTCL_LINK_CTRL_TIMEOUT,
+				   cxg_link_wait_condition,
+				   &wait_data);
     if (status != FWK_SUCCESS) {
-        FWK_LOG_INFO(MOD_NAME "Failed");
-        return status;
+	FWK_LOG_INFO(MOD_NAME "Failed");
+	return status;
     }
 
 
     /* Wait till link down bits are cleared in status register */
     wait_data.cond = CXG_LINK_STATUS_DWN_BIT_CLR;
     status = ctx->timer_api->wait(FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
-                                   CXG_PRTCL_LINK_CTRL_TIMEOUT,
-                                   cxg_link_wait_condition,
-                                   &wait_data);
+				   CXG_PRTCL_LINK_CTRL_TIMEOUT,
+				   cxg_link_wait_condition,
+				   &wait_data);
     if (status != FWK_SUCCESS) {
-        FWK_LOG_INFO(MOD_NAME "Failed");
-        return status;
+	FWK_LOG_INFO(MOD_NAME "Failed");
+	return status;
     }
     FWK_LOG_INFO(MOD_NAME "Done");
     return FWK_SUCCESS;
@@ -466,7 +466,7 @@ int ccix_setup(struct cmn600_ctx *ctx, v
     int status;
 
     struct mod_cmn600_ccix_remote_node_config *ccix_remote_config =
-        (struct mod_cmn600_ccix_remote_node_config *)remote_config;
+	(struct mod_cmn600_ccix_remote_node_config *)remote_config;
 
     FWK_LOG_INFO(MOD_NAME "Programming CCIX gateway...");
 
@@ -475,15 +475,15 @@ int ccix_setup(struct cmn600_ctx *ctx, v
      * to uint8_t
      */
     fwk_assert((ctx->external_rnsam_count > 1) &&
-                (ctx->external_rnsam_count <= (0xFF + 1)));
+		(ctx->external_rnsam_count <= (0xFF + 1)));
 
     /* Number of local RAs */
     local_ra_cnt = ctx->internal_rnsam_count + ctx->external_rnsam_count;
 
     if (ccix_remote_config->smp_mode == true) {
-        status = enable_smp_mode(ctx);
-        if (status != FWK_SUCCESS)
-            return status;
+	status = enable_smp_mode(ctx);
+	if (status != FWK_SUCCESS)
+	    return status;
     }
 
     /* Set initial RAID value to 0. */
@@ -492,23 +492,23 @@ int ccix_setup(struct cmn600_ctx *ctx, v
     offset_id = ctx->chip_id * local_ra_cnt;
 
     for (rnf_ldid = 0; rnf_ldid < ctx->rnf_count; rnf_ldid++) {
-        agent_id = ctx->raid_value + offset_id;
+	agent_id = ctx->raid_value + offset_id;
 
-        /* Program RAID values in CXRA LDID to RAID LUT */
-        program_cxg_ra_rnf_ldid_to_raid_reg(ctx, rnf_ldid, agent_id);
+	/* Program RAID values in CXRA LDID to RAID LUT */
+	program_cxg_ra_rnf_ldid_to_raid_reg(ctx, rnf_ldid, agent_id);
 
-        /*
-         * Program agentid to linkid LUT for
-         * remote agents in CXRA/CXHA/CXLA
-         */
-        program_agentid_to_linkid_reg(ctx, agent_id,
-            ccix_remote_config->ccix_link_id);
-
-        /*
-         * The HN-F ldid to CHI node id valid bit for
-         * local RN-F agents is already programmed
-         */
-        program_cxg_ha_id(ctx, ccix_remote_config->remote_ha_mmap[0].ha_id);
+	/*
+	 * Program agentid to linkid LUT for
+	 * remote agents in CXRA/CXHA/CXLA
+	 */
+	program_agentid_to_linkid_reg(ctx, agent_id,
+	    ccix_remote_config->ccix_link_id);
+
+	/*
+	 * The HN-F ldid to CHI node id valid bit for
+	 * local RN-F agents is already programmed
+	 */
+	program_cxg_ha_id(ctx, ccix_remote_config->remote_ha_mmap[0].ha_id);
     }
 
     /*
@@ -518,67 +518,67 @@ int ccix_setup(struct cmn600_ctx *ctx, v
     unique_remote_rnf_ldid_value = ctx->rnf_count;
 
     for (i = 0; i < ccix_remote_config->remote_rnf_count; i++) {
-        block = i / ctx->rnf_count;
+	block = i / ctx->rnf_count;
 
-        /*
-         * The remote_agent_id should not include the current chip's agent ids.
-         * If `block` is less than the current chip_id, then include the agent
-         * ids of chip 0 till (not including) current chip. If the block is
-         * equal or greater than the current chip, then include the agent id
-         * from next chip till the max chip.
-         */
-        if (block < ctx->chip_id)
-            remote_agent_id = i + (ctx->rnf_count * block);
-        else
-            remote_agent_id = i + (ctx->rnf_count * block) + local_ra_cnt;
-
-        /* Program the CXHA raid to ldid LUT */
-        program_cxg_ha_raid_to_ldid_lut(ctx, remote_agent_id,
-            unique_remote_rnf_ldid_value);
-        /*
-         * Program HN-F ldid to CHI node id for
-         * remote RN-F agents
-         */
-        program_hnf_ldid_to_chi_node_id_reg(ctx,
-            unique_remote_rnf_ldid_value, ctx->cxg_ha_node_id,
-            REMOTE_CCIX_NODE);
+	/*
+	 * The remote_agent_id should not include the current chip's agent ids.
+	 * If `block` is less than the current chip_id, then include the agent
+	 * ids of chip 0 till (not including) current chip. If the block is
+	 * equal or greater than the current chip, then include the agent id
+	 * from next chip till the max chip.
+	 */
+	if (block < ctx->chip_id)
+	    remote_agent_id = i + (ctx->rnf_count * block);
+	else
+	    remote_agent_id = i + (ctx->rnf_count * block) + local_ra_cnt;
+
+	/* Program the CXHA raid to ldid LUT */
+	program_cxg_ha_raid_to_ldid_lut(ctx, remote_agent_id,
+	    unique_remote_rnf_ldid_value);
+	/*
+	 * Program HN-F ldid to CHI node id for
+	 * remote RN-F agents
+	 */
+	program_hnf_ldid_to_chi_node_id_reg(ctx,
+	    unique_remote_rnf_ldid_value, ctx->cxg_ha_node_id,
+	    REMOTE_CCIX_NODE);
 
-        unique_remote_rnf_ldid_value++;
+	unique_remote_rnf_ldid_value++;
     }
 
     offset_id = ctx->chip_id * local_ra_cnt;
 
     for (i = 0; i < ctx->rnd_count; i++) {
-        rnd_ldid = ctx->rnd_ldid[i];
+	rnd_ldid = ctx->rnd_ldid[i];
 
-        /* Determine agent_id of the remote agents */
-        agent_id = ctx->raid_value + offset_id;
+	/* Determine agent_id of the remote agents */
+	agent_id = ctx->raid_value + offset_id;
 
-        /* Program raid values in CXRA LDID to RAID LUT */
-        program_cxg_ra_rnd_ldid_to_raid_reg(ctx, rnd_ldid, agent_id);
+	/* Program raid values in CXRA LDID to RAID LUT */
+	program_cxg_ra_rnd_ldid_to_raid_reg(ctx, rnd_ldid, agent_id);
 
-        /* Program agentid to linkid LUT for remote agents */
-        program_agentid_to_linkid_reg(ctx, agent_id,
-            ccix_remote_config->ccix_link_id);
+	/* Program agentid to linkid LUT for remote agents */
+	program_agentid_to_linkid_reg(ctx, agent_id,
+	    ccix_remote_config->ccix_link_id);
     }
 
     for (i = 0; i < ctx->rni_count; i++) {
-        rni_ldid = ctx->rni_ldid[i];
+	rni_ldid = ctx->rni_ldid[i];
 
-        /* Determine agentid of the remote agents */
-        agent_id = ctx->raid_value + offset_id;
+	/* Determine agentid of the remote agents */
+	agent_id = ctx->raid_value + offset_id;
 
-        /* Program raid values in CXRA LDID to RAID LUT */
-        program_cxg_ra_rni_ldid_to_raid_reg(ctx, rni_ldid, agent_id);
+	/* Program raid values in CXRA LDID to RAID LUT */
+	program_cxg_ra_rni_ldid_to_raid_reg(ctx, rni_ldid, agent_id);
 
-        /* Program agentid to linkid LUT for remote agents */
-        program_agentid_to_linkid_reg(ctx, agent_id,
-            ccix_remote_config->ccix_link_id);
+	/* Program agentid to linkid LUT for remote agents */
+	program_agentid_to_linkid_reg(ctx, agent_id,
+	    ccix_remote_config->ccix_link_id);
     }
 
     program_cxg_ra_sam_addr_region(ctx, ccix_remote_config);
     status = enable_and_start_ccix_link_up_sequence(ctx, ccix_remote_config,
-        ccix_remote_config->ccix_link_id);
+	ccix_remote_config->ccix_link_id);
 
     return status;
 }
@@ -587,15 +587,15 @@ int ccix_exchange_protocol_credit(struct
 {
 
     if (link_id > 2)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     FWK_LOG_INFO(
-        MOD_NAME "Exchanging protocol credits for link %d...", link_id);
+	MOD_NAME "Exchanging protocol credits for link %d...", link_id);
     /* Exchange protocol credits using link up bit */
     ctx->cxg_ra_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_CTRL |=
-        CXG_LINK_CTRL_UP_MASK;
+	CXG_LINK_CTRL_UP_MASK;
     ctx->cxg_ha_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_CTRL |=
-        CXG_LINK_CTRL_UP_MASK;
+	CXG_LINK_CTRL_UP_MASK;
     FWK_LOG_INFO(MOD_NAME "Done");
     return FWK_SUCCESS;
 }
@@ -606,7 +606,7 @@ int ccix_enter_system_coherency(struct c
     int status;
 
     if (link_id > 2)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     wait_data.ctx = ctx;
     wait_data.link_id = link_id;
@@ -614,17 +614,17 @@ int ccix_enter_system_coherency(struct c
     FWK_LOG_INFO(MOD_NAME "Entering system coherency for link %d...", link_id);
     /* Enter system coherency by setting DVMDOMAIN request bit */
     ctx->cxg_ha_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_CTRL |=
-        CXG_LINK_CTRL_DVMDOMAIN_REQ_MASK;
+	CXG_LINK_CTRL_DVMDOMAIN_REQ_MASK;
 
     /* Wait till DVMDOMAIN ACK bit is set in status register */
     wait_data.cond = CXG_LINK_STATUS_HA_DVMDOMAIN_ACK_BIT_SET;
     status = ctx->timer_api->wait(FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
-                                  CXG_PRTCL_LINK_DVMDOMAIN_TIMEOUT,
-                                  cxg_link_wait_condition,
-                                  &wait_data);
+				  CXG_PRTCL_LINK_DVMDOMAIN_TIMEOUT,
+				  cxg_link_wait_condition,
+				  &wait_data);
     if (status != FWK_SUCCESS) {
-        FWK_LOG_INFO(MOD_NAME "Failed");
-        return status;
+	FWK_LOG_INFO(MOD_NAME "Failed");
+	return status;
     }
 
     FWK_LOG_INFO(MOD_NAME "Done");
@@ -637,7 +637,7 @@ int ccix_enter_dvm_domain(struct cmn600_
     int status;
 
     if (link_id > 2)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     wait_data.ctx = ctx;
     wait_data.link_id = link_id;
@@ -645,17 +645,17 @@ int ccix_enter_dvm_domain(struct cmn600_
     FWK_LOG_INFO(MOD_NAME "Entering DVM domain for link %d...", link_id);
     /* Enter system coherency by setting DVMDOMAIN request bit */
     ctx->cxg_ra_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_CTRL |=
-        CXG_LINK_CTRL_DVMDOMAIN_REQ_MASK;
+	CXG_LINK_CTRL_DVMDOMAIN_REQ_MASK;
 
     /* Wait till DVMDOMAIN ACK bit is set in status register */
     wait_data.cond = CXG_LINK_STATUS_RA_DVMDOMAIN_ACK_BIT_SET;
     status = ctx->timer_api->wait(FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
-                                  CXG_PRTCL_LINK_DVMDOMAIN_TIMEOUT,
-                                  cxg_link_wait_condition,
-                                  &wait_data);
+				  CXG_PRTCL_LINK_DVMDOMAIN_TIMEOUT,
+				  cxg_link_wait_condition,
+				  &wait_data);
     if (status != FWK_SUCCESS) {
-        FWK_LOG_INFO(MOD_NAME "Failed");
-        return status;
+	FWK_LOG_INFO(MOD_NAME "Failed");
+	return status;
     }
 
     FWK_LOG_INFO(MOD_NAME "Done");
@@ -666,29 +666,29 @@ void ccix_capabilities_get(struct cmn600
 {
     /* Populate maximum credit send capability */
     ctx->ccix_host_info.ccix_request_credits =
-        (ctx->cxg_ra_reg->CXG_RA_UNIT_INFO & CXG_RA_REQUEST_TRACKER_DEPTH_MASK)
-         >> CXG_RA_REQUEST_TRACKER_DEPTH_VAL;
+	(ctx->cxg_ra_reg->CXG_RA_UNIT_INFO & CXG_RA_REQUEST_TRACKER_DEPTH_MASK)
+	 >> CXG_RA_REQUEST_TRACKER_DEPTH_VAL;
 
     ctx->ccix_host_info.ccix_snoop_credits =
-        (ctx->cxg_ha_reg->CXG_HA_UNIT_INFO & CXG_HA_SNOOP_TRACKER_DEPTH_MASK) >>
-         CXG_HA_SNOOP_TRACKER_DEPTH_VAL;
+	(ctx->cxg_ha_reg->CXG_HA_UNIT_INFO & CXG_HA_SNOOP_TRACKER_DEPTH_MASK) >>
+	 CXG_HA_SNOOP_TRACKER_DEPTH_VAL;
 
     ctx->ccix_host_info.ccix_data_credits =
-        (ctx->cxg_ha_reg->CXG_HA_UNIT_INFO & CXG_HA_WDB_DEPTH_MASK) >>
-          CXG_HA_WDB_DEPTH_VAL;
+	(ctx->cxg_ha_reg->CXG_HA_UNIT_INFO & CXG_HA_WDB_DEPTH_MASK) >>
+	  CXG_HA_WDB_DEPTH_VAL;
 
     /* Populate max packet size capability */
     ctx->ccix_host_info.ccix_max_packet_size =
-        (ctx->cxla_reg->CXLA_CCIX_PROP_CAPABILITIES &
-         CXLA_CCIX_PROP_MAX_PACK_SIZE_MASK) >>
-         CXLA_CCIX_PROP_MAX_PACK_SIZE_SHIFT_VAL;
+	(ctx->cxla_reg->CXLA_CCIX_PROP_CAPABILITIES &
+	 CXLA_CCIX_PROP_MAX_PACK_SIZE_MASK) >>
+	 CXLA_CCIX_PROP_MAX_PACK_SIZE_SHIFT_VAL;
 
     /* Optimized TLP is always supported by CMN-600 */
     ctx->ccix_host_info.ccix_opt_tlp = true;
 
     /* Populate message packing capability */
     ctx->ccix_host_info.ccix_msg_pack_enable =
-        !((ctx->cxla_reg->CXLA_CCIX_PROP_CAPABILITIES &
-          CXLA_CCIX_PROP_MSG_PACK_SHIFT_MASK) >>
-          CXLA_CCIX_PROP_MSG_PACK_SHIFT_VAL);
+	!((ctx->cxla_reg->CXLA_CCIX_PROP_CAPABILITIES &
+	  CXLA_CCIX_PROP_MSG_PACK_SHIFT_MASK) >>
+	  CXLA_CCIX_PROP_MSG_PACK_SHIFT_VAL);
 }
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cmn600/src/mod_cmn600.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/cmn600/src/mod_cmn600.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cmn600/src/mod_cmn600.c
@@ -47,7 +47,7 @@ static const char *const mmap_type_name[
 static inline size_t cmn600_hnf_cache_group_count(size_t hnf_count)
 {
     return (hnf_count + CMN600_HNF_CACHE_GROUP_ENTRIES_PER_GROUP - 1) /
-        CMN600_HNF_CACHE_GROUP_ENTRIES_PER_GROUP;
+	CMN600_HNF_CACHE_GROUP_ENTRIES_PER_GROUP;
 }
 
 struct cmn600_ctx *ctx;
@@ -77,71 +77,71 @@ static void process_node_hnf(struct cmn6
      * only for the even numbered hnf nodes.
      */
     if (config->hnf_cal_mode == true && (node_id % 2 == 1) &&
-        is_cal_mode_supported(ctx->root)) {
+	is_cal_mode_supported(ctx->root)) {
 
-        /* Factor to manipulate the group and bit_pos */
-        cal_mode_factor = 2;
+	/* Factor to manipulate the group and bit_pos */
+	cal_mode_factor = 2;
 
-        /*
-         * Reduce the hnf_count as the current hnf node is not getting included
-         * in the sys_cache_grp_hn_nodeid register
-         */
-        ctx->hnf_count--;
+	/*
+	 * Reduce the hnf_count as the current hnf node is not getting included
+	 * in the sys_cache_grp_hn_nodeid register
+	 */
+	ctx->hnf_count--;
     }
 
     fwk_assert(logical_id < config->snf_count);
 
     group = logical_id /
-        (CMN600_HNF_CACHE_GROUP_ENTRIES_PER_GROUP * cal_mode_factor);
+	(CMN600_HNF_CACHE_GROUP_ENTRIES_PER_GROUP * cal_mode_factor);
     bit_pos = (CMN600_HNF_CACHE_GROUP_ENTRY_BITS_WIDTH / cal_mode_factor) *
-        (logical_id % (CMN600_HNF_CACHE_GROUP_ENTRIES_PER_GROUP *
-                       cal_mode_factor));
+	(logical_id % (CMN600_HNF_CACHE_GROUP_ENTRIES_PER_GROUP *
+		       cal_mode_factor));
 
     /*
      * If CAL mode is set, add only even numbered hnd node to
      * sys_cache_grp_hn_nodeid registers
      */
     if (config->hnf_cal_mode == true && is_cal_mode_supported(ctx->root)) {
-        if (node_id % 2 == 0)
-            ctx->hnf_cache_group[group] += ((uint64_t)get_node_id(hnf)) <<
-                                           bit_pos;
+	if (node_id % 2 == 0)
+	    ctx->hnf_cache_group[group] += ((uint64_t)get_node_id(hnf)) <<
+					   bit_pos;
     } else
-        ctx->hnf_cache_group[group] += ((uint64_t)get_node_id(hnf)) << bit_pos;
+	ctx->hnf_cache_group[group] += ((uint64_t)get_node_id(hnf)) << bit_pos;
 
     /* Set target node */
     hnf->SAM_CONTROL = config->snf_table[logical_id];
 
     if (ctx->chip_id != 0) {
-        base_offset = ((uint64_t)(ctx->config->chip_addr_space *
-                                  ctx->chip_id));
+	base_offset = ((uint64_t)(ctx->config->chip_addr_space *
+				  ctx->chip_id));
     } else
-        base_offset = 0;
+	base_offset = 0;
 
     /*
      * Map sub-regions to this HN-F node
      */
     for (region_idx = 0; region_idx < config->mmap_count; region_idx++) {
-        region = &config->mmap_table[region_idx];
+	region = &config->mmap_table[region_idx];
 
-        /* Skip non sub-regions */
-        if (region->type != MOD_CMN600_REGION_TYPE_SYSCACHE_SUB)
-            continue;
-
-        /* Configure sub-region entry */
-        hnf->SAM_MEMREGION[region_sub_count] = region->node_id |
-            (sam_encode_region_size(region->size) <<
-                CMN600_HNF_SAM_MEMREGION_SIZE_POS) |
-            (((region->base + base_offset) / SAM_GRANULARITY) <<
-              CMN600_HNF_SAM_MEMREGION_BASE_POS) |
-            CMN600_HNF_SAM_MEMREGION_VALID;
+	/* Skip non sub-regions */
+	if (region->type != MOD_CMN600_REGION_TYPE_SYSCACHE_SUB)
+	    continue;
+
+	/* Configure sub-region entry */
+	hnf->SAM_MEMREGION[region_sub_count] = region->node_id |
+	    (sam_encode_region_size(region->size) <<
+		CMN600_HNF_SAM_MEMREGION_SIZE_POS) |
+	    (((region->base + base_offset) / SAM_GRANULARITY) <<
+	      CMN600_HNF_SAM_MEMREGION_BASE_POS) |
+	    CMN600_HNF_SAM_MEMREGION_VALID;
 
-        region_sub_count++;
+	region_sub_count++;
     }
 
     /* Configure the system cache RAM PPU */
     hnf->PPU_PWPR = CMN600_PPU_PWPR_POLICY_ON |
-                    CMN600_PPU_PWPR_OPMODE_FAM |
-                    CMN600_PPU_PWPR_DYN_EN;
+		    CMN600_PPU_PWPR_OPMODE_FAM |
+		    CMN600_PPU_PWPR_DYN_EN;
 }
 
 /*
@@ -162,7 +162,7 @@ static int cmn600_discovery(void)
     const struct mod_cmn600_config *config = ctx->config;
 
     FWK_LOG_INFO(
-        MOD_NAME "CMN-600 revision: %s", get_cmn600_revision_name(ctx->root));
+	MOD_NAME "CMN-600 revision: %s", get_cmn600_revision_name(ctx->root));
     FWK_LOG_INFO(MOD_NAME "Starting discovery...");
 
     fwk_assert(get_node_type(ctx->root) == NODE_TYPE_CFG);
@@ -171,106 +171,106 @@ static int cmn600_discovery(void)
     xp_count = get_node_child_count(ctx->root);
     for (xp_idx = 0; xp_idx < xp_count; xp_idx++) {
 
-        xp = get_child_node(config->base, ctx->root, xp_idx);
-        fwk_assert(get_node_type(xp) == NODE_TYPE_XP);
+	xp = get_child_node(config->base, ctx->root, xp_idx);
+	fwk_assert(get_node_type(xp) == NODE_TYPE_XP);
 
-        FWK_LOG_INFO(MOD_NAME);
+	FWK_LOG_INFO(MOD_NAME);
 
-        FWK_LOG_INFO(
-            MOD_NAME "XP (%d, %d) ID:%d, LID:%d",
-            get_node_pos_x(xp),
-            get_node_pos_y(xp),
-            get_node_id(xp),
-            get_node_logical_id(xp));
-
-        /* Traverse nodes */
-        node_count = get_node_child_count(xp);
-        for (node_idx = 0; node_idx < node_count; node_idx++) {
-
-            node = get_child_node(config->base, xp, node_idx);
-
-            /* External nodes */
-            if (is_child_external(xp, node_idx)) {
-                xp_port = get_port_number(get_child_node_id(xp, node_idx));
-
-                /*
-                 * If the device type is CXRH, CXHA, or CXRA, then the external
-                 * child node is CXLA as every CXRH, CXHA, or CXRA node has a
-                 * corresponding external CXLA node.
-                 */
-                if ((get_device_type(xp, xp_port) == DEVICE_TYPE_CXRH) ||
-                    (get_device_type(xp, xp_port) == DEVICE_TYPE_CXHA) ||
-                    (get_device_type(xp, xp_port) == DEVICE_TYPE_CXRA)) {
-                    ctx->cxla_reg = (void *)node;
-                    FWK_LOG_INFO(
-                        MOD_NAME "  Found CXLA at node ID: %d",
-                        get_child_node_id(xp, node_idx));
-                } else { /* External RN-SAM Node */
-                    ctx->external_rnsam_count++;
-                    FWK_LOG_INFO(
-                        MOD_NAME "  Found external node ID: %d",
-                        get_child_node_id(xp, node_idx));
-                }
-            } else { /* Internal nodes */
-                switch (get_node_type(node)) {
-                case NODE_TYPE_HN_F:
-                    if (ctx->hnf_count >= MAX_HNF_COUNT) {
-                        FWK_LOG_INFO(
-                            MOD_NAME "  hnf count %d >= max limit (%d)",
-                            ctx->hnf_count,
-                            MAX_HNF_COUNT);
-                        return FWK_E_DATA;
-                    }
-                    ctx->hnf_offset[ctx->hnf_count++] = (uint32_t)node;
-                    break;
-
-                case NODE_TYPE_RN_SAM:
-                    ctx->internal_rnsam_count++;
-                    break;
-
-                case NODE_TYPE_RN_D:
-                    if (ctx->rnd_count >= MAX_RND_COUNT) {
-                        FWK_LOG_INFO(
-                            MOD_NAME "  rnd count %d >= max limit (%d)",
-                            ctx->rnd_count,
-                            MAX_RND_COUNT);
-                        return FWK_E_DATA;
-                    }
-                    ctx->rnd_ldid[ctx->rnd_count++] = get_node_logical_id(node);
-                    break;
-
-                case NODE_TYPE_RN_I:
-                    if (ctx->rni_count >= MAX_RNI_COUNT) {
-                        FWK_LOG_INFO(
-                            MOD_NAME "  rni count %d >= max limit (%d)",
-                            ctx->rni_count,
-                            MAX_RNI_COUNT);
-                        return FWK_E_DATA;
-                    }
-                    ctx->rni_ldid[ctx->rni_count++] = get_node_logical_id(node);
-                    break;
-
-                case NODE_TYPE_CXRA:
-                    ctx->cxg_ra_reg = (struct cmn600_cxg_ra_reg *)node;
-                    break;
-
-                case NODE_TYPE_CXHA:
-                    ctx->cxg_ha_reg = (struct cmn600_cxg_ha_reg *)node;
-                    ctx->ccix_host_info.host_ha_count++;
-                    break;
-
-                default:
-                    /* Nothing to be done for other node types */
-                    break;
-                }
-
-                FWK_LOG_INFO(
-                    MOD_NAME "  %s ID:%d, LID:%d",
-                    get_node_type_name(get_node_type(node)),
-                    get_node_id(node),
-                    get_node_logical_id(node));
-            }
-        }
+	FWK_LOG_INFO(
+	    MOD_NAME "XP (%d, %d) ID:%d, LID:%d",
+	    get_node_pos_x(xp),
+	    get_node_pos_y(xp),
+	    get_node_id(xp),
+	    get_node_logical_id(xp));
+
+	/* Traverse nodes */
+	node_count = get_node_child_count(xp);
+	for (node_idx = 0; node_idx < node_count; node_idx++) {
+
+	    node = get_child_node(config->base, xp, node_idx);
+
+	    /* External nodes */
+	    if (is_child_external(xp, node_idx)) {
+		xp_port = get_port_number(get_child_node_id(xp, node_idx));
+
+		/*
+		 * If the device type is CXRH, CXHA, or CXRA, then the external
+		 * child node is CXLA as every CXRH, CXHA, or CXRA node has a
+		 * corresponding external CXLA node.
+		 */
+		if ((get_device_type(xp, xp_port) == DEVICE_TYPE_CXRH) ||
+		    (get_device_type(xp, xp_port) == DEVICE_TYPE_CXHA) ||
+		    (get_device_type(xp, xp_port) == DEVICE_TYPE_CXRA)) {
+		    ctx->cxla_reg = (void *)node;
+		    FWK_LOG_INFO(
+			MOD_NAME "  Found CXLA at node ID: %d",
+			get_child_node_id(xp, node_idx));
+		} else { /* External RN-SAM Node */
+		    ctx->external_rnsam_count++;
+		    FWK_LOG_INFO(
+			MOD_NAME "  Found external node ID: %d",
+			get_child_node_id(xp, node_idx));
+		}
+	    } else { /* Internal nodes */
+		switch (get_node_type(node)) {
+		case NODE_TYPE_HN_F:
+		    if (ctx->hnf_count >= MAX_HNF_COUNT) {
+			FWK_LOG_INFO(
+			    MOD_NAME "  hnf count %d >= max limit (%d)",
+			    ctx->hnf_count,
+			    MAX_HNF_COUNT);
+			return FWK_E_DATA;
+		    }
+		    ctx->hnf_offset[ctx->hnf_count++] = (uint32_t)node;
+		    break;
+
+		case NODE_TYPE_RN_SAM:
+		    ctx->internal_rnsam_count++;
+		    break;
+
+		case NODE_TYPE_RN_D:
+		    if (ctx->rnd_count >= MAX_RND_COUNT) {
+			FWK_LOG_INFO(
+			    MOD_NAME "  rnd count %d >= max limit (%d)",
+			    ctx->rnd_count,
+			    MAX_RND_COUNT);
+			return FWK_E_DATA;
+		    }
+		    ctx->rnd_ldid[ctx->rnd_count++] = get_node_logical_id(node);
+		    break;
+
+		case NODE_TYPE_RN_I:
+		    if (ctx->rni_count >= MAX_RNI_COUNT) {
+			FWK_LOG_INFO(
+			    MOD_NAME "  rni count %d >= max limit (%d)",
+			    ctx->rni_count,
+			    MAX_RNI_COUNT);
+			return FWK_E_DATA;
+		    }
+		    ctx->rni_ldid[ctx->rni_count++] = get_node_logical_id(node);
+		    break;
+
+		case NODE_TYPE_CXRA:
+		    ctx->cxg_ra_reg = (struct cmn600_cxg_ra_reg *)node;
+		    break;
+
+		case NODE_TYPE_CXHA:
+		    ctx->cxg_ha_reg = (struct cmn600_cxg_ha_reg *)node;
+		    ctx->ccix_host_info.host_ha_count++;
+		    break;
+
+		default:
+		    /* Nothing to be done for other node types */
+		    break;
+		}
+
+		FWK_LOG_INFO(
+		    MOD_NAME "  %s ID:%d, LID:%d",
+		    get_node_type_name(get_node_type(node)),
+		    get_node_id(node),
+		    get_node_logical_id(node));
+	    }
+	}
     }
 
     /*
@@ -279,43 +279,43 @@ static int cmn600_discovery(void)
      * RN-SAM count minus the total RN-D, RN-I and CXHA count combined.
      */
     ctx->rnf_count = ctx->internal_rnsam_count + ctx->external_rnsam_count -
-        (ctx->rnd_count + ctx->rni_count + ctx->ccix_host_info.host_ha_count);
+	(ctx->rnd_count + ctx->rni_count + ctx->ccix_host_info.host_ha_count);
 
     if (ctx->rnf_count > MAX_RNF_COUNT) {
-        FWK_LOG_ERR(
-            MOD_NAME "rnf count %d > max limit (%d)",
-            ctx->rnf_count,
-            MAX_RNF_COUNT);
-        return FWK_E_RANGE;
+	FWK_LOG_ERR(
+	    MOD_NAME "rnf count %d > max limit (%d)",
+	    ctx->rnf_count,
+	    MAX_RNF_COUNT);
+	return FWK_E_RANGE;
     }
 
     /* When CAL is present, the number of HN-Fs must be even. */
     if ((ctx->hnf_count % 2 != 0) && (config->hnf_cal_mode == true)) {
-        FWK_LOG_ERR(
-            MOD_NAME "hnf count: %d should be even when CAL mode is set",
-            ctx->hnf_count);
-        return FWK_E_DATA;
+	FWK_LOG_ERR(
+	    MOD_NAME "hnf count: %d should be even when CAL mode is set",
+	    ctx->hnf_count);
+	return FWK_E_DATA;
     }
 
     FWK_LOG_INFO(
-        MOD_NAME "Total internal RN-SAM nodes: %d", ctx->internal_rnsam_count);
+	MOD_NAME "Total internal RN-SAM nodes: %d", ctx->internal_rnsam_count);
     FWK_LOG_INFO(
-        MOD_NAME "Total external RN-SAM nodes: %d", ctx->external_rnsam_count);
+	MOD_NAME "Total external RN-SAM nodes: %d", ctx->external_rnsam_count);
     FWK_LOG_INFO(MOD_NAME "Total HN-F nodes: %d", ctx->hnf_count);
     FWK_LOG_INFO(MOD_NAME "Total RN-F nodes: %d", ctx->rnf_count);
     FWK_LOG_INFO(MOD_NAME "Total RN-D nodes: %d", ctx->rnd_count);
     FWK_LOG_INFO(MOD_NAME "Total RN-I nodes: %d", ctx->rni_count);
 
     if (ctx->cxla_reg != NULL) {
-        FWK_LOG_INFO(MOD_NAME "CCIX CXLA node at: 0x%p", (void *)ctx->cxla_reg);
+	FWK_LOG_INFO(MOD_NAME "CCIX CXLA node at: 0x%p", (void *)ctx->cxla_reg);
     }
     if (ctx->cxg_ra_reg != NULL) {
-        FWK_LOG_INFO(
-            MOD_NAME "CCIX CXRA node at: 0x%p", (void *)ctx->cxg_ra_reg);
+	FWK_LOG_INFO(
+	    MOD_NAME "CCIX CXRA node at: 0x%p", (void *)ctx->cxg_ra_reg);
     }
     if (ctx->cxg_ha_reg != NULL) {
-        FWK_LOG_INFO(
-            MOD_NAME "CCIX CXHA node at: 0x%p", (void *)ctx->cxg_ha_reg);
+	FWK_LOG_INFO(
+	    MOD_NAME "CCIX CXHA node at: 0x%p", (void *)ctx->cxg_ha_reg);
     }
     return FWK_SUCCESS;
 }
@@ -341,43 +341,43 @@ static void cmn600_configure(void)
     /* Traverse cross points (XP) */
     xp_count = get_node_child_count(ctx->root);
     for (xp_idx = 0; xp_idx < xp_count; xp_idx++) {
-        xp = get_child_node(config->base, ctx->root, xp_idx);
-        fwk_assert(get_node_type(xp) == NODE_TYPE_XP);
+	xp = get_child_node(config->base, ctx->root, xp_idx);
+	fwk_assert(get_node_type(xp) == NODE_TYPE_XP);
 
-        /* Traverse nodes */
-        node_count = get_node_child_count(xp);
-        for (node_idx = 0; node_idx < node_count; node_idx++) {
-            node = get_child_node(config->base, xp, node_idx);
-
-            if (is_child_external(xp, node_idx)) {
-                unsigned int node_id = get_child_node_id(xp, node_idx);
-                xp_port = get_port_number(get_child_node_id(xp, node_idx));
-
-                /* Skip if the device type is CXG */
-                if ((get_device_type(xp, xp_port) == DEVICE_TYPE_CXRH) ||
-                    (get_device_type(xp, xp_port) == DEVICE_TYPE_CXHA) ||
-                    (get_device_type(xp, xp_port) == DEVICE_TYPE_CXRA))
-                    continue;
-
-                fwk_assert(xrnsam_entry < ctx->external_rnsam_count);
-
-                ctx->external_rnsam_table[xrnsam_entry].node_id = node_id;
-                ctx->external_rnsam_table[xrnsam_entry].node = node;
-
-                xrnsam_entry++;
-            } else {
-                enum node_type node_type = get_node_type(node);
-
-                if (node_type == NODE_TYPE_RN_SAM) {
-                    fwk_assert(irnsam_entry < ctx->internal_rnsam_count);
-
-                    ctx->internal_rnsam_table[irnsam_entry] = node;
-
-                    irnsam_entry++;
-                } else if (node_type == NODE_TYPE_HN_F)
-                    process_node_hnf(node);
-            }
-        }
+	/* Traverse nodes */
+	node_count = get_node_child_count(xp);
+	for (node_idx = 0; node_idx < node_count; node_idx++) {
+	    node = get_child_node(config->base, xp, node_idx);
+
+	    if (is_child_external(xp, node_idx)) {
+		unsigned int node_id = get_child_node_id(xp, node_idx);
+		xp_port = get_port_number(get_child_node_id(xp, node_idx));
+
+		/* Skip if the device type is CXG */
+		if ((get_device_type(xp, xp_port) == DEVICE_TYPE_CXRH) ||
+		    (get_device_type(xp, xp_port) == DEVICE_TYPE_CXHA) ||
+		    (get_device_type(xp, xp_port) == DEVICE_TYPE_CXRA))
+		    continue;
+
+		fwk_assert(xrnsam_entry < ctx->external_rnsam_count);
+
+		ctx->external_rnsam_table[xrnsam_entry].node_id = node_id;
+		ctx->external_rnsam_table[xrnsam_entry].node = node;
+
+		xrnsam_entry++;
+	    } else {
+		enum node_type node_type = get_node_type(node);
+
+		if (node_type == NODE_TYPE_RN_SAM) {
+		    fwk_assert(irnsam_entry < ctx->internal_rnsam_count);
+
+		    ctx->internal_rnsam_table[irnsam_entry] = node;
+
+		    irnsam_entry++;
+		} else if (node_type == NODE_TYPE_HN_F)
+		    process_node_hnf(node);
+	    }
+	}
     }
 }
 
@@ -387,20 +387,20 @@ static int get_tgt_nodeid_reg_count()
     case CMN600_PERIPH_ID_2_REV_R1_P0:
     case CMN600_PERIPH_ID_2_REV_R1_P1:
     case CMN600_PERIPH_ID_2_REV_R1_P3:
-        return CMN600_RNSAM_NON_HASH_TGT_NODEID_REGS_2;
+	return CMN600_RNSAM_NON_HASH_TGT_NODEID_REGS_2;
 
     case CMN600_PERIPH_ID_2_REV_R1_P2:
     case CMN600_PERIPH_ID_2_REV_R2_P0:
-        return CMN600_RNSAM_NON_HASH_TGT_NODEID_REGS_3;
+	return CMN600_RNSAM_NON_HASH_TGT_NODEID_REGS_3;
 
     case CMN600_PERIPH_ID_2_REV_R3_P0:
     case CMN600_PERIPH_ID_2_REV_R3_P1:
     case CMN600_PERIPH_ID_2_REV_R3_P2:
-        return CMN600_RNSAM_NON_HASH_TGT_NODEID_REGS_5;
+	return CMN600_RNSAM_NON_HASH_TGT_NODEID_REGS_5;
 
     default:
-        fwk_unexpected();
-        return FWK_E_DEVICE;
+	fwk_unexpected();
+	return FWK_E_DEVICE;
     }
 }
 
@@ -426,183 +426,183 @@ int cmn600_setup_sam(struct cmn600_rnsam
 
     status = get_tgt_nodeid_reg_count();
     if (status < 0)
-        return FWK_E_DEVICE;
+	return FWK_E_DEVICE;
     tgt_nodeid_reg_count = (unsigned int)status;
 
     for (region_idx = 0; region_idx < config->mmap_count; region_idx++) {
-        region = &config->mmap_table[region_idx];
+	region = &config->mmap_table[region_idx];
 
-        if (ctx->chip_id != 0) {
-            if (region->type == MOD_CMN600_REGION_TYPE_CCIX)
-                base = 0;
-            else if (region->type == MOD_CMN600_MEMORY_REGION_TYPE_SYSCACHE)
-                base = region->base;
-            else {
-                base = ((uint64_t)(ctx->config->chip_addr_space *
-                                   ctx->chip_id) + region->base);
-            }
-        } else
-            base = region->base;
-
-        (void)mmap_type_name;
-        FWK_LOG_INFO(
-            MOD_NAME "  [0x%08" PRIX32 "%08" PRIX32
-            " - 0x%08" PRIX32 "%08" PRIX32 "] %s",
-            (uint32_t)(base >> 32),
-            (uint32_t)base,
-            (uint32_t)((base + region->size - 1) >> 32),
-            (uint32_t)(base + region->size - 1),
-            mmap_type_name[region->type]);
-
-        switch (region->type) {
-        case MOD_CMN600_MEMORY_REGION_TYPE_IO:
-        case MOD_CMN600_REGION_TYPE_CCIX:
-            /*
-             * Configure memory region
-             */
-            if (region_io_count > CMN600_RNSAM_MAX_NON_HASH_MEM_REGION_ENTRIES(
-                                      tgt_nodeid_reg_count)) {
-                FWK_LOG_ERR(
-                    MOD_NAME
-                    "Non-Hashed Memory can have maximum of %d regions only",
-                    CMN600_RNSAM_MAX_NON_HASH_MEM_REGION_ENTRIES(
-                        tgt_nodeid_reg_count));
-                return FWK_E_DATA;
-            }
-
-            group = region_io_count / CMN600_RNSAM_REGION_ENTRIES_PER_GROUP;
-            bit_pos = (region_io_count %
-                       CMN600_RNSAM_REGION_ENTRIES_PER_GROUP) *
-                      CMN600_RNSAM_REGION_ENTRY_BITS_WIDTH;
-
-            sam_node_type =
-                (region->type == MOD_CMN600_MEMORY_REGION_TYPE_IO) ?
-                    SAM_NODE_TYPE_HN_I : SAM_NODE_TYPE_CXRA;
-            configure_region(&rnsam->NON_HASH_MEM_REGION[group],
-                             bit_pos,
-                             base,
-                             region->size,
-                             sam_node_type);
-            /*
-             * Configure target node
-             */
-            group = region_io_count /
-                CMN600_RNSAM_NON_HASH_TGT_NODEID_ENTRIES_PER_GROUP;
-            if (group > tgt_nodeid_reg_count) {
-                FWK_LOG_ERR(
-                    MOD_NAME
-                    "Non-Hashed Memory can have maximum of %d groups only",
-                    tgt_nodeid_reg_count);
-                return FWK_E_DATA;
-            }
-
-            bit_pos = CMN600_RNSAM_NON_HASH_TGT_NODEID_ENTRY_BITS_WIDTH *
-                      (region_io_count %
-                       CMN600_RNSAM_NON_HASH_TGT_NODEID_ENTRIES_PER_GROUP);
-
-            if (group < CMN600_RNSAM_NON_HASH_TGT_NODEID_REGS_3) {
-                nodeid = &rnsam->NON_HASH_TGT_NODEID[group];
-            } else if (group < CMN600_RNSAM_NON_HASH_TGT_NODEID_REGS_5) {
-                /*
-                 * CMN-600 revisions R3P0 and above has five NON_HASH_TGT_NODEID
-                 * registers. Last two registers are implemented in a different
-                 * offset address
-                 */
-                nodeid = &rnsam->NON_HASH_TGT_NODEID_GRP2
-                              [group - CMN600_RNSAM_NON_HASH_TGT_NODEID_REGS_3];
-            } else {
-                FWK_LOG_ERR(MOD_NAME "Invalid Non-Hashed target node ID group");
-                return FWK_E_DATA;
-            }
-
-            *nodeid &=
-                ~(CMN600_RNSAM_NON_HASH_TGT_NODEID_ENTRY_MASK << bit_pos);
-            *nodeid |=
-                (region->node_id & CMN600_RNSAM_NON_HASH_TGT_NODEID_ENTRY_MASK)
-                << bit_pos;
-
-            region_io_count++;
-            break;
-
-        case MOD_CMN600_MEMORY_REGION_TYPE_SYSCACHE:
-            /*
-             * Configure memory region
-             */
-            if (region_sys_count >= CMN600_RNSAM_MAX_HASH_MEM_REGION_ENTRIES) {
-                FWK_LOG_ERR(
-                    MOD_NAME
-                    "Hashed Memory can have maximum of %d regions only",
-                    CMN600_RNSAM_MAX_HASH_MEM_REGION_ENTRIES);
-                return FWK_E_DATA;
-            }
-
-            group = region_sys_count / CMN600_RNSAM_REGION_ENTRIES_PER_GROUP;
-            bit_pos = (region_sys_count %
-                       CMN600_RNSAM_REGION_ENTRIES_PER_GROUP) *
-                      CMN600_RNSAM_REGION_ENTRY_BITS_WIDTH;
-            configure_region(&rnsam->SYS_CACHE_GRP_REGION[group],
-                             bit_pos,
-                             region->base,
-                             region->size,
-                             SAM_NODE_TYPE_HN_F);
-
-            /* Mark corresponding region as enabled */
-            scg_region = (2 * group) + (bit_pos/32);
-            fwk_assert(scg_region < CMN600_MAX_NUM_SCG);
-            scg_regions_enabled[scg_region] = 1;
-
-            region_sys_count++;
-            break;
-
-        case MOD_CMN600_REGION_TYPE_SYSCACHE_NONHASH:
-            group = region_sys_count / CMN600_RNSAM_REGION_ENTRIES_PER_GROUP;
-            bit_pos = (region_sys_count %
-                       CMN600_RNSAM_REGION_ENTRIES_PER_GROUP) *
-                      CMN600_RNSAM_REGION_ENTRY_BITS_WIDTH;
-            /*
-             * Configure memory region
-             */
-            configure_region(&rnsam->SYS_CACHE_GRP_REGION[group],
-                             bit_pos,
-                             region->base,
-                             region->size,
-                             SAM_NODE_TYPE_HN_I);
-
-            rnsam->SYS_CACHE_GRP_REGION[group] |= (UINT64_C(0x2) << bit_pos);
-            bit_pos = CMN600_RNSAM_NON_HASH_TGT_NODEID_ENTRY_BITS_WIDTH *
-                  ((region_sys_count - 1) %
-                  CMN600_RNSAM_NON_HASH_TGT_NODEID_ENTRIES_PER_GROUP);
-            rnsam->SYS_CACHE_GRP_NOHASH_NODEID &=
-                ~(CMN600_RNSAM_NON_HASH_TGT_NODEID_ENTRY_MASK << bit_pos);
-            rnsam->SYS_CACHE_GRP_NOHASH_NODEID |= (region->node_id &
-                CMN600_RNSAM_NON_HASH_TGT_NODEID_ENTRY_MASK) << bit_pos;
-
-            region_sys_count++;
-            break;
-
-        case MOD_CMN600_REGION_TYPE_SYSCACHE_SUB:
-            /* Do nothing. System cache sub-regions are handled by HN-Fs */
-            break;
-
-        default:
-            fwk_unexpected();
-            return FWK_E_DATA;
-        }
+	if (ctx->chip_id != 0) {
+	    if (region->type == MOD_CMN600_REGION_TYPE_CCIX)
+		base = 0;
+	    else if (region->type == MOD_CMN600_MEMORY_REGION_TYPE_SYSCACHE)
+		base = region->base;
+	    else {
+		base = ((uint64_t)(ctx->config->chip_addr_space *
+				   ctx->chip_id) + region->base);
+	    }
+	} else
+	    base = region->base;
+
+	(void)mmap_type_name;
+	FWK_LOG_INFO(
+	    MOD_NAME "  [0x%08" PRIX32 "%08" PRIX32
+	    " - 0x%08" PRIX32 "%08" PRIX32 "] %s",
+	    (uint32_t)(base >> 32),
+	    (uint32_t)base,
+	    (uint32_t)((base + region->size - 1) >> 32),
+	    (uint32_t)(base + region->size - 1),
+	    mmap_type_name[region->type]);
+
+	switch (region->type) {
+	case MOD_CMN600_MEMORY_REGION_TYPE_IO:
+	case MOD_CMN600_REGION_TYPE_CCIX:
+	    /*
+	     * Configure memory region
+	     */
+	    if (region_io_count > CMN600_RNSAM_MAX_NON_HASH_MEM_REGION_ENTRIES(
+				      tgt_nodeid_reg_count)) {
+		FWK_LOG_ERR(
+		    MOD_NAME
+		    "Non-Hashed Memory can have maximum of %d regions only",
+		    CMN600_RNSAM_MAX_NON_HASH_MEM_REGION_ENTRIES(
+			tgt_nodeid_reg_count));
+		return FWK_E_DATA;
+	    }
+
+	    group = region_io_count / CMN600_RNSAM_REGION_ENTRIES_PER_GROUP;
+	    bit_pos = (region_io_count %
+		       CMN600_RNSAM_REGION_ENTRIES_PER_GROUP) *
+		      CMN600_RNSAM_REGION_ENTRY_BITS_WIDTH;
+
+	    sam_node_type =
+		(region->type == MOD_CMN600_MEMORY_REGION_TYPE_IO) ?
+		    SAM_NODE_TYPE_HN_I : SAM_NODE_TYPE_CXRA;
+	    configure_region(&rnsam->NON_HASH_MEM_REGION[group],
+			     bit_pos,
+			     base,
+			     region->size,
+			     sam_node_type);
+	    /*
+	     * Configure target node
+	     */
+	    group = region_io_count /
+		CMN600_RNSAM_NON_HASH_TGT_NODEID_ENTRIES_PER_GROUP;
+	    if (group > tgt_nodeid_reg_count) {
+		FWK_LOG_ERR(
+		    MOD_NAME
+		    "Non-Hashed Memory can have maximum of %d groups only",
+		    tgt_nodeid_reg_count);
+		return FWK_E_DATA;
+	    }
+
+	    bit_pos = CMN600_RNSAM_NON_HASH_TGT_NODEID_ENTRY_BITS_WIDTH *
+		      (region_io_count %
+		       CMN600_RNSAM_NON_HASH_TGT_NODEID_ENTRIES_PER_GROUP);
+
+	    if (group < CMN600_RNSAM_NON_HASH_TGT_NODEID_REGS_3) {
+		nodeid = &rnsam->NON_HASH_TGT_NODEID[group];
+	    } else if (group < CMN600_RNSAM_NON_HASH_TGT_NODEID_REGS_5) {
+		/*
+		 * CMN-600 revisions R3P0 and above has five NON_HASH_TGT_NODEID
+		 * registers. Last two registers are implemented in a different
+		 * offset address
+		 */
+		nodeid = &rnsam->NON_HASH_TGT_NODEID_GRP2
+			      [group - CMN600_RNSAM_NON_HASH_TGT_NODEID_REGS_3];
+	    } else {
+		FWK_LOG_ERR(MOD_NAME "Invalid Non-Hashed target node ID group");
+		return FWK_E_DATA;
+	    }
+
+	    *nodeid &=
+		~(CMN600_RNSAM_NON_HASH_TGT_NODEID_ENTRY_MASK << bit_pos);
+	    *nodeid |=
+		(region->node_id & CMN600_RNSAM_NON_HASH_TGT_NODEID_ENTRY_MASK)
+		<< bit_pos;
+
+	    region_io_count++;
+	    break;
+
+	case MOD_CMN600_MEMORY_REGION_TYPE_SYSCACHE:
+	    /*
+	     * Configure memory region
+	     */
+	    if (region_sys_count >= CMN600_RNSAM_MAX_HASH_MEM_REGION_ENTRIES) {
+		FWK_LOG_ERR(
+		    MOD_NAME
+		    "Hashed Memory can have maximum of %d regions only",
+		    CMN600_RNSAM_MAX_HASH_MEM_REGION_ENTRIES);
+		return FWK_E_DATA;
+	    }
+
+	    group = region_sys_count / CMN600_RNSAM_REGION_ENTRIES_PER_GROUP;
+	    bit_pos = (region_sys_count %
+		       CMN600_RNSAM_REGION_ENTRIES_PER_GROUP) *
+		      CMN600_RNSAM_REGION_ENTRY_BITS_WIDTH;
+	    configure_region(&rnsam->SYS_CACHE_GRP_REGION[group],
+			     bit_pos,
+			     region->base,
+			     region->size,
+			     SAM_NODE_TYPE_HN_F);
+
+	    /* Mark corresponding region as enabled */
+	    scg_region = (2 * group) + (bit_pos/32);
+	    fwk_assert(scg_region < CMN600_MAX_NUM_SCG);
+	    scg_regions_enabled[scg_region] = 1;
+
+	    region_sys_count++;
+	    break;
+
+	case MOD_CMN600_REGION_TYPE_SYSCACHE_NONHASH:
+	    group = region_sys_count / CMN600_RNSAM_REGION_ENTRIES_PER_GROUP;
+	    bit_pos = (region_sys_count %
+		       CMN600_RNSAM_REGION_ENTRIES_PER_GROUP) *
+		      CMN600_RNSAM_REGION_ENTRY_BITS_WIDTH;
+	    /*
+	     * Configure memory region
+	     */
+	    configure_region(&rnsam->SYS_CACHE_GRP_REGION[group],
+			     bit_pos,
+			     region->base,
+			     region->size,
+			     SAM_NODE_TYPE_HN_I);
+
+	    rnsam->SYS_CACHE_GRP_REGION[group] |= (UINT64_C(0x2) << bit_pos);
+	    bit_pos = CMN600_RNSAM_NON_HASH_TGT_NODEID_ENTRY_BITS_WIDTH *
+		  ((region_sys_count - 1) %
+		  CMN600_RNSAM_NON_HASH_TGT_NODEID_ENTRIES_PER_GROUP);
+	    rnsam->SYS_CACHE_GRP_NOHASH_NODEID &=
+		~(CMN600_RNSAM_NON_HASH_TGT_NODEID_ENTRY_MASK << bit_pos);
+	    rnsam->SYS_CACHE_GRP_NOHASH_NODEID |= (region->node_id &
+		CMN600_RNSAM_NON_HASH_TGT_NODEID_ENTRY_MASK) << bit_pos;
+
+	    region_sys_count++;
+	    break;
+
+	case MOD_CMN600_REGION_TYPE_SYSCACHE_SUB:
+	    /* Do nothing. System cache sub-regions are handled by HN-Fs */
+	    break;
+
+	default:
+	    fwk_unexpected();
+	    return FWK_E_DATA;
+	}
     }
 
     group_count = cmn600_hnf_cache_group_count(ctx->hnf_count);
     for (group = 0; group < group_count; group++)
-        rnsam->SYS_CACHE_GRP_HN_NODEID[group] = ctx->hnf_cache_group[group];
+	rnsam->SYS_CACHE_GRP_HN_NODEID[group] = ctx->hnf_cache_group[group];
 
     /* Program the number of HNFs */
     rnsam->SYS_CACHE_GRP_HN_COUNT = ctx->hnf_count;
 
     /* Use CAL mode only if the CMN600 revision is r2p0 or above */
     if (is_cal_mode_supported(ctx->root) && config->hnf_cal_mode) {
-        for (region_idx = 0; region_idx < CMN600_MAX_NUM_SCG; region_idx++)
-            rnsam->SYS_CACHE_GRP_CAL_MODE |= scg_regions_enabled[region_idx] *
-                (CMN600_RNSAM_SCG_HNF_CAL_MODE_EN <<
-                 (region_idx * CMN600_RNSAM_SCG_HNF_CAL_MODE_SHIFT));
+	for (region_idx = 0; region_idx < CMN600_MAX_NUM_SCG; region_idx++)
+	    rnsam->SYS_CACHE_GRP_CAL_MODE |= scg_regions_enabled[region_idx] *
+		(CMN600_RNSAM_SCG_HNF_CAL_MODE_EN <<
+		 (region_idx * CMN600_RNSAM_SCG_HNF_CAL_MODE_SHIFT));
     }
 
     /* Enable RNSAM */
@@ -618,56 +618,56 @@ static int cmn600_setup(void)
     int status;
 
     if (!ctx->initialized) {
-        status = cmn600_discovery();
-        if (status != FWK_SUCCESS)
-            return status;
-        /*
-         * Allocate resources based on the discovery
-         */
-
-        /* Pointers for the internal RN-SAM nodes */
-        if (ctx->internal_rnsam_count != 0) {
-            ctx->internal_rnsam_table = fwk_mm_calloc(
-                ctx->internal_rnsam_count, sizeof(*ctx->internal_rnsam_table));
-        }
-
-        /* Tuples for the external RN-RAM nodes (including their node IDs) */
-        if (ctx->external_rnsam_count != 0) {
-            ctx->external_rnsam_table = fwk_mm_calloc(
-                ctx->external_rnsam_count, sizeof(*ctx->external_rnsam_table));
-        }
-
-        /* Cache groups */
-        if (ctx->hnf_count != 0) {
-            /*
-             * Allocate enough group descriptors to accommodate all expected
-             * HN-F nodes in the system.
-             */
-            ctx->hnf_cache_group = fwk_mm_calloc(
-                cmn600_hnf_cache_group_count(ctx->hnf_count),
-                sizeof(*ctx->hnf_cache_group));
-        }
+	status = cmn600_discovery();
+	if (status != FWK_SUCCESS)
+	    return status;
+	/*
+	 * Allocate resources based on the discovery
+	 */
+
+	/* Pointers for the internal RN-SAM nodes */
+	if (ctx->internal_rnsam_count != 0) {
+	    ctx->internal_rnsam_table = fwk_mm_calloc(
+		ctx->internal_rnsam_count, sizeof(*ctx->internal_rnsam_table));
+	}
+
+	/* Tuples for the external RN-RAM nodes (including their node IDs) */
+	if (ctx->external_rnsam_count != 0) {
+	    ctx->external_rnsam_table = fwk_mm_calloc(
+		ctx->external_rnsam_count, sizeof(*ctx->external_rnsam_table));
+	}
+
+	/* Cache groups */
+	if (ctx->hnf_count != 0) {
+	    /*
+	     * Allocate enough group descriptors to accommodate all expected
+	     * HN-F nodes in the system.
+	     */
+	    ctx->hnf_cache_group = fwk_mm_calloc(
+		cmn600_hnf_cache_group_count(ctx->hnf_count),
+		sizeof(*ctx->hnf_cache_group));
+	}
     }
 
     cmn600_configure();
 
     /* Setup internal RN-SAM nodes */
     for (rnsam_idx = 0; rnsam_idx < ctx->internal_rnsam_count; rnsam_idx++)
-        cmn600_setup_sam(ctx->internal_rnsam_table[rnsam_idx]);
+	cmn600_setup_sam(ctx->internal_rnsam_table[rnsam_idx]);
 
     /* Capture CCIX Host Topology */
     for (i = 0; i < ctx->config->mmap_count; i++) {
-        if (ctx->config->mmap_table[i].type == MOD_CMN600_REGION_TYPE_CCIX) {
-            ccix_mmap_idx = ctx->ccix_host_info.ccix_host_mmap_count;
-            if (ccix_mmap_idx >= MAX_HA_MMAP_ENTRIES)
-                return FWK_E_DATA;
-
-            ctx->ccix_host_info.ccix_host_mmap[ccix_mmap_idx].base =
-                ctx->config->mmap_table[i].base;
-            ctx->ccix_host_info.ccix_host_mmap[ccix_mmap_idx].size =
-                ctx->config->mmap_table[i].size;
-            ctx->ccix_host_info.ccix_host_mmap_count++;
-        }
+	if (ctx->config->mmap_table[i].type == MOD_CMN600_REGION_TYPE_CCIX) {
+	    ccix_mmap_idx = ctx->ccix_host_info.ccix_host_mmap_count;
+	    if (ccix_mmap_idx >= MAX_HA_MMAP_ENTRIES)
+		return FWK_E_DATA;
+
+	    ctx->ccix_host_info.ccix_host_mmap[ccix_mmap_idx].base =
+		ctx->config->mmap_table[i].base;
+	    ctx->ccix_host_info.ccix_host_mmap[ccix_mmap_idx].size =
+		ctx->config->mmap_table[i].size;
+	    ctx->ccix_host_info.ccix_host_mmap_count++;
+	}
     }
 
     FWK_LOG_INFO(MOD_NAME "Done");
@@ -682,10 +682,10 @@ static int cmn600_setup_rnsam(unsigned i
     unsigned int node_idx;
 
     for (node_idx = 0; node_idx < ctx->external_rnsam_count; node_idx++) {
-        if (ctx->external_rnsam_table[node_idx].node_id == node_id) {
-            cmn600_setup_sam(ctx->external_rnsam_table[node_idx].node);
-            return FWK_SUCCESS;
-        }
+	if (ctx->external_rnsam_table[node_idx].node_id == node_id) {
+	    cmn600_setup_sam(ctx->external_rnsam_table[node_idx].node);
+	    return FWK_SUCCESS;
+	}
     }
 
     return FWK_E_PARAM;
@@ -713,16 +713,16 @@ static int cmn600_ccix_config_get(
     struct mod_cmn600_ccix_host_node_config *config)
 {
     if (ctx->internal_rnsam_count == 0)
-        return FWK_E_DATA;
+	return FWK_E_DATA;
 
     ctx->ccix_host_info.host_ra_count =
-        ctx->internal_rnsam_count + ctx->external_rnsam_count;
+	ctx->internal_rnsam_count + ctx->external_rnsam_count;
     ctx->ccix_host_info.host_sa_count = ctx->config->sa_count;
 
     ccix_capabilities_get(ctx);
 
     memcpy((void *)config, (void *)&ctx->ccix_host_info,
-        sizeof(struct mod_cmn600_ccix_host_node_config));
+	sizeof(struct mod_cmn600_ccix_host_node_config));
     return FWK_SUCCESS;
 }
 
@@ -736,11 +736,11 @@ static int cmn600_ccix_config_set(
 
     status = ccix_setup(ctx, config);
     if (status != FWK_SUCCESS)
-        return status;
+	return status;
 
     for (i = 0; i < ctx->config->mmap_count; i++) {
-        if (ctx->config->mmap_table[i].type == MOD_CMN600_REGION_TYPE_CCIX)
-            cmn600_setup_rnsam(ctx->config->mmap_table[i].node_id);
+	if (ctx->config->mmap_table[i].type == MOD_CMN600_REGION_TYPE_CCIX)
+	    cmn600_setup_rnsam(ctx->config->mmap_table[i].node_id);
     }
     return FWK_SUCCESS;
 }
@@ -780,25 +780,25 @@ static int cmn600_init(fwk_id_t module_i
 
     /* No elements support */
     if (element_count > 0)
-        return FWK_E_DATA;
+	return FWK_E_DATA;
 
     /* Allocate space for the context */
     ctx = fwk_mm_calloc(1, sizeof(*ctx));
 
     if (config->base == 0)
-        return FWK_E_DATA;
+	return FWK_E_DATA;
 
     if ((config->mesh_size_x == 0) || (config->mesh_size_x > CMN600_MESH_X_MAX))
-        return FWK_E_DATA;
+	return FWK_E_DATA;
 
     if ((config->mesh_size_y == 0) || (config->mesh_size_y > CMN600_MESH_Y_MAX))
-        return FWK_E_DATA;
+	return FWK_E_DATA;
 
     if (config->snf_count > CMN600_HNF_CACHE_GROUP_ENTRIES_MAX)
-        return FWK_E_DATA;
+	return FWK_E_DATA;
 
     ctx->root = get_root_node(config->base, config->hnd_node_id,
-        config->mesh_size_x, config->mesh_size_y);
+	config->mesh_size_x, config->mesh_size_y);
 
     ctx->config = config;
 
@@ -812,21 +812,21 @@ static int cmn600_bind(fwk_id_t id, unsi
     /* Use second round only (round numbering is zero-indexed) */
     if (round == 1) {
 
-        /* Bind to the timer component */
-        status = fwk_module_bind(FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
-                                 FWK_ID_API(FWK_MODULE_IDX_TIMER,
-                                            MOD_TIMER_API_IDX_TIMER),
-                                 &ctx->timer_api);
-        if (status != FWK_SUCCESS)
-            return FWK_E_PANIC;
-
-        /* Bind to system info module to obtain multi-chip info */
-        status = fwk_module_bind(FWK_ID_MODULE(FWK_MODULE_IDX_SYSTEM_INFO),
-                                 FWK_ID_API(FWK_MODULE_IDX_SYSTEM_INFO,
-                                            MOD_SYSTEM_INFO_GET_API_IDX),
-                                 &system_info_api);
-        if (status != FWK_SUCCESS)
-            return FWK_E_PANIC;
+	/* Bind to the timer component */
+	status = fwk_module_bind(FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
+				 FWK_ID_API(FWK_MODULE_IDX_TIMER,
+					    MOD_TIMER_API_IDX_TIMER),
+				 &ctx->timer_api);
+	if (status != FWK_SUCCESS)
+	    return FWK_E_PANIC;
+
+	/* Bind to system info module to obtain multi-chip info */
+	status = fwk_module_bind(FWK_ID_MODULE(FWK_MODULE_IDX_SYSTEM_INFO),
+				 FWK_ID_API(FWK_MODULE_IDX_SYSTEM_INFO,
+					    MOD_SYSTEM_INFO_GET_API_IDX),
+				 &system_info_api);
+	if (status != FWK_SUCCESS)
+	    return FWK_E_PANIC;
     }
 
     return FWK_SUCCESS;
@@ -837,12 +837,12 @@ static int cmn600_process_bind_request(f
 {
     switch (fwk_id_get_api_idx(api_id)) {
     case MOD_CMN600_API_IDX_PPU_OBSERVER:
-        *api = &cmn600_observer_api;
-        break;
+	*api = &cmn600_observer_api;
+	break;
 
     case MOD_CMN600_API_IDX_CCIX_CONFIG:
-        *api = &cmn600_ccix_config_api;
-        break;
+	*api = &cmn600_ccix_config_api;
+	break;
     }
 
     return FWK_SUCCESS;
@@ -855,14 +855,14 @@ int cmn600_start(fwk_id_t id)
     int status;
 
     if (fwk_id_is_equal(ctx->config->clock_id, FWK_ID_NONE)) {
-        cmn600_setup();
-        return FWK_SUCCESS;
+	cmn600_setup();
+	return FWK_SUCCESS;
     }
 
     status = system_info_api->get_system_info(&ctx->system_info);
     if (status == FWK_SUCCESS) {
-        chip_id = ctx->system_info->chip_id;
-        mc_mode = ctx->system_info->multi_chip_mode;
+	chip_id = ctx->system_info->chip_id;
+	mc_mode = ctx->system_info->multi_chip_mode;
     }
 
     ctx->chip_id = chip_id;
@@ -873,9 +873,9 @@ int cmn600_start(fwk_id_t id)
 
     /* Register the module for clock state notifications */
     return fwk_notification_subscribe(
-        mod_clock_notification_id_state_changed,
-        ctx->config->clock_id,
-        id);
+	mod_clock_notification_id_state_changed,
+	ctx->config->clock_id,
+	id);
 }
 
 static int cmn600_process_notification(
@@ -885,13 +885,13 @@ static int cmn600_process_notification(
     struct clock_notification_params *params;
 
     fwk_assert(
-        fwk_id_is_equal(event->id, mod_clock_notification_id_state_changed));
+	fwk_id_is_equal(event->id, mod_clock_notification_id_state_changed));
     fwk_assert(fwk_id_is_type(event->target_id, FWK_ID_TYPE_MODULE));
 
     params = (struct clock_notification_params *)event->params;
 
     if (params->new_state == MOD_CLOCK_STATE_RUNNING)
-        cmn600_setup();
+	cmn600_setup();
 
     return FWK_SUCCESS;
 }
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cmn650/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/cmn650/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cmn650/CMakeLists.txt
@@ -14,9 +14,9 @@ target_include_directories(
 target_sources(
     ${SCP_MODULE_TARGET}
     PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/cmn650_ccix.c"
-            "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_cmn650.c"
-            "${CMAKE_CURRENT_SOURCE_DIR}/src/cmn650.c")
+	    "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_cmn650.c"
+	    "${CMAKE_CURRENT_SOURCE_DIR}/src/cmn650.c")
 
 target_link_libraries(
     ${SCP_MODULE_TARGET} PRIVATE module-timer module-clock module-ppu-v1
-                                 module-system-info)
+				 module-system-info)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cmn650/include/mod_cmn650.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/cmn650/include/mod_cmn650.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cmn650/include/mod_cmn650.h
@@ -189,14 +189,14 @@ struct mod_cmn650_ccix_config {
 
     /*! Table of remote region memory map entries */
     const struct mod_cmn650_ra_mem_region_map
-        ra_mmap_table[CMN650_MAX_RA_SAM_ADDR_REGION];
+	ra_mmap_table[CMN650_MAX_RA_SAM_ADDR_REGION];
 
     /*! Number of entries in the ::mod_cmn650_ccix_config::ra_mmap_table */
     size_t ra_mmap_count;
 
     /*! Table of remote agent ids start and end backed by the links */
     struct mod_cmn650_agentid_to_linkid_map
-        remote_agentid_to_linkid_map[CMN650_MAX_CCIX_PROTOCOL_LINKS];
+	remote_agentid_to_linkid_map[CMN650_MAX_CCIX_PROTOCOL_LINKS];
 
     /*! SMP Mode */
     bool smp_mode;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cmn650/src/cmn650.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/cmn650/src/cmn650.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cmn650/src/cmn650.c
@@ -48,7 +48,7 @@ unsigned int get_node_logical_id(void *n
 {
     struct node_header *node = node_base;
     return (node->NODE_INFO & CMN650_NODE_INFO_LOGICAL_ID) >>
-        CMN650_NODE_INFO_LOGICAL_ID_POS;
+	CMN650_NODE_INFO_LOGICAL_ID_POS;
 }
 
 void *get_child_node(uintptr_t base, void *node_base, unsigned int child_index)
@@ -72,8 +72,8 @@ unsigned int get_child_node_id(void *nod
     unsigned int node_id;
 
     node_pointer = (node->CHILD_POINTER[child_index] &
-                    CMN650_CHILD_POINTER_EXT_NODE_POINTER) >>
-        CMN650_CHILD_POINTER_EXT_NODE_POINTER_POS;
+		    CMN650_CHILD_POINTER_EXT_NODE_POINTER) >>
+	CMN650_CHILD_POINTER_EXT_NODE_POINTER_POS;
 
     /*
      * For mesh widths using 2 bits each for X,Y encoding:
@@ -89,7 +89,7 @@ unsigned int get_child_node_id(void *nod
      * NodeID[8:6] = NODE POINTER[11:9]
      */
     node_id = (((node_pointer >> 6) & 0xff) << 3) |
-        ((node_pointer & 0x1) << 2) | ((node_pointer >> 2) & 0x3);
+	((node_pointer & 0x1) << 2) | ((node_pointer >> 2) & 0x3);
 
     return node_id;
 }
@@ -97,7 +97,7 @@ unsigned int get_child_node_id(void *nod
 unsigned int get_cmn650_revision(struct cmn650_cfgm_reg *root)
 {
     return (root->PERIPH_ID[1] & CMN650_PERIPH_ID_2_MASK) >>
-        CMN650_PERIPH_ID_2_REV_POS;
+	CMN650_PERIPH_ID_2_REV_POS;
 }
 
 const char *get_cmn650_revision_name(struct cmn650_cfgm_reg *root)
@@ -106,7 +106,7 @@ const char *get_cmn650_revision_name(str
 
     revision = get_cmn650_revision(root);
     if (revision > CMN650_PERIPH_ID_UNKNOWN_REV)
-        revision = CMN650_PERIPH_ID_UNKNOWN_REV;
+	revision = CMN650_PERIPH_ID_UNKNOWN_REV;
 
     return cmn650_rev_to_name[revision];
 }
@@ -120,30 +120,30 @@ bool is_child_external(void *node_base,
 bool get_port_number(unsigned int child_node_id)
 {
     return (child_node_id >> CMN650_NODE_ID_PORT_POS) &
-        CMN650_NODE_ID_PORT_MASK;
+	CMN650_NODE_ID_PORT_MASK;
 }
 
 unsigned int get_device_type(void *mxp_base, bool port)
 {
     struct cmn650_mxp_reg *mxp = mxp_base;
     return mxp->PORT_CONNECT_INFO[port] &
-        CMN650_MXP_PORT_CONNECT_INFO_DEVICE_TYPE_MASK;
+	CMN650_MXP_PORT_CONNECT_INFO_DEVICE_TYPE_MASK;
 }
 
 bool is_cal_connected(void *mxp_base, uint8_t port)
 {
     struct cmn650_mxp_reg *mxp = mxp_base;
     return (mxp->PORT_CONNECT_INFO[port] &
-            CMN650_MXP_PORT_CONNECT_INFO_CAL_CONNECTED_MASK) >>
-        CMN650_MXP_PORT_CONNECT_INFO_CAL_CONNECTED_POS;
+	    CMN650_MXP_PORT_CONNECT_INFO_CAL_CONNECTED_MASK) >>
+	CMN650_MXP_PORT_CONNECT_INFO_CAL_CONNECTED_POS;
 }
 
 bool is_device_type_rnf(void *mxp_base, uint8_t port)
 {
     return (
-        (get_device_type(mxp_base, port) == DEVICE_TYPE_RN_F_CHIB_ESAM) ||
-        (get_device_type(mxp_base, port) == DEVICE_TYPE_RN_F_CHIC_ESAM) ||
-        (get_device_type(mxp_base, port) == DEVICE_TYPE_RN_F_CHID_ESAM));
+	(get_device_type(mxp_base, port) == DEVICE_TYPE_RN_F_CHIB_ESAM) ||
+	(get_device_type(mxp_base, port) == DEVICE_TYPE_RN_F_CHIC_ESAM) ||
+	(get_device_type(mxp_base, port) == DEVICE_TYPE_RN_F_CHID_ESAM));
 }
 
 uint64_t sam_encode_region_size(uint64_t size)
@@ -209,11 +209,11 @@ const char *get_node_type_name(enum node
 {
     /* Base node IDs */
     if (node_type <= NODE_TYPE_RN_SAM)
-        return type_to_name[node_type];
+	return type_to_name[node_type];
 
     /* CML node IDs */
     if ((node_type >= NODE_TYPE_CML_BASE) && (node_type <= NODE_TYPE_CXLA))
-        return type_to_name_cml[node_type - NODE_TYPE_CML_BASE];
+	return type_to_name_cml[node_type - NODE_TYPE_CML_BASE];
 
     /* Invalid node IDs */
     return type_to_name[NODE_TYPE_INVALID];
@@ -223,7 +223,7 @@ unsigned int get_node_pos_x(void *node_b
 {
     struct node_header *node = node_base;
     return (get_node_id(node) >> (CMN650_NODE_ID_Y_POS + encoding_bits)) &
-        mask_bits;
+	mask_bits;
 }
 
 unsigned int get_node_pos_y(void *node_base)
@@ -253,14 +253,14 @@ struct cmn650_cfgm_reg *get_root_node(
     mask_bits = (1 << encoding_bits) - 1;
     node_pos_y = (hnd_node_id >> CMN650_NODE_ID_Y_POS) & mask_bits;
     node_pos_x =
-        (hnd_node_id >> (CMN650_NODE_ID_Y_POS + encoding_bits)) & mask_bits;
+	(hnd_node_id >> (CMN650_NODE_ID_Y_POS + encoding_bits)) & mask_bits;
     node_port =
-        (hnd_node_id >> CMN650_NODE_ID_PORT_POS) & CMN650_NODE_ID_PORT_MASK;
+	(hnd_node_id >> CMN650_NODE_ID_PORT_POS) & CMN650_NODE_ID_PORT_MASK;
 
     /* Calculate node address offset */
     offset = (node_pos_y << CMN650_ROOT_NODE_OFFSET_Y_POS) |
-        (node_pos_x << (CMN650_ROOT_NODE_OFFSET_Y_POS + encoding_bits)) |
-        (node_port << CMN650_ROOT_NODE_OFFSET_PORT_POS);
+	(node_pos_x << (CMN650_ROOT_NODE_OFFSET_Y_POS + encoding_bits)) |
+	(node_port << CMN650_ROOT_NODE_OFFSET_PORT_POS);
 
     return (struct cmn650_cfgm_reg *)(base + offset);
 }
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cmn650/src/cmn650.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/cmn650/src/cmn650.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cmn650/src/cmn650.h
@@ -91,9 +91,9 @@ enum device_type {
 /* Common node header */
 struct node_header {
     FWK_R uint64_t NODE_INFO;
-          uint8_t  RESERVED0[0x80 - 0x8];
+	  uint8_t  RESERVED0[0x80 - 0x8];
     FWK_R uint64_t CHILD_INFO;
-          uint8_t  RESERVED1[0x100 - 0x88];
+	  uint8_t  RESERVED1[0x100 - 0x88];
     FWK_R uint64_t CHILD_POINTER[256];
 };
 
@@ -109,42 +109,42 @@ enum sam_node_type {
  */
 struct cmn650_rnsam_reg {
     FWK_R  uint64_t NODE_INFO;
-           uint8_t  RESERVED0[0x80 - 0x8];
+	   uint8_t  RESERVED0[0x80 - 0x8];
     FWK_R  uint64_t CHILD_INFO;
-           uint8_t  RESERVED1[0x900 - 0x88];
+	   uint8_t  RESERVED1[0x900 - 0x88];
     FWK_R  uint64_t UNIT_INFO;
-           uint8_t  RESERVED2[0xC00 - 0x908];
+	   uint8_t  RESERVED2[0xC00 - 0x908];
     FWK_RW uint64_t NON_HASH_MEM_REGION[20];
-           uint8_t  RESERVED3[0xD80 - 0xCA0];
+	   uint8_t  RESERVED3[0xD80 - 0xCA0];
     FWK_RW uint64_t NON_HASH_TGT_NODEID[5];
-           uint8_t  RESERVED4[0xE00 - 0xDA8];
+	   uint8_t  RESERVED4[0xE00 - 0xDA8];
     FWK_RW uint64_t SYS_CACHE_GRP_REGION[4];
-           uint8_t  RESERVED5[0xE80 - 0xE20];
+	   uint8_t  RESERVED5[0xE80 - 0xE20];
     FWK_RW uint64_t RNSAM_HASH_ADDR_MASK_REG;
-           uint8_t  RESERVED17[0xEA0 - 0xE88];
+	   uint8_t  RESERVED17[0xEA0 - 0xE88];
     FWK_RW uint64_t SYS_CACHE_GRP_HN_COUNT;
-           uint8_t  RESERVED6[0xF00 - 0xEA8];
+	   uint8_t  RESERVED6[0xF00 - 0xEA8];
     FWK_RW uint64_t SYS_CACHE_GRP_HN_NODEID[16];
-           uint8_t  RESERVED7[0x1000 - 0xF80];
+	   uint8_t  RESERVED7[0x1000 - 0xF80];
     FWK_RW uint64_t SYS_CACHE_GRP_SN_NODEID[16];
-           uint8_t  RESERVED8[0x1100 - 0x1080];
+	   uint8_t  RESERVED8[0x1100 - 0x1080];
     FWK_RW uint64_t STATUS;
-           uint64_t GIC_MEM_REGION;
-           uint8_t  RESERVED9[0x1120 - 0x1110];
+	   uint64_t GIC_MEM_REGION;
+	   uint8_t  RESERVED9[0x1120 - 0x1110];
     FWK_RW uint64_t SYS_CACHE_GRP_CAL_MODE;
-           uint8_t  RESERVED10[0x1140 - 0x1128];
+	   uint8_t  RESERVED10[0x1140 - 0x1128];
     FWK_RW uint64_t SYS_CACHE_GRP_SN_SAM_CFG[2];
-           uint8_t  RESERVED11[0x1180 - 0x1150];
+	   uint8_t  RESERVED11[0x1180 - 0x1150];
     FWK_RW uint64_t SYS_CACHE_GRP_HN_CPA_EN;
-           uint8_t  RESERVED12[0x1190 - 0x1188];
+	   uint8_t  RESERVED12[0x1190 - 0x1188];
     FWK_RW uint64_t SYS_CACHE_GRP_HN_CPA_GRP;
-           uint8_t  RESERVED13[0x11A0 - 0x1198];
+	   uint8_t  RESERVED13[0x11A0 - 0x1198];
     FWK_RW uint64_t CML_PORT_AGGR_MODE_CTRL[2];
-           uint8_t  RESERVED14[0x11C0 - 0x11B0];
+	   uint8_t  RESERVED14[0x11C0 - 0x11B0];
     FWK_RW uint64_t CML_PORT_AGGR_GRP_ADD_MASK[5];
-           uint8_t  RESERVED15[0x11F0 - 0x11E8];
+	   uint8_t  RESERVED15[0x11F0 - 0x11E8];
     FWK_RW uint64_t CML_PORT_AGGR_GRP[2];
-           uint8_t  RESERVED16[0x1208 - 0x1200];
+	   uint8_t  RESERVED16[0x1208 - 0x1200];
     FWK_RW uint64_t CML_PORT_AGGR_CTRL;
 };
 
@@ -153,24 +153,24 @@ struct cmn650_rnsam_reg {
  */
 struct cmn650_hnf_reg {
     FWK_R  uint64_t NODE_INFO;
-           uint8_t  RESERVED0[0x80 - 0x8];
+	   uint8_t  RESERVED0[0x80 - 0x8];
     FWK_R  uint64_t CHILD_INFO;
-           uint8_t  RESERVED1[0x900 - 0x88];
+	   uint8_t  RESERVED1[0x900 - 0x88];
     FWK_R  uint64_t UNIT_INFO;
-           uint8_t  RESERVED2[0xCF0 - 0x908];
+	   uint8_t  RESERVED2[0xCF0 - 0x908];
     FWK_RW uint64_t HN_SAM_HASH_ADDR_MASK_REG;
-           uint8_t  RESERVED8[0xD00 - 0xCF8];
+	   uint8_t  RESERVED8[0xD00 - 0xCF8];
     FWK_RW uint64_t SAM_CONTROL;
     FWK_RW uint64_t SAM_MEMREGION[2];
-           uint8_t  RESERVED3[0xD28 - 0xD18];
+	   uint8_t  RESERVED3[0xD28 - 0xD18];
     FWK_RW uint64_t RN_PHYS_ID[64];
-           uint8_t  RESERVED4[0xF80 - 0xF28];
+	   uint8_t  RESERVED4[0xF80 - 0xF28];
     FWK_RW uint64_t CML_PORT_AGGR_GRP_ADD_MASK[5];
-           uint8_t  RESERVED5[0xFB0 - 0xFA8];
+	   uint8_t  RESERVED5[0xFB0 - 0xFA8];
     FWK_RW uint64_t CML_PORT_AGGR_GRP[2];
-           uint8_t  RESERVED6[0xFD0 - 0xFC0];
+	   uint8_t  RESERVED6[0xFD0 - 0xFC0];
     FWK_RW uint64_t CML_PORT_AGGR_CTRL;
-           uint8_t  RESERVED7[0x1C00 - 0xFD8];
+	   uint8_t  RESERVED7[0x1C00 - 0xFD8];
     FWK_RW uint64_t PPU_PWPR;
 };
 
@@ -187,20 +187,20 @@ struct cxg_link_regs {
  */
 struct cmn650_cxg_ra_reg {
     FWK_R  uint64_t CXG_RA_NODE_INFO;
-           uint8_t  RESERVED0[0x80-0x8];
+	   uint8_t  RESERVED0[0x80-0x8];
     FWK_R  uint64_t CXG_RA_CHILD_INFO;
-           uint8_t  RESERVED1[0x900-0x88];
+	   uint8_t  RESERVED1[0x900-0x88];
     FWK_R  uint64_t CXG_RA_UNIT_INFO;
-           uint8_t  RESERVED2[0x980-0x908];
+	   uint8_t  RESERVED2[0x980-0x908];
     FWK_RW uint64_t CXG_RA_SEC_REG_GRP_OVERRIDE;
-           uint8_t  RESERVED3[0xA00-0x988];
+	   uint8_t  RESERVED3[0xA00-0x988];
     FWK_RW uint64_t CXG_RA_CFG_CTRL;
     FWK_RW uint64_t CXG_RA_AUX_CTRL;
-           uint8_t  RESERVED4[0xDA8-0xA10];
+	   uint8_t  RESERVED4[0xDA8-0xA10];
     FWK_RW uint64_t CXG_RA_SAM_ADDR_REGION_REG[8];
-           uint8_t  RESERVED5[0xE00-0xDE8];
+	   uint8_t  RESERVED5[0xE00-0xDE8];
     FWK_RW uint64_t CXG_RA_SAM_MEM_REGION_LIMIT_REG[8];
-           uint8_t  RESERVED6[0xE60-0xE40];
+	   uint8_t  RESERVED6[0xE60-0xE40];
     FWK_RW uint64_t CXG_RA_AGENTID_TO_LINKID_REG[8];
     FWK_RW uint64_t CXG_RA_RNF_LDID_TO_RAID_REG[8];
     FWK_RW uint64_t CXG_RA_RNI_LDID_TO_RAID_REG[4];
@@ -209,9 +209,9 @@ struct cmn650_cxg_ra_reg {
     FWK_RW uint64_t CXG_RA_RNF_LDID_TO_RAID_VAL;
     FWK_RW uint64_t CXG_RA_RNI_LDID_TO_RAID_VAL;
     FWK_RW uint64_t CXG_RA_RND_LDID_TO_RAID_VAL;
-           uint8_t  RESERVED7[0x1C00-0xF40];
-           struct cxg_link_regs LINK_REGS[3];
-           uint8_t  RESERVED8[0x2000-0x1030];
+	   uint8_t  RESERVED7[0x1C00-0xF40];
+	   struct cxg_link_regs LINK_REGS[3];
+	   uint8_t  RESERVED8[0x2000-0x1030];
     FWK_RW uint64_t CXG_RA_PMU_EVENT_SEL;
 };
 
@@ -221,23 +221,23 @@ struct cmn650_cxg_ra_reg {
 struct cmn650_cxg_ha_reg {
     FWK_R  uint64_t CXG_HA_NODE_INFO;
     FWK_RW uint64_t CXG_HA_ID;
-           uint8_t  RESERVED0[0x80-0x10];
+	   uint8_t  RESERVED0[0x80-0x10];
     FWK_R  uint64_t CXG_HA_CHILD_INFO;
-           uint8_t  RESERVED1[0x900-0x88];
+	   uint8_t  RESERVED1[0x900-0x88];
     FWK_R  uint64_t CXG_HA_UNIT_INFO;
-           uint8_t  RESERVED2[0x980-0x908];
+	   uint8_t  RESERVED2[0x980-0x908];
     FWK_RW uint64_t CXG_HA_SEC_REG_GRP_OVERRIDE;
-           uint8_t  RESERVED3[0xA08-0x988];
+	   uint8_t  RESERVED3[0xA08-0x988];
     FWK_RW uint64_t CXG_HA_AUX_CTRL;
-           uint8_t  RESERVED4[0xC00-0xA10];
+	   uint8_t  RESERVED4[0xC00-0xA10];
     FWK_RW uint64_t CXG_HA_RNF_RAID_TO_LDID_REG[8];
     FWK_RW uint64_t CXG_HA_AGENTID_TO_LINKID_REG[8];
-           uint8_t  RESERVED5[0xD00-0xC80];
+	   uint8_t  RESERVED5[0xD00-0xC80];
     FWK_RW uint64_t CXG_HA_AGENTID_TO_LINKID_VAL;
     FWK_RW uint64_t CXG_HA_RNF_RAID_TO_LDID_VAL;
-           uint8_t  RESERVED6[0x1C00-0xD10];
-           struct cxg_link_regs LINK_REGS[3];
-           uint8_t  RESERVED7[0x2000-0x1030];
+	   uint8_t  RESERVED6[0x1C00-0xD10];
+	   struct cxg_link_regs LINK_REGS[3];
+	   uint8_t  RESERVED7[0x2000-0x1030];
     FWK_RW uint64_t CXG_HA_PMU_EVENT_SEL;
 };
 
@@ -246,20 +246,20 @@ struct cmn650_cxg_ha_reg {
  */
 struct cmn650_cxla_reg {
     FWK_R  uint64_t CXLA_NODE_INFO;
-           uint8_t  RESERVED0[0x80-0x8];
+	   uint8_t  RESERVED0[0x80-0x8];
     FWK_R  uint64_t CXLA_CHILD_INFO;
-           uint8_t  RESERVED1[0x900-0x88];
+	   uint8_t  RESERVED1[0x900-0x88];
     FWK_R  uint64_t CXLA_UNIT_INFO;
-           uint8_t  RESERVED2[0x980-0x908];
+	   uint8_t  RESERVED2[0x980-0x908];
     FWK_RW uint64_t CXLA_SEC_REG_GRP_OVERRIDE;
-           uint8_t  RESERVED3[0xA08-0x988];
+	   uint8_t  RESERVED3[0xA08-0x988];
     FWK_RW uint64_t CXLA_AUX_CTRL;
-           uint8_t  RESERVED4[0xC00-0xA10];
+	   uint8_t  RESERVED4[0xC00-0xA10];
     FWK_R  uint64_t CXLA_CCIX_PROP_CAPABILITIES;
     FWK_RW uint64_t CXLA_CCIX_PROP_CONFIGURED;
     FWK_R  uint64_t CXLA_TX_CXS_ATTR_CAPABILITIES;
     FWK_R  uint64_t CXLA_RX_CXS_ATTR_CAPABILITIES;
-           uint8_t  RESERVED5[0xC30-0xC20];
+	   uint8_t  RESERVED5[0xC30-0xC20];
     FWK_RW uint64_t CXLA_AGENTID_TO_LINKID_REG[8];
     FWK_RW uint64_t CXLA_AGENTID_TO_LINKID_VAL;
     FWK_RW uint64_t CXLA_LINKID_TO_PCIE_BUS_NUM;
@@ -273,7 +273,7 @@ struct cmn650_cfgm_reg {
     FWK_R  uint64_t NODE_INFO;
     FWK_RW uint64_t PERIPH_ID[4];
     FWK_RW uint64_t COMPONENT_ID[2];
-           uint8_t  RESERVED0[0x80 - 0x38];
+	   uint8_t  RESERVED0[0x80 - 0x38];
     FWK_R  uint64_t CHILD_INFO;
 };
 
@@ -283,9 +283,9 @@ struct cmn650_cfgm_reg {
 struct cmn650_mxp_reg {
     FWK_R  uint64_t NODE_INFO;
     FWK_R  uint64_t PORT_CONNECT_INFO[2];
-           uint8_t  RESERVED0[0x80 - 0x18];
+	   uint8_t  RESERVED0[0x80 - 0x18];
     FWK_R  uint64_t CHILD_INFO;
-           uint8_t  RESERVED1[0x100 - 0x88];
+	   uint8_t  RESERVED1[0x100 - 0x88];
     FWK_R  uint64_t CHILD_POINTER[16];
 };
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cmn650/src/cmn650_ccix.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/cmn650/src/cmn650_ccix.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cmn650/src/cmn650_ccix.c
@@ -38,7 +38,7 @@ static const struct mod_cmn650_ccix_conf
 static unsigned int get_ldid(bool program_for_port_aggregation)
 {
     return (program_for_port_aggregation) ? config->port_aggregate_ldid :
-                                            config->ldid;
+					    config->ldid;
 }
 
 static bool cxg_link_wait_condition(void *data)
@@ -52,7 +52,7 @@ static bool cxg_link_wait_condition(void
     fwk_assert(data != NULL);
 
     struct cxg_wait_condition_data *wait_data =
-        (struct cxg_wait_condition_data *)data;
+	(struct cxg_wait_condition_data *)data;
     struct cmn650_device_ctx *ctx = wait_data->ctx;
     unsigned int cxg_ldid = get_ldid(cmn650_ccix_ctx.is_prog_for_port_agg);
     linkid = wait_data->linkid;
@@ -62,58 +62,58 @@ static bool cxg_link_wait_condition(void
 
     switch (wait_data->cond) {
     case CXG_LINK_CTRL_EN_BIT_SET:
-        val1 = cxg_ra_reg->LINK_REGS[linkid].CXG_PRTCL_LINK_CTRL;
-        val2 = cxg_ha_reg->LINK_REGS[linkid].CXG_PRTCL_LINK_CTRL;
-        return (
-            ((val1 & CXG_LINK_CTRL_EN_MASK) &&
-             (val2 & CXG_LINK_CTRL_EN_MASK)) != 0);
+	val1 = cxg_ra_reg->LINK_REGS[linkid].CXG_PRTCL_LINK_CTRL;
+	val2 = cxg_ha_reg->LINK_REGS[linkid].CXG_PRTCL_LINK_CTRL;
+	return (
+	    ((val1 & CXG_LINK_CTRL_EN_MASK) &&
+	     (val2 & CXG_LINK_CTRL_EN_MASK)) != 0);
 
     case CXG_LINK_CTRL_UP_BIT_CLR:
-        val1 = cxg_ra_reg->LINK_REGS[linkid].CXG_PRTCL_LINK_CTRL;
-        val2 = cxg_ha_reg->LINK_REGS[linkid].CXG_PRTCL_LINK_CTRL;
-        return (
-            (((val1 & CXG_LINK_CTRL_UP_MASK) == 0) &&
-             ((val2 & CXG_LINK_CTRL_UP_MASK) == 0)) != 0);
+	val1 = cxg_ra_reg->LINK_REGS[linkid].CXG_PRTCL_LINK_CTRL;
+	val2 = cxg_ha_reg->LINK_REGS[linkid].CXG_PRTCL_LINK_CTRL;
+	return (
+	    (((val1 & CXG_LINK_CTRL_UP_MASK) == 0) &&
+	     ((val2 & CXG_LINK_CTRL_UP_MASK) == 0)) != 0);
 
     case CXG_LINK_STATUS_DWN_BIT_SET:
-        val1 = cxg_ra_reg->LINK_REGS[linkid].CXG_PRTCL_LINK_STATUS;
-        val2 = cxg_ha_reg->LINK_REGS[linkid].CXG_PRTCL_LINK_STATUS;
-        return (
-            ((val1 & CXG_LINK_STATUS_DOWN_MASK) &&
-             (val2 & CXG_LINK_STATUS_DOWN_MASK)) != 0);
+	val1 = cxg_ra_reg->LINK_REGS[linkid].CXG_PRTCL_LINK_STATUS;
+	val2 = cxg_ha_reg->LINK_REGS[linkid].CXG_PRTCL_LINK_STATUS;
+	return (
+	    ((val1 & CXG_LINK_STATUS_DOWN_MASK) &&
+	     (val2 & CXG_LINK_STATUS_DOWN_MASK)) != 0);
 
     case CXG_LINK_STATUS_DWN_BIT_CLR:
-        val1 = cxg_ra_reg->LINK_REGS[linkid].CXG_PRTCL_LINK_STATUS;
-        val2 = cxg_ha_reg->LINK_REGS[linkid].CXG_PRTCL_LINK_STATUS;
-        return (
-            (((val1 & CXG_LINK_STATUS_DOWN_MASK) == 0) &&
-             ((val2 & CXG_LINK_STATUS_DOWN_MASK) == 0)) != 0);
+	val1 = cxg_ra_reg->LINK_REGS[linkid].CXG_PRTCL_LINK_STATUS;
+	val2 = cxg_ha_reg->LINK_REGS[linkid].CXG_PRTCL_LINK_STATUS;
+	return (
+	    (((val1 & CXG_LINK_STATUS_DOWN_MASK) == 0) &&
+	     ((val2 & CXG_LINK_STATUS_DOWN_MASK) == 0)) != 0);
 
     case CXG_LINK_STATUS_ACK_BIT_SET:
-        val1 = cxg_ra_reg->LINK_REGS[linkid].CXG_PRTCL_LINK_STATUS;
-        val2 = cxg_ha_reg->LINK_REGS[linkid].CXG_PRTCL_LINK_STATUS;
-        return (
-            ((val1 & CXG_LINK_STATUS_ACK_MASK) &&
-             (val2 & CXG_LINK_STATUS_ACK_MASK)) != 0);
+	val1 = cxg_ra_reg->LINK_REGS[linkid].CXG_PRTCL_LINK_STATUS;
+	val2 = cxg_ha_reg->LINK_REGS[linkid].CXG_PRTCL_LINK_STATUS;
+	return (
+	    ((val1 & CXG_LINK_STATUS_ACK_MASK) &&
+	     (val2 & CXG_LINK_STATUS_ACK_MASK)) != 0);
 
     case CXG_LINK_STATUS_ACK_BIT_CLR:
-        val1 = cxg_ra_reg->LINK_REGS[linkid].CXG_PRTCL_LINK_STATUS;
-        val2 = cxg_ha_reg->LINK_REGS[linkid].CXG_PRTCL_LINK_STATUS;
-        return (
-            (((val1 & CXG_LINK_STATUS_ACK_MASK) == 0) &&
-             ((val2 & CXG_LINK_STATUS_ACK_MASK) == 0)) != 0);
+	val1 = cxg_ra_reg->LINK_REGS[linkid].CXG_PRTCL_LINK_STATUS;
+	val2 = cxg_ha_reg->LINK_REGS[linkid].CXG_PRTCL_LINK_STATUS;
+	return (
+	    (((val1 & CXG_LINK_STATUS_ACK_MASK) == 0) &&
+	     ((val2 & CXG_LINK_STATUS_ACK_MASK) == 0)) != 0);
 
     case CXG_LINK_STATUS_HA_DVMDOMAIN_ACK_BIT_SET:
-        val1 = cxg_ha_reg->LINK_REGS[linkid].CXG_PRTCL_LINK_STATUS;
-        return (((val1 & CXG_LINK_STATUS_DVMDOMAIN_ACK_MASK)) != 0);
+	val1 = cxg_ha_reg->LINK_REGS[linkid].CXG_PRTCL_LINK_STATUS;
+	return (((val1 & CXG_LINK_STATUS_DVMDOMAIN_ACK_MASK)) != 0);
 
     case CXG_LINK_STATUS_RA_DVMDOMAIN_ACK_BIT_SET:
-        val1 = cxg_ra_reg->LINK_REGS[linkid].CXG_PRTCL_LINK_STATUS;
-        return (((val1 & CXG_LINK_STATUS_DVMDOMAIN_ACK_MASK)) != 0);
+	val1 = cxg_ra_reg->LINK_REGS[linkid].CXG_PRTCL_LINK_STATUS;
+	return (((val1 & CXG_LINK_STATUS_DVMDOMAIN_ACK_MASK)) != 0);
 
     default:
-        fwk_unexpected();
-        return false;
+	fwk_unexpected();
+	return false;
     }
 }
 
@@ -135,7 +135,7 @@ static void program_cxg_ra_rnf_ldid_to_r
 
     /* Adding raid_value into LDID-to-RAID register */
     cxg_ra_reg->CXG_RA_RNF_LDID_TO_RAID_REG[reg_offset] |=
-        ((uint64_t)raid << (ldid_value_offset * NUM_BITS_RESERVED_FOR_RAID));
+	((uint64_t)raid << (ldid_value_offset * NUM_BITS_RESERVED_FOR_RAID));
 
     /* Set corresponding valid bit */
     cxg_ra_reg->CXG_RA_RNF_LDID_TO_RAID_VAL |= ((uint64_t)0x1 << ldid_value);
@@ -161,7 +161,7 @@ static void program_cxg_ra_rni_ldid_to_r
 
     /* Adding raid_value into LDID-to-RAID register */
     cxg_ra_reg->CXG_RA_RNI_LDID_TO_RAID_REG[reg_offset] |=
-        ((uint64_t)raid << (ldid_value_offset * NUM_BITS_RESERVED_FOR_RAID));
+	((uint64_t)raid << (ldid_value_offset * NUM_BITS_RESERVED_FOR_RAID));
 
     /* Set corresponding valid bit */
     cxg_ra_reg->CXG_RA_RNI_LDID_TO_RAID_VAL |= ((uint64_t)0x1 << ldid_value);
@@ -187,7 +187,7 @@ static void program_cxg_ra_rnd_ldid_to_r
 
     /* Adding raid_value into LDID-to-RAID register */
     cxg_ra_reg->CXG_RA_RND_LDID_TO_RAID_REG[reg_offset] |=
-        ((uint64_t)raid << (ldid_value_offset * NUM_BITS_RESERVED_FOR_RAID));
+	((uint64_t)raid << (ldid_value_offset * NUM_BITS_RESERVED_FOR_RAID));
 
     /* Set corresponding valid bit */
     cxg_ra_reg->CXG_RA_RND_LDID_TO_RAID_VAL |= ((uint64_t)0x1 << ldid_value);
@@ -215,50 +215,50 @@ static void program_agentid_to_linkid_re
     cxla_reg = ctx->cxla_reg_table[cxg_ldid].cxla_reg;
 
     for (linkid = 0; linkid < CMN650_MAX_CCIX_PROTOCOL_LINKS; linkid++) {
-        remote_agentid_start =
-            config->remote_agentid_to_linkid_map[linkid].remote_agentid_start;
-        remote_agentid_end =
-            config->remote_agentid_to_linkid_map[linkid].remote_agentid_end;
-
-        /*
-         * Skip configuring link if both start and end are 0, indicating
-         * there's no link
-         */
-        if ((remote_agentid_start == 0) && (remote_agentid_end == 0))
-            continue;
-
-        FWK_LOG_INFO(
-            MOD_NAME "  Remote [AgentID %d - AgentID %d] Link %d",
-            remote_agentid_start,
-            remote_agentid_end,
-            linkid);
-
-        for (agentid = remote_agentid_start; agentid <= remote_agentid_end;
-             agentid++) {
-            /* Each register is 64 bits and holds 8 AgentID/LinkID mappings */
-            reg_index = agentid / 8;
-            agentid_bit_offset = agentid % 8;
-
-            /* Writing AgentID-to-LinkID mappings */
-            cxg_ra_reg->CXG_RA_AGENTID_TO_LINKID_REG[reg_index] |=
-                ((uint64_t)linkid
-                 << (agentid_bit_offset * NUM_BITS_RESERVED_FOR_LINKID));
-
-            cxg_ha_reg->CXG_HA_AGENTID_TO_LINKID_REG[reg_index] |=
-                ((uint64_t)linkid
-                 << (agentid_bit_offset * NUM_BITS_RESERVED_FOR_LINKID));
-
-            cxla_reg->CXLA_AGENTID_TO_LINKID_REG[reg_index] |=
-                ((uint64_t)linkid
-                 << (agentid_bit_offset * NUM_BITS_RESERVED_FOR_LINKID));
-
-            /* Setting corresponding valid bits */
-            cxg_ra_reg->CXG_RA_AGENTID_TO_LINKID_VAL |=
-                ((uint64_t)0x1 << agentid);
-            cxg_ha_reg->CXG_HA_AGENTID_TO_LINKID_VAL |=
-                ((uint64_t)0x1 << agentid);
-            cxla_reg->CXLA_AGENTID_TO_LINKID_VAL |= ((uint64_t)0x1 << agentid);
-        }
+	remote_agentid_start =
+	    config->remote_agentid_to_linkid_map[linkid].remote_agentid_start;
+	remote_agentid_end =
+	    config->remote_agentid_to_linkid_map[linkid].remote_agentid_end;
+
+	/*
+	 * Skip configuring link if both start and end are 0, indicating
+	 * there's no link
+	 */
+	if ((remote_agentid_start == 0) && (remote_agentid_end == 0))
+	    continue;
+
+	FWK_LOG_INFO(
+	    MOD_NAME "  Remote [AgentID %d - AgentID %d] Link %d",
+	    remote_agentid_start,
+	    remote_agentid_end,
+	    linkid);
+
+	for (agentid = remote_agentid_start; agentid <= remote_agentid_end;
+	     agentid++) {
+	    /* Each register is 64 bits and holds 8 AgentID/LinkID mappings */
+	    reg_index = agentid / 8;
+	    agentid_bit_offset = agentid % 8;
+
+	    /* Writing AgentID-to-LinkID mappings */
+	    cxg_ra_reg->CXG_RA_AGENTID_TO_LINKID_REG[reg_index] |=
+		((uint64_t)linkid
+		 << (agentid_bit_offset * NUM_BITS_RESERVED_FOR_LINKID));
+
+	    cxg_ha_reg->CXG_HA_AGENTID_TO_LINKID_REG[reg_index] |=
+		((uint64_t)linkid
+		 << (agentid_bit_offset * NUM_BITS_RESERVED_FOR_LINKID));
+
+	    cxla_reg->CXLA_AGENTID_TO_LINKID_REG[reg_index] |=
+		((uint64_t)linkid
+		 << (agentid_bit_offset * NUM_BITS_RESERVED_FOR_LINKID));
+
+	    /* Setting corresponding valid bits */
+	    cxg_ra_reg->CXG_RA_AGENTID_TO_LINKID_VAL |=
+		((uint64_t)0x1 << agentid);
+	    cxg_ha_reg->CXG_HA_AGENTID_TO_LINKID_VAL |=
+		((uint64_t)0x1 << agentid);
+	    cxla_reg->CXLA_AGENTID_TO_LINKID_VAL |= ((uint64_t)0x1 << agentid);
+	}
     }
 }
 
@@ -270,15 +270,15 @@ static void program_cxg_ha_id(struct cmn
 
     cxg_ha_reg = ctx->cxg_ha_reg_table[cxg_ldid].cxg_ha_reg;
     cxg_haid = cmn650_ccix_ctx.is_prog_for_port_agg ?
-        config->port_aggregate_haid :
-        config->haid;
+	config->port_aggregate_haid :
+	config->haid;
     cxg_ha_reg->CXG_HA_ID = cxg_haid;
 
     FWK_LOG_INFO(
-        MOD_NAME "HAID for CCIX %d with nodeid %d: HAID %d",
-        cxg_ldid,
-        get_node_id(cxg_ha_reg),
-        cxg_haid);
+	MOD_NAME "HAID for CCIX %d with nodeid %d: HAID %d",
+	cxg_ldid,
+	get_node_id(cxg_ha_reg),
+	cxg_haid);
 }
 
 static void program_cxg_ha_raid_to_ldid_lut(
@@ -299,8 +299,8 @@ static void program_cxg_ha_raid_to_ldid_
 
     /* Write RAID-to-LDID mapping (with RNF bit set) */
     cxg_ha_reg->CXG_HA_RNF_RAID_TO_LDID_REG[reg_index] |=
-        ((uint64_t)(ldid_value | CXG_HA_RAID_TO_LDID_RNF_MASK)
-         << (raid_bit_offset * NUM_BITS_RESERVED_FOR_LDID));
+	((uint64_t)(ldid_value | CXG_HA_RAID_TO_LDID_RNF_MASK)
+	 << (raid_bit_offset * NUM_BITS_RESERVED_FOR_LDID));
 
     /* Set corresponding valid bit */
     cxg_ha_reg->CXG_HA_RNF_RAID_TO_LDID_VAL |= ((uint64_t)0x1 << raid_id);
@@ -328,95 +328,95 @@ static void program_hnf_ldid_to_chi_node
     cpa_nodeid_ra = ctx->cxg_ha_reg_table[config->port_aggregate_ldid].node_id;
 
     if (cmn650_ccix_ctx.is_prog_for_port_agg) {
-        /*
-         * if programming for port aggregation support, reset the remote rnf
-         * ldid value to the previous iteration in order to program .
-         */
-        ctx->remote_rnf_ldid_value -= ctx->rnf_count;
+	/*
+	 * if programming for port aggregation support, reset the remote rnf
+	 * ldid value to the previous iteration in order to program .
+	 */
+	ctx->remote_rnf_ldid_value -= ctx->rnf_count;
     }
 
     for (linkid = 0; linkid < CMN650_MAX_CCIX_PROTOCOL_LINKS; linkid++) {
-        remote_agentid_start =
-            config->remote_agentid_to_linkid_map[linkid].remote_agentid_start;
-        remote_agentid_end =
-            config->remote_agentid_to_linkid_map[linkid].remote_agentid_end;
-
-        /*
-         * Skip configuring link if both start and end are 0, indicating
-         * there's no link
-         */
-        if ((remote_agentid_start == 0) && (remote_agentid_end == 0))
-            continue;
-
-        FWK_LOG_INFO(
-            MOD_NAME "  Remote [AgentID %d - AgentID %d] Node Id: %" PRIu32,
-            remote_agentid_start,
-            remote_agentid_end,
-            nodeid_ra);
-
-        for (agentid = remote_agentid_start; agentid <= remote_agentid_end;
-             agentid++) {
-            /* Each 64-bit register holds 2 sets of config data, 32 bits each */
-            reg_index = ctx->remote_rnf_ldid_value / 2;
-            reg_bit_offset = ctx->remote_rnf_ldid_value % 2;
-
-            for (i = 0; i < ctx->hnf_count; i++) {
-                hnf_reg = (struct cmn650_hnf_reg *)ctx->hnf_node[i];
-                /* Write CXHA NodeID, local/remote and valid bit */
-                hnf_reg->RN_PHYS_ID[reg_index] |=
-                    ((uint64_t)(
-                         nodeid_ra |
-                         (REMOTE_CCIX_NODE
-                          << HNF_RN_PHYS_RN_LOCAL_REMOTE_SHIFT_VAL) |
-                         (port_aggregate_group_id
-                          << HNF_RN_PHYS_CPA_GRP_RA_SHIFT_VAL) |
-                         ((uint64_t)(config->port_aggregate)
-                          << HNF_RN_PHYS_CPA_EN_RA_SHIFT_VAL) |
-                         (UINT64_C(0x1) << HNF_RN_PHYS_RN_ID_VALID_SHIFT_VAL))
-                     << (reg_bit_offset * NUM_BITS_RESERVED_FOR_PHYS_ID));
-
-                if (config->port_aggregate) {
-                    group =
-                        (port_aggregate_group_id * NUM_PORTS_PER_CPA_GROUP) /
-                        CMN_PORT_AGGR_GRP_PAG_TGTID_PER_GROUP;
-                    bit_pos =
-                        ((port_aggregate_group_id * NUM_PORTS_PER_CPA_GROUP) *
-                         CMN_PORT_AGGR_GRP_PAG_TGTID_WIDTH);
-
-                    hnf_reg->CML_PORT_AGGR_GRP[group] |= ((uint64_t)nodeid_ra)
-                        << bit_pos;
-
-                    group =
-                        ((port_aggregate_group_id * NUM_PORTS_PER_CPA_GROUP) +
-                         1) /
-                        CMN_PORT_AGGR_GRP_PAG_TGTID_PER_GROUP;
-                    bit_pos =
-                        ((((port_aggregate_group_id * NUM_PORTS_PER_CPA_GROUP) +
-                           1) *
-                          CMN_PORT_AGGR_GRP_PAG_TGTID_WIDTH) %
-                         CMN_PORT_AGGR_GRP_PAG_TGTID_WIDTH_PER_GROUP);
-
-                    hnf_reg->CML_PORT_AGGR_GRP[group] |= (uint64_t)cpa_nodeid_ra
-                        << bit_pos;
-                }
-            }
+	remote_agentid_start =
+	    config->remote_agentid_to_linkid_map[linkid].remote_agentid_start;
+	remote_agentid_end =
+	    config->remote_agentid_to_linkid_map[linkid].remote_agentid_end;
+
+	/*
+	 * Skip configuring link if both start and end are 0, indicating
+	 * there's no link
+	 */
+	if ((remote_agentid_start == 0) && (remote_agentid_end == 0))
+	    continue;
+
+	FWK_LOG_INFO(
+	    MOD_NAME "  Remote [AgentID %d - AgentID %d] Node Id: %" PRIu32,
+	    remote_agentid_start,
+	    remote_agentid_end,
+	    nodeid_ra);
+
+	for (agentid = remote_agentid_start; agentid <= remote_agentid_end;
+	     agentid++) {
+	    /* Each 64-bit register holds 2 sets of config data, 32 bits each */
+	    reg_index = ctx->remote_rnf_ldid_value / 2;
+	    reg_bit_offset = ctx->remote_rnf_ldid_value % 2;
+
+	    for (i = 0; i < ctx->hnf_count; i++) {
+		hnf_reg = (struct cmn650_hnf_reg *)ctx->hnf_node[i];
+		/* Write CXHA NodeID, local/remote and valid bit */
+		hnf_reg->RN_PHYS_ID[reg_index] |=
+		    ((uint64_t)(
+			 nodeid_ra |
+			 (REMOTE_CCIX_NODE
+			  << HNF_RN_PHYS_RN_LOCAL_REMOTE_SHIFT_VAL) |
+			 (port_aggregate_group_id
+			  << HNF_RN_PHYS_CPA_GRP_RA_SHIFT_VAL) |
+			 ((uint64_t)(config->port_aggregate)
+			  << HNF_RN_PHYS_CPA_EN_RA_SHIFT_VAL) |
+			 (UINT64_C(0x1) << HNF_RN_PHYS_RN_ID_VALID_SHIFT_VAL))
+		     << (reg_bit_offset * NUM_BITS_RESERVED_FOR_PHYS_ID));
+
+		if (config->port_aggregate) {
+		    group =
+			(port_aggregate_group_id * NUM_PORTS_PER_CPA_GROUP) /
+			CMN_PORT_AGGR_GRP_PAG_TGTID_PER_GROUP;
+		    bit_pos =
+			((port_aggregate_group_id * NUM_PORTS_PER_CPA_GROUP) *
+			 CMN_PORT_AGGR_GRP_PAG_TGTID_WIDTH);
+
+		    hnf_reg->CML_PORT_AGGR_GRP[group] |= ((uint64_t)nodeid_ra)
+			<< bit_pos;
+
+		    group =
+			((port_aggregate_group_id * NUM_PORTS_PER_CPA_GROUP) +
+			 1) /
+			CMN_PORT_AGGR_GRP_PAG_TGTID_PER_GROUP;
+		    bit_pos =
+			((((port_aggregate_group_id * NUM_PORTS_PER_CPA_GROUP) +
+			   1) *
+			  CMN_PORT_AGGR_GRP_PAG_TGTID_WIDTH) %
+			 CMN_PORT_AGGR_GRP_PAG_TGTID_WIDTH_PER_GROUP);
+
+		    hnf_reg->CML_PORT_AGGR_GRP[group] |= (uint64_t)cpa_nodeid_ra
+			<< bit_pos;
+		}
+	    }
 
-            ctx->remote_rnf_ldid_value++;
-        }
+	    ctx->remote_rnf_ldid_value++;
+	}
     }
 
     if (cmn650_ccix_ctx.is_prog_for_port_agg) {
-        for (i = 0; i < ctx->hnf_count; i++) {
-            hnf_reg = (struct cmn650_hnf_reg *)ctx->hnf_node[i];
+	for (i = 0; i < ctx->hnf_count; i++) {
+	    hnf_reg = (struct cmn650_hnf_reg *)ctx->hnf_node[i];
 
-            /* Considering only 2 port support */
-            hnf_reg->CML_PORT_AGGR_CTRL |= UINT64_C(0x1)
-                << (port_aggregate_group_id *
-                    CML_PORT_AGGR_CTRL_NUM_CXG_PAG_WIDTH);
-        }
+	    /* Considering only 2 port support */
+	    hnf_reg->CML_PORT_AGGR_CTRL |= UINT64_C(0x1)
+		<< (port_aggregate_group_id *
+		    CML_PORT_AGGR_CTRL_NUM_CXG_PAG_WIDTH);
+	}
 
-        /* Finally increment the group id for the next group utilization */
-        port_aggregate_group_id++;
+	/* Finally increment the group id for the next group utilization */
+	port_aggregate_group_id++;
     }
 }
 
@@ -436,7 +436,7 @@ static int enable_smp_mode(
     cxg_ra_reg->CXG_RA_AUX_CTRL |= (1 << CXG_RA_AUX_CTRL_SMP_MODE_EN_SHIFT_VAL);
     cxg_ha_reg->CXG_HA_AUX_CTRL |= (1 << CXG_HA_AUX_CTRL_SMP_MODE_EN_SHIFT_VAL);
     cxla_reg->CXLA_AUX_CTRL |=
-        (UINT64_C(0x1) << CXLA_AUX_CTRL_SMP_MODE_EN_SHIFT_VAL);
+	(UINT64_C(0x1) << CXLA_AUX_CTRL_SMP_MODE_EN_SHIFT_VAL);
 
     FWK_LOG_INFO(MOD_NAME "SMP Mode Enabled");
     return FWK_SUCCESS;
@@ -455,46 +455,46 @@ static void program_cxg_ra_sam_addr_regi
     cxg_ra_reg = ctx->cxg_ra_reg_table[cxg_ldid].cxg_ra_reg;
 
     FWK_LOG_INFO(
-        MOD_NAME "Configuring RA SAM for CXRA NodeID %d",
-        get_node_id(cxg_ra_reg));
+	MOD_NAME "Configuring RA SAM for CXRA NodeID %d",
+	get_node_id(cxg_ra_reg));
 
     for (i = 0; i < CMN650_MAX_RA_SAM_ADDR_REGION; i++) {
-        /* If the size is zero, skip that entry */
-        if (config->ra_mmap_table[i].size == 0)
-            continue;
-
-        /* Size must be a multiple of SAM_GRANULARITY */
-        fwk_assert((config->ra_mmap_table[i].size % (64 * FWK_KIB)) == 0);
-
-        /* Size also must be a power of two */
-        fwk_assert(
-            (config->ra_mmap_table[i].size &
-             (config->ra_mmap_table[i].size - 1)) == 0);
-
-        /* Region base should be naturally aligned to the region size */
-        fwk_assert(
-            config->ra_mmap_table[i].base % config->ra_mmap_table[i].size == 0);
-
-        if (cmn650_ccix_ctx.is_prog_for_port_agg)
-            remote_haid = config->port_aggregate_remote_haid[i];
-        else
-            remote_haid = config->ra_mmap_table[i].remote_haid;
-
-        FWK_LOG_INFO(
-            MOD_NAME "  [0x%llx - 0x%llx] HAID %d",
-            config->ra_mmap_table[i].base,
-            config->ra_mmap_table[i].base + config->ra_mmap_table[i].size - 1,
-            remote_haid);
-
-        blocks = config->ra_mmap_table[i].size / (64 * FWK_KIB);
-        size = fwk_math_log2(blocks);
-
-        cxg_ra_reg->CXG_RA_SAM_ADDR_REGION_REG[i] = size |
-            (config->ra_mmap_table[i].base) |
-            ((uint64_t)remote_haid << SAM_ADDR_TARGET_HAID_SHIFT) |
-            (SAM_ADDR_REG_VALID_MASK);
+	/* If the size is zero, skip that entry */
+	if (config->ra_mmap_table[i].size == 0)
+	    continue;
+
+	/* Size must be a multiple of SAM_GRANULARITY */
+	fwk_assert((config->ra_mmap_table[i].size % (64 * FWK_KIB)) == 0);
+
+	/* Size also must be a power of two */
+	fwk_assert(
+	    (config->ra_mmap_table[i].size &
+	     (config->ra_mmap_table[i].size - 1)) == 0);
+
+	/* Region base should be naturally aligned to the region size */
+	fwk_assert(
+	    config->ra_mmap_table[i].base % config->ra_mmap_table[i].size == 0);
+
+	if (cmn650_ccix_ctx.is_prog_for_port_agg)
+	    remote_haid = config->port_aggregate_remote_haid[i];
+	else
+	    remote_haid = config->ra_mmap_table[i].remote_haid;
+
+	FWK_LOG_INFO(
+	    MOD_NAME "  [0x%llx - 0x%llx] HAID %d",
+	    config->ra_mmap_table[i].base,
+	    config->ra_mmap_table[i].base + config->ra_mmap_table[i].size - 1,
+	    remote_haid);
+
+	blocks = config->ra_mmap_table[i].size / (64 * FWK_KIB);
+	size = fwk_math_log2(blocks);
+
+	cxg_ra_reg->CXG_RA_SAM_ADDR_REGION_REG[i] = size |
+	    (config->ra_mmap_table[i].base) |
+	    ((uint64_t)remote_haid << SAM_ADDR_TARGET_HAID_SHIFT) |
+	    (SAM_ADDR_REG_VALID_MASK);
 
-        i++;
+	i++;
     }
 }
 
@@ -512,7 +512,7 @@ static int enable_and_start_ccix_link_up
     cxg_ha_reg = ctx->cxg_ha_reg_table[cxg_ldid].cxg_ha_reg;
 
     if (linkid > 2)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     wait_data.ctx = ctx;
     wait_data.linkid = linkid;
@@ -525,13 +525,13 @@ static int enable_and_start_ccix_link_up
     /* Wait until link enable bits are set */
     wait_data.cond = CXG_LINK_CTRL_EN_BIT_SET;
     status = ctx->timer_api->wait(
-        FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
-        CXG_PRTCL_LINK_CTRL_TIMEOUT,
-        cxg_link_wait_condition,
-        &wait_data);
+	FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
+	CXG_PRTCL_LINK_CTRL_TIMEOUT,
+	cxg_link_wait_condition,
+	&wait_data);
     if (status != FWK_SUCCESS) {
-        FWK_LOG_ERR(MOD_NAME "Enabling CCIX link %d... Failed", linkid);
-        return status;
+	FWK_LOG_ERR(MOD_NAME "Enabling CCIX link %d... Failed", linkid);
+	return status;
     }
     FWK_LOG_INFO(MOD_NAME "Enabling CCIX link %d... Done", linkid);
 
@@ -539,37 +539,37 @@ static int enable_and_start_ccix_link_up
     /* Wait till link up bits are cleared in control register */
     wait_data.cond = CXG_LINK_CTRL_UP_BIT_CLR;
     status = ctx->timer_api->wait(
-        FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
-        CXG_PRTCL_LINK_CTRL_TIMEOUT,
-        cxg_link_wait_condition,
-        &wait_data);
+	FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
+	CXG_PRTCL_LINK_CTRL_TIMEOUT,
+	cxg_link_wait_condition,
+	&wait_data);
     if (status != FWK_SUCCESS) {
-        FWK_LOG_ERR(MOD_NAME "Verifying link down status... Failed");
-        return status;
+	FWK_LOG_ERR(MOD_NAME "Verifying link down status... Failed");
+	return status;
     }
 
     /* Wait till link down bits are set in status register */
     wait_data.cond = CXG_LINK_STATUS_DWN_BIT_SET;
     status = ctx->timer_api->wait(
-        FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
-        CXG_PRTCL_LINK_CTRL_TIMEOUT,
-        cxg_link_wait_condition,
-        &wait_data);
+	FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
+	CXG_PRTCL_LINK_CTRL_TIMEOUT,
+	cxg_link_wait_condition,
+	&wait_data);
     if (status != FWK_SUCCESS) {
-        FWK_LOG_ERR(MOD_NAME "Verifying link down status... Failed");
-        return status;
+	FWK_LOG_ERR(MOD_NAME "Verifying link down status... Failed");
+	return status;
     }
 
     /* Wait till link ACK bits are cleared in status register */
     wait_data.cond = CXG_LINK_STATUS_ACK_BIT_CLR;
     status = ctx->timer_api->wait(
-        FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
-        CXG_PRTCL_LINK_CTRL_TIMEOUT,
-        cxg_link_wait_condition,
-        &wait_data);
+	FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
+	CXG_PRTCL_LINK_CTRL_TIMEOUT,
+	cxg_link_wait_condition,
+	&wait_data);
     if (status != FWK_SUCCESS) {
-        FWK_LOG_ERR(MOD_NAME "Verifying link down status... Failed");
-        return status;
+	FWK_LOG_ERR(MOD_NAME "Verifying link down status... Failed");
+	return status;
     }
     FWK_LOG_INFO(MOD_NAME "Verifying link down status... Done");
 
@@ -581,25 +581,25 @@ static int enable_and_start_ccix_link_up
     /* Wait till link ACK bits are set in status register */
     wait_data.cond = CXG_LINK_STATUS_ACK_BIT_SET;
     status = ctx->timer_api->wait(
-        FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
-        CXG_PRTCL_LINK_CTRL_TIMEOUT,
-        cxg_link_wait_condition,
-        &wait_data);
+	FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
+	CXG_PRTCL_LINK_CTRL_TIMEOUT,
+	cxg_link_wait_condition,
+	&wait_data);
     if (status != FWK_SUCCESS) {
-        FWK_LOG_ERR(MOD_NAME "Bringing up link... Failed");
-        return status;
+	FWK_LOG_ERR(MOD_NAME "Bringing up link... Failed");
+	return status;
     }
 
     /* Wait till link down bits are cleared in status register */
     wait_data.cond = CXG_LINK_STATUS_DWN_BIT_CLR;
     status = ctx->timer_api->wait(
-        FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
-        CXG_PRTCL_LINK_CTRL_TIMEOUT,
-        cxg_link_wait_condition,
-        &wait_data);
+	FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
+	CXG_PRTCL_LINK_CTRL_TIMEOUT,
+	cxg_link_wait_condition,
+	&wait_data);
     if (status != FWK_SUCCESS) {
-        FWK_LOG_ERR(MOD_NAME "Bringing up link... Failed");
-        return status;
+	FWK_LOG_ERR(MOD_NAME "Bringing up link... Failed");
+	return status;
     }
     FWK_LOG_INFO(MOD_NAME "Bringing up link... Done");
 
@@ -626,119 +626,119 @@ int ccix_setup(
 
     /* Assign the max count among the RNs as local_ra_cnt */
     if ((ctx->rnf_count > ctx->rnd_count) && (ctx->rnf_count > ctx->rni_count))
-        local_ra_cnt = ctx->rnf_count;
+	local_ra_cnt = ctx->rnf_count;
     else if (ctx->rnd_count > ctx->rni_count)
-        local_ra_cnt = ctx->rnd_count;
+	local_ra_cnt = ctx->rnd_count;
     else
-        local_ra_cnt = ctx->rni_count;
+	local_ra_cnt = ctx->rni_count;
 
     cmn650_ccix_ctx.is_prog_for_port_agg = false;
 
     do {
-        /* Set the global config data */
-        config = ccix_config;
+	/* Set the global config data */
+	config = ccix_config;
 
-        if (ccix_config->smp_mode) {
-            status = enable_smp_mode(ctx, ccix_config);
-            if (status != FWK_SUCCESS)
-                return status;
-        }
-
-        /*
-         * In order to assign unique AgentIDs across multiple chips, chip_id is
-         * used as factor to offset the AgentID value
-         */
-        cmn650_ccix_ctx.raid_value = 0;
-        offset_id = chip_id * local_ra_cnt;
-
-        for (rnf_ldid = 0; rnf_ldid < ctx->rnf_count; rnf_ldid++) {
-            agentid = cmn650_ccix_ctx.raid_value + offset_id;
-
-            /* Program RAID values in CXRA LDID to RAID LUT */
-            program_cxg_ra_rnf_ldid_to_raid_reg(ctx, rnf_ldid, agentid);
-        }
-
-        /* Program agentid to linkid LUT for remote agents in CXRA/CXHA/CXLA */
-        program_agentid_to_linkid_reg(ctx, ccix_config);
-
-        /* Program HN-F ldid to CHI NodeID for remote RN-F agents */
-        program_hnf_ldid_to_chi_node_id_reg(ctx, ccix_config);
-
-        /*
-         * unique_remote_rnf_ldid_value is used to keep track of the
-         * ldid of the remote RNF agents
-         */
-        unique_remote_rnf_ldid_value = ctx->rnf_count;
-
-        if (ccix_config->remote_rnf_count && (ctx->rnf_count == 0)) {
-            FWK_LOG_ERR(
-                MOD_NAME
-                "Remote RN-F Count can't be %u when RN-F count is zero",
-                ccix_config->remote_rnf_count);
-            fwk_unexpected();
-        }
-
-        for (i = 0; i < ccix_config->remote_rnf_count; i++) {
-            /*
-             * The remote_agentid should not include the current chip's
-             * AgentIDs. If `block` is less than the current chip_id, then
-             * include the AgentIDs starting from chip 0 till (not including)
-             * current chip. If the `block` is equal or greater than the current
-             * chip, then include the AgentIDs from next chip till the max chip.
-             */
-            if ((i / ctx->rnf_count) < chip_id)
-                remote_agentid = i;
-            else
-                remote_agentid = i + ctx->rnf_count;
-
-            /* Program the CXHA raid to ldid LUT */
-            program_cxg_ha_raid_to_ldid_lut(
-                ctx, remote_agentid, unique_remote_rnf_ldid_value);
-
-            unique_remote_rnf_ldid_value++;
-        }
-
-        /* Program the unique HAID for the CXHA block */
-        program_cxg_ha_id(ctx);
-
-        cmn650_ccix_ctx.raid_value = 0;
-        offset_id = chip_id * local_ra_cnt;
-
-        for (rnd_ldid = 0; rnd_ldid < ctx->rnd_count; rnd_ldid++) {
-            /* Determine agentid of the remote agents */
-            agentid = cmn650_ccix_ctx.raid_value + offset_id;
-
-            /* Program RAID values in CXRA LDID to RAID LUT */
-            program_cxg_ra_rnd_ldid_to_raid_reg(ctx, rnd_ldid, agentid);
-        }
-
-        cmn650_ccix_ctx.raid_value = 0;
-        offset_id = chip_id * local_ra_cnt;
-
-        for (rni_ldid = 0; rni_ldid < ctx->rni_count; rni_ldid++) {
-            /* Determine agentid of the remote agents */
-            agentid = cmn650_ccix_ctx.raid_value + offset_id;
-
-            /* Program RAID values in CXRA LDID to RAID LUT */
-            program_cxg_ra_rni_ldid_to_raid_reg(ctx, rni_ldid, agentid);
-        }
-
-        /*
-         * Program the CXRA SAM with the address range and the corresponding
-         * remote HAID
-         */
-        program_cxg_ra_sam_addr_region(ctx, ccix_config);
-
-        /* Program the Link Control registers present in CXRA/CXHA/CXLA */
-        status = enable_and_start_ccix_link_up_sequence(ctx, 0);
-
-        if (config->port_aggregate &&
-            cmn650_ccix_ctx.is_prog_for_port_agg == false) {
-            cmn650_ccix_ctx.is_prog_for_port_agg = true;
-            FWK_LOG_INFO(MOD_NAME
-                         "Programming CCIX gateway for Port Aggregation...");
-        } else
-            cmn650_ccix_ctx.is_prog_for_port_agg = false;
+	if (ccix_config->smp_mode) {
+	    status = enable_smp_mode(ctx, ccix_config);
+	    if (status != FWK_SUCCESS)
+		return status;
+	}
+
+	/*
+	 * In order to assign unique AgentIDs across multiple chips, chip_id is
+	 * used as factor to offset the AgentID value
+	 */
+	cmn650_ccix_ctx.raid_value = 0;
+	offset_id = chip_id * local_ra_cnt;
+
+	for (rnf_ldid = 0; rnf_ldid < ctx->rnf_count; rnf_ldid++) {
+	    agentid = cmn650_ccix_ctx.raid_value + offset_id;
+
+	    /* Program RAID values in CXRA LDID to RAID LUT */
+	    program_cxg_ra_rnf_ldid_to_raid_reg(ctx, rnf_ldid, agentid);
+	}
+
+	/* Program agentid to linkid LUT for remote agents in CXRA/CXHA/CXLA */
+	program_agentid_to_linkid_reg(ctx, ccix_config);
+
+	/* Program HN-F ldid to CHI NodeID for remote RN-F agents */
+	program_hnf_ldid_to_chi_node_id_reg(ctx, ccix_config);
+
+	/*
+	 * unique_remote_rnf_ldid_value is used to keep track of the
+	 * ldid of the remote RNF agents
+	 */
+	unique_remote_rnf_ldid_value = ctx->rnf_count;
+
+	if (ccix_config->remote_rnf_count && (ctx->rnf_count == 0)) {
+	    FWK_LOG_ERR(
+		MOD_NAME
+		"Remote RN-F Count can't be %u when RN-F count is zero",
+		ccix_config->remote_rnf_count);
+	    fwk_unexpected();
+	}
+
+	for (i = 0; i < ccix_config->remote_rnf_count; i++) {
+	    /*
+	     * The remote_agentid should not include the current chip's
+	     * AgentIDs. If `block` is less than the current chip_id, then
+	     * include the AgentIDs starting from chip 0 till (not including)
+	     * current chip. If the `block` is equal or greater than the current
+	     * chip, then include the AgentIDs from next chip till the max chip.
+	     */
+	    if ((i / ctx->rnf_count) < chip_id)
+		remote_agentid = i;
+	    else
+		remote_agentid = i + ctx->rnf_count;
+
+	    /* Program the CXHA raid to ldid LUT */
+	    program_cxg_ha_raid_to_ldid_lut(
+		ctx, remote_agentid, unique_remote_rnf_ldid_value);
+
+	    unique_remote_rnf_ldid_value++;
+	}
+
+	/* Program the unique HAID for the CXHA block */
+	program_cxg_ha_id(ctx);
+
+	cmn650_ccix_ctx.raid_value = 0;
+	offset_id = chip_id * local_ra_cnt;
+
+	for (rnd_ldid = 0; rnd_ldid < ctx->rnd_count; rnd_ldid++) {
+	    /* Determine agentid of the remote agents */
+	    agentid = cmn650_ccix_ctx.raid_value + offset_id;
+
+	    /* Program RAID values in CXRA LDID to RAID LUT */
+	    program_cxg_ra_rnd_ldid_to_raid_reg(ctx, rnd_ldid, agentid);
+	}
+
+	cmn650_ccix_ctx.raid_value = 0;
+	offset_id = chip_id * local_ra_cnt;
+
+	for (rni_ldid = 0; rni_ldid < ctx->rni_count; rni_ldid++) {
+	    /* Determine agentid of the remote agents */
+	    agentid = cmn650_ccix_ctx.raid_value + offset_id;
+
+	    /* Program RAID values in CXRA LDID to RAID LUT */
+	    program_cxg_ra_rni_ldid_to_raid_reg(ctx, rni_ldid, agentid);
+	}
+
+	/*
+	 * Program the CXRA SAM with the address range and the corresponding
+	 * remote HAID
+	 */
+	program_cxg_ra_sam_addr_region(ctx, ccix_config);
+
+	/* Program the Link Control registers present in CXRA/CXHA/CXLA */
+	status = enable_and_start_ccix_link_up_sequence(ctx, 0);
+
+	if (config->port_aggregate &&
+	    cmn650_ccix_ctx.is_prog_for_port_agg == false) {
+	    cmn650_ccix_ctx.is_prog_for_port_agg = true;
+	    FWK_LOG_INFO(MOD_NAME
+			 "Programming CCIX gateway for Port Aggregation...");
+	} else
+	    cmn650_ccix_ctx.is_prog_for_port_agg = false;
     } while (config->port_aggregate && cmn650_ccix_ctx.is_prog_for_port_agg);
 
     return status;
@@ -756,31 +756,31 @@ int ccix_exchange_protocol_credit(
     cmn650_ccix_ctx.is_prog_for_port_agg = false;
 
     do {
-        config = ccix_config;
-        cxg_ldid = get_ldid(cmn650_ccix_ctx.is_prog_for_port_agg);
-        cxg_ra_reg = ctx->cxg_ra_reg_table[cxg_ldid].cxg_ra_reg;
-        cxg_ha_reg = ctx->cxg_ha_reg_table[cxg_ldid].cxg_ha_reg;
-
-        /* TODO Add support to enable multiple links */
-        linkid = 0;
-
-        FWK_LOG_INFO(
-            MOD_NAME "Exchanging protocol credits for link %d...", linkid);
-        /* Exchange protocol credits using link up bit */
-        cxg_ra_reg->LINK_REGS[linkid].CXG_PRTCL_LINK_CTRL |=
-            CXG_LINK_CTRL_UP_MASK;
-        cxg_ha_reg->LINK_REGS[linkid].CXG_PRTCL_LINK_CTRL |=
-            CXG_LINK_CTRL_UP_MASK;
-        FWK_LOG_INFO(
-            MOD_NAME "Exchanging protocol credits for link %d... Done", linkid);
-
-        if (config->port_aggregate &&
-            cmn650_ccix_ctx.is_prog_for_port_agg == false) {
-            cmn650_ccix_ctx.is_prog_for_port_agg = true;
-            FWK_LOG_INFO(MOD_NAME
-                         "Exchange Protocol Credit for Port Aggregation...");
-        } else
-            cmn650_ccix_ctx.is_prog_for_port_agg = false;
+	config = ccix_config;
+	cxg_ldid = get_ldid(cmn650_ccix_ctx.is_prog_for_port_agg);
+	cxg_ra_reg = ctx->cxg_ra_reg_table[cxg_ldid].cxg_ra_reg;
+	cxg_ha_reg = ctx->cxg_ha_reg_table[cxg_ldid].cxg_ha_reg;
+
+	/* TODO Add support to enable multiple links */
+	linkid = 0;
+
+	FWK_LOG_INFO(
+	    MOD_NAME "Exchanging protocol credits for link %d...", linkid);
+	/* Exchange protocol credits using link up bit */
+	cxg_ra_reg->LINK_REGS[linkid].CXG_PRTCL_LINK_CTRL |=
+	    CXG_LINK_CTRL_UP_MASK;
+	cxg_ha_reg->LINK_REGS[linkid].CXG_PRTCL_LINK_CTRL |=
+	    CXG_LINK_CTRL_UP_MASK;
+	FWK_LOG_INFO(
+	    MOD_NAME "Exchanging protocol credits for link %d... Done", linkid);
+
+	if (config->port_aggregate &&
+	    cmn650_ccix_ctx.is_prog_for_port_agg == false) {
+	    cmn650_ccix_ctx.is_prog_for_port_agg = true;
+	    FWK_LOG_INFO(MOD_NAME
+			 "Exchange Protocol Credit for Port Aggregation...");
+	} else
+	    cmn650_ccix_ctx.is_prog_for_port_agg = false;
     } while (config->port_aggregate && cmn650_ccix_ctx.is_prog_for_port_agg);
 
     return FWK_SUCCESS;
@@ -799,45 +799,45 @@ int ccix_enter_system_coherency(
     cmn650_ccix_ctx.is_prog_for_port_agg = false;
 
     do {
-        config = ccix_config;
-        cxg_ldid = get_ldid(cmn650_ccix_ctx.is_prog_for_port_agg);
-        cxg_ha_reg = ctx->cxg_ha_reg_table[cxg_ldid].cxg_ha_reg;
-
-        /* TODO Add support to enable multiple links */
-        linkid = 0;
-
-        wait_data.ctx = ctx;
-        wait_data.linkid = linkid;
-
-        FWK_LOG_INFO(
-            MOD_NAME "Entering system coherency for link %d...", linkid);
-        /* Enter system coherency by setting DVMDOMAIN request bit */
-        cxg_ha_reg->LINK_REGS[linkid].CXG_PRTCL_LINK_CTRL |=
-            CXG_LINK_CTRL_DVMDOMAIN_REQ_MASK;
-
-        /* Wait till DVMDOMAIN ACK bit is set in status register */
-        wait_data.cond = CXG_LINK_STATUS_HA_DVMDOMAIN_ACK_BIT_SET;
-        status = ctx->timer_api->wait(
-            FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
-            CXG_PRTCL_LINK_DVMDOMAIN_TIMEOUT,
-            cxg_link_wait_condition,
-            &wait_data);
-        if (status != FWK_SUCCESS) {
-            FWK_LOG_ERR(
-                MOD_NAME "Entering system coherency for link %d... Failed",
-                linkid);
-            return status;
-        }
-        FWK_LOG_INFO(
-            MOD_NAME "Entering system coherency for link %d... Done", linkid);
-
-        if (config->port_aggregate &&
-            cmn650_ccix_ctx.is_prog_for_port_agg == false) {
-            cmn650_ccix_ctx.is_prog_for_port_agg = true;
-            FWK_LOG_INFO(MOD_NAME
-                         "Enter system coherency for Port Aggregation...");
-        } else
-            cmn650_ccix_ctx.is_prog_for_port_agg = false;
+	config = ccix_config;
+	cxg_ldid = get_ldid(cmn650_ccix_ctx.is_prog_for_port_agg);
+	cxg_ha_reg = ctx->cxg_ha_reg_table[cxg_ldid].cxg_ha_reg;
+
+	/* TODO Add support to enable multiple links */
+	linkid = 0;
+
+	wait_data.ctx = ctx;
+	wait_data.linkid = linkid;
+
+	FWK_LOG_INFO(
+	    MOD_NAME "Entering system coherency for link %d...", linkid);
+	/* Enter system coherency by setting DVMDOMAIN request bit */
+	cxg_ha_reg->LINK_REGS[linkid].CXG_PRTCL_LINK_CTRL |=
+	    CXG_LINK_CTRL_DVMDOMAIN_REQ_MASK;
+
+	/* Wait till DVMDOMAIN ACK bit is set in status register */
+	wait_data.cond = CXG_LINK_STATUS_HA_DVMDOMAIN_ACK_BIT_SET;
+	status = ctx->timer_api->wait(
+	    FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
+	    CXG_PRTCL_LINK_DVMDOMAIN_TIMEOUT,
+	    cxg_link_wait_condition,
+	    &wait_data);
+	if (status != FWK_SUCCESS) {
+	    FWK_LOG_ERR(
+		MOD_NAME "Entering system coherency for link %d... Failed",
+		linkid);
+	    return status;
+	}
+	FWK_LOG_INFO(
+	    MOD_NAME "Entering system coherency for link %d... Done", linkid);
+
+	if (config->port_aggregate &&
+	    cmn650_ccix_ctx.is_prog_for_port_agg == false) {
+	    cmn650_ccix_ctx.is_prog_for_port_agg = true;
+	    FWK_LOG_INFO(MOD_NAME
+			 "Enter system coherency for Port Aggregation...");
+	} else
+	    cmn650_ccix_ctx.is_prog_for_port_agg = false;
     } while (config->port_aggregate && cmn650_ccix_ctx.is_prog_for_port_agg);
 
     return FWK_SUCCESS;
@@ -856,43 +856,43 @@ int ccix_enter_dvm_domain(
     cmn650_ccix_ctx.is_prog_for_port_agg = false;
 
     do {
-        config = ccix_config;
-        cxg_ldid = get_ldid(cmn650_ccix_ctx.is_prog_for_port_agg);
-        cxg_ra_reg = ctx->cxg_ra_reg_table[cxg_ldid].cxg_ra_reg;
-
-        /* TODO Add support to enable multiple links */
-        linkid = 0;
-
-        wait_data.ctx = ctx;
-        wait_data.linkid = linkid;
-
-        FWK_LOG_INFO(MOD_NAME "Entering DVM domain for link %d...", linkid);
-        /* DVM domain entry by setting DVMDOMAIN request bit */
-        cxg_ra_reg->LINK_REGS[linkid].CXG_PRTCL_LINK_CTRL |=
-            CXG_LINK_CTRL_DVMDOMAIN_REQ_MASK;
-
-        /* Wait till DVMDOMAIN ACK bit is set in status register */
-        wait_data.cond = CXG_LINK_STATUS_RA_DVMDOMAIN_ACK_BIT_SET;
-        status = ctx->timer_api->wait(
-            FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
-            CXG_PRTCL_LINK_DVMDOMAIN_TIMEOUT,
-            cxg_link_wait_condition,
-            &wait_data);
-        if (status != FWK_SUCCESS) {
-            FWK_LOG_ERR(
-                MOD_NAME "Entering DVM domain for link %d... Failed", linkid);
-            return status;
-        }
-        FWK_LOG_INFO(
-            MOD_NAME "Entering DVM domain for link %d... Done", linkid);
-
-        if (config->port_aggregate &&
-            cmn650_ccix_ctx.is_prog_for_port_agg == false) {
-            cmn650_ccix_ctx.is_prog_for_port_agg = true;
-            FWK_LOG_INFO(MOD_NAME
-                         "Entering DVM domain for Port Aggregation...");
-        } else
-            cmn650_ccix_ctx.is_prog_for_port_agg = false;
+	config = ccix_config;
+	cxg_ldid = get_ldid(cmn650_ccix_ctx.is_prog_for_port_agg);
+	cxg_ra_reg = ctx->cxg_ra_reg_table[cxg_ldid].cxg_ra_reg;
+
+	/* TODO Add support to enable multiple links */
+	linkid = 0;
+
+	wait_data.ctx = ctx;
+	wait_data.linkid = linkid;
+
+	FWK_LOG_INFO(MOD_NAME "Entering DVM domain for link %d...", linkid);
+	/* DVM domain entry by setting DVMDOMAIN request bit */
+	cxg_ra_reg->LINK_REGS[linkid].CXG_PRTCL_LINK_CTRL |=
+	    CXG_LINK_CTRL_DVMDOMAIN_REQ_MASK;
+
+	/* Wait till DVMDOMAIN ACK bit is set in status register */
+	wait_data.cond = CXG_LINK_STATUS_RA_DVMDOMAIN_ACK_BIT_SET;
+	status = ctx->timer_api->wait(
+	    FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
+	    CXG_PRTCL_LINK_DVMDOMAIN_TIMEOUT,
+	    cxg_link_wait_condition,
+	    &wait_data);
+	if (status != FWK_SUCCESS) {
+	    FWK_LOG_ERR(
+		MOD_NAME "Entering DVM domain for link %d... Failed", linkid);
+	    return status;
+	}
+	FWK_LOG_INFO(
+	    MOD_NAME "Entering DVM domain for link %d... Done", linkid);
+
+	if (config->port_aggregate &&
+	    cmn650_ccix_ctx.is_prog_for_port_agg == false) {
+	    cmn650_ccix_ctx.is_prog_for_port_agg = true;
+	    FWK_LOG_INFO(MOD_NAME
+			 "Entering DVM domain for Port Aggregation...");
+	} else
+	    cmn650_ccix_ctx.is_prog_for_port_agg = false;
     } while (config->port_aggregate && cmn650_ccix_ctx.is_prog_for_port_agg);
 
     return FWK_SUCCESS;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cmn650/src/mod_cmn650.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/cmn650/src/mod_cmn650.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cmn650/src/mod_cmn650.c
@@ -72,33 +72,33 @@ static void process_node_hnf(struct cmn6
      * sys_cache_grp_hn_nodeid registers.
      */
     if (config->hnf_cal_mode == true && (node_id % 2 == 1)) {
-        /* Factor to manipulate the group and bit_pos */
-        cal_mode_factor = 2;
+	/* Factor to manipulate the group and bit_pos */
+	cal_mode_factor = 2;
     }
 
     fwk_assert(logical_id < config->snf_count);
 
     group = logical_id /
-        (CMN650_HNF_CACHE_GROUP_ENTRIES_PER_GROUP * cal_mode_factor);
+	(CMN650_HNF_CACHE_GROUP_ENTRIES_PER_GROUP * cal_mode_factor);
     bit_pos = (CMN650_HNF_CACHE_GROUP_ENTRY_BITS_WIDTH / cal_mode_factor) *
-        ((logical_id %
-          (CMN650_HNF_CACHE_GROUP_ENTRIES_PER_GROUP * cal_mode_factor)));
+	((logical_id %
+	  (CMN650_HNF_CACHE_GROUP_ENTRIES_PER_GROUP * cal_mode_factor)));
 
     /*
      * If CAL mode is set, add only even numbered hnd node to
      * sys_cache_grp_hn_nodeid registers
      */
     if (config->hnf_cal_mode == true) {
-        if (node_id % 2 == 0) {
-            ctx->hnf_cache_group[group] += ((uint64_t)get_node_id(hnf))
-                << bit_pos;
-            ctx->sn_nodeid_group[group] +=
-                ((uint64_t)config->snf_table[logical_id]) << bit_pos;
-        }
+	if (node_id % 2 == 0) {
+	    ctx->hnf_cache_group[group] += ((uint64_t)get_node_id(hnf))
+		<< bit_pos;
+	    ctx->sn_nodeid_group[group] +=
+		((uint64_t)config->snf_table[logical_id]) << bit_pos;
+	}
     } else {
-        ctx->hnf_cache_group[group] += ((uint64_t)get_node_id(hnf)) << bit_pos;
-        ctx->sn_nodeid_group[group] += ((uint64_t)config->snf_table[logical_id])
-            << bit_pos;
+	ctx->hnf_cache_group[group] += ((uint64_t)get_node_id(hnf)) << bit_pos;
+	ctx->sn_nodeid_group[group] += ((uint64_t)config->snf_table[logical_id])
+	    << bit_pos;
     }
 
     /* Set target node */
@@ -108,29 +108,29 @@ static void process_node_hnf(struct cmn6
      * Map sub-regions to this HN-F node
      */
     for (region_idx = 0; region_idx < config->mmap_count; region_idx++) {
-        region = &config->mmap_table[region_idx];
+	region = &config->mmap_table[region_idx];
 
-        /* Skip non sub-regions */
-        if (region->type != MOD_CMN650_REGION_TYPE_SYSCACHE_SUB)
-            continue;
-
-        /* Offset the base with chip address space base on chip-id */
-        base =
-            ((uint64_t)(ctx->config->chip_addr_space * chip_id)) + region->base;
-
-        /* Configure sub-region entry */
-        hnf->SAM_MEMREGION[region_sub_count] = region->node_id |
-            (sam_encode_region_size(region->size)
-             << CMN650_HNF_SAM_MEMREGION_SIZE_POS) |
-            ((base / SAM_GRANULARITY) << CMN650_HNF_SAM_MEMREGION_BASE_POS) |
-            CMN650_HNF_SAM_MEMREGION_VALID;
+	/* Skip non sub-regions */
+	if (region->type != MOD_CMN650_REGION_TYPE_SYSCACHE_SUB)
+	    continue;
+
+	/* Offset the base with chip address space base on chip-id */
+	base =
+	    ((uint64_t)(ctx->config->chip_addr_space * chip_id)) + region->base;
+
+	/* Configure sub-region entry */
+	hnf->SAM_MEMREGION[region_sub_count] = region->node_id |
+	    (sam_encode_region_size(region->size)
+	     << CMN650_HNF_SAM_MEMREGION_SIZE_POS) |
+	    ((base / SAM_GRANULARITY) << CMN650_HNF_SAM_MEMREGION_BASE_POS) |
+	    CMN650_HNF_SAM_MEMREGION_VALID;
 
-        region_sub_count++;
+	region_sub_count++;
     }
 
     /* Configure the system cache RAM PPU */
     hnf->PPU_PWPR = CMN650_PPU_PWPR_POLICY_ON | CMN650_PPU_PWPR_OPMODE_FAM |
-        CMN650_PPU_PWPR_DYN_EN;
+	CMN650_PPU_PWPR_DYN_EN;
 }
 
 /*
@@ -158,7 +158,7 @@ static int cmn650_discovery(void)
     cxla_reg_count = 0;
 
     FWK_LOG_INFO(
-        MOD_NAME "CMN-650 revision: %s", get_cmn650_revision_name(ctx->root));
+	MOD_NAME "CMN-650 revision: %s", get_cmn650_revision_name(ctx->root));
     FWK_LOG_INFO(MOD_NAME "Starting discovery...");
 
     fwk_assert(get_node_type(ctx->root) == NODE_TYPE_CFG);
@@ -166,176 +166,176 @@ static int cmn650_discovery(void)
     /* Traverse cross points (XP) */
     xp_count = get_node_child_count(ctx->root);
     for (xp_idx = 0; xp_idx < xp_count; xp_idx++) {
-        xp = get_child_node(config->base, ctx->root, xp_idx);
-        fwk_assert(get_node_type(xp) == NODE_TYPE_XP);
+	xp = get_child_node(config->base, ctx->root, xp_idx);
+	fwk_assert(get_node_type(xp) == NODE_TYPE_XP);
 
-        FWK_LOG_INFO(
-            MOD_NAME "XP (%d, %d) ID:%d, LID:%d",
-            get_node_pos_x(xp),
-            get_node_pos_y(xp),
-            get_node_id(xp),
-            get_node_logical_id(xp));
-
-        /* Traverse nodes */
-        node_count = get_node_child_count(xp);
-        for (node_idx = 0; node_idx < node_count; node_idx++) {
-            node = get_child_node(config->base, xp, node_idx);
-            if (is_child_external(xp, node_idx)) { /* External nodes */
-                xp_port = get_port_number(get_child_node_id(xp, node_idx));
-
-                /*
-                 * If the device type is CXRH, CXHA, or CXRA, then the external
-                 * child node is CXLA as every CXRH, CXHA, or CXRA node has a
-                 * corresponding external CXLA node.
-                 */
-                if ((get_device_type(xp, xp_port) == DEVICE_TYPE_CXRH) ||
-                    (get_device_type(xp, xp_port) == DEVICE_TYPE_CXHA) ||
-                    (get_device_type(xp, xp_port) == DEVICE_TYPE_CXRA)) {
-                    cxla_reg_count++;
-                    FWK_LOG_INFO(
-                        MOD_NAME "  Found CXLA at node ID: %d",
-                        get_child_node_id(xp, node_idx));
-                } else { /* External RN-SAM Node */
-                    ctx->external_rnsam_count++;
-                    FWK_LOG_INFO(
-                        MOD_NAME "  Found external node ID: %d",
-                        get_child_node_id(xp, node_idx));
-                }
-            } else { /* Internal nodes */
-                switch (get_node_type(node)) {
-                case NODE_TYPE_HN_F:
-                    if (ctx->hnf_count >= MAX_HNF_COUNT) {
-                        FWK_LOG_INFO(
-                            MOD_NAME "  hnf count %d >= max limit (%d)",
-                            ctx->hnf_count,
-                            MAX_HNF_COUNT);
-                        return FWK_E_DATA;
-                    }
-                    ctx->hnf_count++;
-                    break;
-
-                case NODE_TYPE_RN_SAM:
-                    ctx->internal_rnsam_count++;
-
-                    /*
-                     * RN-F nodes do not have node type identifier and hence the
-                     * count cannot be determined using the node type id.
-                     * Alternatively, check if the device type connected to the
-                     * Crosspoint (XP) is one of the RNF types and determine the
-                     * RN-F count (if CAL connected RN-F, double the count).
-                     */
-                    xp_port = get_port_number(get_node_id(node));
-
-                    if (is_device_type_rnf(xp, xp_port)) {
-                        if (is_cal_connected(xp, xp_port)) {
-                            ctx->rnf_count += 2;
-                            FWK_LOG_INFO(
-                                MOD_NAME
-                                "  RN-F (CAL connected) found at port: %d",
-                                xp_port);
-                        } else {
-                            ctx->rnf_count++;
-                            FWK_LOG_INFO(
-                                MOD_NAME "  RN-F found at port: %d", xp_port);
-                        }
-                    }
-                    break;
-
-                case NODE_TYPE_RN_D:
-                    if ((ctx->rnd_count) >= MAX_RND_COUNT) {
-                        FWK_LOG_ERR(
-                            MOD_NAME "  rnd count %d >= max limit (%d)\n",
-                            ctx->rnd_count,
-                            MAX_RND_COUNT);
-                        return FWK_E_DATA;
-                    }
-                    ctx->rnd_count++;
-                    break;
-
-                case NODE_TYPE_RN_I:
-                    if ((ctx->rni_count) >= MAX_RNI_COUNT) {
-                        FWK_LOG_ERR(
-                            MOD_NAME "  rni count %d >= max limit (%d)\n",
-                            ctx->rni_count,
-                            MAX_RNI_COUNT);
-                        return FWK_E_DATA;
-                    }
-                    ctx->rni_count++;
-                    break;
-
-                case NODE_TYPE_CXRA:
-                    cxg_ra_reg_count++;
-                    break;
-
-                case NODE_TYPE_CXHA:
-                    cxg_ha_reg_count++;
-                    break;
-
-                /* CXLA should not be an internal node */
-                case NODE_TYPE_CXLA:
-                    FWK_LOG_ERR(MOD_NAME
-                                "CXLA node should not be internal node, "
-                                "discovery failed");
-                    return FWK_E_DEVICE;
-                    break;
-
-                default:
-                    /* Nothing to be done for other node types */
-                    break;
-                }
-
-                FWK_LOG_INFO(
-                    MOD_NAME "  %s ID:%d, LID:%d",
-                    get_node_type_name(get_node_type(node)),
-                    get_node_id(node),
-                    get_node_logical_id(node));
-            }
-        }
+	FWK_LOG_INFO(
+	    MOD_NAME "XP (%d, %d) ID:%d, LID:%d",
+	    get_node_pos_x(xp),
+	    get_node_pos_y(xp),
+	    get_node_id(xp),
+	    get_node_logical_id(xp));
+
+	/* Traverse nodes */
+	node_count = get_node_child_count(xp);
+	for (node_idx = 0; node_idx < node_count; node_idx++) {
+	    node = get_child_node(config->base, xp, node_idx);
+	    if (is_child_external(xp, node_idx)) { /* External nodes */
+		xp_port = get_port_number(get_child_node_id(xp, node_idx));
+
+		/*
+		 * If the device type is CXRH, CXHA, or CXRA, then the external
+		 * child node is CXLA as every CXRH, CXHA, or CXRA node has a
+		 * corresponding external CXLA node.
+		 */
+		if ((get_device_type(xp, xp_port) == DEVICE_TYPE_CXRH) ||
+		    (get_device_type(xp, xp_port) == DEVICE_TYPE_CXHA) ||
+		    (get_device_type(xp, xp_port) == DEVICE_TYPE_CXRA)) {
+		    cxla_reg_count++;
+		    FWK_LOG_INFO(
+			MOD_NAME "  Found CXLA at node ID: %d",
+			get_child_node_id(xp, node_idx));
+		} else { /* External RN-SAM Node */
+		    ctx->external_rnsam_count++;
+		    FWK_LOG_INFO(
+			MOD_NAME "  Found external node ID: %d",
+			get_child_node_id(xp, node_idx));
+		}
+	    } else { /* Internal nodes */
+		switch (get_node_type(node)) {
+		case NODE_TYPE_HN_F:
+		    if (ctx->hnf_count >= MAX_HNF_COUNT) {
+			FWK_LOG_INFO(
+			    MOD_NAME "  hnf count %d >= max limit (%d)",
+			    ctx->hnf_count,
+			    MAX_HNF_COUNT);
+			return FWK_E_DATA;
+		    }
+		    ctx->hnf_count++;
+		    break;
+
+		case NODE_TYPE_RN_SAM:
+		    ctx->internal_rnsam_count++;
+
+		    /*
+		     * RN-F nodes do not have node type identifier and hence the
+		     * count cannot be determined using the node type id.
+		     * Alternatively, check if the device type connected to the
+		     * Crosspoint (XP) is one of the RNF types and determine the
+		     * RN-F count (if CAL connected RN-F, double the count).
+		     */
+		    xp_port = get_port_number(get_node_id(node));
+
+		    if (is_device_type_rnf(xp, xp_port)) {
+			if (is_cal_connected(xp, xp_port)) {
+			    ctx->rnf_count += 2;
+			    FWK_LOG_INFO(
+				MOD_NAME
+				"  RN-F (CAL connected) found at port: %d",
+				xp_port);
+			} else {
+			    ctx->rnf_count++;
+			    FWK_LOG_INFO(
+				MOD_NAME "  RN-F found at port: %d", xp_port);
+			}
+		    }
+		    break;
+
+		case NODE_TYPE_RN_D:
+		    if ((ctx->rnd_count) >= MAX_RND_COUNT) {
+			FWK_LOG_ERR(
+			    MOD_NAME "  rnd count %d >= max limit (%d)\n",
+			    ctx->rnd_count,
+			    MAX_RND_COUNT);
+			return FWK_E_DATA;
+		    }
+		    ctx->rnd_count++;
+		    break;
+
+		case NODE_TYPE_RN_I:
+		    if ((ctx->rni_count) >= MAX_RNI_COUNT) {
+			FWK_LOG_ERR(
+			    MOD_NAME "  rni count %d >= max limit (%d)\n",
+			    ctx->rni_count,
+			    MAX_RNI_COUNT);
+			return FWK_E_DATA;
+		    }
+		    ctx->rni_count++;
+		    break;
+
+		case NODE_TYPE_CXRA:
+		    cxg_ra_reg_count++;
+		    break;
+
+		case NODE_TYPE_CXHA:
+		    cxg_ha_reg_count++;
+		    break;
+
+		/* CXLA should not be an internal node */
+		case NODE_TYPE_CXLA:
+		    FWK_LOG_ERR(MOD_NAME
+				"CXLA node should not be internal node, "
+				"discovery failed");
+		    return FWK_E_DEVICE;
+		    break;
+
+		default:
+		    /* Nothing to be done for other node types */
+		    break;
+		}
+
+		FWK_LOG_INFO(
+		    MOD_NAME "  %s ID:%d, LID:%d",
+		    get_node_type_name(get_node_type(node)),
+		    get_node_id(node),
+		    get_node_logical_id(node));
+	    }
+	}
     }
 
     /* When CAL is present, the number of HN-Fs must be even. */
     if ((ctx->hnf_count % 2 != 0) && (config->hnf_cal_mode == true)) {
-        FWK_LOG_ERR(
-            MOD_NAME "hnf count: %d should be even when CAL mode is set",
-            ctx->hnf_count);
-        return FWK_E_DATA;
+	FWK_LOG_ERR(
+	    MOD_NAME "hnf count: %d should be even when CAL mode is set",
+	    ctx->hnf_count);
+	return FWK_E_DATA;
     }
 
     /* Total number of CXG_RA, CXG_HA and CXLA nodes should be equal */
     if ((cxg_ra_reg_count != cxg_ha_reg_count) ||
-        (cxg_ha_reg_count != cxla_reg_count)) {
-        FWK_LOG_ERR(
-            MOD_NAME
-            "Inconsistent count of CXG components detected, discovery failed.\n"
-            " CXG_RA count: %d\n"
-            " CXG_HA count: %d\n"
-            " CXLA   count: %d\n",
-            cxg_ra_reg_count,
-            cxg_ha_reg_count,
-            cxla_reg_count);
-        return FWK_E_DEVICE;
+	(cxg_ha_reg_count != cxla_reg_count)) {
+	FWK_LOG_ERR(
+	    MOD_NAME
+	    "Inconsistent count of CXG components detected, discovery failed.\n"
+	    " CXG_RA count: %d\n"
+	    " CXG_HA count: %d\n"
+	    " CXLA   count: %d\n",
+	    cxg_ra_reg_count,
+	    cxg_ha_reg_count,
+	    cxla_reg_count);
+	return FWK_E_DEVICE;
     }
 
     ctx->ccix_node_count = cxg_ra_reg_count;
 
     if (ctx->rnf_count > MAX_RNF_COUNT) {
-        FWK_LOG_ERR(
-            MOD_NAME "rnf count %d > max limit (%d)\n",
-            ctx->rnf_count,
-            MAX_RNF_COUNT);
-        return FWK_E_RANGE;
+	FWK_LOG_ERR(
+	    MOD_NAME "rnf count %d > max limit (%d)\n",
+	    ctx->rnf_count,
+	    MAX_RNF_COUNT);
+	return FWK_E_RANGE;
     }
 
     FWK_LOG_INFO(
-        MOD_NAME "Total internal RN-SAM nodes: %d", ctx->internal_rnsam_count);
+	MOD_NAME "Total internal RN-SAM nodes: %d", ctx->internal_rnsam_count);
     FWK_LOG_INFO(
-        MOD_NAME "Total external RN-SAM nodes: %d", ctx->external_rnsam_count);
+	MOD_NAME "Total external RN-SAM nodes: %d", ctx->external_rnsam_count);
     FWK_LOG_INFO(MOD_NAME "Total HN-F nodes: %d", ctx->hnf_count);
     FWK_LOG_INFO(MOD_NAME "Total RN-D nodes: %d\n", ctx->rnd_count);
     FWK_LOG_INFO(MOD_NAME "Total RN-F nodes: %d\n", ctx->rnf_count);
     FWK_LOG_INFO(MOD_NAME "Total RN-I nodes: %d\n", ctx->rni_count);
     FWK_LOG_INFO(
-        MOD_NAME "Total CCIX Request Agent nodes: %d", cxg_ra_reg_count);
+	MOD_NAME "Total CCIX Request Agent nodes: %d", cxg_ra_reg_count);
     FWK_LOG_INFO(MOD_NAME "Total CCIX Home Agent nodes: %d", cxg_ha_reg_count);
     FWK_LOG_INFO(MOD_NAME "Total CCIX Link Agent nodes: %d", cxla_reg_count);
 
@@ -367,68 +367,68 @@ static void cmn650_configure(void)
     /* Traverse cross points (XP) */
     xp_count = get_node_child_count(ctx->root);
     for (xp_idx = 0; xp_idx < xp_count; xp_idx++) {
-        xp = get_child_node(config->base, ctx->root, xp_idx);
-        fwk_assert(get_node_type(xp) == NODE_TYPE_XP);
+	xp = get_child_node(config->base, ctx->root, xp_idx);
+	fwk_assert(get_node_type(xp) == NODE_TYPE_XP);
 
-        /* Traverse nodes */
-        node_count = get_node_child_count(xp);
-        for (node_idx = 0; node_idx < node_count; node_idx++) {
-            node = get_child_node(config->base, xp, node_idx);
-            if (is_child_external(xp, node_idx)) {
-                node_id = get_child_node_id(xp, node_idx);
-                xp_port = get_port_number(get_child_node_id(xp, node_idx));
-
-                if ((get_device_type(xp, xp_port) == DEVICE_TYPE_CXRH) ||
-                    (get_device_type(xp, xp_port) == DEVICE_TYPE_CXHA) ||
-                    (get_device_type(xp, xp_port) == DEVICE_TYPE_CXRA)) {
-                    ldid = get_node_logical_id(node);
-                    fwk_assert(ldid < ctx->ccix_node_count);
-
-                    ctx->cxla_reg_table[ldid].node_id = node_id;
-                    ctx->cxla_reg_table[ldid].cxla_reg =
-                        (struct cmn650_cxla_reg *)node;
-                } else {
-                    fwk_assert(xrnsam_entry < ctx->external_rnsam_count);
-
-                    ctx->external_rnsam_table[xrnsam_entry].node_id = node_id;
-                    ctx->external_rnsam_table[xrnsam_entry].node = node;
-
-                    xrnsam_entry++;
-                }
-            } else {
-                enum node_type node_type = get_node_type(node);
-                node_id = get_node_id(node);
-
-                if (node_type == NODE_TYPE_RN_SAM) {
-                    fwk_assert(irnsam_entry < ctx->internal_rnsam_count);
-
-                    ctx->internal_rnsam_table[irnsam_entry] = node;
-
-                    irnsam_entry++;
-                } else if (node_type == NODE_TYPE_CXRA) {
-                    ldid = get_node_logical_id(node);
-                    fwk_assert(ldid < ctx->ccix_node_count);
-
-                    /* Use ldid as index of the cxg_ra table */
-                    ctx->cxg_ra_reg_table[ldid].node_id = node_id;
-                    ctx->cxg_ra_reg_table[ldid].cxg_ra_reg =
-                        (struct cmn650_cxg_ra_reg *)node;
-                } else if (node_type == NODE_TYPE_CXHA) {
-                    ldid = get_node_logical_id(node);
-                    fwk_assert(ldid < ctx->ccix_node_count);
-
-                    /* Use ldid as index of the cxg_ra table */
-                    ctx->cxg_ha_reg_table[ldid].node_id = node_id;
-                    ctx->cxg_ha_reg_table[ldid].cxg_ha_reg =
-                        (struct cmn650_cxg_ha_reg *)node;
-                } else if (node_type == NODE_TYPE_HN_F) {
-                    fwk_assert(hnf_entry < ctx->hnf_count);
-                    ctx->hnf_node[hnf_entry++] = (uintptr_t)(void *)node;
-
-                    process_node_hnf(node);
-                }
-            }
-        }
+	/* Traverse nodes */
+	node_count = get_node_child_count(xp);
+	for (node_idx = 0; node_idx < node_count; node_idx++) {
+	    node = get_child_node(config->base, xp, node_idx);
+	    if (is_child_external(xp, node_idx)) {
+		node_id = get_child_node_id(xp, node_idx);
+		xp_port = get_port_number(get_child_node_id(xp, node_idx));
+
+		if ((get_device_type(xp, xp_port) == DEVICE_TYPE_CXRH) ||
+		    (get_device_type(xp, xp_port) == DEVICE_TYPE_CXHA) ||
+		    (get_device_type(xp, xp_port) == DEVICE_TYPE_CXRA)) {
+		    ldid = get_node_logical_id(node);
+		    fwk_assert(ldid < ctx->ccix_node_count);
+
+		    ctx->cxla_reg_table[ldid].node_id = node_id;
+		    ctx->cxla_reg_table[ldid].cxla_reg =
+			(struct cmn650_cxla_reg *)node;
+		} else {
+		    fwk_assert(xrnsam_entry < ctx->external_rnsam_count);
+
+		    ctx->external_rnsam_table[xrnsam_entry].node_id = node_id;
+		    ctx->external_rnsam_table[xrnsam_entry].node = node;
+
+		    xrnsam_entry++;
+		}
+	    } else {
+		enum node_type node_type = get_node_type(node);
+		node_id = get_node_id(node);
+
+		if (node_type == NODE_TYPE_RN_SAM) {
+		    fwk_assert(irnsam_entry < ctx->internal_rnsam_count);
+
+		    ctx->internal_rnsam_table[irnsam_entry] = node;
+
+		    irnsam_entry++;
+		} else if (node_type == NODE_TYPE_CXRA) {
+		    ldid = get_node_logical_id(node);
+		    fwk_assert(ldid < ctx->ccix_node_count);
+
+		    /* Use ldid as index of the cxg_ra table */
+		    ctx->cxg_ra_reg_table[ldid].node_id = node_id;
+		    ctx->cxg_ra_reg_table[ldid].cxg_ra_reg =
+			(struct cmn650_cxg_ra_reg *)node;
+		} else if (node_type == NODE_TYPE_CXHA) {
+		    ldid = get_node_logical_id(node);
+		    fwk_assert(ldid < ctx->ccix_node_count);
+
+		    /* Use ldid as index of the cxg_ra table */
+		    ctx->cxg_ha_reg_table[ldid].node_id = node_id;
+		    ctx->cxg_ha_reg_table[ldid].cxg_ha_reg =
+			(struct cmn650_cxg_ha_reg *)node;
+		} else if (node_type == NODE_TYPE_HN_F) {
+		    fwk_assert(hnf_entry < ctx->hnf_count);
+		    ctx->hnf_node[hnf_entry++] = (uintptr_t)(void *)node;
+
+		    process_node_hnf(node);
+		}
+	    }
+	}
     }
 }
 
@@ -455,192 +455,192 @@ static int cmn650_setup_sam(struct cmn65
     (void)mmap_type_name;
 
     for (region_idx = 0; region_idx < config->mmap_count; region_idx++) {
-        region = &config->mmap_table[region_idx];
+	region = &config->mmap_table[region_idx];
 
-        /* Offset the base with chip address space base on chip-id */
-        base =
-            ((uint64_t)(ctx->config->chip_addr_space * chip_id) + region->base);
-
-        switch (region->type) {
-        case MOD_CMN650_MEM_REGION_TYPE_IO:
-            /*
-             * Configure memory region
-             */
-            FWK_LOG_INFO(
-                MOD_NAME "  [0x%x%x - 0x%x%x] %s",
-                HIGH_WORD(base),
-                LOW_WORD(base),
-                HIGH_WORD(base + region->size - 1),
-                LOW_WORD(base + region->size - 1),
-                mmap_type_name[region->type]);
-
-            configure_region(
-                &rnsam->NON_HASH_MEM_REGION[region_io_count],
-                base,
-                region->size,
-                SAM_NODE_TYPE_HN_I);
-
-            /*
-             * Configure target node
-             */
-            group = region_io_count /
-                CMN650_RNSAM_NON_HASH_TGT_NODEID_ENTRIES_PER_GROUP;
-            bit_pos = CMN650_RNSAM_NON_HASH_TGT_NODEID_ENTRY_BITS_WIDTH *
-                (region_io_count %
-                 CMN650_RNSAM_NON_HASH_TGT_NODEID_ENTRIES_PER_GROUP);
-
-            rnsam->NON_HASH_TGT_NODEID[group] &=
-                ~(CMN650_RNSAM_NON_HASH_TGT_NODEID_ENTRY_MASK << bit_pos);
-            rnsam->NON_HASH_TGT_NODEID[group] |=
-                (region->node_id & CMN650_RNSAM_NON_HASH_TGT_NODEID_ENTRY_MASK)
-                << bit_pos;
-
-            region_io_count++;
-            break;
-
-        case MOD_CMN650_MEM_REGION_TYPE_SYSCACHE:
-            /*
-             * Configure memory region
-             */
-            FWK_LOG_INFO(
-                MOD_NAME "  [0x%x%x - 0x%x%x] %s",
-                HIGH_WORD(region->base),
-                LOW_WORD(region->base),
-                HIGH_WORD(region->base + region->size - 1),
-                LOW_WORD(region->base + region->size - 1),
-                mmap_type_name[region->type]);
-
-            configure_region(
-                &rnsam->SYS_CACHE_GRP_REGION[region_sys_count],
-                region->base,
-                region->size,
-                SAM_NODE_TYPE_HN_F);
-
-            /* Mark corresponding region as enabled */
-            fwk_assert(region_sys_count < MAX_SCG_COUNT);
-            scg_regions_enabled[region_sys_count] = 1;
-
-            region_sys_count++;
-            break;
-
-        case MOD_CMN650_REGION_TYPE_SYSCACHE_SUB:
-            FWK_LOG_INFO(
-                MOD_NAME "  [0x%x%x - 0x%x%x] %s",
-                HIGH_WORD(region->base),
-                LOW_WORD(region->base),
-                HIGH_WORD(region->base + region->size - 1),
-                LOW_WORD(region->base + region->size - 1),
-                mmap_type_name[region->type]);
-
-            /* System cache sub-regions are handled by HN-Fs */
-            break;
-
-        default:
-            fwk_unexpected();
-            return FWK_E_DATA;
-        }
+	/* Offset the base with chip address space base on chip-id */
+	base =
+	    ((uint64_t)(ctx->config->chip_addr_space * chip_id) + region->base);
+
+	switch (region->type) {
+	case MOD_CMN650_MEM_REGION_TYPE_IO:
+	    /*
+	     * Configure memory region
+	     */
+	    FWK_LOG_INFO(
+		MOD_NAME "  [0x%x%x - 0x%x%x] %s",
+		HIGH_WORD(base),
+		LOW_WORD(base),
+		HIGH_WORD(base + region->size - 1),
+		LOW_WORD(base + region->size - 1),
+		mmap_type_name[region->type]);
+
+	    configure_region(
+		&rnsam->NON_HASH_MEM_REGION[region_io_count],
+		base,
+		region->size,
+		SAM_NODE_TYPE_HN_I);
+
+	    /*
+	     * Configure target node
+	     */
+	    group = region_io_count /
+		CMN650_RNSAM_NON_HASH_TGT_NODEID_ENTRIES_PER_GROUP;
+	    bit_pos = CMN650_RNSAM_NON_HASH_TGT_NODEID_ENTRY_BITS_WIDTH *
+		(region_io_count %
+		 CMN650_RNSAM_NON_HASH_TGT_NODEID_ENTRIES_PER_GROUP);
+
+	    rnsam->NON_HASH_TGT_NODEID[group] &=
+		~(CMN650_RNSAM_NON_HASH_TGT_NODEID_ENTRY_MASK << bit_pos);
+	    rnsam->NON_HASH_TGT_NODEID[group] |=
+		(region->node_id & CMN650_RNSAM_NON_HASH_TGT_NODEID_ENTRY_MASK)
+		<< bit_pos;
+
+	    region_io_count++;
+	    break;
+
+	case MOD_CMN650_MEM_REGION_TYPE_SYSCACHE:
+	    /*
+	     * Configure memory region
+	     */
+	    FWK_LOG_INFO(
+		MOD_NAME "  [0x%x%x - 0x%x%x] %s",
+		HIGH_WORD(region->base),
+		LOW_WORD(region->base),
+		HIGH_WORD(region->base + region->size - 1),
+		LOW_WORD(region->base + region->size - 1),
+		mmap_type_name[region->type]);
+
+	    configure_region(
+		&rnsam->SYS_CACHE_GRP_REGION[region_sys_count],
+		region->base,
+		region->size,
+		SAM_NODE_TYPE_HN_F);
+
+	    /* Mark corresponding region as enabled */
+	    fwk_assert(region_sys_count < MAX_SCG_COUNT);
+	    scg_regions_enabled[region_sys_count] = 1;
+
+	    region_sys_count++;
+	    break;
+
+	case MOD_CMN650_REGION_TYPE_SYSCACHE_SUB:
+	    FWK_LOG_INFO(
+		MOD_NAME "  [0x%x%x - 0x%x%x] %s",
+		HIGH_WORD(region->base),
+		LOW_WORD(region->base),
+		HIGH_WORD(region->base + region->size - 1),
+		LOW_WORD(region->base + region->size - 1),
+		mmap_type_name[region->type]);
+
+	    /* System cache sub-regions are handled by HN-Fs */
+	    break;
+
+	default:
+	    fwk_unexpected();
+	    return FWK_E_DATA;
+	}
     }
 
     /* Do configuration for CCIX Gateway Home Nodes */
     for (idx = 0; idx < config->ccix_table_count; idx++) {
-        region = &config->ccix_config_table[idx].remote_mmap_table;
+	region = &config->ccix_config_table[idx].remote_mmap_table;
 
-        FWK_LOG_INFO(
-            MOD_NAME "  [0x%x%x - 0x%x%x] %s",
-            HIGH_WORD(region->base),
-            LOW_WORD(region->base),
-            HIGH_WORD(region->base + region->size - 1),
-            LOW_WORD(region->base + region->size - 1),
-            mmap_type_name[region->type]);
-
-        switch (region->type) {
-        case MOD_CMN650_REGION_TYPE_CCIX:
-            /*
-             * Configure memory region
-             */
-            configure_region(
-                &rnsam->NON_HASH_MEM_REGION[region_io_count],
-                region->base,
-                region->size,
-                SAM_NODE_TYPE_CXRA);
-
-            /*
-             * Configure target node
-             */
-            cxra_ldid = config->ccix_config_table[idx].ldid;
-            cxra_node_id = ctx->cxg_ra_reg_table[cxra_ldid].node_id;
-            group = region_io_count /
-                CMN650_RNSAM_NON_HASH_TGT_NODEID_ENTRIES_PER_GROUP;
-            bit_pos = CMN650_RNSAM_NON_HASH_TGT_NODEID_ENTRY_BITS_WIDTH *
-                (region_io_count %
-                 CMN650_RNSAM_NON_HASH_TGT_NODEID_ENTRIES_PER_GROUP);
-
-            rnsam->NON_HASH_TGT_NODEID[group] &=
-                ~(CMN650_RNSAM_NON_HASH_TGT_NODEID_ENTRY_MASK << bit_pos);
-            rnsam->NON_HASH_TGT_NODEID[group] |=
-                (cxra_node_id & CMN650_RNSAM_NON_HASH_TGT_NODEID_ENTRY_MASK)
-                << bit_pos;
-
-            /* CPA Programming */
-            if (config->ccix_config_table[idx].port_aggregate) {
-                FWK_LOG_INFO("Enabling CML Port Aggregation:");
-                FWK_LOG_INFO(
-                    " CXRA Node ID: %d as CPA Group ID: %d",
-                    cxra_node_id,
-                    port_aggregate_group_id);
-
-                group =
-                    region_io_count / CML_PORT_AGGR_MODE_CTRL_REGIONS_PER_GROUP;
-                bit_pos = region_io_count *
-                    CML_PORT_AGGR_MODE_CTRL_PAG_WIDTH_PER_REGION;
-
-                rnsam->CML_PORT_AGGR_MODE_CTRL[group] |=
-                    ((UINT64_C(0x1) << (bit_pos)) |
-                     ((uint64_t)port_aggregate_group_id
-                      << (bit_pos + CML_PORT_AGGR_MODE_CTRL_PAG_GRPID_OFFSET)));
-
-                /* Considering only 2 ports per group */
-                rnsam->CML_PORT_AGGR_CTRL |= UINT64_C(0x1)
-                    << (port_aggregate_group_id *
-                        CML_PORT_AGGR_CTRL_NUM_CXG_PAG_WIDTH);
-
-                cpa_cxra_ldid =
-                    config->ccix_config_table[idx].port_aggregate_ldid;
-                cpa_cxra_node_id = ctx->cxg_ra_reg_table[cpa_cxra_ldid].node_id;
-
-                /* Considering only 2 ports per group */
-                group = (port_aggregate_group_id * NUM_PORTS_PER_CPA_GROUP) /
-                    CMN_PORT_AGGR_GRP_PAG_TGTID_PER_GROUP;
-                bit_pos =
-                    ((port_aggregate_group_id * NUM_PORTS_PER_CPA_GROUP) *
-                     CMN_PORT_AGGR_GRP_PAG_TGTID_WIDTH);
-
-                rnsam->CML_PORT_AGGR_GRP[group] |= ((uint64_t)cxra_node_id)
-                    << bit_pos;
-
-                group =
-                    ((port_aggregate_group_id * NUM_PORTS_PER_CPA_GROUP) + 1) /
-                    CMN_PORT_AGGR_GRP_PAG_TGTID_PER_GROUP;
-                bit_pos =
-                    ((((port_aggregate_group_id * NUM_PORTS_PER_CPA_GROUP) +
-                       1) *
-                      CMN_PORT_AGGR_GRP_PAG_TGTID_WIDTH) %
-                     CMN_PORT_AGGR_GRP_PAG_TGTID_WIDTH_PER_GROUP);
-
-                rnsam->CML_PORT_AGGR_GRP[group] |= (uint64_t)cpa_cxra_node_id
-                    << bit_pos;
-
-                /* Increment the group id for the next group */
-                port_aggregate_group_id++;
-            }
-
-            region_io_count++;
-            break;
-
-        default:
-            fwk_unexpected();
-            return FWK_E_DATA;
-        }
+	FWK_LOG_INFO(
+	    MOD_NAME "  [0x%x%x - 0x%x%x] %s",
+	    HIGH_WORD(region->base),
+	    LOW_WORD(region->base),
+	    HIGH_WORD(region->base + region->size - 1),
+	    LOW_WORD(region->base + region->size - 1),
+	    mmap_type_name[region->type]);
+
+	switch (region->type) {
+	case MOD_CMN650_REGION_TYPE_CCIX:
+	    /*
+	     * Configure memory region
+	     */
+	    configure_region(
+		&rnsam->NON_HASH_MEM_REGION[region_io_count],
+		region->base,
+		region->size,
+		SAM_NODE_TYPE_CXRA);
+
+	    /*
+	     * Configure target node
+	     */
+	    cxra_ldid = config->ccix_config_table[idx].ldid;
+	    cxra_node_id = ctx->cxg_ra_reg_table[cxra_ldid].node_id;
+	    group = region_io_count /
+		CMN650_RNSAM_NON_HASH_TGT_NODEID_ENTRIES_PER_GROUP;
+	    bit_pos = CMN650_RNSAM_NON_HASH_TGT_NODEID_ENTRY_BITS_WIDTH *
+		(region_io_count %
+		 CMN650_RNSAM_NON_HASH_TGT_NODEID_ENTRIES_PER_GROUP);
+
+	    rnsam->NON_HASH_TGT_NODEID[group] &=
+		~(CMN650_RNSAM_NON_HASH_TGT_NODEID_ENTRY_MASK << bit_pos);
+	    rnsam->NON_HASH_TGT_NODEID[group] |=
+		(cxra_node_id & CMN650_RNSAM_NON_HASH_TGT_NODEID_ENTRY_MASK)
+		<< bit_pos;
+
+	    /* CPA Programming */
+	    if (config->ccix_config_table[idx].port_aggregate) {
+		FWK_LOG_INFO("Enabling CML Port Aggregation:");
+		FWK_LOG_INFO(
+		    " CXRA Node ID: %d as CPA Group ID: %d",
+		    cxra_node_id,
+		    port_aggregate_group_id);
+
+		group =
+		    region_io_count / CML_PORT_AGGR_MODE_CTRL_REGIONS_PER_GROUP;
+		bit_pos = region_io_count *
+		    CML_PORT_AGGR_MODE_CTRL_PAG_WIDTH_PER_REGION;
+
+		rnsam->CML_PORT_AGGR_MODE_CTRL[group] |=
+		    ((UINT64_C(0x1) << (bit_pos)) |
+		     ((uint64_t)port_aggregate_group_id
+		      << (bit_pos + CML_PORT_AGGR_MODE_CTRL_PAG_GRPID_OFFSET)));
+
+		/* Considering only 2 ports per group */
+		rnsam->CML_PORT_AGGR_CTRL |= UINT64_C(0x1)
+		    << (port_aggregate_group_id *
+			CML_PORT_AGGR_CTRL_NUM_CXG_PAG_WIDTH);
+
+		cpa_cxra_ldid =
+		    config->ccix_config_table[idx].port_aggregate_ldid;
+		cpa_cxra_node_id = ctx->cxg_ra_reg_table[cpa_cxra_ldid].node_id;
+
+		/* Considering only 2 ports per group */
+		group = (port_aggregate_group_id * NUM_PORTS_PER_CPA_GROUP) /
+		    CMN_PORT_AGGR_GRP_PAG_TGTID_PER_GROUP;
+		bit_pos =
+		    ((port_aggregate_group_id * NUM_PORTS_PER_CPA_GROUP) *
+		     CMN_PORT_AGGR_GRP_PAG_TGTID_WIDTH);
+
+		rnsam->CML_PORT_AGGR_GRP[group] |= ((uint64_t)cxra_node_id)
+		    << bit_pos;
+
+		group =
+		    ((port_aggregate_group_id * NUM_PORTS_PER_CPA_GROUP) + 1) /
+		    CMN_PORT_AGGR_GRP_PAG_TGTID_PER_GROUP;
+		bit_pos =
+		    ((((port_aggregate_group_id * NUM_PORTS_PER_CPA_GROUP) +
+		       1) *
+		      CMN_PORT_AGGR_GRP_PAG_TGTID_WIDTH) %
+		     CMN_PORT_AGGR_GRP_PAG_TGTID_WIDTH_PER_GROUP);
+
+		rnsam->CML_PORT_AGGR_GRP[group] |= (uint64_t)cpa_cxra_node_id
+		    << bit_pos;
+
+		/* Increment the group id for the next group */
+		port_aggregate_group_id++;
+	    }
+
+	    region_io_count++;
+	    break;
+
+	default:
+	    fwk_unexpected();
+	    return FWK_E_DATA;
+	}
     }
 
     /*
@@ -649,13 +649,13 @@ static int cmn650_setup_sam(struct cmn65
      * half if CAL mode is enabled.
      */
     if (config->hnf_cal_mode)
-        hnf_count = ctx->hnf_count / 2;
+	hnf_count = ctx->hnf_count / 2;
     else
-        hnf_count = ctx->hnf_count;
+	hnf_count = ctx->hnf_count;
 
     group_count = hnf_count / CMN650_HNF_CACHE_GROUP_ENTRIES_PER_GROUP;
     for (group = 0; group < group_count; group++)
-        rnsam->SYS_CACHE_GRP_HN_NODEID[group] = ctx->hnf_cache_group[group];
+	rnsam->SYS_CACHE_GRP_HN_NODEID[group] = ctx->hnf_cache_group[group];
 
     /* Program the number of HNFs */
     rnsam->SYS_CACHE_GRP_HN_COUNT = hnf_count;
@@ -665,26 +665,26 @@ static int cmn650_setup_sam(struct cmn65
      * enable register.
      */
     if (config->hnf_cal_mode) {
-        for (region_idx = 0; region_idx < MAX_SCG_COUNT; region_idx++)
-            rnsam->SYS_CACHE_GRP_CAL_MODE |= scg_regions_enabled[region_idx] *
-                (CMN650_RNSAM_SCG_HNF_CAL_MODE_EN
-                 << (region_idx * CMN650_RNSAM_SCG_HNF_CAL_MODE_SHIFT));
+	for (region_idx = 0; region_idx < MAX_SCG_COUNT; region_idx++)
+	    rnsam->SYS_CACHE_GRP_CAL_MODE |= scg_regions_enabled[region_idx] *
+		(CMN650_RNSAM_SCG_HNF_CAL_MODE_EN
+		 << (region_idx * CMN650_RNSAM_SCG_HNF_CAL_MODE_SHIFT));
 
     /* Program the SYS_CACHE_GRP_SN_NODEID register for PrefetchTgt */
-        group_count = config->snf_count /
-            (CMN650_RNSAM_SYS_CACHE_GRP_SN_NODEID_ENTRIES_PER_GROUP * 2);
+	group_count = config->snf_count /
+	    (CMN650_RNSAM_SYS_CACHE_GRP_SN_NODEID_ENTRIES_PER_GROUP * 2);
     } else {
-        group_count = config->snf_count /
-            CMN650_RNSAM_SYS_CACHE_GRP_SN_NODEID_ENTRIES_PER_GROUP;
+	group_count = config->snf_count /
+	    CMN650_RNSAM_SYS_CACHE_GRP_SN_NODEID_ENTRIES_PER_GROUP;
     }
 
     for (group = 0; group < group_count; group++)
-        rnsam->SYS_CACHE_GRP_SN_NODEID[group] = ctx->sn_nodeid_group[group];
+	rnsam->SYS_CACHE_GRP_SN_NODEID[group] = ctx->sn_nodeid_group[group];
 
     /* Enable RNSAM */
     rnsam->STATUS = ((uint64_t)config->hnd_node_id
-                     << CMN650_RNSAM_STATUS_DEFAULT_NODEID_POS) |
-        CMN650_RNSAM_STATUS_UNSTALL;
+		     << CMN650_RNSAM_STATUS_DEFAULT_NODEID_POS) |
+	CMN650_RNSAM_STATUS_UNSTALL;
     __sync_synchronize();
 
     return FWK_SUCCESS;
@@ -696,69 +696,69 @@ static int cmn650_setup(void)
     int status;
 
     if (!ctx->initialized) {
-        status = cmn650_discovery();
-        if (status != FWK_SUCCESS)
-            return FWK_SUCCESS;
-
-        /*
-         * Allocate resources based on the discovery
-         */
-
-        /* Pointers for the internal RN-SAM nodes */
-        if (ctx->internal_rnsam_count != 0) {
-            ctx->internal_rnsam_table = fwk_mm_calloc(
-                ctx->internal_rnsam_count, sizeof(*ctx->internal_rnsam_table));
-        }
-
-        /* Tuples for the external RN-RAM nodes (including their node IDs) */
-        if (ctx->external_rnsam_count != 0) {
-            ctx->external_rnsam_table = fwk_mm_calloc(
-                ctx->external_rnsam_count, sizeof(*ctx->external_rnsam_table));
-        }
-
-        /* Cache groups */
-        if (ctx->hnf_count != 0) {
-            /*
-             * Allocate enough group descriptors to accommodate all expected
-             * HN-F nodes in the system.
-             */
-            ctx->hnf_node =
-                fwk_mm_calloc(ctx->hnf_count, sizeof(*ctx->hnf_node));
-            if (ctx->hnf_node == NULL)
-                return FWK_E_NOMEM;
-            ctx->hnf_cache_group = fwk_mm_calloc(
-                ctx->hnf_count / CMN650_HNF_CACHE_GROUP_ENTRIES_PER_GROUP,
-                sizeof(*ctx->hnf_cache_group));
-            ctx->sn_nodeid_group = fwk_mm_calloc(
-                ctx->hnf_count /
-                    CMN650_RNSAM_SYS_CACHE_GRP_SN_NODEID_ENTRIES_PER_GROUP,
-                sizeof(*ctx->sn_nodeid_group));
-        }
-
-        /* Allocate resource for the CCIX Gateway nodes */
-        if (ctx->ccix_node_count != 0) {
-            ctx->cxg_ra_reg_table = fwk_mm_calloc(
-                ctx->ccix_node_count, sizeof(*ctx->cxg_ra_reg_table));
-            if (ctx->cxg_ra_reg_table == NULL)
-                return FWK_E_NOMEM;
-
-            ctx->cxg_ha_reg_table = fwk_mm_calloc(
-                ctx->ccix_node_count, sizeof(*ctx->cxg_ha_reg_table));
-            if (ctx->cxg_ha_reg_table == NULL)
-                return FWK_E_NOMEM;
-
-            ctx->cxla_reg_table = fwk_mm_calloc(
-                ctx->ccix_node_count, sizeof(*ctx->cxla_reg_table));
-            if (ctx->cxla_reg_table == NULL)
-                return FWK_E_NOMEM;
-        }
+	status = cmn650_discovery();
+	if (status != FWK_SUCCESS)
+	    return FWK_SUCCESS;
+
+	/*
+	 * Allocate resources based on the discovery
+	 */
+
+	/* Pointers for the internal RN-SAM nodes */
+	if (ctx->internal_rnsam_count != 0) {
+	    ctx->internal_rnsam_table = fwk_mm_calloc(
+		ctx->internal_rnsam_count, sizeof(*ctx->internal_rnsam_table));
+	}
+
+	/* Tuples for the external RN-RAM nodes (including their node IDs) */
+	if (ctx->external_rnsam_count != 0) {
+	    ctx->external_rnsam_table = fwk_mm_calloc(
+		ctx->external_rnsam_count, sizeof(*ctx->external_rnsam_table));
+	}
+
+	/* Cache groups */
+	if (ctx->hnf_count != 0) {
+	    /*
+	     * Allocate enough group descriptors to accommodate all expected
+	     * HN-F nodes in the system.
+	     */
+	    ctx->hnf_node =
+		fwk_mm_calloc(ctx->hnf_count, sizeof(*ctx->hnf_node));
+	    if (ctx->hnf_node == NULL)
+		return FWK_E_NOMEM;
+	    ctx->hnf_cache_group = fwk_mm_calloc(
+		ctx->hnf_count / CMN650_HNF_CACHE_GROUP_ENTRIES_PER_GROUP,
+		sizeof(*ctx->hnf_cache_group));
+	    ctx->sn_nodeid_group = fwk_mm_calloc(
+		ctx->hnf_count /
+		    CMN650_RNSAM_SYS_CACHE_GRP_SN_NODEID_ENTRIES_PER_GROUP,
+		sizeof(*ctx->sn_nodeid_group));
+	}
+
+	/* Allocate resource for the CCIX Gateway nodes */
+	if (ctx->ccix_node_count != 0) {
+	    ctx->cxg_ra_reg_table = fwk_mm_calloc(
+		ctx->ccix_node_count, sizeof(*ctx->cxg_ra_reg_table));
+	    if (ctx->cxg_ra_reg_table == NULL)
+		return FWK_E_NOMEM;
+
+	    ctx->cxg_ha_reg_table = fwk_mm_calloc(
+		ctx->ccix_node_count, sizeof(*ctx->cxg_ha_reg_table));
+	    if (ctx->cxg_ha_reg_table == NULL)
+		return FWK_E_NOMEM;
+
+	    ctx->cxla_reg_table = fwk_mm_calloc(
+		ctx->ccix_node_count, sizeof(*ctx->cxla_reg_table));
+	    if (ctx->cxla_reg_table == NULL)
+		return FWK_E_NOMEM;
+	}
     }
 
     cmn650_configure();
 
     /* Setup internal RN-SAM nodes */
     for (rnsam_idx = 0; rnsam_idx < ctx->internal_rnsam_count; rnsam_idx++)
-        cmn650_setup_sam(ctx->internal_rnsam_table[rnsam_idx]);
+	cmn650_setup_sam(ctx->internal_rnsam_table[rnsam_idx]);
 
     FWK_LOG_INFO(MOD_NAME "Done");
 
@@ -777,16 +777,16 @@ static int cmn650_ccix_setup(void)
 
     /* Do configuration for CCIX Gateway Nodes and enable the links */
     for (idx = 0; idx < config->ccix_table_count; idx++)
-        ccix_setup(chip_id, ctx, &config->ccix_config_table[idx]);
+	ccix_setup(chip_id, ctx, &config->ccix_config_table[idx]);
 
     /*
      * Exchange protocol credits and enter system coherecy and dvm domain for
      * multichip SMP mode operation.
      */
     for (idx = 0; idx < config->ccix_table_count; idx++) {
-        ccix_exchange_protocol_credit(ctx, &config->ccix_config_table[idx]);
-        ccix_enter_system_coherency(ctx, &config->ccix_config_table[idx]);
-        ccix_enter_dvm_domain(ctx, &config->ccix_config_table[idx]);
+	ccix_exchange_protocol_credit(ctx, &config->ccix_config_table[idx]);
+	ccix_enter_system_coherency(ctx, &config->ccix_config_table[idx]);
+	ccix_enter_dvm_domain(ctx, &config->ccix_config_table[idx]);
     }
     return FWK_SUCCESS;
 }
@@ -796,10 +796,10 @@ static int cmn650_setup_rnsam(unsigned i
     unsigned int node_idx;
 
     for (node_idx = 0; node_idx < ctx->external_rnsam_count; node_idx++) {
-        if (ctx->external_rnsam_table[node_idx].node_id == node_id) {
-            cmn650_setup_sam(ctx->external_rnsam_table[node_idx].node);
-            return FWK_SUCCESS;
-        }
+	if (ctx->external_rnsam_table[node_idx].node_id == node_id) {
+	    cmn650_setup_sam(ctx->external_rnsam_table[node_idx].node);
+	    return FWK_SUCCESS;
+	}
     }
 
     return FWK_E_PARAM;
@@ -830,7 +830,7 @@ static int cmn650_init(
 {
     /* Atleast one device should be passed as element */
     if (device_count == 0)
-        return FWK_E_DATA;
+	return FWK_E_DATA;
 
     /* Allocate space for the device context table */
     ctx = fwk_mm_calloc(device_count, sizeof(struct cmn650_device_ctx));
@@ -851,24 +851,24 @@ static int cmn650_device_init(
     device_ctx->config = data;
 
     if (device_ctx->config->base == 0)
-        return FWK_E_DATA;
+	return FWK_E_DATA;
 
     if ((device_ctx->config->mesh_size_x == 0) ||
-        (device_ctx->config->mesh_size_x > CMN650_MESH_X_MAX))
-        return FWK_E_DATA;
+	(device_ctx->config->mesh_size_x > CMN650_MESH_X_MAX))
+	return FWK_E_DATA;
 
     if ((device_ctx->config->mesh_size_y == 0) ||
-        (device_ctx->config->mesh_size_y > CMN650_MESH_Y_MAX))
-        return FWK_E_DATA;
+	(device_ctx->config->mesh_size_y > CMN650_MESH_Y_MAX))
+	return FWK_E_DATA;
 
     if (device_ctx->config->snf_count > CMN650_HNF_CACHE_GROUP_ENTRIES_MAX)
-        return FWK_E_DATA;
+	return FWK_E_DATA;
 
     device_ctx->root = get_root_node(
-        device_ctx->config->base,
-        device_ctx->config->hnd_node_id,
-        device_ctx->config->mesh_size_x,
-        device_ctx->config->mesh_size_y);
+	device_ctx->config->base,
+	device_ctx->config->hnd_node_id,
+	device_ctx->config->mesh_size_x,
+	device_ctx->config->mesh_size_y);
 
     return FWK_SUCCESS;
 }
@@ -879,25 +879,25 @@ static int cmn650_bind(fwk_id_t id, unsi
     struct cmn650_device_ctx *device_ctx;
 
     if (fwk_id_is_type(id, FWK_ID_TYPE_MODULE)) {
-        /* Bind to system info module to obtain multi-chip info */
-        status = fwk_module_bind(
-            FWK_ID_MODULE(FWK_MODULE_IDX_SYSTEM_INFO),
-            FWK_ID_API(FWK_MODULE_IDX_SYSTEM_INFO, MOD_SYSTEM_INFO_GET_API_IDX),
-            &system_info_api);
-        return status;
+	/* Bind to system info module to obtain multi-chip info */
+	status = fwk_module_bind(
+	    FWK_ID_MODULE(FWK_MODULE_IDX_SYSTEM_INFO),
+	    FWK_ID_API(FWK_MODULE_IDX_SYSTEM_INFO, MOD_SYSTEM_INFO_GET_API_IDX),
+	    &system_info_api);
+	return status;
     }
 
     /* Use second round only (round numbering is zero-indexed) */
     if (round == 1) {
-        device_ctx = ctx + fwk_id_get_element_idx(id);
+	device_ctx = ctx + fwk_id_get_element_idx(id);
 
-        /* Bind to the timer component */
-        status = fwk_module_bind(
-            FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
-            FWK_ID_API(FWK_MODULE_IDX_TIMER, MOD_TIMER_API_IDX_TIMER),
-            &device_ctx->timer_api);
-        if (status != FWK_SUCCESS)
-            return FWK_E_PANIC;
+	/* Bind to the timer component */
+	status = fwk_module_bind(
+	    FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
+	    FWK_ID_API(FWK_MODULE_IDX_TIMER, MOD_TIMER_API_IDX_TIMER),
+	    &device_ctx->timer_api);
+	if (status != FWK_SUCCESS)
+	    return FWK_E_PANIC;
     }
 
     return FWK_SUCCESS;
@@ -919,33 +919,33 @@ int cmn650_start(fwk_id_t id)
 
     /* No need to do anything for element */
     if (!fwk_module_is_valid_element_id(id))
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 
     status = system_info_api->get_system_info(&system_info);
     if (status == FWK_SUCCESS) {
-        chip_id = system_info->chip_id;
-        multi_chip_mode = system_info->multi_chip_mode;
+	chip_id = system_info->chip_id;
+	multi_chip_mode = system_info->multi_chip_mode;
     }
 
     /* No need to anything for other elements */
     if (fwk_id_get_element_idx(id) != chip_id)
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 
     /* Pickup the context based on the chip_id */
     ctx = ctx + fwk_id_get_element_idx(id);
 
     FWK_LOG_INFO(
-        MOD_NAME "Multichip mode: %d Chip ID: %d\n", multi_chip_mode, chip_id);
+	MOD_NAME "Multichip mode: %d Chip ID: %d\n", multi_chip_mode, chip_id);
 
     if (fwk_id_is_equal(ctx->config->clock_id, FWK_ID_NONE)) {
-        cmn650_setup();
-        cmn650_ccix_setup();
-        return FWK_SUCCESS;
+	cmn650_setup();
+	cmn650_ccix_setup();
+	return FWK_SUCCESS;
     }
 
     /* Register the module for clock state notifications */
     return fwk_notification_subscribe(
-        mod_clock_notification_id_state_changed, ctx->config->clock_id, id);
+	mod_clock_notification_id_state_changed, ctx->config->clock_id, id);
 }
 
 static int cmn650_process_notification(
@@ -955,13 +955,13 @@ static int cmn650_process_notification(
     struct clock_notification_params *params;
 
     fwk_assert(
-        fwk_id_is_equal(event->id, mod_clock_notification_id_state_changed));
+	fwk_id_is_equal(event->id, mod_clock_notification_id_state_changed));
     fwk_assert(fwk_id_is_type(event->target_id, FWK_ID_TYPE_ELEMENT));
 
     params = (struct clock_notification_params *)event->params;
     if (params->new_state == MOD_CLOCK_STATE_RUNNING) {
-        cmn650_setup();
-        cmn650_ccix_setup();
+	cmn650_setup();
+	cmn650_ccix_setup();
     }
 
     return FWK_SUCCESS;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cmn700/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/cmn700/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cmn700/CMakeLists.txt
@@ -7,13 +7,13 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include"
-                           PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include"
+			   PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src")
 
 target_sources(
     ${SCP_MODULE_TARGET} PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_cmn700.c"
-                                 "${CMAKE_CURRENT_SOURCE_DIR}/src/cmn700.c"
-                                 "${CMAKE_CURRENT_SOURCE_DIR}/src/cmn700_ccg.c")
+				 "${CMAKE_CURRENT_SOURCE_DIR}/src/cmn700.c"
+				 "${CMAKE_CURRENT_SOURCE_DIR}/src/cmn700_ccg.c")
 
 target_link_libraries(${SCP_MODULE_TARGET}
     PRIVATE module-clock module-timer module-sds module-system-info)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cmn700/include/mod_cmn700.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/cmn700/include/mod_cmn700.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cmn700/include/mod_cmn700.h
@@ -302,14 +302,14 @@ struct mod_cmn700_ccg_config {
 
     /*! Table of remote region memory map entries */
     const struct mod_cmn700_ra_mem_region_map
-        ra_mmap_table[CMN700_MAX_RA_SAM_ADDR_REGION];
+	ra_mmap_table[CMN700_MAX_RA_SAM_ADDR_REGION];
 
     /*! Number of entries in the ::mod_cmn700_ccg_config::ra_mmap_table */
     size_t ra_mmap_count;
 
     /*! Table of remote agent ids start and end backed by the links */
     struct mod_cmn700_agentid_to_linkid_map
-        remote_agentid_to_linkid_map[CMN700_MAX_CCG_PROTOCOL_LINKS];
+	remote_agentid_to_linkid_map[CMN700_MAX_CCG_PROTOCOL_LINKS];
 
     /*! SMP Mode */
     bool smp_mode;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cmn700/src/cmn700.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/cmn700/src/cmn700.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cmn700/src/cmn700.c
@@ -35,7 +35,7 @@ unsigned int get_node_device_port_count(
 {
     struct node_header *node = node_base;
     return (node->NODE_INFO & CMN700_MXP_NODE_INFO_NUM_DEVICE_PORT_MASK) >>
-        CMN700_MXP_NODE_INFO_NUM_DEVICE_PORT_POS;
+	CMN700_MXP_NODE_INFO_NUM_DEVICE_PORT_POS;
 }
 
 unsigned int get_node_child_count(void *node_base)
@@ -60,7 +60,7 @@ unsigned int get_node_logical_id(void *n
 {
     struct node_header *node = node_base;
     return (node->NODE_INFO & CMN700_NODE_INFO_LOGICAL_ID) >>
-        CMN700_NODE_INFO_LOGICAL_ID_POS;
+	CMN700_NODE_INFO_LOGICAL_ID_POS;
 }
 
 void *get_child_node(uintptr_t base, void *node_base, unsigned int child_index)
@@ -84,8 +84,8 @@ unsigned int get_child_node_id(void *nod
     unsigned int node_id;
 
     node_pointer = (node->CHILD_POINTER[child_index] &
-                    CMN700_CHILD_POINTER_EXT_NODE_POINTER) >>
-        CMN700_CHILD_POINTER_EXT_NODE_POINTER_POS;
+		    CMN700_CHILD_POINTER_EXT_NODE_POINTER) >>
+	CMN700_CHILD_POINTER_EXT_NODE_POINTER_POS;
 
     /*
      * For mesh widths using 2 bits each for X,Y encoding:
@@ -101,7 +101,7 @@ unsigned int get_child_node_id(void *nod
      * NodeID[8:6] = NODE POINTER[11:9]
      */
     node_id = (((node_pointer >> 6) & 0xff) << 3) |
-        ((node_pointer & 0x1) << 2) | ((node_pointer >> 2) & 0x3);
+	((node_pointer & 0x1) << 2) | ((node_pointer >> 2) & 0x3);
 
     return node_id;
 }
@@ -109,7 +109,7 @@ unsigned int get_child_node_id(void *nod
 unsigned int get_cmn700_revision(struct cmn700_cfgm_reg *root)
 {
     return (root->PERIPH_ID[1] & CMN700_PERIPH_ID_2_MASK) >>
-        CMN700_PERIPH_ID_2_REV_POS;
+	CMN700_PERIPH_ID_2_REV_POS;
 }
 
 const char *get_cmn700_revision_name(struct cmn700_cfgm_reg *root)
@@ -118,7 +118,7 @@ const char *get_cmn700_revision_name(str
 
     revision = get_cmn700_revision(root);
     if (revision > CMN700_PERIPH_ID_UNKNOWN_REV)
-        revision = CMN700_PERIPH_ID_UNKNOWN_REV;
+	revision = CMN700_PERIPH_ID_UNKNOWN_REV;
 
     return cmn700_rev_to_name[revision];
 }
@@ -134,12 +134,12 @@ unsigned int get_port_number(
     unsigned int xp_port_cnt)
 {
     if (xp_port_cnt == 2) {
-        return (child_node_id >> CMN700_NODE_ID_PORT_POS) &
-            CMN700_NODE_ID_PORT_MASK;
+	return (child_node_id >> CMN700_NODE_ID_PORT_POS) &
+	    CMN700_NODE_ID_PORT_MASK;
     } else {
-        /* For port counts 3 and 4 */
-        return (child_node_id >> CMN700_MULTI_PORTS_NODE_ID_PORT_POS) &
-            CMN700_MULTI_PORTS_NODE_ID_PORT_MASK;
+	/* For port counts 3 and 4 */
+	return (child_node_id >> CMN700_MULTI_PORTS_NODE_ID_PORT_POS) &
+	    CMN700_MULTI_PORTS_NODE_ID_PORT_MASK;
     }
 }
 
@@ -147,39 +147,39 @@ unsigned int get_device_type(void *mxp_b
 {
     struct cmn700_mxp_reg *mxp = mxp_base;
     return mxp->PORT_CONNECT_INFO[port] &
-        CMN700_MXP_PORT_CONNECT_INFO_DEVICE_TYPE_MASK;
+	CMN700_MXP_PORT_CONNECT_INFO_DEVICE_TYPE_MASK;
 }
 
 bool is_cal_connected(void *mxp_base, bool port)
 {
     struct cmn700_mxp_reg *mxp = mxp_base;
     return (mxp->PORT_CONNECT_INFO[port] &
-            CMN700_MXP_PORT_CONNECT_INFO_CAL_CONNECTED_MASK) >>
-        CMN700_MXP_PORT_CONNECT_INFO_CAL_CONNECTED_POS;
+	    CMN700_MXP_PORT_CONNECT_INFO_CAL_CONNECTED_MASK) >>
+	CMN700_MXP_PORT_CONNECT_INFO_CAL_CONNECTED_POS;
 }
 
 bool is_device_type_rnf(void *mxp_base, bool port)
 {
     return (
-        (get_device_type(mxp_base, port) == DEVICE_TYPE_RN_F_CHIB_ESAM) ||
-        (get_device_type(mxp_base, port) == DEVICE_TYPE_RN_F_CHIC_ESAM) ||
-        (get_device_type(mxp_base, port) == DEVICE_TYPE_RN_F_CHID_ESAM) ||
-        (get_device_type(mxp_base, port) == DEVICE_TYPE_RN_F_CHIE_ESAM));
+	(get_device_type(mxp_base, port) == DEVICE_TYPE_RN_F_CHIB_ESAM) ||
+	(get_device_type(mxp_base, port) == DEVICE_TYPE_RN_F_CHIC_ESAM) ||
+	(get_device_type(mxp_base, port) == DEVICE_TYPE_RN_F_CHID_ESAM) ||
+	(get_device_type(mxp_base, port) == DEVICE_TYPE_RN_F_CHIE_ESAM));
 }
 
 static unsigned int get_rnsam_htg_rcomp_lsb_bit_pos(void *rnsam_reg)
 {
     struct cmn700_rnsam_reg *rnsam = rnsam_reg;
     return (
-        rnsam->UNIT_INFO[1] & CMN700_RNSAM_UNIT_INFO_HTG_RCOMP_LSB_PARAM_MASK);
+	rnsam->UNIT_INFO[1] & CMN700_RNSAM_UNIT_INFO_HTG_RCOMP_LSB_PARAM_MASK);
 }
 
 static unsigned int get_rnsam_nonhash_rcomp_lsb_bit_pos(void *rnsam_reg)
 {
     struct cmn700_rnsam_reg *rnsam = rnsam_reg;
     return (rnsam->UNIT_INFO[1] &
-            CMN700_RNSAM_UNIT_INFO_NONHASH_RCOMP_LSB_PARAM_MASK) >>
-        CMN700_RNSAM_UNIT_INFO_NONHASH_RCOMP_LSB_PARAM_POS;
+	    CMN700_RNSAM_UNIT_INFO_NONHASH_RCOMP_LSB_PARAM_MASK) >>
+	CMN700_RNSAM_UNIT_INFO_NONHASH_RCOMP_LSB_PARAM_POS;
 }
 
 static uint64_t get_rnsam_lsb_addr_mask(void *rnsam_reg, enum sam_type sam_type)
@@ -188,8 +188,8 @@ static uint64_t get_rnsam_lsb_addr_mask(
     struct cmn700_rnsam_reg *rnsam = rnsam_reg;
 
     lsb_bit_pos = (sam_type == SAM_TYPE_NON_HASH_MEM_REGION) ?
-        get_rnsam_nonhash_rcomp_lsb_bit_pos(rnsam) :
-        get_rnsam_htg_rcomp_lsb_bit_pos(rnsam);
+	get_rnsam_nonhash_rcomp_lsb_bit_pos(rnsam) :
+	get_rnsam_htg_rcomp_lsb_bit_pos(rnsam);
 
     return (1 << lsb_bit_pos) - 1;
 }
@@ -198,23 +198,23 @@ bool get_rnsam_nonhash_range_comp_en_mod
 {
     struct cmn700_rnsam_reg *rnsam = rnsam_reg;
     return (rnsam->UNIT_INFO[0] &
-            CMN700_RNSAM_UNIT_INFO_NONHASH_RANGE_COMP_EN_MASK) >>
-        CMN700_RNSAM_UNIT_INFO_NONHASH_RANGE_COMP_EN_POS;
+	    CMN700_RNSAM_UNIT_INFO_NONHASH_RANGE_COMP_EN_MASK) >>
+	CMN700_RNSAM_UNIT_INFO_NONHASH_RANGE_COMP_EN_POS;
 }
 
 bool get_rnsam_htg_range_comp_en_mode(void *rnsam_reg)
 {
     struct cmn700_rnsam_reg *rnsam = rnsam_reg;
     return (rnsam->UNIT_INFO[0] &
-            CMN700_RNSAM_UNIT_INFO_HTG_RANGE_COMP_EN_MASK) >>
-        CMN700_RNSAM_UNIT_INFO_HTG_RANGE_COMP_EN_POS;
+	    CMN700_RNSAM_UNIT_INFO_HTG_RANGE_COMP_EN_MASK) >>
+	CMN700_RNSAM_UNIT_INFO_HTG_RANGE_COMP_EN_POS;
 }
 
 bool get_hnsam_range_comp_en_mode(void *hnf_reg)
 {
     struct cmn700_hnf_reg *hnf = hnf_reg;
     return (hnf->UNIT_INFO[1] & CMN700_HNF_UNIT_INFO_HNSAM_RCOMP_EN_MASK) >>
-        CMN700_HNF_UNIT_INFO_HNSAM_RCOMP_EN_POS;
+	CMN700_HNF_UNIT_INFO_HNSAM_RCOMP_EN_POS;
 }
 
 uint64_t sam_encode_region_size(uint64_t size)
@@ -252,53 +252,53 @@ void configure_region(
     fwk_assert(rnsam_reg);
 
     if (sam_type == SAM_TYPE_NON_HASH_MEM_REGION) {
-        if (region_idx >= MAX_NON_HASH_MEM_COUNT) {
-            FWK_LOG_ERR(
-                MOD_NAME
-                "Region idx: %d should be less than max non hash region count "
-                "of %d",
-                region_idx,
-                MAX_NON_HASH_MEM_COUNT);
-            fwk_unexpected();
-        }
-
-        if (region_idx < NON_HASH_MEM_REG_COUNT) {
-            reg = &rnsam->NON_HASH_MEM_REGION[region_idx];
-            reg_cfg2 = &rnsam->NON_HASH_MEM_REGION_CFG2[region_idx];
-        } else {
-            reg = &rnsam->NON_HASH_MEM_REGION_GRP2
-                       [region_idx - NON_HASH_MEM_REG_COUNT];
-            reg_cfg2 = &rnsam->NON_HASH_MEM_REGION_CFG2_GRP2
-                            [region_idx - NON_HASH_MEM_REG_COUNT];
-        }
+	if (region_idx >= MAX_NON_HASH_MEM_COUNT) {
+	    FWK_LOG_ERR(
+		MOD_NAME
+		"Region idx: %d should be less than max non hash region count "
+		"of %d",
+		region_idx,
+		MAX_NON_HASH_MEM_COUNT);
+	    fwk_unexpected();
+	}
+
+	if (region_idx < NON_HASH_MEM_REG_COUNT) {
+	    reg = &rnsam->NON_HASH_MEM_REGION[region_idx];
+	    reg_cfg2 = &rnsam->NON_HASH_MEM_REGION_CFG2[region_idx];
+	} else {
+	    reg = &rnsam->NON_HASH_MEM_REGION_GRP2
+		       [region_idx - NON_HASH_MEM_REG_COUNT];
+	    reg_cfg2 = &rnsam->NON_HASH_MEM_REGION_CFG2_GRP2
+			    [region_idx - NON_HASH_MEM_REG_COUNT];
+	}
     } else if (sam_type == SAM_TYPE_SYS_CACHE_GRP_REGION) {
-        if (region_idx >= MAX_SCG_COUNT) {
-            FWK_LOG_ERR(
-                MOD_NAME
-                "Region idx: %d should be less then max SCG region count of %d",
-                region_idx,
-                MAX_SCG_COUNT);
-            fwk_unexpected();
-        }
-        reg = &rnsam->SYS_CACHE_GRP_REGION[region_idx];
-        reg_cfg2 = &rnsam->HASHED_TGT_GRP_CFG2_REGION[region_idx];
+	if (region_idx >= MAX_SCG_COUNT) {
+	    FWK_LOG_ERR(
+		MOD_NAME
+		"Region idx: %d should be less then max SCG region count of %d",
+		region_idx,
+		MAX_SCG_COUNT);
+	    fwk_unexpected();
+	}
+	reg = &rnsam->SYS_CACHE_GRP_REGION[region_idx];
+	reg_cfg2 = &rnsam->HASHED_TGT_GRP_CFG2_REGION[region_idx];
     } else {
-        FWK_LOG_ERR(MOD_NAME "Unexpected sam_type!");
-        fwk_unexpected();
-        return;
+	FWK_LOG_ERR(MOD_NAME "Unexpected sam_type!");
+	fwk_unexpected();
+	return;
     }
 
     /* Check if the start and end address has to be programmed */
     prog_start_and_end_addr = (sam_type == SAM_TYPE_NON_HASH_MEM_REGION) ?
-        get_rnsam_nonhash_range_comp_en_mode(rnsam) :
-        get_rnsam_htg_range_comp_en_mode(rnsam);
+	get_rnsam_nonhash_range_comp_en_mode(rnsam) :
+	get_rnsam_htg_range_comp_en_mode(rnsam);
 
     if ((!prog_start_and_end_addr) && ((base % size) != 0)) {
-        FWK_LOG_ERR(
-            MOD_NAME "Base: 0x%" PRIx64 " should align with Size: 0x%" PRIx64,
-            base,
-            size);
-        fwk_unexpected();
+	FWK_LOG_ERR(
+	    MOD_NAME "Base: 0x%" PRIx64 " should align with Size: 0x%" PRIx64,
+	    base,
+	    size);
+	fwk_unexpected();
     }
 
     /* Get the LSB mask from LSB bit position defining minimum region size */
@@ -308,14 +308,14 @@ void configure_region(
     value |= node_type << CMN700_RNSAM_REGION_ENTRY_TYPE_POS;
 
     if (prog_start_and_end_addr) {
-        value |= (base & ~lsb_addr_mask);
-        *reg = value;
-        *reg_cfg2 = (base + size - 1) & ~lsb_addr_mask;
+	value |= (base & ~lsb_addr_mask);
+	*reg = value;
+	*reg_cfg2 = (base + size - 1) & ~lsb_addr_mask;
     } else {
-        value |= sam_encode_region_size(size)
-            << CMN700_RNSAM_REGION_ENTRY_SIZE_POS;
-        value |= (base / SAM_GRANULARITY) << CMN700_RNSAM_REGION_ENTRY_BASE_POS;
-        *reg = value;
+	value |= sam_encode_region_size(size)
+	    << CMN700_RNSAM_REGION_ENTRY_SIZE_POS;
+	value |= (base / SAM_GRANULARITY) << CMN700_RNSAM_REGION_ENTRY_BASE_POS;
+	*reg = value;
     }
 }
 
@@ -349,11 +349,11 @@ const char *get_node_type_name(enum node
 {
     /* Base node IDs */
     if (node_type <= NODE_TYPE_HN_P)
-        return type_to_name[node_type];
+	return type_to_name[node_type];
 
     /* CML node IDs */
     if ((node_type >= NODE_TYPE_CML_BASE) && (node_type <= NODE_TYPE_CCLA))
-        return type_to_name_cml[node_type - NODE_TYPE_CML_BASE];
+	return type_to_name_cml[node_type - NODE_TYPE_CML_BASE];
 
     /* Invalid node IDs */
     return type_to_name[NODE_TYPE_INVALID];
@@ -363,7 +363,7 @@ unsigned int get_node_pos_x(void *node_b
 {
     struct node_header *node = node_base;
     return (get_node_id(node) >> (CMN700_NODE_ID_Y_POS + encoding_bits)) &
-        mask_bits;
+	mask_bits;
 }
 
 unsigned int get_node_pos_y(void *node_base)
@@ -385,9 +385,9 @@ void set_encoding_and_masking_bits(const
      * on the mesh size as per CMN700 specification.
      */
     if ((mesh_size_x > 8) || (mesh_size_y > 8)) {
-        encoding_bits = 4;
+	encoding_bits = 4;
     } else {
-        encoding_bits = ((mesh_size_x > 4) || (mesh_size_y > 4)) ? 3 : 2;
+	encoding_bits = ((mesh_size_x > 4) || (mesh_size_y > 4)) ? 3 : 2;
     }
 
     /* Extract node coordinates from the node identifier */
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cmn700/src/cmn700.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/cmn700/src/cmn700.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cmn700/src/cmn700.h
@@ -101,9 +101,9 @@ enum device_type {
 /* Common node header */
 struct node_header {
     FWK_R uint64_t NODE_INFO;
-          uint8_t  RESERVED0[0x80 - 0x8];
+	  uint8_t  RESERVED0[0x80 - 0x8];
     FWK_R uint64_t CHILD_INFO;
-          uint8_t  RESERVED1[0x100 - 0x88];
+	  uint8_t  RESERVED1[0x100 - 0x88];
     FWK_R uint64_t CHILD_POINTER[256];
 };
 
@@ -132,37 +132,37 @@ enum sam_scg_index {
  */
 struct cmn700_rnsam_reg {
     FWK_R   uint64_t  NODE_INFO;
-            uint8_t   RESERVED0[0x80 - 0x8];
+	    uint8_t   RESERVED0[0x80 - 0x8];
     FWK_R   uint64_t  CHILD_INFO;
-            uint8_t   RESERVED1[0x900 - 0x88];
+	    uint8_t   RESERVED1[0x900 - 0x88];
     FWK_R   uint64_t  UNIT_INFO[2];
-            uint8_t   RESERVED2[0xC00 - 0x910];
+	    uint8_t   RESERVED2[0xC00 - 0x910];
     FWK_RW  uint64_t  NON_HASH_MEM_REGION[NON_HASH_MEM_REG_COUNT];
     FWK_RW  uint64_t  NON_HASH_MEM_REGION_CFG2[NON_HASH_MEM_REG_COUNT];
     FWK_RW  uint64_t  NON_HASH_TGT_NODEID[16];
     FWK_RW  uint64_t  SYS_CACHE_GRP_REGION[4];
     FWK_RW  uint64_t  HASHED_TGT_GRP_CFG1_REGION[4];
-            uint8_t   RESERVED5[0xEA0 - 0xE40];
+	    uint8_t   RESERVED5[0xEA0 - 0xE40];
     FWK_RW  uint64_t  SYS_CACHE_GRP_HN_COUNT;
-            uint8_t   RESERVED6[0xEB0 - 0xEA8];
+	    uint8_t   RESERVED6[0xEB0 - 0xEA8];
     FWK_RW  uint64_t  SYS_CACHE_GRP_SN_ATTR[2];
-            uint8_t   RESERVED7[0xF00 - 0xEC0];
+	    uint8_t   RESERVED7[0xF00 - 0xEC0];
     FWK_RW  uint64_t  SYS_CACHE_GRP_HN_NODEID[16];
-            uint8_t   RESERVED8[0x1000 - 0xF80];
+	    uint8_t   RESERVED8[0x1000 - 0xF80];
     FWK_RW  uint64_t  SYS_CACHE_GRP_SN_NODEID[32];
     FWK_RW  uint64_t  STATUS;
-            uint64_t  GIC_MEM_REGION;
-            uint8_t   RESERVED9[0x1120 - 0x1110];
+	    uint64_t  GIC_MEM_REGION;
+	    uint8_t   RESERVED9[0x1120 - 0x1110];
     FWK_RW  uint64_t  SYS_CACHE_GRP_CAL_MODE;
     FWK_RW  uint64_t  HASHED_TARGET_GRP_CAL_MODE[3];
     FWK_RW  uint64_t  SYS_CACHE_GRP_SN_SAM_CFG[4];
-            uint8_t   RESERVED10[0x20C0 - 0x1160];
+	    uint8_t   RESERVED10[0x20C0 - 0x1160];
     FWK_RW  uint64_t  NON_HASH_MEM_REGION_GRP2[NON_HASH_MEM_REG_GRP2_COUNT];
-            uint8_t   RESERVED11[0x24C0 - 0x2200];
+	    uint8_t   RESERVED11[0x24C0 - 0x2200];
     FWK_RW  uint64_t  NON_HASH_MEM_REGION_CFG2_GRP2[NON_HASH_MEM_REG_GRP2_COUNT];
-            uint8_t   RESERVED12[0x3100 - 0x2600];
+	    uint8_t   RESERVED12[0x3100 - 0x2600];
     FWK_RW  uint64_t  HASHED_TGT_GRP_CFG2_REGION[32];
-            uint8_t   RESERVED13[0x3400 - 0x3200];
+	    uint8_t   RESERVED13[0x3400 - 0x3200];
     FWK_RW  uint64_t  HASHED_TARGET_GRP_HASH_CNTL[32];
 };
 
@@ -174,22 +174,22 @@ struct cmn700_rnsam_reg {
  */
 struct cmn700_hnf_reg {
     FWK_R   uint64_t  NODE_INFO;
-            uint8_t   RESERVED0[0x80 - 0x8];
+	    uint8_t   RESERVED0[0x80 - 0x8];
     FWK_R   uint64_t  CHILD_INFO;
-            uint8_t   RESERVED1[0x900 - 0x88];
+	    uint8_t   RESERVED1[0x900 - 0x88];
     FWK_R   uint64_t  UNIT_INFO[2];
-            uint8_t   RESERVED2[0xD00 - 0x910];
+	    uint8_t   RESERVED2[0xD00 - 0x910];
     FWK_RW  uint64_t  SAM_CONTROL;
     FWK_RW  uint64_t  SAM_MEMREGION[2];
-            uint8_t   RESERVED3[0xD28 - 0xD18];
+	    uint8_t   RESERVED3[0xD28 - 0xD18];
     FWK_RW  uint64_t  SAM_CONTROL_2;
-            uint8_t   RESERVED4[0xD38 - 0xD30];
+	    uint8_t   RESERVED4[0xD38 - 0xD30];
     FWK_RW  uint64_t  SAM_MEMREGION_END_ADDR[2];
-            uint8_t   RESERVED5[0x1C00 - 0xD48];
+	    uint8_t   RESERVED5[0x1C00 - 0xD48];
     FWK_RW  uint64_t  PPU_PWPR;
-            uint8_t   RESERVED6[0x3C00 - 0x1C08];
+	    uint8_t   RESERVED6[0x3C00 - 0x1C08];
     FWK_RW  uint64_t
-        HNF_RN_CLUSTER_PHYSID[HNF_RN_CLUSTER_MAX][HNF_RN_PHYIDS_REG_MAX];
+	HNF_RN_CLUSTER_PHYSID[HNF_RN_CLUSTER_MAX][HNF_RN_PHYIDS_REG_MAX];
 };
 
 /*
@@ -205,31 +205,31 @@ struct ccg_link_regs {
  */
 struct cmn700_ccg_ra_reg {
     FWK_R  uint64_t CCG_RA_NODE_INFO;
-           uint8_t  RESERVED0[0x80 - 0x8];
+	   uint8_t  RESERVED0[0x80 - 0x8];
     FWK_R  uint64_t CCG_RA_CHILD_INFO;
-           uint8_t  RESERVED1[0x900 - 0x88];
+	   uint8_t  RESERVED1[0x900 - 0x88];
     FWK_R  uint64_t CCG_RA_UNIT_INFO;
-           uint8_t  RESERVED2[0x980 - 0x908];
+	   uint8_t  RESERVED2[0x980 - 0x908];
     FWK_RW uint64_t CCG_RA_SEC_REG_GRP_OVERRIDE;
-           uint8_t  RESERVED3[0xA00 - 0x988];
+	   uint8_t  RESERVED3[0xA00 - 0x988];
     FWK_RW uint64_t CCG_RA_CFG_CTRL;
     FWK_RW uint64_t CCG_RA_AUX_CTRL;
-           uint8_t  RESERVED4[0xC00 - 0xA10];
+	   uint8_t  RESERVED4[0xC00 - 0xA10];
     FWK_RW uint64_t CCG_RA_SAM_ADDR_REGION_REG[8];
-           uint8_t  RESERVED5[0xD00 - 0xC40];
+	   uint8_t  RESERVED5[0xD00 - 0xC40];
     FWK_RW uint64_t CCG_RA_AGENTID_TO_LINKID_VAL;
-           uint8_t  RESERVED6[0xD10 - 0xD08];
+	   uint8_t  RESERVED6[0xD10 - 0xD08];
     FWK_RW uint64_t CCG_RA_AGENTID_TO_LINKID_REG[8];
-           uint8_t  RESERVED7[0xE00 - 0xD50];
+	   uint8_t  RESERVED7[0xE00 - 0xD50];
     FWK_RW uint64_t CCG_RA_RNI_LDID_TO_EXP_RAID_REG[10];
-           uint8_t  RESERVED8[0xF00 - 0xE50];
+	   uint8_t  RESERVED8[0xF00 - 0xE50];
     FWK_RW uint64_t CCG_RA_RND_LDID_TO_EXP_RAID_REG[10];
-           uint8_t  RESERVED9[0x1000 - 0xF50];
+	   uint8_t  RESERVED9[0x1000 - 0xF50];
     FWK_RW uint64_t CCG_RA_RNF_LDID_TO_EXP_RAID_REG[128];
     FWK_RW uint64_t CCG_RA_RNF_LDID_TO_NODEID_REG[128];
     FWK_RW uint64_t CCG_RA_RNF_LDID_TO_OVRD_LDID_REG[128];
-           struct ccg_link_regs LINK_REGS[3];
-           uint8_t  RESERVED10[0x2000 - 0x1C30];
+	   struct ccg_link_regs LINK_REGS[3];
+	   uint8_t  RESERVED10[0x2000 - 0x1C30];
     FWK_RW uint64_t CCG_RA_PMU_EVENT_SEL;
 };
 
@@ -239,22 +239,22 @@ struct cmn700_ccg_ra_reg {
 struct cmn700_ccg_ha_reg {
     FWK_R  uint64_t CCG_HA_NODE_INFO;
     FWK_RW uint64_t CCG_HA_ID;
-           uint8_t  RESERVED0[0x80 - 0x10];
+	   uint8_t  RESERVED0[0x80 - 0x10];
     FWK_R  uint64_t CCG_HA_CHILD_INFO;
-           uint8_t  RESERVED1[0x900 - 0x88];
+	   uint8_t  RESERVED1[0x900 - 0x88];
     FWK_R  uint64_t CCG_HA_UNIT_INFO[2];
-           uint8_t  RESERVED2[0x980 - 0x910];
+	   uint8_t  RESERVED2[0x980 - 0x910];
     FWK_RW uint64_t CCG_HA_SEC_REG_GRP_OVERRIDE;
-           uint8_t  RESERVED3[0xA00 - 0x988];
+	   uint8_t  RESERVED3[0xA00 - 0x988];
     FWK_RW uint64_t CCG_HA_CFG_CTRL;
     FWK_RW uint64_t CCG_HA_AUX_CTRL;
-           uint8_t  RESERVED4[0xC00 - 0xA10];
+	   uint8_t  RESERVED4[0xC00 - 0xA10];
     FWK_RW uint64_t CCG_HA_RNF_EXP_RAID_TO_LDID_REG[256];
-           uint8_t  RESERVED5[0x1C00 - 0x1400];
-           struct ccg_link_regs LINK_REGS[3];
-           uint8_t  RESERVED6[0x1F00 - 0x1C30];
+	   uint8_t  RESERVED5[0x1C00 - 0x1400];
+	   struct ccg_link_regs LINK_REGS[3];
+	   uint8_t  RESERVED6[0x1F00 - 0x1C30];
     FWK_RW uint64_t CCG_HA_AGENTID_TO_LINKID_REG[8];
-           uint8_t  RESERVED7[0x1FF8 - 0x1F40];
+	   uint8_t  RESERVED7[0x1FF8 - 0x1F40];
     FWK_RW uint64_t CCG_HA_AGENTID_TO_LINKID_VAL;
     FWK_RW uint64_t CCG_HA_PMU_EVENT_SEL;
 };
@@ -264,19 +264,19 @@ struct cmn700_ccg_ha_reg {
  */
 struct cmn700_ccla_reg {
     FWK_R  uint64_t CCLA_NODE_INFO;
-           uint8_t  RESERVED0[0x80 - 0x8];
+	   uint8_t  RESERVED0[0x80 - 0x8];
     FWK_R  uint64_t CCLA_CHILD_INFO;
-           uint8_t  RESERVED1[0x910 - 0x88];
+	   uint8_t  RESERVED1[0x910 - 0x88];
     FWK_R  uint64_t CCLA_UNIT_INFO;
-           uint8_t  RESERVED2[0x988 - 0x918];
+	   uint8_t  RESERVED2[0x988 - 0x918];
     FWK_RW uint64_t CCLA_SEC_REG_GRP_OVERRIDE;
-           uint8_t  RESERVED3[0xB00 - 0x990];
+	   uint8_t  RESERVED3[0xB00 - 0x990];
     FWK_RW uint64_t CCLA_CFG_CTL;
     FWK_RW uint64_t CCLA_AUX_CTRL;
-           uint8_t  RESERVED4[0xC00 - 0xB10];
+	   uint8_t  RESERVED4[0xC00 - 0xB10];
     FWK_R  uint64_t CCLA_CCIX_PROP_CAPABILITIES;
     FWK_RW uint64_t CCLA_CXS_ATTR_CAPABILITIES;
-           uint8_t  RESERVED5[0xD00 - 0xC10];
+	   uint8_t  RESERVED5[0xD00 - 0xC10];
     FWK_RW uint64_t CCLA_PERMSG_PYLD_0_63;
     FWK_RW uint64_t CCLA_PERMSG_PYLD_64_127;
     FWK_RW uint64_t CCLA_PERMSG_PYLD_128_191;
@@ -285,7 +285,7 @@ struct cmn700_ccla_reg {
     FWK_RW uint64_t CCLA_ERR_AGENT_ID;
     FWK_RW uint64_t CCLA_AGENTID_TO_PORTID_REG[8];
     FWK_RW uint64_t CCLA_AGENTID_TO_PORTID_VAL;
-           uint8_t  RESERVED6[0xE00 - 0xD88];
+	   uint8_t  RESERVED6[0xE00 - 0xD88];
     FWK_RW uint64_t CCLA_PORTFWD_CTL;
     FWK_R  uint64_t CCLA_PORTFWD_STATUS;
     FWK_RW uint64_t CCLA_CXL_LINK_RX_CREDIT_CTL;
@@ -296,15 +296,15 @@ struct cmn700_ccla_reg {
     FWK_R  uint64_t CCLA_ULL_STATUS;
     FWK_RW uint64_t CCLA_CXL_LL_ERRINJECT_CTL;
     FWK_R  uint64_t CCLA_CXL_LL_ERRINJECT_STAT;
-           uint8_t  RESERVED7[0x2008 - 0xE40];
+	   uint8_t  RESERVED7[0x2008 - 0xE40];
     FWK_RW uint64_t CCLA_PMU_EVENT_SEL;
-           uint8_t  RESERVED8[0x3000 - 0x2010];
+	   uint8_t  RESERVED8[0x3000 - 0x2010];
     FWK_R  uint64_t CCLA_ERRFR;
     FWK_RW uint64_t CCLA_ERRCTLR;
     FWK_W  uint64_t CCLA_ERRSTATUS;
     FWK_RW uint64_t CCLA_ERRADDR;
     FWK_RW uint64_t CCLA_ERRMISC;
-           uint8_t  RESERVED9[0x3100 - 0x3028];
+	   uint8_t  RESERVED9[0x3100 - 0x3028];
     FWK_R  uint64_t CCLA_ERRFR_NS;
     FWK_RW uint64_t CCLA_ERRCTLR_NS;
     FWK_W  uint64_t CCLA_ERRSTATUS_NS;
@@ -319,7 +319,7 @@ struct cmn700_cfgm_reg {
     FWK_R   uint64_t  NODE_INFO;
     FWK_RW  uint64_t  PERIPH_ID[4];
     FWK_RW  uint64_t  COMPONENT_ID[2];
-            uint8_t   RESERVED0[0x80 - 0x38];
+	    uint8_t   RESERVED0[0x80 - 0x38];
     FWK_R   uint64_t  CHILD_INFO;
 };
 
@@ -331,9 +331,9 @@ struct cmn700_mxp_reg {
     FWK_R  uint64_t  PORT_CONNECT_INFO[6];
     FWK_R  uint64_t  PORT_CONNECT_INFO_EAST;
     FWK_R  uint64_t  PORT_CONNECT_INFO_NORTH;
-           uint8_t   RESERVED0[0x80 - 0x28];
+	   uint8_t   RESERVED0[0x80 - 0x28];
     FWK_R  uint64_t  CHILD_INFO;
-           uint8_t   RESERVED1[0x100 - 0x88];
+	   uint8_t   RESERVED1[0x100 - 0x88];
     FWK_R  uint64_t  CHILD_POINTER[32];
 };
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cmn700/src/cmn700_ccg.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/cmn700/src/cmn700_ccg.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cmn700/src/cmn700_ccg.c
@@ -41,11 +41,11 @@ static unsigned int get_ldid(
     unsigned int ldid;
 
     ldid = (program_for_port_aggregation) ? config->port_aggregate_ldid :
-                                            config->ldid;
+					    config->ldid;
 
     if (ldid >= ctx->ccg_node_count) {
-        FWK_LOG_ERR(MOD_NAME "Unexpected ldid: %d", ldid);
-        fwk_trap();
+	FWK_LOG_ERR(MOD_NAME "Unexpected ldid: %d", ldid);
+	fwk_trap();
     }
 
     return ldid;
@@ -75,63 +75,63 @@ static bool ccg_link_wait_condition(void
 
     switch (wait_data->cond) {
     case CCG_LINK_CTRL_EN_BIT_SET:
-        val1 = ccg_ra_reg->LINK_REGS[linkid].CCG_CCPRTCL_LINK_CTRL;
-        val2 = ccg_ha_reg->LINK_REGS[linkid].CCG_CCPRTCL_LINK_CTRL;
-        ret = ((val1 & CCG_LINK_CTRL_EN_MASK) > 0) &&
-            ((val2 & CCG_LINK_CTRL_EN_MASK) > 0);
-        break;
+	val1 = ccg_ra_reg->LINK_REGS[linkid].CCG_CCPRTCL_LINK_CTRL;
+	val2 = ccg_ha_reg->LINK_REGS[linkid].CCG_CCPRTCL_LINK_CTRL;
+	ret = ((val1 & CCG_LINK_CTRL_EN_MASK) > 0) &&
+	    ((val2 & CCG_LINK_CTRL_EN_MASK) > 0);
+	break;
 
     case CCG_LINK_CTRL_UP_BIT_CLR:
-        val1 = ccg_ra_reg->LINK_REGS[linkid].CCG_CCPRTCL_LINK_CTRL;
-        val2 = ccg_ha_reg->LINK_REGS[linkid].CCG_CCPRTCL_LINK_CTRL;
-        ret =
-            (((val1 & CCG_LINK_CTRL_UP_MASK) == 0) &&
-             ((val2 & CCG_LINK_CTRL_UP_MASK) == 0));
-        break;
+	val1 = ccg_ra_reg->LINK_REGS[linkid].CCG_CCPRTCL_LINK_CTRL;
+	val2 = ccg_ha_reg->LINK_REGS[linkid].CCG_CCPRTCL_LINK_CTRL;
+	ret =
+	    (((val1 & CCG_LINK_CTRL_UP_MASK) == 0) &&
+	     ((val2 & CCG_LINK_CTRL_UP_MASK) == 0));
+	break;
 
     case CCG_LINK_STATUS_DWN_BIT_SET:
-        val1 = ccg_ra_reg->LINK_REGS[linkid].CCG_CCPRTCL_LINK_STATUS;
-        val2 = ccg_ha_reg->LINK_REGS[linkid].CCG_CCPRTCL_LINK_STATUS;
-        ret = ((val1 & CCG_LINK_STATUS_DOWN_MASK) > 0) &&
-            ((val2 & CCG_LINK_STATUS_DOWN_MASK) > 0);
-        break;
+	val1 = ccg_ra_reg->LINK_REGS[linkid].CCG_CCPRTCL_LINK_STATUS;
+	val2 = ccg_ha_reg->LINK_REGS[linkid].CCG_CCPRTCL_LINK_STATUS;
+	ret = ((val1 & CCG_LINK_STATUS_DOWN_MASK) > 0) &&
+	    ((val2 & CCG_LINK_STATUS_DOWN_MASK) > 0);
+	break;
 
     case CCG_LINK_STATUS_DWN_BIT_CLR:
-        val1 = ccg_ra_reg->LINK_REGS[linkid].CCG_CCPRTCL_LINK_STATUS;
-        val2 = ccg_ha_reg->LINK_REGS[linkid].CCG_CCPRTCL_LINK_STATUS;
-        ret =
-            (((val1 & CCG_LINK_STATUS_DOWN_MASK) == 0) &&
-             ((val2 & CCG_LINK_STATUS_DOWN_MASK) == 0));
-        break;
+	val1 = ccg_ra_reg->LINK_REGS[linkid].CCG_CCPRTCL_LINK_STATUS;
+	val2 = ccg_ha_reg->LINK_REGS[linkid].CCG_CCPRTCL_LINK_STATUS;
+	ret =
+	    (((val1 & CCG_LINK_STATUS_DOWN_MASK) == 0) &&
+	     ((val2 & CCG_LINK_STATUS_DOWN_MASK) == 0));
+	break;
 
     case CCG_LINK_STATUS_ACK_BIT_SET:
-        val1 = ccg_ra_reg->LINK_REGS[linkid].CCG_CCPRTCL_LINK_STATUS;
-        val2 = ccg_ha_reg->LINK_REGS[linkid].CCG_CCPRTCL_LINK_STATUS;
-        ret = ((val1 & CCG_LINK_STATUS_ACK_MASK) > 0) &&
-            ((val2 & CCG_LINK_STATUS_ACK_MASK) > 0);
-        break;
+	val1 = ccg_ra_reg->LINK_REGS[linkid].CCG_CCPRTCL_LINK_STATUS;
+	val2 = ccg_ha_reg->LINK_REGS[linkid].CCG_CCPRTCL_LINK_STATUS;
+	ret = ((val1 & CCG_LINK_STATUS_ACK_MASK) > 0) &&
+	    ((val2 & CCG_LINK_STATUS_ACK_MASK) > 0);
+	break;
 
     case CCG_LINK_STATUS_ACK_BIT_CLR:
-        val1 = ccg_ra_reg->LINK_REGS[linkid].CCG_CCPRTCL_LINK_STATUS;
-        val2 = ccg_ha_reg->LINK_REGS[linkid].CCG_CCPRTCL_LINK_STATUS;
-        ret =
-            (((val1 & CCG_LINK_STATUS_ACK_MASK) == 0) &&
-             ((val2 & CCG_LINK_STATUS_ACK_MASK) == 0));
-        break;
+	val1 = ccg_ra_reg->LINK_REGS[linkid].CCG_CCPRTCL_LINK_STATUS;
+	val2 = ccg_ha_reg->LINK_REGS[linkid].CCG_CCPRTCL_LINK_STATUS;
+	ret =
+	    (((val1 & CCG_LINK_STATUS_ACK_MASK) == 0) &&
+	     ((val2 & CCG_LINK_STATUS_ACK_MASK) == 0));
+	break;
 
     case CCG_LINK_STATUS_HA_DVMDOMAIN_ACK_BIT_SET:
-        val1 = ccg_ha_reg->LINK_REGS[linkid].CCG_CCPRTCL_LINK_STATUS;
-        ret = (((val1 & CCG_LINK_STATUS_DVMDOMAIN_ACK_MASK)) != 0);
-        break;
+	val1 = ccg_ha_reg->LINK_REGS[linkid].CCG_CCPRTCL_LINK_STATUS;
+	ret = (((val1 & CCG_LINK_STATUS_DVMDOMAIN_ACK_MASK)) != 0);
+	break;
 
     case CCG_LINK_STATUS_RA_DVMDOMAIN_ACK_BIT_SET:
-        val1 = ccg_ra_reg->LINK_REGS[linkid].CCG_CCPRTCL_LINK_STATUS;
-        ret = (((val1 & CCG_LINK_STATUS_DVMDOMAIN_ACK_MASK)) != 0);
-        break;
+	val1 = ccg_ra_reg->LINK_REGS[linkid].CCG_CCPRTCL_LINK_STATUS;
+	ret = (((val1 & CCG_LINK_STATUS_DVMDOMAIN_ACK_MASK)) != 0);
+	break;
 
     default:
-        fwk_unexpected();
-        ret = false;
+	fwk_unexpected();
+	ret = false;
     }
 
     return ret;
@@ -156,12 +156,12 @@ static void program_ccg_ra_rnf_ldid_to_e
 
     /* Adding raid_value into LDID-to-RAID register */
     ccg_ra_reg->CCG_RA_RNF_LDID_TO_EXP_RAID_REG[reg_offset] |=
-        ((uint64_t)raid << (ldid_value_offset * NUM_BITS_RESERVED_FOR_RAID));
+	((uint64_t)raid << (ldid_value_offset * NUM_BITS_RESERVED_FOR_RAID));
 
     /* Set corresponding valid bit */
     ccg_ra_reg->CCG_RA_RNF_LDID_TO_EXP_RAID_REG[reg_offset] |=
-        (LDID_TO_EXP_RAID_VALID_MASK)
-        << (ldid_value_offset * NUM_BITS_RESERVED_FOR_RAID);
+	(LDID_TO_EXP_RAID_VALID_MASK)
+	<< (ldid_value_offset * NUM_BITS_RESERVED_FOR_RAID);
 
     cmn700_ccg_ctx.raid_value++;
 }
@@ -185,12 +185,12 @@ static void program_ccg_ra_rni_ldid_to_e
 
     /* Adding raid_value into LDID-to-RAID register */
     ccg_ra_reg->CCG_RA_RNI_LDID_TO_EXP_RAID_REG[reg_offset] |=
-        ((uint64_t)raid << (ldid_value_offset * NUM_BITS_RESERVED_FOR_RAID));
+	((uint64_t)raid << (ldid_value_offset * NUM_BITS_RESERVED_FOR_RAID));
 
     /* Set corresponding valid bit */
     ccg_ra_reg->CCG_RA_RNI_LDID_TO_EXP_RAID_REG[reg_offset] |=
-        (LDID_TO_EXP_RAID_VALID_MASK)
-        << (ldid_value_offset * NUM_BITS_RESERVED_FOR_RAID);
+	(LDID_TO_EXP_RAID_VALID_MASK)
+	<< (ldid_value_offset * NUM_BITS_RESERVED_FOR_RAID);
 
     cmn700_ccg_ctx.raid_value++;
 }
@@ -214,12 +214,12 @@ static void program_ccg_ra_rnd_ldid_to_e
 
     /* Adding raid_value into LDID-to-RAID register */
     ccg_ra_reg->CCG_RA_RND_LDID_TO_EXP_RAID_REG[reg_offset] |=
-        ((uint64_t)raid << (ldid_value_offset * NUM_BITS_RESERVED_FOR_RAID));
+	((uint64_t)raid << (ldid_value_offset * NUM_BITS_RESERVED_FOR_RAID));
 
     /* Set corresponding valid bit */
     ccg_ra_reg->CCG_RA_RND_LDID_TO_EXP_RAID_REG[reg_offset] |=
-        (LDID_TO_EXP_RAID_VALID_MASK)
-        << (ldid_value_offset * NUM_BITS_RESERVED_FOR_RAID);
+	(LDID_TO_EXP_RAID_VALID_MASK)
+	<< (ldid_value_offset * NUM_BITS_RESERVED_FOR_RAID);
 
     cmn700_ccg_ctx.raid_value++;
 }
@@ -243,46 +243,46 @@ static void program_agentid_to_linkid_re
     ccg_ha_reg = ctx->ccg_ha_reg_table[ccg_ldid].ccg_ha_reg;
 
     for (linkid = 0; linkid < CMN700_MAX_CCG_PROTOCOL_LINKS; linkid++) {
-        remote_agentid_start =
-            config->remote_agentid_to_linkid_map[linkid].remote_agentid_start;
-        remote_agentid_end =
-            config->remote_agentid_to_linkid_map[linkid].remote_agentid_end;
-
-        /*
-         * Skip configuring link if both start and end are 0, indicating
-         * there's no link
-         */
-        if ((remote_agentid_start == 0) && (remote_agentid_end == 0)) {
-            continue;
-        }
-
-        FWK_LOG_INFO(
-            MOD_NAME "  Remote [AgentID %d - AgentID %d] Link %d",
-            remote_agentid_start,
-            remote_agentid_end,
-            linkid);
-
-        for (agentid = remote_agentid_start; agentid <= remote_agentid_end;
-             agentid++) {
-            /* Each register is 64 bits and holds 8 AgentID/LinkID mappings */
-            reg_index = agentid / 8;
-            agentid_bit_offset = agentid % 8;
-
-            /* Writing AgentID-to-LinkID mappings */
-            ccg_ra_reg->CCG_RA_AGENTID_TO_LINKID_REG[reg_index] |=
-                ((uint64_t)linkid
-                 << (agentid_bit_offset * NUM_BITS_RESERVED_FOR_LINKID));
-
-            ccg_ha_reg->CCG_HA_AGENTID_TO_LINKID_REG[reg_index] |=
-                ((uint64_t)linkid
-                 << (agentid_bit_offset * NUM_BITS_RESERVED_FOR_LINKID));
-
-            /* Setting corresponding valid bits */
-            ccg_ra_reg->CCG_RA_AGENTID_TO_LINKID_VAL |=
-                (UINT64_C(0x1) << agentid);
-            ccg_ha_reg->CCG_HA_AGENTID_TO_LINKID_VAL |=
-                (UINT64_C(0x1) << agentid);
-        }
+	remote_agentid_start =
+	    config->remote_agentid_to_linkid_map[linkid].remote_agentid_start;
+	remote_agentid_end =
+	    config->remote_agentid_to_linkid_map[linkid].remote_agentid_end;
+
+	/*
+	 * Skip configuring link if both start and end are 0, indicating
+	 * there's no link
+	 */
+	if ((remote_agentid_start == 0) && (remote_agentid_end == 0)) {
+	    continue;
+	}
+
+	FWK_LOG_INFO(
+	    MOD_NAME "  Remote [AgentID %d - AgentID %d] Link %d",
+	    remote_agentid_start,
+	    remote_agentid_end,
+	    linkid);
+
+	for (agentid = remote_agentid_start; agentid <= remote_agentid_end;
+	     agentid++) {
+	    /* Each register is 64 bits and holds 8 AgentID/LinkID mappings */
+	    reg_index = agentid / 8;
+	    agentid_bit_offset = agentid % 8;
+
+	    /* Writing AgentID-to-LinkID mappings */
+	    ccg_ra_reg->CCG_RA_AGENTID_TO_LINKID_REG[reg_index] |=
+		((uint64_t)linkid
+		 << (agentid_bit_offset * NUM_BITS_RESERVED_FOR_LINKID));
+
+	    ccg_ha_reg->CCG_HA_AGENTID_TO_LINKID_REG[reg_index] |=
+		((uint64_t)linkid
+		 << (agentid_bit_offset * NUM_BITS_RESERVED_FOR_LINKID));
+
+	    /* Setting corresponding valid bits */
+	    ccg_ra_reg->CCG_RA_AGENTID_TO_LINKID_VAL |=
+		(UINT64_C(0x1) << agentid);
+	    ccg_ha_reg->CCG_HA_AGENTID_TO_LINKID_VAL |=
+		(UINT64_C(0x1) << agentid);
+	}
     }
 }
 
@@ -295,15 +295,15 @@ static void program_ccg_ha_id(struct cmn
     ccg_ldid = get_ldid(ctx, cmn700_ccg_ctx.is_prog_for_port_agg);
     ccg_ha_reg = ctx->ccg_ha_reg_table[ccg_ldid].ccg_ha_reg;
     ccg_haid = cmn700_ccg_ctx.is_prog_for_port_agg ?
-        config->port_aggregate_haid :
-        config->haid;
+	config->port_aggregate_haid :
+	config->haid;
     ccg_ha_reg->CCG_HA_ID = ccg_haid;
 
     FWK_LOG_INFO(
-        MOD_NAME "HAID for CCG %d with nodeid %d: HAID %d",
-        ccg_ldid,
-        get_node_id(ccg_ha_reg),
-        ccg_haid);
+	MOD_NAME "HAID for CCG %d with nodeid %d: HAID %d",
+	ccg_ldid,
+	get_node_id(ccg_ha_reg),
+	ccg_haid);
 }
 
 static void program_ccg_ha_raid_to_ldid_lut(
@@ -325,10 +325,10 @@ static void program_ccg_ha_raid_to_ldid_
 
     /* Write RAID-to-LDID mapping (with RNF bit set) */
     ccg_ha_reg->CCG_HA_RNF_EXP_RAID_TO_LDID_REG[reg_index] |=
-        ((uint64_t)(
-             ldid_value | CCG_HA_RAID_TO_LDID_RNF_MASK |
-             (EXP_RAID_TO_LDID_VALID_MASK))
-         << (raid_bit_offset * NUM_BITS_RESERVED_FOR_LDID));
+	((uint64_t)(
+	     ldid_value | CCG_HA_RAID_TO_LDID_RNF_MASK |
+	     (EXP_RAID_TO_LDID_VALID_MASK))
+	 << (raid_bit_offset * NUM_BITS_RESERVED_FOR_LDID));
 }
 
 static void program_hnf_ldid_to_chi_node_id_reg(
@@ -352,48 +352,48 @@ static void program_hnf_ldid_to_chi_node
     nodeid_ra = ctx->ccg_ha_reg_table[config->ldid].node_id;
 
     if (cmn700_ccg_ctx.is_prog_for_port_agg) {
-        /*
-         * if programming for port aggregation support, reset the remote rnf
-         * ldid value to the previous iteration in order to program.
-         */
-        ctx->remote_rnf_ldid_value -= ctx->rnf_count;
+	/*
+	 * if programming for port aggregation support, reset the remote rnf
+	 * ldid value to the previous iteration in order to program.
+	 */
+	ctx->remote_rnf_ldid_value -= ctx->rnf_count;
     }
 
     for (linkid = 0; linkid < CMN700_MAX_CCG_PROTOCOL_LINKS; linkid++) {
-        remote_agentid_start =
-            config->remote_agentid_to_linkid_map[linkid].remote_agentid_start;
-        remote_agentid_end =
-            config->remote_agentid_to_linkid_map[linkid].remote_agentid_end;
-
-        /*
-         * Skip configuring link if both start and end are 0, indicating
-         * there's no link
-         */
-        if ((remote_agentid_start == 0) && (remote_agentid_end == 0)) {
-            continue;
-        }
-
-        FWK_LOG_INFO(
-            MOD_NAME "  Remote [AgentID %d - AgentID %d] Node Id: %" PRIu32,
-            remote_agentid_start,
-            remote_agentid_end,
-            nodeid_ra);
-
-        for (agentid = remote_agentid_start; agentid <= remote_agentid_end;
-             agentid++) {
-            reg_index = ctx->remote_rnf_ldid_value;
-
-            for (i = 0; i < ctx->hnf_count; i++) {
-                hnf_reg = (struct cmn700_hnf_reg *)ctx->hnf_node[i];
-                /* Write CCHA NodeID, local/remote and valid bit */
-                hnf_reg->HNF_RN_CLUSTER_PHYSID[reg_index][0] |= ((uint64_t)(
-                    nodeid_ra |
-                    (REMOTE_CCG_NODE << HNF_RN_PHYS_RN_LOCAL_REMOTE_SHIFT_VAL) |
-                    (UINT64_C(0x1) << HNF_RN_PHYS_RN_ID_VALID_SHIFT_VAL)));
-            }
+	remote_agentid_start =
+	    config->remote_agentid_to_linkid_map[linkid].remote_agentid_start;
+	remote_agentid_end =
+	    config->remote_agentid_to_linkid_map[linkid].remote_agentid_end;
+
+	/*
+	 * Skip configuring link if both start and end are 0, indicating
+	 * there's no link
+	 */
+	if ((remote_agentid_start == 0) && (remote_agentid_end == 0)) {
+	    continue;
+	}
+
+	FWK_LOG_INFO(
+	    MOD_NAME "  Remote [AgentID %d - AgentID %d] Node Id: %" PRIu32,
+	    remote_agentid_start,
+	    remote_agentid_end,
+	    nodeid_ra);
+
+	for (agentid = remote_agentid_start; agentid <= remote_agentid_end;
+	     agentid++) {
+	    reg_index = ctx->remote_rnf_ldid_value;
+
+	    for (i = 0; i < ctx->hnf_count; i++) {
+		hnf_reg = (struct cmn700_hnf_reg *)ctx->hnf_node[i];
+		/* Write CCHA NodeID, local/remote and valid bit */
+		hnf_reg->HNF_RN_CLUSTER_PHYSID[reg_index][0] |= ((uint64_t)(
+		    nodeid_ra |
+		    (REMOTE_CCG_NODE << HNF_RN_PHYS_RN_LOCAL_REMOTE_SHIFT_VAL) |
+		    (UINT64_C(0x1) << HNF_RN_PHYS_RN_ID_VALID_SHIFT_VAL)));
+	    }
 
-            ctx->remote_rnf_ldid_value++;
-        }
+	    ctx->remote_rnf_ldid_value++;
+	}
     }
 }
 
@@ -410,9 +410,9 @@ static void enable_smp_mode(
     ccg_ha_reg = ctx->ccg_ha_reg_table[ccg_ldid].ccg_ha_reg;
 
     ccg_ra_reg->LINK_REGS[0].CCG_CCPRTCL_LINK_CTRL |=
-        (1 << CCG_RA_CCPRTCL_LINK_CTRL_SMP_MODE_EN_SHIFT_VAL);
+	(1 << CCG_RA_CCPRTCL_LINK_CTRL_SMP_MODE_EN_SHIFT_VAL);
     ccg_ha_reg->LINK_REGS[0].CCG_CCPRTCL_LINK_CTRL |=
-        (1 << CCG_HA_CCPRTCL_LINK_CTRL_SMP_MODE_EN_SHIFT_VAL);
+	(1 << CCG_HA_CCPRTCL_LINK_CTRL_SMP_MODE_EN_SHIFT_VAL);
 
     FWK_LOG_INFO(MOD_NAME "SMP Mode Enabled");
 }
@@ -434,12 +434,12 @@ static bool ccla_ull_status_wait_conditi
     ccla_reg = ctx->ccla_reg_table[ccg_ldid].ccla_reg;
 
     ccla_ull_status_rx_in_run =
-        ((ccla_reg->CCLA_ULL_STATUS & CCLA_ULL_STATUS_SEND_RX_ULL_STATE_MASK) >
-         0);
+	((ccla_reg->CCLA_ULL_STATUS & CCLA_ULL_STATUS_SEND_RX_ULL_STATE_MASK) >
+	 0);
 
     ccla_ull_status_tx_in_run =
-        ((ccla_reg->CCLA_ULL_STATUS & CCLA_ULL_STATUS_SEND_TX_ULL_STATE_MASK) >
-         0);
+	((ccla_reg->CCLA_ULL_STATUS & CCLA_ULL_STATUS_SEND_TX_ULL_STATE_MASK) >
+	 0);
 
     return (ccla_ull_status_rx_in_run && ccla_ull_status_tx_in_run);
 }
@@ -465,24 +465,24 @@ static int enable_ull_to_ull_mode(
 
     /* Wait until link enable bits are set */
     status = ctx->timer_api->wait(
-        FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
-        CCLA_ULL_STATUS_TIMEOUT,
-        ccla_ull_status_wait_condition,
-        ctx);
+	FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
+	CCLA_ULL_STATUS_TIMEOUT,
+	ccla_ull_status_wait_condition,
+	ctx);
 
     if (status != FWK_SUCCESS) {
-        if ((ccla_reg->CCLA_ULL_STATUS &
-             CCLA_ULL_STATUS_SEND_RX_ULL_STATE_MASK) == 0) {
-            FWK_LOG_ERR(MOD_NAME "Rx ULL is in Stop state");
-        }
-
-        if ((ccla_reg->CCLA_ULL_STATUS &
-             CCLA_ULL_STATUS_SEND_TX_ULL_STATE_MASK) == 0) {
-            FWK_LOG_ERR(MOD_NAME "Tx ULL is in Stop state");
-        }
+	if ((ccla_reg->CCLA_ULL_STATUS &
+	     CCLA_ULL_STATUS_SEND_RX_ULL_STATE_MASK) == 0) {
+	    FWK_LOG_ERR(MOD_NAME "Rx ULL is in Stop state");
+	}
+
+	if ((ccla_reg->CCLA_ULL_STATUS &
+	     CCLA_ULL_STATUS_SEND_TX_ULL_STATE_MASK) == 0) {
+	    FWK_LOG_ERR(MOD_NAME "Tx ULL is in Stop state");
+	}
 
-        FWK_LOG_ERR(MOD_NAME "Enabling CCG ULL to ULL mode... Failed");
-        return status;
+	FWK_LOG_ERR(MOD_NAME "Enabling CCG ULL to ULL mode... Failed");
+	return status;
     }
 
     FWK_LOG_INFO(MOD_NAME "Enabling CCG ULL to ULL mode... Done");
@@ -504,46 +504,46 @@ static void program_ccg_ra_sam_addr_regi
     ccg_ra_reg = ctx->ccg_ra_reg_table[ccg_ldid].ccg_ra_reg;
 
     FWK_LOG_INFO(
-        MOD_NAME "Configuring RA SAM for CCRA NodeID %d",
-        get_node_id(ccg_ra_reg));
+	MOD_NAME "Configuring RA SAM for CCRA NodeID %d",
+	get_node_id(ccg_ra_reg));
 
     for (i = 0; i < CMN700_MAX_RA_SAM_ADDR_REGION; i++) {
-        /* If the size is zero, skip that entry */
-        if (config->ra_mmap_table[i].size == 0) {
-            continue;
-        }
-
-        /* Size must be a multiple of SAM_GRANULARITY */
-        fwk_assert((config->ra_mmap_table[i].size % (64 * FWK_KIB)) == 0);
-
-        /* Size also must be a power of two */
-        fwk_assert(
-            (config->ra_mmap_table[i].size &
-             (config->ra_mmap_table[i].size - 1)) == 0);
-
-        /* Region base should be naturally aligned to the region size */
-        fwk_assert(
-            config->ra_mmap_table[i].base % config->ra_mmap_table[i].size == 0);
-
-        if (cmn700_ccg_ctx.is_prog_for_port_agg) {
-            remote_haid = config->port_aggregate_remote_haid[i];
-        } else {
-            remote_haid = config->ra_mmap_table[i].remote_haid;
-        }
-
-        FWK_LOG_INFO(
-            MOD_NAME "  [0x%llx - 0x%llx] HAID %d",
-            config->ra_mmap_table[i].base,
-            config->ra_mmap_table[i].base + config->ra_mmap_table[i].size - 1,
-            remote_haid);
-
-        blocks = config->ra_mmap_table[i].size / (64 * FWK_KIB);
-        size = fwk_math_log2(blocks);
-
-        ccg_ra_reg->CCG_RA_SAM_ADDR_REGION_REG[i] = size |
-            (config->ra_mmap_table[i].base) |
-            ((uint64_t)remote_haid << SAM_ADDR_TARGET_HAID_SHIFT) |
-            (SAM_ADDR_REG_VALID_MASK);
+	/* If the size is zero, skip that entry */
+	if (config->ra_mmap_table[i].size == 0) {
+	    continue;
+	}
+
+	/* Size must be a multiple of SAM_GRANULARITY */
+	fwk_assert((config->ra_mmap_table[i].size % (64 * FWK_KIB)) == 0);
+
+	/* Size also must be a power of two */
+	fwk_assert(
+	    (config->ra_mmap_table[i].size &
+	     (config->ra_mmap_table[i].size - 1)) == 0);
+
+	/* Region base should be naturally aligned to the region size */
+	fwk_assert(
+	    config->ra_mmap_table[i].base % config->ra_mmap_table[i].size == 0);
+
+	if (cmn700_ccg_ctx.is_prog_for_port_agg) {
+	    remote_haid = config->port_aggregate_remote_haid[i];
+	} else {
+	    remote_haid = config->ra_mmap_table[i].remote_haid;
+	}
+
+	FWK_LOG_INFO(
+	    MOD_NAME "  [0x%llx - 0x%llx] HAID %d",
+	    config->ra_mmap_table[i].base,
+	    config->ra_mmap_table[i].base + config->ra_mmap_table[i].size - 1,
+	    remote_haid);
+
+	blocks = config->ra_mmap_table[i].size / (64 * FWK_KIB);
+	size = fwk_math_log2(blocks);
+
+	ccg_ra_reg->CCG_RA_SAM_ADDR_REGION_REG[i] = size |
+	    (config->ra_mmap_table[i].base) |
+	    ((uint64_t)remote_haid << SAM_ADDR_TARGET_HAID_SHIFT) |
+	    (SAM_ADDR_REG_VALID_MASK);
     }
 }
 
@@ -562,7 +562,7 @@ static int enable_and_start_ccg_link_up_
     ccg_ha_reg = ctx->ccg_ha_reg_table[ccg_ldid].ccg_ha_reg;
 
     if (linkid > 2) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     wait_data.ctx = ctx;
@@ -571,20 +571,20 @@ static int enable_and_start_ccg_link_up_
     FWK_LOG_INFO(MOD_NAME "Enabling CCG link %d...", linkid);
     /* Set link enable bit to enable the CCG link */
     ccg_ra_reg->LINK_REGS[linkid].CCG_CCPRTCL_LINK_CTRL |=
-        CCG_LINK_CTRL_EN_MASK;
+	CCG_LINK_CTRL_EN_MASK;
     ccg_ha_reg->LINK_REGS[linkid].CCG_CCPRTCL_LINK_CTRL |=
-        CCG_LINK_CTRL_EN_MASK;
+	CCG_LINK_CTRL_EN_MASK;
 
     /* Wait until link enable bits are set */
     wait_data.cond = CCG_LINK_CTRL_EN_BIT_SET;
     status = ctx->timer_api->wait(
-        FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
-        CCG_CCPRTCL_LINK_CTRL_TIMEOUT,
-        ccg_link_wait_condition,
-        &wait_data);
+	FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
+	CCG_CCPRTCL_LINK_CTRL_TIMEOUT,
+	ccg_link_wait_condition,
+	&wait_data);
     if (status != FWK_SUCCESS) {
-        FWK_LOG_ERR(MOD_NAME "Enabling CCG link %d... Failed", linkid);
-        return status;
+	FWK_LOG_ERR(MOD_NAME "Enabling CCG link %d... Failed", linkid);
+	return status;
     }
     FWK_LOG_INFO(MOD_NAME "Enabling CCG link %d... Done", linkid);
 
@@ -592,69 +592,69 @@ static int enable_and_start_ccg_link_up_
     /* Wait till link up bits are cleared in control register */
     wait_data.cond = CCG_LINK_CTRL_UP_BIT_CLR;
     status = ctx->timer_api->wait(
-        FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
-        CCG_CCPRTCL_LINK_CTRL_TIMEOUT,
-        ccg_link_wait_condition,
-        &wait_data);
+	FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
+	CCG_CCPRTCL_LINK_CTRL_TIMEOUT,
+	ccg_link_wait_condition,
+	&wait_data);
     if (status != FWK_SUCCESS) {
-        FWK_LOG_ERR(MOD_NAME "Verifying link down status... Failed");
-        return status;
+	FWK_LOG_ERR(MOD_NAME "Verifying link down status... Failed");
+	return status;
     }
 
     /* Wait till link down bits are set in status register */
     wait_data.cond = CCG_LINK_STATUS_DWN_BIT_SET;
     status = ctx->timer_api->wait(
-        FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
-        CCG_CCPRTCL_LINK_CTRL_TIMEOUT,
-        ccg_link_wait_condition,
-        &wait_data);
+	FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
+	CCG_CCPRTCL_LINK_CTRL_TIMEOUT,
+	ccg_link_wait_condition,
+	&wait_data);
     if (status != FWK_SUCCESS) {
-        FWK_LOG_ERR(MOD_NAME "Verifying link down status... Failed");
-        return status;
+	FWK_LOG_ERR(MOD_NAME "Verifying link down status... Failed");
+	return status;
     }
 
     /* Wait till link ACK bits are cleared in status register */
     wait_data.cond = CCG_LINK_STATUS_ACK_BIT_CLR;
     status = ctx->timer_api->wait(
-        FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
-        CCG_CCPRTCL_LINK_CTRL_TIMEOUT,
-        ccg_link_wait_condition,
-        &wait_data);
+	FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
+	CCG_CCPRTCL_LINK_CTRL_TIMEOUT,
+	ccg_link_wait_condition,
+	&wait_data);
     if (status != FWK_SUCCESS) {
-        FWK_LOG_ERR(MOD_NAME "Verifying link down status... Failed");
-        return status;
+	FWK_LOG_ERR(MOD_NAME "Verifying link down status... Failed");
+	return status;
     }
     FWK_LOG_INFO(MOD_NAME "Verifying link down status... Done");
 
     FWK_LOG_INFO(MOD_NAME "Bringing up link...");
     /* Bring up link using link request bit */
     ccg_ra_reg->LINK_REGS[linkid].CCG_CCPRTCL_LINK_CTRL |=
-        CCG_LINK_CTRL_REQ_MASK;
+	CCG_LINK_CTRL_REQ_MASK;
     ccg_ha_reg->LINK_REGS[linkid].CCG_CCPRTCL_LINK_CTRL |=
-        CCG_LINK_CTRL_REQ_MASK;
+	CCG_LINK_CTRL_REQ_MASK;
 
     /* Wait till link ACK bits are set in status register */
     wait_data.cond = CCG_LINK_STATUS_ACK_BIT_SET;
     status = ctx->timer_api->wait(
-        FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
-        CCG_CCPRTCL_LINK_CTRL_TIMEOUT,
-        ccg_link_wait_condition,
-        &wait_data);
+	FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
+	CCG_CCPRTCL_LINK_CTRL_TIMEOUT,
+	ccg_link_wait_condition,
+	&wait_data);
     if (status != FWK_SUCCESS) {
-        FWK_LOG_ERR(MOD_NAME "Bringing up link... Failed");
-        return status;
+	FWK_LOG_ERR(MOD_NAME "Bringing up link... Failed");
+	return status;
     }
 
     /* Wait till link down bits are cleared in status register */
     wait_data.cond = CCG_LINK_STATUS_DWN_BIT_CLR;
     status = ctx->timer_api->wait(
-        FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
-        CCG_CCPRTCL_LINK_CTRL_TIMEOUT,
-        ccg_link_wait_condition,
-        &wait_data);
+	FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
+	CCG_CCPRTCL_LINK_CTRL_TIMEOUT,
+	ccg_link_wait_condition,
+	&wait_data);
     if (status != FWK_SUCCESS) {
-        FWK_LOG_ERR(MOD_NAME "Bringing up link... Failed");
-        return status;
+	FWK_LOG_ERR(MOD_NAME "Bringing up link... Failed");
+	return status;
     }
     FWK_LOG_INFO(MOD_NAME "Bringing up link... Done");
 
@@ -681,128 +681,128 @@ int ccg_setup(
 
     /* Assign the max count among the RNs as local_ra_cnt */
     if ((ctx->rnf_count > ctx->rnd_count) &&
-        (ctx->rnf_count > ctx->rni_count)) {
-        local_ra_cnt = ctx->rnf_count;
+	(ctx->rnf_count > ctx->rni_count)) {
+	local_ra_cnt = ctx->rnf_count;
     } else if (ctx->rnd_count > ctx->rni_count) {
-        local_ra_cnt = ctx->rnd_count;
+	local_ra_cnt = ctx->rnd_count;
     } else {
-        local_ra_cnt = ctx->rni_count;
+	local_ra_cnt = ctx->rni_count;
     }
 
     cmn700_ccg_ctx.is_prog_for_port_agg = false;
 
     do {
-        /* Set the global config data */
-        config = ccg_config;
+	/* Set the global config data */
+	config = ccg_config;
 
-        if (ccg_config->smp_mode) {
-            enable_smp_mode(ctx, ccg_config);
-        }
-
-        /*
-         * In order to assign unique AgentIDs across multiple chips, chip_id is
-         * used as factor to offset the AgentID value
-         */
-        cmn700_ccg_ctx.raid_value = 0;
-        offset_id = chip_id * local_ra_cnt;
-
-        for (rnf_ldid = 0; rnf_ldid < ctx->rnf_count; rnf_ldid++) {
-            agentid = cmn700_ccg_ctx.raid_value + offset_id;
-
-            /* Program RAID values in CCRA LDID to RAID LUT */
-            program_ccg_ra_rnf_ldid_to_exp_raid_reg(ctx, rnf_ldid, agentid);
-        }
-
-        /* Program agentid to linkid LUT for remote agents in CCRA/CCHA/CCLA */
-        program_agentid_to_linkid_reg(ctx, ccg_config);
-
-        /* Program HN-F ldid to CHI NodeID for remote RN-F agents */
-        program_hnf_ldid_to_chi_node_id_reg(ctx, ccg_config);
-
-        /*
-         * unique_remote_rnf_ldid_value is used to keep track of the
-         * ldid of the remote RNF agents
-         */
-        unique_remote_rnf_ldid_value = ctx->rnf_count;
-
-        if (ccg_config->remote_rnf_count && (ctx->rnf_count == 0)) {
-            FWK_LOG_ERR(
-                MOD_NAME
-                "Remote RN-F Count can't be %u when RN-F count is zero",
-                ccg_config->remote_rnf_count);
-            fwk_unexpected();
-        }
-
-        for (i = 0; i < ccg_config->remote_rnf_count; i++) {
-            /*
-             * The remote_agentid (RAID) should not include the current chip's
-             * AgentIDs. If `block` is less than the current chip_id, then
-             * include the AgentIDs starting from chip 0 till (not including)
-             * current chip. If the `block` is equal or greater than the current
-             * chip, then include the AgentIDs from next chip till the max chip.
-             */
-            if ((i / ctx->rnf_count) < chip_id) {
-                remote_agentid = (i % ctx->rnf_count) +
-                    (local_ra_cnt * (i / ctx->rnf_count));
-            } else {
-                remote_agentid = (i % ctx->rnf_count) + local_ra_cnt +
-                    (local_ra_cnt * (i / ctx->rnf_count));
-            }
-
-            /* Program the CCHA raid to ldid LUT */
-            program_ccg_ha_raid_to_ldid_lut(
-                ctx, remote_agentid, unique_remote_rnf_ldid_value);
-
-            unique_remote_rnf_ldid_value++;
-        }
-
-        /* Program the unique HAID for the CCHA block */
-        program_ccg_ha_id(ctx);
-
-        cmn700_ccg_ctx.raid_value = 0;
-        offset_id = chip_id * local_ra_cnt;
-
-        for (rnd_ldid = 0; rnd_ldid < ctx->rnd_count; rnd_ldid++) {
-            /* Determine agentid of the remote agents */
-            agentid = cmn700_ccg_ctx.raid_value + offset_id;
-
-            /* Program RAID values in CCRA LDID to RAID LUT */
-            program_ccg_ra_rnd_ldid_to_exp_raid_reg(ctx, rnd_ldid, agentid);
-        }
-
-        cmn700_ccg_ctx.raid_value = 0;
-        offset_id = chip_id * local_ra_cnt;
-
-        for (rni_ldid = 0; rni_ldid < ctx->rni_count; rni_ldid++) {
-            /* Determine agentid of the remote agents */
-            agentid = cmn700_ccg_ctx.raid_value + offset_id;
-
-            /* Program RAID values in CCRA LDID to RAID LUT */
-            program_ccg_ra_rni_ldid_to_exp_raid_reg(ctx, rni_ldid, agentid);
-        }
-
-        /*
-         * Program the CCRA SAM with the address range and the corresponding
-         * remote HAID
-         */
-        program_ccg_ra_sam_addr_region(ctx, ccg_config);
-
-        if (ccg_config->ull_to_ull_mode) {
-            status = enable_ull_to_ull_mode(ctx, ccg_config);
-            if (status != FWK_SUCCESS) {
-                return status;
-            }
-        }
-        /* Program the Link Control registers present in CCRA/CCHA/CCLA */
-        status = enable_and_start_ccg_link_up_sequence(ctx, 0);
-
-        if (config->port_aggregate && !cmn700_ccg_ctx.is_prog_for_port_agg) {
-            cmn700_ccg_ctx.is_prog_for_port_agg = true;
-            FWK_LOG_INFO(MOD_NAME
-                         "Programming CCG gateway for Port Aggregation...");
-        } else {
-            cmn700_ccg_ctx.is_prog_for_port_agg = false;
-        }
+	if (ccg_config->smp_mode) {
+	    enable_smp_mode(ctx, ccg_config);
+	}
+
+	/*
+	 * In order to assign unique AgentIDs across multiple chips, chip_id is
+	 * used as factor to offset the AgentID value
+	 */
+	cmn700_ccg_ctx.raid_value = 0;
+	offset_id = chip_id * local_ra_cnt;
+
+	for (rnf_ldid = 0; rnf_ldid < ctx->rnf_count; rnf_ldid++) {
+	    agentid = cmn700_ccg_ctx.raid_value + offset_id;
+
+	    /* Program RAID values in CCRA LDID to RAID LUT */
+	    program_ccg_ra_rnf_ldid_to_exp_raid_reg(ctx, rnf_ldid, agentid);
+	}
+
+	/* Program agentid to linkid LUT for remote agents in CCRA/CCHA/CCLA */
+	program_agentid_to_linkid_reg(ctx, ccg_config);
+
+	/* Program HN-F ldid to CHI NodeID for remote RN-F agents */
+	program_hnf_ldid_to_chi_node_id_reg(ctx, ccg_config);
+
+	/*
+	 * unique_remote_rnf_ldid_value is used to keep track of the
+	 * ldid of the remote RNF agents
+	 */
+	unique_remote_rnf_ldid_value = ctx->rnf_count;
+
+	if (ccg_config->remote_rnf_count && (ctx->rnf_count == 0)) {
+	    FWK_LOG_ERR(
+		MOD_NAME
+		"Remote RN-F Count can't be %u when RN-F count is zero",
+		ccg_config->remote_rnf_count);
+	    fwk_unexpected();
+	}
+
+	for (i = 0; i < ccg_config->remote_rnf_count; i++) {
+	    /*
+	     * The remote_agentid (RAID) should not include the current chip's
+	     * AgentIDs. If `block` is less than the current chip_id, then
+	     * include the AgentIDs starting from chip 0 till (not including)
+	     * current chip. If the `block` is equal or greater than the current
+	     * chip, then include the AgentIDs from next chip till the max chip.
+	     */
+	    if ((i / ctx->rnf_count) < chip_id) {
+		remote_agentid = (i % ctx->rnf_count) +
+		    (local_ra_cnt * (i / ctx->rnf_count));
+	    } else {
+		remote_agentid = (i % ctx->rnf_count) + local_ra_cnt +
+		    (local_ra_cnt * (i / ctx->rnf_count));
+	    }
+
+	    /* Program the CCHA raid to ldid LUT */
+	    program_ccg_ha_raid_to_ldid_lut(
+		ctx, remote_agentid, unique_remote_rnf_ldid_value);
+
+	    unique_remote_rnf_ldid_value++;
+	}
+
+	/* Program the unique HAID for the CCHA block */
+	program_ccg_ha_id(ctx);
+
+	cmn700_ccg_ctx.raid_value = 0;
+	offset_id = chip_id * local_ra_cnt;
+
+	for (rnd_ldid = 0; rnd_ldid < ctx->rnd_count; rnd_ldid++) {
+	    /* Determine agentid of the remote agents */
+	    agentid = cmn700_ccg_ctx.raid_value + offset_id;
+
+	    /* Program RAID values in CCRA LDID to RAID LUT */
+	    program_ccg_ra_rnd_ldid_to_exp_raid_reg(ctx, rnd_ldid, agentid);
+	}
+
+	cmn700_ccg_ctx.raid_value = 0;
+	offset_id = chip_id * local_ra_cnt;
+
+	for (rni_ldid = 0; rni_ldid < ctx->rni_count; rni_ldid++) {
+	    /* Determine agentid of the remote agents */
+	    agentid = cmn700_ccg_ctx.raid_value + offset_id;
+
+	    /* Program RAID values in CCRA LDID to RAID LUT */
+	    program_ccg_ra_rni_ldid_to_exp_raid_reg(ctx, rni_ldid, agentid);
+	}
+
+	/*
+	 * Program the CCRA SAM with the address range and the corresponding
+	 * remote HAID
+	 */
+	program_ccg_ra_sam_addr_region(ctx, ccg_config);
+
+	if (ccg_config->ull_to_ull_mode) {
+	    status = enable_ull_to_ull_mode(ctx, ccg_config);
+	    if (status != FWK_SUCCESS) {
+		return status;
+	    }
+	}
+	/* Program the Link Control registers present in CCRA/CCHA/CCLA */
+	status = enable_and_start_ccg_link_up_sequence(ctx, 0);
+
+	if (config->port_aggregate && !cmn700_ccg_ctx.is_prog_for_port_agg) {
+	    cmn700_ccg_ctx.is_prog_for_port_agg = true;
+	    FWK_LOG_INFO(MOD_NAME
+			 "Programming CCG gateway for Port Aggregation...");
+	} else {
+	    cmn700_ccg_ctx.is_prog_for_port_agg = false;
+	}
     } while (config->port_aggregate && cmn700_ccg_ctx.is_prog_for_port_agg);
 
     return status;
@@ -820,31 +820,31 @@ int ccg_exchange_protocol_credit(
     cmn700_ccg_ctx.is_prog_for_port_agg = false;
 
     do {
-        config = ccg_config;
-        ccg_ldid = get_ldid(ctx, cmn700_ccg_ctx.is_prog_for_port_agg);
-        ccg_ra_reg = ctx->ccg_ra_reg_table[ccg_ldid].ccg_ra_reg;
-        ccg_ha_reg = ctx->ccg_ha_reg_table[ccg_ldid].ccg_ha_reg;
-
-        /* Current support enables Link 0 only */
-        linkid = 0;
-
-        FWK_LOG_INFO(
-            MOD_NAME "Exchanging protocol credits for link %d...", linkid);
-        /* Exchange protocol credits using link up bit */
-        ccg_ra_reg->LINK_REGS[linkid].CCG_CCPRTCL_LINK_CTRL |=
-            CCG_LINK_CTRL_UP_MASK;
-        ccg_ha_reg->LINK_REGS[linkid].CCG_CCPRTCL_LINK_CTRL |=
-            CCG_LINK_CTRL_UP_MASK;
-        FWK_LOG_INFO(
-            MOD_NAME "Exchanging protocol credits for link %d... Done", linkid);
-
-        if (config->port_aggregate && !cmn700_ccg_ctx.is_prog_for_port_agg) {
-            cmn700_ccg_ctx.is_prog_for_port_agg = true;
-            FWK_LOG_INFO(MOD_NAME
-                         "Exchange Protocol Credit for Port Aggregation...");
-        } else {
-            cmn700_ccg_ctx.is_prog_for_port_agg = false;
-        }
+	config = ccg_config;
+	ccg_ldid = get_ldid(ctx, cmn700_ccg_ctx.is_prog_for_port_agg);
+	ccg_ra_reg = ctx->ccg_ra_reg_table[ccg_ldid].ccg_ra_reg;
+	ccg_ha_reg = ctx->ccg_ha_reg_table[ccg_ldid].ccg_ha_reg;
+
+	/* Current support enables Link 0 only */
+	linkid = 0;
+
+	FWK_LOG_INFO(
+	    MOD_NAME "Exchanging protocol credits for link %d...", linkid);
+	/* Exchange protocol credits using link up bit */
+	ccg_ra_reg->LINK_REGS[linkid].CCG_CCPRTCL_LINK_CTRL |=
+	    CCG_LINK_CTRL_UP_MASK;
+	ccg_ha_reg->LINK_REGS[linkid].CCG_CCPRTCL_LINK_CTRL |=
+	    CCG_LINK_CTRL_UP_MASK;
+	FWK_LOG_INFO(
+	    MOD_NAME "Exchanging protocol credits for link %d... Done", linkid);
+
+	if (config->port_aggregate && !cmn700_ccg_ctx.is_prog_for_port_agg) {
+	    cmn700_ccg_ctx.is_prog_for_port_agg = true;
+	    FWK_LOG_INFO(MOD_NAME
+			 "Exchange Protocol Credit for Port Aggregation...");
+	} else {
+	    cmn700_ccg_ctx.is_prog_for_port_agg = false;
+	}
     } while (config->port_aggregate && cmn700_ccg_ctx.is_prog_for_port_agg);
 
     return FWK_SUCCESS;
@@ -863,45 +863,45 @@ int ccg_enter_system_coherency(
     cmn700_ccg_ctx.is_prog_for_port_agg = false;
 
     do {
-        config = ccg_config;
-        ccg_ldid = get_ldid(ctx, cmn700_ccg_ctx.is_prog_for_port_agg);
-        ccg_ha_reg = ctx->ccg_ha_reg_table[ccg_ldid].ccg_ha_reg;
-
-        /* Current support enables Link 0 only */
-        linkid = 0;
-
-        wait_data.ctx = ctx;
-        wait_data.linkid = linkid;
-
-        FWK_LOG_INFO(
-            MOD_NAME "Entering system coherency for link %d...", linkid);
-        /* Enter system coherency by setting DVMDOMAIN request bit */
-        ccg_ha_reg->LINK_REGS[linkid].CCG_CCPRTCL_LINK_CTRL |=
-            CCG_LINK_CTRL_DVMDOMAIN_REQ_MASK;
-
-        /* Wait till DVMDOMAIN ACK bit is set in status register */
-        wait_data.cond = CCG_LINK_STATUS_HA_DVMDOMAIN_ACK_BIT_SET;
-        status = ctx->timer_api->wait(
-            FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
-            CCG_CCPRTCL_LINK_DVMDOMAIN_TIMEOUT,
-            ccg_link_wait_condition,
-            &wait_data);
-        if (status != FWK_SUCCESS) {
-            FWK_LOG_ERR(
-                MOD_NAME "Entering system coherency for link %d... Failed",
-                linkid);
-            return status;
-        }
-        FWK_LOG_INFO(
-            MOD_NAME "Entering system coherency for link %d... Done", linkid);
-
-        if (config->port_aggregate && !cmn700_ccg_ctx.is_prog_for_port_agg) {
-            cmn700_ccg_ctx.is_prog_for_port_agg = true;
-            FWK_LOG_INFO(MOD_NAME
-                         "Enter system coherency for Port Aggregation...");
-        } else {
-            cmn700_ccg_ctx.is_prog_for_port_agg = false;
-        }
+	config = ccg_config;
+	ccg_ldid = get_ldid(ctx, cmn700_ccg_ctx.is_prog_for_port_agg);
+	ccg_ha_reg = ctx->ccg_ha_reg_table[ccg_ldid].ccg_ha_reg;
+
+	/* Current support enables Link 0 only */
+	linkid = 0;
+
+	wait_data.ctx = ctx;
+	wait_data.linkid = linkid;
+
+	FWK_LOG_INFO(
+	    MOD_NAME "Entering system coherency for link %d...", linkid);
+	/* Enter system coherency by setting DVMDOMAIN request bit */
+	ccg_ha_reg->LINK_REGS[linkid].CCG_CCPRTCL_LINK_CTRL |=
+	    CCG_LINK_CTRL_DVMDOMAIN_REQ_MASK;
+
+	/* Wait till DVMDOMAIN ACK bit is set in status register */
+	wait_data.cond = CCG_LINK_STATUS_HA_DVMDOMAIN_ACK_BIT_SET;
+	status = ctx->timer_api->wait(
+	    FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
+	    CCG_CCPRTCL_LINK_DVMDOMAIN_TIMEOUT,
+	    ccg_link_wait_condition,
+	    &wait_data);
+	if (status != FWK_SUCCESS) {
+	    FWK_LOG_ERR(
+		MOD_NAME "Entering system coherency for link %d... Failed",
+		linkid);
+	    return status;
+	}
+	FWK_LOG_INFO(
+	    MOD_NAME "Entering system coherency for link %d... Done", linkid);
+
+	if (config->port_aggregate && !cmn700_ccg_ctx.is_prog_for_port_agg) {
+	    cmn700_ccg_ctx.is_prog_for_port_agg = true;
+	    FWK_LOG_INFO(MOD_NAME
+			 "Enter system coherency for Port Aggregation...");
+	} else {
+	    cmn700_ccg_ctx.is_prog_for_port_agg = false;
+	}
     } while (config->port_aggregate && cmn700_ccg_ctx.is_prog_for_port_agg);
 
     return FWK_SUCCESS;
@@ -920,43 +920,43 @@ int ccg_enter_dvm_domain(
     cmn700_ccg_ctx.is_prog_for_port_agg = false;
 
     do {
-        config = ccg_config;
-        ccg_ldid = get_ldid(ctx, cmn700_ccg_ctx.is_prog_for_port_agg);
-        ccg_ra_reg = ctx->ccg_ra_reg_table[ccg_ldid].ccg_ra_reg;
-
-        /* Current support enables Link 0 only */
-        linkid = 0;
-
-        wait_data.ctx = ctx;
-        wait_data.linkid = linkid;
-
-        FWK_LOG_INFO(MOD_NAME "Entering DVM domain for link %d...", linkid);
-        /* DVM domain entry by setting DVMDOMAIN request bit */
-        ccg_ra_reg->LINK_REGS[linkid].CCG_CCPRTCL_LINK_CTRL |=
-            CCG_LINK_CTRL_DVMDOMAIN_REQ_MASK;
-
-        /* Wait till DVMDOMAIN ACK bit is set in status register */
-        wait_data.cond = CCG_LINK_STATUS_RA_DVMDOMAIN_ACK_BIT_SET;
-        status = ctx->timer_api->wait(
-            FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
-            CCG_CCPRTCL_LINK_DVMDOMAIN_TIMEOUT,
-            ccg_link_wait_condition,
-            &wait_data);
-        if (status != FWK_SUCCESS) {
-            FWK_LOG_ERR(
-                MOD_NAME "Entering DVM domain for link %d... Failed", linkid);
-            return status;
-        }
-        FWK_LOG_INFO(
-            MOD_NAME "Entering DVM domain for link %d... Done", linkid);
-
-        if (config->port_aggregate && !cmn700_ccg_ctx.is_prog_for_port_agg) {
-            cmn700_ccg_ctx.is_prog_for_port_agg = true;
-            FWK_LOG_INFO(MOD_NAME
-                         "Entering DVM domain for Port Aggregation...");
-        } else {
-            cmn700_ccg_ctx.is_prog_for_port_agg = false;
-        }
+	config = ccg_config;
+	ccg_ldid = get_ldid(ctx, cmn700_ccg_ctx.is_prog_for_port_agg);
+	ccg_ra_reg = ctx->ccg_ra_reg_table[ccg_ldid].ccg_ra_reg;
+
+	/* Current support enables Link 0 only */
+	linkid = 0;
+
+	wait_data.ctx = ctx;
+	wait_data.linkid = linkid;
+
+	FWK_LOG_INFO(MOD_NAME "Entering DVM domain for link %d...", linkid);
+	/* DVM domain entry by setting DVMDOMAIN request bit */
+	ccg_ra_reg->LINK_REGS[linkid].CCG_CCPRTCL_LINK_CTRL |=
+	    CCG_LINK_CTRL_DVMDOMAIN_REQ_MASK;
+
+	/* Wait till DVMDOMAIN ACK bit is set in status register */
+	wait_data.cond = CCG_LINK_STATUS_RA_DVMDOMAIN_ACK_BIT_SET;
+	status = ctx->timer_api->wait(
+	    FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
+	    CCG_CCPRTCL_LINK_DVMDOMAIN_TIMEOUT,
+	    ccg_link_wait_condition,
+	    &wait_data);
+	if (status != FWK_SUCCESS) {
+	    FWK_LOG_ERR(
+		MOD_NAME "Entering DVM domain for link %d... Failed", linkid);
+	    return status;
+	}
+	FWK_LOG_INFO(
+	    MOD_NAME "Entering DVM domain for link %d... Done", linkid);
+
+	if (config->port_aggregate && !cmn700_ccg_ctx.is_prog_for_port_agg) {
+	    cmn700_ccg_ctx.is_prog_for_port_agg = true;
+	    FWK_LOG_INFO(MOD_NAME
+			 "Entering DVM domain for Port Aggregation...");
+	} else {
+	    cmn700_ccg_ctx.is_prog_for_port_agg = false;
+	}
     } while (config->port_aggregate && cmn700_ccg_ctx.is_prog_for_port_agg);
 
     return FWK_SUCCESS;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cmn700/src/mod_cmn700.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/cmn700/src/mod_cmn700.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cmn700/src/mod_cmn700.c
@@ -57,7 +57,7 @@ static struct node_pos *hnf_node_pos;
 static inline size_t cmn700_hnf_cache_group_count(size_t hnf_count)
 {
     return (hnf_count + CMN700_HNF_CACHE_GROUP_ENTRIES_PER_GROUP - 1) /
-        CMN700_HNF_CACHE_GROUP_ENTRIES_PER_GROUP;
+	CMN700_HNF_CACHE_GROUP_ENTRIES_PER_GROUP;
 }
 
 static void process_node_hnf(struct cmn700_hnf_reg *hnf)
@@ -84,96 +84,96 @@ static void process_node_hnf(struct cmn7
 
     /* SN mode with Hierarchical Hashing */
     if (config->hierarchical_hashing_enable && hier_hash_cfg->sn_mode) {
-        sn_mode = hier_hash_cfg->sn_mode;
-        top_address_bit0 = hier_hash_cfg->top_address_bit0;
-        top_address_bit1 = hier_hash_cfg->top_address_bit1;
-
-        snf_count_per_cluster =
-            config->snf_count / hier_hash_cfg->hnf_cluster_count;
-
-        /*
-         * For now, 3-SN mode (three SN-Fs per cluster) is supported, other
-         * modes are not tested.
-         */
-        fwk_assert(snf_count_per_cluster == 3);
-
-        /* Number of HN-Fs in a cluster */
-        hnf_count_per_cluster =
-            ctx->hnf_count / hier_hash_cfg->hnf_cluster_count;
-
-        /* Choose the cluster idx based on the HN-Fs LDID value */
-        hnf_cluster_index = logical_id / hnf_count_per_cluster;
-
-        if (top_address_bit1 <= top_address_bit0) {
-            FWK_LOG_ERR(
-                MOD_NAME
-                "top_address_bit1: %d should be greater than top_address_bit0: "
-                "%d",
-                top_address_bit1,
-                top_address_bit0);
-            fwk_unexpected();
-        }
-
-        sam_control =
-            ((UINT64_C(1) << CMN700_HNF_SAM_CONTROL_SN_MODE_POS(sn_mode)) |
-             ((uint64_t)top_address_bit0
-              << CMN700_HNF_SAM_CONTROL_TOP_ADDR_BIT0_POS) |
-             ((uint64_t)top_address_bit1
-              << CMN700_HNF_SAM_CONTROL_TOP_ADDR_BIT1_POS));
-
-        for (snf_idx_in_cluster = 0; snf_idx_in_cluster < snf_count_per_cluster;
-             snf_idx_in_cluster++) {
-            snf_table_idx = (hnf_cluster_index * snf_count_per_cluster) +
-                snf_idx_in_cluster;
-            sam_control |=
-                ((uint64_t)config->snf_table[snf_table_idx]
-                 << CMN700_HNF_SAM_CONTROL_SN_NODE_ID_POS(snf_idx_in_cluster));
-        }
+	sn_mode = hier_hash_cfg->sn_mode;
+	top_address_bit0 = hier_hash_cfg->top_address_bit0;
+	top_address_bit1 = hier_hash_cfg->top_address_bit1;
+
+	snf_count_per_cluster =
+	    config->snf_count / hier_hash_cfg->hnf_cluster_count;
+
+	/*
+	 * For now, 3-SN mode (three SN-Fs per cluster) is supported, other
+	 * modes are not tested.
+	 */
+	fwk_assert(snf_count_per_cluster == 3);
+
+	/* Number of HN-Fs in a cluster */
+	hnf_count_per_cluster =
+	    ctx->hnf_count / hier_hash_cfg->hnf_cluster_count;
+
+	/* Choose the cluster idx based on the HN-Fs LDID value */
+	hnf_cluster_index = logical_id / hnf_count_per_cluster;
+
+	if (top_address_bit1 <= top_address_bit0) {
+	    FWK_LOG_ERR(
+		MOD_NAME
+		"top_address_bit1: %d should be greater than top_address_bit0: "
+		"%d",
+		top_address_bit1,
+		top_address_bit0);
+	    fwk_unexpected();
+	}
+
+	sam_control =
+	    ((UINT64_C(1) << CMN700_HNF_SAM_CONTROL_SN_MODE_POS(sn_mode)) |
+	     ((uint64_t)top_address_bit0
+	      << CMN700_HNF_SAM_CONTROL_TOP_ADDR_BIT0_POS) |
+	     ((uint64_t)top_address_bit1
+	      << CMN700_HNF_SAM_CONTROL_TOP_ADDR_BIT1_POS));
+
+	for (snf_idx_in_cluster = 0; snf_idx_in_cluster < snf_count_per_cluster;
+	     snf_idx_in_cluster++) {
+	    snf_table_idx = (hnf_cluster_index * snf_count_per_cluster) +
+		snf_idx_in_cluster;
+	    sam_control |=
+		((uint64_t)config->snf_table[snf_table_idx]
+		 << CMN700_HNF_SAM_CONTROL_SN_NODE_ID_POS(snf_idx_in_cluster));
+	}
 
-        hnf->SAM_CONTROL = sam_control;
+	hnf->SAM_CONTROL = sam_control;
     } else {
-        fwk_assert(logical_id < config->snf_count);
+	fwk_assert(logical_id < config->snf_count);
 
-        /* Set target node */
-        hnf->SAM_CONTROL = config->snf_table[logical_id];
+	/* Set target node */
+	hnf->SAM_CONTROL = config->snf_table[logical_id];
     }
 
     /*
      * Map sub-regions to this HN-F node
      */
     for (region_idx = 0; region_idx < config->mmap_count; region_idx++) {
-        region = &config->mmap_table[region_idx];
+	region = &config->mmap_table[region_idx];
 
-        /* Skip non sub-regions */
-        if (region->type != MOD_CMN700_REGION_TYPE_SYSCACHE_SUB)
-            continue;
-
-        /* Offset the base with chip address space base on chip-id */
-        base =
-            ((uint64_t)(ctx->config->chip_addr_space * chip_id)) + region->base;
-
-        /* Configure sub-region entry */
-        if (get_hnsam_range_comp_en_mode(hnf)) {
-            hnf->SAM_MEMREGION[region_sub_count] = region->node_id |
-                ((base / SAM_GRANULARITY)
-                 << CMN700_HNF_SAM_MEMREGION_BASE_POS) |
-                CMN700_HNF_SAM_MEMREGION_VALID;
-            hnf->SAM_MEMREGION_END_ADDR[region_sub_count] =
-                ((base + region->size - 1));
-        } else {
-            hnf->SAM_MEMREGION[region_sub_count] = region->node_id |
-                (sam_encode_region_size(region->size)
-                 << CMN700_HNF_SAM_MEMREGION_SIZE_POS) |
-                ((base / SAM_GRANULARITY)
-                 << CMN700_HNF_SAM_MEMREGION_BASE_POS) |
-                CMN700_HNF_SAM_MEMREGION_VALID;
-        }
-        region_sub_count++;
+	/* Skip non sub-regions */
+	if (region->type != MOD_CMN700_REGION_TYPE_SYSCACHE_SUB)
+	    continue;
+
+	/* Offset the base with chip address space base on chip-id */
+	base =
+	    ((uint64_t)(ctx->config->chip_addr_space * chip_id)) + region->base;
+
+	/* Configure sub-region entry */
+	if (get_hnsam_range_comp_en_mode(hnf)) {
+	    hnf->SAM_MEMREGION[region_sub_count] = region->node_id |
+		((base / SAM_GRANULARITY)
+		 << CMN700_HNF_SAM_MEMREGION_BASE_POS) |
+		CMN700_HNF_SAM_MEMREGION_VALID;
+	    hnf->SAM_MEMREGION_END_ADDR[region_sub_count] =
+		((base + region->size - 1));
+	} else {
+	    hnf->SAM_MEMREGION[region_sub_count] = region->node_id |
+		(sam_encode_region_size(region->size)
+		 << CMN700_HNF_SAM_MEMREGION_SIZE_POS) |
+		((base / SAM_GRANULARITY)
+		 << CMN700_HNF_SAM_MEMREGION_BASE_POS) |
+		CMN700_HNF_SAM_MEMREGION_VALID;
+	}
+	region_sub_count++;
     }
 
     /* Configure the system cache RAM PPU */
     hnf->PPU_PWPR = CMN700_PPU_PWPR_POLICY_ON | CMN700_PPU_PWPR_OPMODE_FAM |
-        CMN700_PPU_PWPR_DYN_EN;
+	CMN700_PPU_PWPR_DYN_EN;
 }
 
 /*
@@ -209,7 +209,7 @@ static int cmn700_discovery(void)
     set_encoding_and_masking_bits(config);
 
     FWK_LOG_INFO(
-        MOD_NAME "CMN-700 revision: %s", get_cmn700_revision_name(ctx->root));
+	MOD_NAME "CMN-700 revision: %s", get_cmn700_revision_name(ctx->root));
     FWK_LOG_INFO(MOD_NAME "Starting discovery...");
     FWK_LOG_DEBUG(MOD_NAME "Rootnode Base address: 0x%x", (uintptr_t)ctx->root);
 
@@ -218,182 +218,182 @@ static int cmn700_discovery(void)
     /* Traverse cross points (XP) */
     xp_count = get_node_child_count(ctx->root);
     for (xp_idx = 0; xp_idx < xp_count; xp_idx++) {
-        xp = get_child_node(config->base, ctx->root, xp_idx);
-        fwk_assert(get_node_type(xp) == NODE_TYPE_XP);
+	xp = get_child_node(config->base, ctx->root, xp_idx);
+	fwk_assert(get_node_type(xp) == NODE_TYPE_XP);
 
-        FWK_LOG_INFO(
-            MOD_NAME "XP (%d, %d) ID:%d, LID:%d",
-            get_node_pos_x(xp),
-            get_node_pos_y(xp),
-            get_node_id(xp),
-            get_node_logical_id(xp));
-
-        /* Traverse nodes */
-        node_count = get_node_child_count(xp);
-        for (node_idx = 0; node_idx < node_count; node_idx++) {
-            node = get_child_node(config->base, xp, node_idx);
-
-            if (is_child_external(xp, node_idx)) { /* External nodes */
-                xp_port = get_port_number(
-                    get_child_node_id(xp, node_idx),
-                    get_node_device_port_count(xp));
-
-                /*
-                 * If the device type is CXRH, CXHA, or CXRA, then the external
-                 * child node is CXLA as every CXRH, CXHA, or CXRA node has a
-                 * corresponding external CXLA node.
-                 */
-                if ((get_device_type(xp, xp_port) == DEVICE_TYPE_CXRH) ||
-                    (get_device_type(xp, xp_port) == DEVICE_TYPE_CXHA) ||
-                    (get_device_type(xp, xp_port) == DEVICE_TYPE_CXRA)) {
-                    cxla_reg_count++;
-                    FWK_LOG_INFO(
-                        MOD_NAME "  Found CXLA at node ID: %d",
-                        get_child_node_id(xp, node_idx));
-                } else { /* External RN-SAM Node */
-                    ctx->external_rnsam_count++;
-                    FWK_LOG_INFO(
-                        MOD_NAME "  Found external node ID: %d addr: %p",
-                        get_child_node_id(xp, node_idx),
-                        xp);
-                }
-            } else { /* Internal nodes */
-                switch (get_node_type(node)) {
-                case NODE_TYPE_HN_F:
-                    if (ctx->hnf_count >= MAX_HNF_COUNT) {
-                        FWK_LOG_INFO(
-                            MOD_NAME "  hnf count %d >= max limit (%d)",
-                            ctx->hnf_count,
-                            MAX_HNF_COUNT);
-                        return FWK_E_DATA;
-                    }
-                    ctx->hnf_count++;
-                    break;
-
-                case NODE_TYPE_RN_SAM:
-                    ctx->internal_rnsam_count++;
-
-                    /*
-                     * RN-F nodes does not have node type identifier and hence
-                     * the count cannot be determined using the node type id.
-                     * Alternatively, check if the device type connected to the
-                     * Crosspoint (XP) is one of the RNF types and determine the
-                     * RN-F count (if CAL connected RN-F, double the count).
-                     */
-                    xp_port = get_port_number(
-                        get_node_id(node), get_node_device_port_count(xp));
-
-                    if (is_device_type_rnf(xp, xp_port)) {
-                        if (is_cal_connected(xp, xp_port)) {
-                            ctx->rnf_count += 2;
-                            FWK_LOG_INFO(
-                                MOD_NAME
-                                "  RN-F (CAL connected) found at port: %d",
-                                xp_port);
-                        } else {
-                            ctx->rnf_count++;
-                            FWK_LOG_INFO(
-                                MOD_NAME "  RN-F found at port: %d", xp_port);
-                        }
-                    }
-                    break;
-
-                case NODE_TYPE_RN_D:
-                    if ((ctx->rnd_count) >= MAX_RND_COUNT) {
-                        FWK_LOG_ERR(
-                            MOD_NAME "  rnd count %d >= max limit (%d)\n",
-                            ctx->rnd_count,
-                            MAX_RND_COUNT);
-                        return FWK_E_DATA;
-                    }
-                    ctx->rnd_count++;
-                    break;
-
-                case NODE_TYPE_RN_I:
-                    if ((ctx->rni_count) >= MAX_RNI_COUNT) {
-                        FWK_LOG_ERR(
-                            MOD_NAME "  rni count %d >= max limit (%d)\n",
-                            ctx->rni_count,
-                            MAX_RNI_COUNT);
-                        return FWK_E_DATA;
-                    }
-                    ctx->rni_count++;
-                    break;
-
-                case NODE_TYPE_CCRA:
-                    ccg_ra_reg_count++;
-                    break;
-
-                case NODE_TYPE_CCHA:
-                    ccg_ha_reg_count++;
-                    break;
-
-                case NODE_TYPE_CCLA:
-                    ccla_reg_count++;
-                    break;
-
-                case NODE_TYPE_CXRA:
-                    cxg_ra_reg_count++;
-                    break;
-
-                case NODE_TYPE_CXHA:
-                    cxg_ha_reg_count++;
-                    break;
-
-                /* CXLA should not be an internal node */
-                case NODE_TYPE_CXLA:
-                    FWK_LOG_ERR(MOD_NAME
-                                "CXLA node should not be internal node, "
-                                "discovery failed");
-                    return FWK_E_DEVICE;
-                    break;
-
-                default:
-                    /* Nothing to be done for other node types */
-                    break;
-                }
-
-                FWK_LOG_INFO(
-                    MOD_NAME "  %s ID:%d, LID:%d",
-                    get_node_type_name(get_node_type(node)),
-                    get_node_id(node),
-                    get_node_logical_id(node));
-            }
-        }
+	FWK_LOG_INFO(
+	    MOD_NAME "XP (%d, %d) ID:%d, LID:%d",
+	    get_node_pos_x(xp),
+	    get_node_pos_y(xp),
+	    get_node_id(xp),
+	    get_node_logical_id(xp));
+
+	/* Traverse nodes */
+	node_count = get_node_child_count(xp);
+	for (node_idx = 0; node_idx < node_count; node_idx++) {
+	    node = get_child_node(config->base, xp, node_idx);
+
+	    if (is_child_external(xp, node_idx)) { /* External nodes */
+		xp_port = get_port_number(
+		    get_child_node_id(xp, node_idx),
+		    get_node_device_port_count(xp));
+
+		/*
+		 * If the device type is CXRH, CXHA, or CXRA, then the external
+		 * child node is CXLA as every CXRH, CXHA, or CXRA node has a
+		 * corresponding external CXLA node.
+		 */
+		if ((get_device_type(xp, xp_port) == DEVICE_TYPE_CXRH) ||
+		    (get_device_type(xp, xp_port) == DEVICE_TYPE_CXHA) ||
+		    (get_device_type(xp, xp_port) == DEVICE_TYPE_CXRA)) {
+		    cxla_reg_count++;
+		    FWK_LOG_INFO(
+			MOD_NAME "  Found CXLA at node ID: %d",
+			get_child_node_id(xp, node_idx));
+		} else { /* External RN-SAM Node */
+		    ctx->external_rnsam_count++;
+		    FWK_LOG_INFO(
+			MOD_NAME "  Found external node ID: %d addr: %p",
+			get_child_node_id(xp, node_idx),
+			xp);
+		}
+	    } else { /* Internal nodes */
+		switch (get_node_type(node)) {
+		case NODE_TYPE_HN_F:
+		    if (ctx->hnf_count >= MAX_HNF_COUNT) {
+			FWK_LOG_INFO(
+			    MOD_NAME "  hnf count %d >= max limit (%d)",
+			    ctx->hnf_count,
+			    MAX_HNF_COUNT);
+			return FWK_E_DATA;
+		    }
+		    ctx->hnf_count++;
+		    break;
+
+		case NODE_TYPE_RN_SAM:
+		    ctx->internal_rnsam_count++;
+
+		    /*
+		     * RN-F nodes does not have node type identifier and hence
+		     * the count cannot be determined using the node type id.
+		     * Alternatively, check if the device type connected to the
+		     * Crosspoint (XP) is one of the RNF types and determine the
+		     * RN-F count (if CAL connected RN-F, double the count).
+		     */
+		    xp_port = get_port_number(
+			get_node_id(node), get_node_device_port_count(xp));
+
+		    if (is_device_type_rnf(xp, xp_port)) {
+			if (is_cal_connected(xp, xp_port)) {
+			    ctx->rnf_count += 2;
+			    FWK_LOG_INFO(
+				MOD_NAME
+				"  RN-F (CAL connected) found at port: %d",
+				xp_port);
+			} else {
+			    ctx->rnf_count++;
+			    FWK_LOG_INFO(
+				MOD_NAME "  RN-F found at port: %d", xp_port);
+			}
+		    }
+		    break;
+
+		case NODE_TYPE_RN_D:
+		    if ((ctx->rnd_count) >= MAX_RND_COUNT) {
+			FWK_LOG_ERR(
+			    MOD_NAME "  rnd count %d >= max limit (%d)\n",
+			    ctx->rnd_count,
+			    MAX_RND_COUNT);
+			return FWK_E_DATA;
+		    }
+		    ctx->rnd_count++;
+		    break;
+
+		case NODE_TYPE_RN_I:
+		    if ((ctx->rni_count) >= MAX_RNI_COUNT) {
+			FWK_LOG_ERR(
+			    MOD_NAME "  rni count %d >= max limit (%d)\n",
+			    ctx->rni_count,
+			    MAX_RNI_COUNT);
+			return FWK_E_DATA;
+		    }
+		    ctx->rni_count++;
+		    break;
+
+		case NODE_TYPE_CCRA:
+		    ccg_ra_reg_count++;
+		    break;
+
+		case NODE_TYPE_CCHA:
+		    ccg_ha_reg_count++;
+		    break;
+
+		case NODE_TYPE_CCLA:
+		    ccla_reg_count++;
+		    break;
+
+		case NODE_TYPE_CXRA:
+		    cxg_ra_reg_count++;
+		    break;
+
+		case NODE_TYPE_CXHA:
+		    cxg_ha_reg_count++;
+		    break;
+
+		/* CXLA should not be an internal node */
+		case NODE_TYPE_CXLA:
+		    FWK_LOG_ERR(MOD_NAME
+				"CXLA node should not be internal node, "
+				"discovery failed");
+		    return FWK_E_DEVICE;
+		    break;
+
+		default:
+		    /* Nothing to be done for other node types */
+		    break;
+		}
+
+		FWK_LOG_INFO(
+		    MOD_NAME "  %s ID:%d, LID:%d",
+		    get_node_type_name(get_node_type(node)),
+		    get_node_id(node),
+		    get_node_logical_id(node));
+	    }
+	}
     }
 
     /* When CAL is present, the number of HN-Fs must be even. */
     if ((ctx->hnf_count % 2 != 0) && (config->hnf_cal_mode == true)) {
-        FWK_LOG_ERR(
-            MOD_NAME "hnf count: %d should be even when CAL mode is set",
-            ctx->hnf_count);
-        return FWK_E_DATA;
+	FWK_LOG_ERR(
+	    MOD_NAME "hnf count: %d should be even when CAL mode is set",
+	    ctx->hnf_count);
+	return FWK_E_DATA;
     }
 
     ctx->ccg_node_count = ccg_ra_reg_count;
 
     if (ctx->rnf_count > MAX_RNF_COUNT) {
-        FWK_LOG_ERR(
-            MOD_NAME "rnf count %d > max limit (%d)\n",
-            ctx->rnf_count,
-            MAX_RNF_COUNT);
-        return FWK_E_RANGE;
+	FWK_LOG_ERR(
+	    MOD_NAME "rnf count %d > max limit (%d)\n",
+	    ctx->rnf_count,
+	    MAX_RNF_COUNT);
+	return FWK_E_RANGE;
     }
 
     FWK_LOG_INFO(
-        MOD_NAME "Total internal RN-SAM nodes: %d", ctx->internal_rnsam_count);
+	MOD_NAME "Total internal RN-SAM nodes: %d", ctx->internal_rnsam_count);
     FWK_LOG_INFO(
-        MOD_NAME "Total external RN-SAM nodes: %d", ctx->external_rnsam_count);
+	MOD_NAME "Total external RN-SAM nodes: %d", ctx->external_rnsam_count);
     FWK_LOG_INFO(MOD_NAME "Total HN-F nodes: %d", ctx->hnf_count);
     FWK_LOG_INFO(MOD_NAME "Total RN-D nodes: %d\n", ctx->rnd_count);
     FWK_LOG_INFO(MOD_NAME "Total RN-F nodes: %d\n", ctx->rnf_count);
     FWK_LOG_INFO(MOD_NAME "Total RN-I nodes: %d\n", ctx->rni_count);
     FWK_LOG_INFO(
-        MOD_NAME "Total CCIX Request Agent nodes: %d", cxg_ra_reg_count);
+	MOD_NAME "Total CCIX Request Agent nodes: %d", cxg_ra_reg_count);
     FWK_LOG_INFO(MOD_NAME "Total CCIX Home Agent nodes: %d", cxg_ha_reg_count);
     FWK_LOG_INFO(MOD_NAME "Total CCIX Link Agent nodes: %d", cxla_reg_count);
     FWK_LOG_INFO(
-        MOD_NAME "Total CCG Request Agent nodes: %d", ccg_ra_reg_count);
+	MOD_NAME "Total CCG Request Agent nodes: %d", ccg_ra_reg_count);
     FWK_LOG_INFO(MOD_NAME "Total CCG Home Agent nodes: %d", ccg_ha_reg_count);
     FWK_LOG_INFO(MOD_NAME "Total CCG Link Agent nodes: %d", ccla_reg_count);
 
@@ -424,76 +424,76 @@ static void cmn700_configure(void)
     /* Traverse cross points (XP) */
     xp_count = get_node_child_count(ctx->root);
     for (xp_idx = 0; xp_idx < xp_count; xp_idx++) {
-        xp = get_child_node(config->base, ctx->root, xp_idx);
-        fwk_assert(get_node_type(xp) == NODE_TYPE_XP);
+	xp = get_child_node(config->base, ctx->root, xp_idx);
+	fwk_assert(get_node_type(xp) == NODE_TYPE_XP);
 
-        /* Traverse nodes */
-        node_count = get_node_child_count(xp);
-        for (node_idx = 0; node_idx < node_count; node_idx++) {
-            node = get_child_node(config->base, xp, node_idx);
-            xp_port = get_port_number(
-                get_child_node_id(xp, node_idx),
-                get_node_device_port_count(xp));
-            if (is_child_external(xp, node_idx)) {
-                node_id = get_child_node_id(xp, node_idx);
-
-                if (!(get_device_type(xp, xp_port) == DEVICE_TYPE_CXRH) &&
-                    !(get_device_type(xp, xp_port) == DEVICE_TYPE_CXHA) &&
-                    !(get_device_type(xp, xp_port) == DEVICE_TYPE_CXRA)) {
-                    fwk_assert(xrnsam_entry < ctx->external_rnsam_count);
-
-                    ctx->external_rnsam_table[xrnsam_entry].node_id = node_id;
-                    ctx->external_rnsam_table[xrnsam_entry].node = node;
-
-                    xrnsam_entry++;
-                }
-            } else {
-                enum node_type node_type = get_node_type(node);
-                node_id = get_node_id(node);
-                if (node_type == NODE_TYPE_RN_SAM) {
-                    fwk_assert(irnsam_entry < ctx->internal_rnsam_count);
-
-                    ctx->internal_rnsam_table[irnsam_entry] = node;
-
-                    irnsam_entry++;
-                } else if (node_type == NODE_TYPE_CCRA) {
-                    ldid = get_node_logical_id(node);
-                    fwk_assert(ldid < ctx->ccg_node_count);
-
-                    /* Use ldid as index of the ccg_ra table */
-                    ctx->ccg_ra_reg_table[ldid].node_id = node_id;
-                    ctx->ccg_ra_reg_table[ldid].ccg_ra_reg =
-                        (struct cmn700_ccg_ra_reg *)node;
-                } else if (node_type == NODE_TYPE_CCHA) {
-                    ldid = get_node_logical_id(node);
-                    fwk_assert(ldid < ctx->ccg_node_count);
-
-                    /* Use ldid as index of the ccg_ra table */
-                    ctx->ccg_ha_reg_table[ldid].node_id = node_id;
-                    ctx->ccg_ha_reg_table[ldid].ccg_ha_reg =
-                        (struct cmn700_ccg_ha_reg *)node;
-                } else if (node_type == NODE_TYPE_CCLA) {
-                    ldid = get_node_logical_id(node);
-
-                    /* Use ldid as index of the ccla table */
-                    ctx->ccla_reg_table[ldid].node_id = node_id;
-                    ctx->ccla_reg_table[ldid].ccla_reg =
-                        (struct cmn700_ccla_reg *)node;
-                } else if (node_type == NODE_TYPE_HN_F) {
-                    logical_id = get_node_logical_id(node);
-                    fwk_assert(logical_id < ctx->hnf_count);
-
-                    ctx->hnf_node[logical_id] = (uintptr_t)(void *)node;
-
-                    hnf_node_pos[logical_id].pos_x = get_node_pos_x(node);
-                    hnf_node_pos[logical_id].pos_y = get_node_pos_y(node);
-                    hnf_node_pos[logical_id].port_num =
-                        get_port_number(node_id, xp_port);
-
-                    process_node_hnf(node);
-                }
-            }
-        }
+	/* Traverse nodes */
+	node_count = get_node_child_count(xp);
+	for (node_idx = 0; node_idx < node_count; node_idx++) {
+	    node = get_child_node(config->base, xp, node_idx);
+	    xp_port = get_port_number(
+		get_child_node_id(xp, node_idx),
+		get_node_device_port_count(xp));
+	    if (is_child_external(xp, node_idx)) {
+		node_id = get_child_node_id(xp, node_idx);
+
+		if (!(get_device_type(xp, xp_port) == DEVICE_TYPE_CXRH) &&
+		    !(get_device_type(xp, xp_port) == DEVICE_TYPE_CXHA) &&
+		    !(get_device_type(xp, xp_port) == DEVICE_TYPE_CXRA)) {
+		    fwk_assert(xrnsam_entry < ctx->external_rnsam_count);
+
+		    ctx->external_rnsam_table[xrnsam_entry].node_id = node_id;
+		    ctx->external_rnsam_table[xrnsam_entry].node = node;
+
+		    xrnsam_entry++;
+		}
+	    } else {
+		enum node_type node_type = get_node_type(node);
+		node_id = get_node_id(node);
+		if (node_type == NODE_TYPE_RN_SAM) {
+		    fwk_assert(irnsam_entry < ctx->internal_rnsam_count);
+
+		    ctx->internal_rnsam_table[irnsam_entry] = node;
+
+		    irnsam_entry++;
+		} else if (node_type == NODE_TYPE_CCRA) {
+		    ldid = get_node_logical_id(node);
+		    fwk_assert(ldid < ctx->ccg_node_count);
+
+		    /* Use ldid as index of the ccg_ra table */
+		    ctx->ccg_ra_reg_table[ldid].node_id = node_id;
+		    ctx->ccg_ra_reg_table[ldid].ccg_ra_reg =
+			(struct cmn700_ccg_ra_reg *)node;
+		} else if (node_type == NODE_TYPE_CCHA) {
+		    ldid = get_node_logical_id(node);
+		    fwk_assert(ldid < ctx->ccg_node_count);
+
+		    /* Use ldid as index of the ccg_ra table */
+		    ctx->ccg_ha_reg_table[ldid].node_id = node_id;
+		    ctx->ccg_ha_reg_table[ldid].ccg_ha_reg =
+			(struct cmn700_ccg_ha_reg *)node;
+		} else if (node_type == NODE_TYPE_CCLA) {
+		    ldid = get_node_logical_id(node);
+
+		    /* Use ldid as index of the ccla table */
+		    ctx->ccla_reg_table[ldid].node_id = node_id;
+		    ctx->ccla_reg_table[ldid].ccla_reg =
+			(struct cmn700_ccla_reg *)node;
+		} else if (node_type == NODE_TYPE_HN_F) {
+		    logical_id = get_node_logical_id(node);
+		    fwk_assert(logical_id < ctx->hnf_count);
+
+		    ctx->hnf_node[logical_id] = (uintptr_t)(void *)node;
+
+		    hnf_node_pos[logical_id].pos_x = get_node_pos_x(node);
+		    hnf_node_pos[logical_id].pos_y = get_node_pos_y(node);
+		    hnf_node_pos[logical_id].port_num =
+			get_port_number(node_id, xp_port);
+
+		    process_node_hnf(node);
+		}
+	    }
+	}
     }
 }
 
@@ -509,24 +509,24 @@ bool is_hnf_inside_rect(
     region_hnf_pos_end = region->hnf_pos_end;
 
     if (((hnf_node_pos.pos_x >= region_hnf_pos_start.pos_x) &&
-         (hnf_node_pos.pos_y >= region_hnf_pos_start.pos_y) &&
-         (hnf_node_pos.pos_x <= region_hnf_pos_end.pos_x) &&
-         (hnf_node_pos.pos_y <= region_hnf_pos_end.pos_y) &&
-         (hnf_node_pos.port_num <= region_hnf_pos_end.port_num))) {
-        if (hnf_node_pos.pos_y == region_hnf_pos_start.pos_y) {
-            if (hnf_node_pos.port_num >= region_hnf_pos_start.port_num) {
-                return true;
-            } else {
-                return false;
-            }
-        } else if (hnf_node_pos.pos_y == region_hnf_pos_end.pos_y) {
-            if (hnf_node_pos.port_num <= region_hnf_pos_end.port_num) {
-                return true;
-            } else {
-                return false;
-            }
-        }
-        return true;
+	 (hnf_node_pos.pos_y >= region_hnf_pos_start.pos_y) &&
+	 (hnf_node_pos.pos_x <= region_hnf_pos_end.pos_x) &&
+	 (hnf_node_pos.pos_y <= region_hnf_pos_end.pos_y) &&
+	 (hnf_node_pos.port_num <= region_hnf_pos_end.port_num))) {
+	if (hnf_node_pos.pos_y == region_hnf_pos_start.pos_y) {
+	    if (hnf_node_pos.port_num >= region_hnf_pos_start.port_num) {
+		return true;
+	    } else {
+		return false;
+	    }
+	} else if (hnf_node_pos.pos_y == region_hnf_pos_end.pos_y) {
+	    if (hnf_node_pos.port_num <= region_hnf_pos_end.port_num) {
+		return true;
+	    } else {
+		return false;
+	    }
+	}
+	return true;
     }
     return false;
 }
@@ -558,184 +558,184 @@ static int cmn700_setup_sam(struct cmn70
     (void)mmap_type_name;
 
     for (region_idx = 0; region_idx < config->mmap_count; region_idx++) {
-        region = &config->mmap_table[region_idx];
+	region = &config->mmap_table[region_idx];
 
-        /* Offset the base with chip address space base on chip-id */
-        base =
-            ((uint64_t)(ctx->config->chip_addr_space * chip_id) + region->base);
-
-        switch (region->type) {
-        case MOD_CMN700_MEM_REGION_TYPE_IO:
-            /*
-             * Configure memory region
-             */
-            FWK_LOG_INFO(
-                MOD_NAME "  [0x%" PRIx64 " - 0x%" PRIx64 "] %s",
-                base,
-                base + region->size - 1,
-                mmap_type_name[region->type]);
-
-            configure_region(
-                rnsam,
-                region_io_count,
-                base,
-                region->size,
-                SAM_NODE_TYPE_HN_I,
-                SAM_TYPE_NON_HASH_MEM_REGION);
-
-            /*
-             * Configure target node
-             */
-            group = region_io_count /
-                CMN700_RNSAM_NON_HASH_TGT_NODEID_ENTRIES_PER_GROUP;
-            bit_pos = CMN700_RNSAM_NON_HASH_TGT_NODEID_ENTRY_BITS_WIDTH *
-                (region_io_count %
-                 CMN700_RNSAM_NON_HASH_TGT_NODEID_ENTRIES_PER_GROUP);
-
-            rnsam->NON_HASH_TGT_NODEID[group] &=
-                ~(CMN700_RNSAM_NON_HASH_TGT_NODEID_ENTRY_MASK << bit_pos);
-            rnsam->NON_HASH_TGT_NODEID[group] |=
-                (region->node_id & CMN700_RNSAM_NON_HASH_TGT_NODEID_ENTRY_MASK)
-                << bit_pos;
-
-            region_io_count++;
-            break;
-
-        case MOD_CMN700_MEM_REGION_TYPE_SYSCACHE:
-            /*
-             * Configure memory region
-             */
-            FWK_LOG_INFO(
-                MOD_NAME "  [0x%" PRIx64 " - 0x%" PRIx64 "] %s",
-                base,
-                region->base + region->size - 1,
-                mmap_type_name[region->type]);
-
-            configure_region(
-                rnsam,
-                region_sys_count,
-                base,
-                region->size,
-                SAM_NODE_TYPE_HN_F,
-                SAM_TYPE_SYS_CACHE_GRP_REGION);
-
-            /* Mark corresponding region as enabled */
-            fwk_assert(region_sys_count < MAX_SCG_COUNT);
-            scg_regions_enabled[region_sys_count] = 1;
-
-            region_sys_count++;
-
-            hnf_count_in_scg = 0;
-            for (logical_id = 0; logical_id < ctx->hnf_count; logical_id++) {
-                hnf_nodeid = get_node_id((void *)ctx->hnf_node[logical_id]);
-
-                if ((config->hnf_cal_mode) && (hnf_nodeid % 2 == 1)) {
-                    /* No need include odd node ids if cal mode is set */
-                    continue;
-                }
-
-                group = hn_nodeid_reg_bits_idx /
-                    CMN700_HNF_CACHE_GROUP_ENTRIES_PER_GROUP;
-
-                bit_pos = CMN700_HNF_CACHE_GROUP_ENTRY_BITS_WIDTH *
-                    ((hn_nodeid_reg_bits_idx %
-                      (CMN700_HNF_CACHE_GROUP_ENTRIES_PER_GROUP)));
-
-                if (is_hnf_inside_rect(hnf_node_pos[logical_id], region)) {
-                    if (config->hnf_cal_mode) {
-                        /*
-                         * If CAL mode is set, add only even numbered hnd node
-                         * to sys_cache_grp_hn_nodeid registers.
-                         */
-                        if (hnf_nodeid % 2 == 0) {
-                            rnsam->SYS_CACHE_GRP_HN_NODEID[group] +=
-                                (uint64_t)hnf_nodeid << bit_pos;
-                            rnsam->SYS_CACHE_GRP_SN_NODEID[group] +=
-                                ((uint64_t)config->snf_table[logical_id])
-                                << bit_pos;
-                            hnf_count_in_scg++;
-                            hn_nodeid_reg_bits_idx++;
-                        }
-                    } else {
-                        rnsam->SYS_CACHE_GRP_HN_NODEID[group] +=
-                            (uint64_t)hnf_nodeid << bit_pos;
-                        rnsam->SYS_CACHE_GRP_SN_NODEID[group] +=
-                            ((uint64_t)config->snf_table[logical_id])
-                            << bit_pos;
-                        hnf_count_in_scg++;
-                        hn_nodeid_reg_bits_idx++;
-                    }
-                }
-            }
-
-            rnsam->SYS_CACHE_GRP_HN_COUNT |= ((uint64_t)hnf_count_in_scg)
-                << CMN700_RNSAM_SYS_CACHE_GRP_HN_CNT_POS(region_sys_count - 1);
-
-            break;
-
-        case MOD_CMN700_REGION_TYPE_SYSCACHE_SUB:
-            FWK_LOG_INFO(
-                MOD_NAME "  [0x%" PRIx64 " - 0x%" PRIx64 "] %s",
-                base,
-                region->base + region->size - 1,
-                mmap_type_name[region->type]);
-
-            /* System cache sub-regions are handled by HN-Fs */
-            break;
-
-        default:
-            fwk_unexpected();
-            return FWK_E_DATA;
-        }
+	/* Offset the base with chip address space base on chip-id */
+	base =
+	    ((uint64_t)(ctx->config->chip_addr_space * chip_id) + region->base);
+
+	switch (region->type) {
+	case MOD_CMN700_MEM_REGION_TYPE_IO:
+	    /*
+	     * Configure memory region
+	     */
+	    FWK_LOG_INFO(
+		MOD_NAME "  [0x%" PRIx64 " - 0x%" PRIx64 "] %s",
+		base,
+		base + region->size - 1,
+		mmap_type_name[region->type]);
+
+	    configure_region(
+		rnsam,
+		region_io_count,
+		base,
+		region->size,
+		SAM_NODE_TYPE_HN_I,
+		SAM_TYPE_NON_HASH_MEM_REGION);
+
+	    /*
+	     * Configure target node
+	     */
+	    group = region_io_count /
+		CMN700_RNSAM_NON_HASH_TGT_NODEID_ENTRIES_PER_GROUP;
+	    bit_pos = CMN700_RNSAM_NON_HASH_TGT_NODEID_ENTRY_BITS_WIDTH *
+		(region_io_count %
+		 CMN700_RNSAM_NON_HASH_TGT_NODEID_ENTRIES_PER_GROUP);
+
+	    rnsam->NON_HASH_TGT_NODEID[group] &=
+		~(CMN700_RNSAM_NON_HASH_TGT_NODEID_ENTRY_MASK << bit_pos);
+	    rnsam->NON_HASH_TGT_NODEID[group] |=
+		(region->node_id & CMN700_RNSAM_NON_HASH_TGT_NODEID_ENTRY_MASK)
+		<< bit_pos;
+
+	    region_io_count++;
+	    break;
+
+	case MOD_CMN700_MEM_REGION_TYPE_SYSCACHE:
+	    /*
+	     * Configure memory region
+	     */
+	    FWK_LOG_INFO(
+		MOD_NAME "  [0x%" PRIx64 " - 0x%" PRIx64 "] %s",
+		base,
+		region->base + region->size - 1,
+		mmap_type_name[region->type]);
+
+	    configure_region(
+		rnsam,
+		region_sys_count,
+		base,
+		region->size,
+		SAM_NODE_TYPE_HN_F,
+		SAM_TYPE_SYS_CACHE_GRP_REGION);
+
+	    /* Mark corresponding region as enabled */
+	    fwk_assert(region_sys_count < MAX_SCG_COUNT);
+	    scg_regions_enabled[region_sys_count] = 1;
+
+	    region_sys_count++;
+
+	    hnf_count_in_scg = 0;
+	    for (logical_id = 0; logical_id < ctx->hnf_count; logical_id++) {
+		hnf_nodeid = get_node_id((void *)ctx->hnf_node[logical_id]);
+
+		if ((config->hnf_cal_mode) && (hnf_nodeid % 2 == 1)) {
+		    /* No need include odd node ids if cal mode is set */
+		    continue;
+		}
+
+		group = hn_nodeid_reg_bits_idx /
+		    CMN700_HNF_CACHE_GROUP_ENTRIES_PER_GROUP;
+
+		bit_pos = CMN700_HNF_CACHE_GROUP_ENTRY_BITS_WIDTH *
+		    ((hn_nodeid_reg_bits_idx %
+		      (CMN700_HNF_CACHE_GROUP_ENTRIES_PER_GROUP)));
+
+		if (is_hnf_inside_rect(hnf_node_pos[logical_id], region)) {
+		    if (config->hnf_cal_mode) {
+			/*
+			 * If CAL mode is set, add only even numbered hnd node
+			 * to sys_cache_grp_hn_nodeid registers.
+			 */
+			if (hnf_nodeid % 2 == 0) {
+			    rnsam->SYS_CACHE_GRP_HN_NODEID[group] +=
+				(uint64_t)hnf_nodeid << bit_pos;
+			    rnsam->SYS_CACHE_GRP_SN_NODEID[group] +=
+				((uint64_t)config->snf_table[logical_id])
+				<< bit_pos;
+			    hnf_count_in_scg++;
+			    hn_nodeid_reg_bits_idx++;
+			}
+		    } else {
+			rnsam->SYS_CACHE_GRP_HN_NODEID[group] +=
+			    (uint64_t)hnf_nodeid << bit_pos;
+			rnsam->SYS_CACHE_GRP_SN_NODEID[group] +=
+			    ((uint64_t)config->snf_table[logical_id])
+			    << bit_pos;
+			hnf_count_in_scg++;
+			hn_nodeid_reg_bits_idx++;
+		    }
+		}
+	    }
+
+	    rnsam->SYS_CACHE_GRP_HN_COUNT |= ((uint64_t)hnf_count_in_scg)
+		<< CMN700_RNSAM_SYS_CACHE_GRP_HN_CNT_POS(region_sys_count - 1);
+
+	    break;
+
+	case MOD_CMN700_REGION_TYPE_SYSCACHE_SUB:
+	    FWK_LOG_INFO(
+		MOD_NAME "  [0x%" PRIx64 " - 0x%" PRIx64 "] %s",
+		base,
+		region->base + region->size - 1,
+		mmap_type_name[region->type]);
+
+	    /* System cache sub-regions are handled by HN-Fs */
+	    break;
+
+	default:
+	    fwk_unexpected();
+	    return FWK_E_DATA;
+	}
     }
 
     /* Do configuration for CCG Nodes */
     for (size_t idx = 0; idx < config->ccg_table_count; idx++) {
-        region = &config->ccg_config_table[idx].remote_mmap_table;
+	region = &config->ccg_config_table[idx].remote_mmap_table;
 
-        FWK_LOG_INFO(
-            MOD_NAME "  [0x%" PRIx64 " - 0x%" PRIx64 "] %s",
-            region->base,
-            region->base + region->size - 1,
-            mmap_type_name[region->type]);
-
-        switch (region->type) {
-        case MOD_CMN700_REGION_TYPE_CCG:
-            /*
-             * Configure memory region
-             */
-            configure_region(
-                rnsam,
-                region_io_count,
-                region->base,
-                region->size,
-                SAM_NODE_TYPE_CXRA,
-                SAM_TYPE_NON_HASH_MEM_REGION);
-
-            /*
-             * Configure target node
-             */
-            cxra_ldid = config->ccg_config_table[idx].ldid;
-            cxra_node_id = ctx->ccg_ra_reg_table[cxra_ldid].node_id;
-            group = region_io_count /
-                CMN700_RNSAM_NON_HASH_TGT_NODEID_ENTRIES_PER_GROUP;
-            bit_pos = CMN700_RNSAM_NON_HASH_TGT_NODEID_ENTRY_BITS_WIDTH *
-                (region_io_count %
-                 CMN700_RNSAM_NON_HASH_TGT_NODEID_ENTRIES_PER_GROUP);
-
-            rnsam->NON_HASH_TGT_NODEID[group] &=
-                ~(CMN700_RNSAM_NON_HASH_TGT_NODEID_ENTRY_MASK << bit_pos);
-            rnsam->NON_HASH_TGT_NODEID[group] |=
-                (cxra_node_id & CMN700_RNSAM_NON_HASH_TGT_NODEID_ENTRY_MASK)
-                << bit_pos;
-
-            region_io_count++;
-            break;
-
-        default:
-            fwk_unexpected();
-            return FWK_E_DATA;
-        }
+	FWK_LOG_INFO(
+	    MOD_NAME "  [0x%" PRIx64 " - 0x%" PRIx64 "] %s",
+	    region->base,
+	    region->base + region->size - 1,
+	    mmap_type_name[region->type]);
+
+	switch (region->type) {
+	case MOD_CMN700_REGION_TYPE_CCG:
+	    /*
+	     * Configure memory region
+	     */
+	    configure_region(
+		rnsam,
+		region_io_count,
+		region->base,
+		region->size,
+		SAM_NODE_TYPE_CXRA,
+		SAM_TYPE_NON_HASH_MEM_REGION);
+
+	    /*
+	     * Configure target node
+	     */
+	    cxra_ldid = config->ccg_config_table[idx].ldid;
+	    cxra_node_id = ctx->ccg_ra_reg_table[cxra_ldid].node_id;
+	    group = region_io_count /
+		CMN700_RNSAM_NON_HASH_TGT_NODEID_ENTRIES_PER_GROUP;
+	    bit_pos = CMN700_RNSAM_NON_HASH_TGT_NODEID_ENTRY_BITS_WIDTH *
+		(region_io_count %
+		 CMN700_RNSAM_NON_HASH_TGT_NODEID_ENTRIES_PER_GROUP);
+
+	    rnsam->NON_HASH_TGT_NODEID[group] &=
+		~(CMN700_RNSAM_NON_HASH_TGT_NODEID_ENTRY_MASK << bit_pos);
+	    rnsam->NON_HASH_TGT_NODEID[group] |=
+		(cxra_node_id & CMN700_RNSAM_NON_HASH_TGT_NODEID_ENTRY_MASK)
+		<< bit_pos;
+
+	    region_io_count++;
+	    break;
+
+	default:
+	    fwk_unexpected();
+	    return FWK_E_DATA;
+	}
     }
     /*
      * If CAL mode is enabled, then only the even numbered HN-F nodes are
@@ -743,66 +743,66 @@ static int cmn700_setup_sam(struct cmn70
      * half if CAL mode is enabled.
      */
     if (config->hnf_cal_mode)
-        hnf_count = ctx->hnf_count / 2;
+	hnf_count = ctx->hnf_count / 2;
     else
-        hnf_count = ctx->hnf_count;
+	hnf_count = ctx->hnf_count;
 
     /*
      * If CAL mode is enabled by the configuration program the SCG CAL Mode
      * enable register.
      */
     if (config->hnf_cal_mode) {
-        for (region_idx = 0; region_idx < MAX_SCG_COUNT; region_idx++)
-            rnsam->SYS_CACHE_GRP_CAL_MODE |= scg_regions_enabled[region_idx] *
-                (CMN700_RNSAM_SCG_HNF_CAL_MODE_EN
-                 << (region_idx * CMN700_RNSAM_SCG_HNF_CAL_MODE_SHIFT));
+	for (region_idx = 0; region_idx < MAX_SCG_COUNT; region_idx++)
+	    rnsam->SYS_CACHE_GRP_CAL_MODE |= scg_regions_enabled[region_idx] *
+		(CMN700_RNSAM_SCG_HNF_CAL_MODE_EN
+		 << (region_idx * CMN700_RNSAM_SCG_HNF_CAL_MODE_SHIFT));
     }
 
     /* Hierarchical Hashing support */
     if (config->hierarchical_hashing_enable) {
-        hier_hash_cfg = &(config->hierarchical_hashing_config);
+	hier_hash_cfg = &(config->hierarchical_hashing_config);
 
-        /* Total number of HN-F clusters */
-        hnf_cluster_count = hier_hash_cfg->hnf_cluster_count;
+	/* Total number of HN-F clusters */
+	hnf_cluster_count = hier_hash_cfg->hnf_cluster_count;
 
-        /* Number of HN-Fs in a cluster */
-        hnf_count_per_cluster =
-            (hnf_count / hnf_cluster_count) / region_sys_count;
-
-        /*
-         * For each SCG/HTG region, configure the hierarchical hashing mode with
-         * number of clusters, hnf count per cluster, hashing address bits etc.
-         * and enable hierarchical hashing for each SCG/HTG region.
-         */
-        for (region_idx = 0; region_idx < region_sys_count; region_idx++) {
-            rnsam->HASHED_TARGET_GRP_HASH_CNTL[region_idx] =
-                ((CMN700_RNSAM_HIERARCHICAL_HASH_EN_MASK
-                  << CMN700_RNSAM_HIERARCHICAL_HASH_EN_POS) |
-                 (fwk_math_log2(hnf_count_per_cluster)
-                  << CMN700_RNSAM_HIER_ENABLE_ADDRESS_STRIPING_POS) |
-                 (hnf_cluster_count << CMN700_RNSAM_HIER_HASH_CLUSTERS_POS) |
-                 (hnf_count_per_cluster << CMN700_RNSAM_HIER_HASH_NODES_POS));
-
-            group =
-                region_idx / CMN700_RNSAM_SYS_CACHE_GRP_SN_ATTR_ENTRIES_PER_GRP;
-            rnsam->SYS_CACHE_GRP_SN_ATTR[group] |= hier_hash_cfg->sn_mode
-                << CMN700_RNSAM_SN_MODE_SYS_CACHE_POS(region_idx);
-
-            group = region_idx /
-                CMN700_RNSAM_SYS_CACHE_GRP_SN_SAM_CFG_ENTRIES_PER_GRP;
-            rnsam->SYS_CACHE_GRP_SN_SAM_CFG[group] |=
-                ((hier_hash_cfg->top_address_bit0
-                  << CMN700_RNSAM_TOP_ADDRESS_BIT0_POS(region_idx)) |
-                 (hier_hash_cfg->top_address_bit1
-                  << CMN700_RNSAM_TOP_ADDRESS_BIT1_POS(region_idx)) |
-                 (hier_hash_cfg->top_address_bit2
-                  << CMN700_RNSAM_TOP_ADDRESS_BIT2_POS(region_idx)));
-        }
+	/* Number of HN-Fs in a cluster */
+	hnf_count_per_cluster =
+	    (hnf_count / hnf_cluster_count) / region_sys_count;
+
+	/*
+	 * For each SCG/HTG region, configure the hierarchical hashing mode with
+	 * number of clusters, hnf count per cluster, hashing address bits etc.
+	 * and enable hierarchical hashing for each SCG/HTG region.
+	 */
+	for (region_idx = 0; region_idx < region_sys_count; region_idx++) {
+	    rnsam->HASHED_TARGET_GRP_HASH_CNTL[region_idx] =
+		((CMN700_RNSAM_HIERARCHICAL_HASH_EN_MASK
+		  << CMN700_RNSAM_HIERARCHICAL_HASH_EN_POS) |
+		 (fwk_math_log2(hnf_count_per_cluster)
+		  << CMN700_RNSAM_HIER_ENABLE_ADDRESS_STRIPING_POS) |
+		 (hnf_cluster_count << CMN700_RNSAM_HIER_HASH_CLUSTERS_POS) |
+		 (hnf_count_per_cluster << CMN700_RNSAM_HIER_HASH_NODES_POS));
+
+	    group =
+		region_idx / CMN700_RNSAM_SYS_CACHE_GRP_SN_ATTR_ENTRIES_PER_GRP;
+	    rnsam->SYS_CACHE_GRP_SN_ATTR[group] |= hier_hash_cfg->sn_mode
+		<< CMN700_RNSAM_SN_MODE_SYS_CACHE_POS(region_idx);
+
+	    group = region_idx /
+		CMN700_RNSAM_SYS_CACHE_GRP_SN_SAM_CFG_ENTRIES_PER_GRP;
+	    rnsam->SYS_CACHE_GRP_SN_SAM_CFG[group] |=
+		((hier_hash_cfg->top_address_bit0
+		  << CMN700_RNSAM_TOP_ADDRESS_BIT0_POS(region_idx)) |
+		 (hier_hash_cfg->top_address_bit1
+		  << CMN700_RNSAM_TOP_ADDRESS_BIT1_POS(region_idx)) |
+		 (hier_hash_cfg->top_address_bit2
+		  << CMN700_RNSAM_TOP_ADDRESS_BIT2_POS(region_idx)));
+	}
     }
 
     /* Enable RNSAM */
     rnsam->STATUS = (rnsam->STATUS | CMN700_RNSAM_STATUS_UNSTALL) &
-        ~(CMN700_RNSAM_STATUS_USE_DEFAULT_TARGET_ID);
+	~(CMN700_RNSAM_STATUS_USE_DEFAULT_TARGET_ID);
     __sync_synchronize();
 
     return FWK_SUCCESS;
@@ -814,58 +814,58 @@ static int cmn700_setup(void)
     int status;
 
     if (!ctx->initialized) {
-        status = cmn700_discovery();
-        if (status != FWK_SUCCESS)
-            return FWK_SUCCESS;
-
-        /*
-         * Allocate resources based on the discovery
-         */
-
-        /* Pointers for the internal RN-SAM nodes */
-        if (ctx->internal_rnsam_count != 0) {
-            ctx->internal_rnsam_table = fwk_mm_calloc(
-                ctx->internal_rnsam_count, sizeof(*ctx->internal_rnsam_table));
-        }
-
-        /* Tuples for the external RN-RAM nodes (including their node IDs) */
-        if (ctx->external_rnsam_count != 0) {
-            ctx->external_rnsam_table = fwk_mm_calloc(
-                ctx->external_rnsam_count, sizeof(*ctx->external_rnsam_table));
-        }
-
-        /* Cache groups */
-        if (ctx->hnf_count != 0) {
-            /*
-             * Allocate enough group descriptors to accommodate all expected
-             * HN-F nodes in the system.
-             */
-            ctx->hnf_node =
-                fwk_mm_calloc(ctx->hnf_count, sizeof(*ctx->hnf_node));
-            hnf_node_pos = fwk_mm_calloc(ctx->hnf_count, sizeof(*hnf_node_pos));
-            if (ctx->hnf_node == NULL)
-                return FWK_E_NOMEM;
-            ctx->hnf_cache_group = fwk_mm_calloc(
-                cmn700_hnf_cache_group_count(ctx->hnf_count),
-                sizeof(*ctx->hnf_cache_group));
-        }
-
-        /* Allocate resource for the CCG nodes */
-        if (ctx->ccg_node_count != 0) {
-            ctx->ccg_ra_reg_table = fwk_mm_calloc(
-                ctx->ccg_node_count, sizeof(*ctx->ccg_ra_reg_table));
-            ctx->ccg_ha_reg_table = fwk_mm_calloc(
-                ctx->ccg_node_count, sizeof(*ctx->ccg_ha_reg_table));
-            ctx->ccla_reg_table = fwk_mm_calloc(
-                ctx->ccg_node_count, sizeof(*ctx->ccla_reg_table));
-        }
+	status = cmn700_discovery();
+	if (status != FWK_SUCCESS)
+	    return FWK_SUCCESS;
+
+	/*
+	 * Allocate resources based on the discovery
+	 */
+
+	/* Pointers for the internal RN-SAM nodes */
+	if (ctx->internal_rnsam_count != 0) {
+	    ctx->internal_rnsam_table = fwk_mm_calloc(
+		ctx->internal_rnsam_count, sizeof(*ctx->internal_rnsam_table));
+	}
+
+	/* Tuples for the external RN-RAM nodes (including their node IDs) */
+	if (ctx->external_rnsam_count != 0) {
+	    ctx->external_rnsam_table = fwk_mm_calloc(
+		ctx->external_rnsam_count, sizeof(*ctx->external_rnsam_table));
+	}
+
+	/* Cache groups */
+	if (ctx->hnf_count != 0) {
+	    /*
+	     * Allocate enough group descriptors to accommodate all expected
+	     * HN-F nodes in the system.
+	     */
+	    ctx->hnf_node =
+		fwk_mm_calloc(ctx->hnf_count, sizeof(*ctx->hnf_node));
+	    hnf_node_pos = fwk_mm_calloc(ctx->hnf_count, sizeof(*hnf_node_pos));
+	    if (ctx->hnf_node == NULL)
+		return FWK_E_NOMEM;
+	    ctx->hnf_cache_group = fwk_mm_calloc(
+		cmn700_hnf_cache_group_count(ctx->hnf_count),
+		sizeof(*ctx->hnf_cache_group));
+	}
+
+	/* Allocate resource for the CCG nodes */
+	if (ctx->ccg_node_count != 0) {
+	    ctx->ccg_ra_reg_table = fwk_mm_calloc(
+		ctx->ccg_node_count, sizeof(*ctx->ccg_ra_reg_table));
+	    ctx->ccg_ha_reg_table = fwk_mm_calloc(
+		ctx->ccg_node_count, sizeof(*ctx->ccg_ha_reg_table));
+	    ctx->ccla_reg_table = fwk_mm_calloc(
+		ctx->ccg_node_count, sizeof(*ctx->ccla_reg_table));
+	}
     }
 
     cmn700_configure();
 
     /* Setup internal RN-SAM nodes */
     for (rnsam_idx = 0; rnsam_idx < ctx->internal_rnsam_count; rnsam_idx++)
-        cmn700_setup_sam(ctx->internal_rnsam_table[rnsam_idx]);
+	cmn700_setup_sam(ctx->internal_rnsam_table[rnsam_idx]);
 
     FWK_LOG_INFO(MOD_NAME "Done");
 
@@ -884,7 +884,7 @@ static void cmn700_ccg_setup(void)
 
     /* Do configuration for CCG Nodes and enable the links */
     for (idx = 0; idx < config->ccg_table_count; idx++) {
-        ccg_setup(chip_id, ctx, &config->ccg_config_table[idx]);
+	ccg_setup(chip_id, ctx, &config->ccg_config_table[idx]);
     }
 
     /*
@@ -892,9 +892,9 @@ static void cmn700_ccg_setup(void)
      * multichip SMP mode operation.
      */
     for (idx = 0; idx < config->ccg_table_count; idx++) {
-        ccg_exchange_protocol_credit(ctx, &config->ccg_config_table[idx]);
-        ccg_enter_system_coherency(ctx, &config->ccg_config_table[idx]);
-        ccg_enter_dvm_domain(ctx, &config->ccg_config_table[idx]);
+	ccg_exchange_protocol_credit(ctx, &config->ccg_config_table[idx]);
+	ccg_enter_system_coherency(ctx, &config->ccg_config_table[idx]);
+	ccg_enter_dvm_domain(ctx, &config->ccg_config_table[idx]);
     }
 }
 
@@ -909,7 +909,7 @@ static int cmn700_init(
 {
     /* Atleast one device should be passed as element */
     if (device_count == 0)
-        return FWK_E_DATA;
+	return FWK_E_DATA;
 
     /* Allocate space for the device context table */
     ctx = fwk_mm_calloc(device_count, sizeof(struct cmn700_device_ctx));
@@ -930,18 +930,18 @@ static int cmn700_device_init(
     device_ctx->config = data;
 
     if (device_ctx->config->base == 0)
-        return FWK_E_DATA;
+	return FWK_E_DATA;
 
     if ((device_ctx->config->mesh_size_x == 0) ||
-        (device_ctx->config->mesh_size_x > CMN700_MESH_X_MAX))
-        return FWK_E_DATA;
+	(device_ctx->config->mesh_size_x > CMN700_MESH_X_MAX))
+	return FWK_E_DATA;
 
     if ((device_ctx->config->mesh_size_y == 0) ||
-        (device_ctx->config->mesh_size_y > CMN700_MESH_Y_MAX))
-        return FWK_E_DATA;
+	(device_ctx->config->mesh_size_y > CMN700_MESH_Y_MAX))
+	return FWK_E_DATA;
 
     if (device_ctx->config->snf_count > CMN700_HNF_CACHE_GROUP_ENTRIES_MAX)
-        return FWK_E_DATA;
+	return FWK_E_DATA;
 
     device_ctx->root = (struct cmn700_cfgm_reg *)device_ctx->config->base;
 
@@ -954,25 +954,25 @@ static int cmn700_bind(fwk_id_t id, unsi
     struct cmn700_device_ctx *device_ctx;
 
     if (fwk_id_is_type(id, FWK_ID_TYPE_MODULE)) {
-        /* Bind to system info module to obtain multi-chip info */
-        status = fwk_module_bind(
-            FWK_ID_MODULE(FWK_MODULE_IDX_SYSTEM_INFO),
-            FWK_ID_API(FWK_MODULE_IDX_SYSTEM_INFO, MOD_SYSTEM_INFO_GET_API_IDX),
-            &system_info_api);
-        return status;
+	/* Bind to system info module to obtain multi-chip info */
+	status = fwk_module_bind(
+	    FWK_ID_MODULE(FWK_MODULE_IDX_SYSTEM_INFO),
+	    FWK_ID_API(FWK_MODULE_IDX_SYSTEM_INFO, MOD_SYSTEM_INFO_GET_API_IDX),
+	    &system_info_api);
+	return status;
     }
 
     /* Use second round only (round numbering is zero-indexed) */
     if (round == 1) {
-        device_ctx = ctx + fwk_id_get_element_idx(id);
+	device_ctx = ctx + fwk_id_get_element_idx(id);
 
-        /* Bind to the timer component */
-        status = fwk_module_bind(
-            FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
-            FWK_ID_API(FWK_MODULE_IDX_TIMER, MOD_TIMER_API_IDX_TIMER),
-            &device_ctx->timer_api);
-        if (status != FWK_SUCCESS)
-            return FWK_E_PANIC;
+	/* Bind to the timer component */
+	status = fwk_module_bind(
+	    FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
+	    FWK_ID_API(FWK_MODULE_IDX_TIMER, MOD_TIMER_API_IDX_TIMER),
+	    &device_ctx->timer_api);
+	if (status != FWK_SUCCESS)
+	    return FWK_E_PANIC;
     }
 
     return FWK_SUCCESS;
@@ -984,35 +984,35 @@ int cmn700_start(fwk_id_t id)
 
     /* No need to do anything for element */
     if (!fwk_module_is_valid_element_id(id))
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 
     status = system_info_api->get_system_info(&system_info);
     if (status == FWK_SUCCESS) {
-        chip_id = system_info->chip_id;
-        multi_chip_mode = system_info->multi_chip_mode;
+	chip_id = system_info->chip_id;
+	multi_chip_mode = system_info->multi_chip_mode;
     }
 
     /* No need to anything for other elements */
     if (fwk_id_get_element_idx(id) != chip_id)
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 
     /* Pickup the context based on the chip_id */
     ctx = ctx + fwk_id_get_element_idx(id);
 
     FWK_LOG_INFO(
-        MOD_NAME "Multichip mode: %s",
-        multi_chip_mode ? "Enabled" : "Disabled");
+	MOD_NAME "Multichip mode: %s",
+	multi_chip_mode ? "Enabled" : "Disabled");
     FWK_LOG_INFO(MOD_NAME "Chip ID: %d", chip_id);
 
     if (fwk_id_is_equal(ctx->config->clock_id, FWK_ID_NONE)) {
-        cmn700_setup();
-        cmn700_ccg_setup();
-        return FWK_SUCCESS;
+	cmn700_setup();
+	cmn700_ccg_setup();
+	return FWK_SUCCESS;
     }
 
     /* Register the module for clock state notifications */
     return fwk_notification_subscribe(
-        mod_clock_notification_id_state_changed, ctx->config->clock_id, id);
+	mod_clock_notification_id_state_changed, ctx->config->clock_id, id);
 }
 
 static int cmn700_process_notification(
@@ -1022,13 +1022,13 @@ static int cmn700_process_notification(
     struct clock_notification_params *params;
 
     fwk_assert(
-        fwk_id_is_equal(event->id, mod_clock_notification_id_state_changed));
+	fwk_id_is_equal(event->id, mod_clock_notification_id_state_changed));
     fwk_assert(fwk_id_is_type(event->target_id, FWK_ID_TYPE_ELEMENT));
 
     params = (struct clock_notification_params *)event->params;
     if (params->new_state == MOD_CLOCK_STATE_RUNNING) {
-        cmn700_setup();
-        cmn700_ccg_setup();
+	cmn700_setup();
+	cmn700_ccg_setup();
     }
 
     return FWK_SUCCESS;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cmn_booker/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/cmn_booker/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cmn_booker/CMakeLists.txt
@@ -7,13 +7,13 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include"
-                           PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include"
+			   PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src")
 
 target_sources(
     ${SCP_MODULE_TARGET}
     PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_cmn_booker.c"
-            "${CMAKE_CURRENT_SOURCE_DIR}/src/cmn_booker.c")
+	    "${CMAKE_CURRENT_SOURCE_DIR}/src/cmn_booker.c")
 
 target_link_libraries(${SCP_MODULE_TARGET}
     PRIVATE module-clock module-ppu-v1)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cmn_booker/src/cmn_booker.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/cmn_booker/src/cmn_booker.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cmn_booker/src/cmn_booker.c
@@ -36,14 +36,14 @@ unsigned int get_node_id(void *node_base
 {
     struct node_header *node = node_base;
     return (node->NODE_INFO & CMN_BOOKER_NODE_INFO_ID) >>
-           CMN_BOOKER_NODE_INFO_ID_POS;
+	   CMN_BOOKER_NODE_INFO_ID_POS;
 }
 
 unsigned int get_node_logical_id(void *node_base)
 {
     struct node_header *node = node_base;
     return (node->NODE_INFO & CMN_BOOKER_NODE_INFO_LOGICAL_ID) >>
-           CMN_BOOKER_NODE_INFO_LOGICAL_ID_POS;
+	   CMN_BOOKER_NODE_INFO_LOGICAL_ID_POS;
 }
 
 void *get_child_node(uintptr_t base, void *node_base, unsigned int child_index)
@@ -68,8 +68,8 @@ unsigned int get_child_node_id(void *nod
     unsigned int node_id;
 
     node_pointer = (node->CHILD_POINTER[child_index] &
-                    CMN_BOOKER_CHILD_POINTER_EXT_NODE_POINTER) >>
-                   CMN_BOOKER_CHILD_POINTER_EXT_NODE_POINTER_POS;
+		    CMN_BOOKER_CHILD_POINTER_EXT_NODE_POINTER) >>
+		   CMN_BOOKER_CHILD_POINTER_EXT_NODE_POINTER_POS;
 
     /*
      * For mesh widths using 2 bits each for X,Y encoding:
@@ -85,8 +85,8 @@ unsigned int get_child_node_id(void *nod
      * NodeID[8:6] = NODE POINTER[11:9]
      */
     node_id = (((node_pointer >> 6) & 0xff) << 3) |
-              ((node_pointer & 0x1) << 2) |
-              ((node_pointer >> 2) & 0x3);
+	      ((node_pointer & 0x1) << 2) |
+	      ((node_pointer >> 2) & 0x3);
 
     return node_id;
 }
@@ -100,14 +100,14 @@ bool is_child_external(void *node_base,
 bool get_port_number(unsigned int child_node_id)
 {
     return (child_node_id >> CMN_BOOKER_NODE_ID_PORT_POS) &
-           CMN_BOOKER_NODE_ID_PORT_MASK;
+	   CMN_BOOKER_NODE_ID_PORT_MASK;
 }
 
 unsigned int get_device_type(void *mxp_base, bool port)
 {
     struct cmn_booker_mxp_reg *mxp = mxp_base;
     return mxp->PORT_CONNECT_INFO[port] &
-           CMN_BOOKER_MXP_PORT_CONNECT_INFO_DEVICE_TYPE_MASK;
+	   CMN_BOOKER_MXP_PORT_CONNECT_INFO_DEVICE_TYPE_MASK;
 }
 
 uint64_t sam_encode_region_size(uint64_t size)
@@ -138,7 +138,7 @@ void configure_region(volatile uint64_t
     value = CMN_BOOKER_RNSAM_REGION_ENTRY_VALID;
     value |= node_type << CMN_BOOKER_RNSAM_REGION_ENTRY_TYPE_POS;
     value |= sam_encode_region_size(size) <<
-             CMN_BOOKER_RNSAM_REGION_ENTRY_SIZE_POS;
+	     CMN_BOOKER_RNSAM_REGION_ENTRY_SIZE_POS;
     value |= (base / SAM_GRANULARITY) << CMN_BOOKER_RNSAM_REGION_ENTRY_BASE_POS;
 
     *reg = value;
@@ -172,12 +172,12 @@ const char *get_node_type_name(enum node
 {
     /* Base node IDs */
     if (node_type <= NODE_TYPE_MTU)
-        return type_to_name[node_type];
+	return type_to_name[node_type];
 
     /* CML node IDs */
     if ((node_type >= NODE_TYPE_CML_BASE) &&
-        (node_type <= NODE_TYPE_CXLA))
-        return type_to_name_cml[node_type - NODE_TYPE_CML_BASE];
+	(node_type <= NODE_TYPE_CXLA))
+	return type_to_name_cml[node_type - NODE_TYPE_CML_BASE];
 
     /* Invalid node IDs */
     return type_to_name[NODE_TYPE_INVALID];
@@ -187,7 +187,7 @@ unsigned int get_node_pos_x(void *node_b
 {
     struct node_header *node = node_base;
     return (get_node_id(node) >> (CMN_BOOKER_NODE_ID_Y_POS + encoding_bits)) &
-           mask_bits;
+	   mask_bits;
 }
 
 unsigned int get_node_pos_y(void *node_base)
@@ -218,24 +218,24 @@ struct cmn_booker_cfgm_reg *get_root_nod
     mask_bits = (1 << encoding_bits) - 1;
     node_pos_y = (hnd_node_id >> CMN_BOOKER_NODE_ID_Y_POS) & mask_bits;
     node_pos_x = (hnd_node_id >> (CMN_BOOKER_NODE_ID_Y_POS + encoding_bits)) &
-                 mask_bits;
+		 mask_bits;
     node_port = (hnd_node_id >> CMN_BOOKER_NODE_ID_PORT_POS) &
-                CMN_BOOKER_NODE_ID_PORT_MASK;
+		CMN_BOOKER_NODE_ID_PORT_MASK;
 
     /* Calculate node address offset */
     if (ports_per_xp > 4) {
-        // Single XP configuration, upto 6 device ports allowed
-        offset = ((node_port & 0x3) << CMN_BOOKER_ROOT_NODE_OFFSET_PORT_POS);
+	// Single XP configuration, upto 6 device ports allowed
+	offset = ((node_port & 0x3) << CMN_BOOKER_ROOT_NODE_OFFSET_PORT_POS);
     } else if (ports_per_xp > 2) {
-        // XPs which have more than 2 device ports
-        offset = (((node_port) >> 1) << CMN_BOOKER_ROOT_NODE_OFFSET_PORT_POS);
+	// XPs which have more than 2 device ports
+	offset = (((node_port) >> 1) << CMN_BOOKER_ROOT_NODE_OFFSET_PORT_POS);
     } else {
-        offset = (node_port << CMN_BOOKER_ROOT_NODE_OFFSET_PORT_POS);
+	offset = (node_port << CMN_BOOKER_ROOT_NODE_OFFSET_PORT_POS);
     }
     /* Calculate node address offset */
     offset = (node_pos_y << CMN_BOOKER_ROOT_NODE_OFFSET_Y_POS) |
-        (node_pos_x << (CMN_BOOKER_ROOT_NODE_OFFSET_Y_POS + encoding_bits)) |
-        offset;
+	(node_pos_x << (CMN_BOOKER_ROOT_NODE_OFFSET_Y_POS + encoding_bits)) |
+	offset;
 
     return (struct cmn_booker_cfgm_reg *)(base + offset);
 }
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cmn_booker/src/cmn_booker.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/cmn_booker/src/cmn_booker.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cmn_booker/src/cmn_booker.h
@@ -73,9 +73,9 @@ enum device_type {
 /* Common node header */
 struct node_header {
     FWK_R uint64_t NODE_INFO;
-          uint8_t  RESERVED0[0x80 - 0x8];
+	  uint8_t  RESERVED0[0x80 - 0x8];
     FWK_R uint64_t CHILD_INFO;
-          uint8_t  RESERVED1[0x100 - 0x88];
+	  uint8_t  RESERVED1[0x100 - 0x88];
     FWK_R uint64_t CHILD_POINTER[256];
 };
 
@@ -91,25 +91,25 @@ enum sam_node_type {
  */
 struct cmn_booker_rnsam_reg {
     FWK_R  uint64_t NODE_INFO;
-           uint8_t  RESERVED0[0x80 - 0x8];
+	   uint8_t  RESERVED0[0x80 - 0x8];
     FWK_R  uint64_t CHILD_INFO;
-           uint8_t  RESERVED1[0x900 - 0x88];
+	   uint8_t  RESERVED1[0x900 - 0x88];
     FWK_R  uint64_t UNIT_INFO;
-           uint8_t  RESERVED2[0xC00 - 0x908];
+	   uint8_t  RESERVED2[0xC00 - 0x908];
     FWK_RW uint64_t NON_HASH_MEM_REGION[20];
-           uint8_t  RESERVED3[0xD80 - 0xCA0];
+	   uint8_t  RESERVED3[0xD80 - 0xCA0];
     FWK_RW uint64_t NON_HASH_TGT_NODEID[5];
-           uint8_t  RESERVED4[0xE00 - 0xDA8];
+	   uint8_t  RESERVED4[0xE00 - 0xDA8];
     FWK_RW uint64_t SYS_CACHE_GRP_REGION[4];
-           uint8_t  RESERVED5[0xEA0 - 0xE20];
+	   uint8_t  RESERVED5[0xEA0 - 0xE20];
     FWK_RW uint64_t SYS_CACHE_GRP_HN_COUNT;
-           uint8_t  RESERVED6[0xF00 - 0xEA8];
+	   uint8_t  RESERVED6[0xF00 - 0xEA8];
     FWK_RW uint64_t SYS_CACHE_GRP_HN_NODEID[16];
-           uint8_t  RESERVED7[0x1000 - 0xF80];
+	   uint8_t  RESERVED7[0x1000 - 0xF80];
     FWK_RW uint64_t SYS_CACHE_GRP_SN_NODEID[16];
-           uint8_t  RESERVED8[0x1100 - 0x1080];
+	   uint8_t  RESERVED8[0x1100 - 0x1080];
     FWK_RW uint64_t STATUS;
-           uint8_t  RESERVED9[0x1120 - 0x1108];
+	   uint8_t  RESERVED9[0x1120 - 0x1108];
     FWK_RW uint64_t SYS_CACHE_GRP_CAL_MODE;
 };
 
@@ -118,14 +118,14 @@ struct cmn_booker_rnsam_reg {
  */
 struct cmn_booker_hnf_reg {
     FWK_R  uint64_t NODE_INFO;
-           uint8_t  RESERVED0[0x80 - 0x8];
+	   uint8_t  RESERVED0[0x80 - 0x8];
     FWK_R  uint64_t CHILD_INFO;
-           uint8_t  RESERVED1[0x900 - 0x88];
+	   uint8_t  RESERVED1[0x900 - 0x88];
     FWK_R  uint64_t UNIT_INFO;
-           uint8_t  RESERVED2[0xD00 - 0x908];
+	   uint8_t  RESERVED2[0xD00 - 0x908];
     FWK_RW uint64_t SAM_CONTROL;
     FWK_RW uint64_t SAM_MEMREGION[2];
-           uint8_t  RESERVED8[0x1C00 - 0xD18];
+	   uint8_t  RESERVED8[0x1C00 - 0xD18];
     FWK_RW uint64_t PPU_PWPR;
 };
 
@@ -136,7 +136,7 @@ struct cmn_booker_cfgm_reg {
     FWK_R  uint64_t NODE_INFO;
     FWK_RW uint64_t PERIPH_ID[4];
     FWK_RW uint64_t COMPONENT_ID[2];
-           uint8_t  RESERVED0[0x80 - 0x38];
+	   uint8_t  RESERVED0[0x80 - 0x38];
     FWK_R  uint64_t CHILD_INFO;
 };
 
@@ -146,9 +146,9 @@ struct cmn_booker_cfgm_reg {
 struct cmn_booker_mxp_reg {
     FWK_R  uint64_t NODE_INFO;
     FWK_R  uint64_t PORT_CONNECT_INFO[2];
-           uint8_t  RESERVED0[0x80 - 0x18];
+	   uint8_t  RESERVED0[0x80 - 0x18];
     FWK_R  uint64_t CHILD_INFO;
-           uint8_t  RESERVED1[0x100 - 0x88];
+	   uint8_t  RESERVED1[0x100 - 0x88];
     FWK_R  uint64_t CHILD_POINTER[16];
 };
 
@@ -157,26 +157,26 @@ struct cmn_booker_mxp_reg {
  */
 struct cmn_booker_hni_reg {
     FWK_R  uint64_t NODE_INFO;
-           uint8_t  RESERVED0[0x80 - 0x8];
+	   uint8_t  RESERVED0[0x80 - 0x8];
     FWK_R  uint64_t CHILD_INFO;
-           uint8_t  RESERVED1[0x900 - 0x88];
+	   uint8_t  RESERVED1[0x900 - 0x88];
     FWK_R  uint64_t UNIT_INFO;
-           uint8_t  RESERVED2[0x980 - 0x908];
+	   uint8_t  RESERVED2[0x980 - 0x908];
     FWK_RW uint64_t SECURE_REGISTER_GROUPS_OVERRIDE;
-           uint8_t  RESERVED3[0xA00 - 0x988];
+	   uint8_t  RESERVED3[0xA00 - 0x988];
     FWK_RW uint64_t CFG_CTL;
     FWK_RW uint64_t AUX_CTL;
-           uint8_t  RESERVED4[0xC00 - 0xA10];
+	   uint8_t  RESERVED4[0xC00 - 0xA10];
     FWK_RW uint64_t SAM_ADDRREGION_CFG[4];
-           uint8_t  RESERVED5[0x2000 - 0xC20];
+	   uint8_t  RESERVED5[0x2000 - 0xC20];
     FWK_RW uint64_t PMU_EVENT_SEL;
-           uint8_t  RESERVED6[0x3000 - 0x2008];
+	   uint8_t  RESERVED6[0x3000 - 0x2008];
     FWK_R  uint64_t ERRFR;
     FWK_RW uint64_t ERRCTL;
     FWK_RW uint64_t ERRSTATUS;
     FWK_RW uint64_t ERRADDR;
     FWK_RW uint64_t ERRMISC;
-           uint8_t  RESERVED7[0x3100 - 0x3028];
+	   uint8_t  RESERVED7[0x3100 - 0x3028];
     FWK_R  uint64_t ERRFR_NS;
     FWK_RW uint64_t ERRCTL_NS;
     FWK_RW uint64_t ERRSTATUS_NS;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cmn_booker/src/mod_cmn_booker.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/cmn_booker/src/mod_cmn_booker.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cmn_booker/src/mod_cmn_booker.c
@@ -59,34 +59,34 @@ static void process_node_hnf(struct cmn_
      * sys_cache_grp_hn_nodeid registers.
      */
     if (config->hnf_cal_mode == true && (node_id % 2 == 1)) {
-        /* Factor to manipulate the group and bit_pos */
-        cal_mode_factor = 2;
+	/* Factor to manipulate the group and bit_pos */
+	cal_mode_factor = 2;
     }
 
     fwk_assert(logical_id < config->snf_count);
 
     group = logical_id /
-            (CMN_BOOKER_HNF_CACHE_GROUP_ENTRIES_PER_GROUP * cal_mode_factor);
+	    (CMN_BOOKER_HNF_CACHE_GROUP_ENTRIES_PER_GROUP * cal_mode_factor);
     bit_pos = (CMN_BOOKER_HNF_CACHE_GROUP_ENTRY_BITS_WIDTH / cal_mode_factor) *
-              ((logical_id %
-                (CMN_BOOKER_HNF_CACHE_GROUP_ENTRIES_PER_GROUP *
-                 cal_mode_factor)));
+	      ((logical_id %
+		(CMN_BOOKER_HNF_CACHE_GROUP_ENTRIES_PER_GROUP *
+		 cal_mode_factor)));
 
     /*
      * If CAL mode is set, add only even numbered hnd node to
      * sys_cache_grp_hn_nodeid registers
      */
     if (config->hnf_cal_mode == true) {
-        if (node_id % 2 == 0) {
-            ctx->hnf_cache_group[group] += ((uint64_t)get_node_id(hnf)) <<
-                                           bit_pos;
-            ctx->sn_nodeid_group[group] +=
-                ((uint64_t)config->snf_table[logical_id]) << bit_pos;
-        }
+	if (node_id % 2 == 0) {
+	    ctx->hnf_cache_group[group] += ((uint64_t)get_node_id(hnf)) <<
+					   bit_pos;
+	    ctx->sn_nodeid_group[group] +=
+		((uint64_t)config->snf_table[logical_id]) << bit_pos;
+	}
     } else {
-        ctx->hnf_cache_group[group] += ((uint64_t)get_node_id(hnf)) << bit_pos;
-        ctx->sn_nodeid_group[group] +=
-            ((uint64_t)config->snf_table[logical_id]) << bit_pos;
+	ctx->hnf_cache_group[group] += ((uint64_t)get_node_id(hnf)) << bit_pos;
+	ctx->sn_nodeid_group[group] +=
+	    ((uint64_t)config->snf_table[logical_id]) << bit_pos;
     }
 
     /* Set target node */
@@ -96,27 +96,27 @@ static void process_node_hnf(struct cmn_
      * Map sub-regions to this HN-F node
      */
     for (region_idx = 0; region_idx < config->mmap_count; region_idx++) {
-        region = &config->mmap_table[region_idx];
+	region = &config->mmap_table[region_idx];
 
-        /* Skip non sub-regions */
-        if (region->type != MOD_CMN_BOOKER_REGION_TYPE_SYSCACHE_SUB)
-            continue;
-
-        /* Configure sub-region entry */
-        hnf->SAM_MEMREGION[region_sub_count] = region->node_id |
-            (sam_encode_region_size(region->size) <<
-             CMN_BOOKER_HNF_SAM_MEMREGION_SIZE_POS) |
-            ((region->base / SAM_GRANULARITY) <<
-             CMN_BOOKER_HNF_SAM_MEMREGION_BASE_POS) |
-            CMN_BOOKER_HNF_SAM_MEMREGION_VALID;
+	/* Skip non sub-regions */
+	if (region->type != MOD_CMN_BOOKER_REGION_TYPE_SYSCACHE_SUB)
+	    continue;
+
+	/* Configure sub-region entry */
+	hnf->SAM_MEMREGION[region_sub_count] = region->node_id |
+	    (sam_encode_region_size(region->size) <<
+	     CMN_BOOKER_HNF_SAM_MEMREGION_SIZE_POS) |
+	    ((region->base / SAM_GRANULARITY) <<
+	     CMN_BOOKER_HNF_SAM_MEMREGION_BASE_POS) |
+	    CMN_BOOKER_HNF_SAM_MEMREGION_VALID;
 
-        region_sub_count++;
+	region_sub_count++;
     }
 
     /* Configure the system cache RAM PPU */
     hnf->PPU_PWPR = CMN_BOOKER_PPU_PWPR_POLICY_ON |
-                    CMN_BOOKER_PPU_PWPR_OPMODE_FAM |
-                    CMN_BOOKER_PPU_PWPR_DYN_EN;
+		    CMN_BOOKER_PPU_PWPR_OPMODE_FAM |
+		    CMN_BOOKER_PPU_PWPR_DYN_EN;
 }
 
 /*
@@ -143,85 +143,85 @@ static int cmn_booker_discovery(void)
     /* Traverse cross points (XP) */
     xp_count = get_node_child_count(ctx->root);
     for (xp_idx = 0; xp_idx < xp_count; xp_idx++) {
-        xp = get_child_node(config->base, ctx->root, xp_idx);
-        fwk_assert(get_node_type(xp) == NODE_TYPE_XP);
+	xp = get_child_node(config->base, ctx->root, xp_idx);
+	fwk_assert(get_node_type(xp) == NODE_TYPE_XP);
 
-        FWK_LOG_INFO(
-            MOD_NAME "XP (%d, %d) ID:%d, LID:%d",
-            get_node_pos_x(xp),
-            get_node_pos_y(xp),
-            get_node_id(xp),
-            get_node_logical_id(xp));
-
-        /* Traverse nodes */
-        node_count = get_node_child_count(xp);
-        for (node_idx = 0; node_idx < node_count; node_idx++) {
-            node = get_child_node(config->base, xp, node_idx);
-            if (is_child_external(xp, node_idx)) { /* External nodes */
-                xp_port = get_port_number(get_child_node_id(xp, node_idx));
-
-                /*
-                 * If the device type is CXRH, CXHA, or CXRA, then the external
-                 * child node is CXLA as every CXRH, CXHA, or CXRA node has a
-                 * corresponding external CXLA node.
-                 */
-                if ((get_device_type(xp, xp_port) == DEVICE_TYPE_CXRH) ||
-                    (get_device_type(xp, xp_port) == DEVICE_TYPE_CXHA) ||
-                    (get_device_type(xp, xp_port) == DEVICE_TYPE_CXRA)) {
-                    FWK_LOG_INFO(
-                        MOD_NAME "  Found CXLA at node ID: %d",
-                        get_child_node_id(xp, node_idx));
-                } else { /* External RN-SAM Node */
-                    ctx->external_rnsam_count++;
-                    FWK_LOG_INFO(
-                        MOD_NAME "  Found external node ID: %d",
-                        get_child_node_id(xp, node_idx));
-                }
-            } else { /* Internal nodes */
-                switch (get_node_type(node)) {
-                case NODE_TYPE_HN_F:
-                    if (ctx->hnf_count >= MAX_HNF_COUNT) {
-                        FWK_LOG_INFO(
-                            MOD_NAME "  hnf count %d >= max limit (%d)",
-                            ctx->hnf_count,
-                            MAX_HNF_COUNT);
-                        return FWK_E_DATA;
-                    }
-                    ctx->hnf_count++;
-                    break;
-
-                case NODE_TYPE_RN_SAM:
-                    ctx->internal_rnsam_count++;
-                    break;
-
-                default:
-                    /* Nothing to be done for other node types */
-                    break;
-                }
-
-                FWK_LOG_INFO(
-                    MOD_NAME "  %s ID:%d, LID:%d",
-                    get_node_type_name(get_node_type(node)),
-                    get_node_id(node),
-                    get_node_logical_id(node));
-            }
-        }
+	FWK_LOG_INFO(
+	    MOD_NAME "XP (%d, %d) ID:%d, LID:%d",
+	    get_node_pos_x(xp),
+	    get_node_pos_y(xp),
+	    get_node_id(xp),
+	    get_node_logical_id(xp));
+
+	/* Traverse nodes */
+	node_count = get_node_child_count(xp);
+	for (node_idx = 0; node_idx < node_count; node_idx++) {
+	    node = get_child_node(config->base, xp, node_idx);
+	    if (is_child_external(xp, node_idx)) { /* External nodes */
+		xp_port = get_port_number(get_child_node_id(xp, node_idx));
+
+		/*
+		 * If the device type is CXRH, CXHA, or CXRA, then the external
+		 * child node is CXLA as every CXRH, CXHA, or CXRA node has a
+		 * corresponding external CXLA node.
+		 */
+		if ((get_device_type(xp, xp_port) == DEVICE_TYPE_CXRH) ||
+		    (get_device_type(xp, xp_port) == DEVICE_TYPE_CXHA) ||
+		    (get_device_type(xp, xp_port) == DEVICE_TYPE_CXRA)) {
+		    FWK_LOG_INFO(
+			MOD_NAME "  Found CXLA at node ID: %d",
+			get_child_node_id(xp, node_idx));
+		} else { /* External RN-SAM Node */
+		    ctx->external_rnsam_count++;
+		    FWK_LOG_INFO(
+			MOD_NAME "  Found external node ID: %d",
+			get_child_node_id(xp, node_idx));
+		}
+	    } else { /* Internal nodes */
+		switch (get_node_type(node)) {
+		case NODE_TYPE_HN_F:
+		    if (ctx->hnf_count >= MAX_HNF_COUNT) {
+			FWK_LOG_INFO(
+			    MOD_NAME "  hnf count %d >= max limit (%d)",
+			    ctx->hnf_count,
+			    MAX_HNF_COUNT);
+			return FWK_E_DATA;
+		    }
+		    ctx->hnf_count++;
+		    break;
+
+		case NODE_TYPE_RN_SAM:
+		    ctx->internal_rnsam_count++;
+		    break;
+
+		default:
+		    /* Nothing to be done for other node types */
+		    break;
+		}
+
+		FWK_LOG_INFO(
+		    MOD_NAME "  %s ID:%d, LID:%d",
+		    get_node_type_name(get_node_type(node)),
+		    get_node_id(node),
+		    get_node_logical_id(node));
+	    }
+	}
     }
 
     /* When CAL is present, the number of HN-Fs must be even. */
     if ((ctx->hnf_count % 2 != 0) && (config->hnf_cal_mode == true)) {
-        FWK_LOG_ERR(
-            MOD_NAME "hnf count: %d should be even when CAL mode is set",
-            ctx->hnf_count);
-        return FWK_E_DATA;
+	FWK_LOG_ERR(
+	    MOD_NAME "hnf count: %d should be even when CAL mode is set",
+	    ctx->hnf_count);
+	return FWK_E_DATA;
     }
 
     FWK_LOG_INFO(MOD_NAME
-        "Total internal RN-SAM nodes: %d", ctx->internal_rnsam_count);
+	"Total internal RN-SAM nodes: %d", ctx->internal_rnsam_count);
     FWK_LOG_INFO(MOD_NAME
-        "Total external RN-SAM nodes: %d", ctx->external_rnsam_count);
+	"Total external RN-SAM nodes: %d", ctx->external_rnsam_count);
     FWK_LOG_INFO(MOD_NAME
-        "Total HN-F nodes: %d", ctx->hnf_count);
+	"Total HN-F nodes: %d", ctx->hnf_count);
 
     return FWK_SUCCESS;
 }
@@ -247,55 +247,55 @@ static void cmn_booker_configure(void)
     /* Traverse cross points (XP) */
     xp_count = get_node_child_count(ctx->root);
     for (xp_idx = 0; xp_idx < xp_count; xp_idx++) {
-        xp = get_child_node(config->base, ctx->root, xp_idx);
-        fwk_assert(get_node_type(xp) == NODE_TYPE_XP);
+	xp = get_child_node(config->base, ctx->root, xp_idx);
+	fwk_assert(get_node_type(xp) == NODE_TYPE_XP);
 
-        /* Traverse nodes */
-        node_count = get_node_child_count(xp);
-        for (node_idx = 0; node_idx < node_count; node_idx++) {
-            node = get_child_node(config->base, xp, node_idx);
-            if (is_child_external(xp, node_idx)) {
-                unsigned int node_id = get_child_node_id(xp, node_idx);
-                xp_port = get_port_number(get_child_node_id(xp, node_idx));
-
-                /* Skip if the device type is CXG */
-                if ((get_device_type(xp, xp_port) == DEVICE_TYPE_CXRH) ||
-                    (get_device_type(xp, xp_port) == DEVICE_TYPE_CXHA) ||
-                    (get_device_type(xp, xp_port) == DEVICE_TYPE_CXRA))
-                    continue;
-
-                fwk_assert(xrnsam_entry < ctx->external_rnsam_count);
-
-                ctx->external_rnsam_table[xrnsam_entry].node_id = node_id;
-                ctx->external_rnsam_table[xrnsam_entry].node = node;
-
-                xrnsam_entry++;
-            } else {
-                enum node_type node_type = get_node_type(node);
-
-                if (node_type == NODE_TYPE_RN_SAM) {
-                    fwk_assert(irnsam_entry < ctx->internal_rnsam_count);
-
-                    ctx->internal_rnsam_table[irnsam_entry] = node;
-
-                    irnsam_entry++;
-                } else if (node_type == NODE_TYPE_HN_F) {
-                    process_node_hnf(node);
-                } else if (node_type == NODE_TYPE_HN_I) {
-                    /*
-                    Disable sending of NDE response error to RN and logging of
-                    error information forthe following requests:
-                    1. Coherent Read
-                    2. CleanUnique/MakeUnique
-                    3. Coherent/CopyBack Write
-                    This enables coherent access through HNI.
-                    */
-                    struct cmn_booker_hni_reg* hni_node =
-                        (struct cmn_booker_hni_reg*) node;
-                    hni_node->CFG_CTL &= ~1;
-                }
-            }
-        }
+	/* Traverse nodes */
+	node_count = get_node_child_count(xp);
+	for (node_idx = 0; node_idx < node_count; node_idx++) {
+	    node = get_child_node(config->base, xp, node_idx);
+	    if (is_child_external(xp, node_idx)) {
+		unsigned int node_id = get_child_node_id(xp, node_idx);
+		xp_port = get_port_number(get_child_node_id(xp, node_idx));
+
+		/* Skip if the device type is CXG */
+		if ((get_device_type(xp, xp_port) == DEVICE_TYPE_CXRH) ||
+		    (get_device_type(xp, xp_port) == DEVICE_TYPE_CXHA) ||
+		    (get_device_type(xp, xp_port) == DEVICE_TYPE_CXRA))
+		    continue;
+
+		fwk_assert(xrnsam_entry < ctx->external_rnsam_count);
+
+		ctx->external_rnsam_table[xrnsam_entry].node_id = node_id;
+		ctx->external_rnsam_table[xrnsam_entry].node = node;
+
+		xrnsam_entry++;
+	    } else {
+		enum node_type node_type = get_node_type(node);
+
+		if (node_type == NODE_TYPE_RN_SAM) {
+		    fwk_assert(irnsam_entry < ctx->internal_rnsam_count);
+
+		    ctx->internal_rnsam_table[irnsam_entry] = node;
+
+		    irnsam_entry++;
+		} else if (node_type == NODE_TYPE_HN_F) {
+		    process_node_hnf(node);
+		} else if (node_type == NODE_TYPE_HN_I) {
+		    /*
+		    Disable sending of NDE response error to RN and logging of
+		    error information forthe following requests:
+		    1. Coherent Read
+		    2. CleanUnique/MakeUnique
+		    3. Coherent/CopyBack Write
+		    This enables coherent access through HNI.
+		    */
+		    struct cmn_booker_hni_reg* hni_node =
+			(struct cmn_booker_hni_reg*) node;
+		    hni_node->CFG_CTL &= ~1;
+		}
+	    }
+	}
     }
 }
 
@@ -315,70 +315,70 @@ static int cmn_booker_setup_sam(struct c
     FWK_LOG_INFO(MOD_NAME "Configuring SAM for node %d", get_node_id(rnsam));
 
     for (region_idx = 0; region_idx < config->mmap_count; region_idx++) {
-        region = &config->mmap_table[region_idx];
+	region = &config->mmap_table[region_idx];
 
-        (void)mmap_type_name;
-        FWK_LOG_INFO(
-            MOD_NAME "  [0x%x%x - 0x%x%x] %s",
-            HIGH_WORD(region->base), LOW_WORD(region->base),
-            HIGH_WORD(region->base + region->size - 1),
-            LOW_WORD(region->base + region->size - 1),
-            mmap_type_name[region->type]);
-
-        switch (region->type) {
-        case MOD_CMN_BOOKER_MEM_REGION_TYPE_IO:
-            /*
-             * Configure memory region
-             */
-            configure_region(&rnsam->NON_HASH_MEM_REGION[region_io_count],
-                             region->base,
-                             region->size,
-                             SAM_NODE_TYPE_HN_I);
-
-            /*
-             * Configure target node
-             */
-            group = region_io_count /
-                    CMN_BOOKER_RNSAM_NON_HASH_TGT_NODEID_ENTRIES_PER_GROUP;
-            bit_pos = CMN_BOOKER_RNSAM_NON_HASH_TGT_NODEID_ENTRY_BITS_WIDTH *
-                      (region_io_count %
-                       CMN_BOOKER_RNSAM_NON_HASH_TGT_NODEID_ENTRIES_PER_GROUP);
-
-            rnsam->NON_HASH_TGT_NODEID[group] &=
-                ~(CMN_BOOKER_RNSAM_NON_HASH_TGT_NODEID_ENTRY_MASK << bit_pos);
-            rnsam->NON_HASH_TGT_NODEID[group] |=
-                (region->node_id &
-                 CMN_BOOKER_RNSAM_NON_HASH_TGT_NODEID_ENTRY_MASK) << bit_pos;
-
-            region_io_count++;
-            break;
-
-        case MOD_CMN_BOOKER_MEM_REGION_TYPE_SYSCACHE:
-            /*
-             * Configure memory region
-             */
-            fwk_assert(region->size <= UINT64_C(1) * FWK_TIB);
-
-            configure_region(&rnsam->SYS_CACHE_GRP_REGION[region_sys_count],
-                             region->base,
-                             region->size,
-                             SAM_NODE_TYPE_HN_F);
-
-            /* Mark corresponding region as enabled */
-            fwk_assert(region_sys_count < MAX_SCG_COUNT);
-            scg_regions_enabled[region_sys_count] = 1;
-
-            region_sys_count++;
-            break;
-
-        case MOD_CMN_BOOKER_REGION_TYPE_SYSCACHE_SUB:
-            /* Do nothing. System cache sub-regions are handled by HN-Fs */
-            break;
-
-        default:
-            fwk_unexpected();
-            return FWK_E_DATA;
-        }
+	(void)mmap_type_name;
+	FWK_LOG_INFO(
+	    MOD_NAME "  [0x%x%x - 0x%x%x] %s",
+	    HIGH_WORD(region->base), LOW_WORD(region->base),
+	    HIGH_WORD(region->base + region->size - 1),
+	    LOW_WORD(region->base + region->size - 1),
+	    mmap_type_name[region->type]);
+
+	switch (region->type) {
+	case MOD_CMN_BOOKER_MEM_REGION_TYPE_IO:
+	    /*
+	     * Configure memory region
+	     */
+	    configure_region(&rnsam->NON_HASH_MEM_REGION[region_io_count],
+			     region->base,
+			     region->size,
+			     SAM_NODE_TYPE_HN_I);
+
+	    /*
+	     * Configure target node
+	     */
+	    group = region_io_count /
+		    CMN_BOOKER_RNSAM_NON_HASH_TGT_NODEID_ENTRIES_PER_GROUP;
+	    bit_pos = CMN_BOOKER_RNSAM_NON_HASH_TGT_NODEID_ENTRY_BITS_WIDTH *
+		      (region_io_count %
+		       CMN_BOOKER_RNSAM_NON_HASH_TGT_NODEID_ENTRIES_PER_GROUP);
+
+	    rnsam->NON_HASH_TGT_NODEID[group] &=
+		~(CMN_BOOKER_RNSAM_NON_HASH_TGT_NODEID_ENTRY_MASK << bit_pos);
+	    rnsam->NON_HASH_TGT_NODEID[group] |=
+		(region->node_id &
+		 CMN_BOOKER_RNSAM_NON_HASH_TGT_NODEID_ENTRY_MASK) << bit_pos;
+
+	    region_io_count++;
+	    break;
+
+	case MOD_CMN_BOOKER_MEM_REGION_TYPE_SYSCACHE:
+	    /*
+	     * Configure memory region
+	     */
+	    fwk_assert(region->size <= UINT64_C(1) * FWK_TIB);
+
+	    configure_region(&rnsam->SYS_CACHE_GRP_REGION[region_sys_count],
+			     region->base,
+			     region->size,
+			     SAM_NODE_TYPE_HN_F);
+
+	    /* Mark corresponding region as enabled */
+	    fwk_assert(region_sys_count < MAX_SCG_COUNT);
+	    scg_regions_enabled[region_sys_count] = 1;
+
+	    region_sys_count++;
+	    break;
+
+	case MOD_CMN_BOOKER_REGION_TYPE_SYSCACHE_SUB:
+	    /* Do nothing. System cache sub-regions are handled by HN-Fs */
+	    break;
+
+	default:
+	    fwk_unexpected();
+	    return FWK_E_DATA;
+	}
     }
 
     /*
@@ -387,13 +387,13 @@ static int cmn_booker_setup_sam(struct c
      * half if CAL mode is enabled.
      */
     if (config->hnf_cal_mode)
-        hnf_count = ctx->hnf_count/2;
+	hnf_count = ctx->hnf_count/2;
     else
-        hnf_count = ctx->hnf_count;
+	hnf_count = ctx->hnf_count;
 
     group_count = hnf_count / CMN_BOOKER_HNF_CACHE_GROUP_ENTRIES_PER_GROUP;
     for (group = 0; group <= group_count; group++)
-        rnsam->SYS_CACHE_GRP_HN_NODEID[group] = ctx->hnf_cache_group[group];
+	rnsam->SYS_CACHE_GRP_HN_NODEID[group] = ctx->hnf_cache_group[group];
 
     /* Program the number of HNFs */
     rnsam->SYS_CACHE_GRP_HN_COUNT = hnf_count;
@@ -403,26 +403,26 @@ static int cmn_booker_setup_sam(struct c
      * enable register.
      */
     if (config->hnf_cal_mode) {
-        for (region_idx = 0; region_idx < MAX_SCG_COUNT; region_idx++)
-            rnsam->SYS_CACHE_GRP_CAL_MODE |= scg_regions_enabled[region_idx] *
-                (CMN_BOOKER_RNSAM_SCG_HNF_CAL_MODE_EN
-                 << (region_idx * CMN_BOOKER_RNSAM_SCG_HNF_CAL_MODE_SHIFT));
-
-        /* Program the SYS_CACHE_GRP_SN_NODEID register for PrefetchTgt */
-        group_count = config->snf_count /
-            (CMN_BOOKER_RNSAM_SYS_CACHE_GRP_SN_NODEID_ENTRIES_PER_GROUP * 2);
+	for (region_idx = 0; region_idx < MAX_SCG_COUNT; region_idx++)
+	    rnsam->SYS_CACHE_GRP_CAL_MODE |= scg_regions_enabled[region_idx] *
+		(CMN_BOOKER_RNSAM_SCG_HNF_CAL_MODE_EN
+		 << (region_idx * CMN_BOOKER_RNSAM_SCG_HNF_CAL_MODE_SHIFT));
+
+	/* Program the SYS_CACHE_GRP_SN_NODEID register for PrefetchTgt */
+	group_count = config->snf_count /
+	    (CMN_BOOKER_RNSAM_SYS_CACHE_GRP_SN_NODEID_ENTRIES_PER_GROUP * 2);
     } else {
-        group_count = config->snf_count /
-            CMN_BOOKER_RNSAM_SYS_CACHE_GRP_SN_NODEID_ENTRIES_PER_GROUP;
+	group_count = config->snf_count /
+	    CMN_BOOKER_RNSAM_SYS_CACHE_GRP_SN_NODEID_ENTRIES_PER_GROUP;
     }
 
     for (group = 0; group <= group_count; group++)
-        rnsam->SYS_CACHE_GRP_SN_NODEID[group] = ctx->sn_nodeid_group[group];
+	rnsam->SYS_CACHE_GRP_SN_NODEID[group] = ctx->sn_nodeid_group[group];
 
     /* Enable RNSAM */
     rnsam->STATUS = ((uint64_t)config->hnd_node_id <<
-                     CMN_BOOKER_RNSAM_STATUS_DEFAULT_NODEID_POS) |
-                    CMN_BOOKER_RNSAM_STATUS_UNSTALL;
+		     CMN_BOOKER_RNSAM_STATUS_DEFAULT_NODEID_POS) |
+		    CMN_BOOKER_RNSAM_STATUS_UNSTALL;
     __sync_synchronize();
 
     return FWK_SUCCESS;
@@ -434,47 +434,47 @@ static int cmn_booker_setup(void)
     int status;
 
     if (!ctx->initialized) {
-        status = cmn_booker_discovery();
-        if (status != FWK_SUCCESS)
-            return FWK_SUCCESS;
-
-        /*
-         * Allocate resources based on the discovery
-         */
-
-        /* Pointers for the internal RN-SAM nodes */
-        if (ctx->internal_rnsam_count != 0) {
-            ctx->internal_rnsam_table = fwk_mm_calloc(
-                ctx->internal_rnsam_count, sizeof(*ctx->internal_rnsam_table));
-        }
-
-        /* Tuples for the external RN-RAM nodes (including their node IDs) */
-        if (ctx->external_rnsam_count != 0) {
-            ctx->external_rnsam_table = fwk_mm_calloc(
-                ctx->external_rnsam_count, sizeof(*ctx->external_rnsam_table));
-        }
-
-        /* Cache groups */
-        if (ctx->hnf_count != 0) {
-            /*
-             * Allocate enough group descriptors to accommodate all expected
-             * HN-F nodes in the system.
-             */
-            ctx->hnf_cache_group = fwk_mm_calloc(
-                ctx->hnf_count / CMN_BOOKER_HNF_CACHE_GROUP_ENTRIES_PER_GROUP,
-                sizeof(*ctx->hnf_cache_group));
-            ctx->sn_nodeid_group = fwk_mm_calloc(
-                ctx->hnf_count /
-                CMN_BOOKER_RNSAM_SYS_CACHE_GRP_SN_NODEID_ENTRIES_PER_GROUP,
-                sizeof(*ctx->sn_nodeid_group));
-        }
+	status = cmn_booker_discovery();
+	if (status != FWK_SUCCESS)
+	    return FWK_SUCCESS;
+
+	/*
+	 * Allocate resources based on the discovery
+	 */
+
+	/* Pointers for the internal RN-SAM nodes */
+	if (ctx->internal_rnsam_count != 0) {
+	    ctx->internal_rnsam_table = fwk_mm_calloc(
+		ctx->internal_rnsam_count, sizeof(*ctx->internal_rnsam_table));
+	}
+
+	/* Tuples for the external RN-RAM nodes (including their node IDs) */
+	if (ctx->external_rnsam_count != 0) {
+	    ctx->external_rnsam_table = fwk_mm_calloc(
+		ctx->external_rnsam_count, sizeof(*ctx->external_rnsam_table));
+	}
+
+	/* Cache groups */
+	if (ctx->hnf_count != 0) {
+	    /*
+	     * Allocate enough group descriptors to accommodate all expected
+	     * HN-F nodes in the system.
+	     */
+	    ctx->hnf_cache_group = fwk_mm_calloc(
+		ctx->hnf_count / CMN_BOOKER_HNF_CACHE_GROUP_ENTRIES_PER_GROUP,
+		sizeof(*ctx->hnf_cache_group));
+	    ctx->sn_nodeid_group = fwk_mm_calloc(
+		ctx->hnf_count /
+		CMN_BOOKER_RNSAM_SYS_CACHE_GRP_SN_NODEID_ENTRIES_PER_GROUP,
+		sizeof(*ctx->sn_nodeid_group));
+	}
     }
 
     cmn_booker_configure();
 
     /* Setup internal RN-SAM nodes */
     for (rnsam_idx = 0; rnsam_idx < ctx->internal_rnsam_count; rnsam_idx++)
-        cmn_booker_setup_sam(ctx->internal_rnsam_table[rnsam_idx]);
+	cmn_booker_setup_sam(ctx->internal_rnsam_table[rnsam_idx]);
 
     FWK_LOG_INFO(MOD_NAME "Done");
 
@@ -488,10 +488,10 @@ static int cmn_booker_setup_rnsam(unsign
     unsigned int node_idx;
 
     for (node_idx = 0; node_idx < ctx->external_rnsam_count; node_idx++) {
-        if (ctx->external_rnsam_table[node_idx].node_id == node_id) {
-            cmn_booker_setup_sam(ctx->external_rnsam_table[node_idx].node);
-            return FWK_SUCCESS;
-        }
+	if (ctx->external_rnsam_table[node_idx].node_id == node_id) {
+	    cmn_booker_setup_sam(ctx->external_rnsam_table[node_idx].node);
+	    return FWK_SUCCESS;
+	}
     }
 
     return FWK_E_PARAM;
@@ -523,33 +523,33 @@ static int cmn_booker_init(fwk_id_t modu
 
     /* No elements support */
     if (element_count > 0)
-        return FWK_E_DATA;
+	return FWK_E_DATA;
 
     /* Allocate space for the context */
     ctx = fwk_mm_calloc(1, sizeof(*ctx));
 
     if (config->base == 0)
-        return FWK_E_DATA;
+	return FWK_E_DATA;
 
     if ((config->mesh_size_x == 0) ||
-        (config->mesh_size_x > CMN_BOOKER_MESH_X_MAX))
-        return FWK_E_DATA;
+	(config->mesh_size_x > CMN_BOOKER_MESH_X_MAX))
+	return FWK_E_DATA;
 
     if ((config->mesh_size_y == 0) ||
-        (config->mesh_size_y > CMN_BOOKER_MESH_Y_MAX))
-        return FWK_E_DATA;
+	(config->mesh_size_y > CMN_BOOKER_MESH_Y_MAX))
+	return FWK_E_DATA;
 
     if (config->snf_count > CMN_BOOKER_HNF_CACHE_GROUP_ENTRIES_MAX)
-        return FWK_E_DATA;
+	return FWK_E_DATA;
 
     fwk_assert(config->ports_per_xp < MAX_PORTS_COUNT);
 
     ctx->root = get_root_node(
-        config->base,
-        config->hnd_node_id,
-        config->mesh_size_x,
-        config->mesh_size_y,
-        config->ports_per_xp);
+	config->base,
+	config->hnd_node_id,
+	config->mesh_size_x,
+	config->mesh_size_y,
+	config->ports_per_xp);
 
     ctx->config = config;
 
@@ -566,13 +566,13 @@ static int cmn_booker_process_bind_reque
 int cmn_booker_start(fwk_id_t id)
 {
     if (fwk_id_is_equal(ctx->config->clock_id, FWK_ID_NONE)) {
-        cmn_booker_setup();
-        return FWK_SUCCESS;
+	cmn_booker_setup();
+	return FWK_SUCCESS;
     }
 
     /* Register the module for clock state notifications */
     return fwk_notification_subscribe(mod_clock_notification_id_state_changed,
-                                      ctx->config->clock_id, id);
+				      ctx->config->clock_id, id);
 }
 
 static int cmn_booker_process_notification(
@@ -582,12 +582,12 @@ static int cmn_booker_process_notificati
     struct clock_notification_params *params;
 
     fwk_assert(
-        fwk_id_is_equal(event->id, mod_clock_notification_id_state_changed));
+	fwk_id_is_equal(event->id, mod_clock_notification_id_state_changed));
     fwk_assert(fwk_id_is_type(event->target_id, FWK_ID_TYPE_MODULE));
 
     params = (struct clock_notification_params *)event->params;
     if (params->new_state == MOD_CLOCK_STATE_RUNNING)
-        cmn_booker_setup();
+	cmn_booker_setup();
 
     return FWK_SUCCESS;
 }
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cmn_skeena/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/cmn_skeena/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cmn_skeena/CMakeLists.txt
@@ -14,9 +14,9 @@ target_include_directories(
 target_sources(
     ${SCP_MODULE_TARGET}
     PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_cmn_skeena.c"
-            "${CMAKE_CURRENT_SOURCE_DIR}/src/cmn_skeena.c"
-            "${CMAKE_CURRENT_SOURCE_DIR}/src/cmn_skeena_ccix.c")
+	    "${CMAKE_CURRENT_SOURCE_DIR}/src/cmn_skeena.c"
+	    "${CMAKE_CURRENT_SOURCE_DIR}/src/cmn_skeena_ccix.c")
 
 target_link_libraries(
     ${SCP_MODULE_TARGET} PRIVATE module-timer module-clock module-ppu-v1
-                                 module-system-info)
+				 module-system-info)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cmn_skeena/src/cmn_skeena.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/cmn_skeena/src/cmn_skeena.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cmn_skeena/src/cmn_skeena.c
@@ -31,14 +31,14 @@ unsigned int get_node_id(void *node_base
 {
     struct node_header *node = node_base;
     return (node->NODE_INFO & CMN_SKEENA_NODE_INFO_ID) >>
-        CMN_SKEENA_NODE_INFO_ID_POS;
+	CMN_SKEENA_NODE_INFO_ID_POS;
 }
 
 unsigned int get_node_logical_id(void *node_base)
 {
     struct node_header *node = node_base;
     return (node->NODE_INFO & CMN_SKEENA_NODE_INFO_LOGICAL_ID) >>
-        CMN_SKEENA_NODE_INFO_LOGICAL_ID_POS;
+	CMN_SKEENA_NODE_INFO_LOGICAL_ID_POS;
 }
 
 void *get_child_node(uintptr_t base, void *node_base, unsigned int child_index)
@@ -62,11 +62,11 @@ unsigned int get_child_node_id(void *nod
     unsigned int device_id;
 
     node_pointer = (node->CHILD_POINTER[child_index] &
-                    CMN_SKEENA_CHILD_POINTER_EXT_NODE_POINTER) >>
-        CMN_SKEENA_CHILD_POINTER_EXT_NODE_POINTER_POS;
+		    CMN_SKEENA_CHILD_POINTER_EXT_NODE_POINTER) >>
+	CMN_SKEENA_CHILD_POINTER_EXT_NODE_POINTER_POS;
 
     device_id = (((node_pointer >> 6) & 0xff) << 3) |
-        ((node_pointer & 0x1) << 2) | ((node_pointer >> 2) & 0x3);
+	((node_pointer & 0x1) << 2) | ((node_pointer >> 2) & 0x3);
 
     return device_id;
 }
@@ -79,8 +79,8 @@ unsigned int get_cmn_skeena_revision(str
 bool is_cal_mode_supported(struct cmn_skeena_cfgm_reg *root)
 {
     return (get_cmn_skeena_revision(root) >= CMN_SKEENA_PERIPH_ID_2_REV_R2_P0) ?
-        true :
-        false;
+	true :
+	false;
 }
 
 bool is_child_external(void *node_base, unsigned int child_index)
@@ -92,14 +92,14 @@ bool is_child_external(void *node_base,
 bool get_port_number(unsigned int child_node_id)
 {
     return (child_node_id >> CMN_SKEENA_NODE_ID_PORT_POS) &
-        CMN_SKEENA_NODE_ID_PORT_MASK;
+	CMN_SKEENA_NODE_ID_PORT_MASK;
 }
 
 unsigned int get_device_type(void *mxp_base, bool port)
 {
     struct cmn_skeena_mxp_reg *mxp = mxp_base;
     return mxp->PORT_CONNECT_INFO[port] &
-        CMN_SKEENA_MXP_PORT_CONNECT_INFO_DEVICE_TYPE_MASK;
+	CMN_SKEENA_MXP_PORT_CONNECT_INFO_DEVICE_TYPE_MASK;
 }
 
 uint64_t sam_encode_region_size(uint64_t size)
@@ -134,7 +134,7 @@ void configure_region(
     value = CMN_SKEENA_RNSAM_REGION_ENTRY_VALID;
     value |= node_type << CMN_SKEENA_RNSAM_REGION_ENTRY_TYPE_POS;
     value |= sam_encode_region_size(size)
-        << CMN_SKEENA_RNSAM_REGION_ENTRY_SIZE_POS;
+	<< CMN_SKEENA_RNSAM_REGION_ENTRY_SIZE_POS;
     value |= (base / SAM_GRANULARITY) << CMN_SKEENA_RNSAM_REGION_ENTRY_BASE_POS;
 
     *reg &= ~(CMN_SKEENA_RNSAM_REGION_ENTRY_MASK << bit_offset);
@@ -166,11 +166,11 @@ const char *get_node_type_name(enum node
 {
     /* Base node IDs */
     if (node_type <= NODE_TYPE_RN_SAM)
-        return type_to_name[node_type];
+	return type_to_name[node_type];
 
     /* CML node IDs */
     if ((node_type >= NODE_TYPE_CML_BASE) && (node_type <= NODE_TYPE_CXLA))
-        return type_to_name_cml[node_type - NODE_TYPE_CML_BASE];
+	return type_to_name_cml[node_type - NODE_TYPE_CML_BASE];
 
     /* Invalid node IDs */
     return type_to_name[NODE_TYPE_INVALID];
@@ -180,7 +180,7 @@ unsigned int get_node_pos_x(void *node_b
 {
     struct node_header *node = node_base;
     return (get_node_id(node) >> (CMN_SKEENA_NODE_ID_Y_POS + encoding_bits)) &
-        mask_bits;
+	mask_bits;
 }
 
 unsigned int get_node_pos_y(void *node_base)
@@ -210,14 +210,14 @@ struct cmn_skeena_cfgm_reg *get_root_nod
     mask_bits = (1 << encoding_bits) - 1;
     node_pos_y = (hnd_node_id >> CMN_SKEENA_NODE_ID_Y_POS) & mask_bits;
     node_pos_x =
-        (hnd_node_id >> (CMN_SKEENA_NODE_ID_Y_POS + encoding_bits)) & mask_bits;
+	(hnd_node_id >> (CMN_SKEENA_NODE_ID_Y_POS + encoding_bits)) & mask_bits;
     node_port = (hnd_node_id >> CMN_SKEENA_NODE_ID_PORT_POS) &
-        CMN_SKEENA_NODE_ID_PORT_MASK;
+	CMN_SKEENA_NODE_ID_PORT_MASK;
 
     /* Calculate node address offset */
     offset = (node_pos_y << CMN_SKEENA_ROOT_NODE_OFFSET_Y_POS) |
-        (node_pos_x << (CMN_SKEENA_ROOT_NODE_OFFSET_Y_POS + encoding_bits)) |
-        (node_port << CMN_SKEENA_ROOT_NODE_OFFSET_PORT_POS);
+	(node_pos_x << (CMN_SKEENA_ROOT_NODE_OFFSET_Y_POS + encoding_bits)) |
+	(node_port << CMN_SKEENA_ROOT_NODE_OFFSET_PORT_POS);
 
     return (struct cmn_skeena_cfgm_reg *)(base + offset);
 }
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cmn_skeena/src/cmn_skeena_ccix.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/cmn_skeena/src/cmn_skeena_ccix.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cmn_skeena/src/cmn_skeena_ccix.c
@@ -36,102 +36,102 @@ static bool cxg_link_wait_condition(void
     fwk_assert(data != NULL);
 
     struct cxg_wait_condition_data *wait_data =
-        (struct cxg_wait_condition_data *)data;
+	(struct cxg_wait_condition_data *)data;
     struct cmn_skeena_ctx *ctx = wait_data->ctx;
     link_id = wait_data->link_id;
 
     switch (wait_data->cond) {
     case CXG_LINK_CTRL_EN_BIT_SET:
-        val1 = ctx->cxg_ra_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_CTRL;
-        val2 = ctx->cxg_ha_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_CTRL;
-        return (
-            ((val1 & CXG_LINK_CTRL_EN_MASK) &&
-             (val2 & CXG_LINK_CTRL_EN_MASK)) != 0);
+	val1 = ctx->cxg_ra_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_CTRL;
+	val2 = ctx->cxg_ha_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_CTRL;
+	return (
+	    ((val1 & CXG_LINK_CTRL_EN_MASK) &&
+	     (val2 & CXG_LINK_CTRL_EN_MASK)) != 0);
 
     case CXG_LINK_CTRL_UP_BIT_CLR:
-        val1 = ctx->cxg_ra_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_CTRL;
-        val2 = ctx->cxg_ha_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_CTRL;
-        return (
-            (((val1 & CXG_LINK_CTRL_UP_MASK) == 0) &&
-             ((val2 & CXG_LINK_CTRL_UP_MASK) == 0)) != 0);
+	val1 = ctx->cxg_ra_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_CTRL;
+	val2 = ctx->cxg_ha_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_CTRL;
+	return (
+	    (((val1 & CXG_LINK_CTRL_UP_MASK) == 0) &&
+	     ((val2 & CXG_LINK_CTRL_UP_MASK) == 0)) != 0);
 
     case CXG_LINK_STATUS_DWN_BIT_SET:
-        val1 = ctx->cxg_ra_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_STATUS;
-        val2 = ctx->cxg_ha_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_STATUS;
-        return (
-            ((val1 & CXG_LINK_STATUS_DOWN_MASK) &&
-             (val2 & CXG_LINK_STATUS_DOWN_MASK)) != 0);
+	val1 = ctx->cxg_ra_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_STATUS;
+	val2 = ctx->cxg_ha_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_STATUS;
+	return (
+	    ((val1 & CXG_LINK_STATUS_DOWN_MASK) &&
+	     (val2 & CXG_LINK_STATUS_DOWN_MASK)) != 0);
 
     case CXG_LINK_STATUS_DWN_BIT_CLR:
-        val1 = ctx->cxg_ra_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_STATUS;
-        val2 = ctx->cxg_ha_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_STATUS;
-        return (
-            (((val1 & CXG_LINK_STATUS_DOWN_MASK) == 0) &&
-             ((val2 & CXG_LINK_STATUS_DOWN_MASK) == 0)) != 0);
+	val1 = ctx->cxg_ra_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_STATUS;
+	val2 = ctx->cxg_ha_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_STATUS;
+	return (
+	    (((val1 & CXG_LINK_STATUS_DOWN_MASK) == 0) &&
+	     ((val2 & CXG_LINK_STATUS_DOWN_MASK) == 0)) != 0);
 
     case CXG_LINK_STATUS_ACK_BIT_SET:
-        val1 = ctx->cxg_ra_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_STATUS;
-        val2 = ctx->cxg_ha_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_STATUS;
-        return (
-            ((val1 & CXG_LINK_STATUS_ACK_MASK) &&
-             (val2 & CXG_LINK_STATUS_ACK_MASK)) != 0);
+	val1 = ctx->cxg_ra_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_STATUS;
+	val2 = ctx->cxg_ha_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_STATUS;
+	return (
+	    ((val1 & CXG_LINK_STATUS_ACK_MASK) &&
+	     (val2 & CXG_LINK_STATUS_ACK_MASK)) != 0);
 
     case CXG_LINK_STATUS_ACK_BIT_CLR:
-        val1 = ctx->cxg_ra_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_STATUS;
-        val2 = ctx->cxg_ha_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_STATUS;
-        return (
-            (((val1 & CXG_LINK_STATUS_ACK_MASK) == 0) &&
-             ((val2 & CXG_LINK_STATUS_ACK_MASK) == 0)) != 0);
+	val1 = ctx->cxg_ra_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_STATUS;
+	val2 = ctx->cxg_ha_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_STATUS;
+	return (
+	    (((val1 & CXG_LINK_STATUS_ACK_MASK) == 0) &&
+	     ((val2 & CXG_LINK_STATUS_ACK_MASK) == 0)) != 0);
 
     case CXG_LINK_STATUS_HA_DVMDOMAIN_ACK_BIT_SET:
-        val1 = ctx->cxg_ha_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_STATUS;
-        return (((val1 & CXG_LINK_STATUS_DVMDOMAIN_ACK_MASK)) != 0);
+	val1 = ctx->cxg_ha_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_STATUS;
+	return (((val1 & CXG_LINK_STATUS_DVMDOMAIN_ACK_MASK)) != 0);
 
     case CXG_LINK_STATUS_RA_DVMDOMAIN_ACK_BIT_SET:
-        val1 = ctx->cxg_ra_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_STATUS;
-        return (((val1 & CXG_LINK_STATUS_DVMDOMAIN_ACK_MASK)) != 0);
+	val1 = ctx->cxg_ra_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_STATUS;
+	return (((val1 & CXG_LINK_STATUS_DVMDOMAIN_ACK_MASK)) != 0);
 
     default:
-        fwk_unexpected();
-        return false;
+	fwk_unexpected();
+	return false;
     }
 }
 
 static int enable_smp_mode(struct cmn_skeena_ctx *ctx)
 {
     if (get_cmn_skeena_revision(ctx->root) ==
-        CMN_SKEENA_PERIPH_ID_2_REV_R2_P0) {
-        /*
-         * CMN-600 revision r2p0 uses read-only bits from the CXG_RA and CXG_HA
-         * registers to determine whether the SMP Mode is enabled in hardware.
-         */
-        if (((ctx->cxg_ra_reg->CXG_RA_UNIT_INFO &
-              CXG_RA_UNIT_INFO_SMP_MODE_RO_MASK) !=
-             CXG_RA_UNIT_INFO_SMP_MODE_RO_MASK) ||
-            ((ctx->cxg_ha_reg->CXG_HA_UNIT_INFO &
-              CXG_HA_UNIT_INFO_SMP_MODE_RO_MASK) !=
-             CXG_HA_UNIT_INFO_SMP_MODE_RO_MASK)) {
-            FWK_LOG_INFO(MOD_NAME "SMP Mode not supported");
-            return FWK_E_SUPPORT;
-        }
+	CMN_SKEENA_PERIPH_ID_2_REV_R2_P0) {
+	/*
+	 * CMN-600 revision r2p0 uses read-only bits from the CXG_RA and CXG_HA
+	 * registers to determine whether the SMP Mode is enabled in hardware.
+	 */
+	if (((ctx->cxg_ra_reg->CXG_RA_UNIT_INFO &
+	      CXG_RA_UNIT_INFO_SMP_MODE_RO_MASK) !=
+	     CXG_RA_UNIT_INFO_SMP_MODE_RO_MASK) ||
+	    ((ctx->cxg_ha_reg->CXG_HA_UNIT_INFO &
+	      CXG_HA_UNIT_INFO_SMP_MODE_RO_MASK) !=
+	     CXG_HA_UNIT_INFO_SMP_MODE_RO_MASK)) {
+	    FWK_LOG_INFO(MOD_NAME "SMP Mode not supported");
+	    return FWK_E_SUPPORT;
+	}
 
-        FWK_LOG_INFO(MOD_NAME "SMP Mode supported");
-        return FWK_SUCCESS;
+	FWK_LOG_INFO(MOD_NAME "SMP Mode supported");
+	return FWK_SUCCESS;
     } else if (
-        get_cmn_skeena_revision(ctx->root) >=
-        CMN_SKEENA_PERIPH_ID_2_REV_R3_P0) {
-        /*
-         * CMN-600 revision r3p0 and above allows the software to configure the
-         * SMP mode
-         */
-        ctx->cxg_ra_reg->CXG_RA_AUX_CTRL |=
-            (1 << CXG_RA_AUX_CTRL_SMP_MODE_RW_SHIFT_VAL);
-        ctx->cxg_ha_reg->CXG_HA_AUX_CTRL |=
-            (1 << CXG_HA_AUX_CTRL_SMP_MODE_RW_SHIFT_VAL);
-        ctx->cxla_reg->CXLA_AUX_CTRL |=
-            ((uint64_t)0x1 << CXLA_AUX_CTRL_SMP_MODE_SHIFT_VAL);
-        FWK_LOG_INFO(MOD_NAME "SMP MODE enabled");
-        return FWK_SUCCESS;
+	get_cmn_skeena_revision(ctx->root) >=
+	CMN_SKEENA_PERIPH_ID_2_REV_R3_P0) {
+	/*
+	 * CMN-600 revision r3p0 and above allows the software to configure the
+	 * SMP mode
+	 */
+	ctx->cxg_ra_reg->CXG_RA_AUX_CTRL |=
+	    (1 << CXG_RA_AUX_CTRL_SMP_MODE_RW_SHIFT_VAL);
+	ctx->cxg_ha_reg->CXG_HA_AUX_CTRL |=
+	    (1 << CXG_HA_AUX_CTRL_SMP_MODE_RW_SHIFT_VAL);
+	ctx->cxla_reg->CXLA_AUX_CTRL |=
+	    ((uint64_t)0x1 << CXLA_AUX_CTRL_SMP_MODE_SHIFT_VAL);
+	FWK_LOG_INFO(MOD_NAME "SMP MODE enabled");
+	return FWK_SUCCESS;
     }
 
     /*
@@ -155,11 +155,11 @@ static void program_cxg_ra_rnf_ldid_to_r
 
     /* Adding raid_value into LDID-to-RAID register */
     ctx->cxg_ra_reg->CXG_RA_RNF_LDID_TO_RAID_REG[reg_offset] |=
-        ((uint64_t)raid << (ldid_value_offset * NUM_BITS_RESERVED_FOR_RAID));
+	((uint64_t)raid << (ldid_value_offset * NUM_BITS_RESERVED_FOR_RAID));
 
     /* Set corresponding valid bit */
     ctx->cxg_ra_reg->CXG_RA_RNF_LDID_TO_RAID_VAL |=
-        ((uint64_t)0x1 << ldid_value);
+	((uint64_t)0x1 << ldid_value);
 
     /* Increment raid_value */
     ctx->raid_value++;
@@ -179,11 +179,11 @@ static void program_cxg_ra_rni_ldid_to_r
 
     /* Adding raid_value into LDID-to-RAID register */
     ctx->cxg_ra_reg->CXG_RA_RNI_LDID_TO_RAID_REG[reg_offset] |=
-        ((uint64_t)raid << (ldid_value_offset * NUM_BITS_RESERVED_FOR_RAID));
+	((uint64_t)raid << (ldid_value_offset * NUM_BITS_RESERVED_FOR_RAID));
 
     /* Set corresponding valid bit */
     ctx->cxg_ra_reg->CXG_RA_RNI_LDID_TO_RAID_VAL |=
-        ((uint64_t)0x1 << ldid_value);
+	((uint64_t)0x1 << ldid_value);
 
     /* Increment raid_value */
     ctx->raid_value++;
@@ -203,11 +203,11 @@ static void program_cxg_ra_rnd_ldid_to_r
 
     /* Adding raid_value into LDID-to-RAID register */
     ctx->cxg_ra_reg->CXG_RA_RND_LDID_TO_RAID_REG[reg_offset] |=
-        ((uint64_t)raid << (ldid_value_offset * NUM_BITS_RESERVED_FOR_RAID));
+	((uint64_t)raid << (ldid_value_offset * NUM_BITS_RESERVED_FOR_RAID));
 
     /* Set corresponding valid bit */
     ctx->cxg_ra_reg->CXG_RA_RND_LDID_TO_RAID_VAL |=
-        ((uint64_t)0x1 << ldid_value);
+	((uint64_t)0x1 << ldid_value);
 
     /* Increment raid_value */
     ctx->raid_value++;
@@ -227,20 +227,20 @@ static void program_agentid_to_linkid_re
 
     /* Writing AgentID-to-LinkID mappings */
     ctx->cxg_ra_reg->CXG_RA_AGENTID_TO_LINKID_REG[reg_index] |=
-        ((uint64_t)link_id
-         << (agentid_bit_offset * NUM_BITS_RESERVED_FOR_LINK_ID));
+	((uint64_t)link_id
+	 << (agentid_bit_offset * NUM_BITS_RESERVED_FOR_LINK_ID));
     ctx->cxg_ha_reg->CXG_HA_AGENTID_TO_LINKID_REG[reg_index] |=
-        ((uint64_t)link_id
-         << (agentid_bit_offset * NUM_BITS_RESERVED_FOR_LINK_ID));
+	((uint64_t)link_id
+	 << (agentid_bit_offset * NUM_BITS_RESERVED_FOR_LINK_ID));
     ctx->cxla_reg->CXLA_AGENTID_TO_LINKID_REG[reg_index] |=
-        ((uint64_t)link_id
-         << (agentid_bit_offset * NUM_BITS_RESERVED_FOR_LINK_ID));
+	((uint64_t)link_id
+	 << (agentid_bit_offset * NUM_BITS_RESERVED_FOR_LINK_ID));
 
     /* Setting corresponding valid bits */
     ctx->cxg_ra_reg->CXG_RA_AGENTID_TO_LINKID_VAL |=
-        ((uint64_t)0x1 << agent_id);
+	((uint64_t)0x1 << agent_id);
     ctx->cxg_ha_reg->CXG_HA_AGENTID_TO_LINKID_VAL |=
-        ((uint64_t)0x1 << agent_id);
+	((uint64_t)0x1 << agent_id);
     ctx->cxla_reg->CXLA_AGENTID_TO_LINKID_VAL |= ((uint64_t)0x1 << agent_id);
 }
 
@@ -267,8 +267,8 @@ static void program_cxg_ha_raid_to_ldid_
 
     /* Write RAID-to-LDID mapping (with RNF bit set) */
     ctx->cxg_ha_reg->CXG_HA_RNF_RAID_TO_LDID_REG[reg_index] |=
-        ((uint64_t)(ldid | CXG_HA_RAID_TO_LDID_RNF_MASK)
-         << (raid_bit_offset * NUM_BITS_RESERVED_FOR_LDID));
+	((uint64_t)(ldid | CXG_HA_RAID_TO_LDID_RNF_MASK)
+	 << (raid_bit_offset * NUM_BITS_RESERVED_FOR_LDID));
 
     /* Set corresponding valid bit */
     ctx->cxg_ha_reg->CXG_HA_RNF_RAID_TO_LDID_VAL |= ((uint64_t)0x1 << raid_id);
@@ -290,14 +290,14 @@ static void program_hnf_ldid_to_chi_node
     reg_bit_offset = ldid % 2;
 
     for (i = 0; i < ctx->hnf_count; i++) {
-        hnf_reg = (struct cmn_skeena_hnf_reg *)ctx->hnf_offset[i];
-        /* Write NodeID, local/remote and valid bit */
-        hnf_reg->RN_PHYS_ID[reg_index] |=
-            ((uint64_t)(
-                 node_id |
-                 (local_remote << HNF_RN_PHYS_RN_LOCAL_REMOTE_SHIFT_VAL) |
-                 ((uint64_t)0x1 << HNF_RN_PHYS_RN_ID_VALID_SHIFT_VAL))
-             << (reg_bit_offset * NUM_BITS_RESERVED_FOR_PHYS_ID));
+	hnf_reg = (struct cmn_skeena_hnf_reg *)ctx->hnf_offset[i];
+	/* Write NodeID, local/remote and valid bit */
+	hnf_reg->RN_PHYS_ID[reg_index] |=
+	    ((uint64_t)(
+		 node_id |
+		 (local_remote << HNF_RN_PHYS_RN_LOCAL_REMOTE_SHIFT_VAL) |
+		 ((uint64_t)0x1 << HNF_RN_PHYS_RN_ID_VALID_SHIFT_VAL))
+	     << (reg_bit_offset * NUM_BITS_RESERVED_FOR_PHYS_ID));
     }
 }
 
@@ -310,25 +310,25 @@ static void program_cxg_ra_sam_addr_regi
     uint64_t sz;
 
     for (i = 0; i < config->remote_ha_mmap_count; i++) {
-        /* Size must be a multiple of SAM_GRANULARITY */
-        fwk_assert((config->remote_ha_mmap[i].size % (64 * 1024)) == 0);
+	/* Size must be a multiple of SAM_GRANULARITY */
+	fwk_assert((config->remote_ha_mmap[i].size % (64 * 1024)) == 0);
 
-        /* Size also must be a power of two */
-        fwk_assert(
-            (config->remote_ha_mmap[i].size &
-             (config->remote_ha_mmap[i].size - 1)) == 0);
-
-        /* Region base should be naturally aligned to the region size */
-        fwk_assert(
-            config->remote_ha_mmap[i].base % config->remote_ha_mmap[i].size ==
-            0);
-
-        blocks = config->remote_ha_mmap[i].size / (64 * 1024);
-        sz = fwk_math_log2(blocks);
-        ctx->cxg_ra_reg->CXG_RA_SAM_ADDR_REGION_REG[i] = sz |
-            (config->remote_ha_mmap[i].base) |
-            ((uint64_t)ctx->cxg_ha_id_remote << SAM_ADDR_HOME_AGENT_ID_SHIFT) |
-            (SAM_ADDR_REG_VALID_MASK);
+	/* Size also must be a power of two */
+	fwk_assert(
+	    (config->remote_ha_mmap[i].size &
+	     (config->remote_ha_mmap[i].size - 1)) == 0);
+
+	/* Region base should be naturally aligned to the region size */
+	fwk_assert(
+	    config->remote_ha_mmap[i].base % config->remote_ha_mmap[i].size ==
+	    0);
+
+	blocks = config->remote_ha_mmap[i].size / (64 * 1024);
+	sz = fwk_math_log2(blocks);
+	ctx->cxg_ra_reg->CXG_RA_SAM_ADDR_REGION_REG[i] = sz |
+	    (config->remote_ha_mmap[i].base) |
+	    ((uint64_t)ctx->cxg_ha_id_remote << SAM_ADDR_HOME_AGENT_ID_SHIFT) |
+	    (SAM_ADDR_REG_VALID_MASK);
     }
 }
 
@@ -342,64 +342,64 @@ static int enable_and_start_ccix_link_up
     struct cxg_wait_condition_data wait_data;
 
     if (link_id > 2)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     wait_data.ctx = ctx;
     wait_data.link_id = link_id;
 
     if (config->ccix_opt_tlp)
-        ctx->cxla_reg->CXLA_CCIX_PROP_CONFIGURED |= PCIE_OPT_HDR_MASK;
+	ctx->cxla_reg->CXLA_CCIX_PROP_CONFIGURED |= PCIE_OPT_HDR_MASK;
     else
-        ctx->cxla_reg->CXLA_CCIX_PROP_CONFIGURED &= ~PCIE_OPT_HDR_MASK;
+	ctx->cxla_reg->CXLA_CCIX_PROP_CONFIGURED &= ~PCIE_OPT_HDR_MASK;
 
     if (config->ccix_msg_pack_enable) {
-        ctx->cxla_reg->CXLA_CCIX_PROP_CONFIGURED &=
-            ~(1U << CXLA_CCIX_PROP_MSG_PACK_SHIFT_VAL);
+	ctx->cxla_reg->CXLA_CCIX_PROP_CONFIGURED &=
+	    ~(1U << CXLA_CCIX_PROP_MSG_PACK_SHIFT_VAL);
     }
 
     ctx->cxla_reg->CXLA_CCIX_PROP_CONFIGURED &=
-        ~CXLA_CCIX_PROP_MAX_PACK_SIZE_MASK;
+	~CXLA_CCIX_PROP_MAX_PACK_SIZE_MASK;
 
     ctx->cxla_reg->CXLA_CCIX_PROP_CONFIGURED |=
-        (config->ccix_max_packet_size
-         << CXLA_CCIX_PROP_MAX_PACK_SIZE_SHIFT_VAL);
+	(config->ccix_max_packet_size
+	 << CXLA_CCIX_PROP_MAX_PACK_SIZE_SHIFT_VAL);
 
     ctx->cxla_reg->CXLA_LINKID_TO_PCIE_BUS_NUM =
-        ((uint64_t)config->pcie_bus_num << (link_id * 16));
+	((uint64_t)config->pcie_bus_num << (link_id * 16));
 
     /* Set up TC1 for PCIe so CCIx uses VC1 */
     val1 =
-        ctx->cxla_reg->CXLA_PCIE_HDR_FIELDS & ~CXLA_PCIE_HDR_TRAFFIC_CLASS_MASK;
+	ctx->cxla_reg->CXLA_PCIE_HDR_FIELDS & ~CXLA_PCIE_HDR_TRAFFIC_CLASS_MASK;
 
     ctx->cxla_reg->CXLA_PCIE_HDR_FIELDS = val1 |
-        ((uint64_t)config->ccix_tc << CXLA_PCIE_HDR_TRAFFIC_CLASS_SHIFT_VAL);
+	((uint64_t)config->ccix_tc << CXLA_PCIE_HDR_TRAFFIC_CLASS_SHIFT_VAL);
 
     val1 = ctx->cxla_reg->CXLA_PCIE_HDR_FIELDS & ~CXLA_PCIE_HDR_VENDOR_ID_MASK;
 
     ctx->cxla_reg->CXLA_PCIE_HDR_FIELDS =
-        val1 | ((uint64_t)CCIX_VENDER_ID << CXLA_PCIE_HDR_VENDOR_ID_SHIFT_VAL);
+	val1 | ((uint64_t)CCIX_VENDER_ID << CXLA_PCIE_HDR_VENDOR_ID_SHIFT_VAL);
 
     FWK_LOG_INFO(
-        MOD_NAME "CXLA_PCIE_HDR_FIELDS: 0x%" PRIx64,
-        ctx->cxla_reg->CXLA_PCIE_HDR_FIELDS);
+	MOD_NAME "CXLA_PCIE_HDR_FIELDS: 0x%" PRIx64,
+	ctx->cxla_reg->CXLA_PCIE_HDR_FIELDS);
 
     FWK_LOG_INFO(MOD_NAME "Enabling CCIX link %d...", link_id);
     /* Set link enable bit to enable the CCIX link */
     ctx->cxg_ra_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_CTRL =
-        CXG_LINK_CTRL_EN_MASK;
+	CXG_LINK_CTRL_EN_MASK;
     ctx->cxg_ha_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_CTRL =
-        CXG_LINK_CTRL_EN_MASK;
+	CXG_LINK_CTRL_EN_MASK;
 
     /* Wait until link enable bits are set */
     wait_data.cond = CXG_LINK_CTRL_EN_BIT_SET;
     status = ctx->timer_api->wait(
-        FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
-        CXG_PRTCL_LINK_CTRL_TIMEOUT,
-        cxg_link_wait_condition,
-        &wait_data);
+	FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
+	CXG_PRTCL_LINK_CTRL_TIMEOUT,
+	cxg_link_wait_condition,
+	&wait_data);
     if (status != FWK_SUCCESS) {
-        FWK_LOG_INFO(MOD_NAME "Failed");
-        return status;
+	FWK_LOG_INFO(MOD_NAME "Failed");
+	return status;
     }
     FWK_LOG_INFO(MOD_NAME "Done");
 
@@ -407,37 +407,37 @@ static int enable_and_start_ccix_link_up
     /* Wait till link up bits are cleared in control register */
     wait_data.cond = CXG_LINK_CTRL_UP_BIT_CLR;
     status = ctx->timer_api->wait(
-        FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
-        CXG_PRTCL_LINK_CTRL_TIMEOUT,
-        cxg_link_wait_condition,
-        &wait_data);
+	FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
+	CXG_PRTCL_LINK_CTRL_TIMEOUT,
+	cxg_link_wait_condition,
+	&wait_data);
     if (status != FWK_SUCCESS) {
-        FWK_LOG_INFO(MOD_NAME "Failed");
-        return status;
+	FWK_LOG_INFO(MOD_NAME "Failed");
+	return status;
     }
 
     /* Wait till link down bits are set in status register */
     wait_data.cond = CXG_LINK_STATUS_DWN_BIT_SET;
     status = ctx->timer_api->wait(
-        FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
-        CXG_PRTCL_LINK_CTRL_TIMEOUT,
-        cxg_link_wait_condition,
-        &wait_data);
+	FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
+	CXG_PRTCL_LINK_CTRL_TIMEOUT,
+	cxg_link_wait_condition,
+	&wait_data);
     if (status != FWK_SUCCESS) {
-        FWK_LOG_INFO(MOD_NAME "Failed");
-        return status;
+	FWK_LOG_INFO(MOD_NAME "Failed");
+	return status;
     }
 
     /* Wait till link ACK bits are cleared in status register */
     wait_data.cond = CXG_LINK_STATUS_ACK_BIT_CLR;
     status = ctx->timer_api->wait(
-        FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
-        CXG_PRTCL_LINK_CTRL_TIMEOUT,
-        cxg_link_wait_condition,
-        &wait_data);
+	FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
+	CXG_PRTCL_LINK_CTRL_TIMEOUT,
+	cxg_link_wait_condition,
+	&wait_data);
     if (status != FWK_SUCCESS) {
-        FWK_LOG_INFO(MOD_NAME "Failed");
-        return status;
+	FWK_LOG_INFO(MOD_NAME "Failed");
+	return status;
     }
 
     FWK_LOG_INFO(MOD_NAME "Done");
@@ -446,32 +446,32 @@ static int enable_and_start_ccix_link_up
 
     /* Bring up link using link request bit */
     ctx->cxg_ra_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_CTRL |=
-        CXG_LINK_CTRL_REQ_MASK;
+	CXG_LINK_CTRL_REQ_MASK;
     ctx->cxg_ha_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_CTRL |=
-        CXG_LINK_CTRL_REQ_MASK;
+	CXG_LINK_CTRL_REQ_MASK;
 
     /* Wait till link ACK bits are set in status register */
     wait_data.cond = CXG_LINK_STATUS_ACK_BIT_SET;
     status = ctx->timer_api->wait(
-        FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
-        CXG_PRTCL_LINK_CTRL_TIMEOUT,
-        cxg_link_wait_condition,
-        &wait_data);
+	FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
+	CXG_PRTCL_LINK_CTRL_TIMEOUT,
+	cxg_link_wait_condition,
+	&wait_data);
     if (status != FWK_SUCCESS) {
-        FWK_LOG_INFO(MOD_NAME "Failed");
-        return status;
+	FWK_LOG_INFO(MOD_NAME "Failed");
+	return status;
     }
 
     /* Wait till link down bits are cleared in status register */
     wait_data.cond = CXG_LINK_STATUS_DWN_BIT_CLR;
     status = ctx->timer_api->wait(
-        FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
-        CXG_PRTCL_LINK_CTRL_TIMEOUT,
-        cxg_link_wait_condition,
-        &wait_data);
+	FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
+	CXG_PRTCL_LINK_CTRL_TIMEOUT,
+	cxg_link_wait_condition,
+	&wait_data);
     if (status != FWK_SUCCESS) {
-        FWK_LOG_INFO(MOD_NAME "Failed");
-        return status;
+	FWK_LOG_INFO(MOD_NAME "Failed");
+	return status;
     }
     FWK_LOG_INFO(MOD_NAME "Done");
     return FWK_SUCCESS;
@@ -492,7 +492,7 @@ int ccix_setup(struct cmn_skeena_ctx *ct
     int status;
 
     struct mod_cmn_skeena_ccix_remote_node_config *ccix_remote_config =
-        (struct mod_cmn_skeena_ccix_remote_node_config *)remote_config;
+	(struct mod_cmn_skeena_ccix_remote_node_config *)remote_config;
 
     FWK_LOG_INFO(MOD_NAME "Programming CCIX gateway...");
 
@@ -501,16 +501,16 @@ int ccix_setup(struct cmn_skeena_ctx *ct
      * to uint8_t
      */
     fwk_assert(
-        (ctx->external_rnsam_count > 1) &&
-        (ctx->external_rnsam_count <= (0xFF + 1)));
+	(ctx->external_rnsam_count > 1) &&
+	(ctx->external_rnsam_count <= (0xFF + 1)));
 
     /* Number of local RAs */
     local_ra_cnt = ctx->internal_rnsam_count + ctx->external_rnsam_count;
 
     if (ccix_remote_config->smp_mode == true) {
-        status = enable_smp_mode(ctx);
-        if (status != FWK_SUCCESS)
-            return status;
+	status = enable_smp_mode(ctx);
+	if (status != FWK_SUCCESS)
+	    return status;
     }
 
     /* Set initial RAID value to 0. */
@@ -519,23 +519,23 @@ int ccix_setup(struct cmn_skeena_ctx *ct
     offset_id = ctx->chip_id * local_ra_cnt;
 
     for (rnf_ldid = 0; rnf_ldid < ctx->rnf_count; rnf_ldid++) {
-        agent_id = ctx->raid_value + offset_id;
+	agent_id = ctx->raid_value + offset_id;
 
-        /* Program RAID values in CXRA LDID to RAID LUT */
-        program_cxg_ra_rnf_ldid_to_raid_reg(ctx, rnf_ldid, agent_id);
+	/* Program RAID values in CXRA LDID to RAID LUT */
+	program_cxg_ra_rnf_ldid_to_raid_reg(ctx, rnf_ldid, agent_id);
 
-        /*
-         * Program agentid to linkid LUT for
-         * remote agents in CXRA/CXHA/CXLA
-         */
-        program_agentid_to_linkid_reg(
-            ctx, agent_id, ccix_remote_config->ccix_link_id);
-
-        /*
-         * The HN-F ldid to CHI node id valid bit for
-         * local RN-F agents is already programmed
-         */
-        program_cxg_ha_id(ctx, ccix_remote_config->remote_ha_mmap[0].ha_id);
+	/*
+	 * Program agentid to linkid LUT for
+	 * remote agents in CXRA/CXHA/CXLA
+	 */
+	program_agentid_to_linkid_reg(
+	    ctx, agent_id, ccix_remote_config->ccix_link_id);
+
+	/*
+	 * The HN-F ldid to CHI node id valid bit for
+	 * local RN-F agents is already programmed
+	 */
+	program_cxg_ha_id(ctx, ccix_remote_config->remote_ha_mmap[0].ha_id);
     }
 
     /*
@@ -545,69 +545,69 @@ int ccix_setup(struct cmn_skeena_ctx *ct
     unique_remote_rnf_ldid_value = ctx->rnf_count;
 
     for (i = 0; i < ccix_remote_config->remote_rnf_count; i++) {
-        block = i / ctx->rnf_count;
+	block = i / ctx->rnf_count;
 
-        /*
-         * The remote_agent_id should not include the current chip's agent ids.
-         * If `block` is less than the current chip_id, then include the agent
-         * ids of chip 0 till (not including) current chip. If the block is
-         * equal or greater than the current chip, then include the agent id
-         * from next chip till the max chip.
-         */
-        if (block < ctx->chip_id)
-            remote_agent_id = i + (ctx->rnf_count * block);
-        else
-            remote_agent_id = i + (ctx->rnf_count * block) + local_ra_cnt;
-
-        /* Program the CXHA raid to ldid LUT */
-        program_cxg_ha_raid_to_ldid_lut(
-            ctx, remote_agent_id, unique_remote_rnf_ldid_value);
-        /*
-         * Program HN-F ldid to CHI node id for
-         * remote RN-F agents
-         */
-        program_hnf_ldid_to_chi_node_id_reg(
-            ctx,
-            unique_remote_rnf_ldid_value,
-            ctx->cxg_ha_node_id,
-            REMOTE_CCIX_NODE);
+	/*
+	 * The remote_agent_id should not include the current chip's agent ids.
+	 * If `block` is less than the current chip_id, then include the agent
+	 * ids of chip 0 till (not including) current chip. If the block is
+	 * equal or greater than the current chip, then include the agent id
+	 * from next chip till the max chip.
+	 */
+	if (block < ctx->chip_id)
+	    remote_agent_id = i + (ctx->rnf_count * block);
+	else
+	    remote_agent_id = i + (ctx->rnf_count * block) + local_ra_cnt;
+
+	/* Program the CXHA raid to ldid LUT */
+	program_cxg_ha_raid_to_ldid_lut(
+	    ctx, remote_agent_id, unique_remote_rnf_ldid_value);
+	/*
+	 * Program HN-F ldid to CHI node id for
+	 * remote RN-F agents
+	 */
+	program_hnf_ldid_to_chi_node_id_reg(
+	    ctx,
+	    unique_remote_rnf_ldid_value,
+	    ctx->cxg_ha_node_id,
+	    REMOTE_CCIX_NODE);
 
-        unique_remote_rnf_ldid_value++;
+	unique_remote_rnf_ldid_value++;
     }
 
     offset_id = ctx->chip_id * local_ra_cnt;
 
     for (i = 0; i < ctx->rnd_count; i++) {
-        rnd_ldid = ctx->rnd_ldid[i];
+	rnd_ldid = ctx->rnd_ldid[i];
 
-        /* Determine agent_id of the remote agents */
-        agent_id = ctx->raid_value + offset_id;
+	/* Determine agent_id of the remote agents */
+	agent_id = ctx->raid_value + offset_id;
 
-        /* Program raid values in CXRA LDID to RAID LUT */
-        program_cxg_ra_rnd_ldid_to_raid_reg(ctx, rnd_ldid, agent_id);
+	/* Program raid values in CXRA LDID to RAID LUT */
+	program_cxg_ra_rnd_ldid_to_raid_reg(ctx, rnd_ldid, agent_id);
 
-        /* Program agentid to linkid LUT for remote agents */
-        program_agentid_to_linkid_reg(
-            ctx, agent_id, ccix_remote_config->ccix_link_id);
+	/* Program agentid to linkid LUT for remote agents */
+	program_agentid_to_linkid_reg(
+	    ctx, agent_id, ccix_remote_config->ccix_link_id);
     }
 
     for (i = 0; i < ctx->rni_count; i++) {
-        rni_ldid = ctx->rni_ldid[i];
+	rni_ldid = ctx->rni_ldid[i];
 
-        /* Determine agentid of the remote agents */
-        agent_id = ctx->raid_value + offset_id;
+	/* Determine agentid of the remote agents */
+	agent_id = ctx->raid_value + offset_id;
 
-        /* Program raid values in CXRA LDID to RAID LUT */
-        program_cxg_ra_rni_ldid_to_raid_reg(ctx, rni_ldid, agent_id);
+	/* Program raid values in CXRA LDID to RAID LUT */
+	program_cxg_ra_rni_ldid_to_raid_reg(ctx, rni_ldid, agent_id);
 
-        /* Program agentid to linkid LUT for remote agents */
-        program_agentid_to_linkid_reg(
-            ctx, agent_id, ccix_remote_config->ccix_link_id);
+	/* Program agentid to linkid LUT for remote agents */
+	program_agentid_to_linkid_reg(
+	    ctx, agent_id, ccix_remote_config->ccix_link_id);
     }
 
     program_cxg_ra_sam_addr_region(ctx, ccix_remote_config);
     status = enable_and_start_ccix_link_up_sequence(
-        ctx, ccix_remote_config, ccix_remote_config->ccix_link_id);
+	ctx, ccix_remote_config, ccix_remote_config->ccix_link_id);
 
     return status;
 }
@@ -615,15 +615,15 @@ int ccix_setup(struct cmn_skeena_ctx *ct
 int ccix_exchange_protocol_credit(struct cmn_skeena_ctx *ctx, uint8_t link_id)
 {
     if (link_id > 2)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     FWK_LOG_INFO(
-        MOD_NAME "Exchanging protocol credits for link %d...", link_id);
+	MOD_NAME "Exchanging protocol credits for link %d...", link_id);
     /* Exchange protocol credits using link up bit */
     ctx->cxg_ra_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_CTRL |=
-        CXG_LINK_CTRL_UP_MASK;
+	CXG_LINK_CTRL_UP_MASK;
     ctx->cxg_ha_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_CTRL |=
-        CXG_LINK_CTRL_UP_MASK;
+	CXG_LINK_CTRL_UP_MASK;
     FWK_LOG_INFO(MOD_NAME "Done");
     return FWK_SUCCESS;
 }
@@ -634,7 +634,7 @@ int ccix_enter_system_coherency(struct c
     int status;
 
     if (link_id > 2)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     wait_data.ctx = ctx;
     wait_data.link_id = link_id;
@@ -642,18 +642,18 @@ int ccix_enter_system_coherency(struct c
     FWK_LOG_INFO(MOD_NAME "Entering system coherency for link %d...", link_id);
     /* Enter system coherency by setting DVMDOMAIN request bit */
     ctx->cxg_ha_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_CTRL |=
-        CXG_LINK_CTRL_DVMDOMAIN_REQ_MASK;
+	CXG_LINK_CTRL_DVMDOMAIN_REQ_MASK;
 
     /* Wait till DVMDOMAIN ACK bit is set in status register */
     wait_data.cond = CXG_LINK_STATUS_HA_DVMDOMAIN_ACK_BIT_SET;
     status = ctx->timer_api->wait(
-        FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
-        CXG_PRTCL_LINK_DVMDOMAIN_TIMEOUT,
-        cxg_link_wait_condition,
-        &wait_data);
+	FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
+	CXG_PRTCL_LINK_DVMDOMAIN_TIMEOUT,
+	cxg_link_wait_condition,
+	&wait_data);
     if (status != FWK_SUCCESS) {
-        FWK_LOG_INFO(MOD_NAME "Failed");
-        return status;
+	FWK_LOG_INFO(MOD_NAME "Failed");
+	return status;
     }
 
     FWK_LOG_INFO(MOD_NAME "Done");
@@ -666,7 +666,7 @@ int ccix_enter_dvm_domain(struct cmn_ske
     int status;
 
     if (link_id > 2)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     wait_data.ctx = ctx;
     wait_data.link_id = link_id;
@@ -674,18 +674,18 @@ int ccix_enter_dvm_domain(struct cmn_ske
     FWK_LOG_INFO(MOD_NAME "Entering DVM domain for link %d...", link_id);
     /* Enter system coherency by setting DVMDOMAIN request bit */
     ctx->cxg_ra_reg->LINK_REGS[link_id].CXG_PRTCL_LINK_CTRL |=
-        CXG_LINK_CTRL_DVMDOMAIN_REQ_MASK;
+	CXG_LINK_CTRL_DVMDOMAIN_REQ_MASK;
 
     /* Wait till DVMDOMAIN ACK bit is set in status register */
     wait_data.cond = CXG_LINK_STATUS_RA_DVMDOMAIN_ACK_BIT_SET;
     status = ctx->timer_api->wait(
-        FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
-        CXG_PRTCL_LINK_DVMDOMAIN_TIMEOUT,
-        cxg_link_wait_condition,
-        &wait_data);
+	FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
+	CXG_PRTCL_LINK_DVMDOMAIN_TIMEOUT,
+	cxg_link_wait_condition,
+	&wait_data);
     if (status != FWK_SUCCESS) {
-        FWK_LOG_INFO(MOD_NAME "Failed");
-        return status;
+	FWK_LOG_INFO(MOD_NAME "Failed");
+	return status;
     }
 
     FWK_LOG_INFO(MOD_NAME "Done");
@@ -696,30 +696,30 @@ void ccix_capabilities_get(struct cmn_sk
 {
     /* Populate maximum credit send capability */
     ctx->ccix_host_info.ccix_request_credits =
-        (ctx->cxg_ra_reg->CXG_RA_UNIT_INFO &
-         CXG_RA_REQUEST_TRACKER_DEPTH_MASK) >>
-        CXG_RA_REQUEST_TRACKER_DEPTH_VAL;
+	(ctx->cxg_ra_reg->CXG_RA_UNIT_INFO &
+	 CXG_RA_REQUEST_TRACKER_DEPTH_MASK) >>
+	CXG_RA_REQUEST_TRACKER_DEPTH_VAL;
 
     ctx->ccix_host_info.ccix_snoop_credits =
-        (ctx->cxg_ha_reg->CXG_HA_UNIT_INFO & CXG_HA_SNOOP_TRACKER_DEPTH_MASK) >>
-        CXG_HA_SNOOP_TRACKER_DEPTH_VAL;
+	(ctx->cxg_ha_reg->CXG_HA_UNIT_INFO & CXG_HA_SNOOP_TRACKER_DEPTH_MASK) >>
+	CXG_HA_SNOOP_TRACKER_DEPTH_VAL;
 
     ctx->ccix_host_info.ccix_data_credits =
-        (ctx->cxg_ha_reg->CXG_HA_UNIT_INFO & CXG_HA_WDB_DEPTH_MASK) >>
-        CXG_HA_WDB_DEPTH_VAL;
+	(ctx->cxg_ha_reg->CXG_HA_UNIT_INFO & CXG_HA_WDB_DEPTH_MASK) >>
+	CXG_HA_WDB_DEPTH_VAL;
 
     /* Populate max packet size capability */
     ctx->ccix_host_info.ccix_max_packet_size =
-        (ctx->cxla_reg->CXLA_CCIX_PROP_CAPABILITIES &
-         CXLA_CCIX_PROP_MAX_PACK_SIZE_MASK) >>
-        CXLA_CCIX_PROP_MAX_PACK_SIZE_SHIFT_VAL;
+	(ctx->cxla_reg->CXLA_CCIX_PROP_CAPABILITIES &
+	 CXLA_CCIX_PROP_MAX_PACK_SIZE_MASK) >>
+	CXLA_CCIX_PROP_MAX_PACK_SIZE_SHIFT_VAL;
 
     /* Optimized TLP is always supported by CMN-600 */
     ctx->ccix_host_info.ccix_opt_tlp = true;
 
     /* Populate message packing capability */
     ctx->ccix_host_info.ccix_msg_pack_enable =
-        !((ctx->cxla_reg->CXLA_CCIX_PROP_CAPABILITIES &
-           CXLA_CCIX_PROP_MSG_PACK_SHIFT_MASK) >>
-          CXLA_CCIX_PROP_MSG_PACK_SHIFT_VAL);
+	!((ctx->cxla_reg->CXLA_CCIX_PROP_CAPABILITIES &
+	   CXLA_CCIX_PROP_MSG_PACK_SHIFT_MASK) >>
+	  CXLA_CCIX_PROP_MSG_PACK_SHIFT_VAL);
 }
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cmn_skeena/src/mod_cmn_skeena.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/cmn_skeena/src/mod_cmn_skeena.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/cmn_skeena/src/mod_cmn_skeena.c
@@ -36,7 +36,7 @@
 static inline size_t cmn_skeena_hnf_cache_group_count(size_t hnf_count)
 {
     return (hnf_count + CMN_SKEENA_HNF_CACHE_GROUP_ENTRIES_PER_GROUP - 1) /
-        CMN_SKEENA_HNF_CACHE_GROUP_ENTRIES_PER_GROUP;
+	CMN_SKEENA_HNF_CACHE_GROUP_ENTRIES_PER_GROUP;
 }
 
 struct cmn_skeena_ctx *ctx;
@@ -66,68 +66,68 @@ static void process_node_hnf(struct cmn_
      * only for the even numbered hnf nodes.
      */
     if (config->hnf_cal_mode == true && (node_id % 2 == 1) &&
-        is_cal_mode_supported(ctx->root)) {
-        /* Factor to manipulate the group and bit_pos */
-        cal_mode_factor = 2;
-
-        /*
-         * Reduce the hnf_count as the current hnf node is not getting included
-         * in the sys_cache_grp_hn_nodeid register
-         */
-        ctx->hnf_count--;
+	is_cal_mode_supported(ctx->root)) {
+	/* Factor to manipulate the group and bit_pos */
+	cal_mode_factor = 2;
+
+	/*
+	 * Reduce the hnf_count as the current hnf node is not getting included
+	 * in the sys_cache_grp_hn_nodeid register
+	 */
+	ctx->hnf_count--;
     }
 
     fwk_assert(logical_id < config->snf_count);
 
     group = logical_id /
-        (CMN_SKEENA_HNF_CACHE_GROUP_ENTRIES_PER_GROUP * cal_mode_factor);
+	(CMN_SKEENA_HNF_CACHE_GROUP_ENTRIES_PER_GROUP * cal_mode_factor);
     bit_pos = (CMN_SKEENA_HNF_CACHE_GROUP_ENTRY_BITS_WIDTH / cal_mode_factor) *
-        (logical_id %
-         (CMN_SKEENA_HNF_CACHE_GROUP_ENTRIES_PER_GROUP * cal_mode_factor));
+	(logical_id %
+	 (CMN_SKEENA_HNF_CACHE_GROUP_ENTRIES_PER_GROUP * cal_mode_factor));
 
     /*
      * If CAL mode is set, add only even numbered hnd node to
      * sys_cache_grp_hn_nodeid registers
      */
     if (config->hnf_cal_mode == true && is_cal_mode_supported(ctx->root)) {
-        if (node_id % 2 == 0)
-            ctx->hnf_cache_group[group] += ((uint64_t)get_node_id(hnf))
-                << bit_pos;
+	if (node_id % 2 == 0)
+	    ctx->hnf_cache_group[group] += ((uint64_t)get_node_id(hnf))
+		<< bit_pos;
     } else
-        ctx->hnf_cache_group[group] += ((uint64_t)get_node_id(hnf)) << bit_pos;
+	ctx->hnf_cache_group[group] += ((uint64_t)get_node_id(hnf)) << bit_pos;
 
     /* Set target node */
     hnf->SAM_CONTROL = config->snf_table[logical_id];
 
     if (ctx->chip_id != 0) {
-        base_offset = ((uint64_t)(ctx->config->chip_addr_space * ctx->chip_id));
+	base_offset = ((uint64_t)(ctx->config->chip_addr_space * ctx->chip_id));
     } else
-        base_offset = 0;
+	base_offset = 0;
 
     /*
      * Map sub-regions to this HN-F node
      */
     for (region_idx = 0; region_idx < config->mmap_count; region_idx++) {
-        region = &config->mmap_table[region_idx];
+	region = &config->mmap_table[region_idx];
 
-        /* Skip non sub-regions */
-        if (region->type != MOD_CMN_SKEENA_REGION_TYPE_SYSCACHE_SUB)
-            continue;
-
-        /* Configure sub-region entry */
-        hnf->SAM_MEMREGION[region_sub_count] = region->node_id |
-            (sam_encode_region_size(region->size)
-             << CMN_SKEENA_HNF_SAM_MEMREGION_SIZE_POS) |
-            (((region->base + base_offset) / SAM_GRANULARITY)
-             << CMN_SKEENA_HNF_SAM_MEMREGION_BASE_POS) |
-            CMN_SKEENA_HNF_SAM_MEMREGION_VALID;
+	/* Skip non sub-regions */
+	if (region->type != MOD_CMN_SKEENA_REGION_TYPE_SYSCACHE_SUB)
+	    continue;
+
+	/* Configure sub-region entry */
+	hnf->SAM_MEMREGION[region_sub_count] = region->node_id |
+	    (sam_encode_region_size(region->size)
+	     << CMN_SKEENA_HNF_SAM_MEMREGION_SIZE_POS) |
+	    (((region->base + base_offset) / SAM_GRANULARITY)
+	     << CMN_SKEENA_HNF_SAM_MEMREGION_BASE_POS) |
+	    CMN_SKEENA_HNF_SAM_MEMREGION_VALID;
 
-        region_sub_count++;
+	region_sub_count++;
     }
 
     /* Configure the system cache RAM PPU */
     hnf->PPU_PWPR = CMN_SKEENA_PPU_PWPR_POLICY_ON |
-        CMN_SKEENA_PPU_PWPR_OPMODE_FAM | CMN_SKEENA_PPU_PWPR_DYN_EN;
+	CMN_SKEENA_PPU_PWPR_OPMODE_FAM | CMN_SKEENA_PPU_PWPR_DYN_EN;
 }
 
 static void process_node_rni(
@@ -142,10 +142,10 @@ static void process_node_rni(
 
     rni = (struct cmn_skeena_rni_reg *)node;
     if (config->rni_pcie_count != 0) {
-        for (i = 0; i < config->rni_pcie_count; i++) {
-            if (config->rni_pcie_table[i] == node_id)
-                rni->CFG_CTL |= CMN_SKEENA_RNI_PCIE_MSTR_PRE;
-        }
+	for (i = 0; i < config->rni_pcie_count; i++) {
+	    if (config->rni_pcie_table[i] == node_id)
+		rni->CFG_CTL |= CMN_SKEENA_RNI_PCIE_MSTR_PRE;
+	}
     }
 }
 
@@ -163,15 +163,15 @@ static void process_node_hni(
 
     hni = (struct cmn_skeena_hni_reg *)node;
     if (config->hni_mmap_count != 0) {
-        for (i = 0; i < config->hni_mmap_count; i++) {
-            region = &config->hni_mmap_table[i];
-            if (region->node_id == node_id) {
-                region_size = __builtin_ctz(region->size / (4ULL * FWK_KIB));
-                hni->SAM_ADDRREGION_CFG[region->region_num] =
-                    (region_size | (region_size << 10) |
-                     ((region->base >> 12) << 20) | region->flags);
-            }
-        }
+	for (i = 0; i < config->hni_mmap_count; i++) {
+	    region = &config->hni_mmap_table[i];
+	    if (region->node_id == node_id) {
+		region_size = __builtin_ctz(region->size / (4ULL * FWK_KIB));
+		hni->SAM_ADDRREGION_CFG[region->region_num] =
+		    (region_size | (region_size << 10) |
+		     ((region->base >> 12) << 20) | region->flags);
+	    }
+	}
     }
 }
 
@@ -199,106 +199,106 @@ static int cmn_skeena_discovery(void)
     /* Traverse cross points (XP) */
     xp_count = get_node_child_count(ctx->root);
     for (xp_idx = 0; xp_idx < xp_count; xp_idx++) {
-        xp = get_child_node(config->base, ctx->root, xp_idx);
-        fwk_assert(get_node_type(xp) == NODE_TYPE_XP);
+	xp = get_child_node(config->base, ctx->root, xp_idx);
+	fwk_assert(get_node_type(xp) == NODE_TYPE_XP);
 
-        FWK_LOG_INFO(MOD_NAME);
+	FWK_LOG_INFO(MOD_NAME);
 
-        FWK_LOG_INFO(
-            MOD_NAME "XP (%d, %d) ID:%d, LID:%d",
-            get_node_pos_x(xp),
-            get_node_pos_y(xp),
-            get_node_id(xp),
-            get_node_logical_id(xp));
-
-        /* Traverse nodes */
-        node_count = get_node_child_count(xp);
-        for (node_idx = 0; node_idx < node_count; node_idx++) {
-            node = get_child_node(config->base, xp, node_idx);
-
-            /* External nodes */
-            if (is_child_external(xp, node_idx)) {
-                xp_port = get_port_number(get_child_node_id(xp, node_idx));
-
-                /*
-                 * If the device type is CXRH, CXHA, or CXRA, then the external
-                 * child node is CXLA as every CXRH, CXHA, or CXRA node has a
-                 * corresponding external CXLA node.
-                 */
-                if ((get_device_type(xp, xp_port) == DEVICE_TYPE_CXRH) ||
-                    (get_device_type(xp, xp_port) == DEVICE_TYPE_CXHA) ||
-                    (get_device_type(xp, xp_port) == DEVICE_TYPE_CXRA)) {
-                    ctx->cxla_reg = (void *)node;
-                    FWK_LOG_INFO(
-                        MOD_NAME "  Found CXLA at node ID: %d",
-                        get_child_node_id(xp, node_idx));
-                } else { /* External RN-SAM Node */
-                    ctx->external_rnsam_count++;
-                    FWK_LOG_INFO(
-                        MOD_NAME "  Found external node ID: %d",
-                        get_child_node_id(xp, node_idx));
-                }
-            } else { /* Internal nodes */
-                switch (get_node_type(node)) {
-                case NODE_TYPE_HN_F:
-                    if (ctx->hnf_count >= MAX_HNF_COUNT) {
-                        FWK_LOG_INFO(
-                            MOD_NAME "  hnf count %d >= max limit (%d)",
-                            ctx->hnf_count,
-                            MAX_HNF_COUNT);
-                        return FWK_E_DATA;
-                    }
-                    ctx->hnf_offset[ctx->hnf_count++] = (uint32_t)node;
-                    break;
-
-                case NODE_TYPE_RN_SAM:
-                    ctx->internal_rnsam_count++;
-                    break;
-
-                case NODE_TYPE_RN_D:
-                    if (ctx->rnd_count >= MAX_RND_COUNT) {
-                        FWK_LOG_INFO(
-                            MOD_NAME "  rnd count %d >= max limit (%d)",
-                            ctx->rnd_count,
-                            MAX_RND_COUNT);
-                        return FWK_E_DATA;
-                    }
-                    ctx->rnd_ldid[ctx->rnd_count++] = get_node_logical_id(node);
-                    break;
-
-                case NODE_TYPE_RN_I:
-                    if (ctx->rni_count >= MAX_RNI_COUNT) {
-                        FWK_LOG_INFO(
-                            MOD_NAME "  rni count %d >= max limit (%d)",
-                            ctx->rni_count,
-                            MAX_RNI_COUNT);
-                        return FWK_E_DATA;
-                    }
-                    ctx->rni_ldid[ctx->rni_count++] = get_node_logical_id(node);
-                    break;
-
-                case NODE_TYPE_CXRA:
-                    ctx->cxg_ra_reg = (struct cmn_skeena_cxg_ra_reg *)node;
-                    break;
-
-                case NODE_TYPE_CXHA:
-                    ctx->cxg_ha_reg = (struct cmn_skeena_cxg_ha_reg *)node;
-                    ctx->ccix_host_info.host_ha_count++;
-                    break;
-
-                default:
-                    /* Nothing to be done for other node types */
-                    break;
-                }
-
-                FWK_LOG_INFO(
-                    MOD_NAME "  %s ID:%d, LID:%d @0x%" PRIX32,
-                    get_node_type_name(get_node_type(node)),
-                    get_node_id(node),
-                    get_node_logical_id(node),
-                    (uint32_t)node);
-            }
-        }
+	FWK_LOG_INFO(
+	    MOD_NAME "XP (%d, %d) ID:%d, LID:%d",
+	    get_node_pos_x(xp),
+	    get_node_pos_y(xp),
+	    get_node_id(xp),
+	    get_node_logical_id(xp));
+
+	/* Traverse nodes */
+	node_count = get_node_child_count(xp);
+	for (node_idx = 0; node_idx < node_count; node_idx++) {
+	    node = get_child_node(config->base, xp, node_idx);
+
+	    /* External nodes */
+	    if (is_child_external(xp, node_idx)) {
+		xp_port = get_port_number(get_child_node_id(xp, node_idx));
+
+		/*
+		 * If the device type is CXRH, CXHA, or CXRA, then the external
+		 * child node is CXLA as every CXRH, CXHA, or CXRA node has a
+		 * corresponding external CXLA node.
+		 */
+		if ((get_device_type(xp, xp_port) == DEVICE_TYPE_CXRH) ||
+		    (get_device_type(xp, xp_port) == DEVICE_TYPE_CXHA) ||
+		    (get_device_type(xp, xp_port) == DEVICE_TYPE_CXRA)) {
+		    ctx->cxla_reg = (void *)node;
+		    FWK_LOG_INFO(
+			MOD_NAME "  Found CXLA at node ID: %d",
+			get_child_node_id(xp, node_idx));
+		} else { /* External RN-SAM Node */
+		    ctx->external_rnsam_count++;
+		    FWK_LOG_INFO(
+			MOD_NAME "  Found external node ID: %d",
+			get_child_node_id(xp, node_idx));
+		}
+	    } else { /* Internal nodes */
+		switch (get_node_type(node)) {
+		case NODE_TYPE_HN_F:
+		    if (ctx->hnf_count >= MAX_HNF_COUNT) {
+			FWK_LOG_INFO(
+			    MOD_NAME "  hnf count %d >= max limit (%d)",
+			    ctx->hnf_count,
+			    MAX_HNF_COUNT);
+			return FWK_E_DATA;
+		    }
+		    ctx->hnf_offset[ctx->hnf_count++] = (uint32_t)node;
+		    break;
+
+		case NODE_TYPE_RN_SAM:
+		    ctx->internal_rnsam_count++;
+		    break;
+
+		case NODE_TYPE_RN_D:
+		    if (ctx->rnd_count >= MAX_RND_COUNT) {
+			FWK_LOG_INFO(
+			    MOD_NAME "  rnd count %d >= max limit (%d)",
+			    ctx->rnd_count,
+			    MAX_RND_COUNT);
+			return FWK_E_DATA;
+		    }
+		    ctx->rnd_ldid[ctx->rnd_count++] = get_node_logical_id(node);
+		    break;
+
+		case NODE_TYPE_RN_I:
+		    if (ctx->rni_count >= MAX_RNI_COUNT) {
+			FWK_LOG_INFO(
+			    MOD_NAME "  rni count %d >= max limit (%d)",
+			    ctx->rni_count,
+			    MAX_RNI_COUNT);
+			return FWK_E_DATA;
+		    }
+		    ctx->rni_ldid[ctx->rni_count++] = get_node_logical_id(node);
+		    break;
+
+		case NODE_TYPE_CXRA:
+		    ctx->cxg_ra_reg = (struct cmn_skeena_cxg_ra_reg *)node;
+		    break;
+
+		case NODE_TYPE_CXHA:
+		    ctx->cxg_ha_reg = (struct cmn_skeena_cxg_ha_reg *)node;
+		    ctx->ccix_host_info.host_ha_count++;
+		    break;
+
+		default:
+		    /* Nothing to be done for other node types */
+		    break;
+		}
+
+		FWK_LOG_INFO(
+		    MOD_NAME "  %s ID:%d, LID:%d @0x%" PRIX32,
+		    get_node_type_name(get_node_type(node)),
+		    get_node_id(node),
+		    get_node_logical_id(node),
+		    (uint32_t)node);
+	    }
+	}
     }
 
     /*
@@ -307,41 +307,41 @@ static int cmn_skeena_discovery(void)
      * RN-SAM count minus the total RN-D, RN-I and CXHA count combined.
      */
     ctx->rnf_count = ctx->internal_rnsam_count + ctx->external_rnsam_count -
-        (ctx->rnd_count + ctx->rni_count + ctx->ccix_host_info.host_ha_count);
+	(ctx->rnd_count + ctx->rni_count + ctx->ccix_host_info.host_ha_count);
 
     if (ctx->rnf_count > MAX_RNF_COUNT) {
-        FWK_LOG_ERR(
-            MOD_NAME "rnf count %d > max limit (%d)",
-            ctx->rnf_count,
-            MAX_RNF_COUNT);
-        return FWK_E_RANGE;
+	FWK_LOG_ERR(
+	    MOD_NAME "rnf count %d > max limit (%d)",
+	    ctx->rnf_count,
+	    MAX_RNF_COUNT);
+	return FWK_E_RANGE;
     }
 
     /* When CAL is present, the number of HN-Fs must be even. */
     if ((ctx->hnf_count % 2 != 0) && (config->hnf_cal_mode == true)) {
-        FWK_LOG_ERR(
-            MOD_NAME "hnf count: %d should be even when CAL mode is set",
-            ctx->hnf_count);
-        return FWK_E_DATA;
+	FWK_LOG_ERR(
+	    MOD_NAME "hnf count: %d should be even when CAL mode is set",
+	    ctx->hnf_count);
+	return FWK_E_DATA;
     }
 
     FWK_LOG_INFO(
-        MOD_NAME "Total internal RN-SAM nodes: %d", ctx->internal_rnsam_count);
+	MOD_NAME "Total internal RN-SAM nodes: %d", ctx->internal_rnsam_count);
     FWK_LOG_INFO(
-        MOD_NAME "Total external RN-SAM nodes: %d", ctx->external_rnsam_count);
+	MOD_NAME "Total external RN-SAM nodes: %d", ctx->external_rnsam_count);
     FWK_LOG_INFO(MOD_NAME "Total HN-F nodes: %d", ctx->hnf_count);
     FWK_LOG_INFO(MOD_NAME "Total RN-F nodes: %d", ctx->rnf_count);
     FWK_LOG_INFO(MOD_NAME "Total RN-D nodes: %d", ctx->rnd_count);
     FWK_LOG_INFO(MOD_NAME "Total RN-I nodes: %d", ctx->rni_count);
 
     if (ctx->cxla_reg != NULL) {
-        FWK_LOG_INFO(MOD_NAME "CCIX CXLA node at: %p", (void *)ctx->cxla_reg);
+	FWK_LOG_INFO(MOD_NAME "CCIX CXLA node at: %p", (void *)ctx->cxla_reg);
     }
     if (ctx->cxg_ra_reg != NULL) {
-        FWK_LOG_INFO(MOD_NAME "CCIX CXRA node at: %p", (void *)ctx->cxg_ra_reg);
+	FWK_LOG_INFO(MOD_NAME "CCIX CXRA node at: %p", (void *)ctx->cxg_ra_reg);
     }
     if (ctx->cxg_ha_reg != NULL) {
-        FWK_LOG_INFO(MOD_NAME "CCIX CXHA node at: %p", (void *)ctx->cxg_ha_reg);
+	FWK_LOG_INFO(MOD_NAME "CCIX CXHA node at: %p", (void *)ctx->cxg_ha_reg);
     }
     return FWK_SUCCESS;
 }
@@ -367,49 +367,49 @@ static void cmn_skeena_configure(void)
     /* Traverse cross points (XP) */
     xp_count = get_node_child_count(ctx->root);
     for (xp_idx = 0; xp_idx < xp_count; xp_idx++) {
-        xp = get_child_node(config->base, ctx->root, xp_idx);
-        fwk_assert(get_node_type(xp) == NODE_TYPE_XP);
+	xp = get_child_node(config->base, ctx->root, xp_idx);
+	fwk_assert(get_node_type(xp) == NODE_TYPE_XP);
 
-        /* Traverse nodes */
-        node_count = get_node_child_count(xp);
-        for (node_idx = 0; node_idx < node_count; node_idx++) {
-            node = get_child_node(config->base, xp, node_idx);
-
-            if (is_child_external(xp, node_idx)) {
-                unsigned int node_id = get_child_node_id(xp, node_idx);
-                xp_port = get_port_number(get_child_node_id(xp, node_idx));
-
-                /* Skip if the device type is CXG */
-                if ((get_device_type(xp, xp_port) == DEVICE_TYPE_CXRH) ||
-                    (get_device_type(xp, xp_port) == DEVICE_TYPE_CXHA) ||
-                    (get_device_type(xp, xp_port) == DEVICE_TYPE_CXRA))
-                    continue;
-
-                fwk_assert(xrnsam_entry < ctx->external_rnsam_count);
-
-                ctx->external_rnsam_table[xrnsam_entry].node_id = node_id;
-                ctx->external_rnsam_table[xrnsam_entry].node = node;
-
-                xrnsam_entry++;
-            } else {
-                enum node_type node_type = get_node_type(node);
-
-                if (node_type == NODE_TYPE_RN_SAM) {
-                    fwk_assert(irnsam_entry < ctx->internal_rnsam_count);
-
-                    ctx->internal_rnsam_table[irnsam_entry] = node;
-
-                    irnsam_entry++;
-                } else if (node_type == NODE_TYPE_HN_F)
-                    process_node_hnf(node);
-                else if (
-                    (node_type == NODE_TYPE_RN_D) ||
-                    (node_type == NODE_TYPE_RN_I))
-                    process_node_rni(config, node, get_node_id(node));
-                else if (node_type == NODE_TYPE_HN_I)
-                    process_node_hni(config, node, get_node_id(node));
-            }
-        }
+	/* Traverse nodes */
+	node_count = get_node_child_count(xp);
+	for (node_idx = 0; node_idx < node_count; node_idx++) {
+	    node = get_child_node(config->base, xp, node_idx);
+
+	    if (is_child_external(xp, node_idx)) {
+		unsigned int node_id = get_child_node_id(xp, node_idx);
+		xp_port = get_port_number(get_child_node_id(xp, node_idx));
+
+		/* Skip if the device type is CXG */
+		if ((get_device_type(xp, xp_port) == DEVICE_TYPE_CXRH) ||
+		    (get_device_type(xp, xp_port) == DEVICE_TYPE_CXHA) ||
+		    (get_device_type(xp, xp_port) == DEVICE_TYPE_CXRA))
+		    continue;
+
+		fwk_assert(xrnsam_entry < ctx->external_rnsam_count);
+
+		ctx->external_rnsam_table[xrnsam_entry].node_id = node_id;
+		ctx->external_rnsam_table[xrnsam_entry].node = node;
+
+		xrnsam_entry++;
+	    } else {
+		enum node_type node_type = get_node_type(node);
+
+		if (node_type == NODE_TYPE_RN_SAM) {
+		    fwk_assert(irnsam_entry < ctx->internal_rnsam_count);
+
+		    ctx->internal_rnsam_table[irnsam_entry] = node;
+
+		    irnsam_entry++;
+		} else if (node_type == NODE_TYPE_HN_F)
+		    process_node_hnf(node);
+		else if (
+		    (node_type == NODE_TYPE_RN_D) ||
+		    (node_type == NODE_TYPE_RN_I))
+		    process_node_rni(config, node, get_node_id(node));
+		else if (node_type == NODE_TYPE_HN_I)
+		    process_node_hni(config, node, get_node_id(node));
+	    }
+	}
     }
 }
 
@@ -442,211 +442,211 @@ int cmn_skeena_setup_sam(struct cmn_skee
     unsigned int scg_regions_enabled[CMN_SKEENA_MAX_NUM_SCG] = { 0, 0, 0, 0 };
 
     FWK_LOG_INFO(
-        MOD_NAME "Configuring RN-SAM for node id %d at 0x%" PRIX32,
-        get_node_id(rnsam),
-        (uint32_t)rnsam);
+	MOD_NAME "Configuring RN-SAM for node id %d at 0x%" PRIX32,
+	get_node_id(rnsam),
+	(uint32_t)rnsam);
 
     for (region_idx = 0; region_idx < config->mmap_count; region_idx++) {
-        region = &config->mmap_table[region_idx];
+	region = &config->mmap_table[region_idx];
 
-        if (ctx->chip_id != 0) {
-            if (region->type == MOD_CMN_SKEENA_REGION_TYPE_CCIX)
-                base = 0;
-            else if (region->type == MOD_CMN_SKEENA_MEMORY_REGION_TYPE_SYSCACHE)
-                base = region->base;
-            else {
-                base =
-                    ((uint64_t)(ctx->config->chip_addr_space * ctx->chip_id) +
-                     region->base);
-            }
-        } else
-            base = region->base;
+	if (ctx->chip_id != 0) {
+	    if (region->type == MOD_CMN_SKEENA_REGION_TYPE_CCIX)
+		base = 0;
+	    else if (region->type == MOD_CMN_SKEENA_MEMORY_REGION_TYPE_SYSCACHE)
+		base = region->base;
+	    else {
+		base =
+		    ((uint64_t)(ctx->config->chip_addr_space * ctx->chip_id) +
+		     region->base);
+	    }
+	} else
+	    base = region->base;
 
 #if FWK_LOG_LEVEL <= FWK_LOG_LEVEL_INFO
-        FWK_LOG_INFO(
-            MOD_NAME "  [0x%08" PRIX32 "%08" PRIX32 " - 0x%08" PRIX32
-                     "%08" PRIX32 "] %s",
-            (uint32_t)(base >> 32),
-            (uint32_t)base,
-            (uint32_t)((base + region->size - 1) >> 32),
-            (uint32_t)(base + region->size - 1),
-            mmap_type_name[region->type]);
+	FWK_LOG_INFO(
+	    MOD_NAME "  [0x%08" PRIX32 "%08" PRIX32 " - 0x%08" PRIX32
+		     "%08" PRIX32 "] %s",
+	    (uint32_t)(base >> 32),
+	    (uint32_t)base,
+	    (uint32_t)((base + region->size - 1) >> 32),
+	    (uint32_t)(base + region->size - 1),
+	    mmap_type_name[region->type]);
 #endif
 
-        switch (region->type) {
-        case MOD_CMN_SKEENA_MEMORY_REGION_TYPE_IO:
-        case MOD_CMN_SKEENA_REGION_TYPE_CCIX:
-            /*
-             * Configure memory region
-             */
-            if (region_io_set_1 &&
-                (region_io_count >
-                 (CMN_SKEENA_RNSAM_MAX_NON_HASH_MEM_REGION_ENTRIES / 2))) {
-                FWK_LOG_ERR(
-                    MOD_NAME
-                    "Non-Hashed Memory can have maximum of %d regions only",
-                    CMN_SKEENA_RNSAM_MAX_NON_HASH_MEM_REGION_ENTRIES);
-                return FWK_E_DATA;
-            }
-
-            group = region_io_count / CMN_SKEENA_RNSAM_REGION_ENTRIES_PER_GROUP;
-            bit_pos =
-                (region_io_count % CMN_SKEENA_RNSAM_REGION_ENTRIES_PER_GROUP) *
-                CMN_SKEENA_RNSAM_REGION_ENTRY_BITS_WIDTH;
-
-            sam_node_type =
-                (region->type == MOD_CMN_SKEENA_MEMORY_REGION_TYPE_IO) ?
-                SAM_NODE_TYPE_HN_I :
-                SAM_NODE_TYPE_CXRA;
-            if (region_io_set_1) {
-                configure_region(
-                    &rnsam->NON_HASH_MEM_REGION1[group],
-                    bit_pos,
-                    base,
-                    region->size,
-                    sam_node_type);
-            } else {
-                configure_region(
-                    &rnsam->NON_HASH_MEM_REGION0[group],
-                    bit_pos,
-                    base,
-                    region->size,
-                    sam_node_type);
-            }
-            /*
-             * Configure target node
-             */
-            group = node_id_io_count /
-                CMN_SKEENA_RNSAM_NON_HASH_TGT_NODEID_ENTRIES_PER_GROUP;
-            bit_pos = CMN_SKEENA_RNSAM_NON_HASH_TGT_NODEID_ENTRY_BITS_WIDTH *
-                (node_id_io_count %
-                 CMN_SKEENA_RNSAM_NON_HASH_TGT_NODEID_ENTRIES_PER_GROUP);
-
-            if (node_id_io_set_1) {
-                rnsam->NON_HASH_TGT_NODEID1[group] &= ~(
-                    CMN_SKEENA_RNSAM_NON_HASH_TGT_NODEID_ENTRY_MASK << bit_pos);
-                rnsam->NON_HASH_TGT_NODEID1[group] |=
-                    (region->node_id &
-                     CMN_SKEENA_RNSAM_NON_HASH_TGT_NODEID_ENTRY_MASK)
-                    << bit_pos;
-            } else {
-                rnsam->NON_HASH_TGT_NODEID0[group] &= ~(
-                    CMN_SKEENA_RNSAM_NON_HASH_TGT_NODEID_ENTRY_MASK << bit_pos);
-                rnsam->NON_HASH_TGT_NODEID0[group] |=
-                    (region->node_id &
-                     CMN_SKEENA_RNSAM_NON_HASH_TGT_NODEID_ENTRY_MASK)
-                    << bit_pos;
-            }
-
-            /*
-             * NON_HASH_MEM_REGION and NON_HASH_TGT_NODEID registers are not
-             * contiguous in RN-SAM memory map. It is divided into two sets.
-             * NON_HASH_MEM_REGION registers are grouped as 5 registers in
-             * in 2 sets each that can hold 10 regions in each set. Similarly
-             * NON_HASH_TGT_NODEID is divided into 2 sets. First set containing
-             * 3 registers (holding 12 target IDs) and second set containing 2
-             * registers (holding 8 target IDs).
-             */
-            region_io_count++;
-            if (region_io_count ==
-                (5 * CMN_SKEENA_RNSAM_REGION_ENTRIES_PER_GROUP)) {
-                region_io_count = 0;
-                region_io_set_1 = true;
-            }
-            node_id_io_count++;
-            if (node_id_io_count ==
-                (3 * CMN_SKEENA_RNSAM_NON_HASH_TGT_NODEID_ENTRIES_PER_GROUP)) {
-                node_id_io_count = 0;
-                node_id_io_set_1 = true;
-            }
-            break;
-
-        case MOD_CMN_SKEENA_MEMORY_REGION_TYPE_SYSCACHE:
-            /*
-             * Configure memory region
-             */
-            if (region_sys_count >=
-                CMN_SKEENA_RNSAM_MAX_HASH_MEM_REGION_ENTRIES) {
-                FWK_LOG_ERR(
-                    MOD_NAME
-                    "Hashed Memory can have maximum of %d regions only",
-                    CMN_SKEENA_RNSAM_MAX_HASH_MEM_REGION_ENTRIES);
-                return FWK_E_DATA;
-            }
-
-            group =
-                region_sys_count / CMN_SKEENA_RNSAM_REGION_ENTRIES_PER_GROUP;
-            bit_pos =
-                (region_sys_count % CMN_SKEENA_RNSAM_REGION_ENTRIES_PER_GROUP) *
-                CMN_SKEENA_RNSAM_REGION_ENTRY_BITS_WIDTH;
-            configure_region(
-                &rnsam->SYS_CACHE_GRP_REGION[group],
-                bit_pos,
-                region->base,
-                region->size,
-                SAM_NODE_TYPE_HN_F);
-
-            /* Mark corresponding region as enabled */
-            scg_region = (2 * group) + (bit_pos / 32);
-            fwk_assert(scg_region < CMN_SKEENA_MAX_NUM_SCG);
-            scg_regions_enabled[scg_region] = 1;
-
-            region_sys_count++;
-            break;
-
-        case MOD_CMN_SKEENA_REGION_TYPE_SYSCACHE_NONHASH:
-            group =
-                region_sys_count / CMN_SKEENA_RNSAM_REGION_ENTRIES_PER_GROUP;
-            bit_pos =
-                (region_sys_count % CMN_SKEENA_RNSAM_REGION_ENTRIES_PER_GROUP) *
-                CMN_SKEENA_RNSAM_REGION_ENTRY_BITS_WIDTH;
-            /*
-             * Configure memory region
-             */
-            configure_region(
-                &rnsam->SYS_CACHE_GRP_REGION[group],
-                bit_pos,
-                region->base,
-                region->size,
-                SAM_NODE_TYPE_HN_I);
-
-            rnsam->SYS_CACHE_GRP_REGION[group] |= (UINT64_C(0x2) << bit_pos);
-            bit_pos = CMN_SKEENA_RNSAM_NON_HASH_TGT_NODEID_ENTRY_BITS_WIDTH *
-                ((region_sys_count - 1) %
-                 CMN_SKEENA_RNSAM_NON_HASH_TGT_NODEID_ENTRIES_PER_GROUP);
-            rnsam->SYS_CACHE_GRP_NOHASH_NODEID &=
-                ~(CMN_SKEENA_RNSAM_NON_HASH_TGT_NODEID_ENTRY_MASK << bit_pos);
-            rnsam->SYS_CACHE_GRP_NOHASH_NODEID |=
-                (region->node_id &
-                 CMN_SKEENA_RNSAM_NON_HASH_TGT_NODEID_ENTRY_MASK)
-                << bit_pos;
-
-            region_sys_count++;
-            break;
-
-        case MOD_CMN_SKEENA_REGION_TYPE_SYSCACHE_SUB:
-            /* Do nothing. System cache sub-regions are handled by HN-Fs */
-            break;
-
-        default:
-            fwk_unexpected();
-            return FWK_E_DATA;
-        }
+	switch (region->type) {
+	case MOD_CMN_SKEENA_MEMORY_REGION_TYPE_IO:
+	case MOD_CMN_SKEENA_REGION_TYPE_CCIX:
+	    /*
+	     * Configure memory region
+	     */
+	    if (region_io_set_1 &&
+		(region_io_count >
+		 (CMN_SKEENA_RNSAM_MAX_NON_HASH_MEM_REGION_ENTRIES / 2))) {
+		FWK_LOG_ERR(
+		    MOD_NAME
+		    "Non-Hashed Memory can have maximum of %d regions only",
+		    CMN_SKEENA_RNSAM_MAX_NON_HASH_MEM_REGION_ENTRIES);
+		return FWK_E_DATA;
+	    }
+
+	    group = region_io_count / CMN_SKEENA_RNSAM_REGION_ENTRIES_PER_GROUP;
+	    bit_pos =
+		(region_io_count % CMN_SKEENA_RNSAM_REGION_ENTRIES_PER_GROUP) *
+		CMN_SKEENA_RNSAM_REGION_ENTRY_BITS_WIDTH;
+
+	    sam_node_type =
+		(region->type == MOD_CMN_SKEENA_MEMORY_REGION_TYPE_IO) ?
+		SAM_NODE_TYPE_HN_I :
+		SAM_NODE_TYPE_CXRA;
+	    if (region_io_set_1) {
+		configure_region(
+		    &rnsam->NON_HASH_MEM_REGION1[group],
+		    bit_pos,
+		    base,
+		    region->size,
+		    sam_node_type);
+	    } else {
+		configure_region(
+		    &rnsam->NON_HASH_MEM_REGION0[group],
+		    bit_pos,
+		    base,
+		    region->size,
+		    sam_node_type);
+	    }
+	    /*
+	     * Configure target node
+	     */
+	    group = node_id_io_count /
+		CMN_SKEENA_RNSAM_NON_HASH_TGT_NODEID_ENTRIES_PER_GROUP;
+	    bit_pos = CMN_SKEENA_RNSAM_NON_HASH_TGT_NODEID_ENTRY_BITS_WIDTH *
+		(node_id_io_count %
+		 CMN_SKEENA_RNSAM_NON_HASH_TGT_NODEID_ENTRIES_PER_GROUP);
+
+	    if (node_id_io_set_1) {
+		rnsam->NON_HASH_TGT_NODEID1[group] &= ~(
+		    CMN_SKEENA_RNSAM_NON_HASH_TGT_NODEID_ENTRY_MASK << bit_pos);
+		rnsam->NON_HASH_TGT_NODEID1[group] |=
+		    (region->node_id &
+		     CMN_SKEENA_RNSAM_NON_HASH_TGT_NODEID_ENTRY_MASK)
+		    << bit_pos;
+	    } else {
+		rnsam->NON_HASH_TGT_NODEID0[group] &= ~(
+		    CMN_SKEENA_RNSAM_NON_HASH_TGT_NODEID_ENTRY_MASK << bit_pos);
+		rnsam->NON_HASH_TGT_NODEID0[group] |=
+		    (region->node_id &
+		     CMN_SKEENA_RNSAM_NON_HASH_TGT_NODEID_ENTRY_MASK)
+		    << bit_pos;
+	    }
+
+	    /*
+	     * NON_HASH_MEM_REGION and NON_HASH_TGT_NODEID registers are not
+	     * contiguous in RN-SAM memory map. It is divided into two sets.
+	     * NON_HASH_MEM_REGION registers are grouped as 5 registers in
+	     * in 2 sets each that can hold 10 regions in each set. Similarly
+	     * NON_HASH_TGT_NODEID is divided into 2 sets. First set containing
+	     * 3 registers (holding 12 target IDs) and second set containing 2
+	     * registers (holding 8 target IDs).
+	     */
+	    region_io_count++;
+	    if (region_io_count ==
+		(5 * CMN_SKEENA_RNSAM_REGION_ENTRIES_PER_GROUP)) {
+		region_io_count = 0;
+		region_io_set_1 = true;
+	    }
+	    node_id_io_count++;
+	    if (node_id_io_count ==
+		(3 * CMN_SKEENA_RNSAM_NON_HASH_TGT_NODEID_ENTRIES_PER_GROUP)) {
+		node_id_io_count = 0;
+		node_id_io_set_1 = true;
+	    }
+	    break;
+
+	case MOD_CMN_SKEENA_MEMORY_REGION_TYPE_SYSCACHE:
+	    /*
+	     * Configure memory region
+	     */
+	    if (region_sys_count >=
+		CMN_SKEENA_RNSAM_MAX_HASH_MEM_REGION_ENTRIES) {
+		FWK_LOG_ERR(
+		    MOD_NAME
+		    "Hashed Memory can have maximum of %d regions only",
+		    CMN_SKEENA_RNSAM_MAX_HASH_MEM_REGION_ENTRIES);
+		return FWK_E_DATA;
+	    }
+
+	    group =
+		region_sys_count / CMN_SKEENA_RNSAM_REGION_ENTRIES_PER_GROUP;
+	    bit_pos =
+		(region_sys_count % CMN_SKEENA_RNSAM_REGION_ENTRIES_PER_GROUP) *
+		CMN_SKEENA_RNSAM_REGION_ENTRY_BITS_WIDTH;
+	    configure_region(
+		&rnsam->SYS_CACHE_GRP_REGION[group],
+		bit_pos,
+		region->base,
+		region->size,
+		SAM_NODE_TYPE_HN_F);
+
+	    /* Mark corresponding region as enabled */
+	    scg_region = (2 * group) + (bit_pos / 32);
+	    fwk_assert(scg_region < CMN_SKEENA_MAX_NUM_SCG);
+	    scg_regions_enabled[scg_region] = 1;
+
+	    region_sys_count++;
+	    break;
+
+	case MOD_CMN_SKEENA_REGION_TYPE_SYSCACHE_NONHASH:
+	    group =
+		region_sys_count / CMN_SKEENA_RNSAM_REGION_ENTRIES_PER_GROUP;
+	    bit_pos =
+		(region_sys_count % CMN_SKEENA_RNSAM_REGION_ENTRIES_PER_GROUP) *
+		CMN_SKEENA_RNSAM_REGION_ENTRY_BITS_WIDTH;
+	    /*
+	     * Configure memory region
+	     */
+	    configure_region(
+		&rnsam->SYS_CACHE_GRP_REGION[group],
+		bit_pos,
+		region->base,
+		region->size,
+		SAM_NODE_TYPE_HN_I);
+
+	    rnsam->SYS_CACHE_GRP_REGION[group] |= (UINT64_C(0x2) << bit_pos);
+	    bit_pos = CMN_SKEENA_RNSAM_NON_HASH_TGT_NODEID_ENTRY_BITS_WIDTH *
+		((region_sys_count - 1) %
+		 CMN_SKEENA_RNSAM_NON_HASH_TGT_NODEID_ENTRIES_PER_GROUP);
+	    rnsam->SYS_CACHE_GRP_NOHASH_NODEID &=
+		~(CMN_SKEENA_RNSAM_NON_HASH_TGT_NODEID_ENTRY_MASK << bit_pos);
+	    rnsam->SYS_CACHE_GRP_NOHASH_NODEID |=
+		(region->node_id &
+		 CMN_SKEENA_RNSAM_NON_HASH_TGT_NODEID_ENTRY_MASK)
+		<< bit_pos;
+
+	    region_sys_count++;
+	    break;
+
+	case MOD_CMN_SKEENA_REGION_TYPE_SYSCACHE_SUB:
+	    /* Do nothing. System cache sub-regions are handled by HN-Fs */
+	    break;
+
+	default:
+	    fwk_unexpected();
+	    return FWK_E_DATA;
+	}
     }
 
     group_count = cmn_skeena_hnf_cache_group_count(ctx->hnf_count);
     for (group = 0; group < group_count; group++)
-        rnsam->SYS_CACHE_GRP_HN_NODEID0[group] = ctx->hnf_cache_group[group];
+	rnsam->SYS_CACHE_GRP_HN_NODEID0[group] = ctx->hnf_cache_group[group];
 
     /* Program the number of HNFs */
     rnsam->SYS_CACHE_GRP_HN_COUNT = ctx->hnf_count;
 
     /* Use CAL mode only if the CMN_SKEENA revision is r2p0 or above */
     if (is_cal_mode_supported(ctx->root) && config->hnf_cal_mode) {
-        for (region_idx = 0; region_idx < CMN_SKEENA_MAX_NUM_SCG; region_idx++)
-            rnsam->SYS_CACHE_GRP_CAL_MODE |= scg_regions_enabled[region_idx] *
-                (CMN_SKEENA_RNSAM_SCG_HNF_CAL_MODE_EN
-                 << (region_idx * CMN_SKEENA_RNSAM_SCG_HNF_CAL_MODE_SHIFT));
+	for (region_idx = 0; region_idx < CMN_SKEENA_MAX_NUM_SCG; region_idx++)
+	    rnsam->SYS_CACHE_GRP_CAL_MODE |= scg_regions_enabled[region_idx] *
+		(CMN_SKEENA_RNSAM_SCG_HNF_CAL_MODE_EN
+		 << (region_idx * CMN_SKEENA_RNSAM_SCG_HNF_CAL_MODE_SHIFT));
     }
 
     /* Enable RNSAM */
@@ -662,57 +662,57 @@ static int cmn_skeena_setup(void)
     int status;
 
     if (!ctx->initialized) {
-        status = cmn_skeena_discovery();
-        if (status != FWK_SUCCESS)
-            return status;
-        /*
-         * Allocate resources based on the discovery
-         */
-
-        /* Pointers for the internal RN-SAM nodes */
-        if (ctx->internal_rnsam_count != 0) {
-            ctx->internal_rnsam_table = fwk_mm_calloc(
-                ctx->internal_rnsam_count, sizeof(*ctx->internal_rnsam_table));
-        }
-
-        /* Tuples for the external RN-RAM nodes (including their node IDs) */
-        if (ctx->external_rnsam_count != 0) {
-            ctx->external_rnsam_table = fwk_mm_calloc(
-                ctx->external_rnsam_count, sizeof(*ctx->external_rnsam_table));
-        }
-
-        /* Cache groups */
-        if (ctx->hnf_count != 0) {
-            /*
-             * Allocate enough group descriptors to accommodate all expected
-             * HN-F nodes in the system.
-             */
-            ctx->hnf_cache_group = fwk_mm_calloc(
-                cmn_skeena_hnf_cache_group_count(ctx->hnf_count),
-                sizeof(*ctx->hnf_cache_group));
-        }
+	status = cmn_skeena_discovery();
+	if (status != FWK_SUCCESS)
+	    return status;
+	/*
+	 * Allocate resources based on the discovery
+	 */
+
+	/* Pointers for the internal RN-SAM nodes */
+	if (ctx->internal_rnsam_count != 0) {
+	    ctx->internal_rnsam_table = fwk_mm_calloc(
+		ctx->internal_rnsam_count, sizeof(*ctx->internal_rnsam_table));
+	}
+
+	/* Tuples for the external RN-RAM nodes (including their node IDs) */
+	if (ctx->external_rnsam_count != 0) {
+	    ctx->external_rnsam_table = fwk_mm_calloc(
+		ctx->external_rnsam_count, sizeof(*ctx->external_rnsam_table));
+	}
+
+	/* Cache groups */
+	if (ctx->hnf_count != 0) {
+	    /*
+	     * Allocate enough group descriptors to accommodate all expected
+	     * HN-F nodes in the system.
+	     */
+	    ctx->hnf_cache_group = fwk_mm_calloc(
+		cmn_skeena_hnf_cache_group_count(ctx->hnf_count),
+		sizeof(*ctx->hnf_cache_group));
+	}
     }
 
     cmn_skeena_configure();
 
     /* Setup internal RN-SAM nodes */
     for (rnsam_idx = 0; rnsam_idx < ctx->internal_rnsam_count; rnsam_idx++)
-        cmn_skeena_setup_sam(ctx->internal_rnsam_table[rnsam_idx]);
+	cmn_skeena_setup_sam(ctx->internal_rnsam_table[rnsam_idx]);
 
     /* Capture CCIX Host Topology */
     for (i = 0; i < ctx->config->mmap_count; i++) {
-        if (ctx->config->mmap_table[i].type ==
-            MOD_CMN_SKEENA_REGION_TYPE_CCIX) {
-            ccix_mmap_idx = ctx->ccix_host_info.ccix_host_mmap_count;
-            if (ccix_mmap_idx >= MAX_HA_MMAP_ENTRIES)
-                return FWK_E_DATA;
-
-            ctx->ccix_host_info.ccix_host_mmap[ccix_mmap_idx].base =
-                ctx->config->mmap_table[i].base;
-            ctx->ccix_host_info.ccix_host_mmap[ccix_mmap_idx].size =
-                ctx->config->mmap_table[i].size;
-            ctx->ccix_host_info.ccix_host_mmap_count++;
-        }
+	if (ctx->config->mmap_table[i].type ==
+	    MOD_CMN_SKEENA_REGION_TYPE_CCIX) {
+	    ccix_mmap_idx = ctx->ccix_host_info.ccix_host_mmap_count;
+	    if (ccix_mmap_idx >= MAX_HA_MMAP_ENTRIES)
+		return FWK_E_DATA;
+
+	    ctx->ccix_host_info.ccix_host_mmap[ccix_mmap_idx].base =
+		ctx->config->mmap_table[i].base;
+	    ctx->ccix_host_info.ccix_host_mmap[ccix_mmap_idx].size =
+		ctx->config->mmap_table[i].size;
+	    ctx->ccix_host_info.ccix_host_mmap_count++;
+	}
     }
 
     FWK_LOG_INFO(MOD_NAME "Done");
@@ -727,10 +727,10 @@ static int cmn_skeena_setup_rnsam(unsign
     unsigned int node_idx;
 
     for (node_idx = 0; node_idx < ctx->external_rnsam_count; node_idx++) {
-        if (ctx->external_rnsam_table[node_idx].node_id == node_id) {
-            cmn_skeena_setup_sam(ctx->external_rnsam_table[node_idx].node);
-            return FWK_SUCCESS;
-        }
+	if (ctx->external_rnsam_table[node_idx].node_id == node_id) {
+	    cmn_skeena_setup_sam(ctx->external_rnsam_table[node_idx].node);
+	    return FWK_SUCCESS;
+	}
     }
 
     return FWK_E_PARAM;
@@ -748,7 +748,7 @@ static void post_ppu_on(void *data)
 
 static const struct mod_ppu_v1_power_state_observer_api
     cmn_skeena_observer_api = {
-        .post_ppu_on = post_ppu_on,
+	.post_ppu_on = post_ppu_on,
     };
 
 /*
@@ -759,18 +759,18 @@ static int cmn_skeena_ccix_config_get(
     struct mod_cmn_skeena_ccix_host_node_config *config)
 {
     if (ctx->internal_rnsam_count == 0)
-        return FWK_E_DATA;
+	return FWK_E_DATA;
 
     ctx->ccix_host_info.host_ra_count =
-        ctx->internal_rnsam_count + ctx->external_rnsam_count;
+	ctx->internal_rnsam_count + ctx->external_rnsam_count;
     ctx->ccix_host_info.host_sa_count = ctx->config->sa_count;
 
     ccix_capabilities_get(ctx);
 
     memcpy(
-        (void *)config,
-        (void *)&ctx->ccix_host_info,
-        sizeof(struct mod_cmn_skeena_ccix_host_node_config));
+	(void *)config,
+	(void *)&ctx->ccix_host_info,
+	sizeof(struct mod_cmn_skeena_ccix_host_node_config));
     return FWK_SUCCESS;
 }
 
@@ -782,11 +782,11 @@ static int cmn_skeena_ccix_config_set(
 
     status = ccix_setup(ctx, config);
     if (status != FWK_SUCCESS)
-        return status;
+	return status;
 
     for (i = 0; i < ctx->config->mmap_count; i++) {
-        if (ctx->config->mmap_table[i].type == MOD_CMN_SKEENA_REGION_TYPE_CCIX)
-            cmn_skeena_setup_rnsam(ctx->config->mmap_table[i].node_id);
+	if (ctx->config->mmap_table[i].type == MOD_CMN_SKEENA_REGION_TYPE_CCIX)
+	    cmn_skeena_setup_rnsam(ctx->config->mmap_table[i].node_id);
     }
     return FWK_SUCCESS;
 }
@@ -808,11 +808,11 @@ static int cmn_skeena_ccix_enter_dvm_dom
 
 static const struct mod_cmn_skeena_ccix_config_api
     cmn_skeena_ccix_config_api = {
-        .get_config = cmn_skeena_ccix_config_get,
-        .set_config = cmn_skeena_ccix_config_set,
-        .exchange_protocol_credit = cmn_skeena_ccix_exchange_protocol_credit,
-        .enter_system_coherency = cmn_skeena_ccix_enter_system_coherency,
-        .enter_dvm_domain = cmn_skeena_ccix_enter_dvm_domain,
+	.get_config = cmn_skeena_ccix_config_get,
+	.set_config = cmn_skeena_ccix_config_set,
+	.exchange_protocol_credit = cmn_skeena_ccix_exchange_protocol_credit,
+	.enter_system_coherency = cmn_skeena_ccix_enter_system_coherency,
+	.enter_dvm_domain = cmn_skeena_ccix_enter_dvm_domain,
     };
 
 /*
@@ -828,30 +828,30 @@ static int cmn_skeena_init(
 
     /* No elements support */
     if (element_count > 0)
-        return FWK_E_DATA;
+	return FWK_E_DATA;
 
     /* Allocate space for the context */
     ctx = fwk_mm_calloc(1, sizeof(*ctx));
 
     if (config->base == 0)
-        return FWK_E_DATA;
+	return FWK_E_DATA;
 
     if ((config->mesh_size_x == 0) ||
-        (config->mesh_size_x > CMN_SKEENA_MESH_X_MAX))
-        return FWK_E_DATA;
+	(config->mesh_size_x > CMN_SKEENA_MESH_X_MAX))
+	return FWK_E_DATA;
 
     if ((config->mesh_size_y == 0) ||
-        (config->mesh_size_y > CMN_SKEENA_MESH_Y_MAX))
-        return FWK_E_DATA;
+	(config->mesh_size_y > CMN_SKEENA_MESH_Y_MAX))
+	return FWK_E_DATA;
 
     if (config->snf_count > CMN_SKEENA_HNF_CACHE_GROUP_ENTRIES_MAX)
-        return FWK_E_DATA;
+	return FWK_E_DATA;
 
     ctx->root = get_root_node(
-        config->base,
-        config->hnd_node_id,
-        config->mesh_size_x,
-        config->mesh_size_y);
+	config->base,
+	config->hnd_node_id,
+	config->mesh_size_x,
+	config->mesh_size_y);
 
     ctx->config = config;
 
@@ -864,21 +864,21 @@ static int cmn_skeena_bind(fwk_id_t id,
 
     /* Use second round only (round numbering is zero-indexed) */
     if (round == 1) {
-        /* Bind to the timer component */
-        status = fwk_module_bind(
-            FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
-            FWK_ID_API(FWK_MODULE_IDX_TIMER, MOD_TIMER_API_IDX_TIMER),
-            &ctx->timer_api);
-        if (status != FWK_SUCCESS)
-            return FWK_E_PANIC;
-
-        /* Bind to system info module to obtain multi-chip info */
-        status = fwk_module_bind(
-            FWK_ID_MODULE(FWK_MODULE_IDX_SYSTEM_INFO),
-            FWK_ID_API(FWK_MODULE_IDX_SYSTEM_INFO, MOD_SYSTEM_INFO_GET_API_IDX),
-            &system_info_api);
-        if (status != FWK_SUCCESS)
-            return FWK_E_PANIC;
+	/* Bind to the timer component */
+	status = fwk_module_bind(
+	    FWK_ID_ELEMENT(FWK_MODULE_IDX_TIMER, 0),
+	    FWK_ID_API(FWK_MODULE_IDX_TIMER, MOD_TIMER_API_IDX_TIMER),
+	    &ctx->timer_api);
+	if (status != FWK_SUCCESS)
+	    return FWK_E_PANIC;
+
+	/* Bind to system info module to obtain multi-chip info */
+	status = fwk_module_bind(
+	    FWK_ID_MODULE(FWK_MODULE_IDX_SYSTEM_INFO),
+	    FWK_ID_API(FWK_MODULE_IDX_SYSTEM_INFO, MOD_SYSTEM_INFO_GET_API_IDX),
+	    &system_info_api);
+	if (status != FWK_SUCCESS)
+	    return FWK_E_PANIC;
     }
 
     return FWK_SUCCESS;
@@ -892,12 +892,12 @@ static int cmn_skeena_process_bind_reque
 {
     switch (fwk_id_get_api_idx(api_id)) {
     case MOD_CMN_SKEENA_API_IDX_PPU_OBSERVER:
-        *api = &cmn_skeena_observer_api;
-        break;
+	*api = &cmn_skeena_observer_api;
+	break;
 
     case MOD_CMN_SKEENA_API_IDX_CCIX_CONFIG:
-        *api = &cmn_skeena_ccix_config_api;
-        break;
+	*api = &cmn_skeena_ccix_config_api;
+	break;
     }
     return FWK_SUCCESS;
 }
@@ -909,14 +909,14 @@ int cmn_skeena_start(fwk_id_t id)
     int status;
 
     if (fwk_id_is_equal(ctx->config->clock_id, FWK_ID_NONE)) {
-        cmn_skeena_setup();
-        return FWK_SUCCESS;
+	cmn_skeena_setup();
+	return FWK_SUCCESS;
     }
 
     status = system_info_api->get_system_info(&ctx->system_info);
     if (status == FWK_SUCCESS) {
-        chip_id = ctx->system_info->chip_id;
-        mc_mode = ctx->system_info->multi_chip_mode;
+	chip_id = ctx->system_info->chip_id;
+	mc_mode = ctx->system_info->multi_chip_mode;
     }
 
     ctx->chip_id = chip_id;
@@ -927,7 +927,7 @@ int cmn_skeena_start(fwk_id_t id)
 
     /* Register the module for clock state notifications */
     return fwk_notification_subscribe(
-        mod_clock_notification_id_state_changed, ctx->config->clock_id, id);
+	mod_clock_notification_id_state_changed, ctx->config->clock_id, id);
 }
 
 static int cmn_skeena_process_notification(
@@ -937,13 +937,13 @@ static int cmn_skeena_process_notificati
     struct clock_notification_params *params;
 
     fwk_assert(
-        fwk_id_is_equal(event->id, mod_clock_notification_id_state_changed));
+	fwk_id_is_equal(event->id, mod_clock_notification_id_state_changed));
     fwk_assert(fwk_id_is_type(event->target_id, FWK_ID_TYPE_MODULE));
 
     params = (struct clock_notification_params *)event->params;
 
     if (params->new_state == MOD_CLOCK_STATE_RUNNING)
-        cmn_skeena_setup();
+	cmn_skeena_setup();
 
     return FWK_SUCCESS;
 }
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/css_clock/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/css_clock/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/css_clock/CMakeLists.txt
@@ -8,10 +8,10 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_css_clock.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_css_clock.c")
 
 target_link_libraries(${SCP_MODULE_TARGET} PRIVATE module-clock
-                                                   module-power-domain)
+						   module-power-domain)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/css_clock/include/mod_css_clock.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/css_clock/include/mod_css_clock.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/css_clock/include/mod_css_clock.h
@@ -121,14 +121,14 @@ struct mod_css_clock_dev_config {
 struct mod_css_clock_direct_api {
     /*! Set the clock device's divider */
     int (*set_div)(fwk_id_t device_id, uint32_t divider_type,
-                   uint32_t divider);
+		   uint32_t divider);
 
     /*! Set the clock device's source (multi-source clocks only) */
     int (*set_source)(fwk_id_t device_id, uint8_t source);
 
     /*! Set the clock device's modulator (multi-source clocks only) */
     int (*set_mod)(fwk_id_t device_id, uint32_t numerator,
-                   uint32_t denominator);
+		   uint32_t denominator);
 
     /*!
      * \brief Handle the condition where the state of a clock's power domain is
@@ -154,9 +154,9 @@ struct mod_css_clock_direct_api {
      * \return One of the standard framework error codes.
      */
     int (*process_pending_power_transition)(
-        fwk_id_t clock_id,
-        unsigned int current_state,
-        unsigned int new_state);
+	fwk_id_t clock_id,
+	unsigned int current_state,
+	unsigned int new_state);
 
     /*!
      * \brief Handle the condition where the state of a clock's power domain
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/css_clock/src/mod_css_clock.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/css_clock/src/mod_css_clock.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/css_clock/src/mod_css_clock.c
@@ -47,117 +47,117 @@ static int compare_rate_entry(const void
 }
 
 static int get_rate_entry(struct css_clock_dev_ctx *ctx, uint64_t target_rate,
-                          struct mod_css_clock_rate **entry)
+			  struct mod_css_clock_rate **entry)
 {
     struct mod_css_clock_rate *current_rate_entry;
 
     if (ctx == NULL)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     if (entry == NULL)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     /* Perform a binary search to find the entry matching the requested rate */
     current_rate_entry = (struct mod_css_clock_rate *) bsearch(&target_rate,
-        ctx->config->rate_table, ctx->config->rate_count,
-        sizeof(struct mod_css_clock_rate), compare_rate_entry);
+	ctx->config->rate_table, ctx->config->rate_count,
+	sizeof(struct mod_css_clock_rate), compare_rate_entry);
 
     if (current_rate_entry == NULL)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     *entry = current_rate_entry;
     return FWK_SUCCESS;
 }
 
 static int set_rate_indexed(struct css_clock_dev_ctx *ctx, uint64_t rate,
-                            enum mod_clock_round_mode round_mode)
+			    enum mod_clock_round_mode round_mode)
 {
     int status;
     unsigned int i;
     struct mod_css_clock_rate *rate_entry;
 
     if (ctx == NULL)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     /* Look up the divider and source settings */
     status = get_rate_entry(ctx, rate, &rate_entry);
     if (status != FWK_SUCCESS)
-        goto exit;
+	goto exit;
 
     /* Switch each member clock away from the PLL source */
     for (i = 0; i < ctx->config->member_count; i++) {
-        status = ctx->clock_api->set_source(ctx->config->member_table[i],
-            ctx->config->clock_switching_source);
-        if (status != FWK_SUCCESS)
-            goto exit;
-
-        status = ctx->clock_api->set_div(ctx->config->member_table[i],
-                                         rate_entry->clock_div_type,
-                                         rate_entry->clock_div);
-        if (status != FWK_SUCCESS)
-            goto exit;
-
-        if (ctx->config->modulation_supported) {
-            status = ctx->clock_api->set_mod(ctx->config->member_table[i],
-                                             rate_entry->clock_mod_numerator,
-                                             rate_entry->clock_mod_denominator);
-            if (status != FWK_SUCCESS)
-                goto exit;
-        }
+	status = ctx->clock_api->set_source(ctx->config->member_table[i],
+	    ctx->config->clock_switching_source);
+	if (status != FWK_SUCCESS)
+	    goto exit;
+
+	status = ctx->clock_api->set_div(ctx->config->member_table[i],
+					 rate_entry->clock_div_type,
+					 rate_entry->clock_div);
+	if (status != FWK_SUCCESS)
+	    goto exit;
+
+	if (ctx->config->modulation_supported) {
+	    status = ctx->clock_api->set_mod(ctx->config->member_table[i],
+					     rate_entry->clock_mod_numerator,
+					     rate_entry->clock_mod_denominator);
+	    if (status != FWK_SUCCESS)
+		goto exit;
+	}
     }
 
     /* Change the PLL to the desired rate */
     status = ctx->pll_api->set_rate(ctx->config->pll_id, rate_entry->pll_rate,
-                                    MOD_CLOCK_ROUND_MODE_NONE);
+				    MOD_CLOCK_ROUND_MODE_NONE);
     if (status != FWK_SUCCESS)
-        goto exit;
+	goto exit;
 
     /* Return each member clock back to the PLL source */
     for (i = 0; i < ctx->config->member_count; i++) {
-        status = ctx->clock_api->set_source(ctx->config->member_table[i],
-                                            rate_entry->clock_source);
-        if (status != FWK_SUCCESS)
-            goto exit;
+	status = ctx->clock_api->set_source(ctx->config->member_table[i],
+					    rate_entry->clock_source);
+	if (status != FWK_SUCCESS)
+	    goto exit;
     }
 
 exit:
     if (status == FWK_SUCCESS)
-        ctx->current_rate = rate;
+	ctx->current_rate = rate;
     return status;
 }
 
 static int set_rate_non_indexed(struct css_clock_dev_ctx *ctx, uint64_t rate,
-                                enum mod_clock_round_mode round_mode)
+				enum mod_clock_round_mode round_mode)
 {
     int status;
     unsigned int i;
 
     if (ctx == NULL)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     /* Switch each member clock away from the PLL source */
     for (i = 0; i < ctx->config->member_count; i++) {
-        status = ctx->clock_api->set_source(ctx->config->member_table[i],
-            ctx->config->clock_switching_source);
-        if (status != FWK_SUCCESS)
-            goto exit;
+	status = ctx->clock_api->set_source(ctx->config->member_table[i],
+	    ctx->config->clock_switching_source);
+	if (status != FWK_SUCCESS)
+	    goto exit;
     }
 
     /* Change the PLL to the desired rate */
     status = ctx->pll_api->set_rate(ctx->config->pll_id, rate, round_mode);
     if (status != FWK_SUCCESS)
-        goto exit;
+	goto exit;
 
     /* Return each member clock back to the PLL source */
     for (i = 0; i < ctx->config->member_count; i++) {
-        status = ctx->clock_api->set_source(ctx->config->member_table[i],
-                                            ctx->config->clock_default_source);
-        if (status != FWK_SUCCESS)
-            goto exit;
+	status = ctx->clock_api->set_source(ctx->config->member_table[i],
+					    ctx->config->clock_default_source);
+	if (status != FWK_SUCCESS)
+	    goto exit;
     }
 
 exit:
     if (status == FWK_SUCCESS)
-        ctx->current_rate = rate;
+	ctx->current_rate = rate;
     return status;
 }
 
@@ -166,16 +166,16 @@ exit:
  */
 
 static int css_clock_set_rate(fwk_id_t dev_id, uint64_t rate,
-                              enum mod_clock_round_mode round_mode)
+			      enum mod_clock_round_mode round_mode)
 {
     struct css_clock_dev_ctx *ctx;
 
     ctx = module_ctx.dev_ctx_table + fwk_id_get_element_idx(dev_id);
 
     if (ctx->config->clock_type == MOD_CSS_CLOCK_TYPE_INDEXED)
-        return set_rate_indexed(ctx, rate, round_mode);
+	return set_rate_indexed(ctx, rate, round_mode);
     else
-        return set_rate_non_indexed(ctx, rate, round_mode);
+	return set_rate_non_indexed(ctx, rate, round_mode);
 }
 
 static int css_clock_get_rate(fwk_id_t dev_id, uint64_t *rate)
@@ -189,30 +189,30 @@ static int css_clock_get_rate(fwk_id_t d
 }
 
 static int css_clock_get_rate_from_index(fwk_id_t dev_id,
-                                         unsigned int rate_index,
-                                         uint64_t *rate)
+					 unsigned int rate_index,
+					 uint64_t *rate)
 {
     struct css_clock_dev_ctx *ctx;
 
     if (rate == NULL)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     ctx = module_ctx.dev_ctx_table + fwk_id_get_element_idx(dev_id);
 
     if (rate_index >= ctx->config->rate_count)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     if (ctx->config->clock_type == MOD_CSS_CLOCK_TYPE_INDEXED) {
-        *rate = ctx->config->rate_table[rate_index].rate;
-        return FWK_SUCCESS;
+	*rate = ctx->config->rate_table[rate_index].rate;
+	return FWK_SUCCESS;
     } else
-        return FWK_E_SUPPORT;
+	return FWK_E_SUPPORT;
 }
 
 static int css_clock_set_state(fwk_id_t dev_id, enum mod_clock_state state)
 {
     if (state == MOD_CLOCK_STATE_RUNNING)
-        return FWK_SUCCESS; /* CSS clocks are always running */
+	return FWK_SUCCESS; /* CSS clocks are always running */
 
     /* CSS clocks cannot be turned off */
     return FWK_E_SUPPORT;
@@ -230,18 +230,18 @@ static int css_clock_get_range(fwk_id_t
     struct css_clock_dev_ctx *ctx;
 
     if (range == NULL)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     ctx = module_ctx.dev_ctx_table + fwk_id_get_element_idx(dev_id);
 
     if (ctx->config->clock_type == MOD_CSS_CLOCK_TYPE_INDEXED) {
-        range->rate_type = MOD_CLOCK_RATE_TYPE_DISCRETE;
-        range->min = ctx->config->rate_table[0].rate;
-        range->max = ctx->config->rate_table[ctx->config->rate_count - 1].rate;
-        range->rate_count = ctx->config->rate_count;
-        return FWK_SUCCESS;
+	range->rate_type = MOD_CLOCK_RATE_TYPE_DISCRETE;
+	range->min = ctx->config->rate_table[0].rate;
+	range->max = ctx->config->rate_table[ctx->config->rate_count - 1].rate;
+	range->rate_count = ctx->config->rate_count;
+	return FWK_SUCCESS;
     } else
-        return ctx->pll_api->get_range(ctx->config->pll_id, range);
+	return ctx->pll_api->get_range(ctx->config->pll_id, range);
 }
 
 static int css_clock_power_state_change(
@@ -258,28 +258,28 @@ static int css_clock_power_state_change(
 
     /* The group's clock driver is not required to handle this transition */
     if (ctx->clock_api->process_power_transition != NULL) {
-        for (clock_idx = 0; clock_idx < dev_config->member_count; clock_idx++) {
-            /* Allow the member clock's driver to perform any required
-             * processing */
-            status = ctx->clock_api->process_power_transition(
-                dev_config->member_table[clock_idx], next_state);
-
-            if (status != FWK_SUCCESS)
-                return status;
-        }
+	for (clock_idx = 0; clock_idx < dev_config->member_count; clock_idx++) {
+	    /* Allow the member clock's driver to perform any required
+	     * processing */
+	    status = ctx->clock_api->process_power_transition(
+		dev_config->member_table[clock_idx], next_state);
+
+	    if (status != FWK_SUCCESS)
+		return status;
+	}
     }
 
     if (next_state == MOD_PD_STATE_ON) {
-        if (ctx->initialized) {
-            /* Restore all clocks in the group to the last frequency */
-            return css_clock_set_rate(dev_id, ctx->current_rate,
-                                      MOD_CLOCK_ROUND_MODE_NONE);
-        } else {
-            ctx->initialized = true;
-            /* Set all clocks in the group to the initial frequency */
-            return css_clock_set_rate(dev_id, dev_config->initial_rate,
-                                      MOD_CLOCK_ROUND_MODE_NONE);
-        }
+	if (ctx->initialized) {
+	    /* Restore all clocks in the group to the last frequency */
+	    return css_clock_set_rate(dev_id, ctx->current_rate,
+				      MOD_CLOCK_ROUND_MODE_NONE);
+	} else {
+	    ctx->initialized = true;
+	    /* Set all clocks in the group to the initial frequency */
+	    return css_clock_set_rate(dev_id, dev_config->initial_rate,
+				      MOD_CLOCK_ROUND_MODE_NONE);
+	}
     }
 
     return FWK_SUCCESS;
@@ -300,15 +300,15 @@ static int css_clock_pending_power_state
 
     /* The group's clock driver is not required to handle this transition */
     if (ctx->clock_api->process_pending_power_transition != NULL) {
-        for (clock_idx = 0; clock_idx < dev_config->member_count; clock_idx++) {
-            /* Allow the member clock's driver to perform any required
-             * processing */
-            status = ctx->clock_api->process_pending_power_transition(
-                dev_config->member_table[clock_idx], current_state, next_state);
-
-            if (status != FWK_SUCCESS)
-                return status;
-        }
+	for (clock_idx = 0; clock_idx < dev_config->member_count; clock_idx++) {
+	    /* Allow the member clock's driver to perform any required
+	     * processing */
+	    status = ctx->clock_api->process_pending_power_transition(
+		dev_config->member_table[clock_idx], current_state, next_state);
+
+	    if (status != FWK_SUCCESS)
+		return status;
+	}
     }
 
     /* Nothing specific to be done in this driver */
@@ -331,21 +331,21 @@ static const struct mod_clock_drv_api ap
  */
 
 static int css_clock_init(fwk_id_t module_id, unsigned int element_count,
-                          const void *data)
+			  const void *data)
 {
     module_ctx.dev_count = element_count;
 
     if (element_count == 0)
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 
     module_ctx.dev_ctx_table = fwk_mm_calloc(element_count,
-                                             sizeof(struct css_clock_dev_ctx));
+					     sizeof(struct css_clock_dev_ctx));
     return FWK_SUCCESS;
 }
 
 static int css_clock_element_init(fwk_id_t element_id,
-                                  unsigned int sub_element_count,
-                                  const void *data)
+				  unsigned int sub_element_count,
+				  const void *data)
 {
     unsigned int i = 0;
     uint64_t current_rate;
@@ -356,17 +356,17 @@ static int css_clock_element_init(fwk_id
     ctx = module_ctx.dev_ctx_table + fwk_id_get_element_idx(element_id);
 
     if (dev_config->clock_type == MOD_CSS_CLOCK_TYPE_INDEXED) {
-        /* Verify that the rate entries in the lookup table are ordered */
-        while (i < dev_config->rate_count) {
-            current_rate = dev_config->rate_table[i].rate;
-
-            /* The rate entries must be in ascending order */
-            if (current_rate < last_rate)
-                return FWK_E_DATA;
-
-            last_rate = current_rate;
-            i++;
-        }
+	/* Verify that the rate entries in the lookup table are ordered */
+	while (i < dev_config->rate_count) {
+	    current_rate = dev_config->rate_table[i].rate;
+
+	    /* The rate entries must be in ascending order */
+	    if (current_rate < last_rate)
+		return FWK_E_DATA;
+
+	    last_rate = current_rate;
+	    i++;
+	}
     }
 
     ctx->config = dev_config;
@@ -382,41 +382,41 @@ static int css_clock_bind(fwk_id_t id, u
     const struct mod_css_clock_dev_config *config;
 
     if (round == 1)
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 
     if (fwk_module_is_valid_module_id(id))
-        /* No module-level binding required */
-        return FWK_SUCCESS;
+	/* No module-level binding required */
+	return FWK_SUCCESS;
 
     ctx = module_ctx.dev_ctx_table + fwk_id_get_element_idx(id);
     config = ctx->config;
 
     /* Ensure that the group has at least one member */
     if (config->member_count == 0)
-        return FWK_E_DATA;
+	return FWK_E_DATA;
 
     /* Bind to the group's common PLL driver */
     status = fwk_module_bind(config->pll_id, config->pll_api_id,
-                             &ctx->pll_api);
+			     &ctx->pll_api);
     if (status != FWK_SUCCESS)
-        return status;
+	return status;
 
     /* Bind to the API used to control the clocks in the group */
     status = fwk_module_bind(config->member_table[0],
-                             config->member_api_id, &ctx->clock_api);
+			     config->member_api_id, &ctx->clock_api);
     if (status != FWK_SUCCESS)
-        return status;
+	return status;
 
     return FWK_SUCCESS;
 }
 
 static int css_clock_process_bind_request(fwk_id_t source_id,
-                                          fwk_id_t target_id, fwk_id_t api_id,
-                                          const void **api)
+					  fwk_id_t target_id, fwk_id_t api_id,
+					  const void **api)
 {
     if (fwk_id_get_api_idx(api_id) != MOD_CSS_CLOCK_API_TYPE_CLOCK)
-        /* The requested API is not supported. */
-        return FWK_E_ACCESS;
+	/* The requested API is not supported. */
+	return FWK_E_ACCESS;
 
     *api = &api_clock;
     return FWK_SUCCESS;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/ddr_phy500/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/ddr_phy500/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/ddr_phy500/CMakeLists.txt
@@ -8,10 +8,10 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_ddr_phy500.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_ddr_phy500.c")
 
 if("dmc500" IN_LIST SCP_MODULES)
     target_link_libraries(${SCP_MODULE_TARGET} PRIVATE module-dmc500)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/ddr_phy500/include/mod_ddr_phy500.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/ddr_phy500/include/mod_ddr_phy500.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/ddr_phy500/include/mod_ddr_phy500.h
@@ -54,7 +54,7 @@ struct mod_ddr_phy500_reg {
     FWK_RW  uint32_t      DFI_PHYMSTR_REQ;
     FWK_RW  uint32_t      DFI_PHYMSTR_ACK;
     FWK_RW  uint32_t      DFI_WR_PREMBL;
-            uint8_t       RESERVED[0x820 - 0x50];
+	    uint8_t       RESERVED[0x820 - 0x50];
     FWK_RW  uint32_t      DELAY_SEL;
     FWK_RW  uint32_t      REF_EN;
     FWK_RW  uint32_t      T_CTRL_UPD_MIN;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/ddr_phy500/src/mod_ddr_phy500.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/ddr_phy500/src/mod_ddr_phy500.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/ddr_phy500/src/mod_ddr_phy500.c
@@ -46,7 +46,7 @@ static int ddr_phy500_config(fwk_id_t el
     FWK_LOG_INFO("[DDR] Initializing PHY at 0x%x", (uintptr_t)ddr);
 
     if (module_config->initialize_init_complete)
-        ddr->INIT_COMPLETE = module_config->ddr_reg_val->INIT_COMPLETE;
+	ddr->INIT_COMPLETE = module_config->ddr_reg_val->INIT_COMPLETE;
 
     ddr->T_CTRL_DELAY   = module_config->ddr_reg_val->T_CTRL_DELAY;
     ddr->READ_DELAY     = module_config->ddr_reg_val->READ_DELAY;
@@ -62,7 +62,7 @@ static int ddr_phy500_config(fwk_id_t el
     ddr->DFI_LP_ACK    = module_config->ddr_reg_val->DFI_LP_ACK;
 
     if (module_config->initialize_ref_en)
-        ddr->REF_EN = module_config->ddr_reg_val->REF_EN;
+	ddr->REF_EN = module_config->ddr_reg_val->REF_EN;
 
     return FWK_SUCCESS;
 }
@@ -76,7 +76,7 @@ static struct mod_dmc_ddr_phy_api ddr_ph
  */
 
 static int ddr_phy500_init(fwk_id_t module_id, unsigned int element_count,
-                           const void *config)
+			   const void *config)
 {
     fwk_assert(config != NULL);
 
@@ -84,7 +84,7 @@ static int ddr_phy500_init(fwk_id_t modu
 }
 
 static int ddr_phy500_element_init(fwk_id_t element_id, unsigned int unused,
-                                   const void *data)
+				   const void *data)
 {
     fwk_assert(data != NULL);
 
@@ -95,11 +95,11 @@ static int ddr_phy500_bind(fwk_id_t id,
 {
     /* Skip the second round (rounds are zero-indexed) */
     if (round == 1)
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 
     /* Nothing to be done for element-level binding */
     if (fwk_module_is_valid_element_id(id))
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 
     return FWK_SUCCESS;
 }
@@ -109,7 +109,7 @@ static int ddr_phy500_process_bind_reque
 {
     /* Binding to elements is not permitted. */
     if (fwk_module_is_valid_element_id(id))
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
 
     *((const void**)api) = &ddr_phy500_api;
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/debug/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/debug/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/debug/CMakeLists.txt
@@ -13,4 +13,4 @@ target_include_directories(
     PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_debug.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_debug.c")
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/debug/doc/module_dedug_architecture.md
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/debug/doc/module_dedug_architecture.md
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/debug/doc/module_dedug_architecture.md
@@ -34,32 +34,32 @@ enabling/disabling DBGSYS power domain.
 debug module can be plugged in.
 
 
-          standard - w/o debug   |    with debug module and debug driver
-                                 |
-              +------------+     |           +------------+
-              |    SCMI    |     |           |   SCMI     |
-              +------------+     |           +------------+
-                                 |
-                                 |
-              +------------+     |           +------------+
-              |  SCMI_PD   |     |           |  SCMI_PD   |
-              +------------+     |           +------------+
-                                 |
-                                 |
-              +------------+     |           +------------+
-              |    PD      |     |           |   DEBUG    |
-              +------------+     |           +------------+
-                                 |
-                                 |                                --+
-              +------------+     |           +------------+         |
-              |   DRIVER   |     |           |DEBUG-DRIVER|         |  platform
-              +------------+     |           +------------+         |  specific
-                                 |                                --+
-                                 |
-                                 |   +-----------+  +-----------+
-                                 |   | MODULE A  |  | MODULE B  |
-                                 |   +-----------+  +-----------+
-                                 |
+	  standard - w/o debug   |    with debug module and debug driver
+				 |
+	      +------------+     |           +------------+
+	      |    SCMI    |     |           |   SCMI     |
+	      +------------+     |           +------------+
+				 |
+				 |
+	      +------------+     |           +------------+
+	      |  SCMI_PD   |     |           |  SCMI_PD   |
+	      +------------+     |           +------------+
+				 |
+				 |
+	      +------------+     |           +------------+
+	      |    PD      |     |           |   DEBUG    |
+	      +------------+     |           +------------+
+				 |
+				 |                                --+
+	      +------------+     |           +------------+         |
+	      |   DRIVER   |     |           |DEBUG-DRIVER|         |  platform
+	      +------------+     |           +------------+         |  specific
+				 |                                --+
+				 |
+				 |   +-----------+  +-----------+
+				 |   | MODULE A  |  | MODULE B  |
+				 |   +-----------+  +-----------+
+				 |
 
 In the right-hand side case, the debug module offers a separation between the
 scmi_power_domain module and other HALs that will be used to fulfill the
@@ -78,38 +78,38 @@ driver.
 The driver will then inform the HAL via the drv_resp api, and the HAL
 won't take any further action, but updating its internal state.
 
-                        DEBUG                   DEBUG-DRIVER
-                          ~                          ~
-                          |                          |
-                          |                          |
-            EV  >>>>>>>>+-+-+                        |
-                        |   |     set_enabled        |
-                        |   +--------------------->+-+-+
-                        |   |                      |   +-----+
-                        |   |                      |   |     |
-                        |   |          PENDING     |   +<----+ ev
-                        |   +<---------------------+-+-+
-                        |   |                        |
-                        +-+-+                        |
-                          |                          |
-                          |                          |
-                        +-+-+    drv_rsp           +-+-+<------ ISR/ev
-                        |   +<---------------------+   |
-                        |   |                      |   |
-                        |   +--------------------->+   |
-                        +-+-+                      |   |
-                          |                        +-+-+
-                          |                          |
-                          ~                          ~
+			DEBUG                   DEBUG-DRIVER
+			  ~                          ~
+			  |                          |
+			  |                          |
+	    EV  >>>>>>>>+-+-+                        |
+			|   |     set_enabled        |
+			|   +--------------------->+-+-+
+			|   |                      |   +-----+
+			|   |                      |   |     |
+			|   |          PENDING     |   +<----+ ev
+			|   +<---------------------+-+-+
+			|   |                        |
+			+-+-+                        |
+			  |                          |
+			  |                          |
+			+-+-+    drv_rsp           +-+-+<------ ISR/ev
+			|   +<---------------------+   |
+			|   |                      |   |
+			|   +--------------------->+   |
+			+-+-+                      |   |
+			  |                        +-+-+
+			  |                          |
+			  ~                          ~
 
 
     LEGEND
     EV           : The module processes the request event originated by the
-                   driver through the driver_input_api
+		   driver through the driver_input_api
     ev           : (optional) A driver may internally generate an event to
-                   process later the request
+		   process later the request
     ISR/ev       : Either and ISR or an event will cause the driver to call
-                   the driver_response api.
+		   the driver_response api.
     drv_rsp      : driver_response api.
     PENDING      : The call returns with the FWK_PENDING status code
     set_enabled  : A request to the driver
@@ -126,27 +126,27 @@ The following diagrams shows the latter
 request coming from the hardware to the HAL. This mechanism ensures that the
 asynchronous support can be preserved.
 
-                        DEBUG                   DEBUG-DRIVER
-                          ~                          ~
-                          |                          |
-                          |                        +-+-+<----- ISR-PWR-UP
-                          |      enable_request    |   |
-                        +-+-+<---------------------+   |
-                        |   |                      |   |
-                        |   +-----+                |   |
-                        |   |     |                |   |
-                        |   +<----+ req_drv        |   |
-                        |   |                      |   |
-                        +-+-+--------------------->+   |
-                          |                        |   |
-                          |                        +-+-+
-                          |                          |
-                          ~                          ~
+			DEBUG                   DEBUG-DRIVER
+			  ~                          ~
+			  |                          |
+			  |                        +-+-+<----- ISR-PWR-UP
+			  |      enable_request    |   |
+			+-+-+<---------------------+   |
+			|   |                      |   |
+			|   +-----+                |   |
+			|   |     |                |   |
+			|   +<----+ req_drv        |   |
+			|   |                      |   |
+			+-+-+--------------------->+   |
+			  |                        |   |
+			  |                        +-+-+
+			  |                          |
+			  ~                          ~
 
     LEGEND
     ISR-PWR-UP     : An interrupt is raised from the platform and the driver
-                     acts
+		     acts
     enable_request : The driver calls the driver_input api to request the HAL
-                     to enable the debug features
+		     to enable the debug features
     req_drv        : The HAL generates the request_event
     ----->         : Direct call/return API
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/debug/include/mod_debug.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/debug/include/mod_debug.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/debug/include/mod_debug.h
@@ -217,7 +217,7 @@ struct mod_debug_driver_input_api {
      * \param response The response data structure.
      */
     void (*request_complete)(fwk_id_t id,
-                             struct mod_debug_response_params *response);
+			     struct mod_debug_response_params *response);
 };
 
 /*!
@@ -228,7 +228,7 @@ struct mod_debug_driver_input_api {
  */
 static const fwk_id_t mod_debug_event_id_req_enable_get =
     FWK_ID_EVENT_INIT(FWK_MODULE_IDX_DEBUG,
-                      MOD_DEBUG_PUBLIC_EVENT_IDX_REQ_ENABLE_GET);
+		      MOD_DEBUG_PUBLIC_EVENT_IDX_REQ_ENABLE_GET);
 
 /*!
  * \brief Set enable event identifier.
@@ -238,7 +238,7 @@ static const fwk_id_t mod_debug_event_id
  */
 static const fwk_id_t mod_debug_event_id_req_enable_set =
     FWK_ID_EVENT_INIT(FWK_MODULE_IDX_DEBUG,
-                      MOD_DEBUG_PUBLIC_EVENT_IDX_REQ_ENABLE_SET);
+		      MOD_DEBUG_PUBLIC_EVENT_IDX_REQ_ENABLE_SET);
 
 /*!
  * \}
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/debug/src/mod_debug.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/debug/src/mod_debug.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/debug/src/mod_debug.c
@@ -23,18 +23,18 @@
 static struct debug_dev_ctx *ctx_table;
 
 static inline void mark_user(fwk_id_t id,
-                             bool enable,
-                             enum scp_debug_user user_id)
+			     bool enable,
+			     enum scp_debug_user user_id)
 {
     uint32_t user_mask = (1 << user_id);
     struct debug_dev_ctx *ctx = &ctx_table[fwk_id_get_element_idx(id)];
 
     if (enable) {
-        /* Add user to the list of users that is enabling this element */
-        ctx->debug_users_mask |= user_mask;
+	/* Add user to the list of users that is enabling this element */
+	ctx->debug_users_mask |= user_mask;
     } else {
-        /* Remove current user */
-        ctx->debug_users_mask &= ~user_mask;
+	/* Remove current user */
+	ctx->debug_users_mask &= ~user_mask;
     }
 }
 
@@ -59,13 +59,13 @@ static int set_enabled(fwk_id_t id, bool
     struct mod_debug_request_params *req_params;
 
     if (!fwk_expect(user_id < SCP_DEBUG_USER_COUNT)) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     ctx = &ctx_table[fwk_id_get_element_idx(id)];
 
     if (ctx->state != DEBUG_IDLE) {
-        return FWK_E_BUSY;
+	return FWK_E_BUSY;
     }
 
     user_mask = (1 << user_id);
@@ -75,42 +75,42 @@ static int set_enabled(fwk_id_t id, bool
      * enabled it.
      */
     if ((enable && (ctx->debug_users_mask & user_mask)) ||
-        (!enable && (!(ctx->debug_users_mask & user_mask)))) {
-        return FWK_E_ACCESS;
+	(!enable && (!(ctx->debug_users_mask & user_mask)))) {
+	return FWK_E_ACCESS;
     }
 
     status = ctx->driver_api->set_enabled(ctx->config->driver_id,
-                                          enable, user_id);
+					  enable, user_id);
     if (status == FWK_PENDING) {
-        if (user_id == SCP_DEBUG_USER_DAP) {
-            /*
-             * In case the request comes from the driver, we won't respond so
-             * we don't even need to put an event
-             */
-            ctx->requester = user_id;
-            ctx->state = DEBUG_SET;
-            return FWK_PENDING;
-        }
-
-        req = (struct fwk_event) {
-            .target_id = id,
-            .id = mod_debug_event_id_req_enable_set,
-            .response_requested = true,
-        };
-
-        req_params = (struct mod_debug_request_params *)&req.params;
-        req_params->user_id = user_id;
-        req_params->enable = enable;
-
-        status = fwk_put_event(&req);
-        if (status == FWK_SUCCESS) {
-            ctx->state = DEBUG_SET;
-            return FWK_PENDING;
-        }
+	if (user_id == SCP_DEBUG_USER_DAP) {
+	    /*
+	     * In case the request comes from the driver, we won't respond so
+	     * we don't even need to put an event
+	     */
+	    ctx->requester = user_id;
+	    ctx->state = DEBUG_SET;
+	    return FWK_PENDING;
+	}
+
+	req = (struct fwk_event) {
+	    .target_id = id,
+	    .id = mod_debug_event_id_req_enable_set,
+	    .response_requested = true,
+	};
+
+	req_params = (struct mod_debug_request_params *)&req.params;
+	req_params->user_id = user_id;
+	req_params->enable = enable;
+
+	status = fwk_put_event(&req);
+	if (status == FWK_SUCCESS) {
+	    ctx->state = DEBUG_SET;
+	    return FWK_PENDING;
+	}
 
-        return status;
+	return status;
     } else if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     mark_user(id, enable, user_id);
@@ -126,34 +126,34 @@ static int get_enabled(fwk_id_t id, bool
     struct mod_debug_request_params *req_params;
 
     if ((enable == NULL) || !fwk_expect(user_id < SCP_DEBUG_USER_COUNT)) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     ctx = &ctx_table[fwk_id_get_element_idx(id)];
 
     if (ctx->state != DEBUG_IDLE) {
-        return FWK_E_BUSY;
+	return FWK_E_BUSY;
     }
 
     status = ctx->driver_api->get_enabled(ctx->config->driver_id,
-                                          enable, user_id);
+					  enable, user_id);
     if (status == FWK_PENDING) {
-        req = (struct fwk_event) {
-            .target_id = id,
-            .id = mod_debug_event_id_req_enable_get,
-            .response_requested = true,
-        };
-
-        req_params = (struct mod_debug_request_params *)&req.params;
-        req_params->user_id = user_id;
-
-        status = fwk_put_event(&req);
-        if (status == FWK_SUCCESS) {
-            ctx->state = DEBUG_GET;
-            return FWK_PENDING;
-        }
+	req = (struct fwk_event) {
+	    .target_id = id,
+	    .id = mod_debug_event_id_req_enable_get,
+	    .response_requested = true,
+	};
+
+	req_params = (struct mod_debug_request_params *)&req.params;
+	req_params->user_id = user_id;
+
+	status = fwk_put_event(&req);
+	if (status == FWK_SUCCESS) {
+	    ctx->state = DEBUG_GET;
+	    return FWK_PENDING;
+	}
 
-        return status;
+	return status;
     }
 
     return status;
@@ -181,8 +181,8 @@ static const struct mod_debug_api debug_
  */
 
 static int enable_request(fwk_id_t id,
-                          bool enable,
-                          enum scp_debug_user user_id)
+			  bool enable,
+			  enum scp_debug_user user_id)
 {
     struct fwk_event req;
     struct mod_debug_request_params *req_params;
@@ -193,9 +193,9 @@ static int enable_request(fwk_id_t id,
     fwk_assert(user_id == SCP_DEBUG_USER_DAP);
 
     req = (struct fwk_event) {
-        .target_id = id,
-        .source_id = ctx->config->driver_id,
-        .id = mod_debug_event_id_req_drv,
+	.target_id = id,
+	.source_id = ctx->config->driver_id,
+	.id = mod_debug_event_id_req_drv,
     };
 
     req_params = (struct mod_debug_request_params *)&req.params;
@@ -210,33 +210,33 @@ static int enable_request(fwk_id_t id,
  */
 
 static void request_complete(fwk_id_t id,
-                             struct mod_debug_response_params *response)
+			     struct mod_debug_response_params *response)
 {
     int status;
     struct fwk_event event;
     struct debug_dev_ctx *ctx;
     struct mod_debug_response_params *resp_params =
-        (struct mod_debug_response_params *)event.params;
+	(struct mod_debug_response_params *)event.params;
 
     fwk_assert(response != NULL);
 
     ctx = &ctx_table[fwk_id_get_element_idx(id)];
 
     if (ctx->requester == SCP_DEBUG_USER_DAP) {
-        mark_user(id, response->enabled, ctx->requester);
-        ctx->state = DEBUG_IDLE;
-        return;
+	mark_user(id, response->enabled, ctx->requester);
+	ctx->state = DEBUG_IDLE;
+	return;
     }
 
     event = (struct fwk_event) {
-        .source_id = ctx->config->driver_id,
-        .target_id = id,
+	.source_id = ctx->config->driver_id,
+	.target_id = id,
     };
 
     if (ctx->state == DEBUG_GET) {
-        event.id = mod_debug_event_id_get_complete;
+	event.id = mod_debug_event_id_get_complete;
     } else {
-        event.id = mod_debug_event_id_set_complete;
+	event.id = mod_debug_event_id_set_complete;
     }
 
     resp_params->status = response->status;
@@ -263,7 +263,7 @@ static int mod_debug_init(
 
     ctx_table = fwk_mm_calloc(element_count, sizeof(struct debug_dev_ctx));
     if (ctx_table == NULL) {
-        return FWK_E_NOMEM;
+	return FWK_E_NOMEM;
     }
 
     return FWK_SUCCESS;
@@ -294,7 +294,7 @@ static int mod_debug_bind(fwk_id_t id, u
     struct mod_debug_dev_config *dev_cfg;
 
     if ((round > 0) || fwk_id_is_type(id, FWK_ID_TYPE_MODULE)) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     ctx = &ctx_table[fwk_id_get_element_idx(id)];
@@ -302,17 +302,17 @@ static int mod_debug_bind(fwk_id_t id, u
 
     /* Bind to debug driver */
     status = fwk_module_bind(
-        dev_cfg->driver_id,
-        dev_cfg->driver_api_id,
-        &driver_api);
+	dev_cfg->driver_id,
+	dev_cfg->driver_api_id,
+	&driver_api);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     /* Validate driver API */
     if ((driver_api->set_enabled == NULL) ||
-        (driver_api->get_enabled == NULL)) {
-        return FWK_E_DATA;
+	(driver_api->get_enabled == NULL)) {
+	return FWK_E_DATA;
     }
 
     ctx->driver_api = driver_api;
@@ -333,26 +333,26 @@ static int mod_debug_process_bind_reques
 
     /* Only binding to elements is allowed */
     if (!fwk_id_is_type(target_id, FWK_ID_TYPE_ELEMENT)) {
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 
     ctx = &ctx_table[fwk_id_get_element_idx(target_id)];
 
     switch (api_idx) {
     case MOD_DEBUG_API_IDX_HAL:
-        *api = &debug_api;
+	*api = &debug_api;
 
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 
     case MOD_DEBUG_API_IDX_DRIVER_INPUT:
-        if (fwk_id_is_equal(source_id, ctx->config->driver_id)) {
-            *api = &driver_input_api;
-        }
+	if (fwk_id_is_equal(source_id, ctx->config->driver_id)) {
+	    *api = &driver_input_api;
+	}
 
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 
     default:
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 }
 
@@ -361,17 +361,17 @@ static int respond(const struct fwk_even
     int status;
     struct fwk_event response;
     struct mod_debug_response_params *resp_params =
-        (struct mod_debug_response_params *)response.params;
+	(struct mod_debug_response_params *)response.params;
     struct mod_debug_response_params *req_result =
-        (struct mod_debug_response_params *)event->params;
+	(struct mod_debug_response_params *)event->params;
     struct debug_dev_ctx *ctx =
-        &ctx_table[fwk_id_get_element_idx(event->target_id)];
+	&ctx_table[fwk_id_get_element_idx(event->target_id)];
 
     ctx->state = DEBUG_IDLE;
 
     status = fwk_get_delayed_response(event->target_id, ctx->cookie, &response);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     resp_params->status = req_result->status;
@@ -380,7 +380,7 @@ static int respond(const struct fwk_even
 }
 
 static int mod_debug_process_event(const struct fwk_event *event,
-                                   struct fwk_event *resp_event)
+				   struct fwk_event *resp_event)
 {
     int status;
     struct debug_dev_ctx *ctx;
@@ -393,45 +393,45 @@ static int mod_debug_process_event(const
     case MOD_DEBUG_PUBLIC_EVENT_IDX_REQ_ENABLE_GET:
     case MOD_DEBUG_PUBLIC_EVENT_IDX_REQ_ENABLE_SET:
     case MOD_DEBUG_EVENT_IDX_REQ_DRV:
-        req_params = (struct mod_debug_request_params *)event->params;
-        ctx->requester = req_params->user_id;
+	req_params = (struct mod_debug_request_params *)event->params;
+	ctx->requester = req_params->user_id;
 
-        /* Request from USER_AP we will need to respond */
-        if (resp_event->response_requested) {
-            ctx->cookie = event->cookie;
-            resp_event->is_delayed_response = true;
-            return FWK_SUCCESS;
-        }
-
-        /*
-         * When there is no response requested, we are processing an event
-         * generated by the driver (MOD_DEBUG_EVENT_IDX_REQ_DRV)
-         */
-        status = set_enabled(ctx->config->driver_id, req_params->enable,
-                            req_params->user_id);
-        if (status == FWK_PENDING) {
-            break;
-        }
-        if (status != FWK_SUCCESS) {
-            return FWK_E_DEVICE;
-        }
+	/* Request from USER_AP we will need to respond */
+	if (resp_event->response_requested) {
+	    ctx->cookie = event->cookie;
+	    resp_event->is_delayed_response = true;
+	    return FWK_SUCCESS;
+	}
+
+	/*
+	 * When there is no response requested, we are processing an event
+	 * generated by the driver (MOD_DEBUG_EVENT_IDX_REQ_DRV)
+	 */
+	status = set_enabled(ctx->config->driver_id, req_params->enable,
+			    req_params->user_id);
+	if (status == FWK_PENDING) {
+	    break;
+	}
+	if (status != FWK_SUCCESS) {
+	    return FWK_E_DEVICE;
+	}
 
-        break;
+	break;
 
     case MOD_DEBUG_EVENT_IDX_SET_COMPLETE:
-        req_result = (struct mod_debug_response_params *)event->params;
+	req_result = (struct mod_debug_response_params *)event->params;
 
-        if (req_result->status == FWK_SUCCESS) {
-            mark_user(event->target_id, req_result->enabled, ctx->requester);
-        }
+	if (req_result->status == FWK_SUCCESS) {
+	    mark_user(event->target_id, req_result->enabled, ctx->requester);
+	}
 
-        return respond(event);
+	return respond(event);
 
     case MOD_DEBUG_EVENT_IDX_GET_COMPLETE:
-        return respond(event);
+	return respond(event);
 
     default:
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     }
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/debugger_cli/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/debugger_cli/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/debugger_cli/CMakeLists.txt
@@ -8,10 +8,10 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_debugger_cli.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_debugger_cli.c")
 
 target_link_libraries(
     ${SCP_MODULE_TARGET}
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/debugger_cli/src/mod_debugger_cli.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/debugger_cli/src/mod_debugger_cli.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/debugger_cli/src/mod_debugger_cli.c
@@ -23,7 +23,7 @@ enum debugger_cli_internal_event_idx {
 
 static const fwk_id_t debugger_cli_event_id_request =
     FWK_ID_EVENT_INIT(FWK_MODULE_IDX_DEBUGGER_CLI,
-        DEBUGGER_CLI_INTERNAL_EVENT_IDX_ENTER_DEBUGGER);
+	DEBUGGER_CLI_INTERNAL_EVENT_IDX_ENTER_DEBUGGER);
 
 static struct mod_timer_alarm_api *alarm_api;
 
@@ -37,19 +37,19 @@ static void alarm_callback(uintptr_t mod
     status = fwk_io_getch(fwk_io_stdin, &ch);
 
     if (status == FWK_SUCCESS) {
-        /* Ctrl-E has been pressed */
-        if (ch == 0x05) {
-            /* Send out an event to start the CLI not inside an ISR */
-            event = &((struct fwk_event){
-                .source_id = FWK_ID_MODULE(module_idx),
-                .target_id = FWK_ID_MODULE(module_idx),
-                .id = debugger_cli_event_id_request
-            });
+	/* Ctrl-E has been pressed */
+	if (ch == 0x05) {
+	    /* Send out an event to start the CLI not inside an ISR */
+	    event = &((struct fwk_event){
+		.source_id = FWK_ID_MODULE(module_idx),
+		.target_id = FWK_ID_MODULE(module_idx),
+		.id = debugger_cli_event_id_request
+	    });
 
-            status = fwk_put_event(event);
+	    status = fwk_put_event(event);
 
-            fwk_assert(status == FWK_SUCCESS);
-        }
+	    fwk_assert(status == FWK_SUCCESS);
+	}
     }
 }
 
@@ -62,8 +62,8 @@ static int start_alarm(fwk_id_t id)
 
     /* Start the UART polling alarm, recurring with the given time period */
     return alarm_api->start(module_config->alarm_id, module_config->poll_period,
-        MOD_TIMER_ALARM_TYPE_PERIODIC, alarm_callback,
-        fwk_id_get_module_idx(id));
+	MOD_TIMER_ALARM_TYPE_PERIODIC, alarm_callback,
+	fwk_id_get_module_idx(id));
 }
 
 static int stop_alarm(fwk_id_t id)
@@ -84,7 +84,7 @@ static int debugger_cli_init(fwk_id_t mo
 }
 
 static int debugger_cli_element_init(fwk_id_t element_id, unsigned int unused,
-                                    const void *data)
+				    const void *data)
 {
     return FWK_SUCCESS;
 }
@@ -95,14 +95,14 @@ static int debugger_cli_bind(fwk_id_t id
 
     /* Only bind in the first round of calls */
     if (round > 0)
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 
     /* Retrieve the module config as specified by the platform config.c file */
     module_config = fwk_module_get_data(id);
 
     /* Bind to the specified alarm in order to poll the UART */
     return fwk_module_bind(module_config->alarm_id, MOD_TIMER_API_ID_ALARM,
-        &alarm_api);
+	&alarm_api);
 }
 
 static int debugger_cli_start(fwk_id_t id)
@@ -111,34 +111,34 @@ static int debugger_cli_start(fwk_id_t i
 }
 
 static int debugger_cli_process_event(const struct fwk_event *event,
-                                      struct fwk_event *resp_event)
+				      struct fwk_event *resp_event)
 {
     int status;
     int start_alarm_status;
 
     switch (event->id.event.event_idx) {
     case DEBUGGER_CLI_INTERNAL_EVENT_IDX_ENTER_DEBUGGER:
-        /* Start the CLI, blocking the rest of the event queue */
-        cli_print("[CLI_DEBUGGER_MODULE] Entering CLI\n");
+	/* Start the CLI, blocking the rest of the event queue */
+	cli_print("[CLI_DEBUGGER_MODULE] Entering CLI\n");
 
-        status = stop_alarm(event->target_id);
-        if (status != FWK_SUCCESS) {
-            goto exit_cli;
-        }
-
-        status = cli_start();
-
-        /* start the alarm regardless of the cli's return status */
-        start_alarm_status = start_alarm(event->target_id);
-        if (status == FWK_SUCCESS) {
-            status = start_alarm_status;
-        }
+	status = stop_alarm(event->target_id);
+	if (status != FWK_SUCCESS) {
+	    goto exit_cli;
+	}
+
+	status = cli_start();
+
+	/* start the alarm regardless of the cli's return status */
+	start_alarm_status = start_alarm(event->target_id);
+	if (status == FWK_SUCCESS) {
+	    status = start_alarm_status;
+	}
 
 exit_cli:
-        cli_print("\n[CLI_DEBUGGER_MODULE] Exiting CLI\n");
-        return status;
+	cli_print("\n[CLI_DEBUGGER_MODULE] Exiting CLI\n");
+	return status;
     default:
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 }
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/dmc500/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/dmc500/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/dmc500/CMakeLists.txt
@@ -8,9 +8,9 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_dmc500.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_dmc500.c")
 
 target_link_libraries(${SCP_MODULE_TARGET} PUBLIC module-timer)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/dmc500/include/mod_dmc500.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/dmc500/include/mod_dmc500.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/dmc500/include/mod_dmc500.h
@@ -481,9 +481,9 @@ struct mod_dmc500_reg {
  * \return The ADDRESS_CONTROL value.
  */
 #define ADDRESS_CONTROL_VAL(RANK, BANK, ROW, COL) (((RANK) << 24) | \
-                                                  ((BANK) << 16) | \
-                                                  ((ROW) << 8) | \
-                                                  (COL))
+						  ((BANK) << 16) | \
+						  ((ROW) << 8) | \
+						  (COL))
 
 /*!
  * \brief Create the MEMORY_TYPE value
@@ -495,8 +495,8 @@ struct mod_dmc500_reg {
  * \return The MEMORY_TYPE value.
  */
 #define MEMORY_TYPE_VAL(BANK_GROUP, WIDTH, TYPE) (((BANK_GROUP) << 16) | \
-                                                 ((WIDTH) << 8) | \
-                                                 (TYPE))
+						 ((WIDTH) << 8) | \
+						 (TYPE))
 
 /*!
  * \brief Create the FORMAT_CONTROL value.
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/dmc500/src/mod_dmc500.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/dmc500/src/mod_dmc500.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/dmc500/src/mod_dmc500.c
@@ -28,13 +28,13 @@ static int dmc500_config(struct mod_dmc5
 
 /* Framework API */
 static int mod_dmc500_init(fwk_id_t module_id, unsigned int element_count,
-                           const void *data)
+			   const void *data)
 {
     return FWK_SUCCESS;
 }
 
 static int mod_dmc500_element_init(fwk_id_t element_id, unsigned int unused,
-                                   const void *data)
+				   const void *data)
 {
     fwk_assert(data != NULL);
 
@@ -48,24 +48,24 @@ static int mod_dmc500_bind(fwk_id_t id,
 
     /* Nothing to do in the second round of calls. */
     if (round == 1)
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 
     /* Nothing to do in case of elements. */
     if (fwk_module_is_valid_element_id(id))
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 
     module_config = fwk_module_get_data(fwk_module_id_dmc500);
     fwk_assert(module_config != NULL);
 
     status = fwk_module_bind(module_config->ddr_phy_module_id,
-                             module_config->ddr_phy_api_id, &ddr_phy_api);
+			     module_config->ddr_phy_api_id, &ddr_phy_api);
     if (status != FWK_SUCCESS)
-        return status;
+	return status;
 
     status = fwk_module_bind(module_config->timer_id,
-                             FWK_ID_API(FWK_MODULE_IDX_TIMER, 0), &timer_api);
+			     FWK_ID_API(FWK_MODULE_IDX_TIMER, 0), &timer_api);
     if (status != FWK_SUCCESS)
-        return status;
+	return status;
 
     return FWK_SUCCESS;
 }
@@ -77,7 +77,7 @@ static int mod_dmc500_start(fwk_id_t id)
 
     /* Nothing to start for the module */
     if (fwk_module_is_valid_module_id(id))
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 
     element_config = fwk_module_get_data(id);
     dmc = (struct mod_dmc500_reg *)element_config->dmc;
@@ -176,21 +176,21 @@ static int dmc500_config(struct mod_dmc5
     dmc->DCB_INTERRUPT_CONTROL = reg_val->DCB_INTERRUPT_CONTROL;
     dmc->PMU_DCB_CONTROL = reg_val->PMU_DCB_CONTROL;
     dmc->PMU_DATA_CONTROL_BLOCK_ATTRIBUTE_MASK_0 =
-        reg_val->PMU_DATA_CONTROL_BLOCK_ATTRIBUTE_MASK_0;
+	reg_val->PMU_DATA_CONTROL_BLOCK_ATTRIBUTE_MASK_0;
     dmc->PMU_DATA_CONTROL_BLOCK_ATTRIBUTE_MATCH_0 =
-        reg_val->PMU_DATA_CONTROL_BLOCK_ATTRIBUTE_MATCH_0;
+	reg_val->PMU_DATA_CONTROL_BLOCK_ATTRIBUTE_MATCH_0;
     dmc->PMU_DATA_CONTROL_BLOCK_ATTRIBUTE_MASK_1 =
-        reg_val->PMU_DATA_CONTROL_BLOCK_ATTRIBUTE_MASK_1;
+	reg_val->PMU_DATA_CONTROL_BLOCK_ATTRIBUTE_MASK_1;
     dmc->PMU_DATA_CONTROL_BLOCK_ATTRIBUTE_MATCH_1 =
-        reg_val->PMU_DATA_CONTROL_BLOCK_ATTRIBUTE_MATCH_1;
+	reg_val->PMU_DATA_CONTROL_BLOCK_ATTRIBUTE_MATCH_1;
     dmc->PMU_DATA_CONTROL_BLOCK_ATTRIBUTE_MASK_2 =
-        reg_val->PMU_DATA_CONTROL_BLOCK_ATTRIBUTE_MASK_2;
+	reg_val->PMU_DATA_CONTROL_BLOCK_ATTRIBUTE_MASK_2;
     dmc->PMU_DATA_CONTROL_BLOCK_ATTRIBUTE_MATCH_2 =
-        reg_val->PMU_DATA_CONTROL_BLOCK_ATTRIBUTE_MATCH_2;
+	reg_val->PMU_DATA_CONTROL_BLOCK_ATTRIBUTE_MATCH_2;
     dmc->PMU_TAG_ENTRIES_ATTRIBUTE_MASK =
-        reg_val->PMU_TAG_ENTRIES_ATTRIBUTE_MASK;
+	reg_val->PMU_TAG_ENTRIES_ATTRIBUTE_MASK;
     dmc->PMU_TAG_ENTRIES_ATTRIBUTE_MATCH =
-        reg_val->PMU_TAG_ENTRIES_ATTRIBUTE_MATCH;
+	reg_val->PMU_TAG_ENTRIES_ATTRIBUTE_MATCH;
     dmc->QE_INTERRUPT_CONTROL = reg_val->QE_INTERRUPT_CONTROL;
     dmc->RANK_TURNAROUND_CONTROL = reg_val->RANK_TURNAROUND_CONTROL;
     dmc->HIT_TURNAROUND_CONTROL = reg_val->HIT_TURNAROUND_CONTROL;
@@ -213,41 +213,41 @@ static int dmc500_config(struct mod_dmc5
     dmc->PMU_QE_CONTROL = reg_val->PMU_QE_CONTROL;
     dmc->PMU_QE_MUX = reg_val->PMU_QE_MUX;
     dmc->PMU_QOS_ENGINE_ATTRIBUTE_MASK_0 =
-        reg_val->PMU_QOS_ENGINE_ATTRIBUTE_MASK_0;
+	reg_val->PMU_QOS_ENGINE_ATTRIBUTE_MASK_0;
     dmc->PMU_QOS_ENGINE_ATTRIBUTE_MATCH_0 =
-        reg_val->PMU_QOS_ENGINE_ATTRIBUTE_MATCH_0;
+	reg_val->PMU_QOS_ENGINE_ATTRIBUTE_MATCH_0;
     dmc->PMU_QOS_ENGINE_ATTRIBUTE_MASK_1 =
-        reg_val->PMU_QOS_ENGINE_ATTRIBUTE_MASK_1;
+	reg_val->PMU_QOS_ENGINE_ATTRIBUTE_MASK_1;
     dmc->PMU_QOS_ENGINE_ATTRIBUTE_MATCH_1 =
-        reg_val->PMU_QOS_ENGINE_ATTRIBUTE_MATCH_1;
+	reg_val->PMU_QOS_ENGINE_ATTRIBUTE_MATCH_1;
     dmc->PMU_QOS_ENGINE_ATTRIBUTE_MASK_2 =
-        reg_val->PMU_QOS_ENGINE_ATTRIBUTE_MASK_2;
+	reg_val->PMU_QOS_ENGINE_ATTRIBUTE_MASK_2;
     dmc->PMU_QOS_ENGINE_ATTRIBUTE_MATCH_2 =
-        reg_val->PMU_QOS_ENGINE_ATTRIBUTE_MATCH_2;
+	reg_val->PMU_QOS_ENGINE_ATTRIBUTE_MATCH_2;
     dmc->PMU_QUEUED_ENTRIES_ATTRIBUTE_MASK =
-        reg_val->PMU_QUEUED_ENTRIES_ATTRIBUTE_MASK;
+	reg_val->PMU_QUEUED_ENTRIES_ATTRIBUTE_MASK;
     dmc->PMU_QUEUED_ENTRIES_ATTRIBUTE_MATCH =
-        reg_val->PMU_QUEUED_ENTRIES_ATTRIBUTE_MATCH;
+	reg_val->PMU_QUEUED_ENTRIES_ATTRIBUTE_MATCH;
     dmc->MI_INTERRUPT_CONTROL = reg_val->MI_INTERRUPT_CONTROL;
     dmc->POWER_DOWN_CONTROL = reg_val->POWER_DOWN_CONTROL;
     dmc->REFRESH_CONTROL = reg_val->REFRESH_CONTROL;
     dmc->PMU_MI_CONTROL = reg_val->PMU_MI_CONTROL;
     dmc->PMU_MEMORY_IF_ATTRIBUTE_MASK_0 =
-        reg_val->PMU_MEMORY_IF_ATTRIBUTE_MASK_0;
+	reg_val->PMU_MEMORY_IF_ATTRIBUTE_MASK_0;
     dmc->PMU_MEMORY_IF_ATTRIBUTE_MATCH_0 =
-        reg_val->PMU_MEMORY_IF_ATTRIBUTE_MATCH_0;
+	reg_val->PMU_MEMORY_IF_ATTRIBUTE_MATCH_0;
     dmc->PMU_MEMORY_IF_ATTRIBUTE_MASK_1 =
-        reg_val->PMU_MEMORY_IF_ATTRIBUTE_MASK_1;
+	reg_val->PMU_MEMORY_IF_ATTRIBUTE_MASK_1;
     dmc->PMU_MEMORY_IF_ATTRIBUTE_MATCH_1 =
-        reg_val->PMU_MEMORY_IF_ATTRIBUTE_MATCH_1;
+	reg_val->PMU_MEMORY_IF_ATTRIBUTE_MATCH_1;
     dmc->PMU_BANK_STATES_ATTRIBUTE_MASK =
-        reg_val->PMU_BANK_STATES_ATTRIBUTE_MASK;
+	reg_val->PMU_BANK_STATES_ATTRIBUTE_MASK;
     dmc->PMU_BANK_STATES_ATTRIBUTE_MATCH =
-        reg_val->PMU_BANK_STATES_ATTRIBUTE_MATCH;
+	reg_val->PMU_BANK_STATES_ATTRIBUTE_MATCH;
     dmc->PMU_RANK_STATES_ATTRIBUTE_MASK =
-        reg_val->PMU_RANK_STATES_ATTRIBUTE_MASK;
+	reg_val->PMU_RANK_STATES_ATTRIBUTE_MASK;
     dmc->PMU_RANK_STATES_ATTRIBUTE_MATCH =
-        reg_val->PMU_RANK_STATES_ATTRIBUTE_MATCH;
+	reg_val->PMU_RANK_STATES_ATTRIBUTE_MATCH;
     dmc->CFG_INTERRUPT_CONTROL = reg_val->CFG_INTERRUPT_CONTROL;
     dmc->T_RDDATA_EN = reg_val->T_RDDATA_EN;
     dmc->T_PHYRDLAT = reg_val->T_PHYRDLAT;
@@ -267,7 +267,7 @@ static int dmc500_config(struct mod_dmc5
 
     status = ddr_phy_api->configure(ddr_phy_id);
     if (status != FWK_SUCCESS)
-        return status;
+	return status;
 
     dmc->PHY_CONFIG = 0x01000001;
     dmc->PHY_CONFIG = 0x01000000;
@@ -282,25 +282,25 @@ static int dmc500_config(struct mod_dmc5
     FWK_LOG_INFO("[DDR] Setting dmc in READY mode");
 
     status = timer_api->time_to_timestamp(module_config->timer_id,
-                                          1000 * 1000, &timeout);
+					  1000 * 1000, &timeout);
     if (status != FWK_SUCCESS)
-        return status;
+	return status;
 
     status = timer_api->get_counter(module_config->timer_id, &counter);
     if (status != FWK_SUCCESS)
-        return status;
+	return status;
 
     timeout += counter;
 
     while ((dmc->MI_STATUS & MOD_DMC500_MI_STATUS_IDLE) !=
-        MOD_DMC500_MI_STATUS_IDLE) {
-        status = timer_api->remaining(module_config->timer_id, timeout,
-                                      &remaining_ticks);
-        if (status != FWK_SUCCESS)
-            return status;
+	MOD_DMC500_MI_STATUS_IDLE) {
+	status = timer_api->remaining(module_config->timer_id, timeout,
+				      &remaining_ticks);
+	if (status != FWK_SUCCESS)
+	    return status;
 
-        if (remaining_ticks == 0)
-            goto timeout;
+	if (remaining_ticks == 0)
+	    goto timeout;
     }
 
     dmc->MI_STATE_CONTROL     = reg_val->MI_STATE_CONTROL;
@@ -311,37 +311,37 @@ static int dmc500_config(struct mod_dmc5
     FWK_LOG_INFO("[DDR] Waiting for Queue stall = 0...");
 
     while ((dmc->QUEUE_STATUS & MOD_DMC500_QUEUE_STATUS_STALL_ACK) != 0) {
-        status = timer_api->remaining(module_config->timer_id, timeout,
-                                      &remaining_ticks);
-        if (status != FWK_SUCCESS)
-            return status;
+	status = timer_api->remaining(module_config->timer_id, timeout,
+				      &remaining_ticks);
+	if (status != FWK_SUCCESS)
+	    return status;
 
-        if (remaining_ticks == 0)
-            goto timeout;
+	if (remaining_ticks == 0)
+	    goto timeout;
     }
 
     FWK_LOG_INFO("[DDR] Waiting for SI0 stall = 0...");
 
     while ((dmc->SI0_SI_STATUS & MOD_DMC500_SI_STATUS_STALL_ACK) != 0) {
-        status = timer_api->remaining(module_config->timer_id, timeout,
-                                      &remaining_ticks);
-        if (status != FWK_SUCCESS)
-            return status;
+	status = timer_api->remaining(module_config->timer_id, timeout,
+				      &remaining_ticks);
+	if (status != FWK_SUCCESS)
+	    return status;
 
-        if (remaining_ticks == 0)
-            goto timeout;
+	if (remaining_ticks == 0)
+	    goto timeout;
     }
 
     FWK_LOG_INFO("[DDR] Waiting for SI1 stall = 0...");
 
     while ((dmc->SI1_SI_STATUS & MOD_DMC500_SI_STATUS_STALL_ACK) != 0) {
-        status = timer_api->remaining(module_config->timer_id, timeout,
-                                      &remaining_ticks);
-        if (status != FWK_SUCCESS)
-            return status;
+	status = timer_api->remaining(module_config->timer_id, timeout,
+				      &remaining_ticks);
+	if (status != FWK_SUCCESS)
+	    return status;
 
-        if (remaining_ticks == 0)
-            goto timeout;
+	if (remaining_ticks == 0)
+	    goto timeout;
     }
 
     FWK_LOG_INFO("[DDR] DMC init done.");
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/dmc620/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/dmc620/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/dmc620/CMakeLists.txt
@@ -8,9 +8,9 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_dmc620.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_dmc620.c")
 
 target_link_libraries(${SCP_MODULE_TARGET} PRIVATE module-clock)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/dmc620/include/mod_dmc620.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/dmc620/include/mod_dmc620.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/dmc620/include/mod_dmc620.h
@@ -102,13 +102,13 @@ struct mod_dmc620_reg {
     FWK_R   uint32_t    MEMC_STATUS;
     FWK_R   uint32_t    MEMC_CONFIG;
     FWK_W   uint32_t    MEMC_CMD;
-            uint32_t    RESERVED1;
+	    uint32_t    RESERVED1;
     FWK_RW  uint32_t    ADDRESS_CONTROL_NEXT;
     FWK_RW  uint32_t    DECODE_CONTROL_NEXT;
     FWK_RW  uint32_t    FORMAT_CONTROL;
     FWK_RW  uint32_t    ADDRESS_MAP_NEXT;
     FWK_RW  uint32_t    LOW_POWER_CONTROL_NEXT;
-            uint32_t    RESERVED2;
+	    uint32_t    RESERVED2;
     FWK_RW  uint32_t    TURNAROUND_CONTROL_NEXT;
     FWK_RW  uint32_t    HIT_TURNAROUND_CONTROL_NEXT;
     FWK_RW  uint32_t    QOS_CLASS_CONTROL_NEXT;
@@ -131,8 +131,8 @@ struct mod_dmc620_reg {
     FWK_RW  uint32_t    ADDRESS_SHUTTER_191_160_NEXT;
     FWK_RW  uint32_t    MEMORY_ADDRESS_MAX_31_00_NEXT;
     FWK_RW  uint32_t    MEMORY_ADDRESS_MAX_43_32_NEXT;
-            struct mod_dmc620_access_address_next ACCESS_ADDRESS_NEXT
-                                            [MOD_DMC620_ACCESS_ADDRESS_COUNT];
+	    struct mod_dmc620_access_address_next ACCESS_ADDRESS_NEXT
+					    [MOD_DMC620_ACCESS_ADDRESS_COUNT];
     FWK_R   uint32_t    CHANNEL_STATUS;
     FWK_R   uint32_t    CHANNEL_STATUS_63_32;
     FWK_RW  uint32_t    DIRECT_ADDR;
@@ -142,11 +142,11 @@ struct mod_dmc620_reg {
     FWK_RW  uint32_t    DCI_STRB;
     FWK_RW  uint32_t    DCI_DATA;
     FWK_RW  uint32_t    REFRESH_CONTROL_NEXT;
-            uint32_t    RESERVED3;
+	    uint32_t    RESERVED3;
     FWK_RW  uint32_t    MEMORY_TYPE_NEXT;
-            uint32_t    RESERVED4;
+	    uint32_t    RESERVED4;
     FWK_RW  uint32_t    FEATURE_CONFIG;
-            uint32_t    RESERVED5;
+	    uint32_t    RESERVED5;
     FWK_RW  uint32_t    NIBBLE_FAILED_031_000;
     FWK_RW  uint32_t    NIBBLE_FAILED_063_032;
     FWK_RW  uint32_t    NIBBLE_FAILED_095_064;
@@ -155,7 +155,7 @@ struct mod_dmc620_reg {
     FWK_RW  uint32_t    QUEUE_ALLOCATE_CONTROL_063_032;
     FWK_RW  uint32_t    QUEUE_ALLOCATE_CONTROL_095_064;
     FWK_RW  uint32_t    QUEUE_ALLOCATE_CONTROL_127_096;
-            uint8_t     RESERVED6[0x16C - 0x158];
+	    uint8_t     RESERVED6[0x16C - 0x158];
     FWK_RW  uint32_t    LINK_ERR_COUNT;
     FWK_RW  uint32_t    SCRUB_CONTROL0_NEXT;
     FWK_RW  uint32_t    SCRUB_ADDRESS_MIN0_NEXT;
@@ -165,18 +165,18 @@ struct mod_dmc620_reg {
     FWK_RW  uint32_t    SCRUB_ADDRESS_MIN1_NEXT;
     FWK_RW  uint32_t    SCRUB_ADDRESS_MAX1_NEXT;
     FWK_R   uint32_t    SCRUB_ADDRESS_CURRENT1;
-            uint8_t     RESERVED7[0x1A0 - 0x190];
+	    uint8_t     RESERVED7[0x1A0 - 0x190];
     FWK_RW  uint32_t    CS_REMAP_CONTROL_31_00_NEXT;
     FWK_RW  uint32_t    CS_REMAP_CONTROL_63_32_NEXT;
     FWK_RW  uint32_t    CS_REMAP_CONTROL_95_64_NEXT;
     FWK_RW  uint32_t    CS_REMAP_CONTROL_127_96_NEXT;
     FWK_RW  uint32_t    CID_REMAP_CONTROL_31_00_NEXT;
     FWK_RW  uint32_t    CID_REMAP_CONTROL_63_32_NEXT;
-            uint8_t     RESERVED8[0x1C0 - 0x1B8];
+	    uint8_t     RESERVED8[0x1C0 - 0x1B8];
     FWK_RW  uint32_t    CKE_REMAP_CONTROL_NEXT;
     FWK_RW  uint32_t    RST_REMAP_CONTROL_NEXT;
     FWK_RW  uint32_t    CK_REMAP_CONTROL_NEXT;
-            uint32_t    RESERVED9;
+	    uint32_t    RESERVED9;
     FWK_RW  uint32_t    POWER_GROUP_CONTROL_31_00_NEXT;
     FWK_RW  uint32_t    POWER_GROUP_CONTROL_63_32_NEXT;
     FWK_RW  uint32_t    POWER_GROUP_CONTROL_95_64_NEXT;
@@ -184,31 +184,31 @@ struct mod_dmc620_reg {
     FWK_RW  uint32_t    PHY_RDWRDATA_CS_MASK_31_00;
     FWK_RW  uint32_t    PHY_RDWRDATA_CS_MASK_63_32;
     FWK_RW  uint32_t    PHY_REQUEST_CS_REMAP;
-            uint32_t    RESERVED10;
+	    uint32_t    RESERVED10;
     FWK_RW  uint32_t    FEATURE_CONTROL_NEXT;
     FWK_RW  uint32_t    MUX_CONTROL_NEXT;
     FWK_RW  uint32_t    RANK_REMAP_CONTROL_NEXT;
-            uint32_t    RESERVED11;
+	    uint32_t    RESERVED11;
     FWK_RW  uint32_t    T_REFI_NEXT;
     FWK_RW  uint32_t    T_RFC_NEXT;
     FWK_RW  uint32_t    T_MRR_NEXT;
     FWK_RW  uint32_t    T_MRW_NEXT;
-            uint8_t     RESERVED12[0x218 - 0x210];
+	    uint8_t     RESERVED12[0x218 - 0x210];
     FWK_RW  uint32_t    T_RCD_NEXT;
     FWK_RW  uint32_t    T_RAS_NEXT;
     FWK_RW  uint32_t    T_RP_NEXT;
     FWK_RW  uint32_t    T_RPALL_NEXT;
     FWK_RW  uint32_t    T_RRD_NEXT;
     FWK_RW  uint32_t    T_ACT_WINDOW_NEXT;
-            uint32_t    RESERVED13;
+	    uint32_t    RESERVED13;
     FWK_RW  uint32_t    T_RTR_NEXT;
     FWK_RW  uint32_t    T_RTW_NEXT;
     FWK_RW  uint32_t    T_RTP_NEXT;
-            uint32_t    RESERVED14;
+	    uint32_t    RESERVED14;
     FWK_RW  uint32_t    T_WR_NEXT;
     FWK_RW  uint32_t    T_WTR_NEXT;
     FWK_RW  uint32_t    T_WTW_NEXT;
-            uint32_t    RESERVED15;
+	    uint32_t    RESERVED15;
     FWK_RW  uint32_t    T_XMPD_NEXT;
     FWK_RW  uint32_t    T_EP_NEXT;
     FWK_RW  uint32_t    T_XP_NEXT;
@@ -220,11 +220,11 @@ struct mod_dmc620_reg {
     FWK_RW  uint32_t    T_PARITY_NEXT;
     FWK_RW  uint32_t    T_ZQCS_NEXT;
     FWK_RW  uint32_t    T_RW_ODT_CLR_NEXT;
-            uint8_t     RESERVED16[0x300 - 0x280];
+	    uint8_t     RESERVED16[0x300 - 0x280];
     FWK_RW  uint32_t    T_RDDATA_EN_NEXT;
     FWK_RW  uint32_t    T_PHYRDLAT_NEXT;
     FWK_RW  uint32_t    T_PHYWRLAT_NEXT;
-            uint32_t    RESERVED17;
+	    uint32_t    RESERVED17;
     FWK_RW  uint32_t    RDLVL_CONTROL_NEXT;
     FWK_RW  uint32_t    RDLVL_MRS_NEXT;
     FWK_RW  uint32_t    T_RDLVL_EN_NEXT;
@@ -233,7 +233,7 @@ struct mod_dmc620_reg {
     FWK_RW  uint32_t    WRLVL_MRS_NEXT;
     FWK_RW  uint32_t    T_WRLVL_EN_NEXT;
     FWK_RW  uint32_t    T_WRLVL_WW_NEXT;
-            uint32_t    RESERVED18;
+	    uint32_t    RESERVED18;
     FWK_R   uint32_t    TRAINING_WRLVL_SLICE_STATUS;
     FWK_R   uint32_t    TRAINING_RDLVL_SLICE_STATUS;
     FWK_R   uint32_t    TRAINING_RDLVL_GATE_SLICE_STATUS;
@@ -244,13 +244,13 @@ struct mod_dmc620_reg {
     FWK_RW  uint32_t    PHY_UPDATE_CONTROL_NEXT;
     FWK_RW  uint32_t    T_ODTH_NEXT;
     FWK_RW  uint32_t    ODT_TIMING_NEXT;
-            uint32_t    RESERVED19;
+	    uint32_t    RESERVED19;
     FWK_RW  uint32_t    ODT_WR_CONTROL_31_00_NEXT;
     FWK_RW  uint32_t    ODT_WR_CONTROL_63_32_NEXT;
     FWK_RW  uint32_t    ODT_RD_CONTROL_31_00_NEXT;
     FWK_RW  uint32_t    ODT_RD_CONTROL_63_32_NEXT;
     FWK_R   uint32_t    TEMPERATURE_READOUT;
-            uint32_t    RESERVED20;
+	    uint32_t    RESERVED20;
     FWK_R   uint32_t    TRAINING_STATUS;
     FWK_R   uint32_t    TRAINING_STATUS_63_32;
     FWK_RW  uint32_t    DQ_MAP_CONTROL_15_00_NEXT;
@@ -258,26 +258,26 @@ struct mod_dmc620_reg {
     FWK_RW  uint32_t    DQ_MAP_CONTROL_47_32_NEXT;
     FWK_RW  uint32_t    DQ_MAP_CONTROL_63_48_NEXT;
     FWK_RW  uint32_t    DQ_MAP_CONTROL_71_64_NEXT;
-            uint32_t    RESERVED21;
+	    uint32_t    RESERVED21;
     FWK_R   uint32_t    RANK_STATUS;
     FWK_R   uint32_t    MODE_CHANGE_STATUS;
-            uint8_t     RESERVED22[0x3B0 - 0x3A0];
+	    uint8_t     RESERVED22[0x3B0 - 0x3A0];
     FWK_RW  uint32_t    ODT_CP_CONTROL_31_00_NEXT;
     FWK_RW  uint32_t    ODT_CP_CONTROL_63_32_NEXT;
-            uint8_t     RESERVED23[0x400 - 0x3B8];
+	    uint8_t     RESERVED23[0x400 - 0x3B8];
     FWK_R   uint32_t    USER_STATUS;
-            uint32_t    RESERVED24;
+	    uint32_t    RESERVED24;
     FWK_RW  uint32_t    USER_CONFIG0_NEXT;
     FWK_RW  uint32_t    USER_CONFIG1_NEXT;
     FWK_RW  uint32_t    USER_CONFIG2;
     FWK_RW  uint32_t    USER_CONFIG3;
-            uint8_t     RESERVED25[0x500 - 0x418];
+	    uint8_t     RESERVED25[0x500 - 0x418];
     FWK_RW  uint32_t    INTERRUPT_CONTROL;
-            uint32_t    RESERVED26;
+	    uint32_t    RESERVED26;
     FWK_W   uint32_t    INTERRUPT_CLR;
-            uint32_t    RESERVED27;
+	    uint32_t    RESERVED27;
     FWK_R   uint32_t    INTERRUPT_STATUS;
-            uint8_t     RESERVED28[0x538 - 0x514];
+	    uint8_t     RESERVED28[0x538 - 0x514];
     FWK_R   uint32_t    FAILED_ACCESS_INT_INFO_31_00;
     FWK_R   uint32_t    FAILED_ACCESS_INT_INFO_63_32;
     FWK_R   uint32_t    FAILED_PROG_INT_INFO_31_00;
@@ -286,10 +286,10 @@ struct mod_dmc620_reg {
     FWK_R   uint32_t    LINK_ERR_INT_INFO_63_32;
     FWK_R   uint32_t    ARCH_FSM_INT_INFO_31_00;
     FWK_R   uint32_t    ARCH_FSM_INT_INFO_63_32;
-            uint8_t     RESERVED29[0x610 - 0x558];
+	    uint8_t     RESERVED29[0x610 - 0x558];
     FWK_RW  uint32_t    T_DB_TRAIN_RESP_NEXT;
     FWK_RW  uint32_t    T_LVL_DISCONNECT_NEXT;
-            uint8_t     RESERVED30[0x620 - 0x618];
+	    uint8_t     RESERVED30[0x620 - 0x618];
     FWK_RW  uint32_t    WDQLVL_CONTROL_NEXT;
     FWK_RW  uint32_t    WDQLVL_VREFDQ_TRAIN_MRS_NEXT;
     FWK_RW  uint32_t    WDQLVL_ADDRESS_31_00_NEXT;
@@ -299,21 +299,21 @@ struct mod_dmc620_reg {
     FWK_RW  uint32_t    T_WDQLVL_RW_NEXT;
     FWK_R   uint32_t    TRAINING_WDQLVL_SLICE_RESP;
     FWK_R   uint32_t    TRAINING_RDLVL_SLICE_RESP;
-            uint8_t     RESERVED31[0x654 - 0x644];
+	    uint8_t     RESERVED31[0x654 - 0x644];
     FWK_RW  uint32_t    PHYMSTR_CONTROL_NEXT;
-            uint8_t     RESERVED32[0x700 - 0x658];
+	    uint8_t     RESERVED32[0x700 - 0x658];
     FWK_R   uint32_t    ERR0FR;
-            uint32_t    RESERVED33;
+	    uint32_t    RESERVED33;
     FWK_RW  uint32_t    ERR0CTLR0;
     FWK_RW  uint32_t    ERR0CTLR1;
     FWK_R   uint32_t    ERR0STATUS;
-            uint8_t     RESERVED34[0x740 - 0x714];
+	    uint8_t     RESERVED34[0x740 - 0x714];
     FWK_R   uint32_t    ERR1FR;
-            uint32_t    RESERVED35;
+	    uint32_t    RESERVED35;
     FWK_R   uint32_t    ERR1CTLR;
-            uint32_t    RESERVED36;
+	    uint32_t    RESERVED36;
     FWK_R   uint32_t    ERR1STATUS;
-            uint32_t    RESERVED37;
+	    uint32_t    RESERVED37;
     FWK_RW  uint32_t    ERR1ADDR0;
     FWK_RW  uint32_t    ERR1ADDR1;
     FWK_RW  uint32_t    ERR1MISC0;
@@ -322,13 +322,13 @@ struct mod_dmc620_reg {
     FWK_RW  uint32_t    ERR1MISC3;
     FWK_RW  uint32_t    ERR1MISC4;
     FWK_RW  uint32_t    ERR1MISC5;
-            uint8_t     RESERVED38[0x780 - 0x778];
+	    uint8_t     RESERVED38[0x780 - 0x778];
     FWK_R   uint32_t    ERR2FR;
-            uint32_t    RESERVED39;
+	    uint32_t    RESERVED39;
     FWK_R   uint32_t    ERR2CTLR;
-            uint32_t    RESERVED40;
+	    uint32_t    RESERVED40;
     FWK_R   uint32_t    ERR2STATUS;
-            uint32_t    RESERVED41;
+	    uint32_t    RESERVED41;
     FWK_RW  uint32_t    ERR2ADDR0;
     FWK_RW  uint32_t    ERR2ADDR1;
     FWK_RW  uint32_t    ERR2MISC0;
@@ -337,70 +337,70 @@ struct mod_dmc620_reg {
     FWK_RW  uint32_t    ERR2MISC3;
     FWK_RW  uint32_t    ERR2MISC4;
     FWK_RW  uint32_t    ERR2MISC5;
-            uint8_t     RESERVED42[0x7C0 - 0x7B8];
+	    uint8_t     RESERVED42[0x7C0 - 0x7B8];
     FWK_R   uint32_t    ERR3FR;
-            uint32_t    RESERVED43;
+	    uint32_t    RESERVED43;
     FWK_R   uint32_t    ERR3CTLR;
-            uint32_t    RESERVED44;
+	    uint32_t    RESERVED44;
     FWK_R   uint32_t    ERR3STATUS;
-            uint32_t    RESERVED45;
+	    uint32_t    RESERVED45;
     FWK_RW  uint32_t    ERR3ADDR0;
     FWK_RW  uint32_t    ERR3ADDR1;
-            uint8_t     RESERVED46[0x800 - 0x7E0];
+	    uint8_t     RESERVED46[0x800 - 0x7E0];
     FWK_R   uint32_t    ERR4FR;
-            uint32_t    RESERVED47;
+	    uint32_t    RESERVED47;
     FWK_R   uint32_t    ERR4CTLR;
-            uint32_t    RESERVED48;
+	    uint32_t    RESERVED48;
     FWK_R   uint32_t    ERR4STATUS;
-            uint32_t    RESERVED49;
+	    uint32_t    RESERVED49;
     FWK_RW  uint32_t    ERR4ADDR0;
     FWK_RW  uint32_t    ERR4ADDR1;
     FWK_RW  uint32_t    ERR4MISC0;
     FWK_RW  uint32_t    ERR4MISC1;
     FWK_RW  uint32_t    ERR4MISC2;
-            uint8_t     RESERVED50[0x840 - 0x82C];
+	    uint8_t     RESERVED50[0x840 - 0x82C];
     FWK_R   uint32_t    ERR5FR;
-            uint32_t    RESERVED51;
+	    uint32_t    RESERVED51;
     FWK_R   uint32_t    ERR5CTLR;
-            uint32_t    RESERVED52;
+	    uint32_t    RESERVED52;
     FWK_R   uint32_t    ERR5STATUS;
-            uint32_t    RESERVED53;
+	    uint32_t    RESERVED53;
     FWK_RW  uint32_t    ERR5ADDR0;
     FWK_RW  uint32_t    ERR5ADDR1;
     FWK_RW  uint32_t    ERR5MISC0;
     FWK_RW  uint32_t    ERR5MISC1;
     FWK_RW  uint32_t    ERR5MISC2;
-            uint8_t     RESERVED54[0x880 - 0x86C];
+	    uint8_t     RESERVED54[0x880 - 0x86C];
     FWK_R   uint32_t    ERR6FR;
-            uint32_t    RESERVED55;
+	    uint32_t    RESERVED55;
     FWK_R   uint32_t    ERR6CTLR;
-            uint32_t    RESERVED56;
+	    uint32_t    RESERVED56;
     FWK_R   uint32_t    ERR6STATUS;
-            uint32_t    RESERVED57;
+	    uint32_t    RESERVED57;
     FWK_RW  uint32_t    ERR6ADDR0;
     FWK_RW  uint32_t    ERR6ADDR1;
     FWK_RW  uint32_t    ERR6MISC0;
     FWK_RW  uint32_t    ERR6MISC1;
-            uint8_t     RESERVED58[0x920 - 0x8A8];
+	    uint8_t     RESERVED58[0x920 - 0x8A8];
     FWK_RW  uint32_t    ERRGSR;
-            uint8_t     RESERVED59[0xA00 - 0x924];
+	    uint8_t     RESERVED59[0xA00 - 0x924];
     FWK_W   uint32_t    PMU_SNAPSHOT_REQ;
     FWK_R   uint32_t    PMU_SNAPSHOT_ACK;
     FWK_RW  uint32_t    PMU_OVERFLOW_STATUS_CLKDIV2;
     FWK_RW  uint32_t    PMU_OVERFLOW_STATUS_CLK;
-            struct mod_dmc620_pmu_counter PMC_CLKDIV2_COUNT[8];
-            struct mod_dmc620_pmu_counter PMC_CLK_COUNT[2];
-            uint8_t     RESERVED60[0xE00 - 0xBA0];
+	    struct mod_dmc620_pmu_counter PMC_CLKDIV2_COUNT[8];
+	    struct mod_dmc620_pmu_counter PMC_CLK_COUNT[2];
+	    uint8_t     RESERVED60[0xE00 - 0xBA0];
     FWK_RW  uint32_t    INTEG_CFG;
-            uint32_t    RESERVED61;
+	    uint32_t    RESERVED61;
     FWK_W   uint32_t    INTEG_OUTPUTS;
-            uint8_t     RESERVED62[0x1010 - 0xE0C];
+	    uint8_t     RESERVED62[0x1010 - 0xE0C];
     FWK_R   uint32_t    ADDRESS_CONTROL_NOW;
     FWK_R   uint32_t    DECODE_CONTROL_NOW;
-            uint32_t    RESERVED63;
+	    uint32_t    RESERVED63;
     FWK_R   uint32_t    ADDRESS_MAP_NOW;
     FWK_R   uint32_t    LOW_POWER_CONTROL_NOW;
-            uint32_t    RESERVED64;
+	    uint32_t    RESERVED64;
     FWK_R   uint32_t    TURNAROUND_CONTROL_NOW;
     FWK_R   uint32_t    HIT_TURNAROUND_CONTROL_NOW;
     FWK_R   uint32_t    QOS_CLASS_CONTROL_NOW;
@@ -423,31 +423,31 @@ struct mod_dmc620_reg {
     FWK_R   uint32_t    ADDRESS_SHUTTER_191_160_NOW;
     FWK_R   uint32_t    MEMORY_ADDRESS_MAX_31_00_NOW;
     FWK_R   uint32_t    MEMORY_ADDRESS_MAX_43_32_NOW;
-            struct mod_dmc620_access_address_now ACCESS_ADDRESS_NOW
-                                            [MOD_DMC620_ACCESS_ADDRESS_COUNT];
-            uint8_t     RESERVED65[0x1110 - 0x1100];
+	    struct mod_dmc620_access_address_now ACCESS_ADDRESS_NOW
+					    [MOD_DMC620_ACCESS_ADDRESS_COUNT];
+	    uint8_t     RESERVED65[0x1110 - 0x1100];
     FWK_R   uint32_t    DCI_REPLAY_TYPE_NOW;
     FWK_R   uint32_t    DIRECT_CONTROL_NOW;
-            uint8_t     RESERVED66[0x1120 - 0x1118];
+	    uint8_t     RESERVED66[0x1120 - 0x1118];
     FWK_R   uint32_t    REFRESH_CONTROL_NOW;
-            uint32_t    RESERVED67;
+	    uint32_t    RESERVED67;
     FWK_R   uint32_t    MEMORY_TYPE_NOW;
-            uint8_t     RESERVED68[0x1170 - 0x112C];
+	    uint8_t     RESERVED68[0x1170 - 0x112C];
     FWK_R   uint32_t    SCRUB_CONTROL0_NOW;
     FWK_R   uint32_t    SCRUB_ADDRESS_MIN0_NOW;
     FWK_R   uint32_t    SCRUB_ADDRESS_MAX0_NOW;
-            uint32_t    RESERVED69;
+	    uint32_t    RESERVED69;
     FWK_R   uint32_t    SCRUB_CONTROL1_NOW;
     FWK_R   uint32_t    SCRUB_ADDRESS_MIN1_NOW;
     FWK_R   uint32_t    SCRUB_ADDRESS_MAX1_NOW;
-            uint8_t     RESERVED70[0x11A0 - 0x118C];
+	    uint8_t     RESERVED70[0x11A0 - 0x118C];
     FWK_R   uint32_t    CS_REMAP_CONTROL_31_00_NOW;
     FWK_R   uint32_t    CS_REMAP_CONTROL_63_32_NOW;
     FWK_R   uint32_t    CS_REMAP_CONTROL_95_64_NOW;
     FWK_R   uint32_t    CS_REMAP_CONTROL_127_96_NOW;
     FWK_R   uint32_t    CID_REMAP_CONTROL_31_00_NOW;
     FWK_R   uint32_t    CID_REMAP_CONTROL_63_32_NOW;
-            uint8_t     RESERVED71[0x11C0 - 0x11B8];
+	    uint8_t     RESERVED71[0x11C0 - 0x11B8];
     FWK_R   uint32_t    CKE_REMAP_CONTROL_31_00_NOW;
     FWK_R   uint32_t    RST_REMAP_CONTROL_31_00_NOW;
     FWK_R   uint32_t    CK_REMAP_CONTROL_31_00_NOW;
@@ -455,31 +455,31 @@ struct mod_dmc620_reg {
     FWK_R   uint32_t    POWER_GROUP_CONTROL_63_32_NOW;
     FWK_R   uint32_t    POWER_GROUP_CONTROL_95_64_NOW;
     FWK_R   uint32_t    POWER_GROUP_CONTROL_127_96_NOW;
-            uint8_t     RESERVED72[0x11F0 - 0x11E0];
+	    uint8_t     RESERVED72[0x11F0 - 0x11E0];
     FWK_R   uint32_t    FEATURE_CONTROL_NOW;
     FWK_R   uint32_t    MUX_CONTROL_NOW;
     FWK_R   uint32_t    RANK_REMAP_CONTROL_NOW;
-            uint32_t    RESERVED73;
+	    uint32_t    RESERVED73;
     FWK_R   uint32_t    T_REFI_NOW;
     FWK_R   uint32_t    T_RFC_NOW;
     FWK_R   uint32_t    T_MRR_NOW;
     FWK_R   uint32_t    T_MRW_NOW;
-            uint8_t     RESERVED74[0x1218 - 0x1210];
+	    uint8_t     RESERVED74[0x1218 - 0x1210];
     FWK_R   uint32_t    T_RCD_NOW;
     FWK_R   uint32_t    T_RAS_NOW;
     FWK_R   uint32_t    T_RP_NOW;
     FWK_R   uint32_t    T_RPALL_NOW;
     FWK_R   uint32_t    T_RRD_NOW;
     FWK_R   uint32_t    T_ACT_WINDOW_NOW;
-            uint32_t    RESERVED75;
+	    uint32_t    RESERVED75;
     FWK_R   uint32_t    T_RTR_NOW;
     FWK_R   uint32_t    T_RTW_NOW;
     FWK_R   uint32_t    T_RTP_NOW;
-            uint32_t    RESERVED76;
+	    uint32_t    RESERVED76;
     FWK_R   uint32_t    T_WR_NOW;
     FWK_R   uint32_t    T_WTR_NOW;
     FWK_R   uint32_t    T_WTW_NOW;
-            uint32_t    RESERVED77;
+	    uint32_t    RESERVED77;
     FWK_R   uint32_t    T_XMPD_NOW;
     FWK_R   uint32_t    T_EP_NOW;
     FWK_R   uint32_t    T_XP_NOW;
@@ -491,11 +491,11 @@ struct mod_dmc620_reg {
     FWK_R   uint32_t    T_PARITY_NOW;
     FWK_R   uint32_t    T_ZQCS_NOW;
     FWK_R   uint32_t    T_RW_ODT_CLR_NOW;
-            uint8_t     RESERVED78[0x1300 - 0x1280];
+	    uint8_t     RESERVED78[0x1300 - 0x1280];
     FWK_R   uint32_t    T_RDDATA_EN_NOW;
     FWK_R   uint32_t    T_PHYRDLAT_NOW;
     FWK_R   uint32_t    T_PHYWRLAT_NOW;
-            uint32_t    RESERVED79;
+	    uint32_t    RESERVED79;
     FWK_R   uint32_t    RDLVL_CONTROL_NOW;
     FWK_R   uint32_t    RDLVL_MRS_NOW;
     FWK_R   uint32_t    T_RDLVL_EN_NOW;
@@ -504,33 +504,33 @@ struct mod_dmc620_reg {
     FWK_R   uint32_t    WRLVL_MRS_NOW;
     FWK_R   uint32_t    T_WRLVL_EN_NOW;
     FWK_R   uint32_t    T_WRLVL_WW_NOW;
-            uint8_t     RESERVED80[0x1348 - 0x1330];
+	    uint8_t     RESERVED80[0x1348 - 0x1330];
     FWK_R   uint32_t    PHY_POWER_CONTROL_NOW;
     FWK_R   uint32_t    T_LPRESP_NOW;
     FWK_R   uint32_t    PHY_UPDATE_CONTROL_NOW;
     FWK_R   uint32_t    T_ODTH_NOW;
     FWK_R   uint32_t    ODT_TIMING_NOW;
-            uint32_t    RESERVED81;
+	    uint32_t    RESERVED81;
     FWK_R   uint32_t    ODT_WR_CONTROL_31_00_NOW;
     FWK_R   uint32_t    ODT_WR_CONTROL_63_32_NOW;
     FWK_R   uint32_t    ODT_RD_CONTROL_31_00_NOW;
     FWK_R   uint32_t    ODT_RD_CONTROL_63_32_NOW;
-            uint8_t     RESERVED82[0x1380 - 0x1370];
+	    uint8_t     RESERVED82[0x1380 - 0x1370];
     FWK_R   uint32_t    DQ_MAP_CONTROL_15_00_NOW;
     FWK_R   uint32_t    DQ_MAP_CONTROL_31_16_NOW;
     FWK_R   uint32_t    DQ_MAP_CONTROL_47_32_NOW;
     FWK_R   uint32_t    DQ_MAP_CONTROL_63_48_NOW;
     FWK_R   uint32_t    DQ_MAP_CONTROL_71_64_NOW;
-            uint8_t     RESERVED83[0x13B0 - 0x1394];
+	    uint8_t     RESERVED83[0x13B0 - 0x1394];
     FWK_R   uint32_t    ODT_CP_CONTROL_31_00_NOW;
     FWK_R   uint32_t    ODT_CP_CONTROL_63_32_NOW;
-            uint8_t     RESERVED84[0x1408 - 0x13B8];
+	    uint8_t     RESERVED84[0x1408 - 0x13B8];
     FWK_R   uint32_t    USER_CONFIG0_NOW;
     FWK_R   uint32_t    USER_CONFIG1_NOW;
-            uint8_t     RESERVED85[0x1610 - 0x1410];
+	    uint8_t     RESERVED85[0x1610 - 0x1410];
     FWK_R   uint32_t    T_DB_TRAIN_RESP_NOW;
     FWK_R   uint32_t    T_LVL_DISCONNECT_NOW;
-            uint8_t     RESERVED86[0x1620 - 0x1618];
+	    uint8_t     RESERVED86[0x1620 - 0x1618];
     FWK_R   uint32_t    WDQLVL_CONTROL_NOW;
     FWK_R   uint32_t    WDQLVL_VREFDQ_TRAIN_MRS_NOW;
     FWK_R   uint32_t    WDQLVL_ADDRESS_31_00_NOW;
@@ -538,11 +538,11 @@ struct mod_dmc620_reg {
     FWK_R   uint32_t    T_WDQLVL_EN_NOW;
     FWK_R   uint32_t    T_WDQLVL_WW_NOW;
     FWK_R   uint32_t    T_WDQLVL_RW_NOW;
-            uint8_t     RESERVED87[0x1654 - 0x163C];
+	    uint8_t     RESERVED87[0x1654 - 0x163C];
     FWK_R   uint32_t    PHYMSTR_CONTROL_NOW;
-            uint8_t     RESERVED88[0x1FD0 - 0x1658];
+	    uint8_t     RESERVED88[0x1FD0 - 0x1658];
     FWK_R   uint32_t    PERIPH_ID_4;
-            uint8_t     RESERVED89[0x1FE0 - 0x1FD4];
+	    uint8_t     RESERVED89[0x1FE0 - 0x1FD4];
     FWK_R   uint32_t    PERIPH_ID_0;
     FWK_R   uint32_t    PERIPH_ID_1;
     FWK_R   uint32_t    PERIPH_ID_2;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/dmc620/src/mod_dmc620.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/dmc620/src/mod_dmc620.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/dmc620/src/mod_dmc620.c
@@ -29,13 +29,13 @@ static int dmc620_config(struct mod_dmc6
 
 /* Framework API */
 static int mod_dmc620_init(fwk_id_t module_id, unsigned int element_count,
-                           const void *config)
+			   const void *config)
 {
     return FWK_SUCCESS;
 }
 
 static int mod_dmc620_element_init(fwk_id_t element_id, unsigned int unused,
-                                   const void *data)
+				   const void *data)
 {
     fwk_assert(data != NULL);
 
@@ -49,20 +49,20 @@ static int mod_dmc620_bind(fwk_id_t id,
 
     /* Nothing to do in the second round of calls. */
     if (round == 1)
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 
     /* Nothing to do in case of elements. */
     if (fwk_module_is_valid_element_id(id))
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 
     module_config = fwk_module_get_data(fwk_module_id_dmc620);
     fwk_assert(module_config != NULL);
 
     if (!(fwk_id_is_equal(module_config->ddr_module_id, FWK_ID_NONE))) {
-        status = fwk_module_bind(module_config->ddr_module_id,
-                                 module_config->ddr_api_id, &ddr_phy_api);
-        if (status != FWK_SUCCESS)
-            return status;
+	status = fwk_module_bind(module_config->ddr_module_id,
+				 module_config->ddr_api_id, &ddr_phy_api);
+	if (status != FWK_SUCCESS)
+	    return status;
     }
 
     return FWK_SUCCESS;
@@ -73,15 +73,15 @@ static int mod_dmc620_start(fwk_id_t id)
     const struct mod_dmc620_element_config *element_config;
 
     if (!fwk_id_is_type(id, FWK_ID_TYPE_ELEMENT))
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 
     element_config = fwk_module_get_data(id);
 
     /* Register elements for clock state notifications */
     return fwk_notification_subscribe(
-        mod_clock_notification_id_state_changed,
-        element_config->clock_id,
-        id);
+	mod_clock_notification_id_state_changed,
+	element_config->clock_id,
+	id);
 }
 
 static int dmc620_notify_system_state_transition_resume(fwk_id_t id)
@@ -102,13 +102,13 @@ static int mod_dmc620_process_notificati
     struct clock_notification_params *params;
 
     fwk_assert(
-        fwk_id_is_equal(event->id, mod_clock_notification_id_state_changed));
+	fwk_id_is_equal(event->id, mod_clock_notification_id_state_changed));
     fwk_assert(fwk_id_is_type(event->target_id, FWK_ID_TYPE_ELEMENT));
 
     params = (struct clock_notification_params *)event->params;
 
     if (params->new_state == MOD_CLOCK_STATE_RUNNING)
-        return dmc620_notify_system_state_transition_resume(event->target_id);
+	return dmc620_notify_system_state_transition_resume(event->target_id);
 
     return FWK_SUCCESS;
 }
@@ -156,13 +156,13 @@ static int dmc620_config(struct mod_dmc6
     dmc->TIMEOUT_CONTROL_NEXT = reg_val->TIMEOUT_CONTROL_NEXT;
     dmc->CREDIT_CONTROL_NEXT = reg_val->CREDIT_CONTROL_NEXT;
     dmc->WRITE_PRIORITY_CONTROL_31_00_NEXT =
-        reg_val->WRITE_PRIORITY_CONTROL_31_00_NEXT;
+	reg_val->WRITE_PRIORITY_CONTROL_31_00_NEXT;
     dmc->WRITE_PRIORITY_CONTROL_63_32_NEXT =
-        reg_val->WRITE_PRIORITY_CONTROL_63_32_NEXT;
+	reg_val->WRITE_PRIORITY_CONTROL_63_32_NEXT;
     dmc->QUEUE_THRESHOLD_CONTROL_31_00_NEXT =
-        reg_val->QUEUE_THRESHOLD_CONTROL_31_00_NEXT;
+	reg_val->QUEUE_THRESHOLD_CONTROL_31_00_NEXT;
     dmc->QUEUE_THRESHOLD_CONTROL_63_32_NEXT =
-        reg_val->QUEUE_THRESHOLD_CONTROL_63_32_NEXT;
+	reg_val->QUEUE_THRESHOLD_CONTROL_63_32_NEXT;
     dmc->ADDRESS_SHUTTER_31_00_NEXT = reg_val->ADDRESS_SHUTTER_31_00_NEXT;
     dmc->ADDRESS_SHUTTER_63_32_NEXT = reg_val->ADDRESS_SHUTTER_63_32_NEXT;
     dmc->ADDRESS_SHUTTER_95_64_NEXT = reg_val->ADDRESS_SHUTTER_95_64_NEXT;
@@ -172,25 +172,25 @@ static int dmc620_config(struct mod_dmc6
     dmc->MEMORY_ADDRESS_MAX_31_00_NEXT = reg_val->MEMORY_ADDRESS_MAX_31_00_NEXT;
     dmc->MEMORY_ADDRESS_MAX_43_32_NEXT = reg_val->MEMORY_ADDRESS_MAX_43_32_NEXT;
     dmc->ACCESS_ADDRESS_NEXT[0].MIN_31_00 =
-        reg_val->ACCESS_ADDRESS_NEXT[0].MIN_31_00;
+	reg_val->ACCESS_ADDRESS_NEXT[0].MIN_31_00;
     dmc->ACCESS_ADDRESS_NEXT[0].MIN_43_32 =
-        reg_val->ACCESS_ADDRESS_NEXT[0].MIN_43_32;
+	reg_val->ACCESS_ADDRESS_NEXT[0].MIN_43_32;
     dmc->ACCESS_ADDRESS_NEXT[1].MIN_31_00 =
-        reg_val->ACCESS_ADDRESS_NEXT[1].MIN_31_00;
+	reg_val->ACCESS_ADDRESS_NEXT[1].MIN_31_00;
     dmc->ACCESS_ADDRESS_NEXT[1].MIN_43_32 =
-        reg_val->ACCESS_ADDRESS_NEXT[1].MIN_43_32;
+	reg_val->ACCESS_ADDRESS_NEXT[1].MIN_43_32;
     dmc->ACCESS_ADDRESS_NEXT[2].MIN_31_00 =
-        reg_val->ACCESS_ADDRESS_NEXT[2].MIN_31_00;
+	reg_val->ACCESS_ADDRESS_NEXT[2].MIN_31_00;
     dmc->ACCESS_ADDRESS_NEXT[3].MIN_31_00 =
-        reg_val->ACCESS_ADDRESS_NEXT[3].MIN_31_00;
+	reg_val->ACCESS_ADDRESS_NEXT[3].MIN_31_00;
     dmc->ACCESS_ADDRESS_NEXT[4].MIN_31_00 =
-        reg_val->ACCESS_ADDRESS_NEXT[4].MIN_31_00;
+	reg_val->ACCESS_ADDRESS_NEXT[4].MIN_31_00;
     dmc->ACCESS_ADDRESS_NEXT[5].MIN_31_00 =
-        reg_val->ACCESS_ADDRESS_NEXT[5].MIN_31_00;
+	reg_val->ACCESS_ADDRESS_NEXT[5].MIN_31_00;
     dmc->ACCESS_ADDRESS_NEXT[6].MIN_31_00 =
-        reg_val->ACCESS_ADDRESS_NEXT[6].MIN_31_00;
+	reg_val->ACCESS_ADDRESS_NEXT[6].MIN_31_00;
     dmc->ACCESS_ADDRESS_NEXT[7].MIN_31_00 =
-        reg_val->ACCESS_ADDRESS_NEXT[7].MIN_31_00;
+	reg_val->ACCESS_ADDRESS_NEXT[7].MIN_31_00;
 
     dmc->DCI_REPLAY_TYPE_NEXT = reg_val->DCI_REPLAY_TYPE_NEXT;
     dmc->DCI_STRB = reg_val->DCI_STRB;
@@ -257,17 +257,17 @@ static int dmc620_config(struct mod_dmc6
     dmc->ERR0CTLR0 = reg_val->ERR0CTLR0;
 
     if (!(fwk_id_is_equal(module_config->ddr_module_id, FWK_ID_NONE)))
-        ddr_phy_api->configure(ddr_id);
+	ddr_phy_api->configure(ddr_id);
 
     for (i = 0; i < 3; i++) /* ~200ns */
-        __NOP();
+	__NOP();
 
     FWK_LOG_INFO("[DDR] Sending direct DDR commands");
 
     module_config->direct_ddr_cmd(dmc);
 
     for (i = 0; i < 5; i++) /* ~400ns */
-        __NOP();
+	__NOP();
 
     /* Switch to READY */
     FWK_LOG_INFO("[DDR] Setting DMC to READY mode");
@@ -276,7 +276,7 @@ static int dmc620_config(struct mod_dmc6
     dmc->MEMC_CMD = MOD_DMC620_MEMC_CMD_EXECUTE;
 
     while ((dmc->MEMC_STATUS & MOD_DMC620_MEMC_CMD) != MOD_DMC620_MEMC_CMD_GO)
-        continue;
+	continue;
 
     FWK_LOG_INFO("[DDR] DMC init done.");
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/dvfs/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/dvfs/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/dvfs/CMakeLists.txt
@@ -10,7 +10,7 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(
     ${SCP_MODULE_TARGET}
@@ -18,5 +18,5 @@ target_sources(
     PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_dvfs.c")
 
 target_link_libraries(${SCP_MODULE_TARGET} PRIVATE module-clock module-psu
-                                                   module-timer
-                                                   module-scmi-perf)
+						   module-timer
+						   module-scmi-perf)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/dvfs/doc/module_dvfs_architecture.md
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/dvfs/doc/module_dvfs_architecture.md
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/dvfs/doc/module_dvfs_architecture.md
@@ -79,11 +79,11 @@ request with the last level/limits value
 3) DVFS domain request in progress(domain, OPP)
 
     Is there a request pending already ?
-        Yes.
-            Overwrite the request OPP with the new OPP.
-        No.
-            Set the initial values for the pending request.
-            number of retries, OPP, etc.
+	Yes.
+	    Overwrite the request OPP with the new OPP.
+	No.
+	    Set the initial values for the pending request.
+	    number of retries, OPP, etc.
     Exit.
 
 
@@ -95,40 +95,40 @@ request with the last level/limits value
       This OPP is now the target OPP.
     - Get the voltage from the domain PSU (*) or use the current value if
       available.
-        If the current voltage is less than the requested voltage
-            Increase the voltage to the OPP voltage (*)
-            Set the frequency to the OPP frequency (*)
-        Else if the current voltage is higher than the requested voltage
-            Set the frequency to the OPP frequency (*)
-            Decrease the voltage to the OPP voltage (*)
+	If the current voltage is less than the requested voltage
+	    Increase the voltage to the OPP voltage (*)
+	    Set the frequency to the OPP frequency (*)
+	Else if the current voltage is higher than the requested voltage
+	    Set the frequency to the OPP frequency (*)
+	    Decrease the voltage to the OPP voltage (*)
     - DVFS set_level operation complete, check status.
-        - SUCCESS
-            Does the domain have a request pending, (see 3 above) ?
-                Yes, go to (5) with the frequency from the _pending_
-                request.
-                No, all done, exit.
-        - FAILURE
-            Does this request require a retry ?
-                Yes.
-                    Increment the total number of retries for this request.
-
-                    If the maximum number of attempts for this request has not
-                    yet been tried go to (5) using the frequency from the
-                    _current_ request.
-
-                    Maximum number of attempts has been reached, discard the
-                    request.
-
-                    Does the domain have a request pending, (see 3 above) ?
-                        Yes, go to (5) with the frequency from the _pending_
-                        request.
-                        No, all done, exit.
-                No.
-                    Discard the current request.
-                    Does the domain have a request pending, (see 3 above) ?
-                        Yes, go to (5) with the frequency from the _pending_
-                        request.
-                        No, all done, exit.
+	- SUCCESS
+	    Does the domain have a request pending, (see 3 above) ?
+		Yes, go to (5) with the frequency from the _pending_
+		request.
+		No, all done, exit.
+	- FAILURE
+	    Does this request require a retry ?
+		Yes.
+		    Increment the total number of retries for this request.
+
+		    If the maximum number of attempts for this request has not
+		    yet been tried go to (5) using the frequency from the
+		    _current_ request.
+
+		    Maximum number of attempts has been reached, discard the
+		    request.
+
+		    Does the domain have a request pending, (see 3 above) ?
+			Yes, go to (5) with the frequency from the _pending_
+			request.
+			No, all done, exit.
+		No.
+		    Discard the current request.
+		    Does the domain have a request pending, (see 3 above) ?
+			Yes, go to (5) with the frequency from the _pending_
+			request.
+			No, all done, exit.
 
 (*) Note that this operation may be asynchronous and in that case will follow
 the deferred_response architecture.
@@ -136,15 +136,15 @@ the deferred_response architecture.
 5) Start next request(domain, frequency)
 
     Does the domain have a timer configured ?
-        Yes.
+	Yes.
 
-            Start the timer. When the callback triggers go to (2).
+	    Start the timer. When the callback triggers go to (2).
 
-            We use a timer with a short delay to start the next request rather
-            than starting the operation immediately. This is configurable for
-            each platform. This may be required for platforms which have a
-            slow bus, eg I2C, to allow the transaction which is currently in
-            progress to cpmplete before we initiate the next request.
+	    We use a timer with a short delay to start the next request rather
+	    than starting the operation immediately. This is configurable for
+	    each platform. This may be required for platforms which have a
+	    slow bus, eg I2C, to allow the transaction which is currently in
+	    progress to cpmplete before we initiate the next request.
 
-        No.
-            Go to (2). Start the next request immediately.
+	No.
+	    Go to (2). Start the next request immediately.
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/dvfs/include/mod_dvfs.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/dvfs/include/mod_dvfs.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/dvfs/include/mod_dvfs.h
@@ -116,8 +116,8 @@ struct mod_dvfs_domain_api {
      * \param [out] opp Sustained operating point.
      */
     int (*get_sustained_opp)(
-        fwk_id_t domain_id,
-        struct mod_dvfs_opp *opp);
+	fwk_id_t domain_id,
+	struct mod_dvfs_opp *opp);
 
     /*!
      * \brief Get an operating point from its index.
@@ -127,9 +127,9 @@ struct mod_dvfs_domain_api {
      * \param [out] opp Requested operating point.
      */
     int (*get_nth_opp)(
-        fwk_id_t domain_id,
-        size_t n,
-        struct mod_dvfs_opp *opp);
+	fwk_id_t domain_id,
+	size_t n,
+	struct mod_dvfs_opp *opp);
 
     /*!
      * \brief Get the number of operating points of a domain.
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/dvfs/src/mod_dvfs.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/dvfs/src/mod_dvfs.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/dvfs/src/mod_dvfs.c
@@ -98,14 +98,14 @@ struct mod_dvfs_domain_ctx {
     const struct mod_dvfs_domain_config *config;
 
     struct {
-        /* Power supply API */
-        const struct mod_psu_device_api *psu;
+	/* Power supply API */
+	const struct mod_psu_device_api *psu;
 
-        /* Clock API */
-        const struct mod_clock_api *clock;
+	/* Clock API */
+	const struct mod_clock_api *clock;
 
-        /* Alarm API for pending requests */
-        const struct mod_timer_alarm_api *alarm_api;
+	/* Alarm API for pending requests */
+	const struct mod_timer_alarm_api *alarm_api;
     } apis;
 
     /* Number of operating points */
@@ -152,9 +152,9 @@ static struct mod_dvfs_domain_ctx *get_d
     uint32_t idx = fwk_id_get_element_idx(domain_id);
 
     if (idx < dvfs_ctx.dvfs_domain_element_count) {
-        return &(*dvfs_ctx.domain_ctx)[idx];
+	return &(*dvfs_ctx.domain_ctx)[idx];
     } else {
-        return NULL;
+	return NULL;
     }
 }
 
@@ -177,13 +177,13 @@ static const struct mod_dvfs_opp *get_op
     size_t opp_idx;
 
     for (opp_idx = 0; opp_idx < ctx->opp_count; opp_idx++) {
-        opp = &ctx->config->opps[opp_idx];
+	opp = &ctx->config->opps[opp_idx];
 
-        if (opp->level != level) {
-            continue;
-        }
+	if (opp->level != level) {
+	    continue;
+	}
 
-        return opp;
+	return opp;
     }
 
     return NULL;
@@ -197,13 +197,13 @@ static const struct mod_dvfs_opp *get_op
     const struct mod_dvfs_opp *opp;
 
     for (opp_idx = 0; opp_idx < ctx->opp_count; opp_idx++) {
-        opp = &ctx->config->opps[opp_idx];
+	opp = &ctx->config->opps[opp_idx];
 
-        if (opp->voltage != voltage) {
-            continue;
-        }
+	if (opp->voltage != voltage) {
+	    continue;
+	}
 
-        return opp;
+	return opp;
     }
 
     return NULL;
@@ -220,13 +220,13 @@ static int put_event_request(
     struct fwk_event_light req;
 
     req = (struct fwk_event_light){
-        .target_id = ctx->domain_id,
-        .id = event_id,
-        .response_requested = ctx->request.response_required,
+	.target_id = ctx->domain_id,
+	.id = event_id,
+	.response_requested = ctx->request.response_required,
     };
 
     if (ctx->request.set_source_id) {
-        req.source_id = ctx->domain_id;
+	req.source_id = ctx->domain_id;
     }
 
     ctx->state = state;
@@ -251,14 +251,14 @@ static int dvfs_set_level_start(
     int status;
 
     if ((new_opp->frequency == ctx->request.new_opp.frequency) &&
-        (new_opp->voltage == ctx->request.new_opp.voltage)) {
-        /*
-         * There are no new requests to be completed and there are no pending
-         * requests. Clean up the domain ctx here.
-         */
-        dvfs_cleanup_request(ctx);
+	(new_opp->voltage == ctx->request.new_opp.voltage)) {
+	/*
+	 * There are no new requests to be completed and there are no pending
+	 * requests. Clean up the domain ctx here.
+	 */
+	dvfs_cleanup_request(ctx);
 
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     ctx->request.cookie = cookie, ctx->request.new_opp = *new_opp;
@@ -281,16 +281,16 @@ static void dvfs_flush_pending_request(s
     int status;
 
     if (ctx->request_pending) {
-        ctx->request_pending = false;
-        status = dvfs_set_level_start(
-            ctx,
-            ctx->pending_request.cookie,
-            &ctx->pending_request.new_opp,
-            ctx->pending_request.retry_request,
-            ctx->pending_request.num_retries);
-        if (status != FWK_SUCCESS) {
-            FWK_LOG_DEBUG("[DVFS] %s @%d", __func__, __LINE__);
-        }
+	ctx->request_pending = false;
+	status = dvfs_set_level_start(
+	    ctx,
+	    ctx->pending_request.cookie,
+	    &ctx->pending_request.new_opp,
+	    ctx->pending_request.retry_request,
+	    ctx->pending_request.num_retries);
+	if (status != FWK_SUCCESS) {
+	    FWK_LOG_DEBUG("[DVFS] %s @%d", __func__, __LINE__);
+	}
     }
     ctx->pending_request = (struct mod_dvfs_request){ 0 };
 }
@@ -302,15 +302,15 @@ static void alarm_callback(uintptr_t par
     int status;
 
     req = (struct fwk_event_light){
-        .target_id = ctx->domain_id,
-        .source_id = ctx->domain_id,
-        .id = mod_dvfs_event_id_retry,
-        .response_requested = ctx->pending_request.response_required,
+	.target_id = ctx->domain_id,
+	.source_id = ctx->domain_id,
+	.id = mod_dvfs_event_id_retry,
+	.response_requested = ctx->pending_request.response_required,
     };
 
     status = fwk_put_event(&req);
     if (status != FWK_SUCCESS) {
-        FWK_LOG_DEBUG("[DVFS] %s @%d", __func__, __LINE__);
+	FWK_LOG_DEBUG("[DVFS] %s @%d", __func__, __LINE__);
     }
 }
 
@@ -319,25 +319,25 @@ static int dvfs_handle_pending_request(s
     int status = FWK_SUCCESS;
 
     if (ctx->state == DVFS_DOMAIN_STATE_RETRY) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     if (ctx->config->retry_ms > 0) {
-        status = ctx->apis.alarm_api->start(
-            ctx->config->alarm_id,
-            ctx->config->retry_ms,
-            MOD_TIMER_ALARM_TYPE_ONCE,
-            alarm_callback,
-            (uintptr_t)ctx);
-        if (status == FWK_SUCCESS) {
-            ctx->state = DVFS_DOMAIN_STATE_RETRY;
-        }
+	status = ctx->apis.alarm_api->start(
+	    ctx->config->alarm_id,
+	    ctx->config->retry_ms,
+	    MOD_TIMER_ALARM_TYPE_ONCE,
+	    alarm_callback,
+	    (uintptr_t)ctx);
+	if (status == FWK_SUCCESS) {
+	    ctx->state = DVFS_DOMAIN_STATE_RETRY;
+	}
     } else {
-        /*
-         * If this domain does not have a timeout configured we start
-         * processing the request immediately.
-         */
-        dvfs_flush_pending_request(ctx);
+	/*
+	 * If this domain does not have a timeout configured we start
+	 * processing the request immediately.
+	 */
+	dvfs_flush_pending_request(ctx);
     }
     return status;
 }
@@ -349,20 +349,20 @@ static void dvfs_create_pending_level_re
     bool retry_request)
 {
     if (ctx->request_pending) {
-        if ((new_opp->frequency == ctx->pending_request.new_opp.frequency) &&
-            (new_opp->voltage == ctx->pending_request.new_opp.voltage)) {
-            return;
-        }
+	if ((new_opp->frequency == ctx->pending_request.new_opp.frequency) &&
+	    (new_opp->voltage == ctx->pending_request.new_opp.voltage)) {
+	    return;
+	}
     } else {
-        if ((new_opp->frequency == ctx->current_opp.frequency) &&
-            (new_opp->voltage == ctx->current_opp.voltage)) {
-            return;
-        }
-
-        ctx->pending_request.num_retries = 0;
-        ctx->pending_request.set_source_id = false;
-        ctx->pending_request.response_required = false;
-        ctx->request_pending = true;
+	if ((new_opp->frequency == ctx->current_opp.frequency) &&
+	    (new_opp->voltage == ctx->current_opp.voltage)) {
+	    return;
+	}
+
+	ctx->pending_request.num_retries = 0;
+	ctx->pending_request.set_source_id = false;
+	ctx->pending_request.response_required = false;
+	ctx->request_pending = true;
     }
 
     ctx->pending_request.new_opp = *new_opp;
@@ -372,7 +372,7 @@ static void dvfs_create_pending_level_re
      * overwrite it.
      */
     if (retry_request) {
-        ctx->pending_request.retry_request = retry_request;
+	ctx->pending_request.retry_request = retry_request;
     }
 
     ctx->pending_request.cookie = cookie;
@@ -387,11 +387,11 @@ static int dvfs_get_sustained_opp(fwk_id
 
     ctx = get_domain_ctx(domain_id);
     if (ctx == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     if (ctx->config->sustained_idx >= ctx->opp_count) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     *opp = ctx->config->opps[ctx->config->sustained_idx];
@@ -407,16 +407,16 @@ static int dvfs_get_nth_opp(
     const struct mod_dvfs_domain_ctx *ctx;
 
     if (opp == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     ctx = get_domain_ctx(domain_id);
     if (ctx == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     if (n >= ctx->opp_count) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     *opp = ctx->config->opps[n];
@@ -434,7 +434,7 @@ static int dvfs_get_level_id(
 
     ctx = get_domain_ctx(domain_id);
     if (ctx == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     /*
@@ -443,11 +443,11 @@ static int dvfs_get_level_id(
      * to speed-up.
      */
     for (idx = 0; idx < ctx->opp_count; idx++) {
-        const struct mod_dvfs_opp *opp = &ctx->config->opps[idx];
-        if (opp->level == level) {
-            *level_id = idx;
-            return FWK_SUCCESS;
-        }
+	const struct mod_dvfs_opp *opp = &ctx->config->opps[idx];
+	if (opp->level == level) {
+	    *level_id = idx;
+	    return FWK_SUCCESS;
+	}
     }
 
     return FWK_E_PARAM;
@@ -458,12 +458,12 @@ static int dvfs_get_opp_count(fwk_id_t d
     const struct mod_dvfs_domain_ctx *ctx;
 
     if (opp_count == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     ctx = get_domain_ctx(domain_id);
     if (ctx == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     *opp_count = ctx->opp_count;
@@ -476,12 +476,12 @@ static int dvfs_get_latency(fwk_id_t dom
     const struct mod_dvfs_domain_ctx *ctx;
 
     if (latency == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     ctx = get_domain_ctx(domain_id);
     if (ctx == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     *latency = ctx->config->latency;
@@ -498,35 +498,35 @@ static int dvfs_get_current_opp(fwk_id_t
     struct mod_dvfs_domain_ctx *ctx;
 
     if (opp == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     ctx = get_domain_ctx(domain_id);
     if (ctx == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     if (ctx->current_opp.level != 0) {
-        opp->level = ctx->current_opp.level;
-        opp->frequency = ctx->current_opp.frequency;
-        opp->voltage = ctx->current_opp.voltage;
-        return FWK_SUCCESS;
+	opp->level = ctx->current_opp.level;
+	opp->frequency = ctx->current_opp.frequency;
+	opp->voltage = ctx->current_opp.voltage;
+	return FWK_SUCCESS;
     }
 
     if (ctx->state != DVFS_DOMAIN_STATE_IDLE) {
-        return FWK_E_BUSY;
+	return FWK_E_BUSY;
     }
 
     ctx->request.response_required = true;
     status =
-        put_event_request(ctx, mod_dvfs_event_id_get_opp, DVFS_DOMAIN_GET_OPP);
+	put_event_request(ctx, mod_dvfs_event_id_get_opp, DVFS_DOMAIN_GET_OPP);
     if (status == FWK_SUCCESS) {
-        /*
-         * We return FWK_PENDING here to indicate to the caller that the
-         * result of the request is pending and will arrive later through
-         * an event.
-         */
-        return FWK_PENDING;
+	/*
+	 * We return FWK_PENDING here to indicate to the caller that the
+	 * result of the request is pending and will arrive later through
+	 * an event.
+	 */
+	return FWK_PENDING;
     }
 
     return status;
@@ -542,29 +542,29 @@ static int dvfs_set_level(fwk_id_t domai
 
     ctx = get_domain_ctx(domain_id);
     if (ctx == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     /* Only accept levels that exist in the operating point table */
     new_opp = get_opp_for_level(ctx, level);
     if (new_opp == NULL) {
-        return FWK_E_RANGE;
+	return FWK_E_RANGE;
     }
 
     if (ctx->state != DVFS_DOMAIN_STATE_IDLE) {
-        dvfs_create_pending_level_request(ctx, cookie, new_opp, false);
+	dvfs_create_pending_level_request(ctx, cookie, new_opp, false);
 
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     if (level == ctx->current_opp.level) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     if (fwk_is_interrupt_context()) {
-        ctx->request.set_source_id = true;
+	ctx->request.set_source_id = true;
     } else {
-        ctx->request.set_source_id = false;
+	ctx->request.set_source_id = false;
     }
     return dvfs_set_level_start(ctx, cookie, new_opp, false, 0);
 }
@@ -597,40 +597,40 @@ static void dvfs_complete_respond(
     bool return_opp = false;
 
     if ((ctx->state == DVFS_DOMAIN_GET_OPP) && (req_status == FWK_SUCCESS)) {
-        return_opp = true;
+	return_opp = true;
     }
 
     if (ctx->cookie != 0) {
-        /*
-         * The request was handled asynchronously, retrieve
-         * the delayed_response and return it to the caller
-         * with the data.
-         */
-        resp_params = (struct mod_dvfs_params_response *)&read_req_event.params;
-        status = fwk_get_delayed_response(
-            ctx->domain_id, ctx->cookie, &read_req_event);
-
-        if (status == FWK_SUCCESS) {
-            resp_params->status = req_status;
-            if (return_opp) {
-                resp_params->performance_level = ctx->current_opp.level;
-            }
-            status = fwk_put_event(&read_req_event);
-            if (status != FWK_SUCCESS) {
-                FWK_LOG_DEBUG("[DVFS] %s @%d", __func__, __LINE__);
-            }
-        }
-        ctx->cookie = 0;
+	/*
+	 * The request was handled asynchronously, retrieve
+	 * the delayed_response and return it to the caller
+	 * with the data.
+	 */
+	resp_params = (struct mod_dvfs_params_response *)&read_req_event.params;
+	status = fwk_get_delayed_response(
+	    ctx->domain_id, ctx->cookie, &read_req_event);
+
+	if (status == FWK_SUCCESS) {
+	    resp_params->status = req_status;
+	    if (return_opp) {
+		resp_params->performance_level = ctx->current_opp.level;
+	    }
+	    status = fwk_put_event(&read_req_event);
+	    if (status != FWK_SUCCESS) {
+		FWK_LOG_DEBUG("[DVFS] %s @%d", __func__, __LINE__);
+	    }
+	}
+	ctx->cookie = 0;
     } else if (resp_event != NULL) {
-        /*
-         * The request is being handled synchronously, return
-         * the data to the caller in the resp_event.
-         */
-        resp_params = (struct mod_dvfs_params_response *)resp_event->params;
-        resp_params->status = req_status;
-        if (return_opp) {
-            resp_params->performance_level = ctx->current_opp.level;
-        }
+	/*
+	 * The request is being handled synchronously, return
+	 * the data to the caller in the resp_event.
+	 */
+	resp_params = (struct mod_dvfs_params_response *)resp_event->params;
+	resp_params->status = req_status;
+	if (return_opp) {
+	    resp_params->performance_level = ctx->current_opp.level;
+	}
     }
 }
 
@@ -642,31 +642,31 @@ static int dvfs_complete(
     int status;
 
     if (ctx->request.response_required) {
-        /*
-         * If the DVFS request requires a response we send it now, no retries
-         * are attempted.
-         */
-        dvfs_complete_respond(ctx, resp_event, req_status);
+	/*
+	 * If the DVFS request requires a response we send it now, no retries
+	 * are attempted.
+	 */
+	dvfs_complete_respond(ctx, resp_event, req_status);
 
     } else if ((req_status != FWK_SUCCESS) && ctx->request.retry_request) {
-        /*
-         * No response required, request has failed, a retry is necessary.
-         */
-        if (ctx->request.num_retries++ < DVFS_MAX_RETRIES) {
-            ctx->pending_request.retry_request = ctx->request.retry_request;
-            ctx->pending_request.num_retries = ctx->request.num_retries;
-            if (!ctx->request_pending) {
-                ctx->pending_request.cookie = ctx->request.cookie;
-                ctx->pending_request.new_opp = ctx->request.new_opp;
-                ctx->request_pending = true;
-            }
-        }
+	/*
+	 * No response required, request has failed, a retry is necessary.
+	 */
+	if (ctx->request.num_retries++ < DVFS_MAX_RETRIES) {
+	    ctx->pending_request.retry_request = ctx->request.retry_request;
+	    ctx->pending_request.num_retries = ctx->request.num_retries;
+	    if (!ctx->request_pending) {
+		ctx->pending_request.cookie = ctx->request.cookie;
+		ctx->pending_request.new_opp = ctx->request.new_opp;
+		ctx->request_pending = true;
+	    }
+	}
     }
 
     /* notify the HAL that the level has been updated */
     if ((req_status == FWK_SUCCESS) && (ctx->state != DVFS_DOMAIN_GET_OPP)) {
-        dvfs_ctx.scmi_perf_updated_api->notify_level_updated(
-            ctx->domain_id, ctx->request.cookie, ctx->current_opp.level);
+	dvfs_ctx.scmi_perf_updated_api->notify_level_updated(
+	    ctx->domain_id, ctx->request.cookie, ctx->current_opp.level);
     }
 
     /*
@@ -675,16 +675,16 @@ static int dvfs_complete(
      * here to prevent another request being processed ahead of this one.
      */
     if (ctx->request_pending) {
-        status = dvfs_handle_pending_request(ctx);
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
+	status = dvfs_handle_pending_request(ctx);
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
     } else {
-        /*
-         * The request has completed and there are no pending requests.
-         * Clean up the domain ctx here.
-         */
-        dvfs_cleanup_request(ctx);
+	/*
+	 * The request has completed and there are no pending requests.
+	 * Clean up the domain ctx here.
+	 */
+	dvfs_cleanup_request(ctx);
     }
 
     return req_status;
@@ -701,83 +701,83 @@ static int dvfs_handle_set_opp(
     int status = FWK_SUCCESS;
 
     if (ctx->request.new_opp.voltage > voltage) {
-        /*
-         * Current < request, increase voltage then set frequency
-         */
-        status = ctx->apis.psu->set_voltage(
-            ctx->config->psu_id, ctx->request.new_opp.voltage);
-
-        if (status == FWK_PENDING) {
-            ctx->state = DVFS_DOMAIN_SET_FREQUENCY;
-            return status;
-        }
-
-        if (status != FWK_SUCCESS) {
-            return dvfs_complete(ctx, NULL, status);
-        }
-
-        /*
-         * Voltage set successsfully, continue to set the frequency
-         */
-        status = ctx->apis.clock->set_rate(
-            ctx->config->clock_id,
-            (uint64_t)ctx->request.new_opp.frequency * FWK_KHZ,
-            MOD_CLOCK_ROUND_MODE_NONE);
-
-        if (status == FWK_PENDING) {
-            ctx->state = DVFS_DOMAIN_SET_OPP_DONE;
-            return status;
-        }
+	/*
+	 * Current < request, increase voltage then set frequency
+	 */
+	status = ctx->apis.psu->set_voltage(
+	    ctx->config->psu_id, ctx->request.new_opp.voltage);
+
+	if (status == FWK_PENDING) {
+	    ctx->state = DVFS_DOMAIN_SET_FREQUENCY;
+	    return status;
+	}
+
+	if (status != FWK_SUCCESS) {
+	    return dvfs_complete(ctx, NULL, status);
+	}
+
+	/*
+	 * Voltage set successsfully, continue to set the frequency
+	 */
+	status = ctx->apis.clock->set_rate(
+	    ctx->config->clock_id,
+	    (uint64_t)ctx->request.new_opp.frequency * FWK_KHZ,
+	    MOD_CLOCK_ROUND_MODE_NONE);
+
+	if (status == FWK_PENDING) {
+	    ctx->state = DVFS_DOMAIN_SET_OPP_DONE;
+	    return status;
+	}
     } else if (ctx->request.new_opp.voltage < voltage) {
-        /*
-         * Current > request, decrease frequency then set voltage
-         */
-        status = ctx->apis.clock->set_rate(
-            ctx->config->clock_id,
-            (uint64_t)ctx->request.new_opp.frequency * FWK_KHZ,
-            MOD_CLOCK_ROUND_MODE_NONE);
-
-        if (status == FWK_PENDING) {
-            ctx->state = DVFS_DOMAIN_SET_VOLTAGE;
-            return status;
-        }
-
-        if (status != FWK_SUCCESS) {
-            return dvfs_complete(ctx, NULL, status);
-        }
-
-        /*
-         * Clock set_rate() completed successfully, continue to set_voltage()
-         */
-        status = ctx->apis.psu->set_voltage(
-            ctx->config->psu_id, ctx->request.new_opp.voltage);
-
-        if (status == FWK_PENDING) {
-            ctx->state = DVFS_DOMAIN_SET_OPP_DONE;
-            return status;
-        }
+	/*
+	 * Current > request, decrease frequency then set voltage
+	 */
+	status = ctx->apis.clock->set_rate(
+	    ctx->config->clock_id,
+	    (uint64_t)ctx->request.new_opp.frequency * FWK_KHZ,
+	    MOD_CLOCK_ROUND_MODE_NONE);
+
+	if (status == FWK_PENDING) {
+	    ctx->state = DVFS_DOMAIN_SET_VOLTAGE;
+	    return status;
+	}
+
+	if (status != FWK_SUCCESS) {
+	    return dvfs_complete(ctx, NULL, status);
+	}
+
+	/*
+	 * Clock set_rate() completed successfully, continue to set_voltage()
+	 */
+	status = ctx->apis.psu->set_voltage(
+	    ctx->config->psu_id, ctx->request.new_opp.voltage);
+
+	if (status == FWK_PENDING) {
+	    ctx->state = DVFS_DOMAIN_SET_OPP_DONE;
+	    return status;
+	}
     } else if (ctx->current_opp.frequency == 0) {
-        /*
-         * At startup the voltage may be set without the frequency having
-         * been set. In this case we must set the frequency regardless of
-         * the voltage.
-         */
-        status = ctx->apis.clock->set_rate(
-            ctx->config->clock_id,
-            (uint64_t)ctx->request.new_opp.frequency * FWK_KHZ,
-            MOD_CLOCK_ROUND_MODE_NONE);
-
-        if (status == FWK_PENDING) {
-            ctx->state = DVFS_DOMAIN_SET_OPP_DONE;
-            return status;
-        }
+	/*
+	 * At startup the voltage may be set without the frequency having
+	 * been set. In this case we must set the frequency regardless of
+	 * the voltage.
+	 */
+	status = ctx->apis.clock->set_rate(
+	    ctx->config->clock_id,
+	    (uint64_t)ctx->request.new_opp.frequency * FWK_KHZ,
+	    MOD_CLOCK_ROUND_MODE_NONE);
+
+	if (status == FWK_PENDING) {
+	    ctx->state = DVFS_DOMAIN_SET_OPP_DONE;
+	    return status;
+	}
     }
 
     /*
      * SET_OPP() completed, return to caller.
      */
     if (status == FWK_SUCCESS) {
-        ctx->current_opp = ctx->request.new_opp;
+	ctx->current_opp = ctx->request.new_opp;
     }
 
     return dvfs_complete(ctx, NULL, status);
@@ -798,11 +798,11 @@ static int dvfs_handle_psu_get_voltage_r
     const struct mod_dvfs_opp *opp;
 
     if (req_status != FWK_SUCCESS) {
-        return dvfs_complete(ctx, resp_event, req_status);
+	return dvfs_complete(ctx, resp_event, req_status);
     }
 
     if (ctx->state == DVFS_DOMAIN_SET_OPP) {
-        return dvfs_handle_set_opp(ctx, voltage);
+	return dvfs_handle_set_opp(ctx, voltage);
     }
 
     /*
@@ -811,7 +811,7 @@ static int dvfs_handle_psu_get_voltage_r
      */
     opp = get_opp_for_voltage(ctx, voltage);
     if (opp == NULL) {
-        return dvfs_complete(ctx, resp_event, FWK_E_DEVICE);
+	return dvfs_complete(ctx, resp_event, FWK_E_DEVICE);
     }
 
     /*
@@ -837,32 +837,32 @@ static int dvfs_handle_psu_set_voltage_r
 {
     int status = FWK_SUCCESS;
     struct mod_psu_driver_response *psu_response =
-        (struct mod_psu_driver_response *)event->params;
+	(struct mod_psu_driver_response *)event->params;
 
     if (psu_response->status != FWK_SUCCESS) {
-        return dvfs_complete(ctx, NULL, psu_response->status);
+	return dvfs_complete(ctx, NULL, psu_response->status);
     }
 
     if (ctx->state == DVFS_DOMAIN_SET_FREQUENCY) {
-        status = ctx->apis.clock->set_rate(
-            ctx->config->clock_id,
-            (uint64_t)ctx->request.new_opp.frequency * FWK_KHZ,
-            MOD_CLOCK_ROUND_MODE_NONE);
-        if (status == FWK_PENDING) {
-            ctx->state = DVFS_DOMAIN_SET_OPP_DONE;
-            return status;
-        }
+	status = ctx->apis.clock->set_rate(
+	    ctx->config->clock_id,
+	    (uint64_t)ctx->request.new_opp.frequency * FWK_KHZ,
+	    MOD_CLOCK_ROUND_MODE_NONE);
+	if (status == FWK_PENDING) {
+	    ctx->state = DVFS_DOMAIN_SET_OPP_DONE;
+	    return status;
+	}
     } else if (ctx->state == DVFS_DOMAIN_SET_OPP_DONE) {
-        status = FWK_SUCCESS;
+	status = FWK_SUCCESS;
     } else {
-        status = FWK_E_DEVICE;
+	status = FWK_E_DEVICE;
     }
 
     /*
      * SET_OPP() completed, return to caller.
      */
     if (status == FWK_SUCCESS) {
-        ctx->current_opp = ctx->request.new_opp;
+	ctx->current_opp = ctx->request.new_opp;
     }
 
     return dvfs_complete(ctx, NULL, status);
@@ -878,34 +878,34 @@ static int dvfs_handle_clk_set_freq_resp
 {
     int status;
     struct mod_clock_driver_resp_params *clock_response =
-        (struct mod_clock_driver_resp_params *)event->params;
+	(struct mod_clock_driver_resp_params *)event->params;
 
     if (clock_response->status != FWK_SUCCESS) {
-        return dvfs_complete(ctx, NULL, clock_response->status);
+	return dvfs_complete(ctx, NULL, clock_response->status);
     }
 
     if (ctx->state == DVFS_DOMAIN_SET_VOLTAGE) {
-        /*
-         * Clock set_rate() completed successfully, continue to set_voltage()
-         */
-        status = ctx->apis.psu->set_voltage(
-            ctx->config->psu_id, ctx->request.new_opp.voltage);
-        if (status == FWK_PENDING) {
-            ctx->state = DVFS_DOMAIN_SET_OPP_DONE;
-            return status;
-        }
+	/*
+	 * Clock set_rate() completed successfully, continue to set_voltage()
+	 */
+	status = ctx->apis.psu->set_voltage(
+	    ctx->config->psu_id, ctx->request.new_opp.voltage);
+	if (status == FWK_PENDING) {
+	    ctx->state = DVFS_DOMAIN_SET_OPP_DONE;
+	    return status;
+	}
 
     } else if (ctx->state == DVFS_DOMAIN_SET_OPP_DONE) {
-        status = FWK_SUCCESS;
+	status = FWK_SUCCESS;
     } else {
-        status = FWK_E_DEVICE;
+	status = FWK_E_DEVICE;
     }
 
     /*
      * SET_OPP() completed, return to caller.
      */
     if (status == FWK_SUCCESS) {
-        ctx->current_opp = ctx->request.new_opp;
+	ctx->current_opp = ctx->request.new_opp;
     }
 
     return dvfs_complete(ctx, NULL, status);
@@ -926,54 +926,54 @@ static int mod_dvfs_process_event(
 
     ctx = get_domain_ctx(event->target_id);
     if (ctx == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     /*
      * local DVFS event from dvfs_get_current_opp()
      */
     if (fwk_id_is_equal(event->id, mod_dvfs_event_id_get_opp)) {
-        status = ctx->apis.psu->get_voltage(
-            ctx->config->psu_id, &ctx->request.new_opp.voltage);
-        if (status == FWK_PENDING) {
-            ctx->cookie = event->cookie;
-            resp_event->is_delayed_response = true;
-            return FWK_SUCCESS;
-        }
-
-        /*
-         * Handle get_voltage() synchronously
-         */
-        status = dvfs_handle_psu_get_voltage_resp(
-            ctx, resp_event, status, ctx->request.new_opp.voltage);
-        if (status == FWK_PENDING) {
-            return FWK_SUCCESS;
-        }
-        return status;
+	status = ctx->apis.psu->get_voltage(
+	    ctx->config->psu_id, &ctx->request.new_opp.voltage);
+	if (status == FWK_PENDING) {
+	    ctx->cookie = event->cookie;
+	    resp_event->is_delayed_response = true;
+	    return FWK_SUCCESS;
+	}
+
+	/*
+	 * Handle get_voltage() synchronously
+	 */
+	status = dvfs_handle_psu_get_voltage_resp(
+	    ctx, resp_event, status, ctx->request.new_opp.voltage);
+	if (status == FWK_PENDING) {
+	    return FWK_SUCCESS;
+	}
+	return status;
     }
 
     /*
      * local DVFS event from dvfs_set_level()
      */
     if (fwk_id_is_equal(event->id, mod_dvfs_event_id_set)) {
-        if (ctx->current_opp.voltage != 0) {
-            voltage = ctx->current_opp.voltage;
-            status = FWK_SUCCESS;
-        } else {
-            status = ctx->apis.psu->get_voltage(ctx->config->psu_id, &voltage);
-            if (status == FWK_PENDING) {
-                return FWK_SUCCESS;
-            }
-        }
-
-        /*
-         * Handle get_voltage() synchronously
-         */
-        status = dvfs_handle_psu_get_voltage_resp(ctx, NULL, status, voltage);
-        if (status == FWK_PENDING) {
-            return FWK_SUCCESS;
-        }
-        return status;
+	if (ctx->current_opp.voltage != 0) {
+	    voltage = ctx->current_opp.voltage;
+	    status = FWK_SUCCESS;
+	} else {
+	    status = ctx->apis.psu->get_voltage(ctx->config->psu_id, &voltage);
+	    if (status == FWK_PENDING) {
+		return FWK_SUCCESS;
+	    }
+	}
+
+	/*
+	 * Handle get_voltage() synchronously
+	 */
+	status = dvfs_handle_psu_get_voltage_resp(ctx, NULL, status, voltage);
+	if (status == FWK_PENDING) {
+	    return FWK_SUCCESS;
+	}
+	return status;
     }
 
     /*
@@ -981,63 +981,63 @@ static int mod_dvfs_process_event(
      * dvfs_handle_pending_request() fires
      */
     if (fwk_id_is_equal(event->id, mod_dvfs_event_id_retry)) {
-        ctx->request.set_source_id = false;
-        ctx->request_pending = false;
-        status = dvfs_set_level_start(
-            ctx,
-            ctx->pending_request.cookie,
-            &ctx->pending_request.new_opp,
-            ctx->pending_request.retry_request,
-            ctx->pending_request.num_retries);
-        ctx->pending_request = (struct mod_dvfs_request){ 0 };
-        return status;
+	ctx->request.set_source_id = false;
+	ctx->request_pending = false;
+	status = dvfs_set_level_start(
+	    ctx,
+	    ctx->pending_request.cookie,
+	    &ctx->pending_request.new_opp,
+	    ctx->pending_request.retry_request,
+	    ctx->pending_request.num_retries);
+	ctx->pending_request = (struct mod_dvfs_request){ 0 };
+	return status;
     }
 
     /*
      * response event from PSU get_voltage()
      */
     if (fwk_id_is_equal(event->id, mod_psu_event_id_get_voltage)) {
-        /*
-         * Handle get_voltage() asynchronously, cookie will have been saved
-         * above so we can safely discard the resp_event.
-         */
-        psu_response = (struct mod_psu_driver_response *)event->params;
-        status = dvfs_handle_psu_get_voltage_resp(
-            ctx, NULL, psu_response->status, psu_response->voltage);
-        if (status == FWK_PENDING) {
-            return FWK_SUCCESS;
-        }
-        return status;
+	/*
+	 * Handle get_voltage() asynchronously, cookie will have been saved
+	 * above so we can safely discard the resp_event.
+	 */
+	psu_response = (struct mod_psu_driver_response *)event->params;
+	status = dvfs_handle_psu_get_voltage_resp(
+	    ctx, NULL, psu_response->status, psu_response->voltage);
+	if (status == FWK_PENDING) {
+	    return FWK_SUCCESS;
+	}
+	return status;
     }
 
     /*
      * response event from SET_OPP() PSU set_voltage()
      */
     if (fwk_id_is_equal(event->id, mod_psu_event_id_set_voltage)) {
-        /*
-         * Handle set_voltage() asynchronously, no response required for
-         * a SET_OPP() request so resp_event discarded.
-         */
-        status = dvfs_handle_psu_set_voltage_resp(ctx, event);
-        if (status == FWK_PENDING) {
-            return FWK_SUCCESS;
-        }
-        return status;
+	/*
+	 * Handle set_voltage() asynchronously, no response required for
+	 * a SET_OPP() request so resp_event discarded.
+	 */
+	status = dvfs_handle_psu_set_voltage_resp(ctx, event);
+	if (status == FWK_PENDING) {
+	    return FWK_SUCCESS;
+	}
+	return status;
     }
 
     /*
      * response event from SET_OPP() Clock set_rate()
      */
     if (fwk_id_is_equal(event->id, mod_clock_event_id_set_rate_request)) {
-        /*
-         * Handle set_frequency() asynchronously, no response required for
-         * a SET_OPP() request so resp_event discarded.
-         */
-        status = dvfs_handle_clk_set_freq_resp(ctx, event);
-        if (status == FWK_PENDING) {
-            return FWK_SUCCESS;
-        }
-        return status;
+	/*
+	 * Handle set_frequency() asynchronously, no response required for
+	 * a SET_OPP() request so resp_event discarded.
+	 */
+	status = dvfs_handle_clk_set_freq_resp(ctx, event);
+	if (status == FWK_PENDING) {
+	    return FWK_SUCCESS;
+	}
+	return status;
     }
 
     return FWK_E_PARAM;
@@ -1054,14 +1054,14 @@ static int dvfs_start(fwk_id_t id)
     struct mod_dvfs_domain_ctx *ctx;
 
     if (!fwk_id_is_type(id, FWK_ID_TYPE_ELEMENT)) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     status = dvfs_get_sustained_opp(id, &sustained_opp);
     if (status == FWK_SUCCESS) {
-        ctx = get_domain_ctx(id);
-        ctx->request.set_source_id = true;
-        status = dvfs_set_level_start(ctx, 0, &sustained_opp, true, 0);
+	ctx = get_domain_ctx(id);
+	ctx->request.set_source_id = true;
+	status = dvfs_set_level_start(ctx, 0, &sustained_opp, true, 0);
     }
 
     return status;
@@ -1073,7 +1073,7 @@ static int dvfs_init(
     const void *data)
 {
     dvfs_ctx.domain_ctx =
-        fwk_mm_calloc(element_count, sizeof((*dvfs_ctx.domain_ctx)[0]));
+	fwk_mm_calloc(element_count, sizeof((*dvfs_ctx.domain_ctx)[0]));
 
     dvfs_ctx.config = (struct mod_dvfs_config *)data;
     dvfs_ctx.dvfs_domain_element_count = element_count;
@@ -1110,32 +1110,32 @@ static int dvfs_bind_element(fwk_id_t do
 
     /* Bind to the power supply module */
     status = fwk_module_bind(
-        ctx->config->psu_id, mod_psu_api_id_device, &ctx->apis.psu);
+	ctx->config->psu_id, mod_psu_api_id_device, &ctx->apis.psu);
     if (status != FWK_SUCCESS) {
-        return FWK_E_PANIC;
+	return FWK_E_PANIC;
     }
 
     /* Bind to the clock module */
     status = fwk_module_bind(
-        ctx->config->clock_id,
-        FWK_ID_API(FWK_MODULE_IDX_CLOCK, 0),
-        &ctx->apis.clock);
+	ctx->config->clock_id,
+	FWK_ID_API(FWK_MODULE_IDX_CLOCK, 0),
+	&ctx->apis.clock);
     if (status != FWK_SUCCESS) {
-        return FWK_E_PANIC;
+	return FWK_E_PANIC;
     }
 
     /* Bind to the alarm HAL if required */
     if (ctx->config->retry_ms > 0) {
 #ifdef BUILD_HAS_MOD_TIMER
-        status = fwk_module_bind(
-            ctx->config->alarm_id,
-            MOD_TIMER_API_ID_ALARM,
-            &ctx->apis.alarm_api);
-        if (status != FWK_SUCCESS) {
-            return FWK_E_PANIC;
-        }
+	status = fwk_module_bind(
+	    ctx->config->alarm_id,
+	    MOD_TIMER_API_ID_ALARM,
+	    &ctx->apis.alarm_api);
+	if (status != FWK_SUCCESS) {
+	    return FWK_E_PANIC;
+	}
 #else
-        return FWK_E_PANIC;
+	return FWK_E_PANIC;
 #endif
     }
 
@@ -1148,21 +1148,21 @@ static int dvfs_bind(fwk_id_t id, unsign
 
     /* Only handle the first round */
     if (round > 0) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     /* Bind our elements */
     if (fwk_id_is_type(id, FWK_ID_TYPE_ELEMENT)) {
-        return dvfs_bind_element(id, round);
+	return dvfs_bind_element(id, round);
     }
 
     /* Bind to SCMI Perf to provide updates */
     status = fwk_module_bind(
-        fwk_module_id_scmi_perf,
-        FWK_ID_API(FWK_MODULE_IDX_SCMI_PERF, MOD_SCMI_PERF_DVFS_UPDATE_API),
-        &dvfs_ctx.scmi_perf_updated_api);
+	fwk_module_id_scmi_perf,
+	FWK_ID_API(FWK_MODULE_IDX_SCMI_PERF, MOD_SCMI_PERF_DVFS_UPDATE_API),
+	&dvfs_ctx.scmi_perf_updated_api);
     if (status != FWK_SUCCESS) {
-        return FWK_E_PANIC;
+	return FWK_E_PANIC;
     }
 
     return FWK_SUCCESS;
@@ -1176,11 +1176,11 @@ static int dvfs_process_bind_request(
 {
     /* Only allow binding to the module */
     if (!fwk_id_is_equal(target_id, fwk_module_id_dvfs)) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     if (!fwk_id_is_equal(api_id, mod_dvfs_api_id_dvfs)) {
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 
     /* We don't do any permissions management */
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/dw_apb_i2c/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/dw_apb_i2c/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/dw_apb_i2c/CMakeLists.txt
@@ -13,7 +13,7 @@ target_include_directories(
     PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_dw_apb_i2c.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_dw_apb_i2c.c")
 
 if("i2c" IN_LIST SCP_MODULES)
     target_link_libraries(${SCP_MODULE_TARGET} PRIVATE module-i2c)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/dw_apb_i2c/src/dw_apb_i2c.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/dw_apb_i2c/src/dw_apb_i2c.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/dw_apb_i2c/src/dw_apb_i2c.h
@@ -23,23 +23,23 @@
  * I2C controller register definitions
  */
 struct dw_apb_i2c_reg {
-           uint8_t        RESERVED0[0x04 - 0x00];
+	   uint8_t        RESERVED0[0x04 - 0x00];
     FWK_RW uint32_t       IC_TAR;
-           uint8_t        RESERVED1[0x10 - 0x08];
+	   uint8_t        RESERVED1[0x10 - 0x08];
     FWK_RW uint32_t       IC_DATA_CMD;
-           uint8_t        RESERVED2[0x2C - 0x14];
+	   uint8_t        RESERVED2[0x2C - 0x14];
     FWK_R  uint32_t       IC_INTR_STAT;
     FWK_RW uint32_t       IC_INTR_MASK;
-           uint8_t        RESERVED3[0x54 - 0x34];
+	   uint8_t        RESERVED3[0x54 - 0x34];
     FWK_R  uint32_t       IC_CLR_TX_ABRT;
-           uint8_t        RESERVED4[0x60 - 0x58];
+	   uint8_t        RESERVED4[0x60 - 0x58];
     FWK_R  uint32_t       IC_CLR_STOP_DET;
-           uint8_t        RESERVED5[0x6C - 0x64];
+	   uint8_t        RESERVED5[0x6C - 0x64];
     FWK_RW uint32_t       IC_ENABLE;
     FWK_R  uint32_t       IC_STATUS;
-           uint8_t        RESERVED6[0x9C - 0x74];
+	   uint8_t        RESERVED6[0x9C - 0x74];
     FWK_R  uint32_t       IC_ENABLE_STATUS;
-           uint8_t        RESERVED7[0x100 - 0xA0];
+	   uint8_t        RESERVED7[0x100 - 0xA0];
 };
 
 #define IC_TAR_ADDRESS                  UINT32_C(0x000003FF)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/dw_apb_i2c/src/mod_dw_apb_i2c.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/dw_apb_i2c/src/mod_dw_apb_i2c.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/dw_apb_i2c/src/mod_dw_apb_i2c.c
@@ -40,7 +40,7 @@ static bool is_i2c_disabled(void *param)
     struct dw_apb_i2c_reg *i2c_reg = (struct dw_apb_i2c_reg *)param;
 
     return ((i2c_reg->IC_ENABLE_STATUS & IC_ENABLE_STATUS_MASK) ==
-        IC_ENABLE_STATUS_DISABLED);
+	IC_ENABLE_STATUS_DISABLED);
 }
 
 static int disable_i2c(struct dw_apb_i2c_ctx *ctx)
@@ -56,12 +56,12 @@ static int disable_i2c(struct dw_apb_i2c
 
     /* Check whether the device is already disabled */
     if (is_i2c_disabled(i2c_reg)) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     /* The bus should be idle */
     if ((ctx->i2c_reg->IC_STATUS & IC_STATUS_MST_ACTIVITY_MASK) != 0) {
-        return FWK_E_DEVICE;
+	return FWK_E_DEVICE;
     }
 
     /* Disable the I2C device */
@@ -69,9 +69,9 @@ static int disable_i2c(struct dw_apb_i2c
 
     /* Wait until the device is disabled */
     status = timer_api->wait(timer_id, I2C_TIMEOUT_US, is_i2c_disabled,
-        i2c_reg);
+	i2c_reg);
     if (status != FWK_SUCCESS) {
-        return FWK_E_TIMEOUT;
+	return FWK_E_TIMEOUT;
     }
 
     return FWK_SUCCESS;
@@ -87,7 +87,7 @@ static int enable_i2c(struct dw_apb_i2c_
     /* Disable the I2C device to configure it */
     status = disable_i2c(ctx);
     if (status != FWK_SUCCESS) {
-        return FWK_E_DEVICE;
+	return FWK_E_DEVICE;
     }
 
     /* Program the target address */
@@ -117,21 +117,21 @@ static void i2c_isr(uintptr_t data)
 
     /* The transaction has completed successfully */
     if (i2c_reg->IC_INTR_STAT & IC_INTR_STOP_DET_MASK) {
-        i2c_reg->IC_CLR_STOP_DET;
-        i2c_status = FWK_SUCCESS;
-        if (ctx->read_on_going) {
-            ctx->read_on_going = false;
-            /* Read the data from the device buffer */
-            for (i = 0; i < ctx->byte_count; i++) {
-                ctx->data[i] =
-                    (uint8_t)(i2c_reg->IC_DATA_CMD & IC_DATA_CMD_DATA_MASK);
-            }
-        }
+	i2c_reg->IC_CLR_STOP_DET;
+	i2c_status = FWK_SUCCESS;
+	if (ctx->read_on_going) {
+	    ctx->read_on_going = false;
+	    /* Read the data from the device buffer */
+	    for (i = 0; i < ctx->byte_count; i++) {
+		ctx->data[i] =
+		    (uint8_t)(i2c_reg->IC_DATA_CMD & IC_DATA_CMD_DATA_MASK);
+	    }
+	}
     }
 
     /* The transaction has been aborted */
     if (i2c_reg->IC_INTR_STAT & IC_INTR_TX_ABRT_MASK) {
-        i2c_reg->IC_CLR_TX_ABRT;
+	i2c_reg->IC_CLR_TX_ABRT;
     }
 
     ctx->i2c_api->transaction_completed(ctx->i2c_id, i2c_status);
@@ -149,26 +149,26 @@ static int transmit_as_controller(
     struct dw_apb_i2c_ctx *ctx;
 
     if (transmit_request->transmit_byte_count > I2C_TRANSMIT_BUFFER_LENGTH) {
-        return FWK_E_SUPPORT;
+	return FWK_E_SUPPORT;
     }
 
     if (transmit_request->target_address == 0) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     ctx = ctx_table + fwk_id_get_element_idx(dev_id);
 
     status = enable_i2c(ctx, transmit_request->target_address);
     if (status != FWK_SUCCESS) {
-        return FWK_E_DEVICE;
+	return FWK_E_DEVICE;
     }
 
     /* The program of the I2C controller cannot be interrupted. */
     flags = fwk_interrupt_global_disable();
 
     for (sent_bytes = 0; sent_bytes < transmit_request->transmit_byte_count;
-         sent_bytes++) {
-        ctx->i2c_reg->IC_DATA_CMD = transmit_request->transmit_data[sent_bytes];
+	 sent_bytes++) {
+	ctx->i2c_reg->IC_DATA_CMD = transmit_request->transmit_data[sent_bytes];
     }
 
     fwk_interrupt_global_enable(flags);
@@ -191,11 +191,11 @@ static int receive_as_controller(
     struct dw_apb_i2c_ctx *ctx;
 
     if (receive_request->receive_byte_count > I2C_RECEIVE_BUFFER_LENGTH) {
-        return FWK_E_SUPPORT;
+	return FWK_E_SUPPORT;
     }
 
     if (receive_request->target_address == 0) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     ctx = ctx_table + fwk_id_get_element_idx(dev_id);
@@ -206,7 +206,7 @@ static int receive_as_controller(
 
     status = enable_i2c(ctx, receive_request->target_address);
     if (status != FWK_SUCCESS) {
-        return FWK_E_DEVICE;
+	return FWK_E_DEVICE;
     }
 
     /* The program of the I2C controller cannot be interrupted. */
@@ -214,7 +214,7 @@ static int receive_as_controller(
 
     /* Program the I2C controller with the expected reply length in bytes. */
     for (i = 0; i < receive_request->receive_byte_count; i++) {
-        ctx->i2c_reg->IC_DATA_CMD = IC_DATA_CMD_READ;
+	ctx->i2c_reg->IC_DATA_CMD = IC_DATA_CMD_READ;
     }
 
     fwk_interrupt_global_enable(flags);
@@ -237,8 +237,8 @@ static const struct mod_i2c_driver_api d
  * Framework handlers
  */
 static int dw_apb_i2c_init(fwk_id_t module_id,
-                               unsigned int element_count,
-                               const void *data)
+			       unsigned int element_count,
+			       const void *data)
 {
     ctx_table = fwk_mm_calloc(element_count, sizeof(*ctx_table));
 
@@ -246,19 +246,19 @@ static int dw_apb_i2c_init(fwk_id_t modu
 }
 
 static int dw_apb_i2c_element_init(fwk_id_t element_id,
-                                       unsigned int sub_element_count,
-                                       const void *data)
+				       unsigned int sub_element_count,
+				       const void *data)
 {
     struct mod_dw_apb_i2c_dev_config *config =
-        (struct mod_dw_apb_i2c_dev_config *)data;
+	(struct mod_dw_apb_i2c_dev_config *)data;
 
     if (config->reg == 0) {
-        return FWK_E_DATA;
+	return FWK_E_DATA;
     }
 
     ctx_table[fwk_id_get_element_idx(element_id)].config = config;
     ctx_table[fwk_id_get_element_idx(element_id)].i2c_reg =
-        (struct dw_apb_i2c_reg *)config->reg;
+	(struct dw_apb_i2c_reg *)config->reg;
 
     return FWK_SUCCESS;
 }
@@ -270,37 +270,37 @@ static int dw_apb_i2c_bind(fwk_id_t id,
     const struct mod_dw_apb_i2c_dev_config *config;
 
     if (!fwk_module_is_valid_element_id(id) || (round == 0)) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     ctx = ctx_table + fwk_id_get_element_idx(id);
     config = ctx->config;
 
     status = fwk_module_bind(config->timer_id, MOD_TIMER_API_ID_TIMER,
-        &ctx->timer_api);
+	&ctx->timer_api);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     return fwk_module_bind(ctx->i2c_id, mod_i2c_api_id_driver_response,
-        &ctx->i2c_api);
+	&ctx->i2c_api);
 }
 
 static int dw_apb_i2c_process_bind_request(fwk_id_t source_id,
-                                           fwk_id_t target_id,
-                                           fwk_id_t api_id,
-                                           const void **api)
+					   fwk_id_t target_id,
+					   fwk_id_t api_id,
+					   const void **api)
 {
     struct dw_apb_i2c_ctx *ctx;
 
     if (!fwk_module_is_valid_element_id(target_id)) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     ctx = ctx_table + fwk_id_get_element_idx(target_id);
 
     if (!fwk_id_is_equal(api_id, mod_dw_apb_i2c_api_id_driver)) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     ctx->i2c_id = source_id;
@@ -318,7 +318,7 @@ static int dw_apb_i2c_start(fwk_id_t id)
 
     /* Nothing to do for the module */
     if (!fwk_module_is_valid_element_id(id)) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     ctx = ctx_table + fwk_id_get_element_idx(id);
@@ -326,17 +326,17 @@ static int dw_apb_i2c_start(fwk_id_t id)
 
     status = fwk_interrupt_set_isr_param(i2c_irq, i2c_isr, (uintptr_t)ctx);
     if (status != FWK_SUCCESS) {
-        return FWK_E_DEVICE;
+	return FWK_E_DEVICE;
     }
 
     status = fwk_interrupt_clear_pending(i2c_irq);
     if (status != FWK_SUCCESS) {
-        return FWK_E_DEVICE;
+	return FWK_E_DEVICE;
     }
 
     status = fwk_interrupt_enable(i2c_irq);
     if (status != FWK_SUCCESS) {
-        return FWK_E_DEVICE;
+	return FWK_E_DEVICE;
     }
 
     return FWK_SUCCESS;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/dwt_pmi/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/dwt_pmi/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/dwt_pmi/CMakeLists.txt
@@ -8,9 +8,9 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_dwt_pmi.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_dwt_pmi.c")
 
 target_link_libraries(${SCP_MODULE_TARGET} PRIVATE module-pmi)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/dwt_pmi/src/mod_dwt_pmi.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/dwt_pmi/src/mod_dwt_pmi.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/dwt_pmi/src/mod_dwt_pmi.c
@@ -65,7 +65,7 @@ static bool check_cyccnt_supported(void)
 
     /* Bit NOCYCCNT is set if no cycle count is supported */
     if ((DWT_CTRL_NOCYCCNT & val) == 0) {
-        return true;
+	return true;
     }
 
     return false;
@@ -74,8 +74,8 @@ static bool check_cyccnt_supported(void)
 static void enable_dwt(void)
 {
     if (!ctx.dwt_enabled) {
-        *(ctx.config->debug_sys_demcr_addr) |= DEBUG_SYSTEM_DEMCR_TRCENA;
-        ctx.dwt_enabled = true;
+	*(ctx.config->debug_sys_demcr_addr) |= DEBUG_SYSTEM_DEMCR_TRCENA;
+	ctx.dwt_enabled = true;
     }
 }
 
@@ -86,12 +86,12 @@ static int enable_cycle_count(void)
     supported = check_cyccnt_supported();
     /* If DWT_CYCNT feature is not available then do not enable cycle count */
     if (!supported) {
-        return FWK_E_DEVICE;
+	return FWK_E_DEVICE;
     }
 
     if (!ctx.dwt_cyccnt_enable) {
-        *(ctx.config->dwt_ctrl_addr) |= DWT_CTRL_CYCCNTENA;
-        ctx.dwt_cyccnt_enable = true;
+	*(ctx.config->dwt_ctrl_addr) |= DWT_CTRL_CYCCNTENA;
+	ctx.dwt_cyccnt_enable = true;
     }
 
     return FWK_SUCCESS;
@@ -100,8 +100,8 @@ static int enable_cycle_count(void)
 static int disable_cycle_count(void)
 {
     if (ctx.dwt_cyccnt_enable) {
-        *(ctx.config->dwt_ctrl_addr) &= ~DWT_CTRL_CYCCNTENA;
-        ctx.dwt_cyccnt_enable = false;
+	*(ctx.config->dwt_ctrl_addr) &= ~DWT_CTRL_CYCCNTENA;
+	ctx.dwt_cyccnt_enable = false;
     }
 
     return FWK_SUCCESS;
@@ -110,7 +110,7 @@ static int disable_cycle_count(void)
 static int get_dwt_cycle_count(uint64_t *cycle_count)
 {
     if (!ctx.dwt_cyccnt_enable) {
-        return FWK_E_DEVICE;
+	return FWK_E_DEVICE;
     }
 
     *cycle_count = (uint64_t)(*(ctx.config->dwt_cyccnt));
@@ -121,7 +121,7 @@ static int get_dwt_cycle_count(uint64_t
 static int set_dwt_cycle_count(uint64_t cycle_count)
 {
     if (!ctx.dwt_cyccnt_enable) {
-        return FWK_E_DEVICE;
+	return FWK_E_DEVICE;
     }
 
     /* DWT cycle count register support only 32 bit count */
@@ -134,7 +134,7 @@ static uint64_t cycle_count_diff(uint64_
 {
     /* Handle wrap codition */
     if (end < start) {
-        return (uint64_t)cycle_diff(start, end);
+	return (uint64_t)cycle_diff(start, end);
     }
 
     return end - start;
@@ -162,8 +162,8 @@ static uint64_t get_current_time(void)
      */
 
     do {
-        counter_high = ctx.config->hw_timer->PCTH;
-        counter_low = ctx.config->hw_timer->PCTL;
+	counter_high = ctx.config->hw_timer->PCTH;
+	counter_low = ctx.config->hw_timer->PCTL;
     } while (counter_high != ctx.config->hw_timer->PCTH);
 
     return ((uint64_t)counter_high << 32U) | counter_low;
@@ -193,7 +193,7 @@ static int dwt_pmi_init(
 
     status = enable_cycle_count();
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     return FWK_SUCCESS;
@@ -207,7 +207,7 @@ static int process_bind_request(
 {
     /* Only allow binding to the module */
     if (!fwk_id_is_equal(target_id, fwk_module_id_dwt_pmi)) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     *api = &dwt_pmi_driver_api;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/fip/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/fip/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/fip/CMakeLists.txt
@@ -7,7 +7,7 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_include_directories(
     ${SCP_MODULE_TARGET} PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/include/internal")
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/fip/include/mod_fip.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/fip/include/mod_fip.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/fip/include/mod_fip.h
@@ -63,72 +63,72 @@ enum mod_fip_toc_entry_type {
 #define FIP_UUID_NULL \
     (struct fip_uuid_desc) \
     { \
-        .image_type = MOD_FIP_TOC_ENTRY_NULL, .uuid = { \
-            0x00, \
-            0x00, \
-            0x00, \
-            0x00, \
-            0x00, \
-            0x00, \
-            0x00, \
-            0x00, \
-            0x00, \
-            0x00, \
-            0x00, \
-            0x00, \
-            0x00, \
-            0x00, \
-            0x00, \
-            0x00 \
-        } \
+	.image_type = MOD_FIP_TOC_ENTRY_NULL, .uuid = { \
+	    0x00, \
+	    0x00, \
+	    0x00, \
+	    0x00, \
+	    0x00, \
+	    0x00, \
+	    0x00, \
+	    0x00, \
+	    0x00, \
+	    0x00, \
+	    0x00, \
+	    0x00, \
+	    0x00, \
+	    0x00, \
+	    0x00, \
+	    0x00 \
+	} \
     }
 
 /*! SCP Firmware BL2 */
 #define FIP_UUID_SCP_BL2 \
     (struct fip_uuid_desc) \
     { \
-        .image_type = MOD_FIP_TOC_ENTRY_SCP_BL2, .uuid = { \
-            0x97, \
-            0x66, \
-            0xfd, \
-            0x3d, \
-            0x89, \
-            0xbe, \
-            0xe8, \
-            0x49, \
-            0xae, \
-            0x5d, \
-            0x78, \
-            0xa1, \
-            0x40, \
-            0x60, \
-            0x82, \
-            0x13 \
-        } \
+	.image_type = MOD_FIP_TOC_ENTRY_SCP_BL2, .uuid = { \
+	    0x97, \
+	    0x66, \
+	    0xfd, \
+	    0x3d, \
+	    0x89, \
+	    0xbe, \
+	    0xe8, \
+	    0x49, \
+	    0xae, \
+	    0x5d, \
+	    0x78, \
+	    0xa1, \
+	    0x40, \
+	    0x60, \
+	    0x82, \
+	    0x13 \
+	} \
     }
 
 /*! TF-A runtime firmware BL31 */
 #define FIP_UUID_TFA_BL31 \
     (struct fip_uuid_desc) \
     { \
-        .image_type = MOD_FIP_TOC_ENTRY_TFA_BL31, .uuid = { \
-            0x47, \
-            0xd4, \
-            0x08, \
-            0x6d, \
-            0x4c, \
-            0xfe, \
-            0x98, \
-            0x46, \
-            0x9b, \
-            0x95, \
-            0x29, \
-            0x50, \
-            0xcb, \
-            0xbd, \
-            0x5a, \
-            0x00 \
-        } \
+	.image_type = MOD_FIP_TOC_ENTRY_TFA_BL31, .uuid = { \
+	    0x47, \
+	    0xd4, \
+	    0x08, \
+	    0x6d, \
+	    0x4c, \
+	    0xfe, \
+	    0x98, \
+	    0x46, \
+	    0x9b, \
+	    0x95, \
+	    0x29, \
+	    0x50, \
+	    0xcb, \
+	    0xbd, \
+	    0x5a, \
+	    0x00 \
+	} \
     }
 
 /*!
@@ -245,10 +245,10 @@ struct mod_fip_api {
      *         platform.
      */
     int (*get_entry)(
-        enum mod_fip_toc_entry_type image_type,
-        struct mod_fip_entry_data *entry,
-        uintptr_t base,
-        size_t limit);
+	enum mod_fip_toc_entry_type image_type,
+	struct mod_fip_entry_data *entry,
+	uintptr_t base,
+	size_t limit);
 };
 
 /*!
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/fip/src/mod_fip.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/fip/src/mod_fip.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/fip/src/mod_fip.c
@@ -37,29 +37,29 @@ static int fip_entry_type_to_uuid(
 
     module_config = fwk_module_get_data(fwk_module_id_fip);
     size_t desc_arr_size =
-        sizeof(fip_uuid_desc_arr) / sizeof(fip_uuid_desc_arr[0]);
+	sizeof(fip_uuid_desc_arr) / sizeof(fip_uuid_desc_arr[0]);
 
     if (type < MOD_FIP_TOC_ENTRY_COUNT) {
-        for (i = 0; i < desc_arr_size; i++) {
-            if (fip_uuid_desc_arr[i].image_type == type) {
-                fwk_str_memcpy(
-                    uuid, fip_uuid_desc_arr[i].uuid, FIP_UUID_ENTRY_SIZE);
-                return FWK_SUCCESS;
-            }
-        }
+	for (i = 0; i < desc_arr_size; i++) {
+	    if (fip_uuid_desc_arr[i].image_type == type) {
+		fwk_str_memcpy(
+		    uuid, fip_uuid_desc_arr[i].uuid, FIP_UUID_ENTRY_SIZE);
+		return FWK_SUCCESS;
+	    }
+	}
     }
 
     if (type >= MOD_FIP_TOC_ENTRY_COUNT &&
-        module_config->custom_fip_uuid_desc_arr != NULL) {
-        for (i = 0; i < module_config->custom_uuid_desc_count; i++) {
-            if (module_config->custom_fip_uuid_desc_arr[i].image_type == type) {
-                fwk_str_memcpy(
-                    uuid,
-                    module_config->custom_fip_uuid_desc_arr[i].uuid,
-                    FIP_UUID_ENTRY_SIZE);
-                return FWK_SUCCESS;
-            }
-        }
+	module_config->custom_fip_uuid_desc_arr != NULL) {
+	for (i = 0; i < module_config->custom_uuid_desc_count; i++) {
+	    if (module_config->custom_fip_uuid_desc_arr[i].image_type == type) {
+		fwk_str_memcpy(
+		    uuid,
+		    module_config->custom_fip_uuid_desc_arr[i].uuid,
+		    FIP_UUID_ENTRY_SIZE);
+		return FWK_SUCCESS;
+	    }
+	}
     }
 
     return FWK_E_PARAM;
@@ -70,11 +70,11 @@ static inline bool uuid_cmp(const uint8_
     int match = 0;
 
     for (unsigned int i = 0; i < FIP_UUID_ENTRY_SIZE; i++) {
-        if (a[i] == b[i]) {
-            match++;
-        } else {
-            match--;
-        }
+	if (a[i] == b[i]) {
+	    match++;
+	} else {
+	    match--;
+	}
     }
 
     return (match == FIP_UUID_ENTRY_SIZE);
@@ -107,15 +107,15 @@ static int fip_get_entry(
     struct fip_toc *toc = (void *)base;
 
     if (!validate_fip_toc(toc)) {
-        /*
-         * The error log message here requires the platform to enable an
-         * always-on logging mechanism in order to detect this failure in
-         * early stages, such as in ROM code.
-         */
-        FWK_LOG_ERR(
-            "[FIP] Invalid FIP ToC header name: [0x%08" PRIX32 "]",
-            toc->header.name);
-        return FWK_E_PARAM;
+	/*
+	 * The error log message here requires the platform to enable an
+	 * always-on logging mechanism in order to detect this failure in
+	 * early stages, such as in ROM code.
+	 */
+	FWK_LOG_ERR(
+	    "[FIP] Invalid FIP ToC header name: [0x%08" PRIX32 "]",
+	    toc->header.name);
+	return FWK_E_PARAM;
     }
 
     toc_entry = toc->entry;
@@ -123,26 +123,26 @@ static int fip_get_entry(
     /* Updates target_uuid field with UUID of corresponding image_type passed */
     status = fip_entry_type_to_uuid(image_type, target_uuid);
     if (status != FWK_SUCCESS)
-        return status;
+	return status;
 
     /*
      * Traverse all FIP ToC entries until the desired entry is found or ToC
      * End Marker is reached
      */
     while (!uuid_cmp(toc_entry->uuid, target_uuid)) {
-        if (uuid_is_null(toc_entry->uuid))
-            return FWK_E_RANGE;
-        toc_entry++;
+	if (uuid_is_null(toc_entry->uuid))
+	    return FWK_E_RANGE;
+	toc_entry++;
     }
 
     /* Sanity checks of the retrieved entry data */
     if (__builtin_add_overflow(
-            (uintptr_t)toc, (uintptr_t)toc_entry->offset_address, &address)) {
-        return FWK_E_DATA;
+	    (uintptr_t)toc, (uintptr_t)toc_entry->offset_address, &address)) {
+	return FWK_E_DATA;
     }
 
     if ((uintptr_t)toc_entry->offset_address + toc_entry->size > limit)
-        return FWK_E_SIZE;
+	return FWK_E_SIZE;
 
     entry_data->base = (void *)address;
     entry_data->size = toc_entry->size;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/gtimer/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/gtimer/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/gtimer/CMakeLists.txt
@@ -8,9 +8,9 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_gtimer.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_gtimer.c")
 
 target_link_libraries(${SCP_MODULE_TARGET} PRIVATE module-clock module-timer)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/gtimer/src/gtimer_reg.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/gtimer/src/gtimer_reg.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/gtimer/src/gtimer_reg.h
@@ -20,13 +20,13 @@ struct cntcontrol_reg {
     FWK_R   uint32_t SR;
     FWK_RW  uint32_t CVL;
     FWK_RW  uint32_t CVH;
-            uint32_t RESERVED0[4];
+	    uint32_t RESERVED0[4];
     FWK_RW  uint32_t FID0;
-            uint8_t  RESERVED1[0xC0 - 0x24];
+	    uint8_t  RESERVED1[0xC0 - 0x24];
     FWK_RW  uint32_t SCR; /* CSS: Synchronization Control Register */
     FWK_R   uint32_t SVL; /* CSS: Synchronized Counter Lower Value Register */
     FWK_R   uint32_t SVU; /* CSS: Synchronized Counter Upper Value Register */
-            uint8_t  RESERVED2[0xFD0 - 0xCC];
+	    uint8_t  RESERVED2[0xFD0 - 0xCC];
     FWK_R   uint32_t PID[11];
 };
 
@@ -45,9 +45,9 @@ struct cntctl_reg {
     FWK_RW  uint32_t FRQ;
     FWK_RW  uint32_t NSAR;
     FWK_R   uint32_t TTIDR;
-            uint8_t  RESERVED0[0x40 - 0x0C];
+	    uint8_t  RESERVED0[0x40 - 0x0C];
     FWK_RW  uint32_t ACR;
-            uint8_t  RESERVED1[0xFD0 - 0x44];
+	    uint8_t  RESERVED1[0xFD0 - 0x44];
     FWK_R   uint32_t PID[11];
 };
 
@@ -79,7 +79,7 @@ struct cntbase_reg {
     FWK_RW  uint32_t V_CVALH;
     FWK_RW  uint32_t V_TVAL;
     FWK_RW  uint32_t V_CTL;
-            uint8_t  RESERVED0[0xFD0 - 0x40];
+	    uint8_t  RESERVED0[0xFD0 - 0x40];
     FWK_R   uint32_t PID[11];
 };
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/gtimer/src/mod_gtimer.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/gtimer/src/mod_gtimer.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/gtimer/src/mod_gtimer.c
@@ -55,8 +55,8 @@ static uint64_t mod_gtimer_get_counter(c
      */
 
     do {
-        counter_high = hw_timer->PCTH;
-        counter_low = hw_timer->PCTL;
+	counter_high = hw_timer->PCTH;
+	counter_low = hw_timer->PCTL;
     } while (counter_high != hw_timer->PCTH);
 
     return ((uint64_t)counter_high << 32) | counter_low;
@@ -109,7 +109,7 @@ static int set_timer(fwk_id_t dev_id, ui
 
     status = get_counter(dev_id, &counter);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     /*
@@ -153,7 +153,7 @@ static int get_frequency(fwk_id_t dev_id
     struct gtimer_dev_ctx *ctx;
 
     if (frequency == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     ctx = mod_gtimer_ctx.table + fwk_id_get_element_idx(dev_id);
@@ -177,17 +177,17 @@ static const struct mod_timer_driver_api
  */
 
 static int gtimer_init(fwk_id_t module_id,
-                       unsigned int element_count,
-                       const void *data)
+		       unsigned int element_count,
+		       const void *data)
 {
     mod_gtimer_ctx.table =
-        fwk_mm_alloc(element_count, sizeof(struct gtimer_dev_ctx));
+	fwk_mm_alloc(element_count, sizeof(struct gtimer_dev_ctx));
 
     return FWK_SUCCESS;
 }
 
 static int gtimer_device_init(fwk_id_t element_id, unsigned int unused,
-                              const void *data)
+			      const void *data)
 {
     int status;
     struct gtimer_dev_ctx *ctx;
@@ -196,12 +196,12 @@ static int gtimer_device_init(fwk_id_t e
 
     ctx->config = data;
     if (ctx->config->hw_timer == 0   ||
-        ctx->config->hw_counter == 0 ||
-        ctx->config->control == 0    ||
-        ctx->config->frequency < GTIMER_FREQUENCY_MIN_HZ ||
-        ctx->config->frequency > GTIMER_FREQUENCY_MAX_HZ) {
+	ctx->config->hw_counter == 0 ||
+	ctx->config->control == 0    ||
+	ctx->config->frequency < GTIMER_FREQUENCY_MIN_HZ ||
+	ctx->config->frequency > GTIMER_FREQUENCY_MAX_HZ) {
 
-        return FWK_E_DEVICE;
+	return FWK_E_DEVICE;
     }
 
     ctx->hw_timer   = (struct cntbase_reg *)ctx->config->hw_timer;
@@ -212,23 +212,23 @@ static int gtimer_device_init(fwk_id_t e
     fwk_assert(status == FWK_SUCCESS);
 
     ctx->hw_counter->ACR = CNTCTL_ACR_RPCT |
-                           CNTCTL_ACR_RVCT |
-                           CNTCTL_ACR_RFRQ |
-                           CNTCTL_ACR_RVOFF|
-                           CNTCTL_ACR_RWPT;
+			   CNTCTL_ACR_RVCT |
+			   CNTCTL_ACR_RFRQ |
+			   CNTCTL_ACR_RVOFF|
+			   CNTCTL_ACR_RWPT;
     ctx->hw_counter->FRQ = ctx->config->frequency;
 
     return FWK_SUCCESS;
 }
 
 static int gtimer_process_bind_request(fwk_id_t requester_id,
-                                       fwk_id_t id,
-                                       fwk_id_t api_type,
-                                       const void **api)
+				       fwk_id_t id,
+				       fwk_id_t api_type,
+				       const void **api)
 {
     /* No binding to the module */
     if (fwk_module_is_valid_module_id(id)) {
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 
     *api = &module_api;
@@ -249,23 +249,23 @@ static int gtimer_start(fwk_id_t id)
     struct gtimer_dev_ctx *ctx;
 
     if (!fwk_id_is_type(id, FWK_ID_TYPE_ELEMENT)) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     ctx = mod_gtimer_ctx.table + fwk_id_get_element_idx(id);
 
     if (ctx->config->skip_cntcontrol_init) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     if (!fwk_id_is_type(ctx->config->clock_id, FWK_ID_TYPE_NONE)) {
-        /* Register for clock state notifications */
-        return fwk_notification_subscribe(
-            mod_clock_notification_id_state_changed,
-            ctx->config->clock_id,
-            id);
+	/* Register for clock state notifications */
+	return fwk_notification_subscribe(
+	    mod_clock_notification_id_state_changed,
+	    ctx->config->clock_id,
+	    id);
     } else {
-        gtimer_control_init(ctx);
+	gtimer_control_init(ctx);
     }
 
     return FWK_SUCCESS;
@@ -279,14 +279,14 @@ static int gtimer_process_notification(
     struct gtimer_dev_ctx *ctx;
 
     fwk_assert(
-        fwk_id_is_equal(event->id, mod_clock_notification_id_state_changed));
+	fwk_id_is_equal(event->id, mod_clock_notification_id_state_changed));
     fwk_assert(fwk_id_is_type(event->target_id, FWK_ID_TYPE_ELEMENT));
 
     params = (struct clock_notification_params *)event->params;
 
     if (params->new_state == MOD_CLOCK_STATE_RUNNING) {
-        ctx = mod_gtimer_ctx.table + fwk_id_get_element_idx(event->target_id);
-        gtimer_control_init(ctx);
+	ctx = mod_gtimer_ctx.table + fwk_id_get_element_idx(event->target_id);
+	gtimer_control_init(ctx);
     }
 
     return FWK_SUCCESS;
@@ -328,6 +328,6 @@ struct fwk_time_driver mod_gtimer_driver
     *ctx = cfg;
 
     return (struct fwk_time_driver){
-        .timestamp = mod_gtimer_timestamp,
+	.timestamp = mod_gtimer_timestamp,
     };
 }
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/i2c/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/i2c/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/i2c/CMakeLists.txt
@@ -8,7 +8,7 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_i2c.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_i2c.c")
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/i2c/doc/module_i2c_architecture.md
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/i2c/doc/module_i2c_architecture.md
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/i2c/doc/module_i2c_architecture.md
@@ -124,7 +124,7 @@ illustrated in the following schematic w
 initiated is a reception request.
 
     Client             I2C         I2C Driver     I2C ISR (Driver)
-                             ...
+			     ...
       |                 |               |               |
       |                 |               |  transaction +-+
       |                 |               |  _completed  | |
@@ -143,7 +143,7 @@ initiated is a reception request.
      | |  event R1     | |              |               |
      +-+               +-+              |               |
       |                 |               |               |
-                             ...
+			     ...
 
     E1   : Request completed event
     R1   : Response to the completed request
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/i2c/include/mod_i2c.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/i2c/include/mod_i2c.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/i2c/include/mod_i2c.h
@@ -113,8 +113,8 @@ struct mod_i2c_driver_api {
      * \return One of the standard framework status codes.
      */
     int (*transmit_as_controller)(
-        fwk_id_t dev_id,
-        struct mod_i2c_request *transmit_request);
+	fwk_id_t dev_id,
+	struct mod_i2c_request *transmit_request);
 
     /*!
      * \brief Request the reception of data as Controller from a selected
@@ -134,8 +134,8 @@ struct mod_i2c_driver_api {
      * \return One of the standard framework status codes.
      */
     int (*receive_as_controller)(
-        fwk_id_t dev_id,
-        struct mod_i2c_request *receive_request);
+	fwk_id_t dev_id,
+	struct mod_i2c_request *receive_request);
 };
 
 /*!
@@ -163,10 +163,10 @@ struct mod_i2c_api {
      * \return One of the standard framework status codes.
      */
     int (*transmit_as_controller)(
-        fwk_id_t dev_id,
-        uint8_t target_address,
-        uint8_t *data,
-        uint8_t byte_count);
+	fwk_id_t dev_id,
+	uint8_t target_address,
+	uint8_t *data,
+	uint8_t byte_count);
 
     /*!
      * \brief Request reception of data as Controller from a selected target.
@@ -189,10 +189,10 @@ struct mod_i2c_api {
      * \return One of the standard framework status codes.
      */
     int (*receive_as_controller)(
-        fwk_id_t dev_id,
-        uint8_t target_address,
-        uint8_t *data,
-        uint8_t byte_count);
+	fwk_id_t dev_id,
+	uint8_t target_address,
+	uint8_t *data,
+	uint8_t byte_count);
 
     /*!
      * \brief Request the transmission followed by the reception of data as
@@ -222,12 +222,12 @@ struct mod_i2c_api {
      * \return One of the standard framework status codes.
      */
     int (*transmit_then_receive_as_controller)(
-        fwk_id_t dev_id,
-        uint8_t target_address,
-        uint8_t *transmit_data,
-        uint8_t *receive_data,
-        uint8_t transmit_byte_count,
-        uint8_t receive_byte_count);
+	fwk_id_t dev_id,
+	uint8_t target_address,
+	uint8_t *transmit_data,
+	uint8_t *receive_data,
+	uint8_t transmit_byte_count,
+	uint8_t receive_byte_count);
 };
 
 /*!
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/i2c/src/mod_i2c.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/i2c/src/mod_i2c.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/i2c/src/mod_i2c.c
@@ -61,42 +61,42 @@ static void get_ctx(fwk_id_t id, struct
 }
 
 static int create_i2c_request(fwk_id_t dev_id,
-                              struct mod_i2c_request *request)
+			      struct mod_i2c_request *request)
 {
     int status;
     struct fwk_event event;
     struct mod_i2c_request *event_param =
-        (struct mod_i2c_request *)event.params;
+	(struct mod_i2c_request *)event.params;
 
     /* The target address should be on 7 bits */
     if (!fwk_expect(request->target_address < 0x80)) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     event = (struct fwk_event) {
-        .target_id = dev_id,
-        .response_requested = true,
+	.target_id = dev_id,
+	.response_requested = true,
     };
 
     *event_param = *request;
 
     if ((request->transmit_byte_count > 0) &&
-        (request->receive_byte_count > 0)) {
-        event.id = mod_i2c_event_id_request_tx_rx;
+	(request->receive_byte_count > 0)) {
+	event.id = mod_i2c_event_id_request_tx_rx;
     } else if (request->transmit_byte_count > 0) {
-        event.id = mod_i2c_event_id_request_tx;
+	event.id = mod_i2c_event_id_request_tx;
     } else if (request->receive_byte_count > 0) {
-        event.id = mod_i2c_event_id_request_rx;
+	event.id = mod_i2c_event_id_request_rx;
     }
 
     status = fwk_put_event(&event);
     if (status == FWK_SUCCESS) {
-        /*
-         * The request has been successfully queued for later processing by the
-         * I2C device but processing of this request has not yet begun. The
-         * caller is notified that the I2C request is in progress.
-         */
-        return FWK_PENDING;
+	/*
+	 * The request has been successfully queued for later processing by the
+	 * I2C device but processing of this request has not yet begun. The
+	 * caller is notified that the I2C request is in progress.
+	 */
+	return FWK_PENDING;
     }
 
     return status;
@@ -112,17 +112,17 @@ static int transmit_as_controller(
     uint8_t byte_count)
 {
     if (!fwk_expect(byte_count != 0)) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     if (!fwk_expect(data != NULL)) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     struct mod_i2c_request request = {
-        .target_address = target_address,
-        .transmit_data = data,
-        .transmit_byte_count = byte_count,
+	.target_address = target_address,
+	.transmit_data = data,
+	.transmit_byte_count = byte_count,
     };
 
     return create_i2c_request(dev_id, &request);
@@ -135,17 +135,17 @@ static int receive_as_controller(
     uint8_t byte_count)
 {
     if (!fwk_expect(byte_count != 0)) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     if (!fwk_expect(data != NULL)) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     struct mod_i2c_request request = {
-        .target_address = target_address,
-        .receive_data = data,
-        .receive_byte_count = byte_count,
+	.target_address = target_address,
+	.receive_data = data,
+	.receive_byte_count = byte_count,
     };
 
     return create_i2c_request(dev_id, &request);
@@ -160,19 +160,19 @@ static int transmit_then_receive_as_cont
     uint8_t receive_byte_count)
 {
     if (!fwk_expect((transmit_byte_count != 0) && (receive_byte_count != 0))) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     if (!fwk_expect((transmit_data != NULL) && (receive_data != NULL))) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     struct mod_i2c_request request = {
-        .target_address = target_address,
-        .transmit_data = transmit_data,
-        .receive_data = receive_data,
-        .transmit_byte_count = transmit_byte_count,
-        .receive_byte_count = receive_byte_count,
+	.target_address = target_address,
+	.transmit_data = transmit_data,
+	.receive_data = receive_data,
+	.transmit_byte_count = transmit_byte_count,
+	.receive_byte_count = receive_byte_count,
     };
 
     return create_i2c_request(dev_id, &request);
@@ -192,12 +192,12 @@ static void transaction_completed(fwk_id
     int status;
     struct fwk_event event;
     struct mod_i2c_event_param* param =
-        (struct mod_i2c_event_param *)event.params;
+	(struct mod_i2c_event_param *)event.params;
 
     event = (struct fwk_event) {
-        .target_id = dev_id,
-        .source_id = dev_id,
-        .id = mod_i2c_event_id_request_completed,
+	.target_id = dev_id,
+	.source_id = dev_id,
+	.id = mod_i2c_event_id_request_completed,
     };
 
     param->status = i2c_status;
@@ -214,8 +214,8 @@ static struct mod_i2c_driver_response_ap
  * Framework handlers
  */
 static int mod_i2c_init(fwk_id_t module_id,
-                        unsigned int element_count,
-                        const void *unused)
+			unsigned int element_count,
+			const void *unused)
 {
     ctx_table = fwk_mm_calloc(element_count, sizeof(ctx_table[0]));
 
@@ -223,8 +223,8 @@ static int mod_i2c_init(fwk_id_t module_
 }
 
 static int mod_i2c_dev_init(fwk_id_t element_id,
-                            unsigned int unused,
-                            const void *data)
+			    unsigned int unused,
+			    const void *data)
 {
     struct mod_i2c_dev_ctx *ctx;
 
@@ -244,48 +244,48 @@ static int mod_i2c_bind(fwk_id_t id, uns
      * Nothing to do for module
      */
     if ((round > 0) || fwk_id_is_type(id, FWK_ID_TYPE_MODULE)) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     ctx = ctx_table + fwk_id_get_element_idx(id);
 
     /* Bind to driver */
     status = fwk_module_bind(ctx->config->driver_id,
-                             ctx->config->api_id,
-                             &ctx->driver_api);
+			     ctx->config->api_id,
+			     &ctx->driver_api);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     if ((ctx->driver_api->transmit_as_controller == NULL) ||
-        (ctx->driver_api->receive_as_controller == NULL)) {
-        return FWK_E_DATA;
+	(ctx->driver_api->receive_as_controller == NULL)) {
+	return FWK_E_DATA;
     }
 
     return FWK_SUCCESS;
 }
 
 static int mod_i2c_process_bind_request(fwk_id_t source_id,
-                                        fwk_id_t target_id,
-                                        fwk_id_t api_id,
-                                        const void **api)
+					fwk_id_t target_id,
+					fwk_id_t api_id,
+					const void **api)
 {
     struct mod_i2c_dev_ctx *ctx;
 
     if (!fwk_id_is_type(target_id, FWK_ID_TYPE_ELEMENT)) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     ctx = ctx_table + fwk_id_get_element_idx(target_id);
 
     if (fwk_id_is_equal(source_id, ctx->config->driver_id)) {
-        if (fwk_id_is_equal(api_id, mod_i2c_api_id_driver_response)) {
-            *api = &driver_response_api;
-        } else {
-            return FWK_E_PARAM;
-        }
+	if (fwk_id_is_equal(api_id, mod_i2c_api_id_driver_response)) {
+	    *api = &driver_response_api;
+	} else {
+	    return FWK_E_PARAM;
+	}
     } else {
-        *api = &i2c_api;
+	*api = &i2c_api;
     }
 
     return FWK_SUCCESS;
@@ -299,11 +299,11 @@ static int respond_to_caller(
     int status;
     struct fwk_event resp;
     struct mod_i2c_event_param *param =
-        (struct mod_i2c_event_param *)resp.params;
+	(struct mod_i2c_event_param *)resp.params;
 
     status = fwk_get_first_delayed_response(dev_id, &resp);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     param->status = (drv_status == FWK_SUCCESS) ? FWK_SUCCESS : FWK_E_DEVICE;
@@ -317,34 +317,34 @@ static int process_request(struct mod_i2
     const struct mod_i2c_driver_api *driver_api = ctx->driver_api;
     fwk_id_t driver_id = ctx->config->driver_id;
     enum mod_i2c_event_idx event_id_type =
-        (enum mod_i2c_event_idx)fwk_id_get_event_idx(event_id);
+	(enum mod_i2c_event_idx)fwk_id_get_event_idx(event_id);
 
     switch (event_id_type) {
     case MOD_I2C_EVENT_IDX_REQUEST_TRANSMIT:
-        ctx->state = MOD_I2C_DEV_TX;
-        drv_status =
-            driver_api->transmit_as_controller(driver_id, &ctx->request);
-        break;
+	ctx->state = MOD_I2C_DEV_TX;
+	drv_status =
+	    driver_api->transmit_as_controller(driver_id, &ctx->request);
+	break;
 
     case MOD_I2C_EVENT_IDX_REQUEST_TRANSMIT_THEN_RECEIVE:
-        ctx->state = MOD_I2C_DEV_TX_RX;
-        drv_status =
-            driver_api->transmit_as_controller(driver_id, &ctx->request);
-
-        if (drv_status != FWK_SUCCESS) {
-            /* The request has failed or been acknowledged */
-            break;
-        }
-        /* fall through */
+	ctx->state = MOD_I2C_DEV_TX_RX;
+	drv_status =
+	    driver_api->transmit_as_controller(driver_id, &ctx->request);
+
+	if (drv_status != FWK_SUCCESS) {
+	    /* The request has failed or been acknowledged */
+	    break;
+	}
+	/* fall through */
 
     case MOD_I2C_EVENT_IDX_REQUEST_RECEIVE:
-        ctx->state = MOD_I2C_DEV_RX;
-        drv_status =
-            driver_api->receive_as_controller(driver_id, &ctx->request);
-        break;
+	ctx->state = MOD_I2C_DEV_RX;
+	drv_status =
+	    driver_api->receive_as_controller(driver_id, &ctx->request);
+	break;
 
     default:
-        break;
+	break;
     }
 
     return drv_status;
@@ -358,18 +358,18 @@ static int reload(fwk_id_t dev_id, struc
 
     status = fwk_is_delayed_response_list_empty(dev_id, &is_empty);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     if (is_empty) {
-        ctx->state = MOD_I2C_DEV_IDLE;
+	ctx->state = MOD_I2C_DEV_IDLE;
     } else {
-        ctx->state = MOD_I2C_DEV_RELOAD;
-        event = (struct fwk_event) {
-            .target_id = dev_id,
-            .id = mod_i2c_event_id_reload,
-        };
-        status = fwk_put_event(&event);
+	ctx->state = MOD_I2C_DEV_RELOAD;
+	event = (struct fwk_event) {
+	    .target_id = dev_id,
+	    .id = mod_i2c_event_id_reload,
+	};
+	status = fwk_put_event(&event);
     }
 
    return status;
@@ -385,17 +385,17 @@ static int process_next_request(fwk_id_t
 
     status = fwk_is_delayed_response_list_empty(dev_id, &is_empty);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     if (is_empty) {
-        ctx->state = MOD_I2C_DEV_IDLE;
-        return FWK_SUCCESS;
+	ctx->state = MOD_I2C_DEV_IDLE;
+	return FWK_SUCCESS;
     }
 
     status = fwk_get_first_delayed_response(dev_id, &delayed_response);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     request = (struct mod_i2c_request *)delayed_response.params;
@@ -403,20 +403,20 @@ static int process_next_request(fwk_id_t
 
     drv_status = process_request(ctx, delayed_response.id);
     if (drv_status != FWK_PENDING) {
-        event_param = (struct mod_i2c_event_param *)delayed_response.params;
-        event_param->status = drv_status;
+	event_param = (struct mod_i2c_event_param *)delayed_response.params;
+	event_param->status = drv_status;
 
-        status = fwk_put_event(&delayed_response);
-        if (status == FWK_SUCCESS) {
-            status = reload(dev_id, ctx);
-        }
+	status = fwk_put_event(&delayed_response);
+	if (status == FWK_SUCCESS) {
+	    status = reload(dev_id, ctx);
+	}
     }
 
     return status;
 }
 
 static int mod_i2c_process_event(const struct fwk_event *event,
-                                 struct fwk_event *resp_event)
+				 struct fwk_event *resp_event)
 {
     fwk_id_t dev_id;
     int status, drv_status;
@@ -433,86 +433,86 @@ static int mod_i2c_process_event(const s
     is_request = fwk_id_get_event_idx(event->id) < MOD_I2C_EVENT_IDX_COUNT;
 
     if (is_request) {
-        if (ctx->state == MOD_I2C_DEV_PANIC) {
-            event_param = (struct mod_i2c_event_param *)resp_event->params;
-            event_param->status = FWK_E_PANIC;
-
-            return FWK_SUCCESS;
-        } else if (ctx->state != MOD_I2C_DEV_IDLE) {
-            resp_event->is_delayed_response = true;
-
-            return FWK_SUCCESS;
-        }
-
-        request = (struct mod_i2c_request *)event->params;
-        ctx->request = *request;
-
-        drv_status = process_request(ctx, event->id);
-
-        if (drv_status == FWK_PENDING) {
-            resp_event->is_delayed_response = true;
-        } else {
-            /* The request has succeeded or failed, respond now */
-            resp_param = (struct mod_i2c_event_param *)resp_event->params;
-
-            resp_param->status = (drv_status == FWK_SUCCESS) ?
-                                 FWK_SUCCESS : FWK_E_DEVICE;
-            ctx->state = MOD_I2C_DEV_IDLE;
-        }
+	if (ctx->state == MOD_I2C_DEV_PANIC) {
+	    event_param = (struct mod_i2c_event_param *)resp_event->params;
+	    event_param->status = FWK_E_PANIC;
+
+	    return FWK_SUCCESS;
+	} else if (ctx->state != MOD_I2C_DEV_IDLE) {
+	    resp_event->is_delayed_response = true;
+
+	    return FWK_SUCCESS;
+	}
+
+	request = (struct mod_i2c_request *)event->params;
+	ctx->request = *request;
+
+	drv_status = process_request(ctx, event->id);
+
+	if (drv_status == FWK_PENDING) {
+	    resp_event->is_delayed_response = true;
+	} else {
+	    /* The request has succeeded or failed, respond now */
+	    resp_param = (struct mod_i2c_event_param *)resp_event->params;
+
+	    resp_param->status = (drv_status == FWK_SUCCESS) ?
+				 FWK_SUCCESS : FWK_E_DEVICE;
+	    ctx->state = MOD_I2C_DEV_IDLE;
+	}
 
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     event_id_type =
-        (enum mod_i2c_internal_event_idx)fwk_id_get_event_idx(event->id);
+	(enum mod_i2c_internal_event_idx)fwk_id_get_event_idx(event->id);
 
     switch (event_id_type) {
     case MOD_I2C_EVENT_IDX_REQUEST_COMPLETED:
-        event_param = (struct mod_i2c_event_param *)event->params;
+	event_param = (struct mod_i2c_event_param *)event->params;
 
-        if ((ctx->state == MOD_I2C_DEV_TX) || (ctx->state == MOD_I2C_DEV_RX)) {
-            status = respond_to_caller(dev_id, ctx, event_param->status);
-            if (status == FWK_SUCCESS) {
-                status = process_next_request(dev_id, ctx);
-            }
-
-        } else if (ctx->state == MOD_I2C_DEV_TX_RX) {
-            drv_status = event_param->status;
-
-            if (drv_status == FWK_SUCCESS) {
-                /* The TX request succeeded, proceed with the RX */
-                ctx->state = MOD_I2C_DEV_RX;
-
-                drv_status = ctx->driver_api->receive_as_controller(
-                    ctx->config->driver_id, &ctx->request);
-
-                if (drv_status == FWK_PENDING) {
-                    status = FWK_SUCCESS;
-                    break;
-                }
-            }
-
-            status = respond_to_caller(dev_id, ctx, drv_status);
-            if (status == FWK_SUCCESS) {
-                status = process_next_request(dev_id, ctx);
-            }
-        } else {
-            status = FWK_E_STATE;
-        }
+	if ((ctx->state == MOD_I2C_DEV_TX) || (ctx->state == MOD_I2C_DEV_RX)) {
+	    status = respond_to_caller(dev_id, ctx, event_param->status);
+	    if (status == FWK_SUCCESS) {
+		status = process_next_request(dev_id, ctx);
+	    }
+
+	} else if (ctx->state == MOD_I2C_DEV_TX_RX) {
+	    drv_status = event_param->status;
+
+	    if (drv_status == FWK_SUCCESS) {
+		/* The TX request succeeded, proceed with the RX */
+		ctx->state = MOD_I2C_DEV_RX;
+
+		drv_status = ctx->driver_api->receive_as_controller(
+		    ctx->config->driver_id, &ctx->request);
+
+		if (drv_status == FWK_PENDING) {
+		    status = FWK_SUCCESS;
+		    break;
+		}
+	    }
+
+	    status = respond_to_caller(dev_id, ctx, drv_status);
+	    if (status == FWK_SUCCESS) {
+		status = process_next_request(dev_id, ctx);
+	    }
+	} else {
+	    status = FWK_E_STATE;
+	}
 
-        break;
+	break;
 
     case MOD_I2C_EVENT_IDX_RELOAD:
-        status = process_next_request(dev_id, ctx);
-        break;
+	status = process_next_request(dev_id, ctx);
+	break;
 
     default:
-        status = FWK_E_PANIC;
-        break;
+	status = FWK_E_PANIC;
+	break;
     }
 
     if (status != FWK_SUCCESS) {
-        ctx->state = MOD_I2C_DEV_PANIC;
+	ctx->state = MOD_I2C_DEV_PANIC;
     }
 
     return status;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/isys_rom/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/isys_rom/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/isys_rom/CMakeLists.txt
@@ -8,6 +8,6 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_isys_rom.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_isys_rom.c")
 
 target_link_libraries(${SCP_MODULE_TARGET} PRIVATE module-bootloader)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/isys_rom/src/mod_isys_rom.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/isys_rom/src/mod_isys_rom.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/isys_rom/src/mod_isys_rom.c
@@ -37,14 +37,14 @@ static int mod_isys_rom_bind(fwk_id_t id
     int status = FWK_SUCCESS;
 
     if (round > 0)
-        goto exit;
+	goto exit;
 
     status = fwk_module_bind(
-        FWK_ID_MODULE(FWK_MODULE_IDX_BOOTLOADER),
-        FWK_ID_API(FWK_MODULE_IDX_BOOTLOADER, 0),
-        &ctx.bootloader_api);
+	FWK_ID_MODULE(FWK_MODULE_IDX_BOOTLOADER),
+	FWK_ID_API(FWK_MODULE_IDX_BOOTLOADER, 0),
+	&ctx.bootloader_api);
     if (status != FWK_SUCCESS)
-        status = FWK_E_PANIC;
+	status = FWK_E_PANIC;
 
 exit:
     return status;
@@ -53,9 +53,9 @@ exit:
 static int mod_isys_rom_start(fwk_id_t id)
 {
     struct fwk_event event = {
-        .source_id = FWK_ID_MODULE(FWK_MODULE_IDX_ISYS_ROM),
-        .target_id = FWK_ID_MODULE(FWK_MODULE_IDX_ISYS_ROM),
-        .id = FWK_ID_EVENT(FWK_MODULE_IDX_ISYS_ROM, MOD_ISYS_ROM_EVENT_RUN),
+	.source_id = FWK_ID_MODULE(FWK_MODULE_IDX_ISYS_ROM),
+	.target_id = FWK_ID_MODULE(FWK_MODULE_IDX_ISYS_ROM),
+	.id = FWK_ID_EVENT(FWK_MODULE_IDX_ISYS_ROM, MOD_ISYS_ROM_EVENT_RUN),
     };
 
     return fwk_put_event(&event);
@@ -70,8 +70,8 @@ static int mod_isys_rom_process_event(
     status = ctx.bootloader_api->load_image();
 
     FWK_LOG_CRIT(
-        "[ISYS-ROM] Failed to load RAM firmware image: %s",
-        fwk_status_str(status));
+	"[ISYS-ROM] Failed to load RAM firmware image: %s",
+	fwk_status_str(status));
 
     fwk_trap();
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/mhu/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/mhu/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/mhu/CMakeLists.txt
@@ -8,9 +8,9 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_mhu.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_mhu.c")
 
 target_link_libraries(${SCP_MODULE_TARGET} PRIVATE module-transport)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/mhu/include/internal/mhu.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/mhu/include/internal/mhu.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/mhu/include/internal/mhu.h
@@ -20,10 +20,10 @@
 struct mhu_reg {
     /*! Status register */
     FWK_R  uint32_t STAT;
-           uint32_t RESERVED0;
+	   uint32_t RESERVED0;
     /*! Set register (sets the value of STAT) */
     FWK_W  uint32_t SET;
-           uint32_t RESERVED1;
+	   uint32_t RESERVED1;
     /*! Clear register (clears STAT) */
     FWK_W  uint32_t CLEAR;
 };
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/mhu/src/mod_mhu.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/mhu/src/mod_mhu.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/mhu/src/mod_mhu.c
@@ -75,41 +75,41 @@ static void mhu_isr(void)
 
     status = fwk_interrupt_get_current(&interrupt);
     if (status != FWK_SUCCESS) {
-        return;
+	return;
     }
 
     for (device_idx = 0; device_idx < mhu_ctx.device_count; device_idx++) {
-        device_ctx = &mhu_ctx.device_ctx_table[device_idx];
-        if (device_ctx->config->irq == interrupt) {
-            break;
-        }
+	device_ctx = &mhu_ctx.device_ctx_table[device_idx];
+	if (device_ctx->config->irq == interrupt) {
+	    break;
+	}
     }
 
     if (device_idx >= mhu_ctx.device_count) {
-        return;
+	return;
     }
 
     reg = (struct mhu_reg *)device_ctx->config->in;
 
     /* Loop over all the slots */
     while (reg->STAT != 0) {
-        slot = (unsigned int)__builtin_ctz(reg->STAT);
+	slot = (unsigned int)__builtin_ctz(reg->STAT);
 
-        /*
-         * If the slot is bound to an TRANSPORT channel, signal the message
-         * to the TRANSPORT channel.
-         */
-        if ((device_ctx->bound_slots & (uint32_t)(1U << slot)) != (uint32_t)0) {
-            transport_channel = &device_ctx->transport_channel_table[slot];
-            status =
-                transport_channel->api->signal_message(transport_channel->id);
-            if (status != FWK_SUCCESS) {
-                FWK_LOG_DEBUG("[MHU] %s @%d", __func__, __LINE__);
-            }
-        }
+	/*
+	 * If the slot is bound to an TRANSPORT channel, signal the message
+	 * to the TRANSPORT channel.
+	 */
+	if ((device_ctx->bound_slots & (uint32_t)(1U << slot)) != (uint32_t)0) {
+	    transport_channel = &device_ctx->transport_channel_table[slot];
+	    status =
+		transport_channel->api->signal_message(transport_channel->id);
+	    if (status != FWK_SUCCESS) {
+		FWK_LOG_DEBUG("[MHU] %s @%d", __func__, __LINE__);
+	    }
+	}
 
-        /* Acknowledge the interrupt */
-        reg->CLEAR = 1U << slot;
+	/* Acknowledge the interrupt */
+	reg->CLEAR = 1U << slot;
     }
 }
 
@@ -142,14 +142,14 @@ const struct mod_transport_driver_api mh
  */
 
 static int mhu_init(fwk_id_t module_id, unsigned int device_count,
-                    const void *unused)
+		    const void *unused)
 {
     if (device_count == 0) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     mhu_ctx.device_ctx_table = fwk_mm_calloc(device_count,
-        sizeof(mhu_ctx.device_ctx_table[0]));
+	sizeof(mhu_ctx.device_ctx_table[0]));
 
     mhu_ctx.device_count = device_count;
 
@@ -157,19 +157,19 @@ static int mhu_init(fwk_id_t module_id,
 }
 
 static int mhu_device_init(fwk_id_t device_id, unsigned int slot_count,
-                           const void *data)
+			   const void *data)
 {
     struct mod_mhu_device_config *config = (struct mod_mhu_device_config *)data;
     struct mhu_device_ctx *device_ctx;
 
     if ((config->in == 0) || (config->out == 0)) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     device_ctx = &mhu_ctx.device_ctx_table[fwk_id_get_element_idx(device_id)];
 
     device_ctx->transport_channel_table = fwk_mm_calloc(
-        slot_count, sizeof(device_ctx->transport_channel_table[0]));
+	slot_count, sizeof(device_ctx->transport_channel_table[0]));
 
     device_ctx->config = config;
     device_ctx->slot_count = slot_count;
@@ -185,46 +185,46 @@ static int mhu_bind(fwk_id_t id, unsigne
     struct mhu_transport_channel *transport_channel;
 
     if ((round == 1U) && fwk_id_is_type(id, FWK_ID_TYPE_ELEMENT)) {
-        device_ctx = &mhu_ctx.device_ctx_table[fwk_id_get_element_idx(id)];
+	device_ctx = &mhu_ctx.device_ctx_table[fwk_id_get_element_idx(id)];
 
-        for (slot = 0; slot < MHU_SLOT_COUNT_MAX; slot++) {
-            if ((device_ctx->bound_slots & (uint32_t)(1U << slot)) ==
-                (uint32_t)0) {
-                continue;
-            }
-
-            transport_channel = &device_ctx->transport_channel_table[slot];
-
-            status = fwk_module_bind(
-                transport_channel->id,
-                FWK_ID_API(
-                    FWK_MODULE_IDX_TRANSPORT,
-                    MOD_TRANSPORT_API_IDX_DRIVER_INPUT),
-                &transport_channel->api);
-            if (status != FWK_SUCCESS) {
-                return status;
-            }
-        }
+	for (slot = 0; slot < MHU_SLOT_COUNT_MAX; slot++) {
+	    if ((device_ctx->bound_slots & (uint32_t)(1U << slot)) ==
+		(uint32_t)0) {
+		continue;
+	    }
+
+	    transport_channel = &device_ctx->transport_channel_table[slot];
+
+	    status = fwk_module_bind(
+		transport_channel->id,
+		FWK_ID_API(
+		    FWK_MODULE_IDX_TRANSPORT,
+		    MOD_TRANSPORT_API_IDX_DRIVER_INPUT),
+		&transport_channel->api);
+	    if (status != FWK_SUCCESS) {
+		return status;
+	    }
+	}
     }
 
     return FWK_SUCCESS;
 }
 
 static int mhu_process_bind_request(fwk_id_t source_id, fwk_id_t target_id,
-                                    fwk_id_t api_id, const void **api)
+				    fwk_id_t api_id, const void **api)
 {
     struct mhu_device_ctx *device_ctx;
     unsigned int slot;
 
     if (!fwk_id_is_type(target_id, FWK_ID_TYPE_SUB_ELEMENT)) {
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 
     device_ctx = &mhu_ctx.device_ctx_table[fwk_id_get_element_idx(target_id)];
     slot = fwk_id_get_sub_element_idx(target_id);
 
     if (device_ctx->bound_slots & (1U << slot)) {
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 
     device_ctx->transport_channel_table[slot].id = source_id;
@@ -241,20 +241,20 @@ static int mhu_start(fwk_id_t id)
     struct mhu_device_ctx *device_ctx;
 
     if (fwk_id_get_type(id) == FWK_ID_TYPE_MODULE) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     device_ctx = &mhu_ctx.device_ctx_table[fwk_id_get_element_idx(id)];
 
     if (device_ctx->bound_slots != 0) {
-        status = fwk_interrupt_set_isr(device_ctx->config->irq, &mhu_isr);
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
-        status = fwk_interrupt_enable(device_ctx->config->irq);
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
+	status = fwk_interrupt_set_isr(device_ctx->config->irq, &mhu_isr);
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
+	status = fwk_interrupt_enable(device_ctx->config->irq);
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
     }
 
     return FWK_SUCCESS;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/mhu2/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/mhu2/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/mhu2/CMakeLists.txt
@@ -13,6 +13,6 @@ target_include_directories(
     PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_mhu2.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_mhu2.c")
 
 target_link_libraries(${SCP_MODULE_TARGET} PRIVATE module-transport)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/mhu2/src/mhu2.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/mhu2/src/mhu2.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/mhu2/src/mhu2.h
@@ -16,7 +16,7 @@
 
 struct mhu2_id_reg {
     FWK_R  uint32_t  PID4;
-           uint8_t   RESERVED1[0x10 - 0x4];
+	   uint8_t   RESERVED1[0x10 - 0x4];
     FWK_R  uint32_t  PID0;
     FWK_R  uint32_t  PID1;
     FWK_R  uint32_t  PID2;
@@ -29,9 +29,9 @@ struct mhu2_id_reg {
 
 struct mhu2_send_channel_reg {
     FWK_R  uint32_t  STAT;
-           uint8_t   RESERVED0[0xC - 0x4];
+	   uint8_t   RESERVED0[0xC - 0x4];
     FWK_W  uint32_t  STAT_SET;
-           uint8_t   RESERVED1[0x20 - 0x10];
+	   uint8_t   RESERVED1[0x20 - 0x10];
 };
 
 struct mhu2_send_reg {
@@ -43,7 +43,7 @@ struct mhu2_send_reg {
     FWK_R  uint32_t  INT_ACCESS_STAT;
     FWK_W  uint32_t  INT_ACCESS_CLR;
     FWK_W  uint32_t  INT_ACCESS_EN;
-           uint8_t   RESERVED0[0xFD0 - 0xF9C];
+	   uint8_t   RESERVED0[0xFD0 - 0xF9C];
     struct mhu2_id_reg id;
 };
 
@@ -51,17 +51,17 @@ struct mhu2_recv_channel_reg {
     FWK_R  uint32_t  STAT;
     FWK_R  uint32_t  STAT_PEND;
     FWK_W  uint32_t  STAT_CLEAR;
-           uint8_t   RESERVED0[0x10 - 0x0C];
+	   uint8_t   RESERVED0[0x10 - 0x0C];
     FWK_R  uint32_t  MASK;
     FWK_W  uint32_t  MASK_SET;
     FWK_W  uint32_t  MASK_CLEAR;
-           uint8_t   RESERVED1[0x20 - 0x1C];
+	   uint8_t   RESERVED1[0x20 - 0x1C];
 };
 
 struct mhu2_recv_reg {
     struct mhu2_recv_channel_reg channel[CHANNEL_MAX];
     FWK_R  uint32_t  MSG_NO_CAP;
-           uint8_t   RESERVED0[0xFD0 - 0xF84];
+	   uint8_t   RESERVED0[0xFD0 - 0xF84];
     struct mhu2_id_reg id;
 };
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/mhu2/src/mod_mhu2.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/mhu2/src/mod_mhu2.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/mhu2/src/mod_mhu2.c
@@ -88,18 +88,18 @@ static void mhu2_isr(uintptr_t ctx_param
     fwk_assert(channel_ctx != NULL);
 
     while (channel_ctx->recv_channel->STAT != 0) {
-        slot = __builtin_ctz(channel_ctx->recv_channel->STAT);
-        /*
-         * If the slot is bound to a transport channel,
-         * signal the message to the corresponding module
-         */
-        if (channel_ctx->bound_slots & (1 << slot)) {
-            bound_channel = &channel_ctx->bound_channels_table[slot];
-
-            bound_channel->driver_input_api->signal_message(bound_channel->id);
-        }
-        /* Acknowledge the interrupt */
-        channel_ctx->recv_channel->STAT_CLEAR = 1 << slot;
+	slot = __builtin_ctz(channel_ctx->recv_channel->STAT);
+	/*
+	 * If the slot is bound to a transport channel,
+	 * signal the message to the corresponding module
+	 */
+	if (channel_ctx->bound_slots & (1 << slot)) {
+	    bound_channel = &channel_ctx->bound_channels_table[slot];
+
+	    bound_channel->driver_input_api->signal_message(bound_channel->id);
+	}
+	/* Acknowledge the interrupt */
+	channel_ctx->recv_channel->STAT_CLEAR = 1 << slot;
     }
 }
 
@@ -120,7 +120,7 @@ static int raise_interrupt(fwk_id_t slot
     /* Turn on receiver */
     send->ACCESS_REQUEST = 1;
     while (send->ACCESS_READY != 1)
-        continue;
+	continue;
 
     channel_ctx->send_channel->STAT_SET |= (1 << slot);
 
@@ -159,8 +159,8 @@ static int mhu2_send_message(
 
     /* Calculate the number of channels required for payload */
     if (payload_size) {
-        channels_used_for_payload =
-            payload_size / MHU_CHANNEL_STATUS_REGISTER_WIDTH;
+	channels_used_for_payload =
+	    payload_size / MHU_CHANNEL_STATUS_REGISTER_WIDTH;
     }
 
     /* Read the number of channels implemented */
@@ -168,28 +168,28 @@ static int mhu2_send_message(
 
     /* Check if minimum number of MHUv2 channels are available */
     if (channel_count < (min_channels_required + channels_used_for_payload)) {
-        FWK_LOG_INFO(
-            "[MHUv2] ERROR! Message length exceeds the number of MHUv2"
-            "channels available\n");
-        return FWK_E_SUPPORT;
+	FWK_LOG_INFO(
+	    "[MHUv2] ERROR! Message length exceeds the number of MHUv2"
+	    "channels available\n");
+	return FWK_E_SUPPORT;
     }
 
     /* Turn on receiver */
     send->ACCESS_REQUEST = 1;
     while (send->ACCESS_READY != 1)
-        continue;
+	continue;
 
     /* Get the channel used for doorbell */
     db_ch = channel_ctx->config->channel;
 
     /* Copy the in-band message to the message status registers */
     for (uint8_t idx = 0; ch < (min_channels_required - 1); ch++) {
-        /* Skip the doorbell channel */
-        if (ch == db_ch)
-            continue;
+	/* Skip the doorbell channel */
+	if (ch == db_ch)
+	    continue;
 
-        send->channel[ch].STAT_SET = msg_ptr[idx];
-        idx++;
+	send->channel[ch].STAT_SET = msg_ptr[idx];
+	idx++;
     }
 
     /*
@@ -197,16 +197,16 @@ static int mhu2_send_message(
      * registers.
      */
     if (payload_size != 0) {
-        for (uint8_t payload_idx = 0;
-             payload_idx < (payload_size / MHU_CHANNEL_STATUS_REGISTER_WIDTH);
-             payload_idx++) {
-            /* Skip the doorbell channel */
-            if ((ch + payload_idx) == db_ch)
-                continue;
-
-            send->channel[payload_idx + ch].STAT_SET =
-                message->payload[payload_idx];
-        }
+	for (uint8_t payload_idx = 0;
+	     payload_idx < (payload_size / MHU_CHANNEL_STATUS_REGISTER_WIDTH);
+	     payload_idx++) {
+	    /* Skip the doorbell channel */
+	    if ((ch + payload_idx) == db_ch)
+		continue;
+
+	    send->channel[payload_idx + ch].STAT_SET =
+		message->payload[payload_idx];
+	}
     }
 
     /* Receiver no longer required */
@@ -238,14 +238,14 @@ static int mhu2_get_message(
 
     /* Copy the in-band message from message status registers */
     for (uint8_t idx = 0; ch < (min_channels_required - 1); ch++) {
-        /* Skip doorbell channel */
-        if (ch == db_ch)
-            continue;
+	/* Skip doorbell channel */
+	if (ch == db_ch)
+	    continue;
 
-        msg_ptr[idx++] = recv->channel[ch].STAT;
+	msg_ptr[idx++] = recv->channel[ch].STAT;
 
-        /* Clear the message status register */
-        recv->channel[ch].STAT_CLEAR = 0xffffffff;
+	/* Clear the message status register */
+	recv->channel[ch].STAT_CLEAR = 0xffffffff;
     }
 
     /* Calculate size of the received payload */
@@ -253,19 +253,19 @@ static int mhu2_get_message(
 
     /* If payload is present, copy it from the message status registers */
     if (payload_size != 0) {
-        for (uint8_t payload_idx = 0;
-             payload_idx < (payload_size / MHU_CHANNEL_STATUS_REGISTER_WIDTH);
-             payload_idx++) {
-            /* Skip doorbell channel */
-            if ((ch + payload_idx) == db_ch)
-                continue;
-
-            message->payload[payload_idx] =
-                recv->channel[payload_idx + ch].STAT;
-
-            /* Clear the message status register */
-            recv->channel[payload_idx + ch].STAT_CLEAR = 0xffffffff;
-        }
+	for (uint8_t payload_idx = 0;
+	     payload_idx < (payload_size / MHU_CHANNEL_STATUS_REGISTER_WIDTH);
+	     payload_idx++) {
+	    /* Skip doorbell channel */
+	    if ((ch + payload_idx) == db_ch)
+		continue;
+
+	    message->payload[payload_idx] =
+		recv->channel[payload_idx + ch].STAT;
+
+	    /* Clear the message status register */
+	    recv->channel[payload_idx + ch].STAT_CLEAR = 0xffffffff;
+	}
     }
 
     return FWK_SUCCESS;
@@ -285,17 +285,17 @@ struct mod_transport_driver_api mhu2_mod
  */
 
 static int mhu2_init(fwk_id_t module_id,
-                     unsigned int channel_count,
-                     const void *unused)
+		     unsigned int channel_count,
+		     const void *unused)
 {
     if (channel_count == 0) {
-        /* There must be at least 1 mhu channel */
-        fwk_unexpected();
-        return FWK_E_PARAM;
+	/* There must be at least 1 mhu channel */
+	fwk_unexpected();
+	return FWK_E_PARAM;
     }
 
     ctx.channel_ctx_table = fwk_mm_calloc(channel_count,
-        sizeof(ctx.channel_ctx_table[0]));
+	sizeof(ctx.channel_ctx_table[0]));
 
     ctx.channel_count = channel_count;
 
@@ -303,24 +303,24 @@ static int mhu2_init(fwk_id_t module_id,
 }
 
 static int mhu2_channel_init(fwk_id_t channel_id,
-                             unsigned int slot_count,
-                             const void *data)
+			     unsigned int slot_count,
+			     const void *data)
 {
     const struct mod_mhu2_channel_config *config = data;
     struct mhu2_channel_ctx *channel_ctx;
     struct mhu2_recv_reg *recv_reg;
 
     if ((config == NULL) || (config->recv == 0) || (config->send == 0)) {
-        fwk_unexpected();
-        return FWK_E_DATA;
+	fwk_unexpected();
+	return FWK_E_DATA;
     }
 
     channel_ctx = &ctx.channel_ctx_table[fwk_id_get_element_idx(channel_id)];
     channel_ctx->send = (struct mhu2_send_reg *)config->send;
 
     if (config->channel >= channel_ctx->send->MSG_NO_CAP) {
-        fwk_unexpected();
-        return FWK_E_DATA;
+	fwk_unexpected();
+	return FWK_E_DATA;
     }
 
     channel_ctx->config = config;
@@ -334,23 +334,23 @@ static int mhu2_channel_init(fwk_id_t ch
      * Only the doorbell channel should be used to raise interrupt.
      */
     for (uint8_t ch = 0; ch < recv_reg->MSG_NO_CAP; ch++) {
-        /* Skip the channel used for doorbell */
-        if (ch == config->channel)
-            continue;
-
-        /*
-         * Set channel mask.
-         *
-         * Masked channels don't generate interrupt when data is written
-         * to them.
-         */
-        recv_reg->channel[ch].MASK_SET = 0xffffffff;
+	/* Skip the channel used for doorbell */
+	if (ch == config->channel)
+	    continue;
+
+	/*
+	 * Set channel mask.
+	 *
+	 * Masked channels don't generate interrupt when data is written
+	 * to them.
+	 */
+	recv_reg->channel[ch].MASK_SET = 0xffffffff;
     }
 #endif
     channel_ctx->recv_channel = &recv_reg->channel[config->channel];
 
     channel_ctx->bound_channels_table =
-        fwk_mm_calloc(slot_count, sizeof(channel_ctx->bound_channels_table[0]));
+	fwk_mm_calloc(slot_count, sizeof(channel_ctx->bound_channels_table[0]));
 
     return FWK_SUCCESS;
 }
@@ -362,37 +362,37 @@ static int mhu2_bind(fwk_id_t id, unsign
     unsigned int slot;
 
     if ((round == 1) && fwk_id_is_type(id, FWK_ID_TYPE_ELEMENT)) {
-        channel_ctx = &ctx.channel_ctx_table[fwk_id_get_element_idx(id)];
+	channel_ctx = &ctx.channel_ctx_table[fwk_id_get_element_idx(id)];
 
-        for (slot = 0; slot < MHU_SLOT_COUNT_MAX; slot++) {
-            struct mhu2_bound_channel *bound_channel;
-            if (!(channel_ctx->bound_slots & (UINT32_C(1) << slot)))
-                continue;
-
-            bound_channel = &channel_ctx->bound_channels_table[slot];
-
-            status = fwk_module_bind(
-                bound_channel->id,
-                FWK_ID_API(
-                    FWK_MODULE_IDX_TRANSPORT,
-                    MOD_TRANSPORT_API_IDX_DRIVER_INPUT),
-                &bound_channel->driver_input_api);
-
-            if (status != FWK_SUCCESS) {
-                /* Unable to bind back to TRANSPORT channel */
-                fwk_unexpected();
-                return status;
-            }
-        }
+	for (slot = 0; slot < MHU_SLOT_COUNT_MAX; slot++) {
+	    struct mhu2_bound_channel *bound_channel;
+	    if (!(channel_ctx->bound_slots & (UINT32_C(1) << slot)))
+		continue;
+
+	    bound_channel = &channel_ctx->bound_channels_table[slot];
+
+	    status = fwk_module_bind(
+		bound_channel->id,
+		FWK_ID_API(
+		    FWK_MODULE_IDX_TRANSPORT,
+		    MOD_TRANSPORT_API_IDX_DRIVER_INPUT),
+		&bound_channel->driver_input_api);
+
+	    if (status != FWK_SUCCESS) {
+		/* Unable to bind back to TRANSPORT channel */
+		fwk_unexpected();
+		return status;
+	    }
+	}
     }
 
     return FWK_SUCCESS;
 }
 
 static int mhu2_process_bind_request(fwk_id_t source_id,
-                                    fwk_id_t target_id,
-                                    fwk_id_t api_id,
-                                    const void **api)
+				    fwk_id_t target_id,
+				    fwk_id_t api_id,
+				    const void **api)
 {
     struct mhu2_channel_ctx *channel_ctx;
     unsigned int slot;
@@ -400,29 +400,29 @@ static int mhu2_process_bind_request(fwk
     api_id_type = (enum mod_mhu2_api_idx)fwk_id_get_api_idx(api_id);
 
     if (!fwk_id_is_type(target_id, FWK_ID_TYPE_SUB_ELEMENT)) {
-        /*
-         * Something tried to bind to the module or an element. Only binding to
-         * a slot (sub-element) is allowed.
-         */
-        fwk_unexpected();
-        return FWK_E_ACCESS;
+	/*
+	 * Something tried to bind to the module or an element. Only binding to
+	 * a slot (sub-element) is allowed.
+	 */
+	fwk_unexpected();
+	return FWK_E_ACCESS;
     }
 
     channel_ctx = &ctx.channel_ctx_table[fwk_id_get_element_idx(target_id)];
     slot = fwk_id_get_sub_element_idx(target_id);
     if (channel_ctx->bound_slots & (1 << slot)) {
-        /* Something tried to bind to a slot that has already been bound to */
-        fwk_unexpected();
-        return FWK_E_ACCESS;
+	/* Something tried to bind to a slot that has already been bound to */
+	fwk_unexpected();
+	return FWK_E_ACCESS;
     }
 
     channel_ctx->bound_channels_table[slot].id = source_id;
     channel_ctx->bound_slots |= 1 << slot;
 
     if (api_id_type != MOD_MHU2_API_IDX_TRANSPORT_DRIVER) {
-        /* Invalid config */
-        fwk_unexpected();
-        return FWK_E_PARAM;
+	/* Invalid config */
+	fwk_unexpected();
+	return FWK_E_PARAM;
     }
 
     *api = &mhu2_mod_transport_driver_api;
@@ -436,25 +436,25 @@ static int mhu2_start(fwk_id_t id)
     struct mhu2_channel_ctx *channel_ctx;
 
     if (fwk_id_get_type(id) == FWK_ID_TYPE_MODULE)
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 
     channel_ctx = &ctx.channel_ctx_table[fwk_id_get_element_idx(id)];
 
     if (channel_ctx->bound_slots != 0) {
-        status = fwk_interrupt_set_isr_param(channel_ctx->config->irq,
-                                             &mhu2_isr,
-                                             (uintptr_t)channel_ctx);
-        if (status != FWK_SUCCESS) {
-            /* Failed to set isr */
-            fwk_unexpected();
-            return status;
-        }
-        status = fwk_interrupt_enable(channel_ctx->config->irq);
-        if (status != FWK_SUCCESS) {
-            /* Failed to enable isr */
-            fwk_unexpected();
-            return status;
-        }
+	status = fwk_interrupt_set_isr_param(channel_ctx->config->irq,
+					     &mhu2_isr,
+					     (uintptr_t)channel_ctx);
+	if (status != FWK_SUCCESS) {
+	    /* Failed to set isr */
+	    fwk_unexpected();
+	    return status;
+	}
+	status = fwk_interrupt_enable(channel_ctx->config->irq);
+	if (status != FWK_SUCCESS) {
+	    /* Failed to enable isr */
+	    fwk_unexpected();
+	    return status;
+	}
     }
 
     return FWK_SUCCESS;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/mhu3/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/mhu3/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/mhu3/CMakeLists.txt
@@ -8,10 +8,10 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_mhu3.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_mhu3.c")
 
 if("transport" IN_LIST SCP_MODULES)
     target_link_libraries(${SCP_MODULE_TARGET} PRIVATE module-transport)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/mhu3/include/internal/mhu3.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/mhu3/include/internal/mhu3.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/mhu3/include/internal/mhu3.h
@@ -141,7 +141,7 @@ struct mhu3_mbx_mdbcw_reg {
 /* Fast Channel Configuration 0 Number of Fast Channels Mask */
 #define MHU3_FCH_CFG0_NUM_FCH_MASK \
     (MHU3_MASK_GENERATE( \
-        MHU3_FCH_CFG0_NUM_FCH_BITSTART, MHU3_FCH_CFG0_NUM_FCH_LEN))
+	MHU3_FCH_CFG0_NUM_FCH_BITSTART, MHU3_FCH_CFG0_NUM_FCH_LEN))
 
 /* Fast Channel Configuration 0 Number of Fast Channel Groups Start Bit */
 #define MHU3_FCH_CFG0_NUM_FCG_BITSTART 15
@@ -150,7 +150,7 @@ struct mhu3_mbx_mdbcw_reg {
 /* Fast Channel Configuration 0 Number of Fast Channel Groups Mask */
 #define MHU3_FCH_CFG0_NUM_FCG_MASK \
     (MHU3_MASK_GENERATE( \
-        MHU3_FCH_CFG0_NUM_FCG_BITSTART, MHU3_FCH_CFG0_NUM_FCG_LEN))
+	MHU3_FCH_CFG0_NUM_FCG_BITSTART, MHU3_FCH_CFG0_NUM_FCG_LEN))
 
 /*
  * Fast Channel Configuration 0 Number of Fast Channels per Fast Channel Group
@@ -168,8 +168,8 @@ struct mhu3_mbx_mdbcw_reg {
  */
 #define MHU3_FCH_CFG0_NUM_FCH_PER_GRP_MASK \
     (MHU3_MASK_GENERATE( \
-        MHU3_FCH_CFG0_NUM_FCH_PER_GRP_BITSTART, \
-        MHU3_FCH_CFG0_NUM_FCH_PER_GRP_LEN))
+	MHU3_FCH_CFG0_NUM_FCH_PER_GRP_BITSTART, \
+	MHU3_FCH_CFG0_NUM_FCH_PER_GRP_LEN))
 
 /* Fast Channel Configuration 0 Fast Channel Word Size Start Bit */
 #define MHU3_FCH_CFG0_FCH_WS_BITSTART 28
@@ -178,7 +178,7 @@ struct mhu3_mbx_mdbcw_reg {
 /* Fast Channel Configuration 0 Fast Channel Word Size Mask */
 #define MHU3_FCH_CFG0_FCH_WS_MASK \
     (MHU3_MASK_GENERATE( \
-        MHU3_FCH_CFG0_FCH_WS_BITSTART, MHU3_FCH_CFG0_FCH_WS_LEN))
+	MHU3_FCH_CFG0_FCH_WS_BITSTART, MHU3_FCH_CFG0_FCH_WS_LEN))
 
 /* Feature Support 0 Doorbell Extension Support Start Bit */
 #define MHU3_FEAT_SPT0_DBE_SPT_BITSTART 3
@@ -187,7 +187,7 @@ struct mhu3_mbx_mdbcw_reg {
 /* Feature Support 0 Doorbell Extension Support Mask */
 #define MHU3_FEAT_SPT0_DBE_SPT_MASK \
     (MHU3_MASK_GENERATE( \
-        MHU3_FEAT_SPT0_DBE_SPT_BITSTART, MHU3_FEAT_SPT0_DBE_SPT_LEN))
+	MHU3_FEAT_SPT0_DBE_SPT_BITSTART, MHU3_FEAT_SPT0_DBE_SPT_LEN))
 
 /* Feature Support 0 FIFO Extension Support Start Bit */
 #define MHU3_FEAT_SPT0_FE_SPT_BITSTART 7
@@ -196,7 +196,7 @@ struct mhu3_mbx_mdbcw_reg {
 /* Feature Support 0 FIFO Extension Support Mask */
 #define MHU3_FEAT_SPT0_FE_SPT_MASK \
     (MHU3_MASK_GENERATE( \
-        MHU3_FEAT_SPT0_FE_SPT_BITSTART, MHU3_FEAT_SPT0_FE_SPT_LEN))
+	MHU3_FEAT_SPT0_FE_SPT_BITSTART, MHU3_FEAT_SPT0_FE_SPT_LEN))
 
 /* Feature Support 0 Fast Channel Extension Support Start Bit */
 #define MHU3_FEAT_SPT0_FCE_SPT_BITSTART 11
@@ -205,7 +205,7 @@ struct mhu3_mbx_mdbcw_reg {
 /* Feature Support 0 Fast Channel Extension Support Mask */
 #define MHU3_FEAT_SPT0_FCE_SPT_MASK \
     (MHU3_MASK_GENERATE( \
-        MHU3_FEAT_SPT0_FCE_SPT_BITSTART, MHU3_FEAT_SPT0_FCE_SPT_LEN))
+	MHU3_FEAT_SPT0_FCE_SPT_BITSTART, MHU3_FEAT_SPT0_FCE_SPT_LEN))
 
 /* Feature Support 0 TrustZone Extension Support Start Bit */
 #define MHU3_FEAT_SPT0_TZE_SPT_BITSTART 15
@@ -214,7 +214,7 @@ struct mhu3_mbx_mdbcw_reg {
 /* Feature Support 0 TrustZone Extension Support Mask */
 #define MHU3_FEAT_SPT0_TZE_SPT_MASK \
     (MHU3_MASK_GENERATE( \
-        MHU3_FEAT_SPT0_TZE_SPT_BITSTART, MHU3_FEAT_SPT0_TZE_SPT_LEN))
+	MHU3_FEAT_SPT0_TZE_SPT_BITSTART, MHU3_FEAT_SPT0_TZE_SPT_LEN))
 
 /* Feature Support 0 Realm Management Extension Support Start Bit */
 #define MHU3_FEAT_SPT0_RME_SPT_BITSTART 19
@@ -223,6 +223,6 @@ struct mhu3_mbx_mdbcw_reg {
 /* Feature Support 0 Realm Management Extension Support Mask */
 #define MHU3_FEAT_SPT0_RME_SPT_MASK \
     (MHU3_MASK_GENERATE( \
-        MHU3_FEAT_SPT0_RME_SPT_BITSTART, MHU3_FEAT_SPT0_RME_SPT_LEN))
+	MHU3_FEAT_SPT0_RME_SPT_BITSTART, MHU3_FEAT_SPT0_RME_SPT_LEN))
 
 #endif /* INTERNAL_MHU3_H */
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/mhu3/include/mod_mhu3.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/mhu3/include/mod_mhu3.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/mhu3/include/mod_mhu3.h
@@ -134,10 +134,10 @@ struct mod_mhu3_channel_config {
     enum mod_mhu3_channel_type type;
     /*! Configuration of the specified channel type */
     union {
-        /*! Doorbell channel configuration */
-        struct mod_mhu3_dbch_config dbch;
-        /*! Fast channel configuration */
-        struct mod_mhu3_fc_config fch;
+	/*! Doorbell channel configuration */
+	struct mod_mhu3_dbch_config dbch;
+	/*! Fast channel configuration */
+	struct mod_mhu3_fc_config fch;
     };
 };
 
@@ -196,15 +196,15 @@ struct mod_mhu3_device_config {
 #define MOD_MHU3_INIT_DBCH( \
     PBX_CH_NUMBER, PBX_FLAG_POS, MBX_CH_NUMBER, MBX_FLAG_POS) \
     { \
-        .type = MOD_MHU3_CHANNEL_TYPE_DBCH, \
-        { \
-            .dbch = { \
-                .pbx_channel = PBX_CH_NUMBER, \
-                .pbx_flag_pos = PBX_FLAG_POS, \
-                .mbx_channel = MBX_CH_NUMBER, \
-                .mbx_flag_pos = MBX_FLAG_POS, \
-            }, \
-        } \
+	.type = MOD_MHU3_CHANNEL_TYPE_DBCH, \
+	{ \
+	    .dbch = { \
+		.pbx_channel = PBX_CH_NUMBER, \
+		.pbx_flag_pos = PBX_FLAG_POS, \
+		.mbx_channel = MBX_CH_NUMBER, \
+		.mbx_flag_pos = MBX_FLAG_POS, \
+	    }, \
+	} \
     }
 
 /*!
@@ -226,14 +226,14 @@ struct mod_mhu3_device_config {
  */
 #define MOD_MHU3_INIT_FCH(FCH_IDX, FCH_GROUP_NUM, FCH_DIRECTION) \
     { \
-        .type = MOD_MHU3_CHANNEL_TYPE_FCH, \
-        { \
-            .fch = { \
-                .idx = FCH_IDX, \
-                .grp_num = FCH_GROUP_NUM, \
-                .direction = FCH_DIRECTION, \
-            }, \
-        } \
+	.type = MOD_MHU3_CHANNEL_TYPE_FCH, \
+	{ \
+	    .fch = { \
+		.idx = FCH_IDX, \
+		.grp_num = FCH_GROUP_NUM, \
+		.direction = FCH_DIRECTION, \
+	    }, \
+	} \
     }
 
 /*!
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/mhu3/src/mod_mhu3.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/mhu3/src/mod_mhu3.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/mhu3/src/mod_mhu3.c
@@ -85,18 +85,18 @@ static void mhu3_isr(void)
 
     status = fwk_interrupt_get_current(&interrupt);
     if (status != FWK_SUCCESS) {
-        return;
+	return;
     }
 
     for (device_idx = 0U; device_idx < mhu3_ctx.device_count; device_idx++) {
-        device_ctx = &mhu3_ctx.device_ctx_table[device_idx];
-        if (device_ctx->config->irq == interrupt) {
-            break;
-        }
+	device_ctx = &mhu3_ctx.device_ctx_table[device_idx];
+	if (device_ctx->config->irq == interrupt) {
+	    break;
+	}
     }
 
     if (device_idx >= mhu3_ctx.device_count) {
-        return;
+	return;
     }
     mbx_reg = (struct mhu3_mbx_reg *)device_ctx->config->in;
     /*
@@ -104,64 +104,64 @@ static void mhu3_isr(void)
      * it every time we have a doorbell channel inside the loop
      */
     mdbcw_reg = (struct mhu3_mbx_mdbcw_reg
-                     *)((uint8_t *)mbx_reg + MHU3_MBX_MDBCW_PAGE_OFFSET);
+		     *)((uint8_t *)mbx_reg + MHU3_MBX_MDBCW_PAGE_OFFSET);
 
     while (pending < device_ctx->channels_count) {
-        channel = &(device_ctx->config->channels[pending]);
-        channel_ctx = &(device_ctx->channel_ctx_table[pending]);
-        switch (channel->type) {
-        case MOD_MHU3_CHANNEL_TYPE_DBCH:
-            /*
-             * This implementation will not work if configured hardware(MHU3)
-             * requires to support more than 32 doorbell channels.
-             *
-             * Status of the interrupts of doorbell channel
-             * is read using MBX_DBCH_INT_ST<n> register where
-             * n = 0..3, that is, each 32 bits of MBX_DBCH_INT_ST<n> will
-             * indicate status of each corresponding doorbell channel
-             * 0b1 indicates interrupt pending.
-             *
-             * NOTE: We only check MBX_DBCH_INT_ST[0] because
-             * although MHUv3 supports upto 128 channels, it is not
-             * expected hardware to be configured more than 32 channels.
-             */
-            if (((1u << channel->dbch.mbx_channel) &
-                 mbx_reg->MBX_DBCH_INT_ST[0]) != 0u) {
-                /*
-                 * Clear Doorbell flag, we should clear only the flag(bit) which
-                 * is set. However, we are using only one flag(bit) of
-                 * corresponding doorbell channel for communication.
-                 */
-                mdbcw_reg[channel->dbch.mbx_channel].MDBCW_CLR |=
-                    (1UL << channel->dbch.mbx_flag_pos);
-                if (channel_ctx->transport_id_bound) {
-                    channel_ctx->transport_api->signal_message(
-                        channel_ctx->transport_id);
-                }
-            }
-            break;
-
-        case MOD_MHU3_CHANNEL_TYPE_FCH:
-            if (((mbx_reg->MBX_FCH_GRP_INT_ST[channel->fch.grp_num] >>
-                  channel->fch.idx) &
-                 1u) != 0u) {
-                channel_ctx = &(device_ctx->channel_ctx_table[pending]);
-                /*
-                 * We only check for whether the callback is NULL as the
-                 * register callback function checks for both callback and
-                 * callback_param before registering so that we can save a few
-                 * cycles here.
-                 */
-                if (channel_ctx->callback != NULL) {
-                    channel_ctx->callback(channel_ctx->callback_param);
-                }
-            }
-            break;
-
-        default:
-            break;
-        }
-        pending++;
+	channel = &(device_ctx->config->channels[pending]);
+	channel_ctx = &(device_ctx->channel_ctx_table[pending]);
+	switch (channel->type) {
+	case MOD_MHU3_CHANNEL_TYPE_DBCH:
+	    /*
+	     * This implementation will not work if configured hardware(MHU3)
+	     * requires to support more than 32 doorbell channels.
+	     *
+	     * Status of the interrupts of doorbell channel
+	     * is read using MBX_DBCH_INT_ST<n> register where
+	     * n = 0..3, that is, each 32 bits of MBX_DBCH_INT_ST<n> will
+	     * indicate status of each corresponding doorbell channel
+	     * 0b1 indicates interrupt pending.
+	     *
+	     * NOTE: We only check MBX_DBCH_INT_ST[0] because
+	     * although MHUv3 supports upto 128 channels, it is not
+	     * expected hardware to be configured more than 32 channels.
+	     */
+	    if (((1u << channel->dbch.mbx_channel) &
+		 mbx_reg->MBX_DBCH_INT_ST[0]) != 0u) {
+		/*
+		 * Clear Doorbell flag, we should clear only the flag(bit) which
+		 * is set. However, we are using only one flag(bit) of
+		 * corresponding doorbell channel for communication.
+		 */
+		mdbcw_reg[channel->dbch.mbx_channel].MDBCW_CLR |=
+		    (1UL << channel->dbch.mbx_flag_pos);
+		if (channel_ctx->transport_id_bound) {
+		    channel_ctx->transport_api->signal_message(
+			channel_ctx->transport_id);
+		}
+	    }
+	    break;
+
+	case MOD_MHU3_CHANNEL_TYPE_FCH:
+	    if (((mbx_reg->MBX_FCH_GRP_INT_ST[channel->fch.grp_num] >>
+		  channel->fch.idx) &
+		 1u) != 0u) {
+		channel_ctx = &(device_ctx->channel_ctx_table[pending]);
+		/*
+		 * We only check for whether the callback is NULL as the
+		 * register callback function checks for both callback and
+		 * callback_param before registering so that we can save a few
+		 * cycles here.
+		 */
+		if (channel_ctx->callback != NULL) {
+		    channel_ctx->callback(channel_ctx->callback_param);
+		}
+	    }
+	    break;
+
+	default:
+	    break;
+	}
+	pending++;
     }
 }
 
@@ -182,26 +182,26 @@ static int mhu3_raise_interrupt(fwk_id_t
 
     switch (channel->type) {
     case MOD_MHU3_CHANNEL_TYPE_DBCH:
-        pdbcw_reg = (struct mhu3_pbx_pdbcw_reg
-                         *)((uint8_t *)pbx_reg + MHU3_PBX_PDBCW_PAGE_OFFSET);
-        dbch_channel = &(channel->dbch);
-        /*
-         * We can use up to 32 flags(bits) per channels for 32 events, however
-         * we are using only 1 flag(bit) per channel
-         */
-        if ((pdbcw_reg[dbch_channel->pbx_channel].PDBCW_ST &
-             0x1u << dbch_channel->pbx_flag_pos) == 0u) {
-            pdbcw_reg[dbch_channel->pbx_channel].PDBCW_SET |= 0x1u
-                << dbch_channel->pbx_flag_pos;
-            status = FWK_SUCCESS;
-        } else {
-            status = FWK_E_STATE;
-        }
-        break;
+	pdbcw_reg = (struct mhu3_pbx_pdbcw_reg
+			 *)((uint8_t *)pbx_reg + MHU3_PBX_PDBCW_PAGE_OFFSET);
+	dbch_channel = &(channel->dbch);
+	/*
+	 * We can use up to 32 flags(bits) per channels for 32 events, however
+	 * we are using only 1 flag(bit) per channel
+	 */
+	if ((pdbcw_reg[dbch_channel->pbx_channel].PDBCW_ST &
+	     0x1u << dbch_channel->pbx_flag_pos) == 0u) {
+	    pdbcw_reg[dbch_channel->pbx_channel].PDBCW_SET |= 0x1u
+		<< dbch_channel->pbx_flag_pos;
+	    status = FWK_SUCCESS;
+	} else {
+	    status = FWK_E_STATE;
+	}
+	break;
 
     default:
-        status = FWK_E_PARAM;
-        break;
+	status = FWK_E_PARAM;
+	break;
     }
 
     return status;
@@ -222,7 +222,7 @@ static int mhu3_get_fch(fwk_id_t fch_id,
     uint32_t fch_cfg0;
 
     if (!fwk_module_is_valid_sub_element_id(fch_id) || (fch == NULL)) {
-        return status;
+	return status;
     }
 
     device_ctx = &mhu3_ctx.device_ctx_table[fwk_id_get_element_idx(fch_id)];
@@ -237,45 +237,45 @@ static int mhu3_get_fch(fwk_id_t fch_id,
 
     switch (channel->type) {
     case MOD_MHU3_CHANNEL_TYPE_FCH:
-        fch_channel = &(channel->fch);
-        if (fch_channel->direction == MOD_MHU3_FCH_DIR_OUT) {
-            fcw_regs = (uintptr_t)pbx_reg + MHU3_PBX_PFCW_PAGE_OFFSET;
-            fcw_regs_target =
-                (uintptr_t)mbx_reg_target + MHU3_MBX_MDFCW_PAGE_OFFSET;
-            fch_cfg0 = pbx_reg->PBX_FCH_CFG0;
-
-            status = FWK_SUCCESS;
-        } else if (fch_channel->direction == MOD_MHU3_FCH_DIR_IN) {
-            fcw_regs = (uintptr_t)mbx_reg + MHU3_MBX_MDFCW_PAGE_OFFSET;
-            fcw_regs_target =
-                (uintptr_t)pbx_reg_target + MHU3_PBX_PFCW_PAGE_OFFSET;
-            fch_cfg0 = mbx_reg->MBX_FCH_CFG0;
-
-            status = FWK_SUCCESS;
-        }
-
-        if (status == FWK_SUCCESS) {
-            fch_offset = fch_channel->idx +
-                fch_channel->grp_num *
-                    MHU3_MASKED_RECOVER(
-                        fch_cfg0 & MHU3_FCH_CFG0_NUM_FCH_PER_GRP_MASK,
-                        MHU3_FCH_CFG0_NUM_FCH_PER_GRP_BITSTART,
-                        MHU3_FCH_CFG0_NUM_FCH_PER_GRP_LEN);
-            fch->length = (MHU3_MASKED_RECOVER(
-                               fch_cfg0 & MHU3_FCH_CFG0_FCH_WS_MASK,
-                               MHU3_FCH_CFG0_FCH_WS_BITSTART,
-                               MHU3_FCH_CFG0_FCH_WS_LEN) == FCH_WS_32BIT) ?
-                sizeof(uint32_t) :
-                sizeof(uint64_t);
-            fch_offset *= fch->length;
-            fch->local_view_address = fcw_regs + fch_offset;
-            fch->target_view_address = fcw_regs_target + fch_offset;
-        }
+	fch_channel = &(channel->fch);
+	if (fch_channel->direction == MOD_MHU3_FCH_DIR_OUT) {
+	    fcw_regs = (uintptr_t)pbx_reg + MHU3_PBX_PFCW_PAGE_OFFSET;
+	    fcw_regs_target =
+		(uintptr_t)mbx_reg_target + MHU3_MBX_MDFCW_PAGE_OFFSET;
+	    fch_cfg0 = pbx_reg->PBX_FCH_CFG0;
+
+	    status = FWK_SUCCESS;
+	} else if (fch_channel->direction == MOD_MHU3_FCH_DIR_IN) {
+	    fcw_regs = (uintptr_t)mbx_reg + MHU3_MBX_MDFCW_PAGE_OFFSET;
+	    fcw_regs_target =
+		(uintptr_t)pbx_reg_target + MHU3_PBX_PFCW_PAGE_OFFSET;
+	    fch_cfg0 = mbx_reg->MBX_FCH_CFG0;
+
+	    status = FWK_SUCCESS;
+	}
+
+	if (status == FWK_SUCCESS) {
+	    fch_offset = fch_channel->idx +
+		fch_channel->grp_num *
+		    MHU3_MASKED_RECOVER(
+			fch_cfg0 & MHU3_FCH_CFG0_NUM_FCH_PER_GRP_MASK,
+			MHU3_FCH_CFG0_NUM_FCH_PER_GRP_BITSTART,
+			MHU3_FCH_CFG0_NUM_FCH_PER_GRP_LEN);
+	    fch->length = (MHU3_MASKED_RECOVER(
+			       fch_cfg0 & MHU3_FCH_CFG0_FCH_WS_MASK,
+			       MHU3_FCH_CFG0_FCH_WS_BITSTART,
+			       MHU3_FCH_CFG0_FCH_WS_LEN) == FCH_WS_32BIT) ?
+		sizeof(uint32_t) :
+		sizeof(uint64_t);
+	    fch_offset *= fch->length;
+	    fch->local_view_address = fcw_regs + fch_offset;
+	    fch->target_view_address = fcw_regs_target + fch_offset;
+	}
 
-        break;
+	break;
 
     default:
-        break;
+	break;
     }
 
     return status;
@@ -291,11 +291,11 @@ static int mhu3_fch_register_callback(
     struct mhu3_channel_ctx *channel_ctx;
 
     if ((fch_callback == NULL) || ((void *)param == NULL)) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     if (!fwk_module_is_valid_sub_element_id(fch_id)) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     device_ctx = &mhu3_ctx.device_ctx_table[fwk_id_get_element_idx(fch_id)];
@@ -327,11 +327,11 @@ static int mhu3_init(
     const void *unused)
 {
     if (device_count == 0U) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     mhu3_ctx.device_ctx_table =
-        fwk_mm_calloc(device_count, sizeof(mhu3_ctx.device_ctx_table[0]));
+	fwk_mm_calloc(device_count, sizeof(mhu3_ctx.device_ctx_table[0]));
 
     mhu3_ctx.device_count = device_count;
 
@@ -345,7 +345,7 @@ static int mhu3_device_init(
 {
     int status = FWK_SUCCESS;
     struct mod_mhu3_device_config *config =
-        (struct mod_mhu3_device_config *)data;
+	(struct mod_mhu3_device_config *)data;
     struct mhu3_device_ctx *device_ctx;
     struct mhu3_mbx_reg *mbx_reg;
     struct mhu3_pbx_reg *pbx_reg;
@@ -354,7 +354,7 @@ static int mhu3_device_init(
     unsigned int channel_num;
 
     if ((config->in == 0u) || (config->out == 0u)) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     device_ctx = &mhu3_ctx.device_ctx_table[fwk_id_get_element_idx(device_id)];
@@ -362,7 +362,7 @@ static int mhu3_device_init(
     device_ctx->config = config;
     device_ctx->channels_count = sub_element_count;
     device_ctx->channel_ctx_table = fwk_mm_calloc(
-        sub_element_count, sizeof(device_ctx->channel_ctx_table[0]));
+	sub_element_count, sizeof(device_ctx->channel_ctx_table[0]));
 
     mbx_reg = (struct mhu3_mbx_reg *)device_ctx->config->in;
     pbx_reg = (struct mhu3_pbx_reg *)device_ctx->config->out;
@@ -372,72 +372,72 @@ static int mhu3_device_init(
      * are correct according to Hardware
      */
     for (channel_num = 0u; channel_num < sub_element_count; channel_num++) {
-        channel = &(device_ctx->config->channels[channel_num]);
-        if (channel->type == MOD_MHU3_CHANNEL_TYPE_FCH) {
-            if (channel->fch.direction == MOD_MHU3_FCH_DIR_IN) {
-                fch_cfg0 = mbx_reg->MBX_FCH_CFG0;
-                ext_suprt = mbx_reg->MBX_FEAT_SPT0;
-            } else {
-                fch_cfg0 = pbx_reg->PBX_FCH_CFG0;
-                ext_suprt = pbx_reg->PBX_FEAT_SPT0;
-            }
-            if (((ext_suprt & MHU3_FEAT_SPT0_FCE_SPT_MASK) == 0u) ||
-                (channel->fch.grp_num >
-                 MHU3_MASKED_RECOVER(
-                     fch_cfg0 & MHU3_FCH_CFG0_NUM_FCG_MASK,
-                     MHU3_FCH_CFG0_NUM_FCG_BITSTART,
-                     MHU3_FCH_CFG0_NUM_FCG_LEN))) {
-                status = FWK_E_PARAM;
-                break;
-            }
-            if ((fch_cfg0 & MHU3_FCH_CFG0_NUM_FCH_PER_GRP_MASK) == 0u) {
-                if (channel->fch.idx >
-                    MHU3_MASKED_RECOVER(
-                        fch_cfg0 & MHU3_FCH_CFG0_NUM_FCH_MASK,
-                        MHU3_FCH_CFG0_NUM_FCH_BITSTART,
-                        MHU3_FCH_CFG0_NUM_FCH_LEN)) {
-                    status = FWK_E_PARAM;
-                    break;
-                }
-            } else {
-                if (channel->fch.idx >
-                    MHU3_MASKED_RECOVER(
-                        fch_cfg0 & MHU3_FCH_CFG0_NUM_FCH_PER_GRP_MASK,
-                        MHU3_FCH_CFG0_NUM_FCH_PER_GRP_BITSTART,
-                        MHU3_FCH_CFG0_NUM_FCH_PER_GRP_LEN)) {
-                    status = FWK_E_PARAM;
-                    break;
-                }
-                if ((channel->fch.grp_num ==
-                     MHU3_MASKED_RECOVER(
-                         fch_cfg0 & MHU3_FCH_CFG0_NUM_FCG_MASK,
-                         MHU3_FCH_CFG0_NUM_FCG_BITSTART,
-                         MHU3_FCH_CFG0_NUM_FCG_LEN)) &&
-                    ((fch_cfg0 & MHU3_FCH_CFG0_NUM_FCG_MASK) != 0u)) {
-                    /*
-                     * Verify that the last Group contains an appropiate amounts
-                     * of Channels.
-                     */
-                    if (channel->fch.idx >=
-                        (MHU3_MASKED_RECOVER(
-                             fch_cfg0 & MHU3_FCH_CFG0_NUM_FCH_MASK,
-                             MHU3_FCH_CFG0_NUM_FCH_BITSTART,
-                             MHU3_FCH_CFG0_NUM_FCH_LEN) -
-                         (MHU3_MASKED_RECOVER(
-                              fch_cfg0 & MHU3_FCH_CFG0_NUM_FCG_MASK,
-                              MHU3_FCH_CFG0_NUM_FCG_BITSTART,
-                              MHU3_FCH_CFG0_NUM_FCG_LEN) -
-                          1u) *
-                             MHU3_MASKED_RECOVER(
-                                 fch_cfg0 & MHU3_FCH_CFG0_NUM_FCH_PER_GRP_MASK,
-                                 MHU3_FCH_CFG0_NUM_FCH_PER_GRP_BITSTART,
-                                 MHU3_FCH_CFG0_NUM_FCH_PER_GRP_LEN))) {
-                        status = FWK_E_PARAM;
-                        break;
-                    }
-                }
-            }
-        }
+	channel = &(device_ctx->config->channels[channel_num]);
+	if (channel->type == MOD_MHU3_CHANNEL_TYPE_FCH) {
+	    if (channel->fch.direction == MOD_MHU3_FCH_DIR_IN) {
+		fch_cfg0 = mbx_reg->MBX_FCH_CFG0;
+		ext_suprt = mbx_reg->MBX_FEAT_SPT0;
+	    } else {
+		fch_cfg0 = pbx_reg->PBX_FCH_CFG0;
+		ext_suprt = pbx_reg->PBX_FEAT_SPT0;
+	    }
+	    if (((ext_suprt & MHU3_FEAT_SPT0_FCE_SPT_MASK) == 0u) ||
+		(channel->fch.grp_num >
+		 MHU3_MASKED_RECOVER(
+		     fch_cfg0 & MHU3_FCH_CFG0_NUM_FCG_MASK,
+		     MHU3_FCH_CFG0_NUM_FCG_BITSTART,
+		     MHU3_FCH_CFG0_NUM_FCG_LEN))) {
+		status = FWK_E_PARAM;
+		break;
+	    }
+	    if ((fch_cfg0 & MHU3_FCH_CFG0_NUM_FCH_PER_GRP_MASK) == 0u) {
+		if (channel->fch.idx >
+		    MHU3_MASKED_RECOVER(
+			fch_cfg0 & MHU3_FCH_CFG0_NUM_FCH_MASK,
+			MHU3_FCH_CFG0_NUM_FCH_BITSTART,
+			MHU3_FCH_CFG0_NUM_FCH_LEN)) {
+		    status = FWK_E_PARAM;
+		    break;
+		}
+	    } else {
+		if (channel->fch.idx >
+		    MHU3_MASKED_RECOVER(
+			fch_cfg0 & MHU3_FCH_CFG0_NUM_FCH_PER_GRP_MASK,
+			MHU3_FCH_CFG0_NUM_FCH_PER_GRP_BITSTART,
+			MHU3_FCH_CFG0_NUM_FCH_PER_GRP_LEN)) {
+		    status = FWK_E_PARAM;
+		    break;
+		}
+		if ((channel->fch.grp_num ==
+		     MHU3_MASKED_RECOVER(
+			 fch_cfg0 & MHU3_FCH_CFG0_NUM_FCG_MASK,
+			 MHU3_FCH_CFG0_NUM_FCG_BITSTART,
+			 MHU3_FCH_CFG0_NUM_FCG_LEN)) &&
+		    ((fch_cfg0 & MHU3_FCH_CFG0_NUM_FCG_MASK) != 0u)) {
+		    /*
+		     * Verify that the last Group contains an appropiate amounts
+		     * of Channels.
+		     */
+		    if (channel->fch.idx >=
+			(MHU3_MASKED_RECOVER(
+			     fch_cfg0 & MHU3_FCH_CFG0_NUM_FCH_MASK,
+			     MHU3_FCH_CFG0_NUM_FCH_BITSTART,
+			     MHU3_FCH_CFG0_NUM_FCH_LEN) -
+			 (MHU3_MASKED_RECOVER(
+			      fch_cfg0 & MHU3_FCH_CFG0_NUM_FCG_MASK,
+			      MHU3_FCH_CFG0_NUM_FCG_BITSTART,
+			      MHU3_FCH_CFG0_NUM_FCG_LEN) -
+			  1u) *
+			     MHU3_MASKED_RECOVER(
+				 fch_cfg0 & MHU3_FCH_CFG0_NUM_FCH_PER_GRP_MASK,
+				 MHU3_FCH_CFG0_NUM_FCH_PER_GRP_BITSTART,
+				 MHU3_FCH_CFG0_NUM_FCH_PER_GRP_LEN))) {
+			status = FWK_E_PARAM;
+			break;
+		    }
+		}
+	    }
+	}
     }
 
     return status;
@@ -451,26 +451,26 @@ static int mhu3_bind(fwk_id_t id, unsign
     unsigned int channel_idx;
 
     if ((round == 1) && fwk_id_is_type(id, FWK_ID_TYPE_ELEMENT)) {
-        device_ctx = &mhu3_ctx.device_ctx_table[fwk_id_get_element_idx(id)];
+	device_ctx = &mhu3_ctx.device_ctx_table[fwk_id_get_element_idx(id)];
 
-        for (channel_idx = 0; channel_idx < device_ctx->channels_count;
-             channel_idx++) {
-            channel_ctx = &device_ctx->channel_ctx_table[channel_idx];
-            if (channel_ctx->transport_id_bound) {
-                status = fwk_module_bind(
-                    channel_ctx->transport_id,
-                    FWK_ID_API(
-                        FWK_MODULE_IDX_TRANSPORT,
-                        MOD_TRANSPORT_API_IDX_DRIVER_INPUT),
-                    channel_ctx->transport_api);
-
-                if (status != FWK_SUCCESS) {
-                    /* Unable to bind back to TRANSPORT channel */
-                    fwk_unexpected();
-                    return status;
-                }
-            }
-        }
+	for (channel_idx = 0; channel_idx < device_ctx->channels_count;
+	     channel_idx++) {
+	    channel_ctx = &device_ctx->channel_ctx_table[channel_idx];
+	    if (channel_ctx->transport_id_bound) {
+		status = fwk_module_bind(
+		    channel_ctx->transport_id,
+		    FWK_ID_API(
+			FWK_MODULE_IDX_TRANSPORT,
+			MOD_TRANSPORT_API_IDX_DRIVER_INPUT),
+		    channel_ctx->transport_api);
+
+		if (status != FWK_SUCCESS) {
+		    /* Unable to bind back to TRANSPORT channel */
+		    fwk_unexpected();
+		    return status;
+		}
+	    }
+	}
     }
 
     return FWK_SUCCESS;
@@ -488,12 +488,12 @@ static int mhu3_process_bind_request(
     unsigned int ch_idx;
 
     if (!fwk_id_is_type(target_id, FWK_ID_TYPE_SUB_ELEMENT)) {
-        /*
-         * Something tried to bind to the module or an element. Only binding to
-         * a slot (sub-element) is allowed.
-         */
-        fwk_unexpected();
-        return FWK_E_ACCESS;
+	/*
+	 * Something tried to bind to the module or an element. Only binding to
+	 * a slot (sub-element) is allowed.
+	 */
+	fwk_unexpected();
+	return FWK_E_ACCESS;
     }
 
     device_ctx = &mhu3_ctx.device_ctx_table[fwk_id_get_element_idx(target_id)];
@@ -504,15 +504,15 @@ static int mhu3_process_bind_request(
 
     switch (api_id_type) {
     case MOD_MHU3_API_IDX_TRANSPORT_DRIVER:
-        channel_ctx->transport_id = source_id;
-        channel_ctx->transport_id_bound = true;
-        *api = &mhu3_mod_transport_driver_api;
-        break;
+	channel_ctx->transport_id = source_id;
+	channel_ctx->transport_id_bound = true;
+	*api = &mhu3_mod_transport_driver_api;
+	break;
 
     default:
-        /* Invalid config */
-        fwk_unexpected();
-        return FWK_E_PARAM;
+	/* Invalid config */
+	fwk_unexpected();
+	return FWK_E_PARAM;
     }
 
     return FWK_SUCCESS;
@@ -527,7 +527,7 @@ static int mhu3_start(fwk_id_t id)
     struct mhu3_mbx_reg *mbx;
 
     if (fwk_id_get_type(id) == FWK_ID_TYPE_MODULE) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     device_ctx = &mhu3_ctx.device_ctx_table[fwk_id_get_element_idx(id)];
@@ -535,28 +535,28 @@ static int mhu3_start(fwk_id_t id)
     pbx = (struct mhu3_pbx_reg *)device_ctx->config->out;
     mbx = (struct mhu3_mbx_reg *)device_ctx->config->in;
     if (pbx != NULL) {
-        pbx->PBX_CTRL |= MHU3_OP_REQ;
+	pbx->PBX_CTRL |= MHU3_OP_REQ;
     }
     if (mbx != NULL) {
-        mbx->MBX_CTRL |= MHU3_OP_REQ;
+	mbx->MBX_CTRL |= MHU3_OP_REQ;
     }
 
     status = fwk_interrupt_set_isr(device_ctx->config->irq, &mhu3_isr);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     status = fwk_interrupt_enable(device_ctx->config->irq);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     for (i = 0u; i < device_ctx->channels_count; i++) {
-        if (device_ctx->config->channels[i].type == MOD_MHU3_CHANNEL_TYPE_FCH) {
-            mbx->MBX_FCH_CTRL |= SCP_MHU3_MBX_INT_EN;
-            mbx->MBX_FCG_INT_EN |= 1u
-                << device_ctx->config->channels[i].fch.grp_num;
-        }
+	if (device_ctx->config->channels[i].type == MOD_MHU3_CHANNEL_TYPE_FCH) {
+	    mbx->MBX_FCH_CTRL |= SCP_MHU3_MBX_INT_EN;
+	    mbx->MBX_FCG_INT_EN |= 1u
+		<< device_ctx->config->channels[i].fch.grp_num;
+	}
     }
 
     return FWK_SUCCESS;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/mhu3/test/config_mhu3.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/mhu3/test/config_mhu3.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/mhu3/test/config_mhu3.h
@@ -68,14 +68,14 @@ struct mod_mhu3_channel_config device_1_
 /* Provide a fake device info */
 static const struct fwk_element element_table[MHU3_DEVICE_IDX_COUNT+1] = {
     [MHU3_DEVICE_IDX_DEVICE_1] = {
-        .name = "",
-        .sub_element_count = FAKE_DEVICE_1_NUM_CH,
-        .data = &(struct mod_mhu3_device_config) {
-            .irq = (unsigned int) MHU3_FAKE_IRQ_COMBINED,
-            .in = (uintptr_t)NULL,
-            .out = (uintptr_t)NULL,
-            .channels = &device_1_channel_config[0],
-        },
+	.name = "",
+	.sub_element_count = FAKE_DEVICE_1_NUM_CH,
+	.data = &(struct mod_mhu3_device_config) {
+	    .irq = (unsigned int) MHU3_FAKE_IRQ_COMBINED,
+	    .in = (uintptr_t)NULL,
+	    .out = (uintptr_t)NULL,
+	    .channels = &device_1_channel_config[0],
+	},
     },
     [MHU3_DEVICE_IDX_COUNT] = { 0 },
 };
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/mhu3/test/mod_mhu3_unit_test.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/mhu3/test/mod_mhu3_unit_test.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/mhu3/test/mod_mhu3_unit_test.c
@@ -65,9 +65,9 @@ void test_mhu3_raise_interrupt_wrong_cha
     channel->type = MHU3_TEST_INVALID_VALUE;
 
     fwk_id_t ch_id = FWK_ID_SUB_ELEMENT(
-        FWK_MODULE_IDX_MHU3,
-        MHU3_DEVICE_IDX_DEVICE_1,
-        FAKE_DEVICE_1_CHANNEL_DBCH_0);
+	FWK_MODULE_IDX_MHU3,
+	MHU3_DEVICE_IDX_DEVICE_1,
+	FAKE_DEVICE_1_CHANNEL_DBCH_0);
 
     status = mhu3_raise_interrupt(ch_id);
     TEST_ASSERT(status == FWK_E_PARAM);
@@ -86,9 +86,9 @@ void test_mhu3_raise_interrupt_valid_cas
     int status;
 
     fwk_id_t ch_id = FWK_ID_SUB_ELEMENT(
-        FWK_MODULE_IDX_MHU3,
-        MHU3_DEVICE_IDX_DEVICE_1,
-        FAKE_DEVICE_1_CHANNEL_DBCH_0_IDX);
+	FWK_MODULE_IDX_MHU3,
+	MHU3_DEVICE_IDX_DEVICE_1,
+	FAKE_DEVICE_1_CHANNEL_DBCH_0_IDX);
 
     status = mhu3_raise_interrupt(ch_id);
     TEST_ASSERT(status == FWK_SUCCESS);
@@ -121,11 +121,11 @@ void test_mhu3_bind_round_1_success(void
 
     device_ctx = &mhu3_ctx.device_ctx_table[MHU3_DEVICE_IDX_DEVICE_1];
     channel_ctx =
-        &device_ctx->channel_ctx_table[FAKE_DEVICE_1_CHANNEL_DBCH_0_IDX];
+	&device_ctx->channel_ctx_table[FAKE_DEVICE_1_CHANNEL_DBCH_0_IDX];
     fwk_module_bind_ExpectAnyArgsAndReturn(FWK_SUCCESS);
     channel_ctx->transport_id_bound = true;
     fwk_id_t device_id =
-        FWK_ID_ELEMENT(FWK_MODULE_IDX_MHU3, MHU3_DEVICE_IDX_DEVICE_1);
+	FWK_ID_ELEMENT(FWK_MODULE_IDX_MHU3, MHU3_DEVICE_IDX_DEVICE_1);
 
     status = mhu3_bind(device_id, 1);
     TEST_ASSERT(status == FWK_SUCCESS);
@@ -146,13 +146,13 @@ void test_mhu3_bind_fail(void)
 
     device_ctx = &mhu3_ctx.device_ctx_table[MHU3_DEVICE_IDX_DEVICE_1];
     channel_ctx =
-        &device_ctx->channel_ctx_table[FAKE_DEVICE_1_CHANNEL_DBCH_0_IDX];
+	&device_ctx->channel_ctx_table[FAKE_DEVICE_1_CHANNEL_DBCH_0_IDX];
 
     /* Force fwk_module_bind to fail */
     fwk_module_bind_ExpectAnyArgsAndReturn(FWK_E_PARAM);
     channel_ctx->transport_id_bound = true;
     fwk_id_t device_id =
-        FWK_ID_ELEMENT(FWK_MODULE_IDX_MHU3, MHU3_DEVICE_IDX_DEVICE_1);
+	FWK_ID_ELEMENT(FWK_MODULE_IDX_MHU3, MHU3_DEVICE_IDX_DEVICE_1);
 
     status = mhu3_bind(device_id, 1);
     channel_ctx->transport_id_bound = false;
@@ -300,13 +300,13 @@ void test_mhu3_get_fch_invalid_channel_t
      */
     fwk_module_is_valid_sub_element_id_ExpectAnyArgsAndReturn(true);
     id = FWK_ID_SUB_ELEMENT(
-        FWK_MODULE_IDX_MHU3,
-        MHU3_DEVICE_IDX_DEVICE_1,
-        FAKE_DEVICE_1_CHANNEL_DBCH_0_IDX); /*
-                                            * Note. no
-                                            * FAKE_DEVICE_1_CHANNEL_DBCH_0_IDX
-                                            * is not a fast channel
-                                            */
+	FWK_MODULE_IDX_MHU3,
+	MHU3_DEVICE_IDX_DEVICE_1,
+	FAKE_DEVICE_1_CHANNEL_DBCH_0_IDX); /*
+					    * Note. no
+					    * FAKE_DEVICE_1_CHANNEL_DBCH_0_IDX
+					    * is not a fast channel
+					    */
 
     status = mhu3_get_fch(id, &fch);
     TEST_ASSERT(status == FWK_E_PARAM);
@@ -324,9 +324,9 @@ void test_mhu3_get_fch_null_fch_addr(voi
      */
     fwk_module_is_valid_sub_element_id_ExpectAnyArgsAndReturn(true);
     id = FWK_ID_SUB_ELEMENT(
-        FWK_MODULE_IDX_MHU3,
-        MHU3_DEVICE_IDX_DEVICE_1,
-        FAKE_DEVICE_1_CHANNEL_FCH_0_IN_IDX);
+	FWK_MODULE_IDX_MHU3,
+	MHU3_DEVICE_IDX_DEVICE_1,
+	FAKE_DEVICE_1_CHANNEL_FCH_0_IN_IDX);
 
     status = mhu3_get_fch(id, null_fch_addr);
     TEST_ASSERT(status == FWK_E_PARAM);
@@ -341,9 +341,9 @@ void test_mhu3_get_fch_valid_fch_dir_in(
     /* Success case, send valid id, address and fast channel direction (in) */
     fwk_module_is_valid_sub_element_id_ExpectAnyArgsAndReturn(true);
     id = FWK_ID_SUB_ELEMENT(
-        FWK_MODULE_IDX_MHU3,
-        MHU3_DEVICE_IDX_DEVICE_1,
-        FAKE_DEVICE_1_CHANNEL_FCH_0_IN_IDX);
+	FWK_MODULE_IDX_MHU3,
+	MHU3_DEVICE_IDX_DEVICE_1,
+	FAKE_DEVICE_1_CHANNEL_FCH_0_IN_IDX);
 
     status = mhu3_get_fch(id, &fch);
     TEST_ASSERT(status == FWK_SUCCESS);
@@ -358,9 +358,9 @@ void test_mhu3_get_fch_valid_fch_dir_out
     /* Success case, send valid id, address and fast channel direction (out) */
     fwk_module_is_valid_sub_element_id_ExpectAnyArgsAndReturn(true);
     id = FWK_ID_SUB_ELEMENT(
-        FWK_MODULE_IDX_MHU3,
-        MHU3_DEVICE_IDX_DEVICE_1,
-        FAKE_DEVICE_1_CHANNEL_FCH_0_OUT_IDX);
+	FWK_MODULE_IDX_MHU3,
+	MHU3_DEVICE_IDX_DEVICE_1,
+	FAKE_DEVICE_1_CHANNEL_FCH_0_OUT_IDX);
 
     status = mhu3_get_fch(id, &fch);
     TEST_ASSERT(status == FWK_SUCCESS);
@@ -373,14 +373,14 @@ void test_mhu3_fch_register_callback_val
 {
     int status;
     fwk_id_t id = FWK_ID_SUB_ELEMENT_INIT(
-        FWK_MODULE_IDX_MHU3,
-        MHU3_DEVICE_IDX_DEVICE_1,
-        FAKE_DEVICE_1_CHANNEL_FCH_0_OUT_IDX);
+	FWK_MODULE_IDX_MHU3,
+	MHU3_DEVICE_IDX_DEVICE_1,
+	FAKE_DEVICE_1_CHANNEL_FCH_0_OUT_IDX);
 
     /* All valid params, expect success */
     fwk_module_is_valid_sub_element_id_ExpectAnyArgsAndReturn(true);
     status =
-        mhu3_fch_register_callback(id, (uintptr_t)&status, fch_callback_test);
+	mhu3_fch_register_callback(id, (uintptr_t)&status, fch_callback_test);
     TEST_ASSERT(status == FWK_SUCCESS);
 }
 
@@ -392,7 +392,7 @@ void test_mhu3_fch_register_callback_inv
     /* Force fwk_module_is_valid_sub_element_id to return false */
     fwk_module_is_valid_sub_element_id_ExpectAnyArgsAndReturn(false);
     status =
-        mhu3_fch_register_callback(id, (uintptr_t)&status, fch_callback_test);
+	mhu3_fch_register_callback(id, (uintptr_t)&status, fch_callback_test);
     TEST_ASSERT(status == FWK_E_PARAM);
 }
 
@@ -400,9 +400,9 @@ void test_mhu3_fch_register_callback_nul
 {
     int status;
     fwk_id_t id = FWK_ID_SUB_ELEMENT_INIT(
-        FWK_MODULE_IDX_MHU3,
-        MHU3_DEVICE_IDX_DEVICE_1,
-        FAKE_DEVICE_1_CHANNEL_FCH_0_OUT_IDX);
+	FWK_MODULE_IDX_MHU3,
+	MHU3_DEVICE_IDX_DEVICE_1,
+	FAKE_DEVICE_1_CHANNEL_FCH_0_OUT_IDX);
 
     /* Check if it returns FWK_E_PARAM if 'param' is NULL */
     status = mhu3_fch_register_callback(id, (uintptr_t)NULL, fch_callback_test);
@@ -413,9 +413,9 @@ void test_mhu3_fch_register_callback_nul
 {
     int status;
     fwk_id_t id = FWK_ID_SUB_ELEMENT_INIT(
-        FWK_MODULE_IDX_MHU3,
-        MHU3_DEVICE_IDX_DEVICE_1,
-        FAKE_DEVICE_1_CHANNEL_FCH_0_OUT_IDX);
+	FWK_MODULE_IDX_MHU3,
+	MHU3_DEVICE_IDX_DEVICE_1,
+	FAKE_DEVICE_1_CHANNEL_FCH_0_OUT_IDX);
 
     /* Check if it returns FWK_E_PARAM if callback address is NULL */
     fwk_module_is_valid_sub_element_id_ExpectAnyArgsAndReturn(true);
@@ -438,26 +438,26 @@ static int mhu3_fake_init(void)
     struct mod_mhu3_device_config *device_config;
 
     device_config =
-        (struct mod_mhu3_device_config *)element_table[MHU3_DEVICE_IDX_DEVICE_1]
-            .data;
+	(struct mod_mhu3_device_config *)element_table[MHU3_DEVICE_IDX_DEVICE_1]
+	    .data;
 
     /* Allocate some memory for MHU fake device1 PBX/MBX */
     tmp_pbx_base = aligned_alloc(
-        sizeof(unsigned long),
-        sizeof(struct mhu3_pbx_reg) + MHU3_PBX_PDBCW_PAGE_OFFSET +
-            sizeof(struct mhu3_pbx_pdbcw_reg));
+	sizeof(unsigned long),
+	sizeof(struct mhu3_pbx_reg) + MHU3_PBX_PDBCW_PAGE_OFFSET +
+	    sizeof(struct mhu3_pbx_pdbcw_reg));
     tmp_mbx_base = aligned_alloc(
-        sizeof(unsigned long),
-        sizeof(struct mhu3_mbx_reg) + MHU3_MBX_MDBCW_PAGE_OFFSET +
-            sizeof(struct mhu3_mbx_mdbcw_reg));
+	sizeof(unsigned long),
+	sizeof(struct mhu3_mbx_reg) + MHU3_MBX_MDBCW_PAGE_OFFSET +
+	    sizeof(struct mhu3_mbx_mdbcw_reg));
 
     fake_device_1_pbx_base = (struct mhu3_pbx_reg *)tmp_pbx_base;
     fake_device_1_mbx_base = (struct mhu3_mbx_reg *)tmp_mbx_base;
 
     fake_device_1_pdbcw = (struct mhu3_pbx_pdbcw_reg
-                               *)(tmp_pbx_base + MHU3_PBX_PDBCW_PAGE_OFFSET);
+			       *)(tmp_pbx_base + MHU3_PBX_PDBCW_PAGE_OFFSET);
     fake_device_1_mdbcw = (struct mhu3_mbx_mdbcw_reg
-                               *)(tmp_mbx_base + MHU3_MBX_MDBCW_PAGE_OFFSET);
+			       *)(tmp_mbx_base + MHU3_MBX_MDBCW_PAGE_OFFSET);
 
     feat_spt0 = (uint32_t *)(&fake_device_1_mbx_base->MBX_FEAT_SPT0);
     *feat_spt0 |= (1U << MHU3_FEAT_SPT0_FCE_SPT_BITSTART);
@@ -470,17 +470,17 @@ static int mhu3_fake_init(void)
     module_id = FWK_ID_MODULE(FWK_MODULE_IDX_MHU3);
     status = mhu3_init(module_id, MHU3_DEVICE_IDX_COUNT, NULL);
     if (status != FWK_SUCCESS) {
-        printf("[MHU3 UT] Can not execute test cases mhu3_init failed\n");
-        return -1;
+	printf("[MHU3 UT] Can not execute test cases mhu3_init failed\n");
+	return -1;
     }
 
     device_id = FWK_ID_ELEMENT(FWK_MODULE_IDX_MHU3, MHU3_DEVICE_IDX_DEVICE_1);
     sub_element_count = FAKE_DEVICE_1_NUM_CH;
     status = mhu3_device_init(device_id, sub_element_count, device_config);
     if (status != FWK_SUCCESS) {
-        printf(
-            "[MHU3 UT] Can not execute test cases mhu3_device_init failed\n");
-        return -1;
+	printf(
+	    "[MHU3 UT] Can not execute test cases mhu3_device_init failed\n");
+	return -1;
     }
 
     return 0;
@@ -492,7 +492,7 @@ int mhu3_test_main(void)
 
     status = mhu3_fake_init();
     if (status != 0) {
-        return status;
+	return status;
     }
 
     UNITY_BEGIN();
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/mock_clock/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/mock_clock/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/mock_clock/CMakeLists.txt
@@ -8,9 +8,9 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_mock_clock.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_mock_clock.c")
 
 target_link_libraries(${SCP_MODULE_TARGET} PRIVATE module-clock)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/mock_clock/src/mod_mock_clock.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/mock_clock/src/mod_mock_clock.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/mock_clock/src/mod_mock_clock.c
@@ -51,10 +51,10 @@ static int get_rate_entry(
 
     /* Find the entry matching the requested rate */
     for (i = 0; i < ctx->config->rate_count; i++) {
-        if (ctx->config->rate_table[i].rate == target_rate) {
-            *entry = (struct mod_mock_clock_rate *)&ctx->config->rate_table[i];
-            return FWK_SUCCESS;
-        }
+	if (ctx->config->rate_table[i].rate == target_rate) {
+	    *entry = (struct mod_mock_clock_rate *)&ctx->config->rate_table[i];
+	    return FWK_SUCCESS;
+	}
     }
 
     return FWK_E_PARAM;
@@ -76,7 +76,7 @@ static int mod_mock_clock_set_rate(
     ctx = mod_mock_clock_get_ctx(clock_id);
 
     if (ctx->state == MOD_CLOCK_STATE_STOPPED) {
-        return FWK_E_STATE;
+	return FWK_E_STATE;
     }
 
     /*
@@ -86,11 +86,11 @@ static int mod_mock_clock_set_rate(
      */
     status = get_rate_entry(ctx, rate, &rate_entry);
     if (status != FWK_SUCCESS) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     ctx->current_rate_index =
-        (unsigned int)(rate_entry - ctx->config->rate_table);
+	(unsigned int)(rate_entry - ctx->config->rate_table);
 
     ctx->rate_initialized = true;
 
@@ -104,7 +104,7 @@ static int mod_mock_clock_get_rate(fwk_i
     ctx = mod_mock_clock_get_ctx(clock_id);
 
     if (!ctx->rate_initialized) {
-        return FWK_E_STATE;
+	return FWK_E_STATE;
     }
 
     *rate = ctx->config->rate_table[ctx->current_rate_index].rate;
@@ -122,7 +122,7 @@ static int mod_mock_clock_get_rate_from_
     ctx = mod_mock_clock_get_ctx(clock_id);
 
     if (rate_index >= ctx->config->rate_count) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     *rate = ctx->config->rate_table[rate_index].rate;
@@ -196,18 +196,18 @@ static int mod_mock_clock_update_input_r
     ctx = mod_mock_clock_get_ctx(clock_id);
 
     if (ctx->state == MOD_CLOCK_STATE_STOPPED || !ctx->rate_initialized) {
-        return FWK_E_STATE;
+	return FWK_E_STATE;
     }
     /*
      * Performs the same operation as set_rate synchronously
      */
     status = get_rate_entry(ctx, input_rate, &rate_entry);
     if (status != FWK_SUCCESS) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     ctx->current_rate_index =
-        (unsigned int)(rate_entry - ctx->config->rate_table);
+	(unsigned int)(rate_entry - ctx->config->rate_table);
 
     return mod_mock_clock_get_rate(clock_id, output_rate);
 }
@@ -255,21 +255,21 @@ static int mod_mock_clock_element_init(
 
     /* Verify that the rate entries in the lookup table are ordered */
     for (rate_index = 0; rate_index < cfg->rate_count; rate_index++) {
-        rate = cfg->rate_table[rate_index].rate;
+	rate = cfg->rate_table[rate_index].rate;
 
-        /* The rate entries must be in ascending order */
-        if (rate < last_rate) {
-            return FWK_E_DATA;
-        }
-
-        last_rate = rate;
-
-        /* If a default rate is set, let it be so */
-        if (cfg->default_rate == rate) {
-            ctx->current_rate_index = rate_index;
-            ctx->rate_initialized = true;
-            ctx->state = MOD_CLOCK_STATE_RUNNING;
-        }
+	/* The rate entries must be in ascending order */
+	if (rate < last_rate) {
+	    return FWK_E_DATA;
+	}
+
+	last_rate = rate;
+
+	/* If a default rate is set, let it be so */
+	if (cfg->default_rate == rate) {
+	    ctx->current_rate_index = rate_index;
+	    ctx->rate_initialized = true;
+	    ctx->state = MOD_CLOCK_STATE_RUNNING;
+	}
     }
 
     ctx->config = cfg;
@@ -286,20 +286,20 @@ static int mod_mock_clock_process_bind_r
     enum mod_mock_clock_api_type api_type = fwk_id_get_api_idx(api_id);
 
     if (!fwk_id_is_type(target_id, FWK_ID_TYPE_ELEMENT)) {
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 
     switch (api_type) {
     case MOD_MOCK_CLOCK_API_TYPE_DRIVER:
-        *api = &mod_mock_clock_driver_api;
-        return FWK_SUCCESS;
+	*api = &mod_mock_clock_driver_api;
+	return FWK_SUCCESS;
 
     case MOD_MOCK_CLOCK_API_TYPE_RESPONSE_DRIVER:
-        *api = &mod_mock_clock_response_driver_api;
-        return FWK_SUCCESS;
+	*api = &mod_mock_clock_response_driver_api;
+	return FWK_SUCCESS;
 
     default:
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 }
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/mock_ppu/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/mock_ppu/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/mock_ppu/CMakeLists.txt
@@ -8,7 +8,7 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(
     ${SCP_MODULE_TARGET} PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_mock_ppu.c")
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/mock_ppu/src/mod_mock_ppu.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/mock_ppu/src/mod_mock_ppu.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/mock_ppu/src/mod_mock_ppu.c
@@ -57,26 +57,26 @@ static int pd_set_state(fwk_id_t pd_id,
 
     switch (state) {
     case MOD_PD_STATE_ON:
-        *pd_ctx->ppu = MOD_PD_STATE_ON;
-        status = pd_ctx->pd_driver_input_api->report_power_state_transition(
-            pd_ctx->bound_id, MOD_PD_STATE_ON);
-        assert(status == FWK_SUCCESS);
-        break;
+	*pd_ctx->ppu = MOD_PD_STATE_ON;
+	status = pd_ctx->pd_driver_input_api->report_power_state_transition(
+	    pd_ctx->bound_id, MOD_PD_STATE_ON);
+	assert(status == FWK_SUCCESS);
+	break;
 
     case MOD_PD_STATE_OFF:
-        *pd_ctx->ppu = MOD_PD_STATE_OFF;
-        status = pd_ctx->pd_driver_input_api->report_power_state_transition(
-            pd_ctx->bound_id, MOD_PD_STATE_OFF);
-        assert(status == FWK_SUCCESS);
-        break;
+	*pd_ctx->ppu = MOD_PD_STATE_OFF;
+	status = pd_ctx->pd_driver_input_api->report_power_state_transition(
+	    pd_ctx->bound_id, MOD_PD_STATE_OFF);
+	assert(status == FWK_SUCCESS);
+	break;
 
     default:
-        FWK_LOG_ERR("[PD] Requested power state (%i) is not supported.\n", state);
-        return FWK_E_PARAM;
+	FWK_LOG_ERR("[PD] Requested power state (%i) is not supported.\n", state);
+	return FWK_E_PARAM;
     }
 
     if (status != FWK_SUCCESS)
-        return FWK_E_PANIC;
+	return FWK_E_PANIC;
 
     return FWK_SUCCESS;
 }
@@ -120,12 +120,12 @@ static const struct mod_pd_driver_api pd
  */
 
 static int mock_ppu_mod_init(fwk_id_t module_id, unsigned int pd_count,
-                           const void *unused)
+			   const void *unused)
 {
     mock_ppu_ctx.pd_ctx_table = fwk_mm_calloc(pd_count,
-                                            sizeof(struct mock_ppu_pd_ctx));
+					    sizeof(struct mock_ppu_pd_ctx));
     if (mock_ppu_ctx.pd_ctx_table == NULL)
-        return FWK_E_NOMEM;
+	return FWK_E_NOMEM;
 
     return FWK_SUCCESS;
 }
@@ -136,7 +136,7 @@ static int mock_ppu_pd_init(fwk_id_t pd_
     struct mock_ppu_pd_ctx *pd_ctx;
 
     if (config->pd_type >= MOD_PD_TYPE_COUNT)
-        return FWK_E_DATA;
+	return FWK_E_DATA;
 
     pd_ctx = mock_ppu_ctx.pd_ctx_table + fwk_id_get_element_idx(pd_id);
     pd_ctx->config = config;
@@ -147,13 +147,13 @@ static int mock_ppu_pd_init(fwk_id_t pd_
     case MOD_PD_TYPE_DEVICE:
     case MOD_PD_TYPE_DEVICE_DEBUG:
     case MOD_PD_TYPE_SYSTEM:
-        if (config->default_power_on)
-            *pd_ctx->ppu = MOD_PD_STATE_ON;
+	if (config->default_power_on)
+	    *pd_ctx->ppu = MOD_PD_STATE_ON;
 
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 
     default:
-        return FWK_E_SUPPORT;
+	return FWK_E_SUPPORT;
     }
 }
 
@@ -164,44 +164,44 @@ static int mock_ppu_bind(fwk_id_t id, un
     /* Nothing to do during the first round of calls where the power module
        will bind to the power domains of this module. */
     if (round == 0)
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 
     /* In the case of the module, nothing to do */
     if (fwk_module_is_valid_module_id(id)) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     pd_ctx = mock_ppu_ctx.pd_ctx_table + fwk_id_get_element_idx(id);
 
     if (fwk_id_is_equal(pd_ctx->bound_id, FWK_ID_NONE))
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 
     switch (fwk_id_get_module_idx(pd_ctx->bound_id)) {
     #ifdef BUILD_HAS_MOD_POWER_DOMAIN
     case FWK_MODULE_IDX_POWER_DOMAIN:
-        return fwk_module_bind(pd_ctx->bound_id,
-                               mod_pd_api_id_driver_input,
-                               &pd_ctx->pd_driver_input_api);
-        break;
+	return fwk_module_bind(pd_ctx->bound_id,
+			       mod_pd_api_id_driver_input,
+			       &pd_ctx->pd_driver_input_api);
+	break;
     #endif
 
     #ifdef BUILD_HAS_MOD_SYSTEM_POWER
     case FWK_MODULE_IDX_SYSTEM_POWER:
-        return fwk_module_bind(pd_ctx->bound_id,
-                               mod_system_power_api_id_pd_driver_input,
-                               &pd_ctx->pd_driver_input_api);
-        break;
+	return fwk_module_bind(pd_ctx->bound_id,
+			       mod_system_power_api_id_pd_driver_input,
+			       &pd_ctx->pd_driver_input_api);
+	break;
     #endif
 
     default:
-        assert(false);
-        return FWK_E_SUPPORT;
+	assert(false);
+	return FWK_E_SUPPORT;
     }
 }
 
 static int mock_ppu_process_bind_request(fwk_id_t source_id,
-                                       fwk_id_t target_id, fwk_id_t not_used,
-                                       const void **api)
+				       fwk_id_t target_id, fwk_id_t not_used,
+				       const void **api)
 {
     struct mock_ppu_pd_ctx *pd_ctx;
 
@@ -209,34 +209,34 @@ static int mock_ppu_process_bind_request
 
     switch (pd_ctx->config->pd_type) {
     case MOD_PD_TYPE_SYSTEM:
-        if (!fwk_id_is_equal(pd_ctx->bound_id, FWK_ID_NONE)) {
-            assert(false);
-            return FWK_E_ACCESS;
-        }
+	if (!fwk_id_is_equal(pd_ctx->bound_id, FWK_ID_NONE)) {
+	    assert(false);
+	    return FWK_E_ACCESS;
+	}
     /* Fallthrough */
 
     case MOD_PD_TYPE_DEVICE:
     case MOD_PD_TYPE_DEVICE_DEBUG:
-        #ifdef BUILD_HAS_MOD_POWER_DOMAIN
-        if (fwk_id_get_module_idx(source_id) == FWK_MODULE_IDX_POWER_DOMAIN) {
-            pd_ctx->bound_id = source_id;
-            *api = &pd_driver;
-            break;
-        }
-        #endif
-        #ifdef BUILD_HAS_MOD_SYSTEM_POWER
-        if (fwk_id_get_module_idx(source_id) == FWK_MODULE_IDX_SYSTEM_POWER) {
-            pd_ctx->bound_id = source_id;
-            *api = &pd_driver;
-            break;
-        }
-        #endif
-        assert(false);
-        return FWK_E_ACCESS;
+	#ifdef BUILD_HAS_MOD_POWER_DOMAIN
+	if (fwk_id_get_module_idx(source_id) == FWK_MODULE_IDX_POWER_DOMAIN) {
+	    pd_ctx->bound_id = source_id;
+	    *api = &pd_driver;
+	    break;
+	}
+	#endif
+	#ifdef BUILD_HAS_MOD_SYSTEM_POWER
+	if (fwk_id_get_module_idx(source_id) == FWK_MODULE_IDX_SYSTEM_POWER) {
+	    pd_ctx->bound_id = source_id;
+	    *api = &pd_driver;
+	    break;
+	}
+	#endif
+	assert(false);
+	return FWK_E_ACCESS;
 
     default:
-        (void)pd_driver;
-        return FWK_E_SUPPORT;
+	(void)pd_driver;
+	return FWK_E_SUPPORT;
     }
 
     return FWK_SUCCESS;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/mock_psu/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/mock_psu/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/mock_psu/CMakeLists.txt
@@ -8,9 +8,9 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_mock_psu.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_mock_psu.c")
 
 target_link_libraries(${SCP_MODULE_TARGET} PRIVATE module-psu module-timer)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/mock_psu/src/mod_mock_psu.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/mock_psu/src/mod_mock_psu.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/mock_psu/src/mod_mock_psu.c
@@ -33,24 +33,24 @@ struct mod_mock_psu_operation {
     enum mod_mock_psu_request request;
 
     union {
-        bool enabled;
-        uint32_t voltage;
+	bool enabled;
+	uint32_t voltage;
     };
 };
 
 static struct mod_mock_psu_mod_ctx {
     struct mod_mock_psu_element_ctx {
-        struct {
-            bool enabled;
-            uint32_t voltage;
-        } state;
-
-        struct mod_mock_psu_operation op;
-
-        struct {
-            const struct mod_timer_alarm_api *alarm;
-            const struct mod_psu_driver_response_api *hal;
-        } apis;
+	struct {
+	    bool enabled;
+	    uint32_t voltage;
+	} state;
+
+	struct mod_mock_psu_operation op;
+
+	struct {
+	    const struct mod_timer_alarm_api *alarm;
+	    const struct mod_psu_driver_response_api *hal;
+	} apis;
     } *elements;
 } mod_mock_psu_ctx;
 
@@ -69,15 +69,15 @@ static int mod_mock_psu_get_cfg_ctx(
     struct mod_mock_psu_element_ctx **ctx)
 {
     if (fwk_id_get_module_idx(element_id) != FWK_MODULE_IDX_MOCK_PSU) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     if (ctx != NULL) {
-        *ctx = mod_mock_psu_get_ctx(element_id);
+	*ctx = mod_mock_psu_get_ctx(element_id);
     }
 
     if (cfg != NULL) {
-        *cfg = fwk_module_get_data(element_id);
+	*cfg = fwk_module_get_data(element_id);
     }
 
     return FWK_SUCCESS;
@@ -88,7 +88,7 @@ static void mod_mock_psu_alarm_callback(
     int status;
 
     fwk_id_t element_id = fwk_id_build_element_id(
-        fwk_module_id_mock_psu, element_idx);
+	fwk_module_id_mock_psu, element_idx);
 
     const struct mod_mock_psu_element_cfg *cfg;
     struct mod_mock_psu_element_ctx *ctx;
@@ -96,36 +96,36 @@ static void mod_mock_psu_alarm_callback(
 
     status = mod_mock_psu_get_cfg_ctx(element_id, &cfg, &ctx);
     if (!fwk_expect(status == FWK_SUCCESS)) {
-        return;
+	return;
     }
 
     response = (struct mod_psu_driver_response){
-        .status = FWK_SUCCESS,
+	.status = FWK_SUCCESS,
     };
 
     switch (ctx->op.request) {
     case MOD_MOCK_PSU_REQUEST_GET_ENABLED:
-        response.enabled = ctx->state.enabled;
+	response.enabled = ctx->state.enabled;
 
-        break;
+	break;
 
     case MOD_MOCK_PSU_REQUEST_SET_ENABLED:
-        ctx->state.enabled = ctx->op.enabled;
+	ctx->state.enabled = ctx->op.enabled;
 
-        break;
+	break;
 
     case MOD_MOCK_PSU_REQUEST_GET_VOLTAGE:
-        response.voltage = ctx->state.voltage;
+	response.voltage = ctx->state.voltage;
 
-        break;
+	break;
 
     case MOD_MOCK_PSU_REQUEST_SET_VOLTAGE:
-        ctx->state.voltage = ctx->op.voltage;
+	ctx->state.voltage = ctx->op.voltage;
 
-        break;
+	break;
 
     default:
-        fwk_unreachable();
+	fwk_unreachable();
     }
 
     ctx->op.request = MOD_MOCK_PSU_REQUEST_NONE;
@@ -146,22 +146,22 @@ static int mod_mock_psu_trigger(
 
     status = mod_mock_psu_get_cfg_ctx(element_id, &cfg, &ctx);
     if (status != FWK_SUCCESS) {
-        goto exit;
+	goto exit;
     }
 
     status = ctx->apis.alarm->start(
-        cfg->async_alarm_id,
-        ASYNC_DELAY,
-        MOD_TIMER_ALARM_TYPE_ONCE,
-        mod_mock_psu_alarm_callback,
-        fwk_id_get_element_idx(element_id));
+	cfg->async_alarm_id,
+	ASYNC_DELAY,
+	MOD_TIMER_ALARM_TYPE_ONCE,
+	mod_mock_psu_alarm_callback,
+	fwk_id_get_element_idx(element_id));
 
     if (status == FWK_SUCCESS) {
-        status = FWK_PENDING;
+	status = FWK_PENDING;
 
-        ctx->op = op;
+	ctx->op = op;
     } else {
-        status = FWK_E_HANDLER;
+	status = FWK_E_HANDLER;
     }
 
 exit:
@@ -179,23 +179,23 @@ static int mod_mock_psu_get_enabled(
 
     status = mod_mock_psu_get_cfg_ctx(element_id, &cfg, &ctx);
     if (status != FWK_SUCCESS) {
-        goto exit;
+	goto exit;
     }
 
     if (ctx->apis.alarm == NULL) {
-        *enabled = ctx->state.enabled;
+	*enabled = ctx->state.enabled;
 
-        goto exit;
+	goto exit;
     }
 
     if (ctx->op.request == MOD_MOCK_PSU_REQUEST_NONE) {
-        struct mod_mock_psu_operation op = {
-            .request = MOD_MOCK_PSU_REQUEST_GET_ENABLED,
-        };
+	struct mod_mock_psu_operation op = {
+	    .request = MOD_MOCK_PSU_REQUEST_GET_ENABLED,
+	};
 
-        status = mod_mock_psu_trigger(element_id, op);
+	status = mod_mock_psu_trigger(element_id, op);
     } else {
-        status = FWK_E_BUSY;
+	status = FWK_E_BUSY;
     }
 
 exit:
@@ -213,24 +213,24 @@ static int mod_mock_psu_set_enabled(
 
     status = mod_mock_psu_get_cfg_ctx(element_id, &cfg, &ctx);
     if (status != FWK_SUCCESS) {
-        goto exit;
+	goto exit;
     }
 
     if (ctx->apis.alarm == NULL) {
-        ctx->state.enabled = enabled;
+	ctx->state.enabled = enabled;
 
-        goto exit;
+	goto exit;
     }
 
     if (ctx->op.request == MOD_MOCK_PSU_REQUEST_NONE) {
-        struct mod_mock_psu_operation op = {
-            .request = MOD_MOCK_PSU_REQUEST_SET_ENABLED,
-            .enabled = enabled,
-        };
+	struct mod_mock_psu_operation op = {
+	    .request = MOD_MOCK_PSU_REQUEST_SET_ENABLED,
+	    .enabled = enabled,
+	};
 
-        status = mod_mock_psu_trigger(element_id, op);
+	status = mod_mock_psu_trigger(element_id, op);
     } else {
-        status = FWK_E_BUSY;
+	status = FWK_E_BUSY;
     }
 
 exit:
@@ -246,23 +246,23 @@ static int mod_mock_psu_get_voltage(fwk_
 
     status = mod_mock_psu_get_cfg_ctx(element_id, &cfg, &ctx);
     if (status != FWK_SUCCESS) {
-        goto exit;
+	goto exit;
     }
 
     if (ctx->apis.alarm == NULL) {
-        *voltage = ctx->state.voltage;
+	*voltage = ctx->state.voltage;
 
-        goto exit;
+	goto exit;
     }
 
     if (ctx->op.request == MOD_MOCK_PSU_REQUEST_NONE) {
-        struct mod_mock_psu_operation op = {
-            .request = MOD_MOCK_PSU_REQUEST_GET_VOLTAGE,
-        };
+	struct mod_mock_psu_operation op = {
+	    .request = MOD_MOCK_PSU_REQUEST_GET_VOLTAGE,
+	};
 
-        status = mod_mock_psu_trigger(element_id, op);
+	status = mod_mock_psu_trigger(element_id, op);
     } else {
-        status = FWK_E_BUSY;
+	status = FWK_E_BUSY;
     }
 
 exit:
@@ -278,24 +278,24 @@ static int mod_mock_psu_set_voltage(fwk_
 
     status = mod_mock_psu_get_cfg_ctx(element_id, &cfg, &ctx);
     if (status != FWK_SUCCESS) {
-        goto exit;
+	goto exit;
     }
 
     if (ctx->apis.alarm == NULL) {
-        ctx->state.voltage = voltage;
+	ctx->state.voltage = voltage;
 
-        goto exit;
+	goto exit;
     }
 
     if (ctx->op.request == MOD_MOCK_PSU_REQUEST_NONE) {
-        struct mod_mock_psu_operation op = {
-            .request = MOD_MOCK_PSU_REQUEST_SET_VOLTAGE,
-            .voltage = voltage,
-        };
+	struct mod_mock_psu_operation op = {
+	    .request = MOD_MOCK_PSU_REQUEST_SET_VOLTAGE,
+	    .voltage = voltage,
+	};
 
-        status = mod_mock_psu_trigger(element_id, op);
+	status = mod_mock_psu_trigger(element_id, op);
     } else {
-        status = FWK_E_BUSY;
+	status = FWK_E_BUSY;
     }
 
 exit:
@@ -317,7 +317,7 @@ static int mod_mock_psu_init(
     fwk_check(data == NULL);
 
     mod_mock_psu_ctx.elements =
-        fwk_mm_calloc(element_count, sizeof(mod_mock_psu_ctx.elements[0]));
+	fwk_mm_calloc(element_count, sizeof(mod_mock_psu_ctx.elements[0]));
 
     return FWK_SUCCESS;
 }
@@ -335,14 +335,14 @@ static int mod_mock_psu_element_init(
     ctx = mod_mock_psu_get_ctx(element_id);
 
     *ctx = (struct mod_mock_psu_element_ctx) {
-        .state = {
-            .enabled = cfg->default_enabled,
-            .voltage = cfg->default_voltage,
-        },
-
-        .op = {
-            .request = MOD_MOCK_PSU_REQUEST_NONE,
-        },
+	.state = {
+	    .enabled = cfg->default_enabled,
+	    .voltage = cfg->default_voltage,
+	},
+
+	.op = {
+	    .request = MOD_MOCK_PSU_REQUEST_NONE,
+	},
     };
 
     return FWK_SUCCESS;
@@ -353,25 +353,25 @@ static int mod_mock_psu_bind(fwk_id_t id
     int status = FWK_SUCCESS;
 
     if ((round == 0) && fwk_id_is_type(id, FWK_ID_TYPE_ELEMENT)) {
-        const struct mod_mock_psu_element_cfg *cfg = fwk_module_get_data(id);
+	const struct mod_mock_psu_element_cfg *cfg = fwk_module_get_data(id);
 
-        if (!fwk_id_is_equal(cfg->async_alarm_id, FWK_ID_NONE)) {
-            struct mod_mock_psu_element_ctx *ctx =
-                mod_mock_psu_get_ctx(id);
-
-            status = fwk_module_bind(
-                cfg->async_alarm_id,
-                cfg->async_alarm_api_id,
-                &ctx->apis.alarm);
-            if (status != FWK_SUCCESS) {
-                goto exit;
-            }
-
-            status = fwk_module_bind(
-                cfg->async_response_id,
-                cfg->async_response_api_id,
-                &ctx->apis.hal);
-        }
+	if (!fwk_id_is_equal(cfg->async_alarm_id, FWK_ID_NONE)) {
+	    struct mod_mock_psu_element_ctx *ctx =
+		mod_mock_psu_get_ctx(id);
+
+	    status = fwk_module_bind(
+		cfg->async_alarm_id,
+		cfg->async_alarm_api_id,
+		&ctx->apis.alarm);
+	    if (status != FWK_SUCCESS) {
+		goto exit;
+	    }
+
+	    status = fwk_module_bind(
+		cfg->async_response_id,
+		cfg->async_response_api_id,
+		&ctx->apis.hal);
+	}
     }
 
 exit:
@@ -387,24 +387,24 @@ static int mod_mock_psu_process_bind_req
     const struct mod_mock_psu_element_cfg *cfg;
 
     if (!fwk_id_is_type(target_id, FWK_ID_TYPE_ELEMENT)) {
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 
     if (!fwk_id_is_equal(api_id, mod_mock_psu_api_id_driver)) {
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 
     cfg = fwk_module_get_data(target_id);
 
     if (!fwk_id_is_equal(cfg->async_alarm_id, FWK_ID_NONE)) {
-        /*
-         * If we are configured to respond asynchronously, the only entity we
-         * accept bind requests from is the entity that we have configured to be
-         * our designated driver respondee.
-         */
-        if (!fwk_id_is_equal(source_id, cfg->async_response_id)) {
-            return FWK_E_ACCESS;
-        }
+	/*
+	 * If we are configured to respond asynchronously, the only entity we
+	 * accept bind requests from is the entity that we have configured to be
+	 * our designated driver respondee.
+	 */
+	if (!fwk_id_is_equal(source_id, cfg->async_response_id)) {
+	    return FWK_E_ACCESS;
+	}
     }
 
     *api = &mod_mock_psu_driver_api;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/mock_sensor/src/mod_mock_sensor.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/mock_sensor/src/mod_mock_sensor.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/mock_sensor/src/mod_mock_sensor.c
@@ -42,18 +42,18 @@ static void mock_sensor_callback(uintptr
     response.status = FWK_SUCCESS;
 #ifdef BUILD_HAS_SCMI_SENSOR_V2
     if (ctx->config->axis_count > 1) {
-        for (i = 0; i < ctx->config->axis_count; i++) {
-            response.axis_value[i] = ctx->config->read_value[i];
-        }
+	for (i = 0; i < ctx->config->axis_count; i++) {
+	    response.axis_value[i] = ctx->config->read_value[i];
+	}
     } else {
-        response.value = *ctx->config->read_value;
+	response.value = *ctx->config->read_value;
     }
 #else
     response.value = *ctx->config->read_value;
 #endif
 
     fwk_id_t sensor_id = fwk_id_build_element_id(
-        fwk_module_id_sensor, (unsigned int)sensor_hal_idx);
+	fwk_module_id_sensor, (unsigned int)sensor_hal_idx);
     ctx->driver_response_api->reading_complete(sensor_id, &response);
 
     return;
@@ -70,13 +70,13 @@ static int get_value(fwk_id_t id, mod_se
     ctx = ctx_table + fwk_id_get_element_idx(id);
 
     status = ctx->alarm_api->start(
-        ctx->config->alarm_id,
-        MOCK_SENSOR_ALARM_DELAY_MS,
-        MOD_TIMER_ALARM_TYPE_ONCE,
-        mock_sensor_callback,
-        (uintptr_t)ctx);
+	ctx->config->alarm_id,
+	MOCK_SENSOR_ALARM_DELAY_MS,
+	MOD_TIMER_ALARM_TYPE_ONCE,
+	mock_sensor_callback,
+	(uintptr_t)ctx);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     /* Mock sensor always defers this request */
@@ -90,7 +90,7 @@ static int get_info(fwk_id_t id, struct
     ctx = ctx_table + fwk_id_get_element_idx(id);
 
     if (info == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     *info = *ctx->config->info;
@@ -133,17 +133,17 @@ static const struct mod_sensor_driver_ap
  * Framework handlers
  */
 static int mock_sensor_init(fwk_id_t module_id,
-                            unsigned int element_count,
-                            const void *data)
+			    unsigned int element_count,
+			    const void *data)
 {
     ctx_table =
-        fwk_mm_calloc(element_count, sizeof(struct mock_sensor_dev_ctx));
+	fwk_mm_calloc(element_count, sizeof(struct mock_sensor_dev_ctx));
     return FWK_SUCCESS;
 }
 
 static int mock_sensor_element_init(fwk_id_t element_id,
-                                    unsigned int unused,
-                                    const void *data)
+				    unsigned int unused,
+				    const void *data)
 {
     struct mock_sensor_dev_ctx *ctx;
 
@@ -161,33 +161,33 @@ static int mock_sensor_bind(fwk_id_t id,
     struct mock_sensor_dev_ctx *ctx;
 
     if ((round > 0) || fwk_id_is_type(id, FWK_ID_TYPE_MODULE)) {
-        /*
-         * Only bind in first round of calls
-         * Nothing to do for module
-         */
-        return FWK_SUCCESS;
+	/*
+	 * Only bind in first round of calls
+	 * Nothing to do for module
+	 */
+	return FWK_SUCCESS;
     }
 
     ctx = ctx_table + fwk_id_get_element_idx(id);
 
     status = fwk_module_bind(
-        ctx->config->alarm_id,
-        FWK_ID_API(FWK_MODULE_IDX_TIMER, 1),
-        &ctx->alarm_api);
+	ctx->config->alarm_id,
+	FWK_ID_API(FWK_MODULE_IDX_TIMER, 1),
+	&ctx->alarm_api);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     return fwk_module_bind(
-        ctx->config->sensor_hal_id,
-        mod_sensor_api_id_driver_response,
-        &ctx->driver_response_api);
+	ctx->config->sensor_hal_id,
+	mod_sensor_api_id_driver_response,
+	&ctx->driver_response_api);
 }
 
 static int mock_sensor_process_bind_request(fwk_id_t source_id,
-                                            fwk_id_t target_id,
-                                            fwk_id_t api_type,
-                                            const void **api)
+					    fwk_id_t target_id,
+					    fwk_id_t api_type,
+					    const void **api)
 {
     *api = &mock_sensor_api;
     return FWK_SUCCESS;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/mock_voltage_domain/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/mock_voltage_domain/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/mock_voltage_domain/CMakeLists.txt
@@ -8,9 +8,9 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_mock_voltage_domain.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_mock_voltage_domain.c")
 
 target_link_libraries(${SCP_MODULE_TARGET} PRIVATE module-voltage-domain module-timer)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/mock_voltage_domain/include/mod_mock_voltage_domain.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/mock_voltage_domain/include/mod_mock_voltage_domain.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/mock_voltage_domain/include/mod_mock_voltage_domain.h
@@ -58,21 +58,21 @@ struct mod_mock_voltage_domain_driver_re
      *      event type of the response.
      */
     union {
-        /*!
-         * \brief `true` if the device is enabled, otherwise `false`.
-         *
-         * \warning Used only in response to a
-         *      ::mod_voltd_drv_api::get_config call.
-         */
-        enum mod_voltd_mode_id mode_id;
+	/*!
+	 * \brief `true` if the device is enabled, otherwise `false`.
+	 *
+	 * \warning Used only in response to a
+	 *      ::mod_voltd_drv_api::get_config call.
+	 */
+	enum mod_voltd_mode_id mode_id;
 
-        /*!
-         * \brief Voltage in microvolts (uV).
-         *
-         * \warning Used only in response to a
-         *      ::mod_voltd_drv_api::get_level call.
-         */
-        int32_t voltage;
+	/*!
+	 * \brief Voltage in microvolts (uV).
+	 *
+	 * \warning Used only in response to a
+	 *      ::mod_voltd_drv_api::get_level call.
+	 */
+	int32_t voltage;
     };
 };
 
@@ -91,8 +91,8 @@ struct mod_mock_voltage_domain_driver_re
      * \param response Driver operation response.
      */
     void (*respond)(
-        fwk_id_t element_id,
-        struct mod_mock_voltage_domain_driver_response response);
+	fwk_id_t element_id,
+	struct mod_mock_voltage_domain_driver_response response);
 };
 
 /*!
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/mock_voltage_domain/src/mod_mock_voltage_domain.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/mock_voltage_domain/src/mod_mock_voltage_domain.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/mock_voltage_domain/src/mod_mock_voltage_domain.c
@@ -30,22 +30,22 @@ struct mod_mock_voltage_domain_operation
     enum mod_mock_voltage_domain_request request;
 
     union {
-        enum mod_voltd_mode_id mode_id;
-        int32_t voltage;
+	enum mod_voltd_mode_id mode_id;
+	int32_t voltage;
     };
 };
 
 struct mod_mock_voltage_domain_ctx {
     struct mod_mock_voltage_domain_element_ctx {
-        enum mod_voltd_mode_id mode_id;
-        int32_t voltage;
+	enum mod_voltd_mode_id mode_id;
+	int32_t voltage;
 
-        struct mod_mock_voltage_domain_operation op;
+	struct mod_mock_voltage_domain_operation op;
 
-        struct {
-            const struct mod_timer_alarm_api *alarm;
-            const struct mod_mock_voltage_domain_driver_response_api *hal;
-        } apis;
+	struct {
+	    const struct mod_timer_alarm_api *alarm;
+	    const struct mod_mock_voltage_domain_driver_response_api *hal;
+	} apis;
     } * elements;
 };
 
@@ -66,18 +66,18 @@ static int mod_mock_voltage_domain_get_c
 {
     int status = FWK_SUCCESS;
     enum fwk_module_idx module_idx =
-        (enum fwk_module_idx)fwk_id_get_module_idx(element_id);
+	(enum fwk_module_idx)fwk_id_get_module_idx(element_id);
 
     if (module_idx == FWK_MODULE_IDX_MOCK_VOLTAGE_DOMAIN) {
-        if (ctx != NULL) {
-            *ctx = mod_mock_voltage_domain_get_ctx(element_id);
-        }
-
-        if (cfg != NULL) {
-            *cfg = fwk_module_get_data(element_id);
-        }
+	if (ctx != NULL) {
+	    *ctx = mod_mock_voltage_domain_get_ctx(element_id);
+	}
+
+	if (cfg != NULL) {
+	    *cfg = fwk_module_get_data(element_id);
+	}
     } else {
-        status = FWK_E_PARAM;
+	status = FWK_E_PARAM;
     }
 
     return status;
@@ -88,7 +88,7 @@ static void mod_mock_voltage_domain_alar
     int status;
 
     fwk_id_t element_id =
-        fwk_id_build_element_id(fwk_module_id_mock_voltage_domain, element_idx);
+	fwk_id_build_element_id(fwk_module_id_mock_voltage_domain, element_idx);
 
     const struct mod_mock_voltage_domain_element_cfg *cfg;
     struct mod_mock_voltage_domain_element_ctx *ctx;
@@ -96,31 +96,31 @@ static void mod_mock_voltage_domain_alar
 
     status = mod_mock_voltage_domain_get_cfg_ctx(element_id, &cfg, &ctx);
     if (fwk_expect(status == FWK_SUCCESS)) {
-        response = (struct mod_mock_voltage_domain_driver_response){
-            .status = FWK_SUCCESS,
-        };
-
-        switch (ctx->op.request) {
-        case MOD_MOCK_VOLTAGE_DOMAIN_REQUEST_GET_MODE_ID:
-            response.mode_id = ctx->mode_id;
-            break;
-        case MOD_MOCK_VOLTAGE_DOMAIN_REQUEST_SET_MODE_ID:
-            ctx->mode_id = ctx->op.mode_id;
-            break;
-        case MOD_MOCK_VOLTAGE_DOMAIN_REQUEST_GET_VOLTAGE:
-            response.voltage = ctx->voltage;
-            break;
-        case MOD_MOCK_VOLTAGE_DOMAIN_REQUEST_SET_VOLTAGE:
-            ctx->voltage = ctx->op.voltage;
-            break;
-        default:
-            fwk_unreachable();
-            break;
-        }
+	response = (struct mod_mock_voltage_domain_driver_response){
+	    .status = FWK_SUCCESS,
+	};
+
+	switch (ctx->op.request) {
+	case MOD_MOCK_VOLTAGE_DOMAIN_REQUEST_GET_MODE_ID:
+	    response.mode_id = ctx->mode_id;
+	    break;
+	case MOD_MOCK_VOLTAGE_DOMAIN_REQUEST_SET_MODE_ID:
+	    ctx->mode_id = ctx->op.mode_id;
+	    break;
+	case MOD_MOCK_VOLTAGE_DOMAIN_REQUEST_GET_VOLTAGE:
+	    response.voltage = ctx->voltage;
+	    break;
+	case MOD_MOCK_VOLTAGE_DOMAIN_REQUEST_SET_VOLTAGE:
+	    ctx->voltage = ctx->op.voltage;
+	    break;
+	default:
+	    fwk_unreachable();
+	    break;
+	}
 
-        ctx->op.request = MOD_MOCK_VOLTAGE_DOMAIN_REQUEST_NONE;
+	ctx->op.request = MOD_MOCK_VOLTAGE_DOMAIN_REQUEST_NONE;
 
-        ctx->apis.hal->respond(cfg->async_response_id, response);
+	ctx->apis.hal->respond(cfg->async_response_id, response);
     }
 }
 
@@ -137,19 +137,19 @@ static int mod_mock_voltage_domain_trigg
 
     status = mod_mock_voltage_domain_get_cfg_ctx(element_id, &cfg, &ctx);
     if (status == FWK_SUCCESS) {
-        status = ctx->apis.alarm->start(
-            cfg->async_alarm_id,
-            ASYNC_DELAY,
-            MOD_TIMER_ALARM_TYPE_ONCE,
-            mod_mock_voltage_domain_alarm_callback,
-            fwk_id_get_element_idx(element_id));
-
-        if (status == FWK_SUCCESS) {
-            status = FWK_PENDING;
-            ctx->op = op;
-        } else {
-            status = FWK_E_HANDLER;
-        }
+	status = ctx->apis.alarm->start(
+	    cfg->async_alarm_id,
+	    ASYNC_DELAY,
+	    MOD_TIMER_ALARM_TYPE_ONCE,
+	    mod_mock_voltage_domain_alarm_callback,
+	    fwk_id_get_element_idx(element_id));
+
+	if (status == FWK_SUCCESS) {
+	    status = FWK_PENDING;
+	    ctx->op = op;
+	} else {
+	    status = FWK_E_HANDLER;
+	}
     }
 
     return status;
@@ -166,16 +166,16 @@ static int mod_mock_voltage_domain_get_l
 
     status = mod_mock_voltage_domain_get_cfg_ctx(element_id, &cfg, &ctx);
     if (status == FWK_SUCCESS) {
-        if (ctx->apis.alarm == NULL) {
-            *voltage = ctx->voltage;
-        } else if (ctx->op.request == MOD_MOCK_VOLTAGE_DOMAIN_REQUEST_NONE) {
-            struct mod_mock_voltage_domain_operation op = {
-                .request = MOD_MOCK_VOLTAGE_DOMAIN_REQUEST_GET_VOLTAGE,
-            };
-            status = mod_mock_voltage_domain_trigger(element_id, op);
-        } else {
-            status = FWK_E_BUSY;
-        }
+	if (ctx->apis.alarm == NULL) {
+	    *voltage = ctx->voltage;
+	} else if (ctx->op.request == MOD_MOCK_VOLTAGE_DOMAIN_REQUEST_NONE) {
+	    struct mod_mock_voltage_domain_operation op = {
+		.request = MOD_MOCK_VOLTAGE_DOMAIN_REQUEST_GET_VOLTAGE,
+	    };
+	    status = mod_mock_voltage_domain_trigger(element_id, op);
+	} else {
+	    status = FWK_E_BUSY;
+	}
     }
 
     return status;
@@ -193,52 +193,52 @@ static int mod_mock_voltage_domain_set_l
 
     status = mod_mock_voltage_domain_get_cfg_ctx(element_id, &cfg, &ctx);
     if (status == FWK_SUCCESS) {
-        switch (cfg->level_type) {
-        case MOD_VOLTD_VOLTAGE_LEVEL_DISCRETE: {
-            for (loop_index = (size_t)0; loop_index < cfg->level_count;
-                 loop_index++) {
-                if (voltage == cfg->voltage_levels[loop_index]) {
-                    break;
-                }
-            }
-            if (loop_index == cfg->level_count) {
-                status = FWK_E_RANGE;
-            }
-            break;
-        }
-
-        case MOD_VOLTD_VOLTAGE_LEVEL_CONTINUOUS: {
-            if ((voltage >= cfg->voltage_levels[0]) &&
-                (voltage <= cfg->voltage_levels[1])) {
-                if (((voltage - cfg->voltage_levels[0]) %
-                     cfg->voltage_levels[2]) != 0) {
-                    status = FWK_E_RANGE;
-                }
-            } else {
-                status = FWK_E_RANGE;
-            }
-            break;
-        }
-
-        default:
-            status = FWK_E_STATE;
-            break;
-        }
-
-        if (status == FWK_SUCCESS) {
-            if (ctx->apis.alarm == NULL) {
-                ctx->voltage = voltage;
-            } else if (
-                ctx->op.request == MOD_MOCK_VOLTAGE_DOMAIN_REQUEST_NONE) {
-                struct mod_mock_voltage_domain_operation op = {
-                    .request = MOD_MOCK_VOLTAGE_DOMAIN_REQUEST_SET_VOLTAGE,
-                    .voltage = voltage,
-                };
-                status = mod_mock_voltage_domain_trigger(element_id, op);
-            } else {
-                status = FWK_E_BUSY;
-            }
-        }
+	switch (cfg->level_type) {
+	case MOD_VOLTD_VOLTAGE_LEVEL_DISCRETE: {
+	    for (loop_index = (size_t)0; loop_index < cfg->level_count;
+		 loop_index++) {
+		if (voltage == cfg->voltage_levels[loop_index]) {
+		    break;
+		}
+	    }
+	    if (loop_index == cfg->level_count) {
+		status = FWK_E_RANGE;
+	    }
+	    break;
+	}
+
+	case MOD_VOLTD_VOLTAGE_LEVEL_CONTINUOUS: {
+	    if ((voltage >= cfg->voltage_levels[0]) &&
+		(voltage <= cfg->voltage_levels[1])) {
+		if (((voltage - cfg->voltage_levels[0]) %
+		     cfg->voltage_levels[2]) != 0) {
+		    status = FWK_E_RANGE;
+		}
+	    } else {
+		status = FWK_E_RANGE;
+	    }
+	    break;
+	}
+
+	default:
+	    status = FWK_E_STATE;
+	    break;
+	}
+
+	if (status == FWK_SUCCESS) {
+	    if (ctx->apis.alarm == NULL) {
+		ctx->voltage = voltage;
+	    } else if (
+		ctx->op.request == MOD_MOCK_VOLTAGE_DOMAIN_REQUEST_NONE) {
+		struct mod_mock_voltage_domain_operation op = {
+		    .request = MOD_MOCK_VOLTAGE_DOMAIN_REQUEST_SET_VOLTAGE,
+		    .voltage = voltage,
+		};
+		status = mod_mock_voltage_domain_trigger(element_id, op);
+	    } else {
+		status = FWK_E_BUSY;
+	    }
+	}
     }
 
     return status;
@@ -256,17 +256,17 @@ static int mod_mock_voltage_domain_get_c
 
     status = mod_mock_voltage_domain_get_cfg_ctx(element_id, &cfg, &ctx);
     if (status == FWK_SUCCESS) {
-        *mode_type = (uint8_t)MOD_VOLTD_MODE_TYPE_ARCH;
-        if (ctx->apis.alarm == NULL) {
-            *mode_id = (uint8_t)ctx->mode_id;
-        } else if (ctx->op.request == MOD_MOCK_VOLTAGE_DOMAIN_REQUEST_NONE) {
-            struct mod_mock_voltage_domain_operation op = {
-                .request = MOD_MOCK_VOLTAGE_DOMAIN_REQUEST_GET_MODE_ID,
-            };
-            status = mod_mock_voltage_domain_trigger(element_id, op);
-        } else {
-            status = FWK_E_BUSY;
-        }
+	*mode_type = (uint8_t)MOD_VOLTD_MODE_TYPE_ARCH;
+	if (ctx->apis.alarm == NULL) {
+	    *mode_id = (uint8_t)ctx->mode_id;
+	} else if (ctx->op.request == MOD_MOCK_VOLTAGE_DOMAIN_REQUEST_NONE) {
+	    struct mod_mock_voltage_domain_operation op = {
+		.request = MOD_MOCK_VOLTAGE_DOMAIN_REQUEST_GET_MODE_ID,
+	    };
+	    status = mod_mock_voltage_domain_trigger(element_id, op);
+	} else {
+	    status = FWK_E_BUSY;
+	}
     }
 
     return status;
@@ -284,23 +284,23 @@ static int mod_mock_voltage_domain_set_c
 
     status = mod_mock_voltage_domain_get_cfg_ctx(element_id, &cfg, &ctx);
     if (status == FWK_SUCCESS) {
-        if (mode_type != (uint8_t)MOD_VOLTD_MODE_TYPE_ARCH) {
-            status = FWK_E_SUPPORT;
-        } else if (
-            (mode_id != (uint8_t)MOD_VOLTD_MODE_ID_OFF) &&
-            (mode_id != (uint8_t)MOD_VOLTD_MODE_ID_ON)) {
-            status = FWK_E_PARAM;
-        } else if (ctx->apis.alarm == NULL) {
-            ctx->mode_id = (enum mod_voltd_mode_id)mode_id;
-        } else if (ctx->op.request == MOD_MOCK_VOLTAGE_DOMAIN_REQUEST_NONE) {
-            struct mod_mock_voltage_domain_operation op = {
-                .request = MOD_MOCK_VOLTAGE_DOMAIN_REQUEST_SET_MODE_ID,
-                .mode_id = (enum mod_voltd_mode_id)mode_id,
-            };
-            status = mod_mock_voltage_domain_trigger(element_id, op);
-        } else {
-            status = FWK_E_BUSY;
-        }
+	if (mode_type != (uint8_t)MOD_VOLTD_MODE_TYPE_ARCH) {
+	    status = FWK_E_SUPPORT;
+	} else if (
+	    (mode_id != (uint8_t)MOD_VOLTD_MODE_ID_OFF) &&
+	    (mode_id != (uint8_t)MOD_VOLTD_MODE_ID_ON)) {
+	    status = FWK_E_PARAM;
+	} else if (ctx->apis.alarm == NULL) {
+	    ctx->mode_id = (enum mod_voltd_mode_id)mode_id;
+	} else if (ctx->op.request == MOD_MOCK_VOLTAGE_DOMAIN_REQUEST_NONE) {
+	    struct mod_mock_voltage_domain_operation op = {
+		.request = MOD_MOCK_VOLTAGE_DOMAIN_REQUEST_SET_MODE_ID,
+		.mode_id = (enum mod_voltd_mode_id)mode_id,
+	    };
+	    status = mod_mock_voltage_domain_trigger(element_id, op);
+	} else {
+	    status = FWK_E_BUSY;
+	}
     }
 
     return status;
@@ -318,14 +318,14 @@ static int mod_mock_voltage_domain_get_i
     status = mod_mock_voltage_domain_get_cfg_ctx(element_id, &cfg, &ctx);
 
     if (status == FWK_SUCCESS) {
-        info->level_range.level_count = cfg->level_count;
-        info->level_range.level_type = cfg->level_type;
-        if (cfg->level_type == MOD_VOLTD_VOLTAGE_LEVEL_CONTINUOUS) {
-            info->level_range.min_uv = cfg->voltage_levels[0];
-            info->level_range.max_uv = cfg->voltage_levels[1];
-            info->level_range.step_uv = cfg->voltage_levels[2];
-        }
-        info->name = fwk_module_get_element_name(element_id);
+	info->level_range.level_count = cfg->level_count;
+	info->level_range.level_type = cfg->level_type;
+	if (cfg->level_type == MOD_VOLTD_VOLTAGE_LEVEL_CONTINUOUS) {
+	    info->level_range.min_uv = cfg->voltage_levels[0];
+	    info->level_range.max_uv = cfg->voltage_levels[1];
+	    info->level_range.step_uv = cfg->voltage_levels[2];
+	}
+	info->name = fwk_module_get_element_name(element_id);
     }
 
     return status;
@@ -344,11 +344,11 @@ static int mod_mock_voltage_domain_get_l
     status = mod_mock_voltage_domain_get_cfg_ctx(element_id, &cfg, &ctx);
 
     if (status == FWK_SUCCESS) {
-        if (index > cfg->level_count) {
-            status = FWK_E_RANGE;
-        } else {
-            *level_uv = cfg->voltage_levels[index];
-        }
+	if (index > cfg->level_count) {
+	    status = FWK_E_RANGE;
+	} else {
+	    *level_uv = cfg->voltage_levels[index];
+	}
     }
 
     return status;
@@ -371,7 +371,7 @@ static int mod_mock_voltage_domain_init(
     fwk_check(data == NULL);
 
     mock_voltage_domain_ctx.elements = fwk_mm_calloc(
-        element_count, sizeof(mock_voltage_domain_ctx.elements[0]));
+	element_count, sizeof(mock_voltage_domain_ctx.elements[0]));
 
     return FWK_SUCCESS;
 }
@@ -389,12 +389,12 @@ static int mod_mock_voltage_domain_eleme
     ctx = mod_mock_voltage_domain_get_ctx(element_id);
 
     *ctx = (struct mod_mock_voltage_domain_element_ctx) {
-        .mode_id = cfg->default_mode_id,
-        .voltage = cfg->default_voltage,
+	.mode_id = cfg->default_mode_id,
+	.voltage = cfg->default_voltage,
 
-        .op = {
-            .request = MOD_MOCK_VOLTAGE_DOMAIN_REQUEST_NONE,
-        },
+	.op = {
+	    .request = MOD_MOCK_VOLTAGE_DOMAIN_REQUEST_NONE,
+	},
     };
 
     return FWK_SUCCESS;
@@ -405,22 +405,22 @@ static int mod_mock_voltage_domain_bind(
     int status = FWK_SUCCESS;
 
     if ((call_round == 0U) && fwk_id_is_type(id, FWK_ID_TYPE_ELEMENT)) {
-        const struct mod_mock_voltage_domain_element_cfg *cfg =
-            fwk_module_get_data(id);
+	const struct mod_mock_voltage_domain_element_cfg *cfg =
+	    fwk_module_get_data(id);
 
-        if (!fwk_id_is_equal(cfg->async_alarm_id, FWK_ID_NONE)) {
-            struct mod_mock_voltage_domain_element_ctx *ctx =
-                mod_mock_voltage_domain_get_ctx(id);
-
-            status = fwk_module_bind(
-                cfg->async_alarm_id, cfg->async_alarm_api_id, &ctx->apis.alarm);
-            if (status == FWK_SUCCESS) {
-                status = fwk_module_bind(
-                    cfg->async_response_id,
-                    cfg->async_response_api_id,
-                    &ctx->apis.hal);
-            }
-        }
+	if (!fwk_id_is_equal(cfg->async_alarm_id, FWK_ID_NONE)) {
+	    struct mod_mock_voltage_domain_element_ctx *ctx =
+		mod_mock_voltage_domain_get_ctx(id);
+
+	    status = fwk_module_bind(
+		cfg->async_alarm_id, cfg->async_alarm_api_id, &ctx->apis.alarm);
+	    if (status == FWK_SUCCESS) {
+		status = fwk_module_bind(
+		    cfg->async_response_id,
+		    cfg->async_response_api_id,
+		    &ctx->apis.hal);
+	    }
+	}
     }
 
     return status;
@@ -435,29 +435,29 @@ static int mod_mock_voltage_domain_proce
     const struct mod_mock_voltage_domain_element_cfg *cfg;
     int status = FWK_SUCCESS;
     enum mod_mock_voltage_domain_api_idx domain_api_idx =
-        (enum mod_mock_voltage_domain_api_idx)fwk_id_get_api_idx(api_id);
+	(enum mod_mock_voltage_domain_api_idx)fwk_id_get_api_idx(api_id);
 
     if (!fwk_id_is_type(target_id, FWK_ID_TYPE_ELEMENT)) {
-        status = FWK_E_ACCESS;
+	status = FWK_E_ACCESS;
     } else if (domain_api_idx == MOD_MOCK_VOLTAGE_DOMAIN_API_IDX_VOLTD) {
-        cfg = fwk_module_get_data(target_id);
-        if (!fwk_id_is_equal(cfg->async_alarm_id, FWK_ID_NONE)) {
-            /*
-             * If we are configured to respond asynchronously, the only entity
-             * we accept bind requests from is the entity that we have
-             * configured to be our designated driver respondee.
-             */
-            if (!fwk_id_is_equal(source_id, cfg->async_response_id)) {
-                status = FWK_E_ACCESS;
-            }
-        }
-
-        if (status == FWK_SUCCESS) {
-            *api = &mock_voltage_domain_driver_api;
-        }
+	cfg = fwk_module_get_data(target_id);
+	if (!fwk_id_is_equal(cfg->async_alarm_id, FWK_ID_NONE)) {
+	    /*
+	     * If we are configured to respond asynchronously, the only entity
+	     * we accept bind requests from is the entity that we have
+	     * configured to be our designated driver respondee.
+	     */
+	    if (!fwk_id_is_equal(source_id, cfg->async_response_id)) {
+		status = FWK_E_ACCESS;
+	    }
+	}
+
+	if (status == FWK_SUCCESS) {
+	    *api = &mock_voltage_domain_driver_api;
+	}
 
     } else {
-        status = FWK_E_ACCESS;
+	status = FWK_E_ACCESS;
     }
 
     return status;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/mpmm/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/mpmm/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/mpmm/CMakeLists.txt
@@ -8,13 +8,13 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src")
+			   PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_mpmm.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_mpmm.c")
 
 target_link_libraries(${SCP_MODULE_TARGET} PRIVATE module-power-domain
-                                                   module-scmi-perf)
+						   module-scmi-perf)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/mpmm/src/mod_mpmm.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/mpmm/src/mod_mpmm.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/mpmm/src/mod_mpmm.c
@@ -98,8 +98,8 @@ static void mpmm_core_get_number_of_thre
     uint32_t *num_thresholds)
 {
     *num_thresholds =
-        ((core_ctx->mpmm->PPMCR >> MPMM_PPMCR_NUM_GEARS_POS) &
-         MPMM_PPMCR_NUM_GEARS_MASK);
+	((core_ctx->mpmm->PPMCR >> MPMM_PPMCR_NUM_GEARS_POS) &
+	 MPMM_PPMCR_NUM_GEARS_MASK);
 }
 
 /* Check if the MPMM throttling and MPMM counters are enabled for a core. */
@@ -108,14 +108,14 @@ static void mpmm_core_check_enabled(
     bool *enabled)
 {
     *enabled =
-        (bool)((core_ctx->mpmm->MPMMCR >> MPMM_MPMMCR_EN_POS) & MPMM_MPMMCR_EN_MASK);
+	(bool)((core_ctx->mpmm->MPMMCR >> MPMM_MPMMCR_EN_POS) & MPMM_MPMMCR_EN_MASK);
 }
 
 /* Set the MPMM threshold for a specific core. */
 static void mpmm_core_set_threshold(struct mod_mpmm_core_ctx *core_ctx)
 {
     core_ctx->mpmm->MPMMCR |=
-        ((core_ctx->threshold & MPMM_MPMMCR_GEAR_MASK) << MPMM_MPMMCR_GEAR_POS);
+	((core_ctx->threshold & MPMM_MPMMCR_GEAR_MASK) << MPMM_MPMMCR_GEAR_POS);
 }
 
 /*
@@ -140,9 +140,9 @@ static struct mod_mpmm_domain_ctx *get_d
     uint32_t idx = fwk_id_get_element_idx(domain_id);
 
     if (idx < mpmm_ctx.mpmm_domain_count) {
-        return &mpmm_ctx.domain_ctx[idx];
+	return &mpmm_ctx.domain_ctx[idx];
     } else {
-        return NULL;
+	return NULL;
     }
 }
 
@@ -155,18 +155,18 @@ static void mpmm_core_counters_delta(
     uint32_t counter_value;
 
     for (i = 0; i < th_count; i++) {
-        mpmm_core_counter_read(core_ctx, i, &counter_value);
+	mpmm_core_counter_read(core_ctx, i, &counter_value);
 
-        /* Calculate the delta */
-        if (counter_value < core_ctx->cached_counters[i]) {
-            /* Counter wraparound case */
-            core_ctx->delta[i] = UINT32_MAX - core_ctx->cached_counters[i];
-            core_ctx->delta[i] += counter_value;
-        } else {
-            core_ctx->delta[i] = counter_value - core_ctx->cached_counters[i];
-        }
-        /* Store the last value */
-        core_ctx->cached_counters[i] = counter_value;
+	/* Calculate the delta */
+	if (counter_value < core_ctx->cached_counters[i]) {
+	    /* Counter wraparound case */
+	    core_ctx->delta[i] = UINT32_MAX - core_ctx->cached_counters[i];
+	    core_ctx->delta[i] += counter_value;
+	} else {
+	    core_ctx->delta[i] = counter_value - core_ctx->cached_counters[i];
+	}
+	/* Store the last value */
+	core_ctx->cached_counters[i] = counter_value;
     }
 }
 
@@ -179,15 +179,15 @@ static uint32_t mpmm_core_threshold_poli
 {
     uint32_t thr_idx;
     uint32_t const highest_gear =
-        domain_ctx->domain_config->num_threshold_counters;
+	domain_ctx->domain_config->num_threshold_counters;
 
     /*
      * Select the highest gear whose counter delta is just below the btc value.
      */
     for (thr_idx = 0; thr_idx < highest_gear; thr_idx++) {
-        if (core_ctx->delta[thr_idx] <= domain_ctx->domain_config->btc) {
-            return thr_idx;
-        }
+	if (core_ctx->delta[thr_idx] <= domain_ctx->domain_config->btc) {
+	    return thr_idx;
+	}
     }
 
     /*
@@ -204,10 +204,10 @@ static void mpmm_domain_set_thresholds(s
     struct mod_mpmm_core_ctx *core_ctx;
 
     for (core_idx = 0; core_idx < ctx->num_cores; core_idx++) {
-        core_ctx = &ctx->core_ctx[core_idx];
-        if (core_ctx->online) {
-            mpmm_core_set_threshold(core_ctx);
-        }
+	core_ctx = &ctx->core_ctx[core_idx];
+	if (core_ctx->online) {
+	    mpmm_core_set_threshold(core_ctx);
+	}
     }
 }
 
@@ -220,8 +220,8 @@ static void mpmm_core_evaluate_threshold
     /* If counters are not enabled exit */
     mpmm_core_check_enabled(core_ctx, &enabled);
     if (!enabled) {
-        core_ctx->threshold = domain_ctx->domain_config->num_threshold_counters;
-        return;
+	core_ctx->threshold = domain_ctx->domain_config->num_threshold_counters;
+	return;
     }
 
     /* Read counters */
@@ -240,9 +240,9 @@ static uint32_t find_perf_limit_from_pct
     int j;
 
     for (j = pct_config->num_perf_limits - 1; j >= 0; j--) {
-        if (threshold_map <= pct_config->threshold_perf[j].threshold_bitmap) {
-            return pct_config->threshold_perf[j].perf_limit;
-        }
+	if (threshold_map <= pct_config->threshold_perf[j].threshold_bitmap) {
+	    return pct_config->threshold_perf[j].perf_limit;
+	}
     }
 
     /* If no threshold_map was found select the default limits */
@@ -257,25 +257,25 @@ static void mpmm_build_threshold_map(str
 
     /* Copy all CPU threshold values to an array */
     for (i = 0, j = 0; i < ctx->num_cores; i++) {
-        if (ctx->core_ctx[i].online) {
-            threshold_array[j] = ctx->core_ctx[i].threshold;
-            j++;
-        }
+	if (ctx->core_ctx[i].online) {
+	    threshold_array[j] = ctx->core_ctx[i].threshold;
+	    j++;
+	}
     }
 
     /* Threshold sorting in descending order */
     for (i = 0; i < ctx->num_cores_online; i++) {
-        for (j = i + 1; j < ctx->num_cores_online; j++) {
-            if (threshold_array[i] > threshold_array[j]) {
-                thr_tmp = threshold_array[i];
-                threshold_array[i] = threshold_array[j];
-                threshold_array[j] = thr_tmp;
-            }
-        }
+	for (j = i + 1; j < ctx->num_cores_online; j++) {
+	    if (threshold_array[i] > threshold_array[j]) {
+		thr_tmp = threshold_array[i];
+		threshold_array[i] = threshold_array[j];
+		threshold_array[j] = thr_tmp;
+	    }
+	}
     }
 
     for (i = 0; i < ctx->num_cores_online; i++) {
-        thr_map |= (threshold_array[i] << (MPMM_THRESHOLD_MAP_NUM_OF_BITS * i));
+	thr_map |= (threshold_array[i] << (MPMM_THRESHOLD_MAP_NUM_OF_BITS * i));
     }
 
     ctx->threshold_map = thr_map;
@@ -292,12 +292,12 @@ static uint32_t mpmm_evaluate_perf_limit
     pct_size = ctx->domain_config->pct_size;
     /* Start from the last index */
     for (pct_idx = (pct_size - 1); pct_idx >= 0; pct_idx--) {
-        /* Find the entry based on the number of online cores */
-        if (ctx->num_cores_online <= pct_config[pct_idx].cores_online) {
-            /* Find the performance limit */
-            return find_perf_limit_from_pct(
-                &pct_config[pct_idx], ctx->threshold_map);
-        }
+	/* Find the entry based on the number of online cores */
+	if (ctx->num_cores_online <= pct_config[pct_idx].cores_online) {
+	    /* Find the performance limit */
+	    return find_perf_limit_from_pct(
+		&pct_config[pct_idx], ctx->threshold_map);
+	}
     }
 
     /* If no entry was found, select the highest number of cores available */
@@ -311,18 +311,18 @@ static void mpmm_monitor_and_control(str
     struct mod_mpmm_core_ctx *core_ctx;
 
     if (domain_ctx->num_cores_online == 0) {
-        return;
+	return;
     }
 
     /* Core level algorithm */
     for (core_idx = 0; core_idx < domain_ctx->num_cores; core_idx++) {
-        core_ctx = &domain_ctx->core_ctx[core_idx];
+	core_ctx = &domain_ctx->core_ctx[core_idx];
 
-        if (!core_ctx->online) {
-            continue;
-        }
+	if (!core_ctx->online) {
+	    continue;
+	}
 
-        mpmm_core_evaluate_threshold(domain_ctx, core_ctx);
+	mpmm_core_evaluate_threshold(domain_ctx, core_ctx);
     }
 
     mpmm_build_threshold_map(domain_ctx);
@@ -347,25 +347,25 @@ static int mpmm_update(struct perf_plugi
      * function argument.
      */
     fwk_id_t perf_id = FWK_ID_ELEMENT(
-        FWK_MODULE_IDX_DVFS, fwk_id_get_element_idx(data->domain_id));
+	FWK_MODULE_IDX_DVFS, fwk_id_get_element_idx(data->domain_id));
 
     for (domain_idx = 0; domain_idx < mpmm_ctx.mpmm_domain_count;
-         domain_idx++) {
-        if (fwk_id_is_equal(
-                mpmm_ctx.domain_ctx[domain_idx].domain_config->perf_id,
-                perf_id)) {
-            break;
-        }
+	 domain_idx++) {
+	if (fwk_id_is_equal(
+		mpmm_ctx.domain_ctx[domain_idx].domain_config->perf_id,
+		perf_id)) {
+	    break;
+	}
     }
 
     if (domain_idx == mpmm_ctx.mpmm_domain_count) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     domain_ctx = &mpmm_ctx.domain_ctx[domain_idx];
 
     if (domain_ctx->num_cores_online == 0) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     mpmm_monitor_and_control(domain_ctx);
@@ -375,8 +375,8 @@ static int mpmm_update(struct perf_plugi
 
     /* set the flag to wait for the transition to complete. */
     if (domain_ctx->perf_limit < domain_ctx->current_perf_level) {
-        domain_ctx->wait_for_perf_transition = true;
-        return FWK_SUCCESS;
+	domain_ctx->wait_for_perf_transition = true;
+	return FWK_SUCCESS;
     }
 
     mpmm_domain_set_thresholds(domain_ctx);
@@ -391,27 +391,27 @@ static int mpmm_report(struct perf_plugi
     struct fwk_event resp_notif;
     struct mod_mpmm_domain_ctx *domain_ctx;
     struct mod_pd_power_state_pre_transition_notification_resp_params
-        *pd_resp_params =
-            (struct mod_pd_power_state_pre_transition_notification_resp_params
-                 *)resp_notif.params;
+	*pd_resp_params =
+	    (struct mod_pd_power_state_pre_transition_notification_resp_params
+		 *)resp_notif.params;
     /*
      * Get the performance element id from the sub-element provided in the
      * function argument.
      */
     fwk_id_t perf_id = FWK_ID_ELEMENT(
-        FWK_MODULE_IDX_DVFS, fwk_id_get_element_idx(data->dep_dom_id));
+	FWK_MODULE_IDX_DVFS, fwk_id_get_element_idx(data->dep_dom_id));
 
     for (domain_idx = 0; domain_idx < mpmm_ctx.mpmm_domain_count;
-         domain_idx++) {
-        if (fwk_id_is_equal(
-                mpmm_ctx.domain_ctx[domain_idx].domain_config->perf_id,
-                perf_id)) {
-            break;
-        }
+	 domain_idx++) {
+	if (fwk_id_is_equal(
+		mpmm_ctx.domain_ctx[domain_idx].domain_config->perf_id,
+		perf_id)) {
+	    break;
+	}
     }
 
     if (domain_idx == mpmm_ctx.mpmm_domain_count) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     domain_ctx = &mpmm_ctx.domain_ctx[domain_idx];
@@ -419,7 +419,7 @@ static int mpmm_report(struct perf_plugi
     domain_ctx->current_perf_level = data->level;
 
     if (!domain_ctx->wait_for_perf_transition) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     domain_ctx->wait_for_perf_transition = false;
@@ -432,23 +432,23 @@ static int mpmm_report(struct perf_plugi
      * so the core can now be turned on.
      */
     for (core_idx = 0; core_idx < domain_ctx->num_cores; core_idx++) {
-        if (domain_ctx->core_ctx[core_idx].pd_blocked) {
-            domain_ctx->core_ctx[core_idx].pd_blocked = false;
+	if (domain_ctx->core_ctx[core_idx].pd_blocked) {
+	    domain_ctx->core_ctx[core_idx].pd_blocked = false;
 
-            status = fwk_get_delayed_response(
-                domain_ctx->domain_id,
-                domain_ctx->core_ctx[core_idx].cookie,
-                &resp_notif);
-            if (status != FWK_SUCCESS) {
-                return status;
-            }
-
-            pd_resp_params->status = FWK_SUCCESS;
-            status = fwk_put_event(&resp_notif);
-            if (status != FWK_SUCCESS) {
-                return status;
-            }
-        }
+	    status = fwk_get_delayed_response(
+		domain_ctx->domain_id,
+		domain_ctx->core_ctx[core_idx].cookie,
+		&resp_notif);
+	    if (status != FWK_SUCCESS) {
+		return status;
+	    }
+
+	    pd_resp_params->status = FWK_SUCCESS;
+	    status = fwk_put_event(&resp_notif);
+	    if (status != FWK_SUCCESS) {
+		return status;
+	    }
+	}
     }
     return FWK_SUCCESS;
 }
@@ -467,12 +467,12 @@ static int mpmm_init(
     const void *data)
 {
     if (element_count == 0) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     mpmm_ctx.mpmm_domain_count = element_count;
     mpmm_ctx.domain_ctx =
-        fwk_mm_calloc(element_count, sizeof(struct mod_mpmm_domain_ctx));
+	fwk_mm_calloc(element_count, sizeof(struct mod_mpmm_domain_ctx));
 
     return FWK_SUCCESS;
 }
@@ -489,8 +489,8 @@ static int mpmm_element_init(
     uint32_t num_thresholds;
 
     if ((sub_element_count == 0) ||
-        (sub_element_count > MPMM_MAX_NUM_CORES_IN_DOMAIN)) {
-        return FWK_E_PARAM;
+	(sub_element_count > MPMM_MAX_NUM_CORES_IN_DOMAIN)) {
+	return FWK_E_PARAM;
     }
 
     domain_ctx = get_domain_ctx(domain_id);
@@ -503,37 +503,37 @@ static int mpmm_element_init(
     fwk_assert(domain_ctx->domain_config->pct != NULL);
 
     if (domain_ctx->domain_config->num_threshold_counters >
-        MPMM_MAX_THRESHOLD_COUNT) {
-        return FWK_E_SUPPORT;
+	MPMM_MAX_THRESHOLD_COUNT) {
+	return FWK_E_SUPPORT;
     }
 
     /* Initialize each core */
     for (core_idx = 0; core_idx < domain_ctx->num_cores; core_idx++) {
-        core_ctx = &domain_ctx->core_ctx[core_idx];
-        core_ctx->core_id = fwk_id_build_sub_element_id(domain_id, core_idx);
-        core_config = &domain_ctx->domain_config->core_config[core_idx];
-        core_ctx->mpmm = (struct mpmm_reg *)core_config->mpmm_reg_base;
-        core_ctx->amu_aux = (struct amu_reg *)core_config->amu_aux_reg_base;
-
-        mpmm_core_get_number_of_thresholds(core_ctx, &num_thresholds);
-
-        if (num_thresholds !=
-            domain_ctx->domain_config->num_threshold_counters) {
-            return FWK_E_DEVICE;
-        }
-
-        /* Create counters storage */
-        core_ctx->cached_counters = fwk_mm_calloc(
-            domain_ctx->domain_config->num_threshold_counters,
-            sizeof(uint32_t));
-        core_ctx->delta = fwk_mm_calloc(
-            domain_ctx->domain_config->num_threshold_counters,
-            sizeof(uint32_t));
-
-        if (core_config->core_starts_online) {
-            domain_ctx->num_cores_online++;
-            core_ctx->online = true;
-        }
+	core_ctx = &domain_ctx->core_ctx[core_idx];
+	core_ctx->core_id = fwk_id_build_sub_element_id(domain_id, core_idx);
+	core_config = &domain_ctx->domain_config->core_config[core_idx];
+	core_ctx->mpmm = (struct mpmm_reg *)core_config->mpmm_reg_base;
+	core_ctx->amu_aux = (struct amu_reg *)core_config->amu_aux_reg_base;
+
+	mpmm_core_get_number_of_thresholds(core_ctx, &num_thresholds);
+
+	if (num_thresholds !=
+	    domain_ctx->domain_config->num_threshold_counters) {
+	    return FWK_E_DEVICE;
+	}
+
+	/* Create counters storage */
+	core_ctx->cached_counters = fwk_mm_calloc(
+	    domain_ctx->domain_config->num_threshold_counters,
+	    sizeof(uint32_t));
+	core_ctx->delta = fwk_mm_calloc(
+	    domain_ctx->domain_config->num_threshold_counters,
+	    sizeof(uint32_t));
+
+	if (core_config->core_starts_online) {
+	    domain_ctx->num_cores_online++;
+	    core_ctx->online = true;
+	}
     }
 
     return FWK_SUCCESS;
@@ -546,28 +546,28 @@ static int mpmm_start(fwk_id_t id)
     struct mod_mpmm_domain_ctx *domain_ctx;
 
     if (fwk_module_is_valid_module_id(id)) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     /* Subscribe to core power state transition */
     domain_ctx = get_domain_ctx(id);
 
     for (i = 0; i < domain_ctx->num_cores; i++) {
-        status = fwk_notification_subscribe(
-            mod_pd_notification_id_power_state_pre_transition,
-            domain_ctx->domain_config->core_config[i].pd_id,
-            domain_ctx->domain_id);
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
-
-        status = fwk_notification_subscribe(
-            mod_pd_notification_id_power_state_transition,
-            domain_ctx->domain_config->core_config[i].pd_id,
-            domain_ctx->domain_id);
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
+	status = fwk_notification_subscribe(
+	    mod_pd_notification_id_power_state_pre_transition,
+	    domain_ctx->domain_config->core_config[i].pd_id,
+	    domain_ctx->domain_id);
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
+
+	status = fwk_notification_subscribe(
+	    mod_pd_notification_id_power_state_transition,
+	    domain_ctx->domain_config->core_config[i].pd_id,
+	    domain_ctx->domain_id);
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
     }
 
     return FWK_SUCCESS;
@@ -578,12 +578,12 @@ static int mpmm_process_notification(
     struct fwk_event *resp_event)
 {
     struct mod_pd_power_state_pre_transition_notification_params
-        *pre_state_params;
+	*pre_state_params;
     struct mod_pd_power_state_transition_notification_params *post_state_params;
     struct mod_pd_power_state_pre_transition_notification_resp_params
-        *pd_resp_params =
-            (struct mod_pd_power_state_pre_transition_notification_resp_params
-                 *)resp_event->params;
+	*pd_resp_params =
+	    (struct mod_pd_power_state_pre_transition_notification_resp_params
+		 *)resp_event->params;
     struct mod_mpmm_domain_ctx *domain_ctx;
     uint32_t core_idx;
     uint32_t perf_limit;
@@ -591,76 +591,76 @@ static int mpmm_process_notification(
     fwk_assert(fwk_module_is_valid_element_id(event->target_id));
     domain_ctx = get_domain_ctx(event->target_id);
     if (domain_ctx == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     /* Find the corresponding core */
     for (core_idx = 0; core_idx < domain_ctx->num_cores; core_idx++) {
-        if (fwk_id_is_equal(
-                domain_ctx->domain_config->core_config[core_idx].pd_id,
-                event->source_id)) {
-            break;
-        }
+	if (fwk_id_is_equal(
+		domain_ctx->domain_config->core_config[core_idx].pd_id,
+		event->source_id)) {
+	    break;
+	}
     }
 
     if (core_idx >= domain_ctx->num_cores) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     if (fwk_id_is_equal(
-            event->id, mod_pd_notification_id_power_state_pre_transition)) {
-        pre_state_params =
-            (struct mod_pd_power_state_pre_transition_notification_params *)
-                event->params;
-        pd_resp_params->status = FWK_SUCCESS;
-        if (pre_state_params->target_state == MOD_PD_STATE_ON) {
-            /* The core is transitioning to online */
-            domain_ctx->num_cores_online++;
-            domain_ctx->core_ctx[core_idx].online = true;
-            /*
-             * After core transition to ON the threshold is set to zero as
-             * defined by the hardware. The next line modifies the threshold
-             * bitmap to include this core threshold.
-             */
-            domain_ctx->threshold_map = domain_ctx->threshold_map
-                << MPMM_THRESHOLD_MAP_NUM_OF_BITS;
-            perf_limit = mpmm_evaluate_perf_limit(domain_ctx);
-
-            /* Set the new limits */
-            struct plugin_limits_req plugin_limit_req = {
-                .domain_id = domain_ctx->domain_config->perf_id,
-                .max_limit = perf_limit,
-            };
-            mpmm_ctx.perf_plugins_handler_api->plugin_set_limits(
-                &plugin_limit_req);
-
-            domain_ctx->perf_limit = perf_limit;
-
-            /*
-             * If the perf limit requested will not trigger a dvfs change, there
-             * is no need to block the power domain state transition.
-             */
-            if (perf_limit >= domain_ctx->current_perf_level) {
-                return FWK_SUCCESS;
-            }
-
-            /* Block the power domain until the new level is applied */
-            domain_ctx->core_ctx[core_idx].pd_blocked = true;
-            domain_ctx->wait_for_perf_transition = true;
-            resp_event->is_delayed_response = true;
-            domain_ctx->core_ctx[core_idx].cookie = event->cookie;
-        }
+	    event->id, mod_pd_notification_id_power_state_pre_transition)) {
+	pre_state_params =
+	    (struct mod_pd_power_state_pre_transition_notification_params *)
+		event->params;
+	pd_resp_params->status = FWK_SUCCESS;
+	if (pre_state_params->target_state == MOD_PD_STATE_ON) {
+	    /* The core is transitioning to online */
+	    domain_ctx->num_cores_online++;
+	    domain_ctx->core_ctx[core_idx].online = true;
+	    /*
+	     * After core transition to ON the threshold is set to zero as
+	     * defined by the hardware. The next line modifies the threshold
+	     * bitmap to include this core threshold.
+	     */
+	    domain_ctx->threshold_map = domain_ctx->threshold_map
+		<< MPMM_THRESHOLD_MAP_NUM_OF_BITS;
+	    perf_limit = mpmm_evaluate_perf_limit(domain_ctx);
+
+	    /* Set the new limits */
+	    struct plugin_limits_req plugin_limit_req = {
+		.domain_id = domain_ctx->domain_config->perf_id,
+		.max_limit = perf_limit,
+	    };
+	    mpmm_ctx.perf_plugins_handler_api->plugin_set_limits(
+		&plugin_limit_req);
+
+	    domain_ctx->perf_limit = perf_limit;
+
+	    /*
+	     * If the perf limit requested will not trigger a dvfs change, there
+	     * is no need to block the power domain state transition.
+	     */
+	    if (perf_limit >= domain_ctx->current_perf_level) {
+		return FWK_SUCCESS;
+	    }
+
+	    /* Block the power domain until the new level is applied */
+	    domain_ctx->core_ctx[core_idx].pd_blocked = true;
+	    domain_ctx->wait_for_perf_transition = true;
+	    resp_event->is_delayed_response = true;
+	    domain_ctx->core_ctx[core_idx].cookie = event->cookie;
+	}
 
     } else if (fwk_id_is_equal(
-                   event->id, mod_pd_notification_id_power_state_transition)) {
-        post_state_params =
-            (struct mod_pd_power_state_transition_notification_params *)
-                event->params;
-        if (post_state_params->state != MOD_PD_STATE_ON) {
-            /* The core transitioned to offline */
-            domain_ctx->num_cores_online--;
-            domain_ctx->core_ctx[core_idx].online = false;
-        }
+		   event->id, mod_pd_notification_id_power_state_transition)) {
+	post_state_params =
+	    (struct mod_pd_power_state_transition_notification_params *)
+		event->params;
+	if (post_state_params->state != MOD_PD_STATE_ON) {
+	    /* The core transitioned to offline */
+	    domain_ctx->num_cores_online--;
+	    domain_ctx->core_ctx[core_idx].online = false;
+	}
     }
 
     return FWK_SUCCESS;
@@ -670,13 +670,13 @@ static int mpmm_bind(fwk_id_t id, unsign
 {
     /* Bind in the second round */
     if ((round == 0) || (!fwk_module_is_valid_module_id(id))) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     return fwk_module_bind(
-        FWK_ID_MODULE(FWK_MODULE_IDX_SCMI_PERF),
-        FWK_ID_API(FWK_MODULE_IDX_SCMI_PERF, MOD_SCMI_PERF_PLUGINS_API),
-        &mpmm_ctx.perf_plugins_handler_api);
+	FWK_ID_MODULE(FWK_MODULE_IDX_SCMI_PERF),
+	FWK_ID_API(FWK_MODULE_IDX_SCMI_PERF, MOD_SCMI_PERF_PLUGINS_API),
+	&mpmm_ctx.perf_plugins_handler_api);
 }
 
 static int mpmm_process_bind_request(
@@ -686,9 +686,9 @@ static int mpmm_process_bind_request(
     const void **api)
 {
     if (fwk_id_is_equal(source_id, FWK_ID_MODULE(FWK_MODULE_IDX_SCMI_PERF))) {
-        *api = &perf_plugins_api;
+	*api = &perf_plugins_api;
     } else {
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 
     return FWK_SUCCESS;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/msg_smt/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/msg_smt/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/msg_smt/CMakeLists.txt
@@ -8,9 +8,9 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_msg_smt.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_msg_smt.c")
 
 target_link_libraries(${SCP_MODULE_TARGET} PRIVATE module-scmi)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/msg_smt/src/mod_msg_smt.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/msg_smt/src/mod_msg_smt.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/msg_smt/src/mod_msg_smt.c
@@ -77,8 +77,8 @@ static struct smt_ctx smt_ctx;
 static int smt_get_secure(fwk_id_t channel_id, bool *secure)
 {
     if (secure == NULL) {
-        fwk_assert(false);
-        return FWK_E_PARAM;
+	fwk_assert(false);
+	return FWK_E_PARAM;
     }
 
     /* No msg mailbox is considered secure */
@@ -92,12 +92,12 @@ static int smt_get_max_payload_size(fwk_
     struct smt_channel_ctx *channel_ctx;
 
     if (size == NULL) {
-        fwk_assert(false);
-        return FWK_E_PARAM;
+	fwk_assert(false);
+	return FWK_E_PARAM;
     }
 
     channel_ctx =
-        &smt_ctx.channel_ctx_table[fwk_id_get_element_idx(channel_id)];
+	&smt_ctx.channel_ctx_table[fwk_id_get_element_idx(channel_id)];
 
     *size = channel_ctx->max_payload_size;
 
@@ -109,15 +109,15 @@ static int smt_get_message_header(fwk_id
     struct smt_channel_ctx *channel_ctx;
 
     if (header == NULL) {
-        fwk_assert(false);
-        return FWK_E_PARAM;
+	fwk_assert(false);
+	return FWK_E_PARAM;
     }
 
     channel_ctx =
-        &smt_ctx.channel_ctx_table[fwk_id_get_element_idx(channel_id)];
+	&smt_ctx.channel_ctx_table[fwk_id_get_element_idx(channel_id)];
 
     if (!channel_ctx->locked) {
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 
     *header = channel_ctx->in->message_header;
@@ -126,53 +126,53 @@ static int smt_get_message_header(fwk_id
 }
 
 static int smt_get_payload(fwk_id_t channel_id,
-                           const void **payload,
-                           size_t *size)
+			   const void **payload,
+			   size_t *size)
 {
     struct smt_channel_ctx *channel_ctx;
 
     if (payload == NULL) {
-        fwk_assert(false);
-        return FWK_E_PARAM;
+	fwk_assert(false);
+	return FWK_E_PARAM;
     }
 
     channel_ctx =
-        &smt_ctx.channel_ctx_table[fwk_id_get_element_idx(channel_id)];
+	&smt_ctx.channel_ctx_table[fwk_id_get_element_idx(channel_id)];
 
     if (!channel_ctx->locked)
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
 
     *payload = channel_ctx->in->payload;
 
     if (size != NULL) {
-        *size = channel_ctx->in_len -
-            sizeof(channel_ctx->in->message_header);
+	*size = channel_ctx->in_len -
+	    sizeof(channel_ctx->in->message_header);
     }
 
     return FWK_SUCCESS;
 }
 
 static int smt_write_payload(fwk_id_t channel_id,
-                             size_t offset,
-                             const void *payload,
-                             size_t size)
+			     size_t offset,
+			     const void *payload,
+			     size_t size)
 {
     struct smt_channel_ctx *channel_ctx;
 
     channel_ctx =
-        &smt_ctx.channel_ctx_table[fwk_id_get_element_idx(channel_id)];
+	&smt_ctx.channel_ctx_table[fwk_id_get_element_idx(channel_id)];
 
     if ((payload == NULL)                         ||
-        (offset  > channel_ctx->max_payload_size) ||
-        (size > channel_ctx->max_payload_size)    ||
-        ((offset + size) > channel_ctx->max_payload_size)) {
+	(offset  > channel_ctx->max_payload_size) ||
+	(size > channel_ctx->max_payload_size)    ||
+	((offset + size) > channel_ctx->max_payload_size)) {
 
-        fwk_assert(false);
-        return FWK_E_PARAM;
+	fwk_assert(false);
+	return FWK_E_PARAM;
     }
 
     if (!channel_ctx->locked)
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
 
     memcpy(((uint8_t*)channel_ctx->out->payload) + offset, payload, size);
 
@@ -189,7 +189,7 @@ static int smt_respond(fwk_id_t channel_
 
     /* Copy the payload from either the write buffer or the payload parameter */
     if (payload) {
-        memcpy(memory->payload, payload, size);
+	memcpy(memory->payload, payload, size);
     }
 
     channel_ctx->locked = false;
@@ -208,14 +208,14 @@ static int smt_transmit(fwk_id_t channel
     struct mod_msg_smt_memory *memory;
 
     if (payload == NULL) {
-        return FWK_E_DATA;
+	return FWK_E_DATA;
     }
 
     channel_ctx = &smt_ctx.channel_ctx_table[fwk_id_get_element_idx(channel_id)];
     memory = ((struct mod_msg_smt_memory *) channel_ctx->out);
 
     if (!channel_ctx->locked) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     memory->message_header = message_header;
@@ -259,9 +259,9 @@ static int smt_requester_handler(struct
 
     /* Let SCMI handle the message */
     status =
-        channel_ctx->scmi_api->signal_message(channel_ctx->scmi_service_id);
+	channel_ctx->scmi_api->signal_message(channel_ctx->scmi_service_id);
     if (status != FWK_SUCCESS) {
-        return FWK_E_HANDLER;
+	return FWK_E_HANDLER;
     }
 
     return FWK_SUCCESS;
@@ -280,18 +280,18 @@ int msg_signal_message(fwk_id_t channel_
     struct smt_channel_ctx *channel_ctx;
 
     channel_ctx =
-        &smt_ctx.channel_ctx_table[fwk_id_get_element_idx(channel_id)];
+	&smt_ctx.channel_ctx_table[fwk_id_get_element_idx(channel_id)];
 
     if (!channel_ctx->msg_smt_mailbox_ready) {
-        /* Discard any message in the mailbox when not ready */
-        FWK_LOG_ERR("[MSG_SMT] Message not valid\n");
+	/* Discard any message in the mailbox when not ready */
+	FWK_LOG_ERR("[MSG_SMT] Message not valid\n");
 
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     /* Check if we are already processing */
     if (channel_ctx->locked) {
-        return FWK_E_STATE;
+	return FWK_E_STATE;
     }
 
     /* Save input/output buffers */
@@ -302,13 +302,13 @@ int msg_signal_message(fwk_id_t channel_
 
     switch (channel_ctx->config->type) {
     case MOD_MSG_SMT_CHANNEL_TYPE_REQUESTER:
-        return smt_requester_handler(channel_ctx);
+	return smt_requester_handler(channel_ctx);
     case MOD_MSG_SMT_CHANNEL_TYPE_COMPLETER:
-        return smt_completer_handler(channel_ctx);
+	return smt_completer_handler(channel_ctx);
     default:
-        /* Invalid config */
-        fwk_assert(false);
-        break;
+	/* Invalid config */
+	fwk_assert(false);
+	break;
     }
 
     return FWK_SUCCESS;
@@ -322,17 +322,17 @@ static const struct mod_msg_smt_driver_i
  * Framework API
  */
 static int msg_init(fwk_id_t module_id, unsigned int element_count,
-                       const void *data)
+		       const void *data)
 {
 
     if (element_count == 0) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     smt_ctx.channel_ctx_table = fwk_mm_calloc(element_count,
-                                              sizeof(*smt_ctx.channel_ctx_table));
+					      sizeof(*smt_ctx.channel_ctx_table));
     if (smt_ctx.channel_ctx_table == NULL) {
-        return FWK_E_NOMEM;
+	return FWK_E_NOMEM;
     }
 
     smt_ctx.channel_count = element_count;
@@ -341,7 +341,7 @@ static int msg_init(fwk_id_t module_id,
 }
 
 static int msg_channel_init(fwk_id_t channel_id, unsigned int slot_count,
-                            const void *data)
+			    const void *data)
 {
     size_t elt_idx = fwk_id_get_element_idx(channel_id);
     struct smt_channel_ctx *channel_ctx = &smt_ctx.channel_ctx_table[elt_idx];
@@ -350,8 +350,8 @@ static int msg_channel_init(fwk_id_t cha
 
     /* Validate channel config */
     if (channel_ctx->config->type >= MOD_MSG_SMT_CHANNEL_TYPE_COUNT) {
-        fwk_assert(false);
-        return FWK_E_DATA;
+	fwk_assert(false);
+	return FWK_E_DATA;
     }
 
     channel_ctx->in = NULL;
@@ -360,7 +360,7 @@ static int msg_channel_init(fwk_id_t cha
     channel_ctx->out_len = 0;
 
     channel_ctx->max_payload_size = channel_ctx->config->mailbox_size -
-        sizeof(struct mod_msg_smt_memory);
+	sizeof(struct mod_msg_smt_memory);
 
     channel_ctx->msg_smt_mailbox_ready = true;
 
@@ -373,90 +373,90 @@ static int msg_bind(fwk_id_t id, unsigne
     struct smt_channel_ctx *channel_ctx;
 
     if (fwk_id_is_type(id, FWK_ID_TYPE_MODULE)) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     channel_ctx = &smt_ctx.channel_ctx_table[fwk_id_get_element_idx(id)];
 
     if (round == 0) {
 
-        status = fwk_module_bind(channel_ctx->config->driver_id,
-                                 channel_ctx->config->driver_api_id,
-                                 &channel_ctx->driver_api);
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
+	status = fwk_module_bind(channel_ctx->config->driver_id,
+				 channel_ctx->config->driver_api_id,
+				 &channel_ctx->driver_api);
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
 
-        channel_ctx->driver_id = channel_ctx->config->driver_id;
+	channel_ctx->driver_id = channel_ctx->config->driver_id;
 
     } else if (round == 1) {
 
-        status = fwk_module_bind(channel_ctx->scmi_service_id,
-                                 FWK_ID_API(FWK_MODULE_IDX_SCMI, MOD_SCMI_API_IDX_TRANSPORT),
-                                 &channel_ctx->scmi_api);
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
+	status = fwk_module_bind(channel_ctx->scmi_service_id,
+				 FWK_ID_API(FWK_MODULE_IDX_SCMI, MOD_SCMI_API_IDX_TRANSPORT),
+				 &channel_ctx->scmi_api);
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
     }
 
     return FWK_SUCCESS;
 }
 
 static int msg_process_bind_request(fwk_id_t source_id,
-                                       fwk_id_t target_id,
-                                       fwk_id_t api_id,
-                                       const void **api)
+				       fwk_id_t target_id,
+				       fwk_id_t api_id,
+				       const void **api)
 {
     struct smt_channel_ctx *channel_ctx = NULL;
     size_t elt_idx;
 
     /* Only bind to a channel (not the whole module) */
     if (!fwk_id_is_type(target_id, FWK_ID_TYPE_ELEMENT)) {
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 
     elt_idx = fwk_id_get_element_idx(target_id);
     if (elt_idx >= smt_ctx.channel_count) {
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 
     channel_ctx = &smt_ctx.channel_ctx_table[elt_idx];
 
     switch (fwk_id_get_api_idx(api_id)) {
     case MOD_MSG_SMT_API_IDX_DRIVER_INPUT:
-        /* Driver input API */
+	/* Driver input API */
 
        /*
-         * Make sure that the element that is trying to bind to us is the
-         * same element that we previously bound to.
-         *
-         * NOTE: We bound to an element but a sub-element should be binding
-         * back to us. This means we cannot use fwk_id_is_equal() because
-         * the ids have different types. For now we compare the indicies
-         * manually.
-         */
-        if (fwk_id_get_module_idx(channel_ctx->driver_id) ==
-            fwk_id_get_module_idx(source_id) &&
-            fwk_id_get_element_idx(channel_ctx->driver_id) ==
-            fwk_id_get_element_idx(source_id)) {
-            *api = &driver_input_api;
+	 * Make sure that the element that is trying to bind to us is the
+	 * same element that we previously bound to.
+	 *
+	 * NOTE: We bound to an element but a sub-element should be binding
+	 * back to us. This means we cannot use fwk_id_is_equal() because
+	 * the ids have different types. For now we compare the indicies
+	 * manually.
+	 */
+	if (fwk_id_get_module_idx(channel_ctx->driver_id) ==
+	    fwk_id_get_module_idx(source_id) &&
+	    fwk_id_get_element_idx(channel_ctx->driver_id) ==
+	    fwk_id_get_element_idx(source_id)) {
+	    *api = &driver_input_api;
        } else {
-            /* A module that we did not bind to is trying to bind to us */
-            fwk_assert(false);
-            return FWK_E_ACCESS;
-        }
-        break;
+	    /* A module that we did not bind to is trying to bind to us */
+	    fwk_assert(false);
+	    return FWK_E_ACCESS;
+	}
+	break;
 
     case MOD_MSG_SMT_API_IDX_SCMI_TRANSPORT:
-        /* SCMI transport API */
-        *api = &smt_mod_scmi_to_transport_api;
-        channel_ctx->scmi_service_id = source_id;
-        break;
+	/* SCMI transport API */
+	*api = &smt_mod_scmi_to_transport_api;
+	channel_ctx->scmi_service_id = source_id;
+	break;
 
     default:
-        /* Invalid API */
-        fwk_assert(false);
-        return FWK_E_PARAM;
+	/* Invalid API */
+	fwk_assert(false);
+	return FWK_E_PARAM;
     }
 
     return FWK_SUCCESS;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/msys_rom/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/msys_rom/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/msys_rom/CMakeLists.txt
@@ -8,11 +8,11 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_msys_rom.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_msys_rom.c")
 
 target_link_libraries(
     ${SCP_MODULE_TARGET} PRIVATE module-bootloader module-power-domain
-                                 module-ppu-v1)
+				 module-ppu-v1)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/msys_rom/include/mod_msys_rom.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/msys_rom/include/mod_msys_rom.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/msys_rom/include/mod_msys_rom.h
@@ -55,7 +55,7 @@ enum mod_msys_rom_notification_idx {
 /*! <tt>SYSTOP powered on</tt> notification identifier */
 static const fwk_id_t mod_msys_rom_notification_id_systop =
     FWK_ID_NOTIFICATION_INIT(FWK_MODULE_IDX_MSYS_ROM,
-                             MOD_MSYS_ROM_NOTIFICATION_IDX_POWER_SYSTOP);
+			     MOD_MSYS_ROM_NOTIFICATION_IDX_POWER_SYSTOP);
 
 /*!
  * \}
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/msys_rom/src/mod_msys_rom.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/msys_rom/src/mod_msys_rom.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/msys_rom/src/mod_msys_rom.c
@@ -45,8 +45,8 @@ static int msys_deferred_setup(void)
 
     /* Initialize the AP context area by zeroing it */
     memset((void *)ctx.rom_config->ap_context_base,
-           0,
-           ctx.rom_config->ap_context_size);
+	   0,
+	   ctx.rom_config->ap_context_size);
 
     /* Power on the primary cluster and cpu */
     ctx.ppu_boot_api->power_mode_on(ctx.rom_config->id_primary_cluster);
@@ -55,8 +55,8 @@ static int msys_deferred_setup(void)
     status = ctx.bootloader_api->load_image();
 
     FWK_LOG_CRIT(
-        "[MSYS-ROM] Failed to load RAM firmware image: %s",
-        fwk_status_str(status));
+	"[MSYS-ROM] Failed to load RAM firmware image: %s",
+	fwk_status_str(status));
 
     return FWK_E_DATA;
 }
@@ -71,8 +71,8 @@ static int msys_rom_init(fwk_id_t module
     ctx.rom_config = data;
 
     if ((ctx.rom_config->ap_context_base == 0) ||
-        (ctx.rom_config->ap_context_size == 0))
-        return FWK_E_RANGE;
+	(ctx.rom_config->ap_context_size == 0))
+	return FWK_E_RANGE;
 
     return FWK_SUCCESS;
 }
@@ -84,20 +84,20 @@ static int msys_rom_bind(fwk_id_t id, un
     /* Use second round only (round numbering is zero-indexed) */
     if (round == 1) {
 
-        /* Bind to the PPU module */
-        status = fwk_module_bind(FWK_ID_MODULE(FWK_MODULE_IDX_PPU_V1),
-                                 FWK_ID_API(FWK_MODULE_IDX_PPU_V1,
-                                            MOD_PPU_V1_API_IDX_BOOT),
-                                 &ctx.ppu_boot_api);
-        if (status != FWK_SUCCESS)
-            return FWK_E_PANIC;
-
-        /* Bind to the bootloader module */
-        status = fwk_module_bind(FWK_ID_MODULE(FWK_MODULE_IDX_BOOTLOADER),
-                                 FWK_ID_API(FWK_MODULE_IDX_BOOTLOADER, 0),
-                                 &ctx.bootloader_api);
-        if (status != FWK_SUCCESS)
-        return FWK_E_PANIC;
+	/* Bind to the PPU module */
+	status = fwk_module_bind(FWK_ID_MODULE(FWK_MODULE_IDX_PPU_V1),
+				 FWK_ID_API(FWK_MODULE_IDX_PPU_V1,
+					    MOD_PPU_V1_API_IDX_BOOT),
+				 &ctx.ppu_boot_api);
+	if (status != FWK_SUCCESS)
+	    return FWK_E_PANIC;
+
+	/* Bind to the bootloader module */
+	status = fwk_module_bind(FWK_ID_MODULE(FWK_MODULE_IDX_BOOTLOADER),
+				 FWK_ID_API(FWK_MODULE_IDX_BOOTLOADER, 0),
+				 &ctx.bootloader_api);
+	if (status != FWK_SUCCESS)
+	return FWK_E_PANIC;
     }
 
     return FWK_SUCCESS;
@@ -106,9 +106,9 @@ static int msys_rom_bind(fwk_id_t id, un
 static int msys_rom_start(fwk_id_t id)
 {
     struct fwk_event event = {
-        .source_id = FWK_ID_MODULE(FWK_MODULE_IDX_MSYS_ROM),
-        .target_id = FWK_ID_MODULE(FWK_MODULE_IDX_MSYS_ROM),
-        .id = FWK_ID_EVENT(FWK_MODULE_IDX_MSYS_ROM, ROM_EVENT_RUN),
+	.source_id = FWK_ID_MODULE(FWK_MODULE_IDX_MSYS_ROM),
+	.target_id = FWK_ID_MODULE(FWK_MODULE_IDX_MSYS_ROM),
+	.id = FWK_ID_EVENT(FWK_MODULE_IDX_MSYS_ROM, ROM_EVENT_RUN),
     };
 
     return fwk_put_event(&event);
@@ -119,26 +119,26 @@ static int msys_rom_process_event(const
 {
     int status;
     struct mod_pd_power_state_transition_notification_params
-        *notification_params;
+	*notification_params;
     struct fwk_event systop_on_event = {
-        .response_requested = true,
-        .id = mod_msys_rom_notification_id_systop,
-        .source_id = FWK_ID_NONE
+	.response_requested = true,
+	.id = mod_msys_rom_notification_id_systop,
+	.source_id = FWK_ID_NONE
     };
 
     /* Notify any subscribers of the SYSTOP power domain state change */
     notification_params =
-        (struct mod_pd_power_state_transition_notification_params *)
-            systop_on_event.params;
+	(struct mod_pd_power_state_transition_notification_params *)
+	    systop_on_event.params;
     notification_params->state = MOD_PD_STATE_ON;
 
     status = fwk_notification_notify(&systop_on_event, &ctx.notification_count);
     if (status != FWK_SUCCESS)
-        return status;
+	return status;
 
     /* Complete remaining setup now if there are no subscribers to respond */
     if (ctx.notification_count == 0)
-        return msys_deferred_setup();
+	return msys_deferred_setup();
 
     return FWK_SUCCESS;
 }
@@ -148,18 +148,18 @@ static int msys_rom_process_notification
     struct fwk_event *resp_event)
 {
     fwk_assert(
-        fwk_id_is_equal(event->id, mod_msys_rom_notification_id_systop));
+	fwk_id_is_equal(event->id, mod_msys_rom_notification_id_systop));
     fwk_assert(event->is_response == true);
 
     /* At least one notification response must be outstanding */
     if (ctx.notification_count == 0) {
-        fwk_unexpected();
-        return FWK_E_PANIC;
+	fwk_unexpected();
+	return FWK_E_PANIC;
     }
 
     /* Complete remaining setup now that all subscribers have responded */
     if ((--ctx.notification_count) == 0)
-        return msys_deferred_setup();
+	return msys_deferred_setup();
 
     return FWK_SUCCESS;
 }
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/optee/clock/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/optee/clock/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/optee/clock/CMakeLists.txt
@@ -8,10 +8,10 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_optee_clock.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_optee_clock.c")
 
 target_link_libraries(${SCP_MODULE_TARGET}
-                      PRIVATE module-clock)
+		      PRIVATE module-clock)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/optee/clock/src/mod_optee_clock.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/optee/clock/src/mod_optee_clock.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/optee/clock/src/mod_optee_clock.c
@@ -42,7 +42,7 @@ static struct optee_clock_module_ctx mod
 static struct optee_clock_dev_ctx *elt_id_to_ctx(fwk_id_t dev_id)
 {
     if (!fwk_module_is_valid_element_id(dev_id)) {
-        return NULL;
+	return NULL;
     }
 
     return module_ctx.dev_ctx + fwk_id_get_element_idx(dev_id);
@@ -61,21 +61,21 @@ static int get_rate(fwk_id_t dev_id, uin
     struct optee_clock_dev_ctx *ctx = elt_id_to_ctx(dev_id);
 
     if ((ctx == NULL) || (rate == NULL)) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     if (!is_exposed(ctx)) {
-        *rate = 0;
-        return FWK_SUCCESS;
+	*rate = 0;
+	return FWK_SUCCESS;
     }
 
     *rate = clk_get_rate(ctx->clk);
 
     FWK_LOG_DEBUG(
-        MOD_NAME "SCMI optee_clock (%u/\"%s\"): clk_get_rate() = %" PRIu64,
-        fwk_id_get_element_idx(dev_id),
-        clk_get_name(ctx->clk),
-        *rate);
+	MOD_NAME "SCMI optee_clock (%u/\"%s\"): clk_get_rate() = %" PRIu64,
+	fwk_id_get_element_idx(dev_id),
+	clk_get_name(ctx->clk),
+	*rate);
 
     return FWK_SUCCESS;
 }
@@ -85,58 +85,58 @@ static int set_state(fwk_id_t dev_id, en
     struct optee_clock_dev_ctx *ctx = elt_id_to_ctx(dev_id);
 
     if (ctx == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     switch (state) {
     case MOD_CLOCK_STATE_STOPPED:
     case MOD_CLOCK_STATE_RUNNING:
-        break;
+	break;
     default:
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     if (!is_exposed(ctx)) {
-        if (state == MOD_CLOCK_STATE_STOPPED) {
-            return FWK_SUCCESS;
-        } else {
-            return FWK_E_ACCESS;
-        }
+	if (state == MOD_CLOCK_STATE_STOPPED) {
+	    return FWK_SUCCESS;
+	} else {
+	    return FWK_E_ACCESS;
+	}
     }
 
     if (state == MOD_CLOCK_STATE_STOPPED) {
-        if (ctx->enabled) {
-            FWK_LOG_DEBUG(
-                MOD_NAME "SCMI optee_clock (%u/\"%s\") disable",
-                fwk_id_get_element_idx(dev_id),
-                clk_get_name(ctx->clk));
-
-            clk_disable(ctx->clk);
-            ctx->enabled = false;
-        } else {
-            FWK_LOG_DEBUG(
-                MOD_NAME "SCMI optee_clock (%u/\"%s\") is already OFF",
-                fwk_id_get_element_idx(dev_id),
-                clk_get_name(ctx->clk));
-        }
+	if (ctx->enabled) {
+	    FWK_LOG_DEBUG(
+		MOD_NAME "SCMI optee_clock (%u/\"%s\") disable",
+		fwk_id_get_element_idx(dev_id),
+		clk_get_name(ctx->clk));
+
+	    clk_disable(ctx->clk);
+	    ctx->enabled = false;
+	} else {
+	    FWK_LOG_DEBUG(
+		MOD_NAME "SCMI optee_clock (%u/\"%s\") is already OFF",
+		fwk_id_get_element_idx(dev_id),
+		clk_get_name(ctx->clk));
+	}
     } else {
-        if (!ctx->enabled) {
-            FWK_LOG_DEBUG(
-                MOD_NAME "SCMI optee_clock (%u/\"%s\") enable",
-                fwk_id_get_element_idx(dev_id),
-                clk_get_name(ctx->clk));
-
-            if (clk_enable(ctx->clk)) {
-                return FWK_E_DEVICE;
-            }
-
-            ctx->enabled = true;
-        } else {
-            FWK_LOG_DEBUG(
-                MOD_NAME "SCMI optee_clock (%u/\"%s\") is already ON",
-                fwk_id_get_element_idx(dev_id),
-                clk_get_name(ctx->clk));
-        }
+	if (!ctx->enabled) {
+	    FWK_LOG_DEBUG(
+		MOD_NAME "SCMI optee_clock (%u/\"%s\") enable",
+		fwk_id_get_element_idx(dev_id),
+		clk_get_name(ctx->clk));
+
+	    if (clk_enable(ctx->clk)) {
+		return FWK_E_DEVICE;
+	    }
+
+	    ctx->enabled = true;
+	} else {
+	    FWK_LOG_DEBUG(
+		MOD_NAME "SCMI optee_clock (%u/\"%s\") is already ON",
+		fwk_id_get_element_idx(dev_id),
+		clk_get_name(ctx->clk));
+	}
     }
 
     return FWK_SUCCESS;
@@ -147,25 +147,25 @@ static int get_state(fwk_id_t dev_id, en
     struct optee_clock_dev_ctx *ctx = elt_id_to_ctx(dev_id);
 
     if ((ctx == NULL) || (state == NULL)) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     if (!is_exposed(ctx)) {
-        *state = MOD_CLOCK_STATE_STOPPED;
-        return FWK_SUCCESS;
+	*state = MOD_CLOCK_STATE_STOPPED;
+	return FWK_SUCCESS;
     }
 
     if (ctx->enabled) {
-        *state = MOD_CLOCK_STATE_RUNNING;
+	*state = MOD_CLOCK_STATE_RUNNING;
     } else {
-        *state = MOD_CLOCK_STATE_STOPPED;
+	*state = MOD_CLOCK_STATE_STOPPED;
     }
 
     FWK_LOG_DEBUG(
-        MOD_NAME "SCMI optee_clock (%u/\"%s\") is %s",
-        fwk_id_get_element_idx(dev_id),
-        clk_get_name(ctx->clk),
-        *state == MOD_CLOCK_STATE_STOPPED ? "off" : "on");
+	MOD_NAME "SCMI optee_clock (%u/\"%s\") is %s",
+	fwk_id_get_element_idx(dev_id),
+	clk_get_name(ctx->clk),
+	*state == MOD_CLOCK_STATE_STOPPED ? "off" : "on");
 
     return FWK_SUCCESS;
 }
@@ -179,43 +179,43 @@ static int get_range(fwk_id_t dev_id, st
     TEE_Result res;
 
     if ((ctx == NULL) || (range == NULL)) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     if (!is_exposed(ctx)) {
-        range->rate_type = MOD_CLOCK_RATE_TYPE_DISCRETE;
-        range->min = 0;
-        range->max = 0;
-        range->rate_count = 1;
+	range->rate_type = MOD_CLOCK_RATE_TYPE_DISCRETE;
+	range->min = 0;
+	range->max = 0;
+	range->rate_count = 1;
 
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     res = clk_get_rates_array(ctx->clk, 0, NULL, &rate_count);
     if (res == TEE_ERROR_NOT_SUPPORTED) {
-        unsigned long max = 0;
+	unsigned long max = 0;
 	unsigned long min = 0;
 	unsigned long step = 0;
 
-        res = clk_get_rates_steps(ctx->clk, &min, &max, &step);
+	res = clk_get_rates_steps(ctx->clk, &min, &max, &step);
 
-        if (res == TEE_ERROR_NOT_SUPPORTED) {
-            range->rate_type = MOD_CLOCK_RATE_TYPE_DISCRETE;
-            range->min = clk_get_rate(ctx->clk);
-            range->max = range->min;
-            range->rate_count = 1;
-        } else if (res != TEE_SUCCESS) {
-            return FWK_E_DEVICE;
-        }
+	if (res == TEE_ERROR_NOT_SUPPORTED) {
+	    range->rate_type = MOD_CLOCK_RATE_TYPE_DISCRETE;
+	    range->min = clk_get_rate(ctx->clk);
+	    range->max = range->min;
+	    range->rate_count = 1;
+	} else if (res != TEE_SUCCESS) {
+	    return FWK_E_DEVICE;
+	}
 
-        range->rate_type = MOD_CLOCK_RATE_TYPE_CONTINUOUS;
+	range->rate_type = MOD_CLOCK_RATE_TYPE_CONTINUOUS;
 	range->min = min;
 	range->max = max;
 	range->step = step;
 
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     } else if (res != TEE_SUCCESS) {
-        return FWK_E_DEVICE;
+	return FWK_E_DEVICE;
     }
 
     range->rate_type = MOD_CLOCK_RATE_TYPE_DISCRETE;
@@ -224,53 +224,53 @@ static int get_range(fwk_id_t dev_id, st
     range->max = 0;
 
     for (rate_idx = 0; rate_idx < rate_count; rate_idx++) {
-        size_t count = 1;
+	size_t count = 1;
 
-        res = clk_get_rates_array(ctx->clk, rate_idx, &rate, &count);
-        fwk_assert(!res && count == 1);
+	res = clk_get_rates_array(ctx->clk, rate_idx, &rate, &count);
+	fwk_assert(!res && count == 1);
 
-        if (rate > range->max) {
-            range->max = rate;
-        } else if (rate < range->min) {
-            range->min = rate;
-        }
+	if (rate > range->max) {
+	    range->max = rate;
+	} else if (rate < range->min) {
+	    range->min = rate;
+	}
     }
 
     return FWK_SUCCESS;
 }
 
 static int set_rate(fwk_id_t dev_id, uint64_t rate,
-                    enum mod_clock_round_mode round_mode)
+		    enum mod_clock_round_mode round_mode)
 {
     struct optee_clock_dev_ctx *ctx = elt_id_to_ctx(dev_id);
     TEE_Result res;
 
     if (ctx == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     if (!is_exposed(ctx)) {
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 
     res = clk_set_rate(ctx->clk, rate);
     if (res == TEE_ERROR_NOT_SUPPORTED) {
-        return FWK_E_SUPPORT;
+	return FWK_E_SUPPORT;
     } else if (res != TEE_SUCCESS) {
-        return FWK_E_DEVICE;
+	return FWK_E_DEVICE;
     }
 
     FWK_LOG_DEBUG(
-        MOD_NAME "SCMI optee_clock (%u/\"%s\"): rate = %" PRIu64,
-        fwk_id_get_element_idx(dev_id),
-        clk_get_name(ctx->clk),
-        rate);
+	MOD_NAME "SCMI optee_clock (%u/\"%s\"): rate = %" PRIu64,
+	fwk_id_get_element_idx(dev_id),
+	clk_get_name(ctx->clk),
+	rate);
 
     return FWK_SUCCESS;
 }
 
 static int get_rate_from_index(fwk_id_t dev_id,
-                               unsigned int rate_index, uint64_t *rate)
+			       unsigned int rate_index, uint64_t *rate)
 {
     struct optee_clock_dev_ctx *ctx = elt_id_to_ctx(dev_id);
     unsigned long rate_ul;
@@ -278,28 +278,28 @@ static int get_rate_from_index(fwk_id_t
     TEE_Result res;
 
     if ((ctx == NULL) || (rate == NULL)) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     if (!is_exposed(ctx)) {
-        *rate = 0;
-        return FWK_SUCCESS;
+	*rate = 0;
+	return FWK_SUCCESS;
     }
 
     res = clk_get_rates_array(ctx->clk, 0, NULL, &rate_count);
     if (res == TEE_ERROR_NOT_SUPPORTED) {
-        if (rate_index > 0) {
-            return FWK_E_PARAM;
-        }
+	if (rate_index > 0) {
+	    return FWK_E_PARAM;
+	}
 
-        *rate = clk_get_rate(ctx->clk);
-        return FWK_SUCCESS;
+	*rate = clk_get_rate(ctx->clk);
+	return FWK_SUCCESS;
     } else if (res != TEE_SUCCESS) {
-        return FWK_E_DEVICE;
+	return FWK_E_DEVICE;
     }
 
     if (rate_index > rate_count) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     rate_count = 1;
@@ -309,11 +309,11 @@ static int get_rate_from_index(fwk_id_t
     *rate = rate_ul;
 
     FWK_LOG_DEBUG(
-        MOD_NAME "SCMI optee_clock (%u/\"%s\"): rate(index %u) = %lu",
-        fwk_id_get_element_idx(dev_id),
-        clk_get_name(ctx->clk),
-        rate_index,
-        rate_ul);
+	MOD_NAME "SCMI optee_clock (%u/\"%s\"): rate(index %u) = %lu",
+	fwk_id_get_element_idx(dev_id),
+	clk_get_name(ctx->clk),
+	rate_index,
+	rate_ul);
 
     return FWK_SUCCESS;
 }
@@ -325,29 +325,29 @@ static int get_duty_cycle(fwk_id_t dev_i
     TEE_Result res;
 
     if (ctx == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     if (!is_exposed(ctx)) {
-        /* Return a dummy value to prevent an error trace */
+	/* Return a dummy value to prevent an error trace */
 	*num = 1;
 	*den = 2;
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     res = clk_get_duty_cycle(ctx->clk, &duty);
 
     FWK_LOG_DEBUG(
-        MOD_NAME "SCMI optee_clock (%u/\"%s\"): %" PRIx32,
-        fwk_id_get_element_idx(dev_id),
-        clk_get_name(ctx->clk),
-        res);
+	MOD_NAME "SCMI optee_clock (%u/\"%s\"): %" PRIx32,
+	fwk_id_get_element_idx(dev_id),
+	clk_get_name(ctx->clk),
+	res);
 
     if (res == TEE_ERROR_NOT_SUPPORTED) {
-        /* Assume a 50% duty cycle */
-        duty = (struct clk_duty){ .num = 1, .den = 2 };
+	/* Assume a 50% duty cycle */
+	duty = (struct clk_duty){ .num = 1, .den = 2 };
     } else if (res != TEE_SUCCESS) {
-        return FWK_E_DEVICE;
+	return FWK_E_DEVICE;
     }
 
     *num = duty.num;
@@ -362,11 +362,11 @@ static int get_rounded_rate(fwk_id_t dev
     struct optee_clock_dev_ctx *ctx = elt_id_to_ctx(dev_id);
 
     if (ctx == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     if (!is_exposed(ctx)) {
-        return FWK_E_SUPPORT;
+	return FWK_E_SUPPORT;
     }
 
     *rounded_rate = (uint64_t)clk_round_rate(ctx->clk, (unsigned long)rate);
@@ -380,8 +380,8 @@ static int stub_process_power_transition
 }
 
 static int stub_pending_power_transition(fwk_id_t dev_id,
-                                         unsigned int current_state,
-                                         unsigned int next_state)
+					 unsigned int current_state,
+					 unsigned int next_state)
 {
     return FWK_E_SUPPORT;
 }
@@ -405,10 +405,10 @@ static const struct mod_clock_drv_api ap
  */
 
 static int optee_clock_init(fwk_id_t module_id, unsigned int count,
-                            const void *data)
+			    const void *data)
 {
     if (count == 0) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     module_ctx.dev_count = count;
@@ -418,7 +418,7 @@ static int optee_clock_init(fwk_id_t mod
 }
 
 static int optee_clock_element_init(fwk_id_t element_id, unsigned int dev_count,
-                                    const void *data)
+				    const void *data)
 {
     struct optee_clock_dev_ctx *ctx = elt_id_to_ctx(element_id);
     const struct mod_optee_clock_config *config = data;
@@ -426,21 +426,21 @@ static int optee_clock_element_init(fwk_
 
     ctx->clk = config->clk;
     if (ctx->clk) {
-        ctx->enabled = config->default_enabled;
+	ctx->enabled = config->default_enabled;
 
-        if (ctx->enabled) {
-            res = clk_enable(ctx->clk);
-            if (res) {
-                return FWK_E_DEVICE;
-            }
-        }
+	if (ctx->enabled) {
+	    res = clk_enable(ctx->clk);
+	    if (res) {
+		return FWK_E_DEVICE;
+	    }
+	}
     }
 
     return FWK_SUCCESS;
 }
 
 static int optee_clock_process_bind_request(fwk_id_t requester_id, fwk_id_t id,
-                                            fwk_id_t api_type, const void **api)
+					    fwk_id_t api_type, const void **api)
 {
     *api = &api_optee_clock;
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/optee/console/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/optee/console/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/optee/console/CMakeLists.txt
@@ -8,16 +8,16 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 # These include directories are relative to SCP-firmware submodule position in
 # optee-os tree
 # Those includes are needed for mutex definitnion that is used in optee_smt
 # notification
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${SCP_OPTEE_DIR}/core/arch/arm/include/"
-                                  "${SCP_OPTEE_DIR}/core/include/"
-                                  "${SCP_OPTEE_DIR}/lib/libutils/ext/include/"
-                                  "${SCP_OPTEE_DIR}/lib/libutee/include/")
+			   PUBLIC "${SCP_OPTEE_DIR}/core/arch/arm/include/"
+				  "${SCP_OPTEE_DIR}/core/include/"
+				  "${SCP_OPTEE_DIR}/lib/libutils/ext/include/"
+				  "${SCP_OPTEE_DIR}/lib/libutee/include/")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_optee_console.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_optee_console.c")
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/optee/console/src/mod_optee_console.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/optee/console/src/mod_optee_console.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/optee/console/src/mod_optee_console.c
@@ -70,20 +70,20 @@ const struct fwk_module module_optee_con
     .element_init = mod_console_element_init,
 
     .adapter =
-        (struct fwk_io_adapter){
-            .open = mod_console_io_open,
-            .getch = mod_console_io_getch,
-            .putch = mod_console_io_putch,
-            .close = mod_console_close,
-        },
+	(struct fwk_io_adapter){
+	    .open = mod_console_io_open,
+	    .getch = mod_console_io_getch,
+	    .putch = mod_console_io_putch,
+	    .close = mod_console_close,
+	},
 };
 
 struct fwk_module_config config_optee_console = {
     .elements = FWK_MODULE_STATIC_ELEMENTS({
-        [0] = {
-            .name = "printf",
-            .data = (void *)1
-        },
-        [1] = { 0 },
+	[0] = {
+	    .name = "printf",
+	    .data = (void *)1
+	},
+	[1] = { 0 },
     }),
 };
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/optee/mbx/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/optee/mbx/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/optee/mbx/CMakeLists.txt
@@ -8,21 +8,21 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 # These include directories are relative to SCP-firmware submodule position in
 # optee-os tree
 # Those includes are needed for mutex defifitnion that is used in optee_smt
 # notification
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${SCP_OPTEE_DIR}/core/include/"
-                                  "${SCP_OPTEE_DIR}/lib/libutils/ext/include/"
-                                  "${SCP_OPTEE_DIR}/lib/libutee/include/")
+			   PUBLIC "${SCP_OPTEE_DIR}/core/include/"
+				  "${SCP_OPTEE_DIR}/lib/libutils/ext/include/"
+				  "${SCP_OPTEE_DIR}/lib/libutee/include/")
 
 target_link_libraries(${SCP_MODULE_TARGET}
-                      PRIVATE module-optee-smt
-                              module-msg-smt
-                              module-scmi)
+		      PRIVATE module-optee-smt
+			      module-msg-smt
+			      module-scmi)
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_optee_mbx.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_optee_mbx.c")
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/optee/mbx/include/mod_optee_mbx.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/optee/mbx/include/mod_optee_mbx.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/optee/mbx/include/mod_optee_mbx.h
@@ -60,7 +60,7 @@ void optee_mbx_signal_smt_message(fwk_id
  * \param out_size Size of the response buffer.
  */
 void optee_mbx_signal_msg_message(fwk_id_t device_id,
-                                  void *in_buf, size_t in_size,
-                                  void *out_buf, size_t *out_size);
+				  void *in_buf, size_t in_size,
+				  void *out_buf, size_t *out_size);
 
 #endif /* MOD_OPTEE_MBX_H */
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/optee/mbx/src/mod_optee_mbx.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/optee/mbx/src/mod_optee_mbx.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/optee/mbx/src/mod_optee_mbx.c
@@ -32,8 +32,8 @@ struct mbx_device_ctx {
     fwk_id_t shmem_id;
 
     union {
-        struct mod_optee_smt_driver_input_api *smt;
-        struct mod_msg_smt_driver_input_api *msg;
+	struct mod_optee_smt_driver_input_api *smt;
+	struct mod_msg_smt_driver_input_api *msg;
     } shmem_api;
 
     size_t *shm_out_size;
@@ -59,45 +59,45 @@ void optee_mbx_signal_smt_message(fwk_id
     unsigned int device_idx = fwk_id_get_element_idx(device_id);
 
     if (device_idx < mbx_ctx.device_count) {
-        device_ctx = &mbx_ctx.device_ctx_table[device_idx];
+	device_ctx = &mbx_ctx.device_ctx_table[device_idx];
 
-        fwk_assert(fwk_id_get_module_idx(device_ctx->shmem_id) ==
-                   FWK_MODULE_IDX_OPTEE_SMT);
+	fwk_assert(fwk_id_get_module_idx(device_ctx->shmem_id) ==
+		   FWK_MODULE_IDX_OPTEE_SMT);
 
-        /* Lock the channel until the message has been processed */
-        mutex_lock(&device_ctx->lock);
+	/* Lock the channel until the message has been processed */
+	mutex_lock(&device_ctx->lock);
 
-        device_ctx->shmem_api.smt->signal_message(device_ctx->shmem_id);
+	device_ctx->shmem_api.smt->signal_message(device_ctx->shmem_id);
     } else {
-        fwk_unexpected();
+	fwk_unexpected();
     }
 }
 #endif
 
 #ifdef BUILD_HAS_MOD_MSG_SMT
 void optee_mbx_signal_msg_message(fwk_id_t device_id, void *in_buf,
-                                  size_t in_size, void *out_buf,
-                                  size_t *out_size)
+				  size_t in_size, void *out_buf,
+				  size_t *out_size)
 {
     struct mbx_device_ctx *device_ctx;
     unsigned int device_idx = fwk_id_get_element_idx(device_id);
 
     if (device_idx < mbx_ctx.device_count) {
-        device_ctx = &mbx_ctx.device_ctx_table[device_idx];
+	device_ctx = &mbx_ctx.device_ctx_table[device_idx];
 
-        fwk_assert(fwk_id_get_module_idx(device_ctx->shmem_id) ==
-                   FWK_MODULE_IDX_MSG_SMT);
+	fwk_assert(fwk_id_get_module_idx(device_ctx->shmem_id) ==
+		   FWK_MODULE_IDX_MSG_SMT);
 
 
-        /* Lock the channel until the message has been processed */
-        mutex_lock(&device_ctx->lock);
+	/* Lock the channel until the message has been processed */
+	mutex_lock(&device_ctx->lock);
 
-        device_ctx->shm_out_size = out_size;
-        device_ctx->shmem_api.msg->signal_message(device_ctx->shmem_id,
-                                                  in_buf, in_size,
-                                                  out_buf, *out_size);
+	device_ctx->shm_out_size = out_size;
+	device_ctx->shmem_api.msg->signal_message(device_ctx->shmem_id,
+						  in_buf, in_size,
+						  out_buf, *out_size);
     } else {
-        fwk_unexpected();
+	fwk_unexpected();
     }
 }
 #endif
@@ -110,7 +110,7 @@ int optee_mbx_get_devices_count(void)
 fwk_id_t optee_mbx_get_device(unsigned int id)
 {
     if (id >= mbx_ctx.device_count)
-        return (fwk_id_t)FWK_ID_NONE_INIT;
+	return (fwk_id_t)FWK_ID_NONE_INIT;
 
     return (fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_OPTEE_MBX, id);
 }
@@ -161,16 +161,16 @@ const struct mod_msg_smt_driver_ouput_ap
  */
 
 static int mbx_init(fwk_id_t module_id, unsigned int device_count,
-                    const void *data)
+		    const void *data)
 {
 
     if (device_count == 0)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     mbx_ctx.device_ctx_table = fwk_mm_calloc(device_count,
-                                             sizeof(*mbx_ctx.device_ctx_table));
+					     sizeof(*mbx_ctx.device_ctx_table));
     if (mbx_ctx.device_ctx_table == NULL) {
-        return FWK_E_NOMEM;
+	return FWK_E_NOMEM;
     }
 
     mbx_ctx.device_count = device_count;
@@ -179,7 +179,7 @@ static int mbx_init(fwk_id_t module_id,
 }
 
 static int mbx_device_init(fwk_id_t device_id, unsigned int slot_count,
-                           const void *data)
+			   const void *data)
 {
     size_t elt_idx = fwk_id_get_element_idx(device_id);
     struct mbx_device_ctx *device_ctx = &mbx_ctx.device_ctx_table[elt_idx];
@@ -197,58 +197,58 @@ static int mbx_bind(fwk_id_t id, unsigne
     struct mbx_device_ctx *device_ctx;
 
     if (fwk_id_is_type(id, FWK_ID_TYPE_MODULE)) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     if (round == 1) {
-        device_ctx = &mbx_ctx.device_ctx_table[fwk_id_get_element_idx(id)];
+	device_ctx = &mbx_ctx.device_ctx_table[fwk_id_get_element_idx(id)];
 
-        status = fwk_module_bind(device_ctx->config->driver_id,
-                                 device_ctx->config->driver_api_id,
-                                 &device_ctx->shmem_api);
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
+	status = fwk_module_bind(device_ctx->config->driver_id,
+				 device_ctx->config->driver_api_id,
+				 &device_ctx->shmem_api);
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
 
-        device_ctx->shmem_id = device_ctx->config->driver_id;
+	device_ctx->shmem_id = device_ctx->config->driver_id;
     }
 
     return FWK_SUCCESS;
 }
 
 static int mbx_process_bind_request(fwk_id_t source_id,
-                                    fwk_id_t target_id,
-                                    fwk_id_t api_id,
-                                    const void **api)
+				    fwk_id_t target_id,
+				    fwk_id_t api_id,
+				    const void **api)
 {
     size_t elt_idx;
 
     if (!fwk_id_is_type(target_id, FWK_ID_TYPE_ELEMENT)) {
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 
     if (fwk_id_get_api_idx(api_id) != 0) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     elt_idx = fwk_id_get_element_idx(target_id);
     if (elt_idx >= mbx_ctx.device_count) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     switch (fwk_id_get_module_idx(source_id)) {
 #ifdef BUILD_HAS_MOD_OPTEE_SMT
     case FWK_MODULE_IDX_OPTEE_SMT:
-        *api = &mbx_smt_api;
-        break;
+	*api = &mbx_smt_api;
+	break;
 #endif
 #ifdef BUILD_HAS_MOD_MSG_SMT
     case FWK_MODULE_IDX_MSG_SMT:
-        *api = &mbx_shm_api;
-        break;
+	*api = &mbx_shm_api;
+	break;
 #endif
     default:
-        return FWK_E_PANIC;
+	return FWK_E_PANIC;
     }
 
     return FWK_SUCCESS;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/optee/reset/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/optee/reset/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/optee/reset/CMakeLists.txt
@@ -8,10 +8,10 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_optee_reset.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_optee_reset.c")
 
 target_link_libraries(${SCP_MODULE_TARGET}
-                      PRIVATE module-reset-domain)
+		      PRIVATE module-reset-domain)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/optee/reset/src/mod_optee_reset.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/optee/reset/src/mod_optee_reset.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/optee/reset/src/mod_optee_reset.c
@@ -49,63 +49,63 @@ static bool is_exposed(struct optee_rese
  * Driver API functions
  */
 static int reset_set_state(fwk_id_t dev_id, enum mod_reset_domain_mode mode,
-                           uint32_t reset_state, uintptr_t cookie)
+			   uint32_t reset_state, uintptr_t cookie)
 {
     struct optee_reset_dev_ctx *ctx = NULL;
     int status = FWK_SUCCESS;
 
     if (!fwk_module_is_valid_element_id(dev_id)) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     ctx = &module_ctx.dev_ctx_table[fwk_id_get_element_idx(dev_id)];
 
     if (!is_exposed(ctx)) {
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 
     /* Whatever the reset_state set, we consider a unique context loss mode */
     if (reset_state) {
-        FWK_LOG_INFO(MOD_NAME "Override requested SCMI reset state %#"PRIx32,
-            reset_state);
+	FWK_LOG_INFO(MOD_NAME "Override requested SCMI reset state %#"PRIx32,
+	    reset_state);
     }
 
     switch (mode) {
     case MOD_RESET_DOMAIN_MODE_EXPLICIT_ASSERT:
-        FWK_LOG_DEBUG(
-            MOD_NAME "SCMI reset %u: assert(%s)",
-            fwk_id_get_element_idx(dev_id),
-            rstctrl_name(ctx->rstctrl));
-
-        if (rstctrl_assert(ctx->rstctrl)) {
-            status = FWK_E_DEVICE;
-        }
-        break;
+	FWK_LOG_DEBUG(
+	    MOD_NAME "SCMI reset %u: assert(%s)",
+	    fwk_id_get_element_idx(dev_id),
+	    rstctrl_name(ctx->rstctrl));
+
+	if (rstctrl_assert(ctx->rstctrl)) {
+	    status = FWK_E_DEVICE;
+	}
+	break;
     case MOD_RESET_DOMAIN_MODE_EXPLICIT_DEASSERT:
-        FWK_LOG_DEBUG(
-            MOD_NAME "SCMI reset %u: deassert(%s)",
-            fwk_id_get_element_idx(dev_id),
-            rstctrl_name(ctx->rstctrl));
-
-        if (rstctrl_deassert(ctx->rstctrl)) {
-            status = FWK_E_DEVICE;
-        }
-        break;
+	FWK_LOG_DEBUG(
+	    MOD_NAME "SCMI reset %u: deassert(%s)",
+	    fwk_id_get_element_idx(dev_id),
+	    rstctrl_name(ctx->rstctrl));
+
+	if (rstctrl_deassert(ctx->rstctrl)) {
+	    status = FWK_E_DEVICE;
+	}
+	break;
     case MOD_RESET_DOMAIN_AUTO_RESET:
-        FWK_LOG_DEBUG(
-            MOD_NAME "SCMI reset %u: cycle(%s)",
-            fwk_id_get_element_idx(dev_id),
-            rstctrl_name(ctx->rstctrl));
-
-        if (rstctrl_assert_to(ctx->rstctrl, TIMEOUT_US_1MS)) {
-            status = FWK_E_TIMEOUT;
-        }
-        if (rstctrl_deassert_to(ctx->rstctrl, TIMEOUT_US_1MS)) {
-            status = FWK_E_TIMEOUT;
-        }
-        break;
+	FWK_LOG_DEBUG(
+	    MOD_NAME "SCMI reset %u: cycle(%s)",
+	    fwk_id_get_element_idx(dev_id),
+	    rstctrl_name(ctx->rstctrl));
+
+	if (rstctrl_assert_to(ctx->rstctrl, TIMEOUT_US_1MS)) {
+	    status = FWK_E_TIMEOUT;
+	}
+	if (rstctrl_deassert_to(ctx->rstctrl, TIMEOUT_US_1MS)) {
+	    status = FWK_E_TIMEOUT;
+	}
+	break;
     default:
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     return status;
@@ -120,29 +120,29 @@ static const struct mod_reset_domain_drv
  */
 
 static int optee_reset_init(fwk_id_t module_id, unsigned int element_count,
-                            const void *data)
+			    const void *data)
 {
     module_ctx.dev_count = element_count;
 
     if (element_count == 0) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     module_ctx.dev_ctx_table = fwk_mm_calloc(element_count,
-                                             sizeof(struct optee_reset_dev_ctx));
+					     sizeof(struct optee_reset_dev_ctx));
 
     return FWK_SUCCESS;
 }
 
 static int optee_reset_element_init(fwk_id_t element_id,
-                                    unsigned int unused __unused,
-                                    const void *data)
+				    unsigned int unused __unused,
+				    const void *data)
 {
     struct optee_reset_dev_ctx *ctx = NULL;
     const struct mod_optee_reset_dev_config *config = NULL;
 
     if (!fwk_module_is_valid_element_id(element_id)) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     ctx = &module_ctx.dev_ctx_table[fwk_id_get_element_idx(element_id)];
@@ -154,7 +154,7 @@ static int optee_reset_element_init(fwk_
 }
 
 static int optee_reset_process_bind_request(fwk_id_t requester_id, fwk_id_t id,
-                                            fwk_id_t api_type, const void **api)
+					    fwk_id_t api_type, const void **api)
 {
     *api = &api_optee_reset;
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/optee/smt/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/optee/smt/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/optee/smt/CMakeLists.txt
@@ -8,10 +8,10 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include"
-                           PRIVATE "${SCP_OPTEE_DIR}/core/lib/scmi-server/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include"
+			   PRIVATE "${SCP_OPTEE_DIR}/core/lib/scmi-server/include")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_optee_smt.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_optee_smt.c")
 
 target_link_libraries(${SCP_MODULE_TARGET} PRIVATE module-scmi)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/optee/smt/src/mod_optee_smt.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/optee/smt/src/mod_optee_smt.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/optee/smt/src/mod_optee_smt.c
@@ -79,16 +79,16 @@ static int smt_get_secure(fwk_id_t chann
     struct smt_channel_ctx *channel_ctx;
 
     if (secure == NULL) {
-        fwk_unexpected();
-        return FWK_E_PARAM;
+	fwk_unexpected();
+	return FWK_E_PARAM;
     }
 
     channel_ctx =
-        &smt_ctx.channel_ctx_table[fwk_id_get_element_idx(channel_id)];
+	&smt_ctx.channel_ctx_table[fwk_id_get_element_idx(channel_id)];
 
     *secure =
-        ((channel_ctx->config->policies & MOD_SMT_POLICY_SECURE) !=
-         (uint32_t)0);
+	((channel_ctx->config->policies & MOD_SMT_POLICY_SECURE) !=
+	 (uint32_t)0);
 
     return FWK_SUCCESS;
 }
@@ -98,12 +98,12 @@ static int smt_get_max_payload_size(fwk_
     struct smt_channel_ctx *channel_ctx;
 
     if (size == NULL) {
-        fwk_unexpected();
-        return FWK_E_PARAM;
+	fwk_unexpected();
+	return FWK_E_PARAM;
     }
 
     channel_ctx =
-        &smt_ctx.channel_ctx_table[fwk_id_get_element_idx(channel_id)];
+	&smt_ctx.channel_ctx_table[fwk_id_get_element_idx(channel_id)];
 
     *size = channel_ctx->max_payload_size;
 
@@ -115,15 +115,15 @@ static int smt_get_message_header(fwk_id
     struct smt_channel_ctx *channel_ctx;
 
     if (header == NULL) {
-        fwk_unexpected();
-        return FWK_E_PARAM;
+	fwk_unexpected();
+	return FWK_E_PARAM;
     }
 
     channel_ctx =
-        &smt_ctx.channel_ctx_table[fwk_id_get_element_idx(channel_id)];
+	&smt_ctx.channel_ctx_table[fwk_id_get_element_idx(channel_id)];
 
     if (!channel_ctx->locked) {
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 
     *header = channel_ctx->in->message_header;
@@ -132,57 +132,57 @@ static int smt_get_message_header(fwk_id
 }
 
 static int smt_get_payload(fwk_id_t channel_id,
-                           const void **payload,
-                           size_t *size)
+			   const void **payload,
+			   size_t *size)
 {
     struct smt_channel_ctx *channel_ctx;
 
     if (payload == NULL) {
-        fwk_unexpected();
-        return FWK_E_PARAM;
+	fwk_unexpected();
+	return FWK_E_PARAM;
     }
 
     channel_ctx =
-        &smt_ctx.channel_ctx_table[fwk_id_get_element_idx(channel_id)];
+	&smt_ctx.channel_ctx_table[fwk_id_get_element_idx(channel_id)];
 
     if (!channel_ctx->locked) {
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 
     *payload = channel_ctx->in->payload;
 
     if (size != NULL) {
-        *size = channel_ctx->in->length -
-            sizeof(channel_ctx->in->message_header);
+	*size = channel_ctx->in->length -
+	    sizeof(channel_ctx->in->message_header);
     }
 
     return FWK_SUCCESS;
 }
 
 static int smt_write_payload(fwk_id_t channel_id,
-                             size_t offset,
-                             const void *payload,
-                             size_t size)
+			     size_t offset,
+			     const void *payload,
+			     size_t size)
 {
     struct smt_channel_ctx *channel_ctx;
 
     channel_ctx =
-        &smt_ctx.channel_ctx_table[fwk_id_get_element_idx(channel_id)];
+	&smt_ctx.channel_ctx_table[fwk_id_get_element_idx(channel_id)];
 
     if ((payload == NULL)                         ||
-        (offset  > channel_ctx->max_payload_size) ||
-        (size > channel_ctx->max_payload_size)    ||
-        ((offset + size) > channel_ctx->max_payload_size)) {
-        fwk_unexpected();
-        return FWK_E_PARAM;
+	(offset  > channel_ctx->max_payload_size) ||
+	(size > channel_ctx->max_payload_size)    ||
+	((offset + size) > channel_ctx->max_payload_size)) {
+	fwk_unexpected();
+	return FWK_E_PARAM;
     }
 
     if (!channel_ctx->locked) {
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 
     fwk_str_memcpy(
-        ((uint8_t *)channel_ctx->out->payload) + offset, payload, size);
+	((uint8_t *)channel_ctx->out->payload) + offset, payload, size);
 
     return FWK_SUCCESS;
 }
@@ -200,9 +200,9 @@ static int smt_respond(fwk_id_t channel_
 
     /* Copy the payload from either the write buffer or the payload parameter */
     fwk_str_memcpy(
-        memory->payload,
-        (payload == NULL ? channel_ctx->out->payload : payload),
-        size);
+	memory->payload,
+	(payload == NULL ? channel_ctx->out->payload : payload),
+	size);
 
     channel_ctx->locked = false;
 
@@ -221,14 +221,14 @@ static int smt_transmit(fwk_id_t channel
     struct mod_optee_smt_memory *memory;
 
     if (payload == NULL) {
-        return FWK_E_DATA;
+	return FWK_E_DATA;
     }
 
     channel_ctx = &smt_ctx.channel_ctx_table[fwk_id_get_element_idx(channel_id)];
     memory = (struct mod_optee_smt_memory *)channel_ctx->mailbox_va;
 
     if (!channel_ctx->locked) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     memory->message_header = message_header;
@@ -299,13 +299,13 @@ static int smt_requester_handler(struct
      * Note: the payload size is permitted to be of size zero.
      */
     if ((in->length < sizeof(in->message_header)) ||
-        ((in->length - sizeof(in->message_header))
-         > channel_ctx->max_payload_size)) {
+	((in->length - sizeof(in->message_header))
+	 > channel_ctx->max_payload_size)) {
 
-        out->status |= MOD_OPTEE_SMT_MAILBOX_STATUS_ERROR_MASK;
-        return smt_respond(channel_ctx->id,
-                           &(int32_t){ SCMI_PROTOCOL_ERROR },
-                           sizeof(int32_t));
+	out->status |= MOD_OPTEE_SMT_MAILBOX_STATUS_ERROR_MASK;
+	return smt_respond(channel_ctx->id,
+			   &(int32_t){ SCMI_PROTOCOL_ERROR },
+			   sizeof(int32_t));
     }
 
     /* Copy payload from shared memory to read buffer */
@@ -314,9 +314,9 @@ static int smt_requester_handler(struct
 
     /* Let SCMI handle the message */
     status =
-        channel_ctx->scmi_api->signal_message(channel_ctx->scmi_service_id);
+	channel_ctx->scmi_api->signal_message(channel_ctx->scmi_service_id);
     if (status != FWK_SUCCESS) {
-        return FWK_E_HANDLER;
+	return FWK_E_HANDLER;
     }
 
     return FWK_SUCCESS;
@@ -335,29 +335,29 @@ static int smt_signal_message(fwk_id_t c
     struct smt_channel_ctx *channel_ctx;
 
     channel_ctx =
-        &smt_ctx.channel_ctx_table[fwk_id_get_element_idx(channel_id)];
+	&smt_ctx.channel_ctx_table[fwk_id_get_element_idx(channel_id)];
 
     if (!channel_ctx->optee_smt_mailbox_ready) {
-        /* Discard any message in the mailbox when not ready */
-        FWK_LOG_ERR("[OPTEE_SMT] Message not valid\n");
+	/* Discard any message in the mailbox when not ready */
+	FWK_LOG_ERR("[OPTEE_SMT] Message not valid\n");
 
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     /* Check if we are already processing */
     if (channel_ctx->locked) {
-        return FWK_E_STATE;
+	return FWK_E_STATE;
     }
 
     switch (channel_ctx->config->type) {
     case MOD_OPTEE_SMT_CHANNEL_TYPE_COMPLETER:
-        return smt_completer_handler(channel_ctx);
+	return smt_completer_handler(channel_ctx);
     case MOD_OPTEE_SMT_CHANNEL_TYPE_REQUESTER:
-        return smt_requester_handler(channel_ctx);
+	return smt_requester_handler(channel_ctx);
     default:
-        /* Invalid config */
-        fwk_unexpected();
-        break;
+	/* Invalid config */
+	fwk_unexpected();
+	break;
     }
 
     return FWK_SUCCESS;
@@ -371,18 +371,18 @@ static const struct mod_optee_smt_driver
  * Framework API
  */
 static int mailbox_init(fwk_id_t module_id, unsigned int element_count,
-                        const void *data)
+			const void *data)
 {
 
     if (element_count == 0) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     smt_ctx.channel_ctx_table =
-        fwk_mm_calloc(element_count, sizeof(*smt_ctx.channel_ctx_table));
+	fwk_mm_calloc(element_count, sizeof(*smt_ctx.channel_ctx_table));
 
     if (smt_ctx.channel_ctx_table == NULL) {
-        return FWK_E_NOMEM;
+	return FWK_E_NOMEM;
     }
 
     smt_ctx.channel_count = element_count;
@@ -391,7 +391,7 @@ static int mailbox_init(fwk_id_t module_
 }
 
 static int mailbox_channel_init(fwk_id_t channel_id, unsigned int slot_count,
-                            const void *data)
+			    const void *data)
 {
     size_t elt_idx = fwk_id_get_element_idx(channel_id);
     struct smt_channel_ctx *channel_ctx = &smt_ctx.channel_ctx_table[elt_idx];
@@ -402,18 +402,18 @@ static int mailbox_channel_init(fwk_id_t
 
     /* Validate channel config */
     if ((config->type >= MOD_OPTEE_SMT_CHANNEL_TYPE_COUNT) ||
-        (config->mailbox_address == 0) || (config->mailbox_size == 0)) {
-        fwk_unexpected();
-        return FWK_E_DATA;
+	(config->mailbox_address == 0) || (config->mailbox_size == 0)) {
+	fwk_unexpected();
+	return FWK_E_DATA;
     }
 
     channel_ctx->mailbox_va = smt_phys_to_virt(config->mailbox_address,
-                                               config->mailbox_size,
-                                               config->policies &
-                                               MOD_SMT_POLICY_SECURE);
+					       config->mailbox_size,
+					       config->policies &
+					       MOD_SMT_POLICY_SECURE);
     if (channel_ctx->mailbox_va == 0) {
-        fwk_unexpected();
-        return FWK_E_DATA;
+	fwk_unexpected();
+	return FWK_E_DATA;
     }
 
     channel_ctx->id = channel_id;
@@ -421,7 +421,7 @@ static int mailbox_channel_init(fwk_id_t
     channel_ctx->out = fwk_mm_alloc(1, config->mailbox_size);
 
     channel_ctx->max_payload_size = config->mailbox_size -
-                                    sizeof(struct mod_optee_smt_memory);
+				    sizeof(struct mod_optee_smt_memory);
 
     memory = (struct mod_optee_smt_memory *)channel_ctx->mailbox_va;
     memory->status = MOD_OPTEE_SMT_MAILBOX_STATUS_FREE_MASK;
@@ -438,95 +438,95 @@ static int mailbox_smt_bind(fwk_id_t id,
     struct smt_channel_ctx *channel_ctx;
 
     if (fwk_id_is_type(id, FWK_ID_TYPE_MODULE)) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     channel_ctx = &smt_ctx.channel_ctx_table[fwk_id_get_element_idx(id)];
 
     if (round == 0) {
 
-        status = fwk_module_bind(channel_ctx->config->driver_id,
-                                 channel_ctx->config->driver_api_id,
-                                 &channel_ctx->driver_api);
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
+	status = fwk_module_bind(channel_ctx->config->driver_id,
+				 channel_ctx->config->driver_api_id,
+				 &channel_ctx->driver_api);
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
 
-        channel_ctx->driver_id = channel_ctx->config->driver_id;
+	channel_ctx->driver_id = channel_ctx->config->driver_id;
 
     } else if (round == 1) {
 
-        status = fwk_module_bind(channel_ctx->scmi_service_id,
-                                 FWK_ID_API(FWK_MODULE_IDX_SCMI,
-                                            MOD_SCMI_API_IDX_TRANSPORT),
-                                 &channel_ctx->scmi_api);
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
+	status = fwk_module_bind(channel_ctx->scmi_service_id,
+				 FWK_ID_API(FWK_MODULE_IDX_SCMI,
+					    MOD_SCMI_API_IDX_TRANSPORT),
+				 &channel_ctx->scmi_api);
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
     }
 
     return FWK_SUCCESS;
 }
 
 static int mailbox_smt_process_bind_request(fwk_id_t source_id,
-                                          fwk_id_t target_id,
-                                          fwk_id_t api_id,
-                                          const void **api)
+					  fwk_id_t target_id,
+					  fwk_id_t api_id,
+					  const void **api)
 {
     struct smt_channel_ctx *channel_ctx = NULL;
     size_t elt_idx;
 
     /* Only bind to a channel (not the whole module) */
     if (!fwk_id_is_type(target_id, FWK_ID_TYPE_ELEMENT)) {
-        /* Tried to bind to something other than a specific channel */
-        fwk_unexpected();
-        return FWK_E_PARAM;
+	/* Tried to bind to something other than a specific channel */
+	fwk_unexpected();
+	return FWK_E_PARAM;
     }
 
     elt_idx = fwk_id_get_element_idx(target_id);
     if (elt_idx >= smt_ctx.channel_count) {
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 
     channel_ctx = &smt_ctx.channel_ctx_table[elt_idx];
 
     switch (fwk_id_get_api_idx(api_id)) {
     case MOD_OPTEE_SMT_API_IDX_DRIVER_INPUT:
-        /* Driver input API */
+	/* Driver input API */
 
-        /*
-         * Make sure that the element that is trying to bind to us is the
-         * same element that we previously bound to.
-         *
-         * NOTE: We bound to an element but a sub-element should be binding
-         * back to us. This means we cannot use fwk_id_is_equal() because
-         * the ids have different types. For now we compare the indicies
-         * manually.
-         */
-        if (fwk_id_get_module_idx(channel_ctx->driver_id) ==
-            fwk_id_get_module_idx(source_id) &&
-            fwk_id_get_element_idx(channel_ctx->driver_id) ==
-            fwk_id_get_element_idx(source_id)) {
-
-            /* Ids are equal */
-            *api = &driver_input_api;
-        } else {
-            /* A module that we did not bind to is trying to bind to us */
-            fwk_unexpected();
-            return FWK_E_ACCESS;
-        }
-        break;
+	/*
+	 * Make sure that the element that is trying to bind to us is the
+	 * same element that we previously bound to.
+	 *
+	 * NOTE: We bound to an element but a sub-element should be binding
+	 * back to us. This means we cannot use fwk_id_is_equal() because
+	 * the ids have different types. For now we compare the indicies
+	 * manually.
+	 */
+	if (fwk_id_get_module_idx(channel_ctx->driver_id) ==
+	    fwk_id_get_module_idx(source_id) &&
+	    fwk_id_get_element_idx(channel_ctx->driver_id) ==
+	    fwk_id_get_element_idx(source_id)) {
+
+	    /* Ids are equal */
+	    *api = &driver_input_api;
+	} else {
+	    /* A module that we did not bind to is trying to bind to us */
+	    fwk_unexpected();
+	    return FWK_E_ACCESS;
+	}
+	break;
 
     case MOD_OPTEE_SMT_API_IDX_SCMI_TRANSPORT:
-        /* SCMI transport API */
-        *api = &smt_mod_scmi_to_transport_api;
-        channel_ctx->scmi_service_id = source_id;
-        break;
+	/* SCMI transport API */
+	*api = &smt_mod_scmi_to_transport_api;
+	channel_ctx->scmi_service_id = source_id;
+	break;
 
     default:
-        /* Invalid API */
-        fwk_unexpected();
-        return FWK_E_PARAM;
+	/* Invalid API */
+	fwk_unexpected();
+	return FWK_E_PARAM;
     }
 
     return FWK_SUCCESS;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/pcid/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/pcid/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/pcid/CMakeLists.txt
@@ -8,7 +8,7 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_pcid.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_pcid.c")
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/pcid/include/mod_pcid.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/pcid/include/mod_pcid.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/pcid/include/mod_pcid.h
@@ -58,7 +58,7 @@ struct mod_pcid_registers {
  * \retval false One or more registers do not have the expected values.
  */
 bool mod_pcid_check_registers(const struct mod_pcid_registers *registers,
-                              const struct mod_pcid_registers *expected);
+			      const struct mod_pcid_registers *expected);
 
 /*!
  * \}
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/pcid/src/mod_pcid.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/pcid/src/mod_pcid.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/pcid/src/mod_pcid.c
@@ -16,7 +16,7 @@
 #include <string.h>
 
 bool mod_pcid_check_registers(const struct mod_pcid_registers *registers,
-                              const struct mod_pcid_registers *expected)
+			      const struct mod_pcid_registers *expected)
 {
     fwk_assert(registers != NULL);
     fwk_assert(expected != NULL);
@@ -25,8 +25,8 @@ bool mod_pcid_check_registers(const stru
 }
 
 static int pcid_init(fwk_id_t module_id,
-                     unsigned int element_count,
-                     const void *data)
+		     unsigned int element_count,
+		     const void *data)
 {
     return FWK_SUCCESS;
 }
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/pcie_integ_ctrl/src/mod_pcie_integ_ctrl.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/pcie_integ_ctrl/src/mod_pcie_integ_ctrl.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/pcie_integ_ctrl/src/mod_pcie_integ_ctrl.c
@@ -51,15 +51,15 @@ static inline void pcie_integ_ctrl_confi
     const struct pcie_ecam_mmio_mmap *cfg)
 {
     reg->ECAM1_START_ADDR = PCIE_INTEG_CTRL_REG_START_ADDR_EN(
-        cfg->ecam1_start_addr, cfg->allow_ns_access);
+	cfg->ecam1_start_addr, cfg->allow_ns_access);
     reg->ECAM1_END_ADDR = PCIE_INTEG_CTRL_REG_END_ADDR(cfg->ecam1_end_addr);
 
     reg->MMIOL_START_ADDR = PCIE_INTEG_CTRL_REG_START_ADDR_EN(
-        cfg->mmiol_start_addr, cfg->allow_ns_access);
+	cfg->mmiol_start_addr, cfg->allow_ns_access);
     reg->MMIOL_END_ADDR = PCIE_INTEG_CTRL_REG_END_ADDR(cfg->mmiol_end_addr);
 
     reg->MMIOH_START_ADDR = PCIE_INTEG_CTRL_REG_START_ADDR_EN(
-        cfg->mmioh_start_addr, cfg->allow_ns_access);
+	cfg->mmioh_start_addr, cfg->allow_ns_access);
     reg->MMIOH_END_ADDR = PCIE_INTEG_CTRL_REG_END_ADDR(cfg->mmioh_end_addr);
 }
 
@@ -82,32 +82,32 @@ static void configure_pcie_ecam_mmio_spa
     cfg = &config->x4_0_ecam_mmio_mmap;
 
     if (cfg->valid) {
-        /* Configure address routing on x4_0 */
-        pcie_integ_ctrl_configure_registers(reg, cfg);
+	/* Configure address routing on x4_0 */
+	pcie_integ_ctrl_configure_registers(reg, cfg);
     }
 
     reg = &pcie_integ_ctrl_reg->pcie_ctrl_x4_1;
     cfg = &config->x4_1_ecam_mmio_mmap;
 
     if (cfg->valid) {
-        /* Configure address routing on x4_1 */
-        pcie_integ_ctrl_configure_registers(reg, cfg);
+	/* Configure address routing on x4_1 */
+	pcie_integ_ctrl_configure_registers(reg, cfg);
     }
 
     reg = &pcie_integ_ctrl_reg->pcie_ctrl_x8;
     cfg = &config->x8_ecam_mmio_mmap;
 
     if (cfg->valid) {
-        /* Configure address routing on x8 */
-        pcie_integ_ctrl_configure_registers(reg, cfg);
+	/* Configure address routing on x8 */
+	pcie_integ_ctrl_configure_registers(reg, cfg);
     }
 
     reg = &pcie_integ_ctrl_reg->pcie_ctrl_x16;
     cfg = &config->x16_ecam_mmio_mmap;
 
     if (cfg->valid) {
-        /* Configure address routing on x16 */
-        pcie_integ_ctrl_configure_registers(reg, cfg);
+	/* Configure address routing on x16 */
+	pcie_integ_ctrl_configure_registers(reg, cfg);
     }
 }
 
@@ -121,9 +121,9 @@ static int pcie_integ_ctrl_init(
     const void *unused)
 {
     if (element_count == 0) {
-        /* There must be at least one pcie integ config data */
-        fwk_unexpected();
-        return FWK_E_PARAM;
+	/* There must be at least one pcie integ config data */
+	fwk_unexpected();
+	return FWK_E_PARAM;
     }
 
     ctx_table = fwk_mm_calloc(element_count, sizeof(ctx_table[0]));
@@ -141,8 +141,8 @@ static int pcie_integ_ctrl_element_init(
 
     config = (struct mod_pcie_integ_ctrl_config *)data;
     if ((config == NULL) || (config->reg_base == 0)) {
-        fwk_unexpected();
-        return FWK_E_DATA;
+	fwk_unexpected();
+	return FWK_E_DATA;
     }
 
     ctx = &ctx_table[fwk_id_get_element_idx(element_id)];
@@ -158,7 +158,7 @@ static int pcie_integ_ctrl_start(fwk_id_
 
     /* Nothing to be done for module start call */
     if (fwk_id_get_type(id) == FWK_ID_TYPE_MODULE) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     ctx = get_ctx(id);
@@ -167,15 +167,15 @@ static int pcie_integ_ctrl_start(fwk_id_
 
     /* Register the element for clock state notifications */
     return fwk_notification_subscribe(
-        mod_clock_notification_id_state_changed, config->clock_id, id);
+	mod_clock_notification_id_state_changed, config->clock_id, id);
 }
 
 static int pcie_integ_ctrl_bind(fwk_id_t id, unsigned int round)
 {
     return fwk_module_bind(
-        FWK_ID_MODULE(FWK_MODULE_IDX_APREMAP),
-        FWK_ID_API(FWK_MODULE_IDX_APREMAP, MOD_APREMAP_API_IDX_CMN_ATRANS),
-        &apremap_cmn_atrans_api);
+	FWK_ID_MODULE(FWK_MODULE_IDX_APREMAP),
+	FWK_ID_API(FWK_MODULE_IDX_APREMAP, MOD_APREMAP_API_IDX_CMN_ATRANS),
+	&apremap_cmn_atrans_api);
 }
 
 static int pcie_integ_ctrl_process_notification(
@@ -186,21 +186,21 @@ static int pcie_integ_ctrl_process_notif
     struct clock_notification_params *params;
 
     fwk_assert(
-        fwk_id_is_equal(event->id, mod_clock_notification_id_state_changed));
+	fwk_id_is_equal(event->id, mod_clock_notification_id_state_changed));
     fwk_assert(fwk_id_is_type(event->target_id, FWK_ID_TYPE_ELEMENT));
 
     params = (struct clock_notification_params *)event->params;
 
     if (params->new_state == MOD_CLOCK_STATE_RUNNING) {
-        ctx = get_ctx(event->target_id);
+	ctx = get_ctx(event->target_id);
 
-        /* Configure the PCIe integration config registers */
-        configure_pcie_ecam_mmio_space(
-            fwk_id_get_element_idx(event->target_id), ctx->config);
-
-        /* Unsubscribe to the notification */
-        return fwk_notification_unsubscribe(
-            event->id, event->source_id, event->target_id);
+	/* Configure the PCIe integration config registers */
+	configure_pcie_ecam_mmio_space(
+	    fwk_id_get_element_idx(event->target_id), ctx->config);
+
+	/* Unsubscribe to the notification */
+	return fwk_notification_unsubscribe(
+	    event->id, event->source_id, event->target_id);
     }
 
     return FWK_SUCCESS;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/pik_clock/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/pik_clock/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/pik_clock/CMakeLists.txt
@@ -8,10 +8,10 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_pik_clock.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_pik_clock.c")
 
 target_link_libraries(${SCP_MODULE_TARGET}
-                      PRIVATE module-clock module-css-clock module-power-domain)
+		      PRIVATE module-clock module-css-clock module-power-domain)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/pik_clock/src/mod_pik_clock.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/pik_clock/src/mod_pik_clock.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/pik_clock/src/mod_pik_clock.c
@@ -127,163 +127,163 @@ static int compare_rate_entry(const void
 }
 
 static int get_rate_entry(struct pik_clock_dev_ctx *ctx, uint64_t target_rate,
-                          struct mod_pik_clock_rate **entry)
+			  struct mod_pik_clock_rate **entry)
 {
     struct mod_pik_clock_rate *current_rate_entry;
 
     if (ctx == NULL)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     if (entry == NULL)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     /* Perform a binary search to find the entry matching the requested rate */
     current_rate_entry = (struct mod_pik_clock_rate *) bsearch(&target_rate,
-        ctx->config->rate_table, ctx->config->rate_count,
-        sizeof(struct mod_pik_clock_rate), compare_rate_entry);
+	ctx->config->rate_table, ctx->config->rate_count,
+	sizeof(struct mod_pik_clock_rate), compare_rate_entry);
 
     if (current_rate_entry == NULL)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     *entry = current_rate_entry;
     return FWK_SUCCESS;
 }
 
 static int ssclock_set_div(struct pik_clock_dev_ctx *ctx, uint32_t divider,
-                           bool wait_after_set)
+			   bool wait_after_set)
 {
     uint32_t clkdiv;
 
     if (divider == 0)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     if (divider > module_ctx.divider_max)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     if (ctx == NULL)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     if (ctx->config->type != MOD_PIK_CLOCK_TYPE_SINGLE_SOURCE)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     /* The resulting divider is the programmed value plus one */
     clkdiv = divider - 1;
 
     /* Set */
     *ctx->config->control_reg =
-                   (*ctx->config->control_reg & ~SSCLK_CONTROL_CLKDIV) |
-                   (clkdiv << SSCLK_CONTROL_CLKDIV_POS);
+		   (*ctx->config->control_reg & ~SSCLK_CONTROL_CLKDIV) |
+		   (clkdiv << SSCLK_CONTROL_CLKDIV_POS);
 
     if (wait_after_set) {
-        while ((*ctx->config->control_reg & SSCLK_CONTROL_CRNTCLKDIV) !=
-               (clkdiv << SSCLK_CONTROL_CRNTCLKDIV_POS))
-            continue;
+	while ((*ctx->config->control_reg & SSCLK_CONTROL_CRNTCLKDIV) !=
+	       (clkdiv << SSCLK_CONTROL_CRNTCLKDIV_POS))
+	    continue;
     }
 
     return FWK_SUCCESS;
 }
 
 static int msclock_set_div(struct pik_clock_dev_ctx *ctx,
-                           enum mod_pik_clock_msclock_divider divider_type,
-                           uint32_t divider,
-                           bool wait_after_set)
+			   enum mod_pik_clock_msclock_divider divider_type,
+			   uint32_t divider,
+			   bool wait_after_set)
 {
     volatile uint32_t * divider_reg;
     uint32_t clkdiv;
 
     if (ctx == NULL)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     if (divider == 0)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     if (divider > module_ctx.divider_max)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     if (ctx->config->type == MOD_PIK_CLOCK_TYPE_SINGLE_SOURCE)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     /* The resulting divider is the programmed value plus one */
     clkdiv = divider - 1;
 
     if (divider_type == MOD_PIK_CLOCK_MSCLOCK_DIVIDER_DIV_SYS)
-        divider_reg = ctx->config->divsys_reg;
+	divider_reg = ctx->config->divsys_reg;
     else if (divider_type == MOD_PIK_CLOCK_MSCLOCK_DIVIDER_DIV_EXT)
-        divider_reg = ctx->config->divext_reg;
+	divider_reg = ctx->config->divext_reg;
     else
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     /* Set */
     *divider_reg = (*divider_reg & ~MSCLK_DIV_CLKDIV) |
-                   (clkdiv << MSCLK_DIV_CLKDIV_POS);
+		   (clkdiv << MSCLK_DIV_CLKDIV_POS);
 
     if (wait_after_set) {
-        while ((*divider_reg & MSCLK_DIV_CRNTCLKDIV) !=
-               (clkdiv << MSCLK_DIV_CRNTCLKDIV_POS))
-            continue;
+	while ((*divider_reg & MSCLK_DIV_CRNTCLKDIV) !=
+	       (clkdiv << MSCLK_DIV_CRNTCLKDIV_POS))
+	    continue;
     }
 
     return FWK_SUCCESS;
 }
 
 static int msclock_set_source(struct pik_clock_dev_ctx *ctx,
-                              enum mod_pik_clock_msclock_source source,
-                              bool wait_after_set)
+			      enum mod_pik_clock_msclock_source source,
+			      bool wait_after_set)
 {
     if (ctx == NULL)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     if (ctx->config->type == MOD_PIK_CLOCK_TYPE_SINGLE_SOURCE)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     /* Set */
     *ctx->config->control_reg =
-                   (*ctx->config->control_reg & ~MSCLK_CONTROL_CLKSEL) |
-                   (source << MSCLK_CONTROL_CLKSEL_POS);
+		   (*ctx->config->control_reg & ~MSCLK_CONTROL_CLKSEL) |
+		   (source << MSCLK_CONTROL_CLKSEL_POS);
 
     if (wait_after_set) {
-        while ((*ctx->config->control_reg & MSCLK_CONTROL_CRNTCLK) !=
-               ((uint32_t)(source << MSCLK_CONTROL_CRNTCLK_POS)))
-            continue;
+	while ((*ctx->config->control_reg & MSCLK_CONTROL_CRNTCLK) !=
+	       ((uint32_t)(source << MSCLK_CONTROL_CRNTCLK_POS)))
+	    continue;
     }
 
     return FWK_SUCCESS;
 }
 
 static int cluster_set_modulator(struct pik_clock_dev_ctx *ctx,
-                                 uint32_t numerator, uint32_t denominator,
-                                 bool wait_after_set)
+				 uint32_t numerator, uint32_t denominator,
+				 bool wait_after_set)
 {
     uint32_t modulator_setting;
 
     if (ctx == NULL)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     if (ctx->config->type != MOD_PIK_CLOCK_TYPE_CLUSTER)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     if (ctx->config->modulator_reg == NULL)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     if (denominator > 255)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     if (denominator == 0)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     if (numerator > 255)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     modulator_setting = (denominator << CLUSCLK_MOD_DENOMINATOR_POS) |
-                        (numerator << CLUSCLK_MOD_NUMERATOR_POS);
+			(numerator << CLUSCLK_MOD_NUMERATOR_POS);
 
 
     *ctx->config->modulator_reg = ((*ctx->config->modulator_reg &
-        ~(CLUSCLK_MOD_DENOMINATOR | CLUSCLK_MOD_NUMERATOR)) |
-        modulator_setting);
+	~(CLUSCLK_MOD_DENOMINATOR | CLUSCLK_MOD_NUMERATOR)) |
+	modulator_setting);
 
     if (wait_after_set) {
-        while ((*ctx->config->modulator_reg & CLUSCLK_MOD_CRNTNUMERATOR) !=
-                (numerator << CLUSCLK_MOD_CRNTNUMERATOR_POS))
-            continue;
-
-        while ((*ctx->config->modulator_reg & CLUSCLK_MOD_CRNTDENOMINATOR) !=
-                (denominator << CLUSCLK_MOD_CRNTDENOMINATOR_POS))
-            continue;
+	while ((*ctx->config->modulator_reg & CLUSCLK_MOD_CRNTNUMERATOR) !=
+		(numerator << CLUSCLK_MOD_CRNTNUMERATOR_POS))
+	    continue;
+
+	while ((*ctx->config->modulator_reg & CLUSCLK_MOD_CRNTDENOMINATOR) !=
+		(denominator << CLUSCLK_MOD_CRNTDENOMINATOR_POS))
+	    continue;
     }
 
     return FWK_SUCCESS;
 }
 
 static int do_pik_clock_set_rate(fwk_id_t dev_id, uint64_t rate,
-                                 enum mod_clock_round_mode round_mode)
+				 enum mod_clock_round_mode round_mode)
 {
     int status;
     struct pik_clock_dev_ctx *ctx;
@@ -294,41 +294,41 @@ static int do_pik_clock_set_rate(fwk_id_
     /* Look up the divider and source settings */
     status = get_rate_entry(ctx, rate, &rate_entry);
     if (status != FWK_SUCCESS)
-        return status;
+	return status;
 
     switch (ctx->config->type) {
     case MOD_PIK_CLOCK_TYPE_SINGLE_SOURCE:
-        status = ssclock_set_div(ctx, rate_entry->divider, false);
-        if (status != FWK_SUCCESS)
-            goto exit;
-        break;
+	status = ssclock_set_div(ctx, rate_entry->divider, false);
+	if (status != FWK_SUCCESS)
+	    goto exit;
+	break;
     case MOD_PIK_CLOCK_TYPE_CLUSTER:
-        /* Modulator feature not currently used */
-        cluster_set_modulator(ctx, 1, 1, false);
-        /* Intentional fall-through */
+	/* Modulator feature not currently used */
+	cluster_set_modulator(ctx, 1, 1, false);
+	/* Intentional fall-through */
     case MOD_PIK_CLOCK_TYPE_MULTI_SOURCE:
-        if (ctx->current_source == MOD_PIK_CLOCK_MSCLOCK_SOURCE_GATED)
-            /* Leave the new rate to be applied when the clock is (re)started */
-            goto exit;
-
-        status = msclock_set_div(ctx, rate_entry->divider_reg,
-                                 rate_entry->divider, false);
-        if (status != FWK_SUCCESS)
-            goto exit;
-
-        status = msclock_set_source(ctx, rate_entry->source, false);
-         if (status != FWK_SUCCESS)
-            goto exit;
+	if (ctx->current_source == MOD_PIK_CLOCK_MSCLOCK_SOURCE_GATED)
+	    /* Leave the new rate to be applied when the clock is (re)started */
+	    goto exit;
+
+	status = msclock_set_div(ctx, rate_entry->divider_reg,
+				 rate_entry->divider, false);
+	if (status != FWK_SUCCESS)
+	    goto exit;
+
+	status = msclock_set_source(ctx, rate_entry->source, false);
+	 if (status != FWK_SUCCESS)
+	    goto exit;
 
-        ctx->current_source = rate_entry->source;
-        break;
+	ctx->current_source = rate_entry->source;
+	break;
     default:
-        return FWK_E_SUPPORT;
+	return FWK_E_SUPPORT;
     }
 
 exit:
     if (status == FWK_SUCCESS)
-        ctx->current_rate = rate;
+	ctx->current_rate = rate;
     return status;
 }
 
@@ -337,17 +337,17 @@ exit:
  */
 
 static int pik_clock_set_rate(fwk_id_t dev_id, uint64_t rate,
-                              enum mod_clock_round_mode round_mode)
+			      enum mod_clock_round_mode round_mode)
 {
     struct pik_clock_dev_ctx *ctx;
 
     ctx = module_ctx.dev_ctx_table + fwk_id_get_element_idx(dev_id);
 
     if (!ctx->initialized)
-        return FWK_E_INIT;
+	return FWK_E_INIT;
 
     if (ctx->current_state == MOD_CLOCK_STATE_STOPPED)
-        return FWK_E_PWRSTATE;
+	return FWK_E_PWRSTATE;
 
     return do_pik_clock_set_rate(dev_id, rate, round_mode);
 }
@@ -357,33 +357,33 @@ static int pik_clock_get_rate(fwk_id_t d
     struct pik_clock_dev_ctx *ctx;
 
     if (rate == NULL)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     ctx = module_ctx.dev_ctx_table + fwk_id_get_element_idx(dev_id);
 
     if (ctx->config->type == MOD_PIK_CLOCK_TYPE_MULTI_SOURCE &&
-        ctx->current_source == MOD_PIK_CLOCK_MSCLOCK_SOURCE_GATED)
-        /* Indicate that the clock is not running */
-        *rate = 0;
+	ctx->current_source == MOD_PIK_CLOCK_MSCLOCK_SOURCE_GATED)
+	/* Indicate that the clock is not running */
+	*rate = 0;
     else
-        *rate = ctx->current_rate;
+	*rate = ctx->current_rate;
 
     return FWK_SUCCESS;
 }
 
 static int pik_clock_get_rate_from_index(fwk_id_t dev_id,
-                                         unsigned int rate_index,
-                                         uint64_t *rate)
+					 unsigned int rate_index,
+					 uint64_t *rate)
 {
     struct pik_clock_dev_ctx *ctx;
 
     if (rate == NULL)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     ctx = module_ctx.dev_ctx_table + fwk_id_get_element_idx(dev_id);
 
     if (rate_index >= ctx->config->rate_count)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     *rate = ctx->config->rate_table[rate_index].rate;
     return FWK_SUCCESS;
@@ -401,38 +401,38 @@ static int pik_clock_set_state(
     ctx = module_ctx.dev_ctx_table + fwk_id_get_element_idx(dev_id);
 
     if (ctx->config->type == MOD_PIK_CLOCK_TYPE_SINGLE_SOURCE)
-        /* Cannot gate single-source clocks */
-        return FWK_E_SUPPORT;
+	/* Cannot gate single-source clocks */
+	return FWK_E_SUPPORT;
 
     if (!ctx->initialized)
-        return FWK_E_INIT;
+	return FWK_E_INIT;
 
     if (ctx->current_state == MOD_CLOCK_STATE_STOPPED)
-        /*
-         * This state from the device context relates only to the clock state
-         * that is derived from its parent power domain.
-         */
-        return FWK_E_PWRSTATE;
+	/*
+	 * This state from the device context relates only to the clock state
+	 * that is derived from its parent power domain.
+	 */
+	return FWK_E_PWRSTATE;
 
     if (target_state == MOD_CLOCK_STATE_STOPPED) {
-        /* The clock is powered and will be gated. */
-        status = msclock_set_source(ctx, MOD_PIK_CLOCK_MSCLOCK_SOURCE_GATED,
-            false);
-        if (status == FWK_SUCCESS)
-            ctx->current_source = MOD_PIK_CLOCK_MSCLOCK_SOURCE_GATED;
+	/* The clock is powered and will be gated. */
+	status = msclock_set_source(ctx, MOD_PIK_CLOCK_MSCLOCK_SOURCE_GATED,
+	    false);
+	if (status == FWK_SUCCESS)
+	    ctx->current_source = MOD_PIK_CLOCK_MSCLOCK_SOURCE_GATED;
 
-        return status;
+	return status;
     } else {
-        /* Look up the divider and source settings */
-        status = get_rate_entry(ctx, ctx->current_rate, &rate_entry);
-        if (status != FWK_SUCCESS)
-            return status;
-
-        status = msclock_set_source(ctx, rate_entry->source, false);
-        if (status == FWK_SUCCESS)
-            ctx->current_source = rate_entry->source;
+	/* Look up the divider and source settings */
+	status = get_rate_entry(ctx, ctx->current_rate, &rate_entry);
+	if (status != FWK_SUCCESS)
+	    return status;
+
+	status = msclock_set_source(ctx, rate_entry->source, false);
+	if (status == FWK_SUCCESS)
+	    ctx->current_source = rate_entry->source;
 
-        return status;
+	return status;
     }
 }
 
@@ -443,12 +443,12 @@ static int pik_clock_get_state(fwk_id_t
     ctx = module_ctx.dev_ctx_table + fwk_id_get_element_idx(dev_id);
 
     if (ctx->config->type == MOD_PIK_CLOCK_TYPE_SINGLE_SOURCE) {
-        /*
-         * Single-source clocks cannot be gated so their running state will be
-         * derived purely from the state of their parent power domain, if any.
-         */
-        *state = ctx->current_state;
-        return FWK_SUCCESS;
+	/*
+	 * Single-source clocks cannot be gated so their running state will be
+	 * derived purely from the state of their parent power domain, if any.
+	 */
+	*state = ctx->current_state;
+	return FWK_SUCCESS;
     }
 
     /*
@@ -456,9 +456,9 @@ static int pik_clock_get_state(fwk_id_t
      * their parent power domain.
      */
     if (ctx->current_source == MOD_PIK_CLOCK_MSCLOCK_SOURCE_GATED)
-        *state = MOD_CLOCK_STATE_STOPPED;
+	*state = MOD_CLOCK_STATE_STOPPED;
     else
-        *state = ctx->current_state;
+	*state = ctx->current_state;
 
     return FWK_SUCCESS;
 }
@@ -468,7 +468,7 @@ static int pik_clock_get_range(fwk_id_t
     struct pik_clock_dev_ctx *ctx;
 
     if (range == NULL)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     ctx = module_ctx.dev_ctx_table + fwk_id_get_element_idx(dev_id);
 
@@ -489,22 +489,22 @@ static int pik_clock_power_state_change(
     ctx = module_ctx.dev_ctx_table + fwk_id_get_element_idx(dev_id);
 
     if (ctx->config->is_group_member)
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
 
     if (state == MOD_PD_STATE_ON) {
-        if (ctx->initialized)
-            /* Restore the previous rate */
-            return do_pik_clock_set_rate(
-                dev_id, ctx->current_rate, MOD_CLOCK_ROUND_MODE_NONE);
-        else {
-            /* Perform deferred initialization and set the initial rate */
-            ctx->current_state = MOD_CLOCK_STATE_RUNNING;
-            ctx->initialized = true;
-            return do_pik_clock_set_rate(
-                dev_id, ctx->config->initial_rate, MOD_CLOCK_ROUND_MODE_NONE);
-        }
+	if (ctx->initialized)
+	    /* Restore the previous rate */
+	    return do_pik_clock_set_rate(
+		dev_id, ctx->current_rate, MOD_CLOCK_ROUND_MODE_NONE);
+	else {
+	    /* Perform deferred initialization and set the initial rate */
+	    ctx->current_state = MOD_CLOCK_STATE_RUNNING;
+	    ctx->initialized = true;
+	    return do_pik_clock_set_rate(
+		dev_id, ctx->config->initial_rate, MOD_CLOCK_ROUND_MODE_NONE);
+	}
     } else
-        ctx->current_state = MOD_CLOCK_STATE_STOPPED;
+	ctx->current_state = MOD_CLOCK_STATE_STOPPED;
 
     return FWK_SUCCESS;
 }
@@ -525,7 +525,7 @@ static const struct mod_clock_drv_api ap
 
 #ifdef BUILD_HAS_MOD_CSS_CLOCK
 static int pik_clock_direct_set_div(fwk_id_t clock_id, uint32_t divider_type,
-                                    uint32_t divider)
+				    uint32_t divider)
 {
     int status;
     struct pik_clock_dev_ctx *ctx;
@@ -534,19 +534,19 @@ static int pik_clock_direct_set_div(fwk_
     fwk_assert(ctx->config->is_group_member);
 
     if (ctx->current_state == MOD_CLOCK_STATE_STOPPED)
-        return FWK_E_PWRSTATE;
+	return FWK_E_PWRSTATE;
 
     switch (ctx->config->type) {
     case MOD_PIK_CLOCK_TYPE_SINGLE_SOURCE:
-        status = ssclock_set_div(ctx, divider, false);
-        break;
+	status = ssclock_set_div(ctx, divider, false);
+	break;
     case MOD_PIK_CLOCK_TYPE_CLUSTER:
     case MOD_PIK_CLOCK_TYPE_MULTI_SOURCE:
-        status = msclock_set_div(ctx,
-            (enum mod_pik_clock_msclock_divider)divider_type, divider, false);
-        break;
+	status = msclock_set_div(ctx,
+	    (enum mod_pik_clock_msclock_divider)divider_type, divider, false);
+	break;
     default:
-        return FWK_E_SUPPORT;
+	return FWK_E_SUPPORT;
     }
 
     return status;
@@ -560,13 +560,13 @@ static int pik_clock_direct_set_source(f
     fwk_assert(ctx->config->is_group_member);
 
     if (ctx->current_state == MOD_CLOCK_STATE_STOPPED)
-        return FWK_E_PWRSTATE;
+	return FWK_E_PWRSTATE;
 
     return msclock_set_source(ctx, source, false);
 }
 
 static int pik_clock_direct_set_mod(fwk_id_t clock_id, uint32_t numerator,
-                                    uint32_t denominator)
+				    uint32_t denominator)
 {
     struct pik_clock_dev_ctx *ctx;
 
@@ -574,7 +574,7 @@ static int pik_clock_direct_set_mod(fwk_
     fwk_assert(ctx->config->is_group_member);
 
     if (ctx->current_state == MOD_CLOCK_STATE_STOPPED)
-        return FWK_E_PWRSTATE;
+	return FWK_E_PWRSTATE;
 
     return cluster_set_modulator(ctx, numerator, denominator, false);
 }
@@ -588,15 +588,15 @@ static int pik_clock_direct_power_state_
     ctx = module_ctx.dev_ctx_table + fwk_id_get_element_idx(dev_id);
 
     if (!ctx->config->is_group_member)
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
 
     if (state == MOD_PD_STATE_ON) {
-        if (!ctx->initialized)
-            /* Perform delayed intialization */
-            ctx->initialized = true;
-        ctx->current_state = MOD_CLOCK_STATE_RUNNING;
+	if (!ctx->initialized)
+	    /* Perform delayed intialization */
+	    ctx->initialized = true;
+	ctx->current_state = MOD_CLOCK_STATE_RUNNING;
     } else
-        ctx->current_state = MOD_CLOCK_STATE_STOPPED;
+	ctx->current_state = MOD_CLOCK_STATE_STOPPED;
 
     return FWK_SUCCESS;
 }
@@ -614,28 +614,28 @@ static const struct mod_css_clock_direct
  */
 
 static int pik_clock_init(fwk_id_t module_id, unsigned int element_count,
-                          const void *data)
+			  const void *data)
 {
     struct mod_pik_clock_module_config *config =
-        (struct mod_pik_clock_module_config *)data;
+	(struct mod_pik_clock_module_config *)data;
     module_ctx.dev_count = element_count;
 
     if (element_count == 0)
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 
     module_ctx.divider_max = ((config == NULL) ?
-        (1 << MOD_PIK_CLOCK_DIVIDER_BITFIELD_WIDTH_5BITS) :
-        config->divider_max);
+	(1 << MOD_PIK_CLOCK_DIVIDER_BITFIELD_WIDTH_5BITS) :
+	config->divider_max);
 
     module_ctx.dev_ctx_table = fwk_mm_calloc(element_count,
-                                             sizeof(struct pik_clock_dev_ctx));
+					     sizeof(struct pik_clock_dev_ctx));
 
     return FWK_SUCCESS;
 }
 
 static int pik_clock_element_init(fwk_id_t element_id,
-                                  unsigned int sub_element_count,
-                                  const void *data)
+				  unsigned int sub_element_count,
+				  const void *data)
 {
     unsigned int i = 0;
     uint64_t current_rate;
@@ -647,14 +647,14 @@ static int pik_clock_element_init(fwk_id
 
     /* Verify that the rate entries in the device's lookup table are ordered */
     while (i < dev_config->rate_count) {
-        current_rate = dev_config->rate_table[i].rate;
+	current_rate = dev_config->rate_table[i].rate;
 
-        /* The rate entries must be in ascending order */
-        if (current_rate < last_rate)
-            return FWK_E_DATA;
+	/* The rate entries must be in ascending order */
+	if (current_rate < last_rate)
+	    return FWK_E_DATA;
 
-        last_rate = current_rate;
-        i++;
+	last_rate = current_rate;
+	i++;
     }
 
     ctx->config = dev_config;
@@ -663,7 +663,7 @@ static int pik_clock_element_init(fwk_id
     ctx->current_source = MOD_PIK_CLOCK_MSCLOCK_SOURCE_MAX;
 
     if (ctx->config->defer_initialization)
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 
     ctx->current_state = MOD_CLOCK_STATE_RUNNING;
     ctx->initialized = true;
@@ -674,38 +674,38 @@ static int pik_clock_element_init(fwk_id
      * driver during the start stage or in response to a notification.
      */
     if (ctx->config->is_group_member)
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 
     return do_pik_clock_set_rate(
-        element_id, dev_config->initial_rate, MOD_CLOCK_ROUND_MODE_NONE);
+	element_id, dev_config->initial_rate, MOD_CLOCK_ROUND_MODE_NONE);
 }
 
 static int pik_clock_process_bind_request(fwk_id_t source_id,
-                                          fwk_id_t target_id, fwk_id_t api_id,
-                                          const void **api)
+					  fwk_id_t target_id, fwk_id_t api_id,
+					  const void **api)
 {
     struct pik_clock_dev_ctx *ctx;
 
     /* Only elements can be bound to as the API depends on the element type */
     if (!fwk_id_is_type(target_id, FWK_ID_TYPE_ELEMENT))
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
 
     ctx = module_ctx.dev_ctx_table + fwk_id_get_element_idx(target_id);
 
     if (ctx->config->is_group_member) {
 #ifdef BUILD_HAS_MOD_CSS_CLOCK
-        /* Only the CSS Clock module can bind to group members. */
-        if (fwk_id_get_module_idx(source_id) == FWK_MODULE_IDX_CSS_CLOCK) {
-            *api = &api_direct;
-            return FWK_SUCCESS;
-        } else
-            return FWK_E_ACCESS;
+	/* Only the CSS Clock module can bind to group members. */
+	if (fwk_id_get_module_idx(source_id) == FWK_MODULE_IDX_CSS_CLOCK) {
+	    *api = &api_direct;
+	    return FWK_SUCCESS;
+	} else
+	    return FWK_E_ACCESS;
 #else
-        /* The CSS Clock module is required to support group members. */
-        return FWK_E_SUPPORT;
+	/* The CSS Clock module is required to support group members. */
+	return FWK_E_SUPPORT;
 #endif
     } else
-        *api = &api_clock;
+	*api = &api_clock;
 
     return FWK_SUCCESS;
 }
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/pl011/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/pl011/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/pl011/CMakeLists.txt
@@ -8,10 +8,10 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_pl011.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_pl011.c")
 
 if("clock" IN_LIST SCP_MODULES)
     target_link_libraries(${SCP_MODULE_TARGET} PRIVATE module-clock)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/pl011/src/mod_pl011.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/pl011/src/mod_pl011.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/pl011/src/mod_pl011.c
@@ -61,34 +61,34 @@ static int mod_pl011_init_ctx(struct mod
 
     element_count = (size_t)fwk_module_get_element_count(fwk_module_id_pl011);
     if (element_count == 0) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     ctx->elements = fwk_mm_alloc(element_count, sizeof(ctx->elements[0]));
     if (!fwk_expect(ctx->elements != NULL)) {
-        return FWK_E_NOMEM;
+	return FWK_E_NOMEM;
     }
 
     for (size_t i = 0; i < element_count; i++) {
-        const struct mod_pl011_element_cfg *cfg =
-            fwk_module_get_data(FWK_ID_ELEMENT(FWK_MODULE_IDX_PL011, i));
+	const struct mod_pl011_element_cfg *cfg =
+	    fwk_module_get_data(FWK_ID_ELEMENT(FWK_MODULE_IDX_PL011, i));
 
-        ctx->elements[i] = (struct mod_pl011_element_ctx){
-            .powered = true, /* Assume the device is always powered */
-            .clocked = true, /* Assume the device is always clocked */
+	ctx->elements[i] = (struct mod_pl011_element_ctx){
+	    .powered = true, /* Assume the device is always powered */
+	    .clocked = true, /* Assume the device is always clocked */
 
-            .open = false,
-        };
+	    .open = false,
+	};
 
 #ifdef BUILD_HAS_MOD_POWER_DOMAIN
-        ctx->elements[i].powered = fwk_id_is_equal(cfg->pd_id, FWK_ID_NONE);
+	ctx->elements[i].powered = fwk_id_is_equal(cfg->pd_id, FWK_ID_NONE);
 #endif
 
 #ifdef BUILD_HAS_MOD_CLOCK
-        ctx->elements[i].clocked = fwk_id_is_equal(cfg->clock_id, FWK_ID_NONE);
+	ctx->elements[i].clocked = fwk_id_is_equal(cfg->clock_id, FWK_ID_NONE);
 #endif
 
-        (void)cfg;
+	(void)cfg;
     }
 
     pl011_ctx.initialized = true;
@@ -144,7 +144,7 @@ static void mod_pl011_enable(fwk_id_t id
 {
     const struct mod_pl011_element_cfg *cfg = fwk_module_get_data(id);
     struct mod_pl011_element_ctx *ctx =
-        &pl011_ctx.elements[fwk_id_get_element_idx(id)];
+	&pl011_ctx.elements[fwk_id_get_element_idx(id)];
 
     struct pl011_reg *reg = (void *)cfg->reg_base;
 
@@ -162,7 +162,7 @@ static void mod_pl011_putch(fwk_id_t id,
 {
     const struct mod_pl011_element_cfg *cfg = fwk_module_get_data(id);
     struct mod_pl011_element_ctx *ctx =
-        &pl011_ctx.elements[fwk_id_get_element_idx(id)];
+	&pl011_ctx.elements[fwk_id_get_element_idx(id)];
 
     struct pl011_reg *reg = (void *)cfg->reg_base;
 
@@ -170,7 +170,7 @@ static void mod_pl011_putch(fwk_id_t id,
     fwk_assert(ctx->clocked);
 
     while (reg->FR & PL011_FR_TXFF) {
-        continue;
+	continue;
     }
 
     reg->DR = (uint16_t)ch;
@@ -180,7 +180,7 @@ static bool mod_pl011_getch(fwk_id_t id,
 {
     const struct mod_pl011_element_cfg *cfg = fwk_module_get_data(id);
     struct mod_pl011_element_ctx *ctx =
-        &pl011_ctx.elements[fwk_id_get_element_idx(id)];
+	&pl011_ctx.elements[fwk_id_get_element_idx(id)];
 
     struct pl011_reg *reg = (void *)cfg->reg_base;
 
@@ -188,7 +188,7 @@ static bool mod_pl011_getch(fwk_id_t id,
     fwk_assert(ctx->clocked);
 
     if (reg->FR & PL011_FR_RXFE) {
-        return false;
+	return false;
     }
 
     *ch = (char)reg->DR;
@@ -200,7 +200,7 @@ static void mod_pl011_flush(fwk_id_t id)
 {
     const struct mod_pl011_element_cfg *cfg = fwk_module_get_data(id);
     struct mod_pl011_element_ctx *ctx =
-        &pl011_ctx.elements[fwk_id_get_element_idx(id)];
+	&pl011_ctx.elements[fwk_id_get_element_idx(id)];
 
     struct pl011_reg *reg = (void *)cfg->reg_base;
 
@@ -208,7 +208,7 @@ static void mod_pl011_flush(fwk_id_t id)
     fwk_assert(ctx->clocked);
 
     while (reg->FR & PL011_FR_BUSY) {
-        continue;
+	continue;
     }
 }
 
@@ -218,7 +218,7 @@ static int mod_pl011_init(
     const void *data)
 {
     if (pl011_ctx.initialized) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     return mod_pl011_init_ctx(&pl011_ctx);
@@ -239,7 +239,7 @@ static int mod_pl011_start(fwk_id_t id)
     const struct mod_pl011_element_cfg *cfg;
 
     if (fwk_id_is_type(id, FWK_ID_TYPE_MODULE)) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     cfg = fwk_module_get_data(id);
@@ -252,19 +252,19 @@ static int mod_pl011_start(fwk_id_t id)
      */
 
     if (!fwk_id_is_equal(cfg->pd_id, FWK_ID_NONE)) {
-        status = fwk_notification_subscribe(
-            mod_pd_notification_id_power_state_transition, cfg->pd_id, id);
-        if (!fwk_expect(status == FWK_SUCCESS)) {
-            return FWK_E_HANDLER;
-        }
-
-        status = fwk_notification_subscribe(
-            mod_pd_notification_id_pre_shutdown,
-            fwk_module_id_power_domain,
-            id);
-        if (!fwk_expect(status == FWK_SUCCESS)) {
-            return FWK_E_HANDLER;
-        }
+	status = fwk_notification_subscribe(
+	    mod_pd_notification_id_power_state_transition, cfg->pd_id, id);
+	if (!fwk_expect(status == FWK_SUCCESS)) {
+	    return FWK_E_HANDLER;
+	}
+
+	status = fwk_notification_subscribe(
+	    mod_pd_notification_id_pre_shutdown,
+	    fwk_module_id_power_domain,
+	    id);
+	if (!fwk_expect(status == FWK_SUCCESS)) {
+	    return FWK_E_HANDLER;
+	}
     }
 #endif
 
@@ -276,11 +276,11 @@ static int mod_pl011_start(fwk_id_t id)
      */
 
     if (!fwk_id_is_equal(cfg->clock_id, FWK_ID_NONE)) {
-        status = fwk_notification_subscribe(
-            mod_clock_notification_id_state_changed, cfg->clock_id, id);
-        if (!fwk_expect(status == FWK_SUCCESS)) {
-            return FWK_E_HANDLER;
-        }
+	status = fwk_notification_subscribe(
+	    mod_clock_notification_id_state_changed, cfg->clock_id, id);
+	if (!fwk_expect(status == FWK_SUCCESS)) {
+	    return FWK_E_HANDLER;
+	}
     }
 #endif
 
@@ -296,22 +296,22 @@ static int mod_pl011_powering_down(fwk_i
     int status;
 
     struct mod_pl011_element_ctx *ctx =
-        &pl011_ctx.elements[fwk_id_get_element_idx(id)];
+	&pl011_ctx.elements[fwk_id_get_element_idx(id)];
     const struct mod_pl011_element_cfg *cfg = fwk_module_get_data(id);
 
     ctx->powered = false; /* This device has gone offline */
 
     /* We don't care about the pre-transition power on notification */
     status = fwk_notification_unsubscribe(
-        mod_pd_notification_id_power_state_pre_transition, cfg->pd_id, id);
+	mod_pd_notification_id_power_state_pre_transition, cfg->pd_id, id);
     if (!fwk_expect(status == FWK_SUCCESS)) {
-        return FWK_E_HANDLER;
+	return FWK_E_HANDLER;
     }
 
     status = fwk_notification_subscribe(
-        mod_pd_notification_id_power_state_transition, cfg->pd_id, id);
+	mod_pd_notification_id_power_state_transition, cfg->pd_id, id);
     if (!fwk_expect(status == FWK_SUCCESS)) {
-        return FWK_E_HANDLER;
+	return FWK_E_HANDLER;
     }
 
     return FWK_SUCCESS;
@@ -322,23 +322,23 @@ static int mod_pl011_powered_on(fwk_id_t
     int status;
 
     struct mod_pl011_element_ctx *ctx =
-        &pl011_ctx.elements[fwk_id_get_element_idx(id)];
+	&pl011_ctx.elements[fwk_id_get_element_idx(id)];
     const struct mod_pl011_element_cfg *cfg = fwk_module_get_data(id);
 
     ctx->powered = true; /* This device is powered again */
 
     /* We need only the pre-transition in order to prepare for power down */
     status = fwk_notification_unsubscribe(
-        mod_pd_notification_id_power_state_transition, cfg->pd_id, id);
+	mod_pd_notification_id_power_state_transition, cfg->pd_id, id);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     status = fwk_notification_subscribe(
-        mod_pd_notification_id_power_state_pre_transition, cfg->pd_id, id);
+	mod_pd_notification_id_power_state_pre_transition, cfg->pd_id, id);
 
     if (ctx->clocked) { /* Re-enable if no clock domain is configured */
-        mod_pl011_enable(id);
+	mod_pl011_enable(id);
     }
 
     return status;
@@ -352,63 +352,63 @@ static int mod_pl011_process_power_notif
 
     switch (fwk_id_get_notification_idx(event->id)) {
     case (unsigned int)MOD_PD_NOTIFICATION_IDX_POWER_STATE_PRE_TRANSITION: {
-        struct mod_pd_power_state_pre_transition_notification_params
-            *pd_pre_transition_params;
-        struct mod_pd_power_state_pre_transition_notification_resp_params
-            *pd_resp_params;
-
-        pd_pre_transition_params =
-            (struct mod_pd_power_state_pre_transition_notification_params *)
-                event->params;
-        pd_resp_params =
-            (struct mod_pd_power_state_pre_transition_notification_resp_params
-                 *)resp_event->params;
+	struct mod_pd_power_state_pre_transition_notification_params
+	    *pd_pre_transition_params;
+	struct mod_pd_power_state_pre_transition_notification_resp_params
+	    *pd_resp_params;
+
+	pd_pre_transition_params =
+	    (struct mod_pd_power_state_pre_transition_notification_params *)
+		event->params;
+	pd_resp_params =
+	    (struct mod_pd_power_state_pre_transition_notification_resp_params
+		 *)resp_event->params;
 
-        switch (pd_pre_transition_params->target_state) {
+	switch (pd_pre_transition_params->target_state) {
 #    ifdef BUILD_HAS_MOD_SYSTEM_POWER
-        case (unsigned int)MOD_SYSTEM_POWER_POWER_STATE_SLEEP0:
-            FWK_FALLTHROUGH;
+	case (unsigned int)MOD_SYSTEM_POWER_POWER_STATE_SLEEP0:
+	    FWK_FALLTHROUGH;
 #    endif
 
-        case (unsigned int)MOD_PD_STATE_OFF:
-            status = mod_pl011_powering_down(event->target_id);
+	case (unsigned int)MOD_PD_STATE_OFF:
+	    status = mod_pl011_powering_down(event->target_id);
 
-            pd_resp_params->status = status; /* Inform the power domain */
+	    pd_resp_params->status = status; /* Inform the power domain */
 
-            break;
+	    break;
 
-        default:
-            break;
-        }
+	default:
+	    break;
+	}
 
-        break;
+	break;
     }
 
     case (unsigned int)MOD_PD_NOTIFICATION_IDX_POWER_STATE_TRANSITION: {
-        struct mod_pd_power_state_transition_notification_params *params =
-            (struct mod_pd_power_state_transition_notification_params *)
-                event->params;
-
-        if (params->state != MOD_PD_STATE_ON) {
-            status = FWK_SUCCESS;
-        } else {
-            status = mod_pl011_powered_on(event->target_id);
-        }
+	struct mod_pd_power_state_transition_notification_params *params =
+	    (struct mod_pd_power_state_transition_notification_params *)
+		event->params;
+
+	if (params->state != MOD_PD_STATE_ON) {
+	    status = FWK_SUCCESS;
+	} else {
+	    status = mod_pl011_powered_on(event->target_id);
+	}
 
-        break;
+	break;
     }
 
     case (unsigned int)MOD_PD_NOTIFICATION_IDX_PRE_SHUTDOWN: {
-        struct mod_pd_pre_shutdown_notif_resp_params
-            *pd_pre_shutdown_resp_params =
-                (struct mod_pd_pre_shutdown_notif_resp_params *)
-                    resp_event->params;
+	struct mod_pd_pre_shutdown_notif_resp_params
+	    *pd_pre_shutdown_resp_params =
+		(struct mod_pd_pre_shutdown_notif_resp_params *)
+		    resp_event->params;
 
-        status = mod_pl011_powering_down(event->target_id);
+	status = mod_pl011_powering_down(event->target_id);
 
-        pd_pre_shutdown_resp_params->status = status;
+	pd_pre_shutdown_resp_params->status = status;
 
-        break;
+	break;
     }
     }
 
@@ -424,17 +424,17 @@ static int mod_pl011_clock_change_pendin
     int status;
 
     const struct clock_notification_params *event_params =
-        (const void *)event->params;
+	(const void *)event->params;
     struct clock_state_change_pending_resp_params *resp_event_params =
-        (void *)resp_event->params;
+	(void *)resp_event->params;
 
     struct mod_pl011_element_ctx *ctx =
-        &pl011_ctx.elements[fwk_id_get_element_idx(event->target_id)];
+	&pl011_ctx.elements[fwk_id_get_element_idx(event->target_id)];
     const struct mod_pl011_element_cfg *cfg =
-        fwk_module_get_data(event->target_id);
+	fwk_module_get_data(event->target_id);
 
     *resp_event_params = (struct clock_state_change_pending_resp_params){
-        .status = FWK_SUCCESS,
+	.status = FWK_SUCCESS,
     };
 
     fwk_assert(event_params->new_state == MOD_CLOCK_STATE_STOPPED);
@@ -444,19 +444,19 @@ static int mod_pl011_clock_change_pendin
     ctx->clocked = false;
 
     status = fwk_notification_unsubscribe(
-        mod_clock_notification_id_state_change_pending,
-        cfg->clock_id,
-        event->target_id);
+	mod_clock_notification_id_state_change_pending,
+	cfg->clock_id,
+	event->target_id);
     if (!fwk_expect(status == FWK_SUCCESS)) {
-        return FWK_E_OS;
+	return FWK_E_OS;
     }
 
     status = fwk_notification_subscribe(
-        mod_clock_notification_id_state_changed,
-        cfg->clock_id,
-        event->target_id);
+	mod_clock_notification_id_state_changed,
+	cfg->clock_id,
+	event->target_id);
     if (!fwk_expect(status == FWK_SUCCESS)) {
-        return FWK_E_OS;
+	return FWK_E_OS;
     }
 
     return FWK_SUCCESS;
@@ -467,35 +467,35 @@ static int mod_pl011_clock_changed(const
     int status;
 
     const struct clock_notification_params *event_params =
-        (const void *)event->params;
+	(const void *)event->params;
 
     struct mod_pl011_element_ctx *ctx =
-        &pl011_ctx.elements[fwk_id_get_element_idx(event->target_id)];
+	&pl011_ctx.elements[fwk_id_get_element_idx(event->target_id)];
     const struct mod_pl011_element_cfg *cfg =
-        fwk_module_get_data(event->target_id);
+	fwk_module_get_data(event->target_id);
 
     fwk_assert(event_params->new_state == MOD_CLOCK_STATE_RUNNING);
 
     ctx->clocked = true;
 
     status = fwk_notification_unsubscribe(
-        mod_clock_notification_id_state_changed,
-        cfg->clock_id,
-        event->target_id);
+	mod_clock_notification_id_state_changed,
+	cfg->clock_id,
+	event->target_id);
     if (status != FWK_SUCCESS) {
-        return FWK_E_OS;
+	return FWK_E_OS;
     }
 
     status = fwk_notification_subscribe(
-        mod_clock_notification_id_state_change_pending,
-        cfg->clock_id,
-        event->target_id);
+	mod_clock_notification_id_state_change_pending,
+	cfg->clock_id,
+	event->target_id);
     if (status != FWK_SUCCESS) {
-        return FWK_E_OS;
+	return FWK_E_OS;
     }
 
     if (ctx->powered) { /* Re-enable if no clock domain is configured */
-        mod_pl011_enable(event->target_id);
+	mod_pl011_enable(event->target_id);
     }
 
     return FWK_SUCCESS;
@@ -509,14 +509,14 @@ static int mod_pl011_process_clock_notif
 
     switch (fwk_id_get_notification_idx(event->id)) {
     case (unsigned int)MOD_CLOCK_NOTIFICATION_IDX_STATE_CHANGED:
-        status = mod_pl011_clock_changed(event);
+	status = mod_pl011_clock_changed(event);
 
-        break;
+	break;
 
     case (unsigned int)MOD_CLOCK_NOTIFICATION_IDX_STATE_CHANGE_PENDING:
-        status = mod_pl011_clock_change_pending(event, resp_event);
+	status = mod_pl011_clock_change_pending(event, resp_event);
 
-        break;
+	break;
     }
 
     return status;
@@ -538,20 +538,20 @@ static int mod_pl011_process_notificatio
     switch (module_idx) {
 #ifdef BUILD_HAS_MOD_POWER_DOMAIN
     case FWK_MODULE_IDX_POWER_DOMAIN:
-        status = mod_pl011_process_power_notification(event, resp_event);
+	status = mod_pl011_process_power_notification(event, resp_event);
 
-        break;
+	break;
 #endif
 
 #ifdef BUILD_HAS_MOD_CLOCK
     case FWK_MODULE_IDX_CLOCK:
-        status = mod_pl011_process_clock_notification(event, resp_event);
+	status = mod_pl011_process_clock_notification(event, resp_event);
 
-        break;
+	break;
 #endif
 
     default:
-        break;
+	break;
     }
 
     return status;
@@ -564,25 +564,25 @@ static int mod_pl011_io_open(const struc
     struct mod_pl011_element_ctx *ctx;
 
     if (!fwk_id_is_type(stream->id, FWK_ID_TYPE_ELEMENT)) {
-        return FWK_E_SUPPORT;
+	return FWK_E_SUPPORT;
     }
 
     if (!pl011_ctx.initialized) {
-        status = mod_pl011_init_ctx(&pl011_ctx);
-        if (status != FWK_SUCCESS) {
-            return FWK_E_STATE;
-        }
+	status = mod_pl011_init_ctx(&pl011_ctx);
+	if (status != FWK_SUCCESS) {
+	    return FWK_E_STATE;
+	}
     }
 
     ctx = &pl011_ctx.elements[fwk_id_get_element_idx(stream->id)];
     if (ctx->open) { /* Refuse to open the same device twice */
-        return FWK_E_BUSY;
+	return FWK_E_BUSY;
     }
 
     ctx->open = true;
 
     if (ctx->powered && ctx->clocked) {
-        mod_pl011_enable(stream->id); /* Enable the device if possible */
+	mod_pl011_enable(stream->id); /* Enable the device if possible */
     }
 
     return FWK_SUCCESS;
@@ -593,19 +593,19 @@ static int mod_pl011_io_getch(
     char *restrict ch)
 {
     const struct mod_pl011_element_ctx *ctx =
-        &pl011_ctx.elements[fwk_id_get_element_idx(stream->id)];
+	&pl011_ctx.elements[fwk_id_get_element_idx(stream->id)];
 
     bool ok = true;
 
     fwk_assert(ctx->open);
 
     if (!ctx->powered || !ctx->clocked) {
-        return FWK_E_PWRSTATE;
+	return FWK_E_PWRSTATE;
     }
 
     ok = mod_pl011_getch(stream->id, ch);
     if (!ok) {
-        return FWK_PENDING;
+	return FWK_PENDING;
     }
 
     return FWK_SUCCESS;
@@ -614,12 +614,12 @@ static int mod_pl011_io_getch(
 static int mod_pl011_io_putch(const struct fwk_io_stream *stream, char ch)
 {
     const struct mod_pl011_element_ctx *ctx =
-        &pl011_ctx.elements[fwk_id_get_element_idx(stream->id)];
+	&pl011_ctx.elements[fwk_id_get_element_idx(stream->id)];
 
     fwk_assert(ctx->open);
 
     if (!ctx->powered || !ctx->clocked) {
-        return FWK_E_PWRSTATE;
+	return FWK_E_PWRSTATE;
     }
 
     mod_pl011_putch(stream->id, ch);
@@ -654,10 +654,10 @@ const struct fwk_module module_pl011 = {
     .process_notification = mod_pl011_process_notification,
 
     .adapter =
-        (struct fwk_io_adapter){
-            .open = mod_pl011_io_open,
-            .getch = mod_pl011_io_getch,
-            .putch = mod_pl011_io_putch,
-            .close = mod_pl011_close,
-        },
+	(struct fwk_io_adapter){
+	    .open = mod_pl011_io_open,
+	    .getch = mod_pl011_io_getch,
+	    .putch = mod_pl011_io_putch,
+	    .close = mod_pl011_close,
+	},
 };
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/pl011/src/pl011.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/pl011/src/pl011.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/pl011/src/pl011.h
@@ -17,35 +17,35 @@
 
 struct pl011_reg {
     FWK_RW  uint16_t DR;
-            uint16_t RESERVED0;
+	    uint16_t RESERVED0;
     union {
-        FWK_RW  uint8_t     RSR;
-        FWK_RW  uint8_t     ECR;
+	FWK_RW  uint8_t     RSR;
+	FWK_RW  uint8_t     ECR;
     };
-            uint8_t  RESERVED1[0x18 - 0x05];
+	    uint8_t  RESERVED1[0x18 - 0x05];
     FWK_R   uint16_t FR;
-            uint16_t RESERVED2[3];
+	    uint16_t RESERVED2[3];
     FWK_RW  uint8_t  ILPR;
-            uint8_t  RESERVED3[3];
+	    uint8_t  RESERVED3[3];
     FWK_RW  uint16_t IBRD;
-            uint16_t RESERVED4;
+	    uint16_t RESERVED4;
     FWK_RW  uint32_t FBRD;
     FWK_RW  uint16_t LCR_H;
-            uint16_t RESERVED5;
+	    uint16_t RESERVED5;
     FWK_RW  uint16_t CR;
-            uint16_t RESERVED6;
+	    uint16_t RESERVED6;
     FWK_RW  uint16_t IFLS;
-            uint16_t RESERVED7;
+	    uint16_t RESERVED7;
     FWK_RW  uint16_t IMSC;
-            uint16_t RESERVED8;
+	    uint16_t RESERVED8;
     FWK_R   uint16_t RIS;
-            uint16_t RESERVED9;
+	    uint16_t RESERVED9;
     FWK_R   uint16_t MIS;
-            uint16_t RESERVED10;
+	    uint16_t RESERVED10;
     FWK_W   uint16_t ICR;
-            uint16_t RESERVED11;
+	    uint16_t RESERVED11;
     FWK_RW  uint16_t DMACR;
-            uint8_t  RESERVED12[0xFE0 - 0x4C];
+	    uint8_t  RESERVED12[0xFE0 - 0x4C];
     FWK_R   uint32_t PID0;
     FWK_R   uint32_t PID1;
     FWK_R   uint32_t PID2;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/pmi/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/pmi/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/pmi/CMakeLists.txt
@@ -8,7 +8,7 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_pmi.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_pmi.c")
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/pmi/src/mod_pmi.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/pmi/src/mod_pmi.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/pmi/src/mod_pmi.c
@@ -81,9 +81,9 @@ static int pmi_bind(fwk_id_t id, unsigne
 {
     /* Bind to PMI driver */
     return fwk_module_bind(
-        pmi_ctx.driver_config->driver_id,
-        pmi_ctx.driver_config->driver_api_id,
-        &pmi_ctx.driver_api);
+	pmi_ctx.driver_config->driver_id,
+	pmi_ctx.driver_config->driver_api_id,
+	&pmi_ctx.driver_api);
 }
 
 static int pmi_bind_request(
@@ -94,8 +94,8 @@ static int pmi_bind_request(
 {
     /* Only allow binding to the module */
     if (!fwk_id_is_equal(target_id, fwk_module_id_pmi) ||
-        !fwk_id_is_equal(api_id, mod_pmi_api_id_hal)) {
-        return FWK_E_PARAM;
+	!fwk_id_is_equal(api_id, mod_pmi_api_id_hal)) {
+	return FWK_E_PARAM;
     }
 
     *api = &mod_pmi_api;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/power_domain/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/power_domain/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/power_domain/CMakeLists.txt
@@ -8,9 +8,9 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(
     ${SCP_MODULE_TARGET}
     PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_power_domain.c"
-            "${CMAKE_CURRENT_SOURCE_DIR}/src/power_domain_utils.c")
+	    "${CMAKE_CURRENT_SOURCE_DIR}/src/power_domain_utils.c")
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/power_domain/include/mod_power_domain.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/power_domain/include/mod_power_domain.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/power_domain/include/mod_power_domain.h
@@ -61,7 +61,7 @@ enum mod_pd_type {
  * \brief Bit width of power domain type within attributes.
  */
 #define MOD_PD_TYPE_BITS_WIDTH ((sizeof(unsigned int) * CHAR_BIT) - \
-                               __builtin_clz(MOD_PD_TYPE_COUNT - 1))
+			       __builtin_clz(MOD_PD_TYPE_COUNT - 1))
 
 /*!
  * \brief Attributes of a power domain.
@@ -72,7 +72,7 @@ struct mod_pd_attributes {
 
     /*! \internal */
     unsigned int not_used : ((sizeof(unsigned int) * CHAR_BIT) -
-                             MOD_PD_TYPE_BITS_WIDTH);
+			     MOD_PD_TYPE_BITS_WIDTH);
 };
 
 /*!
@@ -246,22 +246,22 @@ enum mod_pd_state_mask {
  */
 enum {
     MOD_PD_CS_LEVEL_0_STATE_SHIFT =
-        MOD_PD_LEVEL_0 * MOD_PD_CS_STATE_BITS_PER_LEVEL,
+	MOD_PD_LEVEL_0 * MOD_PD_CS_STATE_BITS_PER_LEVEL,
     MOD_PD_CS_LEVEL_1_STATE_SHIFT =
-        MOD_PD_LEVEL_1 * MOD_PD_CS_STATE_BITS_PER_LEVEL,
+	MOD_PD_LEVEL_1 * MOD_PD_CS_STATE_BITS_PER_LEVEL,
     MOD_PD_CS_LEVEL_2_STATE_SHIFT =
-        MOD_PD_LEVEL_2 * MOD_PD_CS_STATE_BITS_PER_LEVEL,
+	MOD_PD_LEVEL_2 * MOD_PD_CS_STATE_BITS_PER_LEVEL,
     MOD_PD_CS_LEVEL_3_STATE_SHIFT =
-        MOD_PD_LEVEL_3 * MOD_PD_CS_STATE_BITS_PER_LEVEL,
+	MOD_PD_LEVEL_3 * MOD_PD_CS_STATE_BITS_PER_LEVEL,
     MOD_PD_CS_LEVEL_SHIFT =
-        MOD_PD_LEVEL_COUNT * MOD_PD_CS_STATE_BITS_PER_LEVEL,
+	MOD_PD_LEVEL_COUNT * MOD_PD_CS_STATE_BITS_PER_LEVEL,
 };
 
 /*!
  * \brief Compute a composite power domain state.
  */
 #define MOD_PD_COMPOSITE_STATE(HIGHEST_LEVEL, LEVEL_3_STATE, LEVEL_2_STATE, \
-                               LEVEL_1_STATE, LEVEL_0_STATE)                \
+			       LEVEL_1_STATE, LEVEL_0_STATE)                \
     (((HIGHEST_LEVEL) << MOD_PD_CS_LEVEL_SHIFT)         | \
      ((LEVEL_3_STATE) << MOD_PD_CS_LEVEL_3_STATE_SHIFT) | \
      ((LEVEL_2_STATE) << MOD_PD_CS_LEVEL_2_STATE_SHIFT) | \
@@ -394,7 +394,7 @@ struct mod_pd_driver_api {
      *      call to the API.
      */
     int (*shutdown)(fwk_id_t dev_id,
-                    enum mod_pd_system_shutdown system_shutdown);
+		    enum mod_pd_system_shutdown system_shutdown);
 };
 
 /*!
@@ -615,9 +615,9 @@ struct mod_pd_driver_input_api {
      * \retval ::FWK_E_PARAM One or more parameters were invalid.
      */
     int (*set_state)(
-        fwk_id_t pd_id,
-        bool resp_requested,
-        uint32_t composite_state);
+	fwk_id_t pd_id,
+	bool resp_requested,
+	uint32_t composite_state);
 
     /*!
      * \brief Request for a power domain to be reset.
@@ -784,12 +784,12 @@ enum mod_pd_notification_idx {
 /*! Identifier of the power state transition notification */
 static const fwk_id_t mod_pd_notification_id_power_state_transition =
     FWK_ID_NOTIFICATION_INIT(FWK_MODULE_IDX_POWER_DOMAIN,
-                             MOD_PD_NOTIFICATION_IDX_POWER_STATE_TRANSITION);
+			     MOD_PD_NOTIFICATION_IDX_POWER_STATE_TRANSITION);
 
 /*! Identifier of the power state pre-transition notification */
 static const fwk_id_t mod_pd_notification_id_power_state_pre_transition =
     FWK_ID_NOTIFICATION_INIT(FWK_MODULE_IDX_POWER_DOMAIN,
-        MOD_PD_NOTIFICATION_IDX_POWER_STATE_PRE_TRANSITION);
+	MOD_PD_NOTIFICATION_IDX_POWER_STATE_PRE_TRANSITION);
 
 /*!
  * Identifier of the pre-shutdown notification.
@@ -798,7 +798,7 @@ static const fwk_id_t mod_pd_notificatio
  */
 static const fwk_id_t mod_pd_notification_id_pre_shutdown =
     FWK_ID_NOTIFICATION_INIT(FWK_MODULE_IDX_POWER_DOMAIN,
-        MOD_PD_NOTIFICATION_IDX_PRE_SHUTDOWN);
+	MOD_PD_NOTIFICATION_IDX_PRE_SHUTDOWN);
 
 /*!
  * Identifier of the pre-warm reboot notification.
@@ -807,8 +807,8 @@ static const fwk_id_t mod_pd_notificatio
  */
 static const fwk_id_t mod_pd_notification_id_pre_warm_reset =
     FWK_ID_NOTIFICATION_INIT(
-        FWK_MODULE_IDX_POWER_DOMAIN,
-        MOD_PD_NOTIFICATION_IDX_PRE_WARM_RESET);
+	FWK_MODULE_IDX_POWER_DOMAIN,
+	MOD_PD_NOTIFICATION_IDX_PRE_WARM_RESET);
 
 #    endif
 #endif /* BUILD_HAS_NOTIFICATION */
@@ -854,7 +854,7 @@ struct pd_set_state_response {
 /*! Identifier of the public event set_state identifier */
 static const fwk_id_t mod_pd_public_event_id_set_state =
     FWK_ID_EVENT_INIT(FWK_MODULE_IDX_POWER_DOMAIN,
-                      MOD_PD_PUBLIC_EVENT_IDX_SET_STATE);
+		      MOD_PD_PUBLIC_EVENT_IDX_SET_STATE);
 
 #endif
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/power_domain/src/mod_power_domain.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/power_domain/src/mod_power_domain.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/power_domain/src/mod_power_domain.c
@@ -151,11 +151,11 @@ struct pd_ctx {
 
     /* Context for the power state transition notification */
     struct mod_power_state_transition_notification_ctx
-        power_state_transition_notification_ctx;
+	power_state_transition_notification_ctx;
 
     /* Context for the power state pre-transition notification */
     struct mod_power_state_pre_transition_notification_ctx
-        power_state_pre_transition_notification_ctx;
+	power_state_pre_transition_notification_ctx;
 };
 
 struct system_suspend_ctx {
@@ -300,7 +300,7 @@ static const char * const default_state_
 static bool is_valid_state(const struct pd_ctx *pd, unsigned int state)
 {
     return (state < MOD_PD_STATE_COUNT_MAX) &&
-        ((pd->valid_state_mask & ((uint32_t)1 << state)) != (uint32_t)0);
+	((pd->valid_state_mask & ((uint32_t)1 << state)) != (uint32_t)0);
 }
 
 static unsigned int normalize_state(unsigned int state)
@@ -309,24 +309,24 @@ static unsigned int normalize_state(unsi
 
     switch (state_type) {
     case MOD_PD_STATE_OFF:
-        return (MOD_PD_STATE_COUNT_MAX + 1);
+	return (MOD_PD_STATE_COUNT_MAX + 1);
 
     case MOD_PD_STATE_SLEEP:
-        return MOD_PD_STATE_COUNT_MAX;
+	return MOD_PD_STATE_COUNT_MAX;
 
     default:
-        return state;
+	return state;
     }
 }
 
 static bool is_deeper_state(unsigned int state,
-                            unsigned int state_to_compare_to)
+			    unsigned int state_to_compare_to)
 {
     return normalize_state(state) > normalize_state(state_to_compare_to);
 }
 
 static bool is_shallower_state(unsigned int state,
-                               unsigned int state_to_compare_to)
+			       unsigned int state_to_compare_to)
 {
     return normalize_state(state) < normalize_state(state_to_compare_to);
 }
@@ -335,12 +335,12 @@ static bool is_allowed_by_child(const st
     unsigned int parent_state, unsigned int child_state)
 {
     if (parent_state >= child->allowed_state_mask_table_size) {
-        return false;
+	return false;
     }
 
     return (
-        (child->allowed_state_mask_table[parent_state] &
-         ((uint32_t)1 << child_state)) != (uint32_t)0);
+	(child->allowed_state_mask_table[parent_state] &
+	 ((uint32_t)1 << child_state)) != (uint32_t)0);
 }
 
 static bool is_allowed_by_children(const struct pd_ctx *pd, unsigned int state)
@@ -349,11 +349,11 @@ static bool is_allowed_by_children(const
     struct fwk_slist *c_node = NULL;
 
     FWK_LIST_FOR_EACH(
-        &pd->children_list, c_node, struct pd_ctx, child_node, child)
+	&pd->children_list, c_node, struct pd_ctx, child_node, child)
     {
-        if (!is_allowed_by_child(child, state, child->requested_state)) {
-            return false;
-        }
+	if (!is_allowed_by_child(child, state, child->requested_state)) {
+	    return false;
+	}
     }
 
     return true;
@@ -365,11 +365,11 @@ static const char *get_state_name(const
     static char const unknown_name[] = "Unknown";
 
     if (state < pd->config->state_name_table_size) {
-        return pd->config->state_name_table[state];
+	return pd->config->state_name_table[state];
     } else if (state < FWK_ARRAY_SIZE(default_state_name_table)) {
-        return default_state_name_table[state];
+	return default_state_name_table[state];
     } else {
-        return unknown_name;
+	return unknown_name;
     }
 }
 #endif
@@ -379,12 +379,12 @@ static unsigned int number_of_bits_to_sh
     unsigned int num_bits = 0;
 
     if (mask == (uint32_t)0) {
-        return 0;
+	return 0;
     }
 
     while ((mask & (uint32_t)1) == (uint32_t)0) {
-        mask = mask >> 1;
-        num_bits++;
+	mask = mask >> 1;
+	num_bits++;
     }
 
     return num_bits;
@@ -412,32 +412,32 @@ static int get_highest_level_from_compos
     unsigned int table_size;
 
     if (!pd->cs_support) {
-        return 0;
+	return 0;
     }
 
     if (pd->composite_state_levels_mask) {
-        shift = number_of_bits_to_shift(pd->composite_state_levels_mask);
-        level = (pd->composite_state_levels_mask & composite_state) >> shift;
+	shift = number_of_bits_to_shift(pd->composite_state_levels_mask);
+	level = (pd->composite_state_levels_mask & composite_state) >> shift;
     } else {
-        state_mask_table = pd->composite_state_mask_table;
-        table_size = (unsigned int)pd->composite_state_mask_table_size;
+	state_mask_table = pd->composite_state_mask_table;
+	table_size = (unsigned int)pd->composite_state_mask_table_size;
 
-        for (level = 0; ((level < table_size) && (pd != NULL));
-             level++, pd = pd->parent) {
-            state = get_level_state_from_composite_state(
-                state_mask_table, composite_state, (int)level);
-            if (!is_valid_state(pd, state)) {
-                break;
-            }
-        }
-        level--;
+	for (level = 0; ((level < table_size) && (pd != NULL));
+	     level++, pd = pd->parent) {
+	    state = get_level_state_from_composite_state(
+		state_mask_table, composite_state, (int)level);
+	    if (!is_valid_state(pd, state)) {
+		break;
+	    }
+	}
+	level--;
     }
 
     return (int)level;
 }
 
 static bool is_valid_composite_state(struct pd_ctx *target_pd,
-                                     uint32_t composite_state)
+				     uint32_t composite_state)
 {
     unsigned int level, highest_level;
     unsigned int state, child_state = (unsigned int)MOD_PD_STATE_OFF;
@@ -449,48 +449,48 @@ static bool is_valid_composite_state(str
     assert(target_pd != NULL);
 
     if (!pd->cs_support) {
-        goto error;
+	goto error;
     }
 
     highest_level = (unsigned int)get_highest_level_from_composite_state(
-        pd, composite_state);
+	pd, composite_state);
 
     state_mask_table = pd->composite_state_mask_table;
     table_size = (unsigned int)pd->composite_state_mask_table_size;
 
     if (highest_level >= table_size) {
-        goto error;
+	goto error;
     }
 
     for (level = 0; level <= highest_level; level++) {
-        if (pd == NULL) {
-            goto error;
-        }
-
-        state = get_level_state_from_composite_state(
-            state_mask_table, composite_state, (int)level);
-
-        if (!is_valid_state(pd, state)) {
-            goto error;
-        }
-
-        if ((child != NULL) &&
-            !is_allowed_by_child(child, state, child_state)) {
-            goto error;
-        }
-
-        child = pd;
-        child_state = state;
-        pd = pd->parent;
+	if (pd == NULL) {
+	    goto error;
+	}
+
+	state = get_level_state_from_composite_state(
+	    state_mask_table, composite_state, (int)level);
+
+	if (!is_valid_state(pd, state)) {
+	    goto error;
+	}
+
+	if ((child != NULL) &&
+	    !is_allowed_by_child(child, state, child_state)) {
+	    goto error;
+	}
+
+	child = pd;
+	child_state = state;
+	pd = pd->parent;
     }
 
     return true;
 
 error:
     FWK_LOG_ERR(
-        "[PD] Invalid composite state for %s: 0x%" PRIX32,
-        fwk_module_get_element_name(target_pd->id),
-        composite_state);
+	"[PD] Invalid composite state for %s: 0x%" PRIX32,
+	fwk_module_get_element_name(target_pd->id),
+	composite_state);
     return false;
 }
 
@@ -512,23 +512,23 @@ static bool is_upwards_transition_propag
     const uint32_t *state_mask_table;
 
     highest_level =
-        get_highest_level_from_composite_state(lowest_pd, composite_state);
+	get_highest_level_from_composite_state(lowest_pd, composite_state);
 
     if (!lowest_pd->cs_support) {
-        return is_deeper_state(composite_state, lowest_pd->requested_state);
+	return is_deeper_state(composite_state, lowest_pd->requested_state);
     }
 
     state_mask_table = lowest_pd->composite_state_mask_table;
 
     for (level = 0, pd = lowest_pd; (level <= highest_level) && (pd != NULL);
-         level++, pd = pd->parent) {
-        state = get_level_state_from_composite_state(
-            state_mask_table, composite_state, level);
-        if (state == pd->requested_state) {
-            continue;
-        }
+	 level++, pd = pd->parent) {
+	state = get_level_state_from_composite_state(
+	    state_mask_table, composite_state, level);
+	if (state == pd->requested_state) {
+	    continue;
+	}
 
-        return is_deeper_state(state, pd->requested_state);
+	return is_deeper_state(state, pd->requested_state);
     }
 
     return false;
@@ -541,17 +541,17 @@ static int connect_pd_tree(void)
     struct pd_ctx *pd, *parent;
 
     for (index = 0; index < mod_pd_ctx.pd_count; index++) {
-        pd = &mod_pd_ctx.pd_ctx_table[index];
-        if (pd->config->parent_idx >= mod_pd_ctx.pd_count) {
-            pd->parent = NULL;
-            continue;
-        }
-
-        parent = pd->parent = &mod_pd_ctx.pd_ctx_table[pd->config->parent_idx];
-        if (parent == NULL) {
-            return FWK_E_DATA;
-        }
-        fwk_list_push_tail(&parent->children_list, &pd->child_node);
+	pd = &mod_pd_ctx.pd_ctx_table[index];
+	if (pd->config->parent_idx >= mod_pd_ctx.pd_count) {
+	    pd->parent = NULL;
+	    continue;
+	}
+
+	parent = pd->parent = &mod_pd_ctx.pd_ctx_table[pd->config->parent_idx];
+	if (parent == NULL) {
+	    return FWK_E_DATA;
+	}
+	fwk_list_push_tail(&parent->children_list, &pd->child_node);
     }
 
     return FWK_SUCCESS;
@@ -573,17 +573,17 @@ static bool is_allowed_by_parent_and_chi
 
     parent = pd->parent;
     if (parent != NULL) {
-        if (!is_allowed_by_child(pd, parent->current_state, state)) {
-            return false;
-        }
+	if (!is_allowed_by_child(pd, parent->current_state, state)) {
+	    return false;
+	}
     }
 
     FWK_LIST_FOR_EACH(
-        &pd->children_list, c_node, struct pd_ctx, child_node, child)
+	&pd->children_list, c_node, struct pd_ctx, child_node, child)
     {
-        if (!is_allowed_by_child(child, state, child->current_state)) {
-            return false;
-        }
+	if (!is_allowed_by_child(child, state, child->current_state)) {
+	    return false;
+	}
     }
 
     return true;
@@ -604,9 +604,9 @@ static bool check_power_state_pre_transi
     unsigned int state)
 {
     if ((state == pd->power_state_pre_transition_notification_ctx.state) &&
-        pd->power_state_pre_transition_notification_ctx.valid) {
-        return (pd->power_state_pre_transition_notification_ctx.response_status
-                != FWK_SUCCESS);
+	pd->power_state_pre_transition_notification_ctx.valid) {
+	return (pd->power_state_pre_transition_notification_ctx.response_status
+		!= FWK_SUCCESS);
     }
 
     return true;
@@ -627,20 +627,20 @@ static bool initiate_power_state_pre_tra
 #ifdef BUILD_HAS_NOTIFICATION
     unsigned int state;
     struct fwk_event notification_event = {
-        .id = mod_pd_notification_id_power_state_pre_transition,
-        .response_requested = true,
-        .source_id = FWK_ID_NONE
+	.id = mod_pd_notification_id_power_state_pre_transition,
+	.response_requested = true,
+	.source_id = FWK_ID_NONE
     };
     struct mod_pd_power_state_pre_transition_notification_params *params;
     int status;
 
     if (pd->config->disable_state_transition_notifications == true) {
-        return false;
+	return false;
     }
 
     state = pd->requested_state;
     if (!check_power_state_pre_transition_notification(pd, state)) {
-        return false;
+	return false;
     }
 
     /*
@@ -648,30 +648,30 @@ static bool initiate_power_state_pre_tra
      * pre-transition notification, wait for them before to issue the next one.
      */
     if (pd->power_state_pre_transition_notification_ctx.pending_responses !=
-        0) {
-        return true;
+	0) {
+	return true;
     }
 
     params = (struct mod_pd_power_state_pre_transition_notification_params *)
-        notification_event.params;
+	notification_event.params;
     params->current_state = pd->current_state;
     params->target_state = state;
 
     notification_event.source_id = pd->id;
     status = fwk_notification_notify(
-        &notification_event,
-        &pd->power_state_pre_transition_notification_ctx.pending_responses);
+	&notification_event,
+	&pd->power_state_pre_transition_notification_ctx.pending_responses);
     if (status != FWK_SUCCESS) {
-        FWK_LOG_DEBUG("[PD] %s @%d", __func__, __LINE__);
+	FWK_LOG_DEBUG("[PD] %s @%d", __func__, __LINE__);
     }
 
     pd->power_state_pre_transition_notification_ctx.state = state;
     pd->power_state_pre_transition_notification_ctx.response_status =
-        FWK_SUCCESS;
+	FWK_SUCCESS;
     pd->power_state_pre_transition_notification_ctx.valid = true;
 
     return (pd->power_state_pre_transition_notification_ctx.pending_responses
-            != 0);
+	    != 0);
 #else
     return false;
 #endif
@@ -693,36 +693,36 @@ static int initiate_power_state_transiti
     unsigned int state = pd->requested_state;
 
     if ((pd->driver_api->deny != NULL) &&
-        pd->driver_api->deny(pd->driver_id, state)) {
+	pd->driver_api->deny(pd->driver_id, state)) {
 #if FWK_LOG_LEVEL <= FWK_LOG_LEVEL_DEBUG
-        FWK_LOG_DEBUG(
-            "[PD] Transition of %s to state <%s> denied by driver",
-            fwk_module_get_element_name(pd->id),
-            get_state_name(pd, state));
+	FWK_LOG_DEBUG(
+	    "[PD] Transition of %s to state <%s> denied by driver",
+	    fwk_module_get_element_name(pd->id),
+	    get_state_name(pd, state));
 #endif
-        return FWK_E_DEVICE;
+	return FWK_E_DEVICE;
     }
 
     status = pd->driver_api->set_state(pd->driver_id, state);
 
 #if FWK_LOG_LEVEL <= FWK_LOG_LEVEL_DEBUG
     if (status == FWK_SUCCESS) {
-        FWK_LOG_DEBUG(
-            "[PD] Transition of %s from <%s> to <%s> succeeded",
-            fwk_module_get_element_name(pd->id),
-            get_state_name(pd, pd->state_requested_to_driver),
-            get_state_name(pd, state));
+	FWK_LOG_DEBUG(
+	    "[PD] Transition of %s from <%s> to <%s> succeeded",
+	    fwk_module_get_element_name(pd->id),
+	    get_state_name(pd, pd->state_requested_to_driver),
+	    get_state_name(pd, state));
     }
 #endif
 
 #if FWK_LOG_LEVEL <= FWK_LOG_LEVEL_ERROR
     if (status != FWK_SUCCESS) {
-        FWK_LOG_ERR(
-            "[PD] Transition of %s from <%s> to <%s> failed: %s",
-            fwk_module_get_element_name(pd->id),
-            get_state_name(pd, pd->state_requested_to_driver),
-            get_state_name(pd, state),
-            fwk_status_str(status));
+	FWK_LOG_ERR(
+	    "[PD] Transition of %s from <%s> to <%s> failed: %s",
+	    fwk_module_get_element_name(pd->id),
+	    get_state_name(pd, pd->state_requested_to_driver),
+	    get_state_name(pd, state),
+	    fwk_status_str(status));
     }
 #endif
 
@@ -742,19 +742,19 @@ static void respond(struct pd_ctx *pd, i
     int status;
     struct fwk_event resp_event;
     const struct pd_set_state_request *req_params =
-        (struct pd_set_state_request *)(&resp_event.params);
+	(struct pd_set_state_request *)(&resp_event.params);
     struct pd_set_state_response *resp_params =
-        (struct pd_set_state_response *)(&resp_event.params);
+	(struct pd_set_state_response *)(&resp_event.params);
 
     if (!pd->response.pending) {
-        return;
+	return;
     }
 
     status = fwk_get_delayed_response(pd->id, pd->response.cookie, &resp_event);
     pd->response.pending = false;
 
     if (status != FWK_SUCCESS) {
-        return;
+	return;
     }
 
     resp_params->composite_state = req_params->composite_state;
@@ -762,7 +762,7 @@ static void respond(struct pd_ctx *pd, i
 
     status = fwk_put_event(&resp_event);
     if (status != FWK_SUCCESS) {
-        FWK_LOG_DEBUG("[PD] %s @%d", __func__, __LINE__);
+	FWK_LOG_DEBUG("[PD] %s @%d", __func__, __LINE__);
     }
 }
 
@@ -807,7 +807,7 @@ static void process_set_state_request(
      */
     lowest_level = 0;
     highest_level = (unsigned int)get_highest_level_from_composite_state(
-        lowest_pd, composite_state);
+	lowest_pd, composite_state);
     nb_pds = highest_level + 1U;
 
     status = FWK_SUCCESS;
@@ -815,126 +815,126 @@ static void process_set_state_request(
 
     composite_state_operation = pd->cs_support;
     if (composite_state_operation) {
-        state_mask_table = pd->composite_state_mask_table;
+	state_mask_table = pd->composite_state_mask_table;
     }
 
     for (pd_index = 0; pd_index < nb_pds; pd_index++, pd = pd->parent) {
-        if (up) {
-            level = pd_index;
-        } else {
-            /*
-             * When walking down the power domain tree, get the context of the
-             * next power domain to process as well as its level.
-             */
-            pd = lowest_pd;
-            for (level = lowest_level; level < (highest_level - pd_index);
-                 level++) {
-                pd = pd->parent;
-            }
-        }
-
-        if (composite_state_operation) {
-            state = get_level_state_from_composite_state(
-                state_mask_table, composite_state, (int)level);
-        } else {
-            state = composite_state;
-        }
-
-        if (state == pd->requested_state) {
-            continue;
-        }
-
-        /*
-         * Check that the requested power state is compatible with the states
-         * currently requested for the parent and children of the power domain.
-         */
-        parent = pd->parent;
-        if ((parent != NULL) &&
-            (!is_allowed_by_child(pd, parent->requested_state, state))) {
-            status = FWK_E_PWRSTATE;
-            break;
-        }
-
-        if (!is_allowed_by_children(pd, state)) {
-            continue;
-        }
-
-        /*
-         * A new valid power state is requested for the power domain. Send any
-         * pending response concerning the previous requested power state.
-         */
-        pd->requested_state = state;
-        pd->power_state_pre_transition_notification_ctx.valid = false;
-        respond(pd, FWK_E_OVERWRITTEN);
-
-        if (pd->state_requested_to_driver == state) {
-            continue;
-        }
-
-        /*
-         * The driver must be called thus the processing of the set state
-         * request is going to be asynchronous. Assign the responsibility of
-         * the response to the request to the power domain. If there is no
-         * need for a driver call for the ancestors or descendants of the power
-         * domain as part of the processing of the requested composite state,
-         * the response to the request will be sent when the transition to the
-         * new requested power state is completed.
-         */
-        pd_in_charge_of_response = pd;
-
-        /*
-         * If a power state transition has already been initiated for an
-         * ancestor or descendant, we don't initiate the power state transition
-         * now. It will be initiated on completion of the transition of one
-         * of its ancestor or descendant.
-         */
-        if (first_power_state_transition_initiated) {
-            continue;
-        }
-
-        /*
-         * If the parent or a child is not currently in a power state
-         * compatible with the new requested state for the power domain, do not
-         * initiate the transition now as well. It will be initiated when the
-         * parent and the children are in a proper state.
-         */
-        if (!is_allowed_by_parent_and_children(pd, state)) {
-            continue;
-        }
-
-        /*
-         * Defer the power state transition if power state pre-transition
-         * notification responses need to be waited for.
-         */
-        if (initiate_power_state_pre_transition_notification(pd)) {
-            continue;
-        }
-
-        status = initiate_power_state_transition(pd);
-        if (status != FWK_SUCCESS) {
-            /*
-             * If the power state transition failed, then this power domain is
-             * no longer in charge to delay the response.
-             */
-            pd_in_charge_of_response = NULL;
-            break;
-        }
+	if (up) {
+	    level = pd_index;
+	} else {
+	    /*
+	     * When walking down the power domain tree, get the context of the
+	     * next power domain to process as well as its level.
+	     */
+	    pd = lowest_pd;
+	    for (level = lowest_level; level < (highest_level - pd_index);
+		 level++) {
+		pd = pd->parent;
+	    }
+	}
+
+	if (composite_state_operation) {
+	    state = get_level_state_from_composite_state(
+		state_mask_table, composite_state, (int)level);
+	} else {
+	    state = composite_state;
+	}
+
+	if (state == pd->requested_state) {
+	    continue;
+	}
+
+	/*
+	 * Check that the requested power state is compatible with the states
+	 * currently requested for the parent and children of the power domain.
+	 */
+	parent = pd->parent;
+	if ((parent != NULL) &&
+	    (!is_allowed_by_child(pd, parent->requested_state, state))) {
+	    status = FWK_E_PWRSTATE;
+	    break;
+	}
+
+	if (!is_allowed_by_children(pd, state)) {
+	    continue;
+	}
+
+	/*
+	 * A new valid power state is requested for the power domain. Send any
+	 * pending response concerning the previous requested power state.
+	 */
+	pd->requested_state = state;
+	pd->power_state_pre_transition_notification_ctx.valid = false;
+	respond(pd, FWK_E_OVERWRITTEN);
+
+	if (pd->state_requested_to_driver == state) {
+	    continue;
+	}
+
+	/*
+	 * The driver must be called thus the processing of the set state
+	 * request is going to be asynchronous. Assign the responsibility of
+	 * the response to the request to the power domain. If there is no
+	 * need for a driver call for the ancestors or descendants of the power
+	 * domain as part of the processing of the requested composite state,
+	 * the response to the request will be sent when the transition to the
+	 * new requested power state is completed.
+	 */
+	pd_in_charge_of_response = pd;
+
+	/*
+	 * If a power state transition has already been initiated for an
+	 * ancestor or descendant, we don't initiate the power state transition
+	 * now. It will be initiated on completion of the transition of one
+	 * of its ancestor or descendant.
+	 */
+	if (first_power_state_transition_initiated) {
+	    continue;
+	}
+
+	/*
+	 * If the parent or a child is not currently in a power state
+	 * compatible with the new requested state for the power domain, do not
+	 * initiate the transition now as well. It will be initiated when the
+	 * parent and the children are in a proper state.
+	 */
+	if (!is_allowed_by_parent_and_children(pd, state)) {
+	    continue;
+	}
+
+	/*
+	 * Defer the power state transition if power state pre-transition
+	 * notification responses need to be waited for.
+	 */
+	if (initiate_power_state_pre_transition_notification(pd)) {
+	    continue;
+	}
+
+	status = initiate_power_state_transition(pd);
+	if (status != FWK_SUCCESS) {
+	    /*
+	     * If the power state transition failed, then this power domain is
+	     * no longer in charge to delay the response.
+	     */
+	    pd_in_charge_of_response = NULL;
+	    break;
+	}
 
-        first_power_state_transition_initiated = true;
+	first_power_state_transition_initiated = true;
     }
 
     if (!event->response_requested) {
-        return;
+	return;
     }
 
     if (pd_in_charge_of_response != NULL) {
-        resp_event->is_delayed_response = true;
-        resp_event->source_id = pd_in_charge_of_response->id;
-        pd_in_charge_of_response->response.pending = true;
-        pd_in_charge_of_response->response.cookie = resp_event->cookie;
+	resp_event->is_delayed_response = true;
+	resp_event->source_id = pd_in_charge_of_response->id;
+	pd_in_charge_of_response->response.pending = true;
+	pd_in_charge_of_response->response.cookie = resp_event->cookie;
     } else {
-        resp_params->status = status;
-        resp_params->composite_state = composite_state;
+	resp_params->status = status;
+	resp_params->composite_state = composite_state;
     }
 }
 
@@ -955,14 +955,14 @@ static int complete_system_suspend(struc
     struct pd_ctx *pd = target_pd;
     struct fwk_event event, resp_event;
     struct pd_set_state_request *event_params =
-        (struct pd_set_state_request *)event.params;
+	(struct pd_set_state_request *)event.params;
     struct pd_set_state_response *resp_params =
-        (struct pd_set_state_response *)(&resp_event.params);
+	(struct pd_set_state_response *)(&resp_event.params);
     const uint32_t *state_mask_table;
     int table_size;
 
     if (!pd->cs_support) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     state_mask_table = pd->composite_state_mask_table;
@@ -977,13 +977,13 @@ static int complete_system_suspend(struc
      */
     level = 0U;
     do {
-        shift = number_of_bits_to_shift(state_mask_table[level]);
-        composite_state |=
-            ((pd->parent != NULL) ? MOD_PD_STATE_OFF :
-                                    mod_pd_ctx.system_suspend.state)
-            << shift;
-        pd = pd->parent;
-        level++;
+	shift = number_of_bits_to_shift(state_mask_table[level]);
+	composite_state |=
+	    ((pd->parent != NULL) ? MOD_PD_STATE_OFF :
+				    mod_pd_ctx.system_suspend.state)
+	    << shift;
+	pd = pd->parent;
+	level++;
     } while ((pd != NULL) && (level < (unsigned int)table_size));
 
     /*
@@ -1018,35 +1018,35 @@ static void process_get_state_request(st
     int table_size, cs_idx = 0;
 
     if (!pd->cs_support) {
-        *state = pd->current_state;
+	*state = pd->current_state;
     } else {
-        state_mask_table = pd->composite_state_mask_table;
-        table_size = (int)pd->composite_state_mask_table_size;
+	state_mask_table = pd->composite_state_mask_table;
+	table_size = (int)pd->composite_state_mask_table_size;
 
-        /*
-         * Traverse the PD tree bottom-up from current power domain to the top,
-         * collecting node's states and placing them in the correct position in
-         * the composite state.
-         */
-        do {
-            shift = number_of_bits_to_shift(state_mask_table[cs_idx]);
-            composite_state |= pd->current_state << shift;
-            pd = pd->parent;
-            cs_idx++;
-            level++;
-        } while (pd != NULL && cs_idx < table_size);
-
-        /*
-         * Finally, we need to update the highest valid level in
-         * the composite state.
-         */
-        if (base_pd->composite_state_levels_mask) {
-            shift =
-                number_of_bits_to_shift(base_pd->composite_state_levels_mask);
-            composite_state |= (--level) << shift;
-        }
+	/*
+	 * Traverse the PD tree bottom-up from current power domain to the top,
+	 * collecting node's states and placing them in the correct position in
+	 * the composite state.
+	 */
+	do {
+	    shift = number_of_bits_to_shift(state_mask_table[cs_idx]);
+	    composite_state |= pd->current_state << shift;
+	    pd = pd->parent;
+	    cs_idx++;
+	    level++;
+	} while (pd != NULL && cs_idx < table_size);
+
+	/*
+	 * Finally, we need to update the highest valid level in
+	 * the composite state.
+	 */
+	if (base_pd->composite_state_levels_mask) {
+	    shift =
+		number_of_bits_to_shift(base_pd->composite_state_levels_mask);
+	    composite_state |= (--level) << shift;
+	}
 
-        *state = composite_state;
+	*state = composite_state;
     }
 }
 
@@ -1057,7 +1057,7 @@ static void process_get_state_request(st
  * resp_params Parameters of the 'reset' request response to be filled in
  */
 static void process_reset_request(struct pd_ctx *pd,
-                                  struct pd_response *resp_params)
+				  struct pd_response *resp_params)
 {
     int status;
     struct pd_ctx *child = NULL;
@@ -1065,16 +1065,16 @@ static void process_reset_request(struct
 
     status = FWK_E_PWRSTATE;
     if (pd->requested_state == MOD_PD_STATE_OFF) {
-        goto exit;
+	goto exit;
     }
 
     FWK_LIST_FOR_EACH(
-        &pd->children_list, c_node, struct pd_ctx, child_node, child)
+	&pd->children_list, c_node, struct pd_ctx, child_node, child)
     {
-        if ((child->requested_state != MOD_PD_STATE_OFF) ||
-            (child->current_state != MOD_PD_STATE_OFF)) {
-            goto exit;
-        }
+	if ((child->requested_state != MOD_PD_STATE_OFF) ||
+	    (child->current_state != MOD_PD_STATE_OFF)) {
+	    goto exit;
+	}
     }
 
     status = pd->driver_api->reset(pd->driver_id);
@@ -1097,24 +1097,24 @@ static void process_power_state_transiti
     int status;
 
     if (parent == NULL) {
-        return;
+	return;
     }
 
     requested_state = parent->requested_state;
 
     if (parent->state_requested_to_driver == requested_state) {
-        return;
+	return;
     }
 
     if (!is_allowed_by_parent_and_children(parent, requested_state)) {
-        return;
+	return;
     }
 
     if (!initiate_power_state_pre_transition_notification(parent)) {
-        status = initiate_power_state_transition(parent);
-        if (status != FWK_SUCCESS) {
-            FWK_LOG_DEBUG("[PD] %s @%d", __func__, __LINE__);
-        }
+	status = initiate_power_state_transition(parent);
+	if (status != FWK_SUCCESS) {
+	    FWK_LOG_DEBUG("[PD] %s @%d", __func__, __LINE__);
+	}
     }
 }
 
@@ -1133,23 +1133,23 @@ static void process_power_state_transiti
     int status;
 
     FWK_LIST_FOR_EACH(
-        &pd->children_list, c_node, struct pd_ctx, child_node, child)
+	&pd->children_list, c_node, struct pd_ctx, child_node, child)
     {
-        requested_state = child->requested_state;
-        if (child->state_requested_to_driver == requested_state) {
-            continue;
-        }
-
-        if (!is_allowed_by_parent_and_children(child, requested_state)) {
-            return;
-        }
-
-        if (!initiate_power_state_pre_transition_notification(child)) {
-            status = initiate_power_state_transition(child);
-            if (status != FWK_SUCCESS) {
-                FWK_LOG_DEBUG("[PD] %s @%d", __func__, __LINE__);
-            }
-        }
+	requested_state = child->requested_state;
+	if (child->state_requested_to_driver == requested_state) {
+	    continue;
+	}
+
+	if (!is_allowed_by_parent_and_children(child, requested_state)) {
+	    return;
+	}
+
+	if (!initiate_power_state_pre_transition_notification(child)) {
+	    status = initiate_power_state_transition(child);
+	    if (status != FWK_SUCCESS) {
+		FWK_LOG_DEBUG("[PD] %s @%d", __func__, __LINE__);
+	    }
+	}
     }
 }
 
@@ -1166,16 +1166,16 @@ static void process_power_state_transiti
     unsigned int previous_state;
 #ifdef BUILD_HAS_NOTIFICATION
     struct fwk_event notification_event = {
-        .id = mod_pd_notification_id_power_state_transition,
-        .response_requested = true,
-        .source_id = FWK_ID_NONE
+	.id = mod_pd_notification_id_power_state_transition,
+	.response_requested = true,
+	.source_id = FWK_ID_NONE
     };
     struct mod_pd_power_state_transition_notification_params *params;
 #endif
     int status;
 
     if (new_state == pd->requested_state) {
-        respond(pd, FWK_SUCCESS);
+	respond(pd, FWK_SUCCESS);
     }
 
     previous_state = pd->current_state;
@@ -1183,37 +1183,37 @@ static void process_power_state_transiti
 
 #ifdef BUILD_HAS_NOTIFICATION
     if (pd->power_state_transition_notification_ctx.pending_responses == 0 &&
-        pd->config->disable_state_transition_notifications == false) {
-        params = (struct mod_pd_power_state_transition_notification_params *)
-            notification_event.params;
-        params->state = new_state;
-        pd->power_state_transition_notification_ctx.state = new_state;
-        status = fwk_notification_notify(
-            &notification_event,
-            &pd->power_state_transition_notification_ctx.pending_responses);
-        if (status != FWK_SUCCESS) {
-            FWK_LOG_DEBUG("[PD] %s @%d", __func__, __LINE__);
-        }
+	pd->config->disable_state_transition_notifications == false) {
+	params = (struct mod_pd_power_state_transition_notification_params *)
+	    notification_event.params;
+	params->state = new_state;
+	pd->power_state_transition_notification_ctx.state = new_state;
+	status = fwk_notification_notify(
+	    &notification_event,
+	    &pd->power_state_transition_notification_ctx.pending_responses);
+	if (status != FWK_SUCCESS) {
+	    FWK_LOG_DEBUG("[PD] %s @%d", __func__, __LINE__);
+	}
     }
 #endif
 
     if ((mod_pd_ctx.system_suspend.last_core_off_ongoing) &&
-        (pd == mod_pd_ctx.system_suspend.last_core_pd)) {
-        mod_pd_ctx.system_suspend.last_core_off_ongoing = false;
-        status = complete_system_suspend(pd);
-        if (status != FWK_SUCCESS) {
-            FWK_LOG_DEBUG("[PD] %s @%d", __func__, __LINE__);
-        }
+	(pd == mod_pd_ctx.system_suspend.last_core_pd)) {
+	mod_pd_ctx.system_suspend.last_core_off_ongoing = false;
+	status = complete_system_suspend(pd);
+	if (status != FWK_SUCCESS) {
+	    FWK_LOG_DEBUG("[PD] %s @%d", __func__, __LINE__);
+	}
 
-        return;
+	return;
     }
 
     if (pd->parent == NULL) {
-        /* this is the top pd (SYSTOP) */
-        if (mod_pd_ctx.system_suspend.state != MOD_PD_STATE_ON) {
-            /* has gone down, invalidate the system suspend ongoing */
-            mod_pd_ctx.system_suspend.suspend_ongoing = false;
-        }
+	/* this is the top pd (SYSTOP) */
+	if (mod_pd_ctx.system_suspend.state != MOD_PD_STATE_ON) {
+	    /* has gone down, invalidate the system suspend ongoing */
+	    mod_pd_ctx.system_suspend.suspend_ongoing = false;
+	}
     }
 
 #ifdef BUILD_HAS_NOTIFICATION
@@ -1222,22 +1222,22 @@ static void process_power_state_transiti
      * the state change notifications responses have arrived.
      */
     if (pd->power_state_transition_notification_ctx.pending_responses > 0) {
-         /*
-          * Save previous state which will be used once all the notifications
-          * have arrived to continue for deeper or shallower state for the next
-          * power domain.
-          */
-         pd->power_state_transition_notification_ctx.previous_state =
-            previous_state;
+	 /*
+	  * Save previous state which will be used once all the notifications
+	  * have arrived to continue for deeper or shallower state for the next
+	  * power domain.
+	  */
+	 pd->power_state_transition_notification_ctx.previous_state =
+	    previous_state;
 
-         return;
+	 return;
     }
 #endif
 
     if (is_deeper_state(new_state, previous_state)) {
-        process_power_state_transition_report_deeper_state(pd);
+	process_power_state_transition_report_deeper_state(pd);
     } else if (is_shallower_state(new_state, previous_state)) {
-        process_power_state_transition_report_shallower_state(pd);
+	process_power_state_transition_report_shallower_state(pd);
     }
 }
 
@@ -1263,43 +1263,43 @@ static void process_system_suspend_reque
      * but one core and its ancestors.
      */
     for (pd_idx = 0; pd_idx < mod_pd_ctx.pd_count; pd_idx++) {
-        pd = &mod_pd_ctx.pd_ctx_table[pd_idx];
-        if ((pd->requested_state == MOD_PD_STATE_OFF) &&
-            (pd->current_state == MOD_PD_STATE_OFF)) {
-            continue;
-        }
-
-        if (pd->config->attributes.pd_type == MOD_PD_TYPE_CORE) {
-            if (last_core_pd != NULL) {
-                resp_params->status = FWK_E_STATE;
-                return;
-            }
-            last_core_pd = pd;
-        } else if (pd->config->attributes.pd_type == MOD_PD_TYPE_CLUSTER) {
-            if (last_cluster_pd != NULL) {
-                resp_params->status = FWK_E_STATE;
-                return;
-            }
-            last_cluster_pd = pd;
-        }
+	pd = &mod_pd_ctx.pd_ctx_table[pd_idx];
+	if ((pd->requested_state == MOD_PD_STATE_OFF) &&
+	    (pd->current_state == MOD_PD_STATE_OFF)) {
+	    continue;
+	}
+
+	if (pd->config->attributes.pd_type == MOD_PD_TYPE_CORE) {
+	    if (last_core_pd != NULL) {
+		resp_params->status = FWK_E_STATE;
+		return;
+	    }
+	    last_core_pd = pd;
+	} else if (pd->config->attributes.pd_type == MOD_PD_TYPE_CLUSTER) {
+	    if (last_cluster_pd != NULL) {
+		resp_params->status = FWK_E_STATE;
+		return;
+	    }
+	    last_cluster_pd = pd;
+	}
     }
 
     if (last_core_pd == NULL) {
-        status = complete_system_suspend(
-            (last_cluster_pd != NULL) ? last_cluster_pd :
-            mod_pd_ctx.system_pd_ctx);
+	status = complete_system_suspend(
+	    (last_cluster_pd != NULL) ? last_cluster_pd :
+	    mod_pd_ctx.system_pd_ctx);
     } else {
-        status = last_core_pd->driver_api->prepare_core_for_system_suspend(
-            last_core_pd->driver_id);
-        if (status == FWK_SUCCESS) {
-            mod_pd_ctx.system_suspend.last_core_off_ongoing = true;
-
-            mod_pd_ctx.system_suspend.last_core_pd = last_core_pd;
-            mod_pd_ctx.system_suspend.state = req_params->state;
-            last_core_pd->requested_state =
-                last_core_pd->state_requested_to_driver =
-                    (unsigned int)MOD_PD_STATE_OFF;
-        }
+	status = last_core_pd->driver_api->prepare_core_for_system_suspend(
+	    last_core_pd->driver_id);
+	if (status == FWK_SUCCESS) {
+	    mod_pd_ctx.system_suspend.last_core_off_ongoing = true;
+
+	    mod_pd_ctx.system_suspend.last_core_pd = last_core_pd;
+	    mod_pd_ctx.system_suspend.state = req_params->state;
+	    last_core_pd->requested_state =
+		last_core_pd->state_requested_to_driver =
+		    (unsigned int)MOD_PD_STATE_OFF;
+	}
     }
 
     resp_params->status = status;
@@ -1318,45 +1318,45 @@ void perform_shutdown(
     struct mod_pd_driver_api *api;
 
     for (pd_idx = 0; pd_idx < mod_pd_ctx.pd_count; pd_idx++) {
-        pd = &mod_pd_ctx.pd_ctx_table[pd_idx];
-        pd_id = FWK_ID_ELEMENT(FWK_MODULE_IDX_POWER_DOMAIN, pd_idx);
-        api = pd->driver_api;
-
-        (void)pd_id;
-        FWK_LOG_INFO(
-            "[PD] Shutting down %s", fwk_module_get_element_name(pd_id));
-
-        if (api->shutdown != NULL) {
-            status = pd->driver_api->shutdown(pd->driver_id, system_shutdown);
-            if (status == FWK_PENDING) {
-                /*
-                 * Only drivers implementing shutdown API can defer request for
-                 * now.
-                 */
-                return;
-            }
-        } else {
-            if ((api->deny != NULL) &&
-                api->deny(pd->driver_id, MOD_PD_STATE_OFF)) {
-                status = FWK_E_DEVICE;
-            } else {
-                status = api->set_state(pd->driver_id, MOD_PD_STATE_OFF);
-            }
-        }
-
-        if (status != FWK_SUCCESS) {
-            FWK_LOG_ERR(
-                "[PD] Shutdown of %s returned %s (%d)",
-                fwk_module_get_element_name(pd_id),
-                fwk_status_str(status),
-                status);
-        } else {
-            FWK_LOG_INFO(
-                "[PD] %s shutdown", fwk_module_get_element_name(pd_id));
-        }
+	pd = &mod_pd_ctx.pd_ctx_table[pd_idx];
+	pd_id = FWK_ID_ELEMENT(FWK_MODULE_IDX_POWER_DOMAIN, pd_idx);
+	api = pd->driver_api;
+
+	(void)pd_id;
+	FWK_LOG_INFO(
+	    "[PD] Shutting down %s", fwk_module_get_element_name(pd_id));
+
+	if (api->shutdown != NULL) {
+	    status = pd->driver_api->shutdown(pd->driver_id, system_shutdown);
+	    if (status == FWK_PENDING) {
+		/*
+		 * Only drivers implementing shutdown API can defer request for
+		 * now.
+		 */
+		return;
+	    }
+	} else {
+	    if ((api->deny != NULL) &&
+		api->deny(pd->driver_id, MOD_PD_STATE_OFF)) {
+		status = FWK_E_DEVICE;
+	    } else {
+		status = api->set_state(pd->driver_id, MOD_PD_STATE_OFF);
+	    }
+	}
+
+	if (status != FWK_SUCCESS) {
+	    FWK_LOG_ERR(
+		"[PD] Shutdown of %s returned %s (%d)",
+		fwk_module_get_element_name(pd_id),
+		fwk_status_str(status),
+		status);
+	} else {
+	    FWK_LOG_INFO(
+		"[PD] %s shutdown", fwk_module_get_element_name(pd_id));
+	}
 
-        pd->requested_state = pd->state_requested_to_driver =
-            pd->current_state = (unsigned int)MOD_PD_STATE_OFF;
+	pd->requested_state = pd->state_requested_to_driver =
+	    pd->current_state = (unsigned int)MOD_PD_STATE_OFF;
     }
 
     /*
@@ -1366,23 +1366,23 @@ void perform_shutdown(
      * be notified.
      */
     if (resp == NULL) {
-        status = fwk_get_delayed_response(
-            fwk_module_id_power_domain,
-            mod_pd_ctx.system_shutdown.cookie,
-            &delayed_resp);
-        fwk_assert(status == FWK_SUCCESS);
+	status = fwk_get_delayed_response(
+	    fwk_module_id_power_domain,
+	    mod_pd_ctx.system_shutdown.cookie,
+	    &delayed_resp);
+	fwk_assert(status == FWK_SUCCESS);
 
-        delayed_resp.source_id = fwk_module_id_power_domain;
+	delayed_resp.source_id = fwk_module_id_power_domain;
 
-        resp_params = (struct pd_response *)delayed_resp.params;
-        resp_params->status = FWK_E_PANIC;
+	resp_params = (struct pd_response *)delayed_resp.params;
+	resp_params->status = FWK_E_PANIC;
 
-        status = fwk_put_event(&delayed_resp);
+	status = fwk_put_event(&delayed_resp);
     } else {
-        resp_params = (struct pd_response *)resp->params;
-        resp_params->status = FWK_E_PANIC;
+	resp_params = (struct pd_response *)resp->params;
+	resp_params->status = FWK_E_PANIC;
 
-        status = fwk_put_event(resp);
+	status = fwk_put_event(resp);
     }
 
     fwk_assert(status == FWK_SUCCESS);
@@ -1398,8 +1398,8 @@ static int notify_warm_reset(void)
 #ifdef BUILD_HAS_NOTIFICATION
     unsigned int count;
     struct fwk_event notification = {
-        .id = mod_pd_notification_id_pre_warm_reset,
-        .source_id = FWK_ID_MODULE_INIT(FWK_MODULE_IDX_POWER_DOMAIN),
+	.id = mod_pd_notification_id_pre_warm_reset,
+	.source_id = FWK_ID_MODULE_INIT(FWK_MODULE_IDX_POWER_DOMAIN),
     };
 
     return fwk_notification_notify(&notification, &count);
@@ -1416,18 +1416,18 @@ static bool check_and_notify_system_shut
     int status;
 
     struct fwk_event notification = {
-        .id = mod_pd_notification_id_pre_shutdown,
-        .source_id = fwk_module_id_power_domain,
-        .response_requested = true
+	.id = mod_pd_notification_id_pre_shutdown,
+	.source_id = fwk_module_id_power_domain,
+	.response_requested = true
     };
 
     params = (struct mod_pd_pre_shutdown_notif_params *)notification.params;
     params->system_shutdown = system_shutdown;
 
     status = fwk_notification_notify(
-        &notification, &mod_pd_ctx.system_shutdown.notifications_count);
+	&notification, &mod_pd_ctx.system_shutdown.notifications_count);
     if (status != FWK_SUCCESS) {
-        FWK_LOG_DEBUG("[PD] %s @%d", __func__, __LINE__);
+	FWK_LOG_DEBUG("[PD] %s @%d", __func__, __LINE__);
     }
 
     return (mod_pd_ctx.system_shutdown.notifications_count != 0);
@@ -1446,35 +1446,35 @@ static void process_system_shutdown_requ
     enum mod_pd_system_shutdown system_shutdown;
 
     const struct pd_system_shutdown_request *req_params =
-        (struct pd_system_shutdown_request *)event->params;
+	(struct pd_system_shutdown_request *)event->params;
     struct pd_response *resp_params = (struct pd_response *)resp->params;
 
     system_shutdown = req_params->system_shutdown;
 
     switch (system_shutdown) {
     case MOD_PD_SYSTEM_WARM_RESET:
-        resp_params->status = notify_warm_reset();
-        break;
+	resp_params->status = notify_warm_reset();
+	break;
 
     default:
-        /* Check and send pre-shutdown notifications */
-        if (check_and_notify_system_shutdown(system_shutdown)) {
-            mod_pd_ctx.system_shutdown.ongoing = true;
-            mod_pd_ctx.system_shutdown.system_shutdown = system_shutdown;
-
-            mod_pd_ctx.system_shutdown.cookie = event->cookie;
-            resp->is_delayed_response = true;
-
-            /*
-             * The shutdown procedure will be completed once all the
-             * notification responses have been received.
-             */
-            return;
-        }
+	/* Check and send pre-shutdown notifications */
+	if (check_and_notify_system_shutdown(system_shutdown)) {
+	    mod_pd_ctx.system_shutdown.ongoing = true;
+	    mod_pd_ctx.system_shutdown.system_shutdown = system_shutdown;
+
+	    mod_pd_ctx.system_shutdown.cookie = event->cookie;
+	    resp->is_delayed_response = true;
+
+	    /*
+	     * The shutdown procedure will be completed once all the
+	     * notification responses have been received.
+	     */
+	    return;
+	}
 
-        perform_shutdown(system_shutdown, resp);
+	perform_shutdown(system_shutdown, resp);
 
-        resp_params->status = FWK_E_PANIC;
+	resp_params->status = FWK_E_PANIC;
     }
 }
 
@@ -1488,11 +1488,11 @@ static int pd_get_domain_type(fwk_id_t p
     struct pd_ctx *pd;
 
     if (type == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     if (!fwk_module_is_valid_element_id(pd_id)) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     pd = &mod_pd_ctx.pd_ctx_table[fwk_id_get_element_idx(pd_id)];
@@ -1507,11 +1507,11 @@ static int pd_get_domain_parent_id(fwk_i
     const struct pd_ctx *pd;
 
     if (parent_pd_id == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     if (!fwk_module_is_valid_element_id(pd_id)) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     pd = &mod_pd_ctx.pd_ctx_table[fwk_id_get_element_idx(pd_id)];
@@ -1527,26 +1527,26 @@ static int pd_set_state(fwk_id_t pd_id,
     struct pd_ctx *pd;
     struct fwk_event req;
     struct pd_set_state_request *req_params =
-                               (struct pd_set_state_request *)(&req.params);
+			       (struct pd_set_state_request *)(&req.params);
 
     pd = &mod_pd_ctx.pd_ctx_table[fwk_id_get_element_idx(pd_id)];
 
     if (pd->cs_support) {
-        if (!is_valid_composite_state(pd, state)) {
-            return FWK_E_PARAM;
-        }
+	if (!is_valid_composite_state(pd, state)) {
+	    return FWK_E_PARAM;
+	}
     } else {
-        if (!is_valid_state(pd, state)) {
-            return FWK_E_PARAM;
-        }
+	if (!is_valid_state(pd, state)) {
+	    return FWK_E_PARAM;
+	}
     }
 
     req = (struct fwk_event) {
-        .id = FWK_ID_EVENT(FWK_MODULE_IDX_POWER_DOMAIN,
-                           MOD_PD_PUBLIC_EVENT_IDX_SET_STATE),
-        .source_id = pd->driver_id,
-        .target_id = pd_id,
-        .response_requested = response_requested,
+	.id = FWK_ID_EVENT(FWK_MODULE_IDX_POWER_DOMAIN,
+			   MOD_PD_PUBLIC_EVENT_IDX_SET_STATE),
+	.source_id = pd->driver_id,
+	.target_id = pd_id,
+	.response_requested = response_requested,
     };
 
     req_params->composite_state = state;
@@ -1559,7 +1559,7 @@ static int pd_get_state(fwk_id_t pd_id,
     struct pd_ctx *pd = NULL;
 
     if (state == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     pd = &mod_pd_ctx.pd_ctx_table[fwk_id_get_element_idx(pd_id)];
@@ -1573,12 +1573,12 @@ static int pd_system_suspend(unsigned in
 {
     struct fwk_event req;
     struct pd_system_suspend_request *req_params =
-        (struct pd_system_suspend_request *)(&req.params);
+	(struct pd_system_suspend_request *)(&req.params);
 
     req = (struct fwk_event){
-        .id = FWK_ID_EVENT(
-            FWK_MODULE_IDX_POWER_DOMAIN, PD_EVENT_IDX_SYSTEM_SUSPEND),
-        .target_id = fwk_module_id_power_domain,
+	.id = FWK_ID_EVENT(
+	    FWK_MODULE_IDX_POWER_DOMAIN, PD_EVENT_IDX_SYSTEM_SUSPEND),
+	.target_id = fwk_module_id_power_domain,
     };
 
     req_params->state = state;
@@ -1591,19 +1591,19 @@ static int pd_system_shutdown(enum mod_p
     int status;
     struct fwk_event req;
     struct pd_system_shutdown_request *req_params =
-        (struct pd_system_shutdown_request *)(&req.params);
+	(struct pd_system_shutdown_request *)(&req.params);
 
     req = (struct fwk_event) {
-        .id = FWK_ID_EVENT(FWK_MODULE_IDX_POWER_DOMAIN,
-                           PD_EVENT_IDX_SYSTEM_SHUTDOWN),
-        .target_id = fwk_module_id_power_domain,
+	.id = FWK_ID_EVENT(FWK_MODULE_IDX_POWER_DOMAIN,
+			   PD_EVENT_IDX_SYSTEM_SHUTDOWN),
+	.target_id = fwk_module_id_power_domain,
     };
 
     req_params->system_shutdown = system_shutdown;
 
     status = fwk_put_event(&req);
     if (status == FWK_SUCCESS) {
-        return FWK_PENDING;
+	return FWK_PENDING;
     }
 
     return status;
@@ -1619,10 +1619,10 @@ static int pd_reset(fwk_id_t pd_id, bool
     pd = &mod_pd_ctx.pd_ctx_table[fwk_id_get_element_idx(pd_id)];
 
     req = (struct fwk_event_light){
-        .id = FWK_ID_EVENT(FWK_MODULE_IDX_POWER_DOMAIN, PD_EVENT_IDX_RESET),
-        .source_id = pd->driver_id,
-        .target_id = pd_id,
-        .response_requested = response_requested,
+	.id = FWK_ID_EVENT(FWK_MODULE_IDX_POWER_DOMAIN, PD_EVENT_IDX_RESET),
+	.source_id = pd->driver_id,
+	.target_id = pd_id,
+	.response_requested = response_requested,
     };
 
     return fwk_put_event(&req);
@@ -1633,13 +1633,13 @@ static int report_power_state_transition
 {
     struct fwk_event report;
     struct pd_power_state_transition_report *report_params =
-        (struct pd_power_state_transition_report *)(&report.params);
+	(struct pd_power_state_transition_report *)(&report.params);
 
     report = (struct fwk_event){
-        .source_id = pd->driver_id,
-        .target_id = pd->id,
-        .id = FWK_ID_EVENT(FWK_MODULE_IDX_POWER_DOMAIN,
-                           PD_EVENT_IDX_REPORT_POWER_STATE_TRANSITION)
+	.source_id = pd->driver_id,
+	.target_id = pd->id,
+	.id = FWK_ID_EVENT(FWK_MODULE_IDX_POWER_DOMAIN,
+			   PD_EVENT_IDX_REPORT_POWER_STATE_TRANSITION)
     };
     report_params->state = state;
 
@@ -1649,7 +1649,7 @@ static int report_power_state_transition
 static int pd_report_power_state_transition(fwk_id_t pd_id, unsigned int state)
 {
     const struct pd_ctx *pd =
-                        &mod_pd_ctx.pd_ctx_table[fwk_id_get_element_idx(pd_id)];
+			&mod_pd_ctx.pd_ctx_table[fwk_id_get_element_idx(pd_id)];
 
     return report_power_state_transition(pd, state);
 }
@@ -1658,11 +1658,11 @@ static int pd_get_last_core_pd_id(fwk_id
 {
     bool system_suspend_ongoing = mod_pd_ctx.system_suspend.suspend_ongoing;
     if (last_core_pd_id == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     if (!system_suspend_ongoing) {
-        return FWK_E_PWRSTATE;
+	return FWK_E_PWRSTATE;
     }
 
     *last_core_pd_id = mod_pd_ctx.system_suspend.last_core_pd->id;
@@ -1699,17 +1699,17 @@ static const struct mod_pd_driver_input_
  * Framework handlers
  */
 static int pd_init(fwk_id_t module_id, unsigned int dev_count,
-                   const void *data)
+		   const void *data)
 {
     if ((data == NULL) || (dev_count == 0)) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     mod_pd_ctx.config = (struct mod_power_domain_config *)data;
 
     if ((mod_pd_ctx.config->authorized_id_table == NULL) &&
-        (mod_pd_ctx.config->authorized_id_table_size != 0)) {
-        return FWK_E_PARAM;
+	(mod_pd_ctx.config->authorized_id_table_size != 0)) {
+	return FWK_E_PARAM;
     }
 
     mod_pd_ctx.pd_ctx_table = fwk_mm_calloc(dev_count, sizeof(struct pd_ctx));
@@ -1721,10 +1721,10 @@ static int pd_init(fwk_id_t module_id, u
 }
 
 static int pd_power_domain_init(fwk_id_t pd_id, unsigned int unused,
-                                const void *config)
+				const void *config)
 {
     const struct mod_power_domain_element_config *pd_config =
-        (const struct mod_power_domain_element_config *)config;
+	(const struct mod_power_domain_element_config *)config;
     struct pd_ctx *pd;
     unsigned int state;
 
@@ -1733,41 +1733,41 @@ static int pd_power_domain_init(fwk_id_t
     fwk_list_init(&pd->children_list);
 
     if (pd_config->attributes.pd_type >= MOD_PD_TYPE_COUNT) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     if ((pd_config->allowed_state_mask_table == NULL) ||
-        (pd_config->allowed_state_mask_table_size == 0)) {
-        return FWK_E_PARAM;
+	(pd_config->allowed_state_mask_table_size == 0)) {
+	return FWK_E_PARAM;
     }
 
     pd->allowed_state_mask_table = pd_config->allowed_state_mask_table;
     pd->allowed_state_mask_table_size =
-        pd_config->allowed_state_mask_table_size;
+	pd_config->allowed_state_mask_table_size;
 
     for (state = 0; state < pd->allowed_state_mask_table_size; state++) {
-        pd->valid_state_mask |= pd->allowed_state_mask_table[state];
+	pd->valid_state_mask |= pd->allowed_state_mask_table[state];
     }
 
     if ((pd_config->composite_state_mask_table != NULL) &&
-        (pd_config->composite_state_mask_table_size > 0)) {
-        pd->composite_state_mask_table = pd_config->composite_state_mask_table;
-        pd->composite_state_mask_table_size =
-            pd_config->composite_state_mask_table_size;
-        pd->composite_state_levels_mask =
-            pd_config->composite_state_levels_mask;
-        pd->cs_support = true;
+	(pd_config->composite_state_mask_table_size > 0)) {
+	pd->composite_state_mask_table = pd_config->composite_state_mask_table;
+	pd->composite_state_mask_table_size =
+	    pd_config->composite_state_mask_table_size;
+	pd->composite_state_levels_mask =
+	    pd_config->composite_state_levels_mask;
+	pd->cs_support = true;
 
     } else if (pd_config->attributes.pd_type == MOD_PD_TYPE_CORE) {
-        pd->composite_state_mask_table = core_composite_state_mask_table;
-        pd->composite_state_mask_table_size =
-            FWK_ARRAY_SIZE(core_composite_state_mask_table);
-        pd->composite_state_levels_mask = MOD_PD_CS_STATE_MASK
-            << MOD_PD_CS_LEVEL_SHIFT;
-        pd->cs_support = true;
+	pd->composite_state_mask_table = core_composite_state_mask_table;
+	pd->composite_state_mask_table_size =
+	    FWK_ARRAY_SIZE(core_composite_state_mask_table);
+	pd->composite_state_levels_mask = MOD_PD_CS_STATE_MASK
+	    << MOD_PD_CS_LEVEL_SHIFT;
+	pd->cs_support = true;
 
     } else {
-        pd->cs_support = false;
+	pd->cs_support = false;
     }
 
     pd->id = pd_id;
@@ -1782,7 +1782,7 @@ static int pd_post_init(fwk_id_t module_
 
     status = connect_pd_tree();
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     return FWK_SUCCESS;
@@ -1797,11 +1797,11 @@ static int pd_bind(fwk_id_t id, unsigned
 
     /* Nothing to do but during the first round of calls */
     if (round != 0) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     if (fwk_id_is_type(id, FWK_ID_TYPE_MODULE)) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     pd = &mod_pd_ctx.pd_ctx_table[fwk_id_get_element_idx(id)];
@@ -1809,15 +1809,15 @@ static int pd_bind(fwk_id_t id, unsigned
 
     status = fwk_module_bind(config->driver_id, config->api_id, &driver_api);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     pd->driver_id = config->driver_id;
     if ((driver_api->set_state == NULL) || (driver_api->get_state == NULL) ||
-        (driver_api->reset == NULL) ||
-        ((config->attributes.pd_type == MOD_PD_TYPE_CORE) &&
-         (driver_api->prepare_core_for_system_suspend == NULL))) {
-        return FWK_E_PARAM;
+	(driver_api->reset == NULL) ||
+	((config->attributes.pd_type == MOD_PD_TYPE_CORE) &&
+	 (driver_api->prepare_core_for_system_suspend == NULL))) {
+	return FWK_E_PARAM;
     }
 
     pd->driver_api = driver_api;
@@ -1834,158 +1834,158 @@ static int pd_start(fwk_id_t id)
 
     /* Nothing to do for elements */
     if (fwk_module_is_valid_element_id(id)) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     for (index = (int)(mod_pd_ctx.pd_count - 1); index >= 0; index--) {
-        pd = &mod_pd_ctx.pd_ctx_table[index];
-        pd->requested_state = (unsigned int)MOD_PD_STATE_OFF;
-        pd->state_requested_to_driver = (unsigned int)MOD_PD_STATE_OFF;
-        pd->current_state = (unsigned int)MOD_PD_STATE_OFF;
-
-        /*
-         * If the power domain parent is powered down, don't call the driver
-         * to get the power domain state as the power domain registers may
-         * not be accessible. That way, the drivers don't have to care about
-         * this case.
-         */
-        if ((pd->parent != NULL) &&
-            (pd->parent->requested_state == MOD_PD_STATE_OFF)) {
-            continue;
-        }
-
-        /* Get the current power state of the power domain from its driver. */
-        status = pd->driver_api->get_state(pd->driver_id, &state);
-        if (status != FWK_SUCCESS) {
+	pd = &mod_pd_ctx.pd_ctx_table[index];
+	pd->requested_state = (unsigned int)MOD_PD_STATE_OFF;
+	pd->state_requested_to_driver = (unsigned int)MOD_PD_STATE_OFF;
+	pd->current_state = (unsigned int)MOD_PD_STATE_OFF;
+
+	/*
+	 * If the power domain parent is powered down, don't call the driver
+	 * to get the power domain state as the power domain registers may
+	 * not be accessible. That way, the drivers don't have to care about
+	 * this case.
+	 */
+	if ((pd->parent != NULL) &&
+	    (pd->parent->requested_state == MOD_PD_STATE_OFF)) {
+	    continue;
+	}
+
+	/* Get the current power state of the power domain from its driver. */
+	status = pd->driver_api->get_state(pd->driver_id, &state);
+	if (status != FWK_SUCCESS) {
 #if FWK_LOG_LEVEL <= FWK_LOG_LEVEL_ERROR
-            FWK_LOG_ERR(
-                driver_error_msg,
-                fwk_status_str(status),
-                status,
-                __func__,
-                __LINE__);
+	    FWK_LOG_ERR(
+		driver_error_msg,
+		fwk_status_str(status),
+		status,
+		__func__,
+		__LINE__);
 #endif
-        } else {
-            pd->requested_state = pd->state_requested_to_driver = state;
+	} else {
+	    pd->requested_state = pd->state_requested_to_driver = state;
 
-            if (state == MOD_PD_STATE_OFF) {
-                continue;
-            }
-
-            status = report_power_state_transition(pd, state);
-            if (status != FWK_SUCCESS) {
-                FWK_LOG_DEBUG("[PD] %s @%d", __func__, __LINE__);
-            }
-        }
+	    if (state == MOD_PD_STATE_OFF) {
+		continue;
+	    }
+
+	    status = report_power_state_transition(pd, state);
+	    if (status != FWK_SUCCESS) {
+		FWK_LOG_DEBUG("[PD] %s @%d", __func__, __LINE__);
+	    }
+	}
     }
 
     return FWK_SUCCESS;
 }
 
 static int pd_process_bind_request(fwk_id_t source_id, fwk_id_t target_id,
-                                   fwk_id_t api_id, const void **api)
+				   fwk_id_t api_id, const void **api)
 {
     struct pd_ctx *pd;
     unsigned int id_idx;
 
     enum mod_pd_api_idx api_id_type =
-        (enum mod_pd_api_idx)fwk_id_get_api_idx(api_id);
+	(enum mod_pd_api_idx)fwk_id_get_api_idx(api_id);
 
     switch (api_id_type) {
     case MOD_PD_API_IDX_PUBLIC:
-        if (!fwk_id_is_type(target_id, FWK_ID_TYPE_MODULE)) {
-            return FWK_E_ACCESS;
-        }
-        *api = &pd_public_api;
-        break;
+	if (!fwk_id_is_type(target_id, FWK_ID_TYPE_MODULE)) {
+	    return FWK_E_ACCESS;
+	}
+	*api = &pd_public_api;
+	break;
 
     case MOD_PD_API_IDX_RESTRICTED:
-        if (!fwk_id_is_type(target_id, FWK_ID_TYPE_MODULE)) {
-            return FWK_E_ACCESS;
-        }
-        if (mod_pd_ctx.config->authorized_id_table_size == 0) {
-            *api = &pd_restricted_api;
-            return FWK_SUCCESS;
-        }
-        for (id_idx = 0;
-             id_idx < mod_pd_ctx.config->authorized_id_table_size;
-             id_idx++) {
-
-            if (fwk_id_is_equal(source_id,
-                mod_pd_ctx.config->authorized_id_table[id_idx])) {
-                *api = &pd_restricted_api;
-                return FWK_SUCCESS;
-            }
-        }
-        return FWK_E_ACCESS;
+	if (!fwk_id_is_type(target_id, FWK_ID_TYPE_MODULE)) {
+	    return FWK_E_ACCESS;
+	}
+	if (mod_pd_ctx.config->authorized_id_table_size == 0) {
+	    *api = &pd_restricted_api;
+	    return FWK_SUCCESS;
+	}
+	for (id_idx = 0;
+	     id_idx < mod_pd_ctx.config->authorized_id_table_size;
+	     id_idx++) {
+
+	    if (fwk_id_is_equal(source_id,
+		mod_pd_ctx.config->authorized_id_table[id_idx])) {
+		*api = &pd_restricted_api;
+		return FWK_SUCCESS;
+	    }
+	}
+	return FWK_E_ACCESS;
 
     case MOD_PD_API_IDX_DRIVER_INPUT:
-        if (!fwk_id_is_type(target_id, FWK_ID_TYPE_ELEMENT)) {
-            return FWK_E_ACCESS;
-        }
-        pd = &mod_pd_ctx.pd_ctx_table[fwk_id_get_element_idx(target_id)];
-        if (!fwk_id_is_equal(source_id, pd->driver_id)) {
-            return FWK_E_ACCESS;
-        }
-        *api = &pd_driver_input_api;
-        break;
+	if (!fwk_id_is_type(target_id, FWK_ID_TYPE_ELEMENT)) {
+	    return FWK_E_ACCESS;
+	}
+	pd = &mod_pd_ctx.pd_ctx_table[fwk_id_get_element_idx(target_id)];
+	if (!fwk_id_is_equal(source_id, pd->driver_id)) {
+	    return FWK_E_ACCESS;
+	}
+	*api = &pd_driver_input_api;
+	break;
 
     default:
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     return FWK_SUCCESS;
 }
 
 static int pd_process_event(const struct fwk_event *event,
-                            struct fwk_event *resp)
+			    struct fwk_event *resp)
 {
     struct pd_ctx *pd = NULL;
 
     if (fwk_id_is_type(event->target_id, FWK_ID_TYPE_ELEMENT)) {
-        pd = &mod_pd_ctx.pd_ctx_table[fwk_id_get_element_idx(event->target_id)];
+	pd = &mod_pd_ctx.pd_ctx_table[fwk_id_get_element_idx(event->target_id)];
     }
 
     switch (fwk_id_get_event_idx(event->id)) {
     case (unsigned int)MOD_PD_PUBLIC_EVENT_IDX_SET_STATE:
-        fwk_assert(pd != NULL);
+	fwk_assert(pd != NULL);
 
-        process_set_state_request(pd, event, resp);
+	process_set_state_request(pd, event, resp);
 
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 
     case (unsigned int)PD_EVENT_IDX_RESET:
-        fwk_assert(pd != NULL);
+	fwk_assert(pd != NULL);
 
-        process_reset_request(pd, (struct pd_response *)resp->params);
+	process_reset_request(pd, (struct pd_response *)resp->params);
 
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 
     case (unsigned int)PD_EVENT_IDX_REPORT_POWER_STATE_TRANSITION:
-        fwk_assert(pd != NULL);
+	fwk_assert(pd != NULL);
 
-        process_power_state_transition_report(pd,
-            (struct pd_power_state_transition_report *)event->params);
+	process_power_state_transition_report(pd,
+	    (struct pd_power_state_transition_report *)event->params);
 
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 
     case (unsigned int)PD_EVENT_IDX_SYSTEM_SUSPEND:
-        process_system_suspend_request(
-            (struct pd_system_suspend_request *)event->params,
-            (struct pd_response *)resp->params);
+	process_system_suspend_request(
+	    (struct pd_system_suspend_request *)event->params,
+	    (struct pd_response *)resp->params);
 
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 
     case (unsigned int)PD_EVENT_IDX_SYSTEM_SHUTDOWN:
-        process_system_shutdown_request(event, resp);
+	process_system_shutdown_request(event, resp);
 
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 
     default:
-        FWK_LOG_ERR(
-            "[PD] Invalid power state request: %s.", FWK_ID_STR(event->id));
+	FWK_LOG_ERR(
+	    "[PD] Invalid power state request: %s.", FWK_ID_STR(event->id));
 
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 }
 
@@ -1993,15 +1993,15 @@ static int pd_process_event(const struct
 static int process_pre_shutdown_notification_response(void)
 {
     if (mod_pd_ctx.system_shutdown.ongoing) {
-        mod_pd_ctx.system_shutdown.notifications_count--;
+	mod_pd_ctx.system_shutdown.notifications_count--;
 
-        if (mod_pd_ctx.system_shutdown.notifications_count == 0) {
-            /* All notifications for system shutdown have been received */
-            perform_shutdown(mod_pd_ctx.system_shutdown.system_shutdown, NULL);
-        }
-        return FWK_SUCCESS;
+	if (mod_pd_ctx.system_shutdown.notifications_count == 0) {
+	    /* All notifications for system shutdown have been received */
+	    perform_shutdown(mod_pd_ctx.system_shutdown.system_shutdown, NULL);
+	}
+	return FWK_SUCCESS;
     } else {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 }
 
@@ -2010,47 +2010,47 @@ static int process_power_state_pre_trans
     struct mod_pd_power_state_pre_transition_notification_resp_params *params)
 {
     if (pd->power_state_pre_transition_notification_ctx.pending_responses
-        == 0) {
-        fwk_unexpected();
-        return FWK_E_PANIC;
+	== 0) {
+	fwk_unexpected();
+	return FWK_E_PANIC;
     }
 
     if (params->status != FWK_SUCCESS) {
-        pd->power_state_pre_transition_notification_ctx.response_status =
-            FWK_E_DEVICE;
+	pd->power_state_pre_transition_notification_ctx.response_status =
+	    FWK_E_DEVICE;
     }
 
     pd->power_state_pre_transition_notification_ctx.pending_responses--;
     if (pd->power_state_pre_transition_notification_ctx.pending_responses !=
-        0) {
-        return FWK_SUCCESS;
+	0) {
+	return FWK_SUCCESS;
     }
 
     if (pd->power_state_pre_transition_notification_ctx.valid == true) {
-        /*
-         * All the notification responses have been received, the requested
-         * state for the power domain has not changed in the
-         * meantime and all the notified entities agreed on the power state
-         * transition, proceed with it.
-         */
-        if (pd->power_state_pre_transition_notification_ctx.response_status ==
-            FWK_SUCCESS) {
-            return initiate_power_state_transition(pd);
-        }
+	/*
+	 * All the notification responses have been received, the requested
+	 * state for the power domain has not changed in the
+	 * meantime and all the notified entities agreed on the power state
+	 * transition, proceed with it.
+	 */
+	if (pd->power_state_pre_transition_notification_ctx.response_status ==
+	    FWK_SUCCESS) {
+	    return initiate_power_state_transition(pd);
+	}
     } else {
-        /*
-         * All the notification responses have been received but the
-         * requested state for the power domain has changed, start the
-         * processings for the new requested state.
-         */
-        if ((pd->requested_state == pd->state_requested_to_driver) ||
-            (!is_allowed_by_parent_and_children(pd, pd->requested_state))) {
-            return FWK_SUCCESS;
-        }
-
-        if (!initiate_power_state_pre_transition_notification(pd)) {
-            return initiate_power_state_transition(pd);
-        }
+	/*
+	 * All the notification responses have been received but the
+	 * requested state for the power domain has changed, start the
+	 * processings for the new requested state.
+	 */
+	if ((pd->requested_state == pd->state_requested_to_driver) ||
+	    (!is_allowed_by_parent_and_children(pd, pd->requested_state))) {
+	    return FWK_SUCCESS;
+	}
+
+	if (!initiate_power_state_pre_transition_notification(pd)) {
+	    return initiate_power_state_transition(pd);
+	}
     }
 
     return FWK_SUCCESS;
@@ -2063,34 +2063,34 @@ static int process_power_state_transitio
     struct mod_pd_power_state_transition_notification_params *params;
 
     if (pd->power_state_transition_notification_ctx.pending_responses == 0) {
-        fwk_unexpected();
-        return FWK_E_PANIC;
+	fwk_unexpected();
+	return FWK_E_PANIC;
     }
 
     pd->power_state_transition_notification_ctx.pending_responses--;
     if (pd->power_state_transition_notification_ctx.pending_responses != 0) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     if (pd->power_state_transition_notification_ctx.state ==
-        pd->current_state) {
-        /* All notifications received, complete the transition report */
+	pd->current_state) {
+	/* All notifications received, complete the transition report */
 
-        unsigned int previous_state =
-            pd->power_state_transition_notification_ctx.previous_state;
+	unsigned int previous_state =
+	    pd->power_state_transition_notification_ctx.previous_state;
 
-        pd->power_state_transition_notification_ctx.previous_state =
-            pd->current_state;
-        /*
-         * Complete the report state change now that we have all notifications
-         */
-        if (is_deeper_state(pd->current_state, previous_state)) {
-            process_power_state_transition_report_deeper_state(pd);
-        } else if (is_shallower_state(pd->current_state, previous_state)) {
-            process_power_state_transition_report_shallower_state(pd);
-        }
+	pd->power_state_transition_notification_ctx.previous_state =
+	    pd->current_state;
+	/*
+	 * Complete the report state change now that we have all notifications
+	 */
+	if (is_deeper_state(pd->current_state, previous_state)) {
+	    process_power_state_transition_report_deeper_state(pd);
+	} else if (is_shallower_state(pd->current_state, previous_state)) {
+	    process_power_state_transition_report_shallower_state(pd);
+	}
 
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     /*
@@ -2102,45 +2102,45 @@ static int process_power_state_transitio
     notification_event.source_id = FWK_ID_NONE;
 
     params = (struct mod_pd_power_state_transition_notification_params *)
-        notification_event.params;
+	notification_event.params;
     params->state = pd->current_state;
 
     pd->power_state_transition_notification_ctx.state = pd->current_state;
     return fwk_notification_notify(
-        &notification_event,
-        &pd->power_state_transition_notification_ctx.pending_responses);
+	&notification_event,
+	&pd->power_state_transition_notification_ctx.pending_responses);
 }
 
 static int pd_process_notification(const struct fwk_event *event,
-                                   struct fwk_event *resp)
+				   struct fwk_event *resp)
 {
     struct pd_ctx *pd;
 
     /* Only responses are expected. */
     if (!event->is_response) {
-        fwk_unexpected();
-        return FWK_E_SUPPORT;
+	fwk_unexpected();
+	return FWK_E_SUPPORT;
     }
 
     if (fwk_id_is_equal(event->id, mod_pd_notification_id_pre_shutdown)) {
-        return process_pre_shutdown_notification_response();
+	return process_pre_shutdown_notification_response();
     }
 
     if (!fwk_module_is_valid_element_id(event->target_id)) {
-        fwk_unexpected();
-        return FWK_E_PARAM;
+	fwk_unexpected();
+	return FWK_E_PARAM;
     }
 
     pd = &mod_pd_ctx.pd_ctx_table[fwk_id_get_element_idx(event->target_id)];
 
     if (fwk_id_is_equal(
-            event->id, mod_pd_notification_id_power_state_transition)) {
-        return process_power_state_transition_notification_response(pd);
+	    event->id, mod_pd_notification_id_power_state_transition)) {
+	return process_power_state_transition_notification_response(pd);
     }
 
     return process_power_state_pre_transition_notification_response(pd,
-        (struct mod_pd_power_state_pre_transition_notification_resp_params *)
-        event->params);
+	(struct mod_pd_power_state_pre_transition_notification_resp_params *)
+	event->params);
 }
 #endif /* BUILD_HAS_NOTIFICATION */
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/power_domain/src/power_domain_utils.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/power_domain/src/power_domain_utils.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/power_domain/src/power_domain_utils.c
@@ -45,52 +45,52 @@ static int create_core_cluster_pd_elemen
     int status;
 
     pd_config_table = fwk_mm_calloc(
-        core_count + cluster_count,
-        sizeof(struct mod_power_domain_element_config));
+	core_count + cluster_count,
+	sizeof(struct mod_power_domain_element_config));
     if (pd_config_table == NULL) {
-        return FWK_E_NOMEM;
+	return FWK_E_NOMEM;
     }
 
     for (cluster_idx = 0; cluster_idx < cluster_count; cluster_idx++) {
-        for (core_idx = 0; core_idx < cores_per_clusters; core_idx++) {
-            element = &element_table[core_element_counter];
-            pd_config = &pd_config_table[core_element_counter];
-            element->name = fwk_mm_alloc(PD_NAME_SIZE, 1);
-            status = snprintf(
-                (char *)element->name,
-                PD_NAME_SIZE,
-                "CLUS%uCORE%u",
-                cluster_idx,
-                core_idx);
-            if (status < 0) {
-                return FWK_E_PANIC;
-            }
-            element->data = pd_config;
-            pd_config->attributes.pd_type = MOD_PD_TYPE_CORE;
-            pd_config->parent_idx = cluster_idx + core_count;
-            pd_config->driver_id =
-                FWK_ID_ELEMENT(driver_idx, core_element_counter);
-            pd_config->api_id = FWK_ID_API(driver_idx, api_idx);
-            pd_config->allowed_state_mask_table = core_state_table;
-            pd_config->allowed_state_mask_table_size = core_state_table_size;
-            core_element_counter++;
-        }
-        /* Define the cluster configuration */
-        element = &element_table[cluster_idx + core_count];
-        pd_config = &pd_config_table[cluster_idx + core_count];
-        element->name = fwk_mm_alloc(PD_NAME_SIZE, 1);
-        status = snprintf(
-            (char *)element->name, PD_NAME_SIZE, "CLUS%u", cluster_idx);
-        if (status < 0) {
-            return FWK_E_PANIC;
-        }
-        element->data = pd_config;
-        pd_config->attributes.pd_type = MOD_PD_TYPE_CLUSTER;
-        pd_config->driver_id =
-            FWK_ID_ELEMENT(driver_idx, cluster_idx + core_count);
-        pd_config->api_id = FWK_ID_API(driver_idx, api_idx);
-        pd_config->allowed_state_mask_table = cluster_state_table;
-        pd_config->allowed_state_mask_table_size = cluster_state_table_size;
+	for (core_idx = 0; core_idx < cores_per_clusters; core_idx++) {
+	    element = &element_table[core_element_counter];
+	    pd_config = &pd_config_table[core_element_counter];
+	    element->name = fwk_mm_alloc(PD_NAME_SIZE, 1);
+	    status = snprintf(
+		(char *)element->name,
+		PD_NAME_SIZE,
+		"CLUS%uCORE%u",
+		cluster_idx,
+		core_idx);
+	    if (status < 0) {
+		return FWK_E_PANIC;
+	    }
+	    element->data = pd_config;
+	    pd_config->attributes.pd_type = MOD_PD_TYPE_CORE;
+	    pd_config->parent_idx = cluster_idx + core_count;
+	    pd_config->driver_id =
+		FWK_ID_ELEMENT(driver_idx, core_element_counter);
+	    pd_config->api_id = FWK_ID_API(driver_idx, api_idx);
+	    pd_config->allowed_state_mask_table = core_state_table;
+	    pd_config->allowed_state_mask_table_size = core_state_table_size;
+	    core_element_counter++;
+	}
+	/* Define the cluster configuration */
+	element = &element_table[cluster_idx + core_count];
+	pd_config = &pd_config_table[cluster_idx + core_count];
+	element->name = fwk_mm_alloc(PD_NAME_SIZE, 1);
+	status = snprintf(
+	    (char *)element->name, PD_NAME_SIZE, "CLUS%u", cluster_idx);
+	if (status < 0) {
+	    return FWK_E_PANIC;
+	}
+	element->data = pd_config;
+	pd_config->attributes.pd_type = MOD_PD_TYPE_CLUSTER;
+	pd_config->driver_id =
+	    FWK_ID_ELEMENT(driver_idx, cluster_idx + core_count);
+	pd_config->api_id = FWK_ID_API(driver_idx, api_idx);
+	pd_config->allowed_state_mask_table = cluster_state_table;
+	pd_config->allowed_state_mask_table_size = cluster_state_table_size;
     }
 
     return FWK_SUCCESS;
@@ -114,48 +114,48 @@ const struct fwk_element *create_power_d
     int status;
 
     if ((core_count % cluster_count != 0) || (core_count > MAX_CORES_COUNT) ||
-        (static_table_size > MAX_STATIC_ELEMENTS_COUNT)) {
-        return NULL;
+	(static_table_size > MAX_STATIC_ELEMENTS_COUNT)) {
+	return NULL;
     }
 
     element_table = fwk_mm_calloc(
-        core_count + cluster_count + static_table_size + 1, /* Terminator */
-        sizeof(struct fwk_element));
+	core_count + cluster_count + static_table_size + 1, /* Terminator */
+	sizeof(struct fwk_element));
 
     if (element_table == NULL) {
-        return element_table;
+	return element_table;
     }
 
     status = create_core_cluster_pd_element_table(
-        element_table,
-        core_count,
-        cluster_count,
-        driver_idx,
-        api_idx,
-        core_state_table,
-        core_state_table_size,
-        cluster_state_table,
-        cluster_state_table_size);
+	element_table,
+	core_count,
+	cluster_count,
+	driver_idx,
+	api_idx,
+	core_state_table,
+	core_state_table_size,
+	cluster_state_table,
+	cluster_state_table_size);
 
     if (status != FWK_SUCCESS) {
-        return NULL;
+	return NULL;
     }
 
     fwk_str_memcpy(
-        element_table + (core_count + cluster_count),
-        static_table,
-        static_table_size * sizeof(struct fwk_element));
+	element_table + (core_count + cluster_count),
+	static_table,
+	static_table_size * sizeof(struct fwk_element));
 
     /* Calculate SYSTOP index */
     systop_idx =
-        (unsigned int)(core_count + cluster_count + static_table_size - 1);
+	(unsigned int)(core_count + cluster_count + static_table_size - 1);
 
     /* Set Power Domain Parent for all SYSTOP children */
     for (element_idx = core_count; element_idx < systop_idx; element_idx++) {
-        pd_config =
-            (struct mod_power_domain_element_config *)element_table[element_idx]
-                .data;
-        pd_config->parent_idx = systop_idx;
+	pd_config =
+	    (struct mod_power_domain_element_config *)element_table[element_idx]
+		.data;
+	pd_config->parent_idx = systop_idx;
     }
 
     return element_table;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/ppu_v0/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/ppu_v0/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/ppu_v0/CMakeLists.txt
@@ -8,11 +8,11 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(
     ${SCP_MODULE_TARGET} PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_ppu_v0.c"
-                                 "${CMAKE_CURRENT_SOURCE_DIR}/src/ppu_v0.c")
+				 "${CMAKE_CURRENT_SOURCE_DIR}/src/ppu_v0.c")
 
 target_link_libraries(
     module-ppu-v0
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/ppu_v0/src/mod_ppu_v0.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/ppu_v0/src/mod_ppu_v0.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/ppu_v0/src/mod_ppu_v0.c
@@ -82,15 +82,15 @@ static int get_state(struct ppu_v0_reg *
 
     /* Ensure ppu_mode_to_power_state has an entry for each PPU state */
     static_assert(FWK_ARRAY_SIZE(ppu_mode_to_power_state) == PPU_V0_MODE_COUNT,
-                  "[MOD_PPU_V0] ppu_mode_to_power_state size error");
+		  "[MOD_PPU_V0] ppu_mode_to_power_state size error");
 
     ppu_v0_get_power_mode(ppu, &ppu_mode);
     fwk_assert(ppu_mode < PPU_V0_MODE_COUNT);
 
     *state = ppu_mode_to_power_state[ppu_mode];
     if (*state == MODE_UNSUPPORTED) {
-        FWK_LOG_ERR("[PD] Unexpected PPU mode (%i).", ppu_mode);
-        return FWK_E_DEVICE;
+	FWK_LOG_ERR("[PD] Unexpected PPU mode (%i).", ppu_mode);
+	return FWK_E_DEVICE;
     }
 
     return FWK_SUCCESS;
@@ -114,22 +114,22 @@ static int pd_set_state(fwk_id_t pd_id,
 
     switch (state) {
     case MOD_PD_STATE_ON:
-        ppu_v0_set_power_mode(pd_ctx->ppu, PPU_V0_MODE_ON, pd_ctx->timer_ctx);
-        status = pd_ctx->pd_driver_input_api->report_power_state_transition(
-            pd_ctx->bound_id, MOD_PD_STATE_ON);
-        fwk_assert(status == FWK_SUCCESS);
-        break;
+	ppu_v0_set_power_mode(pd_ctx->ppu, PPU_V0_MODE_ON, pd_ctx->timer_ctx);
+	status = pd_ctx->pd_driver_input_api->report_power_state_transition(
+	    pd_ctx->bound_id, MOD_PD_STATE_ON);
+	fwk_assert(status == FWK_SUCCESS);
+	break;
 
     case MOD_PD_STATE_OFF:
-        ppu_v0_set_power_mode(pd_ctx->ppu, PPU_V0_MODE_OFF, pd_ctx->timer_ctx);
-        status = pd_ctx->pd_driver_input_api->report_power_state_transition(
-            pd_ctx->bound_id, MOD_PD_STATE_OFF);
-        fwk_assert(status == FWK_SUCCESS);
-        break;
+	ppu_v0_set_power_mode(pd_ctx->ppu, PPU_V0_MODE_OFF, pd_ctx->timer_ctx);
+	status = pd_ctx->pd_driver_input_api->report_power_state_transition(
+	    pd_ctx->bound_id, MOD_PD_STATE_OFF);
+	fwk_assert(status == FWK_SUCCESS);
+	break;
 
     default:
-        FWK_LOG_ERR("[PD] Requested power state (%i) is not supported.", state);
-        return FWK_E_PARAM;
+	FWK_LOG_ERR("[PD] Requested power state (%i) is not supported.", state);
+	return FWK_E_PARAM;
     }
 
     return status;
@@ -153,10 +153,10 @@ static int pd_reset(fwk_id_t pd_id)
 
     /* Model does not support warm reset at the moment. Using OFF instead. */
     status =
-        ppu_v0_set_power_mode(pd_ctx->ppu, PPU_V0_MODE_OFF, pd_ctx->timer_ctx);
+	ppu_v0_set_power_mode(pd_ctx->ppu, PPU_V0_MODE_OFF, pd_ctx->timer_ctx);
     if (status == FWK_SUCCESS)
-        status = ppu_v0_set_power_mode(
-            pd_ctx->ppu, PPU_V0_MODE_ON, pd_ctx->timer_ctx);
+	status = ppu_v0_set_power_mode(
+	    pd_ctx->ppu, PPU_V0_MODE_ON, pd_ctx->timer_ctx);
 
     return status;
 }
@@ -172,10 +172,10 @@ static const struct mod_pd_driver_api pd
  */
 
 static int ppu_v0_mod_init(fwk_id_t module_id, unsigned int pd_count,
-                           const void *unused)
+			   const void *unused)
 {
     ppu_v0_ctx.pd_ctx_table = fwk_mm_calloc(pd_count,
-                                            sizeof(struct ppu_v0_pd_ctx));
+					    sizeof(struct ppu_v0_pd_ctx));
 
     return FWK_SUCCESS;
 }
@@ -187,7 +187,7 @@ static int ppu_v0_pd_init(fwk_id_t pd_id
     int status;
 
     if (config->pd_type >= MOD_PD_TYPE_COUNT)
-        return FWK_E_DATA;
+	return FWK_E_DATA;
 
     pd_ctx = ppu_v0_ctx.pd_ctx_table + fwk_id_get_element_idx(pd_id);
     pd_ctx->config = config;
@@ -196,18 +196,18 @@ static int ppu_v0_pd_init(fwk_id_t pd_id
 
 #ifdef BUILD_HAS_MOD_TIMER
     if (config->timer_config == NULL) {
-        pd_ctx->timer_ctx = NULL;
+	pd_ctx->timer_ctx = NULL;
     } else {
-        pd_ctx->timer_ctx = fwk_mm_calloc(1, sizeof(struct ppu_v0_timer_ctx));
-        if (pd_ctx->timer_ctx == NULL)
-            return FWK_E_NOMEM;
-        /* Check for valid timeout value if timer ID is specified */
-        if (config->timer_config->set_state_timeout_us == 0)
-            return FWK_E_PARAM;
-        /* Save the timer ID to pd context */
-        pd_ctx->timer_ctx->timer_id = config->timer_config->timer_id;
-        pd_ctx->timer_ctx->delay_us =
-            config->timer_config->set_state_timeout_us;
+	pd_ctx->timer_ctx = fwk_mm_calloc(1, sizeof(struct ppu_v0_timer_ctx));
+	if (pd_ctx->timer_ctx == NULL)
+	    return FWK_E_NOMEM;
+	/* Check for valid timeout value if timer ID is specified */
+	if (config->timer_config->set_state_timeout_us == 0)
+	    return FWK_E_PARAM;
+	/* Save the timer ID to pd context */
+	pd_ctx->timer_ctx->timer_id = config->timer_config->timer_id;
+	pd_ctx->timer_ctx->delay_us =
+	    config->timer_config->set_state_timeout_us;
     }
 #else
     pd_ctx->timer_ctx = NULL;
@@ -216,16 +216,16 @@ static int ppu_v0_pd_init(fwk_id_t pd_id
     case MOD_PD_TYPE_DEVICE:
     case MOD_PD_TYPE_DEVICE_DEBUG:
     case MOD_PD_TYPE_SYSTEM:
-        status = pd_init(pd_ctx);
-        if (status != FWK_SUCCESS)
-            return status;
-
-        if (config->default_power_on)
-            return ppu_v0_set_power_mode(pd_ctx->ppu, PPU_V0_MODE_ON, NULL);
-        return FWK_SUCCESS;
+	status = pd_init(pd_ctx);
+	if (status != FWK_SUCCESS)
+	    return status;
+
+	if (config->default_power_on)
+	    return ppu_v0_set_power_mode(pd_ctx->ppu, PPU_V0_MODE_ON, NULL);
+	return FWK_SUCCESS;
 
     default:
-        return FWK_E_SUPPORT;
+	return FWK_E_SUPPORT;
     }
 }
 
@@ -236,55 +236,55 @@ static int ppu_v0_bind(fwk_id_t id, unsi
     /* Nothing to do during the first round of calls where the power module
        will bind to the power domains of this module. */
     if (round == 0)
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 
     if (fwk_id_is_type(id, FWK_ID_TYPE_MODULE))
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 
     pd_ctx = ppu_v0_ctx.pd_ctx_table + fwk_id_get_element_idx(id);
 
 #ifdef BUILD_HAS_MOD_TIMER
     if (pd_ctx->timer_ctx != NULL) {
-        /* Bind to the timer */
-        status = fwk_module_bind(
-            pd_ctx->timer_ctx->timer_id,
-            MOD_TIMER_API_ID_TIMER,
-            &pd_ctx->timer_ctx->timer_api);
-        if (status != FWK_SUCCESS)
-            return status;
+	/* Bind to the timer */
+	status = fwk_module_bind(
+	    pd_ctx->timer_ctx->timer_id,
+	    MOD_TIMER_API_ID_TIMER,
+	    &pd_ctx->timer_ctx->timer_api);
+	if (status != FWK_SUCCESS)
+	    return status;
     }
 #endif
 
     if (fwk_id_is_equal(pd_ctx->bound_id, FWK_ID_NONE))
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 
     switch (fwk_id_get_module_idx(pd_ctx->bound_id)) {
 #ifdef BUILD_HAS_MOD_POWER_DOMAIN
     case FWK_MODULE_IDX_POWER_DOMAIN:
-        return fwk_module_bind(pd_ctx->bound_id,
-                               mod_pd_api_id_driver_input,
-                               &pd_ctx->pd_driver_input_api);
-        break;
+	return fwk_module_bind(pd_ctx->bound_id,
+			       mod_pd_api_id_driver_input,
+			       &pd_ctx->pd_driver_input_api);
+	break;
     #endif
 
 #ifdef BUILD_HAS_MOD_SYSTEM_POWER
     case FWK_MODULE_IDX_SYSTEM_POWER:
-        return fwk_module_bind(pd_ctx->bound_id,
-                               mod_system_power_api_id_pd_driver_input,
-                               &pd_ctx->pd_driver_input_api);
-        break;
+	return fwk_module_bind(pd_ctx->bound_id,
+			       mod_system_power_api_id_pd_driver_input,
+			       &pd_ctx->pd_driver_input_api);
+	break;
     #endif
 
     default:
-        fwk_unexpected();
-        return FWK_E_SUPPORT;
+	fwk_unexpected();
+	return FWK_E_SUPPORT;
     }
     return status;
 }
 
 static int ppu_v0_process_bind_request(fwk_id_t source_id,
-                                       fwk_id_t target_id, fwk_id_t not_used,
-                                       const void **api)
+				       fwk_id_t target_id, fwk_id_t not_used,
+				       const void **api)
 {
     struct ppu_v0_pd_ctx *pd_ctx;
 
@@ -292,34 +292,34 @@ static int ppu_v0_process_bind_request(f
 
     switch (pd_ctx->config->pd_type) {
     case MOD_PD_TYPE_SYSTEM:
-        if (!fwk_id_is_equal(pd_ctx->bound_id, FWK_ID_NONE)) {
-            fwk_unexpected();
-            return FWK_E_ACCESS;
-        }
+	if (!fwk_id_is_equal(pd_ctx->bound_id, FWK_ID_NONE)) {
+	    fwk_unexpected();
+	    return FWK_E_ACCESS;
+	}
     /* Fallthrough */
 
     case MOD_PD_TYPE_DEVICE:
     case MOD_PD_TYPE_DEVICE_DEBUG:
 #ifdef BUILD_HAS_MOD_POWER_DOMAIN
-        if (fwk_id_get_module_idx(source_id) == FWK_MODULE_IDX_POWER_DOMAIN) {
-            pd_ctx->bound_id = source_id;
-            *api = &pd_driver;
-            break;
-        }
-        #endif
+	if (fwk_id_get_module_idx(source_id) == FWK_MODULE_IDX_POWER_DOMAIN) {
+	    pd_ctx->bound_id = source_id;
+	    *api = &pd_driver;
+	    break;
+	}
+	#endif
 #ifdef BUILD_HAS_MOD_SYSTEM_POWER
-        if (fwk_id_get_module_idx(source_id) == FWK_MODULE_IDX_SYSTEM_POWER) {
-            pd_ctx->bound_id = source_id;
-            *api = &pd_driver;
-            break;
-        }
-        #endif
-        fwk_unexpected();
-        return FWK_E_ACCESS;
+	if (fwk_id_get_module_idx(source_id) == FWK_MODULE_IDX_SYSTEM_POWER) {
+	    pd_ctx->bound_id = source_id;
+	    *api = &pd_driver;
+	    break;
+	}
+	#endif
+	fwk_unexpected();
+	return FWK_E_ACCESS;
 
     default:
-        (void)pd_driver;
-        return FWK_E_SUPPORT;
+	(void)pd_driver;
+	return FWK_E_SUPPORT;
     }
 
     return FWK_SUCCESS;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/ppu_v0/src/ppu_v0.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/ppu_v0/src/ppu_v0.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/ppu_v0/src/ppu_v0.c
@@ -25,8 +25,8 @@ static bool ppu_v0_set_power_status_chec
     params = (struct set_power_status_check_params_v0 *)data;
 
     return (
-        (params->reg->POWER_STATUS &
-         (PPU_V0_PSR_POWSTAT | PPU_V0_PSR_DYNAMIC)) == params->mode);
+	(params->reg->POWER_STATUS &
+	 (PPU_V0_PSR_POWSTAT | PPU_V0_PSR_DYNAMIC)) == params->mode);
 }
 
 void ppu_v0_init(struct ppu_v0_reg *ppu)
@@ -50,7 +50,7 @@ int ppu_v0_request_power_mode(struct ppu
     fwk_assert(mode < PPU_V0_MODE_COUNT);
 
     power_policy = ppu->POWER_POLICY &
-        ~(PPU_V0_PPR_POLICY | PPU_V0_PPR_DYNAMIC_EN);
+	~(PPU_V0_PPR_POLICY | PPU_V0_PPR_DYNAMIC_EN);
     ppu->POWER_POLICY = power_policy | mode;
 
     return FWK_SUCCESS;
@@ -67,19 +67,19 @@ int ppu_v0_set_power_mode(
 
     status = ppu_v0_request_power_mode(ppu, mode);
     if (status != FWK_SUCCESS)
-        return status;
+	return status;
     if (timer_ctx == NULL) {
-        while ((ppu->POWER_STATUS &
-                (PPU_V0_PSR_POWSTAT | PPU_V0_PSR_DYNAMIC)) != mode)
-            continue;
+	while ((ppu->POWER_STATUS &
+		(PPU_V0_PSR_POWSTAT | PPU_V0_PSR_DYNAMIC)) != mode)
+	    continue;
     } else {
-        params.mode = mode;
-        params.reg = ppu;
-        return timer_ctx->timer_api->wait(
-            timer_ctx->timer_id,
-            timer_ctx->delay_us,
-            ppu_v0_set_power_status_check,
-            &params);
+	params.mode = mode;
+	params.reg = ppu;
+	return timer_ctx->timer_api->wait(
+	    timer_ctx->timer_id,
+	    timer_ctx->delay_us,
+	    ppu_v0_set_power_status_check,
+	    &params);
     }
 
     return FWK_SUCCESS;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/ppu_v0/src/ppu_v0.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/ppu_v0/src/ppu_v0.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/ppu_v0/src/ppu_v0.h
@@ -27,23 +27,23 @@ struct ppu_v0_reg {
   FWK_R   uint32_t MISC_IP_CUR_STATUS;
   FWK_R   uint32_t STORED_STATUS;
   FWK_W   uint32_t OFF_MEM_RET_UNLOCK;
-          uint32_t RESERVED0;
+	  uint32_t RESERVED0;
   FWK_RW  uint32_t POWER_CFG;
-          uint32_t RESERVED1[3];
+	  uint32_t RESERVED1[3];
   FWK_RW  uint32_t IMR;
   FWK_RW  uint32_t ISR;
   FWK_RW  uint32_t IESR;
-          uint32_t RESERVED2[5];
+	  uint32_t RESERVED2[5];
   FWK_RW  uint32_t FUNC_RET_RAM_CFG;
   FWK_RW  uint32_t FULL_RET_RAM_CFG;
   FWK_RW  uint32_t MEM_RET_RAM_CFG;
-          uint32_t RESERVED3;
+	  uint32_t RESERVED3;
   FWK_RW  uint32_t MODE_ENTRY_DELAY_TIME_0;
   FWK_RW  uint32_t MODE_ENTRY_DELAY_TIME_1;
-          uint32_t RESERVED4[2];
+	  uint32_t RESERVED4[2];
   FWK_RW  uint32_t DEV_CONTROL_DELAY_CFG_0;
   FWK_RW  uint32_t DEV_CONTROL_DELAY_CFG_1;
-          uint8_t  RESERVED5[0xFC8 - 0x78];
+	  uint8_t  RESERVED5[0xFC8 - 0x78];
   FWK_R   uint32_t IMPLEMENTATION_ID;
   FWK_R   uint32_t ARCHITECTURE_ID;
   FWK_R   uint32_t PID4;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/ppu_v1/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/ppu_v1/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/ppu_v1/CMakeLists.txt
@@ -8,11 +8,11 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(
     ${SCP_MODULE_TARGET} PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_ppu_v1.c"
-                                 "${CMAKE_CURRENT_SOURCE_DIR}/src/ppu_v1.c")
+				 "${CMAKE_CURRENT_SOURCE_DIR}/src/ppu_v1.c")
 
 target_link_libraries(
     module-ppu-v1
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/ppu_v1/src/mod_ppu_v1.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/ppu_v1/src/mod_ppu_v1.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/ppu_v1/src/mod_ppu_v1.c
@@ -113,7 +113,7 @@ static int get_state(struct ppu_v1_reg *
 
     /* Ensure ppu_to_pd_state_v1 has an entry for each PPU state */
     static_assert((FWK_ARRAY_SIZE(ppu_mode_to_power_state) ==
-        PPU_V1_MODE_COUNT), "[PPU_V1] ppu_mode_to_power_state size error");
+	PPU_V1_MODE_COUNT), "[PPU_V1] ppu_mode_to_power_state size error");
 
     mode = ppu_v1_get_power_mode(ppu);
     fwk_assert(mode < PPU_V1_MODE_COUNT);
@@ -121,11 +121,11 @@ static int get_state(struct ppu_v1_reg *
     *state = ppu_mode_to_power_state[mode];
 
     if ((*state == MOD_PD_STATE_OFF) && (ppu_v1_is_dynamic_enabled(ppu)))
-        *state = MOD_PD_STATE_SLEEP;
+	*state = MOD_PD_STATE_SLEEP;
 
     if (*state == MODE_UNSUPPORTED) {
-        FWK_LOG_ERR("[PPU_V1] Unexpected PPU mode (%i).", mode);
-        return FWK_E_DEVICE;
+	FWK_LOG_ERR("[PPU_V1] Unexpected PPU mode (%i).", mode);
+	return FWK_E_DEVICE;
     }
 
     return FWK_SUCCESS;
@@ -140,22 +140,22 @@ static int ppu_v1_pd_set_state(fwk_id_t
 
     switch (state) {
     case MOD_PD_STATE_ON:
-        ppu_v1_set_power_mode(pd_ctx->ppu, PPU_V1_MODE_ON, pd_ctx->timer_ctx);
-        status = pd_ctx->pd_driver_input_api->report_power_state_transition(
-            pd_ctx->bound_id, MOD_PD_STATE_ON);
-        fwk_assert(status == FWK_SUCCESS);
-        break;
+	ppu_v1_set_power_mode(pd_ctx->ppu, PPU_V1_MODE_ON, pd_ctx->timer_ctx);
+	status = pd_ctx->pd_driver_input_api->report_power_state_transition(
+	    pd_ctx->bound_id, MOD_PD_STATE_ON);
+	fwk_assert(status == FWK_SUCCESS);
+	break;
 
     case MOD_PD_STATE_OFF:
-        ppu_v1_set_power_mode(pd_ctx->ppu, PPU_V1_MODE_OFF, pd_ctx->timer_ctx);
-        status = pd_ctx->pd_driver_input_api->report_power_state_transition(
-            pd_ctx->bound_id, MOD_PD_STATE_OFF);
-        fwk_assert(status == FWK_SUCCESS);
-        break;
+	ppu_v1_set_power_mode(pd_ctx->ppu, PPU_V1_MODE_OFF, pd_ctx->timer_ctx);
+	status = pd_ctx->pd_driver_input_api->report_power_state_transition(
+	    pd_ctx->bound_id, MOD_PD_STATE_OFF);
+	fwk_assert(status == FWK_SUCCESS);
+	break;
 
     default:
-        FWK_LOG_ERR("[PD] Requested power state (%i) is not supported.", state);
-        return FWK_E_PARAM;
+	FWK_LOG_ERR("[PD] Requested power state (%i) is not supported.", state);
+	return FWK_E_PARAM;
     }
 
     return status;
@@ -179,10 +179,10 @@ static int ppu_v1_pd_reset(fwk_id_t pd_i
 
     /* Model does not support warm reset at the moment. Using OFF instead. */
     status =
-        ppu_v1_set_power_mode(pd_ctx->ppu, PPU_V1_MODE_OFF, pd_ctx->timer_ctx);
+	ppu_v1_set_power_mode(pd_ctx->ppu, PPU_V1_MODE_OFF, pd_ctx->timer_ctx);
     if (status == FWK_SUCCESS)
-        status = ppu_v1_set_power_mode(
-            pd_ctx->ppu, PPU_V1_MODE_ON, pd_ctx->timer_ctx);
+	status = ppu_v1_set_power_mode(
+	    pd_ctx->ppu, PPU_V1_MODE_ON, pd_ctx->timer_ctx);
 
     return status;
 }
@@ -213,11 +213,11 @@ static int ppu_v1_core_pd_init(struct pp
 
     status = get_state(ppu, &state);
     if (status != FWK_SUCCESS)
-        return status;
+	return status;
 
     if (state == MOD_PD_STATE_ON) {
-        ppu_v1_interrupt_unmask(ppu, PPU_V1_IMR_DYN_POLICY_MIN_IRQ_MASK);
-        ppu_v1_dynamic_enable(ppu, PPU_V1_MODE_OFF);
+	ppu_v1_interrupt_unmask(ppu, PPU_V1_IMR_DYN_POLICY_MIN_IRQ_MASK);
+	ppu_v1_dynamic_enable(ppu, PPU_V1_MODE_OFF);
     }
 
     return FWK_SUCCESS;
@@ -235,53 +235,53 @@ static int ppu_v1_core_pd_set_state(fwk_
 
     switch (state) {
     case MOD_PD_STATE_OFF:
-        ppu_v1_set_input_edge_sensitivity(ppu,
-                                          PPU_V1_MODE_ON,
-                                          PPU_V1_EDGE_SENSITIVITY_MASKED);
-        ppu_v1_interrupt_mask(ppu, PPU_V1_IMR_DYN_POLICY_MIN_IRQ_MASK);
-        ppu_v1_set_power_mode(ppu, PPU_V1_MODE_OFF, pd_ctx->timer_ctx);
-        ppu_v1_lock_off_disable(ppu);
-        ppu_v1_off_unlock(ppu);
-        status = pd_ctx->pd_driver_input_api->report_power_state_transition(
-            pd_ctx->bound_id, MOD_PD_STATE_OFF);
-        fwk_assert(status == FWK_SUCCESS);
-        break;
+	ppu_v1_set_input_edge_sensitivity(ppu,
+					  PPU_V1_MODE_ON,
+					  PPU_V1_EDGE_SENSITIVITY_MASKED);
+	ppu_v1_interrupt_mask(ppu, PPU_V1_IMR_DYN_POLICY_MIN_IRQ_MASK);
+	ppu_v1_set_power_mode(ppu, PPU_V1_MODE_OFF, pd_ctx->timer_ctx);
+	ppu_v1_lock_off_disable(ppu);
+	ppu_v1_off_unlock(ppu);
+	status = pd_ctx->pd_driver_input_api->report_power_state_transition(
+	    pd_ctx->bound_id, MOD_PD_STATE_OFF);
+	fwk_assert(status == FWK_SUCCESS);
+	break;
 
     case MOD_PD_STATE_ON:
-        ppu_v1_interrupt_unmask(ppu, PPU_V1_IMR_DYN_POLICY_MIN_IRQ_MASK);
-        ppu_v1_set_input_edge_sensitivity(ppu,
-                                          PPU_V1_MODE_ON,
-                                          PPU_V1_EDGE_SENSITIVITY_MASKED);
-        ppu_v1_set_power_mode(ppu, PPU_V1_MODE_ON, pd_ctx->timer_ctx);
-        ppu_v1_dynamic_enable(ppu, PPU_V1_MODE_OFF);
-        status = pd_ctx->pd_driver_input_api->report_power_state_transition(
-            pd_ctx->bound_id, MOD_PD_STATE_ON);
-        fwk_assert(status == FWK_SUCCESS);
-        break;
+	ppu_v1_interrupt_unmask(ppu, PPU_V1_IMR_DYN_POLICY_MIN_IRQ_MASK);
+	ppu_v1_set_input_edge_sensitivity(ppu,
+					  PPU_V1_MODE_ON,
+					  PPU_V1_EDGE_SENSITIVITY_MASKED);
+	ppu_v1_set_power_mode(ppu, PPU_V1_MODE_ON, pd_ctx->timer_ctx);
+	ppu_v1_dynamic_enable(ppu, PPU_V1_MODE_OFF);
+	status = pd_ctx->pd_driver_input_api->report_power_state_transition(
+	    pd_ctx->bound_id, MOD_PD_STATE_ON);
+	fwk_assert(status == FWK_SUCCESS);
+	break;
 
     case MOD_PD_STATE_SLEEP:
-        /*
-         * If the dynamic transitions have been enabled then the core is
-         * already in the SLEEP power state or will transit to the SLEEP power
-         * state if the appropriate processing is done on AP side. Thus nothing
-         * to do in that case. If the dynamic transitions are not enabled then
-         * this is an OFF to SLEEP transition.
-         */
-        if (!ppu_v1_is_dynamic_enabled(ppu)) {
-            ppu_v1_dynamic_enable(ppu, PPU_V1_MODE_OFF);
-            ppu_v1_set_input_edge_sensitivity(ppu,
-                                              PPU_V1_MODE_ON,
-                                              PPU_V1_EDGE_SENSITIVITY_MASKED);
-        }
-        status = pd_ctx->pd_driver_input_api->report_power_state_transition(
-            pd_ctx->bound_id, MOD_PD_STATE_SLEEP);
-        fwk_assert(status == FWK_SUCCESS);
-        break;
+	/*
+	 * If the dynamic transitions have been enabled then the core is
+	 * already in the SLEEP power state or will transit to the SLEEP power
+	 * state if the appropriate processing is done on AP side. Thus nothing
+	 * to do in that case. If the dynamic transitions are not enabled then
+	 * this is an OFF to SLEEP transition.
+	 */
+	if (!ppu_v1_is_dynamic_enabled(ppu)) {
+	    ppu_v1_dynamic_enable(ppu, PPU_V1_MODE_OFF);
+	    ppu_v1_set_input_edge_sensitivity(ppu,
+					      PPU_V1_MODE_ON,
+					      PPU_V1_EDGE_SENSITIVITY_MASKED);
+	}
+	status = pd_ctx->pd_driver_input_api->report_power_state_transition(
+	    pd_ctx->bound_id, MOD_PD_STATE_SLEEP);
+	fwk_assert(status == FWK_SUCCESS);
+	break;
 
     default:
-        FWK_LOG_ERR(
-            "[PPU_V1] Requested CPU power state (%i) is not supported!", state);
-        return FWK_E_PARAM;
+	FWK_LOG_ERR(
+	    "[PPU_V1] Requested CPU power state (%i) is not supported!", state);
+	return FWK_E_PARAM;
     }
 
     return status;
@@ -293,7 +293,7 @@ static int ppu_v1_core_pd_reset(fwk_id_t
 
     status = ppu_v1_core_pd_set_state(core_pd_id, MOD_PD_STATE_OFF);
     if (status == FWK_SUCCESS)
-        status = ppu_v1_core_pd_set_state(core_pd_id, MOD_PD_STATE_ON);
+	status = ppu_v1_core_pd_set_state(core_pd_id, MOD_PD_STATE_ON);
 
     return status;
 }
@@ -307,8 +307,8 @@ static int ppu_v1_core_pd_prepare_for_sy
     ppu = pd_ctx->ppu;
 
     ppu_v1_set_input_edge_sensitivity(ppu,
-                                      PPU_V1_MODE_ON,
-                                      PPU_V1_EDGE_SENSITIVITY_MASKED);
+				      PPU_V1_MODE_ON,
+				      PPU_V1_EDGE_SENSITIVITY_MASKED);
     ppu_v1_request_power_mode(ppu, PPU_V1_MODE_OFF);
 
     return FWK_SUCCESS;
@@ -324,43 +324,43 @@ static void core_pd_ppu_interrupt_handle
 
     /* ON request interrupt */
     if (ppu_v1_is_power_active_edge_interrupt(ppu, PPU_V1_MODE_ON)) {
-        ppu_v1_ack_power_active_edge_interrupt(ppu, PPU_V1_MODE_ON);
-        ppu_v1_set_input_edge_sensitivity(ppu,
-                                          PPU_V1_MODE_ON,
-                                          PPU_V1_EDGE_SENSITIVITY_MASKED);
-        ppu_v1_interrupt_unmask(ppu, PPU_V1_IMR_DYN_POLICY_MIN_IRQ_MASK);
-
-        status = pd_ctx->pd_driver_input_api->report_power_state_transition(
-            pd_ctx->bound_id, MOD_PD_STATE_ON);
-        fwk_assert(status == FWK_SUCCESS);
-        (void)status;
+	ppu_v1_ack_power_active_edge_interrupt(ppu, PPU_V1_MODE_ON);
+	ppu_v1_set_input_edge_sensitivity(ppu,
+					  PPU_V1_MODE_ON,
+					  PPU_V1_EDGE_SENSITIVITY_MASKED);
+	ppu_v1_interrupt_unmask(ppu, PPU_V1_IMR_DYN_POLICY_MIN_IRQ_MASK);
+
+	status = pd_ctx->pd_driver_input_api->report_power_state_transition(
+	    pd_ctx->bound_id, MOD_PD_STATE_ON);
+	fwk_assert(status == FWK_SUCCESS);
+	(void)status;
     /* Minimum policy reached interrupt */
     } else if (ppu_v1_is_dyn_policy_min_interrupt(ppu)) {
-        ppu_v1_ack_interrupt(ppu, PPU_V1_ISR_DYN_POLICY_MIN_IRQ);
-        ppu_v1_interrupt_mask(ppu, PPU_V1_IMR_DYN_POLICY_MIN_IRQ_MASK);
+	ppu_v1_ack_interrupt(ppu, PPU_V1_ISR_DYN_POLICY_MIN_IRQ);
+	ppu_v1_interrupt_mask(ppu, PPU_V1_IMR_DYN_POLICY_MIN_IRQ_MASK);
 
-        status = pd_ctx->pd_driver_input_api->report_power_state_transition(
-            pd_ctx->bound_id, MOD_PD_STATE_SLEEP);
-        fwk_assert(status == FWK_SUCCESS);
-        (void)status;
-
-        /*
-         * Enable the core PACTIVE ON signal rising edge interrupt then check if
-         * the PACTIVE ON signal is high. If it is high, we may have missed the
-         * transition from low to high. In that case, just disable the interrupt
-         * and acknowledge it in case it is pending. There is no need to send an
-         * update request as one has already been queued.
-         */
-        ppu_v1_set_input_edge_sensitivity(ppu,
-                                          PPU_V1_MODE_ON,
-                                          PPU_V1_EDGE_SENSITIVITY_RISING_EDGE);
-        if (ppu_v1_is_power_devactive_high(ppu, PPU_V1_MODE_ON)) {
-            ppu_v1_set_input_edge_sensitivity(ppu,
-                                              PPU_V1_MODE_ON,
-                                              PPU_V1_EDGE_SENSITIVITY_MASKED);
-            ppu_v1_ack_power_active_edge_interrupt(ppu, PPU_V1_MODE_ON);
-            ppu_v1_interrupt_unmask(ppu, PPU_V1_IMR_DYN_POLICY_MIN_IRQ_MASK);
-        }
+	status = pd_ctx->pd_driver_input_api->report_power_state_transition(
+	    pd_ctx->bound_id, MOD_PD_STATE_SLEEP);
+	fwk_assert(status == FWK_SUCCESS);
+	(void)status;
+
+	/*
+	 * Enable the core PACTIVE ON signal rising edge interrupt then check if
+	 * the PACTIVE ON signal is high. If it is high, we may have missed the
+	 * transition from low to high. In that case, just disable the interrupt
+	 * and acknowledge it in case it is pending. There is no need to send an
+	 * update request as one has already been queued.
+	 */
+	ppu_v1_set_input_edge_sensitivity(ppu,
+					  PPU_V1_MODE_ON,
+					  PPU_V1_EDGE_SENSITIVITY_RISING_EDGE);
+	if (ppu_v1_is_power_devactive_high(ppu, PPU_V1_MODE_ON)) {
+	    ppu_v1_set_input_edge_sensitivity(ppu,
+					      PPU_V1_MODE_ON,
+					      PPU_V1_EDGE_SENSITIVITY_MASKED);
+	    ppu_v1_ack_power_active_edge_interrupt(ppu, PPU_V1_MODE_ON);
+	    ppu_v1_interrupt_unmask(ppu, PPU_V1_IMR_DYN_POLICY_MIN_IRQ_MASK);
+	}
     }
 }
 
@@ -370,7 +370,7 @@ static const struct mod_pd_driver_api co
     .get_state = ppu_v1_pd_get_state,
     .reset = ppu_v1_core_pd_reset,
     .prepare_core_for_system_suspend =
-        ppu_v1_core_pd_prepare_for_system_suspend,
+	ppu_v1_core_pd_prepare_for_system_suspend,
     .shutdown = ppu_v1_pd_shutdown,
 };
 #endif
@@ -390,9 +390,9 @@ static void unlock_all_cores(struct ppu_
     cluster_pd_ctx = pd_ctx->data;
 
     for (core_idx = 0; core_idx < cluster_pd_ctx->core_count; ++core_idx) {
-        cpu_ppu = cluster_pd_ctx->core_pd_ctx_table[core_idx]->ppu;
-        ppu_v1_lock_off_disable(cpu_ppu);
-        ppu_v1_off_unlock(cpu_ppu);
+	cpu_ppu = cluster_pd_ctx->core_pd_ctx_table[core_idx]->ppu;
+	ppu_v1_lock_off_disable(cpu_ppu);
+	ppu_v1_off_unlock(cpu_ppu);
     }
 }
 
@@ -407,18 +407,18 @@ static bool lock_all_dynamic_cores(struc
     cluster_pd_ctx = pd_ctx->data;
 
     for (core_idx = 0; core_idx < cluster_pd_ctx->core_count; ++core_idx) {
-        cpu_ppu = cluster_pd_ctx->core_pd_ctx_table[core_idx]->ppu;
+	cpu_ppu = cluster_pd_ctx->core_pd_ctx_table[core_idx]->ppu;
 
-        if (!ppu_v1_is_dynamic_enabled(cpu_ppu))
-            continue;
+	if (!ppu_v1_is_dynamic_enabled(cpu_ppu))
+	    continue;
 
-        ppu_v1_lock_off_enable(cpu_ppu);
-        while ((!ppu_v1_is_locked(cpu_ppu)) &&
-               (!ppu_v1_is_power_devactive_high(cpu_ppu, PPU_V1_MODE_ON)))
-            continue;
+	ppu_v1_lock_off_enable(cpu_ppu);
+	while ((!ppu_v1_is_locked(cpu_ppu)) &&
+	       (!ppu_v1_is_power_devactive_high(cpu_ppu, PPU_V1_MODE_ON)))
+	    continue;
 
-        if (ppu_v1_is_power_devactive_high(cpu_ppu, PPU_V1_MODE_ON))
-            return false;
+	if (ppu_v1_is_power_devactive_high(cpu_ppu, PPU_V1_MODE_ON))
+	    return false;
     }
 
     return true;
@@ -434,13 +434,13 @@ static bool cluster_off(struct ppu_v1_pd
     ppu = pd_ctx->ppu;
 
     ppu_v1_set_input_edge_sensitivity(ppu,
-                                      PPU_V1_MODE_ON,
-                                      PPU_V1_EDGE_SENSITIVITY_MASKED);
+				      PPU_V1_MODE_ON,
+				      PPU_V1_EDGE_SENSITIVITY_MASKED);
 
     lock_successful = lock_all_dynamic_cores(pd_ctx);
     if (!lock_successful) {
-        unlock_all_cores(pd_ctx);
-        return false;
+	unlock_all_cores(pd_ctx);
+	return false;
     }
 
     ppu_v1_set_power_mode(ppu, PPU_V1_MODE_OFF, pd_ctx->timer_ctx);
@@ -457,19 +457,19 @@ static void cluster_on(struct ppu_v1_pd_
     ppu = pd_ctx->ppu;
 
     ppu_v1_set_input_edge_sensitivity(ppu,
-                                      PPU_V1_MODE_ON,
-                                      PPU_V1_EDGE_SENSITIVITY_MASKED);
+				      PPU_V1_MODE_ON,
+				      PPU_V1_EDGE_SENSITIVITY_MASKED);
 
     ppu_v1_request_operating_mode(ppu, pd_ctx->config->opmode);
 
     ppu_v1_set_power_mode(ppu, PPU_V1_MODE_ON, pd_ctx->timer_ctx);
     status = pd_ctx->pd_driver_input_api->report_power_state_transition(
-        pd_ctx->bound_id, MOD_PD_STATE_ON);
+	pd_ctx->bound_id, MOD_PD_STATE_ON);
     fwk_assert(status == FWK_SUCCESS);
     (void)status;
 
     if (pd_ctx->observer_api != NULL)
-        pd_ctx->observer_api->post_ppu_on(pd_ctx->config->post_ppu_on_param);
+	pd_ctx->observer_api->post_ppu_on(pd_ctx->config->post_ppu_on_param);
 
     unlock_all_cores(pd_ctx);
 }
@@ -484,16 +484,16 @@ static int ppu_v1_cluster_pd_init(struct
 
     status = get_state(ppu, &state);
     if (status != FWK_SUCCESS)
-        return status;
+	return status;
 
     /* For clusters with operating mode support, enable the dynamic support */
     if (ppu_v1_get_num_opmode(ppu) > 1)
-        ppu_v1_opmode_dynamic_enable(ppu, PPU_V1_OPMODE_00);
+	ppu_v1_opmode_dynamic_enable(ppu, PPU_V1_OPMODE_00);
 
     if (state == MOD_PD_STATE_ON) {
-        ppu_v1_set_input_edge_sensitivity(ppu,
-                                          PPU_V1_MODE_ON,
-                                          PPU_V1_EDGE_SENSITIVITY_FALLING_EDGE);
+	ppu_v1_set_input_edge_sensitivity(ppu,
+					  PPU_V1_MODE_ON,
+					  PPU_V1_EDGE_SENSITIVITY_FALLING_EDGE);
     }
 
     return FWK_SUCCESS;
@@ -501,7 +501,7 @@ static int ppu_v1_cluster_pd_init(struct
 
 #ifdef BUILD_HAS_MOD_POWER_DOMAIN
 static int ppu_v1_cluster_pd_set_state(fwk_id_t cluster_pd_id,
-                                       unsigned int state)
+				       unsigned int state)
 {
     int status;
     struct ppu_v1_pd_ctx *pd_ctx;
@@ -510,25 +510,25 @@ static int ppu_v1_cluster_pd_set_state(f
 
     switch (state) {
     case MOD_PD_STATE_ON:
-        cluster_on(pd_ctx);
+	cluster_on(pd_ctx);
 
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 
     case MOD_PD_STATE_OFF:
-        if (!cluster_off(pd_ctx)) {
-            /* Cluster failed to transition to off */
+	if (!cluster_off(pd_ctx)) {
+	    /* Cluster failed to transition to off */
 
-            return FWK_E_STATE;
-        }
-        status = pd_ctx->pd_driver_input_api->report_power_state_transition(
-            pd_ctx->bound_id, MOD_PD_STATE_OFF);
-        fwk_assert(status == FWK_SUCCESS);
-        return status;
+	    return FWK_E_STATE;
+	}
+	status = pd_ctx->pd_driver_input_api->report_power_state_transition(
+	    pd_ctx->bound_id, MOD_PD_STATE_OFF);
+	fwk_assert(status == FWK_SUCCESS);
+	return status;
 
     default:
-        FWK_LOG_ERR(
-            "[PPU_V1] Requested CPU power state (%i) is not supported!", state);
-        return FWK_E_PARAM;
+	FWK_LOG_ERR(
+	    "[PPU_V1] Requested CPU power state (%i) is not supported!", state);
+	return FWK_E_PARAM;
     }
 }
 #endif
@@ -544,50 +544,50 @@ static void cluster_pd_ppu_interrupt_han
     ppu = pd_ctx->ppu;
 
     if (!ppu_v1_is_power_active_edge_interrupt(ppu, PPU_V1_MODE_ON))
-        return; /* Spurious interrupt */
+	return; /* Spurious interrupt */
 
     ppu_v1_ack_power_active_edge_interrupt(ppu, PPU_V1_MODE_ON);
     current_mode = ppu_v1_get_power_mode(ppu);
 
     switch (current_mode) {
     case PPU_V1_MODE_OFF:
-        /* Cluster has to be powered on */
-        cluster_on(pd_ctx);
-        ppu_v1_set_input_edge_sensitivity(ppu,
-                                          PPU_V1_MODE_ON,
-                                          PPU_V1_EDGE_SENSITIVITY_FALLING_EDGE);
-        return;
+	/* Cluster has to be powered on */
+	cluster_on(pd_ctx);
+	ppu_v1_set_input_edge_sensitivity(ppu,
+					  PPU_V1_MODE_ON,
+					  PPU_V1_EDGE_SENSITIVITY_FALLING_EDGE);
+	return;
 
     case PPU_V1_MODE_ON:
-        /*
-         * It may be possible to turn off the cluster, check all PACTIVE lines
-         * to make sure it is not just requesting a low power mode.
-         */
-        while (current_mode > 0) {
-            if (ppu_v1_is_power_devactive_high(ppu, current_mode--))
-                return;
-        }
-
-        /* All PACTIVE lines are low, so the cluster can be turned off */
-        if (cluster_off(pd_ctx)) {
-            /* Cluster successfuly transitioned to off */
-            ppu_v1_set_input_edge_sensitivity(ppu,
-                PPU_V1_MODE_ON, PPU_V1_EDGE_SENSITIVITY_RISING_EDGE);
-            status = pd_ctx->pd_driver_input_api->report_power_state_transition(
-                pd_ctx->bound_id, MOD_PD_STATE_SLEEP);
-            fwk_assert(status == FWK_SUCCESS);
-            (void)status;
-        } else {
-            /* Cluster did not transition to off */
-            ppu_v1_set_input_edge_sensitivity(ppu,
-                PPU_V1_MODE_ON, PPU_V1_EDGE_SENSITIVITY_FALLING_EDGE);
-        }
-        return;
+	/*
+	 * It may be possible to turn off the cluster, check all PACTIVE lines
+	 * to make sure it is not just requesting a low power mode.
+	 */
+	while (current_mode > 0) {
+	    if (ppu_v1_is_power_devactive_high(ppu, current_mode--))
+		return;
+	}
+
+	/* All PACTIVE lines are low, so the cluster can be turned off */
+	if (cluster_off(pd_ctx)) {
+	    /* Cluster successfuly transitioned to off */
+	    ppu_v1_set_input_edge_sensitivity(ppu,
+		PPU_V1_MODE_ON, PPU_V1_EDGE_SENSITIVITY_RISING_EDGE);
+	    status = pd_ctx->pd_driver_input_api->report_power_state_transition(
+		pd_ctx->bound_id, MOD_PD_STATE_SLEEP);
+	    fwk_assert(status == FWK_SUCCESS);
+	    (void)status;
+	} else {
+	    /* Cluster did not transition to off */
+	    ppu_v1_set_input_edge_sensitivity(ppu,
+		PPU_V1_MODE_ON, PPU_V1_EDGE_SENSITIVITY_FALLING_EDGE);
+	}
+	return;
 
     default:
-        /* Cluster is in an invalid power mode */
-        fwk_unexpected();
-        return;
+	/* Cluster is in an invalid power mode */
+	fwk_unexpected();
+	return;
     }
 }
 
@@ -607,9 +607,9 @@ static void ppu_interrupt_handler(uintpt
     fwk_assert(pd_ctx != NULL);
 
     if (pd_ctx->config->pd_type == MOD_PD_TYPE_CORE)
-        core_pd_ppu_interrupt_handler(pd_ctx);
+	core_pd_ppu_interrupt_handler(pd_ctx);
     else
-        cluster_pd_ppu_interrupt_handler(pd_ctx);
+	cluster_pd_ppu_interrupt_handler(pd_ctx);
 }
 
 static void ppu_isr_api_interrupt_handler(fwk_id_t pd_id)
@@ -617,7 +617,7 @@ static void ppu_isr_api_interrupt_handle
     struct ppu_v1_pd_ctx *pd_ctx;
 
     if (!fwk_id_is_type(pd_id, FWK_ID_TYPE_ELEMENT))
-        return;
+	return;
 
     pd_ctx = ppu_v1_ctx.pd_ctx_table + fwk_id_get_element_idx(pd_id);
     ppu_interrupt_handler((uintptr_t)pd_ctx);
@@ -632,12 +632,12 @@ static int ppu_power_mode_on(fwk_id_t pd
     struct ppu_v1_pd_ctx *pd_ctx;
 
     if (!fwk_id_is_type(pd_id, FWK_ID_TYPE_ELEMENT))
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     pd_ctx = ppu_v1_ctx.pd_ctx_table + fwk_id_get_element_idx(pd_id);
 
     return ppu_v1_set_power_mode(
-        pd_ctx->ppu, PPU_V1_MODE_ON, pd_ctx->timer_ctx);
+	pd_ctx->ppu, PPU_V1_MODE_ON, pd_ctx->timer_ctx);
 }
 
 static const struct ppu_v1_boot_api boot_api = {
@@ -649,10 +649,10 @@ static const struct ppu_v1_boot_api boot
  */
 
 static int ppu_v1_mod_init(fwk_id_t module_id, unsigned int pd_count,
-                           const void *unused)
+			   const void *unused)
 {
     ppu_v1_ctx.pd_ctx_table = fwk_mm_calloc(pd_count,
-                                            sizeof(struct ppu_v1_pd_ctx));
+					    sizeof(struct ppu_v1_pd_ctx));
 
     ppu_v1_ctx.pd_ctx_table_size = pd_count;
 
@@ -665,7 +665,7 @@ static int ppu_v1_pd_init(fwk_id_t pd_id
     struct ppu_v1_pd_ctx *pd_ctx;
 
     if (config->pd_type >= MOD_PD_TYPE_COUNT)
-        return FWK_E_DATA;
+	return FWK_E_DATA;
 
     pd_ctx = ppu_v1_ctx.pd_ctx_table + fwk_id_get_element_idx(pd_id);
     pd_ctx->config = config;
@@ -673,46 +673,46 @@ static int ppu_v1_pd_init(fwk_id_t pd_id
     pd_ctx->bound_id = FWK_ID_NONE;
 
     if (config->ppu.irq != FWK_INTERRUPT_NONE) {
-        fwk_interrupt_set_isr_param(config->ppu.irq,
-                                    ppu_interrupt_handler,
-                                    (uintptr_t)pd_ctx);
+	fwk_interrupt_set_isr_param(config->ppu.irq,
+				    ppu_interrupt_handler,
+				    (uintptr_t)pd_ctx);
     }
 
     if (config->pd_type == MOD_PD_TYPE_CLUSTER) {
-        pd_ctx->data = fwk_mm_calloc(1, sizeof(struct ppu_v1_cluster_pd_ctx));
+	pd_ctx->data = fwk_mm_calloc(1, sizeof(struct ppu_v1_cluster_pd_ctx));
     }
 #ifdef BUILD_HAS_MOD_TIMER
     if (config->timer_config == NULL) {
-        pd_ctx->timer_ctx = NULL;
+	pd_ctx->timer_ctx = NULL;
     } else {
-        pd_ctx->timer_ctx = fwk_mm_calloc(1, sizeof(struct ppu_v1_timer_ctx));
-        if (pd_ctx->timer_ctx == NULL)
-            return FWK_E_NOMEM;
-        /* Check for valid timeout value if timer ID is specified */
-        if (config->timer_config->set_state_timeout_us == 0)
-            return FWK_E_PARAM;
-        /* Save the timer ID to pd context */
-        pd_ctx->timer_ctx->timer_id = config->timer_config->timer_id;
-        pd_ctx->timer_ctx->delay_us =
-            config->timer_config->set_state_timeout_us;
+	pd_ctx->timer_ctx = fwk_mm_calloc(1, sizeof(struct ppu_v1_timer_ctx));
+	if (pd_ctx->timer_ctx == NULL)
+	    return FWK_E_NOMEM;
+	/* Check for valid timeout value if timer ID is specified */
+	if (config->timer_config->set_state_timeout_us == 0)
+	    return FWK_E_PARAM;
+	/* Save the timer ID to pd context */
+	pd_ctx->timer_ctx->timer_id = config->timer_config->timer_id;
+	pd_ctx->timer_ctx->delay_us =
+	    config->timer_config->set_state_timeout_us;
     }
 #else
     pd_ctx->timer_ctx = NULL;
 #endif
     if (config->default_power_on) {
-        switch (config->pd_type) {
-        case MOD_PD_TYPE_DEVICE:
-            /* Fall through */
-        case MOD_PD_TYPE_DEVICE_DEBUG:
-            /* Fall through */
-        case MOD_PD_TYPE_SYSTEM:
-            ppu_v1_init(pd_ctx->ppu);
-            return ppu_v1_set_power_mode(pd_ctx->ppu, PPU_V1_MODE_ON, NULL);
-
-        default:
-            fwk_unexpected();
-            return FWK_E_SUPPORT;
-        }
+	switch (config->pd_type) {
+	case MOD_PD_TYPE_DEVICE:
+	    /* Fall through */
+	case MOD_PD_TYPE_DEVICE_DEBUG:
+	    /* Fall through */
+	case MOD_PD_TYPE_SYSTEM:
+	    ppu_v1_init(pd_ctx->ppu);
+	    return ppu_v1_set_power_mode(pd_ctx->ppu, PPU_V1_MODE_ON, NULL);
+
+	default:
+	    fwk_unexpected();
+	    return FWK_E_SUPPORT;
+	}
     }
     return FWK_SUCCESS;
 }
@@ -726,26 +726,26 @@ static int ppu_v1_post_init(fwk_id_t mod
     struct ppu_v1_cluster_pd_ctx *cluster_pd_specific_ctx;
 
     for (pd_idx = 0; pd_idx < ppu_v1_ctx.pd_ctx_table_size; pd_idx++) {
-        pd_ctx = &ppu_v1_ctx.pd_ctx_table[pd_idx];
-        config = pd_ctx->config;
-        if (config->pd_type != MOD_PD_TYPE_CORE)
-            continue;
-
-        cluster_id = config->cluster_id;
-
-        if ((!fwk_module_is_valid_element_id(cluster_id)) ||
-            (fwk_id_get_module_idx(cluster_id) != FWK_MODULE_IDX_PPU_V1))
-            return FWK_E_PARAM;
-
-        cluster_pd_ctx = &ppu_v1_ctx.pd_ctx_table[
-            fwk_id_get_element_idx(cluster_id)];
-        cluster_pd_specific_ctx = cluster_pd_ctx->data;
+	pd_ctx = &ppu_v1_ctx.pd_ctx_table[pd_idx];
+	config = pd_ctx->config;
+	if (config->pd_type != MOD_PD_TYPE_CORE)
+	    continue;
+
+	cluster_id = config->cluster_id;
+
+	if ((!fwk_module_is_valid_element_id(cluster_id)) ||
+	    (fwk_id_get_module_idx(cluster_id) != FWK_MODULE_IDX_PPU_V1))
+	    return FWK_E_PARAM;
+
+	cluster_pd_ctx = &ppu_v1_ctx.pd_ctx_table[
+	    fwk_id_get_element_idx(cluster_id)];
+	cluster_pd_specific_ctx = cluster_pd_ctx->data;
 
        if (cluster_pd_specific_ctx->core_count >= CORE_PER_CLUSTER_COUNT_MAX)
-           return FWK_E_NOMEM;
+	   return FWK_E_NOMEM;
 
        cluster_pd_specific_ctx->core_pd_ctx_table[
-           cluster_pd_specific_ctx->core_count++] = pd_ctx;
+	   cluster_pd_specific_ctx->core_count++] = pd_ctx;
        pd_ctx->parent_pd_ctx = cluster_pd_ctx;
     }
 
@@ -760,69 +760,69 @@ static int ppu_v1_bind(fwk_id_t id, unsi
     /* Nothing to do during the first round of calls where the power module
        will bind to the power domains of this module. */
     if (round == 0)
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 
     if (fwk_id_is_type(id, FWK_ID_TYPE_MODULE))
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 
     pd_ctx = ppu_v1_ctx.pd_ctx_table + fwk_id_get_element_idx(id);
 
 #ifdef BUILD_HAS_MOD_TIMER
     if (pd_ctx->timer_ctx != NULL &&
-        !fwk_id_is_equal(pd_ctx->timer_ctx->timer_id, FWK_ID_NONE)) {
-        /* Bind to the timer */
-        status = fwk_module_bind(
-            pd_ctx->timer_ctx->timer_id,
-            MOD_TIMER_API_ID_TIMER,
-            &pd_ctx->timer_ctx->timer_api);
-        if (status != FWK_SUCCESS)
-            return status;
+	!fwk_id_is_equal(pd_ctx->timer_ctx->timer_id, FWK_ID_NONE)) {
+	/* Bind to the timer */
+	status = fwk_module_bind(
+	    pd_ctx->timer_ctx->timer_id,
+	    MOD_TIMER_API_ID_TIMER,
+	    &pd_ctx->timer_ctx->timer_api);
+	if (status != FWK_SUCCESS)
+	    return status;
     }
 #endif
 
     if (!fwk_id_is_equal(pd_ctx->config->observer_id, FWK_ID_NONE)) {
-        if (pd_ctx->config->pd_type != MOD_PD_TYPE_CLUSTER) {
-            /* State observation only supported for clusters */
-            fwk_unexpected();
-            return FWK_E_SUPPORT;
-        }
-
-        status = fwk_module_bind(pd_ctx->config->observer_id,
-                                 pd_ctx->config->observer_api,
-                                 &pd_ctx->observer_api);
-        if (status != FWK_SUCCESS)
-            return status;
+	if (pd_ctx->config->pd_type != MOD_PD_TYPE_CLUSTER) {
+	    /* State observation only supported for clusters */
+	    fwk_unexpected();
+	    return FWK_E_SUPPORT;
+	}
+
+	status = fwk_module_bind(pd_ctx->config->observer_id,
+				 pd_ctx->config->observer_api,
+				 &pd_ctx->observer_api);
+	if (status != FWK_SUCCESS)
+	    return status;
     }
 
     if (fwk_id_is_equal(pd_ctx->bound_id, FWK_ID_NONE))
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 
     switch (fwk_id_get_module_idx(pd_ctx->bound_id)) {
 #ifdef BUILD_HAS_MOD_POWER_DOMAIN
     case FWK_MODULE_IDX_POWER_DOMAIN:
-        return fwk_module_bind(pd_ctx->bound_id,
-                               mod_pd_api_id_driver_input,
-                               &pd_ctx->pd_driver_input_api);
-        break;
+	return fwk_module_bind(pd_ctx->bound_id,
+			       mod_pd_api_id_driver_input,
+			       &pd_ctx->pd_driver_input_api);
+	break;
     #endif
 
 #ifdef BUILD_HAS_MOD_SYSTEM_POWER
     case FWK_MODULE_IDX_SYSTEM_POWER:
-        return fwk_module_bind(pd_ctx->bound_id,
-                               mod_system_power_api_id_pd_driver_input,
-                               &pd_ctx->pd_driver_input_api);
-        break;
+	return fwk_module_bind(pd_ctx->bound_id,
+			       mod_system_power_api_id_pd_driver_input,
+			       &pd_ctx->pd_driver_input_api);
+	break;
     #endif
 
     default:
-        fwk_unexpected();
-        return FWK_E_SUPPORT;
+	fwk_unexpected();
+	return FWK_E_SUPPORT;
     }
 }
 
 static int ppu_v1_process_bind_request(fwk_id_t source_id,
-                                       fwk_id_t target_id, fwk_id_t api_id,
-                                       const void **api)
+				       fwk_id_t target_id, fwk_id_t api_id,
+				       const void **api)
 {
     struct ppu_v1_pd_ctx *pd_ctx;
     unsigned int api_idx;
@@ -836,88 +836,88 @@ static int ppu_v1_process_bind_request(f
     api_idx = fwk_id_get_api_idx(api_id);
 
     if (api_idx == MOD_PPU_V1_API_IDX_ISR) {
-        if (!fwk_id_is_type(target_id, FWK_ID_TYPE_MODULE))
-            return FWK_E_SUPPORT;
+	if (!fwk_id_is_type(target_id, FWK_ID_TYPE_MODULE))
+	    return FWK_E_SUPPORT;
 
-        *api = &isr_api;
-        return FWK_SUCCESS;
+	*api = &isr_api;
+	return FWK_SUCCESS;
     }
 
     if (api_idx == MOD_PPU_V1_API_IDX_BOOT) {
-        *api = &boot_api;
-        return FWK_SUCCESS;
+	*api = &boot_api;
+	return FWK_SUCCESS;
     }
 
     if (api_idx != MOD_PPU_V1_API_IDX_POWER_DOMAIN_DRIVER)
-        return FWK_E_SUPPORT;
+	return FWK_E_SUPPORT;
 
     if (!fwk_module_is_valid_element_id(target_id))
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     pd_ctx = ppu_v1_ctx.pd_ctx_table + fwk_id_get_element_idx(target_id);
 
     /* Allow multiple binding only for device power domain for now */
     if ((pd_ctx->config->pd_type != MOD_PD_TYPE_DEVICE) &&
-        (!fwk_id_is_equal(pd_ctx->bound_id, FWK_ID_NONE))) {
-        fwk_unexpected();
-        return FWK_E_ACCESS;
+	(!fwk_id_is_equal(pd_ctx->bound_id, FWK_ID_NONE))) {
+	fwk_unexpected();
+	return FWK_E_ACCESS;
     }
 
 #ifdef BUILD_HAS_MOD_POWER_DOMAIN
     is_power_domain_module = (fwk_id_get_module_idx(source_id) ==
-        FWK_MODULE_IDX_POWER_DOMAIN);
+	FWK_MODULE_IDX_POWER_DOMAIN);
     #endif
 #ifdef BUILD_HAS_MOD_SYSTEM_POWER
     is_system_power_module = (fwk_id_get_module_idx(source_id) ==
-        FWK_MODULE_IDX_SYSTEM_POWER);
+	FWK_MODULE_IDX_SYSTEM_POWER);
     #endif
 
     switch (pd_ctx->config->pd_type) {
     case MOD_PD_TYPE_CORE:
 #ifdef BUILD_HAS_MOD_POWER_DOMAIN
-        if (is_power_domain_module) {
-            *api = &core_pd_driver;
-            pd_ctx->bound_id = source_id;
-            return FWK_SUCCESS;
-        }
+	if (is_power_domain_module) {
+	    *api = &core_pd_driver;
+	    pd_ctx->bound_id = source_id;
+	    return FWK_SUCCESS;
+	}
 #endif
-        break;
+	break;
 
     case MOD_PD_TYPE_CLUSTER:
 #ifdef BUILD_HAS_MOD_POWER_DOMAIN
-        if (is_power_domain_module) {
-            *api = &cluster_pd_driver;
-            pd_ctx->bound_id = source_id;
-            return FWK_SUCCESS;
-        }
+	if (is_power_domain_module) {
+	    *api = &cluster_pd_driver;
+	    pd_ctx->bound_id = source_id;
+	    return FWK_SUCCESS;
+	}
 #endif
-        break;
+	break;
 
     case MOD_PD_TYPE_SYSTEM:
 #ifdef BUILD_HAS_MOD_POWER_DOMAIN
-        if (is_power_domain_module) {
-            *api = &pd_driver;
-            pd_ctx->bound_id = source_id;
-            return FWK_SUCCESS;
-        }
+	if (is_power_domain_module) {
+	    *api = &pd_driver;
+	    pd_ctx->bound_id = source_id;
+	    return FWK_SUCCESS;
+	}
 #endif
 #ifdef BUILD_HAS_MOD_SYSTEM_POWER
-        if (is_system_power_module) {
-            *api = &pd_driver;
-            pd_ctx->bound_id = source_id;
-            return FWK_SUCCESS;
-        }
+	if (is_system_power_module) {
+	    *api = &pd_driver;
+	    pd_ctx->bound_id = source_id;
+	    return FWK_SUCCESS;
+	}
 #endif
-        break;
+	break;
 
     default:
 #ifdef BUILD_HAS_MOD_POWER_DOMAIN
-        if (is_power_domain_module) {
-            pd_ctx->bound_id = source_id;
-        }
+	if (is_power_domain_module) {
+	    pd_ctx->bound_id = source_id;
+	}
 #endif
-        *api = &pd_driver;
-        return FWK_SUCCESS;
+	*api = &pd_driver;
+	return FWK_SUCCESS;
     }
 
     pd_ctx->bound_id = FWK_ID_NONE;
@@ -931,7 +931,7 @@ static int ppu_v1_start(fwk_id_t id)
     const struct mod_ppu_v1_config *module_config;
 
     if (!fwk_id_is_type(id, FWK_ID_TYPE_ELEMENT))
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 
     pd_ctx = ppu_v1_ctx.pd_ctx_table + fwk_id_get_element_idx(id);
     module_config = fwk_module_get_data(fwk_id_build_module_id(id));
@@ -939,21 +939,21 @@ static int ppu_v1_start(fwk_id_t id)
 
     /* Register for power domain transition notifications */
     status = fwk_notification_subscribe(
-        module_config->pd_notification_id,
-        module_config->pd_source_id,
-        id);
+	module_config->pd_notification_id,
+	module_config->pd_source_id,
+	id);
     if (status != FWK_SUCCESS)
-        return status;
+	return status;
 
     switch (pd_ctx->config->pd_type) {
     case MOD_PD_TYPE_CORE:
     case MOD_PD_TYPE_CLUSTER:
-        fwk_interrupt_clear_pending(pd_ctx->config->ppu.irq);
-        fwk_interrupt_enable(pd_ctx->config->ppu.irq);
-        break;
+	fwk_interrupt_clear_pending(pd_ctx->config->ppu.irq);
+	fwk_interrupt_enable(pd_ctx->config->ppu.irq);
+	break;
     default:
-        /* Nothing to be done for other types */
-        break;
+	/* Nothing to be done for other types */
+	break;
     }
 
     return FWK_SUCCESS;
@@ -969,31 +969,31 @@ static int ppu_v1_process_notification(
 
     fwk_assert(fwk_id_is_type(event->target_id, FWK_ID_TYPE_ELEMENT));
     module_config =
-        fwk_module_get_data(fwk_id_build_module_id(event->target_id));
+	fwk_module_get_data(fwk_id_build_module_id(event->target_id));
     assert(
-        fwk_id_is_equal(
-            event->id,
-            module_config->pd_notification_id));
+	fwk_id_is_equal(
+	    event->id,
+	    module_config->pd_notification_id));
     (void)module_config;
 
     params = (struct mod_pd_power_state_transition_notification_params *)
-        event->params;
+	event->params;
 
     if (params->state != MOD_PD_STATE_ON)
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 
     pd_ctx = ppu_v1_ctx.pd_ctx_table + fwk_id_get_element_idx(event->target_id);
 
     switch (pd_ctx->config->pd_type) {
     case MOD_PD_TYPE_CORE:
-        return ppu_v1_core_pd_init(pd_ctx);
+	return ppu_v1_core_pd_init(pd_ctx);
 
     case MOD_PD_TYPE_CLUSTER:
-        return ppu_v1_cluster_pd_init(pd_ctx);
+	return ppu_v1_cluster_pd_init(pd_ctx);
 
     default:
-        ppu_v1_init(pd_ctx->ppu);
-        return FWK_SUCCESS;
+	ppu_v1_init(pd_ctx->ppu);
+	return FWK_SUCCESS;
     }
 }
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/ppu_v1/src/ppu_v1.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/ppu_v1/src/ppu_v1.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/ppu_v1/src/ppu_v1.c
@@ -25,9 +25,9 @@ static bool ppu_v1_set_power_status_chec
     params = (struct set_power_status_check_params_v1 *)data;
 
     return (
-        (params->reg->PWSR &
-         (PPU_V1_PWSR_PWR_STATUS | PPU_V1_PWSR_PWR_DYN_STATUS)) ==
-        params->mode);
+	(params->reg->PWSR &
+	 (PPU_V1_PWSR_PWR_STATUS | PPU_V1_PWSR_PWR_DYN_STATUS)) ==
+	params->mode);
 }
 
 void ppu_v1_init(struct ppu_v1_reg *ppu)
@@ -68,28 +68,28 @@ int ppu_v1_set_power_mode(
     struct set_power_status_check_params_v1 params;
     status = ppu_v1_request_power_mode(ppu, ppu_mode);
     if (status != FWK_SUCCESS)
-        return status;
+	return status;
 
     if (timer_ctx == NULL) {
-        while ((ppu->PWSR &
-                (PPU_V1_PWSR_PWR_STATUS | PPU_V1_PWSR_PWR_DYN_STATUS)) !=
-               ppu_mode)
-            continue;
+	while ((ppu->PWSR &
+		(PPU_V1_PWSR_PWR_STATUS | PPU_V1_PWSR_PWR_DYN_STATUS)) !=
+	       ppu_mode)
+	    continue;
     } else {
-        params.mode = ppu_mode;
-        params.reg = ppu;
-        return timer_ctx->timer_api->wait(
-            timer_ctx->timer_id,
-            timer_ctx->delay_us,
-            ppu_v1_set_power_status_check,
-            &params);
+	params.mode = ppu_mode;
+	params.reg = ppu;
+	return timer_ctx->timer_api->wait(
+	    timer_ctx->timer_id,
+	    timer_ctx->delay_us,
+	    ppu_v1_set_power_status_check,
+	    &params);
     }
 
     return FWK_SUCCESS;
 }
 
 int ppu_v1_request_operating_mode(struct ppu_v1_reg *ppu,
-                                  enum ppu_v1_opmode op_mode)
+				  enum ppu_v1_opmode op_mode)
 {
     uint32_t power_policy;
     fwk_assert(ppu != NULL);
@@ -102,7 +102,7 @@ int ppu_v1_request_operating_mode(struct
 }
 
 void ppu_v1_opmode_dynamic_enable(struct ppu_v1_reg *ppu,
-                                  enum ppu_v1_opmode min_dyn_mode)
+				  enum ppu_v1_opmode min_dyn_mode)
 {
     uint32_t power_policy;
 
@@ -111,14 +111,14 @@ void ppu_v1_opmode_dynamic_enable(struct
 
     power_policy = ppu->PWPR & ~PPU_V1_PWPR_OP_POLICY;
     ppu->PWPR = power_policy |
-                PPU_V1_PWPR_OP_DYN_EN |
-                (min_dyn_mode << PPU_V1_PWPR_OP_POLICY_POS);
+		PPU_V1_PWPR_OP_DYN_EN |
+		(min_dyn_mode << PPU_V1_PWPR_OP_POLICY_POS);
     while ((ppu->PWSR & PPU_V1_PWSR_OP_DYN_STATUS) == 0)
-        continue;
+	continue;
 }
 
 void ppu_v1_dynamic_enable(struct ppu_v1_reg *ppu,
-                           enum ppu_v1_mode min_dyn_state)
+			   enum ppu_v1_mode min_dyn_state)
 {
     uint32_t power_policy;
 
@@ -128,7 +128,7 @@ void ppu_v1_dynamic_enable(struct ppu_v1
     power_policy = ppu->PWPR & ~PPU_V1_PWPR_POLICY;
     ppu->PWPR = power_policy | PPU_V1_PWPR_DYNAMIC_EN | min_dyn_state;
     while ((ppu->PWSR & PPU_V1_PWSR_PWR_DYN_STATUS) == 0)
-        continue;
+	continue;
 }
 
 void ppu_v1_lock_off_enable(struct ppu_v1_reg *ppu)
@@ -164,7 +164,7 @@ enum ppu_v1_opmode ppu_v1_get_operating_
     fwk_assert(ppu != NULL);
 
     return (enum ppu_v1_opmode)
-        ((ppu->PWSR & PPU_V1_PWSR_OP_STATUS) >> PPU_V1_PWSR_OP_STATUS_POS);
+	((ppu->PWSR & PPU_V1_PWSR_OP_STATUS) >> PPU_V1_PWSR_OP_STATUS_POS);
 }
 
 enum ppu_v1_opmode ppu_v1_get_programmed_operating_mode(struct ppu_v1_reg *ppu)
@@ -172,7 +172,7 @@ enum ppu_v1_opmode ppu_v1_get_programmed
     fwk_assert(ppu != NULL);
 
     return (enum ppu_v1_opmode)
-        ((ppu->PWPR & PPU_V1_PWPR_OP_POLICY) >> PPU_V1_PWPR_OP_POLICY_POS);
+	((ppu->PWPR & PPU_V1_PWPR_OP_POLICY) >> PPU_V1_PWPR_OP_POLICY_POS);
 }
 
 bool ppu_v1_is_dynamic_enabled(struct ppu_v1_reg *ppu)
@@ -193,21 +193,21 @@ bool ppu_v1_is_locked(struct ppu_v1_reg
  * DISR register
  */
 bool ppu_v1_is_power_devactive_high(struct ppu_v1_reg *ppu,
-                                    enum ppu_v1_mode ppu_mode)
+				    enum ppu_v1_mode ppu_mode)
 {
     fwk_assert(ppu != NULL);
 
     return (ppu->DISR &
-            (1 << (ppu_mode + PPU_V1_DISR_PWR_DEVACTIVE_STATUS_POS))) != 0;
+	    (1 << (ppu_mode + PPU_V1_DISR_PWR_DEVACTIVE_STATUS_POS))) != 0;
 }
 
 bool ppu_v1_is_op_devactive_high(struct ppu_v1_reg *ppu,
-                                 enum ppu_v1_op_devactive op_devactive)
+				 enum ppu_v1_op_devactive op_devactive)
 {
     fwk_assert(ppu != NULL);
 
     return (ppu->DISR &
-            (1 << (op_devactive + PPU_V1_DISR_OP_DEVACTIVE_STATUS_POS))) != 0;
+	    (1 << (op_devactive + PPU_V1_DISR_OP_DEVACTIVE_STATUS_POS))) != 0;
 }
 
 /*
@@ -298,11 +298,11 @@ void ppu_v1_set_input_edge_sensitivity(s
 
     /* Clear current settings */
     ppu->IESR &= ~(PPU_V1_EDGE_SENSITIVITY_MASK <<
-                   (ppu_mode * PPU_V1_BITS_PER_EDGE_SENSITIVITY));
+		   (ppu_mode * PPU_V1_BITS_PER_EDGE_SENSITIVITY));
 
     /* Update settings */
     ppu->IESR |= edge_sensitivity <<
-                 (ppu_mode * PPU_V1_BITS_PER_EDGE_SENSITIVITY);
+		 (ppu_mode * PPU_V1_BITS_PER_EDGE_SENSITIVITY);
 }
 
 enum ppu_v1_edge_sensitivity ppu_v1_get_input_edge_sensitivity(
@@ -312,8 +312,8 @@ enum ppu_v1_edge_sensitivity ppu_v1_get_
     fwk_assert(ppu_mode < PPU_V1_MODE_COUNT);
 
     return (enum ppu_v1_edge_sensitivity)(
-            (ppu->IESR >> (ppu_mode * PPU_V1_BITS_PER_EDGE_SENSITIVITY)) &
-            PPU_V1_EDGE_SENSITIVITY_MASK);
+	    (ppu->IESR >> (ppu_mode * PPU_V1_BITS_PER_EDGE_SENSITIVITY)) &
+	    PPU_V1_EDGE_SENSITIVITY_MASK);
 }
 
 void ppu_v1_ack_power_active_edge_interrupt(struct ppu_v1_reg *ppu,
@@ -338,11 +338,11 @@ void ppu_v1_set_op_active_edge_sensitivi
 
     /* Clear current settings */
     ppu->OPSR &= ~(PPU_V1_EDGE_SENSITIVITY_MASK <<
-                   (op_devactive * PPU_V1_BITS_PER_EDGE_SENSITIVITY));
+		   (op_devactive * PPU_V1_BITS_PER_EDGE_SENSITIVITY));
 
     /* Update settings */
     ppu->OPSR |= edge_sensitivity <<
-                 (op_devactive * PPU_V1_BITS_PER_EDGE_SENSITIVITY);
+		 (op_devactive * PPU_V1_BITS_PER_EDGE_SENSITIVITY);
 }
 
 enum ppu_v1_edge_sensitivity ppu_v1_get_op_active_edge_sensitivity(
@@ -352,8 +352,8 @@ enum ppu_v1_edge_sensitivity ppu_v1_get_
     fwk_assert(op_devactive < PPU_V1_OP_DEVACTIVE_COUNT);
 
     return (enum ppu_v1_edge_sensitivity)(
-            (ppu->OPSR >> (op_devactive * PPU_V1_BITS_PER_EDGE_SENSITIVITY)) &
-            PPU_V1_EDGE_SENSITIVITY_MASK);
+	    (ppu->OPSR >> (op_devactive * PPU_V1_BITS_PER_EDGE_SENSITIVITY)) &
+	    PPU_V1_EDGE_SENSITIVITY_MASK);
 }
 
 void ppu_v1_ack_op_active_edge_interrupt(struct ppu_v1_reg *ppu,
@@ -379,7 +379,7 @@ bool ppu_v1_is_dyn_policy_min_interrupt(
 unsigned int ppu_v1_get_num_opmode(struct ppu_v1_reg *ppu)
 {
     return ((ppu->IDR0 & PPU_V1_IDR0_NUM_OPMODE)
-            >> PPU_V1_IDR0_NUM_OPMODE_POS) + 1;
+	    >> PPU_V1_IDR0_NUM_OPMODE_POS) + 1;
 }
 
 /*
@@ -390,5 +390,5 @@ unsigned int ppu_v1_get_arch_id(struct p
     fwk_assert(ppu != NULL);
 
     return (ppu->AIDR & (PPU_V1_AIDR_ARCH_REV_MINOR |
-                         PPU_V1_AIDR_ARCH_REV_MAJOR));
+			 PPU_V1_AIDR_ARCH_REV_MAJOR));
 }
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/ppu_v1/src/ppu_v1.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/ppu_v1/src/ppu_v1.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/ppu_v1/src/ppu_v1.h
@@ -27,37 +27,37 @@ struct ppu_v1_reg {
   FWK_RW  uint32_t PWPR;
   FWK_RW  uint32_t PMER;
   FWK_R   uint32_t PWSR;
-          uint32_t RESERVED0;
+	  uint32_t RESERVED0;
   FWK_R   uint32_t DISR;
   FWK_R   uint32_t MISR;
   FWK_R   uint32_t STSR;
   FWK_RW  uint32_t UNLK;
   FWK_RW  uint32_t PWCR;
   FWK_RW  uint32_t PTCR;
-          uint32_t RESERVED1[2];
+	  uint32_t RESERVED1[2];
   FWK_RW  uint32_t IMR;
   FWK_RW  uint32_t AIMR;
   FWK_RW  uint32_t ISR;
   FWK_RW  uint32_t AISR;
   FWK_RW  uint32_t IESR;
   FWK_RW  uint32_t OPSR;
-          uint32_t RESERVED2[2];
+	  uint32_t RESERVED2[2];
   FWK_RW  uint32_t FUNRR;
   FWK_RW  uint32_t FULRR;
   FWK_RW  uint32_t MEMRR;
-          uint8_t  RESERVED3[0x160 - 0x5C];
+	  uint8_t  RESERVED3[0x160 - 0x5C];
   FWK_RW  uint32_t EDTR0;
   FWK_RW  uint32_t EDTR1;
-          uint32_t RESERVED4[2];
+	  uint32_t RESERVED4[2];
   FWK_RW  uint32_t DCCR0;
   FWK_RW  uint32_t DCCR1;
-          uint8_t  RESERVED5[0xFB0 - 0x178];
+	  uint8_t  RESERVED5[0xFB0 - 0x178];
   FWK_R   uint32_t IDR0;
   FWK_R   uint32_t IDR1;
-          uint8_t  RESERVED6[0xFC8 - 0xFB8];
+	  uint8_t  RESERVED6[0xFC8 - 0xFB8];
   FWK_R   uint32_t IIDR;
   FWK_R   uint32_t AIDR;
-          uint8_t  RESERVED7[0x1000 - 0xFD0];
+	  uint8_t  RESERVED7[0x1000 - 0xFD0];
 };
 
 enum ppu_v1_mode {
@@ -259,25 +259,25 @@ int ppu_v1_set_power_mode(
  * Request PPU's power mode and don't wait for the transition.
  */
 int ppu_v1_request_power_mode(struct ppu_v1_reg *ppu,
-                              enum ppu_v1_mode ppu_mode);
+			      enum ppu_v1_mode ppu_mode);
 
 /*
  * Request a change to the PPU's operating mode.
  */
 int ppu_v1_request_operating_mode(struct ppu_v1_reg *ppu,
-                                  enum ppu_v1_opmode op_mode);
+				  enum ppu_v1_opmode op_mode);
 
 /*
  * Enable PPU's dynamic operating mode transitions
  */
 void ppu_v1_opmode_dynamic_enable(struct ppu_v1_reg *ppu,
-                                  enum ppu_v1_opmode min_dyn_mode);
+				  enum ppu_v1_opmode min_dyn_mode);
 
 /*
  * Enable PPU's dynamic power mode transitions
  */
 void ppu_v1_dynamic_enable(struct ppu_v1_reg *ppu,
-                           enum ppu_v1_mode min_dyn_state);
+			   enum ppu_v1_mode min_dyn_state);
 
 /*
  * Enable the lock in the OFF state
@@ -323,13 +323,13 @@ bool ppu_v1_is_locked(struct ppu_v1_reg
  * Check if the DEVACTIVE signal associated to a power mode is high.
  */
 bool ppu_v1_is_power_devactive_high(struct ppu_v1_reg *ppu,
-                                    enum ppu_v1_mode ppu_mode);
+				    enum ppu_v1_mode ppu_mode);
 
 /*
  * Check if the DEVACTIVE signal associated to an operating mode is high.
  */
 bool ppu_v1_is_op_devactive_high(struct ppu_v1_reg *ppu,
-                                 enum ppu_v1_op_devactive op_devactive);
+				 enum ppu_v1_op_devactive op_devactive);
 
 /*
  * Unlock the power domain from the OFF power mode.
@@ -403,13 +403,13 @@ enum ppu_v1_edge_sensitivity ppu_v1_get_
  * Acknowledge a power active edge interrupt.
  */
 void ppu_v1_ack_power_active_edge_interrupt(struct ppu_v1_reg *ppu,
-                                            enum ppu_v1_mode ppu_mode);
+					    enum ppu_v1_mode ppu_mode);
 
 /*
  * Check if a power active edge interrupt is pending.
  */
 bool ppu_v1_is_power_active_edge_interrupt(struct ppu_v1_reg *ppu,
-                                           enum ppu_v1_mode ppu_mode);
+					   enum ppu_v1_mode ppu_mode);
 
 /*
  * Set operating mode active edge sensitivity. See
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/psu/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/psu/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/psu/CMakeLists.txt
@@ -8,7 +8,7 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_psu.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_psu.c")
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/psu/include/mod_psu.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/psu/include/mod_psu.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/psu/include/mod_psu.h
@@ -208,21 +208,21 @@ struct mod_psu_response {
      *      event identifier of the response.
      */
     union {
-        /*!
-         * \brief `true` if the device is enabled, otherwise `false`.
-         *
-         * \warning Used only in response to a ::mod_psu_device_api::get_enabled
-         *      call.
-         */
-        bool enabled;
-
-        /*!
-         * \brief Voltage in millivolts (mV).
-         *
-         * \warning Used only in response to a ::mod_psu_device_api::get_voltage
-         *      call.
-         */
-        uint32_t voltage;
+	/*!
+	 * \brief `true` if the device is enabled, otherwise `false`.
+	 *
+	 * \warning Used only in response to a ::mod_psu_device_api::get_enabled
+	 *      call.
+	 */
+	bool enabled;
+
+	/*!
+	 * \brief Voltage in millivolts (mV).
+	 *
+	 * \warning Used only in response to a ::mod_psu_device_api::get_voltage
+	 *      call.
+	 */
+	uint32_t voltage;
     };
 };
 
@@ -352,21 +352,21 @@ struct mod_psu_driver_response {
      *      event type of the response.
      */
     union {
-        /*!
-         * \brief `true` if the device is enabled, otherwise `false`.
-         *
-         * \warning Used only in response to a
-         *      ::mod_psu_driver_api::get_enabled call.
-         */
-        bool enabled;
-
-        /*!
-         * \brief Voltage in millivolts (mV).
-         *
-         * \warning Used only in response to a
-         *      ::mod_psu_driver_api::get_voltage call.
-         */
-        uint32_t voltage;
+	/*!
+	 * \brief `true` if the device is enabled, otherwise `false`.
+	 *
+	 * \warning Used only in response to a
+	 *      ::mod_psu_driver_api::get_enabled call.
+	 */
+	bool enabled;
+
+	/*!
+	 * \brief Voltage in millivolts (mV).
+	 *
+	 * \warning Used only in response to a
+	 *      ::mod_psu_driver_api::get_voltage call.
+	 */
+	uint32_t voltage;
     };
 };
 
@@ -385,8 +385,8 @@ struct mod_psu_driver_response_api {
      * \param response Driver operation response.
      */
     void (*respond)(
-        fwk_id_t element_id,
-        struct mod_psu_driver_response response);
+	fwk_id_t element_id,
+	struct mod_psu_driver_response response);
 };
 
 /*!
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/psu/src/mod_psu.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/psu/src/mod_psu.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/psu/src/mod_psu.c
@@ -39,9 +39,9 @@ static const fwk_id_t mod_psu_impl_event
 
 static struct mod_psu_mod_ctx {
     struct mod_psu_element_ctx {
-        const struct mod_psu_driver_api *driver;
+	const struct mod_psu_driver_api *driver;
 
-        struct mod_psu_operation op;
+	struct mod_psu_operation op;
     } *elements;
 } mod_psu_ctx;
 
@@ -58,15 +58,15 @@ static int mod_psu_get_cfg_ctx(
     struct mod_psu_element_ctx **ctx)
 {
     if (fwk_id_get_module_idx(element_id) != FWK_MODULE_IDX_PSU) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     if (ctx != NULL) {
-        *ctx = mod_psu_get_element_ctx(element_id);
+	*ctx = mod_psu_get_element_ctx(element_id);
     }
 
     if (cfg != NULL) {
-        *cfg = fwk_module_get_data(element_id);
+	*cfg = fwk_module_get_data(element_id);
     }
 
     return FWK_SUCCESS;
@@ -81,34 +81,34 @@ static int mod_psu_get_enabled(fwk_id_t
 
     status = mod_psu_get_cfg_ctx(element_id, &cfg, &ctx);
     if (status != FWK_SUCCESS) {
-        goto exit;
+	goto exit;
     }
 
     if (ctx->op.state != MOD_PSU_STATE_IDLE) {
-        status = FWK_E_BUSY;
+	status = FWK_E_BUSY;
 
-        goto exit;
+	goto exit;
     }
 
     status = ctx->driver->get_enabled(cfg->driver_id, enabled);
     if (status == FWK_PENDING) {
-        struct fwk_event_light request = {
-            .id = mod_psu_event_id_get_enabled,
-            .target_id = element_id,
-
-            .response_requested = true,
-        };
-
-        status = fwk_put_event(&request);
-        if (status == FWK_SUCCESS) {
-            ctx->op.state = MOD_PSU_STATE_BUSY;
-
-            status = FWK_PENDING;
-        } else {
-            status = FWK_E_STATE;
-        }
+	struct fwk_event_light request = {
+	    .id = mod_psu_event_id_get_enabled,
+	    .target_id = element_id,
+
+	    .response_requested = true,
+	};
+
+	status = fwk_put_event(&request);
+	if (status == FWK_SUCCESS) {
+	    ctx->op.state = MOD_PSU_STATE_BUSY;
+
+	    status = FWK_PENDING;
+	} else {
+	    status = FWK_E_STATE;
+	}
     } else if (status != FWK_SUCCESS) {
-        status = FWK_E_HANDLER;
+	status = FWK_E_HANDLER;
     }
 
 exit:
@@ -124,34 +124,34 @@ static int mod_psu_set_enabled(fwk_id_t
 
     status = mod_psu_get_cfg_ctx(element_id, &cfg, &ctx);
     if (status != FWK_SUCCESS) {
-        goto exit;
+	goto exit;
     }
 
     if (ctx->op.state != MOD_PSU_STATE_IDLE) {
-        status = FWK_E_BUSY;
+	status = FWK_E_BUSY;
 
-        goto exit;
+	goto exit;
     }
 
     status = ctx->driver->set_enabled(cfg->driver_id, enabled);
     if (status == FWK_PENDING) {
-        struct fwk_event_light request = {
-            .id = mod_psu_event_id_set_enabled,
-            .target_id = element_id,
-
-            .response_requested = true,
-        };
-
-        status = fwk_put_event(&request);
-        if (status == FWK_SUCCESS) {
-            ctx->op.state = MOD_PSU_STATE_BUSY;
-
-            status = FWK_PENDING;
-        } else {
-            status = FWK_E_STATE;
-        }
+	struct fwk_event_light request = {
+	    .id = mod_psu_event_id_set_enabled,
+	    .target_id = element_id,
+
+	    .response_requested = true,
+	};
+
+	status = fwk_put_event(&request);
+	if (status == FWK_SUCCESS) {
+	    ctx->op.state = MOD_PSU_STATE_BUSY;
+
+	    status = FWK_PENDING;
+	} else {
+	    status = FWK_E_STATE;
+	}
     } else if (status != FWK_SUCCESS) {
-        status = FWK_E_HANDLER;
+	status = FWK_E_HANDLER;
     }
 
 exit:
@@ -167,34 +167,34 @@ static int mod_psu_get_voltage(fwk_id_t
 
     status = mod_psu_get_cfg_ctx(element_id, &cfg, &ctx);
     if (status != FWK_SUCCESS) {
-        goto exit;
+	goto exit;
     }
 
     if (ctx->op.state != MOD_PSU_STATE_IDLE) {
-        status = FWK_E_BUSY;
+	status = FWK_E_BUSY;
 
-        goto exit;
+	goto exit;
     }
 
     status = ctx->driver->get_voltage(cfg->driver_id, voltage);
     if (status == FWK_PENDING) {
-        struct fwk_event_light request = {
-            .id = mod_psu_event_id_get_voltage,
-            .target_id = element_id,
-
-            .response_requested = true,
-        };
-
-        status = fwk_put_event(&request);
-        if (status == FWK_SUCCESS) {
-            ctx->op.state = MOD_PSU_STATE_BUSY;
-
-            status = FWK_PENDING;
-        } else {
-            status = FWK_E_STATE;
-        }
+	struct fwk_event_light request = {
+	    .id = mod_psu_event_id_get_voltage,
+	    .target_id = element_id,
+
+	    .response_requested = true,
+	};
+
+	status = fwk_put_event(&request);
+	if (status == FWK_SUCCESS) {
+	    ctx->op.state = MOD_PSU_STATE_BUSY;
+
+	    status = FWK_PENDING;
+	} else {
+	    status = FWK_E_STATE;
+	}
     } else if (status != FWK_SUCCESS) {
-        status = FWK_E_HANDLER;
+	status = FWK_E_HANDLER;
     }
 
 exit:
@@ -210,34 +210,34 @@ static int mod_psu_set_voltage(fwk_id_t
 
     status = mod_psu_get_cfg_ctx(element_id, &cfg, &ctx);
     if (status != FWK_SUCCESS) {
-        goto exit;
+	goto exit;
     }
 
     if (ctx->op.state != MOD_PSU_STATE_IDLE) {
-        status = FWK_E_BUSY;
+	status = FWK_E_BUSY;
 
-        goto exit;
+	goto exit;
     }
 
     status = ctx->driver->set_voltage(cfg->driver_id, voltage);
     if (status == FWK_PENDING) {
-        struct fwk_event_light request = {
-            .id = mod_psu_event_id_set_voltage,
-            .target_id = element_id,
-
-            .response_requested = true,
-        };
-
-        status = fwk_put_event(&request);
-        if (status == FWK_SUCCESS) {
-            ctx->op.state = MOD_PSU_STATE_BUSY;
-
-            status = FWK_PENDING;
-        } else {
-            status = FWK_E_STATE;
-        }
+	struct fwk_event_light request = {
+	    .id = mod_psu_event_id_set_voltage,
+	    .target_id = element_id,
+
+	    .response_requested = true,
+	};
+
+	status = fwk_put_event(&request);
+	if (status == FWK_SUCCESS) {
+	    ctx->op.state = MOD_PSU_STATE_BUSY;
+
+	    status = FWK_PENDING;
+	} else {
+	    status = FWK_E_STATE;
+	}
     } else if (status != FWK_SUCCESS) {
-        status = FWK_E_HANDLER;
+	status = FWK_E_HANDLER;
     }
 
 exit:
@@ -264,21 +264,21 @@ static void mod_psu_respond(
 
     status = mod_psu_get_cfg_ctx(element_id, &cfg, &ctx);
     if (!fwk_expect(status == FWK_SUCCESS)) {
-        return;
+	return;
     }
 
     event = (struct fwk_event){
-        .id = mod_psu_impl_event_id_response,
+	.id = mod_psu_impl_event_id_response,
 
-        .source_id = cfg->driver_id,
-        .target_id = element_id,
+	.source_id = cfg->driver_id,
+	.target_id = element_id,
     };
 
     fwk_str_memcpy(event.params, &response, sizeof(response));
 
     status = fwk_put_event(&event);
     if (!fwk_expect(status == FWK_SUCCESS)) {
-        ctx->op.state = MOD_PSU_STATE_IDLE;
+	ctx->op.state = MOD_PSU_STATE_IDLE;
     }
 }
 
@@ -294,7 +294,7 @@ static int mod_psu_init(
     fwk_check(data == NULL);
 
     mod_psu_ctx.elements =
-        fwk_mm_calloc(element_count, sizeof(mod_psu_ctx.elements[0]));
+	fwk_mm_calloc(element_count, sizeof(mod_psu_ctx.elements[0]));
 
     return FWK_SUCCESS;
 }
@@ -311,9 +311,9 @@ static int mod_psu_element_init(
     ctx = mod_psu_get_element_ctx(element_id);
 
     *ctx = (struct mod_psu_element_ctx){
-        .op = (struct mod_psu_operation){
-            .state = MOD_PSU_STATE_IDLE,
-        },
+	.op = (struct mod_psu_operation){
+	    .state = MOD_PSU_STATE_IDLE,
+	},
     };
 
     return FWK_SUCCESS;
@@ -327,7 +327,7 @@ static int mod_psu_bind_element(fwk_id_t
     const struct mod_psu_element_cfg *cfg;
 
     if (round > 0) {
-        goto exit;
+	goto exit;
     }
 
     ctx = mod_psu_get_element_ctx(element_id);
@@ -335,14 +335,14 @@ static int mod_psu_bind_element(fwk_id_t
 
     status = fwk_module_bind(cfg->driver_id, cfg->driver_api_id, &ctx->driver);
     if (status != FWK_SUCCESS) {
-        goto exit;
+	goto exit;
     }
 
     if ((ctx->driver->set_enabled == NULL) ||
-        (ctx->driver->get_enabled == NULL) ||
-        (ctx->driver->set_voltage == NULL) ||
-        (ctx->driver->get_voltage == NULL)) {
-        status = FWK_E_PANIC;
+	(ctx->driver->get_enabled == NULL) ||
+	(ctx->driver->set_voltage == NULL) ||
+	(ctx->driver->get_voltage == NULL)) {
+	status = FWK_E_PANIC;
     }
 
 exit:
@@ -352,7 +352,7 @@ exit:
 static int mod_psu_bind(fwk_id_t id, unsigned int round)
 {
     if (fwk_id_is_type(id, FWK_ID_TYPE_ELEMENT)) {
-        return mod_psu_bind_element(id, round);
+	return mod_psu_bind_element(id, round);
     }
 
     return FWK_SUCCESS;
@@ -367,24 +367,24 @@ static int mod_psu_process_bind_request(
     enum mod_psu_api_idx event_id_type;
 
     if (!fwk_id_is_type(target_id, FWK_ID_TYPE_ELEMENT)) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     event_id_type = (enum mod_psu_api_idx)fwk_id_get_api_idx(api_id);
 
     switch (event_id_type) {
     case MOD_PSU_API_IDX_DEVICE:
-        *api = &psu_device_api;
+	*api = &psu_device_api;
 
-        break;
+	break;
 
     case MOD_PSU_API_IDX_DRIVER_RESPONSE:
-        *api = &psu_driver_response_api;
+	*api = &psu_driver_response_api;
 
-        break;
+	break;
 
     default:
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     return FWK_SUCCESS;
@@ -401,19 +401,19 @@ static int mod_psu_process_event(
     enum mod_psu_event_idx hal_event_id_type;
 
     const struct mod_psu_driver_response *params =
-        (struct mod_psu_driver_response *)event->params;
+	(struct mod_psu_driver_response *)event->params;
     struct mod_psu_response *resp_params =
-        (struct mod_psu_response *)resp_event->params;
+	(struct mod_psu_response *)resp_event->params;
     struct mod_psu_response *hal_params =
-        (struct mod_psu_response *)&hal_event.params;
+	(struct mod_psu_response *)&hal_event.params;
 
     const struct mod_psu_element_cfg *cfg;
     struct mod_psu_element_ctx *ctx;
 
     resp_params->status =
-        mod_psu_get_cfg_ctx(event->target_id, &cfg, &ctx);
+	mod_psu_get_cfg_ctx(event->target_id, &cfg, &ctx);
     if (resp_params->status != FWK_SUCCESS) {
-        goto exit;
+	goto exit;
     }
 
     switch (fwk_id_get_event_idx(event->id)) {
@@ -421,49 +421,49 @@ static int mod_psu_process_event(
     case (unsigned int)MOD_PSU_EVENT_IDX_GET_VOLTAGE:
     case (unsigned int)MOD_PSU_EVENT_IDX_SET_ENABLED:
     case (unsigned int)MOD_PSU_EVENT_IDX_SET_VOLTAGE:
-        ctx->op.cookie = event->cookie;
+	ctx->op.cookie = event->cookie;
 
-        resp_event->is_delayed_response = true;
+	resp_event->is_delayed_response = true;
 
-        break;
+	break;
 
     case (unsigned int)MOD_PSU_IMPL_EVENT_IDX_RESPONSE:
-        ctx->op.state = MOD_PSU_STATE_IDLE;
+	ctx->op.state = MOD_PSU_STATE_IDLE;
 
-        status = fwk_get_delayed_response(
-            event->target_id, ctx->op.cookie, &hal_event);
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
+	status = fwk_get_delayed_response(
+	    event->target_id, ctx->op.cookie, &hal_event);
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
 
-        *hal_params = (struct mod_psu_response){
-            .status = params->status,
-        };
+	*hal_params = (struct mod_psu_response){
+	    .status = params->status,
+	};
 
-        hal_event_id_type =
-            (enum mod_psu_event_idx)fwk_id_get_event_idx(hal_event.id);
+	hal_event_id_type =
+	    (enum mod_psu_event_idx)fwk_id_get_event_idx(hal_event.id);
 
-        switch (hal_event_id_type) {
-        case MOD_PSU_EVENT_IDX_GET_ENABLED:
-            hal_params->enabled = params->enabled;
+	switch (hal_event_id_type) {
+	case MOD_PSU_EVENT_IDX_GET_ENABLED:
+	    hal_params->enabled = params->enabled;
 
-            break;
+	    break;
 
-        case MOD_PSU_EVENT_IDX_GET_VOLTAGE:
-            hal_params->voltage = params->voltage;
+	case MOD_PSU_EVENT_IDX_GET_VOLTAGE:
+	    hal_params->voltage = params->voltage;
 
-            break;
+	    break;
 
-        default:
-            break;
-        }
+	default:
+	    break;
+	}
 
-        status = fwk_put_event(&hal_event);
+	status = fwk_put_event(&hal_event);
 
-        break;
+	break;
 
     default:
-        fwk_unreachable();
+	fwk_unreachable();
     }
 
 exit:
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/reg_sensor/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/reg_sensor/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/reg_sensor/CMakeLists.txt
@@ -8,9 +8,9 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_reg_sensor.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_reg_sensor.c")
 
 target_link_libraries(${SCP_MODULE_TARGET} PRIVATE module-sensor)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/reg_sensor/src/mod_reg_sensor.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/reg_sensor/src/mod_reg_sensor.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/reg_sensor/src/mod_reg_sensor.c
@@ -30,7 +30,7 @@ static int get_value(fwk_id_t id, mod_se
     fwk_assert(config != NULL);
 
     if (value == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     *value = *(mod_sensor_value_t *)config->reg;
@@ -46,7 +46,7 @@ static int get_info(fwk_id_t id, struct
     fwk_assert(config != NULL);
 
     if (info == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     *info = *(config->info);
@@ -63,8 +63,8 @@ static const struct mod_sensor_driver_ap
  * Framework handlers
  */
 static int reg_sensor_init(fwk_id_t module_id,
-                           unsigned int element_count,
-                           const void *unused)
+			   unsigned int element_count,
+			   const void *unused)
 {
     config_table = fwk_mm_alloc(element_count, sizeof(*config_table));
 
@@ -72,14 +72,14 @@ static int reg_sensor_init(fwk_id_t modu
 }
 
 static int reg_sensor_element_init(fwk_id_t element_id,
-                                   unsigned int sub_element_count,
-                                   const void *data)
+				   unsigned int sub_element_count,
+				   const void *data)
 {
     struct mod_reg_sensor_dev_config *config =
-        (struct mod_reg_sensor_dev_config *)data;
+	(struct mod_reg_sensor_dev_config *)data;
 
     if (config->reg == 0) {
-        return FWK_E_DATA;
+	return FWK_E_DATA;
     }
 
     config_table[fwk_id_get_element_idx(element_id)] = config;
@@ -88,9 +88,9 @@ static int reg_sensor_element_init(fwk_i
 }
 
 static int reg_sensor_process_bind_request(fwk_id_t source_id,
-                                           fwk_id_t target_id,
-                                           fwk_id_t api_type,
-                                           const void **api)
+					   fwk_id_t target_id,
+					   fwk_id_t api_type,
+					   const void **api)
 {
     *api = &reg_sensor_api;
     return FWK_SUCCESS;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/reset_domain/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/reset_domain/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/reset_domain/CMakeLists.txt
@@ -8,7 +8,7 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_reset_domain.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_reset_domain.c")
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/reset_domain/include/mod_reset_domain.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/reset_domain/include/mod_reset_domain.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/reset_domain/include/mod_reset_domain.h
@@ -142,9 +142,9 @@ struct mod_reset_domain_api {
      * \retval ::FWK_SUCCESS or one of FWK_E_* error codes.
      */
     int (*set_reset_state)(fwk_id_t element_id,
-                           enum mod_reset_domain_mode mode,
-                           uint32_t reset_state,
-                           uintptr_t cookie);
+			   enum mod_reset_domain_mode mode,
+			   uint32_t reset_state,
+			   uintptr_t cookie);
 };
 
 /*!
@@ -165,9 +165,9 @@ struct mod_reset_domain_drv_api {
      * \retval ::FWK_SUCCESS or one of FWK_E_* error codes.
      */
     int (*set_reset_state)(fwk_id_t dev_id,
-                           enum mod_reset_domain_mode mode,
-                           uint32_t reset_state,
-                           uintptr_t cookie);
+			   enum mod_reset_domain_mode mode,
+			   uint32_t reset_state,
+			   uintptr_t cookie);
 };
 
 /*!
@@ -253,7 +253,7 @@ enum mod_reset_domain_event_idx {
  */
 static const fwk_id_t mod_reset_domain_autoreset_event_id =
     FWK_ID_EVENT_INIT(FWK_MODULE_IDX_RESET_DOMAIN,
-                      MOD_RESET_DOMAIN_EVENT_AUTORESET);
+		      MOD_RESET_DOMAIN_EVENT_AUTORESET);
 
 /*!
  * \}
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/reset_domain/src/mod_reset_domain.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/reset_domain/src/mod_reset_domain.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/reset_domain/src/mod_reset_domain.c
@@ -40,9 +40,9 @@ static struct mod_rd_ctx module_reset_ct
  * API functions
  */
 static int set_reset_state(fwk_id_t reset_dev_id,
-                           enum mod_reset_domain_mode mode,
-                           uint32_t reset_state,
-                           uintptr_t cookie)
+			   enum mod_reset_domain_mode mode,
+			   uint32_t reset_state,
+			   uintptr_t cookie)
 {
     struct rd_dev_ctx *reset_ctx;
     unsigned int reset_domain_idx = fwk_id_get_element_idx(reset_dev_id);
@@ -50,7 +50,7 @@ static int set_reset_state(fwk_id_t rese
     reset_ctx = &module_reset_ctx.dev_ctx_table[reset_domain_idx];
 
     return reset_ctx->driver_api->set_reset_state(reset_ctx->config->driver_id,
-                                                  mode, reset_state, cookie);
+						  mode, reset_state, cookie);
 }
 
 /* HAL API */
@@ -60,33 +60,33 @@ static const struct mod_reset_domain_api
 
 #ifdef BUILD_HAS_NOTIFICATION
 static int reset_issued_notify(fwk_id_t dev_id,
-                               uint32_t reset_state,
-                               uintptr_t cookie)
+			       uint32_t reset_state,
+			       uintptr_t cookie)
 {
     int domain_id = -1;
     unsigned int i;
     struct rd_dev_ctx *reset_ctx;
     unsigned int notification_count;
     struct fwk_event notification_event = {
-        .id = module_reset_ctx.config->notification_id,
-        .source_id = fwk_module_id_reset_domain,
+	.id = module_reset_ctx.config->notification_id,
+	.source_id = fwk_module_id_reset_domain,
     };
 
     struct mod_reset_domain_notification_event_params* params =
-        (struct mod_reset_domain_notification_event_params*)
-        notification_event.params;
+	(struct mod_reset_domain_notification_event_params*)
+	notification_event.params;
 
     /* Loop through device context table to get the associated domain_id */
     for (i = 0; i < module_reset_ctx.dev_count; i++) {
-        reset_ctx = &module_reset_ctx.dev_ctx_table[i];
-        if (fwk_id_is_equal(reset_ctx->config->driver_id, dev_id)) {
-            domain_id = (int)i;
-            break;
-        }
+	reset_ctx = &module_reset_ctx.dev_ctx_table[i];
+	if (fwk_id_is_equal(reset_ctx->config->driver_id, dev_id)) {
+	    domain_id = (int)i;
+	    break;
+	}
     }
 
     if (domain_id < 0)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     params->domain_id = (uint32_t)domain_id;
     params->reset_state = reset_state;
@@ -102,16 +102,16 @@ static int rd_process_event(
 {
 #ifdef BUILD_HAS_NOTIFICATION
     struct mod_reset_domain_autoreset_event_params* params =
-        (struct mod_reset_domain_autoreset_event_params*)event->params;
+	(struct mod_reset_domain_autoreset_event_params*)event->params;
 #endif
 
     if (!fwk_id_is_equal(mod_reset_domain_autoreset_event_id,
-                         event->id))
-        return FWK_E_SUPPORT;
+			 event->id))
+	return FWK_E_SUPPORT;
 
 #ifdef BUILD_HAS_NOTIFICATION
     return reset_issued_notify(params->dev_id,
-                               params->reset_state, params->cookie);
+			       params->reset_state, params->cookie);
 #else
     return FWK_SUCCESS;
 #endif
@@ -121,25 +121,25 @@ static int rd_process_event(
  * Framework handlers
  */
 static int rd_init(fwk_id_t module_id,
-                   unsigned int dev_count,
-                   const void *data)
+		   unsigned int dev_count,
+		   const void *data)
 {
     module_reset_ctx.config = (struct mod_reset_domain_config *)data;
     module_reset_ctx.dev_ctx_table = fwk_mm_calloc(dev_count,
-                                             sizeof(struct rd_dev_ctx));
+					     sizeof(struct rd_dev_ctx));
     module_reset_ctx.dev_count = dev_count;
 
     return FWK_SUCCESS;
 }
 
 static int rd_element_init(fwk_id_t element_id,
-                           unsigned int sub_dev_count,
-                           const void *data)
+			   unsigned int sub_dev_count,
+			   const void *data)
 {
     struct rd_dev_ctx *reset_ctx = NULL;
 
     reset_ctx = &module_reset_ctx.dev_ctx_table[
-        fwk_id_get_element_idx(element_id)];
+	fwk_id_get_element_idx(element_id)];
 
     reset_ctx->config = (const struct mod_reset_domain_dev_config *)data;
 
@@ -152,33 +152,33 @@ static int rd_bind(fwk_id_t id, unsigned
 
     /* Nothing to do during subsequent round of calls */
     if (round != 0)
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 
     if (!fwk_id_is_type(id, FWK_ID_TYPE_ELEMENT))
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 
     reset_ctx = module_reset_ctx.dev_ctx_table + fwk_id_get_element_idx(id);
 
     return fwk_module_bind(reset_ctx->config->driver_id,
-                           reset_ctx->config->driver_api_id,
-                           &reset_ctx->driver_api);
+			   reset_ctx->config->driver_api_id,
+			   &reset_ctx->driver_api);
 }
 
 static int rd_process_bind_request(fwk_id_t source_id,
-                                   fwk_id_t target_id,
-                                   fwk_id_t api_id,
-                                   const void **api)
+				   fwk_id_t target_id,
+				   fwk_id_t api_id,
+				   const void **api)
 {
     enum mod_reset_domain_api_type api_id_type =
-        (enum mod_reset_domain_api_type)fwk_id_get_api_idx(api_id);
+	(enum mod_reset_domain_api_type)fwk_id_get_api_idx(api_id);
 
     switch (api_id_type) {
     case MOD_RESET_DOMAIN_API_TYPE_HAL:
-        *api = &reset_api;
-        break;
+	*api = &reset_api;
+	break;
 
     default:
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 
     return FWK_SUCCESS;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/resource_perms/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/resource_perms/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/resource_perms/CMakeLists.txt
@@ -8,9 +8,9 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_resource_perms.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_resource_perms.c")
 
 target_link_libraries(${SCP_MODULE_TARGET} PUBLIC module-scmi)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/resource_perms/include/mod_resource_perms.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/resource_perms/include/mod_resource_perms.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/resource_perms/include/mod_resource_perms.h
@@ -53,24 +53,24 @@ enum mod_res_perms_permissions {
 enum mod_res_perms_protocol_deny {
     MOD_RES_PERMS_SCMI_ALL_PROTOCOLS_ALLOWED = 0,
     MOD_RES_PERMS_SCMI_BASE_PROTOCOL_DENIED = MOD_RES_PERMS_ACCESS_DENIED
-        << (MOD_SCMI_PROTOCOL_ID_BASE - MOD_RES_PERMS_PROTOCOL_OFFSET),
+	<< (MOD_SCMI_PROTOCOL_ID_BASE - MOD_RES_PERMS_PROTOCOL_OFFSET),
     MOD_RES_PERMS_SCMI_CLOCK_PROTOCOL_DENIED = MOD_RES_PERMS_ACCESS_DENIED
-        << (MOD_SCMI_PROTOCOL_ID_CLOCK - MOD_RES_PERMS_PROTOCOL_OFFSET),
+	<< (MOD_SCMI_PROTOCOL_ID_CLOCK - MOD_RES_PERMS_PROTOCOL_OFFSET),
     MOD_RES_PERMS_SCMI_SYS_POWER_PROTOCOL_DENIED = MOD_RES_PERMS_ACCESS_DENIED
-        << (MOD_SCMI_PROTOCOL_ID_SYS_POWER - MOD_RES_PERMS_PROTOCOL_OFFSET),
+	<< (MOD_SCMI_PROTOCOL_ID_SYS_POWER - MOD_RES_PERMS_PROTOCOL_OFFSET),
     MOD_RES_PERMS_SCMI_POWER_DOMAIN_DENIED = MOD_RES_PERMS_ACCESS_DENIED
-        << (MOD_SCMI_PROTOCOL_ID_POWER_DOMAIN - MOD_RES_PERMS_PROTOCOL_OFFSET),
+	<< (MOD_SCMI_PROTOCOL_ID_POWER_DOMAIN - MOD_RES_PERMS_PROTOCOL_OFFSET),
     MOD_RES_PERMS_SCMI_PERF_PROTOCOL_DENIED = MOD_RES_PERMS_ACCESS_DENIED
-        << (MOD_SCMI_PROTOCOL_ID_PERF - MOD_RES_PERMS_PROTOCOL_OFFSET),
+	<< (MOD_SCMI_PROTOCOL_ID_PERF - MOD_RES_PERMS_PROTOCOL_OFFSET),
     MOD_RES_PERMS_SCMI_SENSOR_PROTOCOL_DENIED = MOD_RES_PERMS_ACCESS_DENIED
-        << (MOD_SCMI_PROTOCOL_ID_SENSOR - MOD_RES_PERMS_PROTOCOL_OFFSET),
+	<< (MOD_SCMI_PROTOCOL_ID_SENSOR - MOD_RES_PERMS_PROTOCOL_OFFSET),
     MOD_RES_PERMS_SCMI_RESET_DOMAIN_PROTOCOL_DENIED =
-        MOD_RES_PERMS_ACCESS_DENIED
-        << (MOD_SCMI_PROTOCOL_ID_RESET_DOMAIN - MOD_RES_PERMS_PROTOCOL_OFFSET),
+	MOD_RES_PERMS_ACCESS_DENIED
+	<< (MOD_SCMI_PROTOCOL_ID_RESET_DOMAIN - MOD_RES_PERMS_PROTOCOL_OFFSET),
     MOD_RES_PERMS_SCMI_VOLTAGE_DOMAIN_PROTOCOL_DENIED =
-        MOD_RES_PERMS_ACCESS_DENIED
-        << (MOD_SCMI_PROTOCOL_ID_VOLTAGE_DOMAIN -
-            MOD_RES_PERMS_PROTOCOL_OFFSET),
+	MOD_RES_PERMS_ACCESS_DENIED
+	<< (MOD_SCMI_PROTOCOL_ID_VOLTAGE_DOMAIN -
+	    MOD_RES_PERMS_PROTOCOL_OFFSET),
 };
 
 /*!
@@ -93,21 +93,21 @@ struct mod_res_agent_protocol_permission
 
 enum mod_res_perms_message_idx {
     MOD_RES_PERMS_SCMI_BASE_MESSAGE_IDX =
-        MOD_SCMI_PROTOCOL_ID_BASE - MOD_RES_PERMS_MESSAGE_OFFSET,
+	MOD_SCMI_PROTOCOL_ID_BASE - MOD_RES_PERMS_MESSAGE_OFFSET,
     MOD_RES_PERMS_SCMI_CLOCK_MESSAGE_IDX =
-        MOD_SCMI_PROTOCOL_ID_CLOCK - MOD_RES_PERMS_MESSAGE_OFFSET,
+	MOD_SCMI_PROTOCOL_ID_CLOCK - MOD_RES_PERMS_MESSAGE_OFFSET,
     MOD_RES_PERMS_SCMI_SYS_POWER_MESSAGE_IDX =
-        MOD_SCMI_PROTOCOL_ID_SYS_POWER - MOD_RES_PERMS_MESSAGE_OFFSET,
+	MOD_SCMI_PROTOCOL_ID_SYS_POWER - MOD_RES_PERMS_MESSAGE_OFFSET,
     MOD_RES_PERMS_SCMI_POWER_DOMAIN_MESSAGE_IDX =
-        MOD_SCMI_PROTOCOL_ID_POWER_DOMAIN - MOD_RES_PERMS_MESSAGE_OFFSET,
+	MOD_SCMI_PROTOCOL_ID_POWER_DOMAIN - MOD_RES_PERMS_MESSAGE_OFFSET,
     MOD_RES_PERMS_SCMI_PERF_MESSAGE_IDX =
-        MOD_SCMI_PROTOCOL_ID_PERF - MOD_RES_PERMS_MESSAGE_OFFSET,
+	MOD_SCMI_PROTOCOL_ID_PERF - MOD_RES_PERMS_MESSAGE_OFFSET,
     MOD_RES_PERMS_SCMI_SENSOR_MESSAGE_IDX =
-        MOD_SCMI_PROTOCOL_ID_SENSOR - MOD_RES_PERMS_MESSAGE_OFFSET,
+	MOD_SCMI_PROTOCOL_ID_SENSOR - MOD_RES_PERMS_MESSAGE_OFFSET,
     MOD_RES_PERMS_SCMI_RESET_DOMAIN_MESSAGE_IDX =
-        MOD_SCMI_PROTOCOL_ID_RESET_DOMAIN - MOD_RES_PERMS_MESSAGE_OFFSET,
+	MOD_SCMI_PROTOCOL_ID_RESET_DOMAIN - MOD_RES_PERMS_MESSAGE_OFFSET,
     MOD_RES_PERMS_SCMI_VOLTAGE_DOMAIN_MESSAGE_IDX =
-        MOD_SCMI_PROTOCOL_ID_VOLTAGE_DOMAIN - MOD_RES_PERMS_MESSAGE_OFFSET,
+	MOD_SCMI_PROTOCOL_ID_VOLTAGE_DOMAIN - MOD_RES_PERMS_MESSAGE_OFFSET,
 };
 
 /*!
@@ -121,13 +121,13 @@ enum mod_res_perms_message_bitmask_base
     MOD_RES_PERMS_SCMI_CLOCK_BITMASK_IDX = MOD_SCMI_CLOCK_ATTRIBUTES,
     MOD_RES_PERMS_SCMI_SYS_POWER_BITMASK_IDX = MOD_SCMI_SYS_POWER_STATE_SET,
     MOD_RES_PERMS_SCMI_POWER_DOMAIN_BITMASK_IDX =
-        MOD_SCMI_PD_POWER_DOMAIN_ATTRIBUTES,
+	MOD_SCMI_PD_POWER_DOMAIN_ATTRIBUTES,
     MOD_RES_PERMS_SCMI_PERF_BITMASK_IDX = MOD_SCMI_PERF_DOMAIN_ATTRIBUTES,
     MOD_RES_PERMS_SCMI_SENSOR_BITMASK_IDX = MOD_SCMI_SENSOR_DESCRIPTION_GET,
     MOD_RES_PERMS_SCMI_RESET_DOMAIN_BITMASK_IDX =
-        MOD_SCMI_RESET_DOMAIN_ATTRIBUTES,
+	MOD_SCMI_RESET_DOMAIN_ATTRIBUTES,
     MOD_RES_PERMS_SCMI_VOLTAGE_DOMAIN_BITMASK_IDX =
-        MOD_SCMI_VOLTD_DOMAIN_ATTRIBUTES,
+	MOD_SCMI_VOLTD_DOMAIN_ATTRIBUTES,
 };
 
 /*!
@@ -178,21 +178,21 @@ struct mod_res_agent_msg_permissions {
  */
 enum mod_res_clock_permissions_idx {
     MOD_RES_PERMS_SCMI_CLOCK_ATTRIBUTES_IDX =
-        MOD_SCMI_CLOCK_ATTRIBUTES - MOD_RES_PERMS_CLOCK_PERMS_OFFSET,
+	MOD_SCMI_CLOCK_ATTRIBUTES - MOD_RES_PERMS_CLOCK_PERMS_OFFSET,
     MOD_RES_PERMS_SCMI_CLOCK_DESCRIBE_RATE_IDX =
-        MOD_SCMI_CLOCK_DESCRIBE_RATES - MOD_RES_PERMS_CLOCK_PERMS_OFFSET,
+	MOD_SCMI_CLOCK_DESCRIBE_RATES - MOD_RES_PERMS_CLOCK_PERMS_OFFSET,
     MOD_RES_PERMS_SCMI_CLOCK_RATE_SET_IDX =
-        MOD_SCMI_CLOCK_RATE_SET - MOD_RES_PERMS_CLOCK_PERMS_OFFSET,
+	MOD_SCMI_CLOCK_RATE_SET - MOD_RES_PERMS_CLOCK_PERMS_OFFSET,
     MOD_RES_PERMS_SCMI_CLOCK_RATE_GET_IDX =
-        MOD_SCMI_CLOCK_RATE_GET - MOD_RES_PERMS_CLOCK_PERMS_OFFSET,
+	MOD_SCMI_CLOCK_RATE_GET - MOD_RES_PERMS_CLOCK_PERMS_OFFSET,
     MOD_RES_PERMS_SCMI_CLOCK_CONFIG_SET_IDX =
-        MOD_SCMI_CLOCK_CONFIG_SET - MOD_RES_PERMS_CLOCK_PERMS_OFFSET,
+	MOD_SCMI_CLOCK_CONFIG_SET - MOD_RES_PERMS_CLOCK_PERMS_OFFSET,
     MOD_RES_PERMS_SCMI_CLOCK_NAME_GET_IDX =
-        MOD_SCMI_CLOCK_NAME_GET - MOD_RES_PERMS_CLOCK_PERMS_OFFSET,
+	MOD_SCMI_CLOCK_NAME_GET - MOD_RES_PERMS_CLOCK_PERMS_OFFSET,
     MOD_RES_PERMS_SCMI_CLOCK_RATE_NOTIFY_IDX =
-        MOD_SCMI_CLOCK_RATE_NOTIFY - MOD_RES_PERMS_CLOCK_PERMS_OFFSET,
+	MOD_SCMI_CLOCK_RATE_NOTIFY - MOD_RES_PERMS_CLOCK_PERMS_OFFSET,
     MOD_RES_PERMS_SCMI_CLOCK_CHANGE_REQUESTED_NOTIFY_IDX =
-        MOD_SCMI_CLOCK_CHANGE_REQUESTED_NOTIFY - MOD_RES_PERMS_CLOCK_PERMS_OFFSET,
+	MOD_SCMI_CLOCK_CHANGE_REQUESTED_NOTIFY - MOD_RES_PERMS_CLOCK_PERMS_OFFSET,
 };
 
 /*!
@@ -206,15 +206,15 @@ enum mod_res_clock_permissions_idx {
  */
 enum mod_res_power_domain_permissions_idx {
     MOD_RES_PERMS_SCMI_POWER_DOMAIN_ATTRIBUTES_IDX =
-        MOD_SCMI_PD_POWER_DOMAIN_ATTRIBUTES -
-        MOD_RES_PERMS_POWER_DOMAIN_PERMS_OFFSET,
+	MOD_SCMI_PD_POWER_DOMAIN_ATTRIBUTES -
+	MOD_RES_PERMS_POWER_DOMAIN_PERMS_OFFSET,
     MOD_RES_PERMS_SCMI_POWER_DOMAIN_STATE_SET_IDX =
-        MOD_SCMI_PD_POWER_STATE_SET - MOD_RES_PERMS_POWER_DOMAIN_PERMS_OFFSET,
+	MOD_SCMI_PD_POWER_STATE_SET - MOD_RES_PERMS_POWER_DOMAIN_PERMS_OFFSET,
     MOD_RES_PERMS_SCMI_POWER_DOMAIN_STATE_GET_IDX =
-        MOD_SCMI_PD_POWER_STATE_GET - MOD_RES_PERMS_POWER_DOMAIN_PERMS_OFFSET,
+	MOD_SCMI_PD_POWER_STATE_GET - MOD_RES_PERMS_POWER_DOMAIN_PERMS_OFFSET,
     MOD_RES_PERMS_SCMI_POWER_DOMAIN_STATE_NOTIFY_IDX =
-        MOD_SCMI_PD_POWER_STATE_NOTIFY -
-        MOD_RES_PERMS_POWER_DOMAIN_PERMS_OFFSET,
+	MOD_SCMI_PD_POWER_STATE_NOTIFY -
+	MOD_RES_PERMS_POWER_DOMAIN_PERMS_OFFSET,
 };
 
 /*!
@@ -227,23 +227,23 @@ enum mod_res_power_domain_permissions_id
  */
 enum mod_res_perf_permissions_idx {
     MOD_RES_PERMS_SCMI_PERF_ATTRIBUTES_IDX =
-        MOD_SCMI_PERF_DOMAIN_ATTRIBUTES - MOD_RES_PERMS_PERF_PERMS_OFFSET,
+	MOD_SCMI_PERF_DOMAIN_ATTRIBUTES - MOD_RES_PERMS_PERF_PERMS_OFFSET,
     MOD_RES_PERMS_SCMI_PERF_DESCRIBE_LEVELS_IDX =
-        MOD_SCMI_PERF_DESCRIBE_LEVELS - MOD_RES_PERMS_PERF_PERMS_OFFSET,
+	MOD_SCMI_PERF_DESCRIBE_LEVELS - MOD_RES_PERMS_PERF_PERMS_OFFSET,
     MOD_RES_PERMS_SCMI_PERF_LIMITS_SET_IDX =
-        MOD_SCMI_PERF_LIMITS_SET - MOD_RES_PERMS_PERF_PERMS_OFFSET,
+	MOD_SCMI_PERF_LIMITS_SET - MOD_RES_PERMS_PERF_PERMS_OFFSET,
     MOD_RES_PERMS_SCMI_PERF_LIMITS_GET_IDX =
-        MOD_SCMI_PERF_LIMITS_GET - MOD_RES_PERMS_PERF_PERMS_OFFSET,
+	MOD_SCMI_PERF_LIMITS_GET - MOD_RES_PERMS_PERF_PERMS_OFFSET,
     MOD_RES_PERMS_SCMI_PERF_LEVEL_SET_IDX =
-        MOD_SCMI_PERF_LEVEL_SET - MOD_RES_PERMS_PERF_PERMS_OFFSET,
+	MOD_SCMI_PERF_LEVEL_SET - MOD_RES_PERMS_PERF_PERMS_OFFSET,
     MOD_RES_PERMS_SCMI_PERF_LEVEL_GET_IDX =
-        MOD_SCMI_PERF_LEVEL_GET - MOD_RES_PERMS_PERF_PERMS_OFFSET,
+	MOD_SCMI_PERF_LEVEL_GET - MOD_RES_PERMS_PERF_PERMS_OFFSET,
     MOD_RES_PERMS_SCMI_PERF_NOTIFY_LIMITS_IDX =
-        MOD_SCMI_PERF_NOTIFY_LIMITS - MOD_RES_PERMS_PERF_PERMS_OFFSET,
+	MOD_SCMI_PERF_NOTIFY_LIMITS - MOD_RES_PERMS_PERF_PERMS_OFFSET,
     MOD_RES_PERMS_SCMI_PERF_NOTIFY_LEVEL_IDX =
-        MOD_SCMI_PERF_NOTIFY_LEVEL - MOD_RES_PERMS_PERF_PERMS_OFFSET,
+	MOD_SCMI_PERF_NOTIFY_LEVEL - MOD_RES_PERMS_PERF_PERMS_OFFSET,
     MOD_RES_PERMS_SCMI_PERF_DESCRIBE_FAST_CHANNEL_IDX =
-        MOD_SCMI_PERF_DESCRIBE_FAST_CHANNEL - MOD_RES_PERMS_PERF_PERMS_OFFSET,
+	MOD_SCMI_PERF_DESCRIBE_FAST_CHANNEL - MOD_RES_PERMS_PERF_PERMS_OFFSET,
 };
 
 /*!
@@ -256,13 +256,13 @@ enum mod_res_perf_permissions_idx {
  */
 enum mod_res_sensor_permissions_idx {
     MOD_RES_PERMS_SCMI_SENSOR_DESCRIPTION_GET_IDX =
-        MOD_SCMI_PERF_DOMAIN_ATTRIBUTES - MOD_RES_PERMS_SENSOR_PERMS_OFFSET,
+	MOD_SCMI_PERF_DOMAIN_ATTRIBUTES - MOD_RES_PERMS_SENSOR_PERMS_OFFSET,
     MOD_RES_PERMS_SCMI_SENSOR_TRIP_POINT_NOTIFY_IDX =
-        MOD_SCMI_SENSOR_TRIP_POINT_NOTIFY - MOD_RES_PERMS_SENSOR_PERMS_OFFSET,
+	MOD_SCMI_SENSOR_TRIP_POINT_NOTIFY - MOD_RES_PERMS_SENSOR_PERMS_OFFSET,
     MOD_RES_PERMS_SCMI_SENSOR_TRIP_POINT_CONFIG_IDX =
-        MOD_SCMI_SENSOR_TRIP_POINT_CONFIG - MOD_RES_PERMS_SENSOR_PERMS_OFFSET,
+	MOD_SCMI_SENSOR_TRIP_POINT_CONFIG - MOD_RES_PERMS_SENSOR_PERMS_OFFSET,
     MOD_RES_PERMS_SCMI_SENSOR_READING_GET_IDX =
-        MOD_SCMI_SENSOR_READING_GET - MOD_RES_PERMS_SENSOR_PERMS_OFFSET,
+	MOD_SCMI_SENSOR_READING_GET - MOD_RES_PERMS_SENSOR_PERMS_OFFSET,
 };
 
 /*!
@@ -275,12 +275,12 @@ enum mod_res_sensor_permissions_idx {
  */
 enum mod_res_reset_domain_permissions_idx {
     MOD_RES_PERMS_SCMI_RESET_DOMAIN_ATTRIBUTES_IDX =
-        MOD_SCMI_RESET_DOMAIN_ATTRIBUTES -
-        MOD_RES_PERMS_RESET_DOMAIN_PERMS_OFFSET,
+	MOD_SCMI_RESET_DOMAIN_ATTRIBUTES -
+	MOD_RES_PERMS_RESET_DOMAIN_PERMS_OFFSET,
     MOD_RES_PERMS_SCMI_RESET_DOMAIN_RESET_REQUEST_IDX =
-        MOD_SCMI_RESET_REQUEST - MOD_RES_PERMS_RESET_DOMAIN_PERMS_OFFSET,
+	MOD_SCMI_RESET_REQUEST - MOD_RES_PERMS_RESET_DOMAIN_PERMS_OFFSET,
     MOD_RES_PERMS_SCMI_RESET_DOMAIN_RESET_NOTIFY_IDX =
-        MOD_SCMI_RESET_NOTIFY - MOD_RES_PERMS_RESET_DOMAIN_PERMS_OFFSET,
+	MOD_SCMI_RESET_NOTIFY - MOD_RES_PERMS_RESET_DOMAIN_PERMS_OFFSET,
 };
 
 /*!
@@ -293,17 +293,17 @@ enum mod_res_reset_domain_permissions_id
  */
 enum mod_res_voltage_domain_permissions_idx {
     MOD_RES_PERMS_SCMI_VOLTD_DOMAIN_ATTRIBUTES_IDX =
-        MOD_SCMI_VOLTD_DOMAIN_ATTRIBUTES - MOD_RES_PERMS_VOLTD_PERMS_OFFSET,
+	MOD_SCMI_VOLTD_DOMAIN_ATTRIBUTES - MOD_RES_PERMS_VOLTD_PERMS_OFFSET,
     MOD_RES_PERMS_SCMI_VOLTD_DESCRIBE_LEVELS_IDX =
-        MOD_SCMI_VOLTD_DESCRIBE_LEVELS - MOD_RES_PERMS_VOLTD_PERMS_OFFSET,
+	MOD_SCMI_VOLTD_DESCRIBE_LEVELS - MOD_RES_PERMS_VOLTD_PERMS_OFFSET,
     MOD_RES_PERMS_SCMI_VOLTD_CONFIG_SET_IDX =
-        MOD_SCMI_VOLTD_CONFIG_SET - MOD_RES_PERMS_VOLTD_PERMS_OFFSET,
+	MOD_SCMI_VOLTD_CONFIG_SET - MOD_RES_PERMS_VOLTD_PERMS_OFFSET,
     MOD_RES_PERMS_SCMI_VOLTD_CONFIG_GET_IDX =
-        MOD_SCMI_VOLTD_CONFIG_GET - MOD_RES_PERMS_VOLTD_PERMS_OFFSET,
+	MOD_SCMI_VOLTD_CONFIG_GET - MOD_RES_PERMS_VOLTD_PERMS_OFFSET,
     MOD_RES_PERMS_SCMI_VOLTD_LEVEL_SET_IDX =
-        MOD_SCMI_VOLTD_LEVEL_SET - MOD_RES_PERMS_VOLTD_PERMS_OFFSET,
+	MOD_SCMI_VOLTD_LEVEL_SET - MOD_RES_PERMS_VOLTD_PERMS_OFFSET,
     MOD_RES_PERMS_SCMI_VOLTD_LEVEL_GET_IDX =
-        MOD_SCMI_VOLTD_LEVEL_GET - MOD_RES_PERMS_VOLTD_PERMS_OFFSET,
+	MOD_SCMI_VOLTD_LEVEL_GET - MOD_RES_PERMS_VOLTD_PERMS_OFFSET,
 };
 
 /*!
@@ -402,8 +402,8 @@ struct mod_res_permissions_api {
      *      permissions to use the protocol.
      */
     enum mod_res_perms_permissions (*agent_has_protocol_permission)(
-        uint32_t agent_id,
-        uint32_t protocol_id);
+	uint32_t agent_id,
+	uint32_t protocol_id);
 
     /*!
      * \brief Check whether the agent has permission to access a message.
@@ -418,9 +418,9 @@ struct mod_res_permissions_api {
      *      permissions to use the message.
      */
     enum mod_res_perms_permissions (*agent_has_message_permission)(
-        uint32_t agent_id,
-        uint32_t protocol_id,
-        uint32_t message_id);
+	uint32_t agent_id,
+	uint32_t protocol_id,
+	uint32_t message_id);
 
     /*!
      * \brief Check whether the agent has permission to access a resource.
@@ -436,10 +436,10 @@ struct mod_res_permissions_api {
      *      permissions to use the resource.
      */
     enum mod_res_perms_permissions (*agent_has_resource_permission)(
-        uint32_t agent_id,
-        uint32_t protocol_id,
-        uint32_t message_id,
-        uint32_t resource_id);
+	uint32_t agent_id,
+	uint32_t protocol_id,
+	uint32_t message_id,
+	uint32_t resource_id);
 
     /*!
      * \brief Set device permissions for an agent
@@ -453,9 +453,9 @@ struct mod_res_permissions_api {
      * \retval ::FWK_E_PARAM  Invalid flags or protocol_ID.
      */
     int (*agent_set_device_permission)(
-        uint32_t agent_id,
-        uint32_t device_id,
-        uint32_t flags);
+	uint32_t agent_id,
+	uint32_t device_id,
+	uint32_t flags);
 
     /*!
      * \brief Set device protocol permissions for an agent
@@ -470,10 +470,10 @@ struct mod_res_permissions_api {
      * \retval ::FWK_E_PARAM  Invalid flags or protocol_ID.
      */
     int (*agent_set_device_protocol_permission)(
-        uint32_t agent_id,
-        uint32_t device_id,
-        uint32_t protocol_id,
-        uint32_t flags);
+	uint32_t agent_id,
+	uint32_t device_id,
+	uint32_t protocol_id,
+	uint32_t flags);
 
     /*!
      * \brief Reset permissions for an agent
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/resource_perms/src/mod_resource_perms.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/resource_perms/src/mod_resource_perms.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/resource_perms/src/mod_resource_perms.c
@@ -123,8 +123,8 @@ __attribute((weak)) int mod_res_agent_id
     uint32_t idx = agent_id - 1;
 
     if (idx < resources_perms_ctx.agent_count) {
-        *agent_idx = idx;
-        return FWK_SUCCESS;
+	*agent_idx = idx;
+	return FWK_SUCCESS;
     }
 
     return FWK_E_PARAM;
@@ -152,8 +152,8 @@ static int mod_res_protocol_id_to_index(
     uint32_t *protocol_idx)
 {
     if ((protocol_id >= MOD_SCMI_PLATFORM_PROTOCOL_ID_MIN) &&
-        (protocol_id <= MOD_SCMI_PLATFORM_PROTOCOL_ID_MAX)) {
-        return mod_res_plat_protocol_id_to_index(protocol_id, protocol_idx);
+	(protocol_id <= MOD_SCMI_PLATFORM_PROTOCOL_ID_MAX)) {
+	return mod_res_plat_protocol_id_to_index(protocol_id, protocol_idx);
     }
 
     switch (protocol_id) {
@@ -165,10 +165,10 @@ static int mod_res_protocol_id_to_index(
     case MOD_SCMI_PROTOCOL_ID_SENSOR:
     case MOD_SCMI_PROTOCOL_ID_RESET_DOMAIN:
     case MOD_SCMI_PROTOCOL_ID_VOLTAGE_DOMAIN:
-        *protocol_idx = protocol_id - MOD_SCMI_PROTOCOL_ID_BASE;
-        return FWK_SUCCESS;
+	*protocol_idx = protocol_id - MOD_SCMI_PROTOCOL_ID_BASE;
+	return FWK_SUCCESS;
     default:
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 }
 
@@ -213,104 +213,104 @@ static int mod_res_message_id_to_index(
     *message_idx = -1;
 
     if ((protocol_id >= MOD_SCMI_PLATFORM_PROTOCOL_ID_MIN) &&
-        (protocol_id <= MOD_SCMI_PLATFORM_PROTOCOL_ID_MAX)) {
-        return mod_res_plat_message_id_to_index(
-            protocol_id, message_id, message_idx);
+	(protocol_id <= MOD_SCMI_PLATFORM_PROTOCOL_ID_MAX)) {
+	return mod_res_plat_message_id_to_index(
+	    protocol_id, message_id, message_idx);
     }
 
     if (message_id < 3) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     switch (protocol_id) {
     case MOD_SCMI_PROTOCOL_ID_BASE:
-        if (message_id >= MOD_SCMI_BASE_COMMAND_COUNT) {
-            return FWK_E_PARAM;
-        }
-        if (message_id == MOD_SCMI_BASE_NOTIFY_ERRORS) {
-            *message_idx = 0;
-        }
-        return FWK_SUCCESS;
+	if (message_id >= MOD_SCMI_BASE_COMMAND_COUNT) {
+	    return FWK_E_PARAM;
+	}
+	if (message_id == MOD_SCMI_BASE_NOTIFY_ERRORS) {
+	    *message_idx = 0;
+	}
+	return FWK_SUCCESS;
 
     case MOD_SCMI_PROTOCOL_ID_POWER_DOMAIN:
-        if (message_id >= MOD_SCMI_PD_POWER_COMMAND_COUNT) {
-            return FWK_E_PARAM;
-        }
-        if ((message_id >= MOD_SCMI_PD_POWER_DOMAIN_ATTRIBUTES) &&
-            (message_id <= MOD_SCMI_PD_POWER_STATE_NOTIFY)) {
-            *message_idx = (int32_t)(
-                message_id - (uint32_t)MOD_SCMI_PD_POWER_DOMAIN_ATTRIBUTES);
-        }
-        return FWK_SUCCESS;
+	if (message_id >= MOD_SCMI_PD_POWER_COMMAND_COUNT) {
+	    return FWK_E_PARAM;
+	}
+	if ((message_id >= MOD_SCMI_PD_POWER_DOMAIN_ATTRIBUTES) &&
+	    (message_id <= MOD_SCMI_PD_POWER_STATE_NOTIFY)) {
+	    *message_idx = (int32_t)(
+		message_id - (uint32_t)MOD_SCMI_PD_POWER_DOMAIN_ATTRIBUTES);
+	}
+	return FWK_SUCCESS;
 
     case MOD_SCMI_PROTOCOL_ID_SYS_POWER:
-        if (message_id >= MOD_SCMI_SYS_POWER_COMMAND_COUNT) {
-            return FWK_E_PARAM;
-        }
-        if ((message_id >= MOD_SCMI_SYS_POWER_STATE_SET) &&
-            (message_id <= MOD_SCMI_SYS_POWER_STATE_NOTIFY)) {
-            *message_idx =
-                (int32_t)(message_id - (uint32_t)MOD_SCMI_SYS_POWER_STATE_SET);
-        }
-        return FWK_SUCCESS;
+	if (message_id >= MOD_SCMI_SYS_POWER_COMMAND_COUNT) {
+	    return FWK_E_PARAM;
+	}
+	if ((message_id >= MOD_SCMI_SYS_POWER_STATE_SET) &&
+	    (message_id <= MOD_SCMI_SYS_POWER_STATE_NOTIFY)) {
+	    *message_idx =
+		(int32_t)(message_id - (uint32_t)MOD_SCMI_SYS_POWER_STATE_SET);
+	}
+	return FWK_SUCCESS;
 
     case MOD_SCMI_PROTOCOL_ID_PERF:
-        if (message_id >= MOD_SCMI_PERF_COMMAND_COUNT) {
-            return FWK_E_PARAM;
-        }
-        if ((message_id >= MOD_SCMI_PERF_DOMAIN_ATTRIBUTES) &&
-            (message_id <= MOD_SCMI_PERF_DESCRIBE_FAST_CHANNEL)) {
-            *message_idx = (int32_t)(
-                message_id - (uint32_t)MOD_SCMI_PERF_DOMAIN_ATTRIBUTES);
-        }
-        return FWK_SUCCESS;
+	if (message_id >= MOD_SCMI_PERF_COMMAND_COUNT) {
+	    return FWK_E_PARAM;
+	}
+	if ((message_id >= MOD_SCMI_PERF_DOMAIN_ATTRIBUTES) &&
+	    (message_id <= MOD_SCMI_PERF_DESCRIBE_FAST_CHANNEL)) {
+	    *message_idx = (int32_t)(
+		message_id - (uint32_t)MOD_SCMI_PERF_DOMAIN_ATTRIBUTES);
+	}
+	return FWK_SUCCESS;
 
     case MOD_SCMI_PROTOCOL_ID_CLOCK:
-        if (message_id >= MOD_SCMI_CLOCK_COMMAND_COUNT) {
-            return FWK_E_PARAM;
-        }
-        if ((message_id >= MOD_SCMI_CLOCK_ATTRIBUTES) &&
-            (message_id <= MOD_SCMI_CLOCK_CONFIG_SET)) {
-            *message_idx =
-                (int32_t)(message_id - (uint32_t)MOD_SCMI_CLOCK_ATTRIBUTES);
-        }
-        return FWK_SUCCESS;
+	if (message_id >= MOD_SCMI_CLOCK_COMMAND_COUNT) {
+	    return FWK_E_PARAM;
+	}
+	if ((message_id >= MOD_SCMI_CLOCK_ATTRIBUTES) &&
+	    (message_id <= MOD_SCMI_CLOCK_CONFIG_SET)) {
+	    *message_idx =
+		(int32_t)(message_id - (uint32_t)MOD_SCMI_CLOCK_ATTRIBUTES);
+	}
+	return FWK_SUCCESS;
 
     case MOD_SCMI_PROTOCOL_ID_SENSOR:
-        if (message_id >= MOD_SCMI_SENSOR_COMMAND_COUNT) {
-            return FWK_E_PARAM;
-        }
-        if ((message_id >= MOD_SCMI_SENSOR_DESCRIPTION_GET) &&
-            (message_id <= MOD_SCMI_SENSOR_READING_GET)) {
-            *message_idx = (int32_t)(
-                message_id - (uint32_t)MOD_SCMI_SENSOR_DESCRIPTION_GET);
-        }
-        return FWK_SUCCESS;
+	if (message_id >= MOD_SCMI_SENSOR_COMMAND_COUNT) {
+	    return FWK_E_PARAM;
+	}
+	if ((message_id >= MOD_SCMI_SENSOR_DESCRIPTION_GET) &&
+	    (message_id <= MOD_SCMI_SENSOR_READING_GET)) {
+	    *message_idx = (int32_t)(
+		message_id - (uint32_t)MOD_SCMI_SENSOR_DESCRIPTION_GET);
+	}
+	return FWK_SUCCESS;
 
     case MOD_SCMI_PROTOCOL_ID_RESET_DOMAIN:
-        if (message_id >= MOD_SCMI_RESET_COMMAND_COUNT) {
-            return FWK_E_PARAM;
-        }
-        if ((message_id >= MOD_SCMI_RESET_DOMAIN_ATTRIBUTES) &&
-            (message_id <= MOD_SCMI_RESET_NOTIFY)) {
-            *message_idx = (int32_t)(
-                message_id - (uint32_t)MOD_SCMI_RESET_DOMAIN_ATTRIBUTES);
-        }
-        return FWK_SUCCESS;
+	if (message_id >= MOD_SCMI_RESET_COMMAND_COUNT) {
+	    return FWK_E_PARAM;
+	}
+	if ((message_id >= MOD_SCMI_RESET_DOMAIN_ATTRIBUTES) &&
+	    (message_id <= MOD_SCMI_RESET_NOTIFY)) {
+	    *message_idx = (int32_t)(
+		message_id - (uint32_t)MOD_SCMI_RESET_DOMAIN_ATTRIBUTES);
+	}
+	return FWK_SUCCESS;
 
     case MOD_SCMI_PROTOCOL_ID_VOLTAGE_DOMAIN:
-        if (message_id >= MOD_SCMI_VOLTD_COMMAND_COUNT) {
-            return FWK_E_PARAM;
-        }
-        if ((message_id >= MOD_SCMI_VOLTD_DOMAIN_ATTRIBUTES) &&
-            (message_id <= MOD_SCMI_VOLTD_LEVEL_GET)) {
-            *message_idx = (int32_t)(
-                message_id - (uint32_t)MOD_SCMI_VOLTD_DOMAIN_ATTRIBUTES);
-        }
-        return FWK_SUCCESS;
+	if (message_id >= MOD_SCMI_VOLTD_COMMAND_COUNT) {
+	    return FWK_E_PARAM;
+	}
+	if ((message_id >= MOD_SCMI_VOLTD_DOMAIN_ATTRIBUTES) &&
+	    (message_id <= MOD_SCMI_VOLTD_LEVEL_GET)) {
+	    *message_idx = (int32_t)(
+		message_id - (uint32_t)MOD_SCMI_VOLTD_DOMAIN_ATTRIBUTES);
+	}
+	return FWK_SUCCESS;
 
     default:
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 }
 
@@ -365,13 +365,13 @@ static int mod_res_resource_id_to_index(
     *resource_idx = -1;
 
     if ((protocol_id >= MOD_SCMI_PLATFORM_PROTOCOL_ID_MIN) &&
-        (protocol_id <= MOD_SCMI_PLATFORM_PROTOCOL_ID_MAX)) {
-        return mod_res_plat_resource_id_to_index(
-            agent_id, protocol_id, message_id, resource_id, resource_idx);
+	(protocol_id <= MOD_SCMI_PLATFORM_PROTOCOL_ID_MAX)) {
+	return mod_res_plat_resource_id_to_index(
+	    agent_id, protocol_id, message_id, resource_id, resource_idx);
     }
 
     if (message_id < 3) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     /*
@@ -382,99 +382,99 @@ static int mod_res_resource_id_to_index(
 
     switch (protocol_id) {
     case MOD_SCMI_PROTOCOL_ID_BASE:
-        /* No per-resource managemment for SCMI Base Protocol */
-        return FWK_E_PARAM;
+	/* No per-resource managemment for SCMI Base Protocol */
+	return FWK_E_PARAM;
 
     case MOD_SCMI_PROTOCOL_ID_SYS_POWER:
-        /* No per-resource managemment for SCMI System Power Protocol */
-        return FWK_E_PARAM;
+	/* No per-resource managemment for SCMI System Power Protocol */
+	return FWK_E_PARAM;
 
     case MOD_SCMI_PROTOCOL_ID_POWER_DOMAIN:
-        if ((message_id >= MOD_SCMI_PD_POWER_DOMAIN_ATTRIBUTES) &&
-            (message_id <= MOD_SCMI_PD_POWER_STATE_NOTIFY)) {
-            message_count = (int)MOD_SCMI_PD_POWER_STATE_NOTIFY -
-                (int)MOD_SCMI_PD_POWER_DOMAIN_ATTRIBUTES + 1;
-            message_offset =
-                (int)(message_id - (uint32_t)MOD_SCMI_PD_POWER_DOMAIN_ATTRIBUTES);
-            resource_size = (int)resources_perms_ctx.pd_count;
-            break;
-        }
-        return FWK_E_PARAM;
+	if ((message_id >= MOD_SCMI_PD_POWER_DOMAIN_ATTRIBUTES) &&
+	    (message_id <= MOD_SCMI_PD_POWER_STATE_NOTIFY)) {
+	    message_count = (int)MOD_SCMI_PD_POWER_STATE_NOTIFY -
+		(int)MOD_SCMI_PD_POWER_DOMAIN_ATTRIBUTES + 1;
+	    message_offset =
+		(int)(message_id - (uint32_t)MOD_SCMI_PD_POWER_DOMAIN_ATTRIBUTES);
+	    resource_size = (int)resources_perms_ctx.pd_count;
+	    break;
+	}
+	return FWK_E_PARAM;
 
     case MOD_SCMI_PROTOCOL_ID_PERF:
-        if ((message_id >= MOD_SCMI_PERF_DOMAIN_ATTRIBUTES) &&
-            (message_id <= MOD_SCMI_PERF_DESCRIBE_FAST_CHANNEL)) {
-            message_count = (int)MOD_SCMI_PERF_DESCRIBE_FAST_CHANNEL -
-                (int)MOD_SCMI_PERF_DOMAIN_ATTRIBUTES + 1;
-            message_offset =
-                (int)(message_id - (uint32_t)MOD_SCMI_PERF_DOMAIN_ATTRIBUTES);
-            resource_size = (int)resources_perms_ctx.perf_count;
-            break;
-        }
-        return FWK_E_PARAM;
+	if ((message_id >= MOD_SCMI_PERF_DOMAIN_ATTRIBUTES) &&
+	    (message_id <= MOD_SCMI_PERF_DESCRIBE_FAST_CHANNEL)) {
+	    message_count = (int)MOD_SCMI_PERF_DESCRIBE_FAST_CHANNEL -
+		(int)MOD_SCMI_PERF_DOMAIN_ATTRIBUTES + 1;
+	    message_offset =
+		(int)(message_id - (uint32_t)MOD_SCMI_PERF_DOMAIN_ATTRIBUTES);
+	    resource_size = (int)resources_perms_ctx.perf_count;
+	    break;
+	}
+	return FWK_E_PARAM;
 
     case MOD_SCMI_PROTOCOL_ID_CLOCK:
-        if ((message_id >= MOD_SCMI_CLOCK_ATTRIBUTES) &&
-            (message_id <= MOD_SCMI_CLOCK_CONFIG_SET)) {
-            message_count = (int)MOD_SCMI_CLOCK_CONFIG_SET -
-                (int)MOD_SCMI_CLOCK_ATTRIBUTES + 1;
-            message_offset =
-                (int)(message_id - (uint32_t)MOD_SCMI_CLOCK_ATTRIBUTES);
-            resource_size = (int)resources_perms_ctx.clock_count;
-            break;
-        }
-        return FWK_E_PARAM;
+	if ((message_id >= MOD_SCMI_CLOCK_ATTRIBUTES) &&
+	    (message_id <= MOD_SCMI_CLOCK_CONFIG_SET)) {
+	    message_count = (int)MOD_SCMI_CLOCK_CONFIG_SET -
+		(int)MOD_SCMI_CLOCK_ATTRIBUTES + 1;
+	    message_offset =
+		(int)(message_id - (uint32_t)MOD_SCMI_CLOCK_ATTRIBUTES);
+	    resource_size = (int)resources_perms_ctx.clock_count;
+	    break;
+	}
+	return FWK_E_PARAM;
 
     case MOD_SCMI_PROTOCOL_ID_SENSOR:
-        if ((message_id >= MOD_SCMI_SENSOR_DESCRIPTION_GET) &&
-            (message_id <= MOD_SCMI_SENSOR_READING_GET)) {
-            message_count = (int)MOD_SCMI_SENSOR_READING_GET -
-                (int)MOD_SCMI_SENSOR_DESCRIPTION_GET + 1;
-            message_offset =
-                (int)(message_id - (uint32_t)MOD_SCMI_SENSOR_DESCRIPTION_GET);
-            resource_size = (int)resources_perms_ctx.sensor_count;
-            break;
-        }
-        return FWK_E_PARAM;
+	if ((message_id >= MOD_SCMI_SENSOR_DESCRIPTION_GET) &&
+	    (message_id <= MOD_SCMI_SENSOR_READING_GET)) {
+	    message_count = (int)MOD_SCMI_SENSOR_READING_GET -
+		(int)MOD_SCMI_SENSOR_DESCRIPTION_GET + 1;
+	    message_offset =
+		(int)(message_id - (uint32_t)MOD_SCMI_SENSOR_DESCRIPTION_GET);
+	    resource_size = (int)resources_perms_ctx.sensor_count;
+	    break;
+	}
+	return FWK_E_PARAM;
 
 #ifdef BUILD_HAS_MOD_SCMI_RESET_DOMAIN
     case MOD_SCMI_PROTOCOL_ID_RESET_DOMAIN:
-        if ((message_id >= MOD_SCMI_RESET_DOMAIN_ATTRIBUTES) &&
-            (message_id <= MOD_SCMI_RESET_NOTIFY)) {
-            message_count = (int)MOD_SCMI_RESET_NOTIFY -
-                (int)MOD_SCMI_RESET_DOMAIN_ATTRIBUTES + 1;
-            message_offset =
-                (int)(message_id - (uint32_t)MOD_SCMI_RESET_DOMAIN_ATTRIBUTES);
-            resource_size = (int)resources_perms_ctx.reset_domain_count;
-            break;
-        }
-        return FWK_E_PARAM;
+	if ((message_id >= MOD_SCMI_RESET_DOMAIN_ATTRIBUTES) &&
+	    (message_id <= MOD_SCMI_RESET_NOTIFY)) {
+	    message_count = (int)MOD_SCMI_RESET_NOTIFY -
+		(int)MOD_SCMI_RESET_DOMAIN_ATTRIBUTES + 1;
+	    message_offset =
+		(int)(message_id - (uint32_t)MOD_SCMI_RESET_DOMAIN_ATTRIBUTES);
+	    resource_size = (int)resources_perms_ctx.reset_domain_count;
+	    break;
+	}
+	return FWK_E_PARAM;
 #endif
 
     case MOD_SCMI_PROTOCOL_ID_VOLTAGE_DOMAIN:
-        if ((message_id >= MOD_SCMI_VOLTD_DOMAIN_ATTRIBUTES) &&
-            (message_id <= MOD_SCMI_VOLTD_LEVEL_GET)) {
-            message_count = (int)MOD_SCMI_VOLTD_LEVEL_GET -
-                (int)MOD_SCMI_VOLTD_DOMAIN_ATTRIBUTES + 1;
-            message_offset =
-                (int)(message_id - (uint32_t)MOD_SCMI_VOLTD_DOMAIN_ATTRIBUTES);
-            resource_size = (int)resources_perms_ctx.voltd_count;
-            break;
-        }
-        return FWK_E_PARAM;
+	if ((message_id >= MOD_SCMI_VOLTD_DOMAIN_ATTRIBUTES) &&
+	    (message_id <= MOD_SCMI_VOLTD_LEVEL_GET)) {
+	    message_count = (int)MOD_SCMI_VOLTD_LEVEL_GET -
+		(int)MOD_SCMI_VOLTD_DOMAIN_ATTRIBUTES + 1;
+	    message_offset =
+		(int)(message_id - (uint32_t)MOD_SCMI_VOLTD_DOMAIN_ATTRIBUTES);
+	    resource_size = (int)resources_perms_ctx.voltd_count;
+	    break;
+	}
+	return FWK_E_PARAM;
 
     default:
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     status = mod_res_agent_id_to_index(agent_id, &agent_idx);
     if ((status != FWK_SUCCESS) ||
-        (agent_idx >= resources_perms_ctx.agent_count)) {
-        return FWK_E_PARAM;
+	(agent_idx >= resources_perms_ctx.agent_count)) {
+	return FWK_E_PARAM;
     }
 
     resource_size =
-        (int)MOD_RES_PERMS_RESOURCE_ELEMENT((unsigned int)resource_size) + 1;
+	(int)MOD_RES_PERMS_RESOURCE_ELEMENT((unsigned int)resource_size) + 1;
 
     /*
      * message_count: the number of messages for the agent.
@@ -486,9 +486,9 @@ static int mod_res_resource_id_to_index(
      * [3]: &agent[agent_idx].message[message_id].resource[resource_id]
      */
     *resource_idx = (int32_t)(
-        (agent_idx * message_count * resource_size) + /* [1] */
-        (message_offset * resource_size) + /* [2] */
-        MOD_RES_PERMS_RESOURCE_ELEMENT(resource_id)); /* [3] */
+	(agent_idx * message_count * resource_size) + /* [1] */
+	(message_offset * resource_size) + /* [2] */
+	MOD_RES_PERMS_RESOURCE_ELEMENT(resource_id)); /* [3] */
 
     return FWK_SUCCESS;
 }
@@ -512,8 +512,8 @@ static int mod_res_resource_id_to_index(
  */
 __attribute((weak)) enum mod_res_perms_permissions
     mod_res_plat_agent_protocol_permissions(
-        uint32_t agent_id,
-        uint32_t protocol_id)
+	uint32_t agent_id,
+	uint32_t protocol_id)
 {
     return MOD_RES_PERMS_ACCESS_DENIED;
 }
@@ -529,35 +529,35 @@ static enum mod_res_perms_permissions ag
 
     /* No Agent:Protocol permissions management */
     if ((agent_id == 0) || (resources_perms_ctx.agent_permissions == NULL) ||
-        (resources_perms_ctx.agent_permissions->agent_protocol_permissions ==
-         NULL)) {
-        return MOD_RES_PERMS_ACCESS_ALLOWED;
+	(resources_perms_ctx.agent_permissions->agent_protocol_permissions ==
+	 NULL)) {
+	return MOD_RES_PERMS_ACCESS_ALLOWED;
     }
 
     if ((protocol_id >= MOD_SCMI_PLATFORM_PROTOCOL_ID_MIN) &&
-        (protocol_id <= MOD_SCMI_PLATFORM_PROTOCOL_ID_MAX)) {
-        return mod_res_plat_agent_protocol_permissions(agent_id, protocol_id);
+	(protocol_id <= MOD_SCMI_PLATFORM_PROTOCOL_ID_MAX)) {
+	return mod_res_plat_agent_protocol_permissions(agent_id, protocol_id);
     }
 
     status = mod_res_agent_id_to_index(agent_id, &agent_idx);
     if ((status != FWK_SUCCESS) ||
-        (agent_idx >= resources_perms_ctx.agent_count)) {
-        return MOD_RES_PERMS_ACCESS_DENIED;
+	(agent_idx >= resources_perms_ctx.agent_count)) {
+	return MOD_RES_PERMS_ACCESS_DENIED;
     }
 
     status = mod_res_protocol_id_to_index(protocol_id, &protocol_idx);
     if ((status != FWK_SUCCESS) ||
-        (protocol_idx >= resources_perms_ctx.protocol_count)) {
-        return MOD_RES_PERMS_ACCESS_DENIED;
+	(protocol_idx >= resources_perms_ctx.protocol_count)) {
+	return MOD_RES_PERMS_ACCESS_DENIED;
     }
 
     perms = resources_perms_ctx.agent_permissions
-                ->agent_protocol_permissions[agent_idx]
-                .protocols;
+		->agent_protocol_permissions[agent_idx]
+		.protocols;
 
     /* Agent:Protocol access denied */
     if (perms & (1U << protocol_idx)) {
-        return MOD_RES_PERMS_ACCESS_DENIED;
+	return MOD_RES_PERMS_ACCESS_DENIED;
     }
 
     return MOD_RES_PERMS_ACCESS_ALLOWED;
@@ -568,9 +568,9 @@ static enum mod_res_perms_permissions ag
  */
 __attribute((weak)) enum mod_res_perms_permissions
     mod_res_plat_agent_message_permissions(
-        uint32_t agent_id,
-        uint32_t protocol_id,
-        uint32_t message_id)
+	uint32_t agent_id,
+	uint32_t protocol_id,
+	uint32_t message_id)
 {
     return MOD_RES_PERMS_ACCESS_DENIED;
 }
@@ -589,52 +589,52 @@ static enum mod_res_perms_permissions ag
 
     /* No permissions management */
     if ((agent_id == 0) || (resources_perms_ctx.agent_permissions == NULL)) {
-        return MOD_RES_PERMS_ACCESS_ALLOWED;
+	return MOD_RES_PERMS_ACCESS_ALLOWED;
     }
 
     if ((protocol_id >= MOD_SCMI_PLATFORM_PROTOCOL_ID_MIN) &&
-        (protocol_id <= MOD_SCMI_PLATFORM_PROTOCOL_ID_MAX)) {
-        return mod_res_plat_agent_message_permissions(
-            agent_id, protocol_id, message_id);
+	(protocol_id <= MOD_SCMI_PLATFORM_PROTOCOL_ID_MAX)) {
+	return mod_res_plat_agent_message_permissions(
+	    agent_id, protocol_id, message_id);
     }
 
     /* Agent:Protocol access denied */
     protocol_perms = agent_protocol_permissions(agent_id, protocol_id);
     if (protocol_perms == MOD_RES_PERMS_ACCESS_DENIED) {
-        return MOD_RES_PERMS_ACCESS_DENIED;
+	return MOD_RES_PERMS_ACCESS_DENIED;
     }
 
     /* No Agent:Protocol:command permissions management */
     if (resources_perms_ctx.agent_permissions->agent_msg_permissions == NULL) {
-        return MOD_RES_PERMS_ACCESS_ALLOWED;
+	return MOD_RES_PERMS_ACCESS_ALLOWED;
     }
 
     status = mod_res_message_id_to_index(protocol_id, message_id, &message_idx);
     if (status != FWK_SUCCESS) {
-        return MOD_RES_PERMS_ACCESS_DENIED;
+	return MOD_RES_PERMS_ACCESS_DENIED;
     }
 
     if (message_idx < 0) {
-        return MOD_RES_PERMS_ACCESS_ALLOWED;
+	return MOD_RES_PERMS_ACCESS_ALLOWED;
     }
 
     status = mod_res_agent_id_to_index(agent_id, &agent_idx);
     if (status != FWK_SUCCESS) {
-        return MOD_RES_PERMS_ACCESS_DENIED;
+	return MOD_RES_PERMS_ACCESS_DENIED;
     }
 
     status = mod_res_protocol_id_to_index(protocol_id, &protocol_idx);
     if (status != FWK_SUCCESS) {
-        return MOD_RES_PERMS_ACCESS_DENIED;
+	return MOD_RES_PERMS_ACCESS_DENIED;
     }
 
     perms =
-        resources_perms_ctx.agent_permissions->agent_msg_permissions[agent_idx]
-            .messages[protocol_idx];
+	resources_perms_ctx.agent_permissions->agent_msg_permissions[agent_idx]
+	    .messages[protocol_idx];
 
     /* Agent:Protocol:message access denied */
     if (perms & (1U << (uint32_t)message_idx)) {
-        return MOD_RES_PERMS_ACCESS_DENIED;
+	return MOD_RES_PERMS_ACCESS_DENIED;
     }
 
     return MOD_RES_PERMS_ACCESS_ALLOWED;
@@ -649,10 +649,10 @@ static enum mod_res_perms_permissions ag
  */
 __attribute((weak)) enum mod_res_perms_permissions
     mod_res_plat_agent_resource_permissions(
-        uint32_t agent_id,
-        uint32_t protocol_id,
-        uint32_t message_id,
-        uint32_t resource_id)
+	uint32_t agent_id,
+	uint32_t protocol_id,
+	uint32_t message_id,
+	uint32_t resource_id)
 {
     return MOD_RES_PERMS_ACCESS_DENIED;
 }
@@ -672,127 +672,127 @@ static enum mod_res_perms_permissions ag
 
     /* No permissions management */
     if ((agent_id == 0) || (resources_perms_ctx.agent_permissions == NULL)) {
-        return MOD_RES_PERMS_ACCESS_ALLOWED;
+	return MOD_RES_PERMS_ACCESS_ALLOWED;
     }
 
     if ((protocol_id >= MOD_SCMI_PLATFORM_PROTOCOL_ID_MIN) &&
-        (protocol_id <= MOD_SCMI_PLATFORM_PROTOCOL_ID_MAX)) {
-        return mod_res_plat_agent_resource_permissions(
-            agent_id, protocol_id, message_id, resource_id);
+	(protocol_id <= MOD_SCMI_PLATFORM_PROTOCOL_ID_MAX)) {
+	return mod_res_plat_agent_resource_permissions(
+	    agent_id, protocol_id, message_id, resource_id);
     }
 
     /* Agent:Protocol:command access denied */
     message_perms =
-        agent_message_permissions(agent_id, protocol_id, message_id);
+	agent_message_permissions(agent_id, protocol_id, message_id);
     if (message_perms == MOD_RES_PERMS_ACCESS_DENIED) {
-        return MOD_RES_PERMS_ACCESS_DENIED;
+	return MOD_RES_PERMS_ACCESS_DENIED;
     }
 
     status = mod_res_message_id_to_index(protocol_id, message_id, &message_idx);
     if (status != FWK_SUCCESS) {
-        return MOD_RES_PERMS_ACCESS_DENIED;
+	return MOD_RES_PERMS_ACCESS_DENIED;
     }
     if (message_idx < 0) {
-        return MOD_RES_PERMS_ACCESS_ALLOWED;
+	return MOD_RES_PERMS_ACCESS_ALLOWED;
     }
 
     status = mod_res_agent_id_to_index(agent_id, &agent_idx);
     if (status != FWK_SUCCESS) {
-        return MOD_RES_PERMS_ACCESS_DENIED;
+	return MOD_RES_PERMS_ACCESS_DENIED;
     }
 
     status = mod_res_resource_id_to_index(
-        agent_id, protocol_id, message_id, resource_id, &resource_idx);
+	agent_id, protocol_id, message_id, resource_id, &resource_idx);
     if (status != FWK_SUCCESS) {
-        return MOD_RES_PERMS_ACCESS_DENIED;
+	return MOD_RES_PERMS_ACCESS_DENIED;
     }
     if (resource_idx < 0) {
-        return MOD_RES_PERMS_ACCESS_ALLOWED;
+	return MOD_RES_PERMS_ACCESS_ALLOWED;
     }
 
     switch (protocol_id) {
     case MOD_SCMI_PROTOCOL_ID_BASE:
-        return MOD_RES_PERMS_ACCESS_DENIED;
+	return MOD_RES_PERMS_ACCESS_DENIED;
 
     case MOD_SCMI_PROTOCOL_ID_POWER_DOMAIN:
-        if (resources_perms_ctx.agent_permissions->scmi_pd_perms == NULL) {
-            return MOD_RES_PERMS_ACCESS_ALLOWED;
-        }
-        if (resource_id >= resources_perms_ctx.pd_count) {
-            return MOD_RES_PERMS_ACCESS_DENIED;
-        }
-        perms =
-            resources_perms_ctx.agent_permissions->scmi_pd_perms[resource_idx];
-        break;
+	if (resources_perms_ctx.agent_permissions->scmi_pd_perms == NULL) {
+	    return MOD_RES_PERMS_ACCESS_ALLOWED;
+	}
+	if (resource_id >= resources_perms_ctx.pd_count) {
+	    return MOD_RES_PERMS_ACCESS_DENIED;
+	}
+	perms =
+	    resources_perms_ctx.agent_permissions->scmi_pd_perms[resource_idx];
+	break;
 
     case MOD_SCMI_PROTOCOL_ID_SYS_POWER:
-        return MOD_RES_PERMS_ACCESS_DENIED;
+	return MOD_RES_PERMS_ACCESS_DENIED;
 
     case MOD_SCMI_PROTOCOL_ID_PERF:
-        if (resources_perms_ctx.agent_permissions->scmi_perf_perms == NULL) {
-            return MOD_RES_PERMS_ACCESS_ALLOWED;
-        }
-        if (resource_id >= resources_perms_ctx.perf_count) {
-            return MOD_RES_PERMS_ACCESS_DENIED;
-        }
-        perms = resources_perms_ctx.agent_permissions
-                    ->scmi_perf_perms[resource_idx];
-        break;
+	if (resources_perms_ctx.agent_permissions->scmi_perf_perms == NULL) {
+	    return MOD_RES_PERMS_ACCESS_ALLOWED;
+	}
+	if (resource_id >= resources_perms_ctx.perf_count) {
+	    return MOD_RES_PERMS_ACCESS_DENIED;
+	}
+	perms = resources_perms_ctx.agent_permissions
+		    ->scmi_perf_perms[resource_idx];
+	break;
 
     case MOD_SCMI_PROTOCOL_ID_CLOCK:
-        if (resources_perms_ctx.agent_permissions->scmi_clock_perms == NULL) {
-            return MOD_RES_PERMS_ACCESS_ALLOWED;
-        }
-        if (resource_id >= resources_perms_ctx.clock_count) {
-            return MOD_RES_PERMS_ACCESS_DENIED;
-        }
-        perms = resources_perms_ctx.agent_permissions
-                    ->scmi_clock_perms[resource_idx];
-        break;
+	if (resources_perms_ctx.agent_permissions->scmi_clock_perms == NULL) {
+	    return MOD_RES_PERMS_ACCESS_ALLOWED;
+	}
+	if (resource_id >= resources_perms_ctx.clock_count) {
+	    return MOD_RES_PERMS_ACCESS_DENIED;
+	}
+	perms = resources_perms_ctx.agent_permissions
+		    ->scmi_clock_perms[resource_idx];
+	break;
 
     case MOD_SCMI_PROTOCOL_ID_SENSOR:
-        if (resources_perms_ctx.agent_permissions->scmi_sensor_perms == NULL) {
-            return MOD_RES_PERMS_ACCESS_ALLOWED;
-        }
-        if (resource_id >= resources_perms_ctx.sensor_count) {
-            return MOD_RES_PERMS_ACCESS_DENIED;
-        }
-        perms = resources_perms_ctx.agent_permissions
-                    ->scmi_sensor_perms[resource_idx];
-        break;
+	if (resources_perms_ctx.agent_permissions->scmi_sensor_perms == NULL) {
+	    return MOD_RES_PERMS_ACCESS_ALLOWED;
+	}
+	if (resource_id >= resources_perms_ctx.sensor_count) {
+	    return MOD_RES_PERMS_ACCESS_DENIED;
+	}
+	perms = resources_perms_ctx.agent_permissions
+		    ->scmi_sensor_perms[resource_idx];
+	break;
 
 #ifdef BUILD_HAS_MOD_SCMI_RESET_DOMAIN
     case MOD_SCMI_PROTOCOL_ID_RESET_DOMAIN:
-        if (resources_perms_ctx.agent_permissions->scmi_reset_domain_perms ==
-            NULL) {
-            return MOD_RES_PERMS_ACCESS_ALLOWED;
-        }
-        if (resource_id >= resources_perms_ctx.reset_domain_count) {
-            return MOD_RES_PERMS_ACCESS_DENIED;
-        }
-        perms = resources_perms_ctx.agent_permissions
-                    ->scmi_reset_domain_perms[resource_idx];
-        break;
+	if (resources_perms_ctx.agent_permissions->scmi_reset_domain_perms ==
+	    NULL) {
+	    return MOD_RES_PERMS_ACCESS_ALLOWED;
+	}
+	if (resource_id >= resources_perms_ctx.reset_domain_count) {
+	    return MOD_RES_PERMS_ACCESS_DENIED;
+	}
+	perms = resources_perms_ctx.agent_permissions
+		    ->scmi_reset_domain_perms[resource_idx];
+	break;
 #endif
 
     case MOD_SCMI_PROTOCOL_ID_VOLTAGE_DOMAIN:
-        if (resources_perms_ctx.agent_permissions->scmi_voltd_perms == NULL) {
-            return MOD_RES_PERMS_ACCESS_ALLOWED;
-        }
-        if (resource_id >= resources_perms_ctx.voltd_count) {
-            return MOD_RES_PERMS_ACCESS_DENIED;
-        }
-        perms = resources_perms_ctx.agent_permissions
-                    ->scmi_voltd_perms[resource_idx];
-        break;
+	if (resources_perms_ctx.agent_permissions->scmi_voltd_perms == NULL) {
+	    return MOD_RES_PERMS_ACCESS_ALLOWED;
+	}
+	if (resource_id >= resources_perms_ctx.voltd_count) {
+	    return MOD_RES_PERMS_ACCESS_DENIED;
+	}
+	perms = resources_perms_ctx.agent_permissions
+		    ->scmi_voltd_perms[resource_idx];
+	break;
 
     default:
-        return MOD_RES_PERMS_ACCESS_DENIED;
+	return MOD_RES_PERMS_ACCESS_DENIED;
     }
 
     /* Agent:Protocol:message:resource access denied */
     if ((perms & (1U << (MOD_RES_PERMS_RESOURCE_BIT(resource_id))))) {
-        return MOD_RES_PERMS_ACCESS_DENIED;
+	return MOD_RES_PERMS_ACCESS_DENIED;
     }
 
     return MOD_RES_PERMS_ACCESS_ALLOWED;
@@ -814,21 +814,21 @@ static int set_agent_resource_message_pe
     mod_res_perms_t permissions;
 
     status = mod_res_resource_id_to_index(
-        agent_id, protocol_id, message_idx, resource_id, &resource_idx);
+	agent_id, protocol_id, message_idx, resource_id, &resource_idx);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     permissions = perms[resource_idx];
 
     flags =
-        ((flags == 0) ? MOD_RES_PERMS_ACCESS_DENIED :
-                        MOD_RES_PERMS_ACCESS_ALLOWED);
+	((flags == 0) ? MOD_RES_PERMS_ACCESS_DENIED :
+			MOD_RES_PERMS_ACCESS_ALLOWED);
 
     if (flags == MOD_RES_PERMS_ACCESS_ALLOWED) {
-        permissions &= ~(1U << (MOD_RES_PERMS_RESOURCE_BIT(resource_id)));
+	permissions &= ~(1U << (MOD_RES_PERMS_RESOURCE_BIT(resource_id)));
     } else {
-        permissions |= (1U << (MOD_RES_PERMS_RESOURCE_BIT(resource_id)));
+	permissions |= (1U << (MOD_RES_PERMS_RESOURCE_BIT(resource_id)));
     }
 
     perms[resource_idx] = permissions;
@@ -845,44 +845,44 @@ static int set_agent_resource_pd_permiss
     int32_t message_idx;
 
     if (resources_perms_ctx.agent_permissions->scmi_pd_perms == NULL) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
     if (resource_id >= resources_perms_ctx.pd_count) {
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 
     /* Do a backup before making the changes if required */
     if ((resources_perms_ctx.config->pd_cmd_count != 0) &&
-        (resources_perms_ctx.config->pd_resource_count != 0) &&
-        (resources_perms_backup.scmi_pd_perms == NULL)) {
-        resources_perms_backup.scmi_pd_perms = (mod_res_perms_t *)fwk_mm_alloc(
-            resources_perms_ctx.agent_count *
-                resources_perms_ctx.config->pd_cmd_count *
-                resources_perms_ctx.config->pd_resource_count,
-            sizeof(mod_res_perms_t));
-        fwk_str_memcpy(
-            resources_perms_backup.scmi_pd_perms,
-            resources_perms_ctx.agent_permissions->scmi_pd_perms,
-            resources_perms_ctx.agent_count *
-                resources_perms_ctx.config->pd_cmd_count *
-                resources_perms_ctx.config->pd_resource_count *
-                sizeof(mod_res_perms_t));
+	(resources_perms_ctx.config->pd_resource_count != 0) &&
+	(resources_perms_backup.scmi_pd_perms == NULL)) {
+	resources_perms_backup.scmi_pd_perms = (mod_res_perms_t *)fwk_mm_alloc(
+	    resources_perms_ctx.agent_count *
+		resources_perms_ctx.config->pd_cmd_count *
+		resources_perms_ctx.config->pd_resource_count,
+	    sizeof(mod_res_perms_t));
+	fwk_str_memcpy(
+	    resources_perms_backup.scmi_pd_perms,
+	    resources_perms_ctx.agent_permissions->scmi_pd_perms,
+	    resources_perms_ctx.agent_count *
+		resources_perms_ctx.config->pd_cmd_count *
+		resources_perms_ctx.config->pd_resource_count *
+		sizeof(mod_res_perms_t));
     }
 
     for (message_idx = (int32_t)MOD_SCMI_PD_POWER_DOMAIN_ATTRIBUTES;
-         message_idx <= MOD_SCMI_PD_POWER_STATE_NOTIFY;
-         message_idx++) {
-        status = set_agent_resource_message_perms(
-            agent_id,
-            MOD_SCMI_PROTOCOL_ID_POWER_DOMAIN,
-            (uint32_t)message_idx,
-            resource_id,
-            resources_perms_ctx.agent_permissions->scmi_pd_perms,
-            flags);
-
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
+	 message_idx <= MOD_SCMI_PD_POWER_STATE_NOTIFY;
+	 message_idx++) {
+	status = set_agent_resource_message_perms(
+	    agent_id,
+	    MOD_SCMI_PROTOCOL_ID_POWER_DOMAIN,
+	    (uint32_t)message_idx,
+	    resource_id,
+	    resources_perms_ctx.agent_permissions->scmi_pd_perms,
+	    flags);
+
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
     }
 
     return FWK_SUCCESS;
@@ -897,45 +897,45 @@ static int set_agent_resource_perf_permi
     int32_t message_idx;
 
     if (resources_perms_ctx.agent_permissions->scmi_perf_perms == NULL) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
     if (resource_id >= resources_perms_ctx.perf_count) {
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 
     /* Do a backup before making the changes if required */
     if ((resources_perms_ctx.config->perf_cmd_count != 0) &&
-        (resources_perms_ctx.config->perf_resource_count != 0) &&
-        (resources_perms_backup.scmi_perf_perms == NULL)) {
-        resources_perms_backup.scmi_perf_perms =
-            (mod_res_perms_t *)fwk_mm_alloc(
-                resources_perms_ctx.agent_count *
-                    resources_perms_ctx.config->perf_cmd_count *
-                    resources_perms_ctx.config->perf_resource_count,
-                sizeof(mod_res_perms_t));
-        fwk_str_memcpy(
-            resources_perms_backup.scmi_perf_perms,
-            resources_perms_ctx.agent_permissions->scmi_perf_perms,
-            resources_perms_ctx.agent_count *
-                resources_perms_ctx.config->perf_cmd_count *
-                resources_perms_ctx.config->perf_resource_count *
-                sizeof(mod_res_perms_t));
+	(resources_perms_ctx.config->perf_resource_count != 0) &&
+	(resources_perms_backup.scmi_perf_perms == NULL)) {
+	resources_perms_backup.scmi_perf_perms =
+	    (mod_res_perms_t *)fwk_mm_alloc(
+		resources_perms_ctx.agent_count *
+		    resources_perms_ctx.config->perf_cmd_count *
+		    resources_perms_ctx.config->perf_resource_count,
+		sizeof(mod_res_perms_t));
+	fwk_str_memcpy(
+	    resources_perms_backup.scmi_perf_perms,
+	    resources_perms_ctx.agent_permissions->scmi_perf_perms,
+	    resources_perms_ctx.agent_count *
+		resources_perms_ctx.config->perf_cmd_count *
+		resources_perms_ctx.config->perf_resource_count *
+		sizeof(mod_res_perms_t));
     }
 
     for (message_idx = (int32_t)MOD_SCMI_PERF_DOMAIN_ATTRIBUTES;
-         message_idx <= MOD_SCMI_PERF_DESCRIBE_FAST_CHANNEL;
-         message_idx++) {
-        status = set_agent_resource_message_perms(
-            agent_id,
-            MOD_SCMI_PROTOCOL_ID_PERF,
-            (uint32_t)message_idx,
-            resource_id,
-            resources_perms_ctx.agent_permissions->scmi_perf_perms,
-            flags);
-
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
+	 message_idx <= MOD_SCMI_PERF_DESCRIBE_FAST_CHANNEL;
+	 message_idx++) {
+	status = set_agent_resource_message_perms(
+	    agent_id,
+	    MOD_SCMI_PROTOCOL_ID_PERF,
+	    (uint32_t)message_idx,
+	    resource_id,
+	    resources_perms_ctx.agent_permissions->scmi_perf_perms,
+	    flags);
+
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
     }
 
     return FWK_SUCCESS;
@@ -950,45 +950,45 @@ static int set_agent_resource_clock_perm
     int32_t message_idx;
 
     if (resources_perms_ctx.agent_permissions->scmi_clock_perms == NULL) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
     if (resource_id >= resources_perms_ctx.clock_count) {
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 
     /* Do a backup before making the changes if required */
     if ((resources_perms_ctx.config->clock_cmd_count != 0) &&
-        (resources_perms_ctx.config->clock_resource_count != 0) &&
-        (resources_perms_backup.scmi_clock_perms == NULL)) {
-        resources_perms_backup.scmi_clock_perms =
-            (mod_res_perms_t *)fwk_mm_alloc(
-                resources_perms_ctx.agent_count *
-                    resources_perms_ctx.config->clock_cmd_count *
-                    resources_perms_ctx.config->clock_resource_count,
-                sizeof(mod_res_perms_t));
-        fwk_str_memcpy(
-            resources_perms_backup.scmi_clock_perms,
-            resources_perms_ctx.agent_permissions->scmi_clock_perms,
-            resources_perms_ctx.agent_count *
-                resources_perms_ctx.config->clock_cmd_count *
-                resources_perms_ctx.config->clock_resource_count *
-                sizeof(mod_res_perms_t));
+	(resources_perms_ctx.config->clock_resource_count != 0) &&
+	(resources_perms_backup.scmi_clock_perms == NULL)) {
+	resources_perms_backup.scmi_clock_perms =
+	    (mod_res_perms_t *)fwk_mm_alloc(
+		resources_perms_ctx.agent_count *
+		    resources_perms_ctx.config->clock_cmd_count *
+		    resources_perms_ctx.config->clock_resource_count,
+		sizeof(mod_res_perms_t));
+	fwk_str_memcpy(
+	    resources_perms_backup.scmi_clock_perms,
+	    resources_perms_ctx.agent_permissions->scmi_clock_perms,
+	    resources_perms_ctx.agent_count *
+		resources_perms_ctx.config->clock_cmd_count *
+		resources_perms_ctx.config->clock_resource_count *
+		sizeof(mod_res_perms_t));
     }
 
     for (message_idx = (int32_t)MOD_SCMI_CLOCK_ATTRIBUTES;
-         message_idx <= MOD_SCMI_CLOCK_CONFIG_SET;
-         message_idx++) {
-        status = set_agent_resource_message_perms(
-            agent_id,
-            MOD_SCMI_PROTOCOL_ID_CLOCK,
-            (uint32_t)message_idx,
-            resource_id,
-            resources_perms_ctx.agent_permissions->scmi_clock_perms,
-            flags);
-
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
+	 message_idx <= MOD_SCMI_CLOCK_CONFIG_SET;
+	 message_idx++) {
+	status = set_agent_resource_message_perms(
+	    agent_id,
+	    MOD_SCMI_PROTOCOL_ID_CLOCK,
+	    (uint32_t)message_idx,
+	    resource_id,
+	    resources_perms_ctx.agent_permissions->scmi_clock_perms,
+	    flags);
+
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
     }
 
     return FWK_SUCCESS;
@@ -1005,46 +1005,46 @@ static int set_agent_resource_reset_perm
     int32_t message_idx;
 
     if (resources_perms_ctx.agent_permissions->scmi_reset_domain_perms ==
-        NULL) {
-        return FWK_SUCCESS;
+	NULL) {
+	return FWK_SUCCESS;
     }
     if (resource_id >= resources_perms_ctx.reset_domain_count) {
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 
     /* Do a backup before making the changes if required */
     if ((resources_perms_ctx.config->reset_domain_cmd_count != 0) &&
-        (resources_perms_ctx.config->reset_domain_resource_count != 0) &&
-        (resources_perms_backup.scmi_reset_domain_perms == NULL)) {
-        resources_perms_backup.scmi_reset_domain_perms =
-            (mod_res_perms_t *)fwk_mm_alloc(
-                resources_perms_ctx.agent_count *
-                    resources_perms_ctx.config->reset_domain_cmd_count *
-                    resources_perms_ctx.config->reset_domain_resource_count,
-                sizeof(mod_res_perms_t));
-        fwk_str_memcpy(
-            resources_perms_backup.scmi_reset_domain_perms,
-            resources_perms_ctx.agent_permissions->scmi_reset_domain_perms,
-            resources_perms_ctx.agent_count *
-                resources_perms_ctx.config->reset_domain_cmd_count *
-                resources_perms_ctx.config->reset_domain_cmd_count *
-                sizeof(mod_res_perms_t));
+	(resources_perms_ctx.config->reset_domain_resource_count != 0) &&
+	(resources_perms_backup.scmi_reset_domain_perms == NULL)) {
+	resources_perms_backup.scmi_reset_domain_perms =
+	    (mod_res_perms_t *)fwk_mm_alloc(
+		resources_perms_ctx.agent_count *
+		    resources_perms_ctx.config->reset_domain_cmd_count *
+		    resources_perms_ctx.config->reset_domain_resource_count,
+		sizeof(mod_res_perms_t));
+	fwk_str_memcpy(
+	    resources_perms_backup.scmi_reset_domain_perms,
+	    resources_perms_ctx.agent_permissions->scmi_reset_domain_perms,
+	    resources_perms_ctx.agent_count *
+		resources_perms_ctx.config->reset_domain_cmd_count *
+		resources_perms_ctx.config->reset_domain_cmd_count *
+		sizeof(mod_res_perms_t));
     }
 
     for (message_idx = MOD_SCMI_RESET_DOMAIN_ATTRIBUTES;
-         message_idx <= MOD_SCMI_RESET_NOTIFY;
-         message_idx++) {
-        status = set_agent_resource_message_perms(
-            agent_id,
-            MOD_SCMI_PROTOCOL_ID_RESET_DOMAIN,
-            message_idx,
-            resource_id,
-            resources_perms_ctx.agent_permissions->scmi_reset_domain_perms,
-            flags);
-
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
+	 message_idx <= MOD_SCMI_RESET_NOTIFY;
+	 message_idx++) {
+	status = set_agent_resource_message_perms(
+	    agent_id,
+	    MOD_SCMI_PROTOCOL_ID_RESET_DOMAIN,
+	    message_idx,
+	    resource_id,
+	    resources_perms_ctx.agent_permissions->scmi_reset_domain_perms,
+	    flags);
+
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
     }
 
     return FWK_SUCCESS;
@@ -1061,45 +1061,45 @@ static int set_agent_resource_voltd_perm
     int32_t message_idx;
 
     if (resources_perms_ctx.agent_permissions->scmi_voltd_perms == NULL) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
     if (resource_id >= resources_perms_ctx.voltd_count) {
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 
     /* Do a backup before making the changes if required */
     if ((resources_perms_ctx.config->voltd_cmd_count != 0) &&
-        (resources_perms_ctx.config->voltd_resource_count != 0) &&
-        (resources_perms_backup.scmi_voltd_perms == NULL)) {
-        resources_perms_backup.scmi_voltd_perms =
-            (mod_res_perms_t *)fwk_mm_alloc(
-                resources_perms_ctx.agent_count *
-                    resources_perms_ctx.config->voltd_cmd_count *
-                    resources_perms_ctx.config->voltd_resource_count,
-                sizeof(mod_res_perms_t));
-        fwk_str_memcpy(
-            resources_perms_backup.scmi_voltd_perms,
-            resources_perms_ctx.agent_permissions->scmi_voltd_perms,
-            resources_perms_ctx.agent_count *
-                resources_perms_ctx.config->voltd_cmd_count *
-                resources_perms_ctx.config->voltd_resource_count *
-                sizeof(mod_res_perms_t));
+	(resources_perms_ctx.config->voltd_resource_count != 0) &&
+	(resources_perms_backup.scmi_voltd_perms == NULL)) {
+	resources_perms_backup.scmi_voltd_perms =
+	    (mod_res_perms_t *)fwk_mm_alloc(
+		resources_perms_ctx.agent_count *
+		    resources_perms_ctx.config->voltd_cmd_count *
+		    resources_perms_ctx.config->voltd_resource_count,
+		sizeof(mod_res_perms_t));
+	fwk_str_memcpy(
+	    resources_perms_backup.scmi_voltd_perms,
+	    resources_perms_ctx.agent_permissions->scmi_voltd_perms,
+	    resources_perms_ctx.agent_count *
+		resources_perms_ctx.config->voltd_cmd_count *
+		resources_perms_ctx.config->voltd_resource_count *
+		sizeof(mod_res_perms_t));
     }
 
     for (message_idx = (int32_t)MOD_SCMI_VOLTD_DOMAIN_ATTRIBUTES;
-         message_idx <= MOD_SCMI_VOLTD_LEVEL_GET;
-         message_idx++) {
-        status = set_agent_resource_message_perms(
-            agent_id,
-            MOD_SCMI_PROTOCOL_ID_VOLTAGE_DOMAIN,
-            (uint32_t)message_idx,
-            resource_id,
-            resources_perms_ctx.agent_permissions->scmi_voltd_perms,
-            flags);
-
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
+	 message_idx <= MOD_SCMI_VOLTD_LEVEL_GET;
+	 message_idx++) {
+	status = set_agent_resource_message_perms(
+	    agent_id,
+	    MOD_SCMI_PROTOCOL_ID_VOLTAGE_DOMAIN,
+	    (uint32_t)message_idx,
+	    resource_id,
+	    resources_perms_ctx.agent_permissions->scmi_voltd_perms,
+	    flags);
+
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
     }
 
     return FWK_SUCCESS;
@@ -1114,45 +1114,45 @@ static int set_agent_resource_sensor_per
     int32_t message_idx;
 
     if (resources_perms_ctx.agent_permissions->scmi_sensor_perms == NULL) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
     if (resource_id >= resources_perms_ctx.sensor_count) {
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 
     /* Do a backup before making the changes if required */
     if ((resources_perms_ctx.config->sensor_cmd_count != 0) &&
-        (resources_perms_ctx.config->sensor_resource_count != 0) &&
-        (resources_perms_backup.scmi_sensor_perms == NULL)) {
-        resources_perms_backup.scmi_sensor_perms =
-            (mod_res_perms_t *)fwk_mm_alloc(
-                resources_perms_ctx.agent_count *
-                    resources_perms_ctx.config->sensor_cmd_count *
-                    resources_perms_ctx.config->sensor_resource_count,
-                sizeof(mod_res_perms_t));
-        fwk_str_memcpy(
-            resources_perms_backup.scmi_sensor_perms,
-            resources_perms_ctx.agent_permissions->scmi_sensor_perms,
-            resources_perms_ctx.agent_count *
-                resources_perms_ctx.config->sensor_cmd_count *
-                resources_perms_ctx.config->sensor_resource_count *
-                sizeof(mod_res_perms_t));
+	(resources_perms_ctx.config->sensor_resource_count != 0) &&
+	(resources_perms_backup.scmi_sensor_perms == NULL)) {
+	resources_perms_backup.scmi_sensor_perms =
+	    (mod_res_perms_t *)fwk_mm_alloc(
+		resources_perms_ctx.agent_count *
+		    resources_perms_ctx.config->sensor_cmd_count *
+		    resources_perms_ctx.config->sensor_resource_count,
+		sizeof(mod_res_perms_t));
+	fwk_str_memcpy(
+	    resources_perms_backup.scmi_sensor_perms,
+	    resources_perms_ctx.agent_permissions->scmi_sensor_perms,
+	    resources_perms_ctx.agent_count *
+		resources_perms_ctx.config->sensor_cmd_count *
+		resources_perms_ctx.config->sensor_resource_count *
+		sizeof(mod_res_perms_t));
     }
 
     for (message_idx = (int32_t)MOD_SCMI_SENSOR_DESCRIPTION_GET;
-         message_idx <= MOD_SCMI_SENSOR_READING_GET;
-         message_idx++) {
-        status = set_agent_resource_message_perms(
-            agent_id,
-            MOD_SCMI_PROTOCOL_ID_SENSOR,
-            (uint32_t)message_idx,
-            resource_id,
-            resources_perms_ctx.agent_permissions->scmi_sensor_perms,
-            flags);
-
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
+	 message_idx <= MOD_SCMI_SENSOR_READING_GET;
+	 message_idx++) {
+	status = set_agent_resource_message_perms(
+	    agent_id,
+	    MOD_SCMI_PROTOCOL_ID_SENSOR,
+	    (uint32_t)message_idx,
+	    resource_id,
+	    resources_perms_ctx.agent_permissions->scmi_sensor_perms,
+	    flags);
+
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
     }
 
     return FWK_SUCCESS;
@@ -1168,48 +1168,48 @@ static int mod_res_agent_set_permissions
 
     switch (type) {
     case MOD_RES_POWER_DOMAIN_DEVICE:
-        status =
-            set_agent_resource_pd_permissions(agent_id, resource_id, flags);
-        break;
+	status =
+	    set_agent_resource_pd_permissions(agent_id, resource_id, flags);
+	break;
 
     case MOD_RES_PERF_DOMAIN_DEVICE:
-        status =
-            set_agent_resource_perf_permissions(agent_id, resource_id, flags);
-        break;
+	status =
+	    set_agent_resource_perf_permissions(agent_id, resource_id, flags);
+	break;
 
     case MOD_RES_CLOCK_DOMAIN_DEVICE:
-        status =
-            set_agent_resource_clock_permissions(agent_id, resource_id, flags);
-        break;
+	status =
+	    set_agent_resource_clock_permissions(agent_id, resource_id, flags);
+	break;
 
     case MOD_RES_SENSOR_DOMAIN_DEVICE:
-        status =
-            set_agent_resource_sensor_permissions(agent_id, resource_id, flags);
-        break;
+	status =
+	    set_agent_resource_sensor_permissions(agent_id, resource_id, flags);
+	break;
 
 #ifdef BUILD_HAS_MOD_SCMI_RESET_DOMAIN
     case MOD_RES_RESET_DOMAIN_DEVICE:
-        status =
-            set_agent_resource_reset_permissions(agent_id, resource_id, flags);
-        break;
+	status =
+	    set_agent_resource_reset_permissions(agent_id, resource_id, flags);
+	break;
 #endif
 
     case MOD_RES_VOLTAGE_DOMAIN_DEVICE:
-        status =
-            set_agent_resource_voltd_permissions(agent_id, resource_id, flags);
-        break;
+	status =
+	    set_agent_resource_voltd_permissions(agent_id, resource_id, flags);
+	break;
 
     default:
-        status = FWK_E_ACCESS;
-        break;
+	status = FWK_E_ACCESS;
+	break;
     }
 
     if (status != FWK_SUCCESS) {
-        FWK_LOG_WARN(
-            "[perms] set_permissions for agent %d type %d device %d failed\n",
-            (int)agent_id,
-            (int)type,
-            (int)resource_id);
+	FWK_LOG_WARN(
+	    "[perms] set_permissions for agent %d type %d device %d failed\n",
+	    (int)agent_id,
+	    (int)type,
+	    (int)resource_id);
     }
 
     return status;
@@ -1227,52 +1227,52 @@ static int mod_res_agent_set_device_perm
 
     /* No device permissons */
     if ((resources_perms_ctx.device_count == 0) ||
-        (resources_perms_ctx.domain_devices == NULL)) {
-        return FWK_E_ACCESS;
+	(resources_perms_ctx.domain_devices == NULL)) {
+	return FWK_E_ACCESS;
     }
 
     if (device_id >= resources_perms_ctx.device_count) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     if ((flags != 0) && (flags != 1)) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     /* Find device */
     for (i = 0; i < resources_perms_ctx.device_count; i++) {
-        if (resources_perms_ctx.domain_devices[i].device_id == device_id) {
-            break;
-        }
+	if (resources_perms_ctx.domain_devices[i].device_id == device_id) {
+	    break;
+	}
     }
 
     if (i == resources_perms_ctx.device_count) {
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 
     dev = resources_perms_ctx.domain_devices[i].domain_devices;
     while (dev->type != MOD_RES_DOMAIN_DEVICE_INVALID) {
-        if (fwk_id_is_equal(dev->device_id, FWK_ID_NONE)) {
-            return FWK_SUCCESS;
-        }
-
-        resource_id = fwk_id_get_element_idx(dev->device_id);
-
-        status = mod_res_agent_set_permissions(
-            dev->type,
-            agent_id,
-            resource_id,
-            (enum mod_res_perms_permissions)flags);
-
-        if (status != FWK_SUCCESS) {
-            FWK_LOG_WARN(
-                "[perms] set_permissions for agent %d device %d failed\n",
-                (int)agent_id,
-                (int)i);
-        }
+	if (fwk_id_is_equal(dev->device_id, FWK_ID_NONE)) {
+	    return FWK_SUCCESS;
+	}
+
+	resource_id = fwk_id_get_element_idx(dev->device_id);
+
+	status = mod_res_agent_set_permissions(
+	    dev->type,
+	    agent_id,
+	    resource_id,
+	    (enum mod_res_perms_permissions)flags);
+
+	if (status != FWK_SUCCESS) {
+	    FWK_LOG_WARN(
+		"[perms] set_permissions for agent %d device %d failed\n",
+		(int)agent_id,
+		(int)i);
+	}
 
-        /* next domain device */
-        dev++;
+	/* next domain device */
+	dev++;
     };
 
     return FWK_SUCCESS;
@@ -1295,82 +1295,82 @@ static int mod_res_agent_set_device_prot
 
     /* No device permissons */
     if ((resources_perms_ctx.device_count == 0) ||
-        (resources_perms_ctx.domain_devices == NULL)) {
-        return FWK_E_ACCESS;
+	(resources_perms_ctx.domain_devices == NULL)) {
+	return FWK_E_ACCESS;
     }
 
     if (device_id >= resources_perms_ctx.device_count) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     if ((flags != 0) && (flags != 1)) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     /* Find device */
     for (i = 0; i < resources_perms_ctx.device_count; i++) {
-        if (resources_perms_ctx.domain_devices[i].device_id == device_id) {
-            break;
-        }
+	if (resources_perms_ctx.domain_devices[i].device_id == device_id) {
+	    break;
+	}
     }
 
     if (i == resources_perms_ctx.device_count) {
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 
     switch (protocol_id) {
     case MOD_SCMI_PROTOCOL_ID_POWER_DOMAIN:
-        dev_type = MOD_RES_POWER_DOMAIN_DEVICE;
-        break;
+	dev_type = MOD_RES_POWER_DOMAIN_DEVICE;
+	break;
     case MOD_SCMI_PROTOCOL_ID_PERF:
-        dev_type = MOD_RES_PERF_DOMAIN_DEVICE;
-        break;
+	dev_type = MOD_RES_PERF_DOMAIN_DEVICE;
+	break;
     case MOD_SCMI_PROTOCOL_ID_CLOCK:
-        dev_type = MOD_RES_CLOCK_DOMAIN_DEVICE;
-        break;
+	dev_type = MOD_RES_CLOCK_DOMAIN_DEVICE;
+	break;
     case MOD_SCMI_PROTOCOL_ID_SENSOR:
-        dev_type = MOD_RES_SENSOR_DOMAIN_DEVICE;
-        break;
+	dev_type = MOD_RES_SENSOR_DOMAIN_DEVICE;
+	break;
     case MOD_SCMI_PROTOCOL_ID_RESET_DOMAIN:
-        dev_type = MOD_RES_RESET_DOMAIN_DEVICE;
-        break;
+	dev_type = MOD_RES_RESET_DOMAIN_DEVICE;
+	break;
     case MOD_SCMI_PROTOCOL_ID_VOLTAGE_DOMAIN:
-        dev_type = MOD_RES_VOLTAGE_DOMAIN_DEVICE;
-        break;
+	dev_type = MOD_RES_VOLTAGE_DOMAIN_DEVICE;
+	break;
     case MOD_SCMI_PROTOCOL_ID_BASE:
     case MOD_SCMI_PROTOCOL_ID_SYS_POWER:
     default:
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 
     dev = resources_perms_ctx.domain_devices[i].domain_devices;
     while (dev->type != MOD_RES_DOMAIN_DEVICE_INVALID) {
-        if (fwk_id_is_equal(dev->device_id, FWK_ID_NONE)) {
-            return FWK_SUCCESS;
-        }
-
-        if (dev->type != dev_type) {
-            dev++;
-            continue;
-        }
-
-        resource_id = fwk_id_get_element_idx(dev->device_id);
-
-        status = mod_res_agent_set_permissions(
-            dev->type,
-            agent_id,
-            resource_id,
-            (enum mod_res_perms_permissions)flags);
-
-        if (status != FWK_SUCCESS) {
-            FWK_LOG_WARN(
-                "[perms] set_permissions for agent %d device %d failed\n",
-                (int)agent_id,
-                (int)i);
-        }
+	if (fwk_id_is_equal(dev->device_id, FWK_ID_NONE)) {
+	    return FWK_SUCCESS;
+	}
+
+	if (dev->type != dev_type) {
+	    dev++;
+	    continue;
+	}
+
+	resource_id = fwk_id_get_element_idx(dev->device_id);
+
+	status = mod_res_agent_set_permissions(
+	    dev->type,
+	    agent_id,
+	    resource_id,
+	    (enum mod_res_perms_permissions)flags);
+
+	if (status != FWK_SUCCESS) {
+	    FWK_LOG_WARN(
+		"[perms] set_permissions for agent %d device %d failed\n",
+		(int)agent_id,
+		(int)i);
+	}
 
-        /* next domain device */
-        dev++;
+	/* next domain device */
+	dev++;
     };
 
     return FWK_SUCCESS;
@@ -1398,49 +1398,49 @@ static void mod_res_agent_copy_config(
 
     switch (protocol_id) {
     case MOD_SCMI_PROTOCOL_ID_POWER_DOMAIN:
-        cmd_count = (int)MOD_SCMI_PD_POWER_COMMAND_COUNT;
-        resource_count = (int)resources_perms_ctx.config->pd_resource_count;
-        break;
+	cmd_count = (int)MOD_SCMI_PD_POWER_COMMAND_COUNT;
+	resource_count = (int)resources_perms_ctx.config->pd_resource_count;
+	break;
     case MOD_SCMI_PROTOCOL_ID_PERF:
-        cmd_count = (int)MOD_SCMI_PERF_COMMAND_COUNT;
-        resource_count = (int)resources_perms_ctx.config->perf_resource_count;
-        break;
+	cmd_count = (int)MOD_SCMI_PERF_COMMAND_COUNT;
+	resource_count = (int)resources_perms_ctx.config->perf_resource_count;
+	break;
     case MOD_SCMI_PROTOCOL_ID_CLOCK:
-        cmd_count = (int)MOD_SCMI_CLOCK_COMMAND_COUNT;
-        resource_count = (int)resources_perms_ctx.config->clock_resource_count;
-        break;
+	cmd_count = (int)MOD_SCMI_CLOCK_COMMAND_COUNT;
+	resource_count = (int)resources_perms_ctx.config->clock_resource_count;
+	break;
     case MOD_SCMI_PROTOCOL_ID_SENSOR:
-        cmd_count = (int)MOD_SCMI_SENSOR_COMMAND_COUNT;
-        resource_count = (int)resources_perms_ctx.config->sensor_resource_count;
-        break;
+	cmd_count = (int)MOD_SCMI_SENSOR_COMMAND_COUNT;
+	resource_count = (int)resources_perms_ctx.config->sensor_resource_count;
+	break;
 #ifdef BUILD_HAS_MOD_SCMI_RESET_DOMAIN
     case MOD_SCMI_PROTOCOL_ID_RESET_DOMAIN:
-        cmd_count = (int)MOD_SCMI_RESET_COMMAND_COUNT;
-        resource_count =
-            (int)resources_perms_ctx.config->reset_domain_resource_count;
-        break;
+	cmd_count = (int)MOD_SCMI_RESET_COMMAND_COUNT;
+	resource_count =
+	    (int)resources_perms_ctx.config->reset_domain_resource_count;
+	break;
 #endif
     case MOD_SCMI_PROTOCOL_ID_VOLTAGE_DOMAIN:
-        cmd_count = (int)MOD_SCMI_VOLTD_COMMAND_COUNT;
-        resource_count = (int)resources_perms_ctx.config->voltd_resource_count;
-        break;
+	cmd_count = (int)MOD_SCMI_VOLTD_COMMAND_COUNT;
+	resource_count = (int)resources_perms_ctx.config->voltd_resource_count;
+	break;
     default:
-        return;
+	return;
     }
 
     for (i = 3; i < cmd_count; i++) { /* commands < 3 are excluded */
-        for (j = 0; j < resource_count; j++) {
-            status = mod_res_resource_id_to_index(
-                agent_id,
-                protocol_id,
-                (uint32_t)i, /* message id */
-                (uint32_t)j, /* resource id */
-                &resource_idx);
-            if ((status != FWK_SUCCESS) || (resource_idx < 0)) {
-                continue;
-            }
-            perms[resource_idx] = backup_perms[resource_idx];
-        }
+	for (j = 0; j < resource_count; j++) {
+	    status = mod_res_resource_id_to_index(
+		agent_id,
+		protocol_id,
+		(uint32_t)i, /* message id */
+		(uint32_t)j, /* resource id */
+		&resource_idx);
+	    if ((status != FWK_SUCCESS) || (resource_idx < 0)) {
+		continue;
+	    }
+	    perms[resource_idx] = backup_perms[resource_idx];
+	}
     }
 }
 
@@ -1448,66 +1448,66 @@ static int mod_res_agent_reset_config(ui
 {
     /* No device permissons */
     if ((resources_perms_ctx.device_count == 0) ||
-        (resources_perms_ctx.domain_devices == NULL)) {
-        return FWK_E_ACCESS;
+	(resources_perms_ctx.domain_devices == NULL)) {
+	return FWK_E_ACCESS;
     }
 
     if (flags == 0) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     if (agent_id >= resources_perms_ctx.agent_count) {
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 
     if (resources_perms_backup.scmi_pd_perms != NULL) {
-        mod_res_agent_copy_config(
-            agent_id,
-            resources_perms_ctx.agent_permissions->scmi_pd_perms,
-            resources_perms_backup.scmi_pd_perms,
-            MOD_SCMI_PROTOCOL_ID_POWER_DOMAIN);
+	mod_res_agent_copy_config(
+	    agent_id,
+	    resources_perms_ctx.agent_permissions->scmi_pd_perms,
+	    resources_perms_backup.scmi_pd_perms,
+	    MOD_SCMI_PROTOCOL_ID_POWER_DOMAIN);
     }
 
     if (resources_perms_backup.scmi_perf_perms != NULL) {
-        mod_res_agent_copy_config(
-            agent_id,
-            resources_perms_ctx.agent_permissions->scmi_perf_perms,
-            resources_perms_backup.scmi_perf_perms,
-            MOD_SCMI_PROTOCOL_ID_PERF);
+	mod_res_agent_copy_config(
+	    agent_id,
+	    resources_perms_ctx.agent_permissions->scmi_perf_perms,
+	    resources_perms_backup.scmi_perf_perms,
+	    MOD_SCMI_PROTOCOL_ID_PERF);
     }
 
     if (resources_perms_backup.scmi_clock_perms != NULL) {
-        mod_res_agent_copy_config(
-            agent_id,
-            resources_perms_ctx.agent_permissions->scmi_clock_perms,
-            resources_perms_backup.scmi_clock_perms,
-            MOD_SCMI_PROTOCOL_ID_CLOCK);
+	mod_res_agent_copy_config(
+	    agent_id,
+	    resources_perms_ctx.agent_permissions->scmi_clock_perms,
+	    resources_perms_backup.scmi_clock_perms,
+	    MOD_SCMI_PROTOCOL_ID_CLOCK);
     }
 
     if (resources_perms_backup.scmi_sensor_perms != NULL) {
-        mod_res_agent_copy_config(
-            agent_id,
-            resources_perms_ctx.agent_permissions->scmi_sensor_perms,
-            resources_perms_backup.scmi_sensor_perms,
-            MOD_SCMI_PROTOCOL_ID_SENSOR);
+	mod_res_agent_copy_config(
+	    agent_id,
+	    resources_perms_ctx.agent_permissions->scmi_sensor_perms,
+	    resources_perms_backup.scmi_sensor_perms,
+	    MOD_SCMI_PROTOCOL_ID_SENSOR);
     }
 
 #ifdef BUILD_HAS_MOD_SCMI_RESET_DOMAIN
     if (resources_perms_backup.scmi_reset_domain_perms != NULL) {
-        mod_res_agent_copy_config(
-            agent_id,
-            resources_perms_ctx.agent_permissions->scmi_reset_domain_perms,
-            resources_perms_backup.scmi_reset_domain_perms,
-            MOD_SCMI_PROTOCOL_ID_RESET_DOMAIN);
+	mod_res_agent_copy_config(
+	    agent_id,
+	    resources_perms_ctx.agent_permissions->scmi_reset_domain_perms,
+	    resources_perms_backup.scmi_reset_domain_perms,
+	    MOD_SCMI_PROTOCOL_ID_RESET_DOMAIN);
     }
 #endif
 
     if (resources_perms_backup.scmi_voltd_perms != NULL) {
-        mod_res_agent_copy_config(
-            agent_id,
-            resources_perms_ctx.agent_permissions->scmi_voltd_perms,
-            resources_perms_backup.scmi_voltd_perms,
-            MOD_SCMI_PROTOCOL_ID_VOLTAGE_DOMAIN);
+	mod_res_agent_copy_config(
+	    agent_id,
+	    resources_perms_ctx.agent_permissions->scmi_voltd_perms,
+	    resources_perms_backup.scmi_voltd_perms,
+	    MOD_SCMI_PROTOCOL_ID_VOLTAGE_DOMAIN);
     }
 
     return FWK_SUCCESS;
@@ -1519,7 +1519,7 @@ static const struct mod_res_permissions_
     .agent_has_resource_permission = agent_resource_permissions,
     .agent_set_device_permission = mod_res_agent_set_device_permission,
     .agent_set_device_protocol_permission =
-        mod_res_agent_set_device_protocol_permission,
+	mod_res_agent_set_device_protocol_permission,
     .agent_reset_config = mod_res_agent_reset_config,
 };
 
@@ -1534,7 +1534,7 @@ static int mod_res_perms_process_bind_re
 {
     /* Only allow binding to the module */
     if (!fwk_id_is_equal(target_id, fwk_module_id_resource_perms)) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     /* We don't do any permissions management */
@@ -1552,21 +1552,21 @@ static int mod_res_perms_resources_init(
 
     config = (struct mod_res_resource_perms_config *)data;
     if (config->agent_permissions != 0x0) {
-        resources_perms_ctx.agent_permissions =
-            (struct mod_res_agent_permission *)config->agent_permissions;
-        resources_perms_ctx.agent_count = config->agent_count;
-        resources_perms_ctx.protocol_count = config->protocol_count;
-        resources_perms_ctx.clock_count = config->clock_count;
-        resources_perms_ctx.sensor_count = config->sensor_count;
-        resources_perms_ctx.pd_count = config->pd_count;
-        resources_perms_ctx.perf_count = config->perf_count;
-        resources_perms_ctx.device_count = config->device_count;
+	resources_perms_ctx.agent_permissions =
+	    (struct mod_res_agent_permission *)config->agent_permissions;
+	resources_perms_ctx.agent_count = config->agent_count;
+	resources_perms_ctx.protocol_count = config->protocol_count;
+	resources_perms_ctx.clock_count = config->clock_count;
+	resources_perms_ctx.sensor_count = config->sensor_count;
+	resources_perms_ctx.pd_count = config->pd_count;
+	resources_perms_ctx.perf_count = config->perf_count;
+	resources_perms_ctx.device_count = config->device_count;
 #ifdef BUILD_HAS_MOD_SCMI_RESET_DOMAIN
-        resources_perms_ctx.reset_domain_count = config->reset_domain_count;
+	resources_perms_ctx.reset_domain_count = config->reset_domain_count;
 #endif
-        resources_perms_ctx.voltd_count = config->voltd_count;
-        resources_perms_ctx.domain_devices =
-            (struct mod_res_device *)config->domain_devices;
+	resources_perms_ctx.voltd_count = config->voltd_count;
+	resources_perms_ctx.domain_devices =
+	    (struct mod_res_device *)config->domain_devices;
     }
     resources_perms_ctx.config = config;
     return FWK_SUCCESS;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi/CMakeLists.txt
@@ -8,7 +8,7 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 set(SCMI_SOURCES)
 list(APPEND SCMI_SOURCES "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_scmi.c")
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi/include/internal/mod_scmi.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi/include/internal/mod_scmi.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi/include/internal/mod_scmi.h
@@ -42,11 +42,11 @@ struct scmi_service_ctx {
      * Copy of the pointer to the 'transmit' function within the transport API.
      */
     int (*transmit)(
-        fwk_id_t transport_id,
-        uint32_t message_header,
-        const void *payload,
-        size_t size,
-        bool request_ack_by_interrupt);
+	fwk_id_t transport_id,
+	uint32_t message_header,
+	const void *payload,
+	size_t size,
+	bool request_ack_by_interrupt);
 
     /* SCMI message token, used by the agent to identify individual messages */
     uint16_t scmi_token;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi/include/mod_scmi.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi/include/mod_scmi.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi/include/mod_scmi.h
@@ -260,7 +260,7 @@ struct mod_scmi_to_transport_api {
      *      errors.
      */
     int (*get_payload)(fwk_id_t channel_id, const void **payload,
-                       size_t *size);
+		       size_t *size);
 
     /*!
      * \brief Write part of a payload to a channel.
@@ -281,7 +281,7 @@ struct mod_scmi_to_transport_api {
      *      errors.
      */
     int (*write_payload)(fwk_id_t channel_id, size_t offset,
-                         const void *payload, size_t size);
+			 const void *payload, size_t size);
 
     /*!
      * \brief Respond to an SCMI message on a channel.
@@ -321,11 +321,11 @@ struct mod_scmi_to_transport_api {
      *      errors.
      */
     int (*transmit)(
-        fwk_id_t channel_id,
-        uint32_t message_header,
-        const void *payload,
-        size_t size,
-        bool request_ack_by_interrupt);
+	fwk_id_t channel_id,
+	uint32_t message_header,
+	const void *payload,
+	size_t size,
+	bool request_ack_by_interrupt);
 
     /*!
      * \brief Release the transport channel context lock.
@@ -416,7 +416,7 @@ struct mod_scmi_to_protocol_api {
      *
      */
     int (*get_scmi_protocol_id)(fwk_id_t protocol_id,
-                                uint8_t *scmi_protocol_id);
+				uint8_t *scmi_protocol_id);
 
     /*! Protocol message handler. */
     mod_scmi_message_handler_t *message_handler;
@@ -440,10 +440,10 @@ struct mod_scmi_notification_api {
      *      errors.
      */
     int (*scmi_notification_init)(
-        unsigned int protocol_id,
-        unsigned int agent_count,
-        unsigned int element_count,
-        unsigned int operation_count);
+	unsigned int protocol_id,
+	unsigned int agent_count,
+	unsigned int element_count,
+	unsigned int operation_count);
 
     /*!
      * \brief Add an agent to subscriber list that requested a notification.
@@ -460,10 +460,10 @@ struct mod_scmi_notification_api {
      *      errors.
      */
     int (*scmi_notification_add_subscriber)(
-        unsigned int protocol_id,
-        unsigned int element_idx,
-        unsigned int operation_id,
-        fwk_id_t service_id);
+	unsigned int protocol_id,
+	unsigned int element_idx,
+	unsigned int operation_id,
+	fwk_id_t service_id);
 
     /*!
      * \brief Remove an agent from subscriber list.
@@ -480,10 +480,10 @@ struct mod_scmi_notification_api {
      *      errors.
      */
     int (*scmi_notification_remove_subscriber)(
-        unsigned int protocol_id,
-        unsigned int agent_idx,
-        unsigned int element_idx,
-        unsigned int operation_id);
+	unsigned int protocol_id,
+	unsigned int agent_idx,
+	unsigned int element_idx,
+	unsigned int operation_id);
 
     /*!
      * \brief Notifiy all agents which requested a specific notification.
@@ -501,11 +501,11 @@ struct mod_scmi_notification_api {
      *      errors.
      */
     int (*scmi_notification_notify)(
-        unsigned int protocol_id,
-        unsigned int operation_id,
-        unsigned int scmi_response_message_id,
-        void *payload_p2a,
-        size_t payload_size);
+	unsigned int protocol_id,
+	unsigned int operation_id,
+	unsigned int scmi_response_message_id,
+	void *payload_p2a,
+	size_t payload_size);
 };
 #endif
 
@@ -593,7 +593,7 @@ struct mod_scmi_from_protocol_api {
      *      errors.
      */
     int (*write_payload)(fwk_id_t service_id, size_t offset,
-                         const void *payload, size_t size);
+			 const void *payload, size_t size);
 
     /*!
      * \brief Respond to an SCMI message on a service.
@@ -621,7 +621,7 @@ struct mod_scmi_from_protocol_api {
      * \param size Size of the payload in bytes.
      */
     void (*notify)(fwk_id_t service_id, int protocol_id, int message_id,
-        const void *payload, size_t size);
+	const void *payload, size_t size);
 };
 
 /*!
@@ -641,13 +641,13 @@ struct mod_scmi_from_protocol_req_api {
      * interrupt is required for this message.
      */
     int (*scmi_send_message)(
-        uint8_t scmi_message_id,
-        uint8_t scmi_protocol_id,
-        uint8_t token,
-        fwk_id_t service_id,
-        const void *payload,
-        size_t payload_size,
-        bool request_ack_by_interrupt);
+	uint8_t scmi_message_id,
+	uint8_t scmi_protocol_id,
+	uint8_t token,
+	fwk_id_t service_id,
+	const void *payload,
+	size_t payload_size,
+	bool request_ack_by_interrupt);
 
     /*!
      * \brief Handle response SCMI message
@@ -676,8 +676,8 @@ enum mod_scmi_notification_idx {
  */
 static const fwk_id_t mod_scmi_notification_id_initialized =
     FWK_ID_NOTIFICATION_INIT(
-        FWK_MODULE_IDX_SCMI,
-        MOD_SCMI_NOTIFICATION_IDX_INITIALIZED);
+	FWK_MODULE_IDX_SCMI,
+	MOD_SCMI_NOTIFICATION_IDX_INITIALIZED);
 
 /*!
  * \}
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi/src/mod_scmi.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi/src/mod_scmi.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi/src/mod_scmi.c
@@ -99,42 +99,42 @@ static uint32_t scmi_message_header(uint
     uint8_t message_type, uint8_t protocol_id, uint8_t token)
 {
     return (
-        (((message_id) << SCMI_MESSAGE_HEADER_MESSAGE_ID_POS) &
-         SCMI_MESSAGE_HEADER_MESSAGE_ID_MASK) |
-        (((message_type) << SCMI_MESSAGE_HEADER_MESSAGE_TYPE_POS) &
-         SCMI_MESSAGE_HEADER_MESSAGE_TYPE_MASK) |
-        (((protocol_id) << SCMI_MESSAGE_HEADER_PROTOCOL_ID_POS) &
-         SCMI_MESSAGE_HEADER_PROTOCOL_ID_MASK) |
-        (((token) << SCMI_MESSAGE_HEADER_TOKEN_POS) &
-         SCMI_MESSAGE_HEADER_TOKEN_MASK));
+	(((message_id) << SCMI_MESSAGE_HEADER_MESSAGE_ID_POS) &
+	 SCMI_MESSAGE_HEADER_MESSAGE_ID_MASK) |
+	(((message_type) << SCMI_MESSAGE_HEADER_MESSAGE_TYPE_POS) &
+	 SCMI_MESSAGE_HEADER_MESSAGE_TYPE_MASK) |
+	(((protocol_id) << SCMI_MESSAGE_HEADER_PROTOCOL_ID_POS) &
+	 SCMI_MESSAGE_HEADER_PROTOCOL_ID_MASK) |
+	(((token) << SCMI_MESSAGE_HEADER_TOKEN_POS) &
+	 SCMI_MESSAGE_HEADER_TOKEN_MASK));
 }
 
 static uint16_t read_message_id(uint32_t message_header)
 {
     return (uint16_t)(
-        ((message_header & SCMI_MESSAGE_HEADER_MESSAGE_ID_MASK) >>
-         SCMI_MESSAGE_HEADER_MESSAGE_ID_POS));
+	((message_header & SCMI_MESSAGE_HEADER_MESSAGE_ID_MASK) >>
+	 SCMI_MESSAGE_HEADER_MESSAGE_ID_POS));
 }
 
 static uint8_t read_message_type(uint32_t message_header)
 {
     return (uint8_t)(
-        ((message_header & SCMI_MESSAGE_HEADER_MESSAGE_TYPE_MASK) >>
-         SCMI_MESSAGE_HEADER_MESSAGE_TYPE_POS));
+	((message_header & SCMI_MESSAGE_HEADER_MESSAGE_TYPE_MASK) >>
+	 SCMI_MESSAGE_HEADER_MESSAGE_TYPE_POS));
 }
 
 static uint8_t read_protocol_id(uint32_t message_header)
 {
     return (uint8_t)(
-        ((message_header & SCMI_MESSAGE_HEADER_PROTOCOL_ID_MASK) >>
-         SCMI_MESSAGE_HEADER_PROTOCOL_ID_POS));
+	((message_header & SCMI_MESSAGE_HEADER_PROTOCOL_ID_MASK) >>
+	 SCMI_MESSAGE_HEADER_PROTOCOL_ID_POS));
 }
 
 static uint16_t read_token(uint32_t message_header)
 {
     return (uint16_t)(
-        ((message_header & SCMI_MESSAGE_HEADER_TOKEN_MASK) >>
-         SCMI_MESSAGE_HEADER_TOKEN_POS));
+	((message_header & SCMI_MESSAGE_HEADER_TOKEN_MASK) >>
+	 SCMI_MESSAGE_HEADER_TOKEN_POS));
 }
 
 static const char *get_message_type_str(const struct scmi_service_ctx *ctx)
@@ -142,22 +142,22 @@ static const char *get_message_type_str(
     enum mod_scmi_message_type message_type = ctx->scmi_message_type;
     switch (message_type) {
     case MOD_SCMI_MESSAGE_TYPE_COMMAND:
-        if (ctx->config->scmi_entity_role == MOD_SCMI_ROLE_PLATFORM) {
-            return "Cmd";
-        } else if (ctx->config->scmi_entity_role == MOD_SCMI_ROLE_AGENT) {
-            return "Cmd-Resp";
-        } else {
-            return "";
-        }
+	if (ctx->config->scmi_entity_role == MOD_SCMI_ROLE_PLATFORM) {
+	    return "Cmd";
+	} else if (ctx->config->scmi_entity_role == MOD_SCMI_ROLE_AGENT) {
+	    return "Cmd-Resp";
+	} else {
+	    return "";
+	}
 
     case MOD_SCMI_MESSAGE_TYPE_DELAYED_RESPONSE:
-        return "Del-Resp";
+	return "Del-Resp";
 
     case MOD_SCMI_MESSAGE_TYPE_NOTIFICATION:
-        return "Notif";
+	return "Notif";
 
     default:
-        return "Invalid message";
+	return "Invalid message";
     }
 }
 
@@ -173,15 +173,15 @@ static int signal_error(fwk_id_t service
     transport_id = ctx->transport_id;
 
     return ctx->respond(transport_id, &(int32_t){SCMI_PROTOCOL_ERROR},
-                        sizeof(int32_t));
+			sizeof(int32_t));
 }
 
 static int signal_message(fwk_id_t service_id)
 {
     struct fwk_event_light event = (struct fwk_event_light){
-        .id = FWK_ID_EVENT(FWK_MODULE_IDX_SCMI, 0),
-        .source_id = FWK_ID_MODULE(FWK_MODULE_IDX_SCMI),
-        .target_id = service_id,
+	.id = FWK_ID_EVENT(FWK_MODULE_IDX_SCMI, 0),
+	.source_id = FWK_ID_MODULE(FWK_MODULE_IDX_SCMI),
+	.target_id = service_id,
     };
 
     return fwk_put_event(&event);
@@ -199,7 +199,7 @@ static const struct mod_scmi_from_transp
 static int get_agent_count(unsigned int *agent_count)
 {
     if (agent_count == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     /* Include the platform in the count */
@@ -214,7 +214,7 @@ static int get_agent_id(fwk_id_t service
     struct scmi_service_ctx *ctx;
 
     if (agent_id == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     ctx = &scmi_ctx.service_ctx_table[fwk_id_get_element_idx(service_id)];
@@ -225,12 +225,12 @@ static int get_agent_id(fwk_id_t service
 }
 
 static int get_agent_type(uint32_t scmi_agent_id,
-                          enum scmi_agent_type *agent_type)
+			  enum scmi_agent_type *agent_type)
 {
     if ((agent_type == NULL) ||
-        (scmi_agent_id > scmi_ctx.config->agent_count) ||
-        (scmi_agent_id == MOD_SCMI_PLATFORM_ID)) {
-        return FWK_E_PARAM;
+	(scmi_agent_id > scmi_ctx.config->agent_count) ||
+	(scmi_agent_id == MOD_SCMI_PLATFORM_ID)) {
+	return FWK_E_PARAM;
     }
 
     *agent_type = scmi_ctx.config->agent_table[scmi_agent_id].type;
@@ -243,7 +243,7 @@ static int get_max_payload_size(fwk_id_t
     struct scmi_service_ctx *ctx;
 
     if (size == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     ctx = &scmi_ctx.service_ctx_table[fwk_id_get_element_idx(service_id)];
@@ -252,14 +252,14 @@ static int get_max_payload_size(fwk_id_t
 }
 
 static int write_payload(fwk_id_t service_id, size_t offset,
-                         const void *payload, size_t size)
+			 const void *payload, size_t size)
 {
     const struct scmi_service_ctx *ctx;
 
     ctx = &scmi_ctx.service_ctx_table[fwk_id_get_element_idx(service_id)];
 
     return ctx->transport_api->write_payload(ctx->transport_id,
-                                             offset, payload, size);
+					     offset, payload, size);
 }
 
 static int respond(fwk_id_t service_id, const void *payload, size_t size)
@@ -283,42 +283,42 @@ static int respond(fwk_id_t service_id,
      */
     if ((payload != NULL) && (*((int32_t *)payload) < SCMI_SUCCESS)) {
 #if FWK_LOG_LEVEL <= FWK_LOG_LEVEL_DEBUG
-        FWK_LOG_DEBUG(
-            "[SCMI] %s: %s [%" PRIu16 " (0x%x:0x%x)] returned error (%d)",
-            service_name,
-            message_type_name,
-            ctx->scmi_token,
-            ctx->scmi_protocol_id,
-            ctx->scmi_message_id,
-            *((int *)payload));
+	FWK_LOG_DEBUG(
+	    "[SCMI] %s: %s [%" PRIu16 " (0x%x:0x%x)] returned error (%d)",
+	    service_name,
+	    message_type_name,
+	    ctx->scmi_token,
+	    ctx->scmi_protocol_id,
+	    ctx->scmi_message_id,
+	    *((int *)payload));
 #else
-        (void)service_name;
-        (void)message_type_name;
+	(void)service_name;
+	(void)message_type_name;
 #endif
     } else {
 #if FWK_LOG_LEVEL <= FWK_LOG_LEVEL_DEBUG
-        FWK_LOG_DEBUG(
-            "[SCMI] %s: %s [%" PRIu16 " (0x%x:0x%x)] returned successfully",
-            service_name,
-            message_type_name,
-            ctx->scmi_token,
-            ctx->scmi_protocol_id,
-            ctx->scmi_message_id);
+	FWK_LOG_DEBUG(
+	    "[SCMI] %s: %s [%" PRIu16 " (0x%x:0x%x)] returned successfully",
+	    service_name,
+	    message_type_name,
+	    ctx->scmi_token,
+	    ctx->scmi_protocol_id,
+	    ctx->scmi_message_id);
 #endif
     }
 
     status = ctx->respond(ctx->transport_id, payload, size);
     if (status != FWK_SUCCESS) {
 #if FWK_LOG_LEVEL <= FWK_LOG_LEVEL_ERROR
-        FWK_LOG_ERR(
-            "[SCMI] %s: %s [%" PRIu16
-            " (0x%x:0x%x)] failed to respond (%s)",
-            service_name,
-            message_type_name,
-            ctx->scmi_token,
-            ctx->scmi_protocol_id,
-            ctx->scmi_message_id,
-            fwk_status_str(status));
+	FWK_LOG_ERR(
+	    "[SCMI] %s: %s [%" PRIu16
+	    " (0x%x:0x%x)] failed to respond (%s)",
+	    service_name,
+	    message_type_name,
+	    ctx->scmi_token,
+	    ctx->scmi_protocol_id,
+	    ctx->scmi_message_id,
+	    fwk_status_str(status));
 #endif
     }
     return status;
@@ -338,39 +338,39 @@ static void scmi_notify(fwk_id_t id, int
      * linked to a P2A channel by the scmi_p2a_id.
      */
     if (fwk_id_is_equal(id, FWK_ID_NONE)) {
-        return;
+	return;
     }
 
     ctx = &scmi_ctx.service_ctx_table[fwk_id_get_element_idx(id)];
     if (ctx == NULL) {
-        return;
+	return;
     }
     /* ctx is the original A2P service channel */
     if (fwk_id_is_equal(ctx->config->scmi_p2a_id, FWK_ID_NONE)) {
-        return;
+	return;
     }
     /* Get the P2A service channel for A2P ctx */
     p2a_ctx = &scmi_ctx.service_ctx_table[fwk_id_get_element_idx(
-        ctx->config->scmi_p2a_id)];
+	ctx->config->scmi_p2a_id)];
     if ((p2a_ctx == NULL) || (p2a_ctx->transmit == NULL)) {
-        return; /* No notification service configured */
+	return; /* No notification service configured */
     }
 
     message_header = scmi_message_header(
-        (uint8_t)message_id,
-        (uint8_t)MOD_SCMI_MESSAGE_TYPE_NOTIFICATION,
-        (uint8_t)protocol_id,
-        0);
+	(uint8_t)message_id,
+	(uint8_t)MOD_SCMI_MESSAGE_TYPE_NOTIFICATION,
+	(uint8_t)protocol_id,
+	0);
 
     request_ack_by_interrupt = false;
     status = p2a_ctx->transmit(
-        p2a_ctx->transport_id,
-        message_header,
-        payload,
-        size,
-        request_ack_by_interrupt);
+	p2a_ctx->transport_id,
+	message_header,
+	payload,
+	size,
+	request_ack_by_interrupt);
     if (status != FWK_SUCCESS) {
-        FWK_LOG_DEBUG("[SCMI] %s @%d", __func__, __LINE__);
+	FWK_LOG_DEBUG("[SCMI] %s @%d", __func__, __LINE__);
     }
 }
 
@@ -390,10 +390,10 @@ int scmi_send_message(
 
     /* Create the SCMI message header from the arguments */
     uint32_t message_header =
-        scmi_message_header(message_id, message_type, protocol_id, token);
+	scmi_message_header(message_id, message_type, protocol_id, token);
 
     if (fwk_id_is_equal(service_id, FWK_ID_NONE)) {
-        return FWK_E_DATA;
+	return FWK_E_DATA;
     }
 
     /* Fetch scmi module context data using the service_id */
@@ -402,7 +402,7 @@ int scmi_send_message(
     ctx = &scmi_ctx.service_ctx_table[fwk_id_get_element_idx(service_id)];
 
     if (ctx == NULL) {
-        return FWK_E_DATA;
+	return FWK_E_DATA;
     }
 
     /* Initalize the transport api pointer to TRANSPORT module api */
@@ -410,28 +410,28 @@ int scmi_send_message(
 
     /* Send the SCMI message using the transport_transmit() API  */
     status = transport_api->transmit(
-        ctx->transport_id,
-        message_header,
-        payload,
-        payload_size,
-        request_ack_by_interrupt);
+	ctx->transport_id,
+	message_header,
+	payload,
+	payload_size,
+	request_ack_by_interrupt);
 
 #if FWK_LOG_LEVEL <= FWK_LOG_LEVEL_ERROR
     if (status == FWK_SUCCESS) {
-        FWK_LOG_DEBUG(
-            "[SCMI] %s: Cmd [%" PRIu16 " (0x%x:0x%x)] was sent",
-            fwk_module_get_element_name(service_id),
-            token,
-            protocol_id,
-            message_id);
+	FWK_LOG_DEBUG(
+	    "[SCMI] %s: Cmd [%" PRIu16 " (0x%x:0x%x)] was sent",
+	    fwk_module_get_element_name(service_id),
+	    token,
+	    protocol_id,
+	    message_id);
     } else {
-        FWK_LOG_ERR(
-            "[SCMI] %s: Cmd [%" PRIu16 " (0x%x:0x%x)] failed to respond (%s)",
-            fwk_module_get_element_name(service_id),
-            token,
-            protocol_id,
-            message_id,
-            fwk_status_str(status));
+	FWK_LOG_ERR(
+	    "[SCMI] %s: Cmd [%" PRIu16 " (0x%x:0x%x)] failed to respond (%s)",
+	    fwk_module_get_element_name(service_id),
+	    token,
+	    protocol_id,
+	    message_id,
+	    fwk_status_str(status));
     }
 #endif
 
@@ -447,7 +447,7 @@ int response_message_handler(fwk_id_t se
 
     /* release the tranport channel lock */
     status =
-        ctx->transport_api->release_transport_channel_lock(ctx->transport_id);
+	ctx->transport_api->release_transport_channel_lock(ctx->transport_id);
 
     return status;
 }
@@ -464,8 +464,8 @@ static const struct mod_scmi_from_protoc
 
 static const struct mod_scmi_from_protocol_req_api
     scmi_from_protocol_req_api = {
-        .scmi_send_message = scmi_send_message,
-        .response_message_handler = response_message_handler,
+	.scmi_send_message = scmi_send_message,
+	.response_message_handler = response_message_handler,
     };
 
 #ifdef BUILD_HAS_SCMI_NOTIFICATIONS
@@ -493,7 +493,7 @@ static int scmi_notification_init(
     int i;
     int total_count;
     struct scmi_notification_subscribers *subscribers =
-        notification_subscribers(protocol_id);
+	notification_subscribers(protocol_id);
 
     subscribers->agent_count = agent_count;
     subscribers->element_count = element_count;
@@ -502,19 +502,19 @@ static int scmi_notification_init(
     total_count = (int)(operation_count * element_count * agent_count);
 
     subscribers->agent_service_ids =
-        fwk_mm_calloc((size_t)total_count, sizeof(fwk_id_t));
+	fwk_mm_calloc((size_t)total_count, sizeof(fwk_id_t));
 
     /*
      * Mark all operations_idx as invalid. This will be updated
      * whenever an agent subscribes to a notification for an operation.
      */
     fwk_str_memset(
-        subscribers->operation_id_to_idx,
-        MOD_SCMI_PROTOCOL_OPERATION_IDX_INVALID,
-        MOD_SCMI_PROTOCOL_MAX_OPERATION_ID);
+	subscribers->operation_id_to_idx,
+	MOD_SCMI_PROTOCOL_OPERATION_IDX_INVALID,
+	MOD_SCMI_PROTOCOL_MAX_OPERATION_ID);
 
     for (i = 0; i < total_count; i++) {
-        subscribers->agent_service_ids[i] = FWK_ID_NONE;
+	subscribers->agent_service_ids[i] = FWK_ID_NONE;
     }
 
     return FWK_SUCCESS;
@@ -538,7 +538,7 @@ static int scmi_notification_service_idx
      * 3. Get the offset of the 2nd array within the first array.
      */
     return (
-        int)(agent_idx + element_idx * agent_count + operation_idx * element_count * agent_count);
+	int)(agent_idx + element_idx * agent_count + operation_idx * element_count * agent_count);
 }
 
 static int scmi_notification_add_subscriber(
@@ -552,11 +552,11 @@ static int scmi_notification_add_subscri
     unsigned int agent_idx;
 
     struct scmi_notification_subscribers *subscribers =
-        notification_subscribers(protocol_id);
+	notification_subscribers(protocol_id);
 
     status = get_agent_id(service_id, &agent_idx);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     fwk_assert(operation_id < MOD_SCMI_PROTOCOL_MAX_OPERATION_ID);
@@ -565,18 +565,18 @@ static int scmi_notification_add_subscri
      * invalid (MOD_SCMI_PROTOCOL_OPERATION_IDX_INVALID)
      */
     if (subscribers->operation_id_to_idx[operation_id] ==
-        MOD_SCMI_PROTOCOL_OPERATION_IDX_INVALID) {
-        fwk_assert(subscribers->operation_idx < subscribers->operation_count);
-        subscribers->operation_id_to_idx[operation_id] =
-            (uint8_t)subscribers->operation_idx++;
+	MOD_SCMI_PROTOCOL_OPERATION_IDX_INVALID) {
+	fwk_assert(subscribers->operation_idx < subscribers->operation_count);
+	subscribers->operation_id_to_idx[operation_id] =
+	    (uint8_t)subscribers->operation_idx++;
     }
 
     service_id_idx = (unsigned int)scmi_notification_service_idx(
-        agent_idx,
-        element_idx,
-        subscribers->operation_id_to_idx[operation_id],
-        subscribers->agent_count,
-        subscribers->element_count);
+	agent_idx,
+	element_idx,
+	subscribers->operation_id_to_idx[operation_id],
+	subscribers->agent_count,
+	subscribers->element_count);
 
     subscribers->agent_service_ids[service_id_idx] = service_id;
 
@@ -593,18 +593,18 @@ static int scmi_notification_remove_subs
     unsigned int service_id_idx;
 
     struct scmi_notification_subscribers *subscribers =
-        notification_subscribers(protocol_id);
+	notification_subscribers(protocol_id);
 
     fwk_assert(operation_id < MOD_SCMI_PROTOCOL_MAX_OPERATION_ID);
 
     operation_idx = subscribers->operation_id_to_idx[operation_id];
 
     service_id_idx = (unsigned int)scmi_notification_service_idx(
-        agent_idx,
-        element_idx,
-        operation_idx,
-        subscribers->agent_count,
-        subscribers->element_count);
+	agent_idx,
+	element_idx,
+	operation_idx,
+	subscribers->agent_count,
+	subscribers->element_count);
 
     subscribers->agent_service_ids[service_id_idx] = FWK_ID_NONE;
 
@@ -624,7 +624,7 @@ static int scmi_notification_notify(
     unsigned int service_id_idx;
 
     struct scmi_notification_subscribers *subscribers =
-        notification_subscribers(protocol_id);
+	notification_subscribers(protocol_id);
 
     fwk_assert(operation_id < MOD_SCMI_PROTOCOL_MAX_OPERATION_ID);
     operation_idx = subscribers->operation_id_to_idx[operation_id];
@@ -636,30 +636,30 @@ static int scmi_notification_notify(
      * scmi_notification_add_subscriber.
      */
     if (operation_idx == MOD_SCMI_PROTOCOL_OPERATION_IDX_INVALID) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     for (i = 0; i < subscribers->element_count; i++) {
-        /* Skip agent 0, platform agent */
-        for (j = 1; j < subscribers->agent_count; j++) {
-            service_id_idx = (unsigned int)scmi_notification_service_idx(
-                j,
-                i,
-                operation_idx,
-                subscribers->agent_count,
-                subscribers->element_count);
-
-            service_id = subscribers->agent_service_ids[service_id_idx];
-
-            if (!fwk_id_is_equal(service_id, FWK_ID_NONE)) {
-                scmi_notify(
-                    service_id,
-                    (int)protocol_id,
-                    (int)scmi_response_id,
-                    payload_p2a,
-                    payload_size);
-            }
-        }
+	/* Skip agent 0, platform agent */
+	for (j = 1; j < subscribers->agent_count; j++) {
+	    service_id_idx = (unsigned int)scmi_notification_service_idx(
+		j,
+		i,
+		operation_idx,
+		subscribers->agent_count,
+		subscribers->element_count);
+
+	    service_id = subscribers->agent_service_ids[service_id_idx];
+
+	    if (!fwk_id_is_equal(service_id, FWK_ID_NONE)) {
+		scmi_notify(
+		    service_id,
+		    (int)protocol_id,
+		    (int)scmi_response_id,
+		    payload_p2a,
+		    payload_size);
+	    }
+	}
     }
 
     return FWK_SUCCESS;
@@ -678,18 +678,18 @@ static struct mod_scmi_notification_api
  */
 
 static int scmi_init(fwk_id_t module_id, unsigned int service_count,
-                     const void *data)
+		     const void *data)
 {
     struct mod_scmi_config *config = (struct mod_scmi_config *)data;
     unsigned int agent_idx;
     const struct mod_scmi_agent *agent;
 
     if (config == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     if (config->agent_count > MOD_SCMI_AGENT_ID_MAX) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     /*
@@ -697,34 +697,34 @@ static int scmi_init(fwk_id_t module_id,
      * the table - that would refer to the platform - is ignored.
      */
     for (agent_idx = MOD_SCMI_PLATFORM_ID + 1;
-         agent_idx <= config->agent_count;
-         agent_idx++) {
-        agent = &config->agent_table[agent_idx];
-        if (agent->type >= SCMI_AGENT_TYPE_COUNT) {
-            return FWK_E_PARAM;
-        }
+	 agent_idx <= config->agent_count;
+	 agent_idx++) {
+	agent = &config->agent_table[agent_idx];
+	if (agent->type >= SCMI_AGENT_TYPE_COUNT) {
+	    return FWK_E_PARAM;
+	}
     }
 
     scmi_ctx.protocol_table = fwk_mm_calloc(
-        config->protocol_count_max + PROTOCOL_TABLE_RESERVED_ENTRIES_COUNT,
-        sizeof(scmi_ctx.protocol_table[0]));
+	config->protocol_count_max + PROTOCOL_TABLE_RESERVED_ENTRIES_COUNT,
+	sizeof(scmi_ctx.protocol_table[0]));
 
     if (config->protocol_requester_count_max != 0) {
-        scmi_ctx.protocol_requester_table = fwk_mm_calloc(
-            config->protocol_requester_count_max,
-            sizeof(scmi_ctx.protocol_requester_table[0]));
+	scmi_ctx.protocol_requester_table = fwk_mm_calloc(
+	    config->protocol_requester_count_max,
+	    sizeof(scmi_ctx.protocol_requester_table[0]));
     } else {
-        scmi_ctx.protocol_requester_table = NULL;
+	scmi_ctx.protocol_requester_table = NULL;
     }
 
     scmi_ctx.service_ctx_table = fwk_mm_calloc(
-        service_count, sizeof(scmi_ctx.service_ctx_table[0]));
+	service_count, sizeof(scmi_ctx.service_ctx_table[0]));
 
 #ifdef BUILD_HAS_BASE_PROTOCOL
     scmi_ctx.protocol_table[PROTOCOL_TABLE_BASE_PROTOCOL_IDX].message_handler =
-        scmi_base_message_handler;
+	scmi_base_message_handler;
     scmi_ctx.scmi_protocol_id_to_idx[MOD_SCMI_PROTOCOL_ID_BASE] =
-        PROTOCOL_TABLE_BASE_PROTOCOL_IDX;
+	PROTOCOL_TABLE_BASE_PROTOCOL_IDX;
     scmi_base_set_api(&scmi_from_protocol_api);
     scmi_base_set_shared_ctx(&scmi_ctx);
 #endif
@@ -734,16 +734,16 @@ static int scmi_init(fwk_id_t module_id,
 }
 
 static int scmi_service_init(fwk_id_t service_id, unsigned int unused,
-                             const void *data)
+			     const void *data)
 {
     const struct mod_scmi_service_config *config =
-        (struct mod_scmi_service_config *)data;
+	(struct mod_scmi_service_config *)data;
     struct scmi_service_ctx *ctx;
 
     if (((config->scmi_agent_id == MOD_SCMI_PLATFORM_ID) ||
-         (config->scmi_agent_id > scmi_ctx.config->agent_count)) &&
-        (config->scmi_entity_role == MOD_SCMI_ROLE_PLATFORM)) {
-        return FWK_E_PARAM;
+	 (config->scmi_agent_id > scmi_ctx.config->agent_count)) &&
+	(config->scmi_entity_role == MOD_SCMI_ROLE_PLATFORM)) {
+	return FWK_E_PARAM;
     }
 
     ctx = &scmi_ctx.service_ctx_table[fwk_id_get_element_idx(service_id)];
@@ -763,101 +763,101 @@ static int scmi_bind(fwk_id_t id, unsign
     uint8_t scmi_protocol_id;
 
     if (round == 0) {
-        if (fwk_id_is_type(id, FWK_ID_TYPE_MODULE)) {
-            return FWK_SUCCESS;
-        }
-
-        ctx = &scmi_ctx.service_ctx_table[fwk_id_get_element_idx(id)];
-        status = fwk_module_bind(ctx->config->transport_id,
-                                 ctx->config->transport_api_id, &transport_api);
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
-
-        if ((transport_api->get_secure == NULL) ||
-            (transport_api->get_max_payload_size == NULL) ||
-            (transport_api->get_message_header == NULL) ||
-            (transport_api->get_payload == NULL) ||
-            (transport_api->write_payload == NULL)) {
-            return FWK_E_DATA;
-        }
-
-        ctx->transport_api = transport_api;
-        ctx->transport_id = ctx->config->transport_id;
-        ctx->respond = transport_api->respond;
-        ctx->transmit = transport_api->transmit;
+	if (fwk_id_is_type(id, FWK_ID_TYPE_MODULE)) {
+	    return FWK_SUCCESS;
+	}
+
+	ctx = &scmi_ctx.service_ctx_table[fwk_id_get_element_idx(id)];
+	status = fwk_module_bind(ctx->config->transport_id,
+				 ctx->config->transport_api_id, &transport_api);
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
+
+	if ((transport_api->get_secure == NULL) ||
+	    (transport_api->get_max_payload_size == NULL) ||
+	    (transport_api->get_message_header == NULL) ||
+	    (transport_api->get_payload == NULL) ||
+	    (transport_api->write_payload == NULL)) {
+	    return FWK_E_DATA;
+	}
+
+	ctx->transport_api = transport_api;
+	ctx->transport_id = ctx->config->transport_id;
+	ctx->respond = transport_api->respond;
+	ctx->transmit = transport_api->transmit;
 
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     if (fwk_id_is_type(id, FWK_ID_TYPE_ELEMENT)) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     for (protocol_idx = 0;
-         protocol_idx < scmi_ctx.protocol_count; protocol_idx++) {
-        protocol = &scmi_ctx.protocol_table[
-            PROTOCOL_TABLE_RESERVED_ENTRIES_COUNT + protocol_idx];
-
-        status = fwk_module_bind(protocol->id,
-            FWK_ID_API(fwk_id_get_module_idx(protocol->id), 0), &protocol_api);
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
-
-        if ((protocol_api->get_scmi_protocol_id == NULL) ||
-            (protocol_api->message_handler == NULL)) {
-            return FWK_E_DATA;
-        }
-        status = protocol_api->get_scmi_protocol_id(protocol->id,
-                                                    &scmi_protocol_id);
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
-
-        if (scmi_ctx.scmi_protocol_id_to_idx[scmi_protocol_id] != 0) {
-            return FWK_E_STATE;
-        }
-
-        scmi_ctx.scmi_protocol_id_to_idx[scmi_protocol_id] =
-            (uint8_t)(protocol_idx + PROTOCOL_TABLE_RESERVED_ENTRIES_COUNT);
-        protocol->message_handler = protocol_api->message_handler;
+	 protocol_idx < scmi_ctx.protocol_count; protocol_idx++) {
+	protocol = &scmi_ctx.protocol_table[
+	    PROTOCOL_TABLE_RESERVED_ENTRIES_COUNT + protocol_idx];
+
+	status = fwk_module_bind(protocol->id,
+	    FWK_ID_API(fwk_id_get_module_idx(protocol->id), 0), &protocol_api);
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
+
+	if ((protocol_api->get_scmi_protocol_id == NULL) ||
+	    (protocol_api->message_handler == NULL)) {
+	    return FWK_E_DATA;
+	}
+	status = protocol_api->get_scmi_protocol_id(protocol->id,
+						    &scmi_protocol_id);
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
+
+	if (scmi_ctx.scmi_protocol_id_to_idx[scmi_protocol_id] != 0) {
+	    return FWK_E_STATE;
+	}
+
+	scmi_ctx.scmi_protocol_id_to_idx[scmi_protocol_id] =
+	    (uint8_t)(protocol_idx + PROTOCOL_TABLE_RESERVED_ENTRIES_COUNT);
+	protocol->message_handler = protocol_api->message_handler;
     }
 
     for (protocol_idx = 0; protocol_idx < scmi_ctx.protocol_requester_count;
-         protocol_idx++) {
-        protocol_req = &scmi_ctx.protocol_requester_table[protocol_idx];
+	 protocol_idx++) {
+	protocol_req = &scmi_ctx.protocol_requester_table[protocol_idx];
 
-        status = fwk_module_bind(
-            protocol_req->id,
-            FWK_ID_API(fwk_id_get_module_idx(protocol_req->id), 0),
-            &protocol_api);
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
-
-        if ((protocol_api->get_scmi_protocol_id == NULL) ||
-            (protocol_api->message_handler == NULL)) {
-            return FWK_E_DATA;
-        }
-        status = protocol_api->get_scmi_protocol_id(
-            protocol_req->id, &scmi_protocol_id);
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
-
-        scmi_ctx.scmi_protocol_requester_id_to_idx[scmi_protocol_id] =
-            (uint8_t)(protocol_idx);
-        protocol_req->message_handler = protocol_api->message_handler;
+	status = fwk_module_bind(
+	    protocol_req->id,
+	    FWK_ID_API(fwk_id_get_module_idx(protocol_req->id), 0),
+	    &protocol_api);
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
+
+	if ((protocol_api->get_scmi_protocol_id == NULL) ||
+	    (protocol_api->message_handler == NULL)) {
+	    return FWK_E_DATA;
+	}
+	status = protocol_api->get_scmi_protocol_id(
+	    protocol_req->id, &scmi_protocol_id);
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
+
+	scmi_ctx.scmi_protocol_requester_id_to_idx[scmi_protocol_id] =
+	    (uint8_t)(protocol_idx);
+	protocol_req->message_handler = protocol_api->message_handler;
     }
 
 #ifdef BUILD_HAS_MOD_RESOURCE_PERMS
     status = fwk_module_bind(
-        FWK_ID_MODULE(FWK_MODULE_IDX_RESOURCE_PERMS),
-        FWK_ID_API(FWK_MODULE_IDX_RESOURCE_PERMS, MOD_RES_PERM_RESOURCE_PERMS),
-        &scmi_ctx.res_perms_api);
+	FWK_ID_MODULE(FWK_MODULE_IDX_RESOURCE_PERMS),
+	FWK_ID_API(FWK_MODULE_IDX_RESOURCE_PERMS, MOD_RES_PERM_RESOURCE_PERMS),
+	&scmi_ctx.res_perms_api);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 #endif
 
@@ -865,7 +865,7 @@ static int scmi_bind(fwk_id_t id, unsign
 }
 
 static int scmi_process_bind_request(fwk_id_t source_id, fwk_id_t target_id,
-                                     fwk_id_t api_id, const void **api)
+				     fwk_id_t api_id, const void **api)
 {
     unsigned int api_idx;
     struct scmi_service_ctx *ctx;
@@ -877,65 +877,65 @@ static int scmi_process_bind_request(fwk
 
     switch (api_id_type) {
     case MOD_SCMI_API_IDX_PROTOCOL:
-        if (!fwk_id_is_type(target_id, FWK_ID_TYPE_MODULE)) {
-            return FWK_E_SUPPORT;
-        }
-
-        if (scmi_ctx.protocol_count >= scmi_ctx.config->protocol_count_max) {
-            return FWK_E_NOMEM;
-        }
-
-        scmi_ctx.protocol_table[PROTOCOL_TABLE_RESERVED_ENTRIES_COUNT +
-                                scmi_ctx.protocol_count++].id = source_id;
-        *api = &scmi_from_protocol_api;
-        break;
+	if (!fwk_id_is_type(target_id, FWK_ID_TYPE_MODULE)) {
+	    return FWK_E_SUPPORT;
+	}
+
+	if (scmi_ctx.protocol_count >= scmi_ctx.config->protocol_count_max) {
+	    return FWK_E_NOMEM;
+	}
+
+	scmi_ctx.protocol_table[PROTOCOL_TABLE_RESERVED_ENTRIES_COUNT +
+				scmi_ctx.protocol_count++].id = source_id;
+	*api = &scmi_from_protocol_api;
+	break;
 
     case MOD_SCMI_API_IDX_PROTOCOL_REQ:
-        if (!fwk_id_is_type(target_id, FWK_ID_TYPE_MODULE)) {
-            return FWK_E_SUPPORT;
-        }
-        if (scmi_ctx.protocol_requester_count >=
-            scmi_ctx.config->protocol_requester_count_max) {
-            return FWK_E_PARAM;
-        }
-
-        scmi_ctx.protocol_requester_table[scmi_ctx.protocol_requester_count++]
-            .id = source_id;
-        *api = &scmi_from_protocol_req_api;
-        break;
+	if (!fwk_id_is_type(target_id, FWK_ID_TYPE_MODULE)) {
+	    return FWK_E_SUPPORT;
+	}
+	if (scmi_ctx.protocol_requester_count >=
+	    scmi_ctx.config->protocol_requester_count_max) {
+	    return FWK_E_PARAM;
+	}
+
+	scmi_ctx.protocol_requester_table[scmi_ctx.protocol_requester_count++]
+	    .id = source_id;
+	*api = &scmi_from_protocol_req_api;
+	break;
 
     case MOD_SCMI_API_IDX_TRANSPORT:
-        if (!fwk_id_is_type(target_id, FWK_ID_TYPE_ELEMENT)) {
-            return FWK_E_SUPPORT;
-        }
-
-        ctx = &scmi_ctx.service_ctx_table[fwk_id_get_element_idx(target_id)];
-        if (!fwk_id_is_equal(source_id, ctx->transport_id)) {
-            return FWK_E_ACCESS;
-        }
+	if (!fwk_id_is_type(target_id, FWK_ID_TYPE_ELEMENT)) {
+	    return FWK_E_SUPPORT;
+	}
+
+	ctx = &scmi_ctx.service_ctx_table[fwk_id_get_element_idx(target_id)];
+	if (!fwk_id_is_equal(source_id, ctx->transport_id)) {
+	    return FWK_E_ACCESS;
+	}
 
-        *api = &scmi_from_transport_api;
-        break;
+	*api = &scmi_from_transport_api;
+	break;
 
 #ifdef BUILD_HAS_SCMI_NOTIFICATIONS
     case MOD_SCMI_API_IDX_NOTIFICATION:
-        if (!fwk_id_is_type(target_id, FWK_ID_TYPE_MODULE)) {
-            return FWK_E_SUPPORT;
-        }
+	if (!fwk_id_is_type(target_id, FWK_ID_TYPE_MODULE)) {
+	    return FWK_E_SUPPORT;
+	}
 
-        *api = &mod_scmi_notif_api;
-        break;
+	*api = &mod_scmi_notif_api;
+	break;
 #endif
 
     default:
-        return FWK_E_SUPPORT;
+	return FWK_E_SUPPORT;
     };
 
     return FWK_SUCCESS;
 }
 
 static int scmi_process_event(const struct fwk_event *event,
-                              struct fwk_event *resp)
+			      struct fwk_event *resp)
 {
     int status;
     struct scmi_service_ctx *ctx;
@@ -963,141 +963,141 @@ static int scmi_process_event(const stru
     status = transport_api->get_message_header(transport_id, &message_header);
     if (status != FWK_SUCCESS) {
 #if FWK_LOG_LEVEL <= FWK_LOG_LEVEL_ERROR
-        FWK_LOG_ERR("[SCMI] %s: Unable to read message header", service_name);
+	FWK_LOG_ERR("[SCMI] %s: Unable to read message header", service_name);
 #else
-        (void)service_name;
+	(void)service_name;
 #endif
-        return status;
+	return status;
     }
 
     status = transport_api->get_payload(transport_id, &payload, &payload_size);
     if (status != FWK_SUCCESS) {
 #if FWK_LOG_LEVEL <= FWK_LOG_LEVEL_ERROR
-        FWK_LOG_ERR("[SCMI] %s: Unable to read message payload", service_name);
+	FWK_LOG_ERR("[SCMI] %s: Unable to read message payload", service_name);
 #else
-        (void)service_name;
+	(void)service_name;
 #endif
-        return status;
+	return status;
     }
 
     ctx->scmi_protocol_id = read_protocol_id(message_header);
     ctx->scmi_message_id = read_message_id(message_header);
     ctx->scmi_message_type =
-        (enum mod_scmi_message_type)read_message_type(message_header);
+	(enum mod_scmi_message_type)read_message_type(message_header);
     ctx->scmi_token = read_token(message_header);
     message_type_name = get_message_type_str(ctx);
 
 #if FWK_LOG_LEVEL <= FWK_LOG_LEVEL_DEBUG
     FWK_LOG_DEBUG(
-        "[SCMI] %s: %s [%" PRIu16 " (0x%x:0x%x)] was received",
-        service_name,
-        message_type_name,
-        ctx->scmi_token,
-        ctx->scmi_protocol_id,
-        ctx->scmi_message_id);
+	"[SCMI] %s: %s [%" PRIu16 " (0x%x:0x%x)] was received",
+	service_name,
+	message_type_name,
+	ctx->scmi_token,
+	ctx->scmi_protocol_id,
+	ctx->scmi_message_id);
 #else
     (void)service_name;
     (void)message_type_name;
 #endif
     if (ctx->config->scmi_entity_role == MOD_SCMI_ROLE_PLATFORM) {
-        protocol_idx = scmi_ctx.scmi_protocol_id_to_idx[ctx->scmi_protocol_id];
-        if (protocol_idx == 0) {
+	protocol_idx = scmi_ctx.scmi_protocol_id_to_idx[ctx->scmi_protocol_id];
+	if (protocol_idx == 0) {
 #if FWK_LOG_LEVEL <= FWK_LOG_LEVEL_ERROR
-            FWK_LOG_ERR(
-                "[SCMI] %s: %s [%" PRIu16
-                "(0x%x:0x%x)] requested an unsupported protocol",
-                service_name,
-                message_type_name,
-                ctx->scmi_token,
-                ctx->scmi_protocol_id,
-                ctx->scmi_message_id);
+	    FWK_LOG_ERR(
+		"[SCMI] %s: %s [%" PRIu16
+		"(0x%x:0x%x)] requested an unsupported protocol",
+		service_name,
+		message_type_name,
+		ctx->scmi_token,
+		ctx->scmi_protocol_id,
+		ctx->scmi_message_id);
 #endif
-            status = ctx->respond(
-                transport_id,
-                &(int32_t){ SCMI_NOT_SUPPORTED },
-                sizeof(int32_t));
-            if (status != FWK_SUCCESS) {
-                FWK_LOG_DEBUG("[SCMI] %s @%d", __func__, __LINE__);
-            }
-            return FWK_SUCCESS;
-        }
+	    status = ctx->respond(
+		transport_id,
+		&(int32_t){ SCMI_NOT_SUPPORTED },
+		sizeof(int32_t));
+	    if (status != FWK_SUCCESS) {
+		FWK_LOG_DEBUG("[SCMI] %s @%d", __func__, __LINE__);
+	    }
+	    return FWK_SUCCESS;
+	}
 
 #ifndef BUILD_HAS_MOD_RESOURCE_PERMS
     status = get_agent_id(event->target_id, &agent_id);
     if (status != FWK_SUCCESS) {
 #    if FWK_LOG_LEVEL <= FWK_LOG_LEVEL_ERROR
-        FWK_LOG_ERR("[SCMI] %s: Unable to get agent id", service_name);
+	FWK_LOG_ERR("[SCMI] %s: Unable to get agent id", service_name);
 #    endif
-        return status;
+	return status;
     }
 
     status = get_agent_type(agent_id, &agent_type);
     if (status != FWK_SUCCESS) {
 #    if FWK_LOG_LEVEL <= FWK_LOG_LEVEL_ERROR
-        FWK_LOG_ERR("[SCMI] %s: Unable to get agent type", service_name);
+	FWK_LOG_ERR("[SCMI] %s: Unable to get agent type", service_name);
 #    endif
-        return status;
+	return status;
     }
 
     if (agent_type == SCMI_AGENT_TYPE_PSCI) {
-        /*
-         * check if the current protocol is within the disabled protocols
-         * list for PSCI agent.
-         */
-        for (dis_protocol_list_psci_index = 0; dis_protocol_list_psci_index <
-             scmi_ctx.config->dis_protocol_count_psci;
-             dis_protocol_list_psci_index++) {
-            if (ctx->scmi_protocol_id ==
-                scmi_ctx.config
-                    ->dis_protocol_list_psci[dis_protocol_list_psci_index]) {
+	/*
+	 * check if the current protocol is within the disabled protocols
+	 * list for PSCI agent.
+	 */
+	for (dis_protocol_list_psci_index = 0; dis_protocol_list_psci_index <
+	     scmi_ctx.config->dis_protocol_count_psci;
+	     dis_protocol_list_psci_index++) {
+	    if (ctx->scmi_protocol_id ==
+		scmi_ctx.config
+		    ->dis_protocol_list_psci[dis_protocol_list_psci_index]) {
 #    if FWK_LOG_LEVEL <= FWK_LOG_LEVEL_ERROR
-                FWK_LOG_ERR(
-                    "[SCMI] %s: %s [%" PRIu16
-                    "(0x%x:0x%x)] requested a denied protocol",
-                    service_name,
-                    message_type_name,
-                    ctx->scmi_token,
-                    ctx->scmi_protocol_id,
-                    ctx->scmi_message_id);
+		FWK_LOG_ERR(
+		    "[SCMI] %s: %s [%" PRIu16
+		    "(0x%x:0x%x)] requested a denied protocol",
+		    service_name,
+		    message_type_name,
+		    ctx->scmi_token,
+		    ctx->scmi_protocol_id,
+		    ctx->scmi_message_id);
 #    endif
-                status = ctx->respond(
-                    transport_id, &(int32_t){ SCMI_DENIED }, sizeof(int32_t));
-                if (status != FWK_SUCCESS) {
-                    FWK_LOG_DEBUG("[SCMI] %s @%d", __func__, __LINE__);
-                }
-                return FWK_SUCCESS;
-            }
-        }
+		status = ctx->respond(
+		    transport_id, &(int32_t){ SCMI_DENIED }, sizeof(int32_t));
+		if (status != FWK_SUCCESS) {
+		    FWK_LOG_DEBUG("[SCMI] %s @%d", __func__, __LINE__);
+		}
+		return FWK_SUCCESS;
+	    }
+	}
     }
 #endif
     protocol = &scmi_ctx.protocol_table[protocol_idx];
     } else if (ctx->config->scmi_entity_role == MOD_SCMI_ROLE_AGENT) {
-        protocol_idx =
-            scmi_ctx.scmi_protocol_requester_id_to_idx[ctx->scmi_protocol_id];
-        protocol = &scmi_ctx.protocol_requester_table[protocol_idx];
+	protocol_idx =
+	    scmi_ctx.scmi_protocol_requester_id_to_idx[ctx->scmi_protocol_id];
+	protocol = &scmi_ctx.protocol_requester_table[protocol_idx];
     } else {
-        return FWK_E_INIT;
+	return FWK_E_INIT;
     }
 
     status = protocol->message_handler(
-        protocol->id,
-        event->target_id,
-        payload,
-        payload_size,
-        ctx->scmi_message_id);
+	protocol->id,
+	event->target_id,
+	payload,
+	payload_size,
+	ctx->scmi_message_id);
 
     if (status != FWK_SUCCESS) {
 #if FWK_LOG_LEVEL <= FWK_LOG_LEVEL_ERROR
-        FWK_LOG_ERR(
-            "[SCMI] %s: %s [%" PRIu16 " (0x%x:0x%x)] handler error (%s)",
-            service_name,
-            message_type_name,
-            ctx->scmi_token,
-            ctx->scmi_protocol_id,
-            ctx->scmi_message_id,
-            fwk_status_str(status));
+	FWK_LOG_ERR(
+	    "[SCMI] %s: %s [%" PRIu16 " (0x%x:0x%x)] handler error (%s)",
+	    service_name,
+	    message_type_name,
+	    ctx->scmi_token,
+	    ctx->scmi_protocol_id,
+	    ctx->scmi_message_id,
+	    fwk_status_str(status));
 #endif
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     /*
@@ -1105,22 +1105,22 @@ static int scmi_process_event(const stru
      * layer that we already received the response and are free
      */
     if (ctx->config->scmi_entity_role == MOD_SCMI_ROLE_AGENT) {
-        status =
-            ctx->transport_api->release_transport_channel_lock(transport_id);
-        if (status != FWK_SUCCESS) {
+	status =
+	    ctx->transport_api->release_transport_channel_lock(transport_id);
+	if (status != FWK_SUCCESS) {
 #if FWK_LOG_LEVEL <= FWK_LOG_LEVEL_ERROR
-            FWK_LOG_ERR(
-                "[SCMI] %s: %s [%" PRIu16
-                " (0x%x:0x%x)] confirm reception error (%s)",
-                service_name,
-                message_type_name,
-                ctx->scmi_token,
-                ctx->scmi_protocol_id,
-                ctx->scmi_message_id,
-                fwk_status_str(status));
+	    FWK_LOG_ERR(
+		"[SCMI] %s: %s [%" PRIu16
+		" (0x%x:0x%x)] confirm reception error (%s)",
+		service_name,
+		message_type_name,
+		ctx->scmi_token,
+		ctx->scmi_protocol_id,
+		ctx->scmi_message_id,
+		fwk_status_str(status));
 #endif
-            return FWK_SUCCESS;
-        }
+	    return FWK_SUCCESS;
+	}
     }
 
     return FWK_SUCCESS;
@@ -1134,31 +1134,31 @@ static int scmi_start(fwk_id_t id)
 
     if (fwk_id_is_type(id, FWK_ID_TYPE_MODULE)) {
 #    ifdef BUILD_HAS_SCMI_NOTIFICATIONS
-        /* scmi_ctx.protocol_count + 1 to include Base protocol */
-        scmi_ctx.scmi_notif_subscribers = fwk_mm_calloc(
-            scmi_ctx.protocol_count + 1,
-            sizeof(struct scmi_notification_subscribers));
+	/* scmi_ctx.protocol_count + 1 to include Base protocol */
+	scmi_ctx.scmi_notif_subscribers = fwk_mm_calloc(
+	    scmi_ctx.protocol_count + 1,
+	    sizeof(struct scmi_notification_subscribers));
 #    endif
 
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     config = fwk_module_get_data(id);
 
     if (fwk_id_is_equal(config->transport_notification_init_id, FWK_ID_NONE)) {
-        /* Notify that the service is immediately ready */
-        struct fwk_event scmi_services_initialized_notification = {
-            .id = mod_scmi_notification_id_initialized,
-            .source_id = id,
-        };
+	/* Notify that the service is immediately ready */
+	struct fwk_event scmi_services_initialized_notification = {
+	    .id = mod_scmi_notification_id_initialized,
+	    .source_id = id,
+	};
 
-        return fwk_notification_notify(&scmi_services_initialized_notification,
-            &notifications_sent);
+	return fwk_notification_notify(&scmi_services_initialized_notification,
+	    &notifications_sent);
     }
 
     return fwk_notification_subscribe(config->transport_notification_init_id,
-                                      config->transport_id,
-                                      id);
+				      config->transport_id,
+				      id);
 #else
     return FWK_SUCCESS;
 #endif
@@ -1166,7 +1166,7 @@ static int scmi_start(fwk_id_t id)
 
 #ifdef BUILD_HAS_NOTIFICATION
 static int scmi_process_notification(const struct fwk_event *event,
-                                     struct fwk_event *resp_event)
+				     struct fwk_event *resp_event)
 {
     const struct mod_scmi_service_config *config;
     unsigned int notifications_sent;
@@ -1175,20 +1175,20 @@ static int scmi_process_notification(con
 
     config = fwk_module_get_data(event->target_id);
     if (config == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     fwk_assert(fwk_id_is_equal(event->id,
-                               config->transport_notification_init_id));
+			       config->transport_notification_init_id));
 
     /* Notify that this service is ready */
     struct fwk_event scmi_services_initialized_notification = {
-        .id = mod_scmi_notification_id_initialized,
-        .source_id = FWK_ID_NONE
+	.id = mod_scmi_notification_id_initialized,
+	.source_id = FWK_ID_NONE
     };
 
     return fwk_notification_notify(&scmi_services_initialized_notification,
-        &notifications_sent);
+	&notifications_sent);
 }
 #endif
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi/src/mod_scmi_base.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi/src/mod_scmi_base.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi/src/mod_scmi_base.c
@@ -37,24 +37,24 @@ static const char *const default_agent_n
 
 static const unsigned int
     base_payload_size_table[MOD_SCMI_BASE_COMMAND_COUNT] = {
-        [MOD_SCMI_PROTOCOL_VERSION] = 0,
-        [MOD_SCMI_PROTOCOL_ATTRIBUTES] = 0,
-        [MOD_SCMI_PROTOCOL_MESSAGE_ATTRIBUTES] =
-            (unsigned int)sizeof(struct scmi_protocol_message_attributes_a2p),
-        [MOD_SCMI_BASE_DISCOVER_VENDOR] = 0,
-        [MOD_SCMI_BASE_DISCOVER_SUB_VENDOR] = 0,
-        [MOD_SCMI_BASE_DISCOVER_IMPLEMENTATION_VERSION] = 0,
-        [MOD_SCMI_BASE_DISCOVER_LIST_PROTOCOLS] =
-            (unsigned int)sizeof(struct scmi_base_discover_list_protocols_a2p),
-        [MOD_SCMI_BASE_DISCOVER_AGENT] =
-            (unsigned int)sizeof(struct scmi_base_discover_agent_a2p),
+	[MOD_SCMI_PROTOCOL_VERSION] = 0,
+	[MOD_SCMI_PROTOCOL_ATTRIBUTES] = 0,
+	[MOD_SCMI_PROTOCOL_MESSAGE_ATTRIBUTES] =
+	    (unsigned int)sizeof(struct scmi_protocol_message_attributes_a2p),
+	[MOD_SCMI_BASE_DISCOVER_VENDOR] = 0,
+	[MOD_SCMI_BASE_DISCOVER_SUB_VENDOR] = 0,
+	[MOD_SCMI_BASE_DISCOVER_IMPLEMENTATION_VERSION] = 0,
+	[MOD_SCMI_BASE_DISCOVER_LIST_PROTOCOLS] =
+	    (unsigned int)sizeof(struct scmi_base_discover_list_protocols_a2p),
+	[MOD_SCMI_BASE_DISCOVER_AGENT] =
+	    (unsigned int)sizeof(struct scmi_base_discover_agent_a2p),
 #ifdef BUILD_HAS_MOD_RESOURCE_PERMS
-        [MOD_SCMI_BASE_SET_DEVICE_PERMISSIONS] =
-            sizeof(struct scmi_base_set_device_permissions_a2p),
-        [MOD_SCMI_BASE_SET_PROTOCOL_PERMISSIONS] =
-            sizeof(struct scmi_base_set_protocol_permissions_a2p),
-        [MOD_SCMI_BASE_RESET_AGENT_CONFIG] =
-            sizeof(struct scmi_base_reset_agent_config_a2p),
+	[MOD_SCMI_BASE_SET_DEVICE_PERMISSIONS] =
+	    sizeof(struct scmi_base_set_device_permissions_a2p),
+	[MOD_SCMI_BASE_SET_PROTOCOL_PERMISSIONS] =
+	    sizeof(struct scmi_base_set_protocol_permissions_a2p),
+	[MOD_SCMI_BASE_RESET_AGENT_CONFIG] =
+	    sizeof(struct scmi_base_reset_agent_config_a2p),
 #endif
     };
 
@@ -100,18 +100,18 @@ static int (*const base_handler_table[MO
     [MOD_SCMI_PROTOCOL_VERSION] = scmi_base_protocol_version_handler,
     [MOD_SCMI_PROTOCOL_ATTRIBUTES] = scmi_base_protocol_attributes_handler,
     [MOD_SCMI_PROTOCOL_MESSAGE_ATTRIBUTES] =
-        scmi_base_protocol_message_attributes_handler,
+	scmi_base_protocol_message_attributes_handler,
     [MOD_SCMI_BASE_DISCOVER_VENDOR] = scmi_base_discover_vendor_handler,
     [MOD_SCMI_BASE_DISCOVER_SUB_VENDOR] = scmi_base_discover_sub_vendor_handler,
     [MOD_SCMI_BASE_DISCOVER_IMPLEMENTATION_VERSION] =
-        scmi_base_discover_implementation_version_handler,
+	scmi_base_discover_implementation_version_handler,
     [MOD_SCMI_BASE_DISCOVER_LIST_PROTOCOLS] =
-        scmi_base_discover_list_protocols_handler,
+	scmi_base_discover_list_protocols_handler,
     [MOD_SCMI_BASE_DISCOVER_AGENT] = scmi_base_discover_agent_handler,
 #ifdef BUILD_HAS_MOD_RESOURCE_PERMS
     [MOD_SCMI_BASE_SET_DEVICE_PERMISSIONS] = scmi_base_set_device_permissions,
     [MOD_SCMI_BASE_SET_PROTOCOL_PERMISSIONS] =
-        scmi_base_set_protocol_permissions,
+	scmi_base_set_protocol_permissions,
     [MOD_SCMI_BASE_RESET_AGENT_CONFIG] = scmi_base_reset_agent_config,
 #endif
 };
@@ -126,12 +126,12 @@ static int scmi_base_protocol_version_ha
     const uint32_t *payload)
 {
     struct scmi_protocol_version_p2a return_values = {
-        .status = (int32_t)SCMI_SUCCESS,
-        .version = SCMI_PROTOCOL_VERSION_BASE,
+	.status = (int32_t)SCMI_SUCCESS,
+	.version = SCMI_PROTOCOL_VERSION_BASE,
     };
 
     return protocol_api->respond(
-        service_id, &return_values, sizeof(return_values));
+	service_id, &return_values, sizeof(return_values));
 }
 
 /*
@@ -156,37 +156,37 @@ static int scmi_base_protocol_attributes
 
     status = protocol_api->get_agent_id(service_id, &agent_id);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
 #ifdef BUILD_HAS_MOD_RESOURCE_PERMS
     for (index = 0, protocol_count = 0, global_protocol_count = 0;
-         (index < FWK_ARRAY_SIZE(shared_scmi_ctx->scmi_protocol_id_to_idx)) &&
-         (global_protocol_count < shared_scmi_ctx->protocol_count);
-         index++) {
-        if ((shared_scmi_ctx->scmi_protocol_id_to_idx[index] == 0) ||
-            (index == MOD_SCMI_PROTOCOL_ID_BASE)) {
-            continue;
-        }
-
-        protocol_id = (uint8_t)index;
-
-        /*
-         * Check that the agent has the permission to access the protocol
-         */
-        perms = shared_scmi_ctx->res_perms_api->agent_has_protocol_permission(
-            agent_id, protocol_id);
-
-        if (perms == MOD_RES_PERMS_ACCESS_ALLOWED) {
-            protocol_count++;
-        }
+	 (index < FWK_ARRAY_SIZE(shared_scmi_ctx->scmi_protocol_id_to_idx)) &&
+	 (global_protocol_count < shared_scmi_ctx->protocol_count);
+	 index++) {
+	if ((shared_scmi_ctx->scmi_protocol_id_to_idx[index] == 0) ||
+	    (index == MOD_SCMI_PROTOCOL_ID_BASE)) {
+	    continue;
+	}
+
+	protocol_id = (uint8_t)index;
+
+	/*
+	 * Check that the agent has the permission to access the protocol
+	 */
+	perms = shared_scmi_ctx->res_perms_api->agent_has_protocol_permission(
+	    agent_id, protocol_id);
+
+	if (perms == MOD_RES_PERMS_ACCESS_ALLOWED) {
+	    protocol_count++;
+	}
 
-        global_protocol_count++;
+	global_protocol_count++;
     }
 #else
     status = protocol_api->get_agent_type(agent_id, &agent_type);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     /*
@@ -194,25 +194,25 @@ static int scmi_base_protocol_attributes
      * for the platform.
      */
     if (agent_type == SCMI_AGENT_TYPE_PSCI) {
-        fwk_assert(
-            shared_scmi_ctx->protocol_count >
-            shared_scmi_ctx->config->dis_protocol_count_psci);
-        protocol_count = shared_scmi_ctx->protocol_count -
-            shared_scmi_ctx->config->dis_protocol_count_psci;
+	fwk_assert(
+	    shared_scmi_ctx->protocol_count >
+	    shared_scmi_ctx->config->dis_protocol_count_psci);
+	protocol_count = shared_scmi_ctx->protocol_count -
+	    shared_scmi_ctx->config->dis_protocol_count_psci;
     } else {
-        protocol_count = shared_scmi_ctx->protocol_count;
+	protocol_count = shared_scmi_ctx->protocol_count;
     }
 #endif
 
     struct scmi_protocol_attributes_p2a return_values = {
-        .status = (int32_t)SCMI_SUCCESS,
+	.status = (int32_t)SCMI_SUCCESS,
     };
 
     return_values.attributes = (uint32_t)SCMI_BASE_PROTOCOL_ATTRIBUTES(
-        protocol_count, shared_scmi_ctx->config->agent_count);
+	protocol_count, shared_scmi_ctx->config->agent_count);
 
     return protocol_api->respond(
-        service_id, &return_values, sizeof(return_values));
+	service_id, &return_values, sizeof(return_values));
 }
 
 /*
@@ -225,15 +225,15 @@ static int scmi_base_protocol_message_at
     struct scmi_protocol_message_attributes_a2p *parameters;
     unsigned int message_id;
     struct scmi_protocol_message_attributes_p2a return_values = {
-        .status = (int32_t)SCMI_NOT_FOUND,
+	.status = (int32_t)SCMI_NOT_FOUND,
     };
 
     parameters = (struct scmi_protocol_message_attributes_a2p *)payload;
     message_id = parameters->message_id;
 
     if ((message_id < FWK_ARRAY_SIZE(base_handler_table)) &&
-        (base_handler_table[message_id] != NULL)) {
-        return_values.status = (int32_t)SCMI_SUCCESS;
+	(base_handler_table[message_id] != NULL)) {
+	return_values.status = (int32_t)SCMI_SUCCESS;
     }
 
     /* For this protocol, all commands have an attributes value of 0, which
@@ -241,10 +241,10 @@ static int scmi_base_protocol_message_at
      */
 
     return protocol_api->respond(
-        service_id,
-        &return_values,
-        (return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
-                                                 sizeof(return_values.status));
+	service_id,
+	&return_values,
+	(return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
+						 sizeof(return_values.status));
 }
 
 /*
@@ -255,18 +255,18 @@ static int scmi_base_discover_vendor_han
     const uint32_t *payload)
 {
     struct scmi_base_discover_vendor_p2a return_values = {
-        .status = (int32_t)SCMI_SUCCESS,
+	.status = (int32_t)SCMI_SUCCESS,
     };
 
     if (shared_scmi_ctx->config->vendor_identifier != NULL) {
-        fwk_str_strncpy(
-            return_values.vendor_identifier,
-            shared_scmi_ctx->config->vendor_identifier,
-            sizeof(return_values.vendor_identifier) - 1);
+	fwk_str_strncpy(
+	    return_values.vendor_identifier,
+	    shared_scmi_ctx->config->vendor_identifier,
+	    sizeof(return_values.vendor_identifier) - 1);
     }
 
     return protocol_api->respond(
-        service_id, &return_values, sizeof(return_values));
+	service_id, &return_values, sizeof(return_values));
 }
 
 /*
@@ -277,18 +277,18 @@ static int scmi_base_discover_sub_vendor
     const uint32_t *payload)
 {
     struct scmi_base_discover_sub_vendor_p2a return_values = {
-        .status = (int32_t)SCMI_SUCCESS,
+	.status = (int32_t)SCMI_SUCCESS,
     };
 
     if (shared_scmi_ctx->config->sub_vendor_identifier != NULL) {
-        fwk_str_strncpy(
-            return_values.sub_vendor_identifier,
-            shared_scmi_ctx->config->sub_vendor_identifier,
-            sizeof(return_values.sub_vendor_identifier) - 1);
+	fwk_str_strncpy(
+	    return_values.sub_vendor_identifier,
+	    shared_scmi_ctx->config->sub_vendor_identifier,
+	    sizeof(return_values.sub_vendor_identifier) - 1);
     }
 
     return protocol_api->respond(
-        service_id, &return_values, sizeof(return_values));
+	service_id, &return_values, sizeof(return_values));
 }
 
 /*
@@ -299,11 +299,11 @@ static int scmi_base_discover_implementa
     const uint32_t *payload)
 {
     struct scmi_protocol_version_p2a return_values = {
-        .status = (int32_t)SCMI_SUCCESS, .version = FWK_BUILD_VERSION
+	.status = (int32_t)SCMI_SUCCESS, .version = FWK_BUILD_VERSION
     };
 
     return protocol_api->respond(
-        service_id, &return_values, sizeof(return_values));
+	service_id, &return_values, sizeof(return_values));
 }
 
 /*
@@ -316,8 +316,8 @@ static int scmi_base_discover_list_proto
     int status, respond_status;
     const struct scmi_base_discover_list_protocols_a2p *parameters;
     struct scmi_base_discover_list_protocols_p2a return_values = {
-        .status = (int32_t)SCMI_GENERIC_ERROR,
-        .num_protocols = 0,
+	.status = (int32_t)SCMI_GENERIC_ERROR,
+	.num_protocols = 0,
     };
     unsigned int skip;
     size_t max_payload_size;
@@ -338,150 +338,150 @@ static int scmi_base_discover_list_proto
 
     status = protocol_api->get_agent_id(service_id, &agent_id);
     if (status != FWK_SUCCESS) {
-        goto error;
+	goto error;
     }
 
 #ifndef BUILD_HAS_MOD_RESOURCE_PERMS
     status = protocol_api->get_agent_type(agent_id, &agent_type);
     if (status != FWK_SUCCESS) {
-        goto error;
+	goto error;
     }
 #endif
 
     status = protocol_api->get_max_payload_size(service_id, &max_payload_size);
     if (status != FWK_SUCCESS) {
-        goto error;
+	goto error;
     }
 
     if (max_payload_size <
-        (sizeof(struct scmi_base_discover_list_protocols_p2a) +
-         sizeof(return_values.protocols[0]))) {
-        status = FWK_E_SIZE;
-        goto error;
+	(sizeof(struct scmi_base_discover_list_protocols_p2a) +
+	 sizeof(return_values.protocols[0]))) {
+	status = FWK_E_SIZE;
+	goto error;
     }
 
     entry_count =
-        max_payload_size - sizeof(struct scmi_base_discover_list_protocols_p2a);
+	max_payload_size - sizeof(struct scmi_base_discover_list_protocols_p2a);
 
     parameters = (const struct scmi_base_discover_list_protocols_a2p *)payload;
     skip = parameters->skip;
 
 #ifdef BUILD_HAS_MOD_RESOURCE_PERMS
     protocol_count_max =
-        (shared_scmi_ctx->protocol_count < (skip + entry_count)) ?
-        shared_scmi_ctx->protocol_count :
-        (skip + entry_count);
+	(shared_scmi_ctx->protocol_count < (skip + entry_count)) ?
+	shared_scmi_ctx->protocol_count :
+	(skip + entry_count);
 #else
 
     if (agent_type == SCMI_AGENT_TYPE_PSCI) {
-        fwk_assert(
-            shared_scmi_ctx->protocol_count >
-            shared_scmi_ctx->config->dis_protocol_count_psci);
-
-        protocol_count_psci = shared_scmi_ctx->protocol_count -
-            shared_scmi_ctx->config->dis_protocol_count_psci;
-
-        protocol_count_max = (protocol_count_psci < (skip + entry_count)) ?
-            protocol_count_psci :
-            (skip + entry_count);
+	fwk_assert(
+	    shared_scmi_ctx->protocol_count >
+	    shared_scmi_ctx->config->dis_protocol_count_psci);
+
+	protocol_count_psci = shared_scmi_ctx->protocol_count -
+	    shared_scmi_ctx->config->dis_protocol_count_psci;
+
+	protocol_count_max = (protocol_count_psci < (skip + entry_count)) ?
+	    protocol_count_psci :
+	    (skip + entry_count);
     } else {
-        protocol_count_max =
-            (shared_scmi_ctx->protocol_count < (skip + entry_count)) ?
-            shared_scmi_ctx->protocol_count :
-            (skip + entry_count);
+	protocol_count_max =
+	    (shared_scmi_ctx->protocol_count < (skip + entry_count)) ?
+	    shared_scmi_ctx->protocol_count :
+	    (skip + entry_count);
     }
 #endif
 
     for (index = 0,
-        protocol_count = 0,
-        payload_size = sizeof(struct scmi_base_discover_list_protocols_p2a);
-         (index < FWK_ARRAY_SIZE(shared_scmi_ctx->scmi_protocol_id_to_idx)) &&
-         (protocol_count < protocol_count_max);
-         index++) {
-        if ((shared_scmi_ctx->scmi_protocol_id_to_idx[index] == 0) ||
-            (index == MOD_SCMI_PROTOCOL_ID_BASE)) {
-            continue;
-        }
+	protocol_count = 0,
+	payload_size = sizeof(struct scmi_base_discover_list_protocols_p2a);
+	 (index < FWK_ARRAY_SIZE(shared_scmi_ctx->scmi_protocol_id_to_idx)) &&
+	 (protocol_count < protocol_count_max);
+	 index++) {
+	if ((shared_scmi_ctx->scmi_protocol_id_to_idx[index] == 0) ||
+	    (index == MOD_SCMI_PROTOCOL_ID_BASE)) {
+	    continue;
+	}
 
-        protocol_id = (uint8_t)index;
+	protocol_id = (uint8_t)index;
 
 #ifdef BUILD_HAS_MOD_RESOURCE_PERMS
-        /*
-         * Check that the agent has the permission to access the protocol
-         */
-        perms = shared_scmi_ctx->res_perms_api->agent_has_protocol_permission(
-            agent_id, protocol_id);
-
-        if (perms == MOD_RES_PERMS_ACCESS_DENIED) {
-            continue;
-        }
+	/*
+	 * Check that the agent has the permission to access the protocol
+	 */
+	perms = shared_scmi_ctx->res_perms_api->agent_has_protocol_permission(
+	    agent_id, protocol_id);
+
+	if (perms == MOD_RES_PERMS_ACCESS_DENIED) {
+	    continue;
+	}
 #else
-        /*
-         * PSCI agents are only allowed access certain protocols defined
-         * for the platform.
-         */
-        if (agent_type == SCMI_AGENT_TYPE_PSCI) {
-            /*
-             * assert if a valid list of disabled protocols is supplied in case
-             * the number of the disabled protocols is not zero. In case the
-             * number of the disabled protocols is zero , then no list needs to
-             * be supplied
-             */
-            fwk_assert(
-                (shared_scmi_ctx->config->dis_protocol_list_psci != NULL) ||
-                (shared_scmi_ctx->config->dis_protocol_count_psci == 0));
-
-            /*
-             * check if the current protocol is within the disabled protocols
-             * list.
-             */
-            for (dis_protocol_list_psci_index = 0;
-                 dis_protocol_list_psci_index <
-                 shared_scmi_ctx->config->dis_protocol_count_psci;
-                 dis_protocol_list_psci_index++) {
-                if (protocol_id ==
-                    shared_scmi_ctx->config->dis_protocol_list_psci
-                        [dis_protocol_list_psci_index]) {
-                    break;
-                }
-            }
-
-            /*
-             * don't include the protocol in case it is in the disabled list
-             */
-            if (dis_protocol_list_psci_index !=
-                shared_scmi_ctx->config->dis_protocol_count_psci) {
-                continue;
-            }
-        }
-#endif
-
-        protocol_count++;
-        if (protocol_count <= skip) {
-            continue;
-        }
-
-        status = protocol_api->write_payload(
-            service_id, payload_size, &protocol_id, sizeof(protocol_id));
-        if (status != FWK_SUCCESS) {
-            goto error;
-        }
-        payload_size += sizeof(protocol_id);
-        avail_protocol_count++;
+	/*
+	 * PSCI agents are only allowed access certain protocols defined
+	 * for the platform.
+	 */
+	if (agent_type == SCMI_AGENT_TYPE_PSCI) {
+	    /*
+	     * assert if a valid list of disabled protocols is supplied in case
+	     * the number of the disabled protocols is not zero. In case the
+	     * number of the disabled protocols is zero , then no list needs to
+	     * be supplied
+	     */
+	    fwk_assert(
+		(shared_scmi_ctx->config->dis_protocol_list_psci != NULL) ||
+		(shared_scmi_ctx->config->dis_protocol_count_psci == 0));
+
+	    /*
+	     * check if the current protocol is within the disabled protocols
+	     * list.
+	     */
+	    for (dis_protocol_list_psci_index = 0;
+		 dis_protocol_list_psci_index <
+		 shared_scmi_ctx->config->dis_protocol_count_psci;
+		 dis_protocol_list_psci_index++) {
+		if (protocol_id ==
+		    shared_scmi_ctx->config->dis_protocol_list_psci
+			[dis_protocol_list_psci_index]) {
+		    break;
+		}
+	    }
+
+	    /*
+	     * don't include the protocol in case it is in the disabled list
+	     */
+	    if (dis_protocol_list_psci_index !=
+		shared_scmi_ctx->config->dis_protocol_count_psci) {
+		continue;
+	    }
+	}
+#endif
+
+	protocol_count++;
+	if (protocol_count <= skip) {
+	    continue;
+	}
+
+	status = protocol_api->write_payload(
+	    service_id, payload_size, &protocol_id, sizeof(protocol_id));
+	if (status != FWK_SUCCESS) {
+	    goto error;
+	}
+	payload_size += sizeof(protocol_id);
+	avail_protocol_count++;
     }
 
     if (skip > protocol_count) {
-        return_values.status = (int32_t)SCMI_INVALID_PARAMETERS;
-        goto error;
+	return_values.status = (int32_t)SCMI_INVALID_PARAMETERS;
+	goto error;
     }
 
     return_values.status = (int32_t)SCMI_SUCCESS;
     return_values.num_protocols = (uint32_t)avail_protocol_count;
 
     status = protocol_api->write_payload(
-        service_id, 0, &return_values, sizeof(return_values));
+	service_id, 0, &return_values, sizeof(return_values));
     if (status != FWK_SUCCESS) {
-        goto error;
+	goto error;
     }
 
     payload_size = FWK_ALIGN_NEXT(payload_size, sizeof(uint32_t));
@@ -490,12 +490,12 @@ static int scmi_base_discover_list_proto
 
 error:
     respond_status = protocol_api->respond(
-        service_id,
-        &return_values,
-        (return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
-                                                 sizeof(return_values.status));
+	service_id,
+	&return_values,
+	(return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
+						 sizeof(return_values.status));
     if (respond_status != FWK_SUCCESS) {
-        FWK_LOG_DEBUG("[SCMI_BASE] %s @%d", __func__, __LINE__);
+	FWK_LOG_DEBUG("[SCMI_BASE] %s @%d", __func__, __LINE__);
     }
 
     return status;
@@ -510,7 +510,7 @@ static int scmi_base_discover_agent_hand
 {
     const struct scmi_base_discover_agent_a2p *parameters;
     struct scmi_base_discover_agent_p2a return_values = {
-        .status = (int32_t)SCMI_NOT_FOUND,
+	.status = (int32_t)SCMI_NOT_FOUND,
     };
     const struct mod_scmi_agent *agent;
 
@@ -523,54 +523,54 @@ static int scmi_base_discover_agent_hand
 
 #if (SCMI_PROTOCOL_VERSION_BASE >= UINT32_C(0x20000))
     if ((parameters->agent_id > shared_scmi_ctx->config->agent_count) &&
-        (parameters->agent_id != 0xFFFFFFFF)) {
-        goto exit;
+	(parameters->agent_id != 0xFFFFFFFF)) {
+	goto exit;
     }
 #else
     if (parameters->agent_id > shared_scmi_ctx->config->agent_count) {
-        goto exit;
+	goto exit;
     }
 #endif
 
     return_values.status = (int32_t)SCMI_SUCCESS;
 
     if (parameters->agent_id == MOD_SCMI_PLATFORM_ID) {
-        static const char name[] = "platform";
+	static const char name[] = "platform";
 
-        static_assert(
-            sizeof(return_values.name) >= sizeof(name),
-            "return_values.name is not large enough to contain name");
+	static_assert(
+	    sizeof(return_values.name) >= sizeof(name),
+	    "return_values.name is not large enough to contain name");
 
-        fwk_str_memcpy(return_values.name, name, sizeof(name));
+	fwk_str_memcpy(return_values.name, name, sizeof(name));
 
 #if (SCMI_PROTOCOL_VERSION_BASE >= UINT32_C(0x20000))
-        return_values.agent_id = MOD_SCMI_PLATFORM_ID;
+	return_values.agent_id = MOD_SCMI_PLATFORM_ID;
 #endif
 
-        goto exit;
+	goto exit;
     }
 
 #if (SCMI_PROTOCOL_VERSION_BASE >= UINT32_C(0x20000))
     if (parameters->agent_id == 0xFFFFFFFF) {
-        /*
-         * An agent can discover its own agent_id and name by passing agent_id
-         * of 0xFFFFFFFF. In this case, the command returns the agent_id and
-         * name of the calling agent.
-         */
-
-        status = protocol_api->get_agent_id(service_id, &agent_id);
-        if (status != FWK_SUCCESS) {
-            return FWK_E_ACCESS;
-        }
-
-        return_values.agent_id = (uint32_t)agent_id;
-
-        fwk_str_strncpy(
-            &return_values.name[0],
-            fwk_module_get_element_name(service_id),
-            sizeof(return_values.name) - 1);
+	/*
+	 * An agent can discover its own agent_id and name by passing agent_id
+	 * of 0xFFFFFFFF. In this case, the command returns the agent_id and
+	 * name of the calling agent.
+	 */
+
+	status = protocol_api->get_agent_id(service_id, &agent_id);
+	if (status != FWK_SUCCESS) {
+	    return FWK_E_ACCESS;
+	}
+
+	return_values.agent_id = (uint32_t)agent_id;
+
+	fwk_str_strncpy(
+	    &return_values.name[0],
+	    fwk_module_get_element_name(service_id),
+	    sizeof(return_values.name) - 1);
 
-        goto exit;
+	goto exit;
     }
 #endif
 
@@ -585,16 +585,16 @@ static int scmi_base_discover_agent_hand
     agent = &shared_scmi_ctx->config->agent_table[parameters->agent_id];
 
     fwk_str_strncpy(
-        return_values.name,
-        (agent->name != NULL) ? agent->name : default_agent_names[agent->type],
-        sizeof(return_values.name) - 1);
+	return_values.name,
+	(agent->name != NULL) ? agent->name : default_agent_names[agent->type],
+	sizeof(return_values.name) - 1);
 
 exit:
     return protocol_api->respond(
-        service_id,
-        &return_values,
-        (return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
-                                                 sizeof(return_values.status));
+	service_id,
+	&return_values,
+	(return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
+						 sizeof(return_values.status));
 }
 
 #ifdef BUILD_HAS_MOD_RESOURCE_PERMS
@@ -608,7 +608,7 @@ static int scmi_base_set_device_permissi
 {
     const struct scmi_base_set_device_permissions_a2p *parameters;
     struct scmi_base_set_device_permissions_p2a return_values = {
-        .status = (int32_t)SCMI_NOT_FOUND,
+	.status = (int32_t)SCMI_NOT_FOUND,
     };
     int status = FWK_SUCCESS;
     int respond_status;
@@ -616,48 +616,48 @@ static int scmi_base_set_device_permissi
     parameters = (const struct scmi_base_set_device_permissions_a2p *)payload;
 
     if (parameters->agent_id > shared_scmi_ctx->config->agent_count) {
-        status = FWK_E_ACCESS;
-        goto exit;
+	status = FWK_E_ACCESS;
+	goto exit;
     }
 
     if (parameters->agent_id == MOD_SCMI_PLATFORM_ID) {
-        return_values.status = (int32_t)SCMI_SUCCESS;
-        goto exit;
+	return_values.status = (int32_t)SCMI_SUCCESS;
+	goto exit;
     }
 
     if (parameters->flags & ~(uint32_t)MOD_RES_PERMS_PERMISSIONS_MASK) {
-        return_values.status = (int32_t)SCMI_INVALID_PARAMETERS;
-        status = FWK_E_PARAM;
-        goto exit;
+	return_values.status = (int32_t)SCMI_INVALID_PARAMETERS;
+	status = FWK_E_PARAM;
+	goto exit;
     }
 
     status = shared_scmi_ctx->res_perms_api->agent_set_device_permission(
-        parameters->agent_id, parameters->device_id, parameters->flags);
+	parameters->agent_id, parameters->device_id, parameters->flags);
 
     switch (status) {
     case FWK_SUCCESS:
-        return_values.status = (int32_t)SCMI_SUCCESS;
-        break;
+	return_values.status = (int32_t)SCMI_SUCCESS;
+	break;
     case FWK_E_PARAM:
-        return_values.status = (int32_t)SCMI_INVALID_PARAMETERS;
-        break;
+	return_values.status = (int32_t)SCMI_INVALID_PARAMETERS;
+	break;
     case FWK_E_ACCESS:
-        return_values.status = (int32_t)SCMI_NOT_FOUND;
-        break;
+	return_values.status = (int32_t)SCMI_NOT_FOUND;
+	break;
     default:
-        return_values.status = (int32_t)SCMI_NOT_SUPPORTED;
-        break;
+	return_values.status = (int32_t)SCMI_NOT_SUPPORTED;
+	break;
     }
 
 exit:
     respond_status = protocol_api->respond(
-        service_id,
-        &return_values,
-        (return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
-                                                 sizeof(return_values.status));
+	service_id,
+	&return_values,
+	(return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
+						 sizeof(return_values.status));
 
     if (respond_status != FWK_SUCCESS) {
-        FWK_LOG_DEBUG("[SCMI_BASE] %s @%d", __func__, __LINE__);
+	FWK_LOG_DEBUG("[SCMI_BASE] %s @%d", __func__, __LINE__);
     }
 
     return status;
@@ -672,7 +672,7 @@ static int scmi_base_set_protocol_permis
 {
     const struct scmi_base_set_protocol_permissions_a2p *parameters;
     struct scmi_base_set_protocol_permissions_p2a return_values = {
-        .status = (int32_t)SCMI_NOT_FOUND,
+	.status = (int32_t)SCMI_NOT_FOUND,
     };
     int status = FWK_SUCCESS;
     int respond_status;
@@ -680,57 +680,57 @@ static int scmi_base_set_protocol_permis
     parameters = (const struct scmi_base_set_protocol_permissions_a2p *)payload;
 
     if (parameters->agent_id > shared_scmi_ctx->config->agent_count) {
-        status = FWK_E_ACCESS;
-        goto exit;
+	status = FWK_E_ACCESS;
+	goto exit;
     }
 
     if (parameters->agent_id == MOD_SCMI_PLATFORM_ID) {
-        return_values.status = (int32_t)SCMI_SUCCESS;
-        goto exit;
+	return_values.status = (int32_t)SCMI_SUCCESS;
+	goto exit;
     }
 
     if (parameters->flags & ~(uint32_t)MOD_RES_PERMS_PERMISSIONS_MASK) {
-        status = FWK_E_PARAM;
-        return_values.status = (int32_t)SCMI_INVALID_PARAMETERS;
-        goto exit;
+	status = FWK_E_PARAM;
+	return_values.status = (int32_t)SCMI_INVALID_PARAMETERS;
+	goto exit;
     }
 
     if (parameters->command_id == MOD_SCMI_PROTOCOL_ID_BASE) {
-        return_values.status = (int32_t)SCMI_DENIED;
-        goto exit;
+	return_values.status = (int32_t)SCMI_DENIED;
+	goto exit;
     }
 
     status =
-        shared_scmi_ctx->res_perms_api->agent_set_device_protocol_permission(
-            parameters->agent_id,
-            parameters->device_id,
-            parameters->command_id,
-            parameters->flags);
+	shared_scmi_ctx->res_perms_api->agent_set_device_protocol_permission(
+	    parameters->agent_id,
+	    parameters->device_id,
+	    parameters->command_id,
+	    parameters->flags);
 
     switch (status) {
     case FWK_SUCCESS:
-        return_values.status = (int32_t)SCMI_SUCCESS;
-        break;
+	return_values.status = (int32_t)SCMI_SUCCESS;
+	break;
     case FWK_E_PARAM:
-        return_values.status = (int32_t)SCMI_INVALID_PARAMETERS;
-        break;
+	return_values.status = (int32_t)SCMI_INVALID_PARAMETERS;
+	break;
     case FWK_E_ACCESS:
-        return_values.status = (int32_t)SCMI_NOT_FOUND;
-        break;
+	return_values.status = (int32_t)SCMI_NOT_FOUND;
+	break;
     default:
-        return_values.status = (int32_t)SCMI_NOT_SUPPORTED;
-        break;
+	return_values.status = (int32_t)SCMI_NOT_SUPPORTED;
+	break;
     }
 
 exit:
     respond_status = protocol_api->respond(
-        service_id,
-        &return_values,
-        (return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
-                                                 sizeof(return_values.status));
+	service_id,
+	&return_values,
+	(return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
+						 sizeof(return_values.status));
 
     if (respond_status != FWK_SUCCESS) {
-        FWK_LOG_DEBUG("[SCMI_BASE] %s @%d", __func__, __LINE__);
+	FWK_LOG_DEBUG("[SCMI_BASE] %s @%d", __func__, __LINE__);
     }
 
     return status;
@@ -745,50 +745,50 @@ static int scmi_base_reset_agent_config(
 {
     const struct scmi_base_reset_agent_config_a2p *parameters;
     struct scmi_base_reset_agent_config_p2a return_values = {
-        .status = (int32_t)SCMI_NOT_FOUND,
+	.status = (int32_t)SCMI_NOT_FOUND,
     };
     int status;
 
     parameters = (const struct scmi_base_reset_agent_config_a2p *)payload;
 
     if (parameters->agent_id > shared_scmi_ctx->config->agent_count) {
-        goto exit;
+	goto exit;
     }
 
     if (parameters->agent_id == MOD_SCMI_PLATFORM_ID) {
-        return_values.status = (int32_t)SCMI_SUCCESS;
-        goto exit;
+	return_values.status = (int32_t)SCMI_SUCCESS;
+	goto exit;
     }
 
     if (parameters->flags & ~(uint32_t)MOD_RES_PERMS_PERMISSIONS_MASK) {
-        return_values.status = (int32_t)SCMI_INVALID_PARAMETERS;
-        goto exit;
+	return_values.status = (int32_t)SCMI_INVALID_PARAMETERS;
+	goto exit;
     }
 
     status = shared_scmi_ctx->res_perms_api->agent_reset_config(
-        parameters->agent_id, parameters->flags);
+	parameters->agent_id, parameters->flags);
 
     switch (status) {
     case FWK_SUCCESS:
-        return_values.status = (int32_t)SCMI_SUCCESS;
-        break;
+	return_values.status = (int32_t)SCMI_SUCCESS;
+	break;
     case FWK_E_PARAM:
-        return_values.status = (int32_t)SCMI_INVALID_PARAMETERS;
-        break;
+	return_values.status = (int32_t)SCMI_INVALID_PARAMETERS;
+	break;
     case FWK_E_ACCESS:
-        return_values.status = (int32_t)SCMI_NOT_FOUND;
-        break;
+	return_values.status = (int32_t)SCMI_NOT_FOUND;
+	break;
     default:
-        return_values.status = (int32_t)SCMI_NOT_SUPPORTED;
-        break;
+	return_values.status = (int32_t)SCMI_NOT_SUPPORTED;
+	break;
     }
 
 exit:
     return protocol_api->respond(
-        service_id,
-        &return_values,
-        (return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
-                                                 sizeof(return_values.status));
+	service_id,
+	&return_values,
+	(return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
+						 sizeof(return_values.status));
 }
 
 /*
@@ -806,23 +806,23 @@ static int scmi_base_permissions_handler
 
     status = protocol_api->get_agent_id(service_id, &agent_id);
     if (status != FWK_SUCCESS) {
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 
     if (message_id < 3) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     /*
      * Check that the agent has permissions to access the message.
      */
     perms = shared_scmi_ctx->res_perms_api->agent_has_message_permission(
-        agent_id, MOD_SCMI_PROTOCOL_ID_BASE, message_id);
+	agent_id, MOD_SCMI_PROTOCOL_ID_BASE, message_id);
 
     if (perms == MOD_RES_PERMS_ACCESS_ALLOWED) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     } else {
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 }
 
@@ -838,26 +838,26 @@ int scmi_base_message_handler(
     int32_t return_value;
 
     static_assert(
-        FWK_ARRAY_SIZE(base_handler_table) ==
-            FWK_ARRAY_SIZE(base_payload_size_table),
-        "[SCMI] Base protocol table sizes not consistent");
+	FWK_ARRAY_SIZE(base_handler_table) ==
+	    FWK_ARRAY_SIZE(base_payload_size_table),
+	"[SCMI] Base protocol table sizes not consistent");
     fwk_assert(payload != NULL);
 
     if (message_id >= FWK_ARRAY_SIZE(base_handler_table)) {
-        return_value = (int32_t)SCMI_NOT_FOUND;
-        goto error;
+	return_value = (int32_t)SCMI_NOT_FOUND;
+	goto error;
     }
 
     if (payload_size != base_payload_size_table[message_id]) {
-        return_value = (int32_t)SCMI_PROTOCOL_ERROR;
-        goto error;
+	return_value = (int32_t)SCMI_PROTOCOL_ERROR;
+	goto error;
     }
 
 #ifdef BUILD_HAS_MOD_RESOURCE_PERMS
     if (scmi_base_permissions_handler(
-            service_id, payload, payload_size, message_id) != FWK_SUCCESS) {
-        return_value = (int32_t)SCMI_DENIED;
-        goto error;
+	    service_id, payload, payload_size, message_id) != FWK_SUCCESS) {
+	return_value = (int32_t)SCMI_DENIED;
+	goto error;
     }
 #endif
 
@@ -865,7 +865,7 @@ int scmi_base_message_handler(
 
 error:
     return protocol_api->respond(
-        service_id, &return_value, sizeof(return_value));
+	service_id, &return_value, sizeof(return_value));
 }
 
 void scmi_base_set_api(const struct mod_scmi_from_protocol_api *api)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi/test/mocks/Mockmod_scmi_extra.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi/test/mocks/Mockmod_scmi_extra.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi/test/mocks/Mockmod_scmi_extra.c
@@ -577,7 +577,7 @@ void CMockExpectParameters_mod_scmi_to_t
 void CMockExpectParameters_mod_scmi_to_transport_api_get_secure(CMOCK_mod_scmi_to_transport_api_get_secure_CALL_INSTANCE* cmock_call_instance, fwk_id_t channel_id, bool* secure)
 {
   memcpy((void*)(&cmock_call_instance->Expected_channel_id), (void*)(&channel_id),
-         sizeof(fwk_id_t[sizeof(channel_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
+	 sizeof(fwk_id_t[sizeof(channel_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
   cmock_call_instance->Expected_secure = secure;
   cmock_call_instance->ReturnThruPtr_secure_Used = 0;
 }
@@ -674,7 +674,7 @@ void CMockExpectParameters_mod_scmi_to_t
 void CMockExpectParameters_mod_scmi_to_transport_api_get_max_payload_size(CMOCK_mod_scmi_to_transport_api_get_max_payload_size_CALL_INSTANCE* cmock_call_instance, fwk_id_t channel_id, size_t* size)
 {
   memcpy((void*)(&cmock_call_instance->Expected_channel_id), (void*)(&channel_id),
-         sizeof(fwk_id_t[sizeof(channel_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
+	 sizeof(fwk_id_t[sizeof(channel_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
   cmock_call_instance->Expected_size = size;
   cmock_call_instance->ReturnThruPtr_size_Used = 0;
 }
@@ -774,7 +774,7 @@ void CMockExpectParameters_mod_scmi_to_t
 void CMockExpectParameters_mod_scmi_to_transport_api_get_message_header(CMOCK_mod_scmi_to_transport_api_get_message_header_CALL_INSTANCE* cmock_call_instance, fwk_id_t channel_id, uint32_t* message_header)
 {
   memcpy((void*)(&cmock_call_instance->Expected_channel_id), (void*)(&channel_id),
-         sizeof(fwk_id_t[sizeof(channel_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
+	 sizeof(fwk_id_t[sizeof(channel_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
   cmock_call_instance->Expected_message_header = message_header;
   cmock_call_instance->ReturnThruPtr_message_header_Used = 0;
 }
@@ -881,7 +881,7 @@ void CMockExpectParameters_mod_scmi_to_t
 void CMockExpectParameters_mod_scmi_to_transport_api_get_payload(CMOCK_mod_scmi_to_transport_api_get_payload_CALL_INSTANCE* cmock_call_instance, fwk_id_t channel_id, const void** payload, size_t* size)
 {
   memcpy((void*)(&cmock_call_instance->Expected_channel_id), (void*)(&channel_id),
-         sizeof(fwk_id_t[sizeof(channel_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
+	 sizeof(fwk_id_t[sizeof(channel_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
   cmock_call_instance->Expected_payload = payload;
   cmock_call_instance->ReturnThruPtr_payload_Used = 0;
   cmock_call_instance->Expected_size = size;
@@ -994,12 +994,12 @@ void CMockExpectParameters_mod_scmi_to_t
 void CMockExpectParameters_mod_scmi_to_transport_api_write_payload(CMOCK_mod_scmi_to_transport_api_write_payload_CALL_INSTANCE* cmock_call_instance, fwk_id_t channel_id, size_t offset, const void* payload, size_t size)
 {
   memcpy((void*)(&cmock_call_instance->Expected_channel_id), (void*)(&channel_id),
-         sizeof(fwk_id_t[sizeof(channel_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
+	 sizeof(fwk_id_t[sizeof(channel_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
   memcpy((void*)(&cmock_call_instance->Expected_offset), (void*)(&offset),
-         sizeof(size_t[sizeof(offset) == sizeof(size_t) ? 1 : -1])); /* add size_t to :treat_as_array if this causes an error */
+	 sizeof(size_t[sizeof(offset) == sizeof(size_t) ? 1 : -1])); /* add size_t to :treat_as_array if this causes an error */
   cmock_call_instance->Expected_payload = payload;
   memcpy((void*)(&cmock_call_instance->Expected_size), (void*)(&size),
-         sizeof(size_t[sizeof(size) == sizeof(size_t) ? 1 : -1])); /* add size_t to :treat_as_array if this causes an error */
+	 sizeof(size_t[sizeof(size) == sizeof(size_t) ? 1 : -1])); /* add size_t to :treat_as_array if this causes an error */
 }
 
 void mod_scmi_to_transport_api_write_payload_CMockExpectAnyArgsAndReturn(UNITY_LINE_TYPE cmock_line, int cmock_to_return)
@@ -1086,10 +1086,10 @@ void CMockExpectParameters_mod_scmi_to_t
 void CMockExpectParameters_mod_scmi_to_transport_api_respond(CMOCK_mod_scmi_to_transport_api_respond_CALL_INSTANCE* cmock_call_instance, fwk_id_t channel_id, const void* payload, size_t size)
 {
   memcpy((void*)(&cmock_call_instance->Expected_channel_id), (void*)(&channel_id),
-         sizeof(fwk_id_t[sizeof(channel_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
+	 sizeof(fwk_id_t[sizeof(channel_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
   cmock_call_instance->Expected_payload = payload;
   memcpy((void*)(&cmock_call_instance->Expected_size), (void*)(&size),
-         sizeof(size_t[sizeof(size) == sizeof(size_t) ? 1 : -1])); /* add size_t to :treat_as_array if this causes an error */
+	 sizeof(size_t[sizeof(size) == sizeof(size_t) ? 1 : -1])); /* add size_t to :treat_as_array if this causes an error */
 }
 
 void mod_scmi_to_transport_api_respond_CMockExpectAnyArgsAndReturn(UNITY_LINE_TYPE cmock_line, int cmock_to_return)
@@ -1184,11 +1184,11 @@ void CMockExpectParameters_mod_scmi_to_t
 void CMockExpectParameters_mod_scmi_to_transport_api_transmit(CMOCK_mod_scmi_to_transport_api_transmit_CALL_INSTANCE* cmock_call_instance, fwk_id_t channel_id, uint32_t message_header, const void* payload, size_t size, bool request_ack_by_interrupt)
 {
   memcpy((void*)(&cmock_call_instance->Expected_channel_id), (void*)(&channel_id),
-         sizeof(fwk_id_t[sizeof(channel_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
+	 sizeof(fwk_id_t[sizeof(channel_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
   cmock_call_instance->Expected_message_header = message_header;
   cmock_call_instance->Expected_payload = payload;
   memcpy((void*)(&cmock_call_instance->Expected_size), (void*)(&size),
-         sizeof(size_t[sizeof(size) == sizeof(size_t) ? 1 : -1])); /* add size_t to :treat_as_array if this causes an error */
+	 sizeof(size_t[sizeof(size) == sizeof(size_t) ? 1 : -1])); /* add size_t to :treat_as_array if this causes an error */
   cmock_call_instance->Expected_request_ack_by_interrupt = request_ack_by_interrupt;
 }
 
@@ -1265,7 +1265,7 @@ void CMockExpectParameters_mod_scmi_to_t
 void CMockExpectParameters_mod_scmi_to_transport_api_release_transport_channel_lock(CMOCK_mod_scmi_to_transport_api_release_transport_channel_lock_CALL_INSTANCE* cmock_call_instance, fwk_id_t channel_id)
 {
   memcpy((void*)(&cmock_call_instance->Expected_channel_id), (void*)(&channel_id),
-         sizeof(fwk_id_t[sizeof(channel_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
+	 sizeof(fwk_id_t[sizeof(channel_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
 }
 
 void mod_scmi_to_transport_api_release_transport_channel_lock_CMockExpectAnyArgsAndReturn(UNITY_LINE_TYPE cmock_line, int cmock_to_return)
@@ -1448,7 +1448,7 @@ void CMockExpectParameters_mod_scmi_from
 void CMockExpectParameters_mod_scmi_from_protocol_get_agent_id(CMOCK_mod_scmi_from_protocol_get_agent_id_CALL_INSTANCE* cmock_call_instance, fwk_id_t service_id, unsigned int* agent_id)
 {
   memcpy((void*)(&cmock_call_instance->Expected_service_id), (void*)(&service_id),
-         sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
+	 sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
   cmock_call_instance->Expected_agent_id = agent_id;
   cmock_call_instance->ReturnThruPtr_agent_id_Used = 0;
 }
@@ -1641,7 +1641,7 @@ void CMockExpectParameters_mod_scmi_from
 void CMockExpectParameters_mod_scmi_from_protocol_get_max_payload_size(CMOCK_mod_scmi_from_protocol_get_max_payload_size_CALL_INSTANCE* cmock_call_instance, fwk_id_t service_id, size_t* size)
 {
   memcpy((void*)(&cmock_call_instance->Expected_service_id), (void*)(&service_id),
-         sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
+	 sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
   cmock_call_instance->Expected_size = size;
   cmock_call_instance->ReturnThruPtr_size_Used = 0;
 }
@@ -1743,12 +1743,12 @@ void CMockExpectParameters_mod_scmi_from
 void CMockExpectParameters_mod_scmi_from_protocol_write_payload(CMOCK_mod_scmi_from_protocol_write_payload_CALL_INSTANCE* cmock_call_instance, fwk_id_t service_id, size_t offset, const void* payload, size_t size)
 {
   memcpy((void*)(&cmock_call_instance->Expected_service_id), (void*)(&service_id),
-         sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
+	 sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
   memcpy((void*)(&cmock_call_instance->Expected_offset), (void*)(&offset),
-         sizeof(size_t[sizeof(offset) == sizeof(size_t) ? 1 : -1])); /* add size_t to :treat_as_array if this causes an error */
+	 sizeof(size_t[sizeof(offset) == sizeof(size_t) ? 1 : -1])); /* add size_t to :treat_as_array if this causes an error */
   cmock_call_instance->Expected_payload = payload;
   memcpy((void*)(&cmock_call_instance->Expected_size), (void*)(&size),
-         sizeof(size_t[sizeof(size) == sizeof(size_t) ? 1 : -1])); /* add size_t to :treat_as_array if this causes an error */
+	 sizeof(size_t[sizeof(size) == sizeof(size_t) ? 1 : -1])); /* add size_t to :treat_as_array if this causes an error */
 }
 
 void mod_scmi_from_protocol_write_payload_CMockExpectAnyArgsAndReturn(UNITY_LINE_TYPE cmock_line, int cmock_to_return)
@@ -1835,10 +1835,10 @@ void CMockExpectParameters_mod_scmi_from
 void CMockExpectParameters_mod_scmi_from_protocol_respond(CMOCK_mod_scmi_from_protocol_respond_CALL_INSTANCE* cmock_call_instance, fwk_id_t service_id, const void* payload, size_t size)
 {
   memcpy((void*)(&cmock_call_instance->Expected_service_id), (void*)(&service_id),
-         sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
+	 sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
   cmock_call_instance->Expected_payload = payload;
   memcpy((void*)(&cmock_call_instance->Expected_size), (void*)(&size),
-         sizeof(size_t[sizeof(size) == sizeof(size_t) ? 1 : -1])); /* add size_t to :treat_as_array if this causes an error */
+	 sizeof(size_t[sizeof(size) == sizeof(size_t) ? 1 : -1])); /* add size_t to :treat_as_array if this causes an error */
 }
 
 void mod_scmi_from_protocol_respond_CMockExpectAnyArgsAndReturn(UNITY_LINE_TYPE cmock_line, int cmock_to_return)
@@ -1932,12 +1932,12 @@ void CMockExpectParameters_mod_scmi_from
 void CMockExpectParameters_mod_scmi_from_protocol_notify(CMOCK_mod_scmi_from_protocol_notify_CALL_INSTANCE* cmock_call_instance, fwk_id_t service_id, int protocol_id, int message_id, const void* payload, size_t size)
 {
   memcpy((void*)(&cmock_call_instance->Expected_service_id), (void*)(&service_id),
-         sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
+	 sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
   cmock_call_instance->Expected_protocol_id = protocol_id;
   cmock_call_instance->Expected_message_id = message_id;
   cmock_call_instance->Expected_payload = payload;
   memcpy((void*)(&cmock_call_instance->Expected_size), (void*)(&size),
-         sizeof(size_t[sizeof(size) == sizeof(size_t) ? 1 : -1])); /* add size_t to :treat_as_array if this causes an error */
+	 sizeof(size_t[sizeof(size) == sizeof(size_t) ? 1 : -1])); /* add size_t to :treat_as_array if this causes an error */
 }
 
 void mod_scmi_from_protocol_notify_CMockExpectAnyArgs(UNITY_LINE_TYPE cmock_line)
@@ -2041,10 +2041,10 @@ void CMockExpectParameters_mod_scmi_from
   cmock_call_instance->Expected_scmi_protocol_id = scmi_protocol_id;
   cmock_call_instance->Expected_token = token;
   memcpy((void*)(&cmock_call_instance->Expected_service_id), (void*)(&service_id),
-         sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
+	 sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
   cmock_call_instance->Expected_payload = payload;
   memcpy((void*)(&cmock_call_instance->Expected_payload_size), (void*)(&payload_size),
-         sizeof(size_t[sizeof(payload_size) == sizeof(size_t) ? 1 : -1])); /* add size_t to :treat_as_array if this causes an error */
+	 sizeof(size_t[sizeof(payload_size) == sizeof(size_t) ? 1 : -1])); /* add size_t to :treat_as_array if this causes an error */
   cmock_call_instance->Expected_request_ack_by_interrupt = request_ack_by_interrupt;
 }
 
@@ -2121,7 +2121,7 @@ void CMockExpectParameters_mod_scmi_from
 void CMockExpectParameters_mod_scmi_from_protocol_response_message_handler(CMOCK_mod_scmi_from_protocol_response_message_handler_CALL_INSTANCE* cmock_call_instance, fwk_id_t service_id)
 {
   memcpy((void*)(&cmock_call_instance->Expected_service_id), (void*)(&service_id),
-         sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
+	 sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
 }
 
 void mod_scmi_from_protocol_response_message_handler_CMockExpectAnyArgsAndReturn(UNITY_LINE_TYPE cmock_line, int cmock_to_return)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi/test/mod_scmi_unit_test.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi/test/mod_scmi_unit_test.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi/test/mod_scmi_unit_test.c
@@ -45,31 +45,31 @@ enum fake_services {
 
 static const struct fwk_element element_table[] = {
     [FAKE_SERVICE_IDX_PSCI] = {
-        .name = "PSCI",
-        .data = &(struct mod_scmi_service_config) {
-            .transport_id = FWK_ID_ELEMENT_INIT(
-                FAKE_MODULE_ID,
-                FAKE_SERVICE_IDX_PSCI),
-            .transport_api_id = FWK_ID_API_INIT(
-                FAKE_MODULE_ID,
-                FAKE_API_IDX_SCMI_TRANSPORT),
-            .scmi_agent_id = (unsigned int) FAKE_SCMI_AGENT_IDX_PSCI,
-            .scmi_p2a_id = FWK_ID_NONE_INIT,
-        },
+	.name = "PSCI",
+	.data = &(struct mod_scmi_service_config) {
+	    .transport_id = FWK_ID_ELEMENT_INIT(
+		FAKE_MODULE_ID,
+		FAKE_SERVICE_IDX_PSCI),
+	    .transport_api_id = FWK_ID_API_INIT(
+		FAKE_MODULE_ID,
+		FAKE_API_IDX_SCMI_TRANSPORT),
+	    .scmi_agent_id = (unsigned int) FAKE_SCMI_AGENT_IDX_PSCI,
+	    .scmi_p2a_id = FWK_ID_NONE_INIT,
+	},
     },
 
     [FAKE_SERVICE_IDX_OSPM] = {
-        .name = "OSPM-A2P-0",
-        .data = &(struct mod_scmi_service_config) {
-            .transport_id = FWK_ID_ELEMENT_INIT(
-                FAKE_MODULE_ID,
-                FAKE_SERVICE_IDX_OSPM),
-            .transport_api_id = FWK_ID_API_INIT(
-                FAKE_MODULE_ID,
-                FAKE_API_IDX_SCMI_TRANSPORT),
-            .scmi_agent_id = (unsigned int) FAKE_SCMI_AGENT_IDX_OSPM,
-            .scmi_p2a_id = FWK_ID_NONE_INIT,
-        },
+	.name = "OSPM-A2P-0",
+	.data = &(struct mod_scmi_service_config) {
+	    .transport_id = FWK_ID_ELEMENT_INIT(
+		FAKE_MODULE_ID,
+		FAKE_SERVICE_IDX_OSPM),
+	    .transport_api_id = FWK_ID_API_INIT(
+		FAKE_MODULE_ID,
+		FAKE_API_IDX_SCMI_TRANSPORT),
+	    .scmi_agent_id = (unsigned int) FAKE_SCMI_AGENT_IDX_OSPM,
+	    .scmi_p2a_id = FWK_ID_NONE_INIT,
+	},
     },
 
     [FAKE_SERVICE_IDX_COUNT] = { 0 },
@@ -78,12 +78,12 @@ static const struct fwk_element element_
 
 static const struct mod_scmi_agent agent_table[] = {
     [FAKE_SCMI_AGENT_IDX_OSPM] = {
-        .type = SCMI_AGENT_TYPE_OSPM,
-        .name = "OSPM",
+	.type = SCMI_AGENT_TYPE_OSPM,
+	.name = "OSPM",
     },
     [FAKE_SCMI_AGENT_IDX_PSCI] = {
-        .type = SCMI_AGENT_TYPE_PSCI,
-        .name = "PSCI",
+	.type = SCMI_AGENT_TYPE_PSCI,
+	.name = "PSCI",
     },
 };
 
@@ -94,13 +94,13 @@ static const struct fwk_element *get_ele
 
 struct fwk_module_config config_scmi = {
     .data =
-        &(struct mod_scmi_config){
-            .protocol_count_max = 6,
-            .agent_count = FWK_ARRAY_SIZE(agent_table) - 1,
-            .agent_table = agent_table,
-            .vendor_identifier = "arm",
-            .sub_vendor_identifier = "arm",
-        },
+	&(struct mod_scmi_config){
+	    .protocol_count_max = 6,
+	    .agent_count = FWK_ARRAY_SIZE(agent_table) - 1,
+	    .agent_table = agent_table,
+	    .vendor_identifier = "arm",
+	    .sub_vendor_identifier = "arm",
+	},
 
     .elements = FWK_MODULE_DYNAMIC_ELEMENTS(get_element_table),
 };
@@ -114,7 +114,7 @@ static struct mod_scmi_to_transport_api
     .respond = mod_scmi_to_transport_api_respond,
     .transmit = mod_scmi_to_transport_api_transmit,
     .release_transport_channel_lock =
-        mod_scmi_to_transport_api_release_transport_channel_lock,
+	mod_scmi_to_transport_api_release_transport_channel_lock,
 };
 
 static const struct mod_scmi_from_protocol_api from_protocol_api = {
@@ -133,23 +133,23 @@ void setUp(void)
     const struct mod_scmi_to_transport_api *transport_api = &to_transport_api;
     scmi_ctx.config = (struct mod_scmi_config *)config_scmi.data;
     scmi_ctx.protocol_table = fwk_mm_calloc(
-        scmi_ctx.config->protocol_count_max +
-            PROTOCOL_TABLE_RESERVED_ENTRIES_COUNT,
-        sizeof(scmi_ctx.protocol_table[0]));
+	scmi_ctx.config->protocol_count_max +
+	    PROTOCOL_TABLE_RESERVED_ENTRIES_COUNT,
+	sizeof(scmi_ctx.protocol_table[0]));
     scmi_ctx.protocol_table[PROTOCOL_TABLE_BASE_PROTOCOL_IDX].message_handler =
-        scmi_base_message_handler;
+	scmi_base_message_handler;
     scmi_ctx.service_ctx_table = fwk_mm_calloc(
-        FAKE_SERVICE_IDX_COUNT, sizeof(scmi_ctx.service_ctx_table[0]));
+	FAKE_SERVICE_IDX_COUNT, sizeof(scmi_ctx.service_ctx_table[0]));
     scmi_ctx.scmi_protocol_id_to_idx[MOD_SCMI_PROTOCOL_ID_BASE] =
-        PROTOCOL_TABLE_BASE_PROTOCOL_IDX;
+	PROTOCOL_TABLE_BASE_PROTOCOL_IDX;
 
     scmi_base_set_shared_ctx(&scmi_ctx);
     scmi_base_set_api(&from_protocol_api);
 
     ctx = &scmi_ctx.service_ctx_table[FAKE_SERVICE_IDX_PSCI];
     ctx->config =
-        (struct mod_scmi_service_config *)element_table[FAKE_SERVICE_IDX_PSCI]
-            .data;
+	(struct mod_scmi_service_config *)element_table[FAKE_SERVICE_IDX_PSCI]
+	    .data;
     ctx->transport_api = transport_api;
     ctx->transport_id = ctx->config->transport_id;
     ctx->respond = transport_api->respond;
@@ -157,8 +157,8 @@ void setUp(void)
 
     ctx = &scmi_ctx.service_ctx_table[FAKE_SERVICE_IDX_OSPM];
     ctx->config =
-        (struct mod_scmi_service_config *)element_table[FAKE_SERVICE_IDX_OSPM]
-            .data;
+	(struct mod_scmi_service_config *)element_table[FAKE_SERVICE_IDX_OSPM]
+	    .data;
     ctx->transport_api = transport_api;
     ctx->transport_id = ctx->config->transport_id;
     ctx->respond = transport_api->respond;
@@ -174,7 +174,7 @@ void test_function_get_max_payload_size_
     int status;
 
     fwk_id_t service_id =
-        FWK_ID_ELEMENT_INIT(FAKE_MODULE_ID, FAKE_SERVICE_IDX_OSPM);
+	FWK_ID_ELEMENT_INIT(FAKE_MODULE_ID, FAKE_SERVICE_IDX_OSPM);
 
     status = get_max_payload_size(service_id, NULL);
     TEST_ASSERT_EQUAL(status, FWK_E_PARAM);
@@ -186,13 +186,13 @@ void test_function_get_max_payload_size_
     size_t size;
 
     fwk_id_t service_id =
-        FWK_ID_ELEMENT_INIT(FAKE_MODULE_ID, FAKE_SERVICE_IDX_OSPM);
+	FWK_ID_ELEMENT_INIT(FAKE_MODULE_ID, FAKE_SERVICE_IDX_OSPM);
 #if !defined(TEST_ON_TARGET)
     fwk_id_get_element_idx_ExpectAndReturn(service_id, FAKE_SERVICE_IDX_OSPM);
 #endif
 
     mod_scmi_to_transport_api_get_max_payload_size_ExpectAnyArgsAndReturn(
-        FWK_SUCCESS);
+	FWK_SUCCESS);
 
     status = get_max_payload_size(service_id, &size);
 
@@ -202,7 +202,7 @@ void test_function_get_max_payload_size_
 void test_function_scmi_base_discover_sub_vendor_handler(void)
 {
     fwk_id_t service_id =
-        FWK_ID_ELEMENT_INIT(FAKE_MODULE_ID, FAKE_SERVICE_IDX_OSPM);
+	FWK_ID_ELEMENT_INIT(FAKE_MODULE_ID, FAKE_SERVICE_IDX_OSPM);
 
 #if !defined(TEST_ON_TARGET)
     fwk_id_get_element_idx_ExpectAndReturn(service_id, FAKE_SERVICE_IDX_OSPM);
@@ -212,7 +212,7 @@ void test_function_scmi_base_discover_su
     mod_scmi_from_protocol_respond_ExpectAnyArgsAndReturn(FWK_SUCCESS);
 
     TEST_ASSERT_EQUAL(
-        FWK_SUCCESS, scmi_base_discover_sub_vendor_handler(service_id, NULL));
+	FWK_SUCCESS, scmi_base_discover_sub_vendor_handler(service_id, NULL));
 }
 
 int scmi_test_main(void)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_apcore/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi_apcore/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_apcore/CMakeLists.txt
@@ -8,7 +8,7 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(
     ${SCP_MODULE_TARGET}
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_apcore/doc/scmi_apcore.md
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi_apcore/doc/scmi_apcore.md
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_apcore/doc/scmi_apcore.md
@@ -121,9 +121,9 @@ Return values:
 * int32 status
     * SUCCESS if the reset address was set successfully.
     * INVALID_PARAMETERS:
-        * Reset address alignment is invalid.
-        * Platform supports only 32-bit addresses and the reset address received
-          is larger than 32-bits.
+	* Reset address alignment is invalid.
+	* Platform supports only 32-bit addresses and the reset address received
+	  is larger than 32-bits.
     * DENIED: The reset address is locked and changes are not permitted.
     * DENIED: The calling agent is not permitted to modify the reset address.
     * See section 4.1.4 of the SCMI specification for status code
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_apcore/include/mod_scmi_apcore.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi_apcore/include/mod_scmi_apcore.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_apcore/include/mod_scmi_apcore.h
@@ -74,7 +74,7 @@ struct mod_scmi_apcore_config {
      *     structures that define the reset registers within the platform.
      */
     const struct mod_scmi_apcore_reset_register_group
-        *reset_register_group_table;
+	*reset_register_group_table;
 
     /*!
      * \brief Number of ::mod_scmi_apcore_reset_register_group structures in
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_apcore/src/mod_scmi_apcore.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi_apcore/src/mod_scmi_apcore.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_apcore/src/mod_scmi_apcore.c
@@ -59,7 +59,7 @@ static int (*const handler_table[])(fwk_
     [MOD_SCMI_PROTOCOL_VERSION] = scmi_apcore_protocol_version_handler,
     [MOD_SCMI_PROTOCOL_ATTRIBUTES] = scmi_apcore_protocol_attributes_handler,
     [MOD_SCMI_PROTOCOL_MESSAGE_ATTRIBUTES] =
-        scmi_apcore_protocol_message_attributes_handler,
+	scmi_apcore_protocol_message_attributes_handler,
     [MOD_SCMI_APCORE_RESET_ADDRESS_SET] = scmi_apcore_reset_address_set_handler,
     [MOD_SCMI_APCORE_RESET_ADDRESS_GET] = scmi_apcore_reset_address_get_handler,
 };
@@ -68,9 +68,9 @@ static const unsigned int payload_size_t
     [MOD_SCMI_PROTOCOL_VERSION] = 0,
     [MOD_SCMI_PROTOCOL_ATTRIBUTES] = 0,
     [MOD_SCMI_PROTOCOL_MESSAGE_ATTRIBUTES] =
-        (unsigned int)sizeof(struct scmi_protocol_message_attributes_a2p),
+	(unsigned int)sizeof(struct scmi_protocol_message_attributes_a2p),
     [MOD_SCMI_APCORE_RESET_ADDRESS_SET] =
-        (unsigned int)sizeof(struct scmi_apcore_reset_address_set_a2p),
+	(unsigned int)sizeof(struct scmi_apcore_reset_address_set_a2p),
     [MOD_SCMI_APCORE_RESET_ADDRESS_GET] = 0,
 };
 
@@ -89,29 +89,29 @@ static int set_reset_address(uint32_t ad
 
     /* Iterate over the reset register group structures */
     for (grp_idx = 0;
-         grp_idx < scmi_apcore_ctx.config->reset_register_group_count;
-         grp_idx++) {
+	 grp_idx < scmi_apcore_ctx.config->reset_register_group_count;
+	 grp_idx++) {
 
-        reg_group =
-            &scmi_apcore_ctx.config->reset_register_group_table[grp_idx];
-        fwk_assert(reg_group->base_register != 0);
-
-        /* Begin with the first register in the group */
-        reset_reg = reg_group->base_register;
-
-        /* Program each reset vector register within the group */
-        for (reg_idx = 0; reg_idx < reg_group->register_count; reg_idx++) {
-            if (scmi_apcore_ctx.config->reset_register_width ==
-                MOD_SCMI_APCORE_REG_WIDTH_32) {
-                /* Treat the register as 32-bit */
-                *(uint32_t *)reset_reg = address_low;
-                reset_reg += sizeof(uint32_t);
-            } else {
-                /* Treat the register as 64-bit */
-                *(uint64_t *)reset_reg = address_composite;
-                reset_reg += sizeof(uint64_t);
-            }
-        }
+	reg_group =
+	    &scmi_apcore_ctx.config->reset_register_group_table[grp_idx];
+	fwk_assert(reg_group->base_register != 0);
+
+	/* Begin with the first register in the group */
+	reset_reg = reg_group->base_register;
+
+	/* Program each reset vector register within the group */
+	for (reg_idx = 0; reg_idx < reg_group->register_count; reg_idx++) {
+	    if (scmi_apcore_ctx.config->reset_register_width ==
+		MOD_SCMI_APCORE_REG_WIDTH_32) {
+		/* Treat the register as 32-bit */
+		*(uint32_t *)reset_reg = address_low;
+		reset_reg += sizeof(uint32_t);
+	    } else {
+		/* Treat the register as 64-bit */
+		*(uint64_t *)reset_reg = address_composite;
+		reset_reg += sizeof(uint64_t);
+	    }
+	}
     }
 
     return FWK_SUCCESS;
@@ -124,12 +124,12 @@ static int scmi_apcore_protocol_version_
     const uint32_t *payload)
 {
     struct scmi_protocol_version_p2a return_values = {
-        .status = (int32_t)SCMI_SUCCESS,
-        .version = MOD_SCMI_PROTOCOL_VERSION_APCORE,
+	.status = (int32_t)SCMI_SUCCESS,
+	.version = MOD_SCMI_PROTOCOL_VERSION_APCORE,
     };
 
     return scmi_apcore_ctx.scmi_api->respond(
-        service_id, &return_values, sizeof(return_values));
+	service_id, &return_values, sizeof(return_values));
 }
 
 /*
@@ -139,18 +139,18 @@ static int scmi_apcore_protocol_attribut
     const uint32_t *payload)
 {
     struct scmi_protocol_attributes_p2a return_values = {
-        .status = (int32_t)SCMI_SUCCESS,
-        .attributes = 0,
+	.status = (int32_t)SCMI_SUCCESS,
+	.attributes = 0,
     };
 
     if (scmi_apcore_ctx.config->reset_register_width ==
-        MOD_SCMI_APCORE_REG_WIDTH_64) {
-        return_values.attributes |=
-            MOD_SCMI_APCORE_PROTOCOL_ATTRIBUTES_64BIT_MASK;
+	MOD_SCMI_APCORE_REG_WIDTH_64) {
+	return_values.attributes |=
+	    MOD_SCMI_APCORE_PROTOCOL_ATTRIBUTES_64BIT_MASK;
     }
 
     return scmi_apcore_ctx.scmi_api->respond(
-        service_id, &return_values, sizeof(return_values));
+	service_id, &return_values, sizeof(return_values));
 }
 
 /*
@@ -163,24 +163,24 @@ static int scmi_apcore_protocol_message_
     const struct scmi_protocol_message_attributes_a2p *parameters;
     unsigned int message_id;
     struct scmi_protocol_message_attributes_p2a return_values = {
-        .status = (int32_t)SCMI_SUCCESS,
-        .attributes = 0,
+	.status = (int32_t)SCMI_SUCCESS,
+	.attributes = 0,
     };
 
     parameters = (const struct scmi_protocol_message_attributes_a2p *)
-        payload;
+	payload;
     message_id = parameters->message_id;
 
     if ((message_id >= FWK_ARRAY_SIZE(handler_table)) ||
-        (handler_table[message_id] == NULL)) {
-        return_values.status = (int32_t)SCMI_NOT_FOUND;
+	(handler_table[message_id] == NULL)) {
+	return_values.status = (int32_t)SCMI_NOT_FOUND;
     }
 
     response_size = (return_values.status == SCMI_SUCCESS) ?
-        sizeof(return_values) : sizeof(return_values.status);
+	sizeof(return_values) : sizeof(return_values.status);
 
     return scmi_apcore_ctx.scmi_api->respond(
-        service_id, &return_values, response_size);
+	service_id, &return_values, response_size);
 }
 
 /*
@@ -194,31 +194,31 @@ static int scmi_apcore_reset_address_set
     enum scmi_agent_type agent_type;
     const struct scmi_apcore_reset_address_set_a2p *parameters;
     struct scmi_apcore_reset_address_set_p2a return_values = {
-        .status = (int32_t)SCMI_GENERIC_ERROR
+	.status = (int32_t)SCMI_GENERIC_ERROR
     };
 
     parameters = (const struct scmi_apcore_reset_address_set_a2p *)payload;
 
     status = scmi_apcore_ctx.scmi_api->get_agent_id(service_id, &agent_id);
     if (status != FWK_SUCCESS) {
-        goto exit;
+	goto exit;
     }
 
     status = scmi_apcore_ctx.scmi_api->get_agent_type(agent_id, &agent_type);
     if (status != FWK_SUCCESS) {
-        goto exit;
+	goto exit;
     }
 
     /* Only the PSCI agent may set the reset address */
     if (agent_type != SCMI_AGENT_TYPE_PSCI) {
-        return_values.status = (int32_t)SCMI_DENIED;
-        goto exit;
+	return_values.status = (int32_t)SCMI_DENIED;
+	goto exit;
     }
 
     /* An agent previously requested that the configuration be locked */
     if (scmi_apcore_ctx.locked) {
-        return_values.status = (int32_t)SCMI_DENIED;
-        goto exit;
+	return_values.status = (int32_t)SCMI_DENIED;
+	goto exit;
     }
 
     /*
@@ -226,34 +226,34 @@ static int scmi_apcore_reset_address_set
      * address utilizing more that 32 bits has been provided.
      */
     if ((parameters->reset_address_high != 0) &&
-        (scmi_apcore_ctx.config->reset_register_width ==
-         MOD_SCMI_APCORE_REG_WIDTH_32)) {
-        return_values.status = (int32_t)SCMI_INVALID_PARAMETERS;
-        goto exit;
+	(scmi_apcore_ctx.config->reset_register_width ==
+	 MOD_SCMI_APCORE_REG_WIDTH_32)) {
+	return_values.status = (int32_t)SCMI_INVALID_PARAMETERS;
+	goto exit;
     }
 
     /* Check for alignment */
     if ((parameters->reset_address_low % 4) != 0) {
-        return_values.status = (int32_t)SCMI_INVALID_PARAMETERS;
-        goto exit;
+	return_values.status = (int32_t)SCMI_INVALID_PARAMETERS;
+	goto exit;
     }
 
     status = set_reset_address(
-        parameters->reset_address_low, parameters->reset_address_high);
+	parameters->reset_address_low, parameters->reset_address_high);
     if (status != FWK_SUCCESS) {
-        goto exit;
+	goto exit;
     }
 
     return_values.status = (int32_t)SCMI_SUCCESS;
 
     /* Lock the configuration if requested */
     if (parameters->attributes & MOD_SCMI_APCORE_RESET_ADDRESS_SET_LOCK_MASK) {
-        scmi_apcore_ctx.locked = true;
+	scmi_apcore_ctx.locked = true;
     }
 
 exit:
     return scmi_apcore_ctx.scmi_api->respond(
-        service_id, &return_values, sizeof(return_values));
+	service_id, &return_values, sizeof(return_values));
 }
 
 /*
@@ -268,50 +268,50 @@ static int scmi_apcore_reset_address_get
     uint64_t reset_address;
     enum scmi_agent_type agent_type;
     struct scmi_apcore_reset_address_get_p2a return_values = {
-        .status = (int32_t)SCMI_GENERIC_ERROR
+	.status = (int32_t)SCMI_GENERIC_ERROR
     };
 
     status = scmi_apcore_ctx.scmi_api->get_agent_id(service_id, &agent_id);
     if (status != FWK_SUCCESS) {
-        goto exit;
+	goto exit;
     }
 
     status = scmi_apcore_ctx.scmi_api->get_agent_type(agent_id, &agent_type);
     if (status != FWK_SUCCESS) {
-        goto exit;
+	goto exit;
     }
 
     /* Only the PSCI agent may get the current reset address */
     if (agent_type != SCMI_AGENT_TYPE_PSCI) {
-        return_values.status = (int32_t)SCMI_DENIED;
-        goto exit;
+	return_values.status = (int32_t)SCMI_DENIED;
+	goto exit;
     }
 
     /* The reset address is common across all reset address registers */
     reg_group = &scmi_apcore_ctx.config->reset_register_group_table[0];
 
     if (scmi_apcore_ctx.config->reset_register_width ==
-        MOD_SCMI_APCORE_REG_WIDTH_32) {
-        reset_address = *(uint32_t *)reg_group->base_register;
-        return_values.reset_address_high = 0;
+	MOD_SCMI_APCORE_REG_WIDTH_32) {
+	reset_address = *(uint32_t *)reg_group->base_register;
+	return_values.reset_address_high = 0;
     } else {
-        reset_address = *(uint64_t *)reg_group->base_register;
-        return_values.reset_address_high =
-            (uint32_t)((reset_address >> 32) & UINT32_MAX);
+	reset_address = *(uint64_t *)reg_group->base_register;
+	return_values.reset_address_high =
+	    (uint32_t)((reset_address >> 32) & UINT32_MAX);
     }
 
     return_values.reset_address_low = (uint32_t)reset_address;
 
     return_values.attributes |=
-        (scmi_apcore_ctx.locked << MOD_SCMI_APCORE_RESET_ADDRESS_GET_LOCK_POS);
+	(scmi_apcore_ctx.locked << MOD_SCMI_APCORE_RESET_ADDRESS_GET_LOCK_POS);
     return_values.status = (int32_t)SCMI_SUCCESS;
 
 exit:
     respond_status = scmi_apcore_ctx.scmi_api->respond(
-        service_id, &return_values, sizeof(return_values));
+	service_id, &return_values, sizeof(return_values));
 
     if (respond_status != FWK_SUCCESS) {
-        FWK_LOG_DEBUG("[SCMI-APCORE] %s @%d", __func__, __LINE__);
+	FWK_LOG_DEBUG("[SCMI-APCORE] %s @%d", __func__, __LINE__);
     }
 
     return status;
@@ -338,25 +338,25 @@ static int scmi_apcore_message_handler(
     int32_t return_value;
 
     static_assert(FWK_ARRAY_SIZE(handler_table) ==
-        FWK_ARRAY_SIZE(payload_size_table),
-        "[SCMI] Core configuration protocol table sizes not consistent");
+	FWK_ARRAY_SIZE(payload_size_table),
+	"[SCMI] Core configuration protocol table sizes not consistent");
     fwk_assert(payload != NULL);
 
     if (message_id >= FWK_ARRAY_SIZE(handler_table)) {
-        return_value = (int32_t)SCMI_NOT_FOUND;
-        goto error;
+	return_value = (int32_t)SCMI_NOT_FOUND;
+	goto error;
     }
 
     if (payload_size != payload_size_table[message_id]) {
-        return_value = (int32_t)SCMI_PROTOCOL_ERROR;
-        goto error;
+	return_value = (int32_t)SCMI_PROTOCOL_ERROR;
+	goto error;
     }
 
     return handler_table[message_id](service_id, payload);
 
 error:
     return scmi_apcore_ctx.scmi_api->respond(
-        service_id, &return_value, sizeof(return_value));
+	service_id, &return_value, sizeof(return_value));
 }
 
 static struct mod_scmi_to_protocol_api scmi_apcore_mod_scmi_to_protocol_api = {
@@ -369,22 +369,22 @@ static struct mod_scmi_to_protocol_api s
  */
 
 static int scmi_apcore_init(fwk_id_t module_id, unsigned int element_count,
-                           const void *data)
+			   const void *data)
 {
     const struct mod_scmi_apcore_config *config =
-        (const struct mod_scmi_apcore_config *)data;
+	(const struct mod_scmi_apcore_config *)data;
 
     if (config == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
     if (config->reset_register_group_table == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
     if (config->reset_register_group_count == 0) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
     if (config->reset_register_width >= MOD_SCMI_APCORE_REG_WIDTH_COUNT) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     scmi_apcore_ctx.config = config;
@@ -395,13 +395,13 @@ static int scmi_apcore_init(fwk_id_t mod
 static int scmi_apcore_bind(fwk_id_t id, unsigned int round)
 {
     if (round == 1) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     /* Bind to the SCMI module, storing an API pointer for later use. */
     return fwk_module_bind(FWK_ID_MODULE(FWK_MODULE_IDX_SCMI),
-        FWK_ID_API(FWK_MODULE_IDX_SCMI, MOD_SCMI_API_IDX_PROTOCOL),
-        &scmi_apcore_ctx.scmi_api);
+	FWK_ID_API(FWK_MODULE_IDX_SCMI, MOD_SCMI_API_IDX_PROTOCOL),
+	&scmi_apcore_ctx.scmi_api);
 }
 
 static int scmi_apcore_process_bind_request(fwk_id_t source_id,
@@ -409,7 +409,7 @@ static int scmi_apcore_process_bind_requ
 {
     /* Only accept binding requests from the SCMI module. */
     if (!fwk_id_is_equal(source_id, FWK_ID_MODULE(FWK_MODULE_IDX_SCMI))) {
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 
     *api = &scmi_apcore_mod_scmi_to_protocol_api;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_clock/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi_clock/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_clock/CMakeLists.txt
@@ -8,10 +8,10 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_scmi_clock.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_scmi_clock.c")
 
 target_link_libraries(
     module-scmi-clock
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_clock/include/internal/scmi_clock.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi_clock/include/internal/scmi_clock.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_clock/include/internal/scmi_clock.h
@@ -90,11 +90,11 @@ struct scmi_clock_generic_p2a {
 
 #define SCMI_CLOCK_PROTOCOL_ATTRIBUTES(MAX_PENDING_TRANSACTIONS, CLOCK_COUNT) \
     ( \
-        ((MAX_PENDING_TRANSACTIONS << \
-            SCMI_CLOCK_PROTOCOL_ATTRIBUTES_MAX_PENDING_TRANSITIONS_POS) & \
-            SCMI_CLOCK_PROTOCOL_ATTRIBUTES_MAX_PENDING_TRANSITIONS_MASK) | \
-        (((CLOCK_COUNT) << SCMI_CLOCK_PROTOCOL_ATTRIBUTES_CLOCK_COUNT_POS) & \
-            SCMI_CLOCK_PROTOCOL_ATTRIBUTES_CLOCK_COUNT_MASK) \
+	((MAX_PENDING_TRANSACTIONS << \
+	    SCMI_CLOCK_PROTOCOL_ATTRIBUTES_MAX_PENDING_TRANSITIONS_POS) & \
+	    SCMI_CLOCK_PROTOCOL_ATTRIBUTES_MAX_PENDING_TRANSITIONS_MASK) | \
+	(((CLOCK_COUNT) << SCMI_CLOCK_PROTOCOL_ATTRIBUTES_CLOCK_COUNT_POS) & \
+	    SCMI_CLOCK_PROTOCOL_ATTRIBUTES_CLOCK_COUNT_MASK) \
     )
 
 /*
@@ -108,8 +108,8 @@ struct scmi_clock_generic_p2a {
 
 #define SCMI_CLOCK_ATTRIBUTES(ENABLED) \
     ( \
-        (((ENABLED) << SCMI_CLOCK_ATTRIBUTES_ENABLED_POS) & \
-            SCMI_CLOCK_ATTRIBUTES_ENABLED_MASK) \
+	(((ENABLED) << SCMI_CLOCK_ATTRIBUTES_ENABLED_POS) & \
+	    SCMI_CLOCK_ATTRIBUTES_ENABLED_MASK) \
     )
 
 struct scmi_clock_attributes_a2p {
@@ -207,13 +207,13 @@ struct scmi_clock_config_set_p2a {
 #define SCMI_CLOCK_DESCRIBE_RATES_NUM_RATES_FLAGS( \
     RATE_COUNT, RETURN_FORMAT, REMAINING_RATES) \
     ( \
-        ((RATE_COUNT << \
-            SCMI_CLOCK_DESCRIBE_RATES_COUNT_POS) & \
-            SCMI_CLOCK_DESCRIBE_RATES_COUNT_MASK) | \
-        ((REMAINING_RATES << SCMI_CLOCK_DESCRIBE_RATES_REMAINING_POS) & \
-            SCMI_CLOCK_DESCRIBE_RATES_REMAINING_MASK) | \
-        ((RETURN_FORMAT << SCMI_CLOCK_DESCRIBE_RATES_FORMAT_POS) & \
-            SCMI_CLOCK_DESCRIBE_RATES_FORMAT_MASK) \
+	((RATE_COUNT << \
+	    SCMI_CLOCK_DESCRIBE_RATES_COUNT_POS) & \
+	    SCMI_CLOCK_DESCRIBE_RATES_COUNT_MASK) | \
+	((REMAINING_RATES << SCMI_CLOCK_DESCRIBE_RATES_REMAINING_POS) & \
+	    SCMI_CLOCK_DESCRIBE_RATES_REMAINING_MASK) | \
+	((RETURN_FORMAT << SCMI_CLOCK_DESCRIBE_RATES_FORMAT_POS) & \
+	    SCMI_CLOCK_DESCRIBE_RATES_FORMAT_MASK) \
     )
 
 #define SCMI_CLOCK_DESCRIBE_RATES_REMAINING_MASK \
@@ -226,7 +226,7 @@ struct scmi_clock_config_set_p2a {
 #define SCMI_CLOCK_RATES_MAX(MAILBOX_SIZE) \
     ((sizeof(struct scmi_clock_describe_rates_p2a) < (MAILBOX_SIZE))  ? \
      (((MAILBOX_SIZE) - sizeof(struct scmi_clock_describe_rates_p2a))   \
-        / sizeof(struct scmi_clock_rate)) : 0)
+	/ sizeof(struct scmi_clock_rate)) : 0)
 
 struct scmi_clock_rate {
     uint32_t low;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_clock/src/mod_scmi_clock.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi_clock/src/mod_scmi_clock.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_clock/src/mod_scmi_clock.c
@@ -95,19 +95,19 @@ struct mod_scmi_clock_ctx {
 
 static const fwk_id_t mod_scmi_clock_event_id_get_state =
     FWK_ID_EVENT_INIT(FWK_MODULE_IDX_SCMI_CLOCK,
-                      SCMI_CLOCK_EVENT_IDX_GET_STATE);
+		      SCMI_CLOCK_EVENT_IDX_GET_STATE);
 
 static const fwk_id_t mod_scmi_clock_event_id_get_rate =
     FWK_ID_EVENT_INIT(FWK_MODULE_IDX_SCMI_CLOCK,
-                      SCMI_CLOCK_EVENT_IDX_GET_RATE);
+		      SCMI_CLOCK_EVENT_IDX_GET_RATE);
 
 static const fwk_id_t mod_scmi_clock_event_id_set_rate =
     FWK_ID_EVENT_INIT(FWK_MODULE_IDX_SCMI_CLOCK,
-                      SCMI_CLOCK_EVENT_IDX_SET_RATE);
+		      SCMI_CLOCK_EVENT_IDX_SET_RATE);
 
 static const fwk_id_t mod_scmi_clock_event_id_set_state =
     FWK_ID_EVENT_INIT(FWK_MODULE_IDX_SCMI_CLOCK,
-                      SCMI_CLOCK_EVENT_IDX_SET_STATE);
+		      SCMI_CLOCK_EVENT_IDX_SET_STATE);
 
 /*
  * SCMI Clock Message Handlers
@@ -150,7 +150,7 @@ static int (*const handler_table[MOD_SCM
     [MOD_SCMI_PROTOCOL_VERSION] = scmi_clock_protocol_version_handler,
     [MOD_SCMI_PROTOCOL_ATTRIBUTES] = scmi_clock_protocol_attributes_handler,
     [MOD_SCMI_PROTOCOL_MESSAGE_ATTRIBUTES] =
-        scmi_clock_protocol_message_attributes_handler,
+	scmi_clock_protocol_message_attributes_handler,
     [MOD_SCMI_CLOCK_ATTRIBUTES] = scmi_clock_attributes_handler,
     [MOD_SCMI_CLOCK_RATE_GET] = scmi_clock_rate_get_handler,
     [MOD_SCMI_CLOCK_RATE_SET] = scmi_clock_rate_set_handler,
@@ -167,27 +167,27 @@ static const unsigned int payload_size_t
     [MOD_SCMI_PROTOCOL_VERSION] = 0,
     [MOD_SCMI_PROTOCOL_ATTRIBUTES] = 0,
     [MOD_SCMI_PROTOCOL_MESSAGE_ATTRIBUTES] =
-        (unsigned int)sizeof(struct scmi_protocol_message_attributes_a2p),
+	(unsigned int)sizeof(struct scmi_protocol_message_attributes_a2p),
     [MOD_SCMI_CLOCK_ATTRIBUTES] =
-        (unsigned int)sizeof(struct scmi_clock_attributes_a2p),
+	(unsigned int)sizeof(struct scmi_clock_attributes_a2p),
     [MOD_SCMI_CLOCK_RATE_GET] =
-        (unsigned int)sizeof(struct scmi_clock_rate_get_a2p),
+	(unsigned int)sizeof(struct scmi_clock_rate_get_a2p),
     [MOD_SCMI_CLOCK_RATE_SET] =
-        (unsigned int)sizeof(struct scmi_clock_rate_set_a2p),
+	(unsigned int)sizeof(struct scmi_clock_rate_set_a2p),
     [MOD_SCMI_CLOCK_CONFIG_SET] =
-        (unsigned int)sizeof(struct scmi_clock_config_set_a2p),
+	(unsigned int)sizeof(struct scmi_clock_config_set_a2p),
     [MOD_SCMI_CLOCK_DESCRIBE_RATES] =
-        (unsigned int)sizeof(struct scmi_clock_describe_rates_a2p),
+	(unsigned int)sizeof(struct scmi_clock_describe_rates_a2p),
     [MOD_SCMI_CLOCK_NAME_GET] =
-        (unsigned int)sizeof(struct scmi_clock_describe_rates_a2p),
+	(unsigned int)sizeof(struct scmi_clock_describe_rates_a2p),
     [MOD_SCMI_CLOCK_RATE_NOTIFY] =
-        (unsigned int)sizeof(struct scmi_clock_rate_notify_a2p),
+	(unsigned int)sizeof(struct scmi_clock_rate_notify_a2p),
     [MOD_SCMI_CLOCK_RATE_CHANGE_REQUESTED_NOTIFY] =
-        (unsigned int)sizeof(struct scmi_clock_rate_change_request_notify_a2p),
+	(unsigned int)sizeof(struct scmi_clock_rate_change_request_notify_a2p),
     [MOD_SCMI_CLOCK_DUTY_CYCLE_GET] =
-        (unsigned int)sizeof(struct scmi_clock_duty_cycle_get_a2p),
+	(unsigned int)sizeof(struct scmi_clock_duty_cycle_get_a2p),
     [MOD_SCMI_CLOCK_ROUND_RATE_GET] =
-        (unsigned int)sizeof(struct scmi_clock_round_rate_get_a2p),
+	(unsigned int)sizeof(struct scmi_clock_round_rate_get_a2p),
 };
 
 /*
@@ -203,15 +203,15 @@ static int scmi_clock_get_agent_entry(
 
     status = scmi_clock_ctx.scmi_api->get_agent_id(service_id, &agent_id);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     if (agent_id >= scmi_clock_ctx.config->agent_count) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     *agent =
-        (struct mod_scmi_clock_agent *)&scmi_clock_ctx.agent_table[agent_id];
+	(struct mod_scmi_clock_agent *)&scmi_clock_ctx.agent_table[agent_id];
 
     return FWK_SUCCESS;
 }
@@ -230,11 +230,11 @@ static int scmi_clock_get_clock_device_e
 
     status = scmi_clock_get_agent_entry(service_id, &agent_entry);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     if (scmi_clock_idx >= agent_entry->device_count) {
-        return FWK_E_RANGE;
+	return FWK_E_RANGE;
     }
 
     *clock_device = &agent_entry->device_table[scmi_clock_idx];
@@ -255,8 +255,8 @@ static inline unsigned int scmi_clock_ge
     fwk_id_t dev_clock_id;
 
     dev_clock_id = scmi_clock_ctx.agent_table[agent_id]
-                       .device_table[scmi_clock_idx]
-                       .element_id;
+		       .device_table[scmi_clock_idx]
+		       .element_id;
 
     return fwk_id_get_element_idx(dev_clock_id);
 }
@@ -300,15 +300,15 @@ static int scmi_clock_ref_count_table_up
 {
     switch (requested_state) {
     case MOD_CLOCK_STATE_RUNNING:
-        scmi_clock_ctx.dev_clock_ref_count_table[dev_clock_idx]++;
-        break;
+	scmi_clock_ctx.dev_clock_ref_count_table[dev_clock_idx]++;
+	break;
 
     case MOD_CLOCK_STATE_STOPPED:
-        scmi_clock_ctx.dev_clock_ref_count_table[dev_clock_idx]--;
-        break;
+	scmi_clock_ctx.dev_clock_ref_count_table[dev_clock_idx]--;
+	break;
 
     default:
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     };
 
     return FWK_SUCCESS;
@@ -325,43 +325,43 @@ static enum mod_scmi_clock_policy_status
 {
     switch (requested_state) {
     case MOD_CLOCK_STATE_RUNNING:
-        /*
-         * Only allow the clock to be running if dev_clock_ref_count_table == 0
-         * before being updated for this call.
-         *
-         * This is the first agent to set the clock RUNNING.
-         */
-        if (scmi_clock_ctx.dev_clock_ref_count_table[dev_clock_idx] == 0) {
-            return MOD_SCMI_CLOCK_EXECUTE_MESSAGE_HANDLER;
-        }
+	/*
+	 * Only allow the clock to be running if dev_clock_ref_count_table == 0
+	 * before being updated for this call.
+	 *
+	 * This is the first agent to set the clock RUNNING.
+	 */
+	if (scmi_clock_ctx.dev_clock_ref_count_table[dev_clock_idx] == 0) {
+	    return MOD_SCMI_CLOCK_EXECUTE_MESSAGE_HANDLER;
+	}
 
-        break;
+	break;
 
     case MOD_CLOCK_STATE_STOPPED:
-        if (scmi_clock_ctx.dev_clock_ref_count_table[dev_clock_idx] == 0) {
-            /* Error trying to stop a stopped clock */
-            FWK_LOG_WARN(
-                "[SCMI-CLK] Invalid STOP request agent:"
-                " %d scmi_clock_id: %d state:%d\n",
-                (int)agent_id,
-                (int)scmi_clock_idx,
-                (int)requested_state);
-            return MOD_SCMI_CLOCK_SKIP_MESSAGE_HANDLER;
-        }
-
-        /*
-         * Only allow the clock to be stopped if dev_clock_ref_count_table == 1
-         * after being updated for this call.
-         *
-         * This is the last agent to set the clock STOPPED.
-         */
-        if (scmi_clock_ctx.dev_clock_ref_count_table[dev_clock_idx] == 1) {
-            return MOD_SCMI_CLOCK_EXECUTE_MESSAGE_HANDLER;
-        }
-        break;
+	if (scmi_clock_ctx.dev_clock_ref_count_table[dev_clock_idx] == 0) {
+	    /* Error trying to stop a stopped clock */
+	    FWK_LOG_WARN(
+		"[SCMI-CLK] Invalid STOP request agent:"
+		" %d scmi_clock_id: %d state:%d\n",
+		(int)agent_id,
+		(int)scmi_clock_idx,
+		(int)requested_state);
+	    return MOD_SCMI_CLOCK_SKIP_MESSAGE_HANDLER;
+	}
+
+	/*
+	 * Only allow the clock to be stopped if dev_clock_ref_count_table == 1
+	 * after being updated for this call.
+	 *
+	 * This is the last agent to set the clock STOPPED.
+	 */
+	if (scmi_clock_ctx.dev_clock_ref_count_table[dev_clock_idx] == 1) {
+	    return MOD_SCMI_CLOCK_EXECUTE_MESSAGE_HANDLER;
+	}
+	break;
 
     default:
-        break;
+	break;
     }
 
     return MOD_SCMI_CLOCK_SKIP_MESSAGE_HANDLER;
@@ -383,11 +383,11 @@ static int mod_scmi_clock_state_update(
 
     /* The agent has already requested to set state to the current state */
     if (requested_state == clock_state) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     scmi_clock_set_agent_clock_state(
-        agent_id, scmi_clock_idx, (uint8_t)requested_state);
+	agent_id, scmi_clock_idx, (uint8_t)requested_state);
 
     return scmi_clock_ref_count_table_update(dev_clock_idx, requested_state);
 }
@@ -407,11 +407,11 @@ static enum mod_scmi_clock_policy_status
 
     /* The agent has already requested to set state to the current state */
     if (requested_state == clock_state) {
-        return MOD_SCMI_CLOCK_SKIP_MESSAGE_HANDLER;
+	return MOD_SCMI_CLOCK_SKIP_MESSAGE_HANDLER;
     }
 
     return scmi_clock_ref_count_table_check(
-        agent_id, scmi_clock_idx, dev_clock_idx, requested_state);
+	agent_id, scmi_clock_idx, dev_clock_idx, requested_state);
 }
 
 static void clock_ref_count_allocate(void)
@@ -423,18 +423,18 @@ static void clock_ref_count_allocate(voi
 
     /* Allocate table of agent:clock_states */
     scmi_clock_ctx.agent_clock_state_table = fwk_mm_calloc(
-        agent_count * clock_devices,
-        sizeof(*scmi_clock_ctx.agent_clock_state_table));
+	agent_count * clock_devices,
+	sizeof(*scmi_clock_ctx.agent_clock_state_table));
 
     /* Allocate table of clock reference counts */
     scmi_clock_ctx.dev_clock_ref_count_table = fwk_mm_calloc(
-        clock_devices, sizeof(*scmi_clock_ctx.dev_clock_ref_count_table));
+	clock_devices, sizeof(*scmi_clock_ctx.dev_clock_ref_count_table));
 }
 
 static void clock_ref_count_init(void)
 {
     unsigned int agent_id, ref_count_table_idx, clock_idx, agent_count,
-        clock_devices;
+	clock_devices;
     const struct mod_scmi_clock_agent *agent;
 
     agent_count = scmi_clock_ctx.config->agent_count;
@@ -442,19 +442,19 @@ static void clock_ref_count_init(void)
 
     /* Set all clocks in RUNNING state if provided by the configuration */
     for (agent_id = 0; agent_id < agent_count; agent_id++) {
-        agent = &scmi_clock_ctx.agent_table[agent_id];
-        for (clock_idx = 0; clock_idx < (unsigned int)agent->device_count;
-             clock_idx++) {
-            if (agent->device_table[clock_idx].starts_enabled) {
-                ref_count_table_idx = (agent_id * clock_devices) + clock_idx;
-                scmi_clock_ctx.agent_clock_state_table[ref_count_table_idx] =
-                    (uint8_t)MOD_CLOCK_STATE_RUNNING;
-
-                ref_count_table_idx = fwk_id_get_element_idx(
-                    agent->device_table[clock_idx].element_id);
-                scmi_clock_ctx.dev_clock_ref_count_table[ref_count_table_idx]++;
-            }
-        }
+	agent = &scmi_clock_ctx.agent_table[agent_id];
+	for (clock_idx = 0; clock_idx < (unsigned int)agent->device_count;
+	     clock_idx++) {
+	    if (agent->device_table[clock_idx].starts_enabled) {
+		ref_count_table_idx = (agent_id * clock_devices) + clock_idx;
+		scmi_clock_ctx.agent_clock_state_table[ref_count_table_idx] =
+		    (uint8_t)MOD_CLOCK_STATE_RUNNING;
+
+		ref_count_table_idx = fwk_id_get_element_idx(
+		    agent->device_table[clock_idx].element_id);
+		scmi_clock_ctx.dev_clock_ref_count_table[ref_count_table_idx]++;
+	    }
+	}
     }
 }
 
@@ -480,17 +480,17 @@ static void clock_ops_update_state(unsig
     int set_policy_status;
 
     if ((status == FWK_SUCCESS) &&
-        (scmi_clock_ctx.clock_ops[clock_dev_idx].request ==
-         SCMI_CLOCK_REQUEST_SET_STATE)) {
-        set_policy_status = mod_scmi_clock_config_set_policy(
-            &policy_status,
-            &scmi_clock_ctx.clock_ops[clock_dev_idx].state,
-            MOD_SCMI_CLOCK_POST_MESSAGE_HANDLER,
-            scmi_clock_ctx.clock_ops[clock_dev_idx].service_id,
-            scmi_clock_ctx.clock_ops[clock_dev_idx].scmi_clock_idx);
-        if (set_policy_status != FWK_SUCCESS) {
-            FWK_LOG_DEBUG("[SCMI-CLK] %s @%d", __func__, __LINE__);
-        }
+	(scmi_clock_ctx.clock_ops[clock_dev_idx].request ==
+	 SCMI_CLOCK_REQUEST_SET_STATE)) {
+	set_policy_status = mod_scmi_clock_config_set_policy(
+	    &policy_status,
+	    &scmi_clock_ctx.clock_ops[clock_dev_idx].state,
+	    MOD_SCMI_CLOCK_POST_MESSAGE_HANDLER,
+	    scmi_clock_ctx.clock_ops[clock_dev_idx].service_id,
+	    scmi_clock_ctx.clock_ops[clock_dev_idx].scmi_clock_idx);
+	if (set_policy_status != FWK_SUCCESS) {
+	    FWK_LOG_DEBUG("[SCMI-CLK] %s @%d", __func__, __LINE__);
+	}
     }
 }
 
@@ -507,42 +507,42 @@ static inline fwk_id_t clock_ops_get_ser
 static inline bool clock_ops_is_available(unsigned int clock_dev_idx)
 {
     return fwk_id_is_equal(scmi_clock_ctx.clock_ops[clock_dev_idx].service_id,
-                           FWK_ID_NONE);
+			   FWK_ID_NONE);
 }
 
 /*
  * Helper for the 'get_state' response
  */
 static void get_state_respond(fwk_id_t clock_dev_id,
-                              fwk_id_t service_id,
-                              enum mod_clock_state *clock_state,
-                              int status)
+			      fwk_id_t service_id,
+			      enum mod_clock_state *clock_state,
+			      int status)
 {
     int respond_status;
     size_t response_size;
     struct scmi_clock_attributes_p2a return_values = { 0 };
 
     if (status == FWK_SUCCESS) {
-        return_values.attributes = SCMI_CLOCK_ATTRIBUTES(
-            (uint32_t)(*clock_state == MOD_CLOCK_STATE_RUNNING));
+	return_values.attributes = SCMI_CLOCK_ATTRIBUTES(
+	    (uint32_t)(*clock_state == MOD_CLOCK_STATE_RUNNING));
 
-        fwk_str_strncpy(
-            return_values.clock_name,
-            fwk_module_get_element_name(clock_dev_id),
-            sizeof(return_values.clock_name) - 1);
+	fwk_str_strncpy(
+	    return_values.clock_name,
+	    fwk_module_get_element_name(clock_dev_id),
+	    sizeof(return_values.clock_name) - 1);
 
-        return_values.status = (int32_t)SCMI_SUCCESS;
-        response_size = sizeof(return_values);
+	return_values.status = (int32_t)SCMI_SUCCESS;
+	response_size = sizeof(return_values);
     } else {
-        return_values.status = (int32_t)SCMI_GENERIC_ERROR;
+	return_values.status = (int32_t)SCMI_GENERIC_ERROR;
 
-        response_size = sizeof(return_values.status);
+	response_size = sizeof(return_values.status);
     }
 
     respond_status = scmi_clock_ctx.scmi_api->respond(
-        service_id, &return_values, response_size);
+	service_id, &return_values, response_size);
     if (respond_status != FWK_SUCCESS) {
-        FWK_LOG_DEBUG("[SCMI-CLK] %s @%d", __func__, __LINE__);
+	FWK_LOG_DEBUG("[SCMI-CLK] %s @%d", __func__, __LINE__);
     }
 }
 
@@ -550,52 +550,52 @@ static void get_state_respond(fwk_id_t c
  * Helper for the 'get_rate' response
  */
 static void get_rate_respond(fwk_id_t service_id,
-                             uint64_t *rate,
-                             int status)
+			     uint64_t *rate,
+			     int status)
 {
     int respond_status;
     size_t response_size;
     struct scmi_clock_rate_get_p2a return_values = { 0 };
 
     if (status == FWK_SUCCESS) {
-        return_values.rate[0] = (uint32_t)*rate;
-        return_values.rate[1] = (uint32_t)(*rate >> 32);
+	return_values.rate[0] = (uint32_t)*rate;
+	return_values.rate[1] = (uint32_t)(*rate >> 32);
 
-        return_values.status = (int32_t)SCMI_SUCCESS;
-        response_size = sizeof(return_values);
+	return_values.status = (int32_t)SCMI_SUCCESS;
+	response_size = sizeof(return_values);
     } else {
-        return_values.status = (int32_t)SCMI_GENERIC_ERROR;
+	return_values.status = (int32_t)SCMI_GENERIC_ERROR;
 
-        response_size = sizeof(return_values.status);
+	response_size = sizeof(return_values.status);
     }
 
     respond_status = scmi_clock_ctx.scmi_api->respond(
-        service_id, &return_values, response_size);
+	service_id, &return_values, response_size);
     if (respond_status != FWK_SUCCESS) {
-        FWK_LOG_DEBUG("[SCMI-CLK] %s @%d", __func__, __LINE__);
+	FWK_LOG_DEBUG("[SCMI-CLK] %s @%d", __func__, __LINE__);
     }
 }
 
 static void request_response(int response_status,
-                             fwk_id_t service_id)
+			     fwk_id_t service_id)
 {
     int respond_status;
     struct scmi_clock_generic_p2a return_values = {
-        .status = (int32_t)SCMI_GENERIC_ERROR
+	.status = (int32_t)SCMI_GENERIC_ERROR
     };
 
     if (response_status == FWK_E_SUPPORT) {
-        return_values.status = (int32_t)SCMI_NOT_SUPPORTED;
+	return_values.status = (int32_t)SCMI_NOT_SUPPORTED;
     }
 
     if (response_status == FWK_E_RANGE) {
-        return_values.status = (int32_t)SCMI_INVALID_PARAMETERS;
+	return_values.status = (int32_t)SCMI_INVALID_PARAMETERS;
     }
 
     respond_status = scmi_clock_ctx.scmi_api->respond(
-        service_id, &return_values, sizeof(return_values.status));
+	service_id, &return_values, sizeof(return_values.status));
     if (respond_status != FWK_SUCCESS) {
-        FWK_LOG_DEBUG("[SCMI-CLK] %s @%d", __func__, __LINE__);
+	FWK_LOG_DEBUG("[SCMI-CLK] %s @%d", __func__, __LINE__);
     }
 }
 
@@ -608,19 +608,19 @@ static void set_request_respond(fwk_id_t
     struct scmi_clock_generic_p2a return_values = { 0 };
 
     if (status == FWK_E_RANGE || status == FWK_E_PARAM) {
-        return_values.status = (int32_t)SCMI_INVALID_PARAMETERS;
+	return_values.status = (int32_t)SCMI_INVALID_PARAMETERS;
     } else if (status == FWK_E_SUPPORT) {
-        return_values.status = (int32_t)SCMI_NOT_SUPPORTED;
+	return_values.status = (int32_t)SCMI_NOT_SUPPORTED;
     } else if (status != FWK_SUCCESS) {
-        return_values.status = (int32_t)SCMI_GENERIC_ERROR;
+	return_values.status = (int32_t)SCMI_GENERIC_ERROR;
     } else {
-        return_values.status = (int32_t)SCMI_SUCCESS;
+	return_values.status = (int32_t)SCMI_SUCCESS;
     }
 
     respond_status = scmi_clock_ctx.scmi_api->respond(
-        service_id, &return_values, sizeof(return_values.status));
+	service_id, &return_values, sizeof(return_values.status));
     if (respond_status != FWK_SUCCESS) {
-        FWK_LOG_DEBUG("[SCMI-CLK] %s @%d", __func__, __LINE__);
+	FWK_LOG_DEBUG("[SCMI-CLK] %s @%d", __func__, __LINE__);
     }
 }
 
@@ -652,33 +652,33 @@ int mod_scmi_clock_config_set_policy(
 
     status = scmi_clock_ctx.scmi_api->get_agent_id(service_id, &agent_id);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     dev_clock_idx = scmi_clock_get_clock_device_idx(agent_id, scmi_clock_idx);
 
     switch (policy_commit) {
     case MOD_SCMI_CLOCK_PRE_MESSAGE_HANDLER:
-        *policy_status = mod_scmi_clock_request_state_check(
-            agent_id, scmi_clock_idx, dev_clock_idx, *state);
-        if (*policy_status == MOD_SCMI_CLOCK_SKIP_MESSAGE_HANDLER) {
-            /*
-             * If the requested state is different from the current state of the
-             * internal clock, it is necessary to update the clock state to
-             * reflect the requested state. This update is required when
-             * skipping the message handler, since the normal update mechanism
-             * will be bypassed.
-             */
-            return mod_scmi_clock_state_update(
-                agent_id, scmi_clock_idx, dev_clock_idx, *state);
-        }
-        return FWK_SUCCESS;
+	*policy_status = mod_scmi_clock_request_state_check(
+	    agent_id, scmi_clock_idx, dev_clock_idx, *state);
+	if (*policy_status == MOD_SCMI_CLOCK_SKIP_MESSAGE_HANDLER) {
+	    /*
+	     * If the requested state is different from the current state of the
+	     * internal clock, it is necessary to update the clock state to
+	     * reflect the requested state. This update is required when
+	     * skipping the message handler, since the normal update mechanism
+	     * will be bypassed.
+	     */
+	    return mod_scmi_clock_state_update(
+		agent_id, scmi_clock_idx, dev_clock_idx, *state);
+	}
+	return FWK_SUCCESS;
     case MOD_SCMI_CLOCK_POST_MESSAGE_HANDLER:
-        return mod_scmi_clock_state_update(
-            agent_id, scmi_clock_idx, dev_clock_idx, *state);
+	return mod_scmi_clock_state_update(
+	    agent_id, scmi_clock_idx, dev_clock_idx, *state);
 
     default:
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     return FWK_E_PARAM;
@@ -697,25 +697,25 @@ static int scmi_clock_permissions_handle
 
     status = scmi_clock_ctx.scmi_api->get_agent_id(service_id, &agent_id);
     if (status != FWK_SUCCESS) {
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 
     if (message_id < 3) {
-        perms = scmi_clock_ctx.res_perms_api->agent_has_protocol_permission(
-            agent_id, MOD_SCMI_PROTOCOL_ID_CLOCK);
-        if (perms == MOD_RES_PERMS_ACCESS_ALLOWED) {
-            return FWK_SUCCESS;
-        }
-        return FWK_E_ACCESS;
+	perms = scmi_clock_ctx.res_perms_api->agent_has_protocol_permission(
+	    agent_id, MOD_SCMI_PROTOCOL_ID_CLOCK);
+	if (perms == MOD_RES_PERMS_ACCESS_ALLOWED) {
+	    return FWK_SUCCESS;
+	}
+	return FWK_E_ACCESS;
     }
 
     perms = scmi_clock_ctx.res_perms_api->agent_has_resource_permission(
-        agent_id, MOD_SCMI_PROTOCOL_ID_CLOCK, message_id, scmi_clock_idx);
+	agent_id, MOD_SCMI_PROTOCOL_ID_CLOCK, message_id, scmi_clock_idx);
 
     if (perms == MOD_RES_PERMS_ACCESS_ALLOWED) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     } else {
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 }
 
@@ -738,65 +738,65 @@ static int create_event_request(
     enum mod_clock_state state = MOD_CLOCK_STATE_COUNT;
 
     if (!clock_ops_is_available(clock_dev_idx)) {
-        return FWK_E_BUSY;
+	return FWK_E_BUSY;
     }
 
     struct fwk_event event = {
-        .target_id = fwk_module_id_scmi_clock,
+	.target_id = fwk_module_id_scmi_clock,
     };
 
     params = (struct scmi_clock_event_request_params *)event.params;
 
     switch (request) {
     case SCMI_CLOCK_REQUEST_GET_STATE:
-        event.id = mod_scmi_clock_event_id_get_state;
-        break;
+	event.id = mod_scmi_clock_event_id_get_state;
+	break;
 
     case SCMI_CLOCK_REQUEST_GET_RATE:
-        event.id = mod_scmi_clock_event_id_get_rate;
-        break;
+	event.id = mod_scmi_clock_event_id_get_rate;
+	break;
 
     case SCMI_CLOCK_REQUEST_SET_RATE:
-        {
-        struct event_set_rate_request_data *rate_data =
-            (struct event_set_rate_request_data *)data;
-
-        request_data.set_rate_data.rate[0] = rate_data->rate[0];
-        request_data.set_rate_data.rate[1] = rate_data->rate[1];
-        request_data.set_rate_data.round_mode = rate_data->round_mode;
+	{
+	struct event_set_rate_request_data *rate_data =
+	    (struct event_set_rate_request_data *)data;
+
+	request_data.set_rate_data.rate[0] = rate_data->rate[0];
+	request_data.set_rate_data.rate[1] = rate_data->rate[1];
+	request_data.set_rate_data.round_mode = rate_data->round_mode;
 
-        params->request_data = request_data;
-        }
+	params->request_data = request_data;
+	}
 
-        event.id = mod_scmi_clock_event_id_set_rate;
-        break;
+	event.id = mod_scmi_clock_event_id_set_rate;
+	break;
 
     case SCMI_CLOCK_REQUEST_SET_STATE:
-        {
-        struct event_set_state_request_data *state_data =
-            (struct event_set_state_request_data *)data;
-        request_data.set_state_data.state = state_data->state;
-        state = state_data->state;
+	{
+	struct event_set_state_request_data *state_data =
+	    (struct event_set_state_request_data *)data;
+	request_data.set_state_data.state = state_data->state;
+	state = state_data->state;
 
-        params->request_data = request_data;
-        }
+	params->request_data = request_data;
+	}
 
-        event.id = mod_scmi_clock_event_id_set_state;
-        break;
+	event.id = mod_scmi_clock_event_id_set_state;
+	break;
 
     default:
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     params->clock_dev_id = clock_id;
 
     status = fwk_put_event(&event);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     clock_ops_set_busy(
-        clock_dev_idx, service_id, scmi_clock_idx, state, request);
+	clock_dev_idx, service_id, scmi_clock_idx, state, request);
 
     return FWK_SUCCESS;
 }
@@ -808,12 +808,12 @@ static int scmi_clock_protocol_version_h
     const uint32_t *payload)
 {
     struct scmi_protocol_version_p2a return_values = {
-        .status = (int32_t)SCMI_SUCCESS,
-        .version = SCMI_PROTOCOL_VERSION_CLOCK,
+	.status = (int32_t)SCMI_SUCCESS,
+	.version = SCMI_PROTOCOL_VERSION_CLOCK,
     };
 
     return scmi_clock_ctx.scmi_api->respond(
-        service_id, &return_values, sizeof(return_values));
+	service_id, &return_values, sizeof(return_values));
 }
 
 /*
@@ -825,25 +825,25 @@ static int scmi_clock_protocol_attribute
     int status, respond_status;
     const struct mod_scmi_clock_agent *agent;
     struct scmi_protocol_attributes_p2a return_values = {
-        .status = (int32_t)SCMI_SUCCESS,
+	.status = (int32_t)SCMI_SUCCESS,
     };
 
     status = scmi_clock_get_agent_entry(service_id, &agent);
     if (status != FWK_SUCCESS) {
-        return_values.status = (int32_t)SCMI_GENERIC_ERROR;
-        goto exit;
+	return_values.status = (int32_t)SCMI_GENERIC_ERROR;
+	goto exit;
     }
 
     return_values.attributes = SCMI_CLOCK_PROTOCOL_ATTRIBUTES(
-            scmi_clock_ctx.max_pending_transactions,
-            agent->device_count
-        );
+	    scmi_clock_ctx.max_pending_transactions,
+	    agent->device_count
+	);
 
 exit:
     respond_status = scmi_clock_ctx.scmi_api->respond(
-        service_id, &return_values, sizeof(return_values));
+	service_id, &return_values, sizeof(return_values));
     if (respond_status != FWK_SUCCESS) {
-        FWK_LOG_DEBUG("[SCMI-CLK] %s @%d", __func__, __LINE__);
+	FWK_LOG_DEBUG("[SCMI-CLK] %s @%d", __func__, __LINE__);
     }
     return status;
 }
@@ -858,25 +858,25 @@ static int scmi_clock_protocol_message_a
     const struct scmi_protocol_message_attributes_a2p *parameters;
     unsigned int message_id;
     struct scmi_protocol_message_attributes_p2a return_values = {
-        .status = (int32_t)SCMI_SUCCESS,
-        .attributes = 0,
+	.status = (int32_t)SCMI_SUCCESS,
+	.attributes = 0,
     };
 
     parameters = (const struct scmi_protocol_message_attributes_a2p*)
-        payload;
+	payload;
     message_id = parameters->message_id;
 
     if ((message_id >= FWK_ARRAY_SIZE(handler_table)) ||
-        (handler_table[message_id] == NULL)) {
-        return_values.status = (int32_t)SCMI_NOT_FOUND;
-        goto exit;
+	(handler_table[message_id] == NULL)) {
+	return_values.status = (int32_t)SCMI_NOT_FOUND;
+	goto exit;
     }
 
 exit:
     response_size = (return_values.status == SCMI_SUCCESS) ?
-        sizeof(return_values) : sizeof(return_values.status);
+	sizeof(return_values) : sizeof(return_values.status);
     return scmi_clock_ctx.scmi_api->respond(
-        service_id, &return_values, response_size);
+	service_id, &return_values, response_size);
 }
 
 /*
@@ -891,54 +891,54 @@ static int scmi_clock_attributes_handler
     size_t response_size;
     const struct scmi_clock_attributes_a2p *parameters;
     struct scmi_clock_attributes_p2a return_values = {
-        .status = (int32_t)SCMI_GENERIC_ERROR
+	.status = (int32_t)SCMI_GENERIC_ERROR
     };
 
     parameters = (const struct scmi_clock_attributes_a2p*)payload;
 
     status = scmi_clock_get_clock_device_entry(
-        service_id, parameters->clock_id, &clock_device);
+	service_id, parameters->clock_id, &clock_device);
     if (status != FWK_SUCCESS) {
-        return_values.status = (int32_t)SCMI_NOT_FOUND;
-        goto exit;
+	return_values.status = (int32_t)SCMI_NOT_FOUND;
+	goto exit;
     }
 
 #ifdef BUILD_HAS_MOD_RESOURCE_PERMS
     status = scmi_clock_permissions_handler(
-        parameters->clock_id,
-        service_id,
-        (unsigned int)MOD_SCMI_CLOCK_ATTRIBUTES);
+	parameters->clock_id,
+	service_id,
+	(unsigned int)MOD_SCMI_CLOCK_ATTRIBUTES);
     if (status != FWK_SUCCESS) {
-        return_values.status = (int32_t)SCMI_DENIED;
-        goto exit;
+	return_values.status = (int32_t)SCMI_DENIED;
+	goto exit;
     }
 #endif
 
     status = create_event_request(
-        clock_device->element_id,
-        service_id,
-        SCMI_CLOCK_REQUEST_GET_STATE,
-        NULL,
-        parameters->clock_id);
+	clock_device->element_id,
+	service_id,
+	SCMI_CLOCK_REQUEST_GET_STATE,
+	NULL,
+	parameters->clock_id);
     if (status == FWK_E_BUSY) {
-        return_values.status = (int32_t)SCMI_BUSY;
-        status = FWK_SUCCESS;
-        goto exit;
+	return_values.status = (int32_t)SCMI_BUSY;
+	status = FWK_SUCCESS;
+	goto exit;
     }
 
     if (status != FWK_SUCCESS) {
-        goto exit;
+	goto exit;
     }
 
     return FWK_SUCCESS;
 
 exit:
     response_size = (return_values.status == SCMI_SUCCESS) ?
-        sizeof(return_values) : sizeof(return_values.status);
+	sizeof(return_values) : sizeof(return_values.status);
     respond_status = scmi_clock_ctx.scmi_api->respond(
-        service_id, &return_values, response_size);
+	service_id, &return_values, response_size);
     if (respond_status != FWK_SUCCESS) {
-        FWK_LOG_DEBUG("[SCMI-CLK] %s @%d", __func__, __LINE__);
+	FWK_LOG_DEBUG("[SCMI-CLK] %s @%d", __func__, __LINE__);
     }
 
     return status;
@@ -955,54 +955,54 @@ static int scmi_clock_rate_get_handler(f
     size_t response_size;
     const struct scmi_clock_rate_get_a2p *parameters;
     struct scmi_clock_rate_get_p2a return_values = {
-        .status = (int32_t)SCMI_GENERIC_ERROR
+	.status = (int32_t)SCMI_GENERIC_ERROR
     };
 
     parameters = (const struct scmi_clock_rate_get_a2p*)payload;
 
     status = scmi_clock_get_clock_device_entry(
-        service_id, parameters->clock_id, &clock_device);
+	service_id, parameters->clock_id, &clock_device);
     if (status != FWK_SUCCESS) {
-        return_values.status = (int32_t)SCMI_NOT_FOUND;
-        goto exit;
+	return_values.status = (int32_t)SCMI_NOT_FOUND;
+	goto exit;
     }
 
 #ifdef BUILD_HAS_MOD_RESOURCE_PERMS
     status = scmi_clock_permissions_handler(
-        parameters->clock_id,
-        service_id,
-        (unsigned int)MOD_SCMI_CLOCK_RATE_GET);
+	parameters->clock_id,
+	service_id,
+	(unsigned int)MOD_SCMI_CLOCK_RATE_GET);
     if (status != FWK_SUCCESS) {
-        return_values.status = (int32_t)SCMI_DENIED;
-        goto exit;
+	return_values.status = (int32_t)SCMI_DENIED;
+	goto exit;
     }
 #endif
 
     status = create_event_request(
-        clock_device->element_id,
-        service_id,
-        SCMI_CLOCK_REQUEST_GET_RATE,
-        NULL,
-        parameters->clock_id);
+	clock_device->element_id,
+	service_id,
+	SCMI_CLOCK_REQUEST_GET_RATE,
+	NULL,
+	parameters->clock_id);
     if (status == FWK_E_BUSY) {
-        return_values.status = (int32_t)SCMI_BUSY;
-        status = FWK_SUCCESS;
-        goto exit;
+	return_values.status = (int32_t)SCMI_BUSY;
+	status = FWK_SUCCESS;
+	goto exit;
     }
 
     if (status != FWK_SUCCESS) {
-        goto exit;
+	goto exit;
     }
 
     return FWK_SUCCESS;
 
 exit:
     response_size = (return_values.status == SCMI_SUCCESS) ?
-        sizeof(return_values) : sizeof(return_values.status);
+	sizeof(return_values) : sizeof(return_values.status);
     respond_status = scmi_clock_ctx.scmi_api->respond(
-        service_id, &return_values, response_size);
+	service_id, &return_values, response_size);
     if (respond_status != FWK_SUCCESS) {
-        FWK_LOG_DEBUG("[SCMI-CLK] %s @%d", __func__, __LINE__);
+	FWK_LOG_DEBUG("[SCMI-CLK] %s @%d", __func__, __LINE__);
     }
 
     return status;
@@ -1025,7 +1025,7 @@ static int scmi_clock_rate_set_handler(f
     enum mod_clock_round_mode round_mode;
     const struct scmi_clock_rate_set_a2p *parameters;
     struct scmi_clock_rate_set_p2a return_values = {
-        .status = (int32_t)SCMI_GENERIC_ERROR
+	.status = (int32_t)SCMI_GENERIC_ERROR
     };
     enum mod_scmi_clock_policy_status policy_status;
 
@@ -1035,96 +1035,96 @@ static int scmi_clock_rate_set_handler(f
     asynchronous = (parameters->flags & SCMI_CLOCK_RATE_SET_ASYNC_MASK) != 0;
 
     if ((parameters->flags & ~SCMI_CLOCK_RATE_SET_FLAGS_MASK) != 0) {
-        return_values.status = (int32_t)SCMI_INVALID_PARAMETERS;
-        goto exit;
+	return_values.status = (int32_t)SCMI_INVALID_PARAMETERS;
+	goto exit;
     }
 
     status = scmi_clock_get_clock_device_entry(
-        service_id, parameters->clock_id, &clock_device);
+	service_id, parameters->clock_id, &clock_device);
     if (status != FWK_SUCCESS) {
-        return_values.status = (int32_t)SCMI_NOT_FOUND;
-        goto exit;
+	return_values.status = (int32_t)SCMI_NOT_FOUND;
+	goto exit;
     }
 
 #ifdef BUILD_HAS_MOD_RESOURCE_PERMS
     status = scmi_clock_permissions_handler(
-        parameters->clock_id,
-        service_id,
-        (unsigned int)MOD_SCMI_CLOCK_RATE_SET);
+	parameters->clock_id,
+	service_id,
+	(unsigned int)MOD_SCMI_CLOCK_RATE_SET);
     if (status != FWK_SUCCESS) {
-        return_values.status = (int32_t)SCMI_DENIED;
-        goto exit;
+	return_values.status = (int32_t)SCMI_DENIED;
+	goto exit;
     }
 #endif
 
     if (asynchronous) {
-        /* Support for async clock set commands not yet implemented */
-        return_values.status = (int32_t)SCMI_NOT_SUPPORTED;
-        goto exit;
+	/* Support for async clock set commands not yet implemented */
+	return_values.status = (int32_t)SCMI_NOT_SUPPORTED;
+	goto exit;
     }
 
     status = scmi_clock_ctx.scmi_api->get_agent_id(service_id, &agent_id);
     if (status != FWK_SUCCESS) {
-        goto exit;
+	goto exit;
     }
 
     rate = ((uint64_t)(parameters->rate[1]) << 32) |
-        (uint64_t)parameters->rate[0];
+	(uint64_t)parameters->rate[0];
     round_mode = round_auto ?
-        MOD_CLOCK_ROUND_MODE_NEAREST :
-        (round_up ? MOD_CLOCK_ROUND_MODE_UP : MOD_CLOCK_ROUND_MODE_DOWN);
+	MOD_CLOCK_ROUND_MODE_NEAREST :
+	(round_up ? MOD_CLOCK_ROUND_MODE_UP : MOD_CLOCK_ROUND_MODE_DOWN);
 
     /*
      * Note that rate and round_mode may be modified by the policy handler.
      */
     status = mod_scmi_clock_rate_set_policy(
-        &policy_status,
-        &round_mode,
-        &rate,
-        MOD_SCMI_CLOCK_PRE_MESSAGE_HANDLER,
-        service_id,
-        parameters->clock_id);
+	&policy_status,
+	&round_mode,
+	&rate,
+	MOD_SCMI_CLOCK_PRE_MESSAGE_HANDLER,
+	service_id,
+	parameters->clock_id);
 
     if (status != FWK_SUCCESS) {
-        return_values.status = (int32_t)SCMI_GENERIC_ERROR;
-        goto exit;
+	return_values.status = (int32_t)SCMI_GENERIC_ERROR;
+	goto exit;
     }
     if (policy_status == MOD_SCMI_CLOCK_SKIP_MESSAGE_HANDLER) {
-        return_values.status = (int32_t)SCMI_SUCCESS;
-        goto exit;
+	return_values.status = (int32_t)SCMI_SUCCESS;
+	goto exit;
     }
 
     struct event_set_rate_request_data data = {
-        .rate = {
-                [0] = (uint32_t)rate,
-                [1] = (uint32_t)(rate >> 32),
-        },
-        .round_mode = round_mode,
+	.rate = {
+		[0] = (uint32_t)rate,
+		[1] = (uint32_t)(rate >> 32),
+	},
+	.round_mode = round_mode,
     };
 
     status = create_event_request(
-        clock_device->element_id,
-        service_id,
-        SCMI_CLOCK_REQUEST_SET_RATE,
-        &data,
-        parameters->clock_id);
+	clock_device->element_id,
+	service_id,
+	SCMI_CLOCK_REQUEST_SET_RATE,
+	&data,
+	parameters->clock_id);
     if (status == FWK_E_BUSY) {
-        return_values.status = (int32_t)SCMI_BUSY;
-        status = FWK_SUCCESS;
-        goto exit;
+	return_values.status = (int32_t)SCMI_BUSY;
+	status = FWK_SUCCESS;
+	goto exit;
     }
 
     if (status != FWK_SUCCESS) {
-        goto exit;
+	goto exit;
     }
 
     return FWK_SUCCESS;
 
 exit:
     response_size = (return_values.status == SCMI_SUCCESS) ?
-        sizeof(return_values) : sizeof(return_values.status);
+	sizeof(return_values) : sizeof(return_values.status);
     return scmi_clock_ctx.scmi_api->respond(
-        service_id, &return_values, response_size);
+	service_id, &return_values, response_size);
 }
 
 /*
@@ -1140,7 +1140,7 @@ static int scmi_clock_config_set_handler
     const struct mod_scmi_clock_device *clock_device;
     unsigned int agent_id;
     struct scmi_clock_rate_set_p2a return_values = {
-        .status = (int32_t)SCMI_GENERIC_ERROR
+	.status = (int32_t)SCMI_GENERIC_ERROR
     };
     enum mod_scmi_clock_policy_status policy_status;
 
@@ -1148,78 +1148,78 @@ static int scmi_clock_config_set_handler
     enable = (parameters->attributes & SCMI_CLOCK_CONFIG_SET_ENABLE_MASK) != 0;
 
     status = scmi_clock_get_clock_device_entry(
-        service_id, parameters->clock_id, &clock_device);
+	service_id, parameters->clock_id, &clock_device);
     if (status != FWK_SUCCESS) {
-        return_values.status = (int32_t)SCMI_NOT_FOUND;
-        goto exit;
+	return_values.status = (int32_t)SCMI_NOT_FOUND;
+	goto exit;
     }
 
     if ((parameters->attributes & ~SCMI_CLOCK_CONFIG_SET_ENABLE_MASK) != 0) {
-        return_values.status = (int32_t)SCMI_INVALID_PARAMETERS;
-        goto exit;
+	return_values.status = (int32_t)SCMI_INVALID_PARAMETERS;
+	goto exit;
     }
 
 #ifdef BUILD_HAS_MOD_RESOURCE_PERMS
     status = scmi_clock_permissions_handler(
-        parameters->clock_id,
-        service_id,
-        (unsigned int)MOD_SCMI_CLOCK_CONFIG_SET);
+	parameters->clock_id,
+	service_id,
+	(unsigned int)MOD_SCMI_CLOCK_CONFIG_SET);
     if (status != FWK_SUCCESS) {
-        return_values.status = (int32_t)SCMI_DENIED;
-        goto exit;
+	return_values.status = (int32_t)SCMI_DENIED;
+	goto exit;
     }
 #endif
 
     struct event_set_state_request_data data = {
-        .state = enable ? MOD_CLOCK_STATE_RUNNING : MOD_CLOCK_STATE_STOPPED
+	.state = enable ? MOD_CLOCK_STATE_RUNNING : MOD_CLOCK_STATE_STOPPED
     };
 
     status = scmi_clock_ctx.scmi_api->get_agent_id(service_id, &agent_id);
     if (status != FWK_SUCCESS) {
-        goto exit;
+	goto exit;
     }
 
     /*
      * Note that data.state may be modified by the policy handler.
      */
     status = mod_scmi_clock_config_set_policy(
-        &policy_status,
-        &data.state,
-        MOD_SCMI_CLOCK_PRE_MESSAGE_HANDLER,
-        service_id,
-        parameters->clock_id);
+	&policy_status,
+	&data.state,
+	MOD_SCMI_CLOCK_PRE_MESSAGE_HANDLER,
+	service_id,
+	parameters->clock_id);
     if (status != FWK_SUCCESS) {
-        return_values.status = (int32_t)SCMI_GENERIC_ERROR;
-        goto exit;
+	return_values.status = (int32_t)SCMI_GENERIC_ERROR;
+	goto exit;
     }
     if (policy_status == MOD_SCMI_CLOCK_SKIP_MESSAGE_HANDLER) {
-        return_values.status = (int32_t)SCMI_SUCCESS;
-        goto exit;
+	return_values.status = (int32_t)SCMI_SUCCESS;
+	goto exit;
     }
 
     status = create_event_request(
-        clock_device->element_id,
-        service_id,
-        SCMI_CLOCK_REQUEST_SET_STATE,
-        &data,
-        parameters->clock_id);
+	clock_device->element_id,
+	service_id,
+	SCMI_CLOCK_REQUEST_SET_STATE,
+	&data,
+	parameters->clock_id);
     if (status == FWK_E_BUSY) {
-        return_values.status = (int32_t)SCMI_BUSY;
-        status = FWK_SUCCESS;
-        goto exit;
+	return_values.status = (int32_t)SCMI_BUSY;
+	status = FWK_SUCCESS;
+	goto exit;
     }
 
     if (status != FWK_SUCCESS) {
-        goto exit;
+	goto exit;
     }
 
     return FWK_SUCCESS;
 
 exit:
     response_size = (return_values.status == SCMI_SUCCESS) ?
-        sizeof(return_values) : sizeof(return_values.status);
+	sizeof(return_values) : sizeof(return_values.status);
     return scmi_clock_ctx.scmi_api->respond(
-        service_id, &return_values, response_size);
+	service_id, &return_values, response_size);
 }
 
 /*
@@ -1242,7 +1242,7 @@ static int scmi_clock_describe_rates_han
     struct mod_clock_info info;
     const struct scmi_clock_describe_rates_a2p *parameters;
     struct scmi_clock_describe_rates_p2a return_values = {
-        .status = (int32_t)SCMI_GENERIC_ERROR
+	.status = (int32_t)SCMI_GENERIC_ERROR
     };
 
     parameters = (const struct scmi_clock_describe_rates_a2p*)payload;
@@ -1250,20 +1250,20 @@ static int scmi_clock_describe_rates_han
     payload_size = (uint32_t)sizeof(return_values);
 
     status = scmi_clock_get_clock_device_entry(
-        service_id, parameters->clock_id, &clock_device);
+	service_id, parameters->clock_id, &clock_device);
     if (status != FWK_SUCCESS) {
-        return_values.status = (int32_t)SCMI_NOT_FOUND;
-        goto exit;
+	return_values.status = (int32_t)SCMI_NOT_FOUND;
+	goto exit;
     }
 
 #ifdef BUILD_HAS_MOD_RESOURCE_PERMS
     status = scmi_clock_permissions_handler(
-        parameters->clock_id,
-        service_id,
-        (unsigned int)MOD_SCMI_CLOCK_DESCRIBE_RATES);
+	parameters->clock_id,
+	service_id,
+	(unsigned int)MOD_SCMI_CLOCK_DESCRIBE_RATES);
     if (status != FWK_SUCCESS) {
-        return_values.status = (int32_t)SCMI_DENIED;
-        goto exit;
+	return_values.status = (int32_t)SCMI_DENIED;
+	goto exit;
     }
 #endif
 
@@ -1272,122 +1272,122 @@ static int scmi_clock_describe_rates_han
      * be returned in one response.
      */
     status = scmi_clock_ctx.scmi_api->get_max_payload_size(
-        service_id, &max_payload_size);
+	service_id, &max_payload_size);
     if (status != FWK_SUCCESS) {
-        goto exit;
+	goto exit;
     }
 
     status = scmi_clock_ctx.clock_api->get_info(clock_device->element_id,
-                                                &info);
+						&info);
     if (status != FWK_SUCCESS) {
-        goto exit;
+	goto exit;
     }
 
     if (info.range.rate_type == MOD_CLOCK_RATE_TYPE_DISCRETE) {
-        /* The clock has a discrete list of frequencies */
+	/* The clock has a discrete list of frequencies */
 
-        if (index >= info.range.rate_count) {
-            return_values.status = (int32_t)SCMI_OUT_OF_RANGE;
-            goto exit;
-        }
-
-        /* Can at least one entry be returned? */
-        if (SCMI_CLOCK_RATES_MAX(max_payload_size) == 0) {
-            status = FWK_E_SIZE;
-            goto exit;
-        }
-
-        /* The number of rates being returned in this payload is defined as the
-         * smaller of:
-         * - The clock rates that are available between the index and the
-             clock's maximum rate.
-           - The number of rates that can be returned in each payload.
-         */
-        rate_count = (unsigned int)FWK_MIN(
-            SCMI_CLOCK_RATES_MAX(max_payload_size),
-            info.range.rate_count - index);
-
-        /*
-         * Because the agent gives a starting index into the clock's rate list
-         * the number of rates remaining is calculated as the number of rates
-         * the clock supports minus the index, with the number of rates being
-         * returned in this payload subtracted.
-         */
-        remaining_rates =
-            (unsigned int)(info.range.rate_count - index) - rate_count;
-
-        /* Give the number of rates sent in the message payload */
-        return_values.num_rates_flags =
-            SCMI_CLOCK_DESCRIBE_RATES_NUM_RATES_FLAGS(
-                rate_count,
-                SCMI_CLOCK_RATE_FORMAT_LIST,
-                remaining_rates
-            );
-
-        /* Set each rate entry in the payload to the associated frequency */
-        for (i = 0; i < rate_count;
-             i++, payload_size += (uint32_t)sizeof(struct scmi_clock_rate)) {
-            status = scmi_clock_ctx.clock_api->get_rate_from_index(
-                clock_device->element_id,
-                index + i,
-                &rate);
-            if (status != FWK_SUCCESS) {
-                goto exit;
-            }
-
-            scmi_rate.low = (uint32_t)rate;
-            scmi_rate.high = (uint32_t)(rate >> 32);
-
-            status = scmi_clock_ctx.scmi_api->write_payload(service_id,
-                payload_size, &scmi_rate, sizeof(scmi_rate));
-            if (status != FWK_SUCCESS) {
-                goto exit;
-            }
-        }
+	if (index >= info.range.rate_count) {
+	    return_values.status = (int32_t)SCMI_OUT_OF_RANGE;
+	    goto exit;
+	}
+
+	/* Can at least one entry be returned? */
+	if (SCMI_CLOCK_RATES_MAX(max_payload_size) == 0) {
+	    status = FWK_E_SIZE;
+	    goto exit;
+	}
+
+	/* The number of rates being returned in this payload is defined as the
+	 * smaller of:
+	 * - The clock rates that are available between the index and the
+	     clock's maximum rate.
+	   - The number of rates that can be returned in each payload.
+	 */
+	rate_count = (unsigned int)FWK_MIN(
+	    SCMI_CLOCK_RATES_MAX(max_payload_size),
+	    info.range.rate_count - index);
+
+	/*
+	 * Because the agent gives a starting index into the clock's rate list
+	 * the number of rates remaining is calculated as the number of rates
+	 * the clock supports minus the index, with the number of rates being
+	 * returned in this payload subtracted.
+	 */
+	remaining_rates =
+	    (unsigned int)(info.range.rate_count - index) - rate_count;
+
+	/* Give the number of rates sent in the message payload */
+	return_values.num_rates_flags =
+	    SCMI_CLOCK_DESCRIBE_RATES_NUM_RATES_FLAGS(
+		rate_count,
+		SCMI_CLOCK_RATE_FORMAT_LIST,
+		remaining_rates
+	    );
+
+	/* Set each rate entry in the payload to the associated frequency */
+	for (i = 0; i < rate_count;
+	     i++, payload_size += (uint32_t)sizeof(struct scmi_clock_rate)) {
+	    status = scmi_clock_ctx.clock_api->get_rate_from_index(
+		clock_device->element_id,
+		index + i,
+		&rate);
+	    if (status != FWK_SUCCESS) {
+		goto exit;
+	    }
+
+	    scmi_rate.low = (uint32_t)rate;
+	    scmi_rate.high = (uint32_t)(rate >> 32);
+
+	    status = scmi_clock_ctx.scmi_api->write_payload(service_id,
+		payload_size, &scmi_rate, sizeof(scmi_rate));
+	    if (status != FWK_SUCCESS) {
+		goto exit;
+	    }
+	}
     } else {
-        /* The clock has a linear stepping */
+	/* The clock has a linear stepping */
 
-        /* Is the payload area large enough to return the complete triplet? */
-        if (SCMI_CLOCK_RATES_MAX(max_payload_size) < 3) {
-            status = FWK_E_SIZE;
-            goto exit;
-        }
-
-        return_values.num_rates_flags =
-            SCMI_CLOCK_DESCRIBE_RATES_NUM_RATES_FLAGS(
-                SCMI_CLOCK_NUM_OF_RATES_RANGE,
-                SCMI_CLOCK_RATE_FORMAT_RANGE,
-                /* No further rates are available */
-                0U);
-
-        /* Store the range data in the range entry in the payload */
-        clock_range[0].low = (uint32_t)info.range.min;
-        clock_range[0].high = (uint32_t)(info.range.min >> 32);
-        clock_range[1].low = (uint32_t)info.range.max;
-        clock_range[1].high = (uint32_t)(info.range.max >> 32);
-        clock_range[2].low = (uint32_t)info.range.step;
-        clock_range[2].high = (uint32_t)(info.range.step >> 32);
-
-        status = scmi_clock_ctx.scmi_api->write_payload(service_id,
-            payload_size, &clock_range, sizeof(clock_range));
-        if (status != FWK_SUCCESS) {
-            goto exit;
-        }
-        payload_size += (uint32_t)sizeof(clock_range);
+	/* Is the payload area large enough to return the complete triplet? */
+	if (SCMI_CLOCK_RATES_MAX(max_payload_size) < 3) {
+	    status = FWK_E_SIZE;
+	    goto exit;
+	}
+
+	return_values.num_rates_flags =
+	    SCMI_CLOCK_DESCRIBE_RATES_NUM_RATES_FLAGS(
+		SCMI_CLOCK_NUM_OF_RATES_RANGE,
+		SCMI_CLOCK_RATE_FORMAT_RANGE,
+		/* No further rates are available */
+		0U);
+
+	/* Store the range data in the range entry in the payload */
+	clock_range[0].low = (uint32_t)info.range.min;
+	clock_range[0].high = (uint32_t)(info.range.min >> 32);
+	clock_range[1].low = (uint32_t)info.range.max;
+	clock_range[1].high = (uint32_t)(info.range.max >> 32);
+	clock_range[2].low = (uint32_t)info.range.step;
+	clock_range[2].high = (uint32_t)(info.range.step >> 32);
+
+	status = scmi_clock_ctx.scmi_api->write_payload(service_id,
+	    payload_size, &clock_range, sizeof(clock_range));
+	if (status != FWK_SUCCESS) {
+	    goto exit;
+	}
+	payload_size += (uint32_t)sizeof(clock_range);
     }
 
     return_values.status = (int32_t)SCMI_SUCCESS;
     status = scmi_clock_ctx.scmi_api->write_payload(service_id, 0,
-        &return_values, sizeof(return_values));
+	&return_values, sizeof(return_values));
 
 exit:
     respond_status = scmi_clock_ctx.scmi_api->respond(
-        service_id,
-        (return_values.status == SCMI_SUCCESS) ? NULL : &return_values.status,
-        (return_values.status == SCMI_SUCCESS) ? payload_size :
-                                                 sizeof(return_values.status));
+	service_id,
+	(return_values.status == SCMI_SUCCESS) ? NULL : &return_values.status,
+	(return_values.status == SCMI_SUCCESS) ? payload_size :
+						 sizeof(return_values.status));
     if (respond_status != FWK_SUCCESS) {
-        FWK_LOG_DEBUG("[SCMI-CLK] %s @%d", __func__, __LINE__);
+	FWK_LOG_DEBUG("[SCMI-CLK] %s @%d", __func__, __LINE__);
     }
 
     return status;
@@ -1401,33 +1401,33 @@ static int scmi_clock_name_get_handler(f
     size_t response_size;
     const struct scmi_clock_name_get_a2p *parameters;
     struct scmi_clock_name_get_p2a return_values = {
-        .status = (int32_t)SCMI_GENERIC_ERROR
+	.status = (int32_t)SCMI_GENERIC_ERROR
     };
 
     parameters = (const struct scmi_clock_name_get_a2p*)payload;
 
     status = scmi_clock_get_clock_device_entry(
-        service_id, parameters->clock_id, &clock_device);
+	service_id, parameters->clock_id, &clock_device);
     if (status != FWK_SUCCESS) {
-        return_values.status = (int32_t)SCMI_NOT_FOUND;
-        goto exit;
+	return_values.status = (int32_t)SCMI_NOT_FOUND;
+	goto exit;
     }
 
 #ifdef BUILD_HAS_MOD_RESOURCE_PERMS
     status = scmi_clock_permissions_handler(
-        parameters->clock_id,
-        service_id,
-        (unsigned int)MOD_SCMI_CLOCK_NAME_GET);
+	parameters->clock_id,
+	service_id,
+	(unsigned int)MOD_SCMI_CLOCK_NAME_GET);
     if (status != FWK_SUCCESS) {
-        return_values.status = (int32_t)SCMI_DENIED;
-        goto exit;
+	return_values.status = (int32_t)SCMI_DENIED;
+	goto exit;
     }
 #endif
 
     fwk_str_strncpy(
-        return_values.clock_name,
-        fwk_module_get_element_name(clock_device->element_id),
-        sizeof(return_values.clock_name) - 1);
+	return_values.clock_name,
+	fwk_module_get_element_name(clock_device->element_id),
+	sizeof(return_values.clock_name) - 1);
 
     return_values.status = (int32_t)SCMI_SUCCESS;
     response_size = sizeof(return_values);
@@ -1436,9 +1436,9 @@ static int scmi_clock_name_get_handler(f
 exit:
     response_size = sizeof(return_values.status);
     respond_status = scmi_clock_ctx.scmi_api->respond(
-        service_id, &return_values, response_size);
+	service_id, &return_values, response_size);
     if (respond_status != FWK_SUCCESS) {
-        FWK_LOG_DEBUG("[SCMI-CLK] %s @%d", __func__, __LINE__);
+	FWK_LOG_DEBUG("[SCMI-CLK] %s @%d", __func__, __LINE__);
     }
 
     return status;
@@ -1448,17 +1448,17 @@ static int scmi_clock_rate_notify_handle
     const uint32_t *payload)
 {
     struct scmi_clock_rate_notify_p2a return_values = {
-        .status = (int32_t)SCMI_NOT_SUPPORTED,
+	.status = (int32_t)SCMI_NOT_SUPPORTED,
     };
     int respond_status;
 
     respond_status = scmi_clock_ctx.scmi_api->respond(
-        service_id,
-        &return_values,
-        sizeof(return_values.status));
+	service_id,
+	&return_values,
+	sizeof(return_values.status));
 
     if (respond_status != FWK_SUCCESS) {
-        FWK_LOG_DEBUG("[SCMI-CLK] %s @%d", __func__, __LINE__);
+	FWK_LOG_DEBUG("[SCMI-CLK] %s @%d", __func__, __LINE__);
     }
 
     return FWK_SUCCESS;
@@ -1468,17 +1468,17 @@ static int scmi_clock_rate_change_reques
     const uint32_t *payload)
 {
     struct scmi_clock_rate_change_request_notify_p2a return_values = {
-        .status = (int32_t)SCMI_NOT_SUPPORTED,
+	.status = (int32_t)SCMI_NOT_SUPPORTED,
     };
     int respond_status;
 
     respond_status = scmi_clock_ctx.scmi_api->respond(
-        service_id,
-        &return_values,
-        sizeof(return_values.status));
+	service_id,
+	&return_values,
+	sizeof(return_values.status));
 
     if (respond_status != FWK_SUCCESS) {
-        FWK_LOG_DEBUG("[SCMI-CLK] %s @%d", __func__, __LINE__);
+	FWK_LOG_DEBUG("[SCMI-CLK] %s @%d", __func__, __LINE__);
     }
 
     return FWK_SUCCESS;
@@ -1490,7 +1490,7 @@ static int scmi_clock_duty_cycle_get_han
 {
     const struct scmi_clock_duty_cycle_get_a2p *parameters;
     struct scmi_clock_duty_cycle_get_p2a return_values = {
-        .status = (int32_t)SCMI_SUCCESS
+	.status = (int32_t)SCMI_SUCCESS
     };
     const struct mod_scmi_clock_device *clock_device;
     unsigned int agent_id;
@@ -1500,44 +1500,44 @@ static int scmi_clock_duty_cycle_get_han
     parameters = (const struct scmi_clock_duty_cycle_get_a2p*)payload;
 
     status = scmi_clock_get_clock_device_entry(
-        service_id, parameters->clock_id, &clock_device);
+	service_id, parameters->clock_id, &clock_device);
     if (status != FWK_SUCCESS) {
-        return_values.status = (int32_t)SCMI_NOT_FOUND;
-        goto exit;
+	return_values.status = (int32_t)SCMI_NOT_FOUND;
+	goto exit;
     }
 
 #ifdef BUILD_HAS_MOD_RESOURCE_PERMS
     /* Test SET_RATE permission until we have a DUTY_CYCLE permission flag */
     status = scmi_clock_permissions_handler(
-        parameters->clock_id,
-        service_id,
-        (unsigned int)MOD_SCMI_CLOCK_RATE_SET);
+	parameters->clock_id,
+	service_id,
+	(unsigned int)MOD_SCMI_CLOCK_RATE_SET);
     if (status != FWK_SUCCESS) {
-        return_values.status = (int32_t)SCMI_DENIED;
-        goto exit;
+	return_values.status = (int32_t)SCMI_DENIED;
+	goto exit;
     }
 #endif
 
     status = scmi_clock_ctx.scmi_api->get_agent_id(service_id, &agent_id);
     if (status != FWK_SUCCESS) {
-        goto exit;
+	goto exit;
     }
 
     status = scmi_clock_ctx.clock_api->get_duty_cycle(clock_device->element_id,
-                                                      &return_values.numerator,
-                                                      &return_values.denominator);
+						      &return_values.numerator,
+						      &return_values.denominator);
 
     if (status == FWK_E_SUPPORT) {
-        return_values.status = (int32_t)SCMI_NOT_SUPPORTED;
+	return_values.status = (int32_t)SCMI_NOT_SUPPORTED;
     } else if (status != FWK_SUCCESS) {
-        return_values.status = (int32_t)SCMI_GENERIC_ERROR;
+	return_values.status = (int32_t)SCMI_GENERIC_ERROR;
     }
 
 exit:
     response_size = (return_values.status == SCMI_SUCCESS) ?
-        sizeof(return_values) : sizeof(return_values.status);
+	sizeof(return_values) : sizeof(return_values.status);
     return scmi_clock_ctx.scmi_api->respond(
-        service_id, &return_values, response_size);
+	service_id, &return_values, response_size);
 }
 
 /*
@@ -1549,7 +1549,7 @@ static int scmi_clock_round_rate_get_han
 {
     const struct scmi_clock_round_rate_get_a2p *parameters;
     struct scmi_clock_round_rate_get_p2a return_values = {
-        .status = (int32_t)SCMI_SUCCESS,
+	.status = (int32_t)SCMI_SUCCESS,
     };
     const struct mod_scmi_clock_device *clock_device;
     unsigned long input_rate;
@@ -1560,64 +1560,64 @@ static int scmi_clock_round_rate_get_han
     parameters = (const struct scmi_clock_round_rate_get_a2p*)payload;
 
     status = scmi_clock_get_clock_device_entry(
-        service_id,
-        parameters->clock_id,
-        &clock_device);
+	service_id,
+	parameters->clock_id,
+	&clock_device);
 
     if (status != FWK_SUCCESS) {
-        return_values.status = (int32_t)SCMI_NOT_FOUND;
-        goto exit;
+	return_values.status = (int32_t)SCMI_NOT_FOUND;
+	goto exit;
     }
 
 #ifdef BUILD_HAS_MOD_RESOURCE_PERMS
     status = scmi_clock_permissions_handler(
-        parameters->clock_id,
-        service_id,
-        (unsigned int)MOD_SCMI_CLOCK_RATE_SET);
+	parameters->clock_id,
+	service_id,
+	(unsigned int)MOD_SCMI_CLOCK_RATE_SET);
     if (status != FWK_SUCCESS) {
-        return_values.status = (int32_t)SCMI_DENIED;
-        goto exit;
+	return_values.status = (int32_t)SCMI_DENIED;
+	goto exit;
     }
 #endif
 
     if ((parameters->flags & SCMI_CLOCK_RATE_SET_ASYNC_MASK) != 0) {
-        /* Support for async clock set commands not yet implemented */
-        return_values.status = (int32_t)SCMI_NOT_SUPPORTED;
-        goto exit;
+	/* Support for async clock set commands not yet implemented */
+	return_values.status = (int32_t)SCMI_NOT_SUPPORTED;
+	goto exit;
     }
 
     input_rate = parameters->rate[0];
     if (parameters->rate[1]) {
-        return_values.status = (int32_t)SCMI_INVALID_PARAMETERS;
-        goto exit;
+	return_values.status = (int32_t)SCMI_INVALID_PARAMETERS;
+	goto exit;
     }
 
     status = scmi_clock_ctx.clock_api->round_rate(clock_device->element_id,
-                                                  input_rate, &output_rate);
+						  input_rate, &output_rate);
     if (status == FWK_E_SUPPORT) {
-        return_values.status = (int32_t)SCMI_NOT_SUPPORTED;
+	return_values.status = (int32_t)SCMI_NOT_SUPPORTED;
     } else if (status != FWK_SUCCESS) {
-        return_values.status = (int32_t)SCMI_GENERIC_ERROR;
+	return_values.status = (int32_t)SCMI_GENERIC_ERROR;
     } else {
-        return_values.rate[0] = (uint32_t)output_rate;
-        return_values.rate[1] = 0;
-        return_values.status = (int32_t)SCMI_SUCCESS;
+	return_values.rate[0] = (uint32_t)output_rate;
+	return_values.rate[1] = 0;
+	return_values.status = (int32_t)SCMI_SUCCESS;
     }
 
 exit:
 
 
     response_size = (return_values.status == SCMI_SUCCESS) ?
-        sizeof(return_values) : sizeof(return_values.status);
+	sizeof(return_values) : sizeof(return_values.status);
     return scmi_clock_ctx.scmi_api->respond(
-        service_id, &return_values, response_size);
+	service_id, &return_values, response_size);
 }
 
 /*
  * SCMI module -> SCMI clock module interface
  */
 static int scmi_clock_get_scmi_protocol_id(fwk_id_t protocol_id,
-                                           uint8_t *scmi_protocol_id)
+					   uint8_t *scmi_protocol_id)
 {
     *scmi_protocol_id = (uint8_t)MOD_SCMI_PROTOCOL_ID_CLOCK;
 
@@ -1630,25 +1630,25 @@ static int scmi_clock_message_handler(fw
     int32_t return_value;
 
     static_assert(FWK_ARRAY_SIZE(handler_table) ==
-        FWK_ARRAY_SIZE(payload_size_table),
-        "[SCMI] Clock management protocol table sizes not consistent");
+	FWK_ARRAY_SIZE(payload_size_table),
+	"[SCMI] Clock management protocol table sizes not consistent");
     fwk_assert(payload != NULL);
 
     if (message_id >= FWK_ARRAY_SIZE(handler_table)) {
-        return_value = (int32_t)SCMI_NOT_FOUND;
-        goto error;
+	return_value = (int32_t)SCMI_NOT_FOUND;
+	goto error;
     }
 
     if (payload_size != payload_size_table[message_id]) {
-        return_value = (int32_t)SCMI_PROTOCOL_ERROR;
-        goto error;
+	return_value = (int32_t)SCMI_PROTOCOL_ERROR;
+	goto error;
     }
 
     return handler_table[message_id](service_id, payload);
 
 error:
     return scmi_clock_ctx.scmi_api->respond(
-        service_id, &return_value, sizeof(return_value));
+	service_id, &return_value, sizeof(return_value));
 }
 
 static struct mod_scmi_to_protocol_api scmi_clock_mod_scmi_to_protocol_api = {
@@ -1661,14 +1661,14 @@ static struct mod_scmi_to_protocol_api s
  */
 
 static int scmi_clock_init(fwk_id_t module_id, unsigned int element_count,
-                           const void *data)
+			   const void *data)
 {
     int clock_devices;
     const struct mod_scmi_clock_config *config =
-        (const struct mod_scmi_clock_config *)data;
+	(const struct mod_scmi_clock_config *)data;
 
     if ((config == NULL) || (config->agent_table == NULL)) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     scmi_clock_ctx.config = config;
@@ -1677,19 +1677,19 @@ static int scmi_clock_init(fwk_id_t modu
 
     clock_devices = fwk_module_get_element_count(fwk_module_id_clock);
     if (clock_devices == FWK_E_PARAM) {
-        return FWK_E_PANIC;
+	return FWK_E_PANIC;
     }
 
     scmi_clock_ctx.clock_devices = clock_devices;
 
     /* Allocate a table of clock operations */
     scmi_clock_ctx.clock_ops =
-        fwk_mm_calloc((unsigned int)clock_devices,
-        sizeof(struct clock_operations));
+	fwk_mm_calloc((unsigned int)clock_devices,
+	sizeof(struct clock_operations));
 
     /* Initialize table */
     for (unsigned int i = 0; i < (unsigned int)clock_devices; i++) {
-        scmi_clock_ctx.clock_ops[i].service_id = FWK_ID_NONE;
+	scmi_clock_ctx.clock_ops[i].service_id = FWK_ID_NONE;
     }
 
     /* Initialize clock reference counter table */
@@ -1704,35 +1704,35 @@ static int scmi_clock_bind(fwk_id_t id,
     int status;
 
     if (round == 1) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     status = fwk_module_bind(FWK_ID_MODULE(FWK_MODULE_IDX_SCMI),
-        FWK_ID_API(FWK_MODULE_IDX_SCMI, MOD_SCMI_API_IDX_PROTOCOL),
-        &scmi_clock_ctx.scmi_api);
+	FWK_ID_API(FWK_MODULE_IDX_SCMI, MOD_SCMI_API_IDX_PROTOCOL),
+	&scmi_clock_ctx.scmi_api);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
 #ifdef BUILD_HAS_MOD_RESOURCE_PERMS
     status = fwk_module_bind(
-        FWK_ID_MODULE(FWK_MODULE_IDX_RESOURCE_PERMS),
-        FWK_ID_API(FWK_MODULE_IDX_RESOURCE_PERMS, MOD_RES_PERM_RESOURCE_PERMS),
-        &scmi_clock_ctx.res_perms_api);
+	FWK_ID_MODULE(FWK_MODULE_IDX_RESOURCE_PERMS),
+	FWK_ID_API(FWK_MODULE_IDX_RESOURCE_PERMS, MOD_RES_PERM_RESOURCE_PERMS),
+	&scmi_clock_ctx.res_perms_api);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 #endif
 
     return fwk_module_bind(FWK_ID_MODULE(FWK_MODULE_IDX_CLOCK),
-        FWK_ID_API(FWK_MODULE_IDX_CLOCK, 0), &scmi_clock_ctx.clock_api);
+	FWK_ID_API(FWK_MODULE_IDX_CLOCK, 0), &scmi_clock_ctx.clock_api);
 }
 
 static int scmi_clock_process_bind_request(fwk_id_t source_id,
     fwk_id_t target_id, fwk_id_t api_id, const void **api)
 {
     if (!fwk_id_is_equal(source_id, FWK_ID_MODULE(FWK_MODULE_IDX_SCMI))) {
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 
     *api = &scmi_clock_mod_scmi_to_protocol_api;
@@ -1762,69 +1762,69 @@ static int process_request_event(const s
 
     switch (event_id_type) {
     case SCMI_CLOCK_EVENT_IDX_GET_STATE:
-        /* Return the SCMI clock state related to the agent */
-        scmi_clock_idx = scmi_clock_ctx.clock_ops[clock_dev_idx].scmi_clock_idx;
-        status = scmi_clock_ctx.scmi_api->get_agent_id(service_id, &agent_id);
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
-
-        agent_clock_state =
-            scmi_clock_get_agent_clock_state(agent_id, scmi_clock_idx);
-        clock_state = (enum mod_clock_state)agent_clock_state;
-        get_state_respond(
-            params->clock_dev_id, service_id, &clock_state, status);
-        break;
+	/* Return the SCMI clock state related to the agent */
+	scmi_clock_idx = scmi_clock_ctx.clock_ops[clock_dev_idx].scmi_clock_idx;
+	status = scmi_clock_ctx.scmi_api->get_agent_id(service_id, &agent_id);
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
+
+	agent_clock_state =
+	    scmi_clock_get_agent_clock_state(agent_id, scmi_clock_idx);
+	clock_state = (enum mod_clock_state)agent_clock_state;
+	get_state_respond(
+	    params->clock_dev_id, service_id, &clock_state, status);
+	break;
 
     case SCMI_CLOCK_EVENT_IDX_GET_RATE:
-        status = scmi_clock_ctx.clock_api->get_rate(params->clock_dev_id,
-                                                    &rate);
-        if (status != FWK_PENDING) {
-            /* Request completed */
-            get_rate_respond(service_id, &rate, status);
-        }
-        break;
+	status = scmi_clock_ctx.clock_api->get_rate(params->clock_dev_id,
+						    &rate);
+	if (status != FWK_PENDING) {
+	    /* Request completed */
+	    get_rate_respond(service_id, &rate, status);
+	}
+	break;
 
     case SCMI_CLOCK_EVENT_IDX_SET_RATE:
-        set_rate_data = params->request_data.set_rate_data;
+	set_rate_data = params->request_data.set_rate_data;
 
-        rate = (uint64_t)set_rate_data.rate[0] +
-               (((uint64_t)set_rate_data.rate[1]) << 32);
+	rate = (uint64_t)set_rate_data.rate[0] +
+	       (((uint64_t)set_rate_data.rate[1]) << 32);
 
-        status =
-            scmi_clock_ctx.clock_api->set_rate(params->clock_dev_id,
-                                               rate,
-                                               set_rate_data.round_mode);
-        if (status != FWK_PENDING) {
-            /* Request completed */
-            set_request_respond(service_id, status);
-            status = FWK_SUCCESS;
-        }
-        break;
+	status =
+	    scmi_clock_ctx.clock_api->set_rate(params->clock_dev_id,
+					       rate,
+					       set_rate_data.round_mode);
+	if (status != FWK_PENDING) {
+	    /* Request completed */
+	    set_request_respond(service_id, status);
+	    status = FWK_SUCCESS;
+	}
+	break;
 
     case SCMI_CLOCK_EVENT_IDX_SET_STATE:
-        set_state_data = params->request_data.set_state_data;
+	set_state_data = params->request_data.set_state_data;
 
-        status = scmi_clock_ctx.clock_api->set_state(params->clock_dev_id,
-                                                     set_state_data.state);
-        if (status != FWK_PENDING) {
-            /* Request completed */
-            set_request_respond(service_id, status);
-            clock_ops_update_state(clock_dev_idx, status);
-            status = FWK_SUCCESS;
-        }
-        break;
+	status = scmi_clock_ctx.clock_api->set_state(params->clock_dev_id,
+						     set_state_data.state);
+	if (status != FWK_PENDING) {
+	    /* Request completed */
+	    set_request_respond(service_id, status);
+	    clock_ops_update_state(clock_dev_idx, status);
+	    status = FWK_SUCCESS;
+	}
+	break;
 
     default:
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     if (status == FWK_PENDING) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     if (status == FWK_SUCCESS) {
-        clock_ops_set_available(clock_dev_idx);
+	clock_ops_set_available(clock_dev_idx);
     }
 
     return status;
@@ -1833,7 +1833,7 @@ static int process_request_event(const s
 static int process_response_event(const struct fwk_event *event)
 {
     struct mod_clock_resp_params *params =
-        (struct mod_clock_resp_params *)event->params;
+	(struct mod_clock_resp_params *)event->params;
     unsigned int clock_dev_idx;
     fwk_id_t service_id;
     enum mod_clock_state clock_state;
@@ -1844,41 +1844,41 @@ static int process_response_event(const
     service_id = clock_ops_get_service(clock_dev_idx);
 
     if (params->status != FWK_SUCCESS) {
-        request_response(params->status, service_id);
+	request_response(params->status, service_id);
     } else {
-        event_id_type =
-            (enum mod_clock_event_idx)fwk_id_get_event_idx(event->id);
+	event_id_type =
+	    (enum mod_clock_event_idx)fwk_id_get_event_idx(event->id);
 
-        switch (event_id_type) {
-        case MOD_CLOCK_EVENT_IDX_GET_STATE_REQUEST:
-            clock_state = params->value.state;
+	switch (event_id_type) {
+	case MOD_CLOCK_EVENT_IDX_GET_STATE_REQUEST:
+	    clock_state = params->value.state;
 
-            get_state_respond(event->source_id, service_id, &clock_state,
-                FWK_SUCCESS);
+	    get_state_respond(event->source_id, service_id, &clock_state,
+		FWK_SUCCESS);
 
-            break;
+	    break;
 
-        case MOD_CLOCK_EVENT_IDX_GET_RATE_REQUEST:
-            rate = params->value.rate;
+	case MOD_CLOCK_EVENT_IDX_GET_RATE_REQUEST:
+	    rate = params->value.rate;
 
-            get_rate_respond(service_id, &rate, FWK_SUCCESS);
+	    get_rate_respond(service_id, &rate, FWK_SUCCESS);
 
-            break;
+	    break;
 
-        case MOD_CLOCK_EVENT_IDX_SET_RATE_REQUEST:
-            set_request_respond(service_id, FWK_SUCCESS);
+	case MOD_CLOCK_EVENT_IDX_SET_RATE_REQUEST:
+	    set_request_respond(service_id, FWK_SUCCESS);
 
-            break;
+	    break;
 
-        case MOD_CLOCK_EVENT_IDX_SET_STATE_REQUEST:
-            set_request_respond(service_id, FWK_SUCCESS);
-            clock_ops_update_state(clock_dev_idx, FWK_SUCCESS);
+	case MOD_CLOCK_EVENT_IDX_SET_STATE_REQUEST:
+	    set_request_respond(service_id, FWK_SUCCESS);
+	    clock_ops_update_state(clock_dev_idx, FWK_SUCCESS);
 
-            break;
+	    break;
 
-        default:
-            return FWK_E_PARAM;
-        }
+	default:
+	    return FWK_E_PARAM;
+	}
     }
     clock_ops_set_available(clock_dev_idx);
 
@@ -1886,18 +1886,18 @@ static int process_response_event(const
 }
 
 static int scmi_clock_process_event(const struct fwk_event *event,
-                                    struct fwk_event *resp_event)
+				    struct fwk_event *resp_event)
 {
     if (fwk_id_get_module_idx(event->source_id) ==
-        fwk_id_get_module_idx(fwk_module_id_scmi)) {
-        /* Request events */
-        return process_request_event(event);
+	fwk_id_get_module_idx(fwk_module_id_scmi)) {
+	/* Request events */
+	return process_request_event(event);
     }
 
     if (fwk_id_get_module_idx(event->source_id) ==
-        fwk_id_get_module_idx(fwk_module_id_clock)) {
-        /* Responses from Clock HAL */
-        return process_response_event(event);
+	fwk_id_get_module_idx(fwk_module_id_clock)) {
+	/* Responses from Clock HAL */
+	return process_response_event(event);
     }
 
     return FWK_E_PARAM;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_clock/test/config_scmi_clock.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi_clock/test/config_scmi_clock.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_clock/test/config_scmi_clock.h
@@ -30,43 +30,43 @@ enum clock_dev_idx {
 
 static const struct mod_scmi_clock_device agent_device_table_ospm[] = {
     {
-        /* VPU */
-        .element_id =
-            FWK_ID_ELEMENT_INIT(FAKE_MODULE_IDX, CLOCK_DEV_IDX_VPU),
-        .starts_enabled = true,
+	/* VPU */
+	.element_id =
+	    FWK_ID_ELEMENT_INIT(FAKE_MODULE_IDX, CLOCK_DEV_IDX_VPU),
+	.starts_enabled = true,
     },
     {
-        /* DPU */
-        .element_id =
-            FWK_ID_ELEMENT_INIT(FAKE_MODULE_IDX, CLOCK_DEV_IDX_DPU),
-        .starts_enabled = true,
+	/* DPU */
+	.element_id =
+	    FWK_ID_ELEMENT_INIT(FAKE_MODULE_IDX, CLOCK_DEV_IDX_DPU),
+	.starts_enabled = true,
     },
     {
-        /* PIXEL_0 */
-        .element_id =
-            FWK_ID_ELEMENT_INIT(FAKE_MODULE_IDX, CLOCK_DEV_IDX_PIXEL_0),
-        .starts_enabled = true,
+	/* PIXEL_0 */
+	.element_id =
+	    FWK_ID_ELEMENT_INIT(FAKE_MODULE_IDX, CLOCK_DEV_IDX_PIXEL_0),
+	.starts_enabled = true,
     },
     {
-        /* PIXEL_1 */
-        .element_id =
-            FWK_ID_ELEMENT_INIT(FAKE_MODULE_IDX, CLOCK_DEV_IDX_PIXEL_1),
-        .starts_enabled = true,
+	/* PIXEL_1 */
+	.element_id =
+	    FWK_ID_ELEMENT_INIT(FAKE_MODULE_IDX, CLOCK_DEV_IDX_PIXEL_1),
+	.starts_enabled = true,
     },
 };
 
 static const struct mod_scmi_clock_agent agent_table[FAKE_SCMI_AGENT_IDX_COUNT] = {
     [FAKE_SCMI_AGENT_IDX_PSCI] = { 0 /* No access */ },
     [FAKE_SCMI_AGENT_IDX_OSPM] = {
-        .device_table = agent_device_table_ospm,
-        .device_count = FWK_ARRAY_SIZE(agent_device_table_ospm),
+	.device_table = agent_device_table_ospm,
+	.device_count = FWK_ARRAY_SIZE(agent_device_table_ospm),
     },
 };
 
 struct fwk_module_config config_scmi_clock = {
     .data = &((struct mod_scmi_clock_config) {
-        .max_pending_transactions = 0,
-        .agent_table = agent_table,
-        .agent_count = FWK_ARRAY_SIZE(agent_table),
+	.max_pending_transactions = 0,
+	.agent_table = agent_table,
+	.agent_count = FWK_ARRAY_SIZE(agent_table),
     }),
 };
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_clock/test/mocks/Mockmod_scmi_clock_extra.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi_clock/test/mocks/Mockmod_scmi_clock_extra.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_clock/test/mocks/Mockmod_scmi_clock_extra.c
@@ -604,7 +604,7 @@ void CMockExpectParameters_mod_scmi_from
 void CMockExpectParameters_mod_scmi_from_protocol_api_get_agent_id(CMOCK_mod_scmi_from_protocol_api_get_agent_id_CALL_INSTANCE* cmock_call_instance, fwk_id_t service_id, unsigned int* agent_id)
 {
   memcpy((void*)(&cmock_call_instance->Expected_service_id), (void*)(&service_id),
-         sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
+	 sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
   cmock_call_instance->Expected_agent_id = agent_id;
   cmock_call_instance->ReturnThruPtr_agent_id_Used = 0;
 }
@@ -797,7 +797,7 @@ void CMockExpectParameters_mod_scmi_from
 void CMockExpectParameters_mod_scmi_from_protocol_api_get_max_payload_size(CMOCK_mod_scmi_from_protocol_api_get_max_payload_size_CALL_INSTANCE* cmock_call_instance, fwk_id_t service_id, size_t* size)
 {
   memcpy((void*)(&cmock_call_instance->Expected_service_id), (void*)(&service_id),
-         sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
+	 sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
   cmock_call_instance->Expected_size = size;
   cmock_call_instance->ReturnThruPtr_size_Used = 0;
 }
@@ -899,12 +899,12 @@ void CMockExpectParameters_mod_scmi_from
 void CMockExpectParameters_mod_scmi_from_protocol_api_write_payload(CMOCK_mod_scmi_from_protocol_api_write_payload_CALL_INSTANCE* cmock_call_instance, fwk_id_t service_id, size_t offset, const void* payload, size_t size)
 {
   memcpy((void*)(&cmock_call_instance->Expected_service_id), (void*)(&service_id),
-         sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
+	 sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
   memcpy((void*)(&cmock_call_instance->Expected_offset), (void*)(&offset),
-         sizeof(size_t[sizeof(offset) == sizeof(size_t) ? 1 : -1])); /* add size_t to :treat_as_array if this causes an error */
+	 sizeof(size_t[sizeof(offset) == sizeof(size_t) ? 1 : -1])); /* add size_t to :treat_as_array if this causes an error */
   cmock_call_instance->Expected_payload = payload;
   memcpy((void*)(&cmock_call_instance->Expected_size), (void*)(&size),
-         sizeof(size_t[sizeof(size) == sizeof(size_t) ? 1 : -1])); /* add size_t to :treat_as_array if this causes an error */
+	 sizeof(size_t[sizeof(size) == sizeof(size_t) ? 1 : -1])); /* add size_t to :treat_as_array if this causes an error */
 }
 
 void mod_scmi_from_protocol_api_write_payload_CMockExpectAnyArgsAndReturn(UNITY_LINE_TYPE cmock_line, int cmock_to_return)
@@ -991,10 +991,10 @@ void CMockExpectParameters_mod_scmi_from
 void CMockExpectParameters_mod_scmi_from_protocol_api_respond(CMOCK_mod_scmi_from_protocol_api_respond_CALL_INSTANCE* cmock_call_instance, fwk_id_t service_id, const void* payload, size_t size)
 {
   memcpy((void*)(&cmock_call_instance->Expected_service_id), (void*)(&service_id),
-         sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
+	 sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
   cmock_call_instance->Expected_payload = payload;
   memcpy((void*)(&cmock_call_instance->Expected_size), (void*)(&size),
-         sizeof(size_t[sizeof(size) == sizeof(size_t) ? 1 : -1])); /* add size_t to :treat_as_array if this causes an error */
+	 sizeof(size_t[sizeof(size) == sizeof(size_t) ? 1 : -1])); /* add size_t to :treat_as_array if this causes an error */
 }
 
 void mod_scmi_from_protocol_api_respond_CMockExpectAnyArgsAndReturn(UNITY_LINE_TYPE cmock_line, int cmock_to_return)
@@ -1088,12 +1088,12 @@ void CMockExpectParameters_mod_scmi_from
 void CMockExpectParameters_mod_scmi_from_protocol_api_notify(CMOCK_mod_scmi_from_protocol_api_notify_CALL_INSTANCE* cmock_call_instance, fwk_id_t service_id, int protocol_id, int message_id, const void* payload, size_t size)
 {
   memcpy((void*)(&cmock_call_instance->Expected_service_id), (void*)(&service_id),
-         sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
+	 sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
   cmock_call_instance->Expected_protocol_id = protocol_id;
   cmock_call_instance->Expected_message_id = message_id;
   cmock_call_instance->Expected_payload = payload;
   memcpy((void*)(&cmock_call_instance->Expected_size), (void*)(&size),
-         sizeof(size_t[sizeof(size) == sizeof(size_t) ? 1 : -1])); /* add size_t to :treat_as_array if this causes an error */
+	 sizeof(size_t[sizeof(size) == sizeof(size_t) ? 1 : -1])); /* add size_t to :treat_as_array if this causes an error */
 }
 
 void mod_scmi_from_protocol_api_notify_CMockExpectAnyArgs(UNITY_LINE_TYPE cmock_line)
@@ -1197,10 +1197,10 @@ void CMockExpectParameters_mod_scmi_from
   cmock_call_instance->Expected_scmi_protocol_id = scmi_protocol_id;
   cmock_call_instance->Expected_token = token;
   memcpy((void*)(&cmock_call_instance->Expected_service_id), (void*)(&service_id),
-         sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
+	 sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
   cmock_call_instance->Expected_payload = payload;
   memcpy((void*)(&cmock_call_instance->Expected_payload_size), (void*)(&payload_size),
-         sizeof(size_t[sizeof(payload_size) == sizeof(size_t) ? 1 : -1])); /* add size_t to :treat_as_array if this causes an error */
+	 sizeof(size_t[sizeof(payload_size) == sizeof(size_t) ? 1 : -1])); /* add size_t to :treat_as_array if this causes an error */
   cmock_call_instance->Expected_request_ack_by_interrupt = request_ack_by_interrupt;
 }
 
@@ -1277,7 +1277,7 @@ void CMockExpectParameters_mod_scmi_from
 void CMockExpectParameters_mod_scmi_from_protocol_api_response_message_handler(CMOCK_mod_scmi_from_protocol_api_response_message_handler_CALL_INSTANCE* cmock_call_instance, fwk_id_t service_id)
 {
   memcpy((void*)(&cmock_call_instance->Expected_service_id), (void*)(&service_id),
-         sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
+	 sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
 }
 
 void mod_scmi_from_protocol_api_response_message_handler_CMockExpectAnyArgsAndReturn(UNITY_LINE_TYPE cmock_line, int cmock_to_return)
@@ -1384,7 +1384,7 @@ void mod_res_permissions_api_agent_has_p
   cmock_call_instance->ExpectAnyArgsBool = (char)0;
   CMockExpectParameters_mod_res_permissions_api_agent_has_protocol_permission(cmock_call_instance, agent_id, protocol_id);
   memcpy((void*)(&cmock_call_instance->ReturnVal), (void*)(&cmock_to_return),
-         sizeof(enum mod_res_perms_permissions[sizeof(cmock_to_return) == sizeof(enum mod_res_perms_permissions) ? 1 : -1])); /* add enum mod_res_perms_permissions to :treat_as_array if this causes an error */
+	 sizeof(enum mod_res_perms_permissions[sizeof(cmock_to_return) == sizeof(enum mod_res_perms_permissions) ? 1 : -1])); /* add enum mod_res_perms_permissions to :treat_as_array if this causes an error */
 }
 
 void mod_res_permissions_api_agent_has_protocol_permission_AddCallback(CMOCK_mod_res_permissions_api_agent_has_protocol_permission_CALLBACK Callback)
@@ -1470,7 +1470,7 @@ void mod_res_permissions_api_agent_has_m
   cmock_call_instance->ExpectAnyArgsBool = (char)0;
   CMockExpectParameters_mod_res_permissions_api_agent_has_message_permission(cmock_call_instance, agent_id, protocol_id, message_id);
   memcpy((void*)(&cmock_call_instance->ReturnVal), (void*)(&cmock_to_return),
-         sizeof(enum mod_res_perms_permissions[sizeof(cmock_to_return) == sizeof(enum mod_res_perms_permissions) ? 1 : -1])); /* add enum mod_res_perms_permissions to :treat_as_array if this causes an error */
+	 sizeof(enum mod_res_perms_permissions[sizeof(cmock_to_return) == sizeof(enum mod_res_perms_permissions) ? 1 : -1])); /* add enum mod_res_perms_permissions to :treat_as_array if this causes an error */
 }
 
 void mod_res_permissions_api_agent_has_message_permission_AddCallback(CMOCK_mod_res_permissions_api_agent_has_message_permission_CALLBACK Callback)
@@ -1561,7 +1561,7 @@ void mod_res_permissions_api_agent_has_r
   cmock_call_instance->ExpectAnyArgsBool = (char)0;
   CMockExpectParameters_mod_res_permissions_api_agent_has_resource_permission(cmock_call_instance, agent_id, protocol_id, message_id, resource_id);
   memcpy((void*)(&cmock_call_instance->ReturnVal), (void*)(&cmock_to_return),
-         sizeof(enum mod_res_perms_permissions[sizeof(cmock_to_return) == sizeof(enum mod_res_perms_permissions) ? 1 : -1])); /* add enum mod_res_perms_permissions to :treat_as_array if this causes an error */
+	 sizeof(enum mod_res_perms_permissions[sizeof(cmock_to_return) == sizeof(enum mod_res_perms_permissions) ? 1 : -1])); /* add enum mod_res_perms_permissions to :treat_as_array if this causes an error */
 }
 
 void mod_res_permissions_api_agent_has_resource_permission_AddCallback(CMOCK_mod_res_permissions_api_agent_has_resource_permission_CALLBACK Callback)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_clock/test/mod_scmi_clock_extra.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi_clock/test/mod_scmi_clock_extra.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_clock/test/mod_scmi_clock_extra.h
@@ -90,7 +90,7 @@ int mod_scmi_from_protocol_api_get_max_p
  *      errors.
  */
 int mod_scmi_from_protocol_api_write_payload(fwk_id_t service_id, size_t offset,
-                                             const void *payload, size_t size);
+					     const void *payload, size_t size);
 
 /*!
  * \brief Respond to an SCMI message on a service.
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_clock/test/mod_scmi_clock_unit_test.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi_clock/test/mod_scmi_clock_unit_test.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_clock/test/mod_scmi_clock_unit_test.c
@@ -64,17 +64,17 @@ void setUp(void)
 
     /* Allocate a table of clock operations */
     scmi_clock_ctx.clock_ops =
-        fwk_mm_calloc((unsigned int)scmi_clock_ctx.clock_devices,
-        sizeof(struct clock_operations));
+	fwk_mm_calloc((unsigned int)scmi_clock_ctx.clock_devices,
+	sizeof(struct clock_operations));
 
     /* Initialize table */
     for (unsigned int i = 0; i < (unsigned int)scmi_clock_ctx.clock_devices; i++) {
-        scmi_clock_ctx.clock_ops[i].service_id = FWK_ID_NONE;
+	scmi_clock_ctx.clock_ops[i].service_id = FWK_ID_NONE;
     }
 
     scmi_clock_ctx.scmi_api = &from_protocol_api;
     #if defined(BUILD_HAS_MOD_RESOURCE_PERMS)
-        scmi_clock_ctx.res_perms_api = &perm_api;
+	scmi_clock_ctx.res_perms_api = &perm_api;
     #endif
 }
 
@@ -90,7 +90,7 @@ int fwk_put_event_callback(struct fwk_ev
     TEST_ASSERT_EQUAL(0x00000001, params->request_data.set_rate_data.rate[0]);
     TEST_ASSERT_EQUAL(0x00000001, params->request_data.set_rate_data.rate[1]);
     TEST_ASSERT_EQUAL(MOD_CLOCK_ROUND_MODE_NEAREST,
-                      params->request_data.set_rate_data.round_mode);
+		      params->request_data.set_rate_data.round_mode);
 
     return FWK_SUCCESS;
 }
@@ -100,23 +100,23 @@ void test_function_set_rate(void)
     int status;
 
     fwk_id_t service_id =
-        FWK_ID_ELEMENT_INIT(FAKE_MODULE_IDX, FAKE_SCMI_AGENT_IDX_OSPM);
+	FWK_ID_ELEMENT_INIT(FAKE_MODULE_IDX, FAKE_SCMI_AGENT_IDX_OSPM);
 
     unsigned int agent_id = FAKE_SCMI_AGENT_IDX_OSPM;
 
     struct scmi_clock_rate_set_a2p payload = {
-        .flags = SCMI_CLOCK_RATE_SET_ROUND_AUTO_MASK,
-        .clock_id = CLOCK_DEV_IDX_VPU,
-        .rate[0] = 0x00000001,
-        .rate[1] = 0x00000001,
+	.flags = SCMI_CLOCK_RATE_SET_ROUND_AUTO_MASK,
+	.clock_id = CLOCK_DEV_IDX_VPU,
+	.rate[0] = 0x00000001,
+	.rate[1] = 0x00000001,
     };
 
     mod_scmi_from_protocol_api_get_agent_id_ExpectAnyArgsAndReturn(FWK_SUCCESS);
     mod_scmi_from_protocol_api_get_agent_id_ReturnThruPtr_agent_id(&agent_id);
 
     #if defined(BUILD_HAS_MOD_RESOURCE_PERMS)
-        mod_scmi_from_protocol_api_get_agent_id_ExpectAnyArgsAndReturn(FWK_SUCCESS);
-        mod_res_permissions_api_agent_has_resource_permission_ExpectAnyArgsAndReturn(MOD_RES_PERMS_ACCESS_ALLOWED);
+	mod_scmi_from_protocol_api_get_agent_id_ExpectAnyArgsAndReturn(FWK_SUCCESS);
+	mod_res_permissions_api_agent_has_resource_permission_ExpectAnyArgsAndReturn(MOD_RES_PERMS_ACCESS_ALLOWED);
     #endif
 
     fwk_module_is_valid_element_id_ExpectAnyArgsAndReturn(true);
@@ -130,11 +130,11 @@ void test_function_set_rate(void)
     __fwk_put_event_Stub(fwk_put_event_callback);
 
     status = scmi_clock_message_handler(
-                                    (fwk_id_t)MOD_SCMI_PROTOCOL_ID_CLOCK,
-                                    service_id,
-                                    (const uint32_t *)&payload,
-                                    payload_size_table[MOD_SCMI_CLOCK_RATE_SET],
-                                    MOD_SCMI_CLOCK_RATE_SET);
+				    (fwk_id_t)MOD_SCMI_PROTOCOL_ID_CLOCK,
+				    service_id,
+				    (const uint32_t *)&payload,
+				    payload_size_table[MOD_SCMI_CLOCK_RATE_SET],
+				    MOD_SCMI_CLOCK_RATE_SET);
 
     TEST_ASSERT_EQUAL(FWK_SUCCESS, status);
 }
@@ -143,9 +143,9 @@ int scmi_test_main(void)
 {
     UNITY_BEGIN();
     #if defined(BUILD_HAS_MOD_RESOURCE_PERMS)
-        RUN_TEST(test_function_set_rate);
+	RUN_TEST(test_function_set_rate);
     #else
-        RUN_TEST(test_function_set_rate);
+	RUN_TEST(test_function_set_rate);
     #endif
     return UNITY_END();
 }
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_perf/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi_perf/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_perf/CMakeLists.txt
@@ -8,24 +8,24 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_scmi_perf.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_scmi_perf.c")
 
 if(SCP_ENABLE_PLUGIN_HANDLER)
     if(NOT SCP_ENABLE_SCMI_PERF_FAST_CHANNELS)
-        MESSAGE(FATAL_ERROR
-                "ERROR: Plugin-handler requires SCMI-perf FastChannels")
+	MESSAGE(FATAL_ERROR
+		"ERROR: Plugin-handler requires SCMI-perf FastChannels")
     endif()
 
     target_sources(
-        ${SCP_MODULE_TARGET}
-        PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/perf_plugins_handler.c")
+	${SCP_MODULE_TARGET}
+	PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/perf_plugins_handler.c")
 endif()
 
 target_link_libraries(${SCP_MODULE_TARGET} PRIVATE module-dvfs module-scmi
-                                                   module-timer)
+						   module-timer)
 
 if("resource-perms" IN_LIST SCP_MODULES)
     target_link_libraries(${SCP_MODULE_TARGET} PRIVATE module-resource-perms)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_perf/doc/perf_plugins_handler.md
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi_perf/doc/perf_plugins_handler.md
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_perf/doc/perf_plugins_handler.md
@@ -60,15 +60,15 @@ request to DVFS accordingly.
     | scmi-perf +<--+                                  |   DVFS    |
     |           |   |                                  |           |
     +-----+- - -+---v-+         +-----------+          +-----------+
-          |           |         |           |
-          |  plugins  +<------->+  plugin x |
-          |  handler  +<---+    |           |
-          +-----------+    |    +-----------+
-                           |    +-----------+
-                           |    |           |
-                           +--->+  plugin y |
-                                |           |
-                                +-----------+
+	  |           |         |           |
+	  |  plugins  +<------->+  plugin x |
+	  |  handler  +<---+    |           |
+	  +-----------+    |    +-----------+
+			   |    +-----------+
+			   |    |           |
+			   +--->+  plugin y |
+				|           |
+				+-----------+
 
 The machinery relies on SCMI FastChannels as a source of performance requests
 and for its periodic polling. At every polling, the performance plugins are
@@ -120,7 +120,7 @@ This way the plugins will have a snapsho
 the logical domains within a physical domain, and the domain identifier provided
 is as follow:
     FWK_ID_SUB_ELEMENT(FWK_MODULE_IDX_SCMI_PERF,
-        physical_domain, last_logical_domain)
+	physical_domain, last_logical_domain)
 
 For plugins whose view is on physical domains, requests coming from logical
 domains will need to appear as they came from a single physical domain (if
@@ -221,64 +221,64 @@ level/limits can be shared with the rema
 ## Configuration Example 1 (plugin with physical/DVFS domains view)
 
     static const struct mod_scmi_perf_domain_config domains[] = {
-        [DVFS_DOMAIN_0] = {
-            .fast_channels_addr_scp = (uint64_t[]) { ... },
-            .fast_channels_addr_ap = (uint64_t[]) { ... },
-        },
-        ...
-        [DVFS_DOMAIN_n] = {
-            .fast_channels_addr_scp = (uint64_t[]) { ... },
-            .fast_channels_addr_ap = (uint64_t[]) { ... },
-        },
+	[DVFS_DOMAIN_0] = {
+	    .fast_channels_addr_scp = (uint64_t[]) { ... },
+	    .fast_channels_addr_ap = (uint64_t[]) { ... },
+	},
+	...
+	[DVFS_DOMAIN_n] = {
+	    .fast_channels_addr_scp = (uint64_t[]) { ... },
+	    .fast_channels_addr_ap = (uint64_t[]) { ... },
+	},
     };
 
     static const struct mod_scmi_plugin_config plugins_table[] = {
-        [0] = {
-            .id = FWK_ID_MODULE_INIT(FWK_MODULE_IDX_<PLUGIN>),
-            .dom_type = PERF_PLUGIN_DOM_TYPE_PHYSICAL,
-        },
+	[0] = {
+	    .id = FWK_ID_MODULE_INIT(FWK_MODULE_IDX_<PLUGIN>),
+	    .dom_type = PERF_PLUGIN_DOM_TYPE_PHYSICAL,
+	},
     };
 
     struct fwk_module_config config_scmi_perf = {
-        .data = &((struct mod_scmi_perf_config){
-            ...
-            .plugins = plugins_table,
-            .plugins_count = FWK_ARRAY_SIZE(plugins_table)
-        }),
+	.data = &((struct mod_scmi_perf_config){
+	    ...
+	    .plugins = plugins_table,
+	    .plugins_count = FWK_ARRAY_SIZE(plugins_table)
+	}),
     };
 
 ## Configuration Example 2 (plugin with logical/SCMI domains view, 2 logical
     domains, 1 DVFS domain)
 
     static const struct mod_scmi_perf_domain_config domains[] = {
-        [SCMI_PERF_DOMAIN_0] = {
-            .fast_channels_addr_scp = (uint64_t[]) { ... },
-            .fast_channels_addr_ap = (uint64_t[]) { ... },
-            .phy_group_id =
-                FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_DVFS, DVFS_ELEMENT_IDX_0),
-        },
-        ...
-        [SCMI_PERF_DOMAIN_1] = {
-            .fast_channels_addr_scp = (uint64_t[]) { ... },
-            .fast_channels_addr_ap = (uint64_t[]) { ... },
-            .phy_group_id =
-                FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_DVFS, DVFS_ELEMENT_IDX_0),
-        },
+	[SCMI_PERF_DOMAIN_0] = {
+	    .fast_channels_addr_scp = (uint64_t[]) { ... },
+	    .fast_channels_addr_ap = (uint64_t[]) { ... },
+	    .phy_group_id =
+		FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_DVFS, DVFS_ELEMENT_IDX_0),
+	},
+	...
+	[SCMI_PERF_DOMAIN_1] = {
+	    .fast_channels_addr_scp = (uint64_t[]) { ... },
+	    .fast_channels_addr_ap = (uint64_t[]) { ... },
+	    .phy_group_id =
+		FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_DVFS, DVFS_ELEMENT_IDX_0),
+	},
     };
 
     static const struct mod_scmi_plugin_config plugins_table[] = {
-        [0] = {
-            .id = FWK_ID_MODULE_INIT(FWK_MODULE_IDX_<PLUGIN>),
-            .dom_type = PERF_PLUGIN_DOM_TYPE_LOGICAL,
-        },
+	[0] = {
+	    .id = FWK_ID_MODULE_INIT(FWK_MODULE_IDX_<PLUGIN>),
+	    .dom_type = PERF_PLUGIN_DOM_TYPE_LOGICAL,
+	},
     };
 
     struct fwk_module_config config_scmi_perf = {
-        .data = &((struct mod_scmi_perf_config){
-            ...
-            .plugins = plugins_table,
-            .plugins_count = FWK_ARRAY_SIZE(plugins_table)
-        }),
+	.data = &((struct mod_scmi_perf_config){
+	    ...
+	    .plugins = plugins_table,
+	    .plugins_count = FWK_ARRAY_SIZE(plugins_table)
+	}),
     };
 
 Note that a plugin in this case can choose either logical or physical view.
@@ -286,19 +286,19 @@ Note that a plugin in this case can choo
 ## Configuration Example 3 (no plugins, 2 logical domains, 1 DVFS domain)
 
     static const struct mod_scmi_perf_domain_config domains[] = {
-        [SCMI_PERF_DOMAIN_0] = {
-            .fast_channels_addr_scp = (uint64_t[]) { ... },
-            .fast_channels_addr_ap = (uint64_t[]) { ... },
-            .phy_group_id =
-                FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_DVFS, DVFS_ELEMENT_IDX_0),
-        },
-        ...
-        [SCMI_PERF_DOMAIN_1] = {
-            .fast_channels_addr_scp = (uint64_t[]) { ... },
-            .fast_channels_addr_ap = (uint64_t[]) { ... },
-            .phy_group_id =
-                FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_DVFS, DVFS_ELEMENT_IDX_0),
-        },
+	[SCMI_PERF_DOMAIN_0] = {
+	    .fast_channels_addr_scp = (uint64_t[]) { ... },
+	    .fast_channels_addr_ap = (uint64_t[]) { ... },
+	    .phy_group_id =
+		FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_DVFS, DVFS_ELEMENT_IDX_0),
+	},
+	...
+	[SCMI_PERF_DOMAIN_1] = {
+	    .fast_channels_addr_scp = (uint64_t[]) { ... },
+	    .fast_channels_addr_ap = (uint64_t[]) { ... },
+	    .phy_group_id =
+		FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_DVFS, DVFS_ELEMENT_IDX_0),
+	},
     };
 
 This example is for cases when only the logical domains aggregation policy is
@@ -307,67 +307,67 @@ required.
 ## Configuration Example 4 (2 plugins with physical domain view)
 
     static const struct mod_scmi_perf_domain_config domains[] = {
-        [DVFS_DOMAIN_0] = {
-            .fast_channels_addr_scp = (uint64_t[]) { ... },
-            .fast_channels_addr_ap = (uint64_t[]) { ... },
-        },
+	[DVFS_DOMAIN_0] = {
+	    .fast_channels_addr_scp = (uint64_t[]) { ... },
+	    .fast_channels_addr_ap = (uint64_t[]) { ... },
+	},
     };
 
     static const struct mod_scmi_plugin_config plugins_table[] = {
-        [0] = {
-            .id = FWK_ID_MODULE_INIT(FWK_MODULE_IDX_<PLUGIN>),
-            .dom_type = PERF_PLUGIN_DOM_TYPE_PHYSICAL,
-        },
-        [1] = {
-            .id = FWK_ID_MODULE_INIT(FWK_MODULE_IDX_<PLUGIN>),
-            .dom_type = PERF_PLUGIN_DOM_TYPE_PHYSICAL,
-        },
+	[0] = {
+	    .id = FWK_ID_MODULE_INIT(FWK_MODULE_IDX_<PLUGIN>),
+	    .dom_type = PERF_PLUGIN_DOM_TYPE_PHYSICAL,
+	},
+	[1] = {
+	    .id = FWK_ID_MODULE_INIT(FWK_MODULE_IDX_<PLUGIN>),
+	    .dom_type = PERF_PLUGIN_DOM_TYPE_PHYSICAL,
+	},
     };
 
     struct fwk_module_config config_scmi_perf = {
-        .data = &((struct mod_scmi_perf_config){
-            ...
-            .plugins = plugins_table,
-            .plugins_count = FWK_ARRAY_SIZE(plugins_table)
-        }),
+	.data = &((struct mod_scmi_perf_config){
+	    ...
+	    .plugins = plugins_table,
+	    .plugins_count = FWK_ARRAY_SIZE(plugins_table)
+	}),
     };
 
 ## Configuration Example 5 (3 plugins with different domain view)
 
     static const struct mod_scmi_perf_domain_config domains[] = {
-        [DVFS_DOMAIN_0] = {
-            .fast_channels_addr_scp = (uint64_t[]) { ... },
-            .fast_channels_addr_ap = (uint64_t[]) { ... },
-        },
-        ...
-        [DVFS_DOMAIN_n] = { ... },
+	[DVFS_DOMAIN_0] = {
+	    .fast_channels_addr_scp = (uint64_t[]) { ... },
+	    .fast_channels_addr_ap = (uint64_t[]) { ... },
+	},
+	...
+	[DVFS_DOMAIN_n] = { ... },
     };
 
     static const struct mod_scmi_plugin_config plugins_table[] = {
-        [0] = {
-            .id = FWK_ID_MODULE_INIT(FWK_MODULE_IDX_<PLUGIN0>),
-            .dom_type = PERF_PLUGIN_DOM_TYPE_<PHYSICAL/LOGICAL>,
-        },
-        [1] = {
-            .id = FWK_ID_MODULE_INIT(FWK_MODULE_IDX_<PLUGIN1>),
-            .dom_type = PERF_PLUGIN_DOM_TYPE_<PHYSICAL/LOGICAL>,
-        },
-        /*
-         * This plugin id needs to be placed as last entry because its view is
-         * _TYPE_FULL (see restrictions section).
-         */
-        [2] = {
-            .id = FWK_ID_MODULE_INIT(FWK_MODULE_IDX_<PLUGIN2>),
-            .dom_type = PERF_PLUGIN_DOM_TYPE_FULL,
-        },
+	[0] = {
+	    .id = FWK_ID_MODULE_INIT(FWK_MODULE_IDX_<PLUGIN0>),
+	    .dom_type = PERF_PLUGIN_DOM_TYPE_<PHYSICAL/LOGICAL>,
+	},
+	[1] = {
+	    .id = FWK_ID_MODULE_INIT(FWK_MODULE_IDX_<PLUGIN1>),
+	    .dom_type = PERF_PLUGIN_DOM_TYPE_<PHYSICAL/LOGICAL>,
+	},
+	/*
+	 * This plugin id needs to be placed as last entry because its view is
+	 * _TYPE_FULL (see restrictions section).
+	 */
+	[2] = {
+	    .id = FWK_ID_MODULE_INIT(FWK_MODULE_IDX_<PLUGIN2>),
+	    .dom_type = PERF_PLUGIN_DOM_TYPE_FULL,
+	},
     };
 
     struct fwk_module_config config_scmi_perf = {
-        .data = &((struct mod_scmi_perf_config){
-            ...
-            .plugins = plugins_table,
-            .plugins_count = FWK_ARRAY_SIZE(plugins_table)
-        }),
+	.data = &((struct mod_scmi_perf_config){
+	    ...
+	    .plugins = plugins_table,
+	    .plugins_count = FWK_ARRAY_SIZE(plugins_table)
+	}),
     };
 
 NOTE: In this example no.5 the order of plugin callbacks is the following:
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_perf/include/internal/scmi_perf.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi_perf/include/internal/scmi_perf.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_perf/include/internal/scmi_perf.h
@@ -39,10 +39,10 @@ enum scmi_perf_notification_id {
 
 #define SCMI_PERF_PROTOCOL_ATTRIBUTES(POWER_MW, NUM_DOMAINS) \
     ( \
-        (((POWER_MW) << SCMI_PERF_PROTOCOL_ATTRIBUTES_POWER_MW_POS) & \
-            SCMI_PERF_PROTOCOL_ATTRIBUTES_POWER_MW_MASK) | \
-        (((NUM_DOMAINS) << SCMI_PERF_PROTOCOL_ATTRIBUTES_NUM_DOMAINS_POS) & \
-            SCMI_PERF_PROTOCOL_ATTRIBUTES_NUM_DOMAINS_MASK) \
+	(((POWER_MW) << SCMI_PERF_PROTOCOL_ATTRIBUTES_POWER_MW_POS) & \
+	    SCMI_PERF_PROTOCOL_ATTRIBUTES_POWER_MW_MASK) | \
+	(((NUM_DOMAINS) << SCMI_PERF_PROTOCOL_ATTRIBUTES_NUM_DOMAINS_POS) & \
+	    SCMI_PERF_PROTOCOL_ATTRIBUTES_NUM_DOMAINS_MASK) \
     )
 
 struct scmi_perf_protocol_attributes_p2a {
@@ -82,24 +82,24 @@ struct scmi_perf_protocol_attributes_p2a
     (UINT32_C(0x1) << SCMI_PERF_DOMAIN_ATTRIBUTES_FAST_CHANNEL_POS)
 
 #define SCMI_PERF_DOMAIN_ATTRIBUTES(LEVEL_NOTIFY, LIMITS_NOTIFY, \
-                                    CAN_SET_LEVEL, CAN_SET_LIMITS, \
-                                    FAST_CHANNEL) \
+				    CAN_SET_LEVEL, CAN_SET_LIMITS, \
+				    FAST_CHANNEL) \
     ( \
-        (((LEVEL_NOTIFY) << \
-            SCMI_PERF_DOMAIN_ATTRIBUTES_LEVEL_NOTIFY_POS) & \
-            SCMI_PERF_DOMAIN_ATTRIBUTES_LEVEL_NOTIFY_MASK) | \
-        (((LIMITS_NOTIFY) << \
-            SCMI_PERF_DOMAIN_ATTRIBUTES_LIMITS_NOTIFY_POS) & \
-            SCMI_PERF_DOMAIN_ATTRIBUTES_LIMITS_NOTIFY_MASK) | \
-        (((CAN_SET_LEVEL) << \
-            SCMI_PERF_DOMAIN_ATTRIBUTES_CAN_SET_LEVEL_POS) & \
-            SCMI_PERF_DOMAIN_ATTRIBUTES_CAN_SET_LEVEL_MASK) | \
-        (((CAN_SET_LIMITS) << \
-            SCMI_PERF_DOMAIN_ATTRIBUTES_CAN_SET_LIMITS_POS) & \
-            SCMI_PERF_DOMAIN_ATTRIBUTES_CAN_SET_LIMITS_MASK) | \
-        (((FAST_CHANNEL) << \
-            SCMI_PERF_DOMAIN_ATTRIBUTES_FAST_CHANNEL_POS) & \
-            SCMI_PERF_DOMAIN_ATTRIBUTES_FAST_CHANNEL_MASK) \
+	(((LEVEL_NOTIFY) << \
+	    SCMI_PERF_DOMAIN_ATTRIBUTES_LEVEL_NOTIFY_POS) & \
+	    SCMI_PERF_DOMAIN_ATTRIBUTES_LEVEL_NOTIFY_MASK) | \
+	(((LIMITS_NOTIFY) << \
+	    SCMI_PERF_DOMAIN_ATTRIBUTES_LIMITS_NOTIFY_POS) & \
+	    SCMI_PERF_DOMAIN_ATTRIBUTES_LIMITS_NOTIFY_MASK) | \
+	(((CAN_SET_LEVEL) << \
+	    SCMI_PERF_DOMAIN_ATTRIBUTES_CAN_SET_LEVEL_POS) & \
+	    SCMI_PERF_DOMAIN_ATTRIBUTES_CAN_SET_LEVEL_MASK) | \
+	(((CAN_SET_LIMITS) << \
+	    SCMI_PERF_DOMAIN_ATTRIBUTES_CAN_SET_LIMITS_POS) & \
+	    SCMI_PERF_DOMAIN_ATTRIBUTES_CAN_SET_LIMITS_MASK) | \
+	(((FAST_CHANNEL) << \
+	    SCMI_PERF_DOMAIN_ATTRIBUTES_FAST_CHANNEL_POS) & \
+	    SCMI_PERF_DOMAIN_ATTRIBUTES_FAST_CHANNEL_MASK) \
     )
 
 struct scmi_perf_domain_attributes_a2p {
@@ -126,7 +126,7 @@ struct scmi_perf_domain_attributes_p2a {
 #define SCMI_PERF_LEVELS_MAX(MAILBOX_SIZE) \
     ((sizeof(struct scmi_perf_describe_levels_p2a) < MAILBOX_SIZE)  ? \
      ((MAILBOX_SIZE - sizeof(struct scmi_perf_describe_levels_p2a))   \
-        / sizeof(struct scmi_perf_level)) : 0)
+	/ sizeof(struct scmi_perf_level)) : 0)
 
 #define SCMI_PERF_LEVEL_ATTRIBUTES_POS  0
 #define SCMI_PERF_LEVEL_ATTRIBUTES_MASK \
@@ -134,7 +134,7 @@ struct scmi_perf_domain_attributes_p2a {
 
 #define SCMI_PERF_LEVEL_ATTRIBUTES(LATENCY) \
     (((LATENCY) << SCMI_PERF_LEVEL_ATTRIBUTES_POS) & \
-        SCMI_PERF_LEVEL_ATTRIBUTES_MASK)
+	SCMI_PERF_LEVEL_ATTRIBUTES_MASK)
 
 struct scmi_perf_level {
     uint32_t performance_level;
@@ -157,9 +157,9 @@ struct scmi_perf_describe_levels_a2p {
 
 #define SCMI_PERF_NUM_LEVELS(NUM_LEVELS, REMAINING_LEVELS) \
     ((((NUM_LEVELS) << SCMI_PERF_NUM_LEVELS_NUM_LEVELS_POS) & \
-        SCMI_PERF_NUM_LEVELS_NUM_LEVELS_MASK) | \
+	SCMI_PERF_NUM_LEVELS_NUM_LEVELS_MASK) | \
      (((REMAINING_LEVELS) << SCMI_PERF_NUM_LEVELS_REMAINING_LEVELS_POS) & \
-        SCMI_PERF_NUM_LEVELS_REMAINING_LEVELS_MASK))
+	SCMI_PERF_NUM_LEVELS_REMAINING_LEVELS_MASK))
 
 struct scmi_perf_describe_levels_p2a {
     int32_t status;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_perf/include/mod_scmi_perf.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi_perf/include/mod_scmi_perf.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_perf/include/mod_scmi_perf.h
@@ -68,11 +68,11 @@ enum mod_scmi_perf_fast_channel_memory_o
     MOD_SCMI_PERF_FAST_CHANNEL_OFFSET_LEVEL_SET = 0,
     MOD_SCMI_PERF_FAST_CHANNEL_OFFSET_LIMIT_SET = sizeof(uint32_t),
     MOD_SCMI_PERF_FAST_CHANNEL_OFFSET_LEVEL_GET =
-        sizeof(uint32_t) + sizeof(struct mod_scmi_perf_fast_channel_limit),
+	sizeof(uint32_t) + sizeof(struct mod_scmi_perf_fast_channel_limit),
     MOD_SCMI_PERF_FAST_CHANNEL_OFFSET_LIMIT_GET =
-        sizeof(uint32_t) * 2 + sizeof(struct mod_scmi_perf_fast_channel_limit),
+	sizeof(uint32_t) * 2 + sizeof(struct mod_scmi_perf_fast_channel_limit),
     MOD_SCMI_PERF_FAST_CHANNEL_OFFSET_TOTAL = sizeof(uint32_t) * 2 +
-        sizeof(struct mod_scmi_perf_fast_channel_limit) * 2
+	sizeof(struct mod_scmi_perf_fast_channel_limit) * 2
 };
 
 /*!
@@ -258,10 +258,10 @@ struct mod_scmi_perf_updated_api {
      * \param range_max Max allowed performance level.
      */
     void (*notify_limits_updated)(
-        fwk_id_t domain_id,
-        uintptr_t cookie,
-        uint32_t range_min,
-        uint32_t range_max);
+	fwk_id_t domain_id,
+	uintptr_t cookie,
+	uint32_t range_min,
+	uint32_t range_max);
 
     /*!
      * \brief Inform SCMI Perf that the domain level has been updated.
@@ -271,9 +271,9 @@ struct mod_scmi_perf_updated_api {
      * \param level The new performance level of the domain.
      */
     void (*notify_level_updated)(
-        fwk_id_t domain_id,
-        uintptr_t cookie,
-        uint32_t level);
+	fwk_id_t domain_id,
+	uintptr_t cookie,
+	uint32_t level);
 };
 
 /*!
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_perf/src/mod_scmi_perf.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi_perf/src/mod_scmi_perf.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_perf/src/mod_scmi_perf.c
@@ -83,7 +83,7 @@ static int (
     [MOD_SCMI_PROTOCOL_VERSION] = scmi_perf_protocol_version_handler,
     [MOD_SCMI_PROTOCOL_ATTRIBUTES] = scmi_perf_protocol_attributes_handler,
     [MOD_SCMI_PROTOCOL_MESSAGE_ATTRIBUTES] =
-        scmi_perf_protocol_message_attributes_handler,
+	scmi_perf_protocol_message_attributes_handler,
     [MOD_SCMI_PERF_DOMAIN_ATTRIBUTES] = scmi_perf_domain_attributes_handler,
     [MOD_SCMI_PERF_DESCRIBE_LEVELS] = scmi_perf_describe_levels_handler,
     [MOD_SCMI_PERF_LIMITS_SET] = scmi_perf_limits_set_handler,
@@ -103,22 +103,22 @@ static unsigned int payload_size_table[M
     [MOD_SCMI_PROTOCOL_VERSION] = 0,
     [MOD_SCMI_PROTOCOL_ATTRIBUTES] = 0,
     [MOD_SCMI_PROTOCOL_MESSAGE_ATTRIBUTES] =
-        (unsigned int)sizeof(struct scmi_protocol_message_attributes_a2p),
+	(unsigned int)sizeof(struct scmi_protocol_message_attributes_a2p),
     [MOD_SCMI_PERF_DOMAIN_ATTRIBUTES] =
-        (unsigned int)sizeof(struct scmi_perf_domain_attributes_a2p),
+	(unsigned int)sizeof(struct scmi_perf_domain_attributes_a2p),
     [MOD_SCMI_PERF_DESCRIBE_LEVELS] =
-        (unsigned int)sizeof(struct scmi_perf_describe_levels_a2p),
+	(unsigned int)sizeof(struct scmi_perf_describe_levels_a2p),
     [MOD_SCMI_PERF_LEVEL_SET] =
-        (unsigned int)sizeof(struct scmi_perf_level_set_a2p),
+	(unsigned int)sizeof(struct scmi_perf_level_set_a2p),
     [MOD_SCMI_PERF_LEVEL_GET] =
-        (unsigned int)sizeof(struct scmi_perf_level_get_a2p),
+	(unsigned int)sizeof(struct scmi_perf_level_get_a2p),
     [MOD_SCMI_PERF_LIMITS_SET] =
-        (unsigned int)sizeof(struct scmi_perf_limits_set_a2p),
+	(unsigned int)sizeof(struct scmi_perf_limits_set_a2p),
     [MOD_SCMI_PERF_LIMITS_GET] =
-        (unsigned int)sizeof(struct scmi_perf_limits_get_a2p),
+	(unsigned int)sizeof(struct scmi_perf_limits_get_a2p),
 #ifdef BUILD_HAS_SCMI_PERF_FAST_CHANNELS
     [MOD_SCMI_PERF_DESCRIBE_FAST_CHANNEL] =
-        sizeof(struct scmi_perf_describe_fc_a2p),
+	sizeof(struct scmi_perf_describe_fc_a2p),
 #endif
 #ifdef BUILD_HAS_SCMI_NOTIFICATIONS
     [MOD_SCMI_PERF_NOTIFY_LIMITS] = sizeof(struct scmi_perf_notify_limits_a2p),
@@ -130,10 +130,10 @@ static unsigned int payload_size_table[M
 static unsigned int fast_channel_elem_size[] = {
     [MOD_SCMI_PERF_FAST_CHANNEL_LEVEL_SET] = sizeof(uint32_t),
     [MOD_SCMI_PERF_FAST_CHANNEL_LIMIT_SET] =
-        sizeof(struct mod_scmi_perf_fast_channel_limit),
+	sizeof(struct mod_scmi_perf_fast_channel_limit),
     [MOD_SCMI_PERF_FAST_CHANNEL_LEVEL_GET] = sizeof(uint32_t),
     [MOD_SCMI_PERF_FAST_CHANNEL_LIMIT_GET] =
-        sizeof(struct mod_scmi_perf_fast_channel_limit)
+	sizeof(struct mod_scmi_perf_fast_channel_limit)
 };
 #endif
 
@@ -241,7 +241,7 @@ struct scmi_perf_event_parameters {
 
 static const fwk_id_t scmi_perf_get_level =
     FWK_ID_EVENT_INIT(FWK_MODULE_IDX_SCMI_PERF,
-                      SCMI_PERF_EVENT_IDX_LEVEL_GET_REQUEST);
+		      SCMI_PERF_EVENT_IDX_LEVEL_GET_REQUEST);
 
 /*
  * SCMI PERF Helpers
@@ -287,14 +287,14 @@ static inline struct mod_scmi_perf_fast_
     const struct mod_scmi_perf_domain_config *domain)
 {
     return (struct mod_scmi_perf_fast_channel_limit
-                *)((uintptr_t)domain->fast_channels_addr_scp
-                       [MOD_SCMI_PERF_FAST_CHANNEL_LIMIT_SET]);
+		*)((uintptr_t)domain->fast_channels_addr_scp
+		       [MOD_SCMI_PERF_FAST_CHANNEL_LIMIT_SET]);
 }
 static inline uint32_t *get_fc_set_level_addr(
     const struct mod_scmi_perf_domain_config *domain)
 {
     return (uint32_t *)((uintptr_t)domain->fast_channels_addr_scp
-                            [MOD_SCMI_PERF_FAST_CHANNEL_LEVEL_SET]);
+			    [MOD_SCMI_PERF_FAST_CHANNEL_LEVEL_SET]);
 }
 #endif
 
@@ -321,34 +321,34 @@ static int find_opp_for_level(
     limit_max = domain_ctx->level_limits.maximum;
 
     for (i = 0; i < opp_table->opp_count; i++) {
-        opp_level = opp_table->opps[i].level;
+	opp_level = opp_table->opps[i].level;
 
-        if ((use_nearest &&
-             ((opp_level < *level) && (opp_level < limit_max))) ||
-            (!use_nearest && (opp_level != *level))) {
-            /*
-             * The current OPP level is either below the desired level
-             * or not exact match found.
-             */
-            continue;
-        } else {
-            /*
-             * Either found exact match, or the current OPP is above the limit.
-             * Must be within limits.
-             */
-            if ((opp_level > limit_max) && (i > 0)) {
-                i--;
-            }
+	if ((use_nearest &&
+	     ((opp_level < *level) && (opp_level < limit_max))) ||
+	    (!use_nearest && (opp_level != *level))) {
+	    /*
+	     * The current OPP level is either below the desired level
+	     * or not exact match found.
+	     */
+	    continue;
+	} else {
+	    /*
+	     * Either found exact match, or the current OPP is above the limit.
+	     * Must be within limits.
+	     */
+	    if ((opp_level > limit_max) && (i > 0)) {
+		i--;
+	    }
 
-            return opp_for_level_found(level, opp_table, i);
-        }
+	    return opp_for_level_found(level, opp_table, i);
+	}
     }
 
     /* Either not exact match or approximate to the highest level */
     if (use_nearest) {
-        i--;
+	i--;
 
-        return opp_for_level_found(level, opp_table, i);
+	return opp_for_level_found(level, opp_table, i);
     }
 
     return FWK_E_RANGE;
@@ -365,14 +365,14 @@ static int perf_set_level(
     domain_ctx = get_ctx(domain_id);
 
     status = find_opp_for_level(
-        domain_ctx, &perf_level, scmi_perf_ctx.config->approximate_level);
+	domain_ctx, &perf_level, scmi_perf_ctx.config->approximate_level);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     if ((perf_level < domain_ctx->level_limits.minimum) ||
-        (perf_level > domain_ctx->level_limits.maximum)) {
-        return FWK_E_RANGE;
+	(perf_level > domain_ctx->level_limits.maximum)) {
+	return FWK_E_RANGE;
     }
 
     return scmi_perf_ctx.dvfs_api->set_level(domain_id, agent_id, perf_level);
@@ -386,14 +386,14 @@ static int validate_new_limits(
     int status;
 
     if (scmi_perf_ctx.config->approximate_level) {
-        /* When approx level is chosen, a level is always found */
-        return FWK_SUCCESS;
+	/* When approx level is chosen, a level is always found */
+	return FWK_SUCCESS;
     }
 
     limit = limits->minimum;
     status = find_opp_for_level(domain_ctx, &limit, false);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     limit = limits->maximum;
@@ -411,45 +411,45 @@ int perf_set_limits(
     bool needs_new_level = true;
 
     if (limits->minimum > limits->maximum) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     domain_ctx = get_ctx(domain_id);
 
     if ((limits->minimum == domain_ctx->level_limits.minimum) &&
-        (limits->maximum == domain_ctx->level_limits.maximum)) {
-        return FWK_SUCCESS;
+	(limits->maximum == domain_ctx->level_limits.maximum)) {
+	return FWK_SUCCESS;
     }
 
     status = validate_new_limits(domain_ctx, limits);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     /* Adjust opp for new limits */
     if (domain_ctx->curr_level < limits->minimum) {
-        needle = limits->minimum;
+	needle = limits->minimum;
     } else if (domain_ctx->curr_level > limits->maximum) {
-        needle = limits->maximum;
+	needle = limits->maximum;
     } else {
-        /* No level transition necessary */
-        needs_new_level = false;
+	/* No level transition necessary */
+	needs_new_level = false;
     }
 
     scmi_perf_notify_limits_updated(
-        domain_id, limits->minimum, limits->maximum);
+	domain_id, limits->minimum, limits->maximum);
 
     domain_ctx->level_limits.minimum = limits->minimum;
     domain_ctx->level_limits.maximum = limits->maximum;
 
     if (!needs_new_level) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     status = find_opp_for_level(
-        domain_ctx, &needle, scmi_perf_ctx.config->approximate_level);
+	domain_ctx, &needle, scmi_perf_ctx.config->approximate_level);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     return scmi_perf_ctx.dvfs_api->set_level(domain_id, agent_id, needle);
@@ -461,9 +461,9 @@ static void adjust_level_for_limits(
     uint32_t *level)
 {
     if (*level < limits->minimum) {
-        *level = limits->minimum;
+	*level = limits->minimum;
     } else if (*level > limits->maximum) {
-        *level = limits->maximum;
+	*level = limits->maximum;
     }
 }
 
@@ -481,31 +481,31 @@ void perf_eval_performance(
     int status;
 
     if (limits->minimum > limits->maximum) {
-        return;
+	return;
     }
 
     domain_ctx = get_ctx(domain_id);
     adjust_level_for_limits(limits, level);
 
     if ((limits->minimum == domain_ctx->level_limits.minimum) &&
-        (limits->maximum == domain_ctx->level_limits.maximum)) {
-        status = find_opp_for_level(domain_ctx, level, true);
-        if (status != FWK_SUCCESS) {
-            FWK_LOG_DEBUG("[SCMI-PERF] %s @%d", __func__, __LINE__);
-        }
+	(limits->maximum == domain_ctx->level_limits.maximum)) {
+	status = find_opp_for_level(domain_ctx, level, true);
+	if (status != FWK_SUCCESS) {
+	    FWK_LOG_DEBUG("[SCMI-PERF] %s @%d", __func__, __LINE__);
+	}
 
-        return;
+	return;
     }
 
     scmi_perf_notify_limits_updated(
-        domain_id, limits->minimum, limits->maximum);
+	domain_id, limits->minimum, limits->maximum);
 
     domain_ctx->level_limits.minimum = limits->minimum;
     domain_ctx->level_limits.maximum = limits->maximum;
 
     status = find_opp_for_level(domain_ctx, level, true);
     if (status != FWK_SUCCESS) {
-        FWK_LOG_DEBUG("[SCMI-PERF] %s @%d", __func__, __LINE__);
+	FWK_LOG_DEBUG("[SCMI-PERF] %s @%d", __func__, __LINE__);
     }
 }
 #endif
@@ -524,10 +524,10 @@ static int get_perf_domain_id(const uint
      * unnecessary code.
      */
     const struct scmi_perf_limits_get_a2p *parameters =
-        (const struct scmi_perf_limits_get_a2p *)payload;
+	(const struct scmi_perf_limits_get_a2p *)payload;
 
     if (parameters->domain_id >= scmi_perf_ctx.domain_count) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     *domain_id = parameters->domain_id;
@@ -545,30 +545,30 @@ static int scmi_perf_permissions_handler
 
     status = scmi_perf_ctx.scmi_api->get_agent_id(service_id, &agent_id);
     if (status != FWK_SUCCESS) {
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 
     if (message_id < 3) {
-        perms = scmi_perf_ctx.res_perms_api->agent_has_protocol_permission(
-            agent_id, MOD_SCMI_PROTOCOL_ID_PERF);
-        if (perms == MOD_RES_PERMS_ACCESS_ALLOWED) {
-            return FWK_SUCCESS;
-        }
-        return FWK_E_ACCESS;
+	perms = scmi_perf_ctx.res_perms_api->agent_has_protocol_permission(
+	    agent_id, MOD_SCMI_PROTOCOL_ID_PERF);
+	if (perms == MOD_RES_PERMS_ACCESS_ALLOWED) {
+	    return FWK_SUCCESS;
+	}
+	return FWK_E_ACCESS;
     }
 
     status = get_perf_domain_id(payload, &domain_id);
     if (status != FWK_SUCCESS) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     perms = scmi_perf_ctx.res_perms_api->agent_has_resource_permission(
-        agent_id, MOD_SCMI_PROTOCOL_ID_PERF, message_id, domain_id);
+	agent_id, MOD_SCMI_PROTOCOL_ID_PERF, message_id, domain_id);
 
     if (perms == MOD_RES_PERMS_ACCESS_ALLOWED) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     } else {
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 }
 
@@ -579,35 +579,35 @@ static int scmi_perf_permissions_handler
  */
 
 static int scmi_perf_protocol_version_handler(fwk_id_t service_id,
-                                              const uint32_t *payload)
+					      const uint32_t *payload)
 {
     struct scmi_protocol_version_p2a return_values = {
-        .status = (int32_t)SCMI_SUCCESS,
-        .version = SCMI_PROTOCOL_VERSION_PERF,
+	.status = (int32_t)SCMI_SUCCESS,
+	.version = SCMI_PROTOCOL_VERSION_PERF,
     };
 
     return scmi_perf_ctx.scmi_api->respond(
-        service_id, &return_values, sizeof(return_values));
+	service_id, &return_values, sizeof(return_values));
 }
 
 static int scmi_perf_protocol_attributes_handler(fwk_id_t service_id,
-                                                 const uint32_t *payload)
+						 const uint32_t *payload)
 {
 #ifdef BUILD_HAS_MOD_STATISTICS
     int status;
 #endif
     struct scmi_perf_protocol_attributes_p2a return_values = {
-        .status = (int32_t)SCMI_SUCCESS,
-        .attributes =
-            SCMI_PERF_PROTOCOL_ATTRIBUTES(true, scmi_perf_ctx.domain_count),
+	.status = (int32_t)SCMI_SUCCESS,
+	.attributes =
+	    SCMI_PERF_PROTOCOL_ATTRIBUTES(true, scmi_perf_ctx.domain_count),
     };
     uint32_t addr_low = 0, addr_high = 0, len = 0;
 
 #ifdef BUILD_HAS_MOD_STATISTICS
     status = scmi_perf_ctx.stats_api->get_statistics_desc(
-        fwk_module_id_scmi_perf, &addr_low, &addr_high, &len);
+	fwk_module_id_scmi_perf, &addr_low, &addr_high, &len);
     if (status != FWK_SUCCESS) {
-        return_values.status = (int32_t)SCMI_GENERIC_ERROR;
+	return_values.status = (int32_t)SCMI_GENERIC_ERROR;
     }
 #endif
 
@@ -616,44 +616,44 @@ static int scmi_perf_protocol_attributes
     return_values.statistics_address_high = addr_high;
 
     return scmi_perf_ctx.scmi_api->respond(
-        service_id, &return_values, sizeof(return_values));
+	service_id, &return_values, sizeof(return_values));
 }
 
 static int scmi_perf_protocol_message_attributes_handler(fwk_id_t service_id,
-                                                       const uint32_t *payload)
+						       const uint32_t *payload)
 {
     const struct scmi_protocol_message_attributes_a2p *parameters;
     struct scmi_protocol_message_attributes_p2a return_values;
 
     parameters = (const struct scmi_protocol_message_attributes_a2p *)
-                 payload;
+		 payload;
 
     if ((parameters->message_id < FWK_ARRAY_SIZE(handler_table)) &&
-        (handler_table[parameters->message_id] != NULL)) {
-        return_values = (struct scmi_protocol_message_attributes_p2a) {
-            .status = SCMI_SUCCESS,
-        };
+	(handler_table[parameters->message_id] != NULL)) {
+	return_values = (struct scmi_protocol_message_attributes_p2a) {
+	    .status = SCMI_SUCCESS,
+	};
     } else {
-        return_values.status = (int32_t)SCMI_NOT_FOUND;
+	return_values.status = (int32_t)SCMI_NOT_FOUND;
     }
 
     return_values.attributes = 0;
 #ifdef BUILD_HAS_SCMI_PERF_FAST_CHANNELS
     if ((parameters->message_id <= MOD_SCMI_PERF_LEVEL_GET) &&
-        (parameters->message_id >= MOD_SCMI_PERF_LIMITS_SET)) {
-        return_values.attributes = 1; /* Fast Channel available */
+	(parameters->message_id >= MOD_SCMI_PERF_LIMITS_SET)) {
+	return_values.attributes = 1; /* Fast Channel available */
     }
 #endif
 
     return scmi_perf_ctx.scmi_api->respond(
-        service_id,
-        &return_values,
-        (return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
-                                                 sizeof(return_values.status));
+	service_id,
+	&return_values,
+	(return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
+						 sizeof(return_values.status));
 }
 
 static int scmi_perf_domain_attributes_handler(fwk_id_t service_id,
-                                               const uint32_t *payload)
+					       const uint32_t *payload)
 {
     int status, respond_status;
     unsigned int agent_id;
@@ -662,7 +662,7 @@ static int scmi_perf_domain_attributes_h
     fwk_id_t domain_id;
     struct mod_dvfs_opp opp;
     struct scmi_perf_domain_attributes_p2a return_values = {
-        .status = (int32_t)SCMI_GENERIC_ERROR,
+	.status = (int32_t)SCMI_GENERIC_ERROR,
     };
     bool notifications = false;
     bool fast_channels = false;
@@ -671,37 +671,37 @@ static int scmi_perf_domain_attributes_h
 
     /* Validate the domain identifier */
     if (parameters->domain_id >= scmi_perf_ctx.domain_count) {
-        status = FWK_SUCCESS;
-        return_values.status = (int32_t)SCMI_NOT_FOUND;
+	status = FWK_SUCCESS;
+	return_values.status = (int32_t)SCMI_NOT_FOUND;
 
-        goto exit;
+	goto exit;
     }
 
     status = scmi_perf_ctx.scmi_api->get_agent_id(service_id, &agent_id);
     if (status != FWK_SUCCESS) {
-        goto exit;
+	goto exit;
     }
 
 #ifndef BUILD_HAS_MOD_RESOURCE_PERMS
     permissions = ((uint32_t)MOD_SCMI_PERF_PERMS_SET_LIMITS) |
-        ((uint32_t)MOD_SCMI_PERF_PERMS_SET_LEVEL);
+	((uint32_t)MOD_SCMI_PERF_PERMS_SET_LEVEL);
 #else
     status = scmi_perf_permissions_handler(
-        service_id, payload, (unsigned int)MOD_SCMI_PERF_LIMITS_SET);
+	service_id, payload, (unsigned int)MOD_SCMI_PERF_LIMITS_SET);
     if (status == FWK_SUCCESS) {
-        permissions = (uint8_t)MOD_SCMI_PERF_PERMS_SET_LIMITS;
+	permissions = (uint8_t)MOD_SCMI_PERF_PERMS_SET_LIMITS;
     }
     status = scmi_perf_permissions_handler(
-        service_id, payload, (unsigned int)MOD_SCMI_PERF_LEVEL_SET);
+	service_id, payload, (unsigned int)MOD_SCMI_PERF_LEVEL_SET);
     if (status == FWK_SUCCESS) {
-        permissions |= (uint32_t)MOD_SCMI_PERF_PERMS_SET_LEVEL;
+	permissions |= (uint32_t)MOD_SCMI_PERF_PERMS_SET_LEVEL;
     }
 #endif
 
     domain_id = get_dvfs_dependency_id(parameters->domain_id);
     status = scmi_perf_ctx.dvfs_api->get_sustained_opp(domain_id, &opp);
     if (status != FWK_SUCCESS) {
-        goto exit;
+	goto exit;
     }
 
 #ifdef BUILD_HAS_SCMI_NOTIFICATIONS
@@ -709,53 +709,53 @@ static int scmi_perf_domain_attributes_h
 #endif
 #ifdef BUILD_HAS_SCMI_PERF_FAST_CHANNELS
     const struct mod_scmi_perf_domain_config *domain =
-        &(*scmi_perf_ctx.config->domains)[parameters->domain_id];
+	&(*scmi_perf_ctx.config->domains)[parameters->domain_id];
 
     if (domain->fast_channels_addr_scp != NULL) {
-        fast_channels = true;
+	fast_channels = true;
     }
 #endif
     return_values = (struct scmi_perf_domain_attributes_p2a){
-        .status = SCMI_SUCCESS,
-        .attributes = SCMI_PERF_DOMAIN_ATTRIBUTES(
-            (uint32_t)notifications,
-            (uint32_t)notifications,
-            ((permissions & (uint32_t)MOD_SCMI_PERF_PERMS_SET_LEVEL) !=
-             (uint32_t)0) ?
-                1U :
-                0U,
-            ((permissions & (uint32_t)MOD_SCMI_PERF_PERMS_SET_LIMITS) !=
-             (uint32_t)0) ?
-                1U :
-                0U,
-            (uint32_t)fast_channels),
-        .rate_limit = 0, /* Unsupported */
-        .sustained_freq = opp.frequency,
-        .sustained_perf_level = opp.level,
+	.status = SCMI_SUCCESS,
+	.attributes = SCMI_PERF_DOMAIN_ATTRIBUTES(
+	    (uint32_t)notifications,
+	    (uint32_t)notifications,
+	    ((permissions & (uint32_t)MOD_SCMI_PERF_PERMS_SET_LEVEL) !=
+	     (uint32_t)0) ?
+		1U :
+		0U,
+	    ((permissions & (uint32_t)MOD_SCMI_PERF_PERMS_SET_LIMITS) !=
+	     (uint32_t)0) ?
+		1U :
+		0U,
+	    (uint32_t)fast_channels),
+	.rate_limit = 0, /* Unsupported */
+	.sustained_freq = opp.frequency,
+	.sustained_perf_level = opp.level,
     };
 
     /* Copy the domain name into the mailbox */
     fwk_str_strncpy(
-        (char *)return_values.name,
-        fwk_module_get_element_name(domain_id),
-        sizeof(return_values.name) - 1);
+	(char *)return_values.name,
+	fwk_module_get_element_name(domain_id),
+	sizeof(return_values.name) - 1);
 
 exit:
     respond_status = scmi_perf_ctx.scmi_api->respond(
-        service_id,
-        &return_values,
-        (return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
-                                                 sizeof(return_values.status));
+	service_id,
+	&return_values,
+	(return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
+						 sizeof(return_values.status));
 
     if (respond_status != FWK_SUCCESS) {
-        FWK_LOG_DEBUG("[SCMI-PERF] %s @%d", __func__, __LINE__);
+	FWK_LOG_DEBUG("[SCMI-PERF] %s @%d", __func__, __LINE__);
     }
 
     return status;
 }
 
 static int scmi_perf_describe_levels_handler(fwk_id_t service_id,
-                                             const uint32_t *payload)
+					     const uint32_t *payload)
 {
     int status, respond_status;
     size_t max_payload_size;
@@ -768,110 +768,110 @@ static int scmi_perf_describe_levels_han
     struct mod_dvfs_opp opp;
     uint16_t latency;
     struct scmi_perf_describe_levels_p2a return_values = {
-        .status = (int32_t)SCMI_GENERIC_ERROR,
+	.status = (int32_t)SCMI_GENERIC_ERROR,
     };
 
     payload_size = sizeof(return_values);
 
     status = scmi_perf_ctx.scmi_api->get_max_payload_size(service_id,
-                                                          &max_payload_size);
+							  &max_payload_size);
     if (status != FWK_SUCCESS) {
-        goto exit;
+	goto exit;
     }
 
     status = (SCMI_PERF_LEVELS_MAX(max_payload_size) > 0) ?
-        FWK_SUCCESS : FWK_E_SIZE;
+	FWK_SUCCESS : FWK_E_SIZE;
     if (status != FWK_SUCCESS) {
-        goto exit;
+	goto exit;
     }
 
     parameters = (const struct scmi_perf_describe_levels_a2p *)payload;
 
     /* Validate the domain identifier */
     if (parameters->domain_id >= scmi_perf_ctx.domain_count) {
-        return_values.status = (int32_t)SCMI_NOT_FOUND;
+	return_values.status = (int32_t)SCMI_NOT_FOUND;
 
-        goto exit;
+	goto exit;
     }
 
     /* Get the number of operating points for the domain */
     domain_id = get_dependency_id(parameters->domain_id);
     status = scmi_perf_ctx.dvfs_api->get_opp_count(domain_id, &opp_count);
     if (status != FWK_SUCCESS) {
-        goto exit;
+	goto exit;
     }
 
     /* Validate level index */
     level_index = parameters->level_index;
     if (level_index >= opp_count) {
-        return_values.status = (int32_t)SCMI_INVALID_PARAMETERS;
+	return_values.status = (int32_t)SCMI_INVALID_PARAMETERS;
 
-        goto exit;
+	goto exit;
     }
 
     /* Identify the maximum number of performance levels we can send at once */
     if (SCMI_PERF_LEVELS_MAX(max_payload_size) < (opp_count - level_index)) {
-        num_levels = (unsigned int)SCMI_PERF_LEVELS_MAX(max_payload_size);
+	num_levels = (unsigned int)SCMI_PERF_LEVELS_MAX(max_payload_size);
     } else {
-        num_levels = (unsigned int)(opp_count - level_index);
+	num_levels = (unsigned int)(opp_count - level_index);
     }
 
     level_index_max = (level_index + num_levels - 1);
 
     status = scmi_perf_ctx.dvfs_api->get_latency(domain_id, &latency);
     if (status != FWK_SUCCESS) {
-        goto exit;
+	goto exit;
     }
 
     /* Copy DVFS data into returned data structure */
     for (; level_index <= level_index_max; level_index++,
-         payload_size += sizeof(perf_level)) {
+	 payload_size += sizeof(perf_level)) {
 
-        status = scmi_perf_ctx.dvfs_api->get_nth_opp(
-            domain_id, level_index, &opp);
-        if (status != FWK_SUCCESS) {
-            goto exit;
-        }
-
-        if (opp.power != 0) {
-            perf_level.power_cost = opp.power;
-        } else {
-            perf_level.power_cost = opp.voltage;
-        }
-        perf_level.performance_level = opp.level;
-        perf_level.attributes = latency;
-
-        status = scmi_perf_ctx.scmi_api->write_payload(service_id, payload_size,
-            &perf_level, sizeof(perf_level));
-        if (status != FWK_SUCCESS) {
-            goto exit;
-        }
+	status = scmi_perf_ctx.dvfs_api->get_nth_opp(
+	    domain_id, level_index, &opp);
+	if (status != FWK_SUCCESS) {
+	    goto exit;
+	}
+
+	if (opp.power != 0) {
+	    perf_level.power_cost = opp.power;
+	} else {
+	    perf_level.power_cost = opp.voltage;
+	}
+	perf_level.performance_level = opp.level;
+	perf_level.attributes = latency;
+
+	status = scmi_perf_ctx.scmi_api->write_payload(service_id, payload_size,
+	    &perf_level, sizeof(perf_level));
+	if (status != FWK_SUCCESS) {
+	    goto exit;
+	}
     }
 
     return_values = (struct scmi_perf_describe_levels_p2a) {
-        .status = SCMI_SUCCESS,
-        .num_levels = SCMI_PERF_NUM_LEVELS(num_levels,
-            (opp_count - level_index_max - 1))
+	.status = SCMI_SUCCESS,
+	.num_levels = SCMI_PERF_NUM_LEVELS(num_levels,
+	    (opp_count - level_index_max - 1))
     };
 
     status = scmi_perf_ctx.scmi_api->write_payload(service_id, 0,
-        &return_values, sizeof(return_values));
+	&return_values, sizeof(return_values));
 
 exit:
     respond_status = scmi_perf_ctx.scmi_api->respond(
-        service_id,
-        (return_values.status == SCMI_SUCCESS) ? NULL : &return_values.status,
-        (return_values.status == SCMI_SUCCESS) ? payload_size :
-                                                 sizeof(return_values.status));
+	service_id,
+	(return_values.status == SCMI_SUCCESS) ? NULL : &return_values.status,
+	(return_values.status == SCMI_SUCCESS) ? payload_size :
+						 sizeof(return_values.status));
     if (respond_status != FWK_SUCCESS) {
-        FWK_LOG_DEBUG("[SCMI-PERF] %s @%d", __func__, __LINE__);
+	FWK_LOG_DEBUG("[SCMI-PERF] %s @%d", __func__, __LINE__);
     }
 
     return status;
 }
 
 static int scmi_perf_limits_set_handler(fwk_id_t service_id,
-                                        const uint32_t *payload)
+					const uint32_t *payload)
 {
     int status, respond_status;
     unsigned int agent_id;
@@ -879,27 +879,27 @@ static int scmi_perf_limits_set_handler(
     uint32_t range_min, range_max;
     fwk_id_t domain_id;
     struct scmi_perf_limits_set_p2a return_values = {
-        .status = (int32_t)SCMI_GENERIC_ERROR,
+	.status = (int32_t)SCMI_GENERIC_ERROR,
     };
     enum mod_scmi_perf_policy_status policy_status;
 
     parameters = (const struct scmi_perf_limits_set_a2p *)payload;
 
     if (parameters->domain_id >= scmi_perf_ctx.domain_count) {
-        status = FWK_SUCCESS;
-        return_values.status = (int32_t)SCMI_NOT_FOUND;
+	status = FWK_SUCCESS;
+	return_values.status = (int32_t)SCMI_NOT_FOUND;
 
-        goto exit;
+	goto exit;
     }
 
     status = scmi_perf_ctx.scmi_api->get_agent_id(service_id, &agent_id);
     if (status != FWK_SUCCESS) {
-        goto exit;
+	goto exit;
     }
 
     if (parameters->range_min > parameters->range_max) {
-        return_values.status = (int32_t)SCMI_INVALID_PARAMETERS;
-        goto exit;
+	return_values.status = (int32_t)SCMI_INVALID_PARAMETERS;
+	goto exit;
     }
 
     domain_id = get_dependency_id(parameters->domain_id);
@@ -907,48 +907,48 @@ static int scmi_perf_limits_set_handler(
     range_max = parameters->range_max;
 
     status = scmi_perf_limits_set_policy(&policy_status, &range_min,
-        &range_max, agent_id, domain_id);
+	&range_max, agent_id, domain_id);
 
     if (status != FWK_SUCCESS) {
-        return_values.status = (int32_t)SCMI_GENERIC_ERROR;
-        goto exit;
+	return_values.status = (int32_t)SCMI_GENERIC_ERROR;
+	goto exit;
     }
     if (policy_status == MOD_SCMI_PERF_SKIP_MESSAGE_HANDLER) {
-        return_values.status = (int32_t)SCMI_SUCCESS;
-        goto exit;
+	return_values.status = (int32_t)SCMI_SUCCESS;
+	goto exit;
     }
 
     status = perf_set_limits(
-        domain_id,
-        agent_id,
-        &((struct mod_scmi_perf_level_limits){ .minimum = range_min,
-                                               .maximum = range_max }));
+	domain_id,
+	agent_id,
+	&((struct mod_scmi_perf_level_limits){ .minimum = range_min,
+					       .maximum = range_max }));
 
     /*
      * Return immediately to the caller, fire-and-forget.
      */
 
     if ((status == FWK_SUCCESS) || (status == FWK_PENDING)) {
-        return_values.status = (int32_t)SCMI_SUCCESS;
+	return_values.status = (int32_t)SCMI_SUCCESS;
     } else {
-        return_values.status = (int32_t)SCMI_OUT_OF_RANGE;
+	return_values.status = (int32_t)SCMI_OUT_OF_RANGE;
     }
 
 exit:
     respond_status = scmi_perf_ctx.scmi_api->respond(
-        service_id,
-        &return_values,
-        (return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
-                                                 sizeof(return_values.status));
+	service_id,
+	&return_values,
+	(return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
+						 sizeof(return_values.status));
     if (respond_status != FWK_SUCCESS) {
-        FWK_LOG_DEBUG("[SCMI-PERF] %s @%d", __func__, __LINE__);
+	FWK_LOG_DEBUG("[SCMI-PERF] %s @%d", __func__, __LINE__);
     }
 
     return status;
 }
 
 static int scmi_perf_limits_get_handler(fwk_id_t service_id,
-                                        const uint32_t *payload)
+					const uint32_t *payload)
 {
     fwk_id_t domain_id;
     const struct scmi_perf_limits_get_a2p *parameters;
@@ -957,9 +957,9 @@ static int scmi_perf_limits_get_handler(
 
     parameters = (const struct scmi_perf_limits_get_a2p *)payload;
     if (parameters->domain_id >= scmi_perf_ctx.domain_count) {
-        return_values.status = (int32_t)SCMI_NOT_FOUND;
+	return_values.status = (int32_t)SCMI_NOT_FOUND;
 
-        goto exit;
+	goto exit;
     }
 
     domain_id = get_dependency_id(parameters->domain_id);
@@ -971,36 +971,36 @@ static int scmi_perf_limits_get_handler(
 
 exit:
     return scmi_perf_ctx.scmi_api->respond(
-        service_id,
-        &return_values,
-        (return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
-                                                 sizeof(return_values.status));
+	service_id,
+	&return_values,
+	(return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
+						 sizeof(return_values.status));
 }
 
 static int scmi_perf_level_set_handler(fwk_id_t service_id,
-                                       const uint32_t *payload)
+				       const uint32_t *payload)
 {
     int status, respond_status;
     unsigned int agent_id;
     const struct scmi_perf_level_set_a2p *parameters;
     fwk_id_t domain_id;
     struct scmi_perf_level_set_p2a return_values = {
-        .status = (int32_t)SCMI_GENERIC_ERROR,
+	.status = (int32_t)SCMI_GENERIC_ERROR,
     };
     uint32_t perf_level;
     enum mod_scmi_perf_policy_status policy_status;
     parameters = (const struct scmi_perf_level_set_a2p *)payload;
 
     if (parameters->domain_id >= scmi_perf_ctx.domain_count) {
-        status = FWK_SUCCESS;
-        return_values.status = (int32_t)SCMI_NOT_FOUND;
+	status = FWK_SUCCESS;
+	return_values.status = (int32_t)SCMI_NOT_FOUND;
 
-        goto exit;
+	goto exit;
     }
 
     status = scmi_perf_ctx.scmi_api->get_agent_id(service_id, &agent_id);
     if (status != FWK_SUCCESS) {
-        goto exit;
+	goto exit;
     }
 
     /*
@@ -1010,15 +1010,15 @@ static int scmi_perf_level_set_handler(f
     perf_level = parameters->performance_level;
 
     status = scmi_perf_level_set_policy(&policy_status, &perf_level, agent_id,
-        domain_id);
+	domain_id);
 
     if (status != FWK_SUCCESS) {
-        return_values.status = (int32_t)SCMI_GENERIC_ERROR;
-        goto exit;
+	return_values.status = (int32_t)SCMI_GENERIC_ERROR;
+	goto exit;
     }
     if (policy_status == MOD_SCMI_PERF_SKIP_MESSAGE_HANDLER) {
-        return_values.status = (int32_t)SCMI_SUCCESS;
-        goto exit;
+	return_values.status = (int32_t)SCMI_SUCCESS;
+	goto exit;
     }
 
     status = perf_set_level(domain_id, agent_id, perf_level);
@@ -1027,27 +1027,27 @@ static int scmi_perf_level_set_handler(f
      * Return immediately to the caller, fire-and-forget.
      */
     if ((status == FWK_SUCCESS) || (status == FWK_PENDING)) {
-        return_values.status = (int32_t)SCMI_SUCCESS;
+	return_values.status = (int32_t)SCMI_SUCCESS;
     } else if (status == FWK_E_RANGE) {
-        return_values.status = (int32_t)SCMI_OUT_OF_RANGE;
+	return_values.status = (int32_t)SCMI_OUT_OF_RANGE;
     }
 
 exit:
     respond_status = scmi_perf_ctx.scmi_api->respond(
-        service_id,
-        &return_values,
-        (return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
-                                                 sizeof(return_values.status));
+	service_id,
+	&return_values,
+	(return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
+						 sizeof(return_values.status));
 
     if (respond_status != FWK_SUCCESS) {
-        FWK_LOG_DEBUG("[SCMI-PERF] %s @%d", __func__, __LINE__);
+	FWK_LOG_DEBUG("[SCMI-PERF] %s @%d", __func__, __LINE__);
     }
 
     return status;
 }
 
 static int scmi_perf_level_get_handler(fwk_id_t service_id,
-                                       const uint32_t *payload)
+				       const uint32_t *payload)
 {
     int status, respond_status;
     const struct scmi_perf_level_get_a2p *parameters;
@@ -1056,29 +1056,29 @@ static int scmi_perf_level_get_handler(f
 
     parameters = (const struct scmi_perf_level_get_a2p *)payload;
     if (parameters->domain_id >= scmi_perf_ctx.domain_count) {
-        status = FWK_SUCCESS;
-        return_values.status = (int32_t)SCMI_NOT_FOUND;
+	status = FWK_SUCCESS;
+	return_values.status = (int32_t)SCMI_NOT_FOUND;
 
-        goto exit;
+	goto exit;
     }
 
     /* Check if there is already a request pending for this domain */
     if (!fwk_id_is_equal(
-            scmi_perf_ctx
-                .perf_ops_table[fwk_id_get_element_idx(
-                    get_dependency_id(parameters->domain_id))]
-                .service_id,
-            FWK_ID_NONE)) {
-        return_values.status = (int32_t)SCMI_BUSY;
-        status = FWK_SUCCESS;
+	    scmi_perf_ctx
+		.perf_ops_table[fwk_id_get_element_idx(
+		    get_dependency_id(parameters->domain_id))]
+		.service_id,
+	    FWK_ID_NONE)) {
+	return_values.status = (int32_t)SCMI_BUSY;
+	status = FWK_SUCCESS;
 
-        goto exit;
+	goto exit;
     }
 
     /* The get_level request is processed within the event being generated */
     struct fwk_event event = {
-        .target_id = fwk_module_id_scmi_perf,
-        .id = scmi_perf_get_level,
+	.target_id = fwk_module_id_scmi_perf,
+	.id = scmi_perf_get_level,
     };
 
     evt_params = (struct scmi_perf_event_parameters *)event.params;
@@ -1086,26 +1086,26 @@ static int scmi_perf_level_get_handler(f
 
     status = fwk_put_event(&event);
     if (status != FWK_SUCCESS) {
-        return_values.status = (int32_t)SCMI_GENERIC_ERROR;
+	return_values.status = (int32_t)SCMI_GENERIC_ERROR;
 
-        goto exit;
+	goto exit;
     }
 
     /* Store service identifier to indicate there is a pending request */
     scmi_perf_ctx.perf_ops_table[fwk_id_get_element_idx(evt_params->domain_id)]
-        .service_id = service_id;
+	.service_id = service_id;
 
     return FWK_SUCCESS;
 
 exit:
     respond_status = scmi_perf_ctx.scmi_api->respond(
-        service_id,
-        &return_values,
-        (return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
-                                                 sizeof(return_values.status));
+	service_id,
+	&return_values,
+	(return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
+						 sizeof(return_values.status));
 
     if (respond_status != FWK_SUCCESS) {
-        FWK_LOG_DEBUG("[SCMI-PERF] %s @%d", __func__, __LINE__);
+	FWK_LOG_DEBUG("[SCMI-PERF] %s @%d", __func__, __LINE__);
     }
 
     return status;
@@ -1113,138 +1113,138 @@ exit:
 
 #ifdef BUILD_HAS_SCMI_NOTIFICATIONS
 static int scmi_perf_limits_notify(fwk_id_t service_id,
-                                   const uint32_t *payload)
+				   const uint32_t *payload)
 {
     unsigned int agent_id;
     int status, respond_status;
     unsigned int id;
     const struct scmi_perf_notify_limits_a2p *parameters;
     struct scmi_perf_notify_limits_p2a return_values = {
-        .status = (int32_t)SCMI_GENERIC_ERROR,
+	.status = (int32_t)SCMI_GENERIC_ERROR,
     };
 
     parameters = (const struct scmi_perf_notify_limits_a2p *)payload;
     id = parameters->domain_id;
     if (id >= scmi_perf_ctx.domain_count) {
-        status = FWK_SUCCESS;
-        return_values.status = (int32_t)SCMI_NOT_FOUND;
+	status = FWK_SUCCESS;
+	return_values.status = (int32_t)SCMI_NOT_FOUND;
 
-        goto exit;
+	goto exit;
     }
 
     if ((parameters->notify_enable &
-         ~SCMI_PERF_NOTIFY_LIMITS_NOTIFY_ENABLE_MASK) != 0x0) {
-        status = FWK_SUCCESS;
-        return_values.status = (int32_t)SCMI_INVALID_PARAMETERS;
+	 ~SCMI_PERF_NOTIFY_LIMITS_NOTIFY_ENABLE_MASK) != 0x0) {
+	status = FWK_SUCCESS;
+	return_values.status = (int32_t)SCMI_INVALID_PARAMETERS;
 
-        goto exit;
+	goto exit;
     }
 
     status = scmi_perf_ctx.scmi_api->get_agent_id(service_id, &agent_id);
     if (status != FWK_SUCCESS) {
-        goto exit;
+	goto exit;
     }
 
     if (parameters->notify_enable) {
-        status = scmi_perf_ctx.scmi_notification_api
-                     ->scmi_notification_add_subscriber(
-                         MOD_SCMI_PROTOCOL_ID_PERF,
-                         id,
-                         MOD_SCMI_PERF_NOTIFY_LIMITS,
-                         service_id);
+	status = scmi_perf_ctx.scmi_notification_api
+		     ->scmi_notification_add_subscriber(
+			 MOD_SCMI_PROTOCOL_ID_PERF,
+			 id,
+			 MOD_SCMI_PERF_NOTIFY_LIMITS,
+			 service_id);
     } else {
-        status = scmi_perf_ctx.scmi_notification_api
-                     ->scmi_notification_remove_subscriber(
-                         MOD_SCMI_PROTOCOL_ID_PERF,
-                         agent_id,
-                         id,
-                         MOD_SCMI_PERF_NOTIFY_LIMITS);
+	status = scmi_perf_ctx.scmi_notification_api
+		     ->scmi_notification_remove_subscriber(
+			 MOD_SCMI_PROTOCOL_ID_PERF,
+			 agent_id,
+			 id,
+			 MOD_SCMI_PERF_NOTIFY_LIMITS);
     }
     if (status != FWK_SUCCESS) {
-        goto exit;
+	goto exit;
     }
 
     return_values.status = (int32_t)SCMI_SUCCESS;
 
 exit:
     respond_status = scmi_perf_ctx.scmi_api->respond(
-        service_id,
-        &return_values,
-        (return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
-                                                 sizeof(return_values.status));
+	service_id,
+	&return_values,
+	(return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
+						 sizeof(return_values.status));
 
     if (respond_status != FWK_SUCCESS) {
-        FWK_LOG_DEBUG("[SCMI-PERF] %s @%d", __func__, __LINE__);
+	FWK_LOG_DEBUG("[SCMI-PERF] %s @%d", __func__, __LINE__);
     }
 
     return status;
 }
 
 static int scmi_perf_level_notify(fwk_id_t service_id,
-                                   const uint32_t *payload)
+				   const uint32_t *payload)
 {
     unsigned int agent_id;
     int status, respond_status;
     unsigned int id;
     const struct scmi_perf_notify_level_a2p *parameters;
     struct scmi_perf_notify_level_p2a return_values = {
-        .status = (int32_t)SCMI_GENERIC_ERROR,
+	.status = (int32_t)SCMI_GENERIC_ERROR,
     };
 
     parameters = (const struct scmi_perf_notify_level_a2p *)payload;
     id = parameters->domain_id;
     if (id >= scmi_perf_ctx.domain_count) {
-        status = FWK_SUCCESS;
-        return_values.status = (int32_t)SCMI_NOT_FOUND;
+	status = FWK_SUCCESS;
+	return_values.status = (int32_t)SCMI_NOT_FOUND;
 
-        goto exit;
+	goto exit;
     }
 
     if ((parameters->notify_enable &
-         ~SCMI_PERF_NOTIFY_LEVEL_NOTIFY_ENABLE_MASK) != 0x0) {
-        status = FWK_SUCCESS;
-        return_values.status = (int32_t)SCMI_INVALID_PARAMETERS;
+	 ~SCMI_PERF_NOTIFY_LEVEL_NOTIFY_ENABLE_MASK) != 0x0) {
+	status = FWK_SUCCESS;
+	return_values.status = (int32_t)SCMI_INVALID_PARAMETERS;
 
-        goto exit;
+	goto exit;
     }
 
     id = parameters->domain_id;
 
     status = scmi_perf_ctx.scmi_api->get_agent_id(service_id, &agent_id);
     if (status != FWK_SUCCESS) {
-        goto exit;
+	goto exit;
     }
 
     if (parameters->notify_enable) {
-        status = scmi_perf_ctx.scmi_notification_api
-                     ->scmi_notification_add_subscriber(
-                         MOD_SCMI_PROTOCOL_ID_PERF,
-                         id,
-                         MOD_SCMI_PERF_NOTIFY_LEVEL,
-                         service_id);
+	status = scmi_perf_ctx.scmi_notification_api
+		     ->scmi_notification_add_subscriber(
+			 MOD_SCMI_PROTOCOL_ID_PERF,
+			 id,
+			 MOD_SCMI_PERF_NOTIFY_LEVEL,
+			 service_id);
     } else {
-        status = scmi_perf_ctx.scmi_notification_api
-                     ->scmi_notification_remove_subscriber(
-                         MOD_SCMI_PROTOCOL_ID_PERF,
-                         agent_id,
-                         id,
-                         MOD_SCMI_PERF_NOTIFY_LEVEL);
+	status = scmi_perf_ctx.scmi_notification_api
+		     ->scmi_notification_remove_subscriber(
+			 MOD_SCMI_PROTOCOL_ID_PERF,
+			 agent_id,
+			 id,
+			 MOD_SCMI_PERF_NOTIFY_LEVEL);
     }
     if (status != FWK_SUCCESS) {
-        goto exit;
+	goto exit;
     }
 
     return_values.status = (int32_t)SCMI_SUCCESS;
 
 exit:
     respond_status = scmi_perf_ctx.scmi_api->respond(
-        service_id,
-        &return_values,
-        (return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
-                                                 sizeof(return_values.status));
+	service_id,
+	&return_values,
+	(return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
+						 sizeof(return_values.status));
 
     if (respond_status != FWK_SUCCESS) {
-        FWK_LOG_DEBUG("[SCMI-PERF] %s @%d", __func__, __LINE__);
+	FWK_LOG_DEBUG("[SCMI-PERF] %s @%d", __func__, __LINE__);
     }
 
     return status;
@@ -1258,12 +1258,12 @@ exit:
  * implementation.
  */
 static int scmi_perf_describe_fast_channels(fwk_id_t service_id,
-                                            const uint32_t *payload)
+					    const uint32_t *payload)
 {
     const struct mod_scmi_perf_domain_config *domain;
     const struct scmi_perf_describe_fc_a2p *parameters;
     struct scmi_perf_describe_fc_p2a return_values = {
-        .status = (int32_t)SCMI_GENERIC_ERROR,
+	.status = (int32_t)SCMI_GENERIC_ERROR,
     };
     uint32_t chan_size, chan_index;
     enum scmi_perf_command_id message_id;
@@ -1272,76 +1272,76 @@ static int scmi_perf_describe_fast_chann
 
     /* Validate the domain identifier */
     if (parameters->domain_id >= scmi_perf_ctx.domain_count) {
-        return_values.status = (int32_t)SCMI_NOT_FOUND;
+	return_values.status = (int32_t)SCMI_NOT_FOUND;
 
-        goto exit;
+	goto exit;
     }
 
     domain = &(*scmi_perf_ctx.config->domains)[parameters->domain_id];
 
     if (domain->fast_channels_addr_scp == NULL) {
-        return_values.status = (int32_t)SCMI_NOT_SUPPORTED;
+	return_values.status = (int32_t)SCMI_NOT_SUPPORTED;
 
-        goto exit;
+	goto exit;
     }
 
     if (parameters->message_id >= MOD_SCMI_PERF_COMMAND_COUNT) {
-        return_values.status = (int32_t)SCMI_NOT_FOUND;
+	return_values.status = (int32_t)SCMI_NOT_FOUND;
 
-        goto exit;
+	goto exit;
     }
 
     message_id = (enum scmi_perf_command_id)parameters->message_id;
 
     switch (message_id) {
     case MOD_SCMI_PERF_LEVEL_GET:
-        chan_index = (uint32_t)MOD_SCMI_PERF_FAST_CHANNEL_LEVEL_GET;
-        chan_size =
-            fast_channel_elem_size[MOD_SCMI_PERF_FAST_CHANNEL_LEVEL_GET];
-        break;
+	chan_index = (uint32_t)MOD_SCMI_PERF_FAST_CHANNEL_LEVEL_GET;
+	chan_size =
+	    fast_channel_elem_size[MOD_SCMI_PERF_FAST_CHANNEL_LEVEL_GET];
+	break;
 
     case MOD_SCMI_PERF_LEVEL_SET:
-        chan_index = (uint32_t)MOD_SCMI_PERF_FAST_CHANNEL_LEVEL_SET;
-        chan_size =
-            fast_channel_elem_size[MOD_SCMI_PERF_FAST_CHANNEL_LEVEL_SET];
-        break;
+	chan_index = (uint32_t)MOD_SCMI_PERF_FAST_CHANNEL_LEVEL_SET;
+	chan_size =
+	    fast_channel_elem_size[MOD_SCMI_PERF_FAST_CHANNEL_LEVEL_SET];
+	break;
 
     case MOD_SCMI_PERF_LIMITS_SET:
-        chan_index = (uint32_t)MOD_SCMI_PERF_FAST_CHANNEL_LIMIT_SET;
-        chan_size =
-            fast_channel_elem_size[MOD_SCMI_PERF_FAST_CHANNEL_LIMIT_SET];
-        break;
+	chan_index = (uint32_t)MOD_SCMI_PERF_FAST_CHANNEL_LIMIT_SET;
+	chan_size =
+	    fast_channel_elem_size[MOD_SCMI_PERF_FAST_CHANNEL_LIMIT_SET];
+	break;
 
     case MOD_SCMI_PERF_LIMITS_GET:
-        chan_index = (uint32_t)MOD_SCMI_PERF_FAST_CHANNEL_LIMIT_GET;
-        chan_size =
-            fast_channel_elem_size[MOD_SCMI_PERF_FAST_CHANNEL_LIMIT_GET];
-        break;
+	chan_index = (uint32_t)MOD_SCMI_PERF_FAST_CHANNEL_LIMIT_GET;
+	chan_size =
+	    fast_channel_elem_size[MOD_SCMI_PERF_FAST_CHANNEL_LIMIT_GET];
+	break;
 
     default:
-        return_values.status = (int32_t)SCMI_NOT_SUPPORTED;
-        goto exit;
+	return_values.status = (int32_t)SCMI_NOT_SUPPORTED;
+	goto exit;
     }
     if (domain->fast_channels_addr_ap == NULL ||
-        domain->fast_channels_addr_ap[chan_index] == 0x0) {
-        return_values.status = (int32_t)SCMI_NOT_SUPPORTED;
-        goto exit;
+	domain->fast_channels_addr_ap[chan_index] == 0x0) {
+	return_values.status = (int32_t)SCMI_NOT_SUPPORTED;
+	goto exit;
     }
     return_values.status = (int32_t)SCMI_SUCCESS;
     return_values.attributes = 0; /* Doorbell not supported */
     return_values.rate_limit = scmi_perf_ctx.fast_channels_rate_limit;
     return_values.chan_addr_low =
-        (uint32_t)(domain->fast_channels_addr_ap[chan_index] & ~0UL);
+	(uint32_t)(domain->fast_channels_addr_ap[chan_index] & ~0UL);
     return_values.chan_addr_high =
-        (uint32_t)(domain->fast_channels_addr_ap[chan_index] >> 32);
+	(uint32_t)(domain->fast_channels_addr_ap[chan_index] >> 32);
     return_values.chan_size = chan_size;
 
 exit:
     return scmi_perf_ctx.scmi_api->respond(
-        service_id,
-        &return_values,
-        (return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
-                                                 sizeof(return_values.status));
+	service_id,
+	&return_values,
+	(return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
+						 sizeof(return_values.status));
 }
 
 /*
@@ -1351,21 +1351,21 @@ static void fast_channel_callback(uintpt
 {
     int status;
     struct fwk_event_light event = (struct fwk_event_light){
-        .id = FWK_ID_EVENT_INIT(
-            FWK_MODULE_IDX_SCMI_PERF,
-            SCMI_PERF_EVENT_IDX_FAST_CHANNELS_PROCESS),
-        .source_id = FWK_ID_MODULE_INIT(FWK_MODULE_IDX_SCMI_PERF),
-        .target_id = FWK_ID_MODULE_INIT(FWK_MODULE_IDX_SCMI_PERF),
+	.id = FWK_ID_EVENT_INIT(
+	    FWK_MODULE_IDX_SCMI_PERF,
+	    SCMI_PERF_EVENT_IDX_FAST_CHANNELS_PROCESS),
+	.source_id = FWK_ID_MODULE_INIT(FWK_MODULE_IDX_SCMI_PERF),
+	.target_id = FWK_ID_MODULE_INIT(FWK_MODULE_IDX_SCMI_PERF),
     };
 
     status = fwk_put_event(&event);
     if (status != FWK_SUCCESS) {
-        FWK_LOG_ERR("[SCMI-PERF] Error creating FC process event.");
-        return;
+	FWK_LOG_ERR("[SCMI-PERF] Error creating FC process event.");
+	return;
     }
 
     if (scmi_perf_ctx.fch_pending_req_count > 0) {
-        FWK_LOG_INFO("[SCMI-PERF] Multiple FC events pending");
+	FWK_LOG_INFO("[SCMI-PERF] Multiple FC events pending");
     }
 
     scmi_perf_ctx.fch_pending_req_count++;
@@ -1385,129 +1385,129 @@ static void fast_channels_process(void)
     struct fc_perf_update update;
 
     for (i = 0; i < scmi_perf_ctx.domain_count; i++) {
-        domain = &(*scmi_perf_ctx.config->domains)[i];
-        if (domain->fast_channels_addr_scp != NULL) {
-            set_limit = get_fc_set_limit_addr(domain);
-            set_level = get_fc_set_level_addr(domain);
-
-            domain_ctx = &scmi_perf_ctx.domain_ctx_table[i];
-
-            if (set_limit != NULL) {
-                tmax = set_limit->range_max;
-                tmin = set_limit->range_min;
-            } else {
-                tmax = domain_ctx->level_limits.maximum;
-                tmin = domain_ctx->level_limits.minimum;
-            }
-
-            tlevel = (set_level != NULL) ? *set_level : domain_ctx->curr_level;
-
-            update = (struct fc_perf_update){
-                .domain_id = get_dependency_id(i),
-                .level = tlevel,
-                .max_limit = tmax,
-                .min_limit = tmin,
-            };
+	domain = &(*scmi_perf_ctx.config->domains)[i];
+	if (domain->fast_channels_addr_scp != NULL) {
+	    set_limit = get_fc_set_limit_addr(domain);
+	    set_level = get_fc_set_level_addr(domain);
+
+	    domain_ctx = &scmi_perf_ctx.domain_ctx_table[i];
+
+	    if (set_limit != NULL) {
+		tmax = set_limit->range_max;
+		tmin = set_limit->range_min;
+	    } else {
+		tmax = domain_ctx->level_limits.maximum;
+		tmin = domain_ctx->level_limits.minimum;
+	    }
+
+	    tlevel = (set_level != NULL) ? *set_level : domain_ctx->curr_level;
+
+	    update = (struct fc_perf_update){
+		.domain_id = get_dependency_id(i),
+		.level = tlevel,
+		.max_limit = tmax,
+		.min_limit = tmin,
+	    };
 
-            perf_plugins_handler_update(i, &update);
-        }
+	    perf_plugins_handler_update(i, &update);
+	}
     }
 
     for (i = 0; i < scmi_perf_ctx.domain_count; i++) {
-        domain = &(*scmi_perf_ctx.config->domains)[i];
-        if (domain->fast_channels_addr_scp != NULL) {
-            set_limit = get_fc_set_limit_addr(domain);
-            set_level = get_fc_set_level_addr(domain);
-
-            domain_ctx = &scmi_perf_ctx.domain_ctx_table[i];
-
-            if (set_limit != NULL) {
-                tmax = set_limit->range_max;
-                tmin = set_limit->range_min;
-            } else {
-                tmax = domain_ctx->level_limits.maximum;
-                tmin = domain_ctx->level_limits.minimum;
-            }
-
-            tlevel = (set_level != NULL) ? *set_level : domain_ctx->curr_level;
-
-            update = (struct fc_perf_update){
-                .domain_id = get_dependency_id(i),
-                .level = tlevel,
-                .max_limit = tmax,
-                .min_limit = tmin,
-            };
-
-            perf_plugins_handler_get(i, &update);
-
-            tlevel = update.level;
-            tmax = update.adj_max_limit;
-            tmin = update.adj_min_limit;
-
-            perf_eval_performance(
-                FWK_ID_ELEMENT(FWK_MODULE_IDX_SCMI_PERF, i),
-                &((struct mod_scmi_perf_level_limits){
-                    .minimum = tmin,
-                    .maximum = tmax,
-                }),
-                &tlevel);
-
-            status = scmi_perf_ctx.dvfs_api->set_level(
-                get_dependency_id(i), 0, tlevel);
-            if (status != FWK_SUCCESS) {
-                FWK_LOG_DEBUG("[SCMI-PERF] %s @%d", __func__, __LINE__);
-            }
+	domain = &(*scmi_perf_ctx.config->domains)[i];
+	if (domain->fast_channels_addr_scp != NULL) {
+	    set_limit = get_fc_set_limit_addr(domain);
+	    set_level = get_fc_set_level_addr(domain);
+
+	    domain_ctx = &scmi_perf_ctx.domain_ctx_table[i];
+
+	    if (set_limit != NULL) {
+		tmax = set_limit->range_max;
+		tmin = set_limit->range_min;
+	    } else {
+		tmax = domain_ctx->level_limits.maximum;
+		tmin = domain_ctx->level_limits.minimum;
+	    }
+
+	    tlevel = (set_level != NULL) ? *set_level : domain_ctx->curr_level;
+
+	    update = (struct fc_perf_update){
+		.domain_id = get_dependency_id(i),
+		.level = tlevel,
+		.max_limit = tmax,
+		.min_limit = tmin,
+	    };
+
+	    perf_plugins_handler_get(i, &update);
+
+	    tlevel = update.level;
+	    tmax = update.adj_max_limit;
+	    tmin = update.adj_min_limit;
+
+	    perf_eval_performance(
+		FWK_ID_ELEMENT(FWK_MODULE_IDX_SCMI_PERF, i),
+		&((struct mod_scmi_perf_level_limits){
+		    .minimum = tmin,
+		    .maximum = tmax,
+		}),
+		&tlevel);
+
+	    status = scmi_perf_ctx.dvfs_api->set_level(
+		get_dependency_id(i), 0, tlevel);
+	    if (status != FWK_SUCCESS) {
+		FWK_LOG_DEBUG("[SCMI-PERF] %s @%d", __func__, __LINE__);
+	    }
 
 #    else
 
     for (i = 0; i < scmi_perf_ctx.domain_count; i++) {
-        domain = &(*scmi_perf_ctx.config->domains)[i];
-        if (domain->fast_channels_addr_scp != NULL) {
-            set_limit = get_fc_set_limit_addr(domain);
-            set_level = get_fc_set_level_addr(domain);
-
-            domain_ctx = &scmi_perf_ctx.domain_ctx_table[i];
-
-            if (set_limit != NULL) {
-                tmax = set_limit->range_max;
-                tmin = set_limit->range_min;
-            } else {
-                tmax = domain_ctx->level_limits.maximum;
-                tmin = domain_ctx->level_limits.minimum;
-            }
-
-            tlevel = (set_level != NULL) ? *set_level : domain_ctx->curr_level;
-
-            /*
-             * Check for set_level
-             */
-            if (set_level != NULL && tlevel > 0) {
-                status = perf_set_level(get_dependency_id(i), 0, tlevel);
-                if (status != FWK_SUCCESS) {
-                    FWK_LOG_DEBUG("[SCMI-PERF] %s @%d", __func__, __LINE__);
-                }
-            }
-            if (set_limit != NULL) {
-                if ((tmax == 0) && (tmin == 0)) {
-                    continue;
-                }
-                status = perf_set_limits(
-                    get_dependency_id(i),
-                    0,
-                    &((struct mod_scmi_perf_level_limits){
-                        .minimum = tmin,
-                        .maximum = tmax,
-                    }));
-                if (status != FWK_SUCCESS) {
-                    FWK_LOG_DEBUG("[SCMI-PERF] %s @%d", __func__, __LINE__);
-                }
-            }
+	domain = &(*scmi_perf_ctx.config->domains)[i];
+	if (domain->fast_channels_addr_scp != NULL) {
+	    set_limit = get_fc_set_limit_addr(domain);
+	    set_level = get_fc_set_level_addr(domain);
+
+	    domain_ctx = &scmi_perf_ctx.domain_ctx_table[i];
+
+	    if (set_limit != NULL) {
+		tmax = set_limit->range_max;
+		tmin = set_limit->range_min;
+	    } else {
+		tmax = domain_ctx->level_limits.maximum;
+		tmin = domain_ctx->level_limits.minimum;
+	    }
+
+	    tlevel = (set_level != NULL) ? *set_level : domain_ctx->curr_level;
+
+	    /*
+	     * Check for set_level
+	     */
+	    if (set_level != NULL && tlevel > 0) {
+		status = perf_set_level(get_dependency_id(i), 0, tlevel);
+		if (status != FWK_SUCCESS) {
+		    FWK_LOG_DEBUG("[SCMI-PERF] %s @%d", __func__, __LINE__);
+		}
+	    }
+	    if (set_limit != NULL) {
+		if ((tmax == 0) && (tmin == 0)) {
+		    continue;
+		}
+		status = perf_set_limits(
+		    get_dependency_id(i),
+		    0,
+		    &((struct mod_scmi_perf_level_limits){
+			.minimum = tmin,
+			.maximum = tmax,
+		    }));
+		if (status != FWK_SUCCESS) {
+		    FWK_LOG_DEBUG("[SCMI-PERF] %s @%d", __func__, __LINE__);
+		}
+	    }
 
 #    endif
-        }
+	}
     }
     if (scmi_perf_ctx.fch_pending_req_count > 0) {
-        scmi_perf_ctx.fch_pending_req_count--;
+	scmi_perf_ctx.fch_pending_req_count--;
     }
 }
 
@@ -1544,7 +1544,7 @@ FWK_WEAK int scmi_perf_level_set_policy(
  */
 
 static int scmi_perf_get_scmi_protocol_id(fwk_id_t protocol_id,
-                                          uint8_t *scmi_protocol_id)
+					  uint8_t *scmi_protocol_id)
 {
     *scmi_protocol_id = (uint8_t)MOD_SCMI_PROTOCOL_ID_PERF;
 
@@ -1560,34 +1560,34 @@ static int scmi_perf_message_handler(fwk
 #endif
 
     static_assert(FWK_ARRAY_SIZE(handler_table) ==
-        FWK_ARRAY_SIZE(payload_size_table),
-        "[SCMI] Performance management protocol table sizes not consistent");
+	FWK_ARRAY_SIZE(payload_size_table),
+	"[SCMI] Performance management protocol table sizes not consistent");
     fwk_assert(payload != NULL);
 
     if (message_id >= FWK_ARRAY_SIZE(handler_table)) {
-        return_value = (int32_t)SCMI_NOT_FOUND;
-        goto error;
+	return_value = (int32_t)SCMI_NOT_FOUND;
+	goto error;
     }
 
     if (handler_table[message_id] == NULL) {
-        return_value = (int32_t)SCMI_NOT_SUPPORTED;
-        goto error;
+	return_value = (int32_t)SCMI_NOT_SUPPORTED;
+	goto error;
     }
 
     if (payload_size != payload_size_table[message_id]) {
-        return_value = (int32_t)SCMI_PROTOCOL_ERROR;
-        goto error;
+	return_value = (int32_t)SCMI_PROTOCOL_ERROR;
+	goto error;
     }
 
 #ifdef BUILD_HAS_MOD_RESOURCE_PERMS
     status = scmi_perf_permissions_handler(service_id, payload, message_id);
     if (status != FWK_SUCCESS) {
-        if (status == FWK_E_PARAM) {
-            return_value = (int32_t)SCMI_NOT_FOUND;
-        } else {
-            return_value = (int32_t)SCMI_DENIED;
-        }
-        goto error;
+	if (status == FWK_E_PARAM) {
+	    return_value = (int32_t)SCMI_NOT_FOUND;
+	} else {
+	    return_value = (int32_t)SCMI_DENIED;
+	}
+	goto error;
     }
 #endif
 
@@ -1595,7 +1595,7 @@ static int scmi_perf_message_handler(fwk
 
 error:
     return scmi_perf_ctx.scmi_api->respond(
-        service_id, &return_value, sizeof(return_value));
+	service_id, &return_value, sizeof(return_value));
 }
 
 static struct mod_scmi_to_protocol_api scmi_perf_mod_scmi_to_protocol_api = {
@@ -1622,10 +1622,10 @@ static void scmi_perf_respond(
     service_id = scmi_perf_ctx.perf_ops_table[idx].service_id;
 
     respond_status =
-        scmi_perf_ctx.scmi_api->respond(service_id, return_values, size);
+	scmi_perf_ctx.scmi_api->respond(service_id, return_values, size);
 
     if (respond_status != FWK_SUCCESS) {
-        FWK_LOG_DEBUG("[SCMI-PERF] %s @%d", __func__, __LINE__);
+	FWK_LOG_DEBUG("[SCMI-PERF] %s @%d", __func__, __LINE__);
     }
 
     /*
@@ -1660,20 +1660,20 @@ static void scmi_perf_notify_limits_upda
 
     domain = &(*scmi_perf_ctx.config->domains)[idx];
     if (domain->fast_channels_addr_scp != NULL) {
-        get_limit = (struct mod_scmi_perf_fast_channel_limit
-                         *)((uintptr_t)domain->fast_channels_addr_scp
-                                [MOD_SCMI_PERF_FAST_CHANNEL_LIMIT_GET]);
-        if (get_limit != NULL) { /* note: get_limit may not be defined */
-            get_limit->range_max = range_max;
-            get_limit->range_min = range_min;
-        }
+	get_limit = (struct mod_scmi_perf_fast_channel_limit
+			 *)((uintptr_t)domain->fast_channels_addr_scp
+				[MOD_SCMI_PERF_FAST_CHANNEL_LIMIT_GET]);
+	if (get_limit != NULL) { /* note: get_limit may not be defined */
+	    get_limit->range_max = range_max;
+	    get_limit->range_min = range_min;
+	}
     }
 
 #ifdef BUILD_HAS_SCMI_PERF_PLUGIN_HANDLER
     struct perf_plugins_perf_report perf_report = {
-        .dep_dom_id = domain_id,
-        .max_limit = range_max,
-        .min_limit = range_min,
+	.dep_dom_id = domain_id,
+	.max_limit = range_max,
+	.min_limit = range_min,
     };
 
     perf_plugins_handler_report(&perf_report);
@@ -1686,13 +1686,13 @@ static void scmi_perf_notify_limits_upda
     limits_changed.range_max = range_max;
 
     status = scmi_perf_ctx.scmi_notification_api->scmi_notification_notify(
-        MOD_SCMI_PROTOCOL_ID_PERF,
-        MOD_SCMI_PERF_NOTIFY_LIMITS,
-        SCMI_PERF_LIMITS_CHANGED,
-        &limits_changed,
-        sizeof(limits_changed));
+	MOD_SCMI_PROTOCOL_ID_PERF,
+	MOD_SCMI_PERF_NOTIFY_LIMITS,
+	SCMI_PERF_LIMITS_CHANGED,
+	&limits_changed,
+	sizeof(limits_changed));
     if (status != FWK_SUCCESS) {
-        FWK_LOG_DEBUG("[SCMI-PERF] %s @%d", __func__, __LINE__);
+	FWK_LOG_DEBUG("[SCMI-PERF] %s @%d", __func__, __LINE__);
     }
 #endif
 }
@@ -1730,22 +1730,22 @@ static void scmi_perf_notify_level_updat
 #ifdef BUILD_HAS_MOD_STATISTICS
     status = scmi_perf_ctx.dvfs_api->get_level_id(domain_id, level, &level_id);
     if (status == FWK_SUCCESS) {
-        status = scmi_perf_ctx.stats_api->update_domain(
-            fwk_module_id_scmi_perf,
-            FWK_ID_ELEMENT(FWK_MODULE_IDX_SCMI_PERF, idx),
-            level_id);
-        if (status != FWK_SUCCESS) {
-            FWK_LOG_DEBUG("[SCMI-PERF] %s @%d", __func__, __LINE__);
-        }
+	status = scmi_perf_ctx.stats_api->update_domain(
+	    fwk_module_id_scmi_perf,
+	    FWK_ID_ELEMENT(FWK_MODULE_IDX_SCMI_PERF, idx),
+	    level_id);
+	if (status != FWK_SUCCESS) {
+	    FWK_LOG_DEBUG("[SCMI-PERF] %s @%d", __func__, __LINE__);
+	}
     } else {
-        FWK_LOG_DEBUG("[SCMI-PERF] %s @%d", __func__, __LINE__);
+	FWK_LOG_DEBUG("[SCMI-PERF] %s @%d", __func__, __LINE__);
     }
 #endif
 
 #ifdef BUILD_HAS_SCMI_PERF_PLUGIN_HANDLER
     struct perf_plugins_perf_report perf_report = {
-        .dep_dom_id = domain_id,
-        .level = level,
+	.dep_dom_id = domain_id,
+	.level = level,
     };
 
     perf_plugins_handler_report(&perf_report);
@@ -1756,32 +1756,32 @@ static void scmi_perf_notify_level_updat
      * has been updated, we update all the relevant logical domains.
      */
     for (unsigned int i = 0; i < scmi_perf_ctx.domain_count; i++) {
-        dep_dom_id = get_dependency_id(i);
+	dep_dom_id = get_dependency_id(i);
 
-        if (fwk_id_get_element_idx(dep_dom_id) ==
-            fwk_id_get_element_idx(domain_id)) {
-            domain = &(*scmi_perf_ctx.config->domains)[i];
-
-            if (domain->fast_channels_addr_scp != NULL) {
-                get_level =
-                    (uint32_t *)((uintptr_t)domain->fast_channels_addr_scp
-                                     [MOD_SCMI_PERF_FAST_CHANNEL_LEVEL_GET]);
-                if (get_level != NULL) { /* get_level may not be defined */
-                    *get_level = level;
-                }
-            }
-        }
+	if (fwk_id_get_element_idx(dep_dom_id) ==
+	    fwk_id_get_element_idx(domain_id)) {
+	    domain = &(*scmi_perf_ctx.config->domains)[i];
+
+	    if (domain->fast_channels_addr_scp != NULL) {
+		get_level =
+		    (uint32_t *)((uintptr_t)domain->fast_channels_addr_scp
+				     [MOD_SCMI_PERF_FAST_CHANNEL_LEVEL_GET]);
+		if (get_level != NULL) { /* get_level may not be defined */
+		    *get_level = level;
+		}
+	    }
+	}
     }
 
 #else
     domain =
-        &(*scmi_perf_ctx.config->domains)[fwk_id_get_element_idx(domain_id)];
+	&(*scmi_perf_ctx.config->domains)[fwk_id_get_element_idx(domain_id)];
     if (domain->fast_channels_addr_scp != NULL) {
-        get_level = (uint32_t *)((uintptr_t)domain->fast_channels_addr_scp
-                                     [MOD_SCMI_PERF_FAST_CHANNEL_LEVEL_GET]);
-        if (get_level != NULL) { /* note: get_level may not be defined */
-            *get_level = level;
-        }
+	get_level = (uint32_t *)((uintptr_t)domain->fast_channels_addr_scp
+				     [MOD_SCMI_PERF_FAST_CHANNEL_LEVEL_GET]);
+	if (get_level != NULL) { /* note: get_level may not be defined */
+	    *get_level = level;
+	}
     }
 #endif
 
@@ -1791,13 +1791,13 @@ static void scmi_perf_notify_level_updat
     level_changed.performance_level = level;
 
     status = scmi_perf_ctx.scmi_notification_api->scmi_notification_notify(
-        MOD_SCMI_PROTOCOL_ID_PERF,
-        MOD_SCMI_PERF_NOTIFY_LEVEL,
-        SCMI_PERF_LEVEL_CHANGED,
-        &level_changed,
-        sizeof(level_changed));
+	MOD_SCMI_PROTOCOL_ID_PERF,
+	MOD_SCMI_PERF_NOTIFY_LEVEL,
+	SCMI_PERF_LEVEL_CHANGED,
+	&level_changed,
+	sizeof(level_changed));
     if (status != FWK_SUCCESS) {
-        FWK_LOG_DEBUG("[SCMI-PERF] %s @%d", __func__, __LINE__);
+	FWK_LOG_DEBUG("[SCMI-PERF] %s @%d", __func__, __LINE__);
     }
 #endif
 
@@ -1813,46 +1813,46 @@ static struct mod_scmi_perf_updated_api
  * Framework handlers
  */
 static int scmi_perf_init(fwk_id_t module_id, unsigned int element_count,
-                          const void *data)
+			  const void *data)
 {
     int dvfs_doms_count;
     uint32_t i;
     const struct mod_scmi_perf_config *config =
-        (const struct mod_scmi_perf_config *)data;
+	(const struct mod_scmi_perf_config *)data;
 
     if ((config == NULL) || (config->domains == NULL)) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     dvfs_doms_count =
-        fwk_module_get_element_count(FWK_ID_MODULE(FWK_MODULE_IDX_DVFS));
+	fwk_module_get_element_count(FWK_ID_MODULE(FWK_MODULE_IDX_DVFS));
     if (dvfs_doms_count <= 0) {
-        return FWK_E_SUPPORT;
+	return FWK_E_SUPPORT;
     }
 
     scmi_perf_ctx.dvfs_doms_count = (unsigned int)dvfs_doms_count;
 
     scmi_perf_ctx.perf_ops_table =
-        fwk_mm_calloc(config->perf_doms_count, sizeof(struct perf_operations));
+	fwk_mm_calloc(config->perf_doms_count, sizeof(struct perf_operations));
 
     scmi_perf_ctx.domain_ctx_table = fwk_mm_calloc(
-        config->perf_doms_count, sizeof(struct scmi_perf_domain_ctx));
+	config->perf_doms_count, sizeof(struct scmi_perf_domain_ctx));
 
     scmi_perf_ctx.config = config;
     scmi_perf_ctx.domain_count = (uint32_t)config->perf_doms_count;
 #ifdef BUILD_HAS_SCMI_PERF_FAST_CHANNELS
     scmi_perf_ctx.fast_channels_alarm_id = config->fast_channels_alarm_id;
     if (config->fast_channels_rate_limit < SCMI_PERF_FC_MIN_RATE_LIMIT) {
-        scmi_perf_ctx.fast_channels_rate_limit = SCMI_PERF_FC_MIN_RATE_LIMIT;
+	scmi_perf_ctx.fast_channels_rate_limit = SCMI_PERF_FC_MIN_RATE_LIMIT;
     } else {
-        scmi_perf_ctx.fast_channels_rate_limit =
-            config->fast_channels_rate_limit;
+	scmi_perf_ctx.fast_channels_rate_limit =
+	    config->fast_channels_rate_limit;
     }
 #endif
 
     /* Initialize table */
     for (i = 0; i < scmi_perf_ctx.domain_count; i++) {
-        scmi_perf_ctx.perf_ops_table[i].service_id = FWK_ID_NONE;
+	scmi_perf_ctx.perf_ops_table[i].service_id = FWK_ID_NONE;
     }
 
 #ifdef BUILD_HAS_SCMI_PERF_PLUGIN_HANDLER
@@ -1868,18 +1868,18 @@ static int scmi_init_notifications(int d
     int status;
 
     status = scmi_perf_ctx.scmi_api->get_agent_count(
-        &scmi_perf_ctx.agent_count);
+	&scmi_perf_ctx.agent_count);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     fwk_assert(scmi_perf_ctx.agent_count != 0u);
 
     status = scmi_perf_ctx.scmi_notification_api->scmi_notification_init(
-        MOD_SCMI_PROTOCOL_ID_PERF,
-        scmi_perf_ctx.agent_count,
-        domains,
-        MOD_SCMI_PERF_NOTIFICATION_COUNT);
+	MOD_SCMI_PROTOCOL_ID_PERF,
+	scmi_perf_ctx.agent_count,
+	domains,
+	MOD_SCMI_PERF_NOTIFICATION_COUNT);
 
     return status;
 }
@@ -1890,102 +1890,102 @@ static int scmi_perf_bind(fwk_id_t id, u
     int status;
 
     if (round == 1) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     status = fwk_module_bind(FWK_ID_MODULE(FWK_MODULE_IDX_SCMI),
-        FWK_ID_API(FWK_MODULE_IDX_SCMI, MOD_SCMI_API_IDX_PROTOCOL),
-        &scmi_perf_ctx.scmi_api);
+	FWK_ID_API(FWK_MODULE_IDX_SCMI, MOD_SCMI_API_IDX_PROTOCOL),
+	&scmi_perf_ctx.scmi_api);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
 #ifdef BUILD_HAS_SCMI_NOTIFICATIONS
     status = fwk_module_bind(
-        FWK_ID_MODULE(FWK_MODULE_IDX_SCMI),
-        FWK_ID_API(FWK_MODULE_IDX_SCMI, MOD_SCMI_API_IDX_NOTIFICATION),
-        &scmi_perf_ctx.scmi_notification_api);
+	FWK_ID_MODULE(FWK_MODULE_IDX_SCMI),
+	FWK_ID_API(FWK_MODULE_IDX_SCMI, MOD_SCMI_API_IDX_NOTIFICATION),
+	&scmi_perf_ctx.scmi_notification_api);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 #endif
 
 #ifdef BUILD_HAS_SCMI_PERF_FAST_CHANNELS
     if (!fwk_id_is_equal(scmi_perf_ctx.config->fast_channels_alarm_id,
-        FWK_ID_NONE)) {
-        status = fwk_module_bind(scmi_perf_ctx.config->fast_channels_alarm_id,
-            MOD_TIMER_API_ID_ALARM, &scmi_perf_ctx.fc_alarm_api);
-        if (status != FWK_SUCCESS) {
-            return FWK_E_PANIC;
-        }
+	FWK_ID_NONE)) {
+	status = fwk_module_bind(scmi_perf_ctx.config->fast_channels_alarm_id,
+	    MOD_TIMER_API_ID_ALARM, &scmi_perf_ctx.fc_alarm_api);
+	if (status != FWK_SUCCESS) {
+	    return FWK_E_PANIC;
+	}
     }
 #endif
 
 #ifdef BUILD_HAS_MOD_STATISTICS
     if (scmi_perf_ctx.config->stats_enabled) {
-        status = fwk_module_bind(FWK_ID_MODULE(FWK_MODULE_IDX_STATISTICS),
-            FWK_ID_API(FWK_MODULE_IDX_STATISTICS, MOD_STATS_API_IDX_STATS),
-            &scmi_perf_ctx.stats_api);
-        if (status != FWK_SUCCESS) {
-            return FWK_E_PANIC;
-        }
+	status = fwk_module_bind(FWK_ID_MODULE(FWK_MODULE_IDX_STATISTICS),
+	    FWK_ID_API(FWK_MODULE_IDX_STATISTICS, MOD_STATS_API_IDX_STATS),
+	    &scmi_perf_ctx.stats_api);
+	if (status != FWK_SUCCESS) {
+	    return FWK_E_PANIC;
+	}
     }
 #endif
 
 #ifdef BUILD_HAS_MOD_RESOURCE_PERMS
     status = fwk_module_bind(
-        FWK_ID_MODULE(FWK_MODULE_IDX_RESOURCE_PERMS),
-        FWK_ID_API(FWK_MODULE_IDX_RESOURCE_PERMS, MOD_RES_PERM_RESOURCE_PERMS),
-        &scmi_perf_ctx.res_perms_api);
+	FWK_ID_MODULE(FWK_MODULE_IDX_RESOURCE_PERMS),
+	FWK_ID_API(FWK_MODULE_IDX_RESOURCE_PERMS, MOD_RES_PERM_RESOURCE_PERMS),
+	&scmi_perf_ctx.res_perms_api);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 #endif
 
 #ifdef BUILD_HAS_SCMI_PERF_PLUGIN_HANDLER
     status = perf_plugins_handler_bind();
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 #endif
 
     return fwk_module_bind(
-        FWK_ID_MODULE(FWK_MODULE_IDX_DVFS),
-        FWK_ID_API(FWK_MODULE_IDX_DVFS, MOD_DVFS_API_IDX_DVFS),
-        &scmi_perf_ctx.dvfs_api);
+	FWK_ID_MODULE(FWK_MODULE_IDX_DVFS),
+	FWK_ID_API(FWK_MODULE_IDX_DVFS, MOD_DVFS_API_IDX_DVFS),
+	&scmi_perf_ctx.dvfs_api);
 }
 
 static int scmi_perf_process_bind_request(fwk_id_t source_id,
     fwk_id_t target_id, fwk_id_t api_id, const void **api)
 {
     enum scmi_perf_api_idx api_id_type =
-        (enum scmi_perf_api_idx)fwk_id_get_api_idx(api_id);
+	(enum scmi_perf_api_idx)fwk_id_get_api_idx(api_id);
 
     switch (api_id_type) {
     case MOD_SCMI_PERF_PROTOCOL_API:
-        *api = &scmi_perf_mod_scmi_to_protocol_api;
-        break;
+	*api = &scmi_perf_mod_scmi_to_protocol_api;
+	break;
 
     case MOD_SCMI_PERF_DVFS_UPDATE_API:
-        if (!fwk_id_is_equal(source_id, fwk_module_id_dvfs)) {
-            /* Only DVFS can use this API */
-            return FWK_E_ACCESS;
-        }
+	if (!fwk_id_is_equal(source_id, fwk_module_id_dvfs)) {
+	    /* Only DVFS can use this API */
+	    return FWK_E_ACCESS;
+	}
 
-        *api = &perf_update_api;
-        break;
+	*api = &perf_update_api;
+	break;
 
     case MOD_SCMI_PERF_PLUGINS_API:
 #ifdef BUILD_HAS_SCMI_PERF_PLUGIN_HANDLER
-        return perf_plugins_handler_process_bind_request(
-            source_id, target_id, api_id, api);
+	return perf_plugins_handler_process_bind_request(
+	    source_id, target_id, api_id, api);
 #else
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
 #endif
-        break;
+	break;
 
     default:
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 
     return FWK_SUCCESS;
@@ -2000,48 +2000,48 @@ static int scmi_perf_stats_start(void)
     unsigned int i;
 
     if (!scmi_perf_ctx.config->stats_enabled) {
-        return FWK_E_SUPPORT;
+	return FWK_E_SUPPORT;
     }
 
     /* Count how many domains have statistics */
     for (i = 0; i < scmi_perf_ctx.domain_count; i++) {
-        domain = &(*scmi_perf_ctx.config->domains)[i];
-        if (domain->stats_collected) {
-            stats_domains++;
-        }
+	domain = &(*scmi_perf_ctx.config->domains)[i];
+	if (domain->stats_collected) {
+	    stats_domains++;
+	}
     }
 
     status = scmi_perf_ctx.stats_api->init_stats(fwk_module_id_scmi_perf,
-        scmi_perf_ctx.domain_count, stats_domains);
+	scmi_perf_ctx.domain_count, stats_domains);
 
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     for (i = 0; i < scmi_perf_ctx.domain_count; i++) {
-        domain = &(*scmi_perf_ctx.config->domains)[i];
-        /* Add this domain to track statistics when needed */
-        if (domain->stats_collected) {
-            fwk_id_t domain_id;
-            size_t opp_count;
-
-            domain_id = get_dependency_id(i);
-            status = scmi_perf_ctx.dvfs_api->get_opp_count(domain_id,
-                &opp_count);
-
-            if (status != FWK_SUCCESS) {
-                return status;
-            }
-
-            status = scmi_perf_ctx.stats_api->add_domain(
-                fwk_module_id_scmi_perf,
-                FWK_ID_ELEMENT(FWK_MODULE_IDX_SCMI_PERF, i),
-                (int)opp_count);
-
-            if (status != FWK_SUCCESS) {
-                return status;
-            }
-        }
+	domain = &(*scmi_perf_ctx.config->domains)[i];
+	/* Add this domain to track statistics when needed */
+	if (domain->stats_collected) {
+	    fwk_id_t domain_id;
+	    size_t opp_count;
+
+	    domain_id = get_dependency_id(i);
+	    status = scmi_perf_ctx.dvfs_api->get_opp_count(domain_id,
+		&opp_count);
+
+	    if (status != FWK_SUCCESS) {
+		return status;
+	    }
+
+	    status = scmi_perf_ctx.stats_api->add_domain(
+		fwk_module_id_scmi_perf,
+		FWK_ID_ELEMENT(FWK_MODULE_IDX_SCMI_PERF, i),
+		(int)opp_count);
+
+	    if (status != FWK_SUCCESS) {
+		return status;
+	    }
+	}
     }
 
     return scmi_perf_ctx.stats_api->start_stats(fwk_module_id_scmi_perf);
@@ -2064,63 +2064,63 @@ static int scmi_perf_start(fwk_id_t id)
     bool has_phy_group;
 
     scmi_perf_ctx.opp_table = fwk_mm_calloc(
-        scmi_perf_ctx.dvfs_doms_count, sizeof(struct perf_opp_table));
+	scmi_perf_ctx.dvfs_doms_count, sizeof(struct perf_opp_table));
 
     for (i = 0; i < scmi_perf_ctx.dvfs_doms_count; i++) {
-        opp_table = &scmi_perf_ctx.opp_table[i];
+	opp_table = &scmi_perf_ctx.opp_table[i];
 
-        domain_id = FWK_ID_ELEMENT(FWK_MODULE_IDX_DVFS, i);
+	domain_id = FWK_ID_ELEMENT(FWK_MODULE_IDX_DVFS, i);
 
-        /* Get the number of levels for this DVFS domain */
-        status = scmi_perf_ctx.dvfs_api->get_opp_count(domain_id, &opp_count);
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
-
-        dvfs_config =
-            (const struct mod_dvfs_domain_config *)fwk_module_get_data(
-                domain_id);
-
-        opp_table->opps = &dvfs_config->opps[0];
-        opp_table->opp_count = opp_count;
-        opp_table->dvfs_id = domain_id;
+	/* Get the number of levels for this DVFS domain */
+	status = scmi_perf_ctx.dvfs_api->get_opp_count(domain_id, &opp_count);
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
+
+	dvfs_config =
+	    (const struct mod_dvfs_domain_config *)fwk_module_get_data(
+		domain_id);
+
+	opp_table->opps = &dvfs_config->opps[0];
+	opp_table->opp_count = opp_count;
+	opp_table->dvfs_id = domain_id;
     }
 
     /* Assign to each performance domain the correct OPP table */
     for (dom_idx = 0; dom_idx < scmi_perf_ctx.domain_count; dom_idx++) {
-        domain_cfg = &(*scmi_perf_ctx.config->domains)[dom_idx];
+	domain_cfg = &(*scmi_perf_ctx.config->domains)[dom_idx];
 
-        has_phy_group = fwk_optional_id_is_defined(domain_cfg->phy_group_id);
-        if (has_phy_group) {
-            domain_id = domain_cfg->phy_group_id;
-        } else {
-            domain_id = FWK_ID_ELEMENT(FWK_MODULE_IDX_DVFS, dom_idx);
-        }
-
-        domain_ctx = &scmi_perf_ctx.domain_ctx_table[dom_idx];
-
-        /*
-         * Search the corresponding physical domain for this performance domain
-         * and assign the correct opp_table.
-         */
-        for (i = 0; i < scmi_perf_ctx.dvfs_doms_count; i++) {
-            opp_table = &scmi_perf_ctx.opp_table[i];
-
-            if (fwk_id_is_equal(opp_table->dvfs_id, domain_id)) {
-                domain_ctx->opp_table = opp_table;
-
-                /* init limits */
-                domain_ctx->level_limits.minimum = opp_table->opps[0].level;
-                domain_ctx->level_limits.maximum =
-                    opp_table->opps[opp_table->opp_count - 1].level;
-
-                break;
-            }
-        }
-        if (domain_ctx->opp_table == NULL) {
-            /* The corresponding physical domain did not have a match */
-            return FWK_E_PANIC;
-        }
+	has_phy_group = fwk_optional_id_is_defined(domain_cfg->phy_group_id);
+	if (has_phy_group) {
+	    domain_id = domain_cfg->phy_group_id;
+	} else {
+	    domain_id = FWK_ID_ELEMENT(FWK_MODULE_IDX_DVFS, dom_idx);
+	}
+
+	domain_ctx = &scmi_perf_ctx.domain_ctx_table[dom_idx];
+
+	/*
+	 * Search the corresponding physical domain for this performance domain
+	 * and assign the correct opp_table.
+	 */
+	for (i = 0; i < scmi_perf_ctx.dvfs_doms_count; i++) {
+	    opp_table = &scmi_perf_ctx.opp_table[i];
+
+	    if (fwk_id_is_equal(opp_table->dvfs_id, domain_id)) {
+		domain_ctx->opp_table = opp_table;
+
+		/* init limits */
+		domain_ctx->level_limits.minimum = opp_table->opps[0].level;
+		domain_ctx->level_limits.maximum =
+		    opp_table->opps[opp_table->opp_count - 1].level;
+
+		break;
+	    }
+	}
+	if (domain_ctx->opp_table == NULL) {
+	    /* The corresponding physical domain did not have a match */
+	    return FWK_E_PANIC;
+	}
     }
 
 #ifdef BUILD_HAS_SCMI_PERF_FAST_CHANNELS
@@ -2134,21 +2134,21 @@ static int scmi_perf_start(fwk_id_t id)
      * Set up the Fast Channel polling if required
      */
     if (!fwk_id_is_equal(scmi_perf_ctx.config->fast_channels_alarm_id,
-        FWK_ID_NONE)) {
-        if (scmi_perf_ctx.config->fast_channels_rate_limit <
-            SCMI_PERF_FC_MIN_RATE_LIMIT) {
-            fc_interval_msecs = (uint32_t)SCMI_PERF_FC_MIN_RATE_LIMIT / 1000;
-        } else {
-            fc_interval_msecs = (uint32_t)
-            scmi_perf_ctx.config->fast_channels_rate_limit / 1000;
-        }
-        status = scmi_perf_ctx.fc_alarm_api->start(
-            scmi_perf_ctx.config->fast_channels_alarm_id,
-            fc_interval_msecs, MOD_TIMER_ALARM_TYPE_PERIODIC,
-            fast_channel_callback, (uintptr_t)0);
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
+	FWK_ID_NONE)) {
+	if (scmi_perf_ctx.config->fast_channels_rate_limit <
+	    SCMI_PERF_FC_MIN_RATE_LIMIT) {
+	    fc_interval_msecs = (uint32_t)SCMI_PERF_FC_MIN_RATE_LIMIT / 1000;
+	} else {
+	    fc_interval_msecs = (uint32_t)
+	    scmi_perf_ctx.config->fast_channels_rate_limit / 1000;
+	}
+	status = scmi_perf_ctx.fc_alarm_api->start(
+	    scmi_perf_ctx.config->fast_channels_alarm_id,
+	    fc_interval_msecs, MOD_TIMER_ALARM_TYPE_PERIODIC,
+	    fast_channel_callback, (uintptr_t)0);
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
     }
 
 #    ifdef BUILD_HAS_SCMI_PERF_PLUGIN_HANDLER
@@ -2160,49 +2160,49 @@ static int scmi_perf_start(fwk_id_t id)
      * OPPs.
      */
     for (i = 0; i < scmi_perf_ctx.domain_count; i++) {
-        domain = &(*scmi_perf_ctx.config->domains)[i];
-        if (domain->fast_channels_addr_scp != NULL) {
-            for (j = 0; j < MOD_SCMI_PERF_FAST_CHANNEL_ADDR_INDEX_COUNT; j++) {
-                fc_elem = (void *)(uintptr_t)domain->fast_channels_addr_scp[j];
-                if (fc_elem != NULL) {
-                    if ((j == MOD_SCMI_PERF_FAST_CHANNEL_LEVEL_SET) ||
-                        (j == MOD_SCMI_PERF_FAST_CHANNEL_LEVEL_GET)) {
-                        status = scmi_perf_ctx.dvfs_api->get_sustained_opp(
-                            get_dependency_id(i), &opp);
-                        if (status != FWK_SUCCESS) {
-                            return status;
-                        }
-
-                        fwk_str_memcpy(
-                            fc_elem, &opp.level, fast_channel_elem_size[j]);
-                    } else {
-                        /* _LIMIT_SET or _LIMIT_GET */
-                        domain_ctx = &scmi_perf_ctx.domain_ctx_table[i];
-                        opp_table = domain_ctx->opp_table;
-
-                        fc_limits =
-                            (struct mod_scmi_perf_fast_channel_limit *)fc_elem;
-                        fc_limits->range_min = opp_table->opps[0].level;
-                        fc_limits->range_max =
-                            opp_table->opps[opp_table->opp_count - 1].level;
-                    }
-                }
-            }
-        }
+	domain = &(*scmi_perf_ctx.config->domains)[i];
+	if (domain->fast_channels_addr_scp != NULL) {
+	    for (j = 0; j < MOD_SCMI_PERF_FAST_CHANNEL_ADDR_INDEX_COUNT; j++) {
+		fc_elem = (void *)(uintptr_t)domain->fast_channels_addr_scp[j];
+		if (fc_elem != NULL) {
+		    if ((j == MOD_SCMI_PERF_FAST_CHANNEL_LEVEL_SET) ||
+			(j == MOD_SCMI_PERF_FAST_CHANNEL_LEVEL_GET)) {
+			status = scmi_perf_ctx.dvfs_api->get_sustained_opp(
+			    get_dependency_id(i), &opp);
+			if (status != FWK_SUCCESS) {
+			    return status;
+			}
+
+			fwk_str_memcpy(
+			    fc_elem, &opp.level, fast_channel_elem_size[j]);
+		    } else {
+			/* _LIMIT_SET or _LIMIT_GET */
+			domain_ctx = &scmi_perf_ctx.domain_ctx_table[i];
+			opp_table = domain_ctx->opp_table;
+
+			fc_limits =
+			    (struct mod_scmi_perf_fast_channel_limit *)fc_elem;
+			fc_limits->range_min = opp_table->opps[0].level;
+			fc_limits->range_max =
+			    opp_table->opps[opp_table->opp_count - 1].level;
+		    }
+		}
+	    }
+	}
     }
 
 #    else
     /* Initialise FastChannels to 0 */
     for (i = 0; i < scmi_perf_ctx.domain_count; i++) {
-        domain = &(*scmi_perf_ctx.config->domains)[i];
-        if (domain->fast_channels_addr_scp != NULL) {
-            for (j = 0; j < MOD_SCMI_PERF_FAST_CHANNEL_ADDR_INDEX_COUNT; j++) {
-                fc_elem = (void *)(uintptr_t)domain->fast_channels_addr_scp[j];
-                if (fc_elem != NULL) {
-                    fwk_str_memset(fc_elem, 0, fast_channel_elem_size[j]);
-                }
-            }
-        }
+	domain = &(*scmi_perf_ctx.config->domains)[i];
+	if (domain->fast_channels_addr_scp != NULL) {
+	    for (j = 0; j < MOD_SCMI_PERF_FAST_CHANNEL_ADDR_INDEX_COUNT; j++) {
+		fc_elem = (void *)(uintptr_t)domain->fast_channels_addr_scp[j];
+		if (fc_elem != NULL) {
+		    fwk_str_memset(fc_elem, 0, fast_channel_elem_size[j]);
+		}
+	    }
+	}
     }
 #    endif
 #endif
@@ -2210,14 +2210,14 @@ static int scmi_perf_start(fwk_id_t id)
 #ifdef BUILD_HAS_MOD_STATISTICS
     status = scmi_perf_stats_start();
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 #endif
 
 #ifdef BUILD_HAS_SCMI_NOTIFICATIONS
     status = scmi_init_notifications((int)scmi_perf_ctx.domain_count);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 #endif
 
@@ -2236,38 +2236,38 @@ static int process_request_event(const s
 
     /* request event to DVFS HAL */
     if (fwk_id_is_equal(event->id, scmi_perf_get_level)) {
-        params = (struct scmi_perf_event_parameters *)event->params;
+	params = (struct scmi_perf_event_parameters *)event->params;
 
-        status = scmi_perf_ctx.dvfs_api->get_current_opp(params->domain_id,
-                                                       &opp);
-        if (status == FWK_SUCCESS) {
-            /* DVFS value is ready */
-            return_values_level = (struct scmi_perf_level_get_p2a){
-                .status = SCMI_SUCCESS,
-                .performance_level = opp.level,
-            };
-
-            scmi_perf_respond(
-                &return_values_level,
-                params->domain_id,
-                (int)sizeof(return_values_level));
-
-            return status;
-        } else if (status == FWK_PENDING) {
-            /* DVFS value will be provided through a response event */
-            return FWK_SUCCESS;
-        } else {
-            return_values_level = (struct scmi_perf_level_get_p2a) {
-                .status = SCMI_HARDWARE_ERROR,
-            };
-
-            scmi_perf_respond(
-                &return_values_level,
-                params->domain_id,
-                (int)sizeof(return_values_level.status));
+	status = scmi_perf_ctx.dvfs_api->get_current_opp(params->domain_id,
+						       &opp);
+	if (status == FWK_SUCCESS) {
+	    /* DVFS value is ready */
+	    return_values_level = (struct scmi_perf_level_get_p2a){
+		.status = SCMI_SUCCESS,
+		.performance_level = opp.level,
+	    };
+
+	    scmi_perf_respond(
+		&return_values_level,
+		params->domain_id,
+		(int)sizeof(return_values_level));
+
+	    return status;
+	} else if (status == FWK_PENDING) {
+	    /* DVFS value will be provided through a response event */
+	    return FWK_SUCCESS;
+	} else {
+	    return_values_level = (struct scmi_perf_level_get_p2a) {
+		.status = SCMI_HARDWARE_ERROR,
+	    };
+
+	    scmi_perf_respond(
+		&return_values_level,
+		params->domain_id,
+		(int)sizeof(return_values_level.status));
 
-            return FWK_E_DEVICE;
-        }
+	    return FWK_E_DEVICE;
+	}
     }
 
     return FWK_E_PARAM;
@@ -2283,18 +2283,18 @@ static int process_response_event(const
     struct scmi_perf_level_get_p2a return_values_level;
 
     if (fwk_id_is_equal(event->id, mod_dvfs_event_id_get_opp)) {
-        params_level = (struct mod_dvfs_params_response *)
-            event->params;
-        return_values_level = (struct scmi_perf_level_get_p2a) {
-            .status = params_level->status,
-            .performance_level = params_level->performance_level,
-        };
-        scmi_perf_respond(
-            &return_values_level,
-            event->source_id,
-            (return_values_level.status == SCMI_SUCCESS) ?
-                (int)sizeof(return_values_level) :
-                (int)sizeof(return_values_level.status));
+	params_level = (struct mod_dvfs_params_response *)
+	    event->params;
+	return_values_level = (struct scmi_perf_level_get_p2a) {
+	    .status = params_level->status,
+	    .performance_level = params_level->performance_level,
+	};
+	scmi_perf_respond(
+	    &return_values_level,
+	    event->source_id,
+	    (return_values_level.status == SCMI_SUCCESS) ?
+		(int)sizeof(return_values_level) :
+		(int)sizeof(return_values_level.status));
     }
 
     return FWK_SUCCESS;
@@ -2305,42 +2305,42 @@ static int process_internal_event(const
 {
     int status;
     enum scmi_perf_event_idx event_idx =
-        (enum scmi_perf_event_idx)fwk_id_get_event_idx(event->id);
+	(enum scmi_perf_event_idx)fwk_id_get_event_idx(event->id);
 
     switch (event_idx) {
 #ifdef BUILD_HAS_SCMI_PERF_FAST_CHANNELS
     case SCMI_PERF_EVENT_IDX_FAST_CHANNELS_PROCESS:
-        fast_channels_process();
-        status = FWK_SUCCESS;
-        break;
+	fast_channels_process();
+	status = FWK_SUCCESS;
+	break;
 #endif
     default:
-        status = FWK_E_PARAM;
-        break;
+	status = FWK_E_PARAM;
+	break;
     }
 
     return status;
 }
 
 static int scmi_perf_process_event(const struct fwk_event *event,
-                                    struct fwk_event *resp_event)
+				    struct fwk_event *resp_event)
 {
     /* Request events from SCMI */
     if (fwk_id_get_module_idx(event->source_id) ==
-        fwk_id_get_module_idx(fwk_module_id_scmi)) {
-        return process_request_event(event);
+	fwk_id_get_module_idx(fwk_module_id_scmi)) {
+	return process_request_event(event);
     }
 
     /* Response events from DVFS */
     if (fwk_id_get_module_idx(event->source_id) ==
-        fwk_id_get_module_idx(fwk_module_id_dvfs)) {
-        return process_response_event(event);
+	fwk_id_get_module_idx(fwk_module_id_dvfs)) {
+	return process_response_event(event);
     }
 
     /* Response internal events */
     if (fwk_id_get_module_idx(event->source_id) ==
-        fwk_id_get_module_idx(fwk_module_id_scmi_perf)) {
-        return process_internal_event(event);
+	fwk_id_get_module_idx(fwk_module_id_scmi_perf)) {
+	return process_internal_event(event);
     }
 
     return FWK_E_PARAM;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_perf/src/perf_plugins_handler.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi_perf/src/perf_plugins_handler.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_perf/src/perf_plugins_handler.c
@@ -88,7 +88,7 @@ static inline struct perf_plugins_dev_ct
 static inline fwk_id_t get_phy_domain_id(fwk_id_t domain_id)
 {
     return FWK_ID_ELEMENT(
-        FWK_MODULE_IDX_DVFS, fwk_id_get_element_idx(domain_id));
+	FWK_MODULE_IDX_DVFS, fwk_id_get_element_idx(domain_id));
 }
 
 fwk_id_t perf_plugins_get_dependency_id(unsigned int dom_idx)
@@ -108,12 +108,12 @@ static int plugin_set_limits(struct plug
     int status;
 
     if (data == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     struct mod_scmi_perf_level_limits req_limits = {
-        .minimum = data->min_limit,
-        .maximum = data->max_limit,
+	.minimum = data->min_limit,
+	.maximum = data->max_limit,
     };
 
     domain_id = get_phy_domain_id(data->domain_id);
@@ -121,20 +121,20 @@ static int plugin_set_limits(struct plug
 
     /* Compare with the aggregated value stored for this physical domain */
     if (req_limits.maximum > dev_ctx->lmax) {
-        req_limits.maximum = dev_ctx->lmax;
+	req_limits.maximum = dev_ctx->lmax;
     }
 
     if (req_limits.minimum < dev_ctx->lmin) {
-        req_limits.minimum = dev_ctx->lmin;
+	req_limits.minimum = dev_ctx->lmin;
     }
 
     if (!are_limits_valid(req_limits.minimum, req_limits.maximum)) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     status = perf_set_limits(domain_id, 0, &req_limits);
     if (status != FWK_SUCCESS) {
-        return FWK_E_DEVICE;
+	return FWK_E_DEVICE;
     }
 
     return FWK_SUCCESS;
@@ -153,8 +153,8 @@ static void write_back_adj_values(
     size_t phy_dom_idx)
 {
     for (unsigned int i = 0; i < (dev_ctx->log_dom_count + 1); i++) {
-        dev_ctx->perf_table.adj_min_limit[i] = dev_ctx->lmin;
-        dev_ctx->perf_table.adj_max_limit[i] = dev_ctx->lmax;
+	dev_ctx->perf_table.adj_min_limit[i] = dev_ctx->lmin;
+	dev_ctx->perf_table.adj_max_limit[i] = dev_ctx->lmax;
     }
 
     perf_plugins_ctx.full_perf_table.adj_min_limit[phy_dom_idx] = dev_ctx->lmin;
@@ -178,65 +178,65 @@ static void plugins_policy_sync_level_li
     adj_min_limit_table = dev_ctx->perf_table.adj_min_limit;
 
     if (dom_type != PERF_PLUGIN_DOM_TYPE_FULL) {
-        phy_dom = dev_ctx->log_dom_count;
-        level_table = dev_ctx->perf_table.level;
-        min_limit_table = dev_ctx->perf_table.min_limit;
-        max_limit_table = dev_ctx->perf_table.max_limit;
-
-        /*
-         * Conservative approach for adjusted values:
-         * - pick the max for the min limit
-         * - pick the min for the max limit
-         */
-        needle_lim_min = min_limit_table[phy_dom];
-        needle_lim_max = max_limit_table[phy_dom];
-
-        /* Find min/max on adjusted values, physical included */
-        for (unsigned int i = 0; i < (phy_dom + 1); i++) {
-            if (adj_min_limit_table[i] >= needle_lim_min) {
-                needle_lim_min = adj_min_limit_table[i];
-            }
-
-            if (adj_max_limit_table[i] <= needle_lim_max) {
-                needle_lim_max = adj_max_limit_table[i];
-            }
-        }
-
-        /*
-         * Now we apply the same policy against the temporary results for the
-         * plugins updates so far.
-         */
-        if (needle_lim_min > dev_ctx->lmin) {
-            dev_ctx->lmin = needle_lim_min;
-        }
-        if (needle_lim_max < dev_ctx->lmax) {
-            dev_ctx->lmax = needle_lim_max;
-        }
+	phy_dom = dev_ctx->log_dom_count;
+	level_table = dev_ctx->perf_table.level;
+	min_limit_table = dev_ctx->perf_table.min_limit;
+	max_limit_table = dev_ctx->perf_table.max_limit;
+
+	/*
+	 * Conservative approach for adjusted values:
+	 * - pick the max for the min limit
+	 * - pick the min for the max limit
+	 */
+	needle_lim_min = min_limit_table[phy_dom];
+	needle_lim_max = max_limit_table[phy_dom];
+
+	/* Find min/max on adjusted values, physical included */
+	for (unsigned int i = 0; i < (phy_dom + 1); i++) {
+	    if (adj_min_limit_table[i] >= needle_lim_min) {
+		needle_lim_min = adj_min_limit_table[i];
+	    }
+
+	    if (adj_max_limit_table[i] <= needle_lim_max) {
+		needle_lim_max = adj_max_limit_table[i];
+	    }
+	}
+
+	/*
+	 * Now we apply the same policy against the temporary results for the
+	 * plugins updates so far.
+	 */
+	if (needle_lim_min > dev_ctx->lmin) {
+	    dev_ctx->lmin = needle_lim_min;
+	}
+	if (needle_lim_max < dev_ctx->lmax) {
+	    dev_ctx->lmax = needle_lim_max;
+	}
 
-        dev_ctx->max = level_table[phy_dom];
+	dev_ctx->max = level_table[phy_dom];
 
-        phy_dom_idx = fwk_id_get_element_idx(dev_ctx->perf_table.domain_id);
-        write_back_adj_values(dev_ctx, (size_t)phy_dom_idx);
+	phy_dom_idx = fwk_id_get_element_idx(dev_ctx->perf_table.domain_id);
+	write_back_adj_values(dev_ctx, (size_t)phy_dom_idx);
 
     } else {
-        /*
-         * This needs to iterate over all the domains, since we are at the last
-         * domain and this plugin may have changed the limits for all the
-         * domains.
-         */
-        for (size_t i = 0; i < perf_plugins_ctx.dvfs_doms_count; i++) {
-            dev_ctx = perf_ph_get_ctx(FWK_ID_ELEMENT(FWK_MODULE_IDX_DVFS, i));
-
-            /* Update with adjusted min-max */
-            if (adj_min_lim_full_table[i] > dev_ctx->lmin) {
-                dev_ctx->lmin = adj_min_lim_full_table[i];
-            }
-            if (adj_max_lim_full_table[i] < dev_ctx->lmax) {
-                dev_ctx->lmax = adj_max_lim_full_table[i];
-            }
+	/*
+	 * This needs to iterate over all the domains, since we are at the last
+	 * domain and this plugin may have changed the limits for all the
+	 * domains.
+	 */
+	for (size_t i = 0; i < perf_plugins_ctx.dvfs_doms_count; i++) {
+	    dev_ctx = perf_ph_get_ctx(FWK_ID_ELEMENT(FWK_MODULE_IDX_DVFS, i));
+
+	    /* Update with adjusted min-max */
+	    if (adj_min_lim_full_table[i] > dev_ctx->lmin) {
+		dev_ctx->lmin = adj_min_lim_full_table[i];
+	    }
+	    if (adj_max_lim_full_table[i] < dev_ctx->lmax) {
+		dev_ctx->lmax = adj_max_lim_full_table[i];
+	    }
 
-            write_back_adj_values(dev_ctx, i);
-        }
+	    write_back_adj_values(dev_ctx, i);
+	}
     }
 }
 
@@ -249,8 +249,8 @@ static void plugins_policy_update(struct
     fc_update->level = dev_ctx->max;
     fc_update->adj_min_limit = dev_ctx->lmin;
     fc_update->adj_max_limit = are_limits_valid(dev_ctx->lmin, dev_ctx->lmax) ?
-        dev_ctx->lmax :
-        dev_ctx->lmin;
+	dev_ctx->lmax :
+	dev_ctx->lmin;
 }
 
 static void plugins_policy_update_no_plugins(struct fc_perf_update *fc_update)
@@ -261,16 +261,16 @@ static void plugins_policy_update_no_plu
     dev_ctx = perf_ph_get_ctx(fc_update->domain_id);
 
     if (dev_ctx->log_dom_count == 1) {
-        /* No plugins, no logical domains */
-        fc_update->adj_max_limit = fc_update->max_limit;
-        fc_update->adj_min_limit = fc_update->min_limit;
+	/* No plugins, no logical domains */
+	fc_update->adj_max_limit = fc_update->max_limit;
+	fc_update->adj_min_limit = fc_update->min_limit;
     } else {
-        /* No plugins, with logical domains */
-        phy_dom = dev_ctx->log_dom_count;
+	/* No plugins, with logical domains */
+	phy_dom = dev_ctx->log_dom_count;
 
-        fc_update->level = dev_ctx->perf_table.level[phy_dom];
-        fc_update->adj_max_limit = dev_ctx->perf_table.adj_max_limit[phy_dom];
-        fc_update->adj_min_limit = dev_ctx->perf_table.adj_min_limit[phy_dom];
+	fc_update->level = dev_ctx->perf_table.level[phy_dom];
+	fc_update->adj_max_limit = dev_ctx->perf_table.adj_max_limit[phy_dom];
+	fc_update->adj_min_limit = dev_ctx->perf_table.adj_min_limit[phy_dom];
     }
 }
 
@@ -298,30 +298,30 @@ static void assign_data_for_plugins(
 
     /* Determine the first entry on the table */
     if (dom_type == PERF_PLUGIN_DOM_TYPE_FULL) {
-        snapshot->level = &perf_plugins_ctx.full_perf_table.level[0];
+	snapshot->level = &perf_plugins_ctx.full_perf_table.level[0];
 
-        snapshot->max_limit = &perf_plugins_ctx.full_perf_table.max_limit[0];
-        snapshot->min_limit = &perf_plugins_ctx.full_perf_table.min_limit[0];
+	snapshot->max_limit = &perf_plugins_ctx.full_perf_table.max_limit[0];
+	snapshot->min_limit = &perf_plugins_ctx.full_perf_table.min_limit[0];
 
-        snapshot->adj_max_limit =
-            &perf_plugins_ctx.full_perf_table.adj_max_limit[0];
-        snapshot->adj_min_limit =
-            &perf_plugins_ctx.full_perf_table.adj_min_limit[0];
+	snapshot->adj_max_limit =
+	    &perf_plugins_ctx.full_perf_table.adj_max_limit[0];
+	snapshot->adj_min_limit =
+	    &perf_plugins_ctx.full_perf_table.adj_min_limit[0];
 
-        return;
+	return;
     } else if (dom_type == PERF_PLUGIN_DOM_TYPE_LOGICAL) {
-        /* Provide the beginning of the table */
-        dom_idx = 0;
+	/* Provide the beginning of the table */
+	dom_idx = 0;
     } else {
-        /* Provide the last column of the table */
-        dom_idx = dev_ctx->log_dom_count;
+	/* Provide the last column of the table */
+	dom_idx = dev_ctx->log_dom_count;
     }
 
     /* Alter the module identifier only */
     snapshot->domain_id = FWK_ID_SUB_ELEMENT(
-        FWK_MODULE_IDX_SCMI_PERF,
-        fwk_id_get_element_idx(id),
-        fwk_id_get_sub_element_idx(id));
+	FWK_MODULE_IDX_SCMI_PERF,
+	fwk_id_get_element_idx(id),
+	fwk_id_get_sub_element_idx(id));
 
     get_perf_table(snapshot, dev_ctx, dom_idx);
 }
@@ -339,15 +339,15 @@ static inline void domain_aggregate(
      * - pick the min for the max limit
      */
     if (this_dom->level[0] > phy_dom->level[0]) {
-        phy_dom->level[0] = this_dom->level[0];
+	phy_dom->level[0] = this_dom->level[0];
     }
 
     if (this_dom->max_limit[0] < phy_dom->max_limit[0]) {
-        phy_dom->max_limit[0] = this_dom->max_limit[0];
+	phy_dom->max_limit[0] = this_dom->max_limit[0];
     }
 
     if (this_dom->min_limit[0] > phy_dom->min_limit[0]) {
-        phy_dom->min_limit[0] = this_dom->min_limit[0];
+	phy_dom->min_limit[0] = this_dom->min_limit[0];
     }
 }
 
@@ -374,14 +374,14 @@ static void store_and_aggregate(struct f
     this_dom.adj_min_limit[0] = fc_update->min_limit;
 
     if (this_dom_idx == 0) {
-        /* Init max/min for physical domain at the 1st iteration */
-        phy_dom.level[0] = 0;
-        phy_dom.max_limit[0] = UINT32_MAX;
-        phy_dom.min_limit[0] = 0;
-
-        /* as well as ctx */
-        dev_ctx->lmin = 0;
-        dev_ctx->lmax = UINT32_MAX;
+	/* Init max/min for physical domain at the 1st iteration */
+	phy_dom.level[0] = 0;
+	phy_dom.max_limit[0] = UINT32_MAX;
+	phy_dom.min_limit[0] = 0;
+
+	/* as well as ctx */
+	dev_ctx->lmin = 0;
+	dev_ctx->lmax = UINT32_MAX;
     }
 
     domain_aggregate(&this_dom, &phy_dom);
@@ -390,20 +390,20 @@ static void store_and_aggregate(struct f
     phy_dom.adj_min_limit[0] = phy_dom.min_limit[0];
 
     if (this_dom_idx == (phy_dom_idx - 1)) {
-        /* Copy values, all-performance table */
-        phy_ix = fwk_id_get_element_idx(fc_update->domain_id);
+	/* Copy values, all-performance table */
+	phy_ix = fwk_id_get_element_idx(fc_update->domain_id);
 
-        perf_plugins_ctx.full_perf_table.level[phy_ix] = phy_dom.level[0];
+	perf_plugins_ctx.full_perf_table.level[phy_ix] = phy_dom.level[0];
 
-        perf_plugins_ctx.full_perf_table.max_limit[phy_ix] =
-            phy_dom.max_limit[0];
-        perf_plugins_ctx.full_perf_table.min_limit[phy_ix] =
-            phy_dom.min_limit[0];
-
-        perf_plugins_ctx.full_perf_table.adj_max_limit[phy_ix] =
-            phy_dom.adj_max_limit[0];
-        perf_plugins_ctx.full_perf_table.adj_min_limit[phy_ix] =
-            phy_dom.adj_min_limit[0];
+	perf_plugins_ctx.full_perf_table.max_limit[phy_ix] =
+	    phy_dom.max_limit[0];
+	perf_plugins_ctx.full_perf_table.min_limit[phy_ix] =
+	    phy_dom.min_limit[0];
+
+	perf_plugins_ctx.full_perf_table.adj_max_limit[phy_ix] =
+	    phy_dom.adj_max_limit[0];
+	perf_plugins_ctx.full_perf_table.adj_min_limit[phy_ix] =
+	    phy_dom.adj_min_limit[0];
     }
 }
 
@@ -433,33 +433,33 @@ void perf_plugins_handler_update(
      * call the plugin - if any - with the data snapshot for this domain.
      */
     if ((this_dom_idx == last_logical_dom_idx) &&
-        (config->plugins_count != 0)) {
-        for (size_t i = 0; i < config->plugins_count; i++) {
-            dom_type = config->plugins[i].dom_type;
-
-            if ((perf_dom_idx != (config->perf_doms_count - 1)) &&
-                (dom_type == PERF_PLUGIN_DOM_TYPE_FULL)) {
-                /*
-                 * For plugins that need the full snapshot, wait until the
-                 * last performance domain.
-                 */
-                continue;
-            }
-
-            assign_data_for_plugins(
-                fc_update->domain_id, &perf_snapshot, dom_type);
-
-            api = perf_plugins_ctx.plugins_api_table[i];
-            status = api->update(&perf_snapshot);
-            if (status != FWK_SUCCESS) {
-                FWK_LOG_DEBUG(
-                    "[P-Handler] Update: Plugin%u returned error %i",
-                    (unsigned int)i,
-                    status);
-            }
+	(config->plugins_count != 0)) {
+	for (size_t i = 0; i < config->plugins_count; i++) {
+	    dom_type = config->plugins[i].dom_type;
+
+	    if ((perf_dom_idx != (config->perf_doms_count - 1)) &&
+		(dom_type == PERF_PLUGIN_DOM_TYPE_FULL)) {
+		/*
+		 * For plugins that need the full snapshot, wait until the
+		 * last performance domain.
+		 */
+		continue;
+	    }
+
+	    assign_data_for_plugins(
+		fc_update->domain_id, &perf_snapshot, dom_type);
+
+	    api = perf_plugins_ctx.plugins_api_table[i];
+	    status = api->update(&perf_snapshot);
+	    if (status != FWK_SUCCESS) {
+		FWK_LOG_DEBUG(
+		    "[P-Handler] Update: Plugin%u returned error %i",
+		    (unsigned int)i,
+		    status);
+	    }
 
-            plugins_policy_sync_level_limits(dev_ctx, dom_type);
-        }
+	    plugins_policy_sync_level_limits(dev_ctx, dom_type);
+	}
     }
 }
 
@@ -479,20 +479,20 @@ void perf_plugins_handler_get(
     config = perf_plugins_ctx.config;
 
     if (this_dom_idx != last_logical_dom_idx) {
-        /*
-         * Any other logical domain, no need to forward the request to DVFS,
-         * thus use previous values.
-         */
-        fc_update->level = dev_ctx->max;
-        fc_update->adj_max_limit = dev_ctx->lmax;
-        fc_update->adj_min_limit = dev_ctx->lmin;
+	/*
+	 * Any other logical domain, no need to forward the request to DVFS,
+	 * thus use previous values.
+	 */
+	fc_update->level = dev_ctx->max;
+	fc_update->adj_max_limit = dev_ctx->lmax;
+	fc_update->adj_min_limit = dev_ctx->lmin;
     } else {
-        if (config->plugins_count == 0) {
-            /* domains coordination only */
-            plugins_policy_update_no_plugins(fc_update);
-        } else {
-            plugins_policy_update(fc_update);
-        }
+	if (config->plugins_count == 0) {
+	    /* domains coordination only */
+	    plugins_policy_update_no_plugins(fc_update);
+	} else {
+	    plugins_policy_update(fc_update);
+	}
     }
 }
 
@@ -502,21 +502,21 @@ void perf_plugins_handler_report(struct
     int status;
 
     if (perf_plugins_ctx.config->plugins_count == 0) {
-        return;
+	return;
     }
 
     for (size_t i = 0; i < perf_plugins_ctx.config->plugins_count; i++) {
-        api = perf_plugins_ctx.plugins_api_table[i];
+	api = perf_plugins_ctx.plugins_api_table[i];
 
-        if (api->report != NULL) {
-            status = api->report(data);
-            if (status != FWK_SUCCESS) {
-                FWK_LOG_DEBUG(
-                    "[P-Handler] Report: Plugin%u returned error %i",
-                    (unsigned int)i,
-                    status);
-            }
-        }
+	if (api->report != NULL) {
+	    status = api->report(data);
+	    if (status != FWK_SUCCESS) {
+		FWK_LOG_DEBUG(
+		    "[P-Handler] Report: Plugin%u returned error %i",
+		    (unsigned int)i,
+		    status);
+	    }
+	}
     }
 }
 
@@ -542,32 +542,32 @@ int perf_plugins_handler_init(const stru
     bool has_phy_group;
 
     dvfs_doms_count =
-        fwk_module_get_element_count(FWK_ID_MODULE(FWK_MODULE_IDX_DVFS));
+	fwk_module_get_element_count(FWK_ID_MODULE(FWK_MODULE_IDX_DVFS));
     if (dvfs_doms_count <= 0) {
-        return FWK_E_SUPPORT;
+	return FWK_E_SUPPORT;
     }
 
     perf_plugins_ctx.dvfs_doms_count = (size_t)dvfs_doms_count;
 
     perf_plugins_ctx.dev_ctx = fwk_mm_calloc(
-        (size_t)dvfs_doms_count, sizeof(struct perf_plugins_dev_ctx));
+	(size_t)dvfs_doms_count, sizeof(struct perf_plugins_dev_ctx));
 
     /*
      * Iterate all over the SCMI performance domain table to build the number of
      * logical domains that belong to the same physical domain.
      */
     for (size_t i = 0; i < config->perf_doms_count; i++) {
-        domain = &(*config->domains)[i];
+	domain = &(*config->domains)[i];
 
-        has_phy_group = fwk_optional_id_is_defined(domain->phy_group_id);
-        if (has_phy_group) {
-            phy_group = fwk_id_get_element_idx(domain->phy_group_id);
-        } else {
-            phy_group = i;
-        }
+	has_phy_group = fwk_optional_id_is_defined(domain->phy_group_id);
+	if (has_phy_group) {
+	    phy_group = fwk_id_get_element_idx(domain->phy_group_id);
+	} else {
+	    phy_group = i;
+	}
 
-        dev_ctx = &perf_plugins_ctx.dev_ctx[phy_group];
-        dev_ctx->log_dom_count++;
+	dev_ctx = &perf_plugins_ctx.dev_ctx[phy_group];
+	dev_ctx->log_dom_count++;
     }
 
     /*
@@ -578,15 +578,15 @@ int perf_plugins_handler_init(const stru
      * At the same time, initialise min and max.
      */
     for (size_t i = 0; i < perf_plugins_ctx.dvfs_doms_count; i++) {
-        dev_ctx = &perf_plugins_ctx.dev_ctx[i];
+	dev_ctx = &perf_plugins_ctx.dev_ctx[i];
 
-        all_doms_count = (size_t)(dev_ctx->log_dom_count + 1);
+	all_doms_count = (size_t)(dev_ctx->log_dom_count + 1);
 
-        perf_plugins_alloc_tables(&dev_ctx->perf_table, all_doms_count);
+	perf_plugins_alloc_tables(&dev_ctx->perf_table, all_doms_count);
 
-        dev_ctx->lmin = 0;
-        dev_ctx->lmax = UINT32_MAX;
-        dev_ctx->perf_table.domain_id = FWK_ID_ELEMENT(FWK_MODULE_IDX_DVFS, i);
+	dev_ctx->lmin = 0;
+	dev_ctx->lmax = UINT32_MAX;
+	dev_ctx->perf_table.domain_id = FWK_ID_ELEMENT(FWK_MODULE_IDX_DVFS, i);
     }
 
     /*
@@ -594,18 +594,18 @@ int perf_plugins_handler_init(const stru
      * physical domain for plugins that require the full picture of performance.
      */
     perf_plugins_alloc_tables(
-        &perf_plugins_ctx.full_perf_table, perf_plugins_ctx.dvfs_doms_count);
+	&perf_plugins_ctx.full_perf_table, perf_plugins_ctx.dvfs_doms_count);
 
     /*
      * The data provided for a full snapshot will always have a module
      * identifier.
      */
     perf_plugins_ctx.full_perf_table.domain_id =
-        FWK_ID_MODULE(FWK_MODULE_IDX_SCMI_PERF);
+	FWK_ID_MODULE(FWK_MODULE_IDX_SCMI_PERF);
 
     /* Allocate a table of dependency domains identifiers */
     perf_plugins_ctx.dep_id_table =
-        fwk_mm_calloc(config->perf_doms_count, sizeof(fwk_id_t));
+	fwk_mm_calloc(config->perf_doms_count, sizeof(fwk_id_t));
 
     domain = &(*config->domains)[0];
 
@@ -617,28 +617,28 @@ int perf_plugins_handler_init(const stru
      * for the same physical domain.
      */
     for (size_t j = 0; j < config->perf_doms_count; j++) {
-        has_phy_group = fwk_optional_id_is_defined(domain->phy_group_id);
-        if (has_phy_group) {
-            /* Custom mapping */
-            pgroup = fwk_id_get_element_idx(domain->phy_group_id);
-            domain = &(*config->domains)[j];
-
-            if (fwk_id_get_element_idx(domain->phy_group_id) != pgroup) {
-                /* Restart count of logical domains */
-                ldom = 0;
-            }
-
-            perf_plugins_ctx.dep_id_table[j] = FWK_ID_SUB_ELEMENT(
-                FWK_MODULE_IDX_DVFS,
-                fwk_id_get_element_idx(domain->phy_group_id),
-                ldom);
-
-            ldom++;
-        } else {
-            /* Direct implicit mapping to dvfs */
-            perf_plugins_ctx.dep_id_table[j] =
-                FWK_ID_SUB_ELEMENT(FWK_MODULE_IDX_DVFS, j, 0);
-        }
+	has_phy_group = fwk_optional_id_is_defined(domain->phy_group_id);
+	if (has_phy_group) {
+	    /* Custom mapping */
+	    pgroup = fwk_id_get_element_idx(domain->phy_group_id);
+	    domain = &(*config->domains)[j];
+
+	    if (fwk_id_get_element_idx(domain->phy_group_id) != pgroup) {
+		/* Restart count of logical domains */
+		ldom = 0;
+	    }
+
+	    perf_plugins_ctx.dep_id_table[j] = FWK_ID_SUB_ELEMENT(
+		FWK_MODULE_IDX_DVFS,
+		fwk_id_get_element_idx(domain->phy_group_id),
+		ldom);
+
+	    ldom++;
+	} else {
+	    /* Direct implicit mapping to dvfs */
+	    perf_plugins_ctx.dep_id_table[j] =
+		FWK_ID_SUB_ELEMENT(FWK_MODULE_IDX_DVFS, j, 0);
+	}
     }
 
     perf_plugins_ctx.config = config;
@@ -653,24 +653,24 @@ int perf_plugins_handler_bind(void)
     int status;
 
     if (config->plugins_count == 0) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     perf_plugins_ctx.plugins_api_table =
-        fwk_mm_calloc(config->plugins_count, sizeof(struct perf_plugins_api *));
+	fwk_mm_calloc(config->plugins_count, sizeof(struct perf_plugins_api *));
 
     /* Bind to all the performance plugins */
     for (size_t i = 0; i < config->plugins_count; i++) {
-        plugin_id = config->plugins[i].id;
+	plugin_id = config->plugins[i].id;
 
-        status = fwk_module_bind(
-            plugin_id,
-            FWK_ID_API(
-                fwk_id_get_module_idx(plugin_id), MOD_SCMI_PERF_PLUGIN_API),
-            &perf_plugins_ctx.plugins_api_table[i]);
-        if (status != FWK_SUCCESS) {
-            return FWK_E_PARAM;
-        }
+	status = fwk_module_bind(
+	    plugin_id,
+	    FWK_ID_API(
+		fwk_id_get_module_idx(plugin_id), MOD_SCMI_PERF_PLUGIN_API),
+	    &perf_plugins_ctx.plugins_api_table[i]);
+	if (status != FWK_SUCCESS) {
+	    return FWK_E_PARAM;
+	}
     }
 
     return FWK_SUCCESS;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_perf/test/config_scmi_perf.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi_perf/test/config_scmi_perf.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_perf/test/config_scmi_perf.h
@@ -29,14 +29,14 @@ enum dvfs_element_idx {
 
 static const struct mod_scmi_perf_domain_config domains[] = {
     [DVFS_ELEMENT_IDX_0] = {
-        .fast_channels_addr_scp =
-            (uint64_t[]){
-                [MOD_SCMI_PERF_FAST_CHANNEL_LEVEL_GET] = 1,
-            },
-        .fast_channels_addr_ap =
-            (uint64_t[]){
-                [MOD_SCMI_PERF_FAST_CHANNEL_LEVEL_GET] = 1,
-            },
+	.fast_channels_addr_scp =
+	    (uint64_t[]){
+		[MOD_SCMI_PERF_FAST_CHANNEL_LEVEL_GET] = 1,
+	    },
+	.fast_channels_addr_ap =
+	    (uint64_t[]){
+		[MOD_SCMI_PERF_FAST_CHANNEL_LEVEL_GET] = 1,
+	    },
     },
     [DVFS_ELEMENT_IDX_1] = {},
     [DVFS_ELEMENT_IDX_2] = {},
@@ -45,20 +45,20 @@ static const struct mod_scmi_perf_domain
 #ifdef BUILD_HAS_SCMI_PERF_PLUGIN_HANDLER
 static const struct mod_scmi_plugin_config plugins_table[] = {
     [0] = {
-        .id = FWK_ID_NONE_INIT,
-        .dom_type = PERF_PLUGIN_DOM_TYPE_PHYSICAL,
+	.id = FWK_ID_NONE_INIT,
+	.dom_type = PERF_PLUGIN_DOM_TYPE_PHYSICAL,
     },
 };
 #endif
 
 struct fwk_module_config config_scmi_perf = {
     .data = &((struct mod_scmi_perf_config){
-        .domains = &domains,
-        .perf_doms_count = FWK_ARRAY_SIZE(domains),
-        .fast_channels_alarm_id = FWK_ID_NONE_INIT,
+	.domains = &domains,
+	.perf_doms_count = FWK_ARRAY_SIZE(domains),
+	.fast_channels_alarm_id = FWK_ID_NONE_INIT,
 #ifdef BUILD_HAS_SCMI_PERF_PLUGIN_HANDLER
-        .plugins = plugins_table,
-        .plugins_count = FWK_ARRAY_SIZE(plugins_table),
+	.plugins = plugins_table,
+	.plugins_count = FWK_ARRAY_SIZE(plugins_table),
 #endif
     }),
 };
@@ -67,32 +67,32 @@ static const struct mod_dvfs_domain_conf
     .latency = 1200,
     .sustained_idx = 2,
     .opps =
-        (struct mod_dvfs_opp[]){
-            {
-                .level = 100 * 1000000UL,
-                .frequency = 101 * FWK_KHZ,
-                .voltage = 102,
-            },
-            {
-                .level = 200 * 1000000UL,
-                .frequency = 201 * FWK_KHZ,
-                .voltage = 202,
-            },
-            {
-                .level = 300 * 1000000UL,
-                .frequency = 301 * FWK_KHZ,
-                .voltage = 302,
-            },
-            {
-                .level = 400 * 1000000UL,
-                .frequency = 401 * FWK_KHZ,
-                .voltage = 402,
-            },
-            {
-                .level = 500 * 1000000UL,
-                .frequency = 501 * FWK_KHZ,
-                .voltage = 502,
-            },
-            { 0 },
-        }
+	(struct mod_dvfs_opp[]){
+	    {
+		.level = 100 * 1000000UL,
+		.frequency = 101 * FWK_KHZ,
+		.voltage = 102,
+	    },
+	    {
+		.level = 200 * 1000000UL,
+		.frequency = 201 * FWK_KHZ,
+		.voltage = 202,
+	    },
+	    {
+		.level = 300 * 1000000UL,
+		.frequency = 301 * FWK_KHZ,
+		.voltage = 302,
+	    },
+	    {
+		.level = 400 * 1000000UL,
+		.frequency = 401 * FWK_KHZ,
+		.voltage = 402,
+	    },
+	    {
+		.level = 500 * 1000000UL,
+		.frequency = 501 * FWK_KHZ,
+		.voltage = 502,
+	    },
+	    { 0 },
+	}
 };
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_perf/test/mocks/Mockmod_scmi_perf_extra.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi_perf/test/mocks/Mockmod_scmi_perf_extra.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_perf/test/mocks/Mockmod_scmi_perf_extra.c
@@ -650,7 +650,7 @@ void CMockExpectParameters_mod_scmi_from
 void CMockExpectParameters_mod_scmi_from_protocol_api_get_agent_id(CMOCK_mod_scmi_from_protocol_api_get_agent_id_CALL_INSTANCE* cmock_call_instance, fwk_id_t service_id, unsigned int* agent_id)
 {
   memcpy((void*)(&cmock_call_instance->Expected_service_id), (void*)(&service_id),
-         sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
+	 sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
   cmock_call_instance->Expected_agent_id = agent_id;
   cmock_call_instance->ReturnThruPtr_agent_id_Used = 0;
 }
@@ -843,7 +843,7 @@ void CMockExpectParameters_mod_scmi_from
 void CMockExpectParameters_mod_scmi_from_protocol_api_get_max_payload_size(CMOCK_mod_scmi_from_protocol_api_get_max_payload_size_CALL_INSTANCE* cmock_call_instance, fwk_id_t service_id, size_t* size)
 {
   memcpy((void*)(&cmock_call_instance->Expected_service_id), (void*)(&service_id),
-         sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
+	 sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
   cmock_call_instance->Expected_size = size;
   cmock_call_instance->ReturnThruPtr_size_Used = 0;
 }
@@ -945,12 +945,12 @@ void CMockExpectParameters_mod_scmi_from
 void CMockExpectParameters_mod_scmi_from_protocol_api_write_payload(CMOCK_mod_scmi_from_protocol_api_write_payload_CALL_INSTANCE* cmock_call_instance, fwk_id_t service_id, size_t offset, const void* payload, size_t size)
 {
   memcpy((void*)(&cmock_call_instance->Expected_service_id), (void*)(&service_id),
-         sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
+	 sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
   memcpy((void*)(&cmock_call_instance->Expected_offset), (void*)(&offset),
-         sizeof(size_t[sizeof(offset) == sizeof(size_t) ? 1 : -1])); /* add size_t to :treat_as_array if this causes an error */
+	 sizeof(size_t[sizeof(offset) == sizeof(size_t) ? 1 : -1])); /* add size_t to :treat_as_array if this causes an error */
   cmock_call_instance->Expected_payload = payload;
   memcpy((void*)(&cmock_call_instance->Expected_size), (void*)(&size),
-         sizeof(size_t[sizeof(size) == sizeof(size_t) ? 1 : -1])); /* add size_t to :treat_as_array if this causes an error */
+	 sizeof(size_t[sizeof(size) == sizeof(size_t) ? 1 : -1])); /* add size_t to :treat_as_array if this causes an error */
 }
 
 void mod_scmi_from_protocol_api_write_payload_CMockExpectAnyArgsAndReturn(UNITY_LINE_TYPE cmock_line, int cmock_to_return)
@@ -1037,10 +1037,10 @@ void CMockExpectParameters_mod_scmi_from
 void CMockExpectParameters_mod_scmi_from_protocol_api_respond(CMOCK_mod_scmi_from_protocol_api_respond_CALL_INSTANCE* cmock_call_instance, fwk_id_t service_id, const void* payload, size_t size)
 {
   memcpy((void*)(&cmock_call_instance->Expected_service_id), (void*)(&service_id),
-         sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
+	 sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
   cmock_call_instance->Expected_payload = payload;
   memcpy((void*)(&cmock_call_instance->Expected_size), (void*)(&size),
-         sizeof(size_t[sizeof(size) == sizeof(size_t) ? 1 : -1])); /* add size_t to :treat_as_array if this causes an error */
+	 sizeof(size_t[sizeof(size) == sizeof(size_t) ? 1 : -1])); /* add size_t to :treat_as_array if this causes an error */
 }
 
 void mod_scmi_from_protocol_api_respond_CMockExpectAnyArgsAndReturn(UNITY_LINE_TYPE cmock_line, int cmock_to_return)
@@ -1134,12 +1134,12 @@ void CMockExpectParameters_mod_scmi_from
 void CMockExpectParameters_mod_scmi_from_protocol_api_notify(CMOCK_mod_scmi_from_protocol_api_notify_CALL_INSTANCE* cmock_call_instance, fwk_id_t service_id, int protocol_id, int message_id, const void* payload, size_t size)
 {
   memcpy((void*)(&cmock_call_instance->Expected_service_id), (void*)(&service_id),
-         sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
+	 sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
   cmock_call_instance->Expected_protocol_id = protocol_id;
   cmock_call_instance->Expected_message_id = message_id;
   cmock_call_instance->Expected_payload = payload;
   memcpy((void*)(&cmock_call_instance->Expected_size), (void*)(&size),
-         sizeof(size_t[sizeof(size) == sizeof(size_t) ? 1 : -1])); /* add size_t to :treat_as_array if this causes an error */
+	 sizeof(size_t[sizeof(size) == sizeof(size_t) ? 1 : -1])); /* add size_t to :treat_as_array if this causes an error */
 }
 
 void mod_scmi_from_protocol_api_notify_CMockExpectAnyArgs(UNITY_LINE_TYPE cmock_line)
@@ -1243,10 +1243,10 @@ void CMockExpectParameters_mod_scmi_from
   cmock_call_instance->Expected_scmi_protocol_id = scmi_protocol_id;
   cmock_call_instance->Expected_token = token;
   memcpy((void*)(&cmock_call_instance->Expected_service_id), (void*)(&service_id),
-         sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
+	 sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
   cmock_call_instance->Expected_payload = payload;
   memcpy((void*)(&cmock_call_instance->Expected_payload_size), (void*)(&payload_size),
-         sizeof(size_t[sizeof(payload_size) == sizeof(size_t) ? 1 : -1])); /* add size_t to :treat_as_array if this causes an error */
+	 sizeof(size_t[sizeof(payload_size) == sizeof(size_t) ? 1 : -1])); /* add size_t to :treat_as_array if this causes an error */
   cmock_call_instance->Expected_request_ack_by_interrupt = request_ack_by_interrupt;
 }
 
@@ -1323,7 +1323,7 @@ void CMockExpectParameters_mod_scmi_from
 void CMockExpectParameters_mod_scmi_from_protocol_api_response_message_handler(CMOCK_mod_scmi_from_protocol_api_response_message_handler_CALL_INSTANCE* cmock_call_instance, fwk_id_t service_id)
 {
   memcpy((void*)(&cmock_call_instance->Expected_service_id), (void*)(&service_id),
-         sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
+	 sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
 }
 
 void mod_scmi_from_protocol_api_response_message_handler_CMockExpectAnyArgsAndReturn(UNITY_LINE_TYPE cmock_line, int cmock_to_return)
@@ -1409,7 +1409,7 @@ void CMockExpectParameters_mod_dvfs_doma
 void CMockExpectParameters_mod_dvfs_domain_api_get_current_opp(CMOCK_mod_dvfs_domain_api_get_current_opp_CALL_INSTANCE* cmock_call_instance, fwk_id_t domain_id, struct mod_dvfs_opp* opp)
 {
   memcpy((void*)(&cmock_call_instance->Expected_domain_id), (void*)(&domain_id),
-         sizeof(fwk_id_t[sizeof(domain_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
+	 sizeof(fwk_id_t[sizeof(domain_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
   cmock_call_instance->Expected_opp = opp;
   cmock_call_instance->ReturnThruPtr_opp_Used = 0;
 }
@@ -1506,7 +1506,7 @@ void CMockExpectParameters_mod_dvfs_doma
 void CMockExpectParameters_mod_dvfs_domain_api_get_sustained_opp(CMOCK_mod_dvfs_domain_api_get_sustained_opp_CALL_INSTANCE* cmock_call_instance, fwk_id_t domain_id, struct mod_dvfs_opp* opp)
 {
   memcpy((void*)(&cmock_call_instance->Expected_domain_id), (void*)(&domain_id),
-         sizeof(fwk_id_t[sizeof(domain_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
+	 sizeof(fwk_id_t[sizeof(domain_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
   cmock_call_instance->Expected_opp = opp;
   cmock_call_instance->ReturnThruPtr_opp_Used = 0;
 }
@@ -1607,9 +1607,9 @@ void CMockExpectParameters_mod_dvfs_doma
 void CMockExpectParameters_mod_dvfs_domain_api_get_nth_opp(CMOCK_mod_dvfs_domain_api_get_nth_opp_CALL_INSTANCE* cmock_call_instance, fwk_id_t domain_id, size_t n, struct mod_dvfs_opp* opp)
 {
   memcpy((void*)(&cmock_call_instance->Expected_domain_id), (void*)(&domain_id),
-         sizeof(fwk_id_t[sizeof(domain_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
+	 sizeof(fwk_id_t[sizeof(domain_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
   memcpy((void*)(&cmock_call_instance->Expected_n), (void*)(&n),
-         sizeof(size_t[sizeof(n) == sizeof(size_t) ? 1 : -1])); /* add size_t to :treat_as_array if this causes an error */
+	 sizeof(size_t[sizeof(n) == sizeof(size_t) ? 1 : -1])); /* add size_t to :treat_as_array if this causes an error */
   cmock_call_instance->Expected_opp = opp;
   cmock_call_instance->ReturnThruPtr_opp_Used = 0;
 }
@@ -1706,7 +1706,7 @@ void CMockExpectParameters_mod_dvfs_doma
 void CMockExpectParameters_mod_dvfs_domain_api_get_opp_count(CMOCK_mod_dvfs_domain_api_get_opp_count_CALL_INSTANCE* cmock_call_instance, fwk_id_t domain_id, size_t* opp_count)
 {
   memcpy((void*)(&cmock_call_instance->Expected_domain_id), (void*)(&domain_id),
-         sizeof(fwk_id_t[sizeof(domain_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
+	 sizeof(fwk_id_t[sizeof(domain_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
   cmock_call_instance->Expected_opp_count = opp_count;
   cmock_call_instance->ReturnThruPtr_opp_count_Used = 0;
 }
@@ -1807,7 +1807,7 @@ void CMockExpectParameters_mod_dvfs_doma
 void CMockExpectParameters_mod_dvfs_domain_api_get_level_id(CMOCK_mod_dvfs_domain_api_get_level_id_CALL_INSTANCE* cmock_call_instance, fwk_id_t domain_id, uint32_t level, size_t* level_id)
 {
   memcpy((void*)(&cmock_call_instance->Expected_domain_id), (void*)(&domain_id),
-         sizeof(fwk_id_t[sizeof(domain_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
+	 sizeof(fwk_id_t[sizeof(domain_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
   cmock_call_instance->Expected_level = level;
   cmock_call_instance->Expected_level_id = level_id;
   cmock_call_instance->ReturnThruPtr_level_id_Used = 0;
@@ -1908,7 +1908,7 @@ void CMockExpectParameters_mod_dvfs_doma
 void CMockExpectParameters_mod_dvfs_domain_api_get_latency(CMOCK_mod_dvfs_domain_api_get_latency_CALL_INSTANCE* cmock_call_instance, fwk_id_t domain_id, uint16_t* latency)
 {
   memcpy((void*)(&cmock_call_instance->Expected_domain_id), (void*)(&domain_id),
-         sizeof(fwk_id_t[sizeof(domain_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
+	 sizeof(fwk_id_t[sizeof(domain_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
   cmock_call_instance->Expected_latency = latency;
   cmock_call_instance->ReturnThruPtr_latency_Used = 0;
 }
@@ -2003,9 +2003,9 @@ void CMockExpectParameters_mod_dvfs_doma
 void CMockExpectParameters_mod_dvfs_domain_api_set_level(CMOCK_mod_dvfs_domain_api_set_level_CALL_INSTANCE* cmock_call_instance, fwk_id_t domain_id, uintptr_t cookie, uint32_t level)
 {
   memcpy((void*)(&cmock_call_instance->Expected_domain_id), (void*)(&domain_id),
-         sizeof(fwk_id_t[sizeof(domain_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
+	 sizeof(fwk_id_t[sizeof(domain_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
   memcpy((void*)(&cmock_call_instance->Expected_cookie), (void*)(&cookie),
-         sizeof(uintptr_t[sizeof(cookie) == sizeof(uintptr_t) ? 1 : -1])); /* add uintptr_t to :treat_as_array if this causes an error */
+	 sizeof(uintptr_t[sizeof(cookie) == sizeof(uintptr_t) ? 1 : -1])); /* add uintptr_t to :treat_as_array if this causes an error */
   cmock_call_instance->Expected_level = level;
 }
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_perf/test/mod_scmi_perf_unit_test.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi_perf/test/mod_scmi_perf_unit_test.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_perf/test/mod_scmi_perf_unit_test.c
@@ -76,10 +76,10 @@ void setUp(void)
     fwk_id_get_api_idx_ExpectAnyArgsAndReturn(MOD_SCMI_PERF_PROTOCOL_API);
 
     module_scmi_perf.process_bind_request(
-        fwk_module_id_scmi,
-        fwk_module_id_scmi_perf,
-        FWK_ID_API(FWK_MODULE_IDX_SCMI_PERF, 0),
-        (const void **)&to_protocol_api);
+	fwk_module_id_scmi,
+	fwk_module_id_scmi_perf,
+	FWK_ID_API(FWK_MODULE_IDX_SCMI_PERF, 0),
+	(const void **)&to_protocol_api);
 
     scmi_perf_ctx.dvfs_api = &dvfs_domain_api;
 }
@@ -128,19 +128,19 @@ void utest_scmi_perf_message_handler_bad
 {
     int status;
     fwk_id_t service_id =
-        FWK_ID_ELEMENT_INIT(TEST_MODULE_IDX, TEST_SCMI_AGENT_IDX_0);
+	FWK_ID_ELEMENT_INIT(TEST_MODULE_IDX, TEST_SCMI_AGENT_IDX_0);
     struct scmi_protocol_version_p2a payload = { 0 };
 
     mod_scmi_from_protocol_api_respond_Stub(
-        version_handler_respond_callback_fail);
+	version_handler_respond_callback_fail);
 
     return_status = SCMI_NOT_SUPPORTED;
     status = scmi_perf_message_handler(
-        (fwk_id_t)MOD_SCMI_PROTOCOL_ID_PERF,
-        service_id,
-        (const uint32_t *)&payload,
-        payload_size_table[MOD_SCMI_PROTOCOL_VERSION],
-        MOD_SCMI_PERF_DESCRIBE_FAST_CHANNEL);
+	(fwk_id_t)MOD_SCMI_PROTOCOL_ID_PERF,
+	service_id,
+	(const uint32_t *)&payload,
+	payload_size_table[MOD_SCMI_PROTOCOL_VERSION],
+	MOD_SCMI_PERF_DESCRIBE_FAST_CHANNEL);
 
     TEST_ASSERT_EQUAL(FWK_SUCCESS, status);
 }
@@ -149,19 +149,19 @@ void utest_scmi_perf_message_handler_not
 {
     int status;
     fwk_id_t service_id =
-        FWK_ID_ELEMENT_INIT(TEST_MODULE_IDX, TEST_SCMI_AGENT_IDX_0);
+	FWK_ID_ELEMENT_INIT(TEST_MODULE_IDX, TEST_SCMI_AGENT_IDX_0);
     struct scmi_protocol_version_p2a payload = { 0 };
 
     mod_scmi_from_protocol_api_respond_Stub(
-        version_handler_respond_callback_fail);
+	version_handler_respond_callback_fail);
 
     return_status = SCMI_NOT_FOUND;
     status = scmi_perf_message_handler(
-        (fwk_id_t)MOD_SCMI_PROTOCOL_ID_PERF,
-        service_id,
-        (const uint32_t *)&payload,
-        payload_size_table[MOD_SCMI_PROTOCOL_VERSION],
-        FWK_ARRAY_SIZE(handler_table));
+	(fwk_id_t)MOD_SCMI_PROTOCOL_ID_PERF,
+	service_id,
+	(const uint32_t *)&payload,
+	payload_size_table[MOD_SCMI_PROTOCOL_VERSION],
+	FWK_ARRAY_SIZE(handler_table));
 
     TEST_ASSERT_EQUAL(FWK_SUCCESS, status);
 }
@@ -170,7 +170,7 @@ void utest_scmi_perf_protocol_version_ha
 {
     int status;
     fwk_id_t service_id =
-        FWK_ID_ELEMENT_INIT(TEST_MODULE_IDX, TEST_SCMI_AGENT_IDX_0);
+	FWK_ID_ELEMENT_INIT(TEST_MODULE_IDX, TEST_SCMI_AGENT_IDX_0);
 
     uint32_t payload = 0;
 
@@ -181,11 +181,11 @@ void utest_scmi_perf_protocol_version_ha
     mod_scmi_from_protocol_api_respond_Stub(version_handler_respond_callback);
 
     status = to_protocol_api->message_handler(
-        (fwk_id_t)MOD_SCMI_PROTOCOL_ID_PERF,
-        service_id,
-        (const uint32_t *)&payload,
-        payload_size_table[MOD_SCMI_PROTOCOL_VERSION],
-        MOD_SCMI_PROTOCOL_VERSION);
+	(fwk_id_t)MOD_SCMI_PROTOCOL_ID_PERF,
+	service_id,
+	(const uint32_t *)&payload,
+	payload_size_table[MOD_SCMI_PROTOCOL_VERSION],
+	MOD_SCMI_PROTOCOL_VERSION);
 
     TEST_ASSERT_EQUAL(FWK_SUCCESS, status);
 }
@@ -206,8 +206,8 @@ int attributes_handler_respond_callback(
 
     TEST_ASSERT_EQUAL((int32_t)SCMI_SUCCESS, return_values->status);
     TEST_ASSERT_EQUAL(
-        SCMI_PERF_PROTOCOL_ATTRIBUTES(true, scmi_perf_ctx.domain_count),
-        return_values->attributes);
+	SCMI_PERF_PROTOCOL_ATTRIBUTES(true, scmi_perf_ctx.domain_count),
+	return_values->attributes);
     TEST_ASSERT_EQUAL(0, return_values->statistics_len);
 
     return FWK_SUCCESS;
@@ -217,18 +217,18 @@ void utest_scmi_perf_protocol_attributes
 {
     int status;
     fwk_id_t service_id =
-        FWK_ID_ELEMENT_INIT(TEST_MODULE_IDX, TEST_SCMI_AGENT_IDX_0);
+	FWK_ID_ELEMENT_INIT(TEST_MODULE_IDX, TEST_SCMI_AGENT_IDX_0);
     struct scmi_protocol_attributes_p2a payload = {};
 
     mod_scmi_from_protocol_api_respond_Stub(
-        attributes_handler_respond_callback);
+	attributes_handler_respond_callback);
 
     status = to_protocol_api->message_handler(
-        (fwk_id_t)MOD_SCMI_PROTOCOL_ID_PERF,
-        service_id,
-        (const uint32_t *)&payload,
-        payload_size_table[MOD_SCMI_PROTOCOL_ATTRIBUTES],
-        MOD_SCMI_PROTOCOL_ATTRIBUTES);
+	(fwk_id_t)MOD_SCMI_PROTOCOL_ID_PERF,
+	service_id,
+	(const uint32_t *)&payload,
+	payload_size_table[MOD_SCMI_PROTOCOL_ATTRIBUTES],
+	MOD_SCMI_PROTOCOL_ATTRIBUTES);
 
     TEST_ASSERT_EQUAL(FWK_SUCCESS, status);
 }
@@ -259,21 +259,21 @@ void utest_scmi_perf_protocol_message_at
 {
     int status;
     fwk_id_t service_id =
-        FWK_ID_ELEMENT_INIT(TEST_MODULE_IDX, TEST_SCMI_AGENT_IDX_0);
+	FWK_ID_ELEMENT_INIT(TEST_MODULE_IDX, TEST_SCMI_AGENT_IDX_0);
 
     struct scmi_protocol_message_attributes_a2p payload = {
-        .message_id = MOD_SCMI_PERF_LIMITS_GET,
+	.message_id = MOD_SCMI_PERF_LIMITS_GET,
     };
 
     mod_scmi_from_protocol_api_respond_Stub(
-        message_attributes_handler_valid_param_respond_callback);
+	message_attributes_handler_valid_param_respond_callback);
 
     status = to_protocol_api->message_handler(
-        (fwk_id_t)MOD_SCMI_PROTOCOL_ID_PERF,
-        service_id,
-        (const uint32_t *)&payload,
-        payload_size_table[MOD_SCMI_PROTOCOL_MESSAGE_ATTRIBUTES],
-        MOD_SCMI_PROTOCOL_MESSAGE_ATTRIBUTES);
+	(fwk_id_t)MOD_SCMI_PROTOCOL_ID_PERF,
+	service_id,
+	(const uint32_t *)&payload,
+	payload_size_table[MOD_SCMI_PROTOCOL_MESSAGE_ATTRIBUTES],
+	MOD_SCMI_PROTOCOL_MESSAGE_ATTRIBUTES);
 
     TEST_ASSERT_EQUAL(FWK_SUCCESS, status);
 }
@@ -298,21 +298,21 @@ void utest_scmi_perf_protocol_message_at
 {
     int status;
     fwk_id_t service_id =
-        FWK_ID_ELEMENT_INIT(TEST_MODULE_IDX, TEST_SCMI_AGENT_IDX_0);
+	FWK_ID_ELEMENT_INIT(TEST_MODULE_IDX, TEST_SCMI_AGENT_IDX_0);
 
     struct scmi_protocol_message_attributes_a2p payload = {
-        .message_id = FWK_ARRAY_SIZE(handler_table),
+	.message_id = FWK_ARRAY_SIZE(handler_table),
     };
 
     mod_scmi_from_protocol_api_respond_Stub(
-        message_attributes_handler_invalid_param_respond_callback);
+	message_attributes_handler_invalid_param_respond_callback);
 
     status = to_protocol_api->message_handler(
-        (fwk_id_t)MOD_SCMI_PROTOCOL_ID_PERF,
-        service_id,
-        (const uint32_t *)&payload,
-        payload_size_table[MOD_SCMI_PROTOCOL_MESSAGE_ATTRIBUTES],
-        MOD_SCMI_PROTOCOL_MESSAGE_ATTRIBUTES);
+	(fwk_id_t)MOD_SCMI_PROTOCOL_ID_PERF,
+	service_id,
+	(const uint32_t *)&payload,
+	payload_size_table[MOD_SCMI_PROTOCOL_MESSAGE_ATTRIBUTES],
+	MOD_SCMI_PROTOCOL_MESSAGE_ATTRIBUTES);
 
     TEST_ASSERT_EQUAL(FWK_SUCCESS, status);
 }
@@ -338,20 +338,20 @@ int domain_attributes_handler_valid_para
 #endif
 
     uint32_t permissions = ((uint32_t)MOD_SCMI_PERF_PERMS_SET_LIMITS) |
-        ((uint32_t)MOD_SCMI_PERF_PERMS_SET_LEVEL);
+	((uint32_t)MOD_SCMI_PERF_PERMS_SET_LEVEL);
 
     uint32_t expected_attributes = SCMI_PERF_DOMAIN_ATTRIBUTES(
-        (uint32_t)notifications,
-        (uint32_t)notifications,
-        ((permissions & (uint32_t)MOD_SCMI_PERF_PERMS_SET_LEVEL) !=
-         (uint32_t)0) ?
-            1U :
-            0U,
-        ((permissions & (uint32_t)MOD_SCMI_PERF_PERMS_SET_LIMITS) !=
-         (uint32_t)0) ?
-            1U :
-            0U,
-        (uint32_t)fast_channels);
+	(uint32_t)notifications,
+	(uint32_t)notifications,
+	((permissions & (uint32_t)MOD_SCMI_PERF_PERMS_SET_LEVEL) !=
+	 (uint32_t)0) ?
+	    1U :
+	    0U,
+	((permissions & (uint32_t)MOD_SCMI_PERF_PERMS_SET_LIMITS) !=
+	 (uint32_t)0) ?
+	    1U :
+	    0U,
+	(uint32_t)fast_channels);
 
     TEST_ASSERT_EQUAL(expected_attributes, return_values->attributes);
 
@@ -360,9 +360,9 @@ int domain_attributes_handler_valid_para
     struct mod_dvfs_opp expected_opp_values = test_dvfs_config.opps[0];
 
     TEST_ASSERT_EQUAL(
-        expected_opp_values.frequency, return_values->sustained_freq);
+	expected_opp_values.frequency, return_values->sustained_freq);
     TEST_ASSERT_EQUAL(
-        expected_opp_values.level, return_values->sustained_perf_level);
+	expected_opp_values.level, return_values->sustained_perf_level);
 
     TEST_ASSERT_EQUAL_STRING(name, (char *)return_values->name);
 
@@ -374,10 +374,10 @@ void utest_scmi_perf_domain_attributes_h
     int status;
 
     fwk_id_t service_id =
-        FWK_ID_ELEMENT_INIT(TEST_MODULE_IDX, TEST_SCMI_AGENT_IDX_0);
+	FWK_ID_ELEMENT_INIT(TEST_MODULE_IDX, TEST_SCMI_AGENT_IDX_0);
 
     struct scmi_perf_domain_attributes_a2p payload = {
-        .domain_id = 0,
+	.domain_id = 0,
     };
 
     unsigned int agent_id = TEST_SCMI_AGENT_IDX_0;
@@ -393,14 +393,14 @@ void utest_scmi_perf_domain_attributes_h
     fwk_module_get_element_name_ExpectAnyArgsAndReturn(name);
 
     mod_scmi_from_protocol_api_respond_Stub(
-        domain_attributes_handler_valid_param_respond_callback);
+	domain_attributes_handler_valid_param_respond_callback);
 
     status = to_protocol_api->message_handler(
-        (fwk_id_t)MOD_SCMI_PROTOCOL_ID_PERF,
-        service_id,
-        (const uint32_t *)&payload,
-        payload_size_table[MOD_SCMI_PERF_DOMAIN_ATTRIBUTES],
-        MOD_SCMI_PERF_DOMAIN_ATTRIBUTES);
+	(fwk_id_t)MOD_SCMI_PROTOCOL_ID_PERF,
+	service_id,
+	(const uint32_t *)&payload,
+	payload_size_table[MOD_SCMI_PERF_DOMAIN_ATTRIBUTES],
+	MOD_SCMI_PERF_DOMAIN_ATTRIBUTES);
 
     TEST_ASSERT_EQUAL(FWK_SUCCESS, status);
 }
@@ -426,21 +426,21 @@ void utest_scmi_perf_domain_attributes_h
     int status;
 
     fwk_id_t service_id =
-        FWK_ID_ELEMENT_INIT(TEST_MODULE_IDX, TEST_SCMI_AGENT_IDX_0);
+	FWK_ID_ELEMENT_INIT(TEST_MODULE_IDX, TEST_SCMI_AGENT_IDX_0);
 
     struct scmi_perf_domain_attributes_a2p payload = {
-        .domain_id = scmi_perf_ctx.domain_count,
+	.domain_id = scmi_perf_ctx.domain_count,
     };
 
     mod_scmi_from_protocol_api_respond_Stub(
-        domain_attributes_handler_invalid_param_respond_callback);
+	domain_attributes_handler_invalid_param_respond_callback);
 
     status = to_protocol_api->message_handler(
-        (fwk_id_t)MOD_SCMI_PROTOCOL_ID_PERF,
-        service_id,
-        (const uint32_t *)&payload,
-        payload_size_table[MOD_SCMI_PERF_DOMAIN_ATTRIBUTES],
-        MOD_SCMI_PERF_DOMAIN_ATTRIBUTES);
+	(fwk_id_t)MOD_SCMI_PROTOCOL_ID_PERF,
+	service_id,
+	(const uint32_t *)&payload,
+	payload_size_table[MOD_SCMI_PERF_DOMAIN_ATTRIBUTES],
+	MOD_SCMI_PERF_DOMAIN_ATTRIBUTES);
 
     TEST_ASSERT_EQUAL(FWK_SUCCESS, status);
 }
@@ -473,26 +473,26 @@ int describe_levels_handler_valid_param_
     int NumCalls)
 {
     if (NumCalls < TEST_OPP_COUNT) {
-        struct scmi_perf_level *returned_perf_level =
-            (struct scmi_perf_level *)payload;
+	struct scmi_perf_level *returned_perf_level =
+	    (struct scmi_perf_level *)payload;
 
-        TEST_ASSERT_EQUAL(
-            test_dvfs_config.opps[NumCalls].voltage,
-            returned_perf_level->power_cost);
-        TEST_ASSERT_EQUAL(
-            test_dvfs_config.opps[NumCalls].level,
-            returned_perf_level->performance_level);
-        TEST_ASSERT_EQUAL(
-            test_dvfs_config.latency, returned_perf_level->attributes);
+	TEST_ASSERT_EQUAL(
+	    test_dvfs_config.opps[NumCalls].voltage,
+	    returned_perf_level->power_cost);
+	TEST_ASSERT_EQUAL(
+	    test_dvfs_config.opps[NumCalls].level,
+	    returned_perf_level->performance_level);
+	TEST_ASSERT_EQUAL(
+	    test_dvfs_config.latency, returned_perf_level->attributes);
 
     } else if (NumCalls == TEST_OPP_COUNT) {
-        struct scmi_perf_describe_levels_p2a *return_values =
-            (struct scmi_perf_describe_levels_p2a *)payload;
-        TEST_ASSERT_EQUAL(SCMI_SUCCESS, return_values->status);
-        TEST_ASSERT_EQUAL(TEST_OPP_COUNT, return_values->num_levels);
+	struct scmi_perf_describe_levels_p2a *return_values =
+	    (struct scmi_perf_describe_levels_p2a *)payload;
+	TEST_ASSERT_EQUAL(SCMI_SUCCESS, return_values->status);
+	TEST_ASSERT_EQUAL(TEST_OPP_COUNT, return_values->num_levels);
 
     } else {
-        return FWK_E_RANGE;
+	return FWK_E_RANGE;
     }
 
     return FWK_SUCCESS;
@@ -503,11 +503,11 @@ void utest_scmi_perf_describe_levels_han
     int status;
 
     fwk_id_t service_id =
-        FWK_ID_ELEMENT_INIT(TEST_MODULE_IDX, TEST_SCMI_AGENT_IDX_0);
+	FWK_ID_ELEMENT_INIT(TEST_MODULE_IDX, TEST_SCMI_AGENT_IDX_0);
 
     struct scmi_perf_describe_levels_a2p payload = {
-        .domain_id = 0,
-        .level_index = 0,
+	.domain_id = 0,
+	.level_index = 0,
     };
 
     size_t size = UINT16_MAX;
@@ -519,7 +519,7 @@ void utest_scmi_perf_describe_levels_han
      * whether the returned levels are correct and in the right order
      */
     mod_scmi_from_protocol_api_get_max_payload_size_ExpectAnyArgsAndReturn(
-        FWK_SUCCESS);
+	FWK_SUCCESS);
     mod_scmi_from_protocol_api_get_max_payload_size_ReturnThruPtr_size(&size);
 
     mod_dvfs_domain_api_get_opp_count_ExpectAnyArgsAndReturn(FWK_SUCCESS);
@@ -531,18 +531,18 @@ void utest_scmi_perf_describe_levels_han
     mod_dvfs_domain_api_get_nth_opp_Stub(get_nth_opp_callback);
 
     mod_scmi_from_protocol_api_write_payload_Stub(
-        describe_levels_handler_valid_param_write_payload_callback);
+	describe_levels_handler_valid_param_write_payload_callback);
 
     /* Disable the previous callback for this function */
     mod_scmi_from_protocol_api_respond_Stub(NULL);
     mod_scmi_from_protocol_api_respond_ExpectAnyArgsAndReturn(FWK_SUCCESS);
 
     status = to_protocol_api->message_handler(
-        (fwk_id_t)MOD_SCMI_PROTOCOL_ID_PERF,
-        service_id,
-        (const uint32_t *)&payload,
-        payload_size_table[MOD_SCMI_PERF_DESCRIBE_LEVELS],
-        MOD_SCMI_PERF_DESCRIBE_LEVELS);
+	(fwk_id_t)MOD_SCMI_PROTOCOL_ID_PERF,
+	service_id,
+	(const uint32_t *)&payload,
+	payload_size_table[MOD_SCMI_PERF_DESCRIBE_LEVELS],
+	MOD_SCMI_PERF_DESCRIBE_LEVELS);
 
     TEST_ASSERT_EQUAL(FWK_SUCCESS, status);
 }
@@ -566,28 +566,28 @@ void utest_scmi_perf_describe_levels_han
     int status;
 
     fwk_id_t service_id =
-        FWK_ID_ELEMENT_INIT(TEST_MODULE_IDX, TEST_SCMI_AGENT_IDX_0);
+	FWK_ID_ELEMENT_INIT(TEST_MODULE_IDX, TEST_SCMI_AGENT_IDX_0);
 
     struct scmi_perf_describe_levels_a2p payload = {
-        .domain_id = scmi_perf_ctx.domain_count,
-        .level_index = 0,
+	.domain_id = scmi_perf_ctx.domain_count,
+	.level_index = 0,
     };
 
     size_t size = UINT16_MAX;
 
     mod_scmi_from_protocol_api_get_max_payload_size_ExpectAnyArgsAndReturn(
-        FWK_SUCCESS);
+	FWK_SUCCESS);
     mod_scmi_from_protocol_api_get_max_payload_size_ReturnThruPtr_size(&size);
 
     mod_scmi_from_protocol_api_respond_Stub(
-        describe_levels_handler_invalid_domain_id_respond_callback);
+	describe_levels_handler_invalid_domain_id_respond_callback);
 
     status = to_protocol_api->message_handler(
-        (fwk_id_t)MOD_SCMI_PROTOCOL_ID_PERF,
-        service_id,
-        (const uint32_t *)&payload,
-        payload_size_table[MOD_SCMI_PERF_DESCRIBE_LEVELS],
-        MOD_SCMI_PERF_DESCRIBE_LEVELS);
+	(fwk_id_t)MOD_SCMI_PROTOCOL_ID_PERF,
+	service_id,
+	(const uint32_t *)&payload,
+	payload_size_table[MOD_SCMI_PERF_DESCRIBE_LEVELS],
+	MOD_SCMI_PERF_DESCRIBE_LEVELS);
 
     TEST_ASSERT_EQUAL(FWK_SUCCESS, status);
 }
@@ -611,32 +611,32 @@ void utest_scmi_perf_describe_levels_han
     int status;
 
     fwk_id_t service_id =
-        FWK_ID_ELEMENT_INIT(TEST_MODULE_IDX, TEST_SCMI_AGENT_IDX_0);
+	FWK_ID_ELEMENT_INIT(TEST_MODULE_IDX, TEST_SCMI_AGENT_IDX_0);
 
     struct scmi_perf_describe_levels_a2p payload = {
-        .domain_id = 0,
-        .level_index = TEST_OPP_COUNT,
+	.domain_id = 0,
+	.level_index = TEST_OPP_COUNT,
     };
 
     size_t size = UINT16_MAX;
     size_t opp_count = TEST_OPP_COUNT;
 
     mod_scmi_from_protocol_api_get_max_payload_size_ExpectAnyArgsAndReturn(
-        FWK_SUCCESS);
+	FWK_SUCCESS);
     mod_scmi_from_protocol_api_get_max_payload_size_ReturnThruPtr_size(&size);
 
     mod_dvfs_domain_api_get_opp_count_ExpectAnyArgsAndReturn(FWK_SUCCESS);
     mod_dvfs_domain_api_get_opp_count_ReturnThruPtr_opp_count(&opp_count);
 
     mod_scmi_from_protocol_api_respond_Stub(
-        describe_levels_handler_invalid_level_index_respond_callback);
+	describe_levels_handler_invalid_level_index_respond_callback);
 
     status = to_protocol_api->message_handler(
-        (fwk_id_t)MOD_SCMI_PROTOCOL_ID_PERF,
-        service_id,
-        (const uint32_t *)&payload,
-        payload_size_table[MOD_SCMI_PERF_DESCRIBE_LEVELS],
-        MOD_SCMI_PERF_DESCRIBE_LEVELS);
+	(fwk_id_t)MOD_SCMI_PROTOCOL_ID_PERF,
+	service_id,
+	(const uint32_t *)&payload,
+	payload_size_table[MOD_SCMI_PERF_DESCRIBE_LEVELS],
+	MOD_SCMI_PERF_DESCRIBE_LEVELS);
 
     TEST_ASSERT_EQUAL(FWK_SUCCESS, status);
 }
@@ -655,24 +655,24 @@ int describe_fast_channels_valid_params_
     int NumCalls)
 {
     struct scmi_perf_describe_fc_p2a *return_values =
-        (struct scmi_perf_describe_fc_p2a *)payload;
+	(struct scmi_perf_describe_fc_p2a *)payload;
 
     TEST_ASSERT_EQUAL((int32_t)SCMI_SUCCESS, return_values->status);
     TEST_ASSERT_EQUAL(0, return_values->attributes);
     TEST_ASSERT_EQUAL(
-        scmi_perf_ctx.fast_channels_rate_limit, return_values->rate_limit);
+	scmi_perf_ctx.fast_channels_rate_limit, return_values->rate_limit);
 
     int chan_index = (uint32_t)MOD_SCMI_PERF_FAST_CHANNEL_LEVEL_GET;
     TEST_ASSERT_EQUAL(
-        (uint32_t)(domains[0].fast_channels_addr_ap[chan_index] & ~0UL),
-        return_values->chan_addr_low);
+	(uint32_t)(domains[0].fast_channels_addr_ap[chan_index] & ~0UL),
+	return_values->chan_addr_low);
     TEST_ASSERT_EQUAL(
-        (uint32_t)(domains[0].fast_channels_addr_ap[chan_index] >> 32),
-        return_values->chan_addr_high);
+	(uint32_t)(domains[0].fast_channels_addr_ap[chan_index] >> 32),
+	return_values->chan_addr_high);
 
     TEST_ASSERT_EQUAL(
-        fast_channel_elem_size[MOD_SCMI_PERF_FAST_CHANNEL_LEVEL_GET],
-        return_values->chan_size);
+	fast_channel_elem_size[MOD_SCMI_PERF_FAST_CHANNEL_LEVEL_GET],
+	return_values->chan_size);
 
     return FWK_SUCCESS;
 }
@@ -682,22 +682,22 @@ void utest_scmi_perf_describe_fast_chann
     int status;
 
     fwk_id_t service_id =
-        FWK_ID_ELEMENT_INIT(TEST_MODULE_IDX, TEST_SCMI_AGENT_IDX_0);
+	FWK_ID_ELEMENT_INIT(TEST_MODULE_IDX, TEST_SCMI_AGENT_IDX_0);
 
     struct scmi_perf_describe_fc_a2p payload = {
-        .domain_id = 0,
-        .message_id = MOD_SCMI_PERF_LEVEL_GET,
+	.domain_id = 0,
+	.message_id = MOD_SCMI_PERF_LEVEL_GET,
     };
 
     mod_scmi_from_protocol_api_respond_Stub(
-        describe_fast_channels_valid_params_respond_callback);
+	describe_fast_channels_valid_params_respond_callback);
 
     status = to_protocol_api->message_handler(
-        (fwk_id_t)MOD_SCMI_PROTOCOL_ID_PERF,
-        service_id,
-        (const uint32_t *)&payload,
-        payload_size_table[MOD_SCMI_PERF_DESCRIBE_FAST_CHANNEL],
-        MOD_SCMI_PERF_DESCRIBE_FAST_CHANNEL);
+	(fwk_id_t)MOD_SCMI_PROTOCOL_ID_PERF,
+	service_id,
+	(const uint32_t *)&payload,
+	payload_size_table[MOD_SCMI_PERF_DESCRIBE_FAST_CHANNEL],
+	MOD_SCMI_PERF_DESCRIBE_FAST_CHANNEL);
 
     TEST_ASSERT_EQUAL(FWK_SUCCESS, status);
 }
@@ -714,7 +714,7 @@ int describe_fast_channels_invalid_domai
     int NumCalls)
 {
     struct scmi_perf_describe_fc_p2a *return_values =
-        (struct scmi_perf_describe_fc_p2a *)payload;
+	(struct scmi_perf_describe_fc_p2a *)payload;
 
     TEST_ASSERT_EQUAL((int32_t)SCMI_NOT_FOUND, return_values->status);
 
@@ -726,22 +726,22 @@ void utest_scmi_perf_describe_fast_chann
     int status;
 
     fwk_id_t service_id =
-        FWK_ID_ELEMENT_INIT(TEST_MODULE_IDX, TEST_SCMI_AGENT_IDX_0);
+	FWK_ID_ELEMENT_INIT(TEST_MODULE_IDX, TEST_SCMI_AGENT_IDX_0);
 
     struct scmi_perf_describe_fc_a2p payload = {
-        .domain_id = scmi_perf_ctx.domain_count,
-        .message_id = MOD_SCMI_PERF_LEVEL_GET,
+	.domain_id = scmi_perf_ctx.domain_count,
+	.message_id = MOD_SCMI_PERF_LEVEL_GET,
     };
 
     mod_scmi_from_protocol_api_respond_Stub(
-        describe_fast_channels_invalid_domain_id_respond_callback);
+	describe_fast_channels_invalid_domain_id_respond_callback);
 
     status = to_protocol_api->message_handler(
-        (fwk_id_t)MOD_SCMI_PROTOCOL_ID_PERF,
-        service_id,
-        (const uint32_t *)&payload,
-        payload_size_table[MOD_SCMI_PERF_DESCRIBE_FAST_CHANNEL],
-        MOD_SCMI_PERF_DESCRIBE_FAST_CHANNEL);
+	(fwk_id_t)MOD_SCMI_PROTOCOL_ID_PERF,
+	service_id,
+	(const uint32_t *)&payload,
+	payload_size_table[MOD_SCMI_PERF_DESCRIBE_FAST_CHANNEL],
+	MOD_SCMI_PERF_DESCRIBE_FAST_CHANNEL);
 
     TEST_ASSERT_EQUAL(FWK_SUCCESS, status);
 }
@@ -758,7 +758,7 @@ int describe_fast_channels_invalid_messa
     int NumCalls)
 {
     struct scmi_perf_describe_fc_p2a *return_values =
-        (struct scmi_perf_describe_fc_p2a *)payload;
+	(struct scmi_perf_describe_fc_p2a *)payload;
 
     TEST_ASSERT_EQUAL((int32_t)SCMI_NOT_FOUND, return_values->status);
 
@@ -770,22 +770,22 @@ void utest_scmi_perf_describe_fast_chann
     int status;
 
     fwk_id_t service_id =
-        FWK_ID_ELEMENT_INIT(TEST_MODULE_IDX, TEST_SCMI_AGENT_IDX_0);
+	FWK_ID_ELEMENT_INIT(TEST_MODULE_IDX, TEST_SCMI_AGENT_IDX_0);
 
     struct scmi_perf_describe_fc_a2p payload = {
-        .domain_id = 0,
-        .message_id = MOD_SCMI_PERF_COMMAND_COUNT,
+	.domain_id = 0,
+	.message_id = MOD_SCMI_PERF_COMMAND_COUNT,
     };
 
     mod_scmi_from_protocol_api_respond_Stub(
-        describe_fast_channels_invalid_message_id_respond_callback);
+	describe_fast_channels_invalid_message_id_respond_callback);
 
     status = to_protocol_api->message_handler(
-        (fwk_id_t)MOD_SCMI_PROTOCOL_ID_PERF,
-        service_id,
-        (const uint32_t *)&payload,
-        payload_size_table[MOD_SCMI_PERF_DESCRIBE_FAST_CHANNEL],
-        MOD_SCMI_PERF_DESCRIBE_FAST_CHANNEL);
+	(fwk_id_t)MOD_SCMI_PROTOCOL_ID_PERF,
+	service_id,
+	(const uint32_t *)&payload,
+	payload_size_table[MOD_SCMI_PERF_DESCRIBE_FAST_CHANNEL],
+	MOD_SCMI_PERF_DESCRIBE_FAST_CHANNEL);
 
     TEST_ASSERT_EQUAL(FWK_SUCCESS, status);
 }
@@ -802,18 +802,18 @@ void utest_find_opp_for_level_valid_leve
     uint32_t valid_level = test_dvfs_config.opps[1].level;
 
     struct perf_opp_table opp_table = {
-        .opps = test_dvfs_config.opps,
-        .opp_count = TEST_OPP_COUNT,
+	.opps = test_dvfs_config.opps,
+	.opp_count = TEST_OPP_COUNT,
     };
 
     struct mod_scmi_perf_level_limits level_limits = {
-        .minimum = test_dvfs_config.opps[0].level,
-        .maximum = test_dvfs_config.opps[TEST_OPP_COUNT - 1].level,
+	.minimum = test_dvfs_config.opps[0].level,
+	.maximum = test_dvfs_config.opps[TEST_OPP_COUNT - 1].level,
     };
 
     struct scmi_perf_domain_ctx domain_ctx = {
-        .level_limits = level_limits,
-        .opp_table = &opp_table,
+	.level_limits = level_limits,
+	.opp_table = &opp_table,
     };
 
     status = find_opp_for_level(&domain_ctx, &valid_level, false);
@@ -831,18 +831,18 @@ void utest_find_opp_for_level_invalid_le
     uint32_t invalid_level = test_dvfs_config.opps[1].level + 1000000UL;
 
     struct perf_opp_table opp_table = {
-        .opps = test_dvfs_config.opps,
-        .opp_count = TEST_OPP_COUNT,
+	.opps = test_dvfs_config.opps,
+	.opp_count = TEST_OPP_COUNT,
     };
 
     struct mod_scmi_perf_level_limits level_limits = {
-        .minimum = test_dvfs_config.opps[0].level,
-        .maximum = test_dvfs_config.opps[TEST_OPP_COUNT - 1].level,
+	.minimum = test_dvfs_config.opps[0].level,
+	.maximum = test_dvfs_config.opps[TEST_OPP_COUNT - 1].level,
     };
 
     struct scmi_perf_domain_ctx domain_ctx = {
-        .level_limits = level_limits,
-        .opp_table = &opp_table,
+	.level_limits = level_limits,
+	.opp_table = &opp_table,
     };
 
     status = find_opp_for_level(&domain_ctx, &invalid_level, false);
@@ -858,18 +858,18 @@ void utest_find_opp_for_level_use_neares
     int status;
 
     struct perf_opp_table opp_table = {
-        .opps = test_dvfs_config.opps,
-        .opp_count = TEST_OPP_COUNT,
+	.opps = test_dvfs_config.opps,
+	.opp_count = TEST_OPP_COUNT,
     };
 
     struct mod_scmi_perf_level_limits level_limits = {
-        .minimum = test_dvfs_config.opps[0].level,
-        .maximum = test_dvfs_config.opps[TEST_OPP_COUNT - 1].level,
+	.minimum = test_dvfs_config.opps[0].level,
+	.maximum = test_dvfs_config.opps[TEST_OPP_COUNT - 1].level,
     };
 
     struct scmi_perf_domain_ctx domain_ctx = {
-        .level_limits = level_limits,
-        .opp_table = &opp_table,
+	.level_limits = level_limits,
+	.opp_table = &opp_table,
     };
 
     uint32_t approximate_level = level_limits.maximum + 1000000UL;
@@ -888,23 +888,23 @@ void utest_validate_new_limits_valid_lim
     int status;
 
     struct perf_opp_table opp_table = {
-        .opps = test_dvfs_config.opps,
-        .opp_count = TEST_OPP_COUNT,
+	.opps = test_dvfs_config.opps,
+	.opp_count = TEST_OPP_COUNT,
     };
 
     struct mod_scmi_perf_level_limits level_limits = {
-        .minimum = test_dvfs_config.opps[0].level,
-        .maximum = test_dvfs_config.opps[TEST_OPP_COUNT - 1].level,
+	.minimum = test_dvfs_config.opps[0].level,
+	.maximum = test_dvfs_config.opps[TEST_OPP_COUNT - 1].level,
     };
 
     struct scmi_perf_domain_ctx domain_ctx = {
-        .level_limits = level_limits,
-        .opp_table = &opp_table,
+	.level_limits = level_limits,
+	.opp_table = &opp_table,
     };
 
     struct mod_scmi_perf_level_limits valid_level_limits = {
-        .minimum = test_dvfs_config.opps[1].level,
-        .maximum = test_dvfs_config.opps[TEST_OPP_COUNT - 1].level,
+	.minimum = test_dvfs_config.opps[1].level,
+	.maximum = test_dvfs_config.opps[TEST_OPP_COUNT - 1].level,
     };
 
     status = validate_new_limits(&domain_ctx, &valid_level_limits);
@@ -920,23 +920,23 @@ void utest_validate_new_limits_invalid_l
     int status;
 
     struct perf_opp_table opp_table = {
-        .opps = test_dvfs_config.opps,
-        .opp_count = TEST_OPP_COUNT,
+	.opps = test_dvfs_config.opps,
+	.opp_count = TEST_OPP_COUNT,
     };
 
     struct mod_scmi_perf_level_limits level_limits = {
-        .minimum = test_dvfs_config.opps[0].level,
-        .maximum = test_dvfs_config.opps[TEST_OPP_COUNT - 1].level,
+	.minimum = test_dvfs_config.opps[0].level,
+	.maximum = test_dvfs_config.opps[TEST_OPP_COUNT - 1].level,
     };
 
     struct scmi_perf_domain_ctx domain_ctx = {
-        .level_limits = level_limits,
-        .opp_table = &opp_table,
+	.level_limits = level_limits,
+	.opp_table = &opp_table,
     };
 
     struct mod_scmi_perf_level_limits invalid_level_limits = {
-        .minimum = test_dvfs_config.opps[0].level + 1000000UL,
-        .maximum = test_dvfs_config.opps[TEST_OPP_COUNT - 1].level,
+	.minimum = test_dvfs_config.opps[0].level + 1000000UL,
+	.maximum = test_dvfs_config.opps[TEST_OPP_COUNT - 1].level,
     };
 
     status = validate_new_limits(&domain_ctx, &invalid_level_limits);
@@ -952,28 +952,28 @@ void utest_validate_new_limits_approxima
     int status;
 
     struct fwk_module_config config_scmi_perf_approximate_level = {
-        .data = &((struct mod_scmi_perf_config){
-            .domains = &domains,
-            .perf_doms_count = FWK_ARRAY_SIZE(domains),
-            .fast_channels_alarm_id = FWK_ID_NONE_INIT,
-            .approximate_level = true,
-        }),
+	.data = &((struct mod_scmi_perf_config){
+	    .domains = &domains,
+	    .perf_doms_count = FWK_ARRAY_SIZE(domains),
+	    .fast_channels_alarm_id = FWK_ID_NONE_INIT,
+	    .approximate_level = true,
+	}),
     };
 
     scmi_perf_ctx.config = config_scmi_perf_approximate_level.data;
 
     struct perf_opp_table opp_table = {
-        .opps = test_dvfs_config.opps,
-        .opp_count = TEST_OPP_COUNT,
+	.opps = test_dvfs_config.opps,
+	.opp_count = TEST_OPP_COUNT,
     };
     struct mod_scmi_perf_level_limits level_limits = {
-        .minimum = test_dvfs_config.opps[0].level - 1000000UL,
-        .maximum = test_dvfs_config.opps[TEST_OPP_COUNT - 1].level + 1000000UL,
+	.minimum = test_dvfs_config.opps[0].level - 1000000UL,
+	.maximum = test_dvfs_config.opps[TEST_OPP_COUNT - 1].level + 1000000UL,
     };
 
     struct scmi_perf_domain_ctx domain_ctx = {
-        .level_limits = level_limits,
-        .opp_table = &opp_table,
+	.level_limits = level_limits,
+	.opp_table = &opp_table,
     };
 
     status = validate_new_limits(&domain_ctx, &level_limits);
@@ -984,8 +984,8 @@ void utest_validate_new_limits_approxima
 void utest_perf_eval_performance_invalid_limits(void)
 {
     struct mod_scmi_perf_level_limits limits = {
-        .minimum = 1000,
-        .maximum = 100,
+	.minimum = 1000,
+	.maximum = 100,
     };
     uint32_t level = 500;
     fwk_id_t perf_id = FWK_ID_ELEMENT_INIT(TEST_MODULE_IDX, 0);
@@ -1001,16 +1001,16 @@ void utest_perf_eval_performance_unchang
 
     domain0_ctx.level_limits.minimum = test_dvfs_config.opps[0].level;
     domain0_ctx.level_limits.maximum =
-        test_dvfs_config.opps[TEST_OPP_COUNT - 1].level;
+	test_dvfs_config.opps[TEST_OPP_COUNT - 1].level;
 
     struct mod_scmi_perf_level_limits limits = {
-        .minimum = test_dvfs_config.opps[0].level,
-        .maximum = test_dvfs_config.opps[TEST_OPP_COUNT - 1].level,
+	.minimum = test_dvfs_config.opps[0].level,
+	.maximum = test_dvfs_config.opps[TEST_OPP_COUNT - 1].level,
     };
 
     domain0_ctx.opp_table = &((struct perf_opp_table){
-        .opps = test_dvfs_config.opps,
-        .opp_count = TEST_OPP_COUNT,
+	.opps = test_dvfs_config.opps,
+	.opp_count = TEST_OPP_COUNT,
     });
 
     scmi_perf_ctx.domain_ctx_table = &domain0_ctx;
@@ -1035,16 +1035,16 @@ void utest_perf_eval_performance_unchang
 
     domain0_ctx.level_limits.minimum = test_dvfs_config.opps[0].level;
     domain0_ctx.level_limits.maximum =
-        test_dvfs_config.opps[TEST_OPP_COUNT - 2].level;
+	test_dvfs_config.opps[TEST_OPP_COUNT - 2].level;
 
     struct mod_scmi_perf_level_limits limits = {
-        .minimum = test_dvfs_config.opps[0].level,
-        .maximum = test_dvfs_config.opps[TEST_OPP_COUNT - 2].level,
+	.minimum = test_dvfs_config.opps[0].level,
+	.maximum = test_dvfs_config.opps[TEST_OPP_COUNT - 2].level,
     };
 
     domain0_ctx.opp_table = &((struct perf_opp_table){
-        .opps = test_dvfs_config.opps,
-        .opp_count = TEST_OPP_COUNT,
+	.opps = test_dvfs_config.opps,
+	.opp_count = TEST_OPP_COUNT,
     });
 
     scmi_perf_ctx.domain_ctx_table = &domain0_ctx;
@@ -1065,16 +1065,16 @@ void utest_perf_eval_performance_unchang
 
     domain0_ctx.level_limits.minimum = test_dvfs_config.opps[1].level;
     domain0_ctx.level_limits.maximum =
-        test_dvfs_config.opps[TEST_OPP_COUNT - 1].level;
+	test_dvfs_config.opps[TEST_OPP_COUNT - 1].level;
 
     struct mod_scmi_perf_level_limits limits = {
-        .minimum = test_dvfs_config.opps[1].level,
-        .maximum = test_dvfs_config.opps[TEST_OPP_COUNT - 1].level,
+	.minimum = test_dvfs_config.opps[1].level,
+	.maximum = test_dvfs_config.opps[TEST_OPP_COUNT - 1].level,
     };
 
     domain0_ctx.opp_table = &((struct perf_opp_table){
-        .opps = test_dvfs_config.opps,
-        .opp_count = TEST_OPP_COUNT,
+	.opps = test_dvfs_config.opps,
+	.opp_count = TEST_OPP_COUNT,
     });
 
     scmi_perf_ctx.domain_ctx_table = &domain0_ctx;
@@ -1096,16 +1096,16 @@ void utest_perf_eval_performance_new_lim
 
     domain_table[1].level_limits.minimum = test_dvfs_config.opps[0].level;
     domain_table[1].level_limits.maximum =
-        test_dvfs_config.opps[TEST_OPP_COUNT - 2].level;
+	test_dvfs_config.opps[TEST_OPP_COUNT - 2].level;
 
     struct mod_scmi_perf_level_limits new_limits = {
-        .minimum = test_dvfs_config.opps[0].level,
-        .maximum = test_dvfs_config.opps[TEST_OPP_COUNT - 1].level,
+	.minimum = test_dvfs_config.opps[0].level,
+	.maximum = test_dvfs_config.opps[TEST_OPP_COUNT - 1].level,
     };
 
     domain_table[1].opp_table = &((struct perf_opp_table){
-        .opps = test_dvfs_config.opps,
-        .opp_count = TEST_OPP_COUNT,
+	.opps = test_dvfs_config.opps,
+	.opp_count = TEST_OPP_COUNT,
     });
 
     scmi_perf_ctx.domain_ctx_table = &domain_table[0];
@@ -1117,8 +1117,8 @@ void utest_perf_eval_performance_new_lim
 
     perf_plugins_ctx.config = config_scmi_perf.data;
     plugins_api_table[0] = &((struct perf_plugins_api){
-        .update = NULL,
-        .report = NULL,
+	.update = NULL,
+	.report = NULL,
     });
     perf_plugins_ctx.plugins_api_table = &plugins_api_table[0];
 
@@ -1134,16 +1134,16 @@ void utest_perf_eval_performance_new_lim
 
     domain_table[1].level_limits.minimum = test_dvfs_config.opps[0].level;
     domain_table[1].level_limits.maximum =
-        test_dvfs_config.opps[TEST_OPP_COUNT - 1].level;
+	test_dvfs_config.opps[TEST_OPP_COUNT - 1].level;
 
     struct mod_scmi_perf_level_limits new_limits = {
-        .minimum = test_dvfs_config.opps[0].level,
-        .maximum = test_dvfs_config.opps[TEST_OPP_COUNT - 2].level,
+	.minimum = test_dvfs_config.opps[0].level,
+	.maximum = test_dvfs_config.opps[TEST_OPP_COUNT - 2].level,
     };
 
     domain_table[1].opp_table = &((struct perf_opp_table){
-        .opps = test_dvfs_config.opps,
-        .opp_count = TEST_OPP_COUNT,
+	.opps = test_dvfs_config.opps,
+	.opp_count = TEST_OPP_COUNT,
     });
 
     scmi_perf_ctx.domain_ctx_table = &domain_table[0];
@@ -1156,8 +1156,8 @@ void utest_perf_eval_performance_new_lim
 
     perf_plugins_ctx.config = config_scmi_perf.data;
     plugins_api_table[0] = &((struct perf_plugins_api){
-        .update = NULL,
-        .report = NULL,
+	.update = NULL,
+	.report = NULL,
     });
     perf_plugins_ctx.plugins_api_table = &plugins_api_table[0];
 
@@ -1173,16 +1173,16 @@ void utest_perf_eval_performance_new_lim
 
     domain_table[1].level_limits.minimum = test_dvfs_config.opps[0].level;
     domain_table[1].level_limits.maximum =
-        test_dvfs_config.opps[TEST_OPP_COUNT - 1].level;
+	test_dvfs_config.opps[TEST_OPP_COUNT - 1].level;
 
     struct mod_scmi_perf_level_limits new_limits = {
-        .minimum = test_dvfs_config.opps[1].level,
-        .maximum = test_dvfs_config.opps[TEST_OPP_COUNT - 1].level,
+	.minimum = test_dvfs_config.opps[1].level,
+	.maximum = test_dvfs_config.opps[TEST_OPP_COUNT - 1].level,
     };
 
     domain_table[1].opp_table = &((struct perf_opp_table){
-        .opps = test_dvfs_config.opps,
-        .opp_count = TEST_OPP_COUNT,
+	.opps = test_dvfs_config.opps,
+	.opp_count = TEST_OPP_COUNT,
     });
 
     scmi_perf_ctx.domain_ctx_table = &domain_table[0];
@@ -1195,8 +1195,8 @@ void utest_perf_eval_performance_new_lim
 
     perf_plugins_ctx.config = config_scmi_perf.data;
     plugins_api_table[0] = &((struct perf_plugins_api){
-        .update = NULL,
-        .report = NULL,
+	.update = NULL,
+	.report = NULL,
     });
     perf_plugins_ctx.plugins_api_table = &plugins_api_table[0];
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_power_domain/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi_power_domain/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_power_domain/CMakeLists.txt
@@ -8,14 +8,14 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(
     ${SCP_MODULE_TARGET}
     PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_scmi_power_domain.c")
 
 target_link_libraries(${SCP_MODULE_TARGET} PRIVATE module-power-domain
-                                                   module-scmi)
+						   module-scmi)
 
 if("resource-perms" IN_LIST SCP_MODULES)
     target_link_libraries(${SCP_MODULE_TARGET} PRIVATE module-resource-perms)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_power_domain/src/mod_scmi_power_domain.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi_power_domain/src/mod_scmi_power_domain.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_power_domain/src/mod_scmi_power_domain.c
@@ -138,11 +138,11 @@ struct event_request_params {
 #ifdef BUILD_HAS_MOD_DEBUG
 static const fwk_id_t mod_scmi_pd_event_id_dbg_enable_set =
     FWK_ID_EVENT_INIT(FWK_MODULE_IDX_SCMI_POWER_DOMAIN,
-                      SCMI_PD_EVENT_IDX_DEBUG_SET);
+		      SCMI_PD_EVENT_IDX_DEBUG_SET);
 
 static const fwk_id_t mod_scmi_pd_event_id_dbg_enable_get =
     FWK_ID_EVENT_INIT(FWK_MODULE_IDX_SCMI_POWER_DOMAIN,
-                      SCMI_PD_EVENT_IDX_DEBUG_GET);
+		      SCMI_PD_EVENT_IDX_DEBUG_GET);
 #endif
 
 static const fwk_id_t mod_scmi_pd_event_id_set_request = FWK_ID_EVENT_INIT(
@@ -161,17 +161,17 @@ static int (*handler_table[MOD_SCMI_PD_P
     [MOD_SCMI_PROTOCOL_VERSION] = scmi_pd_protocol_version_handler,
     [MOD_SCMI_PROTOCOL_ATTRIBUTES] = scmi_pd_protocol_attributes_handler,
     [MOD_SCMI_PROTOCOL_MESSAGE_ATTRIBUTES] =
-        scmi_pd_protocol_message_attributes_handler,
+	scmi_pd_protocol_message_attributes_handler,
     [MOD_SCMI_PD_POWER_DOMAIN_ATTRIBUTES] =
-        scmi_pd_power_domain_attributes_handler,
+	scmi_pd_power_domain_attributes_handler,
     [MOD_SCMI_PD_POWER_STATE_SET] = scmi_pd_power_state_set_handler,
     [MOD_SCMI_PD_POWER_STATE_GET] = scmi_pd_power_state_get_handler,
 
 #ifdef BUILD_HAS_SCMI_NOTIFICATIONS
     [MOD_SCMI_PD_POWER_STATE_NOTIFY] =
-        scmi_pd_power_state_changed_notify_handler,
+	scmi_pd_power_state_changed_notify_handler,
     [MOD_SCMI_PD_POWER_STATE_CHANGE_REQUESTED_NOTIFY] =
-        scmi_pd_power_state_change_requested_notify_handler,
+	scmi_pd_power_state_change_requested_notify_handler,
 #endif
 };
 
@@ -179,25 +179,25 @@ static unsigned int payload_size_table[M
     [MOD_SCMI_PROTOCOL_VERSION] = 0,
     [MOD_SCMI_PROTOCOL_ATTRIBUTES] = 0,
     [MOD_SCMI_PROTOCOL_MESSAGE_ATTRIBUTES] =
-        (unsigned int)sizeof(struct scmi_protocol_message_attributes_a2p),
+	(unsigned int)sizeof(struct scmi_protocol_message_attributes_a2p),
     [MOD_SCMI_PD_POWER_DOMAIN_ATTRIBUTES] =
-        (unsigned int)sizeof(struct scmi_pd_power_domain_attributes_a2p),
+	(unsigned int)sizeof(struct scmi_pd_power_domain_attributes_a2p),
     [MOD_SCMI_PD_POWER_STATE_SET] =
-        (unsigned int)sizeof(struct scmi_pd_power_state_set_a2p),
+	(unsigned int)sizeof(struct scmi_pd_power_state_set_a2p),
     [MOD_SCMI_PD_POWER_STATE_GET] =
-        (unsigned int)sizeof(struct scmi_pd_power_state_get_a2p),
+	(unsigned int)sizeof(struct scmi_pd_power_state_get_a2p),
 
 #ifdef BUILD_HAS_SCMI_NOTIFICATIONS
     [MOD_SCMI_PD_POWER_STATE_NOTIFY] =
-        sizeof(struct scmi_pd_power_state_notify_a2p),
+	sizeof(struct scmi_pd_power_state_notify_a2p),
     [MOD_SCMI_PD_POWER_STATE_CHANGE_REQUESTED_NOTIFY] =
-        sizeof(struct scmi_pd_power_state_notify_a2p),
+	sizeof(struct scmi_pd_power_state_notify_a2p),
 #endif
 };
 
 static uint32_t pd_state_to_scmi_dev_state[MOD_PD_STATE_COUNT] = {
     [MOD_PD_STATE_OFF] =
-        SCMI_PD_DEVICE_STATE_ID_OFF | SCMI_PD_DEVICE_STATE_TYPE,
+	SCMI_PD_DEVICE_STATE_ID_OFF | SCMI_PD_DEVICE_STATE_TYPE,
     [MOD_PD_STATE_ON] = SCMI_PD_DEVICE_STATE_ID_ON,
     /* In case of more supported device states review the map functions */
 };
@@ -254,51 +254,51 @@ static void ops_set_idle(fwk_id_t pd_id)
  */
 
 static int pd_state_to_scmi_device_state(unsigned int pd_state,
-                                         uint32_t *scmi_state)
+					 uint32_t *scmi_state)
 {
     if (pd_state == MOD_PD_STATE_OFF || pd_state == MOD_PD_STATE_ON) {
-        *scmi_state = pd_state_to_scmi_dev_state[pd_state];
+	*scmi_state = pd_state_to_scmi_dev_state[pd_state];
     } else {
-        *scmi_state = pd_state;
+	*scmi_state = pd_state;
     }
 
     return FWK_SUCCESS;
 }
 
 static int scmi_pd_protocol_version_handler(fwk_id_t service_id,
-                                            const uint32_t *payload)
+					    const uint32_t *payload)
 {
     int respond_status;
     struct scmi_protocol_version_p2a return_values = {
-        .status = (int32_t)SCMI_SUCCESS,
-        .version = SCMI_PROTOCOL_VERSION_POWER_DOMAIN,
+	.status = (int32_t)SCMI_SUCCESS,
+	.version = SCMI_PROTOCOL_VERSION_POWER_DOMAIN,
     };
 
     respond_status = scmi_pd_ctx.scmi_api->respond(
-        service_id, &return_values, sizeof(return_values));
+	service_id, &return_values, sizeof(return_values));
 
     if (respond_status != FWK_SUCCESS) {
-        FWK_LOG_DEBUG("[SCMI-power] %s @%d", __func__, __LINE__);
+	FWK_LOG_DEBUG("[SCMI-power] %s @%d", __func__, __LINE__);
     }
 
     return FWK_SUCCESS;
 }
 
 static int scmi_pd_protocol_attributes_handler(fwk_id_t service_id,
-                                               const uint32_t *payload)
+					       const uint32_t *payload)
 {
     struct scmi_pd_protocol_attributes_p2a return_values = {
-        .status = (int32_t)SCMI_SUCCESS,
+	.status = (int32_t)SCMI_SUCCESS,
     };
 
     return_values.attributes = scmi_pd_ctx.domain_count;
 
     return scmi_pd_ctx.scmi_api->respond(
-        service_id, &return_values, sizeof(return_values));
+	service_id, &return_values, sizeof(return_values));
 }
 
 static int scmi_pd_power_domain_attributes_handler(fwk_id_t service_id,
-                                                   const uint32_t *payload)
+						   const uint32_t *payload)
 {
     int status = FWK_SUCCESS;
     int respond_status;
@@ -309,101 +309,101 @@ static int scmi_pd_power_domain_attribut
     unsigned int agent_id;
     enum scmi_agent_type agent_type;
     struct scmi_pd_power_domain_attributes_p2a return_values = {
-        .status = (int32_t)SCMI_GENERIC_ERROR,
+	.status = (int32_t)SCMI_GENERIC_ERROR,
     };
 
     parameters = (const struct scmi_pd_power_domain_attributes_a2p *)payload;
 
     domain_idx = parameters->domain_id;
     if (domain_idx > UINT16_MAX) {
-        return_values.status = (int32_t)SCMI_NOT_FOUND;
-        goto exit;
+	return_values.status = (int32_t)SCMI_NOT_FOUND;
+	goto exit;
     }
 
     pd_id = FWK_ID_ELEMENT(FWK_MODULE_IDX_POWER_DOMAIN, domain_idx);
     if (!fwk_module_is_valid_element_id(pd_id)) {
-        return_values.status = (int32_t)SCMI_NOT_FOUND;
-        goto exit;
+	return_values.status = (int32_t)SCMI_NOT_FOUND;
+	goto exit;
     }
 
     status = scmi_pd_ctx.scmi_api->get_agent_id(service_id, &agent_id);
     if (status != FWK_SUCCESS) {
-        goto exit;
+	goto exit;
     }
 
     status = scmi_pd_ctx.scmi_api->get_agent_type(agent_id, &agent_type);
     if (status != FWK_SUCCESS) {
-        goto exit;
+	goto exit;
     }
 
     status = scmi_pd_ctx.pd_api->get_domain_type(pd_id, &pd_type);
     if (status != FWK_SUCCESS) {
-        goto exit;
+	goto exit;
     }
 
     switch (pd_type) {
     case MOD_PD_TYPE_CORE:
-        /*
-         * For core power domains, the POWER_STATE_SET command is supported
-         * only asynchronously for the PSCI agent. In all other cases, reply
-         * that the command is not supported either synchronously nor
-         * asynchronously.
-         */
-        if (agent_type == SCMI_AGENT_TYPE_PSCI) {
-            return_values.attributes = SCMI_PD_POWER_STATE_SET_ASYNC;
-        }
-        break;
+	/*
+	 * For core power domains, the POWER_STATE_SET command is supported
+	 * only asynchronously for the PSCI agent. In all other cases, reply
+	 * that the command is not supported either synchronously nor
+	 * asynchronously.
+	 */
+	if (agent_type == SCMI_AGENT_TYPE_PSCI) {
+	    return_values.attributes = SCMI_PD_POWER_STATE_SET_ASYNC;
+	}
+	break;
 
     case MOD_PD_TYPE_CLUSTER:
-        /*
-         * For cluster power domains, the POWER_STATE_SET command is supported
-         * only synchronously for the PSCI agent. In all other cases, reply
-         * that the command is not supported either synchronously nor
-         * asynchronously.
-         */
-        if (agent_type == SCMI_AGENT_TYPE_PSCI) {
-            return_values.attributes = SCMI_PD_POWER_STATE_SET_SYNC;
-        }
-        break;
+	/*
+	 * For cluster power domains, the POWER_STATE_SET command is supported
+	 * only synchronously for the PSCI agent. In all other cases, reply
+	 * that the command is not supported either synchronously nor
+	 * asynchronously.
+	 */
+	if (agent_type == SCMI_AGENT_TYPE_PSCI) {
+	    return_values.attributes = SCMI_PD_POWER_STATE_SET_SYNC;
+	}
+	break;
 
     case MOD_PD_TYPE_DEVICE_DEBUG:
     case MOD_PD_TYPE_DEVICE:
-        /*
-         * Support only synchronous POWER_STATE_SET for devices for any agent.
-         * Notifications are also supported for this power domain type.
-         */
+	/*
+	 * Support only synchronous POWER_STATE_SET for devices for any agent.
+	 * Notifications are also supported for this power domain type.
+	 */
 #ifdef BUILD_HAS_SCMI_NOTIFICATIONS
-        return_values.attributes = SCMI_PD_POWER_STATE_SET_SYNC |
-            SCMI_PD_POWER_STATE_CHANGE_NOTIFICATIONS;
+	return_values.attributes = SCMI_PD_POWER_STATE_SET_SYNC |
+	    SCMI_PD_POWER_STATE_CHANGE_NOTIFICATIONS;
 #else
-        return_values.attributes = SCMI_PD_POWER_STATE_SET_SYNC;
+	return_values.attributes = SCMI_PD_POWER_STATE_SET_SYNC;
 #endif
-        break;
+	break;
 
     case MOD_PD_TYPE_SYSTEM:
-        return_values.status = (int32_t)SCMI_NOT_FOUND;
-        /* Fallthrough. */
+	return_values.status = (int32_t)SCMI_NOT_FOUND;
+	/* Fallthrough. */
 
     default:
-        goto exit;
+	goto exit;
     }
 
     fwk_str_strncpy(
-        (char *)return_values.name,
-        fwk_module_get_element_name(pd_id),
-        sizeof(return_values.name) - 1);
+	(char *)return_values.name,
+	fwk_module_get_element_name(pd_id),
+	sizeof(return_values.name) - 1);
 
     return_values.status = (int32_t)SCMI_SUCCESS;
 
 exit:
     respond_status = scmi_pd_ctx.scmi_api->respond(
-        service_id,
-        &return_values,
-        (return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
-                                                 sizeof(return_values.status));
+	service_id,
+	&return_values,
+	(return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
+						 sizeof(return_values.status));
 
     if (respond_status != FWK_SUCCESS) {
-        FWK_LOG_DEBUG("[SCMI-power] %s @%d", __func__, __LINE__);
+	FWK_LOG_DEBUG("[SCMI-power] %s @%d", __func__, __LINE__);
     }
 
     return status;
@@ -414,22 +414,22 @@ static int scmi_pd_protocol_message_attr
 {
     const struct scmi_protocol_message_attributes_a2p *parameters;
     struct scmi_protocol_message_attributes_p2a return_values = {
-        .status = (int32_t)SCMI_NOT_FOUND,
+	.status = (int32_t)SCMI_NOT_FOUND,
     };
 
     parameters = (const struct scmi_protocol_message_attributes_a2p *)
-                  payload;
+		  payload;
 
     if ((parameters->message_id < FWK_ARRAY_SIZE(handler_table)) &&
-        (handler_table[parameters->message_id] != NULL)) {
-        return_values.status = (int32_t)SCMI_SUCCESS;
+	(handler_table[parameters->message_id] != NULL)) {
+	return_values.status = (int32_t)SCMI_SUCCESS;
     }
 
     return scmi_pd_ctx.scmi_api->respond(
-        service_id,
-        &return_values,
-        (return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
-                                                 sizeof(return_values.status));
+	service_id,
+	&return_values,
+	(return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
+						 sizeof(return_values.status));
 }
 
 #ifdef BUILD_HAS_MOD_DEBUG
@@ -449,13 +449,13 @@ static void scmi_pd_power_state_notify(
     message.power_state = power_state;
 
     status = scmi_pd_ctx.scmi_notification_api->scmi_notification_notify(
-        MOD_SCMI_PROTOCOL_ID_POWER_DOMAIN,
-        command_id,
-        notification_message_id,
-        &message,
-        sizeof(message));
+	MOD_SCMI_PROTOCOL_ID_POWER_DOMAIN,
+	command_id,
+	notification_message_id,
+	&message,
+	sizeof(message));
     if (status != FWK_SUCCESS) {
-        FWK_LOG_DEBUG("[SCMI-power] %s @%d", __func__, __LINE__);
+	FWK_LOG_DEBUG("[SCMI-power] %s @%d", __func__, __LINE__);
     }
 #endif
 }
@@ -466,19 +466,19 @@ static int32_t scmi_pd_power_state_set_p
     fwk_id_t *pd_id)
 {
     if (((scmi_params->flags & ~SCMI_PD_POWER_STATE_SET_FLAGS_MASK) != 0x0U) ||
-        ((scmi_params->power_state &
-          ~SCMI_PD_POWER_STATE_SET_POWER_STATE_MASK) != 0x0U)) {
-        return (int32_t)SCMI_INVALID_PARAMETERS;
+	((scmi_params->power_state &
+	  ~SCMI_PD_POWER_STATE_SET_POWER_STATE_MASK) != 0x0U)) {
+	return (int32_t)SCMI_INVALID_PARAMETERS;
     }
 
     if (scmi_params->domain_id > UINT16_MAX) {
-        return (int32_t)SCMI_NOT_FOUND;
+	return (int32_t)SCMI_NOT_FOUND;
     }
 
     *pd_id =
-        FWK_ID_ELEMENT(FWK_MODULE_IDX_POWER_DOMAIN, scmi_params->domain_id);
+	FWK_ID_ELEMENT(FWK_MODULE_IDX_POWER_DOMAIN, scmi_params->domain_id);
     if (!fwk_module_is_valid_element_id(*pd_id)) {
-        return (int32_t)SCMI_NOT_FOUND;
+	return (int32_t)SCMI_NOT_FOUND;
     }
     return (int32_t)SCMI_SUCCESS;
 }
@@ -494,22 +494,22 @@ static int32_t scmi_pd_power_state_set_t
 
     status = scmi_pd_ctx.scmi_api->get_agent_id(service_id, agent_idx);
     if (status != FWK_SUCCESS) {
-        return (int32_t)SCMI_INVALID_PARAMETERS;
+	return (int32_t)SCMI_INVALID_PARAMETERS;
     }
 
     status = scmi_pd_ctx.scmi_api->get_agent_type(*agent_idx, &agent_type);
     if (status != FWK_SUCCESS) {
-        return (int32_t)SCMI_INVALID_PARAMETERS;
+	return (int32_t)SCMI_INVALID_PARAMETERS;
     }
 
     status = scmi_pd_ctx.pd_api->get_domain_type(pd_id, pd_type);
     if (status != FWK_SUCCESS) {
-        return (int32_t)SCMI_INVALID_PARAMETERS;
+	return (int32_t)SCMI_INVALID_PARAMETERS;
     }
 
     if (((*pd_type == MOD_PD_TYPE_CORE) || (*pd_type == MOD_PD_TYPE_CLUSTER)) &&
-        (agent_type != SCMI_AGENT_TYPE_PSCI)) {
-        return (int32_t)SCMI_NOT_SUPPORTED;
+	(agent_type != SCMI_AGENT_TYPE_PSCI)) {
+	return (int32_t)SCMI_NOT_SUPPORTED;
     }
 
     return (int32_t)SCMI_SUCCESS;
@@ -535,97 +535,97 @@ static int scmi_pd_power_state_set_handl
     /* Parameters checking */
     scmi_return = scmi_pd_power_state_set_parameters_check(scmi_params, &pd_id);
     if (scmi_return != SCMI_SUCCESS) {
-        return_values.status = scmi_return;
-        return scmi_pd_ctx.scmi_api->respond(
-            service_id, &return_values, sizeof(return_values.status));
+	return_values.status = scmi_return;
+	return scmi_pd_ctx.scmi_api->respond(
+	    service_id, &return_values, sizeof(return_values.status));
     }
 
     /* Type checking */
     scmi_return = scmi_pd_power_state_set_type_check(
-        service_id, pd_id, &agent_idx, &pd_type);
+	service_id, pd_id, &agent_idx, &pd_type);
     if (scmi_return != SCMI_SUCCESS) {
-        return_values.status = scmi_return;
-        return scmi_pd_ctx.scmi_api->respond(
-            service_id, &return_values, sizeof(return_values.status));
+	return_values.status = scmi_return;
+	return scmi_pd_ctx.scmi_api->respond(
+	    service_id, &return_values, sizeof(return_values.status));
     }
 
     is_sync =
-        ((scmi_params->flags & SCMI_PD_POWER_STATE_SET_ASYNC_FLAG_MASK) ==
-         (uint32_t)0);
+	((scmi_params->flags & SCMI_PD_POWER_STATE_SET_ASYNC_FLAG_MASK) ==
+	 (uint32_t)0);
 
     /* Cluster and Device_debug pd types supports sync requests only*/
     if (((pd_type == MOD_PD_TYPE_CLUSTER) ||
-         (pd_type == MOD_PD_TYPE_DEVICE_DEBUG)) &&
-        (!is_sync)) {
-        return_values.status = (int32_t)SCMI_NOT_SUPPORTED;
-        return scmi_pd_ctx.scmi_api->respond(
-            service_id, &return_values, sizeof(return_values.status));
+	 (pd_type == MOD_PD_TYPE_DEVICE_DEBUG)) &&
+	(!is_sync)) {
+	return_values.status = (int32_t)SCMI_NOT_SUPPORTED;
+	return scmi_pd_ctx.scmi_api->respond(
+	    service_id, &return_values, sizeof(return_values.status));
     } else if (pd_type == MOD_PD_TYPE_CORE) {
-        /*
-         * Async/sync flag is ignored for core power domains as stated
-         * by the specification.
-         */
-        is_sync = false;
+	/*
+	 * Async/sync flag is ignored for core power domains as stated
+	 * by the specification.
+	 */
+	is_sync = false;
     } else {
-        /* Other pd types if any */
+	/* Other pd types if any */
     }
 
     power_state = scmi_params->power_state;
     status = scmi_pd_power_state_set_policy(
-        &policy_status, &power_state, agent_idx, pd_id);
+	&policy_status, &power_state, agent_idx, pd_id);
 
     if (status != FWK_SUCCESS) {
-        return_values.status = (int32_t)SCMI_GENERIC_ERROR;
-        return scmi_pd_ctx.scmi_api->respond(
-            service_id, &return_values, sizeof(return_values.status));
+	return_values.status = (int32_t)SCMI_GENERIC_ERROR;
+	return scmi_pd_ctx.scmi_api->respond(
+	    service_id, &return_values, sizeof(return_values.status));
     }
 
     if (policy_status == MOD_SCMI_PD_SKIP_MESSAGE_HANDLER) {
-        return_values.status = (int32_t)SCMI_SUCCESS;
-        return scmi_pd_ctx.scmi_api->respond(
-            service_id, &return_values, sizeof(return_values));
+	return_values.status = (int32_t)SCMI_SUCCESS;
+	return scmi_pd_ctx.scmi_api->respond(
+	    service_id, &return_values, sizeof(return_values));
     }
 
     if (!is_sync) {
-        /*
-         * For a power domain that is managed asynchronously, schedule the
-         * request and respond to the agent immediately.
-         */
-        status = scmi_pd_ctx.pd_api->set_state(pd_id, false, power_state);
-        if (status == FWK_SUCCESS) {
-            return_values.status = (int32_t)SCMI_SUCCESS;
-            return scmi_pd_ctx.scmi_api->respond(
-                service_id, &return_values, sizeof(return_values));
-        } else {
-            return_values.status = (int32_t)SCMI_GENERIC_ERROR;
-            return scmi_pd_ctx.scmi_api->respond(
-                service_id, &return_values, sizeof(return_values.status));
-        }
+	/*
+	 * For a power domain that is managed asynchronously, schedule the
+	 * request and respond to the agent immediately.
+	 */
+	status = scmi_pd_ctx.pd_api->set_state(pd_id, false, power_state);
+	if (status == FWK_SUCCESS) {
+	    return_values.status = (int32_t)SCMI_SUCCESS;
+	    return scmi_pd_ctx.scmi_api->respond(
+		service_id, &return_values, sizeof(return_values));
+	} else {
+	    return_values.status = (int32_t)SCMI_GENERIC_ERROR;
+	    return scmi_pd_ctx.scmi_api->respond(
+		service_id, &return_values, sizeof(return_values.status));
+	}
     }
 
     /* Sync request handling */
     if (ops_is_busy(pd_id)) {
-        return_values.status = (int32_t)SCMI_BUSY;
-        int respond_status = scmi_pd_ctx.scmi_api->respond(
-            service_id, &return_values, sizeof(return_values.status));
-
-        if (respond_status != FWK_SUCCESS) {
-            FWK_LOG_DEBUG("[SCMI-power] %s @%d", __func__, __LINE__);
-        }
-        return FWK_E_BUSY;
+	return_values.status = (int32_t)SCMI_BUSY;
+	int respond_status = scmi_pd_ctx.scmi_api->respond(
+	    service_id, &return_values, sizeof(return_values.status));
+
+	if (respond_status != FWK_SUCCESS) {
+	    FWK_LOG_DEBUG("[SCMI-power] %s @%d", __func__, __LINE__);
+	}
+	return FWK_E_BUSY;
     }
     ops_set_busy(pd_id, service_id);
     ops_set_agent_id(pd_id, agent_idx);
 
     /* Send the event to handle the request in the scmi_pd context */
     struct fwk_event event = {
-        .target_id = fwk_module_id_scmi_power_domain,
-        .id = mod_scmi_pd_event_id_set_request,
+	.target_id = fwk_module_id_scmi_power_domain,
+	.id = mod_scmi_pd_event_id_set_request,
     };
 
 #ifdef BUILD_HAS_MOD_DEBUG
     if (pd_type == MOD_PD_TYPE_DEVICE_DEBUG) {
-        event.id = mod_scmi_pd_event_id_dbg_enable_set;
+	event.id = mod_scmi_pd_event_id_dbg_enable_set;
     }
 #endif
 
@@ -638,7 +638,7 @@ static int scmi_pd_power_state_set_handl
 }
 
 static int scmi_pd_power_state_get_handler(fwk_id_t service_id,
-                                           const uint32_t *payload)
+					   const uint32_t *payload)
 {
     int status = FWK_SUCCESS;
     int respond_status;
@@ -646,7 +646,7 @@ static int scmi_pd_power_state_get_handl
     unsigned int domain_idx;
     fwk_id_t pd_id;
     struct scmi_pd_power_state_get_p2a return_values = {
-        .status = (int32_t)SCMI_GENERIC_ERROR
+	.status = (int32_t)SCMI_GENERIC_ERROR
     };
     enum mod_pd_type pd_type;
     unsigned int pd_power_state;
@@ -660,80 +660,80 @@ static int scmi_pd_power_state_get_handl
 
     domain_idx = parameters->domain_id;
     if (domain_idx > UINT16_MAX) {
-        return_values.status = (int32_t)SCMI_NOT_FOUND;
-        goto exit;
+	return_values.status = (int32_t)SCMI_NOT_FOUND;
+	goto exit;
     }
 
     pd_id = FWK_ID_ELEMENT(FWK_MODULE_IDX_POWER_DOMAIN, domain_idx);
     if (!fwk_module_is_valid_element_id(pd_id)) {
-        return_values.status = (int32_t)SCMI_NOT_FOUND;
-        goto exit;
+	return_values.status = (int32_t)SCMI_NOT_FOUND;
+	goto exit;
     }
 
     status = scmi_pd_ctx.pd_api->get_domain_type(pd_id, &pd_type);
     if (status != FWK_SUCCESS) {
-        goto exit;
+	goto exit;
     }
 
     switch (pd_type) {
     case MOD_PD_TYPE_CORE:
     case MOD_PD_TYPE_CLUSTER:
-        status = scmi_pd_ctx.pd_api->get_state(pd_id, &power_state);
-        break;
+	status = scmi_pd_ctx.pd_api->get_state(pd_id, &power_state);
+	break;
 
     case MOD_PD_TYPE_DEVICE_DEBUG:
 #ifdef BUILD_HAS_MOD_DEBUG
-        event = (struct fwk_event){
-            .target_id = fwk_module_id_scmi_power_domain,
-            .id = mod_scmi_pd_event_id_dbg_enable_get,
-        };
+	event = (struct fwk_event){
+	    .target_id = fwk_module_id_scmi_power_domain,
+	    .id = mod_scmi_pd_event_id_dbg_enable_get,
+	};
 
-        event_params = (struct event_request_params *)event.params;
+	event_params = (struct event_request_params *)event.params;
 
-        event_params->pd_id = pd_id;
+	event_params->pd_id = pd_id;
 
-        status = fwk_put_event(&event);
-        if (status != FWK_SUCCESS) {
-            break;
-        }
+	status = fwk_put_event(&event);
+	if (status != FWK_SUCCESS) {
+	    break;
+	}
 
-        ops_set_busy(pd_id, service_id);
+	ops_set_busy(pd_id, service_id);
 
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 #endif
     case MOD_PD_TYPE_DEVICE:
 
-        status = scmi_pd_ctx.pd_api->get_state(pd_id, &pd_power_state);
-        if (status != FWK_SUCCESS) {
-            goto exit;
-        }
-
-        status = pd_state_to_scmi_device_state(pd_power_state,
-                                               (uint32_t *) &power_state);
-        break;
+	status = scmi_pd_ctx.pd_api->get_state(pd_id, &pd_power_state);
+	if (status != FWK_SUCCESS) {
+	    goto exit;
+	}
+
+	status = pd_state_to_scmi_device_state(pd_power_state,
+					       (uint32_t *) &power_state);
+	break;
 
     case MOD_PD_TYPE_SYSTEM:
-        return_values.status = (int32_t)SCMI_NOT_FOUND;
-        /* Fallthrough. */
+	return_values.status = (int32_t)SCMI_NOT_FOUND;
+	/* Fallthrough. */
 
     default:
-        goto exit;
+	goto exit;
     }
 
     if (status == FWK_SUCCESS) {
-        return_values.status = (int32_t)SCMI_SUCCESS;
-        return_values.power_state = power_state;
+	return_values.status = (int32_t)SCMI_SUCCESS;
+	return_values.power_state = power_state;
     }
 
 exit:
     respond_status = scmi_pd_ctx.scmi_api->respond(
-        service_id,
-        &return_values,
-        (return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
-                                                 sizeof(return_values.status));
+	service_id,
+	&return_values,
+	(return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
+						 sizeof(return_values.status));
 
     if (respond_status != FWK_SUCCESS) {
-        FWK_LOG_DEBUG("[SCMI-power] %s @%d", __func__, __LINE__);
+	FWK_LOG_DEBUG("[SCMI-power] %s @%d", __func__, __LINE__);
     }
 
     return status;
@@ -752,31 +752,31 @@ static int scmi_pd_power_state_notify_ha
     fwk_id_t pd_id;
     const struct scmi_pd_power_state_notify_a2p *parameters;
     struct scmi_pd_power_state_notify_p2a return_values = {
-        .status = (int32_t)SCMI_GENERIC_ERROR,
+	.status = (int32_t)SCMI_GENERIC_ERROR,
     };
 
     parameters = (const struct scmi_pd_power_state_notify_a2p *)payload;
     domain_idx = parameters->domain_id;
     if (domain_idx >= scmi_pd_ctx.domain_count) {
-        return_values.status = (int32_t)SCMI_NOT_FOUND;
-        goto exit;
+	return_values.status = (int32_t)SCMI_NOT_FOUND;
+	goto exit;
     }
 
     pd_id = FWK_ID_ELEMENT(FWK_MODULE_IDX_POWER_DOMAIN, domain_idx);
     if (!fwk_module_is_valid_element_id(pd_id)) {
-        return_values.status = (int32_t)SCMI_NOT_FOUND;
-        goto exit;
+	return_values.status = (int32_t)SCMI_NOT_FOUND;
+	goto exit;
     }
 
     status = scmi_pd_ctx.pd_api->get_domain_type(pd_id, &pd_type);
     if (status != FWK_SUCCESS) {
-        return_values.status = (int32_t)SCMI_NOT_FOUND;
-        goto exit;
+	return_values.status = (int32_t)SCMI_NOT_FOUND;
+	goto exit;
     }
 
     if ((parameters->notify_enable & ~SCMI_PD_NOTIFY_ENABLE_MASK) != 0x0) {
-        return_values.status = (int32_t)SCMI_INVALID_PARAMETERS;
-        goto exit;
+	return_values.status = (int32_t)SCMI_INVALID_PARAMETERS;
+	goto exit;
     }
 
     /* We are supporting SCMI notifications only for DEVICE type
@@ -788,44 +788,44 @@ static int scmi_pd_power_state_notify_ha
      * agent that is notified"
      */
     if (pd_type != MOD_PD_TYPE_DEVICE && pd_type != MOD_PD_TYPE_DEVICE_DEBUG) {
-        return_values.status = (int32_t)SCMI_NOT_SUPPORTED;
-        goto exit;
+	return_values.status = (int32_t)SCMI_NOT_SUPPORTED;
+	goto exit;
     }
 
     status = scmi_pd_ctx.scmi_api->get_agent_id(service_id, &agent_id);
     if (status != FWK_SUCCESS) {
-        return_values.status = (int32_t)SCMI_NOT_FOUND;
-        goto exit;
+	return_values.status = (int32_t)SCMI_NOT_FOUND;
+	goto exit;
     }
 
     if (parameters->notify_enable) {
-        status =
-            scmi_pd_ctx.scmi_notification_api->scmi_notification_add_subscriber(
-                MOD_SCMI_PROTOCOL_ID_POWER_DOMAIN,
-                domain_idx,
-                command_id,
-                service_id);
+	status =
+	    scmi_pd_ctx.scmi_notification_api->scmi_notification_add_subscriber(
+		MOD_SCMI_PROTOCOL_ID_POWER_DOMAIN,
+		domain_idx,
+		command_id,
+		service_id);
     } else {
-        status = scmi_pd_ctx.scmi_notification_api
-                     ->scmi_notification_remove_subscriber(
-                         MOD_SCMI_PROTOCOL_ID_POWER_DOMAIN,
-                         agent_id,
-                         domain_idx,
-                         command_id);
+	status = scmi_pd_ctx.scmi_notification_api
+		     ->scmi_notification_remove_subscriber(
+			 MOD_SCMI_PROTOCOL_ID_POWER_DOMAIN,
+			 agent_id,
+			 domain_idx,
+			 command_id);
     }
     if (status != FWK_SUCCESS) {
-        return_values.status = (int32_t)SCMI_GENERIC_ERROR;
-        goto exit;
+	return_values.status = (int32_t)SCMI_GENERIC_ERROR;
+	goto exit;
     }
 
     return_values.status = (int32_t)SCMI_SUCCESS;
 
 exit:
     return scmi_pd_ctx.scmi_api->respond(
-        service_id,
-        &return_values,
-        (return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
-                                                 sizeof(return_values.status));
+	service_id,
+	&return_values,
+	(return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
+						 sizeof(return_values.status));
 }
 
 static int scmi_pd_power_state_changed_notify_handler(
@@ -833,7 +833,7 @@ static int scmi_pd_power_state_changed_n
     const uint32_t *payload)
 {
     return scmi_pd_power_state_notify_handler(
-        MOD_SCMI_PD_POWER_STATE_NOTIFY, service_id, payload);
+	MOD_SCMI_PD_POWER_STATE_NOTIFY, service_id, payload);
 }
 
 static int scmi_pd_power_state_change_requested_notify_handler(
@@ -841,7 +841,7 @@ static int scmi_pd_power_state_change_re
     const uint32_t *payload)
 {
     return scmi_pd_power_state_notify_handler(
-        MOD_SCMI_PD_POWER_STATE_CHANGE_REQUESTED_NOTIFY, service_id, payload);
+	MOD_SCMI_PD_POWER_STATE_CHANGE_REQUESTED_NOTIFY, service_id, payload);
 }
 #endif
 
@@ -859,29 +859,29 @@ FWK_WEAK int scmi_pd_power_state_set_pol
 
     status = scmi_pd_ctx.pd_api->get_domain_type(pd_id, &pd_type);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     *policy_status = MOD_SCMI_PD_EXECUTE_MESSAGE_HANDLER;
 
     if ((pd_type == MOD_PD_TYPE_DEVICE) ||
-        (pd_type == MOD_PD_TYPE_DEVICE_DEBUG)) {
-        uint32_t dev_state_idx;
-        uint32_t dev_state_table_size = sizeof(pd_state_to_scmi_dev_state) /
-            sizeof(pd_state_to_scmi_dev_state[0]);
-        uint32_t dev_state = *state;
-        /*
-         * Convert the device SCMI power state sent by the agent,
-         * into the internal power domain state as defined by the
-         * pd_state_to_scmi_dev_state table.
-         */
-        for (dev_state_idx = 0; dev_state_idx < dev_state_table_size;
-             dev_state_idx++) {
-            if (pd_state_to_scmi_dev_state[dev_state_idx] == dev_state) {
-                *state = dev_state_idx;
-                break;
-            }
-        }
+	(pd_type == MOD_PD_TYPE_DEVICE_DEBUG)) {
+	uint32_t dev_state_idx;
+	uint32_t dev_state_table_size = sizeof(pd_state_to_scmi_dev_state) /
+	    sizeof(pd_state_to_scmi_dev_state[0]);
+	uint32_t dev_state = *state;
+	/*
+	 * Convert the device SCMI power state sent by the agent,
+	 * into the internal power domain state as defined by the
+	 * pd_state_to_scmi_dev_state table.
+	 */
+	for (dev_state_idx = 0; dev_state_idx < dev_state_table_size;
+	     dev_state_idx++) {
+	    if (pd_state_to_scmi_dev_state[dev_state_idx] == dev_state) {
+		*state = dev_state_idx;
+		break;
+	    }
+	}
     }
 
     return FWK_SUCCESS;
@@ -900,22 +900,22 @@ static unsigned int get_pd_domain_id(
     const struct scmi_pd_power_state_get_a2p *params_get;
 
     enum scmi_pd_command_id message_id_type =
-        (enum scmi_pd_command_id)message_id;
+	(enum scmi_pd_command_id)message_id;
 
     switch (message_id_type) {
     case MOD_SCMI_PD_POWER_STATE_SET:
-        params_set = (const struct scmi_pd_power_state_set_a2p *)payload;
-        return params_set->domain_id;
+	params_set = (const struct scmi_pd_power_state_set_a2p *)payload;
+	return params_set->domain_id;
 
     default:
-        /*
-         * Every SCMI Power Domains message apart from power_state_set
-         * is formatted with the domain ID as the first message element.
-         * We will use the power_state_set message as a basic format to
-         * retrieve the domain ID to avoid unnecessary code.
-         */
-        params_get = (const struct scmi_pd_power_state_get_a2p *)payload;
-        return params_get->domain_id;
+	/*
+	 * Every SCMI Power Domains message apart from power_state_set
+	 * is formatted with the domain ID as the first message element.
+	 * We will use the power_state_set message as a basic format to
+	 * retrieve the domain ID to avoid unnecessary code.
+	 */
+	params_get = (const struct scmi_pd_power_state_get_a2p *)payload;
+	return params_get->domain_id;
     }
 }
 
@@ -934,42 +934,42 @@ static int scmi_pd_permissions_handler(
 
     status = scmi_pd_ctx.scmi_api->get_agent_id(service_id, &agent_id);
     if (status != FWK_SUCCESS) {
-        *return_values = (int32_t)SCMI_GENERIC_ERROR;
-        return FWK_E_PARAM;
+	*return_values = (int32_t)SCMI_GENERIC_ERROR;
+	return FWK_E_PARAM;
     }
 
     if (message_id < 3) {
-        perms = scmi_pd_ctx.res_perms_api->agent_has_protocol_permission(
-            agent_id, MOD_SCMI_PROTOCOL_ID_POWER_DOMAIN);
-        if (perms == MOD_RES_PERMS_ACCESS_ALLOWED) {
-            return FWK_SUCCESS;
-        }
-        return FWK_E_ACCESS;
+	perms = scmi_pd_ctx.res_perms_api->agent_has_protocol_permission(
+	    agent_id, MOD_SCMI_PROTOCOL_ID_POWER_DOMAIN);
+	if (perms == MOD_RES_PERMS_ACCESS_ALLOWED) {
+	    return FWK_SUCCESS;
+	}
+	return FWK_E_ACCESS;
     }
 
     domain_id = get_pd_domain_id(payload, message_id);
     if (domain_id > UINT16_MAX) {
-        *return_values = (int32_t)SCMI_NOT_FOUND;
-        return FWK_E_ACCESS;
+	*return_values = (int32_t)SCMI_NOT_FOUND;
+	return FWK_E_ACCESS;
     }
 
     pd_id = FWK_ID_ELEMENT(FWK_MODULE_IDX_POWER_DOMAIN, domain_id);
     if (!fwk_module_is_valid_element_id(pd_id)) {
-        *return_values = (int32_t)SCMI_NOT_FOUND;
-        return FWK_E_ACCESS;
+	*return_values = (int32_t)SCMI_NOT_FOUND;
+	return FWK_E_ACCESS;
     }
 
     status = scmi_pd_ctx.pd_api->get_domain_type(pd_id, &pd_type);
     if (status != FWK_SUCCESS) {
-        *return_values = (int32_t)SCMI_GENERIC_ERROR;
-        return FWK_E_ACCESS;
+	*return_values = (int32_t)SCMI_GENERIC_ERROR;
+	return FWK_E_ACCESS;
     }
 
     perms = scmi_pd_ctx.res_perms_api->agent_has_resource_permission(
-        agent_id, MOD_SCMI_PROTOCOL_ID_POWER_DOMAIN, message_id, domain_id);
+	agent_id, MOD_SCMI_PROTOCOL_ID_POWER_DOMAIN, message_id, domain_id);
 
     if (perms == MOD_RES_PERMS_ACCESS_ALLOWED) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     *return_values = (int32_t)SCMI_DENIED;
@@ -982,7 +982,7 @@ static int scmi_pd_permissions_handler(
  * SCMI module -> SCMI power module interface
  */
 static int scmi_pd_get_scmi_protocol_id(fwk_id_t protocol_id,
-                                        uint8_t *scmi_protocol_id)
+					uint8_t *scmi_protocol_id)
 {
     *scmi_protocol_id = (uint8_t)MOD_SCMI_PROTOCOL_ID_POWER_DOMAIN;
 
@@ -998,25 +998,25 @@ static int scmi_pd_message_handler(fwk_i
 #endif
 
     static_assert(FWK_ARRAY_SIZE(handler_table) ==
-        FWK_ARRAY_SIZE(payload_size_table),
-        "[SCMI] Power domain management protocol table sizes not consistent");
+	FWK_ARRAY_SIZE(payload_size_table),
+	"[SCMI] Power domain management protocol table sizes not consistent");
     fwk_assert(payload != NULL);
 
     if (message_id >= FWK_ARRAY_SIZE(handler_table)) {
-        return_value = (int32_t)SCMI_NOT_FOUND;
-        goto error;
+	return_value = (int32_t)SCMI_NOT_FOUND;
+	goto error;
     }
 
     if (payload_size != payload_size_table[message_id]) {
-        return_value = (int32_t)SCMI_PROTOCOL_ERROR;
-        goto error;
+	return_value = (int32_t)SCMI_PROTOCOL_ERROR;
+	goto error;
     }
 
 #ifdef BUILD_HAS_MOD_RESOURCE_PERMS
     status = scmi_pd_permissions_handler(
-        service_id, payload, payload_size, message_id, &return_value);
+	service_id, payload, payload_size, message_id, &return_value);
     if (status != FWK_SUCCESS) {
-        goto error;
+	goto error;
     }
 #endif
 
@@ -1024,7 +1024,7 @@ static int scmi_pd_message_handler(fwk_i
 
 error:
     return scmi_pd_ctx.scmi_api->respond(
-        service_id, &return_value, sizeof(return_value));
+	service_id, &return_value, sizeof(return_value));
 }
 
 static struct mod_scmi_to_protocol_api scmi_pd_mod_scmi_to_protocol_api = {
@@ -1037,54 +1037,54 @@ static struct mod_scmi_to_protocol_api s
  */
 
 static int scmi_pd_init(fwk_id_t module_id, unsigned int element_count,
-                        const void *data)
+			const void *data)
 {
 #ifdef BUILD_HAS_MOD_DEBUG
     struct mod_scmi_pd_config *config = (struct mod_scmi_pd_config *)data;
 #endif
 
     if (element_count != 0) {
-        return FWK_E_SUPPORT;
+	return FWK_E_SUPPORT;
     }
 
     scmi_pd_ctx.domain_count =
-        (unsigned int)fwk_module_get_element_count(fwk_module_id_power_domain);
+	(unsigned int)fwk_module_get_element_count(fwk_module_id_power_domain);
     if (scmi_pd_ctx.domain_count <= 1) {
-        return FWK_E_SUPPORT;
+	return FWK_E_SUPPORT;
     }
 
     /* Do not expose SYSTEM domain (always the last one) to agents and ... */
     scmi_pd_ctx.domain_count--;
     /* ... and expose no more than 0xFFFF number of domains. */
     if (scmi_pd_ctx.domain_count > UINT16_MAX) {
-        scmi_pd_ctx.domain_count = UINT16_MAX;
+	scmi_pd_ctx.domain_count = UINT16_MAX;
     }
 
 #ifdef BUILD_HAS_MOD_DEBUG
     if (config == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     if (fwk_module_is_valid_element_id(config->debug_id) ||
-        fwk_module_is_valid_element_id(config->debug_pd_id)) {
-        scmi_pd_ctx.debug_pd_id = config->debug_pd_id;
-        scmi_pd_ctx.debug_id = config->debug_id;
+	fwk_module_is_valid_element_id(config->debug_pd_id)) {
+	scmi_pd_ctx.debug_pd_id = config->debug_pd_id;
+	scmi_pd_ctx.debug_id = config->debug_id;
     } else {
-        return FWK_E_DATA;
+	return FWK_E_DATA;
     }
 #endif
 
     /* Allocate a table of scmi_pd operations */
     scmi_pd_ctx.ops =
-        fwk_mm_calloc(scmi_pd_ctx.domain_count,
-        sizeof(struct scmi_pd_operations));
+	fwk_mm_calloc(scmi_pd_ctx.domain_count,
+	sizeof(struct scmi_pd_operations));
     if (scmi_pd_ctx.ops == NULL) {
-        return FWK_E_NOMEM;
+	return FWK_E_NOMEM;
     }
 
     /* Initialize table */
     for (unsigned int i = 0; i < scmi_pd_ctx.domain_count; i++) {
-        scmi_pd_ctx.ops[i].service_id = FWK_ID_NONE;
+	scmi_pd_ctx.ops[i].service_id = FWK_ID_NONE;
     }
 
     return FWK_SUCCESS;
@@ -1097,18 +1097,18 @@ static int scmi_init_notifications(unsig
 
     status = scmi_pd_ctx.scmi_api->get_agent_count(&scmi_pd_ctx.agent_count);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
     fwk_assert(scmi_pd_ctx.agent_count != 0u);
 
     status = scmi_pd_ctx.scmi_notification_api->scmi_notification_init(
-        MOD_SCMI_PROTOCOL_ID_POWER_DOMAIN,
-        scmi_pd_ctx.agent_count,
-        domains,
-        MOD_SCMI_PD_NOTIFICATION_COUNT);
+	MOD_SCMI_PROTOCOL_ID_POWER_DOMAIN,
+	scmi_pd_ctx.agent_count,
+	domains,
+	MOD_SCMI_PD_NOTIFICATION_COUNT);
 
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     return FWK_SUCCESS;
@@ -1120,54 +1120,54 @@ static int scmi_pd_bind(fwk_id_t id, uns
     int status;
 
     if (round == 1) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     status = fwk_module_bind(FWK_ID_MODULE(FWK_MODULE_IDX_SCMI),
-        FWK_ID_API(FWK_MODULE_IDX_SCMI, MOD_SCMI_API_IDX_PROTOCOL),
-        &scmi_pd_ctx.scmi_api);
+	FWK_ID_API(FWK_MODULE_IDX_SCMI, MOD_SCMI_API_IDX_PROTOCOL),
+	&scmi_pd_ctx.scmi_api);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
 #ifdef BUILD_HAS_SCMI_NOTIFICATIONS
     status = fwk_module_bind(
-        FWK_ID_MODULE(FWK_MODULE_IDX_SCMI),
-        FWK_ID_API(FWK_MODULE_IDX_SCMI, MOD_SCMI_API_IDX_NOTIFICATION),
-        &scmi_pd_ctx.scmi_notification_api);
+	FWK_ID_MODULE(FWK_MODULE_IDX_SCMI),
+	FWK_ID_API(FWK_MODULE_IDX_SCMI, MOD_SCMI_API_IDX_NOTIFICATION),
+	&scmi_pd_ctx.scmi_notification_api);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 #endif
 
 #ifdef BUILD_HAS_MOD_DEBUG
     status = fwk_module_bind(scmi_pd_ctx.debug_id,
-        FWK_ID_API(FWK_MODULE_IDX_DEBUG, MOD_DEBUG_API_IDX_HAL),
-        &scmi_pd_ctx.debug_api);
+	FWK_ID_API(FWK_MODULE_IDX_DEBUG, MOD_DEBUG_API_IDX_HAL),
+	&scmi_pd_ctx.debug_api);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 #endif
 
 #ifdef BUILD_HAS_MOD_RESOURCE_PERMS
     status = fwk_module_bind(
-        FWK_ID_MODULE(FWK_MODULE_IDX_RESOURCE_PERMS),
-        FWK_ID_API(FWK_MODULE_IDX_RESOURCE_PERMS, MOD_RES_PERM_RESOURCE_PERMS),
-        &scmi_pd_ctx.res_perms_api);
+	FWK_ID_MODULE(FWK_MODULE_IDX_RESOURCE_PERMS),
+	FWK_ID_API(FWK_MODULE_IDX_RESOURCE_PERMS, MOD_RES_PERM_RESOURCE_PERMS),
+	&scmi_pd_ctx.res_perms_api);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 #endif
 
     return fwk_module_bind(fwk_module_id_power_domain, mod_pd_api_id_restricted,
-        &scmi_pd_ctx.pd_api);
+	&scmi_pd_ctx.pd_api);
 }
 
 static int scmi_pd_process_bind_request(fwk_id_t source_id, fwk_id_t target_id,
-                                        fwk_id_t api_id, const void **api)
+					fwk_id_t api_id, const void **api)
 {
     if (!fwk_id_is_equal(source_id, FWK_ID_MODULE(FWK_MODULE_IDX_SCMI))) {
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 
     *api = &scmi_pd_mod_scmi_to_protocol_api;
@@ -1184,71 +1184,71 @@ static int process_request_event(const s
     int status, respond_status;
     bool state_get;
     struct scmi_pd_power_state_get_p2a retval_get = {
-        .status = SCMI_GENERIC_ERROR
+	.status = SCMI_GENERIC_ERROR
     };
     struct scmi_pd_power_state_set_p2a retval_set = {
-        .status = SCMI_GENERIC_ERROR
+	.status = SCMI_GENERIC_ERROR
     };
 #endif
     struct event_request_params *params =
-        (struct event_request_params *)event->params;
+	(struct event_request_params *)event->params;
 
     pd_id = params->pd_id;
 
     switch ((enum scmi_pd_event_idx)fwk_id_get_event_idx(event->id)) {
 #ifdef BUILD_HAS_MOD_DEBUG
     case SCMI_PD_EVENT_IDX_DEBUG_GET:
-        state_get = true;
+	state_get = true;
 
-        status = scmi_pd_ctx.debug_api->get_enabled(scmi_pd_ctx.debug_id,
-                                                    &dbg_enabled,
-                                                    SCP_DEBUG_USER_AP);
-        if (status != FWK_SUCCESS) {
-            break;
-        }
-
-        retval_get.status = SCMI_SUCCESS;
-        retval_get.power_state =
-            dbg_enabled ? pd_state_to_scmi_dev_state[MOD_PD_STATE_ON]
-                        : pd_state_to_scmi_dev_state[MOD_PD_STATE_OFF];
+	status = scmi_pd_ctx.debug_api->get_enabled(scmi_pd_ctx.debug_id,
+						    &dbg_enabled,
+						    SCP_DEBUG_USER_AP);
+	if (status != FWK_SUCCESS) {
+	    break;
+	}
+
+	retval_get.status = SCMI_SUCCESS;
+	retval_get.power_state =
+	    dbg_enabled ? pd_state_to_scmi_dev_state[MOD_PD_STATE_ON]
+			: pd_state_to_scmi_dev_state[MOD_PD_STATE_OFF];
 
-        break;
+	break;
 
     case SCMI_PD_EVENT_IDX_DEBUG_SET:
-        state_get = false;
-        status = scmi_pd_ctx.debug_api->set_enabled(
-                scmi_pd_ctx.debug_id,
-                params->pd_power_state == MOD_PD_STATE_ON,
-                SCP_DEBUG_USER_AP);
-
-        if (status == FWK_SUCCESS) {
-            retval_set.status = SCMI_SUCCESS;
-        }
+	state_get = false;
+	status = scmi_pd_ctx.debug_api->set_enabled(
+		scmi_pd_ctx.debug_id,
+		params->pd_power_state == MOD_PD_STATE_ON,
+		SCP_DEBUG_USER_AP);
+
+	if (status == FWK_SUCCESS) {
+	    retval_set.status = SCMI_SUCCESS;
+	}
 
-        break;
+	break;
 #endif
     case SCMI_PD_EVENT_IDX_SET_STATE:
-        return scmi_pd_ctx.pd_api->set_state(
-            pd_id, true, params->pd_power_state);
+	return scmi_pd_ctx.pd_api->set_state(
+	    pd_id, true, params->pd_power_state);
 
     default:
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
 #ifdef BUILD_HAS_MOD_DEBUG
     if (status != FWK_PENDING) {
-        service_id = ops_get_service(pd_id);
+	service_id = ops_get_service(pd_id);
 
-        respond_status = scmi_pd_ctx.scmi_api->respond(
-            service_id,
-            state_get ? (void *)&retval_get : (void *)&retval_set,
-            state_get ? sizeof(retval_get) : sizeof(retval_set));
-
-        if (respond_status != FWK_SUCCESS) {
-            FWK_LOG_DEBUG("[SCMI-power] %s @%d", __func__, __LINE__);
-        }
+	respond_status = scmi_pd_ctx.scmi_api->respond(
+	    service_id,
+	    state_get ? (void *)&retval_get : (void *)&retval_set,
+	    state_get ? sizeof(retval_get) : sizeof(retval_set));
+
+	if (respond_status != FWK_SUCCESS) {
+	    FWK_LOG_DEBUG("[SCMI-power] %s @%d", __func__, __LINE__);
+	}
 
-        ops_set_idle(pd_id);
+	ops_set_idle(pd_id);
     }
 
     return status;
@@ -1261,84 +1261,84 @@ static int process_response_event(const
     fwk_id_t service_id;
     unsigned int module_idx;
     struct scmi_pd_power_state_set_p2a retval_set = {
-        .status = (int32_t)SCMI_GENERIC_ERROR
+	.status = (int32_t)SCMI_GENERIC_ERROR
     };
 #ifdef BUILD_HAS_MOD_DEBUG
     struct scmi_pd_power_state_get_p2a retval_get = { .status =
-                                                          SCMI_GENERIC_ERROR };
+							  SCMI_GENERIC_ERROR };
 #endif
 
     module_idx = fwk_id_get_module_idx(event->source_id);
 
     if (module_idx == fwk_id_get_module_idx(fwk_module_id_power_domain)) {
-        struct pd_set_state_response *params =
-            (struct pd_set_state_response *)event->params;
+	struct pd_set_state_response *params =
+	    (struct pd_set_state_response *)event->params;
 
-        service_id = ops_get_service(event->source_id);
+	service_id = ops_get_service(event->source_id);
 
-        if (params->status == FWK_SUCCESS) {
-            retval_set.status = (int32_t)SCMI_SUCCESS;
-        }
+	if (params->status == FWK_SUCCESS) {
+	    retval_set.status = (int32_t)SCMI_SUCCESS;
+	}
 
-        respond_status = scmi_pd_ctx.scmi_api->respond(
-            service_id, &retval_set, sizeof(retval_set));
+	respond_status = scmi_pd_ctx.scmi_api->respond(
+	    service_id, &retval_set, sizeof(retval_set));
 
-        if (respond_status != FWK_SUCCESS) {
-            FWK_LOG_DEBUG("[SCMI-power] %s @%d", __func__, __LINE__);
-        }
+	if (respond_status != FWK_SUCCESS) {
+	    FWK_LOG_DEBUG("[SCMI-power] %s @%d", __func__, __LINE__);
+	}
 
-        ops_set_idle(event->source_id);
-        return FWK_SUCCESS;
+	ops_set_idle(event->source_id);
+	return FWK_SUCCESS;
 
 #ifdef BUILD_HAS_MOD_DEBUG
     } else if (module_idx == fwk_id_get_module_idx(fwk_module_id_debug)) {
-        /* Responses from Debug module */
-        struct mod_debug_response_params *params =
-            (struct mod_debug_response_params *)event->params;
-
-        if (params->status == FWK_SUCCESS) {
-            retval_set.status = SCMI_SUCCESS;
-        }
-
-        /*
-         * We know this event comes from the DEBUG HAL for now so we use the
-         * corresponding pd_id.
-         */
-        service_id = ops_get_service(scmi_pd_ctx.debug_pd_id);
-
-        if (fwk_id_get_event_idx(event->id) ==
-            MOD_DEBUG_PUBLIC_EVENT_IDX_REQ_ENABLE_GET) {
-            retval_get.power_state =
-                params->enabled ? pd_state_to_scmi_dev_state[MOD_PD_STATE_ON]
-                                : pd_state_to_scmi_dev_state[MOD_PD_STATE_OFF];
-            respond_status = scmi_pd_ctx.scmi_api->respond(
-                service_id, (void *)&retval_get, sizeof(retval_get));
-            if (respond_status != FWK_SUCCESS) {
-                FWK_LOG_DEBUG("[SCMI-power] %s @%d", __func__, __LINE__);
-            }
-        } else {
-            respond_status = scmi_pd_ctx.scmi_api->respond(
-                service_id, (void *)&retval_set, sizeof(retval_set));
-            if (respond_status != FWK_SUCCESS) {
-                FWK_LOG_DEBUG("[SCMI-power] %s @%d", __func__, __LINE__);
-            }
-        }
-        ops_set_idle(scmi_pd_ctx.debug_pd_id);
-        return FWK_SUCCESS;
+	/* Responses from Debug module */
+	struct mod_debug_response_params *params =
+	    (struct mod_debug_response_params *)event->params;
+
+	if (params->status == FWK_SUCCESS) {
+	    retval_set.status = SCMI_SUCCESS;
+	}
+
+	/*
+	 * We know this event comes from the DEBUG HAL for now so we use the
+	 * corresponding pd_id.
+	 */
+	service_id = ops_get_service(scmi_pd_ctx.debug_pd_id);
+
+	if (fwk_id_get_event_idx(event->id) ==
+	    MOD_DEBUG_PUBLIC_EVENT_IDX_REQ_ENABLE_GET) {
+	    retval_get.power_state =
+		params->enabled ? pd_state_to_scmi_dev_state[MOD_PD_STATE_ON]
+				: pd_state_to_scmi_dev_state[MOD_PD_STATE_OFF];
+	    respond_status = scmi_pd_ctx.scmi_api->respond(
+		service_id, (void *)&retval_get, sizeof(retval_get));
+	    if (respond_status != FWK_SUCCESS) {
+		FWK_LOG_DEBUG("[SCMI-power] %s @%d", __func__, __LINE__);
+	    }
+	} else {
+	    respond_status = scmi_pd_ctx.scmi_api->respond(
+		service_id, (void *)&retval_set, sizeof(retval_set));
+	    if (respond_status != FWK_SUCCESS) {
+		FWK_LOG_DEBUG("[SCMI-power] %s @%d", __func__, __LINE__);
+	    }
+	}
+	ops_set_idle(scmi_pd_ctx.debug_pd_id);
+	return FWK_SUCCESS;
 #endif
 
     } else {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 }
 
 static int scmi_pd_process_event(const struct fwk_event *event,
-                                 struct fwk_event *resp_event)
+				 struct fwk_event *resp_event)
 {
     if (event->is_response) {
-        return process_response_event(event);
+	return process_response_event(event);
     } else {
-        return process_request_event(event);
+	return process_request_event(event);
     }
 }
 
@@ -1351,20 +1351,20 @@ static int scmi_pd_process_notification(
     unsigned int agent_id;
     uint32_t state;
     struct mod_pd_power_state_transition_notification_params *event_params =
-        ((struct mod_pd_power_state_transition_notification_params *)
-            event->params);
+	((struct mod_pd_power_state_transition_notification_params *)
+	    event->params);
 
     if (fwk_id_is_equal(
-            event->id, mod_pd_notification_id_power_state_transition)) {
-        domain_id = fwk_id_get_element_idx(event->source_id);
-        state = event_params->state;
-        agent_id = ops_get_agent_id(event->source_id);
-        scmi_pd_power_state_notify(
-            MOD_SCMI_PD_POWER_STATE_NOTIFY,
-            SCMI_POWER_STATE_CHANGED,
-            domain_id,
-            agent_id,
-            state);
+	    event->id, mod_pd_notification_id_power_state_transition)) {
+	domain_id = fwk_id_get_element_idx(event->source_id);
+	state = event_params->state;
+	agent_id = ops_get_agent_id(event->source_id);
+	scmi_pd_power_state_notify(
+	    MOD_SCMI_PD_POWER_STATE_NOTIFY,
+	    SCMI_POWER_STATE_CHANGED,
+	    domain_id,
+	    agent_id,
+	    state);
     }
 
     return FWK_SUCCESS;
@@ -1377,7 +1377,7 @@ static int scmi_pd_start(fwk_id_t id)
 #ifdef BUILD_HAS_SCMI_NOTIFICATIONS
     status = scmi_init_notifications(scmi_pd_ctx.domain_count);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 #endif
     return status;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_reset_domain/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi_reset_domain/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_reset_domain/CMakeLists.txt
@@ -8,7 +8,7 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(
     ${SCP_MODULE_TARGET}
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_reset_domain/include/mod_scmi_reset_domain.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi_reset_domain/include/mod_scmi_reset_domain.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_reset_domain/include/mod_scmi_reset_domain.h
@@ -164,11 +164,11 @@ enum mod_scmi_reset_domain_policy_status
  * \return Status code representing the result of the operation.
  */
 int scmi_reset_domain_reset_request_policy(
-        enum mod_scmi_reset_domain_policy_status *policy_status,
-        enum mod_reset_domain_mode *mode,
-        uint32_t *reset_state,
-        uint32_t agent_id,
-        uint32_t domain_id);
+	enum mod_scmi_reset_domain_policy_status *policy_status,
+	enum mod_reset_domain_mode *mode,
+	uint32_t *reset_state,
+	uint32_t agent_id,
+	uint32_t domain_id);
 /*!
  * \}
  */
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_reset_domain/src/mod_scmi_reset_domain.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi_reset_domain/src/mod_scmi_reset_domain.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_reset_domain/src/mod_scmi_reset_domain.c
@@ -60,19 +60,19 @@ struct scmi_rd_ctx {
 };
 
 static int protocol_version_handler(fwk_id_t service_id,
-                                    const uint32_t *payload);
+				    const uint32_t *payload);
 
 static int protocol_attributes_handler(fwk_id_t service_id,
-                                       const uint32_t *payload);
+				       const uint32_t *payload);
 static int protocol_message_attributes_handler(fwk_id_t service_id,
-                                               const uint32_t *payload);
+					       const uint32_t *payload);
 static int reset_attributes_handler(fwk_id_t service_id,
-                                    const uint32_t *payload);
+				    const uint32_t *payload);
 static int reset_request_handler(fwk_id_t service_id,
-                                 const uint32_t *payload);
+				 const uint32_t *payload);
 #ifdef BUILD_HAS_SCMI_NOTIFICATIONS
 static int reset_notify_handler(fwk_id_t service_id,
-                                const uint32_t *payload);
+				const uint32_t *payload);
 #endif
 
 /*
@@ -85,7 +85,7 @@ static int (*msg_handler_table[])(fwk_id
     [MOD_SCMI_PROTOCOL_VERSION] = protocol_version_handler,
     [MOD_SCMI_PROTOCOL_ATTRIBUTES] = protocol_attributes_handler,
     [MOD_SCMI_PROTOCOL_MESSAGE_ATTRIBUTES] =
-         protocol_message_attributes_handler,
+	 protocol_message_attributes_handler,
     [MOD_SCMI_RESET_DOMAIN_ATTRIBUTES] = reset_attributes_handler,
     [MOD_SCMI_RESET_REQUEST] = reset_request_handler,
 #ifdef BUILD_HAS_SCMI_NOTIFICATIONS
@@ -97,11 +97,11 @@ static unsigned int payload_size_table[]
     [MOD_SCMI_PROTOCOL_VERSION] = 0,
     [MOD_SCMI_PROTOCOL_ATTRIBUTES] = 0,
     [MOD_SCMI_PROTOCOL_MESSAGE_ATTRIBUTES] =
-        (unsigned int)sizeof(struct scmi_protocol_message_attributes_a2p),
+	(unsigned int)sizeof(struct scmi_protocol_message_attributes_a2p),
     [MOD_SCMI_RESET_DOMAIN_ATTRIBUTES] =
-        (unsigned int)sizeof(struct scmi_reset_domain_attributes_a2p),
+	(unsigned int)sizeof(struct scmi_reset_domain_attributes_a2p),
     [MOD_SCMI_RESET_REQUEST] =
-        (unsigned int)sizeof(struct scmi_reset_domain_request_a2p),
+	(unsigned int)sizeof(struct scmi_reset_domain_request_a2p),
 #ifdef BUILD_HAS_SCMI_NOTIFICATIONS
     [MOD_SCMI_RESET_NOTIFY] = sizeof(struct scmi_reset_domain_notify_a2p),
 #endif
@@ -112,43 +112,43 @@ static unsigned int payload_size_table[]
  */
 
 static int protocol_version_handler(fwk_id_t service_id,
-                                    const uint32_t *payload)
+				    const uint32_t *payload)
 {
     struct scmi_protocol_version_p2a outmsg = {
-        .status = (int32_t)SCMI_SUCCESS,
-        .version = SCMI_PROTOCOL_VERSION_RESET_DOMAIN,
+	.status = (int32_t)SCMI_SUCCESS,
+	.version = SCMI_PROTOCOL_VERSION_RESET_DOMAIN,
     };
 
     return scmi_rd_ctx.scmi_api->respond(service_id, &outmsg, sizeof(outmsg));
 }
 
 static int protocol_attributes_handler(fwk_id_t service_id,
-                                       const uint32_t *payload)
+				       const uint32_t *payload)
 {
     struct scmi_reset_domain_protocol_attributes_p2a outmsg = {
-        .status = (int32_t)SCMI_SUCCESS,
+	.status = (int32_t)SCMI_SUCCESS,
     };
     int status = 0;
     unsigned int agent_id = 0;
 
     status = scmi_rd_ctx.scmi_api->get_agent_id(service_id, &agent_id);
     if (status != FWK_SUCCESS)
-        return status;
+	return status;
 
     if (agent_id >= scmi_rd_ctx.config->agent_count)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     outmsg.attributes = scmi_rd_ctx.config->
-                            agent_table[agent_id].agent_domain_count;
+			    agent_table[agent_id].agent_domain_count;
 
     return scmi_rd_ctx.scmi_api->respond(service_id, &outmsg, sizeof(outmsg));
 }
 
 static int protocol_message_attributes_handler(fwk_id_t service_id,
-                                               const uint32_t *payload)
+					       const uint32_t *payload)
 {
     struct scmi_protocol_message_attributes_p2a outmsg = {
-        .status = (int32_t)SCMI_NOT_FOUND,
+	.status = (int32_t)SCMI_NOT_FOUND,
     };
     size_t outmsg_size = sizeof(outmsg.status);
     struct scmi_protocol_message_attributes_a2p params = { 0 };
@@ -156,9 +156,9 @@ static int protocol_message_attributes_h
     params = *(const struct scmi_protocol_message_attributes_a2p *)payload;
 
     if ((params.message_id < FWK_ARRAY_SIZE(msg_handler_table)) &&
-        (msg_handler_table[params.message_id] != NULL)) {
-        outmsg.status = (int32_t)SCMI_SUCCESS;
-        outmsg_size = sizeof(outmsg);
+	(msg_handler_table[params.message_id] != NULL)) {
+	outmsg.status = (int32_t)SCMI_SUCCESS;
+	outmsg_size = sizeof(outmsg);
     }
 
     return scmi_rd_ctx.scmi_api->respond(service_id, &outmsg, outmsg_size);
@@ -173,10 +173,10 @@ static int get_agent_id(fwk_id_t service
 
     status = scmi_rd_ctx.scmi_api->get_agent_id(service_id, agent_id);
     if (status != FWK_SUCCESS)
-        return status;
+	return status;
 
     if (*agent_id >= scmi_rd_ctx.config->agent_count)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     return FWK_SUCCESS;
 }
@@ -186,14 +186,14 @@ static int get_agent_id(fwk_id_t service
  * \c mod_scmi_reset_domain_agent structure within the agent table.
  */
 static int get_agent_entry(fwk_id_t service_id,
-                           const struct mod_scmi_reset_domain_agent **agent)
+			   const struct mod_scmi_reset_domain_agent **agent)
 {
     int status = 0;
     unsigned int agent_id = 0;
 
     status = get_agent_id(service_id, &agent_id);
     if (status != FWK_SUCCESS)
-        return status;
+	return status;
 
     *agent = &scmi_rd_ctx.config->agent_table[agent_id];
 
@@ -201,25 +201,25 @@ static int get_agent_entry(fwk_id_t serv
 }
 
 static int get_reset_device(fwk_id_t service_id,
-                            unsigned int domain_id,
-                            const struct mod_scmi_reset_domain_device **device)
+			    unsigned int domain_id,
+			    const struct mod_scmi_reset_domain_device **device)
 {
     int status;
     const struct mod_scmi_reset_domain_agent *agent_entry = NULL;
 
     status = get_agent_entry(service_id, &agent_entry);
     if (status != FWK_SUCCESS)
-        return status;
+	return status;
 
     if (domain_id >= agent_entry->agent_domain_count)
-        return FWK_E_RANGE;
+	return FWK_E_RANGE;
 
     *device = &agent_entry->device_table[domain_id];
 
     fwk_assert(fwk_module_is_valid_element_id((*device)->element_id));
 
     if (!fwk_module_is_valid_element_id((*device)->element_id))
-        return FWK_E_PANIC;
+	return FWK_E_PANIC;
 
     return FWK_SUCCESS;
 }
@@ -241,13 +241,13 @@ FWK_WEAK int scmi_reset_domain_reset_req
 
 
 static int reset_attributes_handler(fwk_id_t service_id,
-                                    const uint32_t *payload)
+				    const uint32_t *payload)
 {
     const struct mod_scmi_reset_domain_device *reset_device = NULL;
     struct mod_reset_domain_dev_config *reset_dev_config = NULL;
     struct scmi_reset_domain_attributes_a2p params = { 0 };
     struct scmi_reset_domain_attributes_p2a outmsg = {
-        .status = (int32_t)SCMI_GENERIC_ERROR,
+	.status = (int32_t)SCMI_GENERIC_ERROR,
     };
     size_t outmsg_size = sizeof(outmsg.status);
     int status = FWK_SUCCESS;
@@ -257,50 +257,50 @@ static int reset_attributes_handler(fwk_
 
     status = get_reset_device(service_id, params.domain_id, &reset_device);
     if (status != FWK_SUCCESS) {
-        outmsg.status = (int32_t)SCMI_NOT_FOUND;
-        goto exit;
+	outmsg.status = (int32_t)SCMI_NOT_FOUND;
+	goto exit;
     }
 
     reset_dev_config = (struct mod_reset_domain_dev_config *)
-                           fwk_module_get_data(reset_device->element_id);
+			   fwk_module_get_data(reset_device->element_id);
     /*
      * Currently: no support for async reset.
      */
     outmsg.flags &= (uint32_t)~SCMI_RESET_DOMAIN_ATTR_ASYNC;
 
     if (reset_dev_config->capabilities & MOD_RESET_DOMAIN_CAP_NOTIFICATION)
-        outmsg.flags |= (uint32_t)SCMI_RESET_DOMAIN_ATTR_NOTIF;
+	outmsg.flags |= (uint32_t)SCMI_RESET_DOMAIN_ATTR_NOTIF;
 
     outmsg.latency = reset_dev_config->latency;
 
     strncpy(
-        (char *)outmsg.name,
-        fwk_module_get_element_name(reset_device->element_id),
-        sizeof(outmsg.name) - 1);
+	(char *)outmsg.name,
+	fwk_module_get_element_name(reset_device->element_id),
+	sizeof(outmsg.name) - 1);
 
     outmsg.status = (int32_t)SCMI_SUCCESS;
     outmsg_size = sizeof(outmsg);
 
 exit:
     respond_status =
-        scmi_rd_ctx.scmi_api->respond(service_id, &outmsg, outmsg_size);
+	scmi_rd_ctx.scmi_api->respond(service_id, &outmsg, outmsg_size);
 
     if (respond_status != FWK_SUCCESS) {
-        FWK_LOG_DEBUG("[SCMI-RESET] %s @%d", __func__, __LINE__);
+	FWK_LOG_DEBUG("[SCMI-RESET] %s @%d", __func__, __LINE__);
     }
 
     return status;
 }
 
 static int reset_request_handler(fwk_id_t service_id,
-                                 const uint32_t *payload)
+				 const uint32_t *payload)
 {
     int status, respond_status;
     unsigned int agent_id;
     struct mod_reset_domain_dev_config *reset_dev_config;
     struct scmi_reset_domain_request_a2p params = { 0 };
     struct scmi_reset_domain_request_p2a outmsg = {
-        .status = (int32_t)SCMI_NOT_FOUND
+	.status = (int32_t)SCMI_NOT_FOUND
     };
     enum mod_reset_domain_mode mode = MOD_RESET_DOMAIN_MODE_EXPLICIT_DEASSERT;
 
@@ -313,9 +313,9 @@ static int reset_request_handler(fwk_id_
     params = *(const struct scmi_reset_domain_request_a2p *)payload;
 
     if ((params.flags & ~SCMI_RESET_DOMAIN_FLAGS_MASK) != 0) {
-        status = FWK_SUCCESS;
-        outmsg.status = (int32_t)SCMI_INVALID_PARAMETERS;
-        goto exit;
+	status = FWK_SUCCESS;
+	outmsg.status = (int32_t)SCMI_INVALID_PARAMETERS;
+	goto exit;
     }
 
     /**
@@ -323,27 +323,27 @@ static int reset_request_handler(fwk_id_
      * state type is Architectural.
      */
     if (((params.reset_state & SCMI_RESET_DOMAIN_RESET_STATE_TYPE_MASK) == 0) &&
-        ((params.reset_state & SCMI_RESET_DOMAIN_RESET_STATE_ID_MASK) != 0)) {
-        status = FWK_SUCCESS;
-        outmsg.status = (int32_t)SCMI_INVALID_PARAMETERS;
-        goto exit;
+	((params.reset_state & SCMI_RESET_DOMAIN_RESET_STATE_ID_MASK) != 0)) {
+	status = FWK_SUCCESS;
+	outmsg.status = (int32_t)SCMI_INVALID_PARAMETERS;
+	goto exit;
     }
 
     status = scmi_rd_ctx.scmi_api->get_agent_id(service_id, &agent_id);
     if (status != FWK_SUCCESS)
-        goto exit;
+	goto exit;
 
     if (params.domain_id >= scmi_rd_ctx.plat_reset_domain_count) {
-        status = FWK_E_PARAM;
-        goto exit;
+	status = FWK_E_PARAM;
+	goto exit;
     }
 
     status = get_reset_device(service_id, params.domain_id, &reset_device);
     if (status != FWK_SUCCESS)
-        goto exit;
+	goto exit;
 
     reset_dev_config = (struct mod_reset_domain_dev_config *)
-                       fwk_module_get_data(reset_device->element_id);
+		       fwk_module_get_data(reset_device->element_id);
 
     /* Check if explicit assert is requested.
      * Note, valid request in flags
@@ -353,65 +353,65 @@ static int reset_request_handler(fwk_id_
      *       b101 Auto Reset Async.
      */
     if (!(params.flags & SCMI_RESET_DOMAIN_AUTO)) {
-        /* If auto reset is not requested then check if device supports explicit
-         * assert/de-assert reset.
-         */
-        if (!(reset_dev_config->modes &
-            (MOD_RESET_DOMAIN_MODE_EXPLICIT_ASSERT |
-            MOD_RESET_DOMAIN_MODE_EXPLICIT_DEASSERT))) {
-            outmsg.status = (int32_t)SCMI_NOT_SUPPORTED;
-            goto exit;
-        } else {
-           if (params.flags & SCMI_RESET_DOMAIN_EXPLICIT)
-               mode = MOD_RESET_DOMAIN_MODE_EXPLICIT_ASSERT;
-        }
+	/* If auto reset is not requested then check if device supports explicit
+	 * assert/de-assert reset.
+	 */
+	if (!(reset_dev_config->modes &
+	    (MOD_RESET_DOMAIN_MODE_EXPLICIT_ASSERT |
+	    MOD_RESET_DOMAIN_MODE_EXPLICIT_DEASSERT))) {
+	    outmsg.status = (int32_t)SCMI_NOT_SUPPORTED;
+	    goto exit;
+	} else {
+	   if (params.flags & SCMI_RESET_DOMAIN_EXPLICIT)
+	       mode = MOD_RESET_DOMAIN_MODE_EXPLICIT_ASSERT;
+	}
     } else {
-        mode = (enum mod_reset_domain_mode)SCMI_RESET_DOMAIN_AUTO;
+	mode = (enum mod_reset_domain_mode)SCMI_RESET_DOMAIN_AUTO;
     }
 
     /* Handle async reset request. */
     if (params.flags & SCMI_RESET_DOMAIN_ASYNC) {
-        /* Async reset request is valid only in auto reset mode
-         */
-        if (!(params.flags & SCMI_RESET_DOMAIN_AUTO)) {
-            outmsg.status = (int32_t)SCMI_INVALID_PARAMETERS;
-            goto exit;
-        }
-
-        /* Return not supported as associated device does not support
-         * Async reset.
-         */
-        if (!(reset_dev_config->modes &
-            MOD_RESET_DOMAIN_MODE_AUTO_RESET_ASYNC)) {
-            outmsg.status = (int32_t)SCMI_NOT_SUPPORTED;
-            goto exit;
-        } else {
-            mode |= MOD_RESET_DOMAIN_MODE_AUTO_RESET_ASYNC;
-        }
+	/* Async reset request is valid only in auto reset mode
+	 */
+	if (!(params.flags & SCMI_RESET_DOMAIN_AUTO)) {
+	    outmsg.status = (int32_t)SCMI_INVALID_PARAMETERS;
+	    goto exit;
+	}
+
+	/* Return not supported as associated device does not support
+	 * Async reset.
+	 */
+	if (!(reset_dev_config->modes &
+	    MOD_RESET_DOMAIN_MODE_AUTO_RESET_ASYNC)) {
+	    outmsg.status = (int32_t)SCMI_NOT_SUPPORTED;
+	    goto exit;
+	} else {
+	    mode |= MOD_RESET_DOMAIN_MODE_AUTO_RESET_ASYNC;
+	}
     }
 
     reset_state = params.reset_state;
     status = scmi_reset_domain_reset_request_policy(&policy_status,
-        &mode, &reset_state, agent_id, params.domain_id);
+	&mode, &reset_state, agent_id, params.domain_id);
 
     if (status != FWK_SUCCESS) {
-        outmsg.status = (int32_t)SCMI_GENERIC_ERROR;
-        goto exit;
+	outmsg.status = (int32_t)SCMI_GENERIC_ERROR;
+	goto exit;
     }
     if (policy_status == MOD_SCMI_RESET_DOMAIN_SKIP_MESSAGE_HANDLER) {
-        outmsg.status = (int32_t)SCMI_SUCCESS;
-        goto exit;
+	outmsg.status = (int32_t)SCMI_SUCCESS;
+	goto exit;
     }
 
     outmsg.status = (int32_t)SCMI_NOT_SUPPORTED;
     status = reset_api->set_reset_state(reset_device->element_id,
-                                        mode,
-                                        reset_state,
-                                        (uintptr_t)agent_id);
+					mode,
+					reset_state,
+					(uintptr_t)agent_id);
     if (status != FWK_SUCCESS) {
-        if (status == FWK_E_STATE)
-            outmsg.status = (int32_t)SCMI_HARDWARE_ERROR;
-        goto exit;
+	if (status == FWK_E_STATE)
+	    outmsg.status = (int32_t)SCMI_HARDWARE_ERROR;
+	goto exit;
     }
 
     outmsg.status = (int32_t)SCMI_SUCCESS;
@@ -419,10 +419,10 @@ static int reset_request_handler(fwk_id_
 
 exit:
     respond_status =
-        scmi_rd_ctx.scmi_api->respond(service_id, &outmsg, outmsg_size);
+	scmi_rd_ctx.scmi_api->respond(service_id, &outmsg, outmsg_size);
 
     if (respond_status != FWK_SUCCESS) {
-        FWK_LOG_DEBUG("[SCMI-RESET] %s @%d", __func__, __LINE__);
+	FWK_LOG_DEBUG("[SCMI-RESET] %s @%d", __func__, __LINE__);
     }
 
     return status;
@@ -430,82 +430,82 @@ exit:
 
 #ifdef BUILD_HAS_SCMI_NOTIFICATIONS
 static int reset_notify_handler(fwk_id_t service_id,
-                                const uint32_t *payload)
+				const uint32_t *payload)
 {
     unsigned int agent_id;
     int status, respond_status;
     unsigned int domain_id;
     const struct scmi_reset_domain_notify_a2p *parameters;
     struct scmi_reset_domain_notify_p2a outmsg = {
-        .status = SCMI_GENERIC_ERROR,
+	.status = SCMI_GENERIC_ERROR,
     };
 
     status = get_agent_id(service_id, &agent_id);
     if (status != FWK_SUCCESS)
-        goto exit;
+	goto exit;
 
     parameters = (const struct scmi_reset_domain_notify_a2p *)payload;
 
     domain_id = parameters->domain_id;
     if (domain_id >= scmi_rd_ctx.config->
-        agent_table[agent_id].agent_domain_count) {
-        status = FWK_SUCCESS;
-        outmsg.status = SCMI_NOT_FOUND;
-        goto exit;
+	agent_table[agent_id].agent_domain_count) {
+	status = FWK_SUCCESS;
+	outmsg.status = SCMI_NOT_FOUND;
+	goto exit;
     }
 
     if ((parameters->notify_enable & ~SCMI_RESET_DOMAIN_DO_NOTIFY) != 0) {
-        status = FWK_SUCCESS;
-        outmsg.status = SCMI_INVALID_PARAMETERS;
-        goto exit;
+	status = FWK_SUCCESS;
+	outmsg.status = SCMI_INVALID_PARAMETERS;
+	goto exit;
     }
 
     if (parameters->notify_enable)
-        scmi_rd_ctx.scmi_notification_api->scmi_notification_add_subscriber(
-            MOD_SCMI_PROTOCOL_ID_RESET_DOMAIN,
-            domain_id,
-            MOD_SCMI_RESET_NOTIFY,
-            service_id);
+	scmi_rd_ctx.scmi_notification_api->scmi_notification_add_subscriber(
+	    MOD_SCMI_PROTOCOL_ID_RESET_DOMAIN,
+	    domain_id,
+	    MOD_SCMI_RESET_NOTIFY,
+	    service_id);
     else
-        scmi_rd_ctx.scmi_notification_api->scmi_notification_remove_subscriber(
-            MOD_SCMI_PROTOCOL_ID_RESET_DOMAIN,
-            agent_id,
-            domain_id,
-            MOD_SCMI_RESET_NOTIFY);
+	scmi_rd_ctx.scmi_notification_api->scmi_notification_remove_subscriber(
+	    MOD_SCMI_PROTOCOL_ID_RESET_DOMAIN,
+	    agent_id,
+	    domain_id,
+	    MOD_SCMI_RESET_NOTIFY);
 
     outmsg.status = SCMI_SUCCESS;
 
 exit:
     respond_status = scmi_rd_ctx.scmi_api->respond(
-        service_id,
-        &outmsg,
-        (outmsg.status == SCMI_SUCCESS) ? sizeof(outmsg) :
-                                          sizeof(outmsg.status));
+	service_id,
+	&outmsg,
+	(outmsg.status == SCMI_SUCCESS) ? sizeof(outmsg) :
+					  sizeof(outmsg.status));
 
     if (respond_status != FWK_SUCCESS) {
-        FWK_LOG_DEBUG("[SCMI-RESET] %s @%d", __func__, __LINE__);
+	FWK_LOG_DEBUG("[SCMI-RESET] %s @%d", __func__, __LINE__);
     }
 
     return status;
 }
 
 static void scmi_reset_issued_notify(uint32_t domain_id,
-                                     uint32_t reset_state,
-                                     uintptr_t cookie)
+				     uint32_t reset_state,
+				     uintptr_t cookie)
 {
     struct scmi_reset_domain_issued_p2a reset_issued = {
-         .agent_id = (uint32_t)cookie
+	 .agent_id = (uint32_t)cookie
     };
 
     reset_issued.domain_id = domain_id;
     reset_issued.reset_state = reset_state;
 
     scmi_rd_ctx.scmi_notification_api->scmi_notification_notify(
-        MOD_SCMI_PROTOCOL_ID_RESET_DOMAIN,
-        MOD_SCMI_RESET_NOTIFY,
-        MOD_SCMI_RESET_ISSUED,
-        &reset_issued,
-        sizeof(reset_issued));
+	MOD_SCMI_PROTOCOL_ID_RESET_DOMAIN,
+	MOD_SCMI_RESET_NOTIFY,
+	MOD_SCMI_RESET_ISSUED,
+	&reset_issued,
+	sizeof(reset_issued));
 }
 #endif
 
@@ -533,25 +533,25 @@ static int scmi_reset_domain_permissions
 
     status = scmi_rd_ctx.scmi_api->get_agent_id(service_id, &agent_id);
     if (status != FWK_SUCCESS)
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
 
     if (message_id < 3) {
-        perms = scmi_rd_ctx.res_perms_api->agent_has_protocol_permission(
-            agent_id, MOD_SCMI_PROTOCOL_ID_RESET_DOMAIN);
-        if (perms == MOD_RES_PERMS_ACCESS_ALLOWED)
-            return FWK_SUCCESS;
-        return FWK_E_ACCESS;
+	perms = scmi_rd_ctx.res_perms_api->agent_has_protocol_permission(
+	    agent_id, MOD_SCMI_PROTOCOL_ID_RESET_DOMAIN);
+	if (perms == MOD_RES_PERMS_ACCESS_ALLOWED)
+	    return FWK_SUCCESS;
+	return FWK_E_ACCESS;
     }
 
     domain_id = get_reset_domain_id(payload);
 
     perms = scmi_rd_ctx.res_perms_api->agent_has_resource_permission(
-        agent_id, MOD_SCMI_PROTOCOL_ID_RESET_DOMAIN, message_id, domain_id);
+	agent_id, MOD_SCMI_PROTOCOL_ID_RESET_DOMAIN, message_id, domain_id);
 
     if (perms == MOD_RES_PERMS_ACCESS_ALLOWED)
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     else
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
 }
 #endif
 
@@ -559,7 +559,7 @@ static int scmi_reset_domain_permissions
  * SCMI module -> SCMI reset module interface
  */
 static int scmi_reset_get_scmi_protocol_id(fwk_id_t protocol_id,
-                                           uint8_t *scmi_protocol_id)
+					   uint8_t *scmi_protocol_id)
 {
     *scmi_protocol_id = MOD_SCMI_PROTOCOL_ID_RESET_DOMAIN;
 
@@ -567,34 +567,34 @@ static int scmi_reset_get_scmi_protocol_
 }
 
 static int scmi_reset_message_handler(fwk_id_t protocol_id,
-                                      fwk_id_t service_id,
-                                      const uint32_t *payload,
-                                      size_t payload_size,
-                                      unsigned int message_id)
+				      fwk_id_t service_id,
+				      const uint32_t *payload,
+				      size_t payload_size,
+				      unsigned int message_id)
 {
     int32_t return_value;
 
     static_assert(FWK_ARRAY_SIZE(msg_handler_table) ==
-                  FWK_ARRAY_SIZE(payload_size_table),
-                  "[SCMI] reset domain protocol table sizes not consistent");
+		  FWK_ARRAY_SIZE(payload_size_table),
+		  "[SCMI] reset domain protocol table sizes not consistent");
 
     fwk_assert(payload != NULL);
 
     if (message_id >= FWK_ARRAY_SIZE(msg_handler_table)) {
-        return_value = (int32_t)SCMI_NOT_FOUND;
-        goto error;
+	return_value = (int32_t)SCMI_NOT_FOUND;
+	goto error;
     }
 
     if (payload_size != payload_size_table[message_id]) {
-        return_value = (int32_t)SCMI_PROTOCOL_ERROR;
-        goto error;
+	return_value = (int32_t)SCMI_PROTOCOL_ERROR;
+	goto error;
     }
 
 #ifdef BUILD_HAS_MOD_RESOURCE_PERMS
     if (scmi_reset_domain_permissions_handler(
-            service_id, payload, payload_size, message_id) != FWK_SUCCESS) {
-        return_value = SCMI_DENIED;
-        goto error;
+	    service_id, payload, payload_size, message_id) != FWK_SUCCESS) {
+	return_value = SCMI_DENIED;
+	goto error;
     }
 #endif
 
@@ -602,7 +602,7 @@ static int scmi_reset_message_handler(fw
 
 error:
     return scmi_rd_ctx.scmi_api->respond(
-        service_id, &return_value, sizeof(return_value));
+	service_id, &return_value, sizeof(return_value));
 }
 
 static struct mod_scmi_to_protocol_api scmi_reset_mod_scmi_to_protocol_api = {
@@ -615,15 +615,15 @@ static struct mod_scmi_to_protocol_api s
  */
 
 static int scmi_reset_init(fwk_id_t module_id,
-                           unsigned int element_count,
-                           const void *data)
+			   unsigned int element_count,
+			   const void *data)
 {
     const struct mod_scmi_reset_domain_config *config;
 
     config = (const struct mod_scmi_reset_domain_config *)data;
 
     if ((config == NULL) || (config->agent_table == NULL))
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     scmi_rd_ctx.config = config;
 
@@ -636,10 +636,10 @@ static int scmi_reset_init_notifications
     fwk_assert(scmi_rd_ctx.config->agent_count != 0u);
 
     return scmi_rd_ctx.scmi_notification_api->scmi_notification_init(
-        MOD_SCMI_PROTOCOL_ID_RESET_DOMAIN,
-        scmi_rd_ctx.config->agent_count,
-        scmi_rd_ctx.plat_reset_domain_count,
-        MOD_SCMI_RESET_DOMAIN_NOTIFICATION_COUNT);
+	MOD_SCMI_PROTOCOL_ID_RESET_DOMAIN,
+	scmi_rd_ctx.config->agent_count,
+	scmi_rd_ctx.plat_reset_domain_count,
+	MOD_SCMI_RESET_DOMAIN_NOTIFICATION_COUNT);
 }
 #endif
 
@@ -649,58 +649,58 @@ static int scmi_reset_bind(fwk_id_t id,
     int rst_dom_count;
 
     if (round == 1)
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 
     status = fwk_module_bind(FWK_ID_MODULE(FWK_MODULE_IDX_SCMI),
-                             FWK_ID_API(FWK_MODULE_IDX_SCMI,
-                                        MOD_SCMI_API_IDX_PROTOCOL),
-                             &scmi_rd_ctx.scmi_api);
+			     FWK_ID_API(FWK_MODULE_IDX_SCMI,
+					MOD_SCMI_API_IDX_PROTOCOL),
+			     &scmi_rd_ctx.scmi_api);
     if (status != FWK_SUCCESS)
-        return status;
+	return status;
 
 #ifdef BUILD_HAS_SCMI_NOTIFICATIONS
     status = fwk_module_bind(
-        FWK_ID_MODULE(FWK_MODULE_IDX_SCMI),
-        FWK_ID_API(FWK_MODULE_IDX_SCMI, MOD_SCMI_API_IDX_NOTIFICATION),
-        &scmi_rd_ctx.scmi_notification_api);
+	FWK_ID_MODULE(FWK_MODULE_IDX_SCMI),
+	FWK_ID_API(FWK_MODULE_IDX_SCMI, MOD_SCMI_API_IDX_NOTIFICATION),
+	&scmi_rd_ctx.scmi_notification_api);
     if (status != FWK_SUCCESS)
-        return status;
+	return status;
 #endif
 
     rst_dom_count = fwk_module_get_element_count(
-        FWK_ID_MODULE(FWK_MODULE_IDX_RESET_DOMAIN));
+	FWK_ID_MODULE(FWK_MODULE_IDX_RESET_DOMAIN));
 
     if (rst_dom_count <= 0) {
-        return FWK_E_SUPPORT;
+	return FWK_E_SUPPORT;
     } else {
-        scmi_rd_ctx.plat_reset_domain_count = (uint8_t)rst_dom_count;
+	scmi_rd_ctx.plat_reset_domain_count = (uint8_t)rst_dom_count;
     }
 
 #ifdef BUILD_HAS_MOD_RESOURCE_PERMS
     status = fwk_module_bind(
-        FWK_ID_MODULE(FWK_MODULE_IDX_RESOURCE_PERMS),
-        FWK_ID_API(FWK_MODULE_IDX_RESOURCE_PERMS, MOD_RES_PERM_RESOURCE_PERMS),
-        &scmi_rd_ctx.res_perms_api);
+	FWK_ID_MODULE(FWK_MODULE_IDX_RESOURCE_PERMS),
+	FWK_ID_API(FWK_MODULE_IDX_RESOURCE_PERMS, MOD_RES_PERM_RESOURCE_PERMS),
+	&scmi_rd_ctx.res_perms_api);
     if (status != FWK_SUCCESS)
-        return status;
+	return status;
 #endif
 
     return fwk_module_bind(FWK_ID_MODULE(FWK_MODULE_IDX_RESET_DOMAIN),
-                           FWK_ID_API(FWK_MODULE_IDX_RESET_DOMAIN, 0),
-                           &scmi_rd_ctx.reset_api);
+			   FWK_ID_API(FWK_MODULE_IDX_RESET_DOMAIN, 0),
+			   &scmi_rd_ctx.reset_api);
 }
 
 static int scmi_reset_process_bind_request(fwk_id_t source_id,
-                                           fwk_id_t target_id,
-                                           fwk_id_t api_id, const void **api)
+					   fwk_id_t target_id,
+					   fwk_id_t api_id, const void **api)
 {
     switch ((enum scmi_reset_domain_api_idx)fwk_id_get_api_idx(api_id)) {
     case MOD_SCMI_RESET_DOMAIN_PROTOCOL_API:
-        *api = &scmi_reset_mod_scmi_to_protocol_api;
-        break;
+	*api = &scmi_reset_mod_scmi_to_protocol_api;
+	break;
 
     default:
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 
     return FWK_SUCCESS;
@@ -711,14 +711,14 @@ static int scmi_reset_process_notificati
     struct fwk_event *resp_event)
 {
     struct mod_reset_domain_notification_event_params* params =
-        (struct mod_reset_domain_notification_event_params*)event->params;
+	(struct mod_reset_domain_notification_event_params*)event->params;
 
     if (!fwk_id_is_equal(scmi_rd_ctx.notification_id,
-                         event->id))
-        return FWK_E_SUPPORT;
+			 event->id))
+	return FWK_E_SUPPORT;
 
     scmi_reset_issued_notify(params->domain_id, params->reset_state,
-                             params->cookie);
+			     params->cookie);
 
     return FWK_SUCCESS;
 }
@@ -728,19 +728,19 @@ static int scmi_reset_start(fwk_id_t id)
     int status;
     struct mod_reset_domain_config *config;
     config = (struct mod_reset_domain_config*)fwk_module_get_data(
-        FWK_ID_MODULE(FWK_MODULE_IDX_RESET_DOMAIN)
-        );
+	FWK_ID_MODULE(FWK_MODULE_IDX_RESET_DOMAIN)
+	);
 
     scmi_rd_ctx.notification_id = config->notification_id;
 
     status = scmi_reset_init_notifications();
     if (status != FWK_SUCCESS)
-        return status;
+	return status;
 
     return fwk_notification_subscribe(
-        scmi_rd_ctx.notification_id,
-        FWK_ID_MODULE(FWK_MODULE_IDX_RESET_DOMAIN),
-        id);
+	scmi_rd_ctx.notification_id,
+	FWK_ID_MODULE(FWK_MODULE_IDX_RESET_DOMAIN),
+	id);
 }
 #endif
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_sensor/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi_sensor/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_sensor/CMakeLists.txt
@@ -8,14 +8,14 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_scmi_sensor.c"
-                       "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_scmi_ext_attrib.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_scmi_sensor.c"
+		       "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_scmi_ext_attrib.c")
 
 target_link_libraries(${SCP_MODULE_TARGET} PRIVATE module-scmi module-sensor
-                                                   module-timer)
+						   module-timer)
 
 if("resource-perms" IN_LIST SCP_MODULES)
     target_link_libraries(${SCP_MODULE_TARGET} PRIVATE module-resource-perms)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_sensor/include/internal/scmi_sensor.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi_sensor/include/internal/scmi_sensor.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_sensor/include/internal/scmi_sensor.h
@@ -113,10 +113,10 @@ struct scmi_sensor_trip_point_event_p2a
 
  #define SCMI_SENSOR_DESCS_MAX(MAILBOX_SIZE) \
     ((sizeof(struct scmi_sensor_protocol_description_get_p2a) < MAILBOX_SIZE) \
-        ? ((MAILBOX_SIZE - \
-            sizeof(struct scmi_sensor_protocol_description_get_p2a)) \
-                / sizeof(struct scmi_sensor_desc)) \
-        : 0)
+	? ((MAILBOX_SIZE - \
+	    sizeof(struct scmi_sensor_protocol_description_get_p2a)) \
+		/ sizeof(struct scmi_sensor_desc)) \
+	: 0)
 
 /*
  * SENSOR_AXIS_DESCRIPTION_GET
@@ -124,10 +124,10 @@ struct scmi_sensor_trip_point_event_p2a
 
 #define SCMI_SENSOR_AXIS_DESCS_MAX(MAILBOX_SIZE) \
     ((sizeof(struct scmi_sensor_axis_description_get_p2a) < MAILBOX_SIZE) ? \
-         ((MAILBOX_SIZE - \
-           sizeof(struct scmi_sensor_axis_description_get_p2a)) / \
-          sizeof(struct scmi_sensor_axis_desc)) : \
-         0)
+	 ((MAILBOX_SIZE - \
+	   sizeof(struct scmi_sensor_axis_description_get_p2a)) / \
+	  sizeof(struct scmi_sensor_axis_desc)) : \
+	 0)
 
 /*
  * SENSOR_READ_GET_AXIS_VALUE
@@ -135,10 +135,10 @@ struct scmi_sensor_trip_point_event_p2a
 
 #define SCMI_SENSOR_READ_GET_VALUES_MAX(MAILBOX_SIZE) \
     ((sizeof(struct scmi_sensor_protocol_reading_get_p2a) < MAILBOX_SIZE) ? \
-         ((MAILBOX_SIZE - \
-           sizeof(struct scmi_sensor_protocol_reading_get_p2a)) / \
-          sizeof(struct scmi_sensor_protocol_reading_get_data)) : \
-         0)
+	 ((MAILBOX_SIZE - \
+	   sizeof(struct scmi_sensor_protocol_reading_get_p2a)) / \
+	  sizeof(struct scmi_sensor_protocol_reading_get_data)) : \
+	 0)
 
 #define SCMI_SENSOR_DESC_ATTRS_LOW_SENSOR_ASYNC_READING_POS 31
 #define SCMI_SENSOR_DESC_ATTRS_LOW_SENSOR_EXTENDED_ATTRIBS_POS 8
@@ -153,26 +153,26 @@ struct scmi_sensor_trip_point_event_p2a
 
 #ifdef BUILD_HAS_SCMI_SENSOR_V2
 #    define SCMI_SENSOR_DESC_ATTRIBUTES_LOW( \
-        ASYNC_READING, EXTENDED_ATTRIBUTES, NUM_TRIP_POINTS) \
-        ( \
-            (((ASYNC_READING) \
-              << SCMI_SENSOR_DESC_ATTRS_LOW_SENSOR_ASYNC_READING_POS) & \
-             SCMI_SENSOR_DESC_ATTRS_LOW_SENSOR_ASYNC_READING_MASK) | \
-            (((EXTENDED_ATTRIBUTES) \
-              << SCMI_SENSOR_DESC_ATTRS_LOW_SENSOR_EXTENDED_ATTRIBS_POS) & \
-             SCMI_SENSOR_DESC_ATTRS_LOW_SENSOR_EXTENDED_ATTRIBS_MASK) | \
-            (((NUM_TRIP_POINTS) \
-              << SCMI_SENSOR_DESC_ATTRS_LOW_SENSOR_NUM_TRIP_POINTS_POS) & \
-             SCMI_SENSOR_DESC_ATTRS_LOW_SENSOR_NUM_TRIP_POINTS_MASK))
+	ASYNC_READING, EXTENDED_ATTRIBUTES, NUM_TRIP_POINTS) \
+	( \
+	    (((ASYNC_READING) \
+	      << SCMI_SENSOR_DESC_ATTRS_LOW_SENSOR_ASYNC_READING_POS) & \
+	     SCMI_SENSOR_DESC_ATTRS_LOW_SENSOR_ASYNC_READING_MASK) | \
+	    (((EXTENDED_ATTRIBUTES) \
+	      << SCMI_SENSOR_DESC_ATTRS_LOW_SENSOR_EXTENDED_ATTRIBS_POS) & \
+	     SCMI_SENSOR_DESC_ATTRS_LOW_SENSOR_EXTENDED_ATTRIBS_MASK) | \
+	    (((NUM_TRIP_POINTS) \
+	      << SCMI_SENSOR_DESC_ATTRS_LOW_SENSOR_NUM_TRIP_POINTS_POS) & \
+	     SCMI_SENSOR_DESC_ATTRS_LOW_SENSOR_NUM_TRIP_POINTS_MASK))
 #else
 #    define SCMI_SENSOR_DESC_ATTRIBUTES_LOW(ASYNC_READING, NUM_TRIP_POINTS) \
-        ( \
-            (((ASYNC_READING) \
-              << SCMI_SENSOR_DESC_ATTRS_LOW_SENSOR_ASYNC_READING_POS) & \
-             SCMI_SENSOR_DESC_ATTRS_LOW_SENSOR_ASYNC_READING_MASK) | \
-            (((NUM_TRIP_POINTS) \
-              << SCMI_SENSOR_DESC_ATTRS_LOW_SENSOR_NUM_TRIP_POINTS_POS) & \
-             SCMI_SENSOR_DESC_ATTRS_LOW_SENSOR_NUM_TRIP_POINTS_MASK))
+	( \
+	    (((ASYNC_READING) \
+	      << SCMI_SENSOR_DESC_ATTRS_LOW_SENSOR_ASYNC_READING_POS) & \
+	     SCMI_SENSOR_DESC_ATTRS_LOW_SENSOR_ASYNC_READING_MASK) | \
+	    (((NUM_TRIP_POINTS) \
+	      << SCMI_SENSOR_DESC_ATTRS_LOW_SENSOR_NUM_TRIP_POINTS_POS) & \
+	     SCMI_SENSOR_DESC_ATTRS_LOW_SENSOR_NUM_TRIP_POINTS_MASK))
 #endif
 
 #define SCMI_SENSOR_DESC_ATTRS_HIGH_SENSOR_TYPE_POS              0U
@@ -186,7 +186,7 @@ struct scmi_sensor_trip_point_event_p2a
     (UINT32_C(0x1F) << SCMI_SENSOR_DESC_ATTRS_HIGH_SENSOR_UNIT_MULTIPLIER_POS)
 #define SCMI_SENSOR_DESC_ATTRS_HIGH_SENSOR_UPDATE_MULTIPLIER_MASK \
     (UINT32_C(0x1F) \
-        << SCMI_SENSOR_DESC_ATTRS_HIGH_SENSOR_UPDATE_MULTIPLIER_POS)
+	<< SCMI_SENSOR_DESC_ATTRS_HIGH_SENSOR_UPDATE_MULTIPLIER_POS)
 #define SCMI_SENSOR_DESC_ATTRS_HIGH_SENSOR_UPDATE_INTERVAL_MASK \
     (UINT32_C(0x1F) << SCMI_SENSOR_DESC_ATTRS_HIGH_SENSOR_UPDATE_INTERVAL_POS)
 
@@ -225,12 +225,12 @@ struct scmi_sensor_trip_point_event_p2a
 
 #define SCMI_SENSOR_NUM_SENSOR_FLAGS(NUM_DESCS, NUM_REMAINING_DESCS) \
     ( \
-        (((NUM_DESCS) << \
-            SCMI_SENSOR_NUM_SENSOR_FLAGS_NUM_DESCS_POS) & \
-            SCMI_SENSOR_NUM_SENSOR_FLAGS_NUM_DESCS_MASK) | \
-        (((NUM_REMAINING_DESCS) << \
-            SCMI_SENSOR_NUM_SENSOR_FLAGS_NUM_REMAINING_DESCS_POS) & \
-            SCMI_SENSOR_NUM_SENSOR_FLAGS_NUM_REMAINING_DESCS_MASK) \
+	(((NUM_DESCS) << \
+	    SCMI_SENSOR_NUM_SENSOR_FLAGS_NUM_DESCS_POS) & \
+	    SCMI_SENSOR_NUM_SENSOR_FLAGS_NUM_DESCS_MASK) | \
+	(((NUM_REMAINING_DESCS) << \
+	    SCMI_SENSOR_NUM_SENSOR_FLAGS_NUM_REMAINING_DESCS_POS) & \
+	    SCMI_SENSOR_NUM_SENSOR_FLAGS_NUM_REMAINING_DESCS_MASK) \
     )
 
 #define SCMI_SENSOR_CONFIG_FLAGS_EVENT_CONTROL_POS 0
@@ -270,9 +270,9 @@ struct scmi_sensor_trip_point_event_p2a
 
 #ifdef BUILD_HAS_SCMI_SENSOR_V2
 #    define SCMI_SENSOR_AXIS_DESC_ATTRIBUTES_LOW(EXTENDED_ATTRIBUTES) \
-        (((EXTENDED_ATTRIBUTES) \
-          << SCMI_SENSOR_DESC_ATTRS_LOW_SENSOR_EXTENDED_ATTRIBS_POS) & \
-         SCMI_SENSOR_DESC_ATTRS_LOW_SENSOR_EXTENDED_ATTRIBS_MASK)
+	(((EXTENDED_ATTRIBUTES) \
+	  << SCMI_SENSOR_DESC_ATTRS_LOW_SENSOR_EXTENDED_ATTRIBS_POS) & \
+	 SCMI_SENSOR_DESC_ATTRS_LOW_SENSOR_EXTENDED_ATTRIBS_MASK)
 #endif
 
 #define SCMI_SENSOR_AXIS_DESC_ATTRIBUTES_HIGH(SENSOR_TYPE, UNIT_MULTIPLIER) \
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_sensor/include/mod_scmi_sensor.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi_sensor/include/mod_scmi_sensor.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_sensor/include/mod_scmi_sensor.h
@@ -42,9 +42,9 @@ struct mod_sensor_trip_point_api {
      * \param trip_point_id trip point index.
      */
     void (*notify_sensor_trip_point)(
-        fwk_id_t sensor_id,
-        uint32_t state,
-        uint32_t trip_point_idx);
+	fwk_id_t sensor_id,
+	uint32_t state,
+	uint32_t trip_point_idx);
 };
 
 /*!
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_sensor/src/mod_scmi_ext_attrib.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi_sensor/src/mod_scmi_ext_attrib.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_sensor/src/mod_scmi_ext_attrib.c
@@ -26,33 +26,33 @@ void scmi_sensor_prop_set(
     struct scmi_sensor_desc *desc)
 {
     if (info->hal_info.ext_attributes && !info->multi_axis.support) {
-        desc->sensor_power = info->hal_info.sensor_properties.sensor_power;
-        desc->sensor_resolution = info->hal_info.sensor_properties
-                                      .sensor_property_vals.axis_resolution;
-        desc->sensor_min_range_low = (int32_t)(
-            info->hal_info.sensor_properties.sensor_property_vals
-                .axis_min_range &
-            0xffffffff);
-        desc->sensor_min_range_high = (int32_t)(
-            info->hal_info.sensor_properties.sensor_property_vals
-                .axis_min_range >>
-            32);
-        desc->sensor_max_range_low = (int32_t)(
-            info->hal_info.sensor_properties.sensor_property_vals
-                .axis_max_range &
-            0xffffffff);
-        desc->sensor_min_range_high = (int32_t)(
-            info->hal_info.sensor_properties.sensor_property_vals
-                .axis_max_range >>
-            32);
+	desc->sensor_power = info->hal_info.sensor_properties.sensor_power;
+	desc->sensor_resolution = info->hal_info.sensor_properties
+				      .sensor_property_vals.axis_resolution;
+	desc->sensor_min_range_low = (int32_t)(
+	    info->hal_info.sensor_properties.sensor_property_vals
+		.axis_min_range &
+	    0xffffffff);
+	desc->sensor_min_range_high = (int32_t)(
+	    info->hal_info.sensor_properties.sensor_property_vals
+		.axis_min_range >>
+	    32);
+	desc->sensor_max_range_low = (int32_t)(
+	    info->hal_info.sensor_properties.sensor_property_vals
+		.axis_max_range &
+	    0xffffffff);
+	desc->sensor_min_range_high = (int32_t)(
+	    info->hal_info.sensor_properties.sensor_property_vals
+		.axis_max_range >>
+	    32);
     } else {
-        /* Populate with the default values from the SCMI specification V3.0 */
-        desc->sensor_power = SCMI_SENSOR_EXT_ATTR_POWER;
-        desc->sensor_resolution = SCMI_SENSOR_EXT_ATTR_RESOLUTION_VAL;
-        desc->sensor_min_range_low = SCMI_SENSOR_EXT_ATTR_MIN_RANGE_LOW;
-        desc->sensor_min_range_high = SCMI_SENSOR_EXT_ATTR_MIN_RANGE_HIGH;
-        desc->sensor_max_range_low = SCMI_SENSOR_EXT_ATTR_MAX_RANGE_LOW;
-        desc->sensor_max_range_high = SCMI_SENSOR_EXT_ATTR_MAX_RANGE_HIGH;
+	/* Populate with the default values from the SCMI specification V3.0 */
+	desc->sensor_power = SCMI_SENSOR_EXT_ATTR_POWER;
+	desc->sensor_resolution = SCMI_SENSOR_EXT_ATTR_RESOLUTION_VAL;
+	desc->sensor_min_range_low = SCMI_SENSOR_EXT_ATTR_MIN_RANGE_LOW;
+	desc->sensor_min_range_high = SCMI_SENSOR_EXT_ATTR_MIN_RANGE_HIGH;
+	desc->sensor_max_range_low = SCMI_SENSOR_EXT_ATTR_MAX_RANGE_LOW;
+	desc->sensor_max_range_high = SCMI_SENSOR_EXT_ATTR_MAX_RANGE_HIGH;
     }
 }
 
@@ -61,23 +61,23 @@ void scmi_sensor_axis_prop_set(
     struct scmi_sensor_axis_desc *desc)
 {
     if (axis_values->extended_attribs) {
-        desc->axis_resolution =
-            axis_values->multi_axis_properties.axis_resolution;
-        desc->axis_min_range_low = (int32_t)(
-            axis_values->multi_axis_properties.axis_min_range & 0xffffffff);
-        desc->axis_min_range_high =
-            (int32_t)(axis_values->multi_axis_properties.axis_min_range >> 32);
-        desc->axis_max_range_low = (int32_t)(
-            axis_values->multi_axis_properties.axis_max_range & 0xffffffff);
-        desc->axis_max_range_high =
-            (int32_t)(axis_values->multi_axis_properties.axis_max_range >> 32);
+	desc->axis_resolution =
+	    axis_values->multi_axis_properties.axis_resolution;
+	desc->axis_min_range_low = (int32_t)(
+	    axis_values->multi_axis_properties.axis_min_range & 0xffffffff);
+	desc->axis_min_range_high =
+	    (int32_t)(axis_values->multi_axis_properties.axis_min_range >> 32);
+	desc->axis_max_range_low = (int32_t)(
+	    axis_values->multi_axis_properties.axis_max_range & 0xffffffff);
+	desc->axis_max_range_high =
+	    (int32_t)(axis_values->multi_axis_properties.axis_max_range >> 32);
     } else {
-        /* Populate with the default values from the SCMI specification V3.0 */
-        desc->axis_resolution = SCMI_SENSOR_EXT_ATTR_RESOLUTION_VAL;
-        desc->axis_min_range_low = SCMI_SENSOR_EXT_ATTR_MIN_RANGE_LOW;
-        desc->axis_min_range_high = SCMI_SENSOR_EXT_ATTR_MIN_RANGE_HIGH;
-        desc->axis_max_range_low = SCMI_SENSOR_EXT_ATTR_MAX_RANGE_LOW;
-        desc->axis_max_range_high = SCMI_SENSOR_EXT_ATTR_MAX_RANGE_HIGH;
+	/* Populate with the default values from the SCMI specification V3.0 */
+	desc->axis_resolution = SCMI_SENSOR_EXT_ATTR_RESOLUTION_VAL;
+	desc->axis_min_range_low = SCMI_SENSOR_EXT_ATTR_MIN_RANGE_LOW;
+	desc->axis_min_range_high = SCMI_SENSOR_EXT_ATTR_MIN_RANGE_HIGH;
+	desc->axis_max_range_low = SCMI_SENSOR_EXT_ATTR_MAX_RANGE_LOW;
+	desc->axis_max_range_high = SCMI_SENSOR_EXT_ATTR_MAX_RANGE_HIGH;
     }
 }
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_sensor/src/mod_scmi_sensor.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi_sensor/src/mod_scmi_sensor.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_sensor/src/mod_scmi_sensor.c
@@ -103,7 +103,7 @@ struct scmi_sensor_event_parameters {
 
 static const fwk_id_t mod_scmi_sensor_event_id_get_request =
     FWK_ID_EVENT_INIT(FWK_MODULE_IDX_SCMI_SENSOR,
-                      SCMI_SENSOR_EVENT_IDX_REQUEST);
+		      SCMI_SENSOR_EVENT_IDX_REQUEST);
 
 /*
  * Internal variables.
@@ -116,7 +116,7 @@ static int (*handler_table[MOD_SCMI_SENS
     [MOD_SCMI_PROTOCOL_VERSION] = scmi_sensor_protocol_version_handler,
     [MOD_SCMI_PROTOCOL_ATTRIBUTES] = scmi_sensor_protocol_attributes_handler,
     [MOD_SCMI_PROTOCOL_MESSAGE_ATTRIBUTES] =
-        scmi_sensor_protocol_msg_attributes_handler,
+	scmi_sensor_protocol_msg_attributes_handler,
     [MOD_SCMI_SENSOR_DESCRIPTION_GET] = scmi_sensor_protocol_desc_get_handler,
 #ifdef BUILD_HAS_SCMI_SENSOR_EVENTS
     [MOD_SCMI_SENSOR_TRIP_POINT_NOTIFY] = scmi_sensor_trip_point_notify_handler,
@@ -132,21 +132,21 @@ static unsigned int payload_size_table[M
     [MOD_SCMI_PROTOCOL_VERSION] = 0,
     [MOD_SCMI_PROTOCOL_ATTRIBUTES] = 0,
     [MOD_SCMI_PROTOCOL_MESSAGE_ATTRIBUTES] =
-        (unsigned int)sizeof(struct scmi_protocol_message_attributes_a2p),
+	(unsigned int)sizeof(struct scmi_protocol_message_attributes_a2p),
     [MOD_SCMI_SENSOR_DESCRIPTION_GET] =
-        (unsigned int)sizeof(struct scmi_sensor_protocol_description_get_a2p),
+	(unsigned int)sizeof(struct scmi_sensor_protocol_description_get_a2p),
 #ifdef BUILD_HAS_SCMI_SENSOR_EVENTS
     [MOD_SCMI_SENSOR_TRIP_POINT_NOTIFY] =
-        sizeof(struct scmi_sensor_trip_point_notify_a2p),
+	sizeof(struct scmi_sensor_trip_point_notify_a2p),
     [MOD_SCMI_SENSOR_TRIP_POINT_CONFIG] =
-        sizeof(struct scmi_sensor_trip_point_config_a2p),
+	sizeof(struct scmi_sensor_trip_point_config_a2p),
 #endif
 #ifdef BUILD_HAS_SCMI_SENSOR_V2
     [MOD_SCMI_SENSOR_AXIS_DESCRIPTION_GET] =
-        (unsigned int)sizeof(struct scmi_sensor_axis_description_get_a2p),
+	(unsigned int)sizeof(struct scmi_sensor_axis_description_get_a2p),
 #endif
     [MOD_SCMI_SENSOR_READING_GET] =
-        (unsigned int)sizeof(struct scmi_sensor_protocol_reading_get_a2p),
+	(unsigned int)sizeof(struct scmi_sensor_protocol_reading_get_a2p),
 };
 
 /*
@@ -176,70 +176,70 @@ static int scmi_sensor_reading_respond(
     service_id = scmi_sensor_ctx.sensor_ops_table[sensor_idx].service_id;
 
     if (sensor_data->status != FWK_SUCCESS) {
-        return_values.status = (int32_t)SCMI_HARDWARE_ERROR;
-        goto exit_error;
+	return_values.status = (int32_t)SCMI_HARDWARE_ERROR;
+	goto exit_error;
     }
 
 #ifdef BUILD_HAS_SCMI_SENSOR_V2
     payload_size = sizeof(return_values);
     status = scmi_sensor_ctx.scmi_api->get_max_payload_size(
-        service_id, &max_payload_size);
+	service_id, &max_payload_size);
     if (status != FWK_SUCCESS) {
-        goto exit_unexpected;
+	goto exit_unexpected;
     }
 
     values_max = SCMI_SENSOR_READ_GET_VALUES_MAX(max_payload_size);
     if (values_max == 0 || values_max < sensor_data->axis_count) {
-        /* Can't fit sensor axis value in the payload */
-        return_values.status = (int32_t)SCMI_GENERIC_ERROR;
-        goto exit_error;
+	/* Can't fit sensor axis value in the payload */
+	return_values.status = (int32_t)SCMI_GENERIC_ERROR;
+	goto exit_error;
     }
 
     for (axis_idx = 0; axis_idx < sensor_data->axis_count;
-         ++axis_idx, payload_size += sizeof(axis_value)) {
-        if (sensor_data->axis_count == 1) {
-            axis_value = (struct scmi_sensor_protocol_reading_get_data){
-                .sensor_value_low = (int32_t)sensor_data->value,
-                .sensor_value_high = (int32_t)(sensor_data->value >> 32),
-                .timestamp_low = (uint32_t)sensor_data->timestamp,
-                .timestamp_high = (uint32_t)(sensor_data->timestamp >> 32),
-            };
-        } else {
-            axis_value = (struct scmi_sensor_protocol_reading_get_data){
-                .sensor_value_low = (int32_t)sensor_data->axis_value[axis_idx],
-                .sensor_value_high =
-                    (int32_t)(sensor_data->axis_value[axis_idx] >> 32),
-                .timestamp_low = (uint32_t)sensor_data->timestamp,
-                .timestamp_high = (uint32_t)(sensor_data->timestamp >> 32),
-            };
-        }
-
-        status = scmi_sensor_ctx.scmi_api->write_payload(
-            service_id,
-            payload_size,
-            &axis_value,
-            sizeof(struct scmi_sensor_protocol_reading_get_data));
-        if (status != FWK_SUCCESS) {
-            /* Failed to write sensor axis value into message payload */
-            return_values.status = (int32_t)SCMI_GENERIC_ERROR;
-            goto exit_error;
-        }
+	 ++axis_idx, payload_size += sizeof(axis_value)) {
+	if (sensor_data->axis_count == 1) {
+	    axis_value = (struct scmi_sensor_protocol_reading_get_data){
+		.sensor_value_low = (int32_t)sensor_data->value,
+		.sensor_value_high = (int32_t)(sensor_data->value >> 32),
+		.timestamp_low = (uint32_t)sensor_data->timestamp,
+		.timestamp_high = (uint32_t)(sensor_data->timestamp >> 32),
+	    };
+	} else {
+	    axis_value = (struct scmi_sensor_protocol_reading_get_data){
+		.sensor_value_low = (int32_t)sensor_data->axis_value[axis_idx],
+		.sensor_value_high =
+		    (int32_t)(sensor_data->axis_value[axis_idx] >> 32),
+		.timestamp_low = (uint32_t)sensor_data->timestamp,
+		.timestamp_high = (uint32_t)(sensor_data->timestamp >> 32),
+	    };
+	}
+
+	status = scmi_sensor_ctx.scmi_api->write_payload(
+	    service_id,
+	    payload_size,
+	    &axis_value,
+	    sizeof(struct scmi_sensor_protocol_reading_get_data));
+	if (status != FWK_SUCCESS) {
+	    /* Failed to write sensor axis value into message payload */
+	    return_values.status = (int32_t)SCMI_GENERIC_ERROR;
+	    goto exit_error;
+	}
     }
     return_values.status = (int32_t)SCMI_SUCCESS;
 
     status = scmi_sensor_ctx.scmi_api->write_payload(
-        service_id, 0, &return_values, sizeof(return_values));
+	service_id, 0, &return_values, sizeof(return_values));
     if (status != FWK_SUCCESS) {
-        return_values.status = (int32_t)SCMI_GENERIC_ERROR;
-        goto exit_error;
+	return_values.status = (int32_t)SCMI_GENERIC_ERROR;
+	goto exit_error;
     }
     goto exit;
 
 #else
     return_values = (struct scmi_sensor_protocol_reading_get_p2a){
-        .status = SCMI_SUCCESS,
-        .sensor_value_low = (uint32_t)sensor_data->value,
-        .sensor_value_high = (uint32_t)(sensor_data->value >> 32)
+	.status = SCMI_SUCCESS,
+	.sensor_value_low = (uint32_t)sensor_data->value,
+	.sensor_value_high = (uint32_t)(sensor_data->value >> 32)
     };
     payload = &return_values;
     payload_size = sizeof(return_values);
@@ -257,9 +257,9 @@ exit_error:
     status = FWK_E_PANIC;
 exit:
     respond_status =
-        scmi_sensor_ctx.scmi_api->respond(service_id, payload, payload_size);
+	scmi_sensor_ctx.scmi_api->respond(service_id, payload, payload_size);
     if (respond_status != FWK_SUCCESS) {
-        FWK_LOG_DEBUG("[SCMI-SENS] %s @%d", __func__, __LINE__);
+	FWK_LOG_DEBUG("[SCMI-SENS] %s @%d", __func__, __LINE__);
     }
     /*
      * Set the service identifier to 'none' to indicate the sensor is
@@ -273,202 +273,202 @@ exit:
  * Sensor management protocol implementation
  */
 static int scmi_sensor_protocol_version_handler(fwk_id_t service_id,
-                                                const uint32_t *payload)
+						const uint32_t *payload)
 {
     struct scmi_protocol_version_p2a return_values = {
-        .status = (int32_t)SCMI_SUCCESS,
-        .version = SCMI_PROTOCOL_VERSION_SENSOR,
+	.status = (int32_t)SCMI_SUCCESS,
+	.version = SCMI_PROTOCOL_VERSION_SENSOR,
     };
 
     return scmi_sensor_ctx.scmi_api->respond(
-        service_id, &return_values, sizeof(return_values));
+	service_id, &return_values, sizeof(return_values));
 }
 
 static int scmi_sensor_protocol_attributes_handler(fwk_id_t service_id,
-                                                   const uint32_t *payload)
+						   const uint32_t *payload)
 {
     struct scmi_sensor_protocol_attributes_p2a return_values = {
-        .status = (int32_t)SCMI_SUCCESS,
-        .attributes = scmi_sensor_ctx.sensor_count,
-        .sensor_reg_len = 0, /* Unsupported */
+	.status = (int32_t)SCMI_SUCCESS,
+	.attributes = scmi_sensor_ctx.sensor_count,
+	.sensor_reg_len = 0, /* Unsupported */
     };
 
     return scmi_sensor_ctx.scmi_api->respond(
-        service_id, &return_values, sizeof(return_values));
+	service_id, &return_values, sizeof(return_values));
 }
 
 static int scmi_sensor_protocol_msg_attributes_handler(fwk_id_t service_id,
-                                                       const uint32_t *payload)
+						       const uint32_t *payload)
 {
     const struct scmi_protocol_message_attributes_a2p *parameters;
     struct scmi_protocol_message_attributes_p2a return_values;
 
     parameters = (const struct scmi_protocol_message_attributes_a2p *)
-                 payload;
+		 payload;
 
     if ((parameters->message_id < FWK_ARRAY_SIZE(handler_table)) &&
-        (handler_table[parameters->message_id] != NULL)) {
-        return_values = (struct scmi_protocol_message_attributes_p2a) {
-            .status = SCMI_SUCCESS,
-            /* All commands have an attributes value of 0 */
-            .attributes = 0,
-        };
+	(handler_table[parameters->message_id] != NULL)) {
+	return_values = (struct scmi_protocol_message_attributes_p2a) {
+	    .status = SCMI_SUCCESS,
+	    /* All commands have an attributes value of 0 */
+	    .attributes = 0,
+	};
     } else {
-        return_values.status = (int32_t)SCMI_NOT_FOUND;
+	return_values.status = (int32_t)SCMI_NOT_FOUND;
     }
 
     return scmi_sensor_ctx.scmi_api->respond(
-        service_id,
-        &return_values,
-        (return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
-                                                 sizeof(return_values.status));
+	service_id,
+	&return_values,
+	(return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
+						 sizeof(return_values.status));
 }
 
 static int scmi_sensor_protocol_desc_get_handler(fwk_id_t service_id,
-                                                 const uint32_t *payload)
+						 const uint32_t *payload)
 {
     int status, respond_status;
     size_t payload_size;
     size_t max_payload_size;
     const struct scmi_sensor_protocol_description_get_a2p *parameters =
-               (const struct scmi_sensor_protocol_description_get_a2p *)payload;
+	       (const struct scmi_sensor_protocol_description_get_a2p *)payload;
     struct scmi_sensor_desc desc = { 0 };
     unsigned int num_descs, desc_index, desc_index_max;
     struct mod_sensor_complete_info sensor_info;
     struct scmi_sensor_protocol_description_get_p2a return_values = {
-        .status = (int32_t)SCMI_GENERIC_ERROR,
+	.status = (int32_t)SCMI_GENERIC_ERROR,
     };
     fwk_id_t sensor_id;
 
     payload_size = sizeof(return_values);
 
     status = scmi_sensor_ctx.scmi_api->get_max_payload_size(service_id,
-                                                            &max_payload_size);
+							    &max_payload_size);
     if (status != FWK_SUCCESS) {
-        goto exit;
+	goto exit;
     }
 
     if (SCMI_SENSOR_DESCS_MAX(max_payload_size) == 0) {
-        /* Can't even fit one sensor description in the payload */
-        status = FWK_E_SIZE;
-        goto exit_unexpected;
+	/* Can't even fit one sensor description in the payload */
+	status = FWK_E_SIZE;
+	goto exit_unexpected;
     }
 
     parameters =
-        (const struct scmi_sensor_protocol_description_get_a2p *)payload;
+	(const struct scmi_sensor_protocol_description_get_a2p *)payload;
     desc_index = parameters->desc_index;
 
     if (desc_index >= scmi_sensor_ctx.sensor_count) {
-        return_values.status = (int32_t)SCMI_INVALID_PARAMETERS;
-        goto exit;
+	return_values.status = (int32_t)SCMI_INVALID_PARAMETERS;
+	goto exit;
     }
 
     num_descs = (unsigned int)FWK_MIN(
-        SCMI_SENSOR_DESCS_MAX(max_payload_size),
-        (scmi_sensor_ctx.sensor_count - desc_index));
+	SCMI_SENSOR_DESCS_MAX(max_payload_size),
+	(scmi_sensor_ctx.sensor_count - desc_index));
     desc_index_max = (desc_index + num_descs - 1);
 
     for (; desc_index <= desc_index_max; ++desc_index,
-         payload_size += sizeof(desc)) {
+	 payload_size += sizeof(desc)) {
 
-        desc = (struct scmi_sensor_desc) {
-            .sensor_id = desc_index,
-            .sensor_attributes_low = 0, /* None supported */
-        };
-
-        sensor_id = FWK_ID_ELEMENT(FWK_MODULE_IDX_SENSOR, desc_index);
-        if (!fwk_module_is_valid_element_id(sensor_id)) {
-            /* domain_idx did not map to a sensor device */
-            return_values.status = (int32_t)SCMI_NOT_FOUND;
-            goto exit_unexpected;
-        }
-
-        status = scmi_sensor_ctx.sensor_api->get_info(sensor_id, &sensor_info);
-        if (status != FWK_SUCCESS) {
-            /* Unable to get sensor info */
-            goto exit_unexpected;
-        }
-
-        if (sensor_info.hal_info.type >= MOD_SENSOR_TYPE_COUNT) {
-            /* Invalid sensor type */
-            goto exit_unexpected;
-        }
-
-        if ((sensor_info.hal_info.unit_multiplier <
-             SCMI_SENSOR_DESC_ATTRS_HIGH_SENSOR_UNIT_MULTIPLIER_MIN) ||
-            (sensor_info.hal_info.unit_multiplier >
-             SCMI_SENSOR_DESC_ATTRS_HIGH_SENSOR_UNIT_MULTIPLIER_MAX)) {
-            /* Sensor unit multiplier out of range */
-            goto exit_unexpected;
-        }
-
-        if ((sensor_info.hal_info.update_interval_multiplier <
-             SCMI_SENSOR_DESC_ATTRS_HIGH_SENSOR_UPDATE_MULTIPLIER_MIN) ||
-            (sensor_info.hal_info.update_interval_multiplier >
-             SCMI_SENSOR_DESC_ATTRS_HIGH_SENSOR_UPDATE_MULTIPLIER_MAX)) {
-            /* Sensor update interval multiplier is out of range */
-            goto exit_unexpected;
-        }
-
-        if (sensor_info.hal_info.update_interval >=
-            SCMI_SENSOR_DESC_ATTRS_HIGH_SENSOR_UPDATE_INTERVAL_MASK) {
-            /* Update interval is too big to fit in its mask */
-            goto exit_unexpected;
-        }
-        if (sensor_info.trip_point.count >=
-            SCMI_SENSOR_DESC_ATTRS_LOW_SENSOR_NUM_TRIP_POINTS_MASK) {
-            /* Number of trip points is too big to fit in its mask */
-            goto exit_unexpected;
-        }
+	desc = (struct scmi_sensor_desc) {
+	    .sensor_id = desc_index,
+	    .sensor_attributes_low = 0, /* None supported */
+	};
+
+	sensor_id = FWK_ID_ELEMENT(FWK_MODULE_IDX_SENSOR, desc_index);
+	if (!fwk_module_is_valid_element_id(sensor_id)) {
+	    /* domain_idx did not map to a sensor device */
+	    return_values.status = (int32_t)SCMI_NOT_FOUND;
+	    goto exit_unexpected;
+	}
+
+	status = scmi_sensor_ctx.sensor_api->get_info(sensor_id, &sensor_info);
+	if (status != FWK_SUCCESS) {
+	    /* Unable to get sensor info */
+	    goto exit_unexpected;
+	}
+
+	if (sensor_info.hal_info.type >= MOD_SENSOR_TYPE_COUNT) {
+	    /* Invalid sensor type */
+	    goto exit_unexpected;
+	}
+
+	if ((sensor_info.hal_info.unit_multiplier <
+	     SCMI_SENSOR_DESC_ATTRS_HIGH_SENSOR_UNIT_MULTIPLIER_MIN) ||
+	    (sensor_info.hal_info.unit_multiplier >
+	     SCMI_SENSOR_DESC_ATTRS_HIGH_SENSOR_UNIT_MULTIPLIER_MAX)) {
+	    /* Sensor unit multiplier out of range */
+	    goto exit_unexpected;
+	}
+
+	if ((sensor_info.hal_info.update_interval_multiplier <
+	     SCMI_SENSOR_DESC_ATTRS_HIGH_SENSOR_UPDATE_MULTIPLIER_MIN) ||
+	    (sensor_info.hal_info.update_interval_multiplier >
+	     SCMI_SENSOR_DESC_ATTRS_HIGH_SENSOR_UPDATE_MULTIPLIER_MAX)) {
+	    /* Sensor update interval multiplier is out of range */
+	    goto exit_unexpected;
+	}
+
+	if (sensor_info.hal_info.update_interval >=
+	    SCMI_SENSOR_DESC_ATTRS_HIGH_SENSOR_UPDATE_INTERVAL_MASK) {
+	    /* Update interval is too big to fit in its mask */
+	    goto exit_unexpected;
+	}
+	if (sensor_info.trip_point.count >=
+	    SCMI_SENSOR_DESC_ATTRS_LOW_SENSOR_NUM_TRIP_POINTS_MASK) {
+	    /* Number of trip points is too big to fit in its mask */
+	    goto exit_unexpected;
+	}
 
 #ifdef BUILD_HAS_SCMI_SENSOR_V2
-        desc.sensor_attributes_low = SCMI_SENSOR_DESC_ATTRIBUTES_LOW(
-            0U, /* Asyncronous reading not-supported */
-            sensor_info.hal_info.ext_attributes,
-            (uint32_t)sensor_info.trip_point.count);
+	desc.sensor_attributes_low = SCMI_SENSOR_DESC_ATTRIBUTES_LOW(
+	    0U, /* Asyncronous reading not-supported */
+	    sensor_info.hal_info.ext_attributes,
+	    (uint32_t)sensor_info.trip_point.count);
 #else
-        desc.sensor_attributes_low = SCMI_SENSOR_DESC_ATTRIBUTES_LOW(
-            0U, /* Asyncronous reading not-supported */
-            (uint32_t)sensor_info.trip_point.count);
+	desc.sensor_attributes_low = SCMI_SENSOR_DESC_ATTRIBUTES_LOW(
+	    0U, /* Asyncronous reading not-supported */
+	    (uint32_t)sensor_info.trip_point.count);
 #endif
 
-        desc.sensor_attributes_high = SCMI_SENSOR_DESC_ATTRIBUTES_HIGH(
-            sensor_info.hal_info.type,
-            sensor_info.hal_info.unit_multiplier,
-            (uint32_t)sensor_info.hal_info.update_interval_multiplier,
-            (uint32_t)sensor_info.hal_info.update_interval);
+	desc.sensor_attributes_high = SCMI_SENSOR_DESC_ATTRIBUTES_HIGH(
+	    sensor_info.hal_info.type,
+	    sensor_info.hal_info.unit_multiplier,
+	    (uint32_t)sensor_info.hal_info.update_interval_multiplier,
+	    (uint32_t)sensor_info.hal_info.update_interval);
 
 #ifdef BUILD_HAS_SCMI_SENSOR_V2
-        scmi_sensor_prop_set(&sensor_info, &desc);
+	scmi_sensor_prop_set(&sensor_info, &desc);
 #endif
 
-        /*
-         * Copy sensor name into description struct. Copy n-1 chars to ensure a
-         * NULL terminator at the end. (struct has been zeroed out)
-         */
-        fwk_str_strncpy(
-            desc.sensor_name,
-            fwk_module_get_element_name(sensor_id),
-            sizeof(desc.sensor_name) - 1);
-
-        status = scmi_sensor_ctx.scmi_api->write_payload(service_id,
-            payload_size, &desc, sizeof(struct scmi_sensor_desc));
-        if (status != FWK_SUCCESS) {
-            /* Failed to write sensor description into message payload */
-            goto exit_unexpected;
-        }
+	/*
+	 * Copy sensor name into description struct. Copy n-1 chars to ensure a
+	 * NULL terminator at the end. (struct has been zeroed out)
+	 */
+	fwk_str_strncpy(
+	    desc.sensor_name,
+	    fwk_module_get_element_name(sensor_id),
+	    sizeof(desc.sensor_name) - 1);
+
+	status = scmi_sensor_ctx.scmi_api->write_payload(service_id,
+	    payload_size, &desc, sizeof(struct scmi_sensor_desc));
+	if (status != FWK_SUCCESS) {
+	    /* Failed to write sensor description into message payload */
+	    goto exit_unexpected;
+	}
     }
 
     return_values = (struct scmi_sensor_protocol_description_get_p2a) {
-        .status = SCMI_SUCCESS,
-        .num_sensor_flags = SCMI_SENSOR_NUM_SENSOR_FLAGS(num_descs,
-            (scmi_sensor_ctx.sensor_count - desc_index_max - 1))
+	.status = SCMI_SUCCESS,
+	.num_sensor_flags = SCMI_SENSOR_NUM_SENSOR_FLAGS(num_descs,
+	    (scmi_sensor_ctx.sensor_count - desc_index_max - 1))
     };
 
     status = scmi_sensor_ctx.scmi_api->write_payload(service_id, 0,
-        &return_values, sizeof(return_values));
+	&return_values, sizeof(return_values));
     if (status != FWK_SUCCESS) {
-        return_values.status = (int32_t)SCMI_GENERIC_ERROR;
+	return_values.status = (int32_t)SCMI_GENERIC_ERROR;
     }
     goto exit;
 
@@ -476,13 +476,13 @@ exit_unexpected:
     fwk_unexpected();
 exit:
     respond_status = scmi_sensor_ctx.scmi_api->respond(
-        service_id,
-        (return_values.status == SCMI_SUCCESS) ? NULL : &return_values.status,
-        (return_values.status == SCMI_SUCCESS) ? payload_size :
-                                                 sizeof(return_values.status));
+	service_id,
+	(return_values.status == SCMI_SUCCESS) ? NULL : &return_values.status,
+	(return_values.status == SCMI_SUCCESS) ? payload_size :
+						 sizeof(return_values.status));
 
     if (respond_status != FWK_SUCCESS) {
-        FWK_LOG_DEBUG("[SCMI-SENS] %s @%d", __func__, __LINE__);
+	FWK_LOG_DEBUG("[SCMI-SENS] %s @%d", __func__, __LINE__);
     }
 
     return status;
@@ -498,48 +498,48 @@ static int scmi_sensor_trip_point_notify
     int status, respond_status;
     struct scmi_sensor_trip_point_notify_a2p *parameters;
     struct scmi_sensor_trip_point_notify_p2a return_values = {
-        .status = SCMI_GENERIC_ERROR,
+	.status = SCMI_GENERIC_ERROR,
     };
 
     parameters = (struct scmi_sensor_trip_point_notify_a2p *)payload;
 
     if (parameters->sensor_id >= scmi_sensor_ctx.sensor_count) {
-        /* Sensor does not exist */
-        status = FWK_SUCCESS;
-        return_values.status = SCMI_NOT_FOUND;
-        goto exit;
+	/* Sensor does not exist */
+	status = FWK_SUCCESS;
+	return_values.status = SCMI_NOT_FOUND;
+	goto exit;
     }
 
     status = scmi_sensor_ctx.scmi_api->get_agent_id(service_id, &agent_id);
     if (status != FWK_SUCCESS)
-        goto exit;
+	goto exit;
 
     flags = parameters->flags;
     if (flags & SCMI_SENSOR_CONFIG_FLAGS_EVENT_CONTROL_MASK)
-        scmi_sensor_ctx.scmi_notification_api->scmi_notification_add_subscriber(
-            MOD_SCMI_PROTOCOL_ID_SENSOR,
-            parameters->sensor_id,
-            MOD_SCMI_SENSOR_TRIP_POINT_NOTIFY,
-            service_id);
+	scmi_sensor_ctx.scmi_notification_api->scmi_notification_add_subscriber(
+	    MOD_SCMI_PROTOCOL_ID_SENSOR,
+	    parameters->sensor_id,
+	    MOD_SCMI_SENSOR_TRIP_POINT_NOTIFY,
+	    service_id);
     else
-        scmi_sensor_ctx.scmi_notification_api
-            ->scmi_notification_remove_subscriber(
-                MOD_SCMI_PROTOCOL_ID_SENSOR,
-                agent_id,
-                parameters->sensor_id,
-                MOD_SCMI_PERF_NOTIFY_LEVEL);
+	scmi_sensor_ctx.scmi_notification_api
+	    ->scmi_notification_remove_subscriber(
+		MOD_SCMI_PROTOCOL_ID_SENSOR,
+		agent_id,
+		parameters->sensor_id,
+		MOD_SCMI_PERF_NOTIFY_LEVEL);
 
     return_values.status = SCMI_SUCCESS;
     status = FWK_SUCCESS;
 
 exit:
     respond_status = scmi_sensor_ctx.scmi_api->respond(
-        service_id,
-        &return_values,
-        (return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
-                                                 sizeof(return_values.status));
+	service_id,
+	&return_values,
+	(return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
+						 sizeof(return_values.status));
     if (respond_status != FWK_SUCCESS) {
-        FWK_LOG_DEBUG("[SCMI-SENS] %s @%d", __func__, __LINE__);
+	FWK_LOG_DEBUG("[SCMI-SENS] %s @%d", __func__, __LINE__);
     }
 
     return status;
@@ -562,57 +562,57 @@ static int scmi_sensor_trip_point_config
     parameters = (struct scmi_sensor_trip_point_config_a2p *)payload;
 
     if (parameters->sensor_id >= scmi_sensor_ctx.sensor_count) {
-        /* Sensor does not exist */
-        status = FWK_SUCCESS;
-        return_values.status = SCMI_NOT_FOUND;
-        goto exit;
+	/* Sensor does not exist */
+	status = FWK_SUCCESS;
+	return_values.status = SCMI_NOT_FOUND;
+	goto exit;
     }
     if ((parameters->flags & SCMI_SENSOR_TRIP_POINT_FLAGS_RESERVED1_MASK) ||
-        (parameters->flags & SCMI_SENSOR_TRIP_POINT_FLAGS_RESERVED2_MASK)) {
-        status = FWK_SUCCESS;
-        return_values.status = SCMI_INVALID_PARAMETERS;
-        goto exit;
+	(parameters->flags & SCMI_SENSOR_TRIP_POINT_FLAGS_RESERVED2_MASK)) {
+	status = FWK_SUCCESS;
+	return_values.status = SCMI_INVALID_PARAMETERS;
+	goto exit;
     }
 
     sensor_id = FWK_ID_ELEMENT(FWK_MODULE_IDX_SENSOR, parameters->sensor_id);
     status = scmi_sensor_ctx.sensor_api->get_info(sensor_id, &sensor_info);
     if (status != FWK_SUCCESS) {
-        /* Unable to get sensor info */
-        fwk_unexpected();
-        goto exit;
+	/* Unable to get sensor info */
+	fwk_unexpected();
+	goto exit;
     }
     trip_point_idx =
-        (parameters->flags & SCMI_SENSOR_TRIP_POINT_FLAGS_ID_MASK) >>
-        SCMI_SENSOR_TRIP_POINT_FLAGS_ID_POS;
+	(parameters->flags & SCMI_SENSOR_TRIP_POINT_FLAGS_ID_MASK) >>
+	SCMI_SENSOR_TRIP_POINT_FLAGS_ID_POS;
 
     if (trip_point_idx >= sensor_info.trip_point.count) {
-        /* Sensor Trip point does not exist */
-        status = FWK_SUCCESS;
-        return_values.status = SCMI_INVALID_PARAMETERS;
-        goto exit;
+	/* Sensor Trip point does not exist */
+	status = FWK_SUCCESS;
+	return_values.status = SCMI_INVALID_PARAMETERS;
+	goto exit;
     }
 
     trip_point_param.tp_value =
-        (((uint64_t)parameters->sensor_value_high << 32) & ~0U) |
-        (((uint64_t)parameters->sensor_value_low) & ~0U);
+	(((uint64_t)parameters->sensor_value_high << 32) & ~0U) |
+	(((uint64_t)parameters->sensor_value_low) & ~0U);
 
     trip_point_param.mode =
-        (parameters->flags & SCMI_SENSOR_TRIP_POINT_FLAGS_EV_CTRL_MASK) >>
-        SCMI_SENSOR_TRIP_POINT_FLAGS_EV_CTRL_POS;
+	(parameters->flags & SCMI_SENSOR_TRIP_POINT_FLAGS_EV_CTRL_MASK) >>
+	SCMI_SENSOR_TRIP_POINT_FLAGS_EV_CTRL_POS;
 
     scmi_sensor_ctx.sensor_api->set_trip_point(
-        sensor_id, trip_point_idx, &trip_point_param);
+	sensor_id, trip_point_idx, &trip_point_param);
     return_values.status = SCMI_SUCCESS;
     status = FWK_SUCCESS;
 
 exit:
     respond_status = scmi_sensor_ctx.scmi_api->respond(
-        service_id,
-        &return_values,
-        (return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
-                                                 sizeof(return_values.status));
+	service_id,
+	&return_values,
+	(return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
+						 sizeof(return_values.status));
     if (respond_status != FWK_SUCCESS) {
-        FWK_LOG_DEBUG("[SCMI-SENS] %s @%d", __func__, __LINE__);
+	FWK_LOG_DEBUG("[SCMI-SENS] %s @%d", __func__, __LINE__);
     }
 
     return status;
@@ -628,129 +628,129 @@ static int scmi_sensor_axis_desc_get_han
     size_t payload_size;
     size_t max_payload_size;
     const struct scmi_sensor_axis_description_get_a2p *parameters =
-        (const struct scmi_sensor_axis_description_get_a2p *)payload;
+	(const struct scmi_sensor_axis_description_get_a2p *)payload;
     struct scmi_sensor_axis_desc desc = { 0 };
     unsigned int num_descs, desc_index, desc_index_max;
     struct mod_sensor_axis_info axis_info;
     struct mod_sensor_complete_info sensor_info;
     struct scmi_sensor_axis_description_get_p2a return_values = {
-        .status = (int32_t)SCMI_GENERIC_ERROR,
+	.status = (int32_t)SCMI_GENERIC_ERROR,
     };
     fwk_id_t sensor_id;
 
     payload_size = sizeof(return_values);
 
     status = scmi_sensor_ctx.scmi_api->get_max_payload_size(
-        service_id, &max_payload_size);
+	service_id, &max_payload_size);
     if (status != FWK_SUCCESS) {
-        return_values.status = (int32_t)SCMI_GENERIC_ERROR;
-        goto exit;
+	return_values.status = (int32_t)SCMI_GENERIC_ERROR;
+	goto exit;
     }
 
     if (SCMI_SENSOR_AXIS_DESCS_MAX(max_payload_size) == 0) {
-        /* Can't even fit one sensor axis description in the payload */
-        status = FWK_E_SIZE;
-        goto exit_unexpected;
+	/* Can't even fit one sensor axis description in the payload */
+	status = FWK_E_SIZE;
+	goto exit_unexpected;
     }
 
     parameters = (const struct scmi_sensor_axis_description_get_a2p *)payload;
 
     sensor_id = FWK_ID_ELEMENT(FWK_MODULE_IDX_SENSOR, parameters->sensor_idx);
     if (!fwk_module_is_valid_element_id(sensor_id)) {
-        /* domain_idx did not map to a sensor device */
-        return_values.status = (int32_t)SCMI_NOT_FOUND;
-        goto exit;
+	/* domain_idx did not map to a sensor device */
+	return_values.status = (int32_t)SCMI_NOT_FOUND;
+	goto exit;
     }
 
     status = scmi_sensor_ctx.sensor_api->get_info(sensor_id, &sensor_info);
     if (status != FWK_SUCCESS) {
-        /* Unable to get sensor info */
-        return_values.status = (int32_t)SCMI_GENERIC_ERROR;
-        goto exit;
+	/* Unable to get sensor info */
+	return_values.status = (int32_t)SCMI_GENERIC_ERROR;
+	goto exit;
     }
 
     desc_index = parameters->axis_desc_index;
 
     if (!sensor_info.multi_axis.support) {
-        return_values.status = (int32_t)SCMI_NOT_SUPPORTED;
-        goto exit;
+	return_values.status = (int32_t)SCMI_NOT_SUPPORTED;
+	goto exit;
     }
 
     if (desc_index >= sensor_info.multi_axis.axis_count) {
-        return_values.status = (int32_t)SCMI_INVALID_PARAMETERS;
-        goto exit;
+	return_values.status = (int32_t)SCMI_INVALID_PARAMETERS;
+	goto exit;
     }
 
     num_descs = (unsigned int)FWK_MIN(
-        SCMI_SENSOR_AXIS_DESCS_MAX(max_payload_size),
-        (sensor_info.multi_axis.axis_count - desc_index));
+	SCMI_SENSOR_AXIS_DESCS_MAX(max_payload_size),
+	(sensor_info.multi_axis.axis_count - desc_index));
     desc_index_max = (desc_index + num_descs - 1);
 
     for (; desc_index <= desc_index_max;
-         ++desc_index, payload_size += sizeof(desc)) {
-        status = scmi_sensor_ctx.sensor_api->get_axis_info(
-            sensor_id, desc_index, &axis_info);
-        if (status != FWK_SUCCESS) {
-            /* Unable to get sensor info */
-            return_values.status = (int32_t)SCMI_GENERIC_ERROR;
-            goto exit;
-        }
-
-        desc = (struct scmi_sensor_axis_desc){
-            .axis_idx = desc_index,
-            .axis_attributes_low =
-                SCMI_SENSOR_AXIS_DESC_ATTRIBUTES_LOW(axis_info.extended_attribs)
-        };
-
-        if (axis_info.type >= MOD_SENSOR_TYPE_COUNT) {
-            /* Invalid sensor type */
-            return_values.status = (int32_t)SCMI_NOT_SUPPORTED;
-            goto exit;
-        }
-
-        if ((axis_info.unit_multiplier <
-             SCMI_SENSOR_DESC_ATTRS_HIGH_SENSOR_UNIT_MULTIPLIER_MIN) ||
-            (axis_info.unit_multiplier >
-             SCMI_SENSOR_DESC_ATTRS_HIGH_SENSOR_UNIT_MULTIPLIER_MAX)) {
-            /* Sensor unit multiplier out of range */
-            return_values.status = (int32_t)SCMI_INVALID_PARAMETERS;
-            goto exit;
-        }
-
-        desc.axis_attributes_high = SCMI_SENSOR_AXIS_DESC_ATTRIBUTES_HIGH(
-            axis_info.type, axis_info.unit_multiplier);
-
-        scmi_sensor_axis_prop_set(&axis_info, &desc);
-
-        /*
-         * Copy sensor name into description struct. Copy n-1 chars to ensure a
-         * NULL terminator at the end. (struct has been zeroed out)
-         */
-        fwk_str_strncpy(
-            desc.axis_name, axis_info.name, SCMI_SENSOR_AXIS_NAME_LEN - 1);
-
-        status = scmi_sensor_ctx.scmi_api->write_payload(
-            service_id,
-            payload_size,
-            &desc,
-            sizeof(struct scmi_sensor_axis_desc));
-        if (status != FWK_SUCCESS) {
-            /* Failed to write sensor description into message payload */
-            return_values.status = (int32_t)SCMI_GENERIC_ERROR;
-            goto exit;
-        }
+	 ++desc_index, payload_size += sizeof(desc)) {
+	status = scmi_sensor_ctx.sensor_api->get_axis_info(
+	    sensor_id, desc_index, &axis_info);
+	if (status != FWK_SUCCESS) {
+	    /* Unable to get sensor info */
+	    return_values.status = (int32_t)SCMI_GENERIC_ERROR;
+	    goto exit;
+	}
+
+	desc = (struct scmi_sensor_axis_desc){
+	    .axis_idx = desc_index,
+	    .axis_attributes_low =
+		SCMI_SENSOR_AXIS_DESC_ATTRIBUTES_LOW(axis_info.extended_attribs)
+	};
+
+	if (axis_info.type >= MOD_SENSOR_TYPE_COUNT) {
+	    /* Invalid sensor type */
+	    return_values.status = (int32_t)SCMI_NOT_SUPPORTED;
+	    goto exit;
+	}
+
+	if ((axis_info.unit_multiplier <
+	     SCMI_SENSOR_DESC_ATTRS_HIGH_SENSOR_UNIT_MULTIPLIER_MIN) ||
+	    (axis_info.unit_multiplier >
+	     SCMI_SENSOR_DESC_ATTRS_HIGH_SENSOR_UNIT_MULTIPLIER_MAX)) {
+	    /* Sensor unit multiplier out of range */
+	    return_values.status = (int32_t)SCMI_INVALID_PARAMETERS;
+	    goto exit;
+	}
+
+	desc.axis_attributes_high = SCMI_SENSOR_AXIS_DESC_ATTRIBUTES_HIGH(
+	    axis_info.type, axis_info.unit_multiplier);
+
+	scmi_sensor_axis_prop_set(&axis_info, &desc);
+
+	/*
+	 * Copy sensor name into description struct. Copy n-1 chars to ensure a
+	 * NULL terminator at the end. (struct has been zeroed out)
+	 */
+	fwk_str_strncpy(
+	    desc.axis_name, axis_info.name, SCMI_SENSOR_AXIS_NAME_LEN - 1);
+
+	status = scmi_sensor_ctx.scmi_api->write_payload(
+	    service_id,
+	    payload_size,
+	    &desc,
+	    sizeof(struct scmi_sensor_axis_desc));
+	if (status != FWK_SUCCESS) {
+	    /* Failed to write sensor description into message payload */
+	    return_values.status = (int32_t)SCMI_GENERIC_ERROR;
+	    goto exit;
+	}
     }
 
     return_values = (struct scmi_sensor_axis_description_get_p2a){
-        .status = SCMI_SUCCESS,
-        .num_axis_flags = SCMI_SENSOR_NUM_SENSOR_FLAGS(
-            num_descs, (sensor_info.multi_axis.axis_count - desc_index_max - 1))
+	.status = SCMI_SUCCESS,
+	.num_axis_flags = SCMI_SENSOR_NUM_SENSOR_FLAGS(
+	    num_descs, (sensor_info.multi_axis.axis_count - desc_index_max - 1))
     };
 
     status = scmi_sensor_ctx.scmi_api->write_payload(
-        service_id, 0, &return_values, sizeof(return_values));
+	service_id, 0, &return_values, sizeof(return_values));
     if (status != FWK_SUCCESS) {
-        return_values.status = (int32_t)SCMI_GENERIC_ERROR;
+	return_values.status = (int32_t)SCMI_GENERIC_ERROR;
     }
     goto exit;
 
@@ -758,12 +758,12 @@ exit_unexpected:
     fwk_unexpected();
 exit:
     respond_status = scmi_sensor_ctx.scmi_api->respond(
-        service_id,
-        (return_values.status == SCMI_SUCCESS) ? NULL : &return_values.status,
-        (return_values.status == SCMI_SUCCESS) ? payload_size :
-                                                 sizeof(return_values.status));
+	service_id,
+	(return_values.status == SCMI_SUCCESS) ? NULL : &return_values.status,
+	(return_values.status == SCMI_SUCCESS) ? payload_size :
+						 sizeof(return_values.status));
     if (respond_status != FWK_SUCCESS) {
-        FWK_LOG_DEBUG("[SCMI-SENS] %s @%d", __func__, __LINE__);
+	FWK_LOG_DEBUG("[SCMI-SENS] %s @%d", __func__, __LINE__);
     }
 
     return status;
@@ -771,7 +771,7 @@ exit:
 #endif
 
 static int scmi_sensor_reading_get_handler(fwk_id_t service_id,
-                                           const uint32_t *payload)
+					   const uint32_t *payload)
 {
     const struct scmi_sensor_protocol_reading_get_a2p *parameters;
     struct scmi_sensor_protocol_reading_get_p2a return_values;
@@ -783,56 +783,56 @@ static int scmi_sensor_reading_get_handl
     parameters = (const struct scmi_sensor_protocol_reading_get_a2p *)payload;
 
     if (parameters->sensor_id >= scmi_sensor_ctx.sensor_count) {
-        /* Sensor does not exist */
-        status = FWK_SUCCESS;
-        return_values.status = (int32_t)SCMI_NOT_FOUND;
-        goto exit;
+	/* Sensor does not exist */
+	status = FWK_SUCCESS;
+	return_values.status = (int32_t)SCMI_NOT_FOUND;
+	goto exit;
     }
 
     flags = parameters->flags;
 
     if ((flags & ~SCMI_SENSOR_PROTOCOL_READING_GET_ASYNC_FLAG_MASK) !=
-        (uint32_t)0) {
-        return_values.status = (int32_t)SCMI_INVALID_PARAMETERS;
-        status = FWK_SUCCESS;
-        goto exit;
+	(uint32_t)0) {
+	return_values.status = (int32_t)SCMI_INVALID_PARAMETERS;
+	status = FWK_SUCCESS;
+	goto exit;
     }
 
     /* Reject asynchronous read requests for now */
     if ((flags & SCMI_SENSOR_PROTOCOL_READING_GET_ASYNC_FLAG_MASK) !=
-        (uint32_t)0) {
-        return_values.status = (int32_t)SCMI_NOT_SUPPORTED;
-        status = FWK_SUCCESS;
-        goto exit;
+	(uint32_t)0) {
+	return_values.status = (int32_t)SCMI_NOT_SUPPORTED;
+	status = FWK_SUCCESS;
+	goto exit;
     }
 
     sensor_idx = parameters->sensor_id;
 
     /* Check if there is already a request pending for this sensor */
     if (!fwk_id_is_equal(
-            scmi_sensor_ctx.sensor_ops_table[sensor_idx].service_id,
-            FWK_ID_NONE)){
-        return_values.status = (int32_t)SCMI_BUSY;
-        status = FWK_SUCCESS;
+	    scmi_sensor_ctx.sensor_ops_table[sensor_idx].service_id,
+	    FWK_ID_NONE)){
+	return_values.status = (int32_t)SCMI_BUSY;
+	status = FWK_SUCCESS;
 
-        goto exit;
+	goto exit;
     }
 
     /* The get_data request is processed within the event being generated */
     struct fwk_event event = {
-        .target_id = fwk_module_id_scmi_sensor,
-        .id = mod_scmi_sensor_event_id_get_request,
+	.target_id = fwk_module_id_scmi_sensor,
+	.id = mod_scmi_sensor_event_id_get_request,
     };
 
     params = (struct scmi_sensor_event_parameters *)event.params;
     params->sensor_id = FWK_ID_ELEMENT(FWK_MODULE_IDX_SENSOR,
-                                       sensor_idx);
+				       sensor_idx);
 
     status = fwk_put_event(&event);
     if (status != FWK_SUCCESS) {
-        return_values.status = (int32_t)SCMI_GENERIC_ERROR;
+	return_values.status = (int32_t)SCMI_GENERIC_ERROR;
 
-        goto exit;
+	goto exit;
     }
 
     /* Store service identifier to indicate there is a pending request */
@@ -842,13 +842,13 @@ static int scmi_sensor_reading_get_handl
 
 exit:
     respond_status = scmi_sensor_ctx.scmi_api->respond(
-        service_id,
-        &return_values,
-        (return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
-                                                 sizeof(return_values.status));
+	service_id,
+	&return_values,
+	(return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
+						 sizeof(return_values.status));
 
     if (respond_status != FWK_SUCCESS) {
-        FWK_LOG_DEBUG("[SCMI-SENS] %s @%d", __func__, __LINE__);
+	FWK_LOG_DEBUG("[SCMI-SENS] %s @%d", __func__, __LINE__);
     }
 
     return status;
@@ -858,7 +858,7 @@ exit:
  * SCMI module -> SCMI sensor module interface
  */
 static int scmi_sensor_get_scmi_protocol_id(fwk_id_t protocol_id,
-                                            uint8_t *scmi_protocol_id)
+					    uint8_t *scmi_protocol_id)
 {
     *scmi_protocol_id = (uint8_t)MOD_SCMI_PROTOCOL_ID_SENSOR;
 
@@ -878,7 +878,7 @@ static unsigned int get_sensor_id(const
      * unnecessary code.
      */
     const struct scmi_sensor_protocol_reading_get_a2p *parameters =
-        (const struct scmi_sensor_protocol_reading_get_a2p *)payload;
+	(const struct scmi_sensor_protocol_reading_get_a2p *)payload;
     return parameters->sensor_id;
 }
 
@@ -894,39 +894,39 @@ static int scmi_sensor_permissions_handl
 
     status = scmi_sensor_ctx.scmi_api->get_agent_id(service_id, &agent_id);
     if (status != FWK_SUCCESS) {
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 
     if (message_id < 3) {
-        perms = scmi_sensor_ctx.res_perms_api->agent_has_protocol_permission(
-            agent_id, MOD_SCMI_PROTOCOL_ID_SENSOR);
-        if (perms == MOD_RES_PERMS_ACCESS_ALLOWED) {
-            return FWK_SUCCESS;
-        }
-        return FWK_E_ACCESS;
+	perms = scmi_sensor_ctx.res_perms_api->agent_has_protocol_permission(
+	    agent_id, MOD_SCMI_PROTOCOL_ID_SENSOR);
+	if (perms == MOD_RES_PERMS_ACCESS_ALLOWED) {
+	    return FWK_SUCCESS;
+	}
+	return FWK_E_ACCESS;
     }
 
     sensor_id = get_sensor_id(payload);
     if (sensor_id >= scmi_sensor_ctx.sensor_count) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     perms = scmi_sensor_ctx.res_perms_api->agent_has_resource_permission(
-        agent_id, MOD_SCMI_PROTOCOL_ID_SENSOR, message_id, sensor_id);
+	agent_id, MOD_SCMI_PROTOCOL_ID_SENSOR, message_id, sensor_id);
 
     if (perms == MOD_RES_PERMS_ACCESS_ALLOWED) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     } else {
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 }
 #endif
 
 static int scmi_sensor_message_handler(fwk_id_t protocol_id,
-                                       fwk_id_t service_id,
-                                       const uint32_t *payload,
-                                       size_t payload_size,
-                                       unsigned int message_id)
+				       fwk_id_t service_id,
+				       const uint32_t *payload,
+				       size_t payload_size,
+				       unsigned int message_id)
 {
     int32_t return_value;
 #ifdef BUILD_HAS_MOD_RESOURCE_PERMS
@@ -934,33 +934,33 @@ static int scmi_sensor_message_handler(f
 #endif
 
     static_assert(FWK_ARRAY_SIZE(handler_table) ==
-        FWK_ARRAY_SIZE(payload_size_table),
-        "[SCMI] Sensor management protocol table sizes not consistent");
+	FWK_ARRAY_SIZE(payload_size_table),
+	"[SCMI] Sensor management protocol table sizes not consistent");
     fwk_assert(payload != NULL);
 
     if (message_id >= FWK_ARRAY_SIZE(handler_table)) {
-        return_value = (int32_t)SCMI_NOT_FOUND;
-        goto error;
+	return_value = (int32_t)SCMI_NOT_FOUND;
+	goto error;
     }
 
     if (payload_size != payload_size_table[message_id]) {
-        /* Incorrect payload size or message is not supported */
-        return_value = (int32_t)SCMI_PROTOCOL_ERROR;
-        goto error;
+	/* Incorrect payload size or message is not supported */
+	return_value = (int32_t)SCMI_PROTOCOL_ERROR;
+	goto error;
     }
 
 #ifdef BUILD_HAS_MOD_RESOURCE_PERMS
     status = scmi_sensor_permissions_handler(
-        service_id, payload, payload_size, message_id);
+	service_id, payload, payload_size, message_id);
     if (status != FWK_SUCCESS) {
-        if (status == FWK_E_ACCESS) {
-            return_value = (int32_t)SCMI_DENIED;
-        } else if (message_id == MOD_SCMI_SENSOR_DESCRIPTION_GET) {
-            return_value = (int32_t)SCMI_INVALID_PARAMETERS;
-        } else {
-            return_value = (int32_t)SCMI_NOT_FOUND;
-        }
-        goto error;
+	if (status == FWK_E_ACCESS) {
+	    return_value = (int32_t)SCMI_DENIED;
+	} else if (message_id == MOD_SCMI_SENSOR_DESCRIPTION_GET) {
+	    return_value = (int32_t)SCMI_INVALID_PARAMETERS;
+	} else {
+	    return_value = (int32_t)SCMI_NOT_FOUND;
+	}
+	goto error;
     }
 #endif
 
@@ -968,7 +968,7 @@ static int scmi_sensor_message_handler(f
 
 error:
     return scmi_sensor_ctx.scmi_api->respond(
-        service_id, &return_value, sizeof(return_value));
+	service_id, &return_value, sizeof(return_value));
 }
 
 static struct mod_scmi_to_protocol_api scmi_sensor_mod_scmi_to_protocol_api = {
@@ -988,16 +988,16 @@ static void scmi_sensor_notify_trip_poin
     trip_point_event.sensor_id = fwk_id_get_element_idx(sensor_id);
     trip_point_event.agent_id = 0x0;
     trip_point_event.trip_point_desc =
-        SCMI_SENSOR_TRIP_POINT_EVENT_DESC(state, trip_point_idx);
+	SCMI_SENSOR_TRIP_POINT_EVENT_DESC(state, trip_point_idx);
 
     status = scmi_sensor_ctx.scmi_notification_api->scmi_notification_notify(
-        MOD_SCMI_PROTOCOL_ID_SENSOR,
-        MOD_SCMI_SENSOR_TRIP_POINT_NOTIFY,
-        SCMI_SENSOR_TRIP_POINT_EVENT,
-        &trip_point_event,
-        sizeof(trip_point_event));
+	MOD_SCMI_PROTOCOL_ID_SENSOR,
+	MOD_SCMI_SENSOR_TRIP_POINT_NOTIFY,
+	SCMI_SENSOR_TRIP_POINT_EVENT,
+	&trip_point_event,
+	sizeof(trip_point_event));
     if (status != FWK_SUCCESS) {
-        FWK_LOG_DEBUG("[SCMI-SENS] %s @%d", __func__, __LINE__);
+	FWK_LOG_DEBUG("[SCMI-SENS] %s @%d", __func__, __LINE__);
     }
 #endif
 }
@@ -1009,38 +1009,38 @@ static struct mod_sensor_trip_point_api
  * Framework interface
  */
 static int scmi_sensor_init(fwk_id_t module_id,
-                            unsigned int element_count,
-                            const void *unused)
+			    unsigned int element_count,
+			    const void *unused)
 {
     if (element_count != 0) {
-        /* This module should not have any elements */
-        fwk_unexpected();
-        return FWK_E_SUPPORT;
+	/* This module should not have any elements */
+	fwk_unexpected();
+	return FWK_E_SUPPORT;
     }
 
     scmi_sensor_ctx.sensor_count = (unsigned int)fwk_module_get_element_count(
-        FWK_ID_MODULE(FWK_MODULE_IDX_SENSOR));
+	FWK_ID_MODULE(FWK_MODULE_IDX_SENSOR));
     if (scmi_sensor_ctx.sensor_count == 0) {
-        return FWK_E_SUPPORT;
+	return FWK_E_SUPPORT;
     }
 
     scmi_sensor_ctx.sensor_values = fwk_mm_calloc(
-        scmi_sensor_ctx.sensor_count, sizeof(struct mod_sensor_data));
+	scmi_sensor_ctx.sensor_count, sizeof(struct mod_sensor_data));
 
     /* SCMI protocol uses a 16 bit number to store the number of sensors.
      * So expose no more than 0xFFFF number of sensors. */
     if (scmi_sensor_ctx.sensor_count > UINT16_MAX) {
-        scmi_sensor_ctx.sensor_count = UINT16_MAX;
+	scmi_sensor_ctx.sensor_count = UINT16_MAX;
     }
 
     /* Allocate a table for the sensors state */
     scmi_sensor_ctx.sensor_ops_table =
-        fwk_mm_calloc(scmi_sensor_ctx.sensor_count,
-        sizeof(struct sensor_operations));
+	fwk_mm_calloc(scmi_sensor_ctx.sensor_count,
+	sizeof(struct sensor_operations));
 
     /* Initialize the service identifier for each sensor to 'available' */
     for (unsigned int i = 0; i < scmi_sensor_ctx.sensor_count; i++) {
-        scmi_sensor_ctx.sensor_ops_table[i].service_id = FWK_ID_NONE;
+	scmi_sensor_ctx.sensor_ops_table[i].service_id = FWK_ID_NONE;
     }
 
     return FWK_SUCCESS;
@@ -1052,18 +1052,18 @@ static int scmi_init_notifications(int s
     int status;
 
     status =
-        scmi_sensor_ctx.scmi_api->get_agent_count(&scmi_sensor_ctx.agent_count);
+	scmi_sensor_ctx.scmi_api->get_agent_count(&scmi_sensor_ctx.agent_count);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     fwk_assert(scmi_sensor_ctx.agent_count != 0u);
 
     status = scmi_sensor_ctx.scmi_notification_api->scmi_notification_init(
-        MOD_SCMI_PROTOCOL_ID_SENSOR,
-        scmi_sensor_ctx.agent_count,
-        scmi_sensor_ctx.sensor_count,
-        MOD_SCMI_SENSOR_NOTIFICATION_COUNT);
+	MOD_SCMI_PROTOCOL_ID_SENSOR,
+	scmi_sensor_ctx.agent_count,
+	scmi_sensor_ctx.sensor_count,
+	MOD_SCMI_SENSOR_NOTIFICATION_COUNT);
 
     return status;
 }
@@ -1074,45 +1074,45 @@ static int scmi_sensor_bind(fwk_id_t id,
     int status;
 
     if (round == 1) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     status = fwk_module_bind(FWK_ID_MODULE(FWK_MODULE_IDX_SCMI),
-                             FWK_ID_API(FWK_MODULE_IDX_SCMI,
-                                        MOD_SCMI_API_IDX_PROTOCOL),
-                             &scmi_sensor_ctx.scmi_api);
+			     FWK_ID_API(FWK_MODULE_IDX_SCMI,
+					MOD_SCMI_API_IDX_PROTOCOL),
+			     &scmi_sensor_ctx.scmi_api);
     if (status != FWK_SUCCESS) {
-        /* Failed to bind to SCMI module */
-        fwk_unexpected();
-        return status;
+	/* Failed to bind to SCMI module */
+	fwk_unexpected();
+	return status;
     }
 
     status = fwk_module_bind(FWK_ID_MODULE(FWK_MODULE_IDX_SENSOR),
-                             mod_sensor_api_id_sensor,
-                             &scmi_sensor_ctx.sensor_api);
+			     mod_sensor_api_id_sensor,
+			     &scmi_sensor_ctx.sensor_api);
     if (status != FWK_SUCCESS) {
-        /* Failed to bind to sensor module */
-        fwk_unexpected();
-        return status;
+	/* Failed to bind to sensor module */
+	fwk_unexpected();
+	return status;
     }
 
 #ifdef BUILD_HAS_MOD_RESOURCE_PERMS
     status = fwk_module_bind(
-        FWK_ID_MODULE(FWK_MODULE_IDX_RESOURCE_PERMS),
-        FWK_ID_API(FWK_MODULE_IDX_RESOURCE_PERMS, MOD_RES_PERM_RESOURCE_PERMS),
-        &scmi_sensor_ctx.res_perms_api);
+	FWK_ID_MODULE(FWK_MODULE_IDX_RESOURCE_PERMS),
+	FWK_ID_API(FWK_MODULE_IDX_RESOURCE_PERMS, MOD_RES_PERM_RESOURCE_PERMS),
+	&scmi_sensor_ctx.res_perms_api);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 #endif
 
 #ifdef BUILD_HAS_SCMI_NOTIFICATIONS
     status = fwk_module_bind(
-        FWK_ID_MODULE(FWK_MODULE_IDX_SCMI),
-        FWK_ID_API(FWK_MODULE_IDX_SCMI, MOD_SCMI_API_IDX_NOTIFICATION),
-        &scmi_sensor_ctx.scmi_notification_api);
+	FWK_ID_MODULE(FWK_MODULE_IDX_SCMI),
+	FWK_ID_API(FWK_MODULE_IDX_SCMI, MOD_SCMI_API_IDX_NOTIFICATION),
+	&scmi_sensor_ctx.scmi_notification_api);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 #endif
 
@@ -1126,7 +1126,7 @@ static int scmi_sensor_start(fwk_id_t id
 #ifdef BUILD_HAS_SCMI_NOTIFICATIONS
     status = scmi_init_notifications((int)scmi_sensor_ctx.sensor_count);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 #endif
 
@@ -1134,27 +1134,27 @@ static int scmi_sensor_start(fwk_id_t id
 }
 
 static int scmi_sensor_process_bind_request(fwk_id_t source_id,
-                                            fwk_id_t target_id,
-                                            fwk_id_t api_id,
-                                            const void **api)
+					    fwk_id_t target_id,
+					    fwk_id_t api_id,
+					    const void **api)
 {
     enum scmi_sensor_api_idx api_id_type =
-        (enum scmi_sensor_api_idx)fwk_id_get_api_idx(api_id);
+	(enum scmi_sensor_api_idx)fwk_id_get_api_idx(api_id);
 
     switch (api_id_type) {
     case SCMI_SENSOR_API_IDX_REQUEST:
-        if (!fwk_id_is_equal(source_id, FWK_ID_MODULE(FWK_MODULE_IDX_SCMI))) {
-            return FWK_E_ACCESS;
-        }
-        *api = &scmi_sensor_mod_scmi_to_protocol_api;
-        break;
+	if (!fwk_id_is_equal(source_id, FWK_ID_MODULE(FWK_MODULE_IDX_SCMI))) {
+	    return FWK_E_ACCESS;
+	}
+	*api = &scmi_sensor_mod_scmi_to_protocol_api;
+	break;
 
     case SCMI_SENSOR_API_IDX_TRIP_POINT:
-        *api = &sensor_trip_point_api;
-        break;
+	*api = &sensor_trip_point_api;
+	break;
 
     default:
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
     return FWK_SUCCESS;
 }
@@ -1165,7 +1165,7 @@ static inline struct mod_sensor_data *ge
 }
 
 static int scmi_sensor_process_event(const struct fwk_event *event,
-                                     struct fwk_event *resp_event)
+				     struct fwk_event *resp_event)
 {
     int status;
     struct scmi_sensor_event_parameters *scmi_params;
@@ -1173,26 +1173,26 @@ static int scmi_sensor_process_event(con
 
     /* Request event to sensor HAL */
     if (fwk_id_is_equal(event->id, mod_scmi_sensor_event_id_get_request)) {
-        scmi_params = (struct scmi_sensor_event_parameters *)event->params;
-        sensor_data = get_sensor_data(scmi_params->sensor_id);
-        status = scmi_sensor_ctx.sensor_api->get_data(
-            scmi_params->sensor_id, sensor_data);
-        if (status != FWK_PENDING) {
-            /* Sensor value is ready (successfully or not) */
-            return scmi_sensor_reading_respond(
-                scmi_params->sensor_id, sensor_data);
-
-        } else {
-            /* Sensor value will be provided through a response event */
-            return FWK_SUCCESS;
-        }
+	scmi_params = (struct scmi_sensor_event_parameters *)event->params;
+	sensor_data = get_sensor_data(scmi_params->sensor_id);
+	status = scmi_sensor_ctx.sensor_api->get_data(
+	    scmi_params->sensor_id, sensor_data);
+	if (status != FWK_PENDING) {
+	    /* Sensor value is ready (successfully or not) */
+	    return scmi_sensor_reading_respond(
+		scmi_params->sensor_id, sensor_data);
+
+	} else {
+	    /* Sensor value will be provided through a response event */
+	    return FWK_SUCCESS;
+	}
     }
 
     /* Response event from sensor HAL */
     if (fwk_id_is_equal(event->id, mod_sensor_event_id_read_request)) {
-        sensor_data = get_sensor_data(event->source_id);
+	sensor_data = get_sensor_data(event->source_id);
 
-        return scmi_sensor_reading_respond(event->source_id, sensor_data);
+	return scmi_sensor_reading_respond(event->source_id, sensor_data);
     }
 
     return FWK_SUCCESS;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_sensor_req/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi_sensor_req/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_sensor_req/CMakeLists.txt
@@ -8,9 +8,9 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_scmi_sensor_req.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_scmi_sensor_req.c")
 
 target_link_libraries(${SCP_MODULE_TARGET} PRIVATE module-scmi module-sensor)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_sensor_req/doc/scmi_sensor_req.md
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi_sensor_req/doc/scmi_sensor_req.md
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_sensor_req/doc/scmi_sensor_req.md
@@ -30,21 +30,21 @@ The following sequence diagram demonstra
 involved modules starting from a client requesting sensor data until it gets
 the response.
 
-                            Sequence diagram
+			    Sequence diagram
     +--------+     +------------+     +-----------------+         +------+
     | Client |     | Sensor HAL |     | SCMI Sensor Req |         | SCMI |
     +--------+     +------------+     +-----------------+         +------+
-         | get_data()     |                     |                     |
-         |--------------->| get_value()         |                     |
-         |                |-------------------->| scmi_send_message() |
-         |                |                     |-------------------->|
-                                       ..
-                                       ..
-                                       ..
-                                             scmi_sensor_req_message_handler()
-         |                | reading_complete()  |<--------------------|
-         | put_event()    |<--------------------|                     |
-         |<---------------|                     |                     |
+	 | get_data()     |                     |                     |
+	 |--------------->| get_value()         |                     |
+	 |                |-------------------->| scmi_send_message() |
+	 |                |                     |-------------------->|
+				       ..
+				       ..
+				       ..
+					     scmi_sensor_req_message_handler()
+	 |                | reading_complete()  |<--------------------|
+	 | put_event()    |<--------------------|                     |
+	 |<---------------|                     |                     |
 
 
 ## Configuration
@@ -53,7 +53,7 @@ The following diagram shows an example c
 requester in a primary SCP which needs to get a sensor value accessed by
 another SCP.
 
-                              Configuration example
+			      Configuration example
     +----------------------------------------------------------------------+
     |  Primary SCP                                                         |
     |----------------------------------------------------------------------|
@@ -67,18 +67,18 @@ another SCP.
     | +-------------+ +-------------+ +---------------+ | +-------------+  |
     |                                                   |                  |
     +---------------------------------------------------+------------------+
-                                                        |
-                       +----------------------+         |
-                       |Secondary SCP         |         |
-                       |----------------------|         |
-                       | +------------------+ |         |
-                       | |Sensor HAL        | |         |
-                       | |------------------| |         |
-                       | | +--------------+ | |         |
-                       | | |element_X     |<+-+---------+
-                       | | +--------------+ | | sensor_req_c is configured to
-                       +----------------------+ use sensor_hal_x in the
-                                                completer by setting its ID.
+							|
+		       +----------------------+         |
+		       |Secondary SCP         |         |
+		       |----------------------|         |
+		       | +------------------+ |         |
+		       | |Sensor HAL        | |         |
+		       | |------------------| |         |
+		       | | +--------------+ | |         |
+		       | | |element_X     |<+-+---------+
+		       | | +--------------+ | | sensor_req_c is configured to
+		       +----------------------+ use sensor_hal_x in the
+						completer by setting its ID.
 
 ## Limitations
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_sensor_req/src/mod_scmi_sensor_req.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi_sensor_req/src/mod_scmi_sensor_req.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_sensor_req/src/mod_scmi_sensor_req.c
@@ -72,7 +72,7 @@ static int (*handler_table[MOD_SCMI_SENS
 
 static unsigned int payload_size_table[MOD_SCMI_SENSOR_COMMAND_COUNT] = {
     [MOD_SCMI_SENSOR_READING_GET] =
-        (unsigned int)sizeof(struct scmi_sensor_protocol_reading_get_p2a),
+	(unsigned int)sizeof(struct scmi_sensor_protocol_reading_get_p2a),
 };
 
 static_assert(
@@ -95,16 +95,16 @@ static int get_sensor_hal_id_from_servic
      * given Service ID.
      */
     for (sens_req_idx = 0u; sens_req_idx < scmi_sensor_req_ctx.element_count;
-         sens_req_idx++) {
-        if (fwk_id_is_equal(
-                service_id,
-                scmi_sensor_req_ctx.ctx_table[sens_req_idx]
-                    .config->service_id)) {
-            *sensor_hal_id = scmi_sensor_req_ctx.ctx_table[sens_req_idx]
-                                 .config->sensor_hal_id;
-            status = FWK_SUCCESS;
-            break;
-        }
+	 sens_req_idx++) {
+	if (fwk_id_is_equal(
+		service_id,
+		scmi_sensor_req_ctx.ctx_table[sens_req_idx]
+		    .config->service_id)) {
+	    *sensor_hal_id = scmi_sensor_req_ctx.ctx_table[sens_req_idx]
+				 .config->sensor_hal_id;
+	    status = FWK_SUCCESS;
+	    break;
+	}
     }
 
     return status;
@@ -134,18 +134,18 @@ static int scmi_sensor_req_ret_reading_h
     status = get_sensor_hal_id_from_service_id(service_id, &sensor_hal_id);
 
     if (status == FWK_SUCCESS) {
-        /*
-         * As per the SCMI spec, the return values (payload) are:
-         *  - int32 status
-         *  - readings[n]
-         */
-        ret_status = *((int32_t *)payload);
-        resp_params.status =
-            (ret_status == SCMI_SUCCESS) ? FWK_SUCCESS : FWK_E_DEVICE;
-        payload++;
-        resp_params.value = *((uint64_t *)payload);
-        scmi_sensor_req_ctx.resp_api->reading_complete(
-            sensor_hal_id, &resp_params);
+	/*
+	 * As per the SCMI spec, the return values (payload) are:
+	 *  - int32 status
+	 *  - readings[n]
+	 */
+	ret_status = *((int32_t *)payload);
+	resp_params.status =
+	    (ret_status == SCMI_SUCCESS) ? FWK_SUCCESS : FWK_E_DEVICE;
+	payload++;
+	resp_params.value = *((uint64_t *)payload);
+	scmi_sensor_req_ctx.resp_api->reading_complete(
+	    sensor_hal_id, &resp_params);
     }
 
     return status;
@@ -172,29 +172,29 @@ static int scmi_sensor_req_get_value(fwk
     element_idx = fwk_id_get_element_idx(id);
 
     if (element_idx < scmi_sensor_req_ctx.element_count) {
-        ctx = &(scmi_sensor_req_ctx.ctx_table[element_idx]);
+	ctx = &(scmi_sensor_req_ctx.ctx_table[element_idx]);
 
-        payload.sensor_id = ctx->config->scmi_sensor_id;
-        payload.flags = (uint32_t)(ctx->config->async_flag);
+	payload.sensor_id = ctx->config->scmi_sensor_id;
+	payload.flags = (uint32_t)(ctx->config->async_flag);
 
-        status = scmi_sensor_req_ctx.scmi_api->scmi_send_message(
-            scmi_message_id,
-            scmi_protocol_id,
-            /*
-             * Token is incremented with each message sent to ease
-             * debugging
-             */
-            scmi_sensor_req_ctx.token++,
-            ctx->config->service_id,
-            (const void *)&payload,
-            sizeof(payload),
-            true);
-
-        if (status == FWK_SUCCESS) {
-            status = FWK_PENDING;
-        }
+	status = scmi_sensor_req_ctx.scmi_api->scmi_send_message(
+	    scmi_message_id,
+	    scmi_protocol_id,
+	    /*
+	     * Token is incremented with each message sent to ease
+	     * debugging
+	     */
+	    scmi_sensor_req_ctx.token++,
+	    ctx->config->service_id,
+	    (const void *)&payload,
+	    sizeof(payload),
+	    true);
+
+	if (status == FWK_SUCCESS) {
+	    status = FWK_PENDING;
+	}
     } else {
-        status = FWK_E_PARAM;
+	status = FWK_E_PARAM;
     }
 
     return status;
@@ -234,18 +234,18 @@ static int scmi_sensor_req_message_handl
     fwk_assert(payload != NULL);
 
     if (message_id >= FWK_ARRAY_SIZE(handler_table)) {
-        return FWK_E_RANGE;
+	return FWK_E_RANGE;
     }
 
     if (payload_size != payload_size_table[message_id]) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     handler_status =
-        handler_table[message_id](service_id, payload, payload_size);
+	handler_table[message_id](service_id, payload, payload_size);
 
     resp_status =
-        scmi_sensor_req_ctx.scmi_api->response_message_handler(service_id);
+	scmi_sensor_req_ctx.scmi_api->response_message_handler(service_id);
 
     return (handler_status != FWK_SUCCESS) ? handler_status : resp_status;
 }
@@ -264,13 +264,13 @@ static int scmi_sensor_req_init(
     const void *unused)
 {
     if (element_count == 0) {
-        return FWK_E_DATA;
+	return FWK_E_DATA;
     }
 
     scmi_sensor_req_ctx.element_count = element_count;
 
     scmi_sensor_req_ctx.ctx_table =
-        fwk_mm_calloc(element_count, sizeof(struct scmi_sensor_req_elem_ctx));
+	fwk_mm_calloc(element_count, sizeof(struct scmi_sensor_req_elem_ctx));
 
     return FWK_SUCCESS;
 }
@@ -287,7 +287,7 @@ static int scmi_sensor_req_elem_init(
     fwk_assert(data != NULL);
 
     if (element_idx >= scmi_sensor_req_ctx.element_count) {
-        return FWK_E_DATA;
+	return FWK_E_DATA;
     }
 
     config = (const struct scmi_sensor_req_config *)data;
@@ -303,18 +303,18 @@ static int scmi_sensor_req_bind(fwk_id_t
     int status = FWK_SUCCESS;
 
     if (round == 0) {
-        if (fwk_id_is_type(id, FWK_ID_TYPE_MODULE)) {
-            status = fwk_module_bind(
-                FWK_ID_MODULE(FWK_MODULE_IDX_SCMI),
-                FWK_ID_API(FWK_MODULE_IDX_SCMI, MOD_SCMI_API_IDX_PROTOCOL_REQ),
-                &scmi_sensor_req_ctx.scmi_api);
-        } else {
-            ctx = &(scmi_sensor_req_ctx.ctx_table[fwk_id_get_element_idx(id)]);
-            status = fwk_module_bind(
-                ctx->config->sensor_hal_id,
-                mod_sensor_api_id_driver_response,
-                &scmi_sensor_req_ctx.resp_api);
-        }
+	if (fwk_id_is_type(id, FWK_ID_TYPE_MODULE)) {
+	    status = fwk_module_bind(
+		FWK_ID_MODULE(FWK_MODULE_IDX_SCMI),
+		FWK_ID_API(FWK_MODULE_IDX_SCMI, MOD_SCMI_API_IDX_PROTOCOL_REQ),
+		&scmi_sensor_req_ctx.scmi_api);
+	} else {
+	    ctx = &(scmi_sensor_req_ctx.ctx_table[fwk_id_get_element_idx(id)]);
+	    status = fwk_module_bind(
+		ctx->config->sensor_hal_id,
+		mod_sensor_api_id_driver_response,
+		&scmi_sensor_req_ctx.resp_api);
+	}
     }
 
     return status;
@@ -331,13 +331,13 @@ static int scmi_sensor_req_process_bind_
     fwk_assert(api != NULL);
 
     if (source_idx == FWK_MODULE_IDX_SCMI) {
-        *api = &scmi_sensor_mod_scmi_to_protocol_api;
-        return FWK_SUCCESS;
+	*api = &scmi_sensor_mod_scmi_to_protocol_api;
+	return FWK_SUCCESS;
     }
 
     if (source_idx == FWK_MODULE_IDX_SENSOR) {
-        *api = &scmi_sensor_req_api;
-        return FWK_SUCCESS;
+	*api = &scmi_sensor_req_api;
+	return FWK_SUCCESS;
     }
 
     return FWK_E_ACCESS;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_sensor_req/test/mocks/Mockmod_scmi_sensor_req_extra.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi_sensor_req/test/mocks/Mockmod_scmi_sensor_req_extra.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_sensor_req/test/mocks/Mockmod_scmi_sensor_req_extra.c
@@ -199,7 +199,7 @@ void CMockExpectParameters_reading_compl
 void CMockExpectParameters_reading_complete(CMOCK_reading_complete_CALL_INSTANCE* cmock_call_instance, fwk_id_t id, struct mod_sensor_driver_resp_params* response, int response_Depth)
 {
   memcpy((void*)(&cmock_call_instance->Expected_id), (void*)(&id),
-         sizeof(fwk_id_t[sizeof(id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
+	 sizeof(fwk_id_t[sizeof(id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
   cmock_call_instance->Expected_response = response;
   cmock_call_instance->Expected_response_Depth = response_Depth;
   cmock_call_instance->ReturnThruPtr_response_Used = 0;
@@ -327,11 +327,11 @@ void CMockExpectParameters_scmi_send_mes
   cmock_call_instance->Expected_scmi_protocol_id = scmi_protocol_id;
   cmock_call_instance->Expected_token = token;
   memcpy((void*)(&cmock_call_instance->Expected_service_id), (void*)(&service_id),
-         sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
+	 sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
   cmock_call_instance->Expected_payload = payload;
   cmock_call_instance->Expected_payload_Depth = payload_Depth;
   memcpy((void*)(&cmock_call_instance->Expected_payload_size), (void*)(&payload_size),
-         sizeof(size_t[sizeof(payload_size) == sizeof(size_t) ? 1 : -1])); /* add size_t to :treat_as_array if this causes an error */
+	 sizeof(size_t[sizeof(payload_size) == sizeof(size_t) ? 1 : -1])); /* add size_t to :treat_as_array if this causes an error */
   cmock_call_instance->Expected_request_ack_by_interrupt = request_ack_by_interrupt;
 }
 
@@ -421,7 +421,7 @@ void CMockExpectParameters_response_mess
 void CMockExpectParameters_response_message_handler(CMOCK_response_message_handler_CALL_INSTANCE* cmock_call_instance, fwk_id_t service_id)
 {
   memcpy((void*)(&cmock_call_instance->Expected_service_id), (void*)(&service_id),
-         sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
+	 sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
 }
 
 void response_message_handler_CMockExpectAnyArgsAndReturn(UNITY_LINE_TYPE cmock_line, int cmock_to_return)
@@ -508,11 +508,11 @@ void CMockExpectParameters_fake_message_
 void CMockExpectParameters_fake_message_handler(CMOCK_fake_message_handler_CALL_INSTANCE* cmock_call_instance, fwk_id_t service_id, const uint32_t* payload, int payload_Depth, size_t payload_size)
 {
   memcpy((void*)(&cmock_call_instance->Expected_service_id), (void*)(&service_id),
-         sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
+	 sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
   cmock_call_instance->Expected_payload = payload;
   cmock_call_instance->Expected_payload_Depth = payload_Depth;
   memcpy((void*)(&cmock_call_instance->Expected_payload_size), (void*)(&payload_size),
-         sizeof(size_t[sizeof(payload_size) == sizeof(size_t) ? 1 : -1])); /* add size_t to :treat_as_array if this causes an error */
+	 sizeof(size_t[sizeof(payload_size) == sizeof(size_t) ? 1 : -1])); /* add size_t to :treat_as_array if this causes an error */
 }
 
 void fake_message_handler_CMockExpectAnyArgsAndReturn(UNITY_LINE_TYPE cmock_line, int cmock_to_return)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_sensor_req/test/mod_scmi_sensor_req_unit_test.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi_sensor_req/test/mod_scmi_sensor_req_unit_test.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_sensor_req/test/mod_scmi_sensor_req_unit_test.c
@@ -35,12 +35,12 @@ static const struct fwk_element scmi_sen
     [FAKE_SCMI_SENSOR_REQ_1_IDX] = {
     .name = "SENSOR_REQ_1",
     .data = &((struct scmi_sensor_req_config){
-        .service_id = FWK_ID_ELEMENT_INIT(
-            FWK_MODULE_IDX_SCMI,
-            0),
-        .scmi_sensor_id = 0,
-        .sensor_hal_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_SENSOR,0),
-        .async_flag = SENSOR_REQ_SYNC,
+	.service_id = FWK_ID_ELEMENT_INIT(
+	    FWK_MODULE_IDX_SCMI,
+	    0),
+	.scmi_sensor_id = 0,
+	.sensor_hal_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_SENSOR,0),
+	.async_flag = SENSOR_REQ_SYNC,
     }),
     },
     [FAKE_SCMI_SENSOR_REQ_IDX_COUNT] = {0},
@@ -83,13 +83,13 @@ void test_init(void)
     int status;
 
     status = scmi_sensor_req_init(
-        FWK_ID_MODULE(FWK_MODULE_IDX_SCMI_SENSOR_REQ), 0, NULL);
+	FWK_ID_MODULE(FWK_MODULE_IDX_SCMI_SENSOR_REQ), 0, NULL);
     TEST_ASSERT_EQUAL(status, FWK_E_DATA);
 
     status = scmi_sensor_req_init(
-        FWK_ID_MODULE(FWK_MODULE_IDX_SCMI_SENSOR_REQ),
-        FAKE_SCMI_SENSOR_REQ_IDX_COUNT,
-        NULL);
+	FWK_ID_MODULE(FWK_MODULE_IDX_SCMI_SENSOR_REQ),
+	FAKE_SCMI_SENSOR_REQ_IDX_COUNT,
+	NULL);
     TEST_ASSERT_EQUAL(status, FWK_SUCCESS);
 }
 
@@ -97,28 +97,28 @@ void test_elem_init(void)
 {
     int status;
     fwk_id_t element_id = FWK_ID_ELEMENT(
-        FWK_MODULE_IDX_SCMI_SENSOR_REQ, FAKE_SCMI_SENSOR_REQ_1_IDX);
+	FWK_MODULE_IDX_SCMI_SENSOR_REQ, FAKE_SCMI_SENSOR_REQ_1_IDX);
 
     fwk_id_get_element_idx_ExpectAndReturn(
-        element_id, FAKE_SCMI_SENSOR_REQ_1_IDX);
+	element_id, FAKE_SCMI_SENSOR_REQ_1_IDX);
 
     status = scmi_sensor_req_elem_init(
-        element_id,
-        0,
-        scmi_sensor_req_element_table[FAKE_SCMI_SENSOR_REQ_1_IDX].data);
+	element_id,
+	0,
+	scmi_sensor_req_element_table[FAKE_SCMI_SENSOR_REQ_1_IDX].data);
     TEST_ASSERT_EQUAL(status, FWK_SUCCESS);
 
     /* Test invalid */
     element_id = FWK_ID_ELEMENT(
-        FWK_MODULE_IDX_SCMI_SENSOR_REQ, FAKE_SCMI_SENSOR_REQ_IDX_COUNT);
+	FWK_MODULE_IDX_SCMI_SENSOR_REQ, FAKE_SCMI_SENSOR_REQ_IDX_COUNT);
 
     fwk_id_get_element_idx_ExpectAndReturn(
-        element_id, FAKE_SCMI_SENSOR_REQ_IDX_COUNT);
+	element_id, FAKE_SCMI_SENSOR_REQ_IDX_COUNT);
 
     status = scmi_sensor_req_elem_init(
-        element_id,
-        0,
-        scmi_sensor_req_element_table[FAKE_SCMI_SENSOR_REQ_1_IDX].data);
+	element_id,
+	0,
+	scmi_sensor_req_element_table[FAKE_SCMI_SENSOR_REQ_1_IDX].data);
     TEST_ASSERT_EQUAL(status, FWK_E_DATA);
 }
 
@@ -127,31 +127,31 @@ void test_bind(void)
     int status;
     fwk_id_t expected_api_id;
     fwk_id_t expected_sensor_hal_id =
-        ((struct scmi_sensor_req_config *)
-             scmi_sensor_req_element_table[FAKE_SCMI_SENSOR_REQ_1_IDX]
-                 .data)
-            ->sensor_hal_id;
+	((struct scmi_sensor_req_config *)
+	     scmi_sensor_req_element_table[FAKE_SCMI_SENSOR_REQ_1_IDX]
+		 .data)
+	    ->sensor_hal_id;
 
     expected_api_id =
-        FWK_ID_API(FWK_MODULE_IDX_SCMI, MOD_SCMI_API_IDX_PROTOCOL_REQ);
+	FWK_ID_API(FWK_MODULE_IDX_SCMI, MOD_SCMI_API_IDX_PROTOCOL_REQ);
 
     fwk_id_is_type_ExpectAnyArgsAndReturn(true);
     fwk_module_bind_ExpectAndReturn(
-        scmi_module_id,
-        expected_api_id,
-        &scmi_sensor_req_ctx.scmi_api,
-        FWK_SUCCESS);
+	scmi_module_id,
+	expected_api_id,
+	&scmi_sensor_req_ctx.scmi_api,
+	FWK_SUCCESS);
     status = scmi_sensor_req_bind(scmi_module_id, 0);
     TEST_ASSERT_EQUAL(status, FWK_SUCCESS);
 
     fwk_id_is_type_ExpectAnyArgsAndReturn(false);
     fwk_id_get_element_idx_ExpectAndReturn(
-        expected_sensor_hal_id, FAKE_SCMI_SENSOR_REQ_1_IDX);
+	expected_sensor_hal_id, FAKE_SCMI_SENSOR_REQ_1_IDX);
     fwk_module_bind_ExpectAndReturn(
-        expected_sensor_hal_id,
-        mod_sensor_api_id_driver_response,
-        &scmi_sensor_req_ctx.resp_api,
-        FWK_SUCCESS);
+	expected_sensor_hal_id,
+	mod_sensor_api_id_driver_response,
+	&scmi_sensor_req_ctx.resp_api,
+	FWK_SUCCESS);
     status = scmi_sensor_req_bind(expected_sensor_hal_id, 0);
     TEST_ASSERT_EQUAL(status, FWK_SUCCESS);
 }
@@ -168,21 +168,21 @@ void test_process_bind(void)
 
     fwk_id_get_module_idx_ExpectAndReturn(scmi_module_id, FWK_MODULE_IDX_SCMI);
     status = scmi_sensor_req_process_bind_request(
-        scmi_module_id, target_id, api_id, (const void **)&scmi_api);
+	scmi_module_id, target_id, api_id, (const void **)&scmi_api);
     TEST_ASSERT_EQUAL(status, FWK_SUCCESS);
     TEST_ASSERT_EQUAL_PTR(scmi_api, &scmi_sensor_mod_scmi_to_protocol_api);
 
     sensor_module_id = FWK_ID_MODULE(FWK_MODULE_IDX_SENSOR);
     fwk_id_get_module_idx_ExpectAndReturn(
-        sensor_module_id, FWK_MODULE_IDX_SENSOR);
+	sensor_module_id, FWK_MODULE_IDX_SENSOR);
     status = scmi_sensor_req_process_bind_request(
-        sensor_module_id, target_id, api_id, (const void **)&driver_api);
+	sensor_module_id, target_id, api_id, (const void **)&driver_api);
     TEST_ASSERT_EQUAL(status, FWK_SUCCESS);
     TEST_ASSERT_EQUAL_PTR(driver_api, &scmi_sensor_req_api);
 
     fwk_id_get_module_idx_ExpectAndReturn(invalid_module_id, UINT32_MAX);
     status = scmi_sensor_req_process_bind_request(
-        invalid_module_id, target_id, api_id, (const void **)&driver_api);
+	invalid_module_id, target_id, api_id, (const void **)&driver_api);
     TEST_ASSERT_EQUAL(status, FWK_E_ACCESS);
 }
 
@@ -190,37 +190,37 @@ void test_function_scmi_sensor_req_ret_r
 {
     int status;
     fwk_id_t expected_sensor_hal_id =
-        ((struct scmi_sensor_req_config *)
-             scmi_sensor_req_element_table[FAKE_SCMI_SENSOR_REQ_1_IDX]
-                 .data)
-            ->sensor_hal_id;
+	((struct scmi_sensor_req_config *)
+	     scmi_sensor_req_element_table[FAKE_SCMI_SENSOR_REQ_1_IDX]
+		 .data)
+	    ->sensor_hal_id;
     fwk_id_t expected_service_id =
-        ((struct scmi_sensor_req_config *)
-             scmi_sensor_req_element_table[FAKE_SCMI_SENSOR_REQ_1_IDX]
-                 .data)
-            ->service_id;
+	((struct scmi_sensor_req_config *)
+	     scmi_sensor_req_element_table[FAKE_SCMI_SENSOR_REQ_1_IDX]
+		 .data)
+	    ->service_id;
 
     fwk_id_t error_service_id = { .value = 0xFFFF };
     struct mod_sensor_driver_resp_params expected_resp_params;
     uint32_t payload[10] = { 0 };
 
     expected_resp_params = (struct mod_sensor_driver_resp_params){
-        .status = FWK_SUCCESS,
-        .value = 0,
+	.status = FWK_SUCCESS,
+	.value = 0,
     };
 
     fwk_id_is_equal_ExpectAndReturn(
-        error_service_id, expected_service_id, false);
+	error_service_id, expected_service_id, false);
     status = scmi_sensor_req_ret_reading_handler(
-        error_service_id, payload, sizeof(payload));
+	error_service_id, payload, sizeof(payload));
     TEST_ASSERT_EQUAL(status, FWK_E_PARAM);
 
     fwk_id_is_equal_ExpectAndReturn(
-        expected_service_id, expected_service_id, true);
+	expected_service_id, expected_service_id, true);
     reading_complete_ExpectWithArray(
-        expected_sensor_hal_id, &expected_resp_params, 1);
+	expected_sensor_hal_id, &expected_resp_params, 1);
     status = scmi_sensor_req_ret_reading_handler(
-        expected_service_id, payload, sizeof(payload));
+	expected_service_id, payload, sizeof(payload));
     TEST_ASSERT_EQUAL(status, FWK_SUCCESS);
 }
 
@@ -230,39 +230,39 @@ void test_function_scmi_sensor_req_get_v
     mod_sensor_value_t sensor_value;
     fwk_id_t expected_service_id;
     fwk_id_t sensor_id = FWK_ID_ELEMENT_INIT(
-        FWK_MODULE_IDX_SCMI_SENSOR_REQ, FAKE_SCMI_SENSOR_REQ_1_IDX);
+	FWK_MODULE_IDX_SCMI_SENSOR_REQ, FAKE_SCMI_SENSOR_REQ_1_IDX);
 
     struct scmi_sensor_protocol_reading_get_a2p expected_payload = {
-        .sensor_id =
-            ((struct scmi_sensor_req_config *)
-                 scmi_sensor_req_element_table[FAKE_SCMI_SENSOR_REQ_1_IDX]
-                     .data)
-                ->scmi_sensor_id,
-        .flags = (uint32_t)(
-            ((struct scmi_sensor_req_config *)
-                 scmi_sensor_req_element_table[FAKE_SCMI_SENSOR_REQ_1_IDX]
-                     .data)
-                ->async_flag),
+	.sensor_id =
+	    ((struct scmi_sensor_req_config *)
+		 scmi_sensor_req_element_table[FAKE_SCMI_SENSOR_REQ_1_IDX]
+		     .data)
+		->scmi_sensor_id,
+	.flags = (uint32_t)(
+	    ((struct scmi_sensor_req_config *)
+		 scmi_sensor_req_element_table[FAKE_SCMI_SENSOR_REQ_1_IDX]
+		     .data)
+		->async_flag),
     };
 
     expected_service_id =
-        ((struct scmi_sensor_req_config *)
-             scmi_sensor_req_element_table[FAKE_SCMI_SENSOR_REQ_1_IDX]
-                 .data)
-            ->service_id;
+	((struct scmi_sensor_req_config *)
+	     scmi_sensor_req_element_table[FAKE_SCMI_SENSOR_REQ_1_IDX]
+		 .data)
+	    ->service_id;
 
     fwk_id_get_element_idx_ExpectAndReturn(
-        sensor_id, FAKE_SCMI_SENSOR_REQ_1_IDX);
+	sensor_id, FAKE_SCMI_SENSOR_REQ_1_IDX);
     scmi_send_message_ExpectWithArrayAndReturn(
-        (uint8_t)MOD_SCMI_SENSOR_READING_GET,
-        (uint8_t)MOD_SCMI_PROTOCOL_ID_SENSOR,
-        0,
-        expected_service_id,
-        &expected_payload,
-        1,
-        sizeof(expected_payload),
-        true,
-        FWK_SUCCESS);
+	(uint8_t)MOD_SCMI_SENSOR_READING_GET,
+	(uint8_t)MOD_SCMI_PROTOCOL_ID_SENSOR,
+	0,
+	expected_service_id,
+	&expected_payload,
+	1,
+	sizeof(expected_payload),
+	true,
+	FWK_SUCCESS);
     status = scmi_sensor_req_get_value(sensor_id, &sensor_value);
     TEST_ASSERT_EQUAL(status, FWK_PENDING);
 }
@@ -272,11 +272,11 @@ void test_function_scmi_sensor_req_get_v
     int status;
     mod_sensor_value_t sensor_value;
     fwk_id_t sensor_id = FWK_ID_ELEMENT_INIT(
-        FWK_MODULE_IDX_SCMI_SENSOR_REQ, FAKE_SCMI_SENSOR_REQ_IDX_COUNT);
+	FWK_MODULE_IDX_SCMI_SENSOR_REQ, FAKE_SCMI_SENSOR_REQ_IDX_COUNT);
     ;
 
     fwk_id_get_element_idx_ExpectAndReturn(
-        sensor_id, FAKE_SCMI_SENSOR_REQ_IDX_COUNT);
+	sensor_id, FAKE_SCMI_SENSOR_REQ_IDX_COUNT);
 
     status = scmi_sensor_req_get_value(sensor_id, &sensor_value);
     TEST_ASSERT_EQUAL(status, FWK_E_PARAM);
@@ -297,7 +297,7 @@ void test_function_scmi_sensor_req_get_s
     fwk_id_t protocol_id;
     uint8_t scmi_protocol_id;
     status =
-        scmi_sensor_req_get_scmi_protocol_id(protocol_id, &scmi_protocol_id);
+	scmi_sensor_req_get_scmi_protocol_id(protocol_id, &scmi_protocol_id);
     TEST_ASSERT_EQUAL(status, FWK_SUCCESS);
     TEST_ASSERT_EQUAL(scmi_protocol_id, (uint8_t)MOD_SCMI_PROTOCOL_ID_SENSOR);
 }
@@ -308,44 +308,44 @@ void test_function_scmi_sensor_req_messa
     fwk_id_t expected_service_id;
     fwk_id_t protocol_id;
     uint8_t payload[sizeof(struct scmi_sensor_protocol_reading_get_p2a)] = {
-        0
+	0
     };
 
     expected_service_id =
-        ((struct scmi_sensor_req_config *)
-             scmi_sensor_req_element_table[FAKE_SCMI_SENSOR_REQ_1_IDX]
-                 .data)
-            ->service_id;
+	((struct scmi_sensor_req_config *)
+	     scmi_sensor_req_element_table[FAKE_SCMI_SENSOR_REQ_1_IDX]
+		 .data)
+	    ->service_id;
 
     status = scmi_sensor_req_message_handler(
-        protocol_id,
-        expected_service_id,
-        (uint32_t *)payload,
-        sizeof(payload),
-        sizeof(handler_table));
+	protocol_id,
+	expected_service_id,
+	(uint32_t *)payload,
+	sizeof(payload),
+	sizeof(handler_table));
     TEST_ASSERT_EQUAL(status, FWK_E_RANGE);
 
     status = scmi_sensor_req_message_handler(
-        protocol_id,
-        expected_service_id,
-        (uint32_t *)payload,
-        sizeof(payload) + 1,
-        (uint8_t)MOD_SCMI_SENSOR_READING_GET);
+	protocol_id,
+	expected_service_id,
+	(uint32_t *)payload,
+	sizeof(payload) + 1,
+	(uint8_t)MOD_SCMI_SENSOR_READING_GET);
     TEST_ASSERT_EQUAL(status, FWK_E_PARAM);
 
     fake_message_handler_ExpectWithArrayAndReturn(
-        expected_service_id,
-        (uint32_t *)payload,
-        sizeof(payload),
-        sizeof(payload),
-        FWK_SUCCESS);
+	expected_service_id,
+	(uint32_t *)payload,
+	sizeof(payload),
+	sizeof(payload),
+	FWK_SUCCESS);
     response_message_handler_ExpectAndReturn(expected_service_id, FWK_SUCCESS);
     status = scmi_sensor_req_message_handler(
-        protocol_id,
-        expected_service_id,
-        (uint32_t *)payload,
-        sizeof(payload),
-        (uint8_t)MOD_SCMI_SENSOR_READING_GET);
+	protocol_id,
+	expected_service_id,
+	(uint32_t *)payload,
+	sizeof(payload),
+	(uint8_t)MOD_SCMI_SENSOR_READING_GET);
     TEST_ASSERT_EQUAL(status, FWK_SUCCESS);
 }
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_system_power/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi_system_power/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_system_power/CMakeLists.txt
@@ -8,14 +8,14 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(
     ${SCP_MODULE_TARGET}
     PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_scmi_system_power.c")
 
 target_link_libraries(${SCP_MODULE_TARGET} PRIVATE module-power-domain
-                                                   module-scmi module-timer)
+						   module-scmi module-timer)
 
 if("resource-perms" IN_LIST SCP_MODULES)
     target_link_libraries(${SCP_MODULE_TARGET} PRIVATE module-resource-perms)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_system_power/src/mod_scmi_system_power.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi_system_power/src/mod_scmi_system_power.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_system_power/src/mod_scmi_system_power.c
@@ -74,7 +74,7 @@ static int (*const handler_table[MOD_SCM
     [MOD_SCMI_PROTOCOL_VERSION] = scmi_sys_power_version_handler,
     [MOD_SCMI_PROTOCOL_ATTRIBUTES] = scmi_sys_power_attributes_handler,
     [MOD_SCMI_PROTOCOL_MESSAGE_ATTRIBUTES] =
-        scmi_sys_power_msg_attributes_handler,
+	scmi_sys_power_msg_attributes_handler,
     [MOD_SCMI_SYS_POWER_STATE_SET] = scmi_sys_power_state_set_handler,
     [MOD_SCMI_SYS_POWER_STATE_GET] = scmi_sys_power_state_get_handler,
 #ifdef BUILD_HAS_SCMI_NOTIFICATIONS
@@ -84,24 +84,24 @@ static int (*const handler_table[MOD_SCM
 
 static const unsigned int
     payload_size_table[MOD_SCMI_SYS_POWER_COMMAND_COUNT] = {
-        [MOD_SCMI_PROTOCOL_VERSION] = 0,
-        [MOD_SCMI_PROTOCOL_ATTRIBUTES] = 0,
-        [MOD_SCMI_PROTOCOL_MESSAGE_ATTRIBUTES] =
-            (unsigned int)sizeof(struct scmi_protocol_message_attributes_a2p),
-        [MOD_SCMI_SYS_POWER_STATE_SET] =
-            (unsigned int)sizeof(struct scmi_sys_power_state_set_a2p),
-        [MOD_SCMI_SYS_POWER_STATE_GET] = 0,
+	[MOD_SCMI_PROTOCOL_VERSION] = 0,
+	[MOD_SCMI_PROTOCOL_ATTRIBUTES] = 0,
+	[MOD_SCMI_PROTOCOL_MESSAGE_ATTRIBUTES] =
+	    (unsigned int)sizeof(struct scmi_protocol_message_attributes_a2p),
+	[MOD_SCMI_SYS_POWER_STATE_SET] =
+	    (unsigned int)sizeof(struct scmi_sys_power_state_set_a2p),
+	[MOD_SCMI_SYS_POWER_STATE_GET] = 0,
 #ifdef BUILD_HAS_SCMI_NOTIFICATIONS
-        [MOD_SCMI_SYS_POWER_STATE_NOTIFY] =
-            sizeof(struct scmi_sys_power_state_notify_a2p),
+	[MOD_SCMI_SYS_POWER_STATE_NOTIFY] =
+	    sizeof(struct scmi_sys_power_state_notify_a2p),
 #endif
     };
 
 static enum mod_pd_system_shutdown
     system_state2system_shutdown[SCMI_SYSTEM_STATE_MAX] = {
-        [SCMI_SYSTEM_STATE_SHUTDOWN] = MOD_PD_SYSTEM_SHUTDOWN,
-        [SCMI_SYSTEM_STATE_COLD_RESET] = MOD_PD_SYSTEM_COLD_RESET,
-        [SCMI_SYSTEM_STATE_WARM_RESET] = MOD_PD_SYSTEM_WARM_RESET,
+	[SCMI_SYSTEM_STATE_SHUTDOWN] = MOD_PD_SYSTEM_SHUTDOWN,
+	[SCMI_SYSTEM_STATE_COLD_RESET] = MOD_PD_SYSTEM_COLD_RESET,
+	[SCMI_SYSTEM_STATE_WARM_RESET] = MOD_PD_SYSTEM_WARM_RESET,
     };
 
 static int system_state_get(enum scmi_system_state *system_state)
@@ -111,24 +111,24 @@ static int system_state_get(enum scmi_sy
     enum mod_pd_state state_type;
 
     status = scmi_sys_power_ctx.pd_api->get_state(
-        scmi_sys_power_ctx.system_power_domain_id, &state);
+	scmi_sys_power_ctx.system_power_domain_id, &state);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     state_type = (enum mod_pd_state)state;
 
     switch (state_type) {
     case MOD_PD_STATE_OFF:
-        *system_state = SCMI_SYSTEM_STATE_SHUTDOWN;
-        break;
+	*system_state = SCMI_SYSTEM_STATE_SHUTDOWN;
+	break;
 
     case MOD_PD_STATE_ON:
-        *system_state = SCMI_SYSTEM_STATE_POWER_UP;
-        break;
+	*system_state = SCMI_SYSTEM_STATE_POWER_UP;
+	break;
 
     default:
-        *system_state = SCMI_SYSTEM_STATE_SUSPEND;
+	*system_state = SCMI_SYSTEM_STATE_SUSPEND;
     }
 
     return FWK_SUCCESS;
@@ -145,27 +145,27 @@ static void scmi_sys_power_state_notify(
 
     status = scmi_sys_power_ctx.scmi_api->get_agent_id(service_id, &agent_id);
     if (status != FWK_SUCCESS) {
-        return;
+	return;
     }
 
     return_values.agent_id = (uint32_t)agent_id;
     return_values.system_state = system_state;
     if (forceful) {
-        return_values.flags = 0;
+	return_values.flags = 0;
     } else {
-        return_values.flags = 1;
+	return_values.flags = 1;
     }
 
     for (i = 0u; i < scmi_sys_power_ctx.agent_count; i++) {
-        id =  scmi_sys_power_ctx.system_power_notifications[i];
-        if (fwk_id_is_equal(id, FWK_ID_NONE) ||
-            fwk_id_is_equal(id, service_id)) {
-            continue;
-        }
-
-        scmi_sys_power_ctx.scmi_api->notify(id,
-            MOD_SCMI_PROTOCOL_ID_SYS_POWER, SCMI_SYS_POWER_STATE_SET_NOTIFY,
-            &return_values, sizeof(return_values));
+	id =  scmi_sys_power_ctx.system_power_notifications[i];
+	if (fwk_id_is_equal(id, FWK_ID_NONE) ||
+	    fwk_id_is_equal(id, service_id)) {
+	    continue;
+	}
+
+	scmi_sys_power_ctx.scmi_api->notify(id,
+	    MOD_SCMI_PROTOCOL_ID_SYS_POWER, SCMI_SYS_POWER_STATE_SET_NOTIFY,
+	    &return_values, sizeof(return_values));
     }
 }
 #endif
@@ -177,17 +177,17 @@ static void graceful_timer_callback(uint
 
     FWK_LOG_INFO("SCMI_SYS_POWER: Graceful request timeout...");
     FWK_LOG_INFO(
-        "SCMI_SYS_POWER: Forcing SCMI SYSTEM STATE %d",
-        (int)mod_scmi_system_state);
+	"SCMI_SYS_POWER: Forcing SCMI SYSTEM STATE %d",
+	(int)mod_scmi_system_state);
 
     scmi_sys_power_ctx.start_graceful_process = false;
 
     if (mod_scmi_system_state == SCMI_SYSTEM_STATE_SHUTDOWN) {
-        system_shutdown = system_state2system_shutdown[mod_scmi_system_state];
-        status = scmi_sys_power_ctx.pd_api->system_shutdown(system_shutdown);
-        if (status != FWK_SUCCESS) {
-            FWK_LOG_DEBUG("SCMI_SYS_POWER: %s @%d", __func__, __LINE__);
-        }
+	system_shutdown = system_state2system_shutdown[mod_scmi_system_state];
+	status = scmi_sys_power_ctx.pd_api->system_shutdown(system_shutdown);
+	if (status != FWK_SUCCESS) {
+	    FWK_LOG_DEBUG("SCMI_SYS_POWER: %s @%d", __func__, __LINE__);
+	}
     }
 }
 
@@ -195,37 +195,37 @@ static void graceful_timer_callback(uint
  * PROTOCOL_VERSION
  */
 static int scmi_sys_power_version_handler(fwk_id_t service_id,
-                                          const uint32_t *payload)
+					  const uint32_t *payload)
 {
     struct scmi_protocol_version_p2a return_values = {
-        .status = (int32_t)SCMI_SUCCESS,
-        .version = SCMI_PROTOCOL_VERSION_SYS_POWER,
+	.status = (int32_t)SCMI_SUCCESS,
+	.version = SCMI_PROTOCOL_VERSION_SYS_POWER,
     };
 
     return scmi_sys_power_ctx.scmi_api->respond(
-        service_id, &return_values, sizeof(return_values));
+	service_id, &return_values, sizeof(return_values));
 }
 
 /*
  * PROTOCOL_ATTRIBUTES
  */
 static int scmi_sys_power_attributes_handler(fwk_id_t service_id,
-                                             const uint32_t *payload)
+					     const uint32_t *payload)
 {
     struct scmi_protocol_attributes_p2a return_values = {
-        .status = (int32_t)SCMI_SUCCESS,
-        .attributes = 0,
+	.status = (int32_t)SCMI_SUCCESS,
+	.attributes = 0,
     };
 
     return scmi_sys_power_ctx.scmi_api->respond(
-        service_id, &return_values, sizeof(return_values));
+	service_id, &return_values, sizeof(return_values));
 }
 
 /*
  * PROTOCOL_MESSAGE_ATTRIBUTES
  */
 static int scmi_sys_power_msg_attributes_handler(fwk_id_t service_id,
-                                                 const uint32_t *payload)
+						 const uint32_t *payload)
 {
     const struct scmi_protocol_message_attributes_a2p *parameters;
     unsigned int message_id;
@@ -240,47 +240,47 @@ static int scmi_sys_power_msg_attributes
  */
 #ifndef BUILD_HAS_SCMI_NOTIFICATIONS
     if (message_id == MOD_SCMI_SYS_POWER_STATE_NOTIFY) {
-        return_values.status = (int32_t)SCMI_NOT_SUPPORTED;
+	return_values.status = (int32_t)SCMI_NOT_SUPPORTED;
 
-        goto exit;
+	goto exit;
     }
 #endif
 
     if ((message_id >= FWK_ARRAY_SIZE(handler_table)) ||
-        (handler_table[message_id] == NULL)) {
-        return_values.status = (int32_t)SCMI_NOT_FOUND;
-        goto exit;
+	(handler_table[message_id] == NULL)) {
+	return_values.status = (int32_t)SCMI_NOT_FOUND;
+	goto exit;
     }
 
     return_values = (struct scmi_protocol_message_attributes_p2a) {
-        .status = SCMI_SUCCESS,
-        .attributes = 0,
+	.status = SCMI_SUCCESS,
+	.attributes = 0,
     };
 
     if (message_id == MOD_SCMI_SYS_POWER_STATE_SET) {
-        return_values.attributes |= SYS_POWER_STATE_SET_ATTRIBUTES_SUSPEND |
-                                    SYS_POWER_STATE_SET_ATTRIBUTES_WARM_RESET;
+	return_values.attributes |= SYS_POWER_STATE_SET_ATTRIBUTES_SUSPEND |
+				    SYS_POWER_STATE_SET_ATTRIBUTES_WARM_RESET;
     }
 
 exit:
     return scmi_sys_power_ctx.scmi_api->respond(
-        service_id,
-        &return_values,
-        (return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
-                                                 sizeof(return_values.status));
+	service_id,
+	&return_values,
+	(return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
+						 sizeof(return_values.status));
 }
 
 /*
  * SYSTEM_POWER_STATE_SET
  */
 static int scmi_sys_power_state_set_handler(fwk_id_t service_id,
-                                            const uint32_t *payload)
+					    const uint32_t *payload)
 {
     int status = FWK_SUCCESS;
     int respond_status;
     const struct scmi_sys_power_state_set_a2p *parameters;
     struct scmi_sys_power_state_set_p2a return_values = {
-        .status = (int32_t)SCMI_GENERIC_ERROR,
+	.status = (int32_t)SCMI_GENERIC_ERROR,
     };
     unsigned int agent_id;
     enum scmi_agent_type agent_type;
@@ -292,24 +292,24 @@ static int scmi_sys_power_state_set_hand
     parameters = (const struct scmi_sys_power_state_set_a2p *)payload;
 
     if (parameters->flags & (uint32_t)(~STATE_SET_FLAGS_MASK)) {
-        return_values.status = (int32_t)SCMI_INVALID_PARAMETERS;
-        goto exit;
+	return_values.status = (int32_t)SCMI_INVALID_PARAMETERS;
+	goto exit;
     }
 
     status = scmi_sys_power_ctx.scmi_api->get_agent_id(service_id, &agent_id);
     if (status != FWK_SUCCESS) {
-        goto exit;
+	goto exit;
     }
 
     status = scmi_sys_power_ctx.scmi_api->get_agent_type(agent_id, &agent_type);
     if (status != FWK_SUCCESS) {
-        goto exit;
+	goto exit;
     }
 
     if ((agent_type != SCMI_AGENT_TYPE_PSCI) &&
-        (agent_type != SCMI_AGENT_TYPE_MANAGEMENT)) {
-        return_values.status = (int32_t)SCMI_NOT_SUPPORTED;
-        goto exit;
+	(agent_type != SCMI_AGENT_TYPE_MANAGEMENT)) {
+	return_values.status = (int32_t)SCMI_NOT_SUPPORTED;
+	goto exit;
     }
 
     mod_scmi_system_state = parameters->system_state;
@@ -319,18 +319,18 @@ static int scmi_sys_power_state_set_hand
      * handler.
      */
     status = scmi_sys_power_state_set_policy(
-        &policy_status,
-        &mod_scmi_system_state,
-        service_id,
-        (parameters->flags & (uint32_t)STATE_SET_FLAGS_GRACEFUL_REQUEST) != 0);
+	&policy_status,
+	&mod_scmi_system_state,
+	service_id,
+	(parameters->flags & (uint32_t)STATE_SET_FLAGS_GRACEFUL_REQUEST) != 0);
 
     if (status != FWK_SUCCESS) {
-        return_values.status = (int32_t)SCMI_GENERIC_ERROR;
-        goto exit;
+	return_values.status = (int32_t)SCMI_GENERIC_ERROR;
+	goto exit;
     }
     if (policy_status == MOD_SCMI_SYS_POWER_SKIP_MESSAGE_HANDLER) {
-        return_values.status = (int32_t)SCMI_SUCCESS;
-        goto exit;
+	return_values.status = (int32_t)SCMI_SUCCESS;
+	goto exit;
     }
 
     sys_state_type = (enum scmi_system_state)mod_scmi_system_state;
@@ -339,59 +339,59 @@ static int scmi_sys_power_state_set_hand
     case SCMI_SYSTEM_STATE_SHUTDOWN:
     case SCMI_SYSTEM_STATE_COLD_RESET:
     case SCMI_SYSTEM_STATE_WARM_RESET:
-        system_shutdown = system_state2system_shutdown[mod_scmi_system_state];
-        status = scmi_sys_power_ctx.pd_api->system_shutdown(system_shutdown);
-        if ((status == FWK_SUCCESS) || (status == FWK_PENDING)) {
-            status = FWK_SUCCESS;
-            return_values.status = (int32_t)SCMI_SUCCESS;
-        }
-        goto exit;
-        break;
+	system_shutdown = system_state2system_shutdown[mod_scmi_system_state];
+	status = scmi_sys_power_ctx.pd_api->system_shutdown(system_shutdown);
+	if ((status == FWK_SUCCESS) || (status == FWK_PENDING)) {
+	    status = FWK_SUCCESS;
+	    return_values.status = (int32_t)SCMI_SUCCESS;
+	}
+	goto exit;
+	break;
 
     case SCMI_SYSTEM_STATE_SUSPEND:
-        status = scmi_sys_power_ctx.pd_api->system_suspend(
-            scmi_sys_power_ctx.config->system_suspend_state);
-        if (status != FWK_SUCCESS) {
-            if (status == FWK_E_STATE) {
-                status = FWK_SUCCESS;
-                return_values.status = (int32_t)SCMI_DENIED;
-            }
-            goto exit;
-        }
-        break;
+	status = scmi_sys_power_ctx.pd_api->system_suspend(
+	    scmi_sys_power_ctx.config->system_suspend_state);
+	if (status != FWK_SUCCESS) {
+	    if (status == FWK_E_STATE) {
+		status = FWK_SUCCESS;
+		return_values.status = (int32_t)SCMI_DENIED;
+	    }
+	    goto exit;
+	}
+	break;
 
     case SCMI_SYSTEM_STATE_POWER_UP:
-        if ((agent_type != SCMI_AGENT_TYPE_MANAGEMENT) ||
-            (scmi_sys_power_ctx.config->system_view !=
-             MOD_SCMI_SYSTEM_VIEW_OSPM)) {
-            return_values.status = (int32_t)SCMI_NOT_SUPPORTED;
-            goto exit;
-        }
-
-        status =
-            system_state_get((enum scmi_system_state *)&mod_scmi_system_state);
-        if (status != FWK_SUCCESS) {
-            goto exit;
-        }
-
-        if ((mod_scmi_system_state != SCMI_SYSTEM_STATE_SHUTDOWN) &&
-            (mod_scmi_system_state != SCMI_SYSTEM_STATE_SUSPEND)) {
-            return_values.status = (int32_t)SCMI_DENIED;
-            goto exit;
-        }
-
-        status = scmi_sys_power_ctx.pd_api->set_state(
-            scmi_sys_power_ctx.config->wakeup_power_domain_id,
-            false,
-            scmi_sys_power_ctx.config->wakeup_composite_state);
-        if (status != FWK_SUCCESS) {
-            goto exit;
-        }
-        break;
+	if ((agent_type != SCMI_AGENT_TYPE_MANAGEMENT) ||
+	    (scmi_sys_power_ctx.config->system_view !=
+	     MOD_SCMI_SYSTEM_VIEW_OSPM)) {
+	    return_values.status = (int32_t)SCMI_NOT_SUPPORTED;
+	    goto exit;
+	}
+
+	status =
+	    system_state_get((enum scmi_system_state *)&mod_scmi_system_state);
+	if (status != FWK_SUCCESS) {
+	    goto exit;
+	}
+
+	if ((mod_scmi_system_state != SCMI_SYSTEM_STATE_SHUTDOWN) &&
+	    (mod_scmi_system_state != SCMI_SYSTEM_STATE_SUSPEND)) {
+	    return_values.status = (int32_t)SCMI_DENIED;
+	    goto exit;
+	}
+
+	status = scmi_sys_power_ctx.pd_api->set_state(
+	    scmi_sys_power_ctx.config->wakeup_power_domain_id,
+	    false,
+	    scmi_sys_power_ctx.config->wakeup_composite_state);
+	if (status != FWK_SUCCESS) {
+	    goto exit;
+	}
+	break;
 
     default:
-        return_values.status = (int32_t)SCMI_INVALID_PARAMETERS;
-        goto exit;
+	return_values.status = (int32_t)SCMI_INVALID_PARAMETERS;
+	goto exit;
     };
 
 #ifdef BUILD_HAS_SCMI_NOTIFICATIONS
@@ -400,11 +400,11 @@ static int scmi_sys_power_state_set_hand
      * requests the notifications are sent before executing the command.
      */
     if (((parameters->flags & STATE_SET_FLAGS_GRACEFUL_REQUEST) == 0U) ||
-        (mod_scmi_system_state != SCMI_SYSTEM_STATE_SHUTDOWN)) {
-        scmi_sys_power_state_notify(
-            service_id,
-            mod_scmi_system_state,
-            (parameters->flags & STATE_SET_FLAGS_GRACEFUL_REQUEST) != 0);
+	(mod_scmi_system_state != SCMI_SYSTEM_STATE_SHUTDOWN)) {
+	scmi_sys_power_state_notify(
+	    service_id,
+	    mod_scmi_system_state,
+	    (parameters->flags & STATE_SET_FLAGS_GRACEFUL_REQUEST) != 0);
     }
 #endif
 
@@ -412,13 +412,13 @@ static int scmi_sys_power_state_set_hand
 
 exit:
     respond_status = scmi_sys_power_ctx.scmi_api->respond(
-        service_id,
-        &return_values,
-        (return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
-                                                 sizeof(return_values.status));
+	service_id,
+	&return_values,
+	(return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
+						 sizeof(return_values.status));
 
     if (respond_status != FWK_SUCCESS) {
-        FWK_LOG_DEBUG("SCMI_SYS_POWER: %s @%d", __func__, __LINE__);
+	FWK_LOG_DEBUG("SCMI_SYS_POWER: %s @%d", __func__, __LINE__);
     }
 
     return status;
@@ -428,12 +428,12 @@ exit:
  * SYSTEM_POWER_STATE_GET
  */
 static int scmi_sys_power_state_get_handler(fwk_id_t service_id,
-                                            const uint32_t *payload)
+					    const uint32_t *payload)
 {
     int status = FWK_SUCCESS;
     int respond_status;
     struct scmi_sys_power_state_get_p2a return_values = {
-        .status = (int32_t)SCMI_GENERIC_ERROR,
+	.status = (int32_t)SCMI_GENERIC_ERROR,
     };
     enum scmi_system_state system_state;
     unsigned int agent_id;
@@ -441,42 +441,42 @@ static int scmi_sys_power_state_get_hand
 
     status = scmi_sys_power_ctx.scmi_api->get_agent_id(service_id, &agent_id);
     if (status != FWK_SUCCESS) {
-        goto exit;
+	goto exit;
     }
 
     status = scmi_sys_power_ctx.scmi_api->get_agent_type(agent_id, &agent_type);
     if (status != FWK_SUCCESS) {
-        goto exit;
+	goto exit;
     }
 
     return_values.status = (int32_t)SCMI_NOT_SUPPORTED;
     if (scmi_sys_power_ctx.config->system_view == MOD_SCMI_SYSTEM_VIEW_FULL) {
-        goto exit;
+	goto exit;
     } else {
-        if (agent_type == SCMI_AGENT_TYPE_PSCI) {
-            goto exit;
-        }
-
-        status = system_state_get(&system_state);
-        if (status != FWK_SUCCESS) {
-            goto exit;
-        }
-
-        return_values = (struct scmi_sys_power_state_get_p2a) {
-            .status = SCMI_SUCCESS,
-            .system_state = system_state,
-        };
+	if (agent_type == SCMI_AGENT_TYPE_PSCI) {
+	    goto exit;
+	}
+
+	status = system_state_get(&system_state);
+	if (status != FWK_SUCCESS) {
+	    goto exit;
+	}
+
+	return_values = (struct scmi_sys_power_state_get_p2a) {
+	    .status = SCMI_SUCCESS,
+	    .system_state = system_state,
+	};
     }
 
 exit:
     respond_status = scmi_sys_power_ctx.scmi_api->respond(
-        service_id,
-        &return_values,
-        (return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
-                                                 sizeof(return_values.status));
+	service_id,
+	&return_values,
+	(return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
+						 sizeof(return_values.status));
 
     if (respond_status != FWK_SUCCESS) {
-        FWK_LOG_DEBUG("SCMI_SYS_POWER: %s @%d", __func__, __LINE__);
+	FWK_LOG_DEBUG("SCMI_SYS_POWER: %s @%d", __func__, __LINE__);
     }
 
     return status;
@@ -487,44 +487,44 @@ exit:
  * SYSTEM_POWER_STATE_NOTIFY
  */
 static int scmi_sys_power_state_notify_handler(fwk_id_t service_id,
-                                               const uint32_t *payload)
+					       const uint32_t *payload)
 {
     unsigned int agent_id;
     const struct scmi_sys_power_state_notify_a2p *parameters;
     struct scmi_sys_power_state_notify_p2a return_values = {
-        .status = (int32_t)SCMI_GENERIC_ERROR,
+	.status = (int32_t)SCMI_GENERIC_ERROR,
     };
     int status, respond_status;
 
     status = scmi_sys_power_ctx.scmi_api->get_agent_id(service_id, &agent_id);
     if (status != FWK_SUCCESS) {
-        goto exit;
+	goto exit;
     }
 
     parameters = (const struct scmi_sys_power_state_notify_a2p *)payload;
 
     if (parameters->flags & (~STATE_NOTIFY_FLAGS_MASK)) {
-        return_values.status = (int32_t)SCMI_INVALID_PARAMETERS;
-        goto exit;
+	return_values.status = (int32_t)SCMI_INVALID_PARAMETERS;
+	goto exit;
     }
 
     if (parameters->flags & STATE_NOTIFY_FLAGS_MASK) {
-        scmi_sys_power_ctx.system_power_notifications[agent_id] = service_id;
+	scmi_sys_power_ctx.system_power_notifications[agent_id] = service_id;
     } else {
-        scmi_sys_power_ctx.system_power_notifications[agent_id] = FWK_ID_NONE;
+	scmi_sys_power_ctx.system_power_notifications[agent_id] = FWK_ID_NONE;
     }
 
     return_values.status = (int32_t)SCMI_SUCCESS;
 
 exit:
     respond_status = scmi_sys_power_ctx.scmi_api->respond(
-        service_id,
-        &return_values,
-        (return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
-                                                 sizeof(return_values.status));
+	service_id,
+	&return_values,
+	(return_values.status == SCMI_SUCCESS) ? sizeof(return_values) :
+						 sizeof(return_values.status));
 
     if (respond_status != FWK_SUCCESS) {
-        FWK_LOG_DEBUG("SCMI_SYS_POWER: %s @%d", __func__, __LINE__);
+	FWK_LOG_DEBUG("SCMI_SYS_POWER: %s @%d", __func__, __LINE__);
     }
 
     return FWK_SUCCESS;
@@ -554,33 +554,33 @@ FWK_WEAK int scmi_sys_power_state_set_po
     *policy_status = MOD_SCMI_SYS_POWER_EXECUTE_MESSAGE_HANDLER;
 
     if (graceful) {
-        *policy_status = MOD_SCMI_SYS_POWER_SKIP_MESSAGE_HANDLER;
+	*policy_status = MOD_SCMI_SYS_POWER_SKIP_MESSAGE_HANDLER;
 
-        if (!scmi_sys_power_ctx.start_graceful_process) {
-            /* Only shutdown command is enabled */
-            if (*state != SCMI_SYSTEM_STATE_SHUTDOWN) {
-                return FWK_SUCCESS;
-            }
-            /* Starting the graceful request process */
-            scmi_sys_power_ctx.start_graceful_process = true;
+	if (!scmi_sys_power_ctx.start_graceful_process) {
+	    /* Only shutdown command is enabled */
+	    if (*state != SCMI_SYSTEM_STATE_SHUTDOWN) {
+		return FWK_SUCCESS;
+	    }
+	    /* Starting the graceful request process */
+	    scmi_sys_power_ctx.start_graceful_process = true;
 #ifdef BUILD_HAS_SCMI_NOTIFICATIONS
-            scmi_sys_power_state_notify(service_id, *state, false);
+	    scmi_sys_power_state_notify(service_id, *state, false);
 #endif
 
-            if (scmi_sys_power_ctx.alarm_api != NULL) {
-                status = scmi_sys_power_ctx.alarm_api->start(
-                    scmi_sys_power_ctx.config->alarm_id,
-                    scmi_sys_power_ctx.config->graceful_timeout,
-                    MOD_TIMER_ALARM_TYPE_ONCE,
-                    graceful_timer_callback,
-                    *state);
-                if (status != FWK_SUCCESS) {
-                    FWK_LOG_DEBUG("SCMI_SYS_POWER: %s @%d", __func__, __LINE__);
-                }
-            } else {
-                graceful_timer_callback(*state);
-            }
-        }
+	    if (scmi_sys_power_ctx.alarm_api != NULL) {
+		status = scmi_sys_power_ctx.alarm_api->start(
+		    scmi_sys_power_ctx.config->alarm_id,
+		    scmi_sys_power_ctx.config->graceful_timeout,
+		    MOD_TIMER_ALARM_TYPE_ONCE,
+		    graceful_timer_callback,
+		    *state);
+		if (status != FWK_SUCCESS) {
+		    FWK_LOG_DEBUG("SCMI_SYS_POWER: %s @%d", __func__, __LINE__);
+		}
+	    } else {
+		graceful_timer_callback(*state);
+	    }
+	}
     }
 
     return FWK_SUCCESS;
@@ -603,23 +603,23 @@ static int scmi_sys_power_permissions_ha
 
     status = scmi_sys_power_ctx.scmi_api->get_agent_id(service_id, &agent_id);
     if (status != FWK_SUCCESS) {
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 
     if (message_id < 3) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     /*
      * We check that the agent has permssions to access the command
      */
     perms = scmi_sys_power_ctx.res_perms_api->agent_has_message_permission(
-        agent_id, MOD_SCMI_PROTOCOL_ID_BASE, message_id);
+	agent_id, MOD_SCMI_PROTOCOL_ID_BASE, message_id);
 
     if (perms == MOD_RES_PERMS_ACCESS_ALLOWED) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     } else {
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 }
 
@@ -629,7 +629,7 @@ static int scmi_sys_power_permissions_ha
  * SCMI module -> SCMI system power module interface
  */
 static int scmi_sys_power_get_scmi_protocol_id(fwk_id_t protocol_id,
-                                               uint8_t *scmi_protocol_id)
+					       uint8_t *scmi_protocol_id)
 {
     *scmi_protocol_id = (uint8_t)MOD_SCMI_PROTOCOL_ID_SYS_POWER;
 
@@ -637,10 +637,10 @@ static int scmi_sys_power_get_scmi_proto
 }
 
 static int scmi_sys_power_handler(fwk_id_t protocol_id,
-                                  fwk_id_t service_id,
-                                  const uint32_t *payload,
-                                  size_t payload_size,
-                                  unsigned int message_id)
+				  fwk_id_t service_id,
+				  const uint32_t *payload,
+				  size_t payload_size,
+				  unsigned int message_id)
 {
     int32_t return_value;
 #ifdef BUILD_HAS_MOD_RESOURCE_PERMS
@@ -648,28 +648,28 @@ static int scmi_sys_power_handler(fwk_id
 #endif
 
     static_assert(FWK_ARRAY_SIZE(handler_table) ==
-                  FWK_ARRAY_SIZE(payload_size_table),
-                  "[SCMI] System power protocol table sizes not consistent");
+		  FWK_ARRAY_SIZE(payload_size_table),
+		  "[SCMI] System power protocol table sizes not consistent");
 
     fwk_assert(payload != NULL);
 
     if (message_id >= FWK_ARRAY_SIZE(handler_table)) {
-        return_value = (int32_t)SCMI_NOT_FOUND;
-        goto error;
+	return_value = (int32_t)SCMI_NOT_FOUND;
+	goto error;
     }
 
     if (payload_size != payload_size_table[message_id]) {
-        /* Incorrect payload size or message is not supported */
-        return_value = (int32_t)SCMI_PROTOCOL_ERROR;
-        goto error;
+	/* Incorrect payload size or message is not supported */
+	return_value = (int32_t)SCMI_PROTOCOL_ERROR;
+	goto error;
     }
 
 #ifdef BUILD_HAS_MOD_RESOURCE_PERMS
     status = scmi_sys_power_permissions_handler(
-        service_id, payload, payload_size, message_id);
+	service_id, payload, payload_size, message_id);
     if (status != FWK_SUCCESS) {
-        return_value = (int32_t)SCMI_DENIED;
-        goto error;
+	return_value = (int32_t)SCMI_DENIED;
+	goto error;
     }
 #endif
 
@@ -677,7 +677,7 @@ static int scmi_sys_power_handler(fwk_id
 
 error:
     return scmi_sys_power_ctx.scmi_api->respond(
-        service_id, &return_value, sizeof(return_value));
+	service_id, &return_value, sizeof(return_value));
 }
 
 static struct mod_scmi_to_protocol_api scmi_sys_power_mod_scmi_to_protocol = {
@@ -689,7 +689,7 @@ static struct mod_scmi_to_protocol_api s
  * Framework handlers
  */
 static int scmi_sys_power_init(fwk_id_t module_id, unsigned int element_count,
-                               const void *data)
+			       const void *data)
 {
     scmi_sys_power_ctx.config = data;
     scmi_sys_power_ctx.start_graceful_process = false;
@@ -704,17 +704,17 @@ static int scmi_sys_power_init_notificat
     unsigned int i;
 
     status = scmi_sys_power_ctx.scmi_api->get_agent_count(
-        &scmi_sys_power_ctx.agent_count);
+	&scmi_sys_power_ctx.agent_count);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
     fwk_assert(scmi_sys_power_ctx.agent_count != 0u);
 
     scmi_sys_power_ctx.system_power_notifications =
-        fwk_mm_calloc((size_t)scmi_sys_power_ctx.agent_count, sizeof(fwk_id_t));
+	fwk_mm_calloc((size_t)scmi_sys_power_ctx.agent_count, sizeof(fwk_id_t));
 
     for (i = 0u; i < scmi_sys_power_ctx.agent_count; i++) {
-        scmi_sys_power_ctx.system_power_notifications[i] = FWK_ID_NONE;
+	scmi_sys_power_ctx.system_power_notifications[i] = FWK_ID_NONE;
     }
 
     return FWK_SUCCESS;
@@ -728,42 +728,42 @@ static int scmi_sys_power_bind(fwk_id_t
     int pd_count;
 
     if (round != 0) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     /* Bind to SCMI module */
     status = fwk_module_bind(FWK_ID_MODULE(FWK_MODULE_IDX_SCMI),
-                             FWK_ID_API(FWK_MODULE_IDX_SCMI,
-                                        MOD_SCMI_API_IDX_PROTOCOL),
-                             &scmi_sys_power_ctx.scmi_api);
+			     FWK_ID_API(FWK_MODULE_IDX_SCMI,
+					MOD_SCMI_API_IDX_PROTOCOL),
+			     &scmi_sys_power_ctx.scmi_api);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
 #ifdef BUILD_HAS_MOD_RESOURCE_PERMS
     status = fwk_module_bind(
-        FWK_ID_MODULE(FWK_MODULE_IDX_RESOURCE_PERMS),
-        FWK_ID_API(FWK_MODULE_IDX_RESOURCE_PERMS, MOD_RES_PERM_RESOURCE_PERMS),
-        &scmi_sys_power_ctx.res_perms_api);
+	FWK_ID_MODULE(FWK_MODULE_IDX_RESOURCE_PERMS),
+	FWK_ID_API(FWK_MODULE_IDX_RESOURCE_PERMS, MOD_RES_PERM_RESOURCE_PERMS),
+	&scmi_sys_power_ctx.res_perms_api);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 #endif
 
     /* Bind to POWER DOMAIN module */
     status = fwk_module_bind(fwk_module_id_power_domain,
-        mod_pd_api_id_restricted, &scmi_sys_power_ctx.pd_api);
+	mod_pd_api_id_restricted, &scmi_sys_power_ctx.pd_api);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     pd_count = fwk_module_get_element_count(fwk_module_id_power_domain);
     if (pd_count <= 0) {
-        return FWK_E_SUPPORT;
+	return FWK_E_SUPPORT;
     }
 
     scmi_sys_power_ctx.system_power_domain_id =
-        FWK_ID_ELEMENT(FWK_MODULE_IDX_POWER_DOMAIN, pd_count - 1);
+	FWK_ID_ELEMENT(FWK_MODULE_IDX_POWER_DOMAIN, pd_count - 1);
 
 #ifdef BUILD_HAS_SCMI_NOTIFICATIONS
     /*
@@ -771,14 +771,14 @@ static int scmi_sys_power_bind(fwk_id_t
      * requests.
      */
     if (!fwk_id_is_equal(scmi_sys_power_ctx.config->alarm_id, FWK_ID_NONE)) {
-        status = fwk_module_bind(
-            scmi_sys_power_ctx.config->alarm_id,
-            MOD_TIMER_API_ID_ALARM,
-            &scmi_sys_power_ctx.alarm_api);
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
-        return scmi_sys_power_init_notifications();
+	status = fwk_module_bind(
+	    scmi_sys_power_ctx.config->alarm_id,
+	    MOD_TIMER_API_ID_ALARM,
+	    &scmi_sys_power_ctx.alarm_api);
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
+	return scmi_sys_power_init_notifications();
     }
 #endif
     return FWK_SUCCESS;
@@ -788,7 +788,7 @@ static int scmi_sys_power_process_bind_r
     fwk_id_t _target_id, fwk_id_t api_id, const void **api)
 {
     if (!fwk_id_is_equal(source_id, FWK_ID_MODULE(FWK_MODULE_IDX_SCMI))) {
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 
     *api = &scmi_sys_power_mod_scmi_to_protocol;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_system_power_req/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi_system_power_req/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_system_power_req/CMakeLists.txt
@@ -8,14 +8,14 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(
     ${SCP_MODULE_TARGET}
     PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_scmi_system_power_req.c")
 
 target_link_libraries(${SCP_MODULE_TARGET} PRIVATE module-scmi
-                                                   module-power-domain)
+						   module-power-domain)
 
 if("resource-perms" IN_LIST SCP_MODULES)
     target_link_libraries(${SCP_MODULE_TARGET} PRIVATE module-resource-perms)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_system_power_req/doc/scmi_system_power_req.md
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi_system_power_req/doc/scmi_system_power_req.md
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_system_power_req/doc/scmi_system_power_req.md
@@ -25,23 +25,23 @@ The following sequence diagram demonstra
 involved modules starting from a client sending SCMI System Power commands,
 until it gets the response.
 
-                        Sequence diagram
+			Sequence diagram
     +--------+   +--------------+  +-------------+   +------+     +-----------+
     | Client |   | SCMI Sys Pow |  | Sys Pow Req |   | SCMI |     | Transport |
     +--------+   +--------------+  +-------------+   +------+     +-----------+
-         | state_set()  |               |                  |                |
-         |------------->| state_set()   |                  |                |
-         |              |-------------->|                  |                |
-         |              | RESP PENDING  |                  |                |
-         |              |<--------------|                  |                |
-         |              |               | scmi_send()      |                |
-         |              |               |----------------->|                |
-         |              |               |                  | scmi_send()    |
-         |              |               |                  |--------------->|
-         |              |               |                  | scmi_response()|
-         |              |               |                  |<---------------|
-         |              |               | message_handler()|                |
-         |              |               |<-----------------|                |
+	 | state_set()  |               |                  |                |
+	 |------------->| state_set()   |                  |                |
+	 |              |-------------->|                  |                |
+	 |              | RESP PENDING  |                  |                |
+	 |              |<--------------|                  |                |
+	 |              |               | scmi_send()      |                |
+	 |              |               |----------------->|                |
+	 |              |               |                  | scmi_send()    |
+	 |              |               |                  |--------------->|
+	 |              |               |                  | scmi_response()|
+	 |              |               |                  |<---------------|
+	 |              |               | message_handler()|                |
+	 |              |               |<-----------------|                |
 
 
 ## Configuration
@@ -52,7 +52,7 @@ Power Requester in an MCP which needs to
 The number of protocol_requester_count_max should also be set accordingly
 in the SCMI config.
 
-                            Configuration example
+			    Configuration example
     +-----------------------------------------------------------------------+
     |                                MCP                                    |
     |-----------------------------------------------------------------------|
@@ -66,14 +66,14 @@ in the SCMI config.
     | +-------------+ +--------------+ +----------------+ | +-------------+ |
     |                                                     |                 |
     +-----------------------------------------------------+-----------------+
-                                                          |
-                               +-----------------------+  |
-                               |Primary SCP            |  |
-                               |-----------------------|  |
-                               | +-------------------+ |  |
-                               | | SCMI System Power |<+--+
-                               | |-------------------| |
-                               +-----------------------+
+							  |
+			       +-----------------------+  |
+			       |Primary SCP            |  |
+			       |-----------------------|  |
+			       | +-------------------+ |  |
+			       | | SCMI System Power |<+--+
+			       | |-------------------| |
+			       +-----------------------+
 
 
 ## Limitations
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_system_power_req/include/mod_scmi_system_power_req.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi_system_power_req/include/mod_scmi_system_power_req.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_system_power_req/include/mod_scmi_system_power_req.h
@@ -59,9 +59,9 @@ struct mod_system_power_requester_api {
      * \return Status code representing the result of the operation.
      */
     int (*set_req_state)(
-        bool response_requested,
-        uint32_t state,
-        uint32_t flags);
+	bool response_requested,
+	uint32_t state,
+	uint32_t flags);
 
     /*!
      * \brief Get the state of all the elements of the System Power
@@ -126,7 +126,7 @@ static const fwk_id_t mod_sys_power_req_
  */
 static const fwk_id_t system_power_requester_set_state_request =
     FWK_ID_EVENT_INIT(
-        FWK_MODULE_IDX_SCMI_SYSTEM_POWER_REQ,
-        MOD_SCMI_SPR_EVENT_IDX_SET_STATE);
+	FWK_MODULE_IDX_SCMI_SYSTEM_POWER_REQ,
+	MOD_SCMI_SPR_EVENT_IDX_SET_STATE);
 
 #endif /* MOD_SCMI_SYSTEM_POWER_REQ_H */
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_system_power_req/src/mod_scmi_system_power_req.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi_system_power_req/src/mod_scmi_system_power_req.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_system_power_req/src/mod_scmi_system_power_req.c
@@ -119,13 +119,13 @@ static int (*handler_table[MOD_SCMI_SYS_
     const void *,
     size_t) = {
     [MOD_SCMI_SYS_POWER_REQ_STATE_SET] =
-        scmi_system_power_req_state_set_handler,
+	scmi_system_power_req_state_set_handler,
 };
 
 static const unsigned int
     payload_size_table[MOD_SCMI_SYS_POWER_REQ_COMMAND_COUNT] = {
-        [MOD_SCMI_SYS_POWER_REQ_STATE_SET] =
-            (unsigned int)sizeof(struct scmi_sys_power_req_state_set_a2p),
+	[MOD_SCMI_SYS_POWER_REQ_STATE_SET] =
+	    (unsigned int)sizeof(struct scmi_sys_power_req_state_set_a2p),
     };
 
 static_assert(
@@ -160,15 +160,15 @@ static int scmi_system_power_req_message
     fwk_assert(payload != NULL);
 
     if (message_id >= FWK_ARRAY_SIZE(handler_table)) {
-        return FWK_E_RANGE;
+	return FWK_E_RANGE;
     }
 
     if (payload_size != payload_size_table[message_id]) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     if (handler_table[message_id] == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
     ret_status = handler_table[message_id](service_id, payload, payload_size);
 
@@ -179,8 +179,8 @@ static int scmi_system_power_req_message
 
 static struct mod_scmi_to_protocol_api
     scmi_system_power_req_scmi_to_protocol_api = {
-        .get_scmi_protocol_id = scmi_system_power_req_get_scmi_protocol_id,
-        .message_handler = scmi_system_power_req_message_handler,
+	.get_scmi_protocol_id = scmi_system_power_req_get_scmi_protocol_id,
+	.message_handler = scmi_system_power_req_message_handler,
     };
 
 /*
@@ -198,23 +198,23 @@ static int scmi_system_power_req_state_s
     ret_status = *((const int *)payload);
 
     if (ret_status == SCMI_SUCCESS) {
-        mod_ctx.state = mod_ctx.state_requested;
+	mod_ctx.state = mod_ctx.state_requested;
     }
 
     mod_ctx.state_change_status = ret_status;
 
     if (mod_ctx.response_requested) {
-        struct fwk_event_light req = (struct fwk_event_light){
-            .id = FWK_ID_EVENT(
-                FWK_MODULE_IDX_SCMI_SYSTEM_POWER_REQ,
-                SCMI_SPR_EVENT_IDX_SET_COMPLETE),
-            .target_id = FWK_ID_MODULE(FWK_MODULE_IDX_SCMI_SYSTEM_POWER_REQ),
-        };
-
-        status = fwk_put_event(&req);
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
+	struct fwk_event_light req = (struct fwk_event_light){
+	    .id = FWK_ID_EVENT(
+		FWK_MODULE_IDX_SCMI_SYSTEM_POWER_REQ,
+		SCMI_SPR_EVENT_IDX_SET_COMPLETE),
+	    .target_id = FWK_ID_MODULE(FWK_MODULE_IDX_SCMI_SYSTEM_POWER_REQ),
+	};
+
+	status = fwk_put_event(&req);
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
     }
 
     return ret_status;
@@ -229,15 +229,15 @@ static int scmi_system_power_req_set_sta
 
     struct fwk_event req;
     struct spr_set_state_request *req_params =
-        (struct spr_set_state_request *)(&req.params);
+	(struct spr_set_state_request *)(&req.params);
 
     mod_ctx.state_requested = state;
     mod_ctx.response_requested = response_requested;
 
     req = (struct fwk_event){
-        .id = system_power_requester_set_state_request,
-        .target_id = FWK_ID_MODULE(FWK_MODULE_IDX_SCMI_SYSTEM_POWER_REQ),
-        .response_requested = response_requested,
+	.id = system_power_requester_set_state_request,
+	.target_id = FWK_ID_MODULE(FWK_MODULE_IDX_SCMI_SYSTEM_POWER_REQ),
+	.response_requested = response_requested,
     };
 
     req_params->state = state;
@@ -245,9 +245,9 @@ static int scmi_system_power_req_set_sta
     status = fwk_put_event(&req);
 
     if (status == FWK_SUCCESS) {
-        return FWK_PENDING;
+	return FWK_PENDING;
     } else {
-        return status;
+	return status;
     }
 }
 
@@ -259,35 +259,35 @@ static int process_set_state(fwk_id_t id
     struct scmi_system_power_req_dev_ctx *ctx = &(mod_ctx.dev_ctx_table[0]);
 
     const struct scmi_sys_power_req_state_set_a2p payload = {
-        .flags = flags,
-        .system_state = state,
+	.flags = flags,
+	.system_state = state,
     };
 
     return mod_ctx.scmi_api->scmi_send_message(
-        scmi_message_id,
-        scmi_protocol_id,
-        mod_ctx.token++,
-        ctx->config->service_id,
-        (const void *)&payload,
-        sizeof(payload),
-        true);
+	scmi_message_id,
+	scmi_protocol_id,
+	mod_ctx.token++,
+	ctx->config->service_id,
+	(const void *)&payload,
+	sizeof(payload),
+	true);
 }
 
 static int scmi_system_power_req_get_state(uint32_t *state)
 {
     if (state != NULL) {
-        /* Set State command will cache the response */
-        *state = mod_ctx.state;
+	/* Set State command will cache the response */
+	*state = mod_ctx.state;
 
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
     return FWK_E_PARAM;
 }
 
 static const struct mod_system_power_requester_api
     scmi_system_power_req_driver_api = {
-        .set_req_state = scmi_system_power_req_set_state,
-        .get_req_state = scmi_system_power_req_get_state,
+	.set_req_state = scmi_system_power_req_set_state,
+	.get_req_state = scmi_system_power_req_get_state,
     };
 
 /*
@@ -302,12 +302,12 @@ static int scmi_system_power_req_init(
 
     /* We definitely need elements in this module. */
     if (element_count != 1) {
-        return FWK_E_SUPPORT;
+	return FWK_E_SUPPORT;
     }
 
     mod_ctx.dev_count = element_count;
     mod_ctx.dev_ctx_table =
-        fwk_mm_calloc(element_count, sizeof(mod_ctx.dev_ctx_table[0]));
+	fwk_mm_calloc(element_count, sizeof(mod_ctx.dev_ctx_table[0]));
 
     return FWK_SUCCESS;
 }
@@ -320,17 +320,17 @@ static int scmi_system_power_req_elem_in
     struct scmi_system_power_req_dev_ctx *dev_ctx;
 
     if (fwk_id_get_element_idx(element_id) >= mod_ctx.dev_count) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     dev_ctx = &mod_ctx.dev_ctx_table[fwk_id_get_element_idx(element_id)];
 
     if (data == NULL) {
-        return FWK_E_PANIC;
+	return FWK_E_PANIC;
     }
 
     const struct mod_scmi_system_power_req_dev_config *mod_sys_pow_req_config =
-        (const struct mod_scmi_system_power_req_dev_config *)data;
+	(const struct mod_scmi_system_power_req_dev_config *)data;
 
     dev_ctx->config = mod_sys_pow_req_config;
 
@@ -342,12 +342,12 @@ static int scmi_system_power_req_bind(fw
     int status = FWK_SUCCESS;
 
     if (round == 0) {
-        if (fwk_id_is_type(id, FWK_ID_TYPE_MODULE)) {
-            status = fwk_module_bind(
-                FWK_ID_MODULE(FWK_MODULE_IDX_SCMI),
-                FWK_ID_API(FWK_MODULE_IDX_SCMI, MOD_SCMI_API_IDX_PROTOCOL_REQ),
-                &mod_ctx.scmi_api);
-        }
+	if (fwk_id_is_type(id, FWK_ID_TYPE_MODULE)) {
+	    status = fwk_module_bind(
+		FWK_ID_MODULE(FWK_MODULE_IDX_SCMI),
+		FWK_ID_API(FWK_MODULE_IDX_SCMI, MOD_SCMI_API_IDX_PROTOCOL_REQ),
+		&mod_ctx.scmi_api);
+	}
     }
     return status;
 }
@@ -359,15 +359,15 @@ static int scmi_system_power_req_bind_re
     const void **api)
 {
     if (fwk_id_is_equal(api_id, mod_sys_power_req_scmi_api_id)) {
-        if (!fwk_id_is_equal(
-                fwk_id_build_module_id(requester_id), fwk_module_id_scmi)) {
-            return FWK_E_ACCESS;
-        }
-        *api = &scmi_system_power_req_scmi_to_protocol_api;
+	if (!fwk_id_is_equal(
+		fwk_id_build_module_id(requester_id), fwk_module_id_scmi)) {
+	    return FWK_E_ACCESS;
+	}
+	*api = &scmi_system_power_req_scmi_to_protocol_api;
     } else if (fwk_id_is_equal(api_id, mod_sys_power_req_api_id)) {
-        *api = &scmi_system_power_req_driver_api;
+	*api = &scmi_system_power_req_driver_api;
     } else {
-        return FWK_E_SUPPORT;
+	return FWK_E_SUPPORT;
     }
 
     return FWK_SUCCESS;
@@ -380,36 +380,36 @@ static int scmi_system_power_req_process
     struct fwk_event set_req_event;
     int status;
     struct spr_set_state_request *event_params =
-        (struct spr_set_state_request *)(event->params);
+	(struct spr_set_state_request *)(event->params);
 
     enum scmi_system_power_req_event_idx event_id_type =
-        (enum scmi_system_power_req_event_idx)fwk_id_get_event_idx(event->id);
+	(enum scmi_system_power_req_event_idx)fwk_id_get_event_idx(event->id);
 
     switch (event_id_type) {
     case SCMI_SPR_EVENT_IDX_SET_STATE:
-        if (event->response_requested) {
-            /*
-             * We keep the cookie event of the request that triggers the
-             * state change.
-             */
-            mod_ctx.cookie = event->cookie;
-            resp->is_delayed_response = true;
-        }
-        process_set_state(event->id, event_params->state, event_params->flags);
-        return FWK_SUCCESS;
+	if (event->response_requested) {
+	    /*
+	     * We keep the cookie event of the request that triggers the
+	     * state change.
+	     */
+	    mod_ctx.cookie = event->cookie;
+	    resp->is_delayed_response = true;
+	}
+	process_set_state(event->id, event_params->state, event_params->flags);
+	return FWK_SUCCESS;
 
     case SCMI_SPR_EVENT_IDX_SET_COMPLETE:
-        status = fwk_get_delayed_response(
-            event->target_id, mod_ctx.cookie, &set_req_event);
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
+	status = fwk_get_delayed_response(
+	    event->target_id, mod_ctx.cookie, &set_req_event);
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
 
-        event_params->state = mod_ctx.state_change_status;
-        return fwk_put_event(&set_req_event);
+	event_params->state = mod_ctx.state_change_status;
+	return fwk_put_event(&set_req_event);
 
     default:
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 }
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_system_power_req/test/mocks/Mockmod_scmi_system_power_req_extra.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi_system_power_req/test/mocks/Mockmod_scmi_system_power_req_extra.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_system_power_req/test/mocks/Mockmod_scmi_system_power_req_extra.c
@@ -186,11 +186,11 @@ void CMockExpectParameters_scmi_send_mes
   cmock_call_instance->Expected_scmi_protocol_id = scmi_protocol_id;
   cmock_call_instance->Expected_token = token;
   memcpy((void*)(&cmock_call_instance->Expected_service_id), (void*)(&service_id),
-         sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
+	 sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
   cmock_call_instance->Expected_payload = payload;
   cmock_call_instance->Expected_payload_Depth = payload_Depth;
   memcpy((void*)(&cmock_call_instance->Expected_payload_size), (void*)(&payload_size),
-         sizeof(size_t[sizeof(payload_size) == sizeof(size_t) ? 1 : -1])); /* add size_t to :treat_as_array if this causes an error */
+	 sizeof(size_t[sizeof(payload_size) == sizeof(size_t) ? 1 : -1])); /* add size_t to :treat_as_array if this causes an error */
   cmock_call_instance->Expected_request_ack_by_interrupt = request_ack_by_interrupt;
 }
 
@@ -280,7 +280,7 @@ void CMockExpectParameters_response_mess
 void CMockExpectParameters_response_message_handler(CMOCK_response_message_handler_CALL_INSTANCE* cmock_call_instance, fwk_id_t service_id)
 {
   memcpy((void*)(&cmock_call_instance->Expected_service_id), (void*)(&service_id),
-         sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
+	 sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
 }
 
 void response_message_handler_CMockExpectAnyArgsAndReturn(UNITY_LINE_TYPE cmock_line, int cmock_to_return)
@@ -367,11 +367,11 @@ void CMockExpectParameters_fake_message_
 void CMockExpectParameters_fake_message_handler(CMOCK_fake_message_handler_CALL_INSTANCE* cmock_call_instance, fwk_id_t service_id, const void* payload, int payload_Depth, size_t payload_size)
 {
   memcpy((void*)(&cmock_call_instance->Expected_service_id), (void*)(&service_id),
-         sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
+	 sizeof(fwk_id_t[sizeof(service_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
   cmock_call_instance->Expected_payload = payload;
   cmock_call_instance->Expected_payload_Depth = payload_Depth;
   memcpy((void*)(&cmock_call_instance->Expected_payload_size), (void*)(&payload_size),
-         sizeof(size_t[sizeof(payload_size) == sizeof(size_t) ? 1 : -1])); /* add size_t to :treat_as_array if this causes an error */
+	 sizeof(size_t[sizeof(payload_size) == sizeof(size_t) ? 1 : -1])); /* add size_t to :treat_as_array if this causes an error */
 }
 
 void fake_message_handler_CMockExpectAnyArgsAndReturn(UNITY_LINE_TYPE cmock_line, int cmock_to_return)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_system_power_req/test/mod_scmi_system_power_req_unit_test.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi_system_power_req/test/mod_scmi_system_power_req_unit_test.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_system_power_req/test/mod_scmi_system_power_req_unit_test.c
@@ -33,12 +33,12 @@ enum scp_sys_pow_nums {
 static const struct fwk_element
     system_power_req_element_table[MOD_SCMI_SYS_POWER_REQ_COUNT + 1] = {
     [MOD_SCMI_SYS_POWER_REQ_IDX_0] = {
-        .name = "Fake sys power",
-        .data = &(const struct mod_scmi_system_power_req_dev_config) {
-            .service_id = FWK_ID_ELEMENT_INIT(
-                FWK_MODULE_IDX_SCMI,
-                0),
-        },
+	.name = "Fake sys power",
+	.data = &(const struct mod_scmi_system_power_req_dev_config) {
+	    .service_id = FWK_ID_ELEMENT_INIT(
+		FWK_MODULE_IDX_SCMI,
+		0),
+	},
     },
     [MOD_SCMI_SYS_POWER_REQ_COUNT] = { 0 },
 };
@@ -65,13 +65,13 @@ void setUp(void)
 {
     memset(&mod_ctx, 0, sizeof(mod_ctx));
     memset(
-        &dev_ctx[MOD_SCMI_SYS_POWER_REQ_IDX_0],
-        0,
-        sizeof(struct scmi_system_power_req_dev_ctx));
+	&dev_ctx[MOD_SCMI_SYS_POWER_REQ_IDX_0],
+	0,
+	sizeof(struct scmi_system_power_req_dev_ctx));
     dev_ctx[MOD_SCMI_SYS_POWER_REQ_IDX_0].config =
-        (const struct mod_scmi_system_power_req_dev_config *)
-            system_power_req_element_table[MOD_SCMI_SYS_POWER_REQ_IDX_0]
-                .data;
+	(const struct mod_scmi_system_power_req_dev_config *)
+	    system_power_req_element_table[MOD_SCMI_SYS_POWER_REQ_IDX_0]
+		.data;
 
     mod_ctx.dev_ctx_table = dev_ctx;
     mod_ctx.dev_count = MOD_SCMI_SYS_POWER_REQ_COUNT;
@@ -94,18 +94,18 @@ void test_function_scmi_system_power_req
     memset(&mod_ctx, 0, sizeof(mod_ctx));
 
     status = scmi_system_power_req_init(
-        fwk_module_id_scmi_system_power_req, 0, NULL);
+	fwk_module_id_scmi_system_power_req, 0, NULL);
     TEST_ASSERT_EQUAL(status, FWK_E_SUPPORT);
 
     fwk_mm_calloc_ExpectAndReturn(
-        MOD_SCMI_SYS_POWER_REQ_COUNT,
-        sizeof(struct scmi_system_power_req_dev_ctx),
-        (void *)dev_ctx);
+	MOD_SCMI_SYS_POWER_REQ_COUNT,
+	sizeof(struct scmi_system_power_req_dev_ctx),
+	(void *)dev_ctx);
 
     status = scmi_system_power_req_init(
-        fwk_module_id_scmi_system_power_req,
-        MOD_SCMI_SYS_POWER_REQ_COUNT,
-        NULL);
+	fwk_module_id_scmi_system_power_req,
+	MOD_SCMI_SYS_POWER_REQ_COUNT,
+	NULL);
     TEST_ASSERT_EQUAL(status, FWK_SUCCESS);
     TEST_ASSERT_EQUAL(mod_ctx.dev_count, MOD_SCMI_SYS_POWER_REQ_COUNT);
     TEST_ASSERT_EQUAL(mod_ctx.dev_ctx_table, dev_ctx);
@@ -115,38 +115,38 @@ void test_function_scmi_system_power_req
 {
     int status;
     fwk_id_t element_id = FWK_ID_ELEMENT(
-        FWK_MODULE_IDX_SCMI_SYSTEM_POWER_REQ, MOD_SCMI_SYS_POWER_REQ_IDX_0);
+	FWK_MODULE_IDX_SCMI_SYSTEM_POWER_REQ, MOD_SCMI_SYS_POWER_REQ_IDX_0);
 
     /* Clear element context to ensure it is properly initialized */
     memset(
-        &dev_ctx[MOD_SCMI_SYS_POWER_REQ_IDX_0],
-        0,
-        sizeof(struct scmi_system_power_req_dev_ctx));
+	&dev_ctx[MOD_SCMI_SYS_POWER_REQ_IDX_0],
+	0,
+	sizeof(struct scmi_system_power_req_dev_ctx));
 
     fwk_id_get_element_idx_ExpectAndReturn(
-        element_id, MOD_SCMI_SYS_POWER_REQ_IDX_0);
+	element_id, MOD_SCMI_SYS_POWER_REQ_IDX_0);
     fwk_id_get_element_idx_ExpectAndReturn(
-        element_id, MOD_SCMI_SYS_POWER_REQ_IDX_0);
+	element_id, MOD_SCMI_SYS_POWER_REQ_IDX_0);
 
     status = scmi_system_power_req_elem_init(
-        element_id,
-        0,
-        system_power_req_element_table[MOD_SCMI_SYS_POWER_REQ_IDX_0].data);
+	element_id,
+	0,
+	system_power_req_element_table[MOD_SCMI_SYS_POWER_REQ_IDX_0].data);
     TEST_ASSERT_EQUAL(status, FWK_SUCCESS);
 
     fwk_id_get_element_idx_ExpectAndReturn(
-        element_id, MOD_SCMI_SYS_POWER_REQ_COUNT);
+	element_id, MOD_SCMI_SYS_POWER_REQ_COUNT);
 
     status = scmi_system_power_req_elem_init(
-        element_id,
-        0,
-        system_power_req_element_table[MOD_SCMI_SYS_POWER_REQ_IDX_0].data);
+	element_id,
+	0,
+	system_power_req_element_table[MOD_SCMI_SYS_POWER_REQ_IDX_0].data);
     TEST_ASSERT_EQUAL(status, FWK_E_PARAM);
 
     fwk_id_get_element_idx_ExpectAndReturn(
-        element_id, MOD_SCMI_SYS_POWER_REQ_IDX_0);
+	element_id, MOD_SCMI_SYS_POWER_REQ_IDX_0);
     fwk_id_get_element_idx_ExpectAndReturn(
-        element_id, MOD_SCMI_SYS_POWER_REQ_IDX_0);
+	element_id, MOD_SCMI_SYS_POWER_REQ_IDX_0);
 
     status = scmi_system_power_req_elem_init(element_id, 0, NULL);
     TEST_ASSERT_EQUAL(status, FWK_E_PANIC);
@@ -157,14 +157,14 @@ void test_function_scmi_system_power_req
     int status;
 
     fwk_id_t element_id = FWK_ID_ELEMENT_INIT(
-        FWK_MODULE_IDX_SCMI_SYSTEM_POWER_REQ, MOD_SCMI_SYS_POWER_REQ_IDX_0);
+	FWK_MODULE_IDX_SCMI_SYSTEM_POWER_REQ, MOD_SCMI_SYS_POWER_REQ_IDX_0);
 
     fwk_id_is_type_ExpectAnyArgsAndReturn(true);
     fwk_module_bind_ExpectAndReturn(
-        FWK_ID_MODULE(FWK_MODULE_IDX_SCMI),
-        FWK_ID_API(FWK_MODULE_IDX_SCMI, MOD_SCMI_API_IDX_PROTOCOL_REQ),
-        &mod_ctx.scmi_api,
-        FWK_SUCCESS);
+	FWK_ID_MODULE(FWK_MODULE_IDX_SCMI),
+	FWK_ID_API(FWK_MODULE_IDX_SCMI, MOD_SCMI_API_IDX_PROTOCOL_REQ),
+	&mod_ctx.scmi_api,
+	FWK_SUCCESS);
 
     status = scmi_system_power_req_bind(fwk_module_id_scmi_system_power_req, 0);
     TEST_ASSERT_EQUAL(status, FWK_SUCCESS);
@@ -191,10 +191,10 @@ void test_function_scmi_system_power_req
     fwk_id_build_module_id_ExpectAnyArgsAndReturn(invalid_id);
 
     status = scmi_system_power_req_bind_request(
-        invalid_id,
-        target_id,
-        mod_sys_power_req_scmi_api_id,
-        (const void **)&scmi_api);
+	invalid_id,
+	target_id,
+	mod_sys_power_req_scmi_api_id,
+	(const void **)&scmi_api);
     TEST_ASSERT_EQUAL(status, FWK_E_ACCESS);
 
     fwk_id_is_equal_ExpectAnyArgsAndReturn(true);
@@ -202,22 +202,22 @@ void test_function_scmi_system_power_req
     fwk_id_build_module_id_ExpectAnyArgsAndReturn(fwk_module_id_scmi);
 
     status = scmi_system_power_req_bind_request(
-        fwk_module_id_scmi,
-        target_id,
-        mod_sys_power_req_scmi_api_id,
-        (const void **)&scmi_api);
+	fwk_module_id_scmi,
+	target_id,
+	mod_sys_power_req_scmi_api_id,
+	(const void **)&scmi_api);
     TEST_ASSERT_EQUAL(status, FWK_SUCCESS);
     TEST_ASSERT_EQUAL_PTR(
-        scmi_api, &scmi_system_power_req_scmi_to_protocol_api);
+	scmi_api, &scmi_system_power_req_scmi_to_protocol_api);
 
     fwk_id_is_equal_ExpectAnyArgsAndReturn(false);
     fwk_id_is_equal_ExpectAnyArgsAndReturn(true);
 
     status = scmi_system_power_req_bind_request(
-        fwk_module_id_scmi_system_power,
-        target_id,
-        mod_sys_power_req_api_id,
-        (const void **)&sys_api);
+	fwk_module_id_scmi_system_power,
+	target_id,
+	mod_sys_power_req_api_id,
+	(const void **)&sys_api);
     TEST_ASSERT_EQUAL(status, FWK_SUCCESS);
     TEST_ASSERT_EQUAL_PTR(sys_api, &scmi_system_power_req_driver_api);
 
@@ -225,7 +225,7 @@ void test_function_scmi_system_power_req
     fwk_id_is_equal_ExpectAnyArgsAndReturn(false);
 
     status = scmi_system_power_req_bind_request(
-        invalid_id, invalid_id, invalid_id, (const void **)NULL);
+	invalid_id, invalid_id, invalid_id, (const void **)NULL);
     TEST_ASSERT_EQUAL(status, FWK_E_SUPPORT);
 }
 
@@ -268,23 +268,23 @@ void test_function_scmi_system_power_req
     uint8_t scmi_message_id = (uint8_t)MOD_SCMI_SYS_POWER_REQ_STATE_SET;
 
     const struct scmi_sys_power_req_state_set_a2p payload = {
-        .flags = flags,
-        .system_state = state,
+	.flags = flags,
+	.system_state = state,
     };
 
     scmi_send_message_ExpectWithArrayAndReturn(
-        scmi_message_id,
-        scmi_protocol_id,
-        0,
-        dev_ctx[MOD_SCMI_SYS_POWER_REQ_IDX_0].config->service_id,
-        (const void *)&payload,
-        sizeof(payload),
-        sizeof(payload),
-        true,
-        FWK_SUCCESS);
+	scmi_message_id,
+	scmi_protocol_id,
+	0,
+	dev_ctx[MOD_SCMI_SYS_POWER_REQ_IDX_0].config->service_id,
+	(const void *)&payload,
+	sizeof(payload),
+	sizeof(payload),
+	true,
+	FWK_SUCCESS);
 
     status =
-        process_set_state(fwk_module_id_scmi_system_power_req, state, flags);
+	process_set_state(fwk_module_id_scmi_system_power_req, state, flags);
     TEST_ASSERT_EQUAL(status, FWK_SUCCESS);
 }
 
@@ -297,9 +297,9 @@ void test_scmi_system_power_req_state_se
     payload = SCMI_HARDWARE_ERROR;
 
     status = scmi_system_power_req_state_set_handler(
-        dev_ctx[MOD_SCMI_SYS_POWER_REQ_IDX_0].config->service_id,
-        &payload,
-        sizeof(payload));
+	dev_ctx[MOD_SCMI_SYS_POWER_REQ_IDX_0].config->service_id,
+	&payload,
+	sizeof(payload));
     TEST_ASSERT_NOT_EQUAL(mod_ctx.state, mod_ctx.state_requested);
     TEST_ASSERT_EQUAL(mod_ctx.state, MOD_PD_STATE_ON);
     TEST_ASSERT_EQUAL(mod_ctx.state_requested, MOD_PD_STATE_OFF);
@@ -308,9 +308,9 @@ void test_scmi_system_power_req_state_se
     payload = SCMI_SUCCESS;
 
     status = scmi_system_power_req_state_set_handler(
-        dev_ctx[MOD_SCMI_SYS_POWER_REQ_IDX_0].config->service_id,
-        &payload,
-        sizeof(payload));
+	dev_ctx[MOD_SCMI_SYS_POWER_REQ_IDX_0].config->service_id,
+	&payload,
+	sizeof(payload));
     TEST_ASSERT_EQUAL(mod_ctx.state, mod_ctx.state_requested);
     TEST_ASSERT_EQUAL(mod_ctx.state, MOD_PD_STATE_OFF);
     TEST_ASSERT_EQUAL(status, SCMI_SUCCESS);
@@ -322,10 +322,10 @@ void test_scmi_system_power_req_get_scmi
     fwk_id_t protocol_id;
     uint8_t scmi_protocol_id;
     status = scmi_system_power_req_get_scmi_protocol_id(
-        protocol_id, &scmi_protocol_id);
+	protocol_id, &scmi_protocol_id);
     TEST_ASSERT_EQUAL(status, FWK_SUCCESS);
     TEST_ASSERT_EQUAL(
-        scmi_protocol_id, (uint8_t)MOD_SCMI_PROTOCOL_ID_SYS_POWER);
+	scmi_protocol_id, (uint8_t)MOD_SCMI_PROTOCOL_ID_SYS_POWER);
 }
 
 void test_scmi_system_power_req_message_handler(void)
@@ -336,38 +336,38 @@ void test_scmi_system_power_req_message_
     uint8_t payload[sizeof(struct scmi_sys_power_req_state_set_a2p)] = { 0 };
 
     expected_service_id =
-        dev_ctx[MOD_SCMI_SYS_POWER_REQ_IDX_0].config->service_id;
+	dev_ctx[MOD_SCMI_SYS_POWER_REQ_IDX_0].config->service_id;
 
     status = scmi_system_power_req_message_handler(
-        protocol_id,
-        expected_service_id,
-        (uint32_t *)payload,
-        sizeof(payload),
-        sizeof(handler_table));
+	protocol_id,
+	expected_service_id,
+	(uint32_t *)payload,
+	sizeof(payload),
+	sizeof(handler_table));
     TEST_ASSERT_EQUAL(status, FWK_E_RANGE);
 
     status = scmi_system_power_req_message_handler(
-        protocol_id,
-        expected_service_id,
-        (uint32_t *)payload,
-        sizeof(payload) + 1,
-        (uint8_t)MOD_SCMI_SYS_POWER_REQ_STATE_SET);
+	protocol_id,
+	expected_service_id,
+	(uint32_t *)payload,
+	sizeof(payload) + 1,
+	(uint8_t)MOD_SCMI_SYS_POWER_REQ_STATE_SET);
     TEST_ASSERT_EQUAL(status, FWK_E_PARAM);
 
     fake_message_handler_ExpectWithArrayAndReturn(
-        expected_service_id,
-        (uint32_t *)payload,
-        sizeof(payload),
-        sizeof(payload),
-        FWK_SUCCESS);
+	expected_service_id,
+	(uint32_t *)payload,
+	sizeof(payload),
+	sizeof(payload),
+	FWK_SUCCESS);
     response_message_handler_ExpectAndReturn(expected_service_id, FWK_SUCCESS);
 
     status = scmi_system_power_req_message_handler(
-        protocol_id,
-        expected_service_id,
-        (uint32_t *)payload,
-        sizeof(payload),
-        (uint8_t)MOD_SCMI_SYS_POWER_REQ_STATE_SET);
+	protocol_id,
+	expected_service_id,
+	(uint32_t *)payload,
+	sizeof(payload),
+	(uint8_t)MOD_SCMI_SYS_POWER_REQ_STATE_SET);
     TEST_ASSERT_EQUAL(status, FWK_SUCCESS);
 }
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_voltage_domain/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi_voltage_domain/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_voltage_domain/CMakeLists.txt
@@ -8,13 +8,13 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_scmi_voltage_domain.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_scmi_voltage_domain.c")
 
 target_link_libraries(${SCP_MODULE_TARGET} PRIVATE module-voltage-domain
-                                                   module-scmi)
+						   module-scmi)
 
 if("resource-perms" IN_LIST SCP_MODULES)
     target_link_libraries(${SCP_MODULE_TARGET} PRIVATE module-resource-perms)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_voltage_domain/include/internal/scmi_voltage_domain.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi_voltage_domain/include/internal/scmi_voltage_domain.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_voltage_domain/include/internal/scmi_voltage_domain.h
@@ -133,12 +133,12 @@ struct scmi_voltd_config_get_p2a {
 
 #define SCMI_VOLTD_NUM_LEVELS_FLAGS(_count, _format, _remaining) \
     ( \
-        (((_count) << SCMI_VOLTD_DESCRIBE_LEVELS_COUNT_POS) & \
-         SCMI_VOLTD_DESCRIBE_LEVELS_COUNT_MASK) | \
-        (((_remaining) << SCMI_VOLTD_DESCRIBE_LEVELS_REMAINING_POS) & \
-         SCMI_VOLTD_DESCRIBE_LEVELS_REMAINING_MASK) | \
-        (((_format) << SCMI_VOLTD_DESCRIBE_LEVELS_FORMAT_POS) & \
-         SCMI_VOLTD_DESCRIBE_LEVELS_FORMAT_MASK) \
+	(((_count) << SCMI_VOLTD_DESCRIBE_LEVELS_COUNT_POS) & \
+	 SCMI_VOLTD_DESCRIBE_LEVELS_COUNT_MASK) | \
+	(((_remaining) << SCMI_VOLTD_DESCRIBE_LEVELS_REMAINING_POS) & \
+	 SCMI_VOLTD_DESCRIBE_LEVELS_REMAINING_MASK) | \
+	(((_format) << SCMI_VOLTD_DESCRIBE_LEVELS_FORMAT_POS) & \
+	 SCMI_VOLTD_DESCRIBE_LEVELS_FORMAT_MASK) \
     )
 
 #define SCMI_VOLTD_LEVEL_RANGE_FLAGS \
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_voltage_domain/src/mod_scmi_voltage_domain.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/scmi_voltage_domain/src/mod_scmi_voltage_domain.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/scmi_voltage_domain/src/mod_scmi_voltage_domain.c
@@ -99,7 +99,7 @@ static int (*const handler_table[])(fwk_
     [MOD_SCMI_PROTOCOL_VERSION] = scmi_voltd_protocol_version_handler,
     [MOD_SCMI_PROTOCOL_ATTRIBUTES] = scmi_voltd_protocol_attributes_handler,
     [MOD_SCMI_PROTOCOL_MESSAGE_ATTRIBUTES] =
-        scmi_voltd_protocol_message_attributes_handler,
+	scmi_voltd_protocol_message_attributes_handler,
     [MOD_SCMI_VOLTD_DOMAIN_ATTRIBUTES] = scmi_voltd_domain_attributes_handler,
     [MOD_SCMI_VOLTD_CONFIG_GET] = scmi_voltd_config_get_handler,
     [MOD_SCMI_VOLTD_CONFIG_SET] = scmi_voltd_config_set_handler,
@@ -112,29 +112,29 @@ static const unsigned int payload_size_t
     [MOD_SCMI_PROTOCOL_VERSION] = 0,
     [MOD_SCMI_PROTOCOL_ATTRIBUTES] = 0,
     [MOD_SCMI_PROTOCOL_MESSAGE_ATTRIBUTES] =
-        sizeof(struct scmi_protocol_message_attributes_a2p),
+	sizeof(struct scmi_protocol_message_attributes_a2p),
     [MOD_SCMI_VOLTD_DOMAIN_ATTRIBUTES] = sizeof(struct scmi_voltd_attributes_a2p),
     [MOD_SCMI_VOLTD_CONFIG_GET] = sizeof(struct scmi_voltd_config_get_a2p),
     [MOD_SCMI_VOLTD_CONFIG_SET] = sizeof(struct scmi_voltd_config_set_a2p),
     [MOD_SCMI_VOLTD_LEVEL_GET] = sizeof(struct scmi_voltd_level_get_a2p),
     [MOD_SCMI_VOLTD_LEVEL_SET] = sizeof(struct scmi_voltd_level_set_a2p),
     [MOD_SCMI_VOLTD_DESCRIBE_LEVELS] =
-        sizeof(struct scmi_voltd_describe_levels_a2p),
+	sizeof(struct scmi_voltd_describe_levels_a2p),
 };
 
 /* Get SCMI VOLD agent device from incoming SCMI service ID */
 static int get_agent_entry(fwk_id_t service_id,
-                           const struct mod_scmi_voltd_agent **agent)
+			   const struct mod_scmi_voltd_agent **agent)
 {
     int status;
     unsigned int agent_idx;
 
     status = scmi_voltd_ctx.scmi_api->get_agent_id(service_id, &agent_idx);
     if (status != FWK_SUCCESS)
-        return status;
+	return status;
 
     if (agent_idx >= scmi_voltd_ctx.config->agent_count)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     *agent = scmi_voltd_ctx.agent_table + agent_idx;
 
@@ -143,8 +143,8 @@ static int get_agent_entry(fwk_id_t serv
 
 /* Get SCMI VOLD device from incoming SCMI service ID and element ID */
 static int get_device(fwk_id_t service_id, unsigned int elt_idx,
-                      const struct mod_scmi_voltd_device **out_device,
-                      const struct mod_scmi_voltd_agent **out_agent)
+		      const struct mod_scmi_voltd_device **out_device,
+		      const struct mod_scmi_voltd_agent **out_agent)
 {
     int status = 0;
     const struct mod_scmi_voltd_device *device = NULL;
@@ -152,19 +152,19 @@ static int get_device(fwk_id_t service_i
 
     status = get_agent_entry(service_id, &agent);
     if (status != FWK_SUCCESS)
-        return status;
+	return status;
 
     if (elt_idx >= agent->domain_count)
-        return FWK_E_RANGE;
+	return FWK_E_RANGE;
 
     device = &agent->device_table[elt_idx];
     fwk_module_is_valid_element_id(device->element_id);
 
     if (out_device)
-        *out_device = device;
+	*out_device = device;
 
     if (out_agent)
-        *out_agent = agent;
+	*out_agent = agent;
 
     return FWK_SUCCESS;
 }
@@ -182,7 +182,7 @@ static unsigned int get_domain_id(const
      * unnecessary code.
      */
     const struct scmi_voltd_attributes_a2p *parameters =
-        (const struct scmi_voltd_attributes_a2p *)(void *)payload;
+	(const struct scmi_voltd_attributes_a2p *)(void *)payload;
 
     return parameters->domain_id;
 }
@@ -199,18 +199,18 @@ static int scmi_voltd_permissions_handle
 
     status = scmi_voltd_ctx.scmi_api->get_agent_id(service_id, &agent_id);
     if (status != FWK_SUCCESS)
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
 
     if (message_id < 3)
-        perms = scmi_voltd_ctx.res_perms_api->agent_has_protocol_permission(
-            agent_id, MOD_SCMI_PROTOCOL_ID_VOLTAGE_DOMAIN);
+	perms = scmi_voltd_ctx.res_perms_api->agent_has_protocol_permission(
+	    agent_id, MOD_SCMI_PROTOCOL_ID_VOLTAGE_DOMAIN);
     else
-        perms = scmi_voltd_ctx.res_perms_api->agent_has_resource_permission(
-            agent_id, MOD_SCMI_PROTOCOL_ID_VOLTAGE_DOMAIN, message_id,
-            get_domain_id(payload));
+	perms = scmi_voltd_ctx.res_perms_api->agent_has_resource_permission(
+	    agent_id, MOD_SCMI_PROTOCOL_ID_VOLTAGE_DOMAIN, message_id,
+	    get_domain_id(payload));
 
     if (perms == MOD_RES_PERMS_ACCESS_ALLOWED)
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 
     return FWK_E_ACCESS;
 }
@@ -223,12 +223,12 @@ static int scmi_voltd_protocol_version_h
     const uint32_t *payload)
 {
     struct scmi_protocol_version_p2a outmsg = {
-        .status = SCMI_SUCCESS,
-        .version = SCMI_PROTOCOL_VERSION_VOLTD,
+	.status = SCMI_SUCCESS,
+	.version = SCMI_PROTOCOL_VERSION_VOLTD,
     };
 
     return scmi_voltd_ctx.scmi_api->respond(
-        service_id, &outmsg, sizeof(outmsg));
+	service_id, &outmsg, sizeof(outmsg));
 }
 
 /*
@@ -239,15 +239,15 @@ static int scmi_voltd_protocol_attribute
 {
     const struct mod_scmi_voltd_agent *agent = NULL;
     struct scmi_protocol_attributes_p2a outmsg = {
-        .status = SCMI_GENERIC_ERROR,
+	.status = SCMI_GENERIC_ERROR,
     };
     size_t outmsg_size = sizeof(outmsg.status);
 
     if (get_agent_entry(service_id, &agent) != FWK_SUCCESS)
-        goto exit;
+	goto exit;
 
     fwk_assert(
-        agent->domain_count < SCMI_VOLTD_PROTOCOL_ATTRIBUTES_VOLTD_COUNT_MAX);
+	agent->domain_count < SCMI_VOLTD_PROTOCOL_ATTRIBUTES_VOLTD_COUNT_MAX);
     outmsg.attributes = SCMI_VOLTD_PROTOCOL_ATTRIBUTES(agent->domain_count);
     outmsg.status = SCMI_SUCCESS;
     outmsg_size = sizeof(outmsg);
@@ -265,7 +265,7 @@ static int scmi_voltd_protocol_message_a
     const struct scmi_protocol_message_attributes_a2p *inmsg;
     unsigned int msg_id;
     struct scmi_protocol_message_attributes_p2a outmsg = {
-        .status = SCMI_NOT_FOUND,
+	.status = SCMI_NOT_FOUND,
     };
     size_t outmsg_size = sizeof(outmsg.status);
 
@@ -273,8 +273,8 @@ static int scmi_voltd_protocol_message_a
     msg_id = inmsg->message_id;
 
     if ((msg_id < FWK_ARRAY_SIZE(handler_table)) && handler_table[msg_id]) {
-        outmsg.status = SCMI_SUCCESS;
-        outmsg_size = sizeof(outmsg);
+	outmsg.status = SCMI_SUCCESS;
+	outmsg_size = sizeof(outmsg);
     }
 
     return scmi_voltd_ctx.scmi_api->respond(service_id, &outmsg, outmsg_size);
@@ -284,12 +284,12 @@ static int scmi_voltd_protocol_message_a
  * Voltage Domain Attributes
  */
 static int scmi_voltd_domain_attributes_handler(fwk_id_t service_id,
-                                                const uint32_t *payload)
+						const uint32_t *payload)
 {
     const struct mod_scmi_voltd_device *device = NULL;
     const struct scmi_voltd_attributes_a2p *inmsg = NULL;
     struct scmi_voltd_attributes_p2a outmsg = {
-        .status = SCMI_NOT_FOUND,
+	.status = SCMI_NOT_FOUND,
     };
     size_t outmsg_size = sizeof(outmsg.status);
     int status = 0;
@@ -298,12 +298,12 @@ static int scmi_voltd_domain_attributes_
 
     status = get_device(service_id, inmsg->domain_id, &device, NULL);
     if (status != FWK_SUCCESS)
-        goto exit;
+	goto exit;
 
     strncpy(
-        outmsg.name,
-        fwk_module_get_element_name(device->element_id),
-        sizeof(outmsg.name) - 1);
+	outmsg.name,
+	fwk_module_get_element_name(device->element_id),
+	sizeof(outmsg.name) - 1);
 
     outmsg.status = SCMI_SUCCESS;
     outmsg_size = sizeof(outmsg);
@@ -322,7 +322,7 @@ static int scmi_voltd_config_get_handler
     const struct mod_scmi_voltd_device *device = NULL;
     const struct scmi_voltd_config_get_a2p *inmsg = NULL;
     struct scmi_voltd_config_get_p2a outmsg = {
-        .status = SCMI_GENERIC_ERROR,
+	.status = SCMI_GENERIC_ERROR,
     };
     size_t outmsg_size = sizeof(outmsg.status);
 
@@ -335,44 +335,44 @@ static int scmi_voltd_config_get_handler
 
     status = get_device(service_id, inmsg->domain_id, &device, NULL);
     if (status == FWK_SUCCESS) {
-        status = scmi_voltd_ctx.voltd_api->get_config(
-            device->element_id, &mode_type, &mode_id);
+	status = scmi_voltd_ctx.voltd_api->get_config(
+	    device->element_id, &mode_type, &mode_id);
     }
 
     if (status == FWK_SUCCESS) {
-        voltd_mode_type = (enum mod_voltd_mode_type)mode_type;
-        switch (voltd_mode_type) {
-        case MOD_VOLTD_MODE_TYPE_ARCH: {
-            voltd_mode_id = (enum mod_voltd_mode_id)mode_id;
-            switch (voltd_mode_id) {
-            case MOD_VOLTD_MODE_ID_OFF:
-                outmsg.config = (uint32_t)SCMI_VOLTD_MODE_ID_OFF;
-                break;
-            case MOD_VOLTD_MODE_ID_ON:
-                outmsg.config = (uint32_t)SCMI_VOLTD_MODE_ID_ON;
-                break;
-            default:
-                status = FWK_E_STATE;
-                break;
-            }
-            break;
-        }
-        case MOD_VOLTD_MODE_TYPE_IMPL:
-            outmsg.config = SCMI_VOLTD_CONFIG_MODE_TYPE_BIT | (uint32_t)mode_id;
-            break;
-        default:
-            status = FWK_E_STATE;
-            break;
-        }
+	voltd_mode_type = (enum mod_voltd_mode_type)mode_type;
+	switch (voltd_mode_type) {
+	case MOD_VOLTD_MODE_TYPE_ARCH: {
+	    voltd_mode_id = (enum mod_voltd_mode_id)mode_id;
+	    switch (voltd_mode_id) {
+	    case MOD_VOLTD_MODE_ID_OFF:
+		outmsg.config = (uint32_t)SCMI_VOLTD_MODE_ID_OFF;
+		break;
+	    case MOD_VOLTD_MODE_ID_ON:
+		outmsg.config = (uint32_t)SCMI_VOLTD_MODE_ID_ON;
+		break;
+	    default:
+		status = FWK_E_STATE;
+		break;
+	    }
+	    break;
+	}
+	case MOD_VOLTD_MODE_TYPE_IMPL:
+	    outmsg.config = SCMI_VOLTD_CONFIG_MODE_TYPE_BIT | (uint32_t)mode_id;
+	    break;
+	default:
+	    status = FWK_E_STATE;
+	    break;
+	}
     }
 
     if (status == FWK_SUCCESS) {
-        outmsg.status = SCMI_SUCCESS;
-        outmsg_size = sizeof(outmsg);
+	outmsg.status = SCMI_SUCCESS;
+	outmsg_size = sizeof(outmsg);
     } else if (status == FWK_E_STATE) {
-        outmsg.status = SCMI_GENERIC_ERROR;
+	outmsg.status = SCMI_GENERIC_ERROR;
     } else {
-        outmsg.status = SCMI_NOT_FOUND;
+	outmsg.status = SCMI_NOT_FOUND;
     }
 
     return scmi_voltd_ctx.scmi_api->respond(service_id, &outmsg, outmsg_size);
@@ -385,7 +385,7 @@ static int scmi_voltd_config_set_handler
     const struct mod_scmi_voltd_device *device = NULL;
     const struct scmi_voltd_config_set_a2p *inmsg = NULL;
     struct scmi_voltd_config_set_p2a outmsg = {
-        .status = SCMI_GENERIC_ERROR,
+	.status = SCMI_GENERIC_ERROR,
     };
     size_t outmsg_size = sizeof(outmsg.status);
     uint8_t mode_id;
@@ -397,49 +397,49 @@ static int scmi_voltd_config_set_handler
 
     status = get_device(service_id, inmsg->domain_id, &device, NULL);
     if (status == FWK_SUCCESS) {
-        scmi_mode_type = (enum scmi_voltd_mode_type)(
-            inmsg->config & SCMI_VOLTD_CONFIG_MODE_TYPE_MASK);
-        mode_id = (uint8_t)(inmsg->config & SCMI_VOLTD_CONFIG_MODE_ID_MASK);
-
-        switch (scmi_mode_type) {
-        case SCMI_VOLTD_MODE_TYPE_ARCH: {
-            mode_type = (uint8_t)MOD_VOLTD_MODE_TYPE_ARCH;
-            scmi_mode_id = (enum scmi_voltd_mode_id)(mode_id);
-
-            switch (scmi_mode_id) {
-            case SCMI_VOLTD_MODE_ID_OFF:
-                mode_id = (uint8_t)MOD_VOLTD_MODE_ID_OFF;
-                break;
-            case SCMI_VOLTD_MODE_ID_ON:
-                mode_id = (uint8_t)MOD_VOLTD_MODE_ID_ON;
-                break;
-            default:
-                status = FWK_E_PARAM;
-                break;
-            }
-            break;
-        }
-        case SCMI_VOLTD_MODE_TYPE_IMPL:
-            mode_type = (uint8_t)MOD_VOLTD_MODE_TYPE_IMPL;
-            break;
-        default:
-            status = FWK_E_PARAM;
-            break;
-        }
-
-        if (status == FWK_SUCCESS) {
-            status = scmi_voltd_ctx.voltd_api->set_config(
-                device->element_id, mode_type, mode_id);
-        }
-
-        if (status == FWK_SUCCESS) {
-            outmsg.status = SCMI_SUCCESS;
-        } else {
-            outmsg.status = SCMI_INVALID_PARAMETERS;
-        }
+	scmi_mode_type = (enum scmi_voltd_mode_type)(
+	    inmsg->config & SCMI_VOLTD_CONFIG_MODE_TYPE_MASK);
+	mode_id = (uint8_t)(inmsg->config & SCMI_VOLTD_CONFIG_MODE_ID_MASK);
+
+	switch (scmi_mode_type) {
+	case SCMI_VOLTD_MODE_TYPE_ARCH: {
+	    mode_type = (uint8_t)MOD_VOLTD_MODE_TYPE_ARCH;
+	    scmi_mode_id = (enum scmi_voltd_mode_id)(mode_id);
+
+	    switch (scmi_mode_id) {
+	    case SCMI_VOLTD_MODE_ID_OFF:
+		mode_id = (uint8_t)MOD_VOLTD_MODE_ID_OFF;
+		break;
+	    case SCMI_VOLTD_MODE_ID_ON:
+		mode_id = (uint8_t)MOD_VOLTD_MODE_ID_ON;
+		break;
+	    default:
+		status = FWK_E_PARAM;
+		break;
+	    }
+	    break;
+	}
+	case SCMI_VOLTD_MODE_TYPE_IMPL:
+	    mode_type = (uint8_t)MOD_VOLTD_MODE_TYPE_IMPL;
+	    break;
+	default:
+	    status = FWK_E_PARAM;
+	    break;
+	}
+
+	if (status == FWK_SUCCESS) {
+	    status = scmi_voltd_ctx.voltd_api->set_config(
+		device->element_id, mode_type, mode_id);
+	}
+
+	if (status == FWK_SUCCESS) {
+	    outmsg.status = SCMI_SUCCESS;
+	} else {
+	    outmsg.status = SCMI_INVALID_PARAMETERS;
+	}
 
     } else {
-        outmsg.status = SCMI_NOT_FOUND;
+	outmsg.status = SCMI_NOT_FOUND;
     }
 
     return scmi_voltd_ctx.scmi_api->respond(service_id, &outmsg, outmsg_size);
@@ -449,13 +449,13 @@ static int scmi_voltd_config_set_handler
  * Voltage domain voltage level
  */
 static int scmi_voltd_level_get_handler(fwk_id_t service_id,
-                                        const uint32_t *payload)
+					const uint32_t *payload)
 {
     int status = 0;
     const struct mod_scmi_voltd_device *device = NULL;
     const struct scmi_voltd_level_get_a2p *inmsg = NULL;
     struct scmi_voltd_level_get_p2a outmsg = {
-        .status = SCMI_GENERIC_ERROR,
+	.status = SCMI_GENERIC_ERROR,
     };
     size_t outmsg_size = sizeof(outmsg.status);
     int32_t level = 0;
@@ -464,13 +464,13 @@ static int scmi_voltd_level_get_handler(
 
     status = get_device(service_id, inmsg->domain_id, &device, NULL);
     if (status != FWK_SUCCESS) {
-        outmsg.status = SCMI_NOT_FOUND;
-        goto exit;
+	outmsg.status = SCMI_NOT_FOUND;
+	goto exit;
     }
 
     status = scmi_voltd_ctx.voltd_api->get_level(device->element_id, &level);
     if (status != FWK_SUCCESS)
-        goto exit;
+	goto exit;
 
     outmsg.voltage_level = level;
 
@@ -488,7 +488,7 @@ static int scmi_voltd_level_set_handler(
     const struct scmi_voltd_level_set_a2p *inmsg = NULL;
     const struct mod_scmi_voltd_device *device = NULL;
     struct scmi_voltd_level_set_p2a outmsg = {
-        .status = SCMI_GENERIC_ERROR,
+	.status = SCMI_GENERIC_ERROR,
     };
     size_t outmsg_size = sizeof(outmsg.status);
 
@@ -496,22 +496,22 @@ static int scmi_voltd_level_set_handler(
 
     status = get_device(service_id, inmsg->domain_id, &device, NULL);
     if (status != FWK_SUCCESS) {
-        outmsg.status = SCMI_NOT_FOUND;
-        goto exit;
+	outmsg.status = SCMI_NOT_FOUND;
+	goto exit;
     }
 
     status = scmi_voltd_ctx.voltd_api->set_level(device->element_id,
-                                                 inmsg->voltage_level);
+						 inmsg->voltage_level);
 
     switch (status) {
     case FWK_SUCCESS:
-        outmsg.status = SCMI_SUCCESS;
-        break;
+	outmsg.status = SCMI_SUCCESS;
+	break;
     case FWK_E_RANGE:
-        outmsg.status = SCMI_INVALID_PARAMETERS;
-        break;
+	outmsg.status = SCMI_INVALID_PARAMETERS;
+	break;
     default:
-        break;
+	break;
     }
 
 exit:
@@ -529,7 +529,7 @@ static int scmi_voltd_describe_levels_ha
     const struct mod_scmi_voltd_device *device = NULL;
     const struct scmi_voltd_describe_levels_a2p *inmsg = NULL;
     struct scmi_voltd_describe_levels_p2a outmsg = {
-        .status = SCMI_GENERIC_ERROR,
+	.status = SCMI_GENERIC_ERROR,
     };
     const struct mod_scmi_from_protocol_api *scmi_api = scmi_voltd_ctx.scmi_api;
     const struct mod_voltd_api *voltd_api = scmi_voltd_ctx.voltd_api;
@@ -544,8 +544,8 @@ static int scmi_voltd_describe_levels_ha
 
     status = get_device(service_id, inmsg->domain_id, &device, NULL);
     if (status != FWK_SUCCESS) {
-        outmsg.status = SCMI_NOT_FOUND;
-        goto exit;
+	outmsg.status = SCMI_NOT_FOUND;
+	goto exit;
     }
 
     /*
@@ -554,94 +554,94 @@ static int scmi_voltd_describe_levels_ha
      */
     status = scmi_api->get_max_payload_size(service_id, &max_payload_size);
     if (status != FWK_SUCCESS)
-        goto exit;
+	goto exit;
 
     if (max_payload_size < sizeof(outmsg))
-        max_level_items = 0;
+	max_level_items = 0;
     else
-        max_level_items = (max_payload_size - sizeof(outmsg)) / sizeof(int32_t);
+	max_level_items = (max_payload_size - sizeof(outmsg)) / sizeof(int32_t);
 
     status = voltd_api->get_info(device->element_id, &info);
     if (status != FWK_SUCCESS)
-        goto exit;
+	goto exit;
 
     if (info.level_range.level_type == MOD_VOLTD_VOLTAGE_LEVEL_DISCRETE) {
-        /* The domain has a discrete list of voltage levels */
-        int32_t level_uv = 0;
-        unsigned int i = 0;
-        unsigned int level_count = 0;
-        unsigned int remaining_levels;
-
-        if (level_index >= info.level_range.level_count) {
-            outmsg.status = SCMI_OUT_OF_RANGE;
-            goto exit;
-        }
-
-        /* Can at least one entry be returned? */
-        if (!max_level_items) {
-            status = FWK_E_SIZE;
-            goto exit;
-        }
-
-        level_count = FWK_MIN(max_level_items,
-                              info.level_range.level_count - level_index);
-
-        remaining_levels = (info.level_range.level_count - level_index) -
-                           level_count;
-
-        /* Set number of returned levels in the message payload */
-        outmsg.flags = SCMI_VOLTD_LEVEL_LIST_FLAGS(level_count,
-                                                   remaining_levels);
-
-        /* Set each level entry in the payload to the related voltage level */
-        for (i = 0; i < level_count; i++, payload_size += sizeof(level_uv)) {
-            status = voltd_api->get_level_from_index(device->element_id,
-                                                     level_index + i, &level_uv);
-            if (status != FWK_SUCCESS)
-                goto exit;
-
-            status = scmi_api->write_payload(service_id, payload_size,
-                                             &level_uv, sizeof(level_uv));
-            if (status != FWK_SUCCESS)
-                goto exit;
-        }
+	/* The domain has a discrete list of voltage levels */
+	int32_t level_uv = 0;
+	unsigned int i = 0;
+	unsigned int level_count = 0;
+	unsigned int remaining_levels;
+
+	if (level_index >= info.level_range.level_count) {
+	    outmsg.status = SCMI_OUT_OF_RANGE;
+	    goto exit;
+	}
+
+	/* Can at least one entry be returned? */
+	if (!max_level_items) {
+	    status = FWK_E_SIZE;
+	    goto exit;
+	}
+
+	level_count = FWK_MIN(max_level_items,
+			      info.level_range.level_count - level_index);
+
+	remaining_levels = (info.level_range.level_count - level_index) -
+			   level_count;
+
+	/* Set number of returned levels in the message payload */
+	outmsg.flags = SCMI_VOLTD_LEVEL_LIST_FLAGS(level_count,
+						   remaining_levels);
+
+	/* Set each level entry in the payload to the related voltage level */
+	for (i = 0; i < level_count; i++, payload_size += sizeof(level_uv)) {
+	    status = voltd_api->get_level_from_index(device->element_id,
+						     level_index + i, &level_uv);
+	    if (status != FWK_SUCCESS)
+		goto exit;
+
+	    status = scmi_api->write_payload(service_id, payload_size,
+					     &level_uv, sizeof(level_uv));
+	    if (status != FWK_SUCCESS)
+		goto exit;
+	}
     } else {
-        /* The voltage domain has a linear level stepping */
-        int32_t voltd_range[3] = { info.level_range.min_uv,
-                                   info.level_range.max_uv,
-                                   info.level_range.step_uv };
-
-        /* Is the payload area large enough to return the complete triplet? */
-        if (max_level_items < 3) {
-            status = FWK_E_SIZE;
-            goto exit;
-        }
-
-        outmsg.flags = SCMI_VOLTD_LEVEL_RANGE_FLAGS;
-
-        status = scmi_api->write_payload(service_id, payload_size,
-                                         &voltd_range, sizeof(voltd_range));
-        if (status != FWK_SUCCESS)
-            goto exit;
+	/* The voltage domain has a linear level stepping */
+	int32_t voltd_range[3] = { info.level_range.min_uv,
+				   info.level_range.max_uv,
+				   info.level_range.step_uv };
+
+	/* Is the payload area large enough to return the complete triplet? */
+	if (max_level_items < 3) {
+	    status = FWK_E_SIZE;
+	    goto exit;
+	}
+
+	outmsg.flags = SCMI_VOLTD_LEVEL_RANGE_FLAGS;
+
+	status = scmi_api->write_payload(service_id, payload_size,
+					 &voltd_range, sizeof(voltd_range));
+	if (status != FWK_SUCCESS)
+	    goto exit;
 
-        payload_size += sizeof(voltd_range);
+	payload_size += sizeof(voltd_range);
     }
 
     outmsg.status = SCMI_SUCCESS;
     status = scmi_api->write_payload(service_id, 0, &outmsg, sizeof(outmsg));
     if (status)
-        outmsg.status = SCMI_GENERIC_ERROR;
+	outmsg.status = SCMI_GENERIC_ERROR;
 
 exit:
     if (outmsg.status == SCMI_SUCCESS) {
-        respond_status = scmi_api->respond(service_id, NULL, payload_size);
+	respond_status = scmi_api->respond(service_id, NULL, payload_size);
     } else {
-        respond_status = scmi_api->respond(
-            service_id, &outmsg.status, sizeof(&outmsg.status));
+	respond_status = scmi_api->respond(
+	    service_id, &outmsg.status, sizeof(&outmsg.status));
     }
 
     if (respond_status != FWK_SUCCESS) {
-        FWK_LOG_DEBUG("[SCMI-VOLT] %s @%d", __func__, __LINE__);
+	FWK_LOG_DEBUG("[SCMI-VOLT] %s @%d", __func__, __LINE__);
     }
     // Return status or return FWK_SUCCESS???
     return status;
@@ -651,7 +651,7 @@ exit:
  * Dispatch SCMI message to SCMI voltd module interface
  */
 static int scmi_voltd_get_scmi_protocol_id(fwk_id_t protocol_id,
-                                           uint8_t *scmi_protocol_id)
+					   uint8_t *scmi_protocol_id)
 {
     *scmi_protocol_id = MOD_SCMI_PROTOCOL_ID_VOLTAGE_DOMAIN;
 
@@ -664,25 +664,25 @@ static int scmi_voltd_message_handler(fw
     int32_t outmsg;
 
     static_assert(FWK_ARRAY_SIZE(handler_table) ==
-        FWK_ARRAY_SIZE(payload_size_table),
-        "[SCMI] Inconsistent voltage domain management protocol table sizes");
+	FWK_ARRAY_SIZE(payload_size_table),
+	"[SCMI] Inconsistent voltage domain management protocol table sizes");
     fwk_assert(payload != NULL);
 
     if (message_id >= FWK_ARRAY_SIZE(handler_table)) {
-        outmsg = SCMI_NOT_FOUND;
-        goto error;
+	outmsg = SCMI_NOT_FOUND;
+	goto error;
     }
 
     if (payload_size != payload_size_table[message_id]) {
-        outmsg = SCMI_PROTOCOL_ERROR;
-        goto error;
+	outmsg = SCMI_PROTOCOL_ERROR;
+	goto error;
     }
 
 #ifdef BUILD_HAS_MOD_RESOURCE_PERMS
     if (scmi_voltd_permissions_handler(service_id, payload, payload_size,
-                                       message_id) != FWK_SUCCESS) {
-        outmsg = SCMI_DENIED;
-        goto error;
+				       message_id) != FWK_SUCCESS) {
+	outmsg = SCMI_DENIED;
+	goto error;
     }
 #endif
 
@@ -690,7 +690,7 @@ static int scmi_voltd_message_handler(fw
 
 error:
     return scmi_voltd_ctx.scmi_api->respond(
-        service_id, &outmsg, sizeof(outmsg));
+	service_id, &outmsg, sizeof(outmsg));
 }
 
 static struct mod_scmi_to_protocol_api scmi_voltd_mod_scmi_to_protocol_api = {
@@ -702,25 +702,25 @@ static struct mod_scmi_to_protocol_api s
  * Framework handlers
  */
 static int scmi_voltd_init(fwk_id_t module_id, unsigned int element_count,
-                           const void *data)
+			   const void *data)
 {
     int voltd_devices;
     const struct mod_scmi_voltd_config *config =
-        (const struct mod_scmi_voltd_config *)data;
+	(const struct mod_scmi_voltd_config *)data;
 
     if ((config == NULL) || (config->agent_table == NULL))
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     scmi_voltd_ctx.config = config;
     scmi_voltd_ctx.agent_table = config->agent_table;
 
     voltd_devices = fwk_module_get_element_count(fwk_module_id_voltage_domain);
     if (voltd_devices == FWK_E_PARAM)
-        return FWK_E_PANIC;
+	return FWK_E_PANIC;
 
     scmi_voltd_ctx.voltd_devices = voltd_devices;
     scmi_voltd_ctx.voltd_ops = fwk_mm_calloc((unsigned int)voltd_devices,
-                                             sizeof(struct voltd_operations));
+					     sizeof(struct voltd_operations));
 
     return FWK_SUCCESS;
 }
@@ -730,34 +730,34 @@ static int scmi_voltd_bind(fwk_id_t id,
     int status = 0;
 
     if (round == 1)
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 
     status = fwk_module_bind(FWK_ID_MODULE(FWK_MODULE_IDX_SCMI),
-                             FWK_ID_API(FWK_MODULE_IDX_SCMI,
-                                        MOD_SCMI_API_IDX_PROTOCOL),
-                             &scmi_voltd_ctx.scmi_api);
+			     FWK_ID_API(FWK_MODULE_IDX_SCMI,
+					MOD_SCMI_API_IDX_PROTOCOL),
+			     &scmi_voltd_ctx.scmi_api);
     if (status != FWK_SUCCESS)
-        return status;
+	return status;
 
 #ifdef BUILD_HAS_MOD_RESOURCE_PERMS
     status = fwk_module_bind(FWK_ID_MODULE(FWK_MODULE_IDX_RESOURCE_PERMS),
-                             FWK_ID_API(FWK_MODULE_IDX_RESOURCE_PERMS,
-                                        MOD_RES_PERM_RESOURCE_PERMS),
-                             &scmi_voltd_ctx.res_perms_api);
+			     FWK_ID_API(FWK_MODULE_IDX_RESOURCE_PERMS,
+					MOD_RES_PERM_RESOURCE_PERMS),
+			     &scmi_voltd_ctx.res_perms_api);
     if (status != FWK_SUCCESS)
-        return status;
+	return status;
 #endif
 
     return fwk_module_bind(FWK_ID_MODULE(FWK_MODULE_IDX_VOLTAGE_DOMAIN),
-                           FWK_ID_API(FWK_MODULE_IDX_VOLTAGE_DOMAIN, 0),
-                           &scmi_voltd_ctx.voltd_api);
+			   FWK_ID_API(FWK_MODULE_IDX_VOLTAGE_DOMAIN, 0),
+			   &scmi_voltd_ctx.voltd_api);
 }
 
 static int scmi_voltd_process_bind_request(fwk_id_t source_id,
     fwk_id_t target_id, fwk_id_t api_id, const void **api)
 {
     if (!fwk_id_is_equal(source_id, FWK_ID_MODULE(FWK_MODULE_IDX_SCMI)))
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
 
     *api = &scmi_voltd_mod_scmi_to_protocol_api;
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/sds/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/sds/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/sds/CMakeLists.txt
@@ -8,9 +8,9 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_sds.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_sds.c")
 
 target_link_libraries(${SCP_MODULE_TARGET} PRIVATE module-clock)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/sds/include/mod_sds.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/sds/include/mod_sds.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/sds/include/mod_sds.h
@@ -140,8 +140,8 @@ enum mod_sds_notification_idx {
  */
 static const fwk_id_t mod_sds_notification_id_initialized =
     FWK_ID_NOTIFICATION_INIT(
-        FWK_MODULE_IDX_SDS,
-        MOD_SDS_NOTIFICATION_IDX_INITIALIZED);
+	FWK_MODULE_IDX_SDS,
+	MOD_SDS_NOTIFICATION_IDX_INITIALIZED);
 
 /*!
  * \brief Module interface.
@@ -177,7 +177,7 @@ struct mod_sds_api {
      * \retval ::FWK_E_RANGE The field extends outside of the structure bounds.
      */
     int (*struct_write)(uint32_t structure_id, unsigned int offset,
-                        const void *data, size_t size);
+			const void *data, size_t size);
 
     /*!
      * \brief Read data from within a Shared Data Structure.
@@ -212,7 +212,7 @@ struct mod_sds_api {
      * \retval ::FWK_E_RANGE The field extends outside of the structure bounds.
      */
     int (*struct_read)(uint32_t structure_id, unsigned int offset, void *data,
-                       size_t size);
+		       size_t size);
 
     /*!
      * \brief Mark a Shared Data Structure as valid and ready for use by
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/sds/src/mod_sds.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/sds/src/mod_sds.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/sds/src/mod_sds.c
@@ -42,7 +42,7 @@
     FWK_ALIGN_NEXT(MIN_STRUCT_SIZE, MIN_STRUCT_ALIGNMENT)
 /* Minimum region size in bytes */
 #define MIN_REGION_SIZE (sizeof(struct region_descriptor) + \
-                         MIN_ALIGNED_STRUCT_SIZE)
+			 MIN_ALIGNED_STRUCT_SIZE)
 
 /* Header containing Shared Data Structure metadata */
 struct structure_header {
@@ -105,11 +105,11 @@ struct region_descriptor {
 /* Module context structure*/
 struct sds_ctx {
     struct {
-        /* Remaining, unused memory in the SDS Memory Region, in bytes. */
-        size_t free_mem_size;
+	/* Remaining, unused memory in the SDS Memory Region, in bytes. */
+	size_t free_mem_size;
 
-        /* Pointer to the next free memory address in the SDS Memory Region. */
-        volatile char *free_mem_base;
+	/* Pointer to the next free memory address in the SDS Memory Region. */
+	volatile char *free_mem_base;
     } *regions;
 };
 
@@ -125,33 +125,33 @@ static struct sds_ctx ctx;
  * Header contain obviously invalid data.
  */
 static bool header_is_valid(const volatile struct region_descriptor *region,
-                            const volatile struct structure_header *header)
+			    const volatile struct structure_header *header)
 {
     const volatile char *region_tail, *struct_tail;
 
     if (header->id == 0) {
-        return false; /* Zero is not a valid identifier */
+	return false; /* Zero is not a valid identifier */
     }
 
     if ((header->id >> MOD_SDS_ID_VERSION_MAJOR_POS) == 0) {
-        return false; /* 0 is not a valid major version */
+	return false; /* 0 is not a valid major version */
     }
 
     if (header->size < MIN_ALIGNED_STRUCT_SIZE) {
-        return false; /* Padded structure size is less than the minimum */
+	return false; /* Padded structure size is less than the minimum */
     }
 
     region_tail = (const volatile char *)region + region->region_size;
     struct_tail = (const volatile char *)header
-        + sizeof(struct structure_header)
-        + header->size;
+	+ sizeof(struct structure_header)
+	+ header->size;
     if (struct_tail > region_tail) {
-        /* Structure exceeds the capacity of the SDS Memory Region */
-        return false;
+	/* Structure exceeds the capacity of the SDS Memory Region */
+	return false;
     }
 
     if ((header->size % MIN_STRUCT_ALIGNMENT) != 0) {
-        return false; /* Structure does not meet alignment requirements */
+	return false; /* Structure does not meet alignment requirements */
     }
 
     return true;
@@ -163,11 +163,11 @@ static int validate_structure_access(
     size_t access_size)
 {
     if ((offset >= structure_size) || (access_size > structure_size)) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     if ((structure_size - offset) < access_size) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     return FWK_SUCCESS;
@@ -191,8 +191,8 @@ static int validate_structure_access(
  * If a structure with the given ID is not present then FWK_E_PARAM is returned.
  */
 static int get_structure_info(uint32_t structure_id,
-                              struct structure_header *header,
-                              volatile char **structure_base)
+			      struct structure_header *header,
+			      volatile char **structure_base)
 {
    volatile struct structure_header *current_header;
    size_t offset, region_size, struct_count, region_idx, struct_idx;
@@ -212,27 +212,27 @@ static int get_structure_info(uint32_t s
        offset = sizeof(struct region_descriptor);
        /* Iterate over structure headers to find one with a matching ID */
        for (struct_idx = 0; struct_idx < struct_count; struct_idx++) {
-           current_header = (volatile struct structure_header *)(
-                region_base + offset);
-           if (!header_is_valid(region_desc, current_header)) {
-               return FWK_E_DATA;
-           }
-
-           if (current_header->id == structure_id) {
-               if (structure_base != NULL) {
-                   *structure_base = ((volatile char *)current_header
-                                      + sizeof(struct structure_header));
-               }
-
-               *header = *current_header;
-               return FWK_SUCCESS;
-           }
-
-           offset += current_header->size;
-           offset += sizeof(struct structure_header);
-           if (offset >= region_size) {
-               return FWK_E_RANGE;
-           }
+	   current_header = (volatile struct structure_header *)(
+		region_base + offset);
+	   if (!header_is_valid(region_desc, current_header)) {
+	       return FWK_E_DATA;
+	   }
+
+	   if (current_header->id == structure_id) {
+	       if (structure_base != NULL) {
+		   *structure_base = ((volatile char *)current_header
+				      + sizeof(struct structure_header));
+	       }
+
+	       *header = *current_header;
+	       return FWK_SUCCESS;
+	   }
+
+	   offset += current_header->size;
+	   offset += sizeof(struct structure_header);
+	   if (offset >= region_size) {
+	       return FWK_E_RANGE;
+	   }
        }
    }
 
@@ -264,8 +264,8 @@ static int struct_alloc(const struct mod
     const size_t region_idx = struct_desc->region_id;
 
     if (struct_desc->size < MIN_STRUCT_SIZE) {
-        status = FWK_E_PARAM;
-        goto exit;
+	status = FWK_E_PARAM;
+	goto exit;
     }
 
     config = fwk_module_get_data(fwk_module_id_sds);
@@ -273,21 +273,21 @@ static int struct_alloc(const struct mod
     fwk_assert(region_idx < config->region_count);
 
     padded_size =
-        (unsigned int)FWK_ALIGN_NEXT(struct_desc->size, MIN_STRUCT_ALIGNMENT);
+	(unsigned int)FWK_ALIGN_NEXT(struct_desc->size, MIN_STRUCT_ALIGNMENT);
 
     region_desc = (volatile struct region_descriptor *)(
-        config->regions[region_idx].base);
+	config->regions[region_idx].base);
     free_mem_base = &(ctx.regions[region_idx].free_mem_base);
     free_mem_size = &(ctx.regions[region_idx].free_mem_size);
 
     if ((padded_size + sizeof(*header)) > *free_mem_size) {
-        status = FWK_E_NOMEM;
-        goto exit;
+	status = FWK_E_NOMEM;
+	goto exit;
     }
 
     if (structure_exists(struct_desc->id)) {
-        status = FWK_E_RANGE;
-        goto exit;
+	status = FWK_E_RANGE;
+	goto exit;
     }
 
     /* Create the Structure Header */
@@ -300,7 +300,7 @@ static int struct_alloc(const struct mod
 
     /* Zero the memory reserved for the structure, avoiding the header */
     for (unsigned int i = 0; i < padded_size; i++) {
-        (*free_mem_base)[i] = '\0';
+	(*free_mem_base)[i] = '\0';
     }
     *free_mem_base += padded_size;
     *free_mem_size -= padded_size;
@@ -339,32 +339,32 @@ static int reinitialize_memory_region(
 
     region_desc = (volatile struct region_descriptor *)(region_config->base);
     if (region_desc->signature != REGION_SIGNATURE) {
-        return FWK_E_DATA;
+	return FWK_E_DATA;
     }
 
     if (region_desc->version_major != SUPPORTED_VERSION_MAJOR) {
-        return FWK_E_DATA;
+	return FWK_E_DATA;
     }
 
     mem_used = (uint32_t)sizeof(struct region_descriptor);
     for (struct_idx = 0; struct_idx < region_desc->structure_count;
-        struct_idx++) {
-        header = (volatile struct structure_header *)(
-            (volatile char *)region_config->base + mem_used);
-
-        if (!header_is_valid(region_desc, header)) {
-            return FWK_E_DATA; /* Unexpected invalid header */
-        }
-
-        mem_used += header->size;
-        mem_used += (uint32_t)sizeof(struct structure_header);
-        if (mem_used > region_desc->region_size) {
-            return FWK_E_SIZE;
-        }
+	struct_idx++) {
+	header = (volatile struct structure_header *)(
+	    (volatile char *)region_config->base + mem_used);
+
+	if (!header_is_valid(region_desc, header)) {
+	    return FWK_E_DATA; /* Unexpected invalid header */
+	}
+
+	mem_used += header->size;
+	mem_used += (uint32_t)sizeof(struct structure_header);
+	if (mem_used > region_desc->region_size) {
+	    return FWK_E_SIZE;
+	}
     }
 
     if (mem_used > region_config->size) {
-        return FWK_E_SIZE;
+	return FWK_E_SIZE;
     }
 
     /*
@@ -378,7 +378,7 @@ static int reinitialize_memory_region(
     region_desc->region_size = (uint32_t)region_config->size;
     ctx.regions[region_idx].free_mem_size = region_config->size - mem_used;
     ctx.regions[region_idx].free_mem_base =
-        (volatile char *)region_config->base + mem_used;
+	(volatile char *)region_config->base + mem_used;
 
     return FWK_SUCCESS;
 }
@@ -387,12 +387,12 @@ static int reinitialize_memory_region(
  * Initialize an empty SDS Memory Region so that it is ready for use.
  */
 static int create_memory_region(const struct mod_sds_region_desc* region_config,
-                                unsigned int region_idx)
+				unsigned int region_idx)
 {
     volatile struct region_descriptor *region_desc;
 
     if (region_config->size < MIN_REGION_SIZE) {
-        return FWK_E_NOMEM;
+	return FWK_E_NOMEM;
     }
 
     region_desc = (volatile struct region_descriptor *)region_config->base;
@@ -403,17 +403,17 @@ static int create_memory_region(const st
     region_desc->region_size = (uint32_t)region_config->size;
 
     ctx.regions[region_idx].free_mem_size = region_config->size
-        - sizeof(struct region_descriptor);
+	- sizeof(struct region_descriptor);
     ctx.regions[region_idx].free_mem_base =
-        (volatile char *)region_config->base + sizeof(struct region_descriptor);
+	(volatile char *)region_config->base + sizeof(struct region_descriptor);
     fwk_assert(((uintptr_t)ctx.regions[region_idx].free_mem_base %
-        MIN_STRUCT_ALIGNMENT) == 0);
+	MIN_STRUCT_ALIGNMENT) == 0);
 
     return FWK_SUCCESS;
 }
 
 static int struct_write(uint32_t structure_id, unsigned int offset,
-                        const void *data, size_t size)
+			const void *data, size_t size)
 {
     int status;
     volatile char *structure_base;
@@ -425,16 +425,16 @@ static int struct_write(uint32_t structu
     /* Look up the Structure Header by its identifier */
     status = get_structure_info(structure_id, &header, &structure_base);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     status = validate_structure_access(header.size, offset, size);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     for (unsigned int i = 0; i < size; i++) {
-        structure_base[offset + i] = ((const char*)data)[i];
+	structure_base[offset + i] = ((const char*)data)[i];
     }
 
     return FWK_SUCCESS;
@@ -450,12 +450,12 @@ static int struct_finalize(uint32_t stru
     /* Check that the structure being finalized exists */
     status = get_structure_info(structure_id, &header, &structure_base);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     /* Update the valid flag of the header within the SDS Memory Region */
     header_mem = (volatile struct structure_header *)(
-        structure_base - sizeof(header));
+	structure_base - sizeof(header));
     header_mem->valid = true;
 
     return FWK_SUCCESS;
@@ -467,23 +467,23 @@ static int struct_init(const struct mod_
 
     /* If the structure does not already exist, allocate it. */
     if (!structure_exists(struct_desc->id)) {
-        status = struct_alloc(struct_desc);
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
+	status = struct_alloc(struct_desc);
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
     }
 
     if (struct_desc->payload != NULL) {
-        status = struct_write(struct_desc->id, 0, struct_desc->payload,
-                              struct_desc->size);
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
+	status = struct_write(struct_desc->id, 0, struct_desc->payload,
+			      struct_desc->size);
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
     }
 
     /* Finalize the structure immediately if required */
     if (struct_desc->finalize) {
-        status = struct_finalize(struct_desc->id);
+	status = struct_finalize(struct_desc->id);
     }
 
     return status;
@@ -500,36 +500,36 @@ static int init_sds(void)
     const struct mod_sds_region_desc *region_config;
     unsigned int notification_count;
     struct fwk_event notification_event = {
-        .id = mod_sds_notification_id_initialized,
-        .source_id = fwk_module_id_sds,
+	.id = mod_sds_notification_id_initialized,
+	.source_id = fwk_module_id_sds,
     };
 
     config = fwk_module_get_data(fwk_module_id_sds);
 
     for (region_idx = 0; region_idx < config->region_count; region_idx++) {
-        region_config = &(config->regions[region_idx]);
-        /*
-         * Either reinitialize the memory region,
-         * or create it for the first time
-         */
-        status = reinitialize_memory_region(region_config, region_idx);
-        if (status != FWK_SUCCESS) {
-            status = create_memory_region(region_config, region_idx);
-            if (status != FWK_SUCCESS) {
-                return status;
-            }
-        }
+	region_config = &(config->regions[region_idx]);
+	/*
+	 * Either reinitialize the memory region,
+	 * or create it for the first time
+	 */
+	status = reinitialize_memory_region(region_config, region_idx);
+	if (status != FWK_SUCCESS) {
+	    status = create_memory_region(region_config, region_idx);
+	    if (status != FWK_SUCCESS) {
+		return status;
+	    }
+	}
     }
 
     element_count = fwk_module_get_element_count(fwk_module_id_sds);
     for (element_idx = 0; element_idx < element_count; ++element_idx) {
-        struct_desc = fwk_module_get_data(fwk_id_build_element_id(
-            fwk_module_id_sds, (unsigned int)element_idx));
+	struct_desc = fwk_module_get_data(fwk_id_build_element_id(
+	    fwk_module_id_sds, (unsigned int)element_idx));
 
-        status = struct_init(struct_desc);
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
+	status = struct_init(struct_desc);
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
     }
 
     return fwk_notification_notify(&notification_event, &notification_count);
@@ -540,47 +540,47 @@ static int init_sds(void)
  */
 
 static int sds_struct_write(uint32_t structure_id, unsigned int offset,
-                            const void *data, size_t size)
+			    const void *data, size_t size)
 {
     if (data == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     if (size == 0) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     return struct_write(structure_id, offset, data, size);
 }
 
 static int sds_struct_read(uint32_t structure_id, unsigned int offset,
-                           void *data, size_t size)
+			   void *data, size_t size)
 {
     int status;
     volatile char *structure_base;
     struct structure_header header;
 
     if (data == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     if (size == 0) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     /* Check if a structure with this ID exists */
     status = get_structure_info(structure_id, &header, &structure_base);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     status = validate_structure_access(header.size, offset, size);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     for (unsigned int i = 0; i < size; i++) {
-        ((char*)data)[i] = structure_base[offset + i];
+	((char*)data)[i] = structure_base[offset + i];
     }
 
     return FWK_SUCCESS;
@@ -602,14 +602,14 @@ static const struct mod_sds_api module_a
  */
 
 static int sds_init(fwk_id_t module_id, unsigned int element_count,
-                    const void *data)
+		    const void *data)
 {
     const struct mod_sds_config *config;
     void *region_base;
     unsigned int region_idx;
 
     if (data == NULL) {
-        return FWK_E_PANIC;
+	return FWK_E_PANIC;
     }
 
     config = (const struct mod_sds_config *)data;
@@ -617,34 +617,34 @@ static int sds_init(fwk_id_t module_id,
     fwk_assert((MIN_STRUCT_ALIGNMENT % MIN_FIELD_ALIGNMENT) == 0);
 
     for (region_idx = 0; region_idx < config->region_count; region_idx++) {
-        region_base = config->regions[region_idx].base;
-        if (region_base == NULL) {
-            return FWK_E_PARAM;
-        }
-        if (((uintptr_t)region_base % MIN_STRUCT_ALIGNMENT) > 0) {
-            return FWK_E_PARAM;
-        }
+	region_base = config->regions[region_idx].base;
+	if (region_base == NULL) {
+	    return FWK_E_PARAM;
+	}
+	if (((uintptr_t)region_base % MIN_STRUCT_ALIGNMENT) > 0) {
+	    return FWK_E_PARAM;
+	}
     }
 
     ctx.regions = fwk_mm_alloc(config->region_count, sizeof(ctx.regions[0]));
     if (ctx.regions == NULL) {
-        return FWK_E_NOMEM;
+	return FWK_E_NOMEM;
     }
 
     return FWK_SUCCESS;
 }
 
 static int sds_element_init(fwk_id_t element_id, unsigned int unused,
-                            const void *data)
+			    const void *data)
 {
     return FWK_SUCCESS;
 }
 
 static int sds_process_bind_request(fwk_id_t requester_id, fwk_id_t id,
-                                    fwk_id_t api_id, const void **api)
+				    fwk_id_t api_id, const void **api)
 {
     if (!fwk_module_is_valid_module_id(requester_id)) {
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 
     *api = &module_api;
@@ -658,17 +658,17 @@ static int sds_start(fwk_id_t id)
 #endif
 
     if (!fwk_id_is_type(id, FWK_ID_TYPE_MODULE)) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
 #ifdef BUILD_HAS_MOD_CLOCK
     config = fwk_module_get_data(fwk_module_id_sds);
     if (!fwk_id_is_equal(config->clock_id, FWK_ID_NONE)) {
-        /* Register the module for clock state notifications */
-        return fwk_notification_subscribe(
-            mod_clock_notification_id_state_changed,
-            config->clock_id,
-            id);
+	/* Register the module for clock state notifications */
+	return fwk_notification_subscribe(
+	    mod_clock_notification_id_state_changed,
+	    config->clock_id,
+	    id);
     }
 #endif
 
@@ -683,12 +683,12 @@ static int sds_process_notification(
     struct clock_notification_params *params;
 
     fwk_assert(fwk_id_is_equal(event->id,
-                               mod_clock_notification_id_state_changed));
+			       mod_clock_notification_id_state_changed));
     fwk_assert(fwk_id_is_type(event->target_id, FWK_ID_TYPE_MODULE));
 
     params = (struct clock_notification_params *)event->params;
     if (params->new_state != MOD_CLOCK_STATE_RUNNING) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     return init_sds();
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/sensor/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/sensor/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/sensor/CMakeLists.txt
@@ -8,11 +8,11 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(
     ${SCP_MODULE_TARGET}
     PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_sensor.c"
-            "${CMAKE_CURRENT_SOURCE_DIR}/src/sensor_extended.c")
+	    "${CMAKE_CURRENT_SOURCE_DIR}/src/sensor_extended.c")
 
 target_link_libraries(${SCP_MODULE_TARGET} PRIVATE module-scmi-sensor)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/sensor/include/mod_sensor.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/sensor/include/mod_sensor.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/sensor/include/mod_sensor.h
@@ -313,10 +313,10 @@ struct mod_sensor_complete_info {
 #ifdef BUILD_HAS_SENSOR_MULTI_AXIS
     /*! Sensor multi axis information */
     struct {
-        /*! Multi axis supported feature */
-        bool support;
-        /*! Number of axis configured */
-        unsigned int axis_count;
+	/*! Multi axis supported feature */
+	bool support;
+	/*! Number of axis configured */
+	unsigned int axis_count;
     } multi_axis;
 #endif
 };
@@ -384,10 +384,10 @@ struct mod_sensor_data {
 
     /*! Sensor value */
     union {
-        /*! Sensor N-axis value */
-        mod_sensor_value_t *axis_value;
-        /*! Sensor scalar value */
-        mod_sensor_value_t value;
+	/*! Sensor N-axis value */
+	mod_sensor_value_t *axis_value;
+	/*! Sensor scalar value */
+	mod_sensor_value_t value;
     };
 
 #ifdef BUILD_HAS_SENSOR_TIMESTAMP
@@ -466,9 +466,9 @@ struct mod_sensor_driver_api {
      * \return One of the standard framework error codes.
      */
     int (*get_axis_info)(
-        fwk_id_t id,
-        uint32_t axis,
-        struct mod_sensor_axis_info *info);
+	fwk_id_t id,
+	uint32_t axis,
+	struct mod_sensor_axis_info *info);
 #endif
 };
 
@@ -521,9 +521,9 @@ struct mod_sensor_api {
      * \return One of the standard framework error codes.
      */
     int (*set_trip_point)(
-        fwk_id_t id,
-        uint32_t trip_point_idx,
-        struct mod_sensor_trip_point_params *params);
+	fwk_id_t id,
+	uint32_t trip_point_idx,
+	struct mod_sensor_trip_point_params *params);
 
     /*!
      * \brief Get trip point.
@@ -538,9 +538,9 @@ struct mod_sensor_api {
      * \return One of the standard framework error codes.
      */
     int (*get_trip_point)(
-        fwk_id_t id,
-        uint32_t trip_point_idx,
-        struct mod_sensor_trip_point_params *params);
+	fwk_id_t id,
+	uint32_t trip_point_idx,
+	struct mod_sensor_trip_point_params *params);
 
 #ifdef BUILD_HAS_SENSOR_TIMESTAMP
     /*!
@@ -556,8 +556,8 @@ struct mod_sensor_api {
      * \return One of the standard framework error codes.
      */
     int (*set_timestamp_config)(
-        fwk_id_t id,
-        const struct mod_sensor_timestamp_info *config);
+	fwk_id_t id,
+	const struct mod_sensor_timestamp_info *config);
 
     /*!
      * \brief Read timestamp configuration
@@ -571,8 +571,8 @@ struct mod_sensor_api {
      * \return One of the standard framework error codes.
      */
     int (*get_timestamp_config)(
-        fwk_id_t id,
-        struct mod_sensor_timestamp_info *config);
+	fwk_id_t id,
+	struct mod_sensor_timestamp_info *config);
 
     /*!
      * \brief Get axis sensor information.
@@ -585,9 +585,9 @@ struct mod_sensor_api {
      * \return One of the standard framework error codes.
      */
     int (*get_axis_info)(
-        fwk_id_t id,
-        uint32_t axis,
-        struct mod_sensor_axis_info *info);
+	fwk_id_t id,
+	uint32_t axis,
+	struct mod_sensor_axis_info *info);
 #endif
 };
 
@@ -600,10 +600,10 @@ struct mod_sensor_driver_resp_params {
 
     /*! Sensor value */
     union {
-        /*! Sensor N-axis value */
-        mod_sensor_value_t *axis_value;
-        /*! Sensor scalar value */
-        mod_sensor_value_t value;
+	/*! Sensor N-axis value */
+	mod_sensor_value_t *axis_value;
+	/*! Sensor scalar value */
+	mod_sensor_value_t value;
     };
 };
 
@@ -621,7 +621,7 @@ struct mod_sensor_driver_response_api {
      * \param[out] response The response data structure.
      */
     void (*reading_complete)(fwk_id_t id,
-                             struct mod_sensor_driver_resp_params *response);
+			     struct mod_sensor_driver_resp_params *response);
 };
 
 /*!
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/sensor/src/mod_sensor.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/sensor/src/mod_sensor.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/sensor/src/mod_sensor.c
@@ -35,13 +35,13 @@ struct sensor_dev_ctx *sensor_get_ctx(fw
 }
 
 static int get_ctx_if_valid_call(fwk_id_t id,
-                                 void *data,
-                                 struct sensor_dev_ctx **ctx)
+				 void *data,
+				 struct sensor_dev_ctx **ctx)
 {
     fwk_assert(ctx != NULL);
 
     if (!fwk_expect(data != NULL)) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     *ctx = ctx_table + fwk_id_get_element_idx(id);
@@ -61,11 +61,11 @@ static inline void sensor_data_copy(
 
 #ifdef BUILD_HAS_SENSOR_MULTI_AXIS
     if (dest->axis_count > 1) {
-        dest->axis_value = value;
-        fwk_str_memcpy(
-            dest->axis_value,
-            origin->axis_value,
-            sizeof(uint64_t) * dest->axis_count);
+	dest->axis_value = value;
+	fwk_str_memcpy(
+	    dest->axis_value,
+	    origin->axis_value,
+	    sizeof(uint64_t) * dest->axis_count);
     }
 #endif
 }
@@ -83,23 +83,23 @@ static bool trip_point_evaluate(
 
     switch (ctx->params.mode) {
     case MOD_SENSOR_TRIP_POINT_MODE_POSITIVE:
-        if (!ctx->above_threshold && value > threshold)
-            trigger = true;
-        break;
+	if (!ctx->above_threshold && value > threshold)
+	    trigger = true;
+	break;
 
     case MOD_SENSOR_TRIP_POINT_MODE_NEGATIVE:
-        if (ctx->above_threshold && (value <= threshold))
-            trigger = true;
-        break;
+	if (ctx->above_threshold && (value <= threshold))
+	    trigger = true;
+	break;
 
     case MOD_SENSOR_TRIP_POINT_MODE_TRANSITION:
-        if ((!ctx->above_threshold && value > threshold) ||
-            (ctx->above_threshold && value <= threshold))
-            trigger = true;
-        break;
+	if ((!ctx->above_threshold && value > threshold) ||
+	    (ctx->above_threshold && value <= threshold))
+	    trigger = true;
+	break;
 
     default:
-        break;
+	break;
     }
 
     ctx->above_threshold = new_above_threshold;
@@ -117,16 +117,16 @@ static void trip_point_process(fwk_id_t
     ctx = ctx_table + fwk_id_get_element_idx(id);
 
     if (!ctx->trip_point_ctx->enabled) {
-        return;
+	return;
     }
 
     for (i = 0; i < ctx->config->trip_point.count; i++) {
-        if (trip_point_evaluate(&(ctx->trip_point_ctx[i]), data->value)) {
-            /* Handle trip point event*/
-            if (sensor_mod_ctx.sensor_trip_point_api != NULL)
-                sensor_mod_ctx.sensor_trip_point_api->notify_sensor_trip_point(
-                    id, ctx->trip_point_ctx->above_threshold, i);
-        }
+	if (trip_point_evaluate(&(ctx->trip_point_ctx[i]), data->value)) {
+	    /* Handle trip point event*/
+	    if (sensor_mod_ctx.sensor_trip_point_api != NULL)
+		sensor_mod_ctx.sensor_trip_point_api->notify_sensor_trip_point(
+		    id, ctx->trip_point_ctx->above_threshold, i);
+	}
     }
 }
 #endif
@@ -140,49 +140,49 @@ static int get_data(fwk_id_t id, struct
     struct sensor_dev_ctx *ctx;
     struct fwk_event req;
     struct mod_sensor_event_params *event_params =
-        (struct mod_sensor_event_params *)req.params;
+	(struct mod_sensor_event_params *)req.params;
 
     status = get_ctx_if_valid_call(id, data, &ctx);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     if (ctx->concurrency_readings.dequeuing) {
-        /* Prevent new reading request while dequeuing pending readings
-         * cached data is returned
-         */
-        sensor_data_copy(data, &ctx->last_read);
-        return ctx->last_read.status;
+	/* Prevent new reading request while dequeuing pending readings
+	 * cached data is returned
+	 */
+	sensor_data_copy(data, &ctx->last_read);
+	return ctx->last_read.status;
     }
 
     if (ctx->concurrency_readings.pending_requests == 0) {
-        status = ctx->driver_api->get_value(
-            ctx->config->driver_id, &ctx->last_read.value);
-        ctx->last_read.status = status;
-        if (status == FWK_SUCCESS) {
+	status = ctx->driver_api->get_value(
+	    ctx->config->driver_id, &ctx->last_read.value);
+	ctx->last_read.status = status;
+	if (status == FWK_SUCCESS) {
 #ifdef BUILD_HAS_SCMI_SENSOR_EVENTS
-            trip_point_process(id, &ctx->last_read);
+	    trip_point_process(id, &ctx->last_read);
 #endif
 #ifdef BUILD_HAS_SENSOR_TIMESTAMP
-            ctx->last_read.timestamp = sensor_get_timestamp(id);
+	    ctx->last_read.timestamp = sensor_get_timestamp(id);
 #endif
-            sensor_data_copy(data, &ctx->last_read);
+	    sensor_data_copy(data, &ctx->last_read);
 
-            return status;
-        } else if (status != FWK_PENDING) {
-            return status;
-        }
+	    return status;
+	} else if (status != FWK_PENDING) {
+	    return status;
+	}
     }
 
     if (ctx->concurrency_readings.pending_requests >=
-        SENSOR_MAX_PENDING_REQUESTS) {
-        return FWK_E_BUSY;
+	SENSOR_MAX_PENDING_REQUESTS) {
+	return FWK_E_BUSY;
     }
 
     req = (struct fwk_event){
-        .target_id = id,
-        .id = mod_sensor_event_id_read_request,
-        .response_requested = true,
+	.target_id = id,
+	.id = mod_sensor_event_id_read_request,
+	.response_requested = true,
     };
 
     /* Save data address to copy return values in there */
@@ -190,7 +190,7 @@ static int get_data(fwk_id_t id, struct
 
     status = fwk_put_event(&req);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     ctx->concurrency_readings.pending_requests++;
@@ -209,27 +209,27 @@ static int get_info(fwk_id_t id, struct
 
     status = get_ctx_if_valid_call(id, info, &ctx);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     status = ctx->driver_api->get_info(ctx->config->driver_id, &info->hal_info);
     if (!fwk_expect(status == FWK_SUCCESS)) {
-        return FWK_E_DEVICE;
+	return FWK_E_DEVICE;
     }
     info->trip_point = ctx->config->trip_point;
 
 #ifdef BUILD_HAS_SENSOR_TIMESTAMP
     status = sensor_get_timestamp_config(id, &info->timestamp);
     if (status == FWK_E_SUPPORT) {
-        info->timestamp.timestamp_support = false;
+	info->timestamp.timestamp_support = false;
     } else {
-        return status;
+	return status;
     }
 #endif
 #ifdef BUILD_HAS_SENSOR_MULTI_AXIS
     if (ctx->axis_count > 1) {
-        info->multi_axis.support = true;
-        info->multi_axis.axis_count = ctx->axis_count;
+	info->multi_axis.support = true;
+	info->multi_axis.axis_count = ctx->axis_count;
     }
 #endif
 
@@ -248,7 +248,7 @@ static int sensor_get_trip_point(
     ctx = ctx_table + fwk_id_get_element_idx(id);
 
     if (trip_point_idx >= ctx->config->trip_point.count) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     *params = ctx->trip_point_ctx[trip_point_idx].params;
@@ -264,13 +264,13 @@ static int sensor_set_trip_point(
     struct sensor_dev_ctx *ctx;
 
     if (params == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     ctx = ctx_table + fwk_id_get_element_idx(id);
 
     if (trip_point_idx >= ctx->config->trip_point.count) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     ctx->trip_point_ctx[trip_point_idx].params = *params;
@@ -298,47 +298,47 @@ static struct mod_sensor_api sensor_api
  * Driver response API.
  */
 static void reading_complete(fwk_id_t dev_id,
-                             struct mod_sensor_driver_resp_params *response)
+			     struct mod_sensor_driver_resp_params *response)
 {
     int status = FWK_SUCCESS;
     struct fwk_event event;
     struct sensor_dev_ctx *ctx;
 
     if (!fwk_expect(fwk_id_get_module_idx(dev_id) == FWK_MODULE_IDX_SENSOR)) {
-        return;
+	return;
     }
 
     ctx = &ctx_table[fwk_id_get_element_idx(dev_id)];
     event = (struct fwk_event) {
-        .id = mod_sensor_event_id_read_complete,
-        .source_id = ctx->config->driver_id,
-        .target_id = dev_id,
+	.id = mod_sensor_event_id_read_complete,
+	.source_id = ctx->config->driver_id,
+	.target_id = dev_id,
     };
 
     if (response != NULL) {
-        ctx->last_read.status = response->status;
+	ctx->last_read.status = response->status;
 
 #ifdef BUILD_HAS_SENSOR_TIMESTAMP
-        ctx->last_read.timestamp = sensor_get_timestamp(dev_id);
+	ctx->last_read.timestamp = sensor_get_timestamp(dev_id);
 #endif
 #ifdef BUILD_HAS_SENSOR_MULTI_AXIS
-        if (ctx->axis_count > 1) {
-            fwk_str_memcpy(
-                ctx->last_read.axis_value,
-                response->axis_value,
-                sizeof(uint64_t) * ctx->axis_count);
-        } else {
-            ctx->last_read.value = response->value;
-        }
+	if (ctx->axis_count > 1) {
+	    fwk_str_memcpy(
+		ctx->last_read.axis_value,
+		response->axis_value,
+		sizeof(uint64_t) * ctx->axis_count);
+	} else {
+	    ctx->last_read.value = response->value;
+	}
 #else
-        ctx->last_read.value = response->value;
+	ctx->last_read.value = response->value;
 #endif
 
 #ifdef BUILD_HAS_SCMI_SENSOR_EVENTS
-        trip_point_process(dev_id, &ctx->last_read);
+	trip_point_process(dev_id, &ctx->last_read);
 #endif
     } else {
-        ctx->last_read.status = FWK_E_DEVICE;
+	ctx->last_read.status = FWK_E_DEVICE;
     }
 
     ctx->concurrency_readings.dequeuing = true;
@@ -370,8 +370,8 @@ static int sensor_init(
 }
 
 static int sensor_dev_init(fwk_id_t element_id,
-                           unsigned int unused,
-                           const void *data)
+			   unsigned int unused,
+			   const void *data)
 {
     struct sensor_dev_ctx *ctx;
     struct mod_sensor_dev_config *config;
@@ -384,11 +384,11 @@ static int sensor_dev_init(fwk_id_t elem
     ctx->config = config;
 
     if (config->trip_point.count > 0) {
-        ctx->trip_point_ctx = fwk_mm_calloc(
-            config->trip_point.count, sizeof(struct sensor_trip_point_ctx));
-        ctx->trip_point_ctx->enabled = true;
+	ctx->trip_point_ctx = fwk_mm_calloc(
+	    config->trip_point.count, sizeof(struct sensor_trip_point_ctx));
+	ctx->trip_point_ctx->enabled = true;
     } else {
-        ctx->trip_point_ctx = NULL;
+	ctx->trip_point_ctx = NULL;
     }
 
     /* Pre-init last read with an invalid status */
@@ -411,42 +411,42 @@ static int sensor_bind(fwk_id_t id, unsi
     struct mod_sensor_driver_api *driver = NULL;
 
     if (round > 0) {
-        /*
-         * Only bind in first round of calls
-         */
-        return FWK_SUCCESS;
+	/*
+	 * Only bind in first round of calls
+	 */
+	return FWK_SUCCESS;
     }
     if (fwk_id_is_type(id, FWK_ID_TYPE_MODULE)) {
-        if (sensor_mod_ctx.config == NULL) {
-            return FWK_SUCCESS;
-        }
+	if (sensor_mod_ctx.config == NULL) {
+	    return FWK_SUCCESS;
+	}
 
 #ifdef BUILD_HAS_NOTIFICATION
-        if (fwk_id_is_equal(
-                sensor_mod_ctx.config->notification_id, FWK_ID_NONE)) {
-            return FWK_SUCCESS;
-        }
-
-        return fwk_module_bind(
-            sensor_mod_ctx.config->notification_id,
-            sensor_mod_ctx.config->trip_point_api_id,
-            &sensor_mod_ctx.sensor_trip_point_api);
+	if (fwk_id_is_equal(
+		sensor_mod_ctx.config->notification_id, FWK_ID_NONE)) {
+	    return FWK_SUCCESS;
+	}
+
+	return fwk_module_bind(
+	    sensor_mod_ctx.config->notification_id,
+	    sensor_mod_ctx.config->trip_point_api_id,
+	    &sensor_mod_ctx.sensor_trip_point_api);
 #else
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 #endif
     }
     ctx = ctx_table + fwk_id_get_element_idx(id);
     /* Bind to driver */
     status = fwk_module_bind(ctx->config->driver_id,
-        ctx->config->driver_api_id,
-        &driver);
+	ctx->config->driver_api_id,
+	&driver);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     /* Validate driver API */
     if ((driver == NULL) || (driver->get_value == NULL)) {
-        return FWK_E_DATA;
+	return FWK_E_DATA;
     }
 
     ctx->driver_api = driver;
@@ -459,50 +459,50 @@ int sensor_start(fwk_id_t id)
     int status;
 
     if (fwk_id_is_type(id, FWK_ID_TYPE_MODULE)) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
     status = sensor_axis_start(id);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
     return FWK_SUCCESS;
 }
 #endif
 
 static int sensor_process_bind_request(fwk_id_t source_id,
-                                       fwk_id_t target_id,
-                                       fwk_id_t api_id,
-                                       const void **api)
+				       fwk_id_t target_id,
+				       fwk_id_t api_id,
+				       const void **api)
 {
     struct sensor_dev_ctx *ctx;
     fwk_id_t driver_id;
 
     if (fwk_id_is_equal(api_id, mod_sensor_api_id_sensor)) {
-        *api = &sensor_api;
+	*api = &sensor_api;
 
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     if (fwk_id_is_equal(api_id, mod_sensor_api_id_driver_response)) {
-        if (!fwk_id_is_type(target_id, FWK_ID_TYPE_ELEMENT)) {
-            return FWK_E_PARAM;
-        }
-
-        ctx = ctx_table + fwk_id_get_element_idx(target_id);
-        driver_id = ctx->config->driver_id;
-
-        /* Allow element to sub-element binding */
-        if ((fwk_id_get_module_idx(driver_id) ==
-            fwk_id_get_module_idx(source_id)) &&
-            (fwk_id_get_element_idx(driver_id) ==
-            fwk_id_get_element_idx(source_id))) {
-
-            *api = &sensor_driver_response_api;
-
-            return FWK_SUCCESS;
-        } else {
-            return FWK_E_ACCESS;
-        }
+	if (!fwk_id_is_type(target_id, FWK_ID_TYPE_ELEMENT)) {
+	    return FWK_E_PARAM;
+	}
+
+	ctx = ctx_table + fwk_id_get_element_idx(target_id);
+	driver_id = ctx->config->driver_id;
+
+	/* Allow element to sub-element binding */
+	if ((fwk_id_get_module_idx(driver_id) ==
+	    fwk_id_get_module_idx(source_id)) &&
+	    (fwk_id_get_element_idx(driver_id) ==
+	    fwk_id_get_element_idx(source_id))) {
+
+	    *api = &sensor_driver_response_api;
+
+	    return FWK_SUCCESS;
+	} else {
+	    return FWK_E_ACCESS;
+	}
     }
 
     return FWK_E_PARAM;
@@ -516,34 +516,34 @@ static int process_pending_requests(
     bool list_is_empty;
     struct fwk_event delayed_response;
     struct mod_sensor_event_params *response_params =
-        (struct mod_sensor_event_params *)delayed_response.params;
+	(struct mod_sensor_event_params *)delayed_response.params;
     struct sensor_dev_ctx *ctx;
 
     status = fwk_is_delayed_response_list_empty(dev_id, &list_is_empty);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     ctx = &ctx_table[fwk_id_get_element_idx(dev_id)];
 
     for (; !list_is_empty && ctx->concurrency_readings.pending_requests > 0;
-         ctx->concurrency_readings.pending_requests--) {
-        status = fwk_get_first_delayed_response(dev_id, &delayed_response);
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
-
-        sensor_data_copy(response_params->sensor_data, &ctx->last_read);
-
-        status = fwk_put_event(&delayed_response);
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
-
-        status = fwk_is_delayed_response_list_empty(dev_id, &list_is_empty);
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
+	 ctx->concurrency_readings.pending_requests--) {
+	status = fwk_get_first_delayed_response(dev_id, &delayed_response);
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
+
+	sensor_data_copy(response_params->sensor_data, &ctx->last_read);
+
+	status = fwk_put_event(&delayed_response);
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
+
+	status = fwk_is_delayed_response_list_empty(dev_id, &list_is_empty);
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
     }
 
     ctx->concurrency_readings.pending_requests = 0;
@@ -553,17 +553,17 @@ static int process_pending_requests(
 }
 
 static int sensor_process_event(const struct fwk_event *event,
-                                struct fwk_event *resp_event)
+				struct fwk_event *resp_event)
 {
     int status;
     struct sensor_dev_ctx *ctx;
     struct fwk_event read_req_event;
     struct mod_sensor_event_params *event_params =
-        (struct mod_sensor_event_params *)read_req_event.params;
+	(struct mod_sensor_event_params *)read_req_event.params;
     enum mod_sensor_event_idx event_id_type;
 
     if (!fwk_module_is_valid_element_id(event->target_id)) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     ctx = ctx_table + fwk_id_get_element_idx(event->target_id);
@@ -572,43 +572,43 @@ static int sensor_process_event(const st
 
     switch (event_id_type) {
     case SENSOR_EVENT_IDX_READ_REQUEST:
-        if (ctx->concurrency_readings.pending_requests == 1) {
-            /*
-             * We keep the cookie event of the request that triggers the
-             * reading.
-             */
-            ctx->cookie = event->cookie;
-        }
-        resp_event->is_delayed_response = true;
+	if (ctx->concurrency_readings.pending_requests == 1) {
+	    /*
+	     * We keep the cookie event of the request that triggers the
+	     * reading.
+	     */
+	    ctx->cookie = event->cookie;
+	}
+	resp_event->is_delayed_response = true;
 
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 
     case SENSOR_EVENT_IDX_READ_COMPLETE:
-        status = fwk_get_delayed_response(
-            event->target_id, ctx->cookie, &read_req_event);
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
-
-        sensor_data_copy(
-            (struct mod_sensor_data *)event_params->sensor_data,
-            &ctx->last_read);
-
-        status = fwk_put_event(&read_req_event);
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
-
-        /*
-         * After a read complete event all pending requests are processed.
-         * We are processing pending events until it reaches a new reading
-         * or the event queue is empty.
-         */
-        return process_pending_requests(
-            event->target_id, (const struct mod_sensor_data *)event->params);
+	status = fwk_get_delayed_response(
+	    event->target_id, ctx->cookie, &read_req_event);
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
+
+	sensor_data_copy(
+	    (struct mod_sensor_data *)event_params->sensor_data,
+	    &ctx->last_read);
+
+	status = fwk_put_event(&read_req_event);
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
+
+	/*
+	 * After a read complete event all pending requests are processed.
+	 * We are processing pending events until it reaches a new reading
+	 * or the event queue is empty.
+	 */
+	return process_pending_requests(
+	    event->target_id, (const struct mod_sensor_data *)event->params);
 
     default:
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 }
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/sensor/src/sensor.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/sensor/src/sensor.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/sensor/src/sensor.h
@@ -49,8 +49,8 @@ struct sensor_dev_ctx {
     uint32_t cookie;
 
     struct {
-        uint32_t pending_requests;
-        bool dequeuing;
+	uint32_t pending_requests;
+	bool dequeuing;
     } concurrency_readings;
 
     struct mod_sensor_data last_read;
@@ -89,7 +89,7 @@ enum mod_sensor_event_idx {
  */
 static const fwk_id_t mod_sensor_event_id_read_complete =
     FWK_ID_EVENT_INIT(FWK_MODULE_IDX_SENSOR,
-                      SENSOR_EVENT_IDX_READ_COMPLETE);
+		      SENSOR_EVENT_IDX_READ_COMPLETE);
 
 #ifdef BUILD_HAS_SENSOR_TIMESTAMP
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/sensor/src/sensor_extended.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/sensor/src/sensor_extended.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/sensor/src/sensor_extended.c
@@ -24,7 +24,7 @@ int sensor_timestamp_dev_init(fwk_id_t i
 {
     ctx->timestamp.timestamp_support = ctx->config->timestamp.timestamp_support;
     if (ctx->timestamp.timestamp_support) {
-        return sensor_set_timestamp_config(id, &ctx->config->timestamp);
+	return sensor_set_timestamp_config(id, &ctx->config->timestamp);
     }
     return FWK_SUCCESS;
 }
@@ -37,13 +37,13 @@ int sensor_set_timestamp_config(
 
     fwk_assert(!fwk_id_is_equal(id, FWK_ID_NONE));
     if (config == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     ctx = sensor_get_ctx(id);
 
     if (!ctx->timestamp.timestamp_support) {
-        return FWK_E_SUPPORT;
+	return FWK_E_SUPPORT;
     }
 
     ctx->timestamp.enabled = config->enabled;
@@ -60,13 +60,13 @@ int sensor_get_timestamp_config(
 
     fwk_assert(!fwk_id_is_equal(id, FWK_ID_NONE));
     if (config == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     ctx = sensor_get_ctx(id);
 
     if (!ctx->timestamp.timestamp_support) {
-        return FWK_E_SUPPORT;
+	return FWK_E_SUPPORT;
     }
 
     config->timestamp_support = ctx->timestamp.timestamp_support;
@@ -81,7 +81,7 @@ static uint64_t pow_unsigned(uint64_t ba
 {
     uint64_t result = 1;
     for (; exponent > 0; exponent--) {
-        result *= base;
+	result *= base;
     }
     return result;
 }
@@ -95,7 +95,7 @@ uint64_t sensor_get_timestamp(fwk_id_t i
     ctx = sensor_get_ctx(id);
 
     if (!ctx->timestamp.enabled) {
-        return 0;
+	return 0;
     }
 
     timestamp = FWK_NS(fwk_time_current());
@@ -107,7 +107,7 @@ uint64_t sensor_get_timestamp(fwk_id_t i
      */
     normalized_exponent = ctx->timestamp.exponent + 9;
     if (normalized_exponent > 0) {
-        return timestamp / pow_unsigned(10, normalized_exponent);
+	return timestamp / pow_unsigned(10, normalized_exponent);
     }
     return timestamp / pow_unsigned(10, -normalized_exponent);
 }
@@ -127,40 +127,40 @@ int sensor_axis_start(fwk_id_t id)
     ctx = sensor_get_ctx(id);
 
     if (ctx->driver_api->get_axis_count) {
-        ctx->axis_count =
-            ctx->driver_api->get_axis_count(ctx->config->driver_id);
+	ctx->axis_count =
+	    ctx->driver_api->get_axis_count(ctx->config->driver_id);
     } else {
-        ctx->axis_count = 1;
+	ctx->axis_count = 1;
     }
     ctx->last_read.axis_value =
-        fwk_mm_calloc(ctx->axis_count, sizeof(uint64_t));
+	fwk_mm_calloc(ctx->axis_count, sizeof(uint64_t));
     ctx->last_read.axis_count = ctx->axis_count;
 
     if (ctx->config->trip_point.count > 0) {
-        for (i = 0; i < ctx->axis_count; i++) {
-            status = sensor_get_axis_info(id, i, &axis_info);
-            if (status != FWK_SUCCESS) {
-                return status;
-            }
-
-            /* Save value of first axis type */
-            if (i == 0) {
-                type = axis_info.type;
-            } else if (axis_info.type != type) {
-                ctx->trip_point_ctx->enabled = false;
-
-                /*
-                 * Valid situation where axis have different types, for that
-                 * reason trip points are disabled for this particular sensor.
-                 */
-                FWK_LOG_INFO(
-                    "[Sensor] Trip points are disable. Different axis type");
-
-                return FWK_SUCCESS;
-            }
-        }
+	for (i = 0; i < ctx->axis_count; i++) {
+	    status = sensor_get_axis_info(id, i, &axis_info);
+	    if (status != FWK_SUCCESS) {
+		return status;
+	    }
+
+	    /* Save value of first axis type */
+	    if (i == 0) {
+		type = axis_info.type;
+	    } else if (axis_info.type != type) {
+		ctx->trip_point_ctx->enabled = false;
+
+		/*
+		 * Valid situation where axis have different types, for that
+		 * reason trip points are disabled for this particular sensor.
+		 */
+		FWK_LOG_INFO(
+		    "[Sensor] Trip points are disable. Different axis type");
+
+		return FWK_SUCCESS;
+	    }
+	}
 
-        ctx->trip_point_ctx->enabled = true;
+	ctx->trip_point_ctx->enabled = true;
     }
 
     return FWK_SUCCESS;
@@ -174,13 +174,13 @@ int sensor_get_axis_info(
     struct sensor_dev_ctx *ctx;
 
     if (info == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     ctx = sensor_get_ctx(id);
 
     if (axis > ctx->axis_count) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     ctx->driver_api->get_axis_info(ctx->config->driver_id, axis, info);
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/sid/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/sid/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/sid/CMakeLists.txt
@@ -8,10 +8,10 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_sid.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_sid.c")
 
 target_link_libraries(${SCP_MODULE_TARGET} PRIVATE module-pcid
-                                                   module-system-info)
+						   module-system-info)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/sid/src/mod_sid.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/sid/src/mod_sid.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/sid/src/mod_sid.c
@@ -26,9 +26,9 @@ static struct mod_system_info sys_info;
 int mod_sid_get_system_info(const struct mod_sid_info **system_info)
 {
     if (initialized) {
-        *system_info = &info;
+	*system_info = &info;
 
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     return FWK_E_INIT;
@@ -37,7 +37,7 @@ int mod_sid_get_system_info(const struct
 static struct mod_system_info *get_driver_data(void)
 {
     if (!initialized)
-        return NULL;
+	return NULL;
 
     return &sys_info;
 }
@@ -61,46 +61,46 @@ static int sid_init(
     struct sid_reg *sid_reg;
 
     if ((config == NULL) || (config->sid_base == 0) || (element_count == 0)) {
-        fwk_unexpected();
-        return FWK_E_DATA;
+	fwk_unexpected();
+	return FWK_E_DATA;
     }
 
     sid_reg = (struct sid_reg *)config->sid_base;
 
     fwk_assert(
-        mod_pcid_check_registers(&sid_reg->pcid, &config->pcid_expected));
+	mod_pcid_check_registers(&sid_reg->pcid, &config->pcid_expected));
 
     info.system_major_revision =
-        (sid_reg->SYSTEM_ID & SID_SYS_SOC_ID_MAJOR_REVISION_MASK)
-        >> SID_SYS_SOC_ID_MAJOR_REVISION_POS;
+	(sid_reg->SYSTEM_ID & SID_SYS_SOC_ID_MAJOR_REVISION_MASK)
+	>> SID_SYS_SOC_ID_MAJOR_REVISION_POS;
 
     info.system_minor_revision =
-        (sid_reg->SYSTEM_ID & SID_SYS_SOC_ID_MINOR_REVISION_MASK)
-        >> SID_SYS_SOC_ID_MINOR_REVISION_POS;
+	(sid_reg->SYSTEM_ID & SID_SYS_SOC_ID_MINOR_REVISION_MASK)
+	>> SID_SYS_SOC_ID_MINOR_REVISION_POS;
 
     info.system_designer_id =
-        (sid_reg->SYSTEM_ID & SID_SYS_SOC_ID_DESIGNER_ID_MASK)
-        >> SID_SYS_SOC_ID_DESIGNER_ID_POS;
+	(sid_reg->SYSTEM_ID & SID_SYS_SOC_ID_DESIGNER_ID_MASK)
+	>> SID_SYS_SOC_ID_DESIGNER_ID_POS;
 
     info.system_part_number =
-        sid_reg->SYSTEM_ID & SID_SYS_SOC_ID_PART_NUMBER_MASK;
+	sid_reg->SYSTEM_ID & SID_SYS_SOC_ID_PART_NUMBER_MASK;
 
     info.soc_major_revision =
-        (sid_reg->SOC_ID & SID_SYS_SOC_ID_MAJOR_REVISION_MASK)
-        >> SID_SYS_SOC_ID_MAJOR_REVISION_POS;
+	(sid_reg->SOC_ID & SID_SYS_SOC_ID_MAJOR_REVISION_MASK)
+	>> SID_SYS_SOC_ID_MAJOR_REVISION_POS;
 
     info.soc_minor_revision =
-        (sid_reg->SOC_ID & SID_SYS_SOC_ID_MINOR_REVISION_MASK)
-        >> SID_SYS_SOC_ID_MINOR_REVISION_POS;
+	(sid_reg->SOC_ID & SID_SYS_SOC_ID_MINOR_REVISION_MASK)
+	>> SID_SYS_SOC_ID_MINOR_REVISION_POS;
 
     info.soc_designer_id =
-        (sid_reg->SOC_ID & SID_SYS_SOC_ID_DESIGNER_ID_MASK)
-        >> SID_SYS_SOC_ID_DESIGNER_ID_POS;
+	(sid_reg->SOC_ID & SID_SYS_SOC_ID_DESIGNER_ID_MASK)
+	>> SID_SYS_SOC_ID_DESIGNER_ID_POS;
 
     info.soc_part_number = sid_reg->SOC_ID & SID_SYS_SOC_ID_PART_NUMBER_MASK;
 
     info.multi_chip_mode = (sid_reg->NODE_ID & SID_SYS_MULTI_CHIP_MODE_MASK)
-                            >> SID_SYS_MULTI_CHIP_MODE_POS;
+			    >> SID_SYS_MULTI_CHIP_MODE_POS;
     info.node_number = sid_reg->NODE_ID & SID_SYS_NODE_NUMBER_MASK;
 
     info.config_number = sid_reg->SYSTEM_CFG;
@@ -119,32 +119,32 @@ static int sid_subsystem_init(
 
     subsystem_config = data;
     if (subsystem_config->part_number == info.system_part_number) {
-        info.system_idx = fwk_id_get_element_idx(subsystem_id);
-        info.name = fwk_module_get_element_name(
-            FWK_ID_ELEMENT(FWK_MODULE_IDX_SID, info.system_idx));
-
-        /* Populate the system info structure */
-        sys_info.product_id = info.system_part_number;
-        sys_info.config_id = info.config_number;
-        sys_info.chip_id = info.node_number;
-        sys_info.multi_chip_mode = info.multi_chip_mode;
-        sys_info.name = info.name;
+	info.system_idx = fwk_id_get_element_idx(subsystem_id);
+	info.name = fwk_module_get_element_name(
+	    FWK_ID_ELEMENT(FWK_MODULE_IDX_SID, info.system_idx));
+
+	/* Populate the system info structure */
+	sys_info.product_id = info.system_part_number;
+	sys_info.config_id = info.config_number;
+	sys_info.chip_id = info.node_number;
+	sys_info.multi_chip_mode = info.multi_chip_mode;
+	sys_info.name = info.name;
 
-        initialized = true;
+	initialized = true;
     }
 
     return FWK_SUCCESS;
 }
 
 static int sid_process_bind_request(fwk_id_t requester_id, fwk_id_t targer_id,
-        fwk_id_t api_id, const void **api)
+	fwk_id_t api_id, const void **api)
 {
     switch (fwk_id_get_api_idx(api_id)) {
     case MOD_SID_SYSTEM_INFO_DRIVER_DATA_API_IDX:
-        *api = &get_sys_info_driver_data;
-        break;
+	*api = &get_sys_info_driver_data;
+	break;
     default:
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
     return FWK_SUCCESS;
 }
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/sid/src/sid_reg.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/sid/src/sid_reg.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/sid/src/sid_reg.h
@@ -15,15 +15,15 @@
 #include <stdint.h>
 
 struct sid_reg {
-            uint8_t   RESERVED0[0x40 - 0];
+	    uint8_t   RESERVED0[0x40 - 0];
     FWK_R   uint32_t  SYSTEM_ID;
-            uint8_t   RESERVED1[0x50 - 0x44];
+	    uint8_t   RESERVED1[0x50 - 0x44];
     FWK_R   uint32_t  SOC_ID;
-            uint8_t   RESERVED2[0x60 - 0x54];
+	    uint8_t   RESERVED2[0x60 - 0x54];
     FWK_R   uint32_t  NODE_ID;
-            uint8_t   RESERVED3[0x70 - 0x64];
+	    uint8_t   RESERVED3[0x70 - 0x64];
     FWK_R   uint32_t  SYSTEM_CFG;
-            uint8_t   RESERVED4[0xFD0 - 0x74];
+	    uint8_t   RESERVED4[0xFD0 - 0x74];
     const struct mod_pcid_registers pcid;
 };
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/sp805/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/sp805/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/sp805/CMakeLists.txt
@@ -8,9 +8,9 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_sp805.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_sp805.c")
 
 target_link_libraries(${SCP_MODULE_TARGET} PRIVATE module-clock)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/sp805/src/mod_sp805.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/sp805/src/mod_sp805.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/sp805/src/mod_sp805.c
@@ -44,10 +44,10 @@ static void enable_sp805_interrupt(void)
      * when the counter value reaches zero.
      */
     if (ctx.config->sp805_irq == FWK_INTERRUPT_NONE) {
-        FWK_LOG_ERR(MOD_NAME
-                    "Interrupt number not specified in config data, failed to "
-                    "enable watchdog!");
-        return;
+	FWK_LOG_ERR(MOD_NAME
+		    "Interrupt number not specified in config data, failed to "
+		    "enable watchdog!");
+	return;
     }
 
     ctx.reg_base->LOCK = ENABLE_WR_ACCESS;
@@ -57,14 +57,14 @@ static void enable_sp805_interrupt(void)
     ctx.reg_base->CONTROL = RESET_EN | INT_EN;
     ctx.reg_base->LOCK = DISABLE_WR_ACCESS;
     fwk_interrupt_set_isr_param(
-        ctx.config->sp805_irq, sp805_isr, (uintptr_t)NULL);
+	ctx.config->sp805_irq, sp805_isr, (uintptr_t)NULL);
 
     fwk_interrupt_clear_pending(ctx.config->sp805_irq);
     fwk_interrupt_enable(ctx.config->sp805_irq);
 
     FWK_LOG_INFO(
-        MOD_NAME "WatchDog Interrupt enabled, WdogLoad value: %x",
-        (unsigned int)ctx.reg_base->LOAD);
+	MOD_NAME "WatchDog Interrupt enabled, WdogLoad value: %x",
+	(unsigned int)ctx.reg_base->LOAD);
 }
 
 /*
@@ -80,8 +80,8 @@ static int mod_sp805_init(
 
     ctx.reg_base = (struct sp805_reg *)ctx.config->reg_base;
     if (ctx.reg_base == NULL) {
-        FWK_LOG_ERR(MOD_NAME "Register base address missing in config data");
-        return FWK_E_DATA;
+	FWK_LOG_ERR(MOD_NAME "Register base address missing in config data");
+	return FWK_E_DATA;
     }
 
     return FWK_SUCCESS;
@@ -90,11 +90,11 @@ static int mod_sp805_init(
 static int mod_sp805_start(fwk_id_t id)
 {
     if (!fwk_id_is_type(ctx.config->driver_id, FWK_ID_TYPE_NONE)) {
-        /* Register for clock state notifications */
-        return fwk_notification_subscribe(
-            mod_clock_notification_id_state_changed, ctx.config->driver_id, id);
+	/* Register for clock state notifications */
+	return fwk_notification_subscribe(
+	    mod_clock_notification_id_state_changed, ctx.config->driver_id, id);
     } else {
-        enable_sp805_interrupt();
+	enable_sp805_interrupt();
     }
 
     return FWK_SUCCESS;
@@ -107,11 +107,11 @@ static int mod_sp805_process_notificatio
     struct clock_notification_params *params;
 
     fwk_assert(
-        fwk_id_is_equal(event->id, mod_clock_notification_id_state_changed));
+	fwk_id_is_equal(event->id, mod_clock_notification_id_state_changed));
 
     params = (struct clock_notification_params *)event->params;
     if (params->new_state == MOD_CLOCK_STATE_RUNNING) {
-        enable_sp805_interrupt();
+	enable_sp805_interrupt();
     }
 
     return FWK_SUCCESS;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/ssc/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/ssc/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/ssc/CMakeLists.txt
@@ -13,6 +13,6 @@ target_include_directories(
     PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_ssc.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_ssc.c")
 
 target_link_libraries(${SCP_MODULE_TARGET} PRIVATE module-system-info)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/ssc/src/mod_ssc.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/ssc/src/mod_ssc.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/ssc/src/mod_ssc.c
@@ -24,7 +24,7 @@ static struct mod_system_info sys_info;
 static struct mod_system_info *get_driver_data(void)
 {
     if (!initialized)
-        return NULL;
+	return NULL;
 
     return &sys_info;
 }
@@ -40,28 +40,28 @@ static struct mod_system_info_get_driver
  * Framework Handlers
  */
 static int ssc_init(fwk_id_t module_id, unsigned int count,
-                          const void *data)
+			  const void *data)
 {
     const struct mod_ssc_config *config = data;
     struct ssc_reg *ssc_reg;
 
     if ((config == NULL) || (config->ssc_base == 0))
-        return FWK_E_DATA;
+	return FWK_E_DATA;
 
     ssc_reg = (struct ssc_reg *)config->ssc_base;
 
     chip_id = (ssc_reg->SSC_CHIPID_ST) & SSC_CHIPID_ST_CHIP_ID_MASK;
     multi_chip_mode = ((ssc_reg->SSC_CHIPID_ST) &
-                       SSC_CHIPID_ST_MULTI_CHIP_MODE_MASK) >>
-                      SSC_CHIPID_ST_MULTI_CHIP_MODE_POS;
+		       SSC_CHIPID_ST_MULTI_CHIP_MODE_MASK) >>
+		      SSC_CHIPID_ST_MULTI_CHIP_MODE_POS;
     part_number = (ssc_reg->PID0 & SSC_PID0_PART_NUMBER_MASK) |
-                  ((ssc_reg->PID1 & SSC_PID1_PART_NUMBER_MASK) <<
-                   SSC_PID1_PART_NUMBER_POS);
+		  ((ssc_reg->PID1 & SSC_PID1_PART_NUMBER_MASK) <<
+		   SSC_PID1_PART_NUMBER_POS);
     revision_number = (ssc_reg->PID2 & SSC_PID2_REVISION_NUMBER_MASK) >>
-                      SSC_PID2_REVISION_NUMBER_POS;
+		      SSC_PID2_REVISION_NUMBER_POS;
 
     if (config->ssc_debug_cfg_set != 0)
-        ssc_reg->SSC_DBGCFG_SET = config->ssc_debug_cfg_set;
+	ssc_reg->SSC_DBGCFG_SET = config->ssc_debug_cfg_set;
 
     /* Populate the system info structure */
     sys_info.product_id = part_number;
@@ -76,14 +76,14 @@ static int ssc_init(fwk_id_t module_id,
 }
 
 static int ssc_process_bind_request(fwk_id_t requester_id, fwk_id_t targer_id,
-        fwk_id_t api_id, const void **api)
+	fwk_id_t api_id, const void **api)
 {
     switch (fwk_id_get_api_idx(api_id)) {
     case MOD_SSC_SYSTEM_INFO_DRIVER_DATA_API_IDX:
-        *api = &get_sys_info_driver_data;
-        break;
+	*api = &get_sys_info_driver_data;
+	break;
     default:
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
     return FWK_SUCCESS;
 }
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/ssc/src/ssc_reg.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/ssc/src/ssc_reg.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/ssc/src/ssc_reg.h
@@ -16,21 +16,21 @@
  * \brief System Security Control (SSC) register definitions
  */
 struct ssc_reg {
-           uint8_t  RESERVED1[0x10 - 0x0];
+	   uint8_t  RESERVED1[0x10 - 0x0];
     FWK_R  uint32_t SSC_DBGCFG_STAT;
     FWK_W  uint32_t SSC_DBGCFG_SET;
     FWK_W  uint32_t SSC_DBGCFG_CLR;
-           uint8_t  RESERVED2[0x28 - 0x1C];
+	   uint8_t  RESERVED2[0x28 - 0x1C];
     FWK_RW uint32_t SSC_AUXDBGCFG;
-           uint8_t  RESERVED3[0x30 - 0x2C];
+	   uint8_t  RESERVED3[0x30 - 0x2C];
     FWK_RW uint32_t SSC_GPRETN;
-           uint8_t  RESERVED4[0x40 - 0x34];
+	   uint8_t  RESERVED4[0x40 - 0x34];
     FWK_R  uint32_t SSC_VERSION;
-           uint8_t  RESERVED5[0x500 - 0x44];
+	   uint8_t  RESERVED5[0x500 - 0x44];
     FWK_R  uint32_t SSC_CHIPID_ST;
-           uint8_t  RESERVED6[0xFD0 - 0x504];
+	   uint8_t  RESERVED6[0xFD0 - 0x504];
     FWK_R  uint32_t PID4;
-           uint8_t  RESERVED7[0xFE0 - 0xFD4];
+	   uint8_t  RESERVED7[0xFE0 - 0xFD4];
     FWK_R  uint32_t PID0;
     FWK_R  uint32_t PID1;
     FWK_R  uint32_t PID2;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/statistics/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/statistics/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/statistics/CMakeLists.txt
@@ -8,9 +8,9 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_stats.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_stats.c")
 
 target_link_libraries(${SCP_MODULE_TARGET} PRIVATE module-timer)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/statistics/include/mod_stats.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/statistics/include/mod_stats.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/statistics/include/mod_stats.h
@@ -223,9 +223,9 @@ struct mod_stats_api {
      * \param used_domains Total number of domains that have statistics.
      */
     int (*init_stats)(
-        fwk_id_t module_id,
-        int domain_count,
-        int used_domains);
+	fwk_id_t module_id,
+	int domain_count,
+	int used_domains);
 
     /*!
      * \brief Start the statistics for the given module.
@@ -242,9 +242,9 @@ struct mod_stats_api {
      * \param level_count Total number of levels in this domain.
      */
     int (*add_domain)(
-        fwk_id_t module_id,
-        fwk_id_t domain_id,
-        int level_count);
+	fwk_id_t module_id,
+	fwk_id_t domain_id,
+	int level_count);
 
     /*!
      * \brief Update the domain statistics with new level ID set.
@@ -254,9 +254,9 @@ struct mod_stats_api {
      * \param level_id Current operating level ID.
      */
     int (*update_domain)(
-        fwk_id_t module_id,
-        fwk_id_t domain_id,
-        uint32_t level_id);
+	fwk_id_t module_id,
+	fwk_id_t domain_id,
+	uint32_t level_id);
 
     /*!
      * \brief Get low and high addresses of statistics in AP address space
@@ -268,10 +268,10 @@ struct mod_stats_api {
      * \param [out] length of the statistics memory region
      */
     int (*get_statistics_desc)(
-        fwk_id_t module_id,
-        uint32_t *addr_low,
-        uint32_t *addr_high,
-        uint32_t *len);
+	fwk_id_t module_id,
+	uint32_t *addr_low,
+	uint32_t *addr_high,
+	uint32_t *len);
 };
 /*!
  * \}
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/statistics/src/mod_stats.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/statistics/src/mod_stats.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/statistics/src/mod_stats.c
@@ -47,13 +47,13 @@ static struct mod_stats_ctx stats_ctx;
 static struct mod_stats_info *get_module_stats_info(fwk_id_t module_id)
 {
     if (fwk_id_get_module_idx(module_id) ==
-        fwk_id_get_module_idx(fwk_module_id_scmi_perf)) {
-        return stats_ctx.perf_stats;
+	fwk_id_get_module_idx(fwk_module_id_scmi_perf)) {
+	return stats_ctx.perf_stats;
     }
 
     if (fwk_id_get_module_idx(module_id) ==
-        fwk_id_get_module_idx(fwk_module_id_scmi_power_domain)) {
-        return stats_ctx.power_stats;
+	fwk_id_get_module_idx(fwk_module_id_scmi_power_domain)) {
+	return stats_ctx.power_stats;
     }
 
     return NULL;
@@ -65,17 +65,17 @@ static int set_module_stats_info(fwk_id_
     int ret = FWK_E_PARAM;
 
     if (fwk_id_get_module_idx(module_id) ==
-        fwk_id_get_module_idx(fwk_module_id_scmi_perf)) {
-        stats_ctx.perf_stats = stats;
-        stats->type_signature = STATS_SIGN_PERF;
-        ret = FWK_SUCCESS;
+	fwk_id_get_module_idx(fwk_module_id_scmi_perf)) {
+	stats_ctx.perf_stats = stats;
+	stats->type_signature = STATS_SIGN_PERF;
+	ret = FWK_SUCCESS;
     }
 
     if (fwk_id_get_module_idx(module_id) ==
-        fwk_id_get_module_idx(fwk_module_id_scmi_power_domain)) {
-        stats_ctx.power_stats = stats;
-        stats->type_signature = STATS_SIGN_POWR;
-        ret = FWK_SUCCESS;
+	fwk_id_get_module_idx(fwk_module_id_scmi_power_domain)) {
+	stats_ctx.power_stats = stats;
+	stats->type_signature = STATS_SIGN_POWR;
+	ret = FWK_SUCCESS;
     }
 
     return ret;
@@ -96,7 +96,7 @@ static int allocate_domain_stats(fwk_id_
 
     stats = get_module_stats_info(module_id);
     if (stats == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     idx = fwk_id_get_element_idx(domain_id);
@@ -115,10 +115,10 @@ static int allocate_domain_stats(fwk_id_
     stats_size += sizeof(struct mod_stats_domain_stats_data);
 
     if (stats_size > (stats_ctx.config->stats_region_size -
-        stats_ctx.avail_mem_offset)) {
-        FWK_LOG_ERR("[STATS]: Error, size of statistics region too small\n");
-        stats->mode = STATS_INTERNAL_ERROR;
-        return FWK_E_NOMEM;
+	stats_ctx.avail_mem_offset)) {
+	FWK_LOG_ERR("[STATS]: Error, size of statistics region too small\n");
+	stats->mode = STATS_INTERNAL_ERROR;
+	return FWK_E_NOMEM;
     }
 
     se_map = stats->context->se_stats_map;
@@ -131,9 +131,9 @@ static int allocate_domain_stats(fwk_id_
 
     /* Address used in SCP to get domain statistics in the shared region */
     scp_stats_addr = stats_ctx.config->scp_stats_addr +
-                     stats_ctx.avail_mem_offset;
+		     stats_ctx.avail_mem_offset;
     se_map->se_stats[idx] = (struct mod_stats_domain_stats_data *)
-                            scp_stats_addr;
+			    scp_stats_addr;
 
     /* Shrink the free space in the shared region */
     stats_ctx.avail_mem_offset += stats_size;
@@ -141,9 +141,9 @@ static int allocate_domain_stats(fwk_id_
     stats->used_mem_size += stats_size;
 
     FWK_LOG_DEBUG(
-        "[STATS]: stats addr %lx, stats_size=%luB\n",
-        (uint32_t)scp_stats_addr,
-        stats_size);
+	"[STATS]: stats addr %lx, stats_size=%luB\n",
+	(uint32_t)scp_stats_addr,
+	stats_size);
 
     return FWK_SUCCESS;
 }
@@ -154,16 +154,16 @@ static int _allocate_header(struct mod_s
     stats->desc_header_size += domain_count * sizeof(uint32_t);
 
     if (stats->desc_header_size > (stats_ctx.config->stats_region_size -
-        stats_ctx.avail_mem_offset)) {
-        FWK_LOG_ERR("[STATS]: Error, size of statistics region too small\n");
-        stats->mode = STATS_INTERNAL_ERROR;
-        return FWK_E_NOMEM;
+	stats_ctx.avail_mem_offset)) {
+	FWK_LOG_ERR("[STATS]: Error, size of statistics region too small\n");
+	stats->mode = STATS_INTERNAL_ERROR;
+	return FWK_E_NOMEM;
     }
 
     stats->desc_header_offset = stats_ctx.avail_mem_offset;
     stats->desc_header = (struct mod_stats_desc_header *)
-                         (stats_ctx.config->scp_stats_addr +
-                          stats_ctx.avail_mem_offset);
+			 (stats_ctx.config->scp_stats_addr +
+			  stats_ctx.avail_mem_offset);
 
     stats->used_mem_size += stats->desc_header_size;
     stats_ctx.avail_mem_offset += stats->desc_header_size;
@@ -182,14 +182,14 @@ _allocate_stats_context(int domain_count
     stats = fwk_mm_calloc(1, sizeof(struct mod_stats_info));
     stats->context = fwk_mm_calloc(1, sizeof(struct mod_stats_context));
     stats->context->se_index_map =
-        (int *)fwk_mm_calloc((size_t)domain_count, sizeof(int));
+	(int *)fwk_mm_calloc((size_t)domain_count, sizeof(int));
 
     stats->mode = STATS_SETUP;
 
     /* Set default values indicating that the domain is not used in
      * statistics collection */
     for (i = 0; i < domain_count; i++) {
-        stats->context->se_index_map[i] = FWK_E_SUPPORT;
+	stats->context->se_index_map[i] = FWK_E_SUPPORT;
     }
 
     stats->context->se_total_num = domain_count;
@@ -203,13 +203,13 @@ _allocate_stats_context(int domain_count
     se_map_size += sizeof(struct mod_stats_map);
 
     stats->context->se_stats_map =
-        (struct mod_stats_map *)fwk_mm_calloc(1, (size_t)se_map_size);
+	(struct mod_stats_map *)fwk_mm_calloc(1, (size_t)se_map_size);
     se_map = stats->context->se_stats_map;
 
     se_map->se_level_count =
-        (int *)fwk_mm_calloc((size_t)used_domains, sizeof(int));
+	(int *)fwk_mm_calloc((size_t)used_domains, sizeof(int));
     se_map->se_curr_level =
-        (uint32_t *)fwk_mm_calloc((size_t)used_domains, sizeof(uint32_t));
+	(uint32_t *)fwk_mm_calloc((size_t)used_domains, sizeof(uint32_t));
 
     return stats;
 }
@@ -222,7 +222,7 @@ static int stats_init_module(fwk_id_t mo
     int ret;
 
     FWK_LOG_INFO("[STATS]: init module, total_domains=%d used=%d\n",
-                 domain_count, used_domains);
+		 domain_count, used_domains);
 
     stats = _allocate_stats_context(domain_count, used_domains);
     ret = set_module_stats_info(module_id, stats);
@@ -231,7 +231,7 @@ static int stats_init_module(fwk_id_t mo
 
     ret = _allocate_header(stats, domain_count);
     if (ret != FWK_SUCCESS) {
-        return ret;
+	return ret;
     }
 
     stats->desc_header->signature = stats->type_signature;
@@ -246,17 +246,17 @@ static int stats_start_module(fwk_id_t m
 
     stats = get_module_stats_info(module_id);
     if (stats == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     if (stats->mode == STATS_SETUP) {
-        if (stats->context->last_stats_id == stats->context->se_used_num) {
-            stats->mode = STATS_INITIALIZED;
-            return FWK_SUCCESS;
-        } else {
-            stats->mode = STATS_NOT_SUPPORTED;
-            return FWK_E_SUPPORT;
-        }
+	if (stats->context->last_stats_id == stats->context->se_used_num) {
+	    stats->mode = STATS_INITIALIZED;
+	    return FWK_SUCCESS;
+	} else {
+	    stats->mode = STATS_NOT_SUPPORTED;
+	    return FWK_E_SUPPORT;
+	}
     }
 
     return FWK_E_SUPPORT;
@@ -273,14 +273,14 @@ get_domain_section_data(fwk_id_t module_
     idx = fwk_id_get_element_idx(domain_id);
     stats = get_module_stats_info(module_id);
     if (stats == NULL) {
-        return NULL;
+	return NULL;
     }
 
     fwk_assert((int)idx < stats->context->se_total_num);
 
     stats_id = stats->context->se_index_map[idx];
     if (stats_id != FWK_E_SUPPORT) {
-        domain_stats = stats->context->se_stats_map->se_stats[stats_id];
+	domain_stats = stats->context->se_stats_map->se_stats[stats_id];
     }
 
     return domain_stats;
@@ -299,26 +299,26 @@ static int stats_add_domain(fwk_id_t mod
     idx = fwk_id_get_element_idx(domain_id);
     stats = get_module_stats_info(module_id);
     if (stats == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     fwk_assert((int)idx < stats->context->se_total_num);
 
     ret = allocate_domain_stats(module_id, domain_id, level_count);
     if (ret != FWK_SUCCESS) {
-        return ret;
+	return ret;
     }
 
     domain_stats = get_domain_section_data(module_id, domain_id);
     if (domain_stats == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     domain_stats->level_count = (uint16_t)level_count;
 
     for (i = 0; i < level_count; i++) {
-        level_stats = &domain_stats->level[i];
-        level_stats->level_id = (uint32_t)i;
+	level_stats = &domain_stats->level[i];
+	level_stats->level_id = (uint32_t)i;
     }
 
     return FWK_SUCCESS;
@@ -352,16 +352,16 @@ stats_update_domain(fwk_id_t module_id,
 
     stats = get_module_stats_info(module_id);
     if (stats == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     if (stats->mode != STATS_INITIALIZED) {
-        return FWK_E_SUPPORT;
+	return FWK_E_SUPPORT;
     }
 
     domain_stats = get_domain_section_data(module_id, domain_id);
     if (domain_stats == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     idx = fwk_id_get_element_idx(domain_id);
@@ -370,7 +370,7 @@ stats_update_domain(fwk_id_t module_id,
     stats_id = stats->context->se_index_map[idx];
 
     if ((int)level_id >= se_map->se_level_count[stats_id]) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     ts_now_us = _get_curret_ts_us();
@@ -406,11 +406,11 @@ get_statistics_desc(fwk_id_t module_id,
 
     stats = get_module_stats_info(module_id);
     if (stats == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     if (stats->mode != STATS_INITIALIZED) {
-        return FWK_E_SUPPORT;
+	return FWK_E_SUPPORT;
     }
 
     ap_stats_addr = stats_ctx.config->ap_stats_addr;
@@ -445,36 +445,36 @@ static void update_all_domains_current_l
 
     stats = get_module_stats_info(module_id);
     if (stats == NULL) {
-        return;
+	return;
     }
 
     if (stats->mode != STATS_INITIALIZED) {
-        return;
+	return;
     }
 
     for (i = 0; i < stats->context->se_total_num; i++) {
-        domain_id = FWK_ID_ELEMENT(fwk_id_get_module_idx(module_id), i);
-        domain_stats = get_domain_section_data(module_id, domain_id);
-        if (domain_stats == NULL) {
-            continue;
-        }
+	domain_id = FWK_ID_ELEMENT(fwk_id_get_module_idx(module_id), i);
+	domain_stats = get_domain_section_data(module_id, domain_id);
+	if (domain_stats == NULL) {
+	    continue;
+	}
 
-        se_map = stats->context->se_stats_map;
-        stats_id = stats->context->se_index_map[i];
+	se_map = stats->context->se_stats_map;
+	stats_id = stats->context->se_index_map[i];
 
-        flags = fwk_interrupt_global_disable();
+	flags = fwk_interrupt_global_disable();
 
-        ts_now_us = _get_curret_ts_us();
+	ts_now_us = _get_curret_ts_us();
 
-        /* Update current operation level statistics */
-        delta_t = ts_now_us - domain_stats->ts_last_change_us;
+	/* Update current operation level statistics */
+	delta_t = ts_now_us - domain_stats->ts_last_change_us;
 
-        curr_level_id = se_map->se_curr_level[stats_id];
-        level_stats = &domain_stats->level[curr_level_id];
-        level_stats->total_residency_us += delta_t;
-        domain_stats->ts_last_change_us = ts_now_us;
+	curr_level_id = se_map->se_curr_level[stats_id];
+	level_stats = &domain_stats->level[curr_level_id];
+	level_stats->total_residency_us += delta_t;
+	domain_stats->ts_last_change_us = ts_now_us;
 
-        fwk_interrupt_global_enable(flags);
+	fwk_interrupt_global_enable(flags);
     }
 
 }
@@ -499,8 +499,8 @@ static int stats_init(fwk_id_t module_id
     const struct mod_stats_config_info *config = data;
 
     if (config == NULL || config->stats_region_size == 0) {
-        FWK_LOG_INFO("STATS: statistics are not configured\n");
-        return FWK_E_SUPPORT;
+	FWK_LOG_INFO("STATS: statistics are not configured\n");
+	return FWK_E_SUPPORT;
     }
 
     /* Force platform to populate all addresses with valid data */
@@ -510,7 +510,7 @@ static int stats_init(fwk_id_t module_id
 
     /* Make sure that there is no stale data */
     fwk_str_memset(
-        (void *)config->scp_stats_addr, 0, config->stats_region_size);
+	(void *)config->scp_stats_addr, 0, config->stats_region_size);
 
     stats_ctx.config = config;
     stats_ctx.avail_mem_offset = 0;
@@ -535,16 +535,16 @@ static int stats_start(fwk_id_t id)
     int status;
 
     if (!fwk_id_is_equal(stats_ctx.config->alarm_id, FWK_ID_NONE)) {
-        status = stats_ctx.alarm_api->start(stats_ctx.config->alarm_id,
-            STATS_UPDATE_PERIOD_MS, MOD_TIMER_ALARM_TYPE_PERIODIC,
-            periodic_update_callback, (uintptr_t)0);
-
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
+	status = stats_ctx.alarm_api->start(stats_ctx.config->alarm_id,
+	    STATS_UPDATE_PERIOD_MS, MOD_TIMER_ALARM_TYPE_PERIODIC,
+	    periodic_update_callback, (uintptr_t)0);
+
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
     } else {
-        FWK_LOG_ERR("[STATS]: failed to start period updates\n");
-        return FWK_E_SUPPORT;
+	FWK_LOG_ERR("[STATS]: failed to start period updates\n");
+	return FWK_E_SUPPORT;
     }
 
     return FWK_SUCCESS;
@@ -555,15 +555,15 @@ static int stats_bind(fwk_id_t id, unsig
     int status;
 
     if (round >= 1) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     if (!fwk_id_is_equal(stats_ctx.config->alarm_id, FWK_ID_NONE)) {
-        status = fwk_module_bind(stats_ctx.config->alarm_id,
-            MOD_TIMER_API_ID_ALARM, &stats_ctx.alarm_api);
-        if (status != FWK_SUCCESS) {
-            return FWK_E_PANIC;
-        }
+	status = fwk_module_bind(stats_ctx.config->alarm_id,
+	    MOD_TIMER_API_ID_ALARM, &stats_ctx.alarm_api);
+	if (status != FWK_SUCCESS) {
+	    return FWK_E_PANIC;
+	}
     }
 
     return FWK_SUCCESS;
@@ -576,21 +576,21 @@ static int process_bind_request(fwk_id_t
 {
     /* Only allow binding to the module */
     if (!fwk_id_is_equal(target_id, fwk_module_id_statistics)) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     *api = &mod_statistics_api;
 
     /* Request from SCMI Performance domain statistics */
     if (fwk_id_get_module_idx(source_id) ==
-        fwk_id_get_module_idx(fwk_module_id_scmi_perf)) {
-        return register_module_stats(fwk_module_id_scmi_perf);
+	fwk_id_get_module_idx(fwk_module_id_scmi_perf)) {
+	return register_module_stats(fwk_module_id_scmi_perf);
     }
 
     /* Request from SCMI Power domain statistics*/
     if (fwk_id_get_module_idx(source_id) ==
-        fwk_id_get_module_idx(fwk_module_id_scmi_power_domain)) {
-        return register_module_stats(fwk_module_id_scmi_power_domain);
+	fwk_id_get_module_idx(fwk_module_id_scmi_power_domain)) {
+	return register_module_stats(fwk_module_id_scmi_power_domain);
     }
 
     return FWK_E_PARAM;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/stdio/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/stdio/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/stdio/CMakeLists.txt
@@ -8,7 +8,7 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_stdio.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_stdio.c")
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/stdio/include/mod_stdio.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/stdio/include/mod_stdio.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/stdio/include/mod_stdio.h
@@ -96,15 +96,15 @@ struct mod_stdio_element_cfg {
      * \brief Element data.
      */
     union {
-        /*!
-         * \brief `FILE *` representing an existing stream.
-         */
-        FILE *stream;
+	/*!
+	 * \brief `FILE *` representing an existing stream.
+	 */
+	FILE *stream;
 
-        /*!
-         * \brief File to open.
-         */
-        struct mod_stdio_file file;
+	/*!
+	 * \brief File to open.
+	 */
+	struct mod_stdio_file file;
     };
 };
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/stdio/src/mod_stdio.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/stdio/src/mod_stdio.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/stdio/src/mod_stdio.c
@@ -32,9 +32,9 @@ static int mod_stdio_init_ctx(void)
     size_t element_count = fwk_module_get_element_count(fwk_module_id_stdio);
 
     mod_stdio_ctx.elements =
-        fwk_mm_calloc(element_count, sizeof(*mod_stdio_ctx.elements));
+	fwk_mm_calloc(element_count, sizeof(*mod_stdio_ctx.elements));
     if (mod_stdio_ctx.elements == NULL)
-        return FWK_E_NOMEM;
+	return FWK_E_NOMEM;
 
     mod_stdio_ctx.initialized = true;
 
@@ -47,7 +47,7 @@ static int mod_stdio_init(
     const void *specific_config)
 {
     if (!mod_stdio_ctx.initialized)
-        return mod_stdio_init_ctx();
+	return mod_stdio_init_ctx();
 
     return FWK_SUCCESS;
 }
@@ -70,12 +70,12 @@ static int mod_stdio_open(const struct f
     unsigned int element_idx;
 
     if (!fwk_id_is_type(stream->id, FWK_ID_TYPE_ELEMENT))
-        return FWK_E_SUPPORT;
+	return FWK_E_SUPPORT;
 
     if (!mod_stdio_ctx.initialized) {
-        status = mod_stdio_init_ctx();
-        if (status != FWK_SUCCESS)
-            return status;
+	status = mod_stdio_init_ctx();
+	if (status != FWK_SUCCESS)
+	    return status;
     }
 
     element_idx = fwk_id_get_element_idx(stream->id);
@@ -84,11 +84,11 @@ static int mod_stdio_open(const struct f
     ctx = &mod_stdio_ctx.elements[element_idx];
 
     if (cfg->type == MOD_STDIO_ELEMENT_TYPE_PATH) {
-        ctx->stream = fopen(cfg->file.path, cfg->file.mode);
-        if (ctx->stream == NULL)
-            status = FWK_E_OS;
+	ctx->stream = fopen(cfg->file.path, cfg->file.mode);
+	if (ctx->stream == NULL)
+	    status = FWK_E_OS;
     } else {
-        ctx->stream = cfg->stream;
+	ctx->stream = cfg->stream;
     }
 
     return status;
@@ -97,16 +97,16 @@ static int mod_stdio_open(const struct f
 static int mod_stdio_getc(const struct fwk_io_stream *stream, char *ch)
 {
     struct mod_stdio_element_ctx *ctx =
-        &mod_stdio_ctx.elements[fwk_id_get_element_idx(stream->id)];
+	&mod_stdio_ctx.elements[fwk_id_get_element_idx(stream->id)];
 
     int ich = fgetc(ctx->stream);
 
     *ch = (char)ich;
 
     if (ferror(ctx->stream))
-        return FWK_E_OS;
+	return FWK_E_OS;
     else if (feof(ctx->stream))
-        return FWK_PENDING;
+	return FWK_PENDING;
 
     return FWK_SUCCESS;
 }
@@ -114,12 +114,12 @@ static int mod_stdio_getc(const struct f
 static int mod_stdio_putc(const struct fwk_io_stream *stream, char ch)
 {
     struct mod_stdio_element_ctx *ctx =
-        &mod_stdio_ctx.elements[fwk_id_get_element_idx(stream->id)];
+	&mod_stdio_ctx.elements[fwk_id_get_element_idx(stream->id)];
 
     fputc(ch, ctx->stream);
 
     if (ferror(ctx->stream))
-        return FWK_E_OS;
+	return FWK_E_OS;
 
     return FWK_SUCCESS;
 }
@@ -139,7 +139,7 @@ static int mod_stdio_close(const struct
     ctx = &mod_stdio_ctx.elements[element_idx];
 
     if (cfg->type == MOD_STDIO_ELEMENT_TYPE_PATH)
-        status = (fclose(ctx->stream) == 0) ? FWK_SUCCESS : FWK_E_OS;
+	status = (fclose(ctx->stream) == 0) ? FWK_SUCCESS : FWK_E_OS;
 
     return status;
 }
@@ -151,9 +151,9 @@ const struct fwk_module module_stdio = {
     .element_init = mod_stdio_element_init,
 
     .adapter = {
-        .open = mod_stdio_open,
-        .getch = mod_stdio_getc,
-        .putch = mod_stdio_putc,
-        .close = mod_stdio_close,
+	.open = mod_stdio_open,
+	.getch = mod_stdio_getc,
+	.putch = mod_stdio_putc,
+	.close = mod_stdio_close,
     },
 };
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/system_info/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/system_info/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/system_info/CMakeLists.txt
@@ -8,7 +8,7 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_system_info.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_system_info.c")
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/system_info/src/mod_system_info.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/system_info/src/mod_system_info.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/system_info/src/mod_system_info.c
@@ -29,9 +29,9 @@ static struct mod_system_info_get_driver
 static int get_system_info(const struct mod_system_info **sys_info)
 {
     if (system_info == NULL) {
-        system_info = get_driver_data->get_driver_data();
-        if (system_info == NULL)
-            return FWK_E_SUPPORT;
+	system_info = get_driver_data->get_driver_data();
+	if (system_info == NULL)
+	    return FWK_E_SUPPORT;
     }
 
     *sys_info = system_info;
@@ -50,13 +50,13 @@ static struct mod_system_info_get_info_a
  * Framework handlers
  */
 static int system_info_init(fwk_id_t module_id, unsigned int element_count,
-        const void *data)
+	const void *data)
 {
     fwk_assert(data != NULL);
 
     /* No elements support */
     if (element_count > 0)
-        return FWK_E_DATA;
+	return FWK_E_DATA;
 
     config = data;
     return FWK_SUCCESS;
@@ -67,32 +67,32 @@ static int system_info_bind(fwk_id_t id,
     int status = FWK_SUCCESS;
 
     if (round == 1) {
-        if (!fwk_id_is_equal(config->system_info_driver_module_id,
-                             FWK_ID_NONE)) {
+	if (!fwk_id_is_equal(config->system_info_driver_module_id,
+			     FWK_ID_NONE)) {
 
-            /* If module ID is provided, API ID shouldn't be NONE */
-            fwk_assert(!fwk_id_is_equal(config->system_info_driver_data_api_id,
-                                        FWK_ID_NONE));
-
-            status = fwk_module_bind(config->system_info_driver_module_id,
-                                     config->system_info_driver_data_api_id,
-                                     &get_driver_data);
-            if (status != FWK_SUCCESS)
-                return FWK_E_PANIC;
-        }
+	    /* If module ID is provided, API ID shouldn't be NONE */
+	    fwk_assert(!fwk_id_is_equal(config->system_info_driver_data_api_id,
+					FWK_ID_NONE));
+
+	    status = fwk_module_bind(config->system_info_driver_module_id,
+				     config->system_info_driver_data_api_id,
+				     &get_driver_data);
+	    if (status != FWK_SUCCESS)
+		return FWK_E_PANIC;
+	}
     }
     return status;
 }
 
 static int system_info_process_bind_request(fwk_id_t requester_id,
-        fwk_id_t targer_id, fwk_id_t api_id, const void **api)
+	fwk_id_t targer_id, fwk_id_t api_id, const void **api)
 {
     switch (fwk_id_get_api_idx(api_id)) {
     case MOD_SYSTEM_INFO_GET_API_IDX:
-        *api = &get_system_info_api;
-        break;
+	*api = &get_system_info_api;
+	break;
     default:
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
     return FWK_SUCCESS;
 }
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/system_pll/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/system_pll/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/system_pll/CMakeLists.txt
@@ -8,10 +8,10 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_system_pll.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_system_pll.c")
 
 target_link_libraries(${SCP_MODULE_TARGET} PRIVATE module-clock
-                                                   module-power-domain)
+						   module-power-domain)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/system_pll/src/mod_system_pll.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/system_pll/src/mod_system_pll.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/system_pll/src/mod_system_pll.c
@@ -51,11 +51,11 @@ static unsigned int freq_to_half_cycle_p
 
     /* Check if the given frequency is a multiple of 1 KHz */
     if (freq_hz % MOD_SYSTEM_PLL_MIN_INTERVAL != 0)
-        return 0;
+	return 0;
 
     freq_khz = freq_hz / FWK_KHZ;
     if (freq_khz == 0)
-        return 0;
+	return 0;
 
     /*
      * Legend:
@@ -84,7 +84,7 @@ static unsigned int freq_to_half_cycle_p
  */
 
 static int system_pll_set_rate(fwk_id_t dev_id, uint64_t rate,
-                               enum mod_clock_round_mode round_mode)
+			       enum mod_clock_round_mode round_mode)
 {
     uint64_t rounded_rate;
     uint64_t rounded_rate_alt;
@@ -97,49 +97,49 @@ static int system_pll_set_rate(fwk_id_t
     ctx = module_ctx.dev_ctx_table + fwk_id_get_element_idx(dev_id);
 
     if (ctx->current_state == MOD_CLOCK_STATE_STOPPED)
-        return FWK_E_PWRSTATE;
+	return FWK_E_PWRSTATE;
 
     /* If the given rate is not attainable as-is then round as requested */
     if ((rate % ctx->config->min_step) > 0) {
-        switch (round_mode) {
-        case MOD_CLOCK_ROUND_MODE_NONE:
-            return FWK_E_RANGE;
-        case MOD_CLOCK_ROUND_MODE_NEAREST:
-            rounded_rate = FWK_ALIGN_PREVIOUS(rate, ctx->config->min_step);
-            rounded_rate_alt = FWK_ALIGN_NEXT(rate, ctx->config->min_step);
-
-            /* Select the rounded rate that is closest to the given rate */
-            if ((rate - rounded_rate) > (rounded_rate_alt - rate))
-                rounded_rate = rounded_rate_alt;
-            break;
-        case MOD_CLOCK_ROUND_MODE_DOWN:
-            rounded_rate = FWK_ALIGN_PREVIOUS(rate, ctx->config->min_step);
-            break;
-        case MOD_CLOCK_ROUND_MODE_UP:
-            rounded_rate = FWK_ALIGN_NEXT(rate, ctx->config->min_step);
-            break;
-        default:
-            return FWK_E_SUPPORT;
-        }
+	switch (round_mode) {
+	case MOD_CLOCK_ROUND_MODE_NONE:
+	    return FWK_E_RANGE;
+	case MOD_CLOCK_ROUND_MODE_NEAREST:
+	    rounded_rate = FWK_ALIGN_PREVIOUS(rate, ctx->config->min_step);
+	    rounded_rate_alt = FWK_ALIGN_NEXT(rate, ctx->config->min_step);
+
+	    /* Select the rounded rate that is closest to the given rate */
+	    if ((rate - rounded_rate) > (rounded_rate_alt - rate))
+		rounded_rate = rounded_rate_alt;
+	    break;
+	case MOD_CLOCK_ROUND_MODE_DOWN:
+	    rounded_rate = FWK_ALIGN_PREVIOUS(rate, ctx->config->min_step);
+	    break;
+	case MOD_CLOCK_ROUND_MODE_UP:
+	    rounded_rate = FWK_ALIGN_NEXT(rate, ctx->config->min_step);
+	    break;
+	default:
+	    return FWK_E_SUPPORT;
+	}
     } else
-        rounded_rate = rate;
+	rounded_rate = rate;
 
     if (rounded_rate < ctx->config->min_rate)
-        return FWK_E_RANGE;
+	return FWK_E_RANGE;
     if (rounded_rate > ctx->config->max_rate)
-        return FWK_E_RANGE;
+	return FWK_E_RANGE;
 
     picoseconds = freq_to_half_cycle_ps(rounded_rate);
 
     if (picoseconds == 0)
-        return FWK_E_RANGE;
+	return FWK_E_RANGE;
 
     *ctx->config->control_reg = picoseconds;
 
     if (ctx->config->status_reg != NULL) {
-        /* Wait until the PLL has locked */
-        while ((*ctx->config->status_reg & ctx->config->lock_flag_mask) == 0)
-            continue;
+	/* Wait until the PLL has locked */
+	while ((*ctx->config->status_reg & ctx->config->lock_flag_mask) == 0)
+	    continue;
     }
 
     ctx->current_rate = rounded_rate;
@@ -152,9 +152,9 @@ static int system_pll_get_rate(fwk_id_t
     struct system_pll_dev_ctx *ctx;
 
     if (!fwk_module_is_valid_element_id(dev_id))
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     if (rate == NULL)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     ctx = module_ctx.dev_ctx_table + fwk_id_get_element_idx(dev_id);
     *rate = ctx->current_rate;
@@ -163,8 +163,8 @@ static int system_pll_get_rate(fwk_id_t
 }
 
 static int system_pll_get_rate_from_index(fwk_id_t dev_id,
-                                          unsigned int rate_index,
-                                          uint64_t *rate)
+					  unsigned int rate_index,
+					  uint64_t *rate)
 {
     /* PLLs have a continuous range of rates and are not indexed */
     return FWK_E_SUPPORT;
@@ -173,7 +173,7 @@ static int system_pll_get_rate_from_inde
 static int system_pll_set_state(fwk_id_t dev_id, enum mod_clock_state state)
 {
     if (state == MOD_CLOCK_STATE_RUNNING)
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 
     /* PLLs can only be stopped by a parent power domain state change. */
     return FWK_E_SUPPORT;
@@ -184,9 +184,9 @@ static int system_pll_get_state(fwk_id_t
     struct system_pll_dev_ctx *ctx;
 
     if (!fwk_module_is_valid_element_id(dev_id))
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     if (state == NULL)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     ctx = module_ctx.dev_ctx_table + fwk_id_get_element_idx(dev_id);
     *state = ctx->current_state;
@@ -199,9 +199,9 @@ static int system_pll_get_range(fwk_id_t
     struct system_pll_dev_ctx *ctx;
 
     if (!fwk_module_is_valid_element_id(dev_id))
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     if (range == NULL)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     ctx = module_ctx.dev_ctx_table + fwk_id_get_element_idx(dev_id);
 
@@ -223,17 +223,17 @@ static int system_pll_power_state_change
     ctx = module_ctx.dev_ctx_table + fwk_id_get_element_idx(dev_id);
 
     if (state != MOD_PD_STATE_ON)
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 
     ctx->current_state = MOD_CLOCK_STATE_RUNNING;
 
     if (ctx->initialized) {
-        /* Restore the previous rate */
-        rate = ctx->current_rate;
+	/* Restore the previous rate */
+	rate = ctx->current_rate;
     } else {
-        /* Initialize the PLL to its default rate */
-        ctx->initialized = true;
-        rate = ctx->config->initial_rate;
+	/* Initialize the PLL to its default rate */
+	ctx->initialized = true;
+	rate = ctx->config->initial_rate;
     }
 
     return system_pll_set_rate(dev_id, rate, MOD_CLOCK_ROUND_MODE_NONE);
@@ -249,8 +249,8 @@ static int system_pll_power_state_pendin
     ctx = module_ctx.dev_ctx_table + fwk_id_get_element_idx(dev_id);
 
     if (next_state == MOD_PD_STATE_OFF) {
-        /* Just mark the PLL as stopped */
-        ctx->current_state = MOD_CLOCK_STATE_STOPPED;
+	/* Just mark the PLL as stopped */
+	ctx->current_state = MOD_CLOCK_STATE_STOPPED;
     }
 
     return FWK_SUCCESS;
@@ -272,20 +272,20 @@ static const struct mod_clock_drv_api ap
  */
 
 static int system_pll_init(fwk_id_t module_id, unsigned int element_count,
-                             const void *data)
+			     const void *data)
 {
     module_ctx.dev_count = element_count;
 
     if (element_count == 0)
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 
     module_ctx.dev_ctx_table = fwk_mm_calloc(element_count,
-                                             sizeof(struct system_pll_dev_ctx));
+					     sizeof(struct system_pll_dev_ctx));
     return FWK_SUCCESS;
 }
 
 static int system_pll_element_init(fwk_id_t element_id, unsigned int unused,
-                                  const void *data)
+				  const void *data)
 {
     struct system_pll_dev_ctx *ctx;
     const struct mod_system_pll_dev_config *dev_config = data;
@@ -295,16 +295,16 @@ static int system_pll_element_init(fwk_i
     ctx->config = dev_config;
 
     if (ctx->config->defer_initialization)
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 
     ctx->initialized = true;
     ctx->current_state = MOD_CLOCK_STATE_RUNNING;
     return system_pll_set_rate(element_id, ctx->config->initial_rate,
-                                MOD_CLOCK_ROUND_MODE_NONE);
+				MOD_CLOCK_ROUND_MODE_NONE);
 }
 
 static int system_pll_process_bind_request(fwk_id_t requester_id, fwk_id_t id,
-                                        fwk_id_t api_type, const void **api)
+					fwk_id_t api_type, const void **api)
 {
     *api = &api_system_pll;
     return FWK_SUCCESS;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/system_power/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/system_power/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/system_power/CMakeLists.txt
@@ -8,9 +8,9 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_system_power.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_system_power.c")
 
 target_link_libraries(${SCP_MODULE_TARGET} PUBLIC module-power-domain)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/system_power/include/mod_system_power.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/system_power/include/mod_system_power.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/system_power/include/mod_system_power.h
@@ -123,7 +123,7 @@ struct mod_system_power_driver_api {
      * \return One of the standard framework error codes.
      */
     int (*platform_interrupts)(enum mod_system_power_platform_interrupt_cmd
-                               isr_cmd);
+			       isr_cmd);
 };
 
 /*!
@@ -151,12 +151,12 @@ enum mod_system_power_api_idx {
 /*! Identifier of the power domain driver API */
 static const fwk_id_t mod_system_power_api_id_pd_driver =
     FWK_ID_API_INIT(FWK_MODULE_IDX_SYSTEM_POWER,
-                    MOD_SYSTEM_POWER_API_IDX_PD_DRIVER);
+		    MOD_SYSTEM_POWER_API_IDX_PD_DRIVER);
 
 /*! Identifier of the power domain driver input API */
 static const fwk_id_t mod_system_power_api_id_pd_driver_input =
     FWK_ID_API_INIT(FWK_MODULE_IDX_SYSTEM_POWER,
-                    MOD_SYSTEM_POWER_API_IDX_PD_DRIVER_INPUT);
+		    MOD_SYSTEM_POWER_API_IDX_PD_DRIVER_INPUT);
 #endif
 
 /*!
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/system_power/src/mod_system_power.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/system_power/src/mod_system_power.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/system_power/src/mod_system_power.c
@@ -26,11 +26,11 @@
 /* SoC wakeup composite state */
 #define MOD_SYSTEM_POWER_SOC_WAKEUP_STATE \
     MOD_PD_COMPOSITE_STATE( \
-        (unsigned int)MOD_PD_LEVEL_2, \
-        0U, \
-        (unsigned int)MOD_PD_STATE_ON, \
-        (unsigned int)MOD_PD_STATE_ON, \
-        (unsigned int)MOD_PD_STATE_ON)
+	(unsigned int)MOD_PD_LEVEL_2, \
+	0U, \
+	(unsigned int)MOD_PD_STATE_ON, \
+	(unsigned int)MOD_PD_STATE_ON, \
+	(unsigned int)MOD_PD_STATE_ON)
 
 /* Element context */
 struct system_power_dev_ctx {
@@ -92,11 +92,11 @@ static void ext_ppus_set_state(enum mod_
     int status;
 
     for (i = 0; i < system_power_ctx.config->ext_ppus_count; i++) {
-        status = system_power_ctx.ext_ppu_apis[i]->set_state(
-            system_power_ctx.config->ext_ppus[i].ppu_id, state);
-        if (status != FWK_SUCCESS) {
-            FWK_LOG_DEBUG("[SYS-POW] ext-ppu%i %s @%d", i, __func__, __LINE__);
-        }
+	status = system_power_ctx.ext_ppu_apis[i]->set_state(
+	    system_power_ctx.config->ext_ppus[i].ppu_id, state);
+	if (status != FWK_SUCCESS) {
+	    FWK_LOG_DEBUG("[SYS-POW] ext-ppu%i %s @%d", i, __func__, __LINE__);
+	}
     }
 }
 
@@ -109,17 +109,17 @@ static void ext_ppus_shutdown(enum mod_p
 
     /* Shutdown external PPUs */
     for (i = 0; i < system_power_ctx.config->ext_ppus_count; i++) {
-        api = system_power_ctx.ext_ppu_apis[i];
-        ppu_id = system_power_ctx.config->ext_ppus[i].ppu_id;
+	api = system_power_ctx.ext_ppu_apis[i];
+	ppu_id = system_power_ctx.config->ext_ppus[i].ppu_id;
 
-        if (api->shutdown != NULL) {
-            status = api->shutdown(ppu_id, system_shutdown);
-        } else {
-            status = api->set_state(ppu_id, MOD_PD_STATE_OFF);
-        }
-        if (status != FWK_SUCCESS) {
-            FWK_LOG_DEBUG("[SYS-POW] ext-ppu%i %s @%d", i, __func__, __LINE__);
-        }
+	if (api->shutdown != NULL) {
+	    status = api->shutdown(ppu_id, system_shutdown);
+	} else {
+	    status = api->set_state(ppu_id, MOD_PD_STATE_OFF);
+	}
+	if (status != FWK_SUCCESS) {
+	    FWK_LOG_DEBUG("[SYS-POW] ext-ppu%i %s @%d", i, __func__, __LINE__);
+	}
     }
 }
 
@@ -131,15 +131,15 @@ static int set_system_power_state(unsign
     const uint8_t *sys_state_table;
 
     for (i = 0; i < system_power_ctx.dev_count; i++) {
-        dev_ctx = &system_power_ctx.dev_ctx_table[i];
+	dev_ctx = &system_power_ctx.dev_ctx_table[i];
 
-        sys_state_table = dev_ctx->config->sys_state_table;
+	sys_state_table = dev_ctx->config->sys_state_table;
 
-        status = dev_ctx->sys_ppu_api->set_state(dev_ctx->config->sys_ppu_id,
-                                                 sys_state_table[state]);
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
+	status = dev_ctx->sys_ppu_api->set_state(dev_ctx->config->sys_ppu_id,
+						 sys_state_table[state]);
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
     }
 
     return FWK_SUCCESS;
@@ -156,21 +156,21 @@ static int shutdown_system_power_ppus(
     int status;
 
     for (i = 0; i < system_power_ctx.dev_count; i++) {
-        dev_ctx = &system_power_ctx.dev_ctx_table[i];
+	dev_ctx = &system_power_ctx.dev_ctx_table[i];
 
-        api = dev_ctx->sys_ppu_api;
-        ppu_id = dev_ctx->config->sys_ppu_id;
+	api = dev_ctx->sys_ppu_api;
+	ppu_id = dev_ctx->config->sys_ppu_id;
 
-        if (api->shutdown != NULL) {
-            status = api->shutdown(ppu_id, system_shutdown);
-        } else {
-            state = dev_ctx->config->sys_state_table[MOD_PD_STATE_OFF];
-
-            status = api->set_state(ppu_id, state);
-        }
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
+	if (api->shutdown != NULL) {
+	    status = api->shutdown(ppu_id, system_shutdown);
+	} else {
+	    state = dev_ctx->config->sys_state_table[MOD_PD_STATE_OFF];
+
+	    status = api->set_state(ppu_id, state);
+	}
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
     }
 
     return FWK_SUCCESS;
@@ -181,18 +181,18 @@ static int disable_all_irqs(void)
     int status = FWK_SUCCESS;
 
     if (system_power_ctx.config->soc_wakeup_irq != FWK_INTERRUPT_NONE) {
-        status = fwk_interrupt_disable(system_power_ctx.config->soc_wakeup_irq);
-        if (status != FWK_SUCCESS) {
-            return FWK_E_DEVICE;
-        }
+	status = fwk_interrupt_disable(system_power_ctx.config->soc_wakeup_irq);
+	if (status != FWK_SUCCESS) {
+	    return FWK_E_DEVICE;
+	}
     }
 
     if (system_power_ctx.driver_api->platform_interrupts != NULL) {
-        status = system_power_ctx.driver_api->platform_interrupts(
-            MOD_SYSTEM_POWER_PLATFORM_INTERRUPT_CMD_DISABLE);
-        if (status != FWK_SUCCESS) {
-            status = FWK_E_DEVICE;
-        }
+	status = system_power_ctx.driver_api->platform_interrupts(
+	    MOD_SYSTEM_POWER_PLATFORM_INTERRUPT_CMD_DISABLE);
+	if (status != FWK_SUCCESS) {
+	    status = FWK_E_DEVICE;
+	}
     }
 
     return status;
@@ -206,7 +206,7 @@ static int shutdown(
 
     status = disable_all_irqs();
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     /* Shutdown external PPUs */
@@ -217,7 +217,7 @@ static int shutdown(
     /* Shutdown system PPUs */
     status = shutdown_system_power_ppus(system_shutdown);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     return FWK_SUCCESS;
@@ -233,7 +233,7 @@ static int system_power_set_state(fwk_id
     unsigned int soc_wakeup_irq;
 
     if (!fwk_expect(state < MOD_SYSTEM_POWER_POWER_STATE_COUNT)) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     soc_wakeup_irq = system_power_ctx.config->soc_wakeup_irq;
@@ -242,81 +242,81 @@ static int system_power_set_state(fwk_id
 
     switch (state) {
     case (unsigned int)MOD_PD_STATE_ON:
-        status = disable_all_irqs();
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
-
-        status = set_system_power_state(state);
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
+	status = disable_all_irqs();
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
+
+	status = set_system_power_state(state);
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
 
-        ext_ppus_set_state(MOD_PD_STATE_ON);
+	ext_ppus_set_state(MOD_PD_STATE_ON);
 
-        break;
+	break;
 
     case (unsigned int)MOD_SYSTEM_POWER_POWER_STATE_SLEEP0:
-        ext_ppus_set_state(MOD_PD_STATE_OFF);
+	ext_ppus_set_state(MOD_PD_STATE_OFF);
 
-        status = fwk_interrupt_clear_pending(soc_wakeup_irq);
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
-
-        if (system_power_ctx.driver_api->platform_interrupts != NULL) {
-            status =
-                system_power_ctx.driver_api->platform_interrupts(
-                    MOD_SYSTEM_POWER_PLATFORM_INTERRUPT_CMD_CLEAR_PENDING);
-            if (status != FWK_SUCCESS) {
-                return FWK_E_DEVICE;
-            }
-        }
-
-        status = set_system_power_state(state);
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
-
-        /* Store the identifier of the power domain of the last standing core */
-        status = system_power_ctx.pd_driver_input_api->get_last_core_pd_id(
-            &system_power_ctx.last_core_pd_id);
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
-
-        status = fwk_interrupt_enable(soc_wakeup_irq);
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
-
-        if (system_power_ctx.driver_api->platform_interrupts != NULL) {
-            status = system_power_ctx.driver_api->platform_interrupts(
-                MOD_SYSTEM_POWER_PLATFORM_INTERRUPT_CMD_ENABLE);
-            if (status != FWK_SUCCESS) {
-                return FWK_E_DEVICE;
-            }
-        }
+	status = fwk_interrupt_clear_pending(soc_wakeup_irq);
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
+
+	if (system_power_ctx.driver_api->platform_interrupts != NULL) {
+	    status =
+		system_power_ctx.driver_api->platform_interrupts(
+		    MOD_SYSTEM_POWER_PLATFORM_INTERRUPT_CMD_CLEAR_PENDING);
+	    if (status != FWK_SUCCESS) {
+		return FWK_E_DEVICE;
+	    }
+	}
+
+	status = set_system_power_state(state);
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
+
+	/* Store the identifier of the power domain of the last standing core */
+	status = system_power_ctx.pd_driver_input_api->get_last_core_pd_id(
+	    &system_power_ctx.last_core_pd_id);
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
+
+	status = fwk_interrupt_enable(soc_wakeup_irq);
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
+
+	if (system_power_ctx.driver_api->platform_interrupts != NULL) {
+	    status = system_power_ctx.driver_api->platform_interrupts(
+		MOD_SYSTEM_POWER_PLATFORM_INTERRUPT_CMD_ENABLE);
+	    if (status != FWK_SUCCESS) {
+		return FWK_E_DEVICE;
+	    }
+	}
 
-        break;
+	break;
 
     case (unsigned int)MOD_PD_STATE_OFF:
-        status = disable_all_irqs();
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
-
-        ext_ppus_set_state(MOD_PD_STATE_OFF);
-
-        status = set_system_power_state(state);
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
+	status = disable_all_irqs();
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
+
+	ext_ppus_set_state(MOD_PD_STATE_OFF);
+
+	status = set_system_power_state(state);
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
 
-        break;
+	break;
 
     default:
-        return FWK_E_SUPPORT;
+	return FWK_E_SUPPORT;
     }
 
     return FWK_SUCCESS;
@@ -335,13 +335,13 @@ static int system_power_reset(fwk_id_t p
 }
 
 static int system_power_shutdown(fwk_id_t pd_id,
-                                enum mod_pd_system_shutdown system_shutdown)
+				enum mod_pd_system_shutdown system_shutdown)
 {
     int status;
 
     status = shutdown(pd_id, system_shutdown);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     return system_power_ctx.driver_api->system_shutdown(system_shutdown);
@@ -354,11 +354,11 @@ static void soc_wakeup_handler(void)
 
     status = disable_all_irqs();
     if (status != FWK_SUCCESS) {
-        fwk_trap();
+	fwk_trap();
     }
 
     status = system_power_ctx.pd_restricted_api->set_state(
-        system_power_ctx.last_core_pd_id, false, state);
+	system_power_ctx.last_core_pd_id, false, state);
     fwk_check(status == FWK_SUCCESS);
 }
 
@@ -381,7 +381,7 @@ static int system_power_report_power_sta
     sys_ppu_transition_count++;
 
     if (sys_ppu_transition_count < system_power_ctx.dev_count) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     system_power_ctx.state = system_power_ctx.requested_state;
@@ -389,7 +389,7 @@ static int system_power_report_power_sta
     sys_ppu_transition_count = 0;
 
     return system_power_ctx.pd_driver_input_api->report_power_state_transition(
-        system_power_ctx.mod_pd_system_id, system_power_ctx.state);
+	system_power_ctx.mod_pd_system_id, system_power_ctx.state);
 }
 
 static const struct mod_pd_driver_input_api
@@ -402,8 +402,8 @@ static const struct mod_pd_driver_input_
  */
 
 static int system_power_mod_init(fwk_id_t module_id,
-                                unsigned int element_count,
-                                const void *data)
+				unsigned int element_count,
+				const void *data)
 {
     const struct mod_system_power_config *config;
 
@@ -415,38 +415,38 @@ static int system_power_mod_init(fwk_id_
     system_power_ctx.dev_count = element_count;
 
     system_power_ctx.dev_ctx_table =
-        fwk_mm_calloc(element_count, sizeof(struct system_power_dev_ctx));
+	fwk_mm_calloc(element_count, sizeof(struct system_power_dev_ctx));
 
     if (system_power_ctx.config->ext_ppus_count > 0) {
-        system_power_ctx.ext_ppu_apis = fwk_mm_calloc(
-            system_power_ctx.config->ext_ppus_count,
-            sizeof(system_power_ctx.ext_ppu_apis[0]));
+	system_power_ctx.ext_ppu_apis = fwk_mm_calloc(
+	    system_power_ctx.config->ext_ppus_count,
+	    sizeof(system_power_ctx.ext_ppu_apis[0]));
     }
 
     if (system_power_ctx.config->soc_wakeup_irq != FWK_INTERRUPT_NONE) {
-        return fwk_interrupt_set_isr(system_power_ctx.config->soc_wakeup_irq,
-                                     soc_wakeup_handler);
+	return fwk_interrupt_set_isr(system_power_ctx.config->soc_wakeup_irq,
+				     soc_wakeup_handler);
     }
 
     return FWK_SUCCESS;
 }
 
 static int system_power_mod_element_init(fwk_id_t element_id,
-                                         unsigned int unused,
-                                         const void *data)
+					 unsigned int unused,
+					 const void *data)
 {
     struct system_power_dev_ctx *dev_ctx;
 
     fwk_assert(data != NULL);
 
     dev_ctx =
-        system_power_ctx.dev_ctx_table + fwk_id_get_element_idx(element_id);
+	system_power_ctx.dev_ctx_table + fwk_id_get_element_idx(element_id);
 
     dev_ctx->config = data;
 
     /* Ensure a system state table is provided */
     if (dev_ctx->config->sys_state_table == NULL) {
-        return FWK_E_DATA;
+	return FWK_E_DATA;
     }
 
     return FWK_SUCCESS;
@@ -460,92 +460,92 @@ static int system_power_bind(fwk_id_t id
     struct system_power_dev_ctx *dev_ctx;
 
     if (round == 1) {
-        if (!fwk_id_is_type(id, FWK_ID_TYPE_MODULE)) {
-            return FWK_SUCCESS;
-        }
-
-        /*
-         * During the first round of binding, the power domain module should
-         * have bound to the power domain driver API provided by the present
-         * module. Bind back to the power domain driver input API provided by
-         * the system_power_ctx.mod_pd_system_id power domain module element to
-         * report power state transitions of the system power domains.
-         */
-        return fwk_module_bind(
-            system_power_ctx.mod_pd_system_id,
-            mod_pd_api_id_driver_input,
-            &system_power_ctx.pd_driver_input_api);
+	if (!fwk_id_is_type(id, FWK_ID_TYPE_MODULE)) {
+	    return FWK_SUCCESS;
+	}
+
+	/*
+	 * During the first round of binding, the power domain module should
+	 * have bound to the power domain driver API provided by the present
+	 * module. Bind back to the power domain driver input API provided by
+	 * the system_power_ctx.mod_pd_system_id power domain module element to
+	 * report power state transitions of the system power domains.
+	 */
+	return fwk_module_bind(
+	    system_power_ctx.mod_pd_system_id,
+	    mod_pd_api_id_driver_input,
+	    &system_power_ctx.pd_driver_input_api);
     }
 
     if (fwk_id_is_type(id, FWK_ID_TYPE_MODULE)) {
 
-        config = system_power_ctx.config;
+	config = system_power_ctx.config;
 
-        for (i = 0; i < config->ext_ppus_count; i++) {
-            status = fwk_module_bind(
-                config->ext_ppus[i].ppu_id,
-                config->ext_ppus[i].api_id,
-                &system_power_ctx.ext_ppu_apis[i]);
-            if (status != FWK_SUCCESS) {
-                return status;
-            }
-        }
-
-        status = fwk_module_bind(config->driver_id,
-            config->driver_api_id,
-            &system_power_ctx.driver_api);
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
-
-        return fwk_module_bind(
-            fwk_module_id_power_domain,
-            mod_pd_api_id_restricted,
-            &system_power_ctx.pd_restricted_api);
+	for (i = 0; i < config->ext_ppus_count; i++) {
+	    status = fwk_module_bind(
+		config->ext_ppus[i].ppu_id,
+		config->ext_ppus[i].api_id,
+		&system_power_ctx.ext_ppu_apis[i]);
+	    if (status != FWK_SUCCESS) {
+		return status;
+	    }
+	}
+
+	status = fwk_module_bind(config->driver_id,
+	    config->driver_api_id,
+	    &system_power_ctx.driver_api);
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
+
+	return fwk_module_bind(
+	    fwk_module_id_power_domain,
+	    mod_pd_api_id_restricted,
+	    &system_power_ctx.pd_restricted_api);
     }
 
     dev_ctx = system_power_ctx.dev_ctx_table + fwk_id_get_element_idx(id);
 
     return fwk_module_bind(dev_ctx->config->sys_ppu_id,
-                           dev_ctx->config->api_id,
-                           &dev_ctx->sys_ppu_api);
+			   dev_ctx->config->api_id,
+			   &dev_ctx->sys_ppu_api);
 }
 
 static int system_power_process_bind_request(fwk_id_t requester_id,
-                                             fwk_id_t pd_id,
-                                             fwk_id_t api_id,
-                                             const void **api)
+					     fwk_id_t pd_id,
+					     fwk_id_t api_id,
+					     const void **api)
 {
     unsigned int dev_idx;
     struct system_power_dev_ctx *dev_ctx;
 
     if (fwk_id_is_equal(api_id, mod_system_power_api_id_pd_driver)) {
-        if (!fwk_id_is_equal(
-                fwk_id_build_module_id(requester_id),
-                fwk_module_id_power_domain)) {
-            return FWK_E_ACCESS;
-        }
+	if (!fwk_id_is_equal(
+		fwk_id_build_module_id(requester_id),
+		fwk_module_id_power_domain)) {
+	    return FWK_E_ACCESS;
+	}
 
-        *api = &system_power_power_domain_driver_api;
-         system_power_ctx.mod_pd_system_id = requester_id;
+	*api = &system_power_power_domain_driver_api;
+	 system_power_ctx.mod_pd_system_id = requester_id;
     } else {
-        for (dev_idx = 0; dev_idx < system_power_ctx.dev_count; dev_idx++) {
-            dev_ctx = &system_power_ctx.dev_ctx_table[dev_idx];
+	for (dev_idx = 0; dev_idx < system_power_ctx.dev_count; dev_idx++) {
+	    dev_ctx = &system_power_ctx.dev_ctx_table[dev_idx];
 
-            /*
-             * If requester_id refers to a system PPU configured by any one of
-             * our elements, break when dev_idx reaches that element.
-             */
-            if (fwk_id_is_equal(requester_id, dev_ctx->config->sys_ppu_id)) {
-                break;
-            }
-        }
-        if (dev_idx >= system_power_ctx.dev_count) {
-            /* Requester_id does not refer to any configured system PPU */
-             return FWK_E_ACCESS;
-        }
+	    /*
+	     * If requester_id refers to a system PPU configured by any one of
+	     * our elements, break when dev_idx reaches that element.
+	     */
+	    if (fwk_id_is_equal(requester_id, dev_ctx->config->sys_ppu_id)) {
+		break;
+	    }
+	}
+	if (dev_idx >= system_power_ctx.dev_count) {
+	    /* Requester_id does not refer to any configured system PPU */
+	     return FWK_E_ACCESS;
+	}
 
-        *api = &system_power_power_domain_driver_input_api;
+	*api = &system_power_power_domain_driver_input_api;
     }
 
     return FWK_SUCCESS;
@@ -556,16 +556,16 @@ static int system_power_start(fwk_id_t i
     int status;
 
     if (system_power_ctx.driver_api->platform_interrupts != NULL) {
-        status = system_power_ctx.driver_api->platform_interrupts(
-            MOD_SYSTEM_POWER_PLATFORM_INTERRUPT_CMD_INIT);
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
+	status = system_power_ctx.driver_api->platform_interrupts(
+	    MOD_SYSTEM_POWER_PLATFORM_INTERRUPT_CMD_INIT);
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
     }
 
     /* Configure initial power state */
     system_power_ctx.state =
-        (unsigned int)system_power_ctx.config->initial_system_power_state;
+	(unsigned int)system_power_ctx.config->initial_system_power_state;
 
     return FWK_SUCCESS;
 }
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/thermal_mgmt/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/thermal_mgmt/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/thermal_mgmt/CMakeLists.txt
@@ -8,12 +8,12 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_thermal_mgmt.c"
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/power_allocation.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_thermal_mgmt.c"
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/power_allocation.c")
 
 target_link_libraries(${SCP_MODULE_TARGET} PRIVATE module-dvfs
-                                                   module-scmi-perf
-                                                   module-sensor)
+						   module-scmi-perf
+						   module-sensor)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/thermal_mgmt/doc/thermal_mgmt.md
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/thermal_mgmt/doc/thermal_mgmt.md
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/thermal_mgmt/doc/thermal_mgmt.md
@@ -32,25 +32,25 @@ temperature sensor.
 The main two blocks composing Thermal Mgmt are the PI (Proportional and
 Integral) control loop and the power divider.
 
-                               Thermal Design
+			       Thermal Design
     current temp                Power (TDP)      weight (config)
-         |                           |             |   |
-         |                           |             |   |
-         |                           |             |   |
-         v                           v             v   v
+	 |                           |             |   |
+	 |                           |             |   |
+	 |                           |             |   |
+	 v                           v             v   v
        +-+-+     +---------+       +-+-+        +--+---+--+
        |   +---->+ PI Ctrl +------>+   +------->+  power  +----> power granted
        +-+-+     +---------+       +---+   +--->+ divider +---->
-         ^                                 |    +--+---+--+
-         |                                 |       ^   ^
-         |    performance request ---------+       |   |
-         |                                   +-----+   +-----+
+	 ^                                 |    +--+---+--+
+	 |                                 |       ^   ^
+	 |    performance request ---------+       |   |
+	 |                                   +-----+   +-----+
     control temp                             |               |
-                                             v               v
-                                         +---+---+       +---+---+
-                                         | power |       | power |
-                                         | model |       | model |
-                                         +-------+       +-------+
+					     v               v
+					 +---+---+       +---+---+
+					 | power |       | power |
+					 | model |       | model |
+					 +-------+       +-------+
 
 
 The control loop reacts to temperature deviations from the control settings and
@@ -200,55 +200,55 @@ implementation depending on system confi
 ```C
 static struct mod_thermal_mgmt_actor_config actor_table_domain0[] = {
     [0] = {
-        .driver_id =
-            FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_PLAT_POWER_MODEL, 0),
-        .dvfs_domain_id =
-            FWK_ID_ELEMENT_INIT(
-                FWK_MODULE_IDX_DVFS, DVFS_ELEMENT_IDX_ACTOR0),
-        .weight = 100,
+	.driver_id =
+	    FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_PLAT_POWER_MODEL, 0),
+	.dvfs_domain_id =
+	    FWK_ID_ELEMENT_INIT(
+		FWK_MODULE_IDX_DVFS, DVFS_ELEMENT_IDX_ACTOR0),
+	.weight = 100,
     },
     [1] = {
-        .driver_id =
-            FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_PLAT_POWER_MODEL, 1),
-        .dvfs_domain_id =
-            FWK_ID_ELEMENT_INIT(
-                FWK_MODULE_IDX_DVFS, DVFS_ELEMENT_IDX_ACTOR1),
-        .weight = 200,
+	.driver_id =
+	    FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_PLAT_POWER_MODEL, 1),
+	.dvfs_domain_id =
+	    FWK_ID_ELEMENT_INIT(
+		FWK_MODULE_IDX_DVFS, DVFS_ELEMENT_IDX_ACTOR1),
+	.weight = 200,
     },
 };
 
 struct fwk_element thermal_mgmt_domains_elem_table = {
     [0] = {
-        .name = "Thermal Domain 0",
-        .data = &((struct mod_thermal_mgmt_dev_config){
-            .slow_loop_mult = 20,
-
-            .tdp = 5000,
-            .pi_controller = {
-                .switch_on_temperature = 50,
-                .control_temperature = 60,
-                .integral_cutoff = 0,
-                .integral_max = 100,
-                .k_p_undershoot = 1,
-                .k_p_overshoot = 1,
-                .k_integral = 1,
-            },
-
-            .sensor_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_SENSOR, 0),
-            .temp_protection = &((struct mod_thermal_mgmt_protection_config){
-                .driver_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_, 0),
-                .driver_api_id = FWK_ID_API_INIT(
-                    FWK_MODULE_IDX_,
-                    0),
-                .warn_temp_threshold = 60,
-                .crit_temp_threshold = 85,
-            }),
-            .driver_api_id =
-                FWK_ID_API_INIT(FWK_MODULE_IDX_PLAT_POWER_MODEL, 0),
-            .thermal_actors_table = actor_table_domain0,
-            .thermal_actors_count = FWK_ARRAY_SIZE(actor_table_domain0),
-        }),
-        .elements = FWK_MODULE_DYNAMIC_ELEMENTS(get_thermal_mgmt_element_table),
+	.name = "Thermal Domain 0",
+	.data = &((struct mod_thermal_mgmt_dev_config){
+	    .slow_loop_mult = 20,
+
+	    .tdp = 5000,
+	    .pi_controller = {
+		.switch_on_temperature = 50,
+		.control_temperature = 60,
+		.integral_cutoff = 0,
+		.integral_max = 100,
+		.k_p_undershoot = 1,
+		.k_p_overshoot = 1,
+		.k_integral = 1,
+	    },
+
+	    .sensor_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_SENSOR, 0),
+	    .temp_protection = &((struct mod_thermal_mgmt_protection_config){
+		.driver_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_, 0),
+		.driver_api_id = FWK_ID_API_INIT(
+		    FWK_MODULE_IDX_,
+		    0),
+		.warn_temp_threshold = 60,
+		.crit_temp_threshold = 85,
+	    }),
+	    .driver_api_id =
+		FWK_ID_API_INIT(FWK_MODULE_IDX_PLAT_POWER_MODEL, 0),
+	    .thermal_actors_table = actor_table_domain0,
+	    .thermal_actors_count = FWK_ARRAY_SIZE(actor_table_domain0),
+	}),
+	.elements = FWK_MODULE_DYNAMIC_ELEMENTS(get_thermal_mgmt_element_table),
     },
     [1] = { 0 } /* Termination description */
 };
@@ -293,70 +293,70 @@ struct mod_thermal_mgmt_power_model_api
 ```C
 static struct mod_thermal_mgmt_actor_config actor_table_domain0[] = {
     [0] = {
-        .driver_id =
-            FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_PLAT_POWER_MODEL, 0),
-        .dvfs_domain_id =
-            FWK_ID_ELEMENT_INIT(
-                FWK_MODULE_IDX_DVFS, DVFS_ELEMENT_IDX_ACTOR0),
-        .weight = 100,
-        .activity_factor = &((struct mod_thermal_mgmt_activity_factor_config){
-            .driver_id = FWK_ID_ELEMENT_INIT(
-                FWK_MODULE_IDX_PLATFORM_ACTIVITY,
-                0),
-            .driver_api_id = FWK_ID_API_INIT(
-                FWK_MODULE_IDX_PLATFORM_ACTIVITY,
-                MOD_PLATFORM_ACTIVITY_DRIVER_API_IDX),
-        }),
+	.driver_id =
+	    FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_PLAT_POWER_MODEL, 0),
+	.dvfs_domain_id =
+	    FWK_ID_ELEMENT_INIT(
+		FWK_MODULE_IDX_DVFS, DVFS_ELEMENT_IDX_ACTOR0),
+	.weight = 100,
+	.activity_factor = &((struct mod_thermal_mgmt_activity_factor_config){
+	    .driver_id = FWK_ID_ELEMENT_INIT(
+		FWK_MODULE_IDX_PLATFORM_ACTIVITY,
+		0),
+	    .driver_api_id = FWK_ID_API_INIT(
+		FWK_MODULE_IDX_PLATFORM_ACTIVITY,
+		MOD_PLATFORM_ACTIVITY_DRIVER_API_IDX),
+	}),
     },
     [1] = {
-        .driver_id =
-            FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_PLAT_POWER_MODEL, 1),
-        .dvfs_domain_id =
-            FWK_ID_ELEMENT_INIT(
-                FWK_MODULE_IDX_DVFS, DVFS_ELEMENT_IDX_ACTOR1),
-        .weight = 200,
-        .activity_factor = &((struct mod_thermal_mgmt_activity_factor_config){
-            .driver_id = FWK_ID_ELEMENT_INIT(
-                FWK_MODULE_IDX_PLATFORM_ACTIVITY,
-                1),
-            .driver_api_id = FWK_ID_API_INIT(
-                FWK_MODULE_IDX_PLATFORM_ACTIVITY,
-                MOD_PLATFORM_ACTIVITY_DRIVER_API_IDX),
+	.driver_id =
+	    FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_PLAT_POWER_MODEL, 1),
+	.dvfs_domain_id =
+	    FWK_ID_ELEMENT_INIT(
+		FWK_MODULE_IDX_DVFS, DVFS_ELEMENT_IDX_ACTOR1),
+	.weight = 200,
+	.activity_factor = &((struct mod_thermal_mgmt_activity_factor_config){
+	    .driver_id = FWK_ID_ELEMENT_INIT(
+		FWK_MODULE_IDX_PLATFORM_ACTIVITY,
+		1),
+	    .driver_api_id = FWK_ID_API_INIT(
+		FWK_MODULE_IDX_PLATFORM_ACTIVITY,
+		MOD_PLATFORM_ACTIVITY_DRIVER_API_IDX),
     },
 };
 
 struct fwk_element thermal_mgmt_domains_elem_table = {
     [0] = {
-        .name = "Thermal Domain 0",
-        .data = &((struct mod_thermal_mgmt_dev_config){
-            .slow_loop_mult = 20,
-
-            .tdp = 5000,
-            .pi_controller = {
-                .switch_on_temperature = 50,
-                .control_temperature = 60,
-                .integral_cutoff = 0,
-                .integral_max = 100,
-                .k_p_undershoot = 1,
-                .k_p_overshoot = 1,
-                .k_integral = 1,
-            },
-
-            .sensor_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_SENSOR, 0),
-            .temp_protection = &((struct mod_thermal_mgmt_protection_config){
-                .driver_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_, 0),
-                .driver_api_id = FWK_ID_API_INIT(
-                    FWK_MODULE_IDX_,
-                    0),
-                .warn_temp_threshold = 60,
-                .crit_temp_threshold = 85,
-            }),
-            .driver_api_id =
-                FWK_ID_API_INIT(FWK_MODULE_IDX_PLAT_POWER_MODEL, 0),
-            .thermal_actors_table = actor_table_domain0,
-            .thermal_actors_count = FWK_ARRAY_SIZE(actor_table_domain0),
-        }),
-        .elements = FWK_MODULE_DYNAMIC_ELEMENTS(get_thermal_mgmt_element_table),
+	.name = "Thermal Domain 0",
+	.data = &((struct mod_thermal_mgmt_dev_config){
+	    .slow_loop_mult = 20,
+
+	    .tdp = 5000,
+	    .pi_controller = {
+		.switch_on_temperature = 50,
+		.control_temperature = 60,
+		.integral_cutoff = 0,
+		.integral_max = 100,
+		.k_p_undershoot = 1,
+		.k_p_overshoot = 1,
+		.k_integral = 1,
+	    },
+
+	    .sensor_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_SENSOR, 0),
+	    .temp_protection = &((struct mod_thermal_mgmt_protection_config){
+		.driver_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_, 0),
+		.driver_api_id = FWK_ID_API_INIT(
+		    FWK_MODULE_IDX_,
+		    0),
+		.warn_temp_threshold = 60,
+		.crit_temp_threshold = 85,
+	    }),
+	    .driver_api_id =
+		FWK_ID_API_INIT(FWK_MODULE_IDX_PLAT_POWER_MODEL, 0),
+	    .thermal_actors_table = actor_table_domain0,
+	    .thermal_actors_count = FWK_ARRAY_SIZE(actor_table_domain0),
+	}),
+	.elements = FWK_MODULE_DYNAMIC_ELEMENTS(get_thermal_mgmt_element_table),
     },
     [1] = { 0 } /* Termination description */
 };
@@ -397,20 +397,20 @@ There is the possibility to only configu
 ```C
 struct fwk_element thermal_mgmt_domains_elem_table = {
     [0] = {
-        .name = "Thermal Domain 0",
-        .data = &((struct mod_thermal_mgmt_dev_config){
-            .slow_loop_mult = 5,
-            .sensor_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_SENSOR, 0),
-            .temp_protection = &((struct mod_thermal_mgmt_protection_config){
-                .driver_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_, 0),
-                .driver_api_id = FWK_ID_API_INIT(
-                    FWK_MODULE_IDX_,
-                    0),
-                .warn_temp_threshold = 60,
-                .crit_temp_threshold = 85,
-            }),
-        }),
-        .elements = FWK_MODULE_DYNAMIC_ELEMENTS(get_thermal_mgmt_element_table),
+	.name = "Thermal Domain 0",
+	.data = &((struct mod_thermal_mgmt_dev_config){
+	    .slow_loop_mult = 5,
+	    .sensor_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_SENSOR, 0),
+	    .temp_protection = &((struct mod_thermal_mgmt_protection_config){
+		.driver_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_, 0),
+		.driver_api_id = FWK_ID_API_INIT(
+		    FWK_MODULE_IDX_,
+		    0),
+		.warn_temp_threshold = 60,
+		.crit_temp_threshold = 85,
+	    }),
+	}),
+	.elements = FWK_MODULE_DYNAMIC_ELEMENTS(get_thermal_mgmt_element_table),
     },
     [1] = { 0 } /* Termination description */
 };
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/thermal_mgmt/include/mod_thermal_mgmt.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/thermal_mgmt/include/mod_thermal_mgmt.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/thermal_mgmt/include/mod_thermal_mgmt.h
@@ -133,50 +133,50 @@ struct mod_thermal_mgmt_dev_config {
      *      threshold the power is allocated only on bias coefficients.
      */
     struct {
-        /*!
-         * \brief Switch-on temperature threshold.
-         *
-         * \details The temperature above which the PI loop runs. Below this
-         *      threshold the power is allocated only on bias coefficients.
-         */
-        uint32_t switch_on_temperature;
-
-        /*!
-         * \brief Control temperature
-         *
-         * \details The temperature that the system will achive once stabilised.
-         *      Due to the PI nature of the controller, some
-         *      overshoot/undershoot may occur. Note that the controller can
-         *      only limit the temperature by placing a limit to the power to
-         *      the heat source. It has no direct control on the heat source
-         *      itself and therefore only the upper limit can be controlled.
-         */
-        uint32_t control_temperature;
-
-        /*!
-         * \brief Integral cut-off threshold.
-         *
-         * \details Below this value the errors are accumulated. This is useful
-         *      to avoid accumulating errors when the temperature is below the
-         *      target.
-         */
-        int32_t integral_cutoff;
-
-        /*!
-         * \brief Integral maximum.
-         *
-         * \details This is the upper limit the accumulated errors.
-         */
-        int32_t integral_max;
+	/*!
+	 * \brief Switch-on temperature threshold.
+	 *
+	 * \details The temperature above which the PI loop runs. Below this
+	 *      threshold the power is allocated only on bias coefficients.
+	 */
+	uint32_t switch_on_temperature;
+
+	/*!
+	 * \brief Control temperature
+	 *
+	 * \details The temperature that the system will achive once stabilised.
+	 *      Due to the PI nature of the controller, some
+	 *      overshoot/undershoot may occur. Note that the controller can
+	 *      only limit the temperature by placing a limit to the power to
+	 *      the heat source. It has no direct control on the heat source
+	 *      itself and therefore only the upper limit can be controlled.
+	 */
+	uint32_t control_temperature;
+
+	/*!
+	 * \brief Integral cut-off threshold.
+	 *
+	 * \details Below this value the errors are accumulated. This is useful
+	 *      to avoid accumulating errors when the temperature is below the
+	 *      target.
+	 */
+	int32_t integral_cutoff;
+
+	/*!
+	 * \brief Integral maximum.
+	 *
+	 * \details This is the upper limit the accumulated errors.
+	 */
+	int32_t integral_max;
 
-        /*! Proportional term when undershooting (PI loop)*/
-        int32_t k_p_undershoot;
+	/*! Proportional term when undershooting (PI loop)*/
+	int32_t k_p_undershoot;
 
-        /*! Proportional term when overhooting (PI loop) */
-        int32_t k_p_overshoot;
+	/*! Proportional term when overhooting (PI loop) */
+	int32_t k_p_overshoot;
 
-        /*! Integral term (PI loop) */
-        int32_t k_integral;
+	/*! Integral term (PI loop) */
+	int32_t k_integral;
     } pi_controller;
 
     /*! Temperature sensor identifier */
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/thermal_mgmt/src/mod_thermal_mgmt.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/thermal_mgmt/src/mod_thermal_mgmt.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/thermal_mgmt/src/mod_thermal_mgmt.c
@@ -43,73 +43,73 @@ static void pi_control(fwk_id_t id)
     dev_ctx = get_dev_ctx(id);
 
     if (dev_ctx->cur_temp <
-        dev_ctx->config->pi_controller.switch_on_temperature) {
-        /* The PI loop is not activated */
-        dev_ctx->integral_error = 0;
-        dev_ctx->thermal_allocatable_power = (uint32_t)dev_ctx->config->tdp;
+	dev_ctx->config->pi_controller.switch_on_temperature) {
+	/* The PI loop is not activated */
+	dev_ctx->integral_error = 0;
+	dev_ctx->thermal_allocatable_power = (uint32_t)dev_ctx->config->tdp;
 
-        return;
+	return;
     }
 
     k_i = dev_ctx->config->pi_controller.k_integral;
 
     err = (int32_t)dev_ctx->config->pi_controller.control_temperature -
-        (int32_t)dev_ctx->cur_temp;
+	(int32_t)dev_ctx->cur_temp;
 
     k_p = (err < 0) ? dev_ctx->config->pi_controller.k_p_overshoot :
-                      dev_ctx->config->pi_controller.k_p_undershoot;
+		      dev_ctx->config->pi_controller.k_p_undershoot;
 
     /* Evaluate the integral term */
     if (((err > 0) && (dev_ctx->integral_error < (INT32_MAX - err))) ||
-        ((err < 0) && (dev_ctx->integral_error > (INT32_MIN - err)))) {
-        terr = dev_ctx->integral_error + err;
+	((err < 0) && (dev_ctx->integral_error > (INT32_MIN - err)))) {
+	terr = dev_ctx->integral_error + err;
 
-        if ((err < dev_ctx->config->pi_controller.integral_cutoff) &&
-            (terr < dev_ctx->config->pi_controller.integral_max)) {
-            /*
-             * The error is below the cutoff value and,
-             * the accumulated error is still within the maximum permissible
-             * value, thus continue integration.
-             */
-            dev_ctx->integral_error = terr;
-        }
+	if ((err < dev_ctx->config->pi_controller.integral_cutoff) &&
+	    (terr < dev_ctx->config->pi_controller.integral_max)) {
+	    /*
+	     * The error is below the cutoff value and,
+	     * the accumulated error is still within the maximum permissible
+	     * value, thus continue integration.
+	     */
+	    dev_ctx->integral_error = terr;
+	}
     }
 
     pi_power = (k_p * err) + (k_i * dev_ctx->integral_error);
 
     if (pi_power + (int32_t)dev_ctx->config->tdp > 0) {
-        dev_ctx->thermal_allocatable_power =
-            pi_power + (uint32_t)dev_ctx->config->tdp;
+	dev_ctx->thermal_allocatable_power =
+	    pi_power + (uint32_t)dev_ctx->config->tdp;
     } else {
-        dev_ctx->thermal_allocatable_power = 0;
+	dev_ctx->thermal_allocatable_power = 0;
     }
 }
 
 static void thermal_protection(struct mod_thermal_mgmt_dev_ctx *dev_ctx)
 {
     if (dev_ctx->cur_temp >=
-        dev_ctx->config->temp_protection->crit_temp_threshold) {
-        FWK_LOG_CRIT(
-            "[THERMAL][%s] temp (%u) reached critical threshold!\n",
-            fwk_module_get_element_name(dev_ctx->id),
-            (unsigned int)dev_ctx->cur_temp);
-
-        if (dev_ctx->thermal_protection_api->critical != NULL) {
-            dev_ctx->thermal_protection_api->critical(
-                dev_ctx->config->temp_protection->driver_id, dev_ctx->id);
-        }
+	dev_ctx->config->temp_protection->crit_temp_threshold) {
+	FWK_LOG_CRIT(
+	    "[THERMAL][%s] temp (%u) reached critical threshold!\n",
+	    fwk_module_get_element_name(dev_ctx->id),
+	    (unsigned int)dev_ctx->cur_temp);
+
+	if (dev_ctx->thermal_protection_api->critical != NULL) {
+	    dev_ctx->thermal_protection_api->critical(
+		dev_ctx->config->temp_protection->driver_id, dev_ctx->id);
+	}
     } else if (
-        dev_ctx->cur_temp >=
-        dev_ctx->config->temp_protection->warn_temp_threshold) {
-        FWK_LOG_WARN(
-            "[THERMAL][%s] temp (%u) reached warning threshold!\n",
-            fwk_module_get_element_name(dev_ctx->id),
-            (unsigned int)dev_ctx->cur_temp);
-
-        if (dev_ctx->thermal_protection_api->warning != NULL) {
-            dev_ctx->thermal_protection_api->warning(
-                dev_ctx->config->temp_protection->driver_id, dev_ctx->id);
-        }
+	dev_ctx->cur_temp >=
+	dev_ctx->config->temp_protection->warn_temp_threshold) {
+	FWK_LOG_WARN(
+	    "[THERMAL][%s] temp (%u) reached warning threshold!\n",
+	    fwk_module_get_element_name(dev_ctx->id),
+	    (unsigned int)dev_ctx->cur_temp);
+
+	if (dev_ctx->thermal_protection_api->warning != NULL) {
+	    dev_ctx->thermal_protection_api->warning(
+		dev_ctx->config->temp_protection->driver_id, dev_ctx->id);
+	}
     }
 }
 
@@ -118,9 +118,9 @@ static int read_temperature(fwk_id_t id)
 #if THERMAL_HAS_ASYNC_SENSORS
     /* Initiate the temperature reading sequence */
     struct fwk_event_light event = {
-        .source_id = id,
-        .target_id = id,
-        .id = mod_thermal_event_id_read_temp,
+	.source_id = id,
+	.target_id = id,
+	.id = mod_thermal_event_id_read_temp,
     };
 
     return fwk_put_event(&event);
@@ -131,11 +131,11 @@ static int read_temperature(fwk_id_t id)
     dev_ctx = get_dev_ctx(id);
 
     status = dev_ctx->sensor_api->get_data(
-        dev_ctx->config->sensor_id, &dev_ctx->sensor_data);
+	dev_ctx->config->sensor_id, &dev_ctx->sensor_data);
     if (status == FWK_SUCCESS) {
-        dev_ctx->cur_temp = (uint32_t)dev_ctx->sensor_data.value;
+	dev_ctx->cur_temp = (uint32_t)dev_ctx->sensor_data.value;
 
-        dev_ctx->control_needs_update = true;
+	dev_ctx->control_needs_update = true;
     }
 
     return status;
@@ -151,39 +151,39 @@ static int control_update(fwk_id_t id)
 
     dev_ctx->tick_counter++;
     if (dev_ctx->tick_counter > dev_ctx->config->slow_loop_mult) {
-        dev_ctx->tick_counter = 0;
+	dev_ctx->tick_counter = 0;
 
-        if (dev_ctx->control_needs_update) {
-            /* The last reading was not processed */
-            FWK_LOG_WARN("[TPM] Failed to process last reading\n");
-
-            return FWK_E_PANIC;
-        }
-
-        /*
-         * ASYNC: The new temperature is not yet available but should be ready
-         * by next fast-loop tick.
-         * SYNC: The new temperature is ready.
-         *
-         * Either way we need to attempt to continue to process the loop.
-         */
-        status = read_temperature(id);
-        if (status != FWK_SUCCESS) {
-            return FWK_E_DEVICE;
-        }
+	if (dev_ctx->control_needs_update) {
+	    /* The last reading was not processed */
+	    FWK_LOG_WARN("[TPM] Failed to process last reading\n");
+
+	    return FWK_E_PANIC;
+	}
+
+	/*
+	 * ASYNC: The new temperature is not yet available but should be ready
+	 * by next fast-loop tick.
+	 * SYNC: The new temperature is ready.
+	 *
+	 * Either way we need to attempt to continue to process the loop.
+	 */
+	status = read_temperature(id);
+	if (status != FWK_SUCCESS) {
+	    return FWK_E_DEVICE;
+	}
     }
 
     if (dev_ctx->control_needs_update) {
-        dev_ctx->control_needs_update = false;
-        if (dev_ctx->config->thermal_actors_count > 0) {
-            pi_control(id);
-        }
-
-        dev_ctx->tot_spare_power = 0;
-
-        if (dev_ctx->config->temp_protection != NULL) {
-            thermal_protection(dev_ctx);
-        }
+	dev_ctx->control_needs_update = false;
+	if (dev_ctx->config->thermal_actors_count > 0) {
+	    pi_control(id);
+	}
+
+	dev_ctx->tot_spare_power = 0;
+
+	if (dev_ctx->config->temp_protection != NULL) {
+	    thermal_protection(dev_ctx);
+	}
     }
 
     return FWK_SUCCESS;
@@ -202,16 +202,16 @@ static int thermal_update(struct perf_pl
     int status;
 
     for (dev_idx = 0; dev_idx < mod_ctx.dev_ctx_count; dev_idx++) {
-        dev_id = FWK_ID_ELEMENT(FWK_MODULE_IDX_THERMAL_MGMT, dev_idx);
-        dev_ctx = get_dev_ctx(dev_id);
+	dev_id = FWK_ID_ELEMENT(FWK_MODULE_IDX_THERMAL_MGMT, dev_idx);
+	dev_ctx = get_dev_ctx(dev_id);
 
-        status = control_update(dev_ctx->id);
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
-        if (dev_ctx->config->thermal_actors_count > 0) {
-            distribute_power(dev_ctx->id, data->level, data->adj_max_limit);
-        }
+	status = control_update(dev_ctx->id);
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
+	if (dev_ctx->config->thermal_actors_count > 0) {
+	    distribute_power(dev_ctx->id, data->level, data->adj_max_limit);
+	}
     }
 
     return FWK_SUCCESS;
@@ -231,7 +231,7 @@ static int thermal_mgmt_init(
     const void *data)
 {
     mod_ctx.dev_ctx_table =
-        fwk_mm_calloc(element_count, sizeof(struct mod_thermal_mgmt_dev_ctx));
+	fwk_mm_calloc(element_count, sizeof(struct mod_thermal_mgmt_dev_ctx));
     mod_ctx.dev_ctx_count = element_count;
 
     return FWK_SUCCESS;
@@ -250,7 +250,7 @@ static int thermal_mgmt_dev_init(
     uint32_t actor;
 
     if (data == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     config = (struct mod_thermal_mgmt_dev_config *)data;
@@ -259,39 +259,39 @@ static int thermal_mgmt_dev_init(
     dev_ctx->id = element_id;
 
     if (dev_ctx->config->thermal_actors_count > 0) {
-        /* Assume TDP until PI loop is updated */
-        dev_ctx->thermal_allocatable_power = (uint32_t)dev_ctx->config->tdp;
+	/* Assume TDP until PI loop is updated */
+	dev_ctx->thermal_allocatable_power = (uint32_t)dev_ctx->config->tdp;
 
-        dev_ctx->actor_ctx_table = fwk_mm_calloc(
-            dev_ctx->config->thermal_actors_count,
-            sizeof(struct mod_thermal_mgmt_actor_ctx));
+	dev_ctx->actor_ctx_table = fwk_mm_calloc(
+	    dev_ctx->config->thermal_actors_count,
+	    sizeof(struct mod_thermal_mgmt_actor_ctx));
     } else if (dev_ctx->config->temp_protection == NULL) {
-        /* There is neither temperature protection enabled nor actors defined */
-        return FWK_E_PARAM;
+	/* There is neither temperature protection enabled nor actors defined */
+	return FWK_E_PARAM;
     }
 
     for (actor = 0; actor < dev_ctx->config->thermal_actors_count; actor++) {
-        /* Get actor context and set configuration */
-        actor_ctx = get_actor_ctx(dev_ctx, actor);
-        actor_ctx->config = &config->thermal_actors_table[actor];
-
-        if (!fwk_module_is_valid_element_id(
-                actor_ctx->config->dvfs_domain_id)) {
-            return FWK_E_PARAM;
-        }
-
-        if (!fwk_id_type_is_valid(actor_ctx->config->driver_id) ||
-            fwk_id_is_equal(actor_ctx->config->driver_id, FWK_ID_NONE)) {
-            return FWK_E_PARAM;
-        }
-
-        sum_weights = actor_ctx->config->weight * config->tdp * config->tdp;
-
-        if (sum_weights > UINT32_MAX) {
-            FWK_LOG_WARN(
-                "[THERMAL] WARN: Possible overflow for device %u",
-                fwk_id_get_element_idx(element_id));
-        }
+	/* Get actor context and set configuration */
+	actor_ctx = get_actor_ctx(dev_ctx, actor);
+	actor_ctx->config = &config->thermal_actors_table[actor];
+
+	if (!fwk_module_is_valid_element_id(
+		actor_ctx->config->dvfs_domain_id)) {
+	    return FWK_E_PARAM;
+	}
+
+	if (!fwk_id_type_is_valid(actor_ctx->config->driver_id) ||
+	    fwk_id_is_equal(actor_ctx->config->driver_id, FWK_ID_NONE)) {
+	    return FWK_E_PARAM;
+	}
+
+	sum_weights = actor_ctx->config->weight * config->tdp * config->tdp;
+
+	if (sum_weights > UINT32_MAX) {
+	    FWK_LOG_WARN(
+		"[THERMAL] WARN: Possible overflow for device %u",
+		fwk_id_get_element_idx(element_id));
+	}
     }
 
     return FWK_SUCCESS;
@@ -305,58 +305,58 @@ static int thermal_mgmt_bind(fwk_id_t id
     struct mod_thermal_mgmt_actor_ctx *actor_ctx;
 
     if (round > 0) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     if (fwk_id_is_type(id, FWK_ID_TYPE_MODULE)) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     dev_ctx = get_dev_ctx(id);
 
     /* Bind to sensor */
     status = fwk_module_bind(
-        dev_ctx->config->sensor_id,
-        mod_sensor_api_id_sensor,
-        &dev_ctx->sensor_api);
+	dev_ctx->config->sensor_id,
+	mod_sensor_api_id_sensor,
+	&dev_ctx->sensor_api);
     if (status != FWK_SUCCESS) {
-        return FWK_E_PANIC;
+	return FWK_E_PANIC;
     }
 
     if (dev_ctx->config->temp_protection != NULL) {
-        /* Bind to thermal protection driver */
-        status = fwk_module_bind(
-            dev_ctx->config->temp_protection->driver_id,
-            dev_ctx->config->temp_protection->driver_api_id,
-            &dev_ctx->thermal_protection_api);
-        if (status != FWK_SUCCESS) {
-            return FWK_E_PANIC;
-        }
+	/* Bind to thermal protection driver */
+	status = fwk_module_bind(
+	    dev_ctx->config->temp_protection->driver_id,
+	    dev_ctx->config->temp_protection->driver_api_id,
+	    &dev_ctx->thermal_protection_api);
+	if (status != FWK_SUCCESS) {
+	    return FWK_E_PANIC;
+	}
     }
 
     /* Bind to a respective thermal driver */
     status = fwk_module_bind(
-        FWK_ID_MODULE(fwk_id_get_module_idx(dev_ctx->config->driver_api_id)),
-        dev_ctx->config->driver_api_id,
-        &dev_ctx->driver_api);
+	FWK_ID_MODULE(fwk_id_get_module_idx(dev_ctx->config->driver_api_id)),
+	dev_ctx->config->driver_api_id,
+	&dev_ctx->driver_api);
     if (status != FWK_SUCCESS) {
-        return FWK_E_PANIC;
+	return FWK_E_PANIC;
     }
 
     for (actor = 0; actor < dev_ctx->config->thermal_actors_count; actor++) {
-        /* Get actor context and bind */
-        actor_ctx = get_actor_ctx(dev_ctx, actor);
+	/* Get actor context and bind */
+	actor_ctx = get_actor_ctx(dev_ctx, actor);
 
-        if (actor_ctx->config->activity_factor != NULL) {
-            /* Bind to thermal protection driver */
-            status = fwk_module_bind(
-                actor_ctx->config->activity_factor->driver_id,
-                actor_ctx->config->activity_factor->driver_api_id,
-                &actor_ctx->activity_api);
-            if (status != FWK_SUCCESS) {
-                return FWK_E_PANIC;
-            }
-        }
+	if (actor_ctx->config->activity_factor != NULL) {
+	    /* Bind to thermal protection driver */
+	    status = fwk_module_bind(
+		actor_ctx->config->activity_factor->driver_id,
+		actor_ctx->config->activity_factor->driver_api_id,
+		&actor_ctx->activity_api);
+	    if (status != FWK_SUCCESS) {
+		return FWK_E_PANIC;
+	    }
+	}
     }
 
     return FWK_SUCCESS;
@@ -384,28 +384,28 @@ static int thermal_mgmt_process_event(
     dev_ctx = get_dev_ctx(event->target_id);
 
     if (fwk_id_is_equal(event->id, mod_thermal_event_id_read_temp)) {
-        /* Temperature-reading event */
-        status = dev_ctx->sensor_api->get_data(
-            dev_ctx->config->sensor_id, &dev_ctx->sensor_data);
-        if (status == FWK_SUCCESS) {
-            dev_ctx->cur_temp = (uint32_t)dev_ctx->sensor_data.value;
-        }
+	/* Temperature-reading event */
+	status = dev_ctx->sensor_api->get_data(
+	    dev_ctx->config->sensor_id, &dev_ctx->sensor_data);
+	if (status == FWK_SUCCESS) {
+	    dev_ctx->cur_temp = (uint32_t)dev_ctx->sensor_data.value;
+	}
     } else if (fwk_id_is_equal(event->id, mod_sensor_event_id_read_request)) {
-        /* Response event from Sensor HAL */
-        if (dev_ctx->sensor_data.status == FWK_SUCCESS) {
-            dev_ctx->cur_temp = (uint32_t)dev_ctx->sensor_data.value;
-            status = FWK_SUCCESS;
-        } else {
-            status = FWK_E_DEVICE;
-        }
+	/* Response event from Sensor HAL */
+	if (dev_ctx->sensor_data.status == FWK_SUCCESS) {
+	    dev_ctx->cur_temp = (uint32_t)dev_ctx->sensor_data.value;
+	    status = FWK_SUCCESS;
+	} else {
+	    status = FWK_E_DEVICE;
+	}
     } else {
-        status = FWK_E_PARAM;
+	status = FWK_E_PARAM;
     }
 
     if (status == FWK_SUCCESS) {
-        dev_ctx->control_needs_update = true;
+	dev_ctx->control_needs_update = true;
     } else if (status == FWK_PENDING) {
-        status = FWK_SUCCESS;
+	status = FWK_SUCCESS;
     }
 
     return status;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/thermal_mgmt/src/power_allocation.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/thermal_mgmt/src/power_allocation.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/thermal_mgmt/src/power_allocation.c
@@ -60,20 +60,20 @@ static void allocate_power(
     struct mod_thermal_mgmt_actor_ctx *actor_ctx)
 {
     actor_ctx->granted_power =
-        ((actor_ctx->config->weight * actor_ctx->demand_power) *
-         dev_ctx->allocatable_power) /
-        dev_ctx->tot_weighted_demand_power;
+	((actor_ctx->config->weight * actor_ctx->demand_power) *
+	 dev_ctx->allocatable_power) /
+	dev_ctx->tot_weighted_demand_power;
 
     if (actor_ctx->granted_power > actor_ctx->demand_power) {
-        actor_ctx->spare_power =
-            actor_ctx->granted_power - actor_ctx->demand_power;
-        actor_ctx->power_deficit = 0;
+	actor_ctx->spare_power =
+	    actor_ctx->granted_power - actor_ctx->demand_power;
+	actor_ctx->power_deficit = 0;
 
-        actor_ctx->granted_power = actor_ctx->demand_power;
+	actor_ctx->granted_power = actor_ctx->demand_power;
     } else {
-        actor_ctx->spare_power = 0;
-        actor_ctx->power_deficit =
-            actor_ctx->demand_power - actor_ctx->granted_power;
+	actor_ctx->spare_power = 0;
+	actor_ctx->power_deficit =
+	    actor_ctx->demand_power - actor_ctx->granted_power;
     }
 
     dev_ctx->tot_spare_power += actor_ctx->spare_power;
@@ -90,30 +90,30 @@ static void re_allocate_power(
     struct mod_thermal_mgmt_actor_ctx *actor_ctx)
 {
     if (dev_ctx->tot_spare_power == 0) {
-        return;
+	return;
     }
 
     if (actor_ctx->power_deficit > 0) {
-        /*
-         * The actor has been given less than requested, and it may still take
-         * some power
-         */
-        actor_ctx->granted_power +=
-            (actor_ctx->power_deficit * dev_ctx->tot_spare_power) /
-            dev_ctx->tot_power_deficit;
-
-        if (actor_ctx->granted_power > actor_ctx->demand_power) {
-            dev_ctx->carry_over_power +=
-                actor_ctx->granted_power - actor_ctx->demand_power;
-
-            actor_ctx->granted_power = actor_ctx->demand_power;
-        } else {
-            /*
-             * The actor has received the power it requested. The amount of
-             * power left can be used in the next fast-loop.
-             */
-            dev_ctx->carry_over_power += actor_ctx->spare_power;
-        }
+	/*
+	 * The actor has been given less than requested, and it may still take
+	 * some power
+	 */
+	actor_ctx->granted_power +=
+	    (actor_ctx->power_deficit * dev_ctx->tot_spare_power) /
+	    dev_ctx->tot_power_deficit;
+
+	if (actor_ctx->granted_power > actor_ctx->demand_power) {
+	    dev_ctx->carry_over_power +=
+		actor_ctx->granted_power - actor_ctx->demand_power;
+
+	    actor_ctx->granted_power = actor_ctx->demand_power;
+	} else {
+	    /*
+	     * The actor has received the power it requested. The amount of
+	     * power left can be used in the next fast-loop.
+	     */
+	    dev_ctx->carry_over_power += actor_ctx->spare_power;
+	}
     }
 }
 
@@ -147,39 +147,39 @@ void distribute_power(fwk_id_t id, uint3
      * Initialise the actors' demand power.
      */
     for (actor_idx = 0; actor_idx < actors_count; actor_idx++) {
-        actor_ctx = get_actor_ctx(dev_ctx, actor_idx);
-        dom = get_dvfs_domain_idx(actor_ctx);
+	actor_ctx = get_actor_ctx(dev_ctx, actor_idx);
+	dom = get_dvfs_domain_idx(actor_ctx);
 
-        /* Here we take into account limits already placed by other plugins */
-        if (perf_request[dom] > perf_limit[dom]) {
-            dev_perf_request = perf_limit[dom];
-        } else {
-            dev_perf_request = perf_request[dom];
-        }
-
-        get_actor_power(dev_ctx, actor_ctx, dev_perf_request);
-        if (actor_ctx->activity_api != NULL) {
-            status = actor_ctx->activity_api->get_activity_factor(
-                actor_ctx->config->activity_factor->driver_id, &activity);
-            if (status != FWK_SUCCESS) {
-                FWK_LOG_INFO(
-                    "[THERMAL] Failed to get activity factor (%u,%u)",
-                    fwk_id_get_element_idx(id),
-                    actor_idx);
-                continue;
-            }
-
-            /* Calculate used power and accumulate idle power */
-            prev_used_power = (actor_ctx->granted_power * activity) / 1024;
-            idle_power += actor_ctx->granted_power - prev_used_power;
-        }
+	/* Here we take into account limits already placed by other plugins */
+	if (perf_request[dom] > perf_limit[dom]) {
+	    dev_perf_request = perf_limit[dom];
+	} else {
+	    dev_perf_request = perf_request[dom];
+	}
+
+	get_actor_power(dev_ctx, actor_ctx, dev_perf_request);
+	if (actor_ctx->activity_api != NULL) {
+	    status = actor_ctx->activity_api->get_activity_factor(
+		actor_ctx->config->activity_factor->driver_id, &activity);
+	    if (status != FWK_SUCCESS) {
+		FWK_LOG_INFO(
+		    "[THERMAL] Failed to get activity factor (%u,%u)",
+		    fwk_id_get_element_idx(id),
+		    actor_idx);
+		continue;
+	    }
+
+	    /* Calculate used power and accumulate idle power */
+	    prev_used_power = (actor_ctx->granted_power * activity) / 1024;
+	    idle_power += actor_ctx->granted_power - prev_used_power;
+	}
 
-        dev_ctx->tot_weighted_demand_power +=
-            actor_ctx->config->weight * actor_ctx->demand_power;
+	dev_ctx->tot_weighted_demand_power +=
+	    actor_ctx->config->weight * actor_ctx->demand_power;
     }
 
     dev_ctx->allocatable_power =
-        dev_ctx->thermal_allocatable_power + idle_power;
+	dev_ctx->thermal_allocatable_power + idle_power;
 
     /*
      * STEP 1:
@@ -187,9 +187,9 @@ void distribute_power(fwk_id_t id, uint3
      * their power demand.
      */
     for (actor_idx = 0; actor_idx < actors_count; actor_idx++) {
-        actor_ctx = get_actor_ctx(dev_ctx, actor_idx);
+	actor_ctx = get_actor_ctx(dev_ctx, actor_idx);
 
-        allocate_power(dev_ctx, actor_ctx);
+	allocate_power(dev_ctx, actor_ctx);
     }
 
     /*
@@ -202,21 +202,21 @@ void distribute_power(fwk_id_t id, uint3
     dev_ctx->carry_over_power = 0;
 
     for (actor_idx = 0; actor_idx < actors_count; actor_idx++) {
-        actor_ctx = get_actor_ctx(dev_ctx, actor_idx);
-        dom = get_dvfs_domain_idx(actor_ctx);
+	actor_ctx = get_actor_ctx(dev_ctx, actor_idx);
+	dom = get_dvfs_domain_idx(actor_ctx);
 
-        re_allocate_power(dev_ctx, actor_ctx);
+	re_allocate_power(dev_ctx, actor_ctx);
 
-        get_actor_level(dev_ctx, actor_ctx, &new_perf_limit);
+	get_actor_level(dev_ctx, actor_ctx, &new_perf_limit);
 
-        /*
-         * If we have been granted the power we requested (which is at most the
-         * limit already placed by other plugins), then there's no need to limit
-         * further.
-         */
-        if (!is_power_request_satisfied(actor_ctx) &&
-            (new_perf_limit < perf_limit[dom])) {
-            perf_limit[dom] = new_perf_limit;
-        }
+	/*
+	 * If we have been granted the power we requested (which is at most the
+	 * limit already placed by other plugins), then there's no need to limit
+	 * further.
+	 */
+	if (!is_power_request_satisfied(actor_ctx) &&
+	    (new_perf_limit < perf_limit[dom])) {
+	    perf_limit[dom] = new_perf_limit;
+	}
     }
 }
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/thermal_mgmt/test/config_thermal_mgmt.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/thermal_mgmt/test/config_thermal_mgmt.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/thermal_mgmt/test/config_thermal_mgmt.h
@@ -49,58 +49,58 @@ enum fake_activity_factor_api {
  */
 static struct mod_thermal_mgmt_actor_config actor_table_domain0[2] = {
     [0] = {
-        .driver_id = FWK_ID_ELEMENT_INIT(
-            FWK_MODULE_IDX_FAKE_POWER_MODEL,
-            FAKE_ACTOR_0),
-        .dvfs_domain_id = FWK_ID_ELEMENT_INIT(
-            FWK_MODULE_IDX_DVFS,
-            FAKE_ACTOR_0),
-        .weight = 100,
-        .activity_factor =
-            &((struct mod_thermal_mgmt_activity_factor_config){
-                .driver_id = FWK_ID_ELEMENT_INIT(
-                    FWK_MODULE_IDX_FAKE_ACTIVITY_COUNTER,
-                    FAKE_ACTIVITY_FACTOR_0),
-                .driver_api_id = FWK_ID_API_INIT(
-                    FWK_MODULE_IDX_FAKE_ACTIVITY_COUNTER,
-                    FAKE_ACTIVITY_FACTOR_API),
-            }),
+	.driver_id = FWK_ID_ELEMENT_INIT(
+	    FWK_MODULE_IDX_FAKE_POWER_MODEL,
+	    FAKE_ACTOR_0),
+	.dvfs_domain_id = FWK_ID_ELEMENT_INIT(
+	    FWK_MODULE_IDX_DVFS,
+	    FAKE_ACTOR_0),
+	.weight = 100,
+	.activity_factor =
+	    &((struct mod_thermal_mgmt_activity_factor_config){
+		.driver_id = FWK_ID_ELEMENT_INIT(
+		    FWK_MODULE_IDX_FAKE_ACTIVITY_COUNTER,
+		    FAKE_ACTIVITY_FACTOR_0),
+		.driver_api_id = FWK_ID_API_INIT(
+		    FWK_MODULE_IDX_FAKE_ACTIVITY_COUNTER,
+		    FAKE_ACTIVITY_FACTOR_API),
+	    }),
     },
     [1] = {
-        .driver_id = FWK_ID_ELEMENT_INIT(
-            FWK_MODULE_IDX_FAKE_POWER_MODEL,
-            FAKE_ACTOR_1),
-        .dvfs_domain_id =
-            FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_DVFS, FAKE_ACTOR_1),
-        .weight = 100,
+	.driver_id = FWK_ID_ELEMENT_INIT(
+	    FWK_MODULE_IDX_FAKE_POWER_MODEL,
+	    FAKE_ACTOR_1),
+	.dvfs_domain_id =
+	    FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_DVFS, FAKE_ACTOR_1),
+	.weight = 100,
     },
 };
 
 static struct mod_thermal_mgmt_actor_config actor_table_domain1[2] = {
     [0] = {
-        .driver_id = FWK_ID_ELEMENT_INIT(
-            FWK_MODULE_IDX_FAKE_POWER_MODEL,
-            FAKE_ACTOR_2),
-        .dvfs_domain_id = FWK_ID_ELEMENT_INIT(
-            FWK_MODULE_IDX_DVFS,
-            FAKE_ACTOR_2),
-        .weight = 100,
+	.driver_id = FWK_ID_ELEMENT_INIT(
+	    FWK_MODULE_IDX_FAKE_POWER_MODEL,
+	    FAKE_ACTOR_2),
+	.dvfs_domain_id = FWK_ID_ELEMENT_INIT(
+	    FWK_MODULE_IDX_DVFS,
+	    FAKE_ACTOR_2),
+	.weight = 100,
     },
     [1] = {
-        .driver_id = FWK_ID_ELEMENT_INIT(
-            FWK_MODULE_IDX_FAKE_POWER_MODEL,
-            FAKE_ACTOR_3),
-        .dvfs_domain_id = FWK_ID_ELEMENT_INIT(
-            FWK_MODULE_IDX_DVFS,
-            FAKE_ACTOR_3),
-        .weight = 100,
+	.driver_id = FWK_ID_ELEMENT_INIT(
+	    FWK_MODULE_IDX_FAKE_POWER_MODEL,
+	    FAKE_ACTOR_3),
+	.dvfs_domain_id = FWK_ID_ELEMENT_INIT(
+	    FWK_MODULE_IDX_DVFS,
+	    FAKE_ACTOR_3),
+	.weight = 100,
     },
 };
 
 static struct mod_thermal_mgmt_actor_config
     *actor_table_domain[MOD_THERMAL_MGMT_DOM_COUNT] = {
-        [MOD_THERMAL_MGMT_DOM_0] = actor_table_domain0,
-        [MOD_THERMAL_MGMT_DOM_1] = actor_table_domain1,
+	[MOD_THERMAL_MGMT_DOM_0] = actor_table_domain0,
+	[MOD_THERMAL_MGMT_DOM_1] = actor_table_domain1,
     };
 
 static struct mod_thermal_mgmt_protection_config temp_protection = {
@@ -113,43 +113,43 @@ static struct mod_thermal_mgmt_protectio
 static const struct mod_thermal_mgmt_dev_config
     dev_config_default_table[MOD_THERMAL_MGMT_DOM_COUNT] = {
     [MOD_THERMAL_MGMT_DOM_0] = {
-        .slow_loop_mult = 2,
-        .tdp = 10,
-        .pi_controller = {
-            .switch_on_temperature = 50,
-            .control_temperature = 60,
-            .integral_cutoff = 0,
-            .integral_max = 100,
-            .k_p_undershoot = 1,
-            .k_p_overshoot = 1,
-            .k_integral = 1,
-        },
-        .sensor_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_SENSOR, FAKE_SENSOR_0),
-        .driver_api_id = FWK_ID_API_INIT(
-            FWK_MODULE_IDX_FAKE_POWER_MODEL,
-            0),
-        .thermal_actors_table = actor_table_domain0,
-        .thermal_actors_count = 2,
-        .temp_protection = &temp_protection,
+	.slow_loop_mult = 2,
+	.tdp = 10,
+	.pi_controller = {
+	    .switch_on_temperature = 50,
+	    .control_temperature = 60,
+	    .integral_cutoff = 0,
+	    .integral_max = 100,
+	    .k_p_undershoot = 1,
+	    .k_p_overshoot = 1,
+	    .k_integral = 1,
+	},
+	.sensor_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_SENSOR, FAKE_SENSOR_0),
+	.driver_api_id = FWK_ID_API_INIT(
+	    FWK_MODULE_IDX_FAKE_POWER_MODEL,
+	    0),
+	.thermal_actors_table = actor_table_domain0,
+	.thermal_actors_count = 2,
+	.temp_protection = &temp_protection,
     },
     [MOD_THERMAL_MGMT_DOM_1] = {
-        .slow_loop_mult = 2,
-        .tdp = 10,
-        .pi_controller = {
-            .switch_on_temperature = 50,
-            .control_temperature = 60,
-            .integral_cutoff = 0,
-            .integral_max = 100,
-            .k_p_undershoot = 1,
-            .k_p_overshoot = 1,
-            .k_integral = 1,
-        },
-        .sensor_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_SENSOR, FAKE_SENSOR_1),
-        .driver_api_id = FWK_ID_API_INIT(
-            FWK_MODULE_IDX_FAKE_POWER_MODEL,
-            0),
-        .thermal_actors_table = actor_table_domain1,
-        .thermal_actors_count = 2,
+	.slow_loop_mult = 2,
+	.tdp = 10,
+	.pi_controller = {
+	    .switch_on_temperature = 50,
+	    .control_temperature = 60,
+	    .integral_cutoff = 0,
+	    .integral_max = 100,
+	    .k_p_undershoot = 1,
+	    .k_p_overshoot = 1,
+	    .k_integral = 1,
+	},
+	.sensor_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_SENSOR, FAKE_SENSOR_1),
+	.driver_api_id = FWK_ID_API_INIT(
+	    FWK_MODULE_IDX_FAKE_POWER_MODEL,
+	    0),
+	.thermal_actors_table = actor_table_domain1,
+	.thermal_actors_count = 2,
     },
 };
 
@@ -158,14 +158,14 @@ struct mod_thermal_mgmt_dev_config dev_c
 static const struct fwk_element
     thermal_mgmt_domains_elem_table[MOD_THERMAL_MGMT_DOM_COUNT + 1] = {
     [MOD_THERMAL_MGMT_DOM_0] = {
-        .name = "Fake Thermal Domain 0",
-        .data = (struct mod_thermal_mgmt_dev_config *)
-            &dev_config_table[MOD_THERMAL_MGMT_DOM_0],
+	.name = "Fake Thermal Domain 0",
+	.data = (struct mod_thermal_mgmt_dev_config *)
+	    &dev_config_table[MOD_THERMAL_MGMT_DOM_0],
     },
     [MOD_THERMAL_MGMT_DOM_1] = {
-        .name = "Fake Thermal Domain 1",
-        .data = (struct mod_thermal_mgmt_dev_config *)
-            &dev_config_table[MOD_THERMAL_MGMT_DOM_1],
+	.name = "Fake Thermal Domain 1",
+	.data = (struct mod_thermal_mgmt_dev_config *)
+	    &dev_config_table[MOD_THERMAL_MGMT_DOM_1],
     },
     [MOD_THERMAL_MGMT_DOM_COUNT] = { 0 } /* Termination description */
 };
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/thermal_mgmt/test/mocks/Mockmod_thermal_mgmt_extra.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/thermal_mgmt/test/mocks/Mockmod_thermal_mgmt_extra.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/thermal_mgmt/test/mocks/Mockmod_thermal_mgmt_extra.c
@@ -187,9 +187,9 @@ void CMockExpectParameters_mod_thermal_m
 void CMockExpectParameters_mod_thermal_mgmt_protection_api_warning(CMOCK_mod_thermal_mgmt_protection_api_warning_CALL_INSTANCE* cmock_call_instance, fwk_id_t driver_id, fwk_id_t thermal_id)
 {
   memcpy((void*)(&cmock_call_instance->Expected_driver_id), (void*)(&driver_id),
-         sizeof(fwk_id_t[sizeof(driver_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
+	 sizeof(fwk_id_t[sizeof(driver_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
   memcpy((void*)(&cmock_call_instance->Expected_thermal_id), (void*)(&thermal_id),
-         sizeof(fwk_id_t[sizeof(thermal_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
+	 sizeof(fwk_id_t[sizeof(thermal_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
 }
 
 void mod_thermal_mgmt_protection_api_warning_CMockExpectAnyArgs(UNITY_LINE_TYPE cmock_line)
@@ -266,9 +266,9 @@ void CMockExpectParameters_mod_thermal_m
 void CMockExpectParameters_mod_thermal_mgmt_protection_api_critical(CMOCK_mod_thermal_mgmt_protection_api_critical_CALL_INSTANCE* cmock_call_instance, fwk_id_t driver_id, fwk_id_t thermal_id)
 {
   memcpy((void*)(&cmock_call_instance->Expected_driver_id), (void*)(&driver_id),
-         sizeof(fwk_id_t[sizeof(driver_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
+	 sizeof(fwk_id_t[sizeof(driver_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
   memcpy((void*)(&cmock_call_instance->Expected_thermal_id), (void*)(&thermal_id),
-         sizeof(fwk_id_t[sizeof(thermal_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
+	 sizeof(fwk_id_t[sizeof(thermal_id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
 }
 
 void mod_thermal_mgmt_protection_api_critical_CMockExpectAnyArgs(UNITY_LINE_TYPE cmock_line)
@@ -355,7 +355,7 @@ void CMockExpectParameters_mod_sensor_ge
 void CMockExpectParameters_mod_sensor_get_data(CMOCK_mod_sensor_get_data_CALL_INSTANCE* cmock_call_instance, fwk_id_t id, struct mod_sensor_data* data, int data_Depth)
 {
   memcpy((void*)(&cmock_call_instance->Expected_id), (void*)(&id),
-         sizeof(fwk_id_t[sizeof(id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
+	 sizeof(fwk_id_t[sizeof(id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
   cmock_call_instance->Expected_data = data;
   cmock_call_instance->Expected_data_Depth = data_Depth;
   cmock_call_instance->ReturnThruPtr_data_Used = 0;
@@ -481,7 +481,7 @@ void CMockExpectParameters_distribute_po
 void CMockExpectParameters_distribute_power(CMOCK_distribute_power_CALL_INSTANCE* cmock_call_instance, fwk_id_t id, uint32_t* perf_request, int perf_request_Depth, uint32_t* perf_limit, int perf_limit_Depth)
 {
   memcpy((void*)(&cmock_call_instance->Expected_id), (void*)(&id),
-         sizeof(fwk_id_t[sizeof(id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
+	 sizeof(fwk_id_t[sizeof(id) == sizeof(fwk_id_t) ? 1 : -1])); /* add fwk_id_t to :treat_as_array if this causes an error */
   cmock_call_instance->Expected_perf_request = perf_request;
   cmock_call_instance->Expected_perf_request_Depth = perf_request_Depth;
   cmock_call_instance->ReturnThruPtr_perf_request_Used = 0;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/thermal_mgmt/test/mod_thermal_mgmt_unit_test.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/thermal_mgmt/test/mod_thermal_mgmt_unit_test.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/thermal_mgmt/test/mod_thermal_mgmt_unit_test.c
@@ -36,34 +36,34 @@ void setUp(void)
 
     /* Set default values for configuration structure */
     memcpy(
-        dev_config_table,
-        dev_config_default_table,
-        sizeof(*dev_config_table) * MOD_THERMAL_MGMT_DOM_COUNT);
+	dev_config_table,
+	dev_config_default_table,
+	sizeof(*dev_config_table) * MOD_THERMAL_MGMT_DOM_COUNT);
 
     for (dev_idx = 0; dev_idx < MOD_THERMAL_MGMT_DOM_COUNT; dev_idx++) {
-        dev_ctx = &mod_ctx.dev_ctx_table[dev_idx];
-        memset(dev_ctx, 0, sizeof(*dev_ctx));
+	dev_ctx = &mod_ctx.dev_ctx_table[dev_idx];
+	memset(dev_ctx, 0, sizeof(*dev_ctx));
 
-        config = (struct mod_thermal_mgmt_dev_config *)
-                     thermal_mgmt_domains_elem_table[dev_idx]
-                         .data;
-
-        /* Configure device context */
-        dev_ctx->config = config;
-        dev_ctx->id = FWK_ID_ELEMENT(FWK_MODULE_IDX_THERMAL_MGMT, dev_idx);
-        dev_ctx->thermal_allocatable_power = (uint32_t)dev_ctx->config->tdp;
-        dev_ctx->actor_ctx_table = &actor_ctx_table[dev_idx][0];
-        dev_ctx->thermal_protection_api = &thermal_protection_api;
-        dev_ctx->sensor_api = &sensor_api;
-
-        /* Configure actors context */
-        for (actor_idx = 0; actor_idx < dev_ctx->config->thermal_actors_count;
-             actor_idx++) {
-            actor_ctx = &dev_ctx->actor_ctx_table[actor_idx];
-            memset(actor_ctx, 0, sizeof(*actor_ctx));
+	config = (struct mod_thermal_mgmt_dev_config *)
+		     thermal_mgmt_domains_elem_table[dev_idx]
+			 .data;
+
+	/* Configure device context */
+	dev_ctx->config = config;
+	dev_ctx->id = FWK_ID_ELEMENT(FWK_MODULE_IDX_THERMAL_MGMT, dev_idx);
+	dev_ctx->thermal_allocatable_power = (uint32_t)dev_ctx->config->tdp;
+	dev_ctx->actor_ctx_table = &actor_ctx_table[dev_idx][0];
+	dev_ctx->thermal_protection_api = &thermal_protection_api;
+	dev_ctx->sensor_api = &sensor_api;
+
+	/* Configure actors context */
+	for (actor_idx = 0; actor_idx < dev_ctx->config->thermal_actors_count;
+	     actor_idx++) {
+	    actor_ctx = &dev_ctx->actor_ctx_table[actor_idx];
+	    memset(actor_ctx, 0, sizeof(*actor_ctx));
 
-            actor_ctx->config = &(config->thermal_actors_table[actor_idx]);
-        }
+	    actor_ctx->config = &(config->thermal_actors_table[actor_idx]);
+	}
     }
 }
 
@@ -79,12 +79,12 @@ void test_thermal_mgmt_init(void)
     memset(&mod_ctx, 0, sizeof(mod_ctx));
 
     fwk_mm_calloc_ExpectAndReturn(
-        MOD_THERMAL_MGMT_DOM_COUNT,
-        sizeof(struct mod_thermal_mgmt_dev_ctx),
-        dev_ctx_table);
+	MOD_THERMAL_MGMT_DOM_COUNT,
+	sizeof(struct mod_thermal_mgmt_dev_ctx),
+	dev_ctx_table);
 
     status = thermal_mgmt_init(
-        fwk_module_id_thermal_mgmt, MOD_THERMAL_MGMT_DOM_COUNT, NULL);
+	fwk_module_id_thermal_mgmt, MOD_THERMAL_MGMT_DOM_COUNT, NULL);
     TEST_ASSERT_EQUAL(status, FWK_SUCCESS);
     TEST_ASSERT_EQUAL(mod_ctx.dev_ctx_table, dev_ctx_table);
 }
@@ -132,7 +132,7 @@ void test_thermal_mgmt_dev_init_error_in
     fwk_mm_calloc_ExpectAnyArgsAndReturn(actor_table_domain[0]);
     fwk_module_is_valid_element_id_ExpectAnyArgsAndReturn(true);
     fwk_id_type_is_valid_ExpectAndReturn(
-        actor_table_domain[0][0].driver_id, false);
+	actor_table_domain[0][0].driver_id, false);
 
     status = thermal_mgmt_dev_init(element_id, 0, &dev_config_table[0]);
     TEST_ASSERT_EQUAL(status, FWK_E_PARAM);
@@ -141,7 +141,7 @@ void test_thermal_mgmt_dev_init_error_in
     fwk_mm_calloc_ExpectAnyArgsAndReturn(actor_table_domain[0]);
     fwk_module_is_valid_element_id_ExpectAnyArgsAndReturn(true);
     fwk_id_type_is_valid_ExpectAndReturn(
-        actor_table_domain[0][0].driver_id, true);
+	actor_table_domain[0][0].driver_id, true);
     fwk_id_is_equal_ExpectAnyArgsAndReturn(true);
 
     status = thermal_mgmt_dev_init(element_id, 0, &dev_config_table[0]);
@@ -156,28 +156,28 @@ void test_thermal_mgmt_dev_init_success(
     struct mod_thermal_mgmt_dev_ctx *dev_ctx;
 
     for (dev_idx = 0; dev_idx < MOD_THERMAL_MGMT_DOM_COUNT; dev_idx++) {
-        element_id = FWK_ID_ELEMENT(FWK_MODULE_IDX_THERMAL_MGMT, dev_idx);
-        dev_ctx = &mod_ctx.dev_ctx_table[dev_idx];
-        config = &dev_config_table[dev_idx];
-        /* Reset module context configuration */
-        memset(dev_ctx, 0, sizeof(*dev_ctx));
-
-        fwk_id_get_element_idx_ExpectAndReturn(element_id, dev_idx);
-        fwk_mm_calloc_ExpectAndReturn(
-            config->thermal_actors_count,
-            sizeof(struct mod_thermal_mgmt_actor_ctx),
-            actor_table_domain[dev_idx]);
-        for (actor_idx = 0; actor_idx < config->thermal_actors_count;
-             actor_idx++) {
-            fwk_module_is_valid_element_id_ExpectAnyArgsAndReturn(true);
-            fwk_id_type_is_valid_ExpectAnyArgsAndReturn(true);
-            fwk_id_is_equal_ExpectAnyArgsAndReturn(false);
-        }
-
-        status = thermal_mgmt_dev_init(element_id, dev_idx, config);
-        TEST_ASSERT_EQUAL(status, FWK_SUCCESS);
-        TEST_ASSERT_EQUAL(
-            dev_ctx->actor_ctx_table, actor_table_domain[dev_idx]);
+	element_id = FWK_ID_ELEMENT(FWK_MODULE_IDX_THERMAL_MGMT, dev_idx);
+	dev_ctx = &mod_ctx.dev_ctx_table[dev_idx];
+	config = &dev_config_table[dev_idx];
+	/* Reset module context configuration */
+	memset(dev_ctx, 0, sizeof(*dev_ctx));
+
+	fwk_id_get_element_idx_ExpectAndReturn(element_id, dev_idx);
+	fwk_mm_calloc_ExpectAndReturn(
+	    config->thermal_actors_count,
+	    sizeof(struct mod_thermal_mgmt_actor_ctx),
+	    actor_table_domain[dev_idx]);
+	for (actor_idx = 0; actor_idx < config->thermal_actors_count;
+	     actor_idx++) {
+	    fwk_module_is_valid_element_id_ExpectAnyArgsAndReturn(true);
+	    fwk_id_type_is_valid_ExpectAnyArgsAndReturn(true);
+	    fwk_id_is_equal_ExpectAnyArgsAndReturn(false);
+	}
+
+	status = thermal_mgmt_dev_init(element_id, dev_idx, config);
+	TEST_ASSERT_EQUAL(status, FWK_SUCCESS);
+	TEST_ASSERT_EQUAL(
+	    dev_ctx->actor_ctx_table, actor_table_domain[dev_idx]);
     }
 }
 
@@ -193,7 +193,7 @@ void test_thermal_mgmt_bind_module(void)
     int status;
 
     fwk_id_is_type_ExpectAndReturn(
-        fwk_module_id_thermal_mgmt, FWK_ID_TYPE_MODULE, true);
+	fwk_module_id_thermal_mgmt, FWK_ID_TYPE_MODULE, true);
 
     status = thermal_mgmt_bind(fwk_module_id_thermal_mgmt, 0);
     TEST_ASSERT_EQUAL(status, FWK_SUCCESS);
@@ -215,13 +215,13 @@ void test_thermal_mgmt_bind_fail(void)
      * times.
      */
     for (i = 0; i < 4; i++) {
-        for (j = 0; j < i; j++) {
-            fwk_module_bind_ExpectAnyArgsAndReturn(FWK_SUCCESS);
-        }
-        fwk_module_bind_ExpectAnyArgsAndReturn(FWK_E_PARAM);
+	for (j = 0; j < i; j++) {
+	    fwk_module_bind_ExpectAnyArgsAndReturn(FWK_SUCCESS);
+	}
+	fwk_module_bind_ExpectAnyArgsAndReturn(FWK_E_PARAM);
 
-        status = thermal_mgmt_bind(element_id, 0);
-        TEST_ASSERT_EQUAL(status, FWK_E_PANIC);
+	status = thermal_mgmt_bind(element_id, 0);
+	TEST_ASSERT_EQUAL(status, FWK_E_PANIC);
     }
 
     fwk_id_is_type_StopIgnore();
@@ -242,27 +242,27 @@ void test_thermal_mgmt_bind_success(void
     fwk_id_is_type_ExpectAndReturn(element_id, FWK_ID_TYPE_MODULE, false);
     fwk_id_get_element_idx_ExpectAndReturn(element_id, 0);
     fwk_module_bind_ExpectAndReturn(
-        dev_ctx->config->sensor_id,
-        mod_sensor_api_id_sensor,
-        &dev_ctx->sensor_api,
-        FWK_SUCCESS);
+	dev_ctx->config->sensor_id,
+	mod_sensor_api_id_sensor,
+	&dev_ctx->sensor_api,
+	FWK_SUCCESS);
     fwk_module_bind_ExpectAndReturn(
-        dev_ctx->config->temp_protection->driver_id,
-        dev_ctx->config->temp_protection->driver_api_id,
-        &dev_ctx->thermal_protection_api,
-        FWK_SUCCESS);
+	dev_ctx->config->temp_protection->driver_id,
+	dev_ctx->config->temp_protection->driver_api_id,
+	&dev_ctx->thermal_protection_api,
+	FWK_SUCCESS);
     fwk_id_get_module_idx_ExpectAndReturn(
-        dev_ctx->config->driver_api_id, FWK_MODULE_IDX_FAKE_POWER_MODEL);
+	dev_ctx->config->driver_api_id, FWK_MODULE_IDX_FAKE_POWER_MODEL);
     fwk_module_bind_ExpectAndReturn(
-        fwk_module_id_fake_power_model,
-        dev_ctx->config->driver_api_id,
-        &dev_ctx->driver_api,
-        FWK_SUCCESS);
+	fwk_module_id_fake_power_model,
+	dev_ctx->config->driver_api_id,
+	&dev_ctx->driver_api,
+	FWK_SUCCESS);
     fwk_module_bind_ExpectAndReturn(
-        actor_ctx->config->activity_factor->driver_id,
-        actor_ctx->config->activity_factor->driver_api_id,
-        &actor_ctx->activity_api,
-        FWK_SUCCESS);
+	actor_ctx->config->activity_factor->driver_id,
+	actor_ctx->config->activity_factor->driver_api_id,
+	&actor_ctx->activity_api,
+	FWK_SUCCESS);
     status = thermal_mgmt_bind(element_id, 0);
     TEST_ASSERT_EQUAL(status, FWK_SUCCESS);
 }
@@ -273,7 +273,7 @@ void test_thermal_mgmt_process_bind_requ
     int status;
 
     status = thermal_mgmt_process_bind_request(
-        FWK_ID_NONE, FWK_ID_NONE, FWK_ID_NONE, (const void **)&api);
+	FWK_ID_NONE, FWK_ID_NONE, FWK_ID_NONE, (const void **)&api);
     TEST_ASSERT_EQUAL(status, FWK_SUCCESS);
     TEST_ASSERT_EQUAL(api, &mod_thermal_perf_plugins_api);
 }
@@ -283,9 +283,9 @@ void test_thermal_mgmt_process_event_inv
 {
     int status;
     struct fwk_event event = {
-        .source_id = FWK_ID_NONE,
-        .target_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_THERMAL_MGMT, 0),
-        .id = FWK_ID_NONE,
+	.source_id = FWK_ID_NONE,
+	.target_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_THERMAL_MGMT, 0),
+	.id = FWK_ID_NONE,
     };
 
     fwk_id_get_element_idx_ExpectAndReturn(event.target_id, 0);
@@ -301,16 +301,16 @@ void test_thermal_mgmt_process_event_rea
     struct mod_thermal_mgmt_dev_ctx *dev_ctx;
     int status;
     struct fwk_event event = {
-        .source_id = FWK_ID_NONE,
-        .target_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_THERMAL_MGMT, 0),
-        .id = FWK_ID_NONE,
+	.source_id = FWK_ID_NONE,
+	.target_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_THERMAL_MGMT, 0),
+	.id = FWK_ID_NONE,
     };
     dev_ctx = &mod_ctx.dev_ctx_table[0];
 
     fwk_id_get_element_idx_ExpectAndReturn(event.target_id, 0);
     fwk_id_is_equal_ExpectAnyArgsAndReturn(true);
     mod_sensor_get_data_ExpectAndReturn(
-        dev_ctx->config->sensor_id, &dev_ctx->sensor_data, FWK_PENDING);
+	dev_ctx->config->sensor_id, &dev_ctx->sensor_data, FWK_PENDING);
     status = thermal_mgmt_process_event(&event, NULL);
     TEST_ASSERT_EQUAL(status, FWK_SUCCESS);
 }
@@ -320,9 +320,9 @@ void test_thermal_mgmt_process_event_rea
     struct mod_thermal_mgmt_dev_ctx *dev_ctx;
     int status;
     struct fwk_event event = {
-        .source_id = FWK_ID_NONE,
-        .target_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_THERMAL_MGMT, 0),
-        .id = FWK_ID_NONE,
+	.source_id = FWK_ID_NONE,
+	.target_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_THERMAL_MGMT, 0),
+	.id = FWK_ID_NONE,
     };
     dev_ctx = &mod_ctx.dev_ctx_table[0];
 
@@ -337,7 +337,7 @@ void test_thermal_mgmt_process_event_rea
     fwk_id_get_element_idx_ExpectAndReturn(event.target_id, 0);
     fwk_id_is_equal_ExpectAnyArgsAndReturn(true);
     mod_sensor_get_data_ExpectAndReturn(
-        dev_ctx->config->sensor_id, &dev_ctx->sensor_data, FWK_SUCCESS);
+	dev_ctx->config->sensor_id, &dev_ctx->sensor_data, FWK_SUCCESS);
     status = thermal_mgmt_process_event(&event, NULL);
     TEST_ASSERT_EQUAL(status, FWK_SUCCESS);
     TEST_ASSERT_EQUAL(dev_ctx->cur_temp, dev_ctx->sensor_data.value);
@@ -348,9 +348,9 @@ void test_thermal_mgmt_process_event_rea
     struct mod_thermal_mgmt_dev_ctx *dev_ctx;
     int status;
     struct fwk_event event = {
-        .source_id = FWK_ID_NONE,
-        .target_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_THERMAL_MGMT, 0),
-        .id = FWK_ID_NONE,
+	.source_id = FWK_ID_NONE,
+	.target_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_THERMAL_MGMT, 0),
+	.id = FWK_ID_NONE,
     };
     dev_ctx = &mod_ctx.dev_ctx_table[0];
     dev_ctx->sensor_data.status = FWK_E_DEVICE;
@@ -368,9 +368,9 @@ void test_thermal_mgmt_process_event_rea
     struct mod_thermal_mgmt_dev_ctx *dev_ctx;
     int status;
     struct fwk_event event = {
-        .source_id = FWK_ID_NONE,
-        .target_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_THERMAL_MGMT, 0),
-        .id = FWK_ID_NONE,
+	.source_id = FWK_ID_NONE,
+	.target_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_THERMAL_MGMT, 0),
+	.id = FWK_ID_NONE,
     };
     dev_ctx = &mod_ctx.dev_ctx_table[0];
     dev_ctx->sensor_data.status = FWK_SUCCESS;
@@ -394,18 +394,18 @@ void test_thermal_mgmt_thermal_protectio
     dev_ctx->cur_temp = 71;
 
     fwk_module_get_element_name_ExpectAndReturn(
-        dev_ctx->id, thermal_mgmt_domains_elem_table[0].name);
+	dev_ctx->id, thermal_mgmt_domains_elem_table[0].name);
     mod_thermal_mgmt_protection_api_warning_Expect(
-        dev_ctx->config->temp_protection->driver_id, dev_ctx->id);
+	dev_ctx->config->temp_protection->driver_id, dev_ctx->id);
 
     thermal_protection(dev_ctx);
 
     dev_ctx->cur_temp = 81;
 
     fwk_module_get_element_name_ExpectAndReturn(
-        dev_ctx->id, thermal_mgmt_domains_elem_table[0].name);
+	dev_ctx->id, thermal_mgmt_domains_elem_table[0].name);
     mod_thermal_mgmt_protection_api_critical_Expect(
-        dev_ctx->config->temp_protection->driver_id, dev_ctx->id);
+	dev_ctx->config->temp_protection->driver_id, dev_ctx->id);
 
     thermal_protection(dev_ctx);
 }
@@ -434,7 +434,7 @@ void test_thermal_mgmt_read_temperature_
 
     fwk_id_get_element_idx_ExpectAndReturn(element_id, 0);
     mod_sensor_get_data_ExpectAndReturn(
-        dev_ctx->config->sensor_id, &dev_ctx->sensor_data, FWK_PENDING);
+	dev_ctx->config->sensor_id, &dev_ctx->sensor_data, FWK_PENDING);
 
     status = read_temperature(element_id);
     TEST_ASSERT_EQUAL(status, FWK_PENDING);
@@ -460,7 +460,7 @@ void test_thermal_mgmt_read_temperature_
 
     fwk_id_get_element_idx_ExpectAndReturn(element_id, 0);
     mod_sensor_get_data_ExpectAndReturn(
-        dev_ctx->config->sensor_id, &dev_ctx->sensor_data, FWK_SUCCESS);
+	dev_ctx->config->sensor_id, &dev_ctx->sensor_data, FWK_SUCCESS);
 
     status = read_temperature(element_id);
     TEST_ASSERT_EQUAL(status, FWK_SUCCESS);
@@ -483,7 +483,7 @@ void test_thermal_mgmt_pi_control_switch
     pi_control(element_id);
     TEST_ASSERT_EQUAL(dev_ctx->integral_error, 0);
     TEST_ASSERT_EQUAL(
-        dev_ctx->thermal_allocatable_power, (uint32_t)dev_ctx->config->tdp);
+	dev_ctx->thermal_allocatable_power, (uint32_t)dev_ctx->config->tdp);
 }
 
 void test_thermal_mgmt_pi_control_anti_windup(void)
@@ -544,7 +544,7 @@ void test_thermal_mgmt_pi_control_positi
     k_i = dev_ctx->config->pi_controller.k_integral = 1;
     k_p = dev_ctx->config->pi_controller.k_p_undershoot = 2;
     err =
-        dev_ctx->config->pi_controller.control_temperature - dev_ctx->cur_temp;
+	dev_ctx->config->pi_controller.control_temperature - dev_ctx->cur_temp;
     terr = err + dev_ctx->integral_error;
     allocatable_power = k_p * err + k_i * terr + dev_ctx->config->tdp;
 
@@ -666,17 +666,17 @@ void test_thermal_mgmt_thermal_update_su
     data.adj_max_limit = &adj_max_limit;
 
     for (i = 0; i < mod_ctx.dev_ctx_count; i++) {
-        fwk_id_t element_id =
-            FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_THERMAL_MGMT, i);
-        struct mod_thermal_mgmt_dev_ctx *dev_ctx;
-
-        dev_ctx = &mod_ctx.dev_ctx_table[i];
-        dev_ctx->tick_counter = 0;
-        dev_ctx->control_needs_update = false;
-
-        fwk_id_get_element_idx_ExpectAndReturn(element_id, i);
-        fwk_id_get_element_idx_ExpectAndReturn(element_id, i);
-        distribute_power_Expect(dev_ctx->id, data.level, data.adj_max_limit);
+	fwk_id_t element_id =
+	    FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_THERMAL_MGMT, i);
+	struct mod_thermal_mgmt_dev_ctx *dev_ctx;
+
+	dev_ctx = &mod_ctx.dev_ctx_table[i];
+	dev_ctx->tick_counter = 0;
+	dev_ctx->control_needs_update = false;
+
+	fwk_id_get_element_idx_ExpectAndReturn(element_id, i);
+	fwk_id_get_element_idx_ExpectAndReturn(element_id, i);
+	distribute_power_Expect(dev_ctx->id, data.level, data.adj_max_limit);
     }
 
     status = thermal_update(&data);
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/timer/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/timer/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/timer/CMakeLists.txt
@@ -8,7 +8,7 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_timer.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_timer.c")
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/timer/include/mod_timer.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/timer/include/mod_timer.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/timer/include/mod_timer.h
@@ -51,13 +51,13 @@ enum mod_timer_api_idx {
  * \brief Timer API ID
  */
 #define MOD_TIMER_API_ID_TIMER FWK_ID_API(FWK_MODULE_IDX_TIMER, \
-                                          MOD_TIMER_API_IDX_TIMER)
+					  MOD_TIMER_API_IDX_TIMER)
 
 /*!
  * \brief Alarm API ID
  */
 #define MOD_TIMER_API_ID_ALARM FWK_ID_API(FWK_MODULE_IDX_TIMER, \
-                                          MOD_TIMER_API_IDX_ALARM)
+					  MOD_TIMER_API_IDX_ALARM)
 
 /*!
  * \brief Alarm type.
@@ -156,8 +156,8 @@ struct mod_timer_api {
      * \retval One of the other specific error codes described by the framework.
      */
     int (*time_to_timestamp)(fwk_id_t dev_id,
-                             uint32_t microseconds,
-                             uint64_t *timestamp);
+			     uint32_t microseconds,
+			     uint64_t *timestamp);
 
     /*!
      * \brief Get the current counter value of a given timer.
@@ -210,9 +210,9 @@ struct mod_timer_api {
      * \retval One of the other specific error codes described by the framework.
      */
     int (*wait)(fwk_id_t dev_id,
-                uint32_t microseconds,
-                bool (*cond)(void*),
-                void *data);
+		uint32_t microseconds,
+		bool (*cond)(void*),
+		void *data);
 
     /*!
      * \brief Get the time difference, expressed in timer ticks, between the
@@ -234,8 +234,8 @@ struct mod_timer_api {
      * \note remaining_ticks is also a timestamp.
      */
     int (*remaining)(fwk_id_t dev_id,
-                     uint64_t timestamp,
-                     uint64_t *remaining_ticks);
+		     uint64_t timestamp,
+		     uint64_t *remaining_ticks);
 
     /*!
      * \brief Get the number of ticks before the next alarm trigger of a given
@@ -256,8 +256,8 @@ struct mod_timer_api {
      * \return One of the other specific error codes described by the framework.
      */
     int (*get_next_alarm_remaining)(fwk_id_t dev_id,
-                                    bool *has_alarm,
-                                    uint64_t *remaining_ticks);
+				    bool *has_alarm,
+				    uint64_t *remaining_ticks);
 };
 
 /*!
@@ -297,10 +297,10 @@ struct mod_timer_alarm_api {
      * \return One of the other specific error codes described by the framework.
      */
     int (*start)(fwk_id_t alarm_id,
-                 unsigned int milliseconds,
-                 enum mod_timer_alarm_type type,
-                 void (*callback)(uintptr_t param),
-                 uintptr_t param);
+		 unsigned int milliseconds,
+		 enum mod_timer_alarm_type type,
+		 void (*callback)(uintptr_t param),
+		 uintptr_t param);
 
     /*!
      * \brief Stop a previously started alarm.
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/timer/src/mod_timer.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/timer/src/mod_timer.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/timer/src/mod_timer.c
@@ -88,7 +88,7 @@ static int _time_to_timestamp(
 
     status = ctx->driver->get_frequency(ctx->driver_dev_id, &frequency);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     *timestamp = ((uint64_t)frequency * microseconds) / 1000000;
@@ -109,12 +109,12 @@ static int _timestamp_from_now(
 
     status = _time_to_timestamp(ctx, microseconds, timestamp);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     status = ctx->driver->get_counter(ctx->driver_dev_id, &counter);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     *timestamp += counter;
@@ -135,14 +135,14 @@ static int _remaining(
 
     status = ctx->driver->get_counter(ctx->driver_dev_id, &counter);
     if (!fwk_expect(status == FWK_SUCCESS)) {
-        return status;
+	return status;
     }
 
     /* If timestamp is in the past, remaining_ticks is set to zero. */
     if (timestamp < counter) {
-        *remaining_ticks = 0;
+	*remaining_ticks = 0;
     } else {
-        *remaining_ticks = timestamp - counter;
+	*remaining_ticks = timestamp - counter;
     }
 
     return FWK_SUCCESS;
@@ -156,19 +156,19 @@ static void _configure_timer_with_next_a
     fwk_assert(ctx != NULL);
 
     alarm_head =
-        (struct alarm_sub_element_ctx *)fwk_list_head(&ctx->alarms_active);
+	(struct alarm_sub_element_ctx *)fwk_list_head(&ctx->alarms_active);
     if (alarm_head != NULL) {
-        /* Configure timer device */
-        status =
-            ctx->driver->set_timer(ctx->driver_dev_id, alarm_head->timestamp);
-        if (status != FWK_SUCCESS) {
-            FWK_LOG_DEBUG("[Timer] %s @%d", __func__, __LINE__);
-        }
-
-        status = ctx->driver->enable(ctx->driver_dev_id);
-        if (status != FWK_SUCCESS) {
-            FWK_LOG_DEBUG("[Timer] %s @%d", __func__, __LINE__);
-        }
+	/* Configure timer device */
+	status =
+	    ctx->driver->set_timer(ctx->driver_dev_id, alarm_head->timestamp);
+	if (status != FWK_SUCCESS) {
+	    FWK_LOG_DEBUG("[Timer] %s @%d", __func__, __LINE__);
+	}
+
+	status = ctx->driver->enable(ctx->driver_dev_id);
+	if (status != FWK_SUCCESS) {
+	    FWK_LOG_DEBUG("[Timer] %s @%d", __func__, __LINE__);
+	}
     }
 }
 
@@ -190,14 +190,14 @@ static void _insert_alarm_ctx_into_activ
     alarm = FWK_LIST_GET(alarm_node, struct alarm_sub_element_ctx, node);
 
     while ((alarm_node != NULL) && (alarm_new->timestamp > alarm->timestamp)) {
-        alarm_node = fwk_list_next(&ctx->alarms_active, alarm_node);
-        alarm = FWK_LIST_GET(alarm_node, struct alarm_sub_element_ctx, node);
+	alarm_node = fwk_list_next(&ctx->alarms_active, alarm_node);
+	alarm = FWK_LIST_GET(alarm_node, struct alarm_sub_element_ctx, node);
     }
 
     /* Insert alarm_new just BEFORE the alarm that was found */
     fwk_list_insert(&ctx->alarms_active,
-                    &(alarm_new->node),
-                    alarm_node);
+		    &(alarm_new->node),
+		    alarm_node);
 
     alarm_new->activated = true;
 }
@@ -214,20 +214,20 @@ static int get_frequency(fwk_id_t dev_id
     ctx = &ctx_table[fwk_id_get_element_idx(dev_id)];
 
     if (frequency == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     return ctx->driver->get_frequency(ctx->driver_dev_id, frequency);
 }
 
 static int time_to_timestamp(fwk_id_t dev_id,
-                             uint32_t microseconds,
-                             uint64_t *timestamp)
+			     uint32_t microseconds,
+			     uint64_t *timestamp)
 {
     struct timer_dev_ctx *ctx;
 
     if (timestamp == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     ctx = &ctx_table[fwk_id_get_element_idx(dev_id)];
@@ -242,7 +242,7 @@ static int get_counter(fwk_id_t dev_id,
     ctx = &ctx_table[fwk_id_get_element_idx(dev_id)];
 
     if (counter == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     /* Read counter */
@@ -259,23 +259,23 @@ static int delay(fwk_id_t dev_id, uint32
 
     status = _timestamp_from_now(ctx, microseconds, &counter_limit);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     do {
-        status = ctx->driver->get_counter(ctx->driver_dev_id, &counter);
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
+	status = ctx->driver->get_counter(ctx->driver_dev_id, &counter);
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
     } while (counter < counter_limit);
 
     return FWK_SUCCESS;
 }
 
 static int wait(fwk_id_t dev_id,
-                uint32_t microseconds,
-                bool (*cond)(void*),
-                void *data)
+		uint32_t microseconds,
+		bool (*cond)(void*),
+		void *data)
 {
     struct timer_dev_ctx *ctx;
     int status;
@@ -285,61 +285,61 @@ static int wait(fwk_id_t dev_id,
 
     status = _timestamp_from_now(ctx, microseconds, &counter_limit);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     while (true) {
-        if (cond(data)) {
-            return FWK_SUCCESS;
-        }
-
-        status = ctx->driver->get_counter(ctx->driver_dev_id, &counter);
-        if (status != FWK_SUCCESS) {
-            return FWK_E_DEVICE;
-        }
-
-        /*
-         * If the time to wait is over, check condition one last time.
-         */
-        if (counter > counter_limit) {
-            if (cond(data)) {
-                return FWK_SUCCESS;
-            } else {
-                return FWK_E_TIMEOUT;
-            }
-        }
+	if (cond(data)) {
+	    return FWK_SUCCESS;
+	}
+
+	status = ctx->driver->get_counter(ctx->driver_dev_id, &counter);
+	if (status != FWK_SUCCESS) {
+	    return FWK_E_DEVICE;
+	}
+
+	/*
+	 * If the time to wait is over, check condition one last time.
+	 */
+	if (counter > counter_limit) {
+	    if (cond(data)) {
+		return FWK_SUCCESS;
+	    } else {
+		return FWK_E_TIMEOUT;
+	    }
+	}
     }
 }
 
 static int remaining(fwk_id_t dev_id,
-                     uint64_t timestamp,
-                     uint64_t *remaining_ticks)
+		     uint64_t timestamp,
+		     uint64_t *remaining_ticks)
 {
     struct timer_dev_ctx *ctx;
 
     ctx = &ctx_table[fwk_id_get_element_idx(dev_id)];
 
     if (remaining_ticks == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     return _remaining(ctx, timestamp, remaining_ticks);
 }
 
 static int get_next_alarm_remaining(fwk_id_t dev_id,
-                                    bool *has_alarm,
-                                    uint64_t *remaining_ticks)
+				    bool *has_alarm,
+				    uint64_t *remaining_ticks)
 {
     int status, exit_status;
     const struct timer_dev_ctx *ctx;
     const struct alarm_sub_element_ctx *alarm_ctx;
     const struct fwk_dlist_node *alarm_ctx_node;
     if (has_alarm == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     if (remaining_ticks == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     ctx = &ctx_table[fwk_id_get_element_idx(dev_id)];
@@ -350,24 +350,24 @@ static int get_next_alarm_remaining(fwk_
      */
     status = ctx->driver->disable(ctx->driver_dev_id);
     if (status != FWK_SUCCESS) {
-        return FWK_E_DEVICE;
+	return FWK_E_DEVICE;
     }
 
     *has_alarm = !fwk_list_is_empty(&ctx->alarms_active);
 
     if (*has_alarm) {
-        alarm_ctx_node = fwk_list_head(&ctx->alarms_active);
-        alarm_ctx =
-            FWK_LIST_GET(alarm_ctx_node, struct alarm_sub_element_ctx, node);
+	alarm_ctx_node = fwk_list_head(&ctx->alarms_active);
+	alarm_ctx =
+	    FWK_LIST_GET(alarm_ctx_node, struct alarm_sub_element_ctx, node);
 
-        exit_status = _remaining(ctx, alarm_ctx->timestamp, remaining_ticks);
+	exit_status = _remaining(ctx, alarm_ctx->timestamp, remaining_ticks);
     } else {
-        exit_status = FWK_E_PARAM;
+	exit_status = FWK_E_PARAM;
     }
 
     status = ctx->driver->enable(ctx->driver_dev_id);
     if (status != FWK_SUCCESS) {
-        return FWK_E_DEVICE;
+	return FWK_E_DEVICE;
     }
 
     return exit_status;
@@ -401,23 +401,23 @@ static int alarm_stop(fwk_id_t alarm_id)
     status = fwk_interrupt_get_current(&interrupt);
     switch (status) {
     case FWK_E_STATE:
-        /* Not within an ISR */
-        break;
+	/* Not within an ISR */
+	break;
 
     case FWK_SUCCESS:
-        /* Within an ISR */
+	/* Within an ISR */
 
-        if (interrupt == ctx->config->timer_irq) {
-            /*
-             * The interrupt handler is the interrupt handler for the alarm's
-             * timer
-             */
-            break;
-        }
-        /* Fall-through */
+	if (interrupt == ctx->config->timer_irq) {
+	    /*
+	     * The interrupt handler is the interrupt handler for the alarm's
+	     * timer
+	     */
+	    break;
+	}
+	/* Fall-through */
 
     default:
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 
     alarm = &ctx->alarm_pool[fwk_id_get_sub_element_idx(alarm_id)];
@@ -425,22 +425,22 @@ static int alarm_stop(fwk_id_t alarm_id)
     /* Prevent possible data races with the timer interrupt */
     status = ctx->driver->disable(ctx->driver_dev_id);
     if (status != FWK_SUCCESS) {
-        return FWK_E_DEVICE;
+	return FWK_E_DEVICE;
     }
 
     if (!alarm->started) {
-        status = ctx->driver->enable(ctx->driver_dev_id);
-        if (status != FWK_SUCCESS) {
-            return FWK_E_DEVICE;
-        } else {
-            return FWK_E_STATE;
-        }
+	status = ctx->driver->enable(ctx->driver_dev_id);
+	if (status != FWK_SUCCESS) {
+	    return FWK_E_DEVICE;
+	} else {
+	    return FWK_E_STATE;
+	}
     }
 
     alarm->started = false;
 
     if (!alarm->activated) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     /*
@@ -454,7 +454,7 @@ static int alarm_stop(fwk_id_t alarm_id)
      */
     status = fwk_interrupt_clear_pending(ctx->config->timer_irq);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     fwk_list_remove(&ctx->alarms_active, (struct fwk_dlist_node *)alarm);
@@ -466,10 +466,10 @@ static int alarm_stop(fwk_id_t alarm_id)
 }
 
 static int alarm_start(fwk_id_t alarm_id,
-                       unsigned int milliseconds,
-                       enum mod_timer_alarm_type type,
-                       void (*callback)(uintptr_t param),
-                       uintptr_t param)
+		       unsigned int milliseconds,
+		       enum mod_timer_alarm_type type,
+		       void (*callback)(uintptr_t param),
+		       uintptr_t param)
 {
     int status;
     struct timer_dev_ctx *ctx;
@@ -480,21 +480,21 @@ static int alarm_start(fwk_id_t alarm_id
 
     status = fwk_interrupt_get_current(&interrupt);
     if (status != FWK_E_STATE) {
-        /*
-         * Could not attain call context OR this function is called from an
-         * interrupt handler.
-         */
-        return FWK_E_ACCESS;
+	/*
+	 * Could not attain call context OR this function is called from an
+	 * interrupt handler.
+	 */
+	return FWK_E_ACCESS;
     }
 
     ctx = ctx_table + fwk_id_get_element_idx(alarm_id);
     alarm = &ctx->alarm_pool[fwk_id_get_sub_element_idx(alarm_id)];
 
     if (alarm->started) {
-        status = alarm_stop(alarm_id);
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
+	status = alarm_stop(alarm_id);
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
     }
 
     alarm->started = true;
@@ -506,19 +506,19 @@ static int alarm_start(fwk_id_t alarm_id
     alarm->callback = callback;
     alarm->param = param;
     alarm->periodic =
-        (type == MOD_TIMER_ALARM_TYPE_PERIODIC ? true : false);
+	(type == MOD_TIMER_ALARM_TYPE_PERIODIC ? true : false);
     alarm->microseconds = milliseconds * 1000;
     status = _timestamp_from_now(ctx,
-                                 alarm->microseconds,
-                                 &alarm->timestamp);
+				 alarm->microseconds,
+				 &alarm->timestamp);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     /* Disable timer interrupts to work with the active queue */
     status = ctx->driver->disable(ctx->driver_dev_id);
     if (status != FWK_SUCCESS) {
-        return FWK_E_DEVICE;
+	return FWK_E_DEVICE;
     }
 
     _insert_alarm_ctx_into_active_queue(ctx, alarm);
@@ -545,21 +545,21 @@ static void timer_isr(uintptr_t ctx_ptr)
     /* Disable timer interrupts to work with the active queue */
     status = ctx->driver->disable(ctx->driver_dev_id);
     if (status != FWK_SUCCESS) {
-        FWK_LOG_DEBUG("[Timer] %s @%d", __func__, __LINE__);
+	FWK_LOG_DEBUG("[Timer] %s @%d", __func__, __LINE__);
     }
 
     status = fwk_interrupt_clear_pending(ctx->config->timer_irq);
     if (status != FWK_SUCCESS) {
-        FWK_LOG_DEBUG("[Timer] %s @%d", __func__, __LINE__);
+	FWK_LOG_DEBUG("[Timer] %s @%d", __func__, __LINE__);
     }
 
     alarm =
-        (struct alarm_sub_element_ctx *)fwk_list_pop_head(&ctx->alarms_active);
+	(struct alarm_sub_element_ctx *)fwk_list_pop_head(&ctx->alarms_active);
 
     if (alarm == NULL) {
-        /* Timer interrupt triggered without any alarm in the active queue */
-        fwk_unexpected();
-        return;
+	/* Timer interrupt triggered without any alarm in the active queue */
+	fwk_unexpected();
+	return;
     }
 
     alarm->activated = false;
@@ -568,17 +568,17 @@ static void timer_isr(uintptr_t ctx_ptr)
     alarm->callback(alarm->param);
 
     if (alarm->periodic && alarm->started) {
-        /* Put this alarm back into the active queue */
-        status = _time_to_timestamp(ctx, alarm->microseconds, &timestamp);
+	/* Put this alarm back into the active queue */
+	status = _time_to_timestamp(ctx, alarm->microseconds, &timestamp);
 
-        if (status == FWK_SUCCESS) {
-            alarm->timestamp += timestamp;
-            _insert_alarm_ctx_into_active_queue(ctx, alarm);
-        } else {
-            FWK_LOG_ERR(
-                "[Timer] Error: Periodic alarm could not be added "
-                "back into queue.");
-        }
+	if (status == FWK_SUCCESS) {
+	    alarm->timestamp += timestamp;
+	    _insert_alarm_ctx_into_active_queue(ctx, alarm);
+	} else {
+	    FWK_LOG_ERR(
+		"[Timer] Error: Periodic alarm could not be added "
+		"back into queue.");
+	}
     }
 
     _configure_timer_with_next_alarm(ctx);
@@ -589,8 +589,8 @@ static void timer_isr(uintptr_t ctx_ptr)
  */
 
 static int timer_init(fwk_id_t module_id,
-                      unsigned int element_count,
-                      const void *data)
+		      unsigned int element_count,
+		      const void *data)
 {
     ctx_table = fwk_mm_calloc(element_count, sizeof(struct timer_dev_ctx));
 
@@ -598,7 +598,7 @@ static int timer_init(fwk_id_t module_id
 }
 
 static int timer_device_init(fwk_id_t element_id, unsigned int alarm_count,
-                             const void *data)
+			     const void *data)
 {
     struct timer_dev_ctx *ctx;
 
@@ -608,8 +608,8 @@ static int timer_device_init(fwk_id_t el
     ctx->config = data;
 
     if (alarm_count > 0) {
-        ctx->alarm_pool =
-            fwk_mm_calloc(alarm_count, sizeof(struct alarm_sub_element_ctx));
+	ctx->alarm_pool =
+	    fwk_mm_calloc(alarm_count, sizeof(struct alarm_sub_element_ctx));
     }
 
     return FWK_SUCCESS;
@@ -624,11 +624,11 @@ static int timer_bind(fwk_id_t id, unsig
 
     /* Nothing to do after the initial round. */
     if (round > 0) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     if (fwk_id_is_type(id, FWK_ID_TYPE_MODULE)) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     ctx = ctx_table + fwk_id_get_element_idx(id);
@@ -637,16 +637,16 @@ static int timer_bind(fwk_id_t id, unsig
     /* Bind to the driver API for the current device */
     driver_module_idx = fwk_id_get_module_idx(ctx->driver_dev_id);
     status = fwk_module_bind(ctx->driver_dev_id,
-                             FWK_ID_API(driver_module_idx, 0),
-                             &driver);
+			     FWK_ID_API(driver_module_idx, 0),
+			     &driver);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     /* Check that the driver API is completely fulfilled */
     if (driver->enable == NULL || driver->disable == NULL ||
-        driver->get_counter == NULL || driver->get_frequency == NULL) {
-        return FWK_E_DEVICE;
+	driver->get_counter == NULL || driver->get_frequency == NULL) {
+	return FWK_E_DEVICE;
     }
 
     ctx->driver = driver;
@@ -655,36 +655,36 @@ static int timer_bind(fwk_id_t id, unsig
 }
 
 static int timer_process_bind_request(fwk_id_t requester_id,
-                                      fwk_id_t id,
-                                      fwk_id_t api_id,
-                                      const void **api)
+				      fwk_id_t id,
+				      fwk_id_t api_id,
+				      const void **api)
 {
     struct timer_dev_ctx *ctx;
     struct alarm_sub_element_ctx *alarm_ctx;
 
     if (fwk_id_is_equal(api_id, MOD_TIMER_API_ID_TIMER)) {
-        if (!fwk_module_is_valid_element_id(id)) {
-            fwk_unexpected();
-            return FWK_E_PARAM;
-        }
+	if (!fwk_module_is_valid_element_id(id)) {
+	    fwk_unexpected();
+	    return FWK_E_PARAM;
+	}
 
-        *api = &timer_api;
-        return FWK_SUCCESS;
+	*api = &timer_api;
+	return FWK_SUCCESS;
     }
 
     /* Alarm API requested */
 
     if (!fwk_module_is_valid_sub_element_id(id)) {
-        fwk_unexpected();
-        return FWK_E_PARAM;
+	fwk_unexpected();
+	return FWK_E_PARAM;
     }
 
     ctx = ctx_table + fwk_id_get_element_idx(id);
     alarm_ctx = &ctx->alarm_pool[fwk_id_get_sub_element_idx(id)];
 
     if (alarm_ctx->bound) {
-        fwk_unexpected();
-        return FWK_E_STATE;
+	fwk_unexpected();
+	return FWK_E_STATE;
     }
 
     alarm_ctx->bound = true;
@@ -699,7 +699,7 @@ static int timer_start(fwk_id_t id)
     struct timer_dev_ctx *ctx;
 
     if (!fwk_module_is_valid_element_id(id)) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     ctx = ctx_table + fwk_id_get_element_idx(id);
@@ -707,9 +707,9 @@ static int timer_start(fwk_id_t id)
     fwk_list_init(&ctx->alarms_active);
 
     status = fwk_interrupt_set_isr_param(
-        ctx->config->timer_irq, timer_isr, (uintptr_t)ctx);
+	ctx->config->timer_irq, timer_isr, (uintptr_t)ctx);
     if (status != FWK_SUCCESS) {
-        return status;
+	return status;
     }
 
     return fwk_interrupt_enable(ctx->config->timer_irq);
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/traffic_cop/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/traffic_cop/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/traffic_cop/CMakeLists.txt
@@ -8,10 +8,10 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_traffic_cop.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_traffic_cop.c")
 
 target_link_libraries(${SCP_MODULE_TARGET} PRIVATE module-power-domain
-                                                   module-scmi-perf)
+						   module-scmi-perf)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/traffic_cop/src/mod_traffic_cop.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/traffic_cop/src/mod_traffic_cop.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/traffic_cop/src/mod_traffic_cop.c
@@ -70,9 +70,9 @@ static struct mod_tcop_domain_ctx *get_d
     uint32_t idx = fwk_id_get_element_idx(domain_id);
 
     if (idx < tcop_ctx.tcop_domain_count) {
-        return &tcop_ctx.domain_ctx[idx];
+	return &tcop_ctx.domain_ctx[idx];
     } else {
-        return NULL;
+	return NULL;
     }
 }
 
@@ -87,15 +87,15 @@ static uint32_t tcop_evaluate_perf_limit
     pct_size = ctx->domain_config->pct_size;
     /* Start from last table index */
     for (pct_idx = (pct_size - 1); pct_idx >= 0; pct_idx--) {
-        /* Search for the table entry that matches the number of cores online */
-        if (ctx->num_cores_online <= pct_config[pct_idx].cores_online) {
-            return pct_config[pct_idx].perf_limit;
-        }
+	/* Search for the table entry that matches the number of cores online */
+	if (ctx->num_cores_online <= pct_config[pct_idx].cores_online) {
+	    return pct_config[pct_idx].perf_limit;
+	}
     }
 
     FWK_LOG_WARN(
-        "[TRAFFIC_COP] No entry found in the PCT for %ld online cores",
-        (long)ctx->num_cores_online);
+	"[TRAFFIC_COP] No entry found in the PCT for %ld online cores",
+	(long)ctx->num_cores_online);
 
     return 0;
 }
@@ -113,19 +113,19 @@ static int tcop_update(struct perf_plugi
      * function argument.
      */
     fwk_id_t perf_id = FWK_ID_ELEMENT(
-        FWK_MODULE_IDX_DVFS, fwk_id_get_element_idx(data->domain_id));
+	FWK_MODULE_IDX_DVFS, fwk_id_get_element_idx(data->domain_id));
 
     for (domain_idx = 0; domain_idx < tcop_ctx.tcop_domain_count;
-         domain_idx++) {
-        if (fwk_id_is_equal(
-                tcop_ctx.domain_ctx[domain_idx].domain_config->perf_id,
-                perf_id)) {
-            break;
-        }
+	 domain_idx++) {
+	if (fwk_id_is_equal(
+		tcop_ctx.domain_ctx[domain_idx].domain_config->perf_id,
+		perf_id)) {
+	    break;
+	}
     }
 
     if (domain_idx == tcop_ctx.tcop_domain_count) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     domain_ctx = &tcop_ctx.domain_ctx[domain_idx];
@@ -143,27 +143,27 @@ static int tcop_report(struct perf_plugi
     struct fwk_event resp_notif;
     struct mod_tcop_domain_ctx *domain_ctx;
     struct mod_pd_power_state_pre_transition_notification_resp_params
-        *pd_resp_params =
-            (struct mod_pd_power_state_pre_transition_notification_resp_params
-                 *)resp_notif.params;
+	*pd_resp_params =
+	    (struct mod_pd_power_state_pre_transition_notification_resp_params
+		 *)resp_notif.params;
     /*
      * Get the performance element id from the sub-element provided in the
      * function argument.
      */
     fwk_id_t perf_id = FWK_ID_ELEMENT(
-        FWK_MODULE_IDX_DVFS, fwk_id_get_element_idx(data->dep_dom_id));
+	FWK_MODULE_IDX_DVFS, fwk_id_get_element_idx(data->dep_dom_id));
 
     for (domain_idx = 0; domain_idx < tcop_ctx.tcop_domain_count;
-         domain_idx++) {
-        if (fwk_id_is_equal(
-                tcop_ctx.domain_ctx[domain_idx].domain_config->perf_id,
-                perf_id)) {
-            break;
-        }
+	 domain_idx++) {
+	if (fwk_id_is_equal(
+		tcop_ctx.domain_ctx[domain_idx].domain_config->perf_id,
+		perf_id)) {
+	    break;
+	}
     }
 
     if (domain_idx == tcop_ctx.tcop_domain_count) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     domain_ctx = &tcop_ctx.domain_ctx[domain_idx];
@@ -175,23 +175,23 @@ static int tcop_report(struct perf_plugi
      * notification so the core can now be turned on.
      */
     for (core_idx = 0; core_idx < domain_ctx->num_cores; core_idx++) {
-        if (domain_ctx->core_ctx[core_idx].pd_blocked) {
-            domain_ctx->core_ctx[core_idx].pd_blocked = false;
+	if (domain_ctx->core_ctx[core_idx].pd_blocked) {
+	    domain_ctx->core_ctx[core_idx].pd_blocked = false;
 
-            status = fwk_get_delayed_response(
-                domain_ctx->domain_id,
-                domain_ctx->core_ctx[core_idx].cookie,
-                &resp_notif);
-            if (status != FWK_SUCCESS) {
-                return status;
-            }
-
-            pd_resp_params->status = FWK_SUCCESS;
-            status = fwk_put_event(&resp_notif);
-            if (status != FWK_SUCCESS) {
-                return status;
-            }
-        }
+	    status = fwk_get_delayed_response(
+		domain_ctx->domain_id,
+		domain_ctx->core_ctx[core_idx].cookie,
+		&resp_notif);
+	    if (status != FWK_SUCCESS) {
+		return status;
+	    }
+
+	    pd_resp_params->status = FWK_SUCCESS;
+	    status = fwk_put_event(&resp_notif);
+	    if (status != FWK_SUCCESS) {
+		return status;
+	    }
+	}
     }
 
     return FWK_SUCCESS;
@@ -211,12 +211,12 @@ static int tcop_init(
     const void *data)
 {
     if (element_count == 0) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     tcop_ctx.tcop_domain_count = element_count;
     tcop_ctx.domain_ctx =
-        fwk_mm_calloc(element_count, sizeof(struct mod_tcop_domain_ctx));
+	fwk_mm_calloc(element_count, sizeof(struct mod_tcop_domain_ctx));
 
     return FWK_SUCCESS;
 }
@@ -232,7 +232,7 @@ static int tcop_element_init(
     uint32_t core_idx;
 
     if (sub_element_count == 0) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     domain_ctx = get_domain_ctx(domain_id);
@@ -244,17 +244,17 @@ static int tcop_element_init(
     fwk_assert(domain_ctx->domain_config->pct != NULL);
 
     domain_ctx->core_ctx =
-        fwk_mm_calloc(sub_element_count, sizeof(struct mod_tcop_core_ctx));
+	fwk_mm_calloc(sub_element_count, sizeof(struct mod_tcop_core_ctx));
 
     /* Initialize each core */
     for (core_idx = 0; core_idx < domain_ctx->num_cores; core_idx++) {
-        core_ctx = &domain_ctx->core_ctx[core_idx];
-        core_config = &domain_ctx->domain_config->core_config[core_idx];
+	core_ctx = &domain_ctx->core_ctx[core_idx];
+	core_config = &domain_ctx->domain_config->core_config[core_idx];
 
-        if (core_config->core_starts_online) {
-            domain_ctx->num_cores_online++;
-            core_ctx->online = true;
-        }
+	if (core_config->core_starts_online) {
+	    domain_ctx->num_cores_online++;
+	    core_ctx->online = true;
+	}
     }
 
     return FWK_SUCCESS;
@@ -264,13 +264,13 @@ static int tcop_bind(fwk_id_t id, unsign
 {
     /* Bind in the second round */
     if ((round == 0) || (!fwk_module_is_valid_module_id(id))) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     return fwk_module_bind(
-        FWK_ID_MODULE(FWK_MODULE_IDX_SCMI_PERF),
-        FWK_ID_API(FWK_MODULE_IDX_SCMI_PERF, MOD_SCMI_PERF_PLUGINS_API),
-        &tcop_ctx.perf_plugins_handler_api);
+	FWK_ID_MODULE(FWK_MODULE_IDX_SCMI_PERF),
+	FWK_ID_API(FWK_MODULE_IDX_SCMI_PERF, MOD_SCMI_PERF_PLUGINS_API),
+	&tcop_ctx.perf_plugins_handler_api);
 }
 
 static int tcop_process_bind_request(
@@ -280,9 +280,9 @@ static int tcop_process_bind_request(
     const void **api)
 {
     if (fwk_id_is_equal(source_id, FWK_ID_MODULE(FWK_MODULE_IDX_SCMI_PERF))) {
-        *api = &perf_plugins_api;
+	*api = &perf_plugins_api;
     } else {
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 
     return FWK_SUCCESS;
@@ -295,28 +295,28 @@ static int tcop_start(fwk_id_t id)
     struct mod_tcop_domain_ctx *domain_ctx;
 
     if (fwk_module_is_valid_module_id(id)) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     /* Subscribe to core power state transition */
     domain_ctx = get_domain_ctx(id);
 
     for (i = 0; i < domain_ctx->num_cores; i++) {
-        status = fwk_notification_subscribe(
-            mod_pd_notification_id_power_state_pre_transition,
-            domain_ctx->domain_config->core_config[i].pd_id,
-            domain_ctx->domain_id);
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
-
-        status = fwk_notification_subscribe(
-            mod_pd_notification_id_power_state_transition,
-            domain_ctx->domain_config->core_config[i].pd_id,
-            domain_ctx->domain_id);
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
+	status = fwk_notification_subscribe(
+	    mod_pd_notification_id_power_state_pre_transition,
+	    domain_ctx->domain_config->core_config[i].pd_id,
+	    domain_ctx->domain_id);
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
+
+	status = fwk_notification_subscribe(
+	    mod_pd_notification_id_power_state_transition,
+	    domain_ctx->domain_config->core_config[i].pd_id,
+	    domain_ctx->domain_id);
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
     }
 
     /*
@@ -333,83 +333,83 @@ static int tcop_process_notification(
     struct fwk_event *resp_event)
 {
     struct mod_pd_power_state_pre_transition_notification_params
-        *pre_state_params;
+	*pre_state_params;
     struct mod_pd_power_state_transition_notification_params *post_state_params;
     struct mod_pd_power_state_pre_transition_notification_resp_params
-        *pd_resp_params =
-            (struct mod_pd_power_state_pre_transition_notification_resp_params
-                 *)resp_event->params;
+	*pd_resp_params =
+	    (struct mod_pd_power_state_pre_transition_notification_resp_params
+		 *)resp_event->params;
     struct mod_tcop_domain_ctx *domain_ctx;
     uint32_t core_idx;
 
     fwk_assert(fwk_module_is_valid_element_id(event->target_id));
     domain_ctx = get_domain_ctx(event->target_id);
     if (domain_ctx == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     /* Find the corresponding core */
     for (core_idx = 0; core_idx < domain_ctx->num_cores; core_idx++) {
-        if (fwk_id_is_equal(
-                domain_ctx->domain_config->core_config[core_idx].pd_id,
-                event->source_id)) {
-            break;
-        }
+	if (fwk_id_is_equal(
+		domain_ctx->domain_config->core_config[core_idx].pd_id,
+		event->source_id)) {
+	    break;
+	}
     }
 
     if (core_idx >= domain_ctx->num_cores) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     if (fwk_id_is_equal(
-            event->id, mod_pd_notification_id_power_state_pre_transition)) {
-        pre_state_params =
-            (struct mod_pd_power_state_pre_transition_notification_params *)
-                event->params;
-        pd_resp_params->status = FWK_SUCCESS;
-        if (pre_state_params->target_state == MOD_PD_STATE_ON) {
-            /* The core is transitioning to online */
-            domain_ctx->num_cores_online++;
-            domain_ctx->core_ctx[core_idx].online = true;
-            domain_ctx->perf_limit = tcop_evaluate_perf_limit(domain_ctx);
-
-            /* Set the new limits */
-            struct plugin_limits_req plugin_limit_req = {
-                .domain_id = domain_ctx->domain_config->perf_id,
-                .max_limit = domain_ctx->perf_limit,
-            };
-
-            tcop_ctx.perf_plugins_handler_api->plugin_set_limits(
-                &plugin_limit_req);
-
-            /*
-             * If the perf limit requested will not trigger a dvfs change, there
-             * is no need to block the power domain state transition.
-             */
-            if (domain_ctx->perf_limit >= domain_ctx->current_perf_level) {
-                return FWK_SUCCESS;
-            }
-
-            /* Block the power domain until the new level is applied */
-            domain_ctx->core_ctx[core_idx].pd_blocked = true;
-            domain_ctx->core_ctx[core_idx].cookie = event->cookie;
-            resp_event->is_delayed_response = true;
-        }
+	    event->id, mod_pd_notification_id_power_state_pre_transition)) {
+	pre_state_params =
+	    (struct mod_pd_power_state_pre_transition_notification_params *)
+		event->params;
+	pd_resp_params->status = FWK_SUCCESS;
+	if (pre_state_params->target_state == MOD_PD_STATE_ON) {
+	    /* The core is transitioning to online */
+	    domain_ctx->num_cores_online++;
+	    domain_ctx->core_ctx[core_idx].online = true;
+	    domain_ctx->perf_limit = tcop_evaluate_perf_limit(domain_ctx);
+
+	    /* Set the new limits */
+	    struct plugin_limits_req plugin_limit_req = {
+		.domain_id = domain_ctx->domain_config->perf_id,
+		.max_limit = domain_ctx->perf_limit,
+	    };
+
+	    tcop_ctx.perf_plugins_handler_api->plugin_set_limits(
+		&plugin_limit_req);
+
+	    /*
+	     * If the perf limit requested will not trigger a dvfs change, there
+	     * is no need to block the power domain state transition.
+	     */
+	    if (domain_ctx->perf_limit >= domain_ctx->current_perf_level) {
+		return FWK_SUCCESS;
+	    }
+
+	    /* Block the power domain until the new level is applied */
+	    domain_ctx->core_ctx[core_idx].pd_blocked = true;
+	    domain_ctx->core_ctx[core_idx].cookie = event->cookie;
+	    resp_event->is_delayed_response = true;
+	}
 
     } else if (fwk_id_is_equal(
-                   event->id, mod_pd_notification_id_power_state_transition)) {
-        post_state_params =
-            (struct mod_pd_power_state_transition_notification_params *)
-                event->params;
-        if (post_state_params->state != MOD_PD_STATE_ON) {
-            /* The core transitioned to offline */
-            domain_ctx->num_cores_online--;
-            domain_ctx->core_ctx[core_idx].online = false;
-            domain_ctx->perf_limit = tcop_evaluate_perf_limit(domain_ctx);
-            if (domain_ctx->perf_limit == 0) {
-                return FWK_E_PARAM;
-            }
-        }
+		   event->id, mod_pd_notification_id_power_state_transition)) {
+	post_state_params =
+	    (struct mod_pd_power_state_transition_notification_params *)
+		event->params;
+	if (post_state_params->state != MOD_PD_STATE_ON) {
+	    /* The core transitioned to offline */
+	    domain_ctx->num_cores_online--;
+	    domain_ctx->core_ctx[core_idx].online = false;
+	    domain_ctx->perf_limit = tcop_evaluate_perf_limit(domain_ctx);
+	    if (domain_ctx->perf_limit == 0) {
+		return FWK_E_PARAM;
+	    }
+	}
     }
 
     return FWK_SUCCESS;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/traffic_cop/test/config_traffic_cop.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/traffic_cop/test/config_traffic_cop.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/traffic_cop/test/config_traffic_cop.h
@@ -41,43 +41,43 @@ enum fake_tcop_element_idx {
 
 static struct mod_tcop_pct_table fake_pct_table[FAKE_MOD_TCOP_PCT_COUNT] = {
     [FAKE_MOD_TCOP_PCT_0] = {
-        .cores_online = 2,
-        .perf_limit = 1001 * 1000000UL,
+	.cores_online = 2,
+	.perf_limit = 1001 * 1000000UL,
     },
     [FAKE_MOD_TCOP_PCT_1] = {
-        .cores_online = 1,
-        .perf_limit = 1002 * 1000000UL,
+	.cores_online = 1,
+	.perf_limit = 1002 * 1000000UL,
     },
 };
 
 static const struct mod_tcop_core_config
     fake_core_config[FAKE_MOD_TCOP_CORE_CFG_COUNT] = {
     [FAKE_MOD_TCOP_CORE_CFG_0] = {
-        .pd_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_POWER_DOMAIN, 0),
-        .core_starts_online = true,
-        },
+	.pd_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_POWER_DOMAIN, 0),
+	.core_starts_online = true,
+	},
     [FAKE_MOD_TCOP_CORE_CFG_1] = {
-        .pd_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_POWER_DOMAIN, 1),
-        .core_starts_online = false,
-        },
+	.pd_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_POWER_DOMAIN, 1),
+	.core_starts_online = false,
+	},
 };
 
 static const struct mod_tcop_domain_config
     fake_domain_conf[FAKE_MOD_TCOP_DOM_COUNT] = {
     [FAKE_MOD_TCOP_DOM_0] = {
-        .perf_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_DVFS, 0),
-        .pct = fake_pct_table,
-        .pct_size = FWK_ARRAY_SIZE(fake_pct_table),
-        .core_config = fake_core_config,
+	.perf_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_DVFS, 0),
+	.pct = fake_pct_table,
+	.pct_size = FWK_ARRAY_SIZE(fake_pct_table),
+	.core_config = fake_core_config,
     },
     [FAKE_MOD_TCOP_DOM_1] = { { 0 } },
 };
 
 static const struct fwk_element element_table[FAKE_MOD_TCOP_ELEM_COUNT] = {
     [FAKE_MOD_TCOP_ELEM_0] = {
-        .name = "ELEM-0",
-        .sub_element_count = 2,
-        .data = fake_domain_conf,
+	.name = "ELEM-0",
+	.sub_element_count = 2,
+	.data = fake_domain_conf,
     },
     [FAKE_MOD_TCOP_ELEM_1] = { 0 },
 };
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/traffic_cop/test/mod_traffic_cop_unit_test.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/traffic_cop/test/mod_traffic_cop_unit_test.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/traffic_cop/test/mod_traffic_cop_unit_test.c
@@ -46,13 +46,13 @@ void setUp(void)
 
     /* Initialize each core */
     for (core_idx = 0; core_idx < domain_ctx->num_cores; core_idx++) {
-        core_ctx = &domain_ctx->core_ctx[core_idx];
-        core_config = &domain_ctx->domain_config->core_config[core_idx];
+	core_ctx = &domain_ctx->core_ctx[core_idx];
+	core_config = &domain_ctx->domain_config->core_config[core_idx];
 
-        if (core_config->core_starts_online) {
-            domain_ctx->num_cores_online++;
-            core_ctx->online = true;
-        }
+	if (core_config->core_starts_online) {
+	    domain_ctx->num_cores_online++;
+	    core_ctx->online = true;
+	}
     }
 }
 
@@ -65,7 +65,7 @@ void test_function_tcop_start_mod_id_suc
     int status;
 
     fwk_module_is_valid_module_id_ExpectAndReturn(
-        fwk_module_id_traffic_cop, true);
+	fwk_module_id_traffic_cop, true);
 
     status = tcop_start(fwk_module_id_traffic_cop);
     TEST_ASSERT_EQUAL(status, FWK_SUCCESS);
@@ -92,7 +92,7 @@ int fwk_notif_sub_cback_fail_subscribe_p
     int cmock_num_calls)
 {
     if (cmock_num_calls == 0) {
-        return FWK_E_STATE;
+	return FWK_E_STATE;
     }
 
     return FWK_SUCCESS;
@@ -105,7 +105,7 @@ int fwk_notif_sub_cback_fail_subscribe_p
     int cmock_num_calls)
 {
     if (cmock_num_calls == 2) {
-        return FWK_E_STATE;
+	return FWK_E_STATE;
     }
 
     return FWK_SUCCESS;
@@ -120,7 +120,7 @@ void test_function_tcop_start_notif_subs
 
     fwk_id_get_element_idx_ExpectAndReturn(elem_id, 0);
     fwk_notification_subscribe_Stub(
-        fwk_notif_sub_cback_fail_subscribe_pre_transition);
+	fwk_notif_sub_cback_fail_subscribe_pre_transition);
 
     status = tcop_start(elem_id);
     TEST_ASSERT_EQUAL(status, FWK_E_STATE);
@@ -135,7 +135,7 @@ void test_function_tcop_start_notif_subs
 
     fwk_id_get_element_idx_ExpectAndReturn(elem_id, 0);
     fwk_notification_subscribe_Stub(
-        fwk_notif_sub_cback_fail_subscribe_post_transition);
+	fwk_notif_sub_cback_fail_subscribe_post_transition);
 
     status = tcop_start(elem_id);
     TEST_ASSERT_EQUAL(status, FWK_E_STATE);
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/transport/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/transport/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/transport/CMakeLists.txt
@@ -8,10 +8,10 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_transport.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_transport.c")
 
 if("scmi" IN_LIST SCP_MODULES)
     target_link_libraries(${SCP_MODULE_TARGET} PRIVATE module-scmi)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/transport/doc/transport.md
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/transport/doc/transport.md
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/transport/doc/transport.md
@@ -22,19 +22,19 @@ drivers like MHU, MHUv2, etc. It also de
 to notify the client module on receiving a message.
 
 ```
-                   +--------+
-                   | Shared |
-                   | Memory |
-                   +--------+
-                        ^
+		   +--------+
+		   | Shared |
+		   | Memory |
+		   +--------+
+			^
 +--------+              |                +--------+       +-----------------+
 | DRIVER |<-+           |            +-->|  SCMI  |<----->| PROTOCOL MODULE |
 +--------+  |           |            |   +--------+       +-----------------+
-            |           |            |
+	    |           |            |
 +--------+  |    +------+------+     |
 | DRIVER |<-+--->|  TRANSPORT  |<----+
 +--------+  |    +-------------+     |
-            |                        |   +--------+
+	    |                        |   +--------+
 +--------+  |                        +-->| MODULE |
 | DRIVER |<-+                            +--------+
 +--------+
@@ -94,50 +94,50 @@ from the signal API. The recipient modul
 and payload from the transport module and then processes the message.
 
 ```
-        +--------+                    +-----------+                           +--------+
-        | DRIVER |                    | TRANSPORT |                           | MODULE |
-        +---+----+                    +-----+-----+                           +----+---+
+	+--------+                    +-----------+                           +--------+
+	| DRIVER |                    | TRANSPORT |                           | MODULE |
+	+---+----+                    +-----+-----+                           +----+---+
  Interrupt  |                               |                                      |
 ----------->+---+                           |                                      |
-            |   | isr()                     |                                      |
-            |<--+                           |                                      |
-            |        signal_message()       |                                      |
-            +------------------------------>|                                      |
-            |                               |                                      |
-            |          get_message()        |                                      |
-            |<------------------------------+                                      |
-            |                               |                                      |
-            +----+Copy message from         |                                      |
-            |    |message status registers  |                                      |
-            |<---+                          |                                      |
-            |       return status           |                                      |
-            +------------------------------>|                                      |
-            |                               +-----+                                |
-            |                               |     |                                |
-            |                               |     | Verify the message             |
-            |                               |     |                                |
-            |                               |     |                                |
-            |                               |<----+                                |
-            |                               |         signal_message()             |
-            |                               +------------------------------------->|
-            |                               |          return status               |
-            |        return status          |<-------------------------------------+
-            |<------------------------------+                                      |
-            |                               |                                      |
-            |                               |          get_message_header()        |
-            |                               |<-------------------------------------+
-            |                               |                                      |
-            |                               |          get_payload()               |
-            |                               |<-------------------------------------+
-            |                               |                                      +-----+
-            |                               |                                      |     |
-            |                               | transport_release_channel_lock() or  |     | Process the message
-            |                               |  respond()                           |<----+
-            |                               |<-------------------------------------+
-            |                               |                                      |
-            |                               |           return status              |
-            |                               +------------------------------------->|
-            |                               |                                      |
+	    |   | isr()                     |                                      |
+	    |<--+                           |                                      |
+	    |        signal_message()       |                                      |
+	    +------------------------------>|                                      |
+	    |                               |                                      |
+	    |          get_message()        |                                      |
+	    |<------------------------------+                                      |
+	    |                               |                                      |
+	    +----+Copy message from         |                                      |
+	    |    |message status registers  |                                      |
+	    |<---+                          |                                      |
+	    |       return status           |                                      |
+	    +------------------------------>|                                      |
+	    |                               +-----+                                |
+	    |                               |     |                                |
+	    |                               |     | Verify the message             |
+	    |                               |     |                                |
+	    |                               |     |                                |
+	    |                               |<----+                                |
+	    |                               |         signal_message()             |
+	    |                               +------------------------------------->|
+	    |                               |          return status               |
+	    |        return status          |<-------------------------------------+
+	    |<------------------------------+                                      |
+	    |                               |                                      |
+	    |                               |          get_message_header()        |
+	    |                               |<-------------------------------------+
+	    |                               |                                      |
+	    |                               |          get_payload()               |
+	    |                               |<-------------------------------------+
+	    |                               |                                      +-----+
+	    |                               |                                      |     |
+	    |                               | transport_release_channel_lock() or  |     | Process the message
+	    |                               |  respond()                           |<----+
+	    |                               |<-------------------------------------+
+	    |                               |                                      |
+	    |                               |           return status              |
+	    |                               +------------------------------------->|
+	    |                               |                                      |
 ```
 ## OUT-BAND message communication
 
@@ -190,39 +190,39 @@ recipient module then retrieves the mess
 transport module and then processes the message.
 
 ```
-        +--------+                      +---------+                      +--------+
-        | DRIVER |                      |TRANSPORT|                      | MODULE |
-        +---+----+                      +----+----+                      +----+---+
-            |                                |                                |
+	+--------+                      +---------+                      +--------+
+	| DRIVER |                      |TRANSPORT|                      | MODULE |
+	+---+----+                      +----+----+                      +----+---+
+	    |                                |                                |
   Interrupt |                                |                                |
 ----------->+---+                            |                                |
-            |   | isr()                      |                                |
-            |   |                            |                                |
-            |<--+                            |                                |
-            |          signal_message()      |                                |
-            +------------------------------->+----+                           |
-            |                                |    |Check mailbox status       |
-            |                                |    |Copy the message header    |
-            |                                |    |Verify & copy the payload  |
-            |                                |<---+                           |
-            |                                |      signal_message()          |
-            |                                +------------------------------->|
-            |                                |        return status           |
-            |         return status          |<-------------------------------+
-            |<-------------------------------+                                |
-            |                                |     get_message_header()       |
-            |                                |<-------------------------------+
-            |                                |        get_payload()           |
-            |                                |<-------------------------------+
-            |                                |                                +------+
-            |                                |                                |      |
-            |                                |                                |      | Process the message
-            |                                |transport_release_channel_lock()|      |
-            |                                |     or transport_respond()     |<-----+
-            |                                |<-------------------------------+
-            |                                |        return status           |
-            |                                +------------------------------->|
-            |                                |                                |
+	    |   | isr()                      |                                |
+	    |   |                            |                                |
+	    |<--+                            |                                |
+	    |          signal_message()      |                                |
+	    +------------------------------->+----+                           |
+	    |                                |    |Check mailbox status       |
+	    |                                |    |Copy the message header    |
+	    |                                |    |Verify & copy the payload  |
+	    |                                |<---+                           |
+	    |                                |      signal_message()          |
+	    |                                +------------------------------->|
+	    |                                |        return status           |
+	    |         return status          |<-------------------------------+
+	    |<-------------------------------+                                |
+	    |                                |     get_message_header()       |
+	    |                                |<-------------------------------+
+	    |                                |        get_payload()           |
+	    |                                |<-------------------------------+
+	    |                                |                                +------+
+	    |                                |                                |      |
+	    |                                |                                |      | Process the message
+	    |                                |transport_release_channel_lock()|      |
+	    |                                |     or transport_respond()     |<-----+
+	    |                                |<-------------------------------+
+	    |                                |        return status           |
+	    |                                +------------------------------->|
+	    |                                |                                |
 
 ```
 
@@ -318,64 +318,64 @@ shared memory.
 
 static const struct fwk_element transport_element_table[] = {
     [0] = {
-        .name = "SCP2MCP_SCMI_TRANSPORT",
-        .data = &((
-            struct mod_transport_channel_config) {
-            .transport_type = MOD_TRANSPORT_CHANNEL_TRANSPORT_TYPE_IN_BAND,
-            .channel_type = MOD_TRANSPORT_CHANNEL_TYPE_REQUESTER,
-            .in_band_mailbox_size = SCP_SCMI_PAYLOAD_SIZE,
-            .driver_id =
-                FWK_ID_SUB_ELEMENT_INIT(
-                    FWK_MODULE_IDX_MHU2,
-                    SCP_PLATFORM_MHU_DEVICE_IDX_SCP2MCP,
-                    0),
-            .driver_api_id =
-                FWK_ID_API_INIT(
-                    FWK_MODULE_IDX_MHU2,
-                    1),
-        }),
+	.name = "SCP2MCP_SCMI_TRANSPORT",
+	.data = &((
+	    struct mod_transport_channel_config) {
+	    .transport_type = MOD_TRANSPORT_CHANNEL_TRANSPORT_TYPE_IN_BAND,
+	    .channel_type = MOD_TRANSPORT_CHANNEL_TYPE_REQUESTER,
+	    .in_band_mailbox_size = SCP_SCMI_PAYLOAD_SIZE,
+	    .driver_id =
+		FWK_ID_SUB_ELEMENT_INIT(
+		    FWK_MODULE_IDX_MHU2,
+		    SCP_PLATFORM_MHU_DEVICE_IDX_SCP2MCP,
+		    0),
+	    .driver_api_id =
+		FWK_ID_API_INIT(
+		    FWK_MODULE_IDX_MHU2,
+		    1),
+	}),
     },
     [1] = {
-        .name = "SCP2MCP_FIRMWARE_TRANSPORT",
-        .data = &((
-            struct mod_transport_channel_config) {
-            .transport_type = MOD_TRANSPORT_CHANNEL_TRANSPORT_TYPE_OUT_BAND,
-            .channel_type = MOD_TRANSPORT_CHANNEL_TYPE_COMPLETER,
-            .policies =  MOD_TRANSPORT_POLICY_INIT_MAILBOX,
-            .out_band_mailbox_address = (uintptr_t) SCP_PAYLOAD_NS_A2P_BASE,
-            .out_band_mailbox_size = SCP_PAYLOAD_SIZE,
-            .signal_api_id = FWK_ID_API_INIT(
-                                FWK_MODULE_IDX_TEST,
-                                0),
-            .driver_id =
-                FWK_ID_SUB_ELEMENT_INIT(
-                    FWK_MODULE_IDX_MHU2,
-                    SCP_PLATFORM_MHU_DEVICE_IDX_SCP2MCP,
-                    1),
-            .driver_api_id =
-                FWK_ID_API_INIT(
-                    FWK_MODULE_IDX_MHU2,
-                    1),
-        }),
+	.name = "SCP2MCP_FIRMWARE_TRANSPORT",
+	.data = &((
+	    struct mod_transport_channel_config) {
+	    .transport_type = MOD_TRANSPORT_CHANNEL_TRANSPORT_TYPE_OUT_BAND,
+	    .channel_type = MOD_TRANSPORT_CHANNEL_TYPE_COMPLETER,
+	    .policies =  MOD_TRANSPORT_POLICY_INIT_MAILBOX,
+	    .out_band_mailbox_address = (uintptr_t) SCP_PAYLOAD_NS_A2P_BASE,
+	    .out_band_mailbox_size = SCP_PAYLOAD_SIZE,
+	    .signal_api_id = FWK_ID_API_INIT(
+				FWK_MODULE_IDX_TEST,
+				0),
+	    .driver_id =
+		FWK_ID_SUB_ELEMENT_INIT(
+		    FWK_MODULE_IDX_MHU2,
+		    SCP_PLATFORM_MHU_DEVICE_IDX_SCP2MCP,
+		    1),
+	    .driver_api_id =
+		FWK_ID_API_INIT(
+		    FWK_MODULE_IDX_MHU2,
+		    1),
+	}),
     },
     [2] = {
-        .name = "SCP2MCP_EVENT",
-        .data = &((
-            struct mod_transport_channel_config) {
-            .transport_type = MOD_TRANSPORT_CHANNEL_TRANSPORT_TYPE_NONE,
-            .signal_api_id = FWK_ID_API_INIT(
-                                FWK_MODULE_IDX_TEST,
-                                0),
-            .driver_id =
-                FWK_ID_SUB_ELEMENT_INIT(
-                    FWK_MODULE_IDX_MHU2,
-                    SCP_PLATFORM_MHU_DEVICE_IDX_SCP2MCP,
-                    2),
-            .driver_api_id =
-                FWK_ID_API_INIT(
-                    FWK_MODULE_IDX_MHU2,
-                    1),
-        }),
+	.name = "SCP2MCP_EVENT",
+	.data = &((
+	    struct mod_transport_channel_config) {
+	    .transport_type = MOD_TRANSPORT_CHANNEL_TRANSPORT_TYPE_NONE,
+	    .signal_api_id = FWK_ID_API_INIT(
+				FWK_MODULE_IDX_TEST,
+				0),
+	    .driver_id =
+		FWK_ID_SUB_ELEMENT_INIT(
+		    FWK_MODULE_IDX_MHU2,
+		    SCP_PLATFORM_MHU_DEVICE_IDX_SCP2MCP,
+		    2),
+	    .driver_api_id =
+		FWK_ID_API_INIT(
+		    FWK_MODULE_IDX_MHU2,
+		    1),
+	}),
     },
     [3] = { 0 },
 };
@@ -387,9 +387,9 @@ static const struct fwk_element *transpo
 
     config = (struct mod_transport_channel_config *)(transport_element_table[idx].data);
     config->pd_source_id = FWK_ID_ELEMENT(
-        FWK_MODULE_IDX_POWER_DOMAIN,
-        platform_get_core_count() + platform_get_cluster_count() +
-            PD_STATIC_DEV_IDX_SYSTOP);
+	FWK_MODULE_IDX_POWER_DOMAIN,
+	platform_get_core_count() + platform_get_cluster_count() +
+	    PD_STATIC_DEV_IDX_SYSTOP);
 
     return transport_element_table;
 }
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/transport/include/mod_transport.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/transport/include/mod_transport.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/transport/include/mod_transport.h
@@ -216,8 +216,8 @@ struct mod_transport_driver_api {
      *      errors
      */
     int (*send_message)(
-        struct mod_transport_buffer *message,
-        fwk_id_t device_id);
+	struct mod_transport_buffer *message,
+	fwk_id_t device_id);
 
     /*!
      * \brief Retrieve the in-band message from the driver
@@ -230,7 +230,7 @@ struct mod_transport_driver_api {
      *      errors
      */
     int (
-        *get_message)(struct mod_transport_buffer *message, fwk_id_t device_id);
+	*get_message)(struct mod_transport_buffer *message, fwk_id_t device_id);
 #endif
 
     /*!
@@ -265,9 +265,9 @@ struct mod_transport_driver_api {
      * \retval ::FWK_SUCCESS The operation succeeded.
      */
     int (*fch_register_callback)(
-        fwk_id_t fch_id,
-        uintptr_t param,
-        void (*fch_callback)(uintptr_t param));
+	fwk_id_t fch_id,
+	uintptr_t param,
+	void (*fch_callback)(uintptr_t param));
 #endif
 };
 
@@ -376,10 +376,10 @@ struct mod_transport_firmware_api {
      *      errors.
      */
     int (*write_payload)(
-        fwk_id_t channel_id,
-        size_t offset,
-        const void *payload,
-        size_t size);
+	fwk_id_t channel_id,
+	size_t offset,
+	const void *payload,
+	size_t size);
 
     /*!
      * \brief Respond to message on a channel.
@@ -413,11 +413,11 @@ struct mod_transport_firmware_api {
      *      errors.
      */
     int (*transmit)(
-        fwk_id_t channel_id,
-        uint32_t message_header,
-        const void *payload,
-        size_t size,
-        bool request_ack_by_interrupt);
+	fwk_id_t channel_id,
+	uint32_t message_header,
+	const void *payload,
+	size_t size,
+	bool request_ack_by_interrupt);
 
     /*!
      * \brief Release the transport channel context lock.
@@ -504,9 +504,9 @@ struct mod_transport_fast_channels_api {
      * \retval ::FWK_SUCCESS The operation succeeded.
      */
     int (*transport_fch_register_callback)(
-        fwk_id_t fch_id,
-        uintptr_t param,
-        void (*fch_callback)(uintptr_t param));
+	fwk_id_t fch_id,
+	uintptr_t param,
+	void (*fch_callback)(uintptr_t param));
 };
 #endif
 
@@ -547,8 +547,8 @@ enum mod_transport_notification_idx {
  */
 static const fwk_id_t mod_transport_notification_id_initialized =
     FWK_ID_NOTIFICATION_INIT(
-        FWK_MODULE_IDX_TRANSPORT,
-        MOD_TRANSPORT_NOTIFICATION_IDX_INITIALIZED);
+	FWK_MODULE_IDX_TRANSPORT,
+	MOD_TRANSPORT_NOTIFICATION_IDX_INITIALIZED);
 
 /*!
  * @}
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/transport/src/mod_transport.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/transport/src/mod_transport.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/transport/src/mod_transport.c
@@ -62,11 +62,11 @@ struct transport_channel_ctx {
     /* Service APIs to signal incoming messages or errors */
     union mod_transport_signal_api {
 #ifdef BUILD_HAS_MOD_SCMI
-        /* For SCMI messages or errors */
-        struct mod_scmi_from_transport_api *scmi_signal_api;
+	/* For SCMI messages or errors */
+	struct mod_scmi_from_transport_api *scmi_signal_api;
 #endif
-        /* For Firmware messages or errors */
-        struct mod_transport_firmware_signal_api *firmware_signal_api;
+	/* For Firmware messages or errors */
+	struct mod_transport_firmware_signal_api *firmware_signal_api;
     } transport_signal;
 
     /* Flag indicating the service bound to the channel is of type SCMI */
@@ -94,16 +94,16 @@ static int transport_get_secure(fwk_id_t
     struct transport_channel_ctx *channel_ctx;
 
     if (secure == NULL) {
-        fwk_unexpected();
-        return FWK_E_PARAM;
+	fwk_unexpected();
+	return FWK_E_PARAM;
     }
 
     channel_ctx =
-        &transport_ctx.channel_ctx_table[fwk_id_get_element_idx(channel_id)];
+	&transport_ctx.channel_ctx_table[fwk_id_get_element_idx(channel_id)];
 
     *secure =
-        ((channel_ctx->config->policies & MOD_TRANSPORT_POLICY_SECURE) !=
-         (uint32_t)0);
+	((channel_ctx->config->policies & MOD_TRANSPORT_POLICY_SECURE) !=
+	 (uint32_t)0);
 
     return FWK_SUCCESS;
 }
@@ -113,12 +113,12 @@ static int transport_get_max_payload_siz
     struct transport_channel_ctx *channel_ctx;
 
     if (size == NULL) {
-        fwk_unexpected();
-        return FWK_E_PARAM;
+	fwk_unexpected();
+	return FWK_E_PARAM;
     }
 
     channel_ctx =
-        &transport_ctx.channel_ctx_table[fwk_id_get_element_idx(channel_id)];
+	&transport_ctx.channel_ctx_table[fwk_id_get_element_idx(channel_id)];
 
     *size = channel_ctx->max_payload_size;
 
@@ -130,19 +130,19 @@ static int transport_get_message_header(
     struct transport_channel_ctx *channel_ctx;
 
     if (header == NULL) {
-        fwk_unexpected();
-        return FWK_E_PARAM;
+	fwk_unexpected();
+	return FWK_E_PARAM;
     }
 
     channel_ctx =
-        &transport_ctx.channel_ctx_table[fwk_id_get_element_idx(channel_id)];
+	&transport_ctx.channel_ctx_table[fwk_id_get_element_idx(channel_id)];
 
     fwk_assert(
-        channel_ctx->config->transport_type !=
-        MOD_TRANSPORT_CHANNEL_TRANSPORT_TYPE_NONE);
+	channel_ctx->config->transport_type !=
+	MOD_TRANSPORT_CHANNEL_TRANSPORT_TYPE_NONE);
 
     if (!channel_ctx->locked) {
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 
     *header = channel_ctx->in->message_header;
@@ -158,19 +158,19 @@ static int transport_get_payload(
     struct transport_channel_ctx *channel_ctx;
 
     if (payload == NULL || size == NULL) {
-        fwk_unexpected();
-        return FWK_E_PARAM;
+	fwk_unexpected();
+	return FWK_E_PARAM;
     }
 
     channel_ctx =
-        &transport_ctx.channel_ctx_table[fwk_id_get_element_idx(channel_id)];
+	&transport_ctx.channel_ctx_table[fwk_id_get_element_idx(channel_id)];
 
     fwk_assert(
-        channel_ctx->config->transport_type !=
-        MOD_TRANSPORT_CHANNEL_TRANSPORT_TYPE_NONE);
+	channel_ctx->config->transport_type !=
+	MOD_TRANSPORT_CHANNEL_TRANSPORT_TYPE_NONE);
 
     if (!channel_ctx->locked) {
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 
     *payload = channel_ctx->in->payload;
@@ -189,24 +189,24 @@ static int transport_write_payload(
     struct transport_channel_ctx *channel_ctx;
 
     channel_ctx =
-        &transport_ctx.channel_ctx_table[fwk_id_get_element_idx(channel_id)];
+	&transport_ctx.channel_ctx_table[fwk_id_get_element_idx(channel_id)];
 
     fwk_assert(
-        channel_ctx->config->transport_type !=
-        MOD_TRANSPORT_CHANNEL_TRANSPORT_TYPE_NONE);
+	channel_ctx->config->transport_type !=
+	MOD_TRANSPORT_CHANNEL_TRANSPORT_TYPE_NONE);
 
     if ((payload == NULL) ||
-        ((offset + size) > channel_ctx->max_payload_size)) {
-        fwk_unexpected();
-        return FWK_E_PARAM;
+	((offset + size) > channel_ctx->max_payload_size)) {
+	fwk_unexpected();
+	return FWK_E_PARAM;
     }
 
     if (!channel_ctx->locked) {
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 
     fwk_str_memcpy(
-        ((uint8_t *)channel_ctx->out->payload) + offset, payload, size);
+	((uint8_t *)channel_ctx->out->payload) + offset, payload, size);
 
     return FWK_SUCCESS;
 }
@@ -223,41 +223,41 @@ static int transport_respond(
     unsigned int flags;
 
     channel_ctx =
-        &transport_ctx.channel_ctx_table[fwk_id_get_element_idx(channel_id)];
+	&transport_ctx.channel_ctx_table[fwk_id_get_element_idx(channel_id)];
 
     transport_type = channel_ctx->config->transport_type;
 
     fwk_assert(transport_type != MOD_TRANSPORT_CHANNEL_TRANSPORT_TYPE_NONE);
 
     if (transport_type == MOD_TRANSPORT_CHANNEL_TRANSPORT_TYPE_OUT_BAND) {
-        /* Use shared mailbox for out-band messages */
-        buffer = ((struct mod_transport_buffer *)
-                      channel_ctx->config->out_band_mailbox_address);
-
-        /* Copy the header and other fields from the write buffer */
-        fwk_str_memcpy(
-            buffer, channel_ctx->out, sizeof(struct mod_transport_buffer));
-
-        /*
-         * Copy the payload from either the write buffer or the payload
-         * parameter.
-         */
-        fwk_str_memcpy(
-            buffer->payload,
-            (payload == NULL ? channel_ctx->out->payload : payload),
-            size);
+	/* Use shared mailbox for out-band messages */
+	buffer = ((struct mod_transport_buffer *)
+		      channel_ctx->config->out_band_mailbox_address);
+
+	/* Copy the header and other fields from the write buffer */
+	fwk_str_memcpy(
+	    buffer, channel_ctx->out, sizeof(struct mod_transport_buffer));
+
+	/*
+	 * Copy the payload from either the write buffer or the payload
+	 * parameter.
+	 */
+	fwk_str_memcpy(
+	    buffer->payload,
+	    (payload == NULL ? channel_ctx->out->payload : payload),
+	    size);
     } else {
 #ifdef BUILD_HAS_INBAND_MSG_SUPPORT
-        /* Use internal write buffer for in-band messages */
-        buffer = channel_ctx->out;
+	/* Use internal write buffer for in-band messages */
+	buffer = channel_ctx->out;
 
-        /* Copy the payload from the payload parameter */
-        if (payload != NULL) {
-            fwk_str_memcpy(buffer->payload, payload, size);
-        }
+	/* Copy the payload from the payload parameter */
+	if (payload != NULL) {
+	    fwk_str_memcpy(buffer->payload, payload, size);
+	}
 #else
-        FWK_LOG_ERR("%s ERROR. IN-BAND MESSAGE NOT SUPPORTED!\n", MOD_NAME);
-        return FWK_E_SUPPORT;
+	FWK_LOG_ERR("%s ERROR. IN-BAND MESSAGE NOT SUPPORTED!\n", MOD_NAME);
+	return FWK_E_SUPPORT;
 #endif
     }
 
@@ -279,19 +279,19 @@ static int transport_respond(
 
 #ifdef BUILD_HAS_INBAND_MSG_SUPPORT
     if (transport_type == MOD_TRANSPORT_CHANNEL_TRANSPORT_TYPE_IN_BAND) {
-        /* Send the response message using driver module API */
-        status = channel_ctx->driver_api->send_message(
-            buffer, channel_ctx->config->driver_id);
-
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
+	/* Send the response message using driver module API */
+	status = channel_ctx->driver_api->send_message(
+	    buffer, channel_ctx->config->driver_id);
+
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
     }
 #endif
 
     if (buffer->flags & MOD_TRANSPORT_MAILBOX_FLAGS_IENABLED_MASK) {
-        status = channel_ctx->driver_api->trigger_event(
-            channel_ctx->config->driver_id);
+	status = channel_ctx->driver_api->trigger_event(
+	    channel_ctx->config->driver_id);
     }
 
     return status;
@@ -312,50 +312,50 @@ static int transport_transmit(
     enum mod_transport_channel_transport_type transport_type;
 
     channel_ctx =
-        &transport_ctx.channel_ctx_table[fwk_id_get_element_idx(channel_id)];
+	&transport_ctx.channel_ctx_table[fwk_id_get_element_idx(channel_id)];
 
     transport_type = channel_ctx->config->transport_type;
 
     fwk_assert(transport_type != MOD_TRANSPORT_CHANNEL_TRANSPORT_TYPE_NONE);
 
     if (transport_type == MOD_TRANSPORT_CHANNEL_TRANSPORT_TYPE_OUT_BAND) {
-        /* Use shared mailbox for out-band messages */
-        buffer = ((struct mod_transport_buffer *)
-                      channel_ctx->config->out_band_mailbox_address);
-        /*
-         * If the agent/platform has not yet read the previous message we
-         * abandon this transmission. We don't want to poll on the BUSY/FREE
-         * bit, and while it is probably safe to just overwrite the data
-         * the agent/platform could be in the process of reading.
-         */
-        if ((buffer->status & MOD_TRANSPORT_MAILBOX_STATUS_FREE_MASK) ==
-            (uint32_t)0) {
-            return FWK_E_BUSY;
-        }
+	/* Use shared mailbox for out-band messages */
+	buffer = ((struct mod_transport_buffer *)
+		      channel_ctx->config->out_band_mailbox_address);
+	/*
+	 * If the agent/platform has not yet read the previous message we
+	 * abandon this transmission. We don't want to poll on the BUSY/FREE
+	 * bit, and while it is probably safe to just overwrite the data
+	 * the agent/platform could be in the process of reading.
+	 */
+	if ((buffer->status & MOD_TRANSPORT_MAILBOX_STATUS_FREE_MASK) ==
+	    (uint32_t)0) {
+	    return FWK_E_BUSY;
+	}
     } else {
 #ifdef BUILD_HAS_INBAND_MSG_SUPPORT
-        /* Use internal write buffer for in-band messages */
-        buffer = channel_ctx->out;
-        /* reserved fields must be set to zero */
-        buffer->reserved0 = 0;
-        buffer->reserved1 = 0;
+	/* Use internal write buffer for in-band messages */
+	buffer = channel_ctx->out;
+	/* reserved fields must be set to zero */
+	buffer->reserved0 = 0;
+	buffer->reserved1 = 0;
 #else
-        FWK_LOG_ERR("%s ERROR. IN-BAND MESSAGES NOT SUPPORTED!\n", MOD_NAME);
-        return FWK_E_SUPPORT;
+	FWK_LOG_ERR("%s ERROR. IN-BAND MESSAGES NOT SUPPORTED!\n", MOD_NAME);
+	return FWK_E_SUPPORT;
 #endif
     }
 
     buffer->message_header = message_header;
 
     if (request_ack_by_interrupt) {
-        buffer->flags |= MOD_TRANSPORT_MAILBOX_FLAGS_IENABLED_MASK;
+	buffer->flags |= MOD_TRANSPORT_MAILBOX_FLAGS_IENABLED_MASK;
     } else {
-        buffer->flags &= ~MOD_TRANSPORT_MAILBOX_FLAGS_IENABLED_MASK;
+	buffer->flags &= ~MOD_TRANSPORT_MAILBOX_FLAGS_IENABLED_MASK;
     }
 
     /* Copy the payload */
     if (payload != NULL && size != 0) {
-        fwk_str_memcpy(buffer->payload, payload, size);
+	fwk_str_memcpy(buffer->payload, payload, size);
     }
 
     buffer->length = (volatile uint32_t)(sizeof(buffer->message_header) + size);
@@ -364,19 +364,19 @@ static int transport_transmit(
 
 #ifdef BUILD_HAS_INBAND_MSG_SUPPORT
     if (transport_type == MOD_TRANSPORT_CHANNEL_TRANSPORT_TYPE_IN_BAND) {
-        /* Send the SCMI message using driver module API */
-        status = channel_ctx->driver_api->send_message(
-            channel_ctx->out, channel_ctx->config->driver_id);
-
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
+	/* Send the SCMI message using driver module API */
+	status = channel_ctx->driver_api->send_message(
+	    channel_ctx->out, channel_ctx->config->driver_id);
+
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
     }
 #endif
 
     /* Notify the agent/platform and return */
     return channel_ctx->driver_api->trigger_event(
-        channel_ctx->config->driver_id);
+	channel_ctx->config->driver_id);
 }
 
 static int transport_release_channel_lock(fwk_id_t channel_id)
@@ -384,7 +384,7 @@ static int transport_release_channel_loc
     struct transport_channel_ctx *channel_ctx;
 
     channel_ctx =
-        &transport_ctx.channel_ctx_table[fwk_id_get_element_idx(channel_id)];
+	&transport_ctx.channel_ctx_table[fwk_id_get_element_idx(channel_id)];
 
     /*
      * If the received message is a response message, then release
@@ -404,27 +404,27 @@ static int transport_trigger_interrupt(f
     struct transport_channel_ctx *channel_ctx;
 
     channel_ctx =
-        &transport_ctx.channel_ctx_table[fwk_id_get_element_idx(channel_id)];
+	&transport_ctx.channel_ctx_table[fwk_id_get_element_idx(channel_id)];
 
     fwk_assert(
-        channel_ctx->config->transport_type ==
-        MOD_TRANSPORT_CHANNEL_TRANSPORT_TYPE_NONE);
+	channel_ctx->config->transport_type ==
+	MOD_TRANSPORT_CHANNEL_TRANSPORT_TYPE_NONE);
 
     return channel_ctx->driver_api->trigger_event(
-        channel_ctx->config->driver_id);
+	channel_ctx->config->driver_id);
 }
 
 #ifdef BUILD_HAS_MOD_SCMI
 static const struct mod_scmi_to_transport_api
     transport_mod_scmi_to_transport_api = {
-        .get_secure = transport_get_secure,
-        .get_max_payload_size = transport_get_max_payload_size,
-        .get_message_header = transport_get_message_header,
-        .get_payload = transport_get_payload,
-        .write_payload = transport_write_payload,
-        .respond = transport_respond,
-        .transmit = transport_transmit,
-        .release_transport_channel_lock = transport_release_channel_lock,
+	.get_secure = transport_get_secure,
+	.get_max_payload_size = transport_get_max_payload_size,
+	.get_message_header = transport_get_message_header,
+	.get_payload = transport_get_payload,
+	.write_payload = transport_write_payload,
+	.respond = transport_respond,
+	.transmit = transport_transmit,
+	.release_transport_channel_lock = transport_release_channel_lock,
     };
 #endif
 
@@ -446,19 +446,19 @@ static int transport_get_fch(fwk_id_t fc
     struct transport_channel_ctx *channel_ctx;
 
     if (fch == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     channel_ctx =
-        &transport_ctx.channel_ctx_table[fwk_id_get_element_idx(fch_id)];
+	&transport_ctx.channel_ctx_table[fwk_id_get_element_idx(fch_id)];
 
     fwk_assert(
-        channel_ctx->config->transport_type ==
-        MOD_TRANSPORT_CHANNEL_TRANSPORT_TYPE_FAST_CHANNELS);
+	channel_ctx->config->transport_type ==
+	MOD_TRANSPORT_CHANNEL_TRANSPORT_TYPE_FAST_CHANNELS);
 
     /* Get fast channel from the driver */
     return channel_ctx->driver_api->get_fch(
-        channel_ctx->config->driver_id, fch);
+	channel_ctx->config->driver_id, fch);
 }
 
 static int transport_fch_register_callback(
@@ -469,25 +469,25 @@ static int transport_fch_register_callba
     struct transport_channel_ctx *channel_ctx;
 
     if (fch_callback == NULL) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     channel_ctx =
-        &transport_ctx.channel_ctx_table[fwk_id_get_element_idx(fch_id)];
+	&transport_ctx.channel_ctx_table[fwk_id_get_element_idx(fch_id)];
 
     fwk_assert(
-        channel_ctx->config->transport_type ==
-        MOD_TRANSPORT_CHANNEL_TRANSPORT_TYPE_FAST_CHANNELS);
+	channel_ctx->config->transport_type ==
+	MOD_TRANSPORT_CHANNEL_TRANSPORT_TYPE_FAST_CHANNELS);
 
     /* Provide callback function pointer to driver and get it registered */
     return channel_ctx->driver_api->fch_register_callback(
-        channel_ctx->config->driver_id, param, fch_callback);
+	channel_ctx->config->driver_id, param, fch_callback);
 }
 
 static const struct mod_transport_fast_channels_api
     transport_fast_channels_api = {
-        .transport_get_fch = transport_get_fch,
-        .transport_fch_register_callback = transport_fch_register_callback,
+	.transport_get_fch = transport_get_fch,
+	.transport_fch_register_callback = transport_fch_register_callback,
     };
 #endif
 
@@ -502,57 +502,57 @@ static int transport_message_handler(str
 
     /* Check if we are already processing */
     if (channel_ctx->locked) {
-        return FWK_E_STATE;
+	return FWK_E_STATE;
     }
 
     in = channel_ctx->in;
     out = channel_ctx->out;
 
     if (transport_type == MOD_TRANSPORT_CHANNEL_TRANSPORT_TYPE_OUT_BAND) {
-        shared_memory = ((struct mod_transport_buffer *)
-                             channel_ctx->config->out_band_mailbox_address);
+	shared_memory = ((struct mod_transport_buffer *)
+			     channel_ctx->config->out_band_mailbox_address);
 
-        if (channel_ctx->config->channel_type ==
-            MOD_TRANSPORT_CHANNEL_TYPE_COMPLETER) {
-            /*
-             * The completer type channel is used for handling the requests.
-             *
-             * Check if we have the ownership of shared mailbox. We don't
-             * have the mailbox ownership if the mailbox status is 1 (free).
-             */
-            if (shared_memory->status &
-                MOD_TRANSPORT_MAILBOX_STATUS_FREE_MASK) {
-                FWK_LOG_ERR(
-                    "%s Mailbox ownership error on completer channel %u",
-                    MOD_NAME,
-                    fwk_id_get_element_idx(channel_ctx->id));
-
-                return FWK_E_STATE;
-            }
-        } else if (
-            channel_ctx->config->channel_type ==
-            MOD_TRANSPORT_CHANNEL_TYPE_REQUESTER) {
-            /*
-             * The requester type channel is used for handling the responses.
-             *
-             * Check if we have the ownership of shared mailbox. We don't
-             * have the mailbox ownership if the mailbox status is 0 (busy).
-             */
-            if ((shared_memory->status &
-                 MOD_TRANSPORT_MAILBOX_STATUS_FREE_MASK) == 0) {
-                FWK_LOG_ERR(
-                    "%s Mailbox ownership error on requester channel %u",
-                    MOD_NAME,
-                    fwk_id_get_element_idx(channel_ctx->id));
-
-                return FWK_E_STATE;
-            }
-        }
-        /*
-         * Copy the contents from shared mailbox to internal read buffer.
-         * note: payload is not copied yet.
-         */
-        fwk_str_memcpy(in, shared_memory, sizeof(struct mod_transport_buffer));
+	if (channel_ctx->config->channel_type ==
+	    MOD_TRANSPORT_CHANNEL_TYPE_COMPLETER) {
+	    /*
+	     * The completer type channel is used for handling the requests.
+	     *
+	     * Check if we have the ownership of shared mailbox. We don't
+	     * have the mailbox ownership if the mailbox status is 1 (free).
+	     */
+	    if (shared_memory->status &
+		MOD_TRANSPORT_MAILBOX_STATUS_FREE_MASK) {
+		FWK_LOG_ERR(
+		    "%s Mailbox ownership error on completer channel %u",
+		    MOD_NAME,
+		    fwk_id_get_element_idx(channel_ctx->id));
+
+		return FWK_E_STATE;
+	    }
+	} else if (
+	    channel_ctx->config->channel_type ==
+	    MOD_TRANSPORT_CHANNEL_TYPE_REQUESTER) {
+	    /*
+	     * The requester type channel is used for handling the responses.
+	     *
+	     * Check if we have the ownership of shared mailbox. We don't
+	     * have the mailbox ownership if the mailbox status is 0 (busy).
+	     */
+	    if ((shared_memory->status &
+		 MOD_TRANSPORT_MAILBOX_STATUS_FREE_MASK) == 0) {
+		FWK_LOG_ERR(
+		    "%s Mailbox ownership error on requester channel %u",
+		    MOD_NAME,
+		    fwk_id_get_element_idx(channel_ctx->id));
+
+		return FWK_E_STATE;
+	    }
+	}
+	/*
+	 * Copy the contents from shared mailbox to internal read buffer.
+	 * note: payload is not copied yet.
+	 */
+	fwk_str_memcpy(in, shared_memory, sizeof(struct mod_transport_buffer));
     }
     /*
      * Set the channel context as locked until the bound service completes
@@ -562,9 +562,9 @@ static int transport_message_handler(str
 
 #ifdef BUILD_HAS_INBAND_MSG_SUPPORT
     if (transport_type == MOD_TRANSPORT_CHANNEL_TRANSPORT_TYPE_IN_BAND) {
-        /* get the message from the driver */
-        channel_ctx->driver_api->get_message(
-            in, channel_ctx->config->driver_id);
+	/* get the message from the driver */
+	channel_ctx->driver_api->get_message(
+	    in, channel_ctx->config->driver_id);
     }
 #endif
     /* mirror contents in the read & write buffers (Payload not copied) */
@@ -582,58 +582,58 @@ static int transport_message_handler(str
      * Note: the payload size is permitted to be of size zero.
      */
     if ((in->length < sizeof(in->message_header)) ||
-        ((in->length - sizeof(in->message_header)) >
-         channel_ctx->max_payload_size)) {
-        out->status |= MOD_TRANSPORT_MAILBOX_STATUS_ERROR_MASK;
+	((in->length - sizeof(in->message_header)) >
+	 channel_ctx->max_payload_size)) {
+	out->status |= MOD_TRANSPORT_MAILBOX_STATUS_ERROR_MASK;
 
-        if (channel_ctx->is_scmi) {
+	if (channel_ctx->is_scmi) {
 #ifdef BUILD_HAS_MOD_SCMI
-            status =
-                channel_ctx->transport_signal.scmi_signal_api->signal_error(
-                    channel_ctx->service_id);
+	    status =
+		channel_ctx->transport_signal.scmi_signal_api->signal_error(
+		    channel_ctx->service_id);
 #else
-            FWK_LOG_INFO(
-                "%s Error! SCMI module not included in the build\n", MOD_NAME);
-            return FWK_E_SUPPORT;
-#endif
-        } else {
-            status =
-                channel_ctx->transport_signal.firmware_signal_api->signal_error(
-                    channel_ctx->service_id);
-        }
+	    FWK_LOG_INFO(
+		"%s Error! SCMI module not included in the build\n", MOD_NAME);
+	    return FWK_E_SUPPORT;
+#endif
+	} else {
+	    status =
+		channel_ctx->transport_signal.firmware_signal_api->signal_error(
+		    channel_ctx->service_id);
+	}
     }
 
     if (transport_type == MOD_TRANSPORT_CHANNEL_TRANSPORT_TYPE_OUT_BAND) {
-        shared_memory = ((struct mod_transport_buffer *)
-                             channel_ctx->config->out_band_mailbox_address);
+	shared_memory = ((struct mod_transport_buffer *)
+			     channel_ctx->config->out_band_mailbox_address);
 
-        payload_size = in->length - sizeof(in->message_header);
-        if (payload_size != 0) {
-            /* Copy payload from shared memory to read buffer */
-            fwk_str_memcpy(in->payload, shared_memory->payload, payload_size);
-        }
+	payload_size = in->length - sizeof(in->message_header);
+	if (payload_size != 0) {
+	    /* Copy payload from shared memory to read buffer */
+	    fwk_str_memcpy(in->payload, shared_memory->payload, payload_size);
+	}
     }
 
     /* Let the subscribed service handle the message */
     if (channel_ctx->is_scmi) {
 #ifdef BUILD_HAS_MOD_SCMI
-        /* Signal the SCMI service */
-        status = channel_ctx->transport_signal.scmi_signal_api->signal_message(
-            channel_ctx->service_id);
+	/* Signal the SCMI service */
+	status = channel_ctx->transport_signal.scmi_signal_api->signal_message(
+	    channel_ctx->service_id);
 #else
-        FWK_LOG_INFO(
-            "%s Error! SCMI module not included in the build\n", MOD_NAME);
-        return FWK_E_SUPPORT;
+	FWK_LOG_INFO(
+	    "%s Error! SCMI module not included in the build\n", MOD_NAME);
+	return FWK_E_SUPPORT;
 #endif
     } else {
-        /* Signal the service */
-        status =
-            channel_ctx->transport_signal.firmware_signal_api->signal_message(
-                channel_ctx->service_id);
+	/* Signal the service */
+	status =
+	    channel_ctx->transport_signal.firmware_signal_api->signal_message(
+		channel_ctx->service_id);
     }
 
     if (status != FWK_SUCCESS) {
-        return FWK_E_HANDLER;
+	return FWK_E_HANDLER;
     }
 
     return status;
@@ -648,30 +648,30 @@ static int transport_signal_message(fwk_
     struct transport_channel_ctx *channel_ctx;
 
     channel_ctx =
-        &transport_ctx.channel_ctx_table[fwk_id_get_element_idx(channel_id)];
+	&transport_ctx.channel_ctx_table[fwk_id_get_element_idx(channel_id)];
 
     /*
      * If the channel is used for events only, then signal the module.
      * since, there wouldn't be any messages for this channel.
      */
     if (channel_ctx->config->transport_type ==
-        MOD_TRANSPORT_CHANNEL_TRANSPORT_TYPE_NONE) {
-        /* Signal the service */
-        status =
-            channel_ctx->transport_signal.firmware_signal_api->signal_message(
-                channel_ctx->service_id);
+	MOD_TRANSPORT_CHANNEL_TRANSPORT_TYPE_NONE) {
+	/* Signal the service */
+	status =
+	    channel_ctx->transport_signal.firmware_signal_api->signal_message(
+		channel_ctx->service_id);
 
-        return status;
+	return status;
     }
 
     if (channel_ctx->config->transport_type ==
-        MOD_TRANSPORT_CHANNEL_TRANSPORT_TYPE_OUT_BAND) {
-        if (!channel_ctx->out_band_mailbox_ready) {
-            /* Discard any message in the mailbox when not ready */
-            FWK_LOG_ERR("%s Out-band message not valid", MOD_NAME);
+	MOD_TRANSPORT_CHANNEL_TRANSPORT_TYPE_OUT_BAND) {
+	if (!channel_ctx->out_band_mailbox_ready) {
+	    /* Discard any message in the mailbox when not ready */
+	    FWK_LOG_ERR("%s Out-band message not valid", MOD_NAME);
 
-            return FWK_SUCCESS;
-        }
+	    return FWK_SUCCESS;
+	}
     }
     return transport_message_handler(channel_ctx);
 }
@@ -685,29 +685,29 @@ static int transport_mailbox_init(struct
     int status = FWK_SUCCESS;
 
     if ((channel_ctx->config->policies & MOD_TRANSPORT_POLICY_INIT_MAILBOX) !=
-        (uint32_t)0) {
-        unsigned int notifications_sent;
+	(uint32_t)0) {
+	unsigned int notifications_sent;
 
-        /* Only the completer channel should initialize the shared mailbox */
-        if (channel_ctx->config->channel_type ==
-            MOD_TRANSPORT_CHANNEL_TYPE_COMPLETER) {
-            /* Initialize mailbox such that the requester has ownership */
-            *((struct mod_transport_buffer *)
-                  channel_ctx->config->out_band_mailbox_address) =
-                (struct mod_transport_buffer){
-                    .status = (1U << MOD_TRANSPORT_MAILBOX_STATUS_FREE_POS)
-                };
-        }
-        /* Notify that this mailbox is initialized */
-        struct fwk_event transport_channel_initialized_notification = {
-            .id = mod_transport_notification_id_initialized,
-            .source_id = channel_ctx->id,
-        };
+	/* Only the completer channel should initialize the shared mailbox */
+	if (channel_ctx->config->channel_type ==
+	    MOD_TRANSPORT_CHANNEL_TYPE_COMPLETER) {
+	    /* Initialize mailbox such that the requester has ownership */
+	    *((struct mod_transport_buffer *)
+		  channel_ctx->config->out_band_mailbox_address) =
+		(struct mod_transport_buffer){
+		    .status = (1U << MOD_TRANSPORT_MAILBOX_STATUS_FREE_POS)
+		};
+	}
+	/* Notify that this mailbox is initialized */
+	struct fwk_event transport_channel_initialized_notification = {
+	    .id = mod_transport_notification_id_initialized,
+	    .source_id = channel_ctx->id,
+	};
 
-        channel_ctx->out_band_mailbox_ready = true;
+	channel_ctx->out_band_mailbox_ready = true;
 
-        status = fwk_notification_notify(
-            &transport_channel_initialized_notification, &notifications_sent);
+	status = fwk_notification_notify(
+	    &transport_channel_initialized_notification, &notifications_sent);
     }
     return status;
 }
@@ -721,7 +721,7 @@ static int transport_init(
     const void *data)
 {
     transport_ctx.channel_ctx_table = fwk_mm_calloc(
-        element_count, sizeof(transport_ctx.channel_ctx_table[0]));
+	element_count, sizeof(transport_ctx.channel_ctx_table[0]));
     transport_ctx.channel_count = element_count;
 
     return FWK_SUCCESS;
@@ -735,79 +735,79 @@ static int transport_channel_init(
     struct transport_channel_ctx *channel_ctx;
 
     channel_ctx =
-        &transport_ctx.channel_ctx_table[fwk_id_get_element_idx(channel_id)];
+	&transport_ctx.channel_ctx_table[fwk_id_get_element_idx(channel_id)];
     channel_ctx->config = (struct mod_transport_channel_config *)data;
 
     /* Validate channel config */
     if ((channel_ctx->config->channel_type >=
-         MOD_TRANSPORT_CHANNEL_TYPE_COUNT) ||
-        (channel_ctx->config->transport_type >=
-         MOD_TRANSPORT_CHANNEL_TRANSPORT_TYPE_COUNT)) {
-        fwk_unexpected();
-        return FWK_E_DATA;
+	 MOD_TRANSPORT_CHANNEL_TYPE_COUNT) ||
+	(channel_ctx->config->transport_type >=
+	 MOD_TRANSPORT_CHANNEL_TRANSPORT_TYPE_COUNT)) {
+	fwk_unexpected();
+	return FWK_E_DATA;
     }
 
     /* Validate out-band mailbox address and size */
     if ((channel_ctx->config->transport_type ==
-         MOD_TRANSPORT_CHANNEL_TRANSPORT_TYPE_OUT_BAND) &&
-        ((channel_ctx->config->out_band_mailbox_address == 0) ||
-         (channel_ctx->config->out_band_mailbox_size == 0))) {
-        fwk_unexpected();
-        return FWK_E_DATA;
+	 MOD_TRANSPORT_CHANNEL_TRANSPORT_TYPE_OUT_BAND) &&
+	((channel_ctx->config->out_band_mailbox_address == 0) ||
+	 (channel_ctx->config->out_band_mailbox_size == 0))) {
+	fwk_unexpected();
+	return FWK_E_DATA;
     }
 
 #ifdef BUILD_HAS_INBAND_MSG_SUPPORT
     /* Validate in-band mailbox size */
     if ((channel_ctx->config->transport_type ==
-         MOD_TRANSPORT_CHANNEL_TRANSPORT_TYPE_IN_BAND) &&
-        (channel_ctx->config->in_band_mailbox_size == 0)) {
-        fwk_unexpected();
-        return FWK_E_DATA;
+	 MOD_TRANSPORT_CHANNEL_TRANSPORT_TYPE_IN_BAND) &&
+	(channel_ctx->config->in_band_mailbox_size == 0)) {
+	fwk_unexpected();
+	return FWK_E_DATA;
     }
 #endif
     channel_ctx->id = channel_id;
 
     switch (channel_ctx->config->transport_type) {
     case MOD_TRANSPORT_CHANNEL_TRANSPORT_TYPE_OUT_BAND:
-        channel_ctx->in =
-            fwk_mm_alloc(1, channel_ctx->config->out_band_mailbox_size);
-        channel_ctx->out =
-            fwk_mm_alloc(1, channel_ctx->config->out_band_mailbox_size);
-        channel_ctx->max_payload_size =
-            channel_ctx->config->out_band_mailbox_size -
-            sizeof(struct mod_transport_buffer);
-        break;
+	channel_ctx->in =
+	    fwk_mm_alloc(1, channel_ctx->config->out_band_mailbox_size);
+	channel_ctx->out =
+	    fwk_mm_alloc(1, channel_ctx->config->out_band_mailbox_size);
+	channel_ctx->max_payload_size =
+	    channel_ctx->config->out_band_mailbox_size -
+	    sizeof(struct mod_transport_buffer);
+	break;
 
 #ifdef BUILD_HAS_INBAND_MSG_SUPPORT
     case MOD_TRANSPORT_CHANNEL_TRANSPORT_TYPE_IN_BAND:
-        channel_ctx->in =
-            fwk_mm_alloc(1, channel_ctx->config->in_band_mailbox_size);
-        channel_ctx->out =
-            fwk_mm_alloc(1, channel_ctx->config->in_band_mailbox_size);
-        channel_ctx->max_payload_size =
-            channel_ctx->config->in_band_mailbox_size -
-            sizeof(struct mod_transport_buffer);
-        break;
+	channel_ctx->in =
+	    fwk_mm_alloc(1, channel_ctx->config->in_band_mailbox_size);
+	channel_ctx->out =
+	    fwk_mm_alloc(1, channel_ctx->config->in_band_mailbox_size);
+	channel_ctx->max_payload_size =
+	    channel_ctx->config->in_band_mailbox_size -
+	    sizeof(struct mod_transport_buffer);
+	break;
 #endif
 
     case MOD_TRANSPORT_CHANNEL_TRANSPORT_TYPE_NONE:
-        /* This channel must be used for sending/receiving events only */
-        channel_ctx->in = NULL;
-        channel_ctx->out = NULL;
-        channel_ctx->max_payload_size = 0;
-        break;
+	/* This channel must be used for sending/receiving events only */
+	channel_ctx->in = NULL;
+	channel_ctx->out = NULL;
+	channel_ctx->max_payload_size = 0;
+	break;
 
 #ifdef BUILD_HAS_FAST_CHANNELS
     case MOD_TRANSPORT_CHANNEL_TRANSPORT_TYPE_FAST_CHANNELS:
-        /* This transport channel is used for Fast channels only */
-        channel_ctx->in = NULL;
-        channel_ctx->out = NULL;
-        channel_ctx->max_payload_size = 0;
-        break;
+	/* This transport channel is used for Fast channels only */
+	channel_ctx->in = NULL;
+	channel_ctx->out = NULL;
+	channel_ctx->max_payload_size = 0;
+	break;
 #endif
 
     default:
-        return FWK_E_DATA;
+	return FWK_E_DATA;
     }
 
     return FWK_SUCCESS;
@@ -820,60 +820,60 @@ static int transport_bind(fwk_id_t id, u
 
     /* bind to driver API */
     if (round == 0) {
-        if (fwk_id_is_type(id, FWK_ID_TYPE_MODULE)) {
-            return FWK_SUCCESS;
-        }
-        channel_ctx =
-            &transport_ctx.channel_ctx_table[fwk_id_get_element_idx(id)];
-        status = fwk_module_bind(
-            channel_ctx->config->driver_id,
-            channel_ctx->config->driver_api_id,
-            &channel_ctx->driver_api);
-        if (status != FWK_SUCCESS) {
-            return status;
-        }
+	if (fwk_id_is_type(id, FWK_ID_TYPE_MODULE)) {
+	    return FWK_SUCCESS;
+	}
+	channel_ctx =
+	    &transport_ctx.channel_ctx_table[fwk_id_get_element_idx(id)];
+	status = fwk_module_bind(
+	    channel_ctx->config->driver_id,
+	    channel_ctx->config->driver_api_id,
+	    &channel_ctx->driver_api);
+	if (status != FWK_SUCCESS) {
+	    return status;
+	}
     }
 
     /* bind to module signal API */
     if (round == 1) {
-        if (!fwk_id_is_type(id, FWK_ID_TYPE_ELEMENT)) {
-            return FWK_SUCCESS;
-        }
+	if (!fwk_id_is_type(id, FWK_ID_TYPE_ELEMENT)) {
+	    return FWK_SUCCESS;
+	}
 
-        channel_ctx =
-            &transport_ctx.channel_ctx_table[fwk_id_get_element_idx(id)];
+	channel_ctx =
+	    &transport_ctx.channel_ctx_table[fwk_id_get_element_idx(id)];
 
 #ifdef BUILD_HAS_FAST_CHANNELS
-        if (channel_ctx->config->transport_type ==
-            MOD_TRANSPORT_CHANNEL_TRANSPORT_TYPE_FAST_CHANNELS) {
-            /*
-             * Don't bind to signal API for fast channels since the
-             * driver module will notify the client module directly.
-             */
-            return FWK_SUCCESS;
-        }
+	if (channel_ctx->config->transport_type ==
+	    MOD_TRANSPORT_CHANNEL_TRANSPORT_TYPE_FAST_CHANNELS) {
+	    /*
+	     * Don't bind to signal API for fast channels since the
+	     * driver module will notify the client module directly.
+	     */
+	    return FWK_SUCCESS;
+	}
 #endif
 
 #ifdef BUILD_HAS_MOD_SCMI
-        if (fwk_id_is_equal(
-                fwk_id_build_module_id(channel_ctx->service_id),
-                fwk_module_id_scmi)) {
-            status = fwk_module_bind(
-                channel_ctx->service_id,
-                FWK_ID_API(FWK_MODULE_IDX_SCMI, MOD_SCMI_API_IDX_TRANSPORT),
-                &channel_ctx->transport_signal.scmi_signal_api);
-
-            channel_ctx->is_scmi = true;
-            return status;
-        }
-#endif
-        status = fwk_module_bind(
-            channel_ctx->service_id,
-            channel_ctx->config->signal_api_id,
-            &channel_ctx->transport_signal.firmware_signal_api);
+	if (fwk_id_is_equal(
+		fwk_id_build_module_id(channel_ctx->service_id),
+		fwk_module_id_scmi)) {
+	    status = fwk_module_bind(
+		channel_ctx->service_id,
+		FWK_ID_API(FWK_MODULE_IDX_SCMI, MOD_SCMI_API_IDX_TRANSPORT),
+		&channel_ctx->transport_signal.scmi_signal_api);
+
+	    channel_ctx->is_scmi = true;
+	    return status;
+	}
+#endif
+	status = fwk_module_bind(
+	    channel_ctx->service_id,
+	    channel_ctx->config->signal_api_id,
+	    &channel_ctx->transport_signal.firmware_signal_api);
 
-        channel_ctx->is_scmi = false;
-        return status;
+	channel_ctx->is_scmi = false;
+	return status;
     }
 
     return FWK_SUCCESS;
@@ -891,67 +891,67 @@ static int transport_process_bind_reques
 
     /* Only allow binding to a channel (not the whole module) */
     if (!fwk_id_is_type(target_id, FWK_ID_TYPE_ELEMENT)) {
-        /* Tried to bind to something other than a specific channel */
-        fwk_unexpected();
-        return FWK_E_PARAM;
+	/* Tried to bind to something other than a specific channel */
+	fwk_unexpected();
+	return FWK_E_PARAM;
     }
 
     channel_ctx =
-        &transport_ctx.channel_ctx_table[fwk_id_get_element_idx(target_id)];
+	&transport_ctx.channel_ctx_table[fwk_id_get_element_idx(target_id)];
 
     api_id_type = (enum mod_transport_api_idx)fwk_id_get_api_idx(api_id);
 
     switch (api_id_type) {
     case MOD_TRANSPORT_API_IDX_DRIVER_INPUT:
-        /* Driver input API */
+	/* Driver input API */
 
-        /*
-         * Make sure that the element that is trying to bind to us is the
-         * same element that we previously bound to.
-         *
-         * NOTE: We bound to an element but a sub-element should be binding
-         * back to us. This means we cannot use fwk_id_is_equal() because
-         * the ids have different types. For now we compare the indicies
-         * manually.
-         */
-        if (fwk_id_get_module_idx(channel_ctx->config->driver_id) ==
-                fwk_id_get_module_idx(source_id) &&
-            fwk_id_get_element_idx(channel_ctx->config->driver_id) ==
-                fwk_id_get_element_idx(source_id)) {
-            *api = &driver_input_api;
-        } else {
-            /* A module that we did not bind to is trying to bind to us */
-            fwk_unexpected();
-            return FWK_E_ACCESS;
-        }
-        break;
+	/*
+	 * Make sure that the element that is trying to bind to us is the
+	 * same element that we previously bound to.
+	 *
+	 * NOTE: We bound to an element but a sub-element should be binding
+	 * back to us. This means we cannot use fwk_id_is_equal() because
+	 * the ids have different types. For now we compare the indicies
+	 * manually.
+	 */
+	if (fwk_id_get_module_idx(channel_ctx->config->driver_id) ==
+		fwk_id_get_module_idx(source_id) &&
+	    fwk_id_get_element_idx(channel_ctx->config->driver_id) ==
+		fwk_id_get_element_idx(source_id)) {
+	    *api = &driver_input_api;
+	} else {
+	    /* A module that we did not bind to is trying to bind to us */
+	    fwk_unexpected();
+	    return FWK_E_ACCESS;
+	}
+	break;
 
 #ifdef BUILD_HAS_MOD_SCMI
     case MOD_TRANSPORT_API_IDX_SCMI_TO_TRANSPORT:
-        /* SCMI transport API */
-        *api = &transport_mod_scmi_to_transport_api;
-        channel_ctx->service_id = source_id;
-        break;
+	/* SCMI transport API */
+	*api = &transport_mod_scmi_to_transport_api;
+	channel_ctx->service_id = source_id;
+	break;
 #endif
 
     case MOD_TRANSPORT_API_IDX_FIRMWARE:
-        /* transport API for Firmware messages */
-        *api = &transport_firmware_api;
-        channel_ctx->service_id = source_id;
-        break;
+	/* transport API for Firmware messages */
+	*api = &transport_firmware_api;
+	channel_ctx->service_id = source_id;
+	break;
 
 #ifdef BUILD_HAS_FAST_CHANNELS
     case MOD_TRANSPORT_API_IDX_FAST_CHANNELS:
-        /* Fast Channels transport API */
-        *api = &transport_fast_channels_api;
-        channel_ctx->service_id = source_id;
-        break;
+	/* Fast Channels transport API */
+	*api = &transport_fast_channels_api;
+	channel_ctx->service_id = source_id;
+	break;
 #endif
 
     default:
-        /* Invalid API */
-        fwk_unexpected();
-        return FWK_E_PARAM;
+	/* Invalid API */
+	fwk_unexpected();
+	return FWK_E_PARAM;
     }
 
     return FWK_SUCCESS;
@@ -963,25 +963,25 @@ static int transport_start(fwk_id_t id)
     struct transport_channel_ctx *channel_ctx;
 
     if (!fwk_id_is_type(id, FWK_ID_TYPE_ELEMENT)) {
-        return status;
+	return status;
     }
 
     channel_ctx = &transport_ctx.channel_ctx_table[fwk_id_get_element_idx(id)];
 
     if (channel_ctx->config->transport_type ==
-        MOD_TRANSPORT_CHANNEL_TRANSPORT_TYPE_OUT_BAND) {
+	MOD_TRANSPORT_CHANNEL_TRANSPORT_TYPE_OUT_BAND) {
 #ifdef BUILD_HAS_MOD_POWER_DOMAIN
-        /* Register for power domain state transition notifications */
-        return fwk_notification_subscribe(
-            mod_pd_notification_id_power_state_transition,
-            channel_ctx->config->pd_source_id,
-            id);
+	/* Register for power domain state transition notifications */
+	return fwk_notification_subscribe(
+	    mod_pd_notification_id_power_state_transition,
+	    channel_ctx->config->pd_source_id,
+	    id);
 #else
-        /*
-         * Initialize the mailbox immediately, if power domain module
-         * is not included in the firmware build.
-         */
-        return transport_mailbox_init(channel_ctx);
+	/*
+	 * Initialize the mailbox immediately, if power domain module
+	 * is not included in the firmware build.
+	 */
+	return transport_mailbox_init(channel_ctx);
 #endif
     }
     return status;
@@ -996,24 +996,24 @@ static int transport_process_notificatio
     int status = FWK_SUCCESS;
 
     channel_ctx =
-        &transport_ctx
-             .channel_ctx_table[fwk_id_get_element_idx(event->target_id)];
+	&transport_ctx
+	     .channel_ctx_table[fwk_id_get_element_idx(event->target_id)];
 
     struct mod_pd_power_state_transition_notification_params *params;
 
     assert(fwk_id_is_equal(
-        event->id, mod_pd_notification_id_power_state_transition));
+	event->id, mod_pd_notification_id_power_state_transition));
     fwk_assert(fwk_id_is_type(event->target_id, FWK_ID_TYPE_ELEMENT));
 
     params = (struct mod_pd_power_state_transition_notification_params *)
-                 event->params;
+		 event->params;
 
     if (params->state != MOD_PD_STATE_ON) {
-        if (params->state == MOD_PD_STATE_OFF) {
-            channel_ctx->out_band_mailbox_ready = false;
-        }
+	if (params->state == MOD_PD_STATE_OFF) {
+	    channel_ctx->out_band_mailbox_ready = false;
+	}
     } else {
-        status = transport_mailbox_init(channel_ctx);
+	status = transport_mailbox_init(channel_ctx);
     }
     return status;
 }
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/ut/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/ut/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/ut/CMakeLists.txt
@@ -13,10 +13,10 @@ set(CMOCK_SRC_ROOT ${CONTRIB_ROOT}/cmock
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_ut.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_ut.c")
 
 set(UNIT_TEST_TARGET ${SCP_MODULE_TARGET})
 add_subdirectory(${CMAKE_SOURCE_DIR}/module/scmi/test
@@ -26,9 +26,9 @@ set(UNIT_TEST_ROOT ${CMAKE_SOURCE_DIR}/u
 
 target_include_directories(
     ${UNIT_TEST_TARGET} PRIVATE ${UNIT_TEST_ROOT}/unity_mocks ${CMOCK_SRC_ROOT}
-                                ${UNITY_SRC_ROOT})
+				${UNITY_SRC_ROOT})
 target_compile_definitions(${UNIT_TEST_TARGET} PRIVATE -DTEST_TARGET=1)
 target_sources(
     ${UNIT_TEST_TARGET}
     PRIVATE ${UNITY_SRC_ROOT}/unity.c ${CMOCK_SRC_ROOT}/cmock.c
-            ${UNIT_TEST_ROOT}/unity_mocks/scp_unity.c)
+	    ${UNIT_TEST_ROOT}/unity_mocks/scp_unity.c)
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/voltage_domain/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/voltage_domain/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/voltage_domain/CMakeLists.txt
@@ -8,7 +8,7 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include")
 
 target_sources(${SCP_MODULE_TARGET}
-               PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_voltage_domain.c")
+	       PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_voltage_domain.c")
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/voltage_domain/include/mod_voltage_domain.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/voltage_domain/include/mod_voltage_domain.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/voltage_domain/include/mod_voltage_domain.h
@@ -215,9 +215,9 @@ struct mod_voltd_drv_api {
      * \return One of the standard framework error codes.
      */
      int (*get_level_from_index)(
-         fwk_id_t elt_id,
-         unsigned int index,
-         int32_t *level_uv);
+	 fwk_id_t elt_id,
+	 unsigned int index,
+	 int32_t *level_uv);
 };
 
 /*!
@@ -327,9 +327,9 @@ struct mod_voltd_api {
      * \return One of the standard framework error codes.
      */
     int (*get_level_from_index)(
-        fwk_id_t voltd_id,
-        unsigned int index,
-        int32_t *level_uv);
+	fwk_id_t voltd_id,
+	unsigned int index,
+	int32_t *level_uv);
 };
 
 /*!
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/voltage_domain/src/mod_voltage_domain.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/module/voltage_domain/src/mod_voltage_domain.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/module/voltage_domain/src/mod_voltage_domain.c
@@ -57,7 +57,7 @@ static int voltd_set_level(fwk_id_t volt
     get_ctx(voltd_id, &ctx);
 
     if (!ctx->api->set_level)
-        return FWK_E_SUPPORT;
+	return FWK_E_SUPPORT;
 
     return ctx->api->set_level(ctx->config->driver_id, level_uv);
 }
@@ -69,10 +69,10 @@ static int voltd_get_level(fwk_id_t volt
     get_ctx(voltd_id, &ctx);
 
     if (level_uv == NULL)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     if (!ctx->api->get_level)
-        return FWK_E_SUPPORT;
+	return FWK_E_SUPPORT;
 
     return ctx->api->get_level(ctx->config->driver_id, level_uv);
 }
@@ -87,7 +87,7 @@ static int voltd_set_config(
     get_ctx(voltd_id, &ctx);
 
     if (!ctx->api->set_config)
-        return FWK_E_SUPPORT;
+	return FWK_E_SUPPORT;
 
     return ctx->api->set_config(ctx->config->driver_id, mode_type, mode_id);
 }
@@ -102,10 +102,10 @@ static int voltd_get_config(
     get_ctx(voltd_id, &ctx);
 
     if (mode_id == NULL)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     if (!ctx->api->get_config)
-        return FWK_E_SUPPORT;
+	return FWK_E_SUPPORT;
 
     return ctx->api->get_config(ctx->config->driver_id, mode_type, mode_id);
 }
@@ -122,7 +122,7 @@ static int voltd_get_info(fwk_id_t voltd
     status = ctx->api->get_info(ctx->config->driver_id, info);
 
     if (status == FWK_SUCCESS && info->name == NULL)
-        info->name = fwk_module_get_element_name(voltd_id);
+	info->name = fwk_module_get_element_name(voltd_id);
 
     return status;
 }
@@ -137,10 +137,10 @@ static int voltd_get_level_from_index(
     get_ctx(dev_id, &ctx);
 
     if (level_uv == NULL)
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     return ctx->api->get_level_from_index(ctx->config->driver_id, index,
-                                          level_uv);
+					  level_uv);
 }
 
 static const struct mod_voltd_api voltd_api = {
@@ -157,18 +157,18 @@ static const struct mod_voltd_api voltd_
  */
 
 static int voltd_init(fwk_id_t module_id, unsigned int element_count,
-                      const void *data)
+		      const void *data)
 {
     if (element_count == 0)
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 
     module_ctx.dev_ctx_table = fwk_mm_calloc(element_count,
-                                             sizeof(struct voltd_dev_ctx));
+					     sizeof(struct voltd_dev_ctx));
     return FWK_SUCCESS;
 }
 
 static int voltd_dev_init(fwk_id_t element_id, unsigned int sub_element_count,
-                          const void *data)
+			  const void *data)
 {
     struct voltd_dev_ctx *ctx;
     const struct mod_voltd_dev_config *dev_config = data;
@@ -184,31 +184,31 @@ static int voltd_bind(fwk_id_t id, unsig
     struct voltd_dev_ctx *ctx;
 
     if (round == 1)
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
 
     if (!fwk_id_is_type(id, FWK_ID_TYPE_ELEMENT)) {
-        /* Only element binding is supported */
-        return FWK_SUCCESS;
+	/* Only element binding is supported */
+	return FWK_SUCCESS;
     }
 
     ctx = &module_ctx.dev_ctx_table[fwk_id_get_element_idx(id)];
 
     return fwk_module_bind(ctx->config->driver_id,
-                           ctx->config->api_id,
-                           &ctx->api);
+			   ctx->config->api_id,
+			   &ctx->api);
 }
 
 static int voltd_process_bind_request(fwk_id_t source_id, fwk_id_t target_id,
-                                      fwk_id_t api_id, const void **api)
+				      fwk_id_t api_id, const void **api)
 {
     enum mod_voltd_api_type api_type = fwk_id_get_api_idx(api_id);
 
     switch (api_type) {
     case MOD_VOLTD_API_TYPE_HAL:
-        *api = &voltd_api;
-        break;
+	*api = &voltd_api;
+	break;
     default:
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
     }
 
     return FWK_SUCCESS;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-fvp/fw/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/product/optee-fvp/fw/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-fvp/fw/CMakeLists.txt
@@ -16,21 +16,21 @@ add_library(${SCP_FIRMWARE_TARGET})
 target_include_directories(
     ${SCP_FIRMWARE_TARGET}
     PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/../include"
-           "${CMAKE_CURRENT_SOURCE_DIR}")
+	   "${CMAKE_CURRENT_SOURCE_DIR}")
 
 target_sources(
     ${SCP_FIRMWARE_TARGET}
     PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/config_mbx_smt.c"
-            "${CMAKE_CURRENT_SOURCE_DIR}/config_scmi.c"
-            "${CMAKE_CURRENT_SOURCE_DIR}/config_mock_clock.c"
-            "${CMAKE_CURRENT_SOURCE_DIR}/config_vpll.c"
-            "${CMAKE_CURRENT_SOURCE_DIR}/config_clock.c"
-            "${CMAKE_CURRENT_SOURCE_DIR}/config_scmi_clock.c"
-            "${CMAKE_CURRENT_SOURCE_DIR}/config_mock_ppu.c"
-            "${CMAKE_CURRENT_SOURCE_DIR}/config_power_domain.c"
-            "${CMAKE_CURRENT_SOURCE_DIR}/config_scmi_power_domain.c"
-            "${CMAKE_CURRENT_SOURCE_DIR}/config_mock_psu.c"
-            "${CMAKE_CURRENT_SOURCE_DIR}/config_psu.c"
-            "${CMAKE_CURRENT_SOURCE_DIR}/config_dvfs.c"
-            "${CMAKE_CURRENT_SOURCE_DIR}/config_scmi_perf.c"
-            "${CMAKE_CURRENT_SOURCE_DIR}/config_sensor.c")
+	    "${CMAKE_CURRENT_SOURCE_DIR}/config_scmi.c"
+	    "${CMAKE_CURRENT_SOURCE_DIR}/config_mock_clock.c"
+	    "${CMAKE_CURRENT_SOURCE_DIR}/config_vpll.c"
+	    "${CMAKE_CURRENT_SOURCE_DIR}/config_clock.c"
+	    "${CMAKE_CURRENT_SOURCE_DIR}/config_scmi_clock.c"
+	    "${CMAKE_CURRENT_SOURCE_DIR}/config_mock_ppu.c"
+	    "${CMAKE_CURRENT_SOURCE_DIR}/config_power_domain.c"
+	    "${CMAKE_CURRENT_SOURCE_DIR}/config_scmi_power_domain.c"
+	    "${CMAKE_CURRENT_SOURCE_DIR}/config_mock_psu.c"
+	    "${CMAKE_CURRENT_SOURCE_DIR}/config_psu.c"
+	    "${CMAKE_CURRENT_SOURCE_DIR}/config_dvfs.c"
+	    "${CMAKE_CURRENT_SOURCE_DIR}/config_scmi_perf.c"
+	    "${CMAKE_CURRENT_SOURCE_DIR}/config_sensor.c")
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-fvp/fw/config_clock.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/product/optee-fvp/fw/config_clock.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-fvp/fw/config_clock.c
@@ -14,92 +14,92 @@
 
 static struct fwk_element clock_dev_desc_table[] = {
     [CLOCK_DEV_IDX_BIG] = {
-        .name = "CPU_GROUP_BIG",
-        .data = &((struct mod_clock_dev_config) {
-            .driver_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_SYSTEM_PLL, 0),
-            .api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_SYSTEM_PLL,
-                                       MOD_SYSTEM_PLL_API_TYPE_DEFAULT),
-        }),
+	.name = "CPU_GROUP_BIG",
+	.data = &((struct mod_clock_dev_config) {
+	    .driver_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_SYSTEM_PLL, 0),
+	    .api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_SYSTEM_PLL,
+				       MOD_SYSTEM_PLL_API_TYPE_DEFAULT),
+	}),
     },
     [CLOCK_DEV_IDX_LITTLE] = {
-        .name = "CPU_GROUP_LITTLE",
-        .data = &((struct mod_clock_dev_config) {
-            .driver_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_SYSTEM_PLL, 1),
-            .api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_SYSTEM_PLL,
-                                       MOD_SYSTEM_PLL_API_TYPE_DEFAULT),
-        }),
+	.name = "CPU_GROUP_LITTLE",
+	.data = &((struct mod_clock_dev_config) {
+	    .driver_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_SYSTEM_PLL, 1),
+	    .api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_SYSTEM_PLL,
+				       MOD_SYSTEM_PLL_API_TYPE_DEFAULT),
+	}),
     },
     [CLOCK_DEV_IDX_GPU] = {
-        .name = "GPU",
-        .data = &((struct mod_clock_dev_config) {
-            .driver_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_SYSTEM_PLL, 2),
-            .api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_SYSTEM_PLL,
-                                       MOD_SYSTEM_PLL_API_TYPE_DEFAULT),
-        }),
+	.name = "GPU",
+	.data = &((struct mod_clock_dev_config) {
+	    .driver_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_SYSTEM_PLL, 2),
+	    .api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_SYSTEM_PLL,
+				       MOD_SYSTEM_PLL_API_TYPE_DEFAULT),
+	}),
     },
     [CLOCK_DEV_IDX_VPU] = {
-        .name = "VPU",
-        .data = &((struct mod_clock_dev_config) {
-            .driver_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_SYSTEM_PLL, 3),
-            .api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_SYSTEM_PLL,
-                                       MOD_SYSTEM_PLL_API_TYPE_DEFAULT),
-        }),
+	.name = "VPU",
+	.data = &((struct mod_clock_dev_config) {
+	    .driver_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_SYSTEM_PLL, 3),
+	    .api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_SYSTEM_PLL,
+				       MOD_SYSTEM_PLL_API_TYPE_DEFAULT),
+	}),
     },
     [CLOCK_DEV_IDX_DPU] = {
-        .name = "DPU",
-        .data = &((struct mod_clock_dev_config) {
-            .driver_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_SYSTEM_PLL, 4),
-            .api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_SYSTEM_PLL,
-                                       MOD_SYSTEM_PLL_API_TYPE_DEFAULT),
-        }),
+	.name = "DPU",
+	.data = &((struct mod_clock_dev_config) {
+	    .driver_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_SYSTEM_PLL, 4),
+	    .api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_SYSTEM_PLL,
+				       MOD_SYSTEM_PLL_API_TYPE_DEFAULT),
+	}),
     },
     [CLOCK_DEV_IDX_PIXEL_0] = {
-        .name = "PIXEL_0",
-        .data = &((struct mod_clock_dev_config) {
-            .driver_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_SYSTEM_PLL, 5),
-            .api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_SYSTEM_PLL,
-                                       MOD_SYSTEM_PLL_API_TYPE_DEFAULT),
-        }),
+	.name = "PIXEL_0",
+	.data = &((struct mod_clock_dev_config) {
+	    .driver_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_SYSTEM_PLL, 5),
+	    .api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_SYSTEM_PLL,
+				       MOD_SYSTEM_PLL_API_TYPE_DEFAULT),
+	}),
     },
     [CLOCK_DEV_IDX_PIXEL_1] = {
-        .name = "PIXEL_1",
-        .data = &((struct mod_clock_dev_config) {
-            .driver_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_SYSTEM_PLL, 6),
-            .api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_SYSTEM_PLL,
-                                       MOD_SYSTEM_PLL_API_TYPE_DEFAULT),
-        }),
+	.name = "PIXEL_1",
+	.data = &((struct mod_clock_dev_config) {
+	    .driver_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_SYSTEM_PLL, 6),
+	    .api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_SYSTEM_PLL,
+				       MOD_SYSTEM_PLL_API_TYPE_DEFAULT),
+	}),
     },
     [CLOCK_DEV_IDX_MOCK_0] = {
-        .name = "CLOCK_0",
-        .data = &((struct mod_clock_dev_config) {
-            .driver_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_MOCK_CLOCK, 0),
-            .api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_MOCK_CLOCK,
-                                       MOD_MOCK_CLOCK_API_TYPE_DRIVER),
-        }),
+	.name = "CLOCK_0",
+	.data = &((struct mod_clock_dev_config) {
+	    .driver_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_MOCK_CLOCK, 0),
+	    .api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_MOCK_CLOCK,
+				       MOD_MOCK_CLOCK_API_TYPE_DRIVER),
+	}),
     },
     [CLOCK_DEV_IDX_MOCK_1] = {
-        .name = "CLOCK_1",
-        .data = &((struct mod_clock_dev_config) {
-            .driver_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_MOCK_CLOCK, 1),
-            .api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_MOCK_CLOCK,
-                                       MOD_MOCK_CLOCK_API_TYPE_DRIVER),
-        }),
+	.name = "CLOCK_1",
+	.data = &((struct mod_clock_dev_config) {
+	    .driver_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_MOCK_CLOCK, 1),
+	    .api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_MOCK_CLOCK,
+				       MOD_MOCK_CLOCK_API_TYPE_DRIVER),
+	}),
     },
     [CLOCK_DEV_IDX_MOCK_2] = {
-        .name = "CLOCK_2",
-        .data = &((struct mod_clock_dev_config) {
-            .driver_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_MOCK_CLOCK, 2),
-            .api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_MOCK_CLOCK,
-                                       MOD_MOCK_CLOCK_API_TYPE_DRIVER),
-        }),
+	.name = "CLOCK_2",
+	.data = &((struct mod_clock_dev_config) {
+	    .driver_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_MOCK_CLOCK, 2),
+	    .api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_MOCK_CLOCK,
+				       MOD_MOCK_CLOCK_API_TYPE_DRIVER),
+	}),
     },
     [CLOCK_DEV_IDX_MOCK_3] = {
-        .name = "CLOCK_3",
-        .data = &((struct mod_clock_dev_config) {
-            .driver_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_MOCK_CLOCK, 3),
-            .api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_MOCK_CLOCK,
-                                       MOD_MOCK_CLOCK_API_TYPE_DRIVER),
-        }),
+	.name = "CLOCK_3",
+	.data = &((struct mod_clock_dev_config) {
+	    .driver_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_MOCK_CLOCK, 3),
+	    .api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_MOCK_CLOCK,
+				       MOD_MOCK_CLOCK_API_TYPE_DRIVER),
+	}),
     },
    [CLOCK_DEV_IDX_COUNT] = { 0 }, /* Termination description. */
 };
@@ -111,17 +111,17 @@ static const struct fwk_element *clock_g
 
     /* Configure all clocks to respond to changes in SYSTOP power state */
     for (i = 0; i < CLOCK_DEV_IDX_COUNT; i++) {
-        dev_config =
-            (struct mod_clock_dev_config *)clock_dev_desc_table[i].data;
-        dev_config->pd_source_id = FWK_ID_NONE;
+	dev_config =
+	    (struct mod_clock_dev_config *)clock_dev_desc_table[i].data;
+	dev_config->pd_source_id = FWK_ID_NONE;
     }
     return clock_dev_desc_table;
 }
 
 struct fwk_module_config config_clock = {
     .data = &((struct mod_clock_config){
-        .pd_transition_notification_id = FWK_ID_NONE_INIT,
-        .pd_pre_transition_notification_id = FWK_ID_NONE_INIT,
+	.pd_transition_notification_id = FWK_ID_NONE_INIT,
+	.pd_pre_transition_notification_id = FWK_ID_NONE_INIT,
     }),
     .elements = FWK_MODULE_DYNAMIC_ELEMENTS(clock_get_dev_desc_table),
 };
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-fvp/fw/config_dvfs.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/product/optee-fvp/fw/config_dvfs.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-fvp/fw/config_dvfs.c
@@ -17,37 +17,37 @@ static const struct mod_dvfs_domain_conf
     .latency = 1200,
     .sustained_idx = 2,
     .opps = (struct mod_dvfs_opp[]) {
-        {
-            .level = 665 * 1000UL,
-            .frequency = 665 * 1000UL,
-            .voltage = 100,
-            .power = 665*100*100/10000,
-        },
-        {
-            .level = 998 * 1000UL,
-            .frequency = 998 * 1000UL,
-            .voltage = 110,
-            .power = 998*110*110/10000,
-        },
-        {
-            .level = 1330 * 1000UL,
-            .frequency = 1330 * 1000UL,
-            .voltage = 120,
-            .power = 1330*120*120/10000,
-        },
-        {
-            .level = 1463 * 1000UL,
-            .frequency = 1463 * 1000UL,
-            .voltage = 130,
-            .power = 1463*130*130/10000,
-        },
-        {
-            .level = 1596 * 1000UL,
-            .frequency = 1596 * 1000UL,
-            .voltage = 140,
-            .power = 1596*140*140/10000,
-        },
-        { 0 }
+	{
+	    .level = 665 * 1000UL,
+	    .frequency = 665 * 1000UL,
+	    .voltage = 100,
+	    .power = 665*100*100/10000,
+	},
+	{
+	    .level = 998 * 1000UL,
+	    .frequency = 998 * 1000UL,
+	    .voltage = 110,
+	    .power = 998*110*110/10000,
+	},
+	{
+	    .level = 1330 * 1000UL,
+	    .frequency = 1330 * 1000UL,
+	    .voltage = 120,
+	    .power = 1330*120*120/10000,
+	},
+	{
+	    .level = 1463 * 1000UL,
+	    .frequency = 1463 * 1000UL,
+	    .voltage = 130,
+	    .power = 1463*130*130/10000,
+	},
+	{
+	    .level = 1596 * 1000UL,
+	    .frequency = 1596 * 1000UL,
+	    .voltage = 140,
+	    .power = 1596*140*140/10000,
+	},
+	{ 0 }
     }
 };
 
@@ -57,37 +57,37 @@ static const struct mod_dvfs_domain_conf
     .latency = 1200,
     .sustained_idx = 2,
     .opps = (struct mod_dvfs_opp[]) {
-        {
-            .level = 1313 * 1000UL,
-            .frequency = 1313 * 1000UL ,
-            .voltage = 100,
-            .power = 1313,
-        },
-        {
-            .level = 1531 * 1000UL,
-            .frequency = 1531 * 1000UL,
-            .voltage = 200,
-            .power = 1531,
-        },
-        {
-            .level = 1750 * 1000UL,
-            .frequency = 1750 * 1000UL,
-            .voltage = 300,
-            .power = 1750,
-        },
-        {
-            .level = 2100 * 1000UL,
-            .frequency = 2100 * 1000UL,
-            .voltage = 400,
-            .power = 2100,
-        },
-        {
-            .level = 2450 * 1000UL,
-            .frequency = 2450 * 1000UL,
-            .voltage = 500,
-            .power = 2450,
-        },
-        { 0 }
+	{
+	    .level = 1313 * 1000UL,
+	    .frequency = 1313 * 1000UL ,
+	    .voltage = 100,
+	    .power = 1313,
+	},
+	{
+	    .level = 1531 * 1000UL,
+	    .frequency = 1531 * 1000UL,
+	    .voltage = 200,
+	    .power = 1531,
+	},
+	{
+	    .level = 1750 * 1000UL,
+	    .frequency = 1750 * 1000UL,
+	    .voltage = 300,
+	    .power = 1750,
+	},
+	{
+	    .level = 2100 * 1000UL,
+	    .frequency = 2100 * 1000UL,
+	    .voltage = 400,
+	    .power = 2100,
+	},
+	{
+	    .level = 2450 * 1000UL,
+	    .frequency = 2450 * 1000UL,
+	    .voltage = 500,
+	    .power = 2450,
+	},
+	{ 0 }
     }
 };
 
@@ -97,47 +97,47 @@ static const struct mod_dvfs_domain_conf
     .latency = 1200,
     .sustained_idx = 4,
     .opps = (struct mod_dvfs_opp[]) {
-        {
-            .level = 450 * 1000UL,
-            .frequency = 450 * 1000UL,
-            .voltage = 100,
-        },
-        {
-            .level = 487500,
-            .frequency = 487500,
-            .voltage = 200,
-        },
-        {
-            .level = 525 * 1000UL,
-            .frequency = 525 * 1000UL,
-            .voltage = 300,
-        },
-        {
-            .level = 562500,
-            .frequency = 562500,
-            .voltage = 400,
-        },
-        {
-            .level = 600 * 1000UL,
-            .frequency = 600 * 1000UL,
-            .voltage = 500,
-        },
-        { 0 }
+	{
+	    .level = 450 * 1000UL,
+	    .frequency = 450 * 1000UL,
+	    .voltage = 100,
+	},
+	{
+	    .level = 487500,
+	    .frequency = 487500,
+	    .voltage = 200,
+	},
+	{
+	    .level = 525 * 1000UL,
+	    .frequency = 525 * 1000UL,
+	    .voltage = 300,
+	},
+	{
+	    .level = 562500,
+	    .frequency = 562500,
+	    .voltage = 400,
+	},
+	{
+	    .level = 600 * 1000UL,
+	    .frequency = 600 * 1000UL,
+	    .voltage = 500,
+	},
+	{ 0 }
     }
 };
 
 static const struct fwk_element element_table[] = {
     [DVFS_ELEMENT_IDX_LITTLE] = {
-        .name = "CPU_GROUP_LITTLE",
-        .data = &cpu_group_little,
+	.name = "CPU_GROUP_LITTLE",
+	.data = &cpu_group_little,
     },
     [DVFS_ELEMENT_IDX_BIG] = {
-        .name = "CPU_GROUP_BIG",
-        .data = &cpu_group_big,
+	.name = "CPU_GROUP_BIG",
+	.data = &cpu_group_big,
     },
     [DVFS_ELEMENT_IDX_GPU] = {
-        .name = "GPU",
-        .data = &gpu,
+	.name = "GPU",
+	.data = &gpu,
     },
     { 0 }
 };
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-fvp/fw/config_mbx_smt.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/product/optee-fvp/fw/config_mbx_smt.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-fvp/fw/config_mbx_smt.c
@@ -14,31 +14,31 @@
 
 static const struct fwk_element mbx_element_table[] = {
     [SCMI_CHANNEL_DEVICE_IDX_PSCI] = {
-        .name = "OP-TEE for PSCI",
+	.name = "OP-TEE for PSCI",
     .data = &((struct mod_optee_mbx_channel_config) {
-            .driver_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_MSG_SMT,
-                                         SCMI_SERVICE_IDX_PSCI),
-            .driver_api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_MSG_SMT,
-                                             MOD_MSG_SMT_API_IDX_DRIVER_INPUT),
-        })
+	    .driver_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_MSG_SMT,
+					 SCMI_SERVICE_IDX_PSCI),
+	    .driver_api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_MSG_SMT,
+					     MOD_MSG_SMT_API_IDX_DRIVER_INPUT),
+	})
     },
     [SCMI_CHANNEL_DEVICE_IDX_OSPM_0] = {
-        .name = "OP-TEE for OSPM #0",
-        .data = &((struct mod_optee_mbx_channel_config) {
-            .driver_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_MSG_SMT,
-                                         SCMI_SERVICE_IDX_OSPM_0),
-            .driver_api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_MSG_SMT,
-                                             MOD_MSG_SMT_API_IDX_DRIVER_INPUT),
-        })
+	.name = "OP-TEE for OSPM #0",
+	.data = &((struct mod_optee_mbx_channel_config) {
+	    .driver_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_MSG_SMT,
+					 SCMI_SERVICE_IDX_OSPM_0),
+	    .driver_api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_MSG_SMT,
+					     MOD_MSG_SMT_API_IDX_DRIVER_INPUT),
+	})
     },
     [SCMI_CHANNEL_DEVICE_IDX_OSPM_1] = {
-        .name = "OP-TEE for OSPM #1",
-        .data = &((struct mod_optee_mbx_channel_config) {
-            .driver_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_MSG_SMT,
-                                         SCMI_SERVICE_IDX_OSPM_1),
-            .driver_api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_MSG_SMT,
-                                             MOD_MSG_SMT_API_IDX_DRIVER_INPUT),
-        })
+	.name = "OP-TEE for OSPM #1",
+	.data = &((struct mod_optee_mbx_channel_config) {
+	    .driver_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_MSG_SMT,
+					 SCMI_SERVICE_IDX_OSPM_1),
+	    .driver_api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_MSG_SMT,
+					     MOD_MSG_SMT_API_IDX_DRIVER_INPUT),
+	})
     },
     [SCMI_CHANNEL_DEVICE_IDX_COUNT] = { 0 },
 };
@@ -54,31 +54,31 @@ struct fwk_module_config config_optee_mb
 
 static struct fwk_element smt_element_table[] = {
     [SCMI_SERVICE_IDX_PSCI] = {
-        .name = "PSCI",
-        .data = &((struct mod_msg_smt_channel_config) {
-            .type = MOD_MSG_SMT_CHANNEL_TYPE_REQUESTER,
-            .driver_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_OPTEE_MBX,
-                                                 SCMI_CHANNEL_DEVICE_IDX_PSCI),
-            .driver_api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_OPTEE_MBX, 0),
-        })
+	.name = "PSCI",
+	.data = &((struct mod_msg_smt_channel_config) {
+	    .type = MOD_MSG_SMT_CHANNEL_TYPE_REQUESTER,
+	    .driver_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_OPTEE_MBX,
+						 SCMI_CHANNEL_DEVICE_IDX_PSCI),
+	    .driver_api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_OPTEE_MBX, 0),
+	})
     },
     [SCMI_SERVICE_IDX_OSPM_0] = {
-        .name = "OSPM0",
-        .data = &((struct mod_msg_smt_channel_config) {
-            .type = MOD_MSG_SMT_CHANNEL_TYPE_REQUESTER,
-            .driver_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_OPTEE_MBX,
-                                                 SCMI_CHANNEL_DEVICE_IDX_OSPM_0),
-            .driver_api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_OPTEE_MBX, 0),
-        })
+	.name = "OSPM0",
+	.data = &((struct mod_msg_smt_channel_config) {
+	    .type = MOD_MSG_SMT_CHANNEL_TYPE_REQUESTER,
+	    .driver_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_OPTEE_MBX,
+						 SCMI_CHANNEL_DEVICE_IDX_OSPM_0),
+	    .driver_api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_OPTEE_MBX, 0),
+	})
     },
     [SCMI_SERVICE_IDX_OSPM_1] = {
-        .name = "OSPM1",
-        .data = &((struct mod_msg_smt_channel_config) {
-            .type = MOD_MSG_SMT_CHANNEL_TYPE_REQUESTER,
-            .driver_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_OPTEE_MBX,
-                                                 SCMI_CHANNEL_DEVICE_IDX_OSPM_1),
-            .driver_api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_OPTEE_MBX, 0),
-        })
+	.name = "OSPM1",
+	.data = &((struct mod_msg_smt_channel_config) {
+	    .type = MOD_MSG_SMT_CHANNEL_TYPE_REQUESTER,
+	    .driver_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_OPTEE_MBX,
+						 SCMI_CHANNEL_DEVICE_IDX_OSPM_1),
+	    .driver_api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_OPTEE_MBX, 0),
+	})
     },
     [SCMI_SERVICE_IDX_COUNT] = { 0 },
 };
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-fvp/fw/config_mock_clock.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/product/optee-fvp/fw/config_mock_clock.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-fvp/fw/config_mock_clock.c
@@ -30,41 +30,41 @@ static const struct mod_mock_clock_rate
 
 static const struct fwk_element element_table[] = {
     [MOD_MOCK_CLOCK_ELEMENT_IDX_0] =
-        {
-            .name = "MOCK_CLK0",
-            .data = &(const struct mod_mock_clock_element_cfg){
-                    .rate_table = clk0_rate_table,
-                    .rate_count = FWK_ARRAY_SIZE(clk0_rate_table),
-                    .default_rate = 1536000,
-                },
-        },
+	{
+	    .name = "MOCK_CLK0",
+	    .data = &(const struct mod_mock_clock_element_cfg){
+		    .rate_table = clk0_rate_table,
+		    .rate_count = FWK_ARRAY_SIZE(clk0_rate_table),
+		    .default_rate = 1536000,
+		},
+	},
     [MOD_MOCK_CLOCK_ELEMENT_IDX_1] =
-        {
-            .name = "MOCK_CLK1",
-            .data = &(const struct mod_mock_clock_element_cfg){
-                    .rate_table = clk1_rate_table,
-                    .rate_count = FWK_ARRAY_SIZE(clk1_rate_table),
-                    .default_rate = 2116800,
-                },
-        },
+	{
+	    .name = "MOCK_CLK1",
+	    .data = &(const struct mod_mock_clock_element_cfg){
+		    .rate_table = clk1_rate_table,
+		    .rate_count = FWK_ARRAY_SIZE(clk1_rate_table),
+		    .default_rate = 2116800,
+		},
+	},
     [MOD_MOCK_CLOCK_ELEMENT_IDX_2] =
-        {
-            .name = "MOCK_CLK2",
-            .data = &(const struct mod_mock_clock_element_cfg){
-                    .rate_table = clk2_rate_table,
-                    .rate_count = FWK_ARRAY_SIZE(clk2_rate_table),
-                    .default_rate = 2304000,
-                },
-        },
+	{
+	    .name = "MOCK_CLK2",
+	    .data = &(const struct mod_mock_clock_element_cfg){
+		    .rate_table = clk2_rate_table,
+		    .rate_count = FWK_ARRAY_SIZE(clk2_rate_table),
+		    .default_rate = 2304000,
+		},
+	},
     [MOD_MOCK_CLOCK_ELEMENT_IDX_3] =
-        {
-            .name = "MOCK_CLK3",
-            .data = &(const struct mod_mock_clock_element_cfg){
-                    .rate_table = clk3_rate_table,
-                    .rate_count = FWK_ARRAY_SIZE(clk3_rate_table),
-                    .default_rate = 4233600,
-                },
-        },
+	{
+	    .name = "MOCK_CLK3",
+	    .data = &(const struct mod_mock_clock_element_cfg){
+		    .rate_table = clk3_rate_table,
+		    .rate_count = FWK_ARRAY_SIZE(clk3_rate_table),
+		    .default_rate = 4233600,
+		},
+	},
     [MOD_MOCK_CLOCK_ELEMENT_IDX_COUNT] = { 0 }, /* Termination description. */
 };
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-fvp/fw/config_mock_ppu.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/product/optee-fvp/fw/config_mock_ppu.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-fvp/fw/config_mock_ppu.c
@@ -21,107 +21,107 @@ uint32_t vppu_regsys[16];
 
 static struct fwk_element host_ppu_v0_element_table[] = {
    [MOCK_PPU_ELEMENT_IDX_CPU0] = {
-        .name = "CPU0",
-        .data = &((struct mod_mock_ppu_pd_config) {
-            .pd_type = MOD_PD_TYPE_DEVICE,
-            .ppu.reg_base = (uintptr_t)vppu_regcluster,
-        }),
+	.name = "CPU0",
+	.data = &((struct mod_mock_ppu_pd_config) {
+	    .pd_type = MOD_PD_TYPE_DEVICE,
+	    .ppu.reg_base = (uintptr_t)vppu_regcluster,
+	}),
     },
    [MOCK_PPU_ELEMENT_IDX_CPU1] = {
-        .name = "CPU1",
-        .data = &((struct mod_mock_ppu_pd_config) {
-            .pd_type = MOD_PD_TYPE_DEVICE,
-            .ppu.reg_base = (uintptr_t)vppu_regcluster,
-        }),
+	.name = "CPU1",
+	.data = &((struct mod_mock_ppu_pd_config) {
+	    .pd_type = MOD_PD_TYPE_DEVICE,
+	    .ppu.reg_base = (uintptr_t)vppu_regcluster,
+	}),
     },
    [MOCK_PPU_ELEMENT_IDX_CPU2] = {
-        .name = "CPU2",
-        .data = &((struct mod_mock_ppu_pd_config) {
-            .pd_type = MOD_PD_TYPE_DEVICE,
-            .ppu.reg_base = (uintptr_t)vppu_regcluster,
-        }),
+	.name = "CPU2",
+	.data = &((struct mod_mock_ppu_pd_config) {
+	    .pd_type = MOD_PD_TYPE_DEVICE,
+	    .ppu.reg_base = (uintptr_t)vppu_regcluster,
+	}),
     },
    [MOCK_PPU_ELEMENT_IDX_CPU3] = {
-        .name = "CPU3",
-        .data = &((struct mod_mock_ppu_pd_config) {
-            .pd_type = MOD_PD_TYPE_DEVICE,
-            .ppu.reg_base = (uintptr_t)vppu_regcluster,
-        }),
+	.name = "CPU3",
+	.data = &((struct mod_mock_ppu_pd_config) {
+	    .pd_type = MOD_PD_TYPE_DEVICE,
+	    .ppu.reg_base = (uintptr_t)vppu_regcluster,
+	}),
     },
    [MOCK_PPU_ELEMENT_IDX_CLUSTER0] = {
-        .name = "CLUSTER0",
-        .data = &((struct mod_mock_ppu_pd_config) {
-            .pd_type = MOD_PD_TYPE_DEVICE,
-            .ppu.reg_base = (uintptr_t)vppu_regcluster,
-        }),
+	.name = "CLUSTER0",
+	.data = &((struct mod_mock_ppu_pd_config) {
+	    .pd_type = MOD_PD_TYPE_DEVICE,
+	    .ppu.reg_base = (uintptr_t)vppu_regcluster,
+	}),
     },
    [MOCK_PPU_ELEMENT_IDX_CLUSTER1] = {
-        .name = "CLUSTER1",
-        .data = &((struct mod_mock_ppu_pd_config) {
-            .pd_type = MOD_PD_TYPE_DEVICE,
-            .ppu.reg_base = (uintptr_t)vppu_regcluster,
-        }),
+	.name = "CLUSTER1",
+	.data = &((struct mod_mock_ppu_pd_config) {
+	    .pd_type = MOD_PD_TYPE_DEVICE,
+	    .ppu.reg_base = (uintptr_t)vppu_regcluster,
+	}),
     },
     [MOCK_PPU_ELEMENT_IDX_DBGTOP] = {
-        .name = "DBGTOP",
-        .data = &((struct mod_mock_ppu_pd_config) {
-            .pd_type = MOD_PD_TYPE_DEVICE_DEBUG,
-            .ppu.reg_base = (uintptr_t)vppu_regdbg,
-        }),
+	.name = "DBGTOP",
+	.data = &((struct mod_mock_ppu_pd_config) {
+	    .pd_type = MOD_PD_TYPE_DEVICE_DEBUG,
+	    .ppu.reg_base = (uintptr_t)vppu_regdbg,
+	}),
     },
     [MOCK_PPU_ELEMENT_IDX_DPU0TOP] = {
-        .name = "DPU0TOP",
-        .data = &((struct mod_mock_ppu_pd_config) {
-            .pd_type = MOD_PD_TYPE_DEVICE,
-            .ppu.reg_base = (uintptr_t)vppu_regdpu0,
-            .default_power_on = true,
-        }),
+	.name = "DPU0TOP",
+	.data = &((struct mod_mock_ppu_pd_config) {
+	    .pd_type = MOD_PD_TYPE_DEVICE,
+	    .ppu.reg_base = (uintptr_t)vppu_regdpu0,
+	    .default_power_on = true,
+	}),
     },
     [MOCK_PPU_ELEMENT_IDX_DPU1TOP] = {
-        .name = "DPU1TOP",
-        .data = &((struct mod_mock_ppu_pd_config) {
-            .pd_type = MOD_PD_TYPE_DEVICE,
-            .ppu.reg_base = (uintptr_t)vppu_regdpu1,
-            .default_power_on = true,
-        }),
+	.name = "DPU1TOP",
+	.data = &((struct mod_mock_ppu_pd_config) {
+	    .pd_type = MOD_PD_TYPE_DEVICE,
+	    .ppu.reg_base = (uintptr_t)vppu_regdpu1,
+	    .default_power_on = true,
+	}),
     },
     [MOCK_PPU_ELEMENT_IDX_GPUTOP] = {
-        .name = "GPUTOP",
-        .data = &((struct mod_mock_ppu_pd_config) {
-            .pd_type = MOD_PD_TYPE_DEVICE,
-            .ppu.reg_base = (uintptr_t)vppu_reggpu,
-            .default_power_on = true,
-        }),
+	.name = "GPUTOP",
+	.data = &((struct mod_mock_ppu_pd_config) {
+	    .pd_type = MOD_PD_TYPE_DEVICE,
+	    .ppu.reg_base = (uintptr_t)vppu_reggpu,
+	    .default_power_on = true,
+	}),
     },
     [MOCK_PPU_ELEMENT_IDX_VPUTOP] = {
-        .name = "VPUTOP",
-        .data = &((struct mod_mock_ppu_pd_config) {
-            .pd_type = MOD_PD_TYPE_DEVICE,
-            .ppu.reg_base = (uintptr_t)vppu_regvpu,
-            .default_power_on = true,
-        }),
+	.name = "VPUTOP",
+	.data = &((struct mod_mock_ppu_pd_config) {
+	    .pd_type = MOD_PD_TYPE_DEVICE,
+	    .ppu.reg_base = (uintptr_t)vppu_regvpu,
+	    .default_power_on = true,
+	}),
     },
     [MOCK_PPU_ELEMENT_IDX_SYS0] = {
-        .name = "SYS0",
-        .data = &((struct mod_mock_ppu_pd_config) {
-            .pd_type = MOD_PD_TYPE_SYSTEM,
-            .ppu.reg_base = (uintptr_t)vppu_regsys0,
-        }),
+	.name = "SYS0",
+	.data = &((struct mod_mock_ppu_pd_config) {
+	    .pd_type = MOD_PD_TYPE_SYSTEM,
+	    .ppu.reg_base = (uintptr_t)vppu_regsys0,
+	}),
     },
     [MOCK_PPU_ELEMENT_IDX_SYS1] = {
-        .name = "SYS1",
-        .data = &((struct mod_mock_ppu_pd_config) {
-            .pd_type = MOD_PD_TYPE_SYSTEM,
-            .ppu.reg_base = (uintptr_t)vppu_regsys1,
-        }),
+	.name = "SYS1",
+	.data = &((struct mod_mock_ppu_pd_config) {
+	    .pd_type = MOD_PD_TYPE_SYSTEM,
+	    .ppu.reg_base = (uintptr_t)vppu_regsys1,
+	}),
     },
    [MOCK_PPU_ELEMENT_IDX_SYSTEM] = {
-        .name = "SYSTEM",
-        .data = &((struct mod_mock_ppu_pd_config) {
-            .pd_type = MOD_PD_TYPE_DEVICE,
-            .ppu.reg_base = (uintptr_t)vppu_regsys,
-            .default_power_on = true,
-        }),
+	.name = "SYSTEM",
+	.data = &((struct mod_mock_ppu_pd_config) {
+	    .pd_type = MOD_PD_TYPE_DEVICE,
+	    .ppu.reg_base = (uintptr_t)vppu_regsys,
+	    .default_power_on = true,
+	}),
     },
     [MOCK_PPU_ELEMENT_IDX_COUNT] = { 0 }, /* Termination entry */
 };
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-fvp/fw/config_mock_psu.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/product/optee-fvp/fw/config_mock_psu.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-fvp/fw/config_mock_psu.c
@@ -11,36 +11,36 @@
 
 static const struct fwk_element element_table[] = {
     [CONFIG_MOCK_PSU_ELEMENT_IDX_CPU_GROUP_LITTLE] = {
-        .name = "CPU_GROUP_LITTLE",
-        .data = &(const struct mod_mock_psu_element_cfg) {
-            .default_enabled = true,
-            .default_voltage = 100,
-            .async_alarm_id =  FWK_ID_NONE_INIT,
-        },
+	.name = "CPU_GROUP_LITTLE",
+	.data = &(const struct mod_mock_psu_element_cfg) {
+	    .default_enabled = true,
+	    .default_voltage = 100,
+	    .async_alarm_id =  FWK_ID_NONE_INIT,
+	},
     },
     [CONFIG_MOCK_PSU_ELEMENT_IDX_CPU_GROUP_BIG] = {
-        .name = "CPU_GROUP_BIG",
-        .data = &(const struct mod_mock_psu_element_cfg) {
-            .default_enabled = true,
-            .default_voltage = 100,
-            .async_alarm_id = FWK_ID_NONE_INIT,
-        },
+	.name = "CPU_GROUP_BIG",
+	.data = &(const struct mod_mock_psu_element_cfg) {
+	    .default_enabled = true,
+	    .default_voltage = 100,
+	    .async_alarm_id = FWK_ID_NONE_INIT,
+	},
     },
     [CONFIG_MOCK_PSU_ELEMENT_IDX_GPU] = {
-        .name = "GPU",
-        .data = &(const struct mod_mock_psu_element_cfg) {
-            .default_enabled = true,
-            .default_voltage = 100,
-            .async_alarm_id = FWK_ID_NONE_INIT,
-        },
+	.name = "GPU",
+	.data = &(const struct mod_mock_psu_element_cfg) {
+	    .default_enabled = true,
+	    .default_voltage = 100,
+	    .async_alarm_id = FWK_ID_NONE_INIT,
+	},
     },
     [CONFIG_MOCK_PSU_ELEMENT_IDX_VPU] = {
-        .name = "VPU",
-        .data = &(const struct mod_mock_psu_element_cfg) {
-            .default_enabled = true,
-            .default_voltage = 100,
-            .async_alarm_id = FWK_ID_NONE_INIT,
-        },
+	.name = "VPU",
+	.data = &(const struct mod_mock_psu_element_cfg) {
+	    .default_enabled = true,
+	    .default_voltage = 100,
+	    .async_alarm_id = FWK_ID_NONE_INIT,
+	},
     },
     { 0 }
 };
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-fvp/fw/config_power_domain.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/product/optee-fvp/fw/config_power_domain.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-fvp/fw/config_power_domain.c
@@ -39,7 +39,7 @@ static const uint32_t core_pd_allowed_st
     [MOD_PD_STATE_OFF] = MOD_PD_STATE_OFF_MASK,
     [MOD_PD_STATE_SLEEP] = MOD_PD_STATE_OFF_MASK | MOD_PD_STATE_SLEEP_MASK,
     [MOD_PD_STATE_ON] = MOD_PD_STATE_OFF_MASK | MOD_PD_STATE_ON_MASK |
-        MOD_PD_STATE_SLEEP_MASK,
+	MOD_PD_STATE_SLEEP_MASK,
 };
 
 /* Power module specific configuration data (none) */
@@ -47,77 +47,77 @@ static const struct mod_power_domain_con
 
 static struct fwk_element fvp_power_domain_static_element_table[] = {
      [CONFIG_POWER_DOMAIN_SYSTOP_CHILD_DBGTOP] = {
-        .name = "DBGTOP",
-        .data = &((struct mod_power_domain_element_config) {
-            .attributes.pd_type = MOD_PD_TYPE_DEVICE,
-            .driver_id = FWK_ID_ELEMENT_INIT(
-                FWK_MODULE_IDX_MOCK_PPU, MOCK_PPU_ELEMENT_IDX_DBGTOP),
-            .api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_MOCK_PPU, 0),
-            .allowed_state_mask_table = toplevel_allowed_state_mask_table,
-            .allowed_state_mask_table_size =
-                FWK_ARRAY_SIZE(toplevel_allowed_state_mask_table)
-        }),
+	.name = "DBGTOP",
+	.data = &((struct mod_power_domain_element_config) {
+	    .attributes.pd_type = MOD_PD_TYPE_DEVICE,
+	    .driver_id = FWK_ID_ELEMENT_INIT(
+		FWK_MODULE_IDX_MOCK_PPU, MOCK_PPU_ELEMENT_IDX_DBGTOP),
+	    .api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_MOCK_PPU, 0),
+	    .allowed_state_mask_table = toplevel_allowed_state_mask_table,
+	    .allowed_state_mask_table_size =
+		FWK_ARRAY_SIZE(toplevel_allowed_state_mask_table)
+	}),
     },
     [CONFIG_POWER_DOMAIN_SYSTOP_CHILD_DPU0TOP] = {
-        .name = "DPU0TOP",
-        .data = &((struct mod_power_domain_element_config) {
-            .attributes.pd_type = MOD_PD_TYPE_DEVICE,
-            .driver_id = FWK_ID_ELEMENT_INIT(
-                FWK_MODULE_IDX_MOCK_PPU, MOCK_PPU_ELEMENT_IDX_DPU0TOP),
-            .api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_MOCK_PPU, 0),
-            .allowed_state_mask_table = toplevel_allowed_state_mask_table,
-            .allowed_state_mask_table_size =
-                FWK_ARRAY_SIZE(toplevel_allowed_state_mask_table)
-        }),
+	.name = "DPU0TOP",
+	.data = &((struct mod_power_domain_element_config) {
+	    .attributes.pd_type = MOD_PD_TYPE_DEVICE,
+	    .driver_id = FWK_ID_ELEMENT_INIT(
+		FWK_MODULE_IDX_MOCK_PPU, MOCK_PPU_ELEMENT_IDX_DPU0TOP),
+	    .api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_MOCK_PPU, 0),
+	    .allowed_state_mask_table = toplevel_allowed_state_mask_table,
+	    .allowed_state_mask_table_size =
+		FWK_ARRAY_SIZE(toplevel_allowed_state_mask_table)
+	}),
     },
     [CONFIG_POWER_DOMAIN_SYSTOP_CHILD_DPU1TOP] = {
-        .name = "DPU1TOP",
-        .data = &((struct mod_power_domain_element_config) {
-            .attributes.pd_type = MOD_PD_TYPE_DEVICE,
-            .driver_id = FWK_ID_ELEMENT_INIT(
-                FWK_MODULE_IDX_MOCK_PPU, MOCK_PPU_ELEMENT_IDX_DPU1TOP),
-            .api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_MOCK_PPU, 0),
-            .allowed_state_mask_table = toplevel_allowed_state_mask_table,
-            .allowed_state_mask_table_size =
-                FWK_ARRAY_SIZE(toplevel_allowed_state_mask_table)
-        }),
+	.name = "DPU1TOP",
+	.data = &((struct mod_power_domain_element_config) {
+	    .attributes.pd_type = MOD_PD_TYPE_DEVICE,
+	    .driver_id = FWK_ID_ELEMENT_INIT(
+		FWK_MODULE_IDX_MOCK_PPU, MOCK_PPU_ELEMENT_IDX_DPU1TOP),
+	    .api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_MOCK_PPU, 0),
+	    .allowed_state_mask_table = toplevel_allowed_state_mask_table,
+	    .allowed_state_mask_table_size =
+		FWK_ARRAY_SIZE(toplevel_allowed_state_mask_table)
+	}),
     },
     [CONFIG_POWER_DOMAIN_SYSTOP_CHILD_GPUTOP] = {
-        .name = "GPUTOP",
-        .data = &((struct mod_power_domain_element_config) {
-            .attributes.pd_type = MOD_PD_TYPE_DEVICE,
-            .driver_id = FWK_ID_ELEMENT_INIT(
-                FWK_MODULE_IDX_MOCK_PPU, MOCK_PPU_ELEMENT_IDX_GPUTOP),
-            .api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_MOCK_PPU, 0),
-            .allowed_state_mask_table = toplevel_allowed_state_mask_table,
-            .allowed_state_mask_table_size =
-                FWK_ARRAY_SIZE(toplevel_allowed_state_mask_table)
-        }),
+	.name = "GPUTOP",
+	.data = &((struct mod_power_domain_element_config) {
+	    .attributes.pd_type = MOD_PD_TYPE_DEVICE,
+	    .driver_id = FWK_ID_ELEMENT_INIT(
+		FWK_MODULE_IDX_MOCK_PPU, MOCK_PPU_ELEMENT_IDX_GPUTOP),
+	    .api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_MOCK_PPU, 0),
+	    .allowed_state_mask_table = toplevel_allowed_state_mask_table,
+	    .allowed_state_mask_table_size =
+		FWK_ARRAY_SIZE(toplevel_allowed_state_mask_table)
+	}),
     },
     [CONFIG_POWER_DOMAIN_SYSTOP_CHILD_VPUTOP] = {
-        .name = "VPUTOP",
-        .data = &((struct mod_power_domain_element_config) {
-            .attributes.pd_type = MOD_PD_TYPE_DEVICE,
-            .driver_id = FWK_ID_ELEMENT_INIT(
-                FWK_MODULE_IDX_MOCK_PPU, MOCK_PPU_ELEMENT_IDX_VPUTOP),
-            .api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_MOCK_PPU, 0),
-            .allowed_state_mask_table = toplevel_allowed_state_mask_table,
-            .allowed_state_mask_table_size =
-                FWK_ARRAY_SIZE(toplevel_allowed_state_mask_table)
-        }),
+	.name = "VPUTOP",
+	.data = &((struct mod_power_domain_element_config) {
+	    .attributes.pd_type = MOD_PD_TYPE_DEVICE,
+	    .driver_id = FWK_ID_ELEMENT_INIT(
+		FWK_MODULE_IDX_MOCK_PPU, MOCK_PPU_ELEMENT_IDX_VPUTOP),
+	    .api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_MOCK_PPU, 0),
+	    .allowed_state_mask_table = toplevel_allowed_state_mask_table,
+	    .allowed_state_mask_table_size =
+		FWK_ARRAY_SIZE(toplevel_allowed_state_mask_table)
+	}),
     },
     [CONFIG_POWER_DOMAIN_SYSTOP_CHILD_COUNT] = {
-        .name = "SYSTOP",
-        .data = &((struct mod_power_domain_element_config) {
-            .parent_idx = UINT32_MAX,
-            .attributes.pd_type = MOD_PD_TYPE_DEVICE,
-            .driver_id = FWK_ID_ELEMENT_INIT(
-                FWK_MODULE_IDX_MOCK_PPU, MOCK_PPU_ELEMENT_IDX_SYSTEM),
-            .api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_MOCK_PPU, 0),
-            .allowed_state_mask_table = toplevel_allowed_state_mask_table,
-            .allowed_state_mask_table_size =
-                FWK_ARRAY_SIZE(toplevel_allowed_state_mask_table)
-        }),
+	.name = "SYSTOP",
+	.data = &((struct mod_power_domain_element_config) {
+	    .parent_idx = UINT32_MAX,
+	    .attributes.pd_type = MOD_PD_TYPE_DEVICE,
+	    .driver_id = FWK_ID_ELEMENT_INIT(
+		FWK_MODULE_IDX_MOCK_PPU, MOCK_PPU_ELEMENT_IDX_SYSTEM),
+	    .api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_MOCK_PPU, 0),
+	    .allowed_state_mask_table = toplevel_allowed_state_mask_table,
+	    .allowed_state_mask_table_size =
+		FWK_ARRAY_SIZE(toplevel_allowed_state_mask_table)
+	}),
     },
 };
 
@@ -130,16 +130,16 @@ static const struct fwk_element *fvp_pow
     const struct fwk_element *element_table;
 
     element_table = create_power_domain_element_table(
-        4,
-        2,
-        FWK_MODULE_IDX_MOCK_PPU,
-        0,
-        core_pd_allowed_state_mask_table,
-        FWK_ARRAY_SIZE(core_pd_allowed_state_mask_table),
-        cluster_pd_allowed_state_mask_table,
-        FWK_ARRAY_SIZE(cluster_pd_allowed_state_mask_table),
-        fvp_power_domain_static_element_table,
-        FWK_ARRAY_SIZE(fvp_power_domain_static_element_table));
+	4,
+	2,
+	FWK_MODULE_IDX_MOCK_PPU,
+	0,
+	core_pd_allowed_state_mask_table,
+	FWK_ARRAY_SIZE(core_pd_allowed_state_mask_table),
+	cluster_pd_allowed_state_mask_table,
+	FWK_ARRAY_SIZE(cluster_pd_allowed_state_mask_table),
+	fvp_power_domain_static_element_table,
+	FWK_ARRAY_SIZE(fvp_power_domain_static_element_table));
 
     return element_table;
 }
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-fvp/fw/config_psu.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/product/optee-fvp/fw/config_psu.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-fvp/fw/config_psu.c
@@ -12,36 +12,36 @@
 
 static const struct fwk_element element_table[] = {
     {
-        .name = "CPU_GROUP_LITTLE",
-        .data = &(const struct mod_psu_element_cfg) {
-            .driver_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_MOCK_PSU, 0),
-            .driver_api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_MOCK_PSU,
-                                             MOD_MOCK_PSU_API_IDX_DRIVER)
-        },
+	.name = "CPU_GROUP_LITTLE",
+	.data = &(const struct mod_psu_element_cfg) {
+	    .driver_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_MOCK_PSU, 0),
+	    .driver_api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_MOCK_PSU,
+					     MOD_MOCK_PSU_API_IDX_DRIVER)
+	},
     },
     {
-        .name = "CPU_GROUP_BIG",
-        .data = &(const struct mod_psu_element_cfg) {
-            .driver_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_MOCK_PSU, 1),
-            .driver_api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_MOCK_PSU,
-                                             MOD_MOCK_PSU_API_IDX_DRIVER)
-        },
+	.name = "CPU_GROUP_BIG",
+	.data = &(const struct mod_psu_element_cfg) {
+	    .driver_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_MOCK_PSU, 1),
+	    .driver_api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_MOCK_PSU,
+					     MOD_MOCK_PSU_API_IDX_DRIVER)
+	},
     },
     {
-        .name = "GPU",
-        .data = &(const struct mod_psu_element_cfg) {
-            .driver_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_MOCK_PSU, 2),
-            .driver_api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_MOCK_PSU,
-                                             MOD_MOCK_PSU_API_IDX_DRIVER)
-        },
+	.name = "GPU",
+	.data = &(const struct mod_psu_element_cfg) {
+	    .driver_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_MOCK_PSU, 2),
+	    .driver_api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_MOCK_PSU,
+					     MOD_MOCK_PSU_API_IDX_DRIVER)
+	},
     },
     {
-        .name = "VPU",
-        .data = &(const struct mod_psu_element_cfg) {
-            .driver_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_MOCK_PSU, 3),
-            .driver_api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_MOCK_PSU,
-                                             MOD_MOCK_PSU_API_IDX_DRIVER)
-        },
+	.name = "VPU",
+	.data = &(const struct mod_psu_element_cfg) {
+	    .driver_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_MOCK_PSU, 3),
+	    .driver_api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_MOCK_PSU,
+					     MOD_MOCK_PSU_API_IDX_DRIVER)
+	},
     },
     { 0 }
 };
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-fvp/fw/config_scmi.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/product/optee-fvp/fw/config_scmi.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-fvp/fw/config_scmi.c
@@ -14,40 +14,40 @@
 
 static const struct fwk_element service_table[] = {
     [SCMI_SERVICE_IDX_PSCI] = {
-        .name = "agent PSCI",
-        .data = &((struct mod_scmi_service_config) {
-            .transport_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_MSG_SMT,
-                                                SCMI_SERVICE_IDX_PSCI),
-            .transport_api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_MSG_SMT,
-                                                MOD_MSG_SMT_API_IDX_SCMI_TRANSPORT),
-            .scmi_agent_id = SCMI_AGENT_ID_PSCI,
-            .transport_notification_init_id = FWK_ID_NONE_INIT,
-            .scmi_p2a_id = FWK_ID_NONE_INIT,
-        }),
+	.name = "agent PSCI",
+	.data = &((struct mod_scmi_service_config) {
+	    .transport_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_MSG_SMT,
+						SCMI_SERVICE_IDX_PSCI),
+	    .transport_api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_MSG_SMT,
+						MOD_MSG_SMT_API_IDX_SCMI_TRANSPORT),
+	    .scmi_agent_id = SCMI_AGENT_ID_PSCI,
+	    .transport_notification_init_id = FWK_ID_NONE_INIT,
+	    .scmi_p2a_id = FWK_ID_NONE_INIT,
+	}),
     },
     [SCMI_SERVICE_IDX_OSPM_0] = {
-        .name = "agent OSPM0",
-        .data = &((struct mod_scmi_service_config) {
-            .transport_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_MSG_SMT,
-                                                SCMI_SERVICE_IDX_OSPM_0),
-            .transport_api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_MSG_SMT,
-                                                MOD_MSG_SMT_API_IDX_SCMI_TRANSPORT),
-            .scmi_agent_id = SCMI_AGENT_ID_OSPM,
-            .transport_notification_init_id = FWK_ID_NONE_INIT,
-            .scmi_p2a_id = FWK_ID_NONE_INIT,
-        }),
+	.name = "agent OSPM0",
+	.data = &((struct mod_scmi_service_config) {
+	    .transport_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_MSG_SMT,
+						SCMI_SERVICE_IDX_OSPM_0),
+	    .transport_api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_MSG_SMT,
+						MOD_MSG_SMT_API_IDX_SCMI_TRANSPORT),
+	    .scmi_agent_id = SCMI_AGENT_ID_OSPM,
+	    .transport_notification_init_id = FWK_ID_NONE_INIT,
+	    .scmi_p2a_id = FWK_ID_NONE_INIT,
+	}),
     },
     [SCMI_SERVICE_IDX_OSPM_1] = {
-        .name = "agent OSPM1",
-        .data = &((struct mod_scmi_service_config) {
-            .transport_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_MSG_SMT,
-                                                SCMI_SERVICE_IDX_OSPM_1),
-            .transport_api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_MSG_SMT,
-                                                MOD_MSG_SMT_API_IDX_SCMI_TRANSPORT),
-            .scmi_agent_id = SCMI_AGENT_ID_PERF,
-            .transport_notification_init_id = FWK_ID_NONE_INIT,
-            .scmi_p2a_id = FWK_ID_NONE_INIT,
-        }),
+	.name = "agent OSPM1",
+	.data = &((struct mod_scmi_service_config) {
+	    .transport_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_MSG_SMT,
+						SCMI_SERVICE_IDX_OSPM_1),
+	    .transport_api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_MSG_SMT,
+						MOD_MSG_SMT_API_IDX_SCMI_TRANSPORT),
+	    .scmi_agent_id = SCMI_AGENT_ID_PERF,
+	    .transport_notification_init_id = FWK_ID_NONE_INIT,
+	    .scmi_p2a_id = FWK_ID_NONE_INIT,
+	}),
     },
     [SCMI_SERVICE_IDX_COUNT] = { 0 }
 };
@@ -59,26 +59,26 @@ static const struct fwk_element *get_ser
 
 static const struct mod_scmi_agent agent_table[] = {
     [SCMI_AGENT_ID_OSPM] = {
-        .type = SCMI_AGENT_TYPE_OSPM,
-        .name = "OSPM",
+	.type = SCMI_AGENT_TYPE_OSPM,
+	.name = "OSPM",
     },
     [SCMI_AGENT_ID_PSCI] = {
-        .type = SCMI_AGENT_TYPE_PSCI,
-        .name = "PSCI",
+	.type = SCMI_AGENT_TYPE_PSCI,
+	.name = "PSCI",
     },
    [SCMI_AGENT_ID_PERF] = {
-        .type = SCMI_AGENT_TYPE_OSPM,
-        .name = "PERF",
+	.type = SCMI_AGENT_TYPE_OSPM,
+	.name = "PERF",
     },
 };
 
 struct fwk_module_config config_scmi = {
     .data = &((struct mod_scmi_config) {
-        .protocol_count_max = 9,
-        .agent_count = FWK_ARRAY_SIZE(agent_table) - 1,
-        .agent_table = agent_table,
-        .vendor_identifier = "Linaro",
-        .sub_vendor_identifier = "PMWG",
+	.protocol_count_max = 9,
+	.agent_count = FWK_ARRAY_SIZE(agent_table) - 1,
+	.agent_table = agent_table,
+	.vendor_identifier = "Linaro",
+	.sub_vendor_identifier = "PMWG",
     }),
     .elements = FWK_MODULE_DYNAMIC_ELEMENTS(get_service_table),
 };
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-fvp/fw/config_scmi_clock.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/product/optee-fvp/fw/config_scmi_clock.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-fvp/fw/config_scmi_clock.c
@@ -13,93 +13,93 @@
 
 static const struct mod_scmi_clock_device agent_device_table_ospm[] = {
     {
-        /* MOCK_0 */
-        .element_id =
-            FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_CLOCK, CLOCK_DEV_IDX_MOCK_0),
-        .starts_enabled = true,
+	/* MOCK_0 */
+	.element_id =
+	    FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_CLOCK, CLOCK_DEV_IDX_MOCK_0),
+	.starts_enabled = true,
     },
     {
-        /* MOCK_1 */
-        .element_id =
-            FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_CLOCK, CLOCK_DEV_IDX_MOCK_1),
-        .starts_enabled = true,
+	/* MOCK_1 */
+	.element_id =
+	    FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_CLOCK, CLOCK_DEV_IDX_MOCK_1),
+	.starts_enabled = true,
     },
     {
-        /* VPU */
-        .element_id =
-            FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_CLOCK, CLOCK_DEV_IDX_VPU),
-        .starts_enabled = true,
+	/* VPU */
+	.element_id =
+	    FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_CLOCK, CLOCK_DEV_IDX_VPU),
+	.starts_enabled = true,
     },
     {
-        /* MOCK_3 */
-        .element_id =
-            FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_CLOCK, CLOCK_DEV_IDX_MOCK_3),
-        .starts_enabled = true,
+	/* MOCK_3 */
+	.element_id =
+	    FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_CLOCK, CLOCK_DEV_IDX_MOCK_3),
+	.starts_enabled = true,
     },
     {
-        /* DPU */
-        .element_id =
-            FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_CLOCK, CLOCK_DEV_IDX_DPU),
-        .starts_enabled = true,
+	/* DPU */
+	.element_id =
+	    FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_CLOCK, CLOCK_DEV_IDX_DPU),
+	.starts_enabled = true,
     },
     {
-        /* PIXEL_0 */
-        .element_id =
-            FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_CLOCK, CLOCK_DEV_IDX_PIXEL_0),
-        .starts_enabled = true,
+	/* PIXEL_0 */
+	.element_id =
+	    FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_CLOCK, CLOCK_DEV_IDX_PIXEL_0),
+	.starts_enabled = true,
     },
     {
-        /* PIXEL_1 */
-        .element_id =
-            FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_CLOCK, CLOCK_DEV_IDX_PIXEL_1),
-        .starts_enabled = true,
+	/* PIXEL_1 */
+	.element_id =
+	    FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_CLOCK, CLOCK_DEV_IDX_PIXEL_1),
+	.starts_enabled = true,
     },
 };
 
 static const struct mod_scmi_clock_device agent_device_table_perf[] = {
     {
-        /* MOCK_0 */
-        .element_id =
-            FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_CLOCK, CLOCK_DEV_IDX_MOCK_0),
+	/* MOCK_0 */
+	.element_id =
+	    FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_CLOCK, CLOCK_DEV_IDX_MOCK_0),
     },
     {
-        /* MOCK_2 */
-        .element_id =
-            FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_CLOCK, CLOCK_DEV_IDX_MOCK_2),
+	/* MOCK_2 */
+	.element_id =
+	    FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_CLOCK, CLOCK_DEV_IDX_MOCK_2),
     },
     {
-        /* MOCK_3 */
-        .element_id =
-            FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_CLOCK, CLOCK_DEV_IDX_MOCK_3),
+	/* MOCK_3 */
+	.element_id =
+	    FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_CLOCK, CLOCK_DEV_IDX_MOCK_3),
     },
     {
-        /* PIXEL_0 */
-        .element_id =
-            FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_CLOCK, CLOCK_DEV_IDX_PIXEL_0),
+	/* PIXEL_0 */
+	.element_id =
+	    FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_CLOCK, CLOCK_DEV_IDX_PIXEL_0),
     },
     {
-        /* PIXEL_1 */
-        .element_id =
-            FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_CLOCK, CLOCK_DEV_IDX_PIXEL_1),
+	/* PIXEL_1 */
+	.element_id =
+	    FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_CLOCK, CLOCK_DEV_IDX_PIXEL_1),
     },
 };
 
 static const struct mod_scmi_clock_agent agent_table[SCMI_AGENT_ID_COUNT] = {
     [SCMI_AGENT_ID_OSPM] = {
-        .device_table = agent_device_table_ospm,
-        .device_count = FWK_ARRAY_SIZE(agent_device_table_ospm),
+	.device_table = agent_device_table_ospm,
+	.device_count = FWK_ARRAY_SIZE(agent_device_table_ospm),
     },
     [SCMI_AGENT_ID_PSCI] = { 0 /* No access */ },
     [SCMI_AGENT_ID_PERF] = {
-        .device_table = agent_device_table_perf,
-        .device_count = FWK_ARRAY_SIZE(agent_device_table_perf),
+	.device_table = agent_device_table_perf,
+	.device_count = FWK_ARRAY_SIZE(agent_device_table_perf),
     },
 };
 
 struct fwk_module_config config_scmi_clock = {
     .data = &((struct mod_scmi_clock_config) {
-        .max_pending_transactions = 0,
-        .agent_table = agent_table,
-        .agent_count = FWK_ARRAY_SIZE(agent_table),
+	.max_pending_transactions = 0,
+	.agent_table = agent_table,
+	.agent_count = FWK_ARRAY_SIZE(agent_table),
     }),
 };
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-fvp/fw/config_scmi_perf.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/product/optee-fvp/fw/config_scmi_perf.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-fvp/fw/config_scmi_perf.c
@@ -12,20 +12,20 @@
 
 static const struct mod_scmi_perf_domain_config domains[] = {
     [DVFS_ELEMENT_IDX_LITTLE] = {
-        .fast_channels_addr_scp = 0x0,
+	.fast_channels_addr_scp = 0x0,
     },
     [DVFS_ELEMENT_IDX_BIG] = {
-        .fast_channels_addr_scp = 0x0,
+	.fast_channels_addr_scp = 0x0,
     },
     [DVFS_ELEMENT_IDX_GPU] = {
-        .fast_channels_addr_scp = 0x0,
+	.fast_channels_addr_scp = 0x0,
     },
 };
 
 struct fwk_module_config config_scmi_perf = {
     .data = &((struct mod_scmi_perf_config) {
-        .domains = &domains,
-        .perf_doms_count = FWK_ARRAY_SIZE(domains),
-        .fast_channels_alarm_id = FWK_ID_NONE_INIT,
+	.domains = &domains,
+	.perf_doms_count = FWK_ARRAY_SIZE(domains),
+	.fast_channels_alarm_id = FWK_ID_NONE_INIT,
     }),
 };
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-fvp/fw/config_sensor.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/product/optee-fvp/fw/config_sensor.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-fvp/fw/config_sensor.c
@@ -26,18 +26,18 @@ static struct mod_sensor_info info_soc_t
 
 static const struct fwk_element reg_sensor_element_table[] = {
     [SENSOR_DEV_SOC_TEMP] = {
-        .name = "Soc Temperature",
-        .data = &((struct mod_reg_sensor_dev_config) {
-            .reg = (uintptr_t)(soc_temp),
-            .info = &info_soc_temperature,
-        }),
+	.name = "Soc Temperature",
+	.data = &((struct mod_reg_sensor_dev_config) {
+	    .reg = (uintptr_t)(soc_temp),
+	    .info = &info_soc_temperature,
+	}),
     },
     [SENSOR_DEV_DDR_TEMP] = {
-        .name = "DDR Temperature",
-        .data = &((struct mod_reg_sensor_dev_config) {
-            .reg = (uintptr_t)(ddr_temp),
-            .info = &info_soc_temperature,
-        }),
+	.name = "DDR Temperature",
+	.data = &((struct mod_reg_sensor_dev_config) {
+	    .reg = (uintptr_t)(ddr_temp),
+	    .info = &info_soc_temperature,
+	}),
     },
     [SENSOR_DEV_COUNT] = { 0 },
 };
@@ -56,26 +56,26 @@ struct fwk_module_config config_reg_sens
  */
 static const struct fwk_element sensor_element_table[] = {
     [0] = {
-        .name = "Soc Temperature",
-        .data = &((const struct mod_sensor_dev_config) {
-            .driver_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_REG_SENSOR,
-                                             SENSOR_DEV_SOC_TEMP),
-            .driver_api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_REG_SENSOR, 0),
-        .trip_point = {
-            .count = 2,
-        }
-        }),
+	.name = "Soc Temperature",
+	.data = &((const struct mod_sensor_dev_config) {
+	    .driver_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_REG_SENSOR,
+					     SENSOR_DEV_SOC_TEMP),
+	    .driver_api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_REG_SENSOR, 0),
+	.trip_point = {
+	    .count = 2,
+	}
+	}),
     },
     [1] = {
-        .name = "DDR Temperature",
-        .data = &((const struct mod_sensor_dev_config) {
-            .driver_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_REG_SENSOR,
-                                             SENSOR_DEV_DDR_TEMP),
-            .driver_api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_REG_SENSOR, 0),
-        .trip_point = {
-            .count = 2,
-        }
-        }),
+	.name = "DDR Temperature",
+	.data = &((const struct mod_sensor_dev_config) {
+	    .driver_id = FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_REG_SENSOR,
+					     SENSOR_DEV_DDR_TEMP),
+	    .driver_api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_REG_SENSOR, 0),
+	.trip_point = {
+	    .count = 2,
+	}
+	}),
     },
     [2] = { 0 },
 };
@@ -88,7 +88,7 @@ static const struct mod_sensor_config se
 #ifdef BUILD_HAS_SCMI_NOTIFICATIONS
     .notification_id = FWK_ID_MODULE_INIT(FWK_MODULE_IDX_SCMI_SENSOR),
     .trip_point_api_id = FWK_ID_API_INIT(FWK_MODULE_IDX_SCMI_SENSOR,
-                SCMI_SENSOR_API_IDX_TRIP_POINT),
+		SCMI_SENSOR_API_IDX_TRIP_POINT),
 #else
     .notification_id = FWK_ID_NONE_INIT
 #endif
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-fvp/fw/config_vpll.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/product/optee-fvp/fw/config_vpll.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-fvp/fw/config_vpll.c
@@ -12,81 +12,81 @@ uint32_t ctrl_reg[7];
 
 static const struct fwk_element system_pll_element_table[] = {
     {
-        .name = "CPU_PLL_0",
-        .data = &((struct mod_system_pll_dev_config) {
-        .control_reg = &ctrl_reg[0],
-            .initial_rate = 1330 * FWK_MHZ,
-            .min_rate = MOD_SYSTEM_PLL_MIN_RATE,
-            .max_rate = MOD_SYSTEM_PLL_MAX_RATE,
-            .min_step = MOD_SYSTEM_PLL_MIN_INTERVAL,
-            .defer_initialization = false,
-        }),
-    },
-    {
-        .name = "CPU_PLL_1",
-        .data = &((struct mod_system_pll_dev_config) {
-        .control_reg = &ctrl_reg[1],
-            .initial_rate = 1750 * FWK_MHZ,
-            .min_rate = MOD_SYSTEM_PLL_MIN_RATE,
-            .max_rate = MOD_SYSTEM_PLL_MAX_RATE,
-            .min_step = MOD_SYSTEM_PLL_MIN_INTERVAL,
-            .defer_initialization = false,
-        }),
-    },
-    {
-        .name = "GPU_PLL",
-        .data = &((struct mod_system_pll_dev_config) {
-        .control_reg = &ctrl_reg[2],
-            .initial_rate = 600 * FWK_MHZ,
-            .min_rate = MOD_SYSTEM_PLL_MIN_RATE,
-            .max_rate = MOD_SYSTEM_PLL_MAX_RATE,
-            .min_step = MOD_SYSTEM_PLL_MIN_INTERVAL,
-            .defer_initialization = false,
-        }),
-    },
-    {
-        .name = "DPU_PLL",
-        .data = &((struct mod_system_pll_dev_config) {
-        .control_reg = &ctrl_reg[3],
-            .initial_rate = 260 * FWK_MHZ,
-            .min_rate = MOD_SYSTEM_PLL_MIN_RATE,
-            .max_rate = MOD_SYSTEM_PLL_MAX_RATE,
-            .min_step = MOD_SYSTEM_PLL_MIN_INTERVAL,
-            .defer_initialization = false,
-        }),
-    },
-    {
-        .name = "VPU_PLL",
-        .data = &((struct mod_system_pll_dev_config) {
-        .control_reg = &ctrl_reg[4],
-            .initial_rate = 600 * FWK_MHZ,
-            .min_rate = MOD_SYSTEM_PLL_MIN_RATE,
-            .max_rate = MOD_SYSTEM_PLL_MAX_RATE,
-            .min_step = MOD_SYSTEM_PLL_MIN_INTERVAL,
-            .defer_initialization = false,
-        }),
-    },
-    {
-        .name = "PIX0_PLL",
-        .data = &((struct mod_system_pll_dev_config) {
-        .control_reg = &ctrl_reg[5],
-            .initial_rate = 594 * FWK_MHZ,
-            .min_rate = 12500 * FWK_KHZ,
-            .max_rate = 594 * FWK_MHZ,
-            .min_step = 250 * FWK_KHZ,
-            .defer_initialization = false,
-        }),
-    },
-    {
-        .name = "PIX1_PLL",
-        .data = &((struct mod_system_pll_dev_config) {
-        .control_reg = &ctrl_reg[6],
-            .initial_rate = 594 * FWK_MHZ,
-            .min_rate = 12500 * FWK_KHZ,
-            .max_rate = 594 * FWK_MHZ,
-            .min_step = 250 * FWK_KHZ,
-            .defer_initialization = false,
-        }),
+	.name = "CPU_PLL_0",
+	.data = &((struct mod_system_pll_dev_config) {
+	.control_reg = &ctrl_reg[0],
+	    .initial_rate = 1330 * FWK_MHZ,
+	    .min_rate = MOD_SYSTEM_PLL_MIN_RATE,
+	    .max_rate = MOD_SYSTEM_PLL_MAX_RATE,
+	    .min_step = MOD_SYSTEM_PLL_MIN_INTERVAL,
+	    .defer_initialization = false,
+	}),
+    },
+    {
+	.name = "CPU_PLL_1",
+	.data = &((struct mod_system_pll_dev_config) {
+	.control_reg = &ctrl_reg[1],
+	    .initial_rate = 1750 * FWK_MHZ,
+	    .min_rate = MOD_SYSTEM_PLL_MIN_RATE,
+	    .max_rate = MOD_SYSTEM_PLL_MAX_RATE,
+	    .min_step = MOD_SYSTEM_PLL_MIN_INTERVAL,
+	    .defer_initialization = false,
+	}),
+    },
+    {
+	.name = "GPU_PLL",
+	.data = &((struct mod_system_pll_dev_config) {
+	.control_reg = &ctrl_reg[2],
+	    .initial_rate = 600 * FWK_MHZ,
+	    .min_rate = MOD_SYSTEM_PLL_MIN_RATE,
+	    .max_rate = MOD_SYSTEM_PLL_MAX_RATE,
+	    .min_step = MOD_SYSTEM_PLL_MIN_INTERVAL,
+	    .defer_initialization = false,
+	}),
+    },
+    {
+	.name = "DPU_PLL",
+	.data = &((struct mod_system_pll_dev_config) {
+	.control_reg = &ctrl_reg[3],
+	    .initial_rate = 260 * FWK_MHZ,
+	    .min_rate = MOD_SYSTEM_PLL_MIN_RATE,
+	    .max_rate = MOD_SYSTEM_PLL_MAX_RATE,
+	    .min_step = MOD_SYSTEM_PLL_MIN_INTERVAL,
+	    .defer_initialization = false,
+	}),
+    },
+    {
+	.name = "VPU_PLL",
+	.data = &((struct mod_system_pll_dev_config) {
+	.control_reg = &ctrl_reg[4],
+	    .initial_rate = 600 * FWK_MHZ,
+	    .min_rate = MOD_SYSTEM_PLL_MIN_RATE,
+	    .max_rate = MOD_SYSTEM_PLL_MAX_RATE,
+	    .min_step = MOD_SYSTEM_PLL_MIN_INTERVAL,
+	    .defer_initialization = false,
+	}),
+    },
+    {
+	.name = "PIX0_PLL",
+	.data = &((struct mod_system_pll_dev_config) {
+	.control_reg = &ctrl_reg[5],
+	    .initial_rate = 594 * FWK_MHZ,
+	    .min_rate = 12500 * FWK_KHZ,
+	    .max_rate = 594 * FWK_MHZ,
+	    .min_step = 250 * FWK_KHZ,
+	    .defer_initialization = false,
+	}),
+    },
+    {
+	.name = "PIX1_PLL",
+	.data = &((struct mod_system_pll_dev_config) {
+	.control_reg = &ctrl_reg[6],
+	    .initial_rate = 594 * FWK_MHZ,
+	    .min_rate = 12500 * FWK_KHZ,
+	    .max_rate = 594 * FWK_MHZ,
+	    .min_step = 250 * FWK_KHZ,
+	    .defer_initialization = false,
+	}),
     },
     { 0 }, /* Termination description. */
 };
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp1/fw/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp1/fw/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp1/fw/CMakeLists.txt
@@ -16,7 +16,7 @@ add_library(${SCP_FIRMWARE_TARGET})
 target_include_directories(
     ${SCP_FIRMWARE_TARGET}
     PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/../include"
-           "${CMAKE_CURRENT_SOURCE_DIR}")
+	   "${CMAKE_CURRENT_SOURCE_DIR}")
 
 target_sources(
     ${SCP_FIRMWARE_TARGET}
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp1/fw/config_all.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp1/fw/config_all.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp1/fw/config_all.c
@@ -138,8 +138,8 @@ struct fwk_module_config config_msg_smt
 #ifdef CFG_SCPFW_MOD_SCMI_CLOCK
 struct fwk_module_config config_scmi_clock = {
     .data = &((struct mod_scmi_clock_config){
-        .agent_table = NULL,			/* Allocated during initialization */
-        .agent_count = 0,			/* Set during initialization */
+	.agent_table = NULL,			/* Allocated during initialization */
+	.agent_count = 0,			/* Set during initialization */
     }),
 };
 #endif
@@ -170,8 +170,8 @@ struct fwk_module_config config_optee_cl
 #ifdef CFG_SCPFW_MOD_RESET_DOMAIN
 struct fwk_module_config config_scmi_reset_domain = {
     .data = &((struct mod_scmi_reset_domain_config){
-        .agent_table = NULL,			/* Allocated during initialization */
-        .agent_count = 0,			/* Set during initialization */
+	.agent_table = NULL,			/* Allocated during initialization */
+	.agent_count = 0,			/* Set during initialization */
     }),
 };
 
@@ -200,8 +200,8 @@ struct fwk_module_config config_optee_re
 /* Config data for scmi_voltage_domain, voltage_domain and optee_regu modules */
 struct fwk_module_config config_scmi_voltage_domain = {
     .data = &((struct mod_scmi_voltd_config){
-        .agent_table = NULL,			/* Allocated during initialization */
-        .agent_count = 0,			/* Set during initialization */
+	.agent_table = NULL,			/* Allocated during initialization */
+	.agent_count = 0,			/* Set during initialization */
     }),
 };
 
@@ -230,11 +230,11 @@ struct fwk_module_config config_stm32_re
 /* Config data for scmi_perf and dvfs  module */
 struct fwk_module_config config_scmi_perf = {
     .data = &((struct mod_scmi_perf_config){
-        .domains = NULL,			/* Allocated during initialization */
-        .perf_doms_count = 0,			/* Set during initialization */
-        .fast_channels_alarm_id = FWK_ID_NONE_INIT,
+	.domains = NULL,			/* Allocated during initialization */
+	.perf_doms_count = 0,			/* Set during initialization */
+	.fast_channels_alarm_id = FWK_ID_NONE_INIT,
 #ifdef BUILD_HAS_MOD_STATISTICS
-        .stats_enabled = true,
+	.stats_enabled = true,
 #endif
     }),
 };
@@ -309,24 +309,24 @@ static void count_resources(struct scpfw
     size_t i, j;
 
     for (i = 0; i < cfg->agent_count; i++) {
-        struct scpfw_agent_config *agent_cfg = cfg->agent_config + i;
+	struct scpfw_agent_config *agent_cfg = cfg->agent_config + i;
 
-        scpfw_resource_counter.channel_count += agent_cfg->channel_count;
+	scpfw_resource_counter.channel_count += agent_cfg->channel_count;
 
-        for (j = 0; j < agent_cfg->channel_count; j++) {
-            struct scpfw_channel_config *channel_cfg = agent_cfg->channel_config + j;
+	for (j = 0; j < agent_cfg->channel_count; j++) {
+	    struct scpfw_channel_config *channel_cfg = agent_cfg->channel_config + j;
 
-            /* Clocks for scmi_clock and for DVFS */
-            scpfw_resource_counter.clock_count += channel_cfg->clock_count;
-            scpfw_resource_counter.clock_count += channel_cfg->perfd_count;
-            /* Reset for smci_reset only */
-            scpfw_resource_counter.reset_count += channel_cfg->reset_count;
-            /* Regulators for smci_voltage_domain only */
-            scpfw_resource_counter.regu_count += channel_cfg->voltd_count;
-            /* DVFS and PSU DVFS only */
-            scpfw_resource_counter.dvfs_count += channel_cfg->perfd_count;
-            scpfw_resource_counter.psu_count += channel_cfg->perfd_count;
-        }
+	    /* Clocks for scmi_clock and for DVFS */
+	    scpfw_resource_counter.clock_count += channel_cfg->clock_count;
+	    scpfw_resource_counter.clock_count += channel_cfg->perfd_count;
+	    /* Reset for smci_reset only */
+	    scpfw_resource_counter.reset_count += channel_cfg->reset_count;
+	    /* Regulators for smci_voltage_domain only */
+	    scpfw_resource_counter.regu_count += channel_cfg->voltd_count;
+	    /* DVFS and PSU DVFS only */
+	    scpfw_resource_counter.dvfs_count += channel_cfg->perfd_count;
+	    scpfw_resource_counter.psu_count += channel_cfg->perfd_count;
+	}
     }
 
 #ifndef CFG_SCPFW_MOD_CLOCK
@@ -361,7 +361,7 @@ static void allocate_global_resources(st
 #ifdef CFG_SCPFW_MOD_SCMI_CLOCK
     /* SCMI clock domains resources */
     scmi_clk_agent_tbl = fwk_mm_calloc(scmi_agent_count,
-                                       sizeof(*scmi_clk_agent_tbl));
+				       sizeof(*scmi_clk_agent_tbl));
     scmi_clock_config = (void *)config_scmi_clock.data;
     scmi_clock_config->agent_table = scmi_clk_agent_tbl;
     scmi_clock_config->agent_count = scmi_agent_count;
@@ -370,74 +370,74 @@ static void allocate_global_resources(st
 #ifdef CFG_SCPFW_MOD_CLOCK
     /* Clock domains resources */
     optee_clock_cfg = fwk_mm_calloc(scpfw_resource_counter.clock_count,
-                                    sizeof(*optee_clock_cfg));
+				    sizeof(*optee_clock_cfg));
     optee_clock_elt = fwk_mm_calloc(scpfw_resource_counter.clock_count + 1,
-                                    sizeof(*optee_clock_elt));
+				    sizeof(*optee_clock_elt));
 
     clock_data = fwk_mm_calloc(scpfw_resource_counter.clock_count,
-                               sizeof(*clock_data));
+			       sizeof(*clock_data));
     clock_elt = fwk_mm_calloc(scpfw_resource_counter.clock_count + 1,
-                              sizeof(*clock_elt));
+			      sizeof(*clock_elt));
 #endif
 
 #ifdef CFG_SCPFW_MOD_RESET_DOMAIN
     /* SCMI reset domains resources */
     scmi_reset_agent_tbl = fwk_mm_calloc(scmi_agent_count,
-                                         sizeof(*scmi_reset_agent_tbl));
+					 sizeof(*scmi_reset_agent_tbl));
     scmi_reset_config = (void *)config_scmi_reset_domain.data;
     scmi_reset_config->agent_table = scmi_reset_agent_tbl;
     scmi_reset_config->agent_count = scmi_agent_count;
 
     optee_reset_data = fwk_mm_calloc(scpfw_resource_counter.reset_count,
-                                     sizeof(*optee_reset_data));
+				     sizeof(*optee_reset_data));
     optee_reset_elt = fwk_mm_calloc(scpfw_resource_counter.reset_count + 1,
-                                    sizeof(*optee_reset_elt));
+				    sizeof(*optee_reset_elt));
 
     reset_data = fwk_mm_calloc(scpfw_resource_counter.reset_count,
-                               sizeof(*reset_data));
+			       sizeof(*reset_data));
     reset_elt = fwk_mm_calloc(scpfw_resource_counter.reset_count + 1,
-                              sizeof(*reset_elt));
+			      sizeof(*reset_elt));
 #endif
 
 #ifdef CFG_SCPFW_MOD_DVFS
     /* PSU and related optee PSU regulator resources */
     psu_optee_regu_elt = fwk_mm_calloc(scpfw_resource_counter.dvfs_count + 1,
-                                  sizeof(*psu_optee_regu_elt));
+				  sizeof(*psu_optee_regu_elt));
     psu_optee_regu_data = fwk_mm_calloc(scpfw_resource_counter.dvfs_count,
-                                  sizeof(*psu_optee_regu_data));
+				  sizeof(*psu_optee_regu_data));
 
     psu_elt = fwk_mm_calloc(scpfw_resource_counter.psu_count + 1,
-                            sizeof(*psu_elt));
+			    sizeof(*psu_elt));
     psu_data = fwk_mm_calloc(scpfw_resource_counter.psu_count,
-                             sizeof(*psu_data));
+			     sizeof(*psu_data));
 
     /* DVFS and SCMI performance management resources */
     dvfs_elt = fwk_mm_calloc(scpfw_resource_counter.dvfs_count + 1,
-                             sizeof(*dvfs_elt));
+			     sizeof(*dvfs_elt));
     dvfs_data = fwk_mm_calloc(scpfw_resource_counter.dvfs_count,
-                              sizeof(*dvfs_data));
+			      sizeof(*dvfs_data));
 
     scmi_perf_domain_data = fwk_mm_calloc(scpfw_resource_counter.dvfs_count,
-                                          sizeof(*scmi_perf_domain_data));
+					  sizeof(*scmi_perf_domain_data));
 #endif
 
 #ifdef CFG_SCPFW_MOD_VOLTAGE_DOMAIN
     /* SCMI voltage domains resources */
     scmi_voltd_agent_tbl = fwk_mm_calloc(scmi_agent_count,
-                                         sizeof(*scmi_voltd_agent_tbl));
+					 sizeof(*scmi_voltd_agent_tbl));
     scmi_voltd_config = (void *)config_scmi_voltage_domain.data;
     scmi_voltd_config->agent_table = scmi_voltd_agent_tbl;
     scmi_voltd_config->agent_count = scmi_agent_count;
 
     optee_regu_data = fwk_mm_calloc(scpfw_resource_counter.regu_count,
-                                    sizeof(*optee_regu_data));
+				    sizeof(*optee_regu_data));
     optee_regu_elt = fwk_mm_calloc(scpfw_resource_counter.regu_count + 1,
-                                    sizeof(*optee_regu_elt));
+				    sizeof(*optee_regu_elt));
 
     voltd_data = fwk_mm_calloc(scpfw_resource_counter.regu_count,
-                               sizeof(*voltd_data));
+			       sizeof(*voltd_data));
     voltd_elt = fwk_mm_calloc(scpfw_resource_counter.regu_count + 1,
-                              sizeof(*voltd_elt));
+			      sizeof(*voltd_elt));
 #endif
 }
 
@@ -449,80 +449,80 @@ static void set_scmi_comm_resources(stru
     size_t scmi_agent_count = cfg->agent_count + 1;
 
     scmi_agent_table = fwk_mm_calloc(scmi_agent_count,
-                                     sizeof(*scmi_agent_table));
+				     sizeof(*scmi_agent_table));
 
     scmi_service_elt = fwk_mm_calloc(scpfw_resource_counter.channel_count + 1,
-                                     sizeof(*scmi_service_elt));
+				     sizeof(*scmi_service_elt));
 
     msg_smt_elt = fwk_mm_calloc(scpfw_resource_counter.channel_count + 1,
-                                sizeof(*msg_smt_elt));
+				sizeof(*msg_smt_elt));
     msg_smt_data = fwk_mm_calloc(scpfw_resource_counter.channel_count,
-                                 sizeof(*msg_smt_data));
+				 sizeof(*msg_smt_data));
 
     optee_mbx_elt = fwk_mm_calloc(scpfw_resource_counter.channel_count + 1,
-                                  sizeof(*optee_mbx_elt));
+				  sizeof(*optee_mbx_elt));
     optee_mbx_data = fwk_mm_calloc(scpfw_resource_counter.channel_count,
-                                   sizeof(*optee_mbx_data));
+				   sizeof(*optee_mbx_data));
 
     /* Set now the uniqnue scmi module instance configuration data */
     scmi_data = (struct mod_scmi_config){
-        .agent_table = scmi_agent_table,
-        .agent_count = scmi_agent_count,
-        .protocol_count_max = 9,
-        .vendor_identifier = "ST",
-        .sub_vendor_identifier = "ST",
+	.agent_table = scmi_agent_table,
+	.agent_count = scmi_agent_count,
+	.protocol_count_max = 9,
+	.vendor_identifier = "ST",
+	.sub_vendor_identifier = "ST",
     };
 
     channel_index = 0;
 
     for (i = 0; i < cfg->agent_count; i++) {
-        struct scpfw_agent_config *agent_cfg = cfg->agent_config + i;
-        size_t agent_index = i + 1;
+	struct scpfw_agent_config *agent_cfg = cfg->agent_config + i;
+	size_t agent_index = i + 1;
 
-        scmi_agent_table[agent_index].type = SCMI_AGENT_TYPE_OSPM;
-        scmi_agent_table[agent_index].name = agent_cfg->name;
+	scmi_agent_table[agent_index].type = SCMI_AGENT_TYPE_OSPM;
+	scmi_agent_table[agent_index].name = agent_cfg->name;
 
-        for (j = 0; j < agent_cfg->channel_count; j++) {
-            struct scpfw_channel_config *channel_cfg = agent_cfg->channel_config + j;
-            struct mod_scmi_service_config *service_data;
-
-            service_data = fwk_mm_calloc(1, sizeof(*service_data));
-            *service_data = (struct mod_scmi_service_config){
-                .transport_id = (fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_MSG_SMT, 0),
-                .transport_api_id = (fwk_id_t)FWK_ID_API_INIT(FWK_MODULE_IDX_MSG_SMT,
-                                                              MOD_MSG_SMT_API_IDX_SCMI_TRANSPORT),
-                .scmi_agent_id = agent_cfg->agent_id,
-                .scmi_p2a_id = FWK_ID_NONE_INIT,
-            };
-
-            /* Currently expect 1 agent with ID SCMI_AGENT_ID_NSEC0 (1) */
-            fwk_assert(service_data->scmi_agent_id == SCMI_AGENT_ID_NSEC0);
-
-            scmi_service_elt[channel_index].name = channel_cfg->name;
-            scmi_service_elt[channel_index].data = service_data;
-
-            msg_smt_elt[channel_index].name = channel_cfg->name;
-            msg_smt_elt[channel_index].data = (void *)(msg_smt_data + channel_index);
-
-            msg_smt_data[channel_index] = (struct mod_msg_smt_channel_config){
-                .type = MOD_MSG_SMT_CHANNEL_TYPE_REQUESTER,
-                .mailbox_size = 128,
-                .driver_id = (fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_OPTEE_MBX,
-                                                           channel_index),
-                .driver_api_id = (fwk_id_t)FWK_ID_API_INIT(FWK_MODULE_IDX_OPTEE_MBX, 0),
-            };
-
-            optee_mbx_elt[channel_index].name = channel_cfg->name;
-            optee_mbx_elt[channel_index].data = (void *)(optee_mbx_data + channel_index);
-
-            optee_mbx_data[channel_index] = (struct mod_optee_mbx_channel_config){
-                .driver_id = (fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_MSG_SMT, channel_index),
-                .driver_api_id = (fwk_id_t)FWK_ID_API_INIT(FWK_MODULE_IDX_MSG_SMT,
-                                                           MOD_MSG_SMT_API_IDX_DRIVER_INPUT),
-            };
+	for (j = 0; j < agent_cfg->channel_count; j++) {
+	    struct scpfw_channel_config *channel_cfg = agent_cfg->channel_config + j;
+	    struct mod_scmi_service_config *service_data;
+
+	    service_data = fwk_mm_calloc(1, sizeof(*service_data));
+	    *service_data = (struct mod_scmi_service_config){
+		.transport_id = (fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_MSG_SMT, 0),
+		.transport_api_id = (fwk_id_t)FWK_ID_API_INIT(FWK_MODULE_IDX_MSG_SMT,
+							      MOD_MSG_SMT_API_IDX_SCMI_TRANSPORT),
+		.scmi_agent_id = agent_cfg->agent_id,
+		.scmi_p2a_id = FWK_ID_NONE_INIT,
+	    };
+
+	    /* Currently expect 1 agent with ID SCMI_AGENT_ID_NSEC0 (1) */
+	    fwk_assert(service_data->scmi_agent_id == SCMI_AGENT_ID_NSEC0);
+
+	    scmi_service_elt[channel_index].name = channel_cfg->name;
+	    scmi_service_elt[channel_index].data = service_data;
+
+	    msg_smt_elt[channel_index].name = channel_cfg->name;
+	    msg_smt_elt[channel_index].data = (void *)(msg_smt_data + channel_index);
+
+	    msg_smt_data[channel_index] = (struct mod_msg_smt_channel_config){
+		.type = MOD_MSG_SMT_CHANNEL_TYPE_REQUESTER,
+		.mailbox_size = 128,
+		.driver_id = (fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_OPTEE_MBX,
+							   channel_index),
+		.driver_api_id = (fwk_id_t)FWK_ID_API_INIT(FWK_MODULE_IDX_OPTEE_MBX, 0),
+	    };
+
+	    optee_mbx_elt[channel_index].name = channel_cfg->name;
+	    optee_mbx_elt[channel_index].data = (void *)(optee_mbx_data + channel_index);
+
+	    optee_mbx_data[channel_index] = (struct mod_optee_mbx_channel_config){
+		.driver_id = (fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_MSG_SMT, channel_index),
+		.driver_api_id = (fwk_id_t)FWK_ID_API_INIT(FWK_MODULE_IDX_MSG_SMT,
+							   MOD_MSG_SMT_API_IDX_DRIVER_INPUT),
+	    };
 
-            channel_index++;
-        }
+	    channel_index++;
+	}
     }
 };
 
@@ -531,263 +531,263 @@ static void set_resources(struct scpfw_c
     size_t i, j, k, l __maybe_unused;
 
     for (i = 0; i < cfg->agent_count; i++) {
-        struct scpfw_agent_config *agent_cfg = cfg->agent_config + i;
-        size_t agent_index = i + 1;
+	struct scpfw_agent_config *agent_cfg = cfg->agent_config + i;
+	size_t agent_index = i + 1;
 
-        if (agent_index != agent_cfg->agent_id) {
-            panic("scpfw config expects agent ID is agent index");
-        }
+	if (agent_index != agent_cfg->agent_id) {
+	    panic("scpfw config expects agent ID is agent index");
+	}
 
-        for (j = 0; j < agent_cfg->channel_count; j++) {
-            struct scpfw_channel_config *channel_cfg = agent_cfg->channel_config + j;
+	for (j = 0; j < agent_cfg->channel_count; j++) {
+	    struct scpfw_channel_config *channel_cfg = agent_cfg->channel_config + j;
 
 #ifdef CFG_SCPFW_MOD_SCMI_CLOCK
-            /* Add first SCMI clock. We will add later the clocks used for DVFS */
-            if (channel_cfg->clock_count) {
-                size_t clock_index = scpfw_resource_counter.clock_index;
-                struct mod_scmi_clock_device *dev = NULL;
-
-                /* Set SCMI clocks array for the SCMI agent */
-                dev = fwk_mm_calloc(channel_cfg->clock_count,
-                                    sizeof(struct mod_scmi_clock_device));
-
-                fwk_assert(!scmi_clk_agent_tbl[agent_index].device_table);
-                scmi_clk_agent_tbl[agent_index].device_count = channel_cfg->clock_count;
-                scmi_clk_agent_tbl[agent_index].device_table = dev;
-
-                /* Set clock and optee/clock elements and config data */
-                for (k = 0; k < channel_cfg->clock_count; k++) {
-                    struct scmi_clock *clock_cfg = channel_cfg->clock + k;
-
-                    dev[k].element_id =
-                        (fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_CLOCK, clock_index);
-
-                    optee_clock_cfg[clock_index].clk = clock_cfg->clk;
-                    optee_clock_cfg[clock_index].default_enabled = clock_cfg->enabled;
-
-                    optee_clock_elt[clock_index].name = clock_cfg->name;
-                    optee_clock_elt[clock_index].data = (void *)(optee_clock_cfg + clock_index);
-
-                    clock_data[clock_index] = (struct mod_clock_dev_config){
-                        .driver_id = (fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_OPTEE_CLOCK,
-                                                                   clock_index),
-                        .api_id = (fwk_id_t)FWK_ID_API_INIT(FWK_MODULE_IDX_OPTEE_CLOCK,
-                                                            0),
-                        .pd_source_id = FWK_ID_NONE,
-                    };
+	    /* Add first SCMI clock. We will add later the clocks used for DVFS */
+	    if (channel_cfg->clock_count) {
+		size_t clock_index = scpfw_resource_counter.clock_index;
+		struct mod_scmi_clock_device *dev = NULL;
+
+		/* Set SCMI clocks array for the SCMI agent */
+		dev = fwk_mm_calloc(channel_cfg->clock_count,
+				    sizeof(struct mod_scmi_clock_device));
+
+		fwk_assert(!scmi_clk_agent_tbl[agent_index].device_table);
+		scmi_clk_agent_tbl[agent_index].device_count = channel_cfg->clock_count;
+		scmi_clk_agent_tbl[agent_index].device_table = dev;
+
+		/* Set clock and optee/clock elements and config data */
+		for (k = 0; k < channel_cfg->clock_count; k++) {
+		    struct scmi_clock *clock_cfg = channel_cfg->clock + k;
+
+		    dev[k].element_id =
+			(fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_CLOCK, clock_index);
+
+		    optee_clock_cfg[clock_index].clk = clock_cfg->clk;
+		    optee_clock_cfg[clock_index].default_enabled = clock_cfg->enabled;
+
+		    optee_clock_elt[clock_index].name = clock_cfg->name;
+		    optee_clock_elt[clock_index].data = (void *)(optee_clock_cfg + clock_index);
+
+		    clock_data[clock_index] = (struct mod_clock_dev_config){
+			.driver_id = (fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_OPTEE_CLOCK,
+								   clock_index),
+			.api_id = (fwk_id_t)FWK_ID_API_INIT(FWK_MODULE_IDX_OPTEE_CLOCK,
+							    0),
+			.pd_source_id = FWK_ID_NONE,
+		    };
 
-                    clock_elt[clock_index].name = clock_cfg->name;
-                    clock_elt[clock_index].data = (void *)(clock_data + clock_index);
+		    clock_elt[clock_index].name = clock_cfg->name;
+		    clock_elt[clock_index].data = (void *)(clock_data + clock_index);
 
-                    clock_index++;
-                }
+		    clock_index++;
+		}
 
-                scpfw_resource_counter.clock_index = clock_index;
-            }
+		scpfw_resource_counter.clock_index = clock_index;
+	    }
 #endif
 
 #ifdef CFG_SCPFW_MOD_RESET_DOMAIN
-            if (channel_cfg->reset_count) {
-                struct mod_scmi_reset_domain_device *dev = NULL;
-                size_t reset_index = scpfw_resource_counter.reset_index;
-
-                /* Set SCMI reset domains array for the SCMI agent */
-                dev = fwk_mm_calloc(channel_cfg->reset_count, sizeof(*dev));
-
-                fwk_assert(!scmi_reset_agent_tbl[agent_index].device_table);
-                scmi_reset_agent_tbl[agent_index].agent_domain_count = channel_cfg->reset_count;
-                scmi_reset_agent_tbl[agent_index].device_table = dev;
-
-                /* Set reset_domain and optee/reset elements and config data */
-                for (k = 0; k < channel_cfg->reset_count; k++) {
-                    struct scmi_reset *reset_cfg = channel_cfg->reset + k;
-
-                    dev[k].element_id =
-                        (fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_RESET_DOMAIN,
-                                                      reset_index);
-
-                    optee_reset_data[reset_index].rstctrl = reset_cfg->rstctrl;
-
-                    optee_reset_elt[reset_index].name = reset_cfg->name;
-                    optee_reset_elt[reset_index].data =
-                        (void *)(optee_reset_data + reset_index);
-
-                    reset_data[reset_index] = (struct mod_reset_domain_dev_config){
-                        .driver_id =
-                            (fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_OPTEE_RESET,
-                                                          reset_index),
-                        .driver_api_id =
-                            (fwk_id_t)FWK_ID_API_INIT(FWK_MODULE_IDX_OPTEE_RESET, 0),
-                        .modes = MOD_RESET_DOMAIN_AUTO_RESET |
-                                 MOD_RESET_DOMAIN_MODE_EXPLICIT_ASSERT |
-                                 MOD_RESET_DOMAIN_MODE_EXPLICIT_DEASSERT,
-                    };
+	    if (channel_cfg->reset_count) {
+		struct mod_scmi_reset_domain_device *dev = NULL;
+		size_t reset_index = scpfw_resource_counter.reset_index;
+
+		/* Set SCMI reset domains array for the SCMI agent */
+		dev = fwk_mm_calloc(channel_cfg->reset_count, sizeof(*dev));
+
+		fwk_assert(!scmi_reset_agent_tbl[agent_index].device_table);
+		scmi_reset_agent_tbl[agent_index].agent_domain_count = channel_cfg->reset_count;
+		scmi_reset_agent_tbl[agent_index].device_table = dev;
+
+		/* Set reset_domain and optee/reset elements and config data */
+		for (k = 0; k < channel_cfg->reset_count; k++) {
+		    struct scmi_reset *reset_cfg = channel_cfg->reset + k;
+
+		    dev[k].element_id =
+			(fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_RESET_DOMAIN,
+						      reset_index);
+
+		    optee_reset_data[reset_index].rstctrl = reset_cfg->rstctrl;
+
+		    optee_reset_elt[reset_index].name = reset_cfg->name;
+		    optee_reset_elt[reset_index].data =
+			(void *)(optee_reset_data + reset_index);
+
+		    reset_data[reset_index] = (struct mod_reset_domain_dev_config){
+			.driver_id =
+			    (fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_OPTEE_RESET,
+							  reset_index),
+			.driver_api_id =
+			    (fwk_id_t)FWK_ID_API_INIT(FWK_MODULE_IDX_OPTEE_RESET, 0),
+			.modes = MOD_RESET_DOMAIN_AUTO_RESET |
+				 MOD_RESET_DOMAIN_MODE_EXPLICIT_ASSERT |
+				 MOD_RESET_DOMAIN_MODE_EXPLICIT_DEASSERT,
+		    };
 
-                    reset_elt[reset_index].name = reset_cfg->name;
-                    reset_elt[reset_index].data = (void *)(reset_data + reset_index);
+		    reset_elt[reset_index].name = reset_cfg->name;
+		    reset_elt[reset_index].data = (void *)(reset_data + reset_index);
 
-                    reset_index++;
-                }
+		    reset_index++;
+		}
 
-                scpfw_resource_counter.reset_index = reset_index;
-            }
+		scpfw_resource_counter.reset_index = reset_index;
+	    }
 #endif
 
 #ifdef CFG_SCPFW_MOD_VOLTAGE_DOMAIN
-            if (channel_cfg->voltd_count) {
-                size_t regu_index = scpfw_resource_counter.regu_index;
-                struct mod_scmi_voltd_device *dev = NULL;
-
-                /* Set SCMI voltage domains array for the SCMI agent */
-                dev = fwk_mm_calloc(channel_cfg->voltd_count,
-                                    sizeof(struct mod_scmi_voltd_device));
-
-                fwk_assert(!scmi_voltd_agent_tbl[agent_index].device_table);
-                scmi_voltd_agent_tbl[agent_index].domain_count = channel_cfg->voltd_count;
-                scmi_voltd_agent_tbl[agent_index].device_table = dev;
-
-                /* Set voltage_domain and optee/regu elements and config data */
-                for (k = 0; k < channel_cfg->voltd_count; k++) {
-                    struct scmi_voltd *voltd_cfg = channel_cfg->voltd + k;
-                    static const char reserved[] = "reserved";
-                    const char *name = NULL;
-
-                    if (voltd_cfg->rdev) {
-                        name = voltd_cfg->rdev->reg_name;
-                    } else {
-                        name = reserved;
-                    }
-
-                    dev[regu_index].element_id =
-                       (fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_VOLTAGE_DOMAIN, k);
-
-                    optee_regu_elt[regu_index].name = name;
-                    optee_regu_elt[regu_index].data = (void *)(optee_regu_data + regu_index);
-                    optee_regu_data[regu_index].rdev = voltd_cfg->rdev;
-                    optee_regu_data[regu_index].default_enabled = voltd_cfg->enabled;
-
-                    voltd_data[regu_index].driver_id =
-                        (fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_STM32_REGU_CONSUMER,
-                                                      regu_index);
-                    voltd_data[regu_index].api_id =
-                        (fwk_id_t)FWK_ID_API_INIT(FWK_MODULE_IDX_STM32_REGU_CONSUMER, 0);
-
-                    voltd_elt[regu_index].name = name;
-                    voltd_elt[regu_index].data = (void *)(voltd_data + regu_index);
-                    regu_index++;
-                }
+	    if (channel_cfg->voltd_count) {
+		size_t regu_index = scpfw_resource_counter.regu_index;
+		struct mod_scmi_voltd_device *dev = NULL;
+
+		/* Set SCMI voltage domains array for the SCMI agent */
+		dev = fwk_mm_calloc(channel_cfg->voltd_count,
+				    sizeof(struct mod_scmi_voltd_device));
+
+		fwk_assert(!scmi_voltd_agent_tbl[agent_index].device_table);
+		scmi_voltd_agent_tbl[agent_index].domain_count = channel_cfg->voltd_count;
+		scmi_voltd_agent_tbl[agent_index].device_table = dev;
+
+		/* Set voltage_domain and optee/regu elements and config data */
+		for (k = 0; k < channel_cfg->voltd_count; k++) {
+		    struct scmi_voltd *voltd_cfg = channel_cfg->voltd + k;
+		    static const char reserved[] = "reserved";
+		    const char *name = NULL;
+
+		    if (voltd_cfg->rdev) {
+			name = voltd_cfg->rdev->reg_name;
+		    } else {
+			name = reserved;
+		    }
+
+		    dev[regu_index].element_id =
+		       (fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_VOLTAGE_DOMAIN, k);
+
+		    optee_regu_elt[regu_index].name = name;
+		    optee_regu_elt[regu_index].data = (void *)(optee_regu_data + regu_index);
+		    optee_regu_data[regu_index].rdev = voltd_cfg->rdev;
+		    optee_regu_data[regu_index].default_enabled = voltd_cfg->enabled;
+
+		    voltd_data[regu_index].driver_id =
+			(fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_STM32_REGU_CONSUMER,
+						      regu_index);
+		    voltd_data[regu_index].api_id =
+			(fwk_id_t)FWK_ID_API_INIT(FWK_MODULE_IDX_STM32_REGU_CONSUMER, 0);
+
+		    voltd_elt[regu_index].name = name;
+		    voltd_elt[regu_index].data = (void *)(voltd_data + regu_index);
+		    regu_index++;
+		}
 
-                scpfw_resource_counter.regu_index = regu_index;
-            }
+		scpfw_resource_counter.regu_index = regu_index;
+	    }
 #endif
 
 #ifdef CFG_SCPFW_MOD_DVFS
-            if (channel_cfg->perfd_count) {
-                size_t clock_index = scpfw_resource_counter.clock_index;
-                size_t psu_index = scpfw_resource_counter.psu_index;
-                size_t dvfs_index = scpfw_resource_counter.dvfs_index;
-
-                for (k = 0; k < channel_cfg->perfd_count; k++) {
-                    struct mod_scmi_perf_config *scmi_perf_data = NULL;
-                    struct scmi_perfd *perfd_cfg = channel_cfg->perfd + k;
-
-                    /*
-                     * DVFS with SCMI performance management domains
-                     * 1 initial scmi_perf instance defines the number of DVFS's
-                     * For each DVFS instance:
-                     * - 1 instance (elt/config) of dvfs, psu, optee/psu, clock, optee/clock
-                     * Clocks and optee/clocks are already allocated but not yet set.
-                     */
-
-                    /* scmi_perf: data defines the DVFS domains indices */
-                    scmi_perf_domain_data[dvfs_index] = (struct mod_scmi_perf_domain_config){ };
-
-                    scmi_perf_data = (void *)config_scmi_perf.data;
-
-                    scmi_perf_data[dvfs_index].domains = (void *)scmi_perf_domain_data;
-                    scmi_perf_data[dvfs_index].perf_doms_count = scpfw_resource_counter.dvfs_count;
-                    scmi_perf_data[dvfs_index].fast_channels_alarm_id = (fwk_id_t)FWK_ID_NONE_INIT;
+	    if (channel_cfg->perfd_count) {
+		size_t clock_index = scpfw_resource_counter.clock_index;
+		size_t psu_index = scpfw_resource_counter.psu_index;
+		size_t dvfs_index = scpfw_resource_counter.dvfs_index;
+
+		for (k = 0; k < channel_cfg->perfd_count; k++) {
+		    struct mod_scmi_perf_config *scmi_perf_data = NULL;
+		    struct scmi_perfd *perfd_cfg = channel_cfg->perfd + k;
+
+		    /*
+		     * DVFS with SCMI performance management domains
+		     * 1 initial scmi_perf instance defines the number of DVFS's
+		     * For each DVFS instance:
+		     * - 1 instance (elt/config) of dvfs, psu, optee/psu, clock, optee/clock
+		     * Clocks and optee/clocks are already allocated but not yet set.
+		     */
+
+		    /* scmi_perf: data defines the DVFS domains indices */
+		    scmi_perf_domain_data[dvfs_index] = (struct mod_scmi_perf_domain_config){ };
+
+		    scmi_perf_data = (void *)config_scmi_perf.data;
+
+		    scmi_perf_data[dvfs_index].domains = (void *)scmi_perf_domain_data;
+		    scmi_perf_data[dvfs_index].perf_doms_count = scpfw_resource_counter.dvfs_count;
+		    scmi_perf_data[dvfs_index].fast_channels_alarm_id = (fwk_id_t)FWK_ID_NONE_INIT;
 #ifdef BUILD_HAS_MOD_STATISTICS
-                    scmi_perf_data[dvfs_index].stats_enabled = true;
+		    scmi_perf_data[dvfs_index].stats_enabled = true;
 #endif
 
-                    /* dvfs instances: 1 instance per expose DVFS service */
-                    dvfs_data[dvfs_index].psu_id =
-                        (fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_PSU, psu_index);
-                    dvfs_data[dvfs_index].clock_id =
-                        (fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_CLOCK, clock_index);
-                    dvfs_data[dvfs_index].latency = 0; /* not set, used for async access */
-                    dvfs_data[dvfs_index].sustained_idx = perfd_cfg->initial_opp;
-                    dvfs_data[dvfs_index].opps =
-                        fwk_mm_calloc(perfd_cfg->dvfs_opp_count,
-                                      sizeof(struct mod_dvfs_opp));
-
-                    for (l = 0; l < perfd_cfg->dvfs_opp_count; l++) {
-                        uint64_t power = 0;
-
-                        /* Quick estimation of power consumpsion */
-                        power = perfd_cfg->dvfs_opp_khz[l];
-                        power *= perfd_cfg->dvfs_opp_mv[l];
-                        power *= perfd_cfg->dvfs_opp_mv[l];
-                        power /= 100 * 1000 * 1000;
-
-                        dvfs_data[dvfs_index].opps[l] = (struct mod_dvfs_opp){
-                            .level = perfd_cfg->dvfs_opp_khz[l] * 1000UL,
-                            .frequency = perfd_cfg->dvfs_opp_khz[l],
-                            .voltage = perfd_cfg->dvfs_opp_mv[l],
-                            .power = (uint32_t)power,
-                        };
-                    }
-
-                    dvfs_elt[dvfs_index].name = perfd_cfg->name;
-                    dvfs_elt[dvfs_index].data = (void *)(dvfs_data + dvfs_index);
-
-                    /* Module psu_optee module (elements and  configuration data) */
-                    psu_optee_regu_elt[psu_index].name = perfd_cfg->rdev->reg_name;
-                    psu_optee_regu_elt[psu_index].sub_element_count = scpfw_resource_counter.psu_count;
-                    psu_optee_regu_elt[psu_index].data = (void *)(psu_optee_regu_data + psu_index);
-
-                    psu_optee_regu_data[psu_index].rdev = perfd_cfg->rdev;
-
-                    /* Module psu (elements and  configuration data) */
-                    psu_elt[psu_index].name = perfd_cfg->rdev->reg_name;
-                    psu_elt[psu_index].data = (void *)(psu_data + psu_index);
-                    psu_data[psu_index].driver_id =
-                        (fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_PSU_OPTEE_REGULATOR, psu_index);
-                    psu_data[psu_index].driver_api_id =
-                        (fwk_id_t)FWK_ID_API_INIT(FWK_MODULE_IDX_PSU_OPTEE_REGULATOR, 0);
-
-                    /* Module clock and optee_clock */
-                    fwk_assert(!clock_elt[clock_index].data);
-
-                    clock_elt[clock_index].name = perfd_cfg->clk->name;
-                    clock_elt[clock_index].data = (void *)(clock_data + clock_index);
-
-                    clock_data[clock_index] = (struct mod_clock_dev_config){
-                        .driver_id = (fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_OPTEE_CLOCK,
-                                                                   clock_index),
-                        .api_id = (fwk_id_t)FWK_ID_API_INIT(FWK_MODULE_IDX_OPTEE_CLOCK,
-                                                            0),
-                        .pd_source_id = FWK_ID_NONE,
-                    };
-
-                    optee_clock_cfg[clock_index].clk = perfd_cfg->clk;
-                    optee_clock_cfg[clock_index].default_enabled = false;
-
-                    optee_clock_elt[clock_index].name = perfd_cfg->clk->name;
-                    optee_clock_elt[clock_index].data = (void *)(optee_clock_cfg + clock_index);
-
-                    clock_index++;
-                    psu_index++;
-                    dvfs_index++;
-                }
-
-                scpfw_resource_counter.clock_index = clock_index;
-                scpfw_resource_counter.psu_index = psu_index;
-                scpfw_resource_counter.dvfs_index = dvfs_index;
-            }
+		    /* dvfs instances: 1 instance per expose DVFS service */
+		    dvfs_data[dvfs_index].psu_id =
+			(fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_PSU, psu_index);
+		    dvfs_data[dvfs_index].clock_id =
+			(fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_CLOCK, clock_index);
+		    dvfs_data[dvfs_index].latency = 0; /* not set, used for async access */
+		    dvfs_data[dvfs_index].sustained_idx = perfd_cfg->initial_opp;
+		    dvfs_data[dvfs_index].opps =
+			fwk_mm_calloc(perfd_cfg->dvfs_opp_count,
+				      sizeof(struct mod_dvfs_opp));
+
+		    for (l = 0; l < perfd_cfg->dvfs_opp_count; l++) {
+			uint64_t power = 0;
+
+			/* Quick estimation of power consumpsion */
+			power = perfd_cfg->dvfs_opp_khz[l];
+			power *= perfd_cfg->dvfs_opp_mv[l];
+			power *= perfd_cfg->dvfs_opp_mv[l];
+			power /= 100 * 1000 * 1000;
+
+			dvfs_data[dvfs_index].opps[l] = (struct mod_dvfs_opp){
+			    .level = perfd_cfg->dvfs_opp_khz[l] * 1000UL,
+			    .frequency = perfd_cfg->dvfs_opp_khz[l],
+			    .voltage = perfd_cfg->dvfs_opp_mv[l],
+			    .power = (uint32_t)power,
+			};
+		    }
+
+		    dvfs_elt[dvfs_index].name = perfd_cfg->name;
+		    dvfs_elt[dvfs_index].data = (void *)(dvfs_data + dvfs_index);
+
+		    /* Module psu_optee module (elements and  configuration data) */
+		    psu_optee_regu_elt[psu_index].name = perfd_cfg->rdev->reg_name;
+		    psu_optee_regu_elt[psu_index].sub_element_count = scpfw_resource_counter.psu_count;
+		    psu_optee_regu_elt[psu_index].data = (void *)(psu_optee_regu_data + psu_index);
+
+		    psu_optee_regu_data[psu_index].rdev = perfd_cfg->rdev;
+
+		    /* Module psu (elements and  configuration data) */
+		    psu_elt[psu_index].name = perfd_cfg->rdev->reg_name;
+		    psu_elt[psu_index].data = (void *)(psu_data + psu_index);
+		    psu_data[psu_index].driver_id =
+			(fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_PSU_OPTEE_REGULATOR, psu_index);
+		    psu_data[psu_index].driver_api_id =
+			(fwk_id_t)FWK_ID_API_INIT(FWK_MODULE_IDX_PSU_OPTEE_REGULATOR, 0);
+
+		    /* Module clock and optee_clock */
+		    fwk_assert(!clock_elt[clock_index].data);
+
+		    clock_elt[clock_index].name = perfd_cfg->clk->name;
+		    clock_elt[clock_index].data = (void *)(clock_data + clock_index);
+
+		    clock_data[clock_index] = (struct mod_clock_dev_config){
+			.driver_id = (fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_OPTEE_CLOCK,
+								   clock_index),
+			.api_id = (fwk_id_t)FWK_ID_API_INIT(FWK_MODULE_IDX_OPTEE_CLOCK,
+							    0),
+			.pd_source_id = FWK_ID_NONE,
+		    };
+
+		    optee_clock_cfg[clock_index].clk = perfd_cfg->clk;
+		    optee_clock_cfg[clock_index].default_enabled = false;
+
+		    optee_clock_elt[clock_index].name = perfd_cfg->clk->name;
+		    optee_clock_elt[clock_index].data = (void *)(optee_clock_cfg + clock_index);
+
+		    clock_index++;
+		    psu_index++;
+		    dvfs_index++;
+		}
+
+		scpfw_resource_counter.clock_index = clock_index;
+		scpfw_resource_counter.psu_index = psu_index;
+		scpfw_resource_counter.dvfs_index = dvfs_index;
+	    }
 #endif
-        }
+	}
     }
 }
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp1/module/psu_optee_regulator/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp1/module/psu_optee_regulator/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp1/module/psu_optee_regulator/CMakeLists.txt
@@ -8,8 +8,8 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include"
-                                  "${SCP_OPTEE_DIR}/core/arch/arm/plat-stm32mp2")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include"
+				  "${SCP_OPTEE_DIR}/core/arch/arm/plat-stm32mp2")
 
 target_sources(
     ${SCP_MODULE_TARGET} PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_psu_optee_regulator.c")
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp1/module/psu_optee_regulator/src/mod_psu_optee_regulator.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp1/module/psu_optee_regulator/src/mod_psu_optee_regulator.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp1/module/psu_optee_regulator/src/mod_psu_optee_regulator.c
@@ -46,7 +46,7 @@ static struct rdev *get_regulator(fwk_id
 
     elt_index = fwk_id_get_element_idx(id);
     if (elt_index >= module_ctx.dev_count)
-        return NULL;
+	return NULL;
 
     return module_ctx.config[elt_index].rdev;
 }
@@ -61,7 +61,7 @@ static int psu_optee_regulator_set_enabl
 
     regulator = get_regulator(id);
     if (!regulator) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     DEBUG_MSG("PSU set %s %s", regulator_name(regulator),
@@ -74,9 +74,9 @@ static int psu_optee_regulator_set_enabl
     }
 
     if (res) {
-        return FWK_E_HANDLER;
+	return FWK_E_HANDLER;
     } else {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 }
 
@@ -86,7 +86,7 @@ static int psu_optee_regulator_get_enabl
 
    regulator = get_regulator(id);
    if (!regulator || (enabled == NULL)) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     *enabled = regulator_is_enabled((const struct rdev *)regulator);
@@ -104,7 +104,7 @@ static int psu_optee_regulator_set_volta
 
     regulator = get_regulator(id);
     if (!regulator) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     DEBUG_MSG("PSU set regulator %s level: %"PRIu32"mV",
@@ -113,10 +113,10 @@ static int psu_optee_regulator_set_volta
     res = regulator_set_voltage(regulator, level_mv);
 
     if (res) {
-        return FWK_E_HANDLER;
+	return FWK_E_HANDLER;
     } else {
 
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 }
 
@@ -128,7 +128,7 @@ static int psu_optee_regulator_get_volta
 
     regulator = get_regulator(id);
     if (!regulator || (voltage == NULL)) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     res = regulator_get_voltage(regulator, &level_mv);
@@ -137,7 +137,7 @@ static int psu_optee_regulator_get_volta
 	      regulator_name(regulator), (uint32_t)level_mv, res);
 
     if (res) {
-        return FWK_E_HANDLER;
+	return FWK_E_HANDLER;
     }
 
     *voltage = level_mv;
@@ -153,8 +153,8 @@ static struct mod_psu_driver_api psu_dri
 };
 
 static int psu_optee_regulator_init(fwk_id_t module_id,
-                                    unsigned int element_count,
-                                    const void *data)
+				    unsigned int element_count,
+				    const void *data)
 {
     return FWK_SUCCESS;
 }
@@ -172,9 +172,9 @@ static int psu_optee_regulator_element_i
 }
 
 static int psu_optee_regulator_process_bind_request(fwk_id_t source_id,
-                                                    fwk_id_t target_id,
-                                                    fwk_id_t api_id,
-                                                    const void **api)
+						    fwk_id_t target_id,
+						    fwk_id_t api_id,
+						    const void **api)
 {
     *api = &psu_driver_api;
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp1/module/stm32_regu_consumer/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp1/module/stm32_regu_consumer/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp1/module/stm32_regu_consumer/CMakeLists.txt
@@ -8,8 +8,8 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include"
-                                  "${SCP_OPTEE_DIR}/core/arch/arm/plat-stm32mp2")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include"
+				  "${SCP_OPTEE_DIR}/core/arch/arm/plat-stm32mp2")
 
 target_sources(
     ${SCP_MODULE_TARGET} PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_stm32_regu_consumer.c")
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp1/module/stm32_regu_consumer/src/mod_stm32_regu_consumer.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp1/module/stm32_regu_consumer/src/mod_stm32_regu_consumer.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp1/module/stm32_regu_consumer/src/mod_stm32_regu_consumer.c
@@ -53,10 +53,10 @@ static int find_ctx(fwk_id_t dev_id,
     ctx = module_ctx.dev_ctx_table + fwk_id_get_element_idx(dev_id);
 
     if (!fwk_module_is_valid_element_id(dev_id))
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     if (!ctx->rdev)
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
 
     *out_ctx = ctx;
 
@@ -71,14 +71,14 @@ static int stm32_regu_consumer_get_confi
 
     ret = find_ctx(dev_id, &ctx);
     if (ret)
-        return ret;
+	return ret;
 
     *mode_type = MOD_VOLTD_MODE_TYPE_ARCH;
 
     if (ctx->enabled)
-        *mode_id = MOD_VOLTD_MODE_ID_ON;
+	*mode_id = MOD_VOLTD_MODE_ID_ON;
     else
-        *mode_id = MOD_VOLTD_MODE_ID_OFF;
+	*mode_id = MOD_VOLTD_MODE_ID_OFF;
 
     DEBUG_MSG("SCMI voltd %u: get config PMIC %s = %#"PRIx8", %#"PRIx8,
 	      fwk_id_get_element_idx(dev_id), regu_name(ctx->rdev), *mode_type,
@@ -95,33 +95,33 @@ static int stm32_regu_consumer_set_confi
 
     ret = find_ctx(dev_id, &ctx);
     if (ret)
-        return ret;
+	return ret;
 
     if (mode_type != MOD_VOLTD_MODE_TYPE_ARCH) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     switch (mode_id) {
     case MOD_VOLTD_MODE_ID_ON:
-        if (!ctx->enabled) {
-            if (regulator_enable(ctx->rdev))
-                return FWK_E_DEVICE;
-
-            ctx->enabled = true;
-        }
-        break;
+	if (!ctx->enabled) {
+	    if (regulator_enable(ctx->rdev))
+		return FWK_E_DEVICE;
+
+	    ctx->enabled = true;
+	}
+	break;
 
     case MOD_VOLTD_MODE_ID_OFF:
-        if (ctx->enabled) {
-            if (regulator_disable(ctx->rdev))
-                return FWK_E_DEVICE;
-
-            ctx->enabled = false;
-        }
-        break;
+	if (ctx->enabled) {
+	    if (regulator_disable(ctx->rdev))
+		return FWK_E_DEVICE;
+
+	    ctx->enabled = false;
+	}
+	break;
 
     default:
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     DEBUG_MSG("SCMI voltd %u: set config PMIC %s to type %#"PRIx8" / mode %#"PRIx8,
@@ -139,10 +139,10 @@ static int stm32_regu_consumer_get_level
 
     ret = find_ctx(dev_id, &ctx);
     if (ret)
-        return ret;
+	return ret;
 
     if (regulator_get_voltage(ctx->rdev, &level_mv))
-        return FWK_E_PANIC;
+	return FWK_E_PANIC;
 
     *level_uv = (int)level_mv * 1000;
 
@@ -158,16 +158,16 @@ static int stm32_regu_consumer_set_level
     int ret = FWK_E_PANIC;
 
     if (level_uv / 1000 > UINT16_MAX) {
-        FWK_LOG_ERR("Volatge level too high (mV shall fit in 16 bits)");
-        return FWK_E_PARAM;
+	FWK_LOG_ERR("Volatge level too high (mV shall fit in 16 bits)");
+	return FWK_E_PARAM;
     }
 
     ret = find_ctx(dev_id, &ctx);
     if (ret)
-        return ret;
+	return ret;
 
     if (regulator_set_voltage(ctx->rdev, level_uv / 1000))
-        return FWK_E_DEVICE;
+	return FWK_E_DEVICE;
 
     DEBUG_MSG("SCMI voltd %u: set level PMIC %s to %d",
 	      fwk_id_get_element_idx(dev_id), regu_name(ctx->rdev), level_uv);
@@ -205,22 +205,22 @@ static int stm32_regu_consumer_get_info(
 
     ret = find_ctx(dev_id, &ctx);
     if (ret == FWK_E_ACCESS) {
-        static const char reserved[] = "reserved";
+	static const char reserved[] = "reserved";
 
-        memset(info, 0, sizeof(*info));
-        info->name = reserved;
-        info->level_range.level_type = MOD_VOLTD_VOLTAGE_LEVEL_DISCRETE;
-        info->level_range.level_count = 1;
-        info->level_range.min_uv = 0;
-        info->level_range.max_uv = 0;
+	memset(info, 0, sizeof(*info));
+	info->name = reserved;
+	info->level_range.level_type = MOD_VOLTD_VOLTAGE_LEVEL_DISCRETE;
+	info->level_range.level_count = 1;
+	info->level_range.min_uv = 0;
+	info->level_range.max_uv = 0;
 
 	return FWK_SUCCESS;
     } else if (ret != FWK_SUCCESS) {
-        return ret;
+	return ret;
     }
 
     if (regulator_list_voltages(ctx->rdev, &levels, &full_count)) {
-        return FWK_E_SUPPORT;
+	return FWK_E_SUPPORT;
     }
 
     memset(info, 0, sizeof(*info));
@@ -248,22 +248,22 @@ static int stm32_regu_consumer_level_fro
 
     ret = find_ctx(dev_id, &ctx);
     if (ret == FWK_E_ACCESS) {
-        if (index > 0) {
-            return FWK_E_RANGE;
-        }
-        *level_uv = 0;
+	if (index > 0) {
+	    return FWK_E_RANGE;
+	}
+	*level_uv = 0;
 
 	return FWK_SUCCESS;
     } else if (ret != FWK_SUCCESS) {
-        return ret;
+	return ret;
     }
 
     if (regulator_list_voltages(ctx->rdev, &levels, &full_count)) {
-        return FWK_E_SUPPORT;
+	return FWK_E_SUPPORT;
     }
 
     if (index >= full_count)
-        return FWK_E_RANGE;
+	return FWK_E_RANGE;
 
     *level_uv = (int32_t)levels[index] * 1000;
 
@@ -293,8 +293,8 @@ static int stm32_regu_consumer_init(fwk_
     module_ctx.dev_count = element_count;
 
     if (element_count)
-        module_ctx.dev_ctx_table =
-            fwk_mm_calloc(element_count, sizeof(*module_ctx.dev_ctx_table));
+	module_ctx.dev_ctx_table =
+	    fwk_mm_calloc(element_count, sizeof(*module_ctx.dev_ctx_table));
 
     return FWK_SUCCESS;
 }
@@ -307,7 +307,7 @@ static int stm32_regu_consumer_element_i
     struct stm32_regu_consumer_dev_ctx *ctx;
 
     if (!fwk_module_is_valid_element_id(element_id))
-        return FWK_E_PANIC;
+	return FWK_E_PANIC;
 
     ctx = module_ctx.dev_ctx_table + fwk_id_get_element_idx(element_id);
 
@@ -315,10 +315,10 @@ static int stm32_regu_consumer_element_i
     ctx->enabled = dev_config->default_enabled;
 
     if (ctx->enabled) {
-        if (!ctx->rdev)
-            return FWK_E_PARAM;
-        if (regulator_enable(ctx->rdev))
-            return FWK_E_DEVICE;
+	if (!ctx->rdev)
+	    return FWK_E_PARAM;
+	if (regulator_enable(ctx->rdev))
+	    return FWK_E_DEVICE;
     }
 
     return FWK_SUCCESS;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp2/fw/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp2/fw/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp2/fw/CMakeLists.txt
@@ -16,7 +16,7 @@ add_library(${SCP_FIRMWARE_TARGET})
 target_include_directories(
     ${SCP_FIRMWARE_TARGET}
     PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/../include"
-           "${CMAKE_CURRENT_SOURCE_DIR}")
+	   "${CMAKE_CURRENT_SOURCE_DIR}")
 
 target_sources(
     ${SCP_FIRMWARE_TARGET}
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp2/fw/config_all.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp2/fw/config_all.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp2/fw/config_all.c
@@ -141,8 +141,8 @@ struct fwk_module_config config_msg_smt
 #ifdef CFG_SCPFW_MOD_SCMI_CLOCK
 struct fwk_module_config config_scmi_clock = {
     .data = &((struct mod_scmi_clock_config){
-        .agent_table = NULL,			/* Allocated during initialization */
-        .agent_count = 0,			/* Set during initialization */
+	.agent_table = NULL,			/* Allocated during initialization */
+	.agent_count = 0,			/* Set during initialization */
     }),
 };
 #endif
@@ -173,8 +173,8 @@ struct fwk_module_config config_optee_cl
 #ifdef CFG_SCPFW_MOD_RESET_DOMAIN
 struct fwk_module_config config_scmi_reset_domain = {
     .data = &((struct mod_scmi_reset_domain_config){
-        .agent_table = NULL,			/* Allocated during initialization */
-        .agent_count = 0,			/* Set during initialization */
+	.agent_table = NULL,			/* Allocated during initialization */
+	.agent_count = 0,			/* Set during initialization */
     }),
 };
 
@@ -203,8 +203,8 @@ struct fwk_module_config config_optee_re
 /* Config data for scmi_voltage_domain, voltage_domain and optee_regu modules */
 struct fwk_module_config config_scmi_voltage_domain = {
     .data = &((struct mod_scmi_voltd_config){
-        .agent_table = NULL,			/* Allocated during initialization */
-        .agent_count = 0,			/* Set during initialization */
+	.agent_table = NULL,			/* Allocated during initialization */
+	.agent_count = 0,			/* Set during initialization */
     }),
 };
 
@@ -233,11 +233,11 @@ struct fwk_module_config config_stm32_re
 /* Config data for scmi_perf and dvfs  module */
 struct fwk_module_config config_scmi_perf = {
     .data = &((struct mod_scmi_perf_config){
-        .domains = NULL,			/* Allocated during initialization */
-        .perf_doms_count = 0,			/* Set during initialization */
-        .fast_channels_alarm_id = FWK_ID_NONE_INIT,
+	.domains = NULL,			/* Allocated during initialization */
+	.perf_doms_count = 0,			/* Set during initialization */
+	.fast_channels_alarm_id = FWK_ID_NONE_INIT,
 #ifdef BUILD_HAS_MOD_STATISTICS
-        .stats_enabled = true,
+	.stats_enabled = true,
 #endif
     }),
 };
@@ -352,26 +352,26 @@ static void count_resources(struct scpfw
     size_t i, j;
 
     for (i = 0; i < cfg->agent_count; i++) {
-        struct scpfw_agent_config *agent_cfg = cfg->agent_config + i;
+	struct scpfw_agent_config *agent_cfg = cfg->agent_config + i;
 
-        scpfw_resource_counter.channel_count += agent_cfg->channel_count;
+	scpfw_resource_counter.channel_count += agent_cfg->channel_count;
 
-        for (j = 0; j < agent_cfg->channel_count; j++) {
-            struct scpfw_channel_config *channel_cfg = agent_cfg->channel_config + j;
+	for (j = 0; j < agent_cfg->channel_count; j++) {
+	    struct scpfw_channel_config *channel_cfg = agent_cfg->channel_config + j;
 
-            /* Clocks for scmi_clock and for DVFS */
-            scpfw_resource_counter.clock_count += channel_cfg->clock_count;
-            scpfw_resource_counter.clock_count += channel_cfg->perfd_count;
-            /* Reset for smci_reset only */
-            scpfw_resource_counter.reset_count += channel_cfg->reset_count;
-            /* Regulators for smci_voltage_domain only */
-            scpfw_resource_counter.regu_count += channel_cfg->voltd_count;
-            /* DVFS and PSU DVFS only */
-            scpfw_resource_counter.dvfs_count += channel_cfg->perfd_count;
-            scpfw_resource_counter.psu_count += channel_cfg->perfd_count;
-            /* Power domains */
-            scpfw_resource_counter.pd_count += channel_cfg->pd_count;
-        }
+	    /* Clocks for scmi_clock and for DVFS */
+	    scpfw_resource_counter.clock_count += channel_cfg->clock_count;
+	    scpfw_resource_counter.clock_count += channel_cfg->perfd_count;
+	    /* Reset for smci_reset only */
+	    scpfw_resource_counter.reset_count += channel_cfg->reset_count;
+	    /* Regulators for smci_voltage_domain only */
+	    scpfw_resource_counter.regu_count += channel_cfg->voltd_count;
+	    /* DVFS and PSU DVFS only */
+	    scpfw_resource_counter.dvfs_count += channel_cfg->perfd_count;
+	    scpfw_resource_counter.psu_count += channel_cfg->perfd_count;
+	    /* Power domains */
+	    scpfw_resource_counter.pd_count += channel_cfg->pd_count;
+	}
     }
 
 #ifndef CFG_SCPFW_MOD_CLOCK
@@ -409,7 +409,7 @@ static void allocate_global_resources(st
 #ifdef CFG_SCPFW_MOD_SCMI_CLOCK
     /* SCMI clock domains resources */
     scmi_clk_agent_tbl = fwk_mm_calloc(scmi_agent_count,
-                                       sizeof(*scmi_clk_agent_tbl));
+				       sizeof(*scmi_clk_agent_tbl));
     scmi_clock_config = (void *)config_scmi_clock.data;
     scmi_clock_config->agent_table = scmi_clk_agent_tbl;
     scmi_clock_config->agent_count = scmi_agent_count;
@@ -418,82 +418,82 @@ static void allocate_global_resources(st
 #ifdef CFG_SCPFW_MOD_CLOCK
     /* Clock domains resources */
     optee_clock_cfg = fwk_mm_calloc(scpfw_resource_counter.clock_count,
-                                    sizeof(*optee_clock_cfg));
+				    sizeof(*optee_clock_cfg));
     optee_clock_elt = fwk_mm_calloc(scpfw_resource_counter.clock_count + 1,
-                                    sizeof(*optee_clock_elt));
+				    sizeof(*optee_clock_elt));
 
     clock_data = fwk_mm_calloc(scpfw_resource_counter.clock_count,
-                               sizeof(*clock_data));
+			       sizeof(*clock_data));
     clock_elt = fwk_mm_calloc(scpfw_resource_counter.clock_count + 1,
-                              sizeof(*clock_elt));
+			      sizeof(*clock_elt));
 #endif
 
 #ifdef CFG_SCPFW_MOD_RESET_DOMAIN
     /* SCMI reset domains resources */
     scmi_reset_agent_tbl = fwk_mm_calloc(scmi_agent_count,
-                                         sizeof(*scmi_reset_agent_tbl));
+					 sizeof(*scmi_reset_agent_tbl));
     scmi_reset_config = (void *)config_scmi_reset_domain.data;
     scmi_reset_config->agent_table = scmi_reset_agent_tbl;
     scmi_reset_config->agent_count = scmi_agent_count;
 
     optee_reset_data = fwk_mm_calloc(scpfw_resource_counter.reset_count,
-                                     sizeof(*optee_reset_data));
+				     sizeof(*optee_reset_data));
     optee_reset_elt = fwk_mm_calloc(scpfw_resource_counter.reset_count + 1,
-                                    sizeof(*optee_reset_elt));
+				    sizeof(*optee_reset_elt));
 
     reset_data = fwk_mm_calloc(scpfw_resource_counter.reset_count,
-                               sizeof(*reset_data));
+			       sizeof(*reset_data));
     reset_elt = fwk_mm_calloc(scpfw_resource_counter.reset_count + 1,
-                              sizeof(*reset_elt));
+			      sizeof(*reset_elt));
 #endif
 
 #ifdef CFG_SCPFW_MOD_DVFS
     /* PSU and related optee PSU regulator resources */
     psu_optee_regu_elt = fwk_mm_calloc(scpfw_resource_counter.dvfs_count + 1,
-                                  sizeof(*psu_optee_regu_elt));
+				  sizeof(*psu_optee_regu_elt));
     psu_optee_regu_data = fwk_mm_calloc(scpfw_resource_counter.dvfs_count,
-                                  sizeof(*psu_optee_regu_data));
+				  sizeof(*psu_optee_regu_data));
 
     psu_elt = fwk_mm_calloc(scpfw_resource_counter.psu_count + 1,
-                            sizeof(*psu_elt));
+			    sizeof(*psu_elt));
     psu_data = fwk_mm_calloc(scpfw_resource_counter.psu_count,
-                             sizeof(*psu_data));
+			     sizeof(*psu_data));
 
     /* DVFS and SCMI performance management resources */
     dvfs_elt = fwk_mm_calloc(scpfw_resource_counter.dvfs_count + 1,
-                             sizeof(*dvfs_elt));
+			     sizeof(*dvfs_elt));
     dvfs_data = fwk_mm_calloc(scpfw_resource_counter.dvfs_count,
-                              sizeof(*dvfs_data));
+			      sizeof(*dvfs_data));
 
     scmi_perf_domain_data = fwk_mm_calloc(scpfw_resource_counter.dvfs_count,
-                                          sizeof(*scmi_perf_domain_data));
+					  sizeof(*scmi_perf_domain_data));
 #endif
 
 #ifdef CFG_SCPFW_MOD_VOLTAGE_DOMAIN
     /* SCMI voltage domains resources */
     scmi_voltd_agent_tbl = fwk_mm_calloc(scmi_agent_count,
-                                         sizeof(*scmi_voltd_agent_tbl));
+					 sizeof(*scmi_voltd_agent_tbl));
     scmi_voltd_config = (void *)config_scmi_voltage_domain.data;
     scmi_voltd_config->agent_table = scmi_voltd_agent_tbl;
     scmi_voltd_config->agent_count = scmi_agent_count;
 
     optee_regu_data = fwk_mm_calloc(scpfw_resource_counter.regu_count,
-                                    sizeof(*optee_regu_data));
+				    sizeof(*optee_regu_data));
     optee_regu_elt = fwk_mm_calloc(scpfw_resource_counter.regu_count + 1,
-                                    sizeof(*optee_regu_elt));
+				    sizeof(*optee_regu_elt));
 
     voltd_data = fwk_mm_calloc(scpfw_resource_counter.regu_count,
-                               sizeof(*voltd_data));
+			       sizeof(*voltd_data));
     voltd_elt = fwk_mm_calloc(scpfw_resource_counter.regu_count + 1,
-                              sizeof(*voltd_elt));
+			      sizeof(*voltd_elt));
 #endif
 
 #ifdef CFG_SCPFW_MOD_POWER_DOMAIN
     /* <pd_count> power domains + 1 system domain + 1 empty cell */
     pd_elt = fwk_mm_calloc(scpfw_resource_counter.pd_count + 2,
-                              sizeof(*pd_elt));
+			      sizeof(*pd_elt));
     stm32_pd_elt = fwk_mm_calloc(scpfw_resource_counter.pd_count + 1,
-                              sizeof(*stm32_pd_elt));
+			      sizeof(*stm32_pd_elt));
 #endif
 }
 
@@ -505,80 +505,80 @@ static void set_scmi_comm_resources(stru
     size_t scmi_agent_count = cfg->agent_count + 1;
 
     scmi_agent_table = fwk_mm_calloc(scmi_agent_count,
-                                     sizeof(*scmi_agent_table));
+				     sizeof(*scmi_agent_table));
 
     scmi_service_elt = fwk_mm_calloc(scpfw_resource_counter.channel_count + 1,
-                                     sizeof(*scmi_service_elt));
+				     sizeof(*scmi_service_elt));
 
     msg_smt_elt = fwk_mm_calloc(scpfw_resource_counter.channel_count + 1,
-                                sizeof(*msg_smt_elt));
+				sizeof(*msg_smt_elt));
     msg_smt_data = fwk_mm_calloc(scpfw_resource_counter.channel_count,
-                                 sizeof(*msg_smt_data));
+				 sizeof(*msg_smt_data));
 
     optee_mbx_elt = fwk_mm_calloc(scpfw_resource_counter.channel_count + 1,
-                                  sizeof(*optee_mbx_elt));
+				  sizeof(*optee_mbx_elt));
     optee_mbx_data = fwk_mm_calloc(scpfw_resource_counter.channel_count,
-                                   sizeof(*optee_mbx_data));
+				   sizeof(*optee_mbx_data));
 
     /* Set now the uniqnue scmi module instance configuration data */
     scmi_data = (struct mod_scmi_config){
-        .agent_table = scmi_agent_table,
-        .agent_count = scmi_agent_count,
-        .protocol_count_max = 9,
-        .vendor_identifier = "ST",
-        .sub_vendor_identifier = "ST",
+	.agent_table = scmi_agent_table,
+	.agent_count = scmi_agent_count,
+	.protocol_count_max = 9,
+	.vendor_identifier = "ST",
+	.sub_vendor_identifier = "ST",
     };
 
     channel_index = 0;
 
     for (i = 0; i < cfg->agent_count; i++) {
-        struct scpfw_agent_config *agent_cfg = cfg->agent_config + i;
-        size_t agent_index = i + 1;
+	struct scpfw_agent_config *agent_cfg = cfg->agent_config + i;
+	size_t agent_index = i + 1;
 
-        scmi_agent_table[agent_index].type = SCMI_AGENT_TYPE_OSPM;
-        scmi_agent_table[agent_index].name = agent_cfg->name;
+	scmi_agent_table[agent_index].type = SCMI_AGENT_TYPE_OSPM;
+	scmi_agent_table[agent_index].name = agent_cfg->name;
 
-        for (j = 0; j < agent_cfg->channel_count; j++) {
-            struct scpfw_channel_config *channel_cfg = agent_cfg->channel_config + j;
-            struct mod_scmi_service_config *service_data;
-
-            service_data = fwk_mm_calloc(1, sizeof(*service_data));
-            *service_data = (struct mod_scmi_service_config){
-                .transport_id = (fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_MSG_SMT, 0),
-                .transport_api_id = (fwk_id_t)FWK_ID_API_INIT(FWK_MODULE_IDX_MSG_SMT,
-                                                              MOD_MSG_SMT_API_IDX_SCMI_TRANSPORT),
-                .scmi_agent_id = agent_cfg->agent_id,
-                .scmi_p2a_id = FWK_ID_NONE_INIT,
-            };
-
-            /* Currently expect 1 agent with ID SCMI_AGENT_ID_NSEC0 (1) */
-            fwk_assert(service_data->scmi_agent_id == SCMI_AGENT_ID_NSEC0);
-
-            scmi_service_elt[channel_index].name = channel_cfg->name;
-            scmi_service_elt[channel_index].data = service_data;
-
-            msg_smt_elt[channel_index].name = channel_cfg->name;
-            msg_smt_elt[channel_index].data = (void *)(msg_smt_data + channel_index);
-
-            msg_smt_data[channel_index] = (struct mod_msg_smt_channel_config){
-                .type = MOD_MSG_SMT_CHANNEL_TYPE_REQUESTER,
-                .mailbox_size = 128,
-                .driver_id = (fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_OPTEE_MBX,
-                                                           channel_index),
-                .driver_api_id = (fwk_id_t)FWK_ID_API_INIT(FWK_MODULE_IDX_OPTEE_MBX, 0),
-            };
-
-            optee_mbx_elt[channel_index].name = channel_cfg->name;
-            optee_mbx_elt[channel_index].data = (void *)(optee_mbx_data + channel_index);
-
-            optee_mbx_data[channel_index] = (struct mod_optee_mbx_channel_config){
-                .driver_id = (fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_MSG_SMT, channel_index),
-                .driver_api_id = (fwk_id_t)FWK_ID_API_INIT(FWK_MODULE_IDX_MSG_SMT,
-                                                           MOD_MSG_SMT_API_IDX_DRIVER_INPUT),
-            };
+	for (j = 0; j < agent_cfg->channel_count; j++) {
+	    struct scpfw_channel_config *channel_cfg = agent_cfg->channel_config + j;
+	    struct mod_scmi_service_config *service_data;
+
+	    service_data = fwk_mm_calloc(1, sizeof(*service_data));
+	    *service_data = (struct mod_scmi_service_config){
+		.transport_id = (fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_MSG_SMT, 0),
+		.transport_api_id = (fwk_id_t)FWK_ID_API_INIT(FWK_MODULE_IDX_MSG_SMT,
+							      MOD_MSG_SMT_API_IDX_SCMI_TRANSPORT),
+		.scmi_agent_id = agent_cfg->agent_id,
+		.scmi_p2a_id = FWK_ID_NONE_INIT,
+	    };
+
+	    /* Currently expect 1 agent with ID SCMI_AGENT_ID_NSEC0 (1) */
+	    fwk_assert(service_data->scmi_agent_id == SCMI_AGENT_ID_NSEC0);
+
+	    scmi_service_elt[channel_index].name = channel_cfg->name;
+	    scmi_service_elt[channel_index].data = service_data;
+
+	    msg_smt_elt[channel_index].name = channel_cfg->name;
+	    msg_smt_elt[channel_index].data = (void *)(msg_smt_data + channel_index);
+
+	    msg_smt_data[channel_index] = (struct mod_msg_smt_channel_config){
+		.type = MOD_MSG_SMT_CHANNEL_TYPE_REQUESTER,
+		.mailbox_size = 128,
+		.driver_id = (fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_OPTEE_MBX,
+							   channel_index),
+		.driver_api_id = (fwk_id_t)FWK_ID_API_INIT(FWK_MODULE_IDX_OPTEE_MBX, 0),
+	    };
+
+	    optee_mbx_elt[channel_index].name = channel_cfg->name;
+	    optee_mbx_elt[channel_index].data = (void *)(optee_mbx_data + channel_index);
+
+	    optee_mbx_data[channel_index] = (struct mod_optee_mbx_channel_config){
+		.driver_id = (fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_MSG_SMT, channel_index),
+		.driver_api_id = (fwk_id_t)FWK_ID_API_INIT(FWK_MODULE_IDX_MSG_SMT,
+							   MOD_MSG_SMT_API_IDX_DRIVER_INPUT),
+	    };
 
-            channel_index++;
-        }
+	    channel_index++;
+	}
     }
 };
 
@@ -587,319 +587,319 @@ static void set_resources(struct scpfw_c
     size_t i, j, k, l;
 
     for (i = 0; i < cfg->agent_count; i++) {
-        struct scpfw_agent_config *agent_cfg = cfg->agent_config + i;
-        size_t agent_index = i + 1;
+	struct scpfw_agent_config *agent_cfg = cfg->agent_config + i;
+	size_t agent_index = i + 1;
 
-        if (agent_index != agent_cfg->agent_id) {
-            panic("scpfw config expects agent ID is agent index");
-        }
+	if (agent_index != agent_cfg->agent_id) {
+	    panic("scpfw config expects agent ID is agent index");
+	}
 
-        for (j = 0; j < agent_cfg->channel_count; j++) {
-            struct scpfw_channel_config *channel_cfg = agent_cfg->channel_config + j;
+	for (j = 0; j < agent_cfg->channel_count; j++) {
+	    struct scpfw_channel_config *channel_cfg = agent_cfg->channel_config + j;
 
 #ifdef CFG_SCPFW_MOD_SCMI_CLOCK
-            /* Add first SCMI clock. We will add later the clocks used for DVFS */
-            if (channel_cfg->clock_count) {
-                size_t clock_index = scpfw_resource_counter.clock_index;
-                struct mod_scmi_clock_device *dev = NULL;
-
-                /* Set SCMI clocks array for the SCMI agent */
-                dev = fwk_mm_calloc(channel_cfg->clock_count,
-                                    sizeof(struct mod_scmi_clock_device));
-
-                fwk_assert(!scmi_clk_agent_tbl[agent_index].device_table);
-                scmi_clk_agent_tbl[agent_index].device_count = channel_cfg->clock_count;
-                scmi_clk_agent_tbl[agent_index].device_table = dev;
-
-                /* Set clock and optee/clock elements and config data */
-                for (k = 0; k < channel_cfg->clock_count; k++) {
-                    struct scmi_clock *clock_cfg = channel_cfg->clock + k;
-
-                    dev[k].element_id =
-                        (fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_CLOCK, clock_index);
-
-                    optee_clock_cfg[clock_index].clk = clock_cfg->clk;
-                    optee_clock_cfg[clock_index].default_enabled = clock_cfg->enabled;
-
-                    optee_clock_elt[clock_index].name = clock_cfg->name;
-                    optee_clock_elt[clock_index].data = (void *)(optee_clock_cfg + clock_index);
-
-                    clock_data[clock_index] = (struct mod_clock_dev_config){
-                        .driver_id = (fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_OPTEE_CLOCK,
-                                                                   clock_index),
-                        .api_id = (fwk_id_t)FWK_ID_API_INIT(FWK_MODULE_IDX_OPTEE_CLOCK,
-                                                            0),
-                        .pd_source_id = FWK_ID_NONE,
-                   };
+	    /* Add first SCMI clock. We will add later the clocks used for DVFS */
+	    if (channel_cfg->clock_count) {
+		size_t clock_index = scpfw_resource_counter.clock_index;
+		struct mod_scmi_clock_device *dev = NULL;
+
+		/* Set SCMI clocks array for the SCMI agent */
+		dev = fwk_mm_calloc(channel_cfg->clock_count,
+				    sizeof(struct mod_scmi_clock_device));
+
+		fwk_assert(!scmi_clk_agent_tbl[agent_index].device_table);
+		scmi_clk_agent_tbl[agent_index].device_count = channel_cfg->clock_count;
+		scmi_clk_agent_tbl[agent_index].device_table = dev;
+
+		/* Set clock and optee/clock elements and config data */
+		for (k = 0; k < channel_cfg->clock_count; k++) {
+		    struct scmi_clock *clock_cfg = channel_cfg->clock + k;
+
+		    dev[k].element_id =
+			(fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_CLOCK, clock_index);
+
+		    optee_clock_cfg[clock_index].clk = clock_cfg->clk;
+		    optee_clock_cfg[clock_index].default_enabled = clock_cfg->enabled;
+
+		    optee_clock_elt[clock_index].name = clock_cfg->name;
+		    optee_clock_elt[clock_index].data = (void *)(optee_clock_cfg + clock_index);
+
+		    clock_data[clock_index] = (struct mod_clock_dev_config){
+			.driver_id = (fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_OPTEE_CLOCK,
+								   clock_index),
+			.api_id = (fwk_id_t)FWK_ID_API_INIT(FWK_MODULE_IDX_OPTEE_CLOCK,
+							    0),
+			.pd_source_id = FWK_ID_NONE,
+		   };
 
-                    clock_elt[clock_index].name = clock_cfg->name;
-                    clock_elt[clock_index].data = (void *)(clock_data + clock_index);
+		    clock_elt[clock_index].name = clock_cfg->name;
+		    clock_elt[clock_index].data = (void *)(clock_data + clock_index);
 
-                    clock_index++;
-                }
+		    clock_index++;
+		}
 
-                scpfw_resource_counter.clock_index = clock_index;
-            }
+		scpfw_resource_counter.clock_index = clock_index;
+	    }
 #endif
 
 #ifdef CFG_SCPFW_MOD_RESET_DOMAIN
-            if (channel_cfg->reset_count) {
-                struct mod_scmi_reset_domain_device *dev = NULL;
-                size_t reset_index = scpfw_resource_counter.reset_index;
-
-                /* Set SCMI reset domains array for the SCMI agent */
-                dev = fwk_mm_calloc(channel_cfg->reset_count, sizeof(*dev));
-
-                fwk_assert(!scmi_reset_agent_tbl[agent_index].device_table);
-                scmi_reset_agent_tbl[agent_index].agent_domain_count = channel_cfg->reset_count;
-                scmi_reset_agent_tbl[agent_index].device_table = dev;
-
-                /* Set reset_domain and optee/reset elements and config data */
-                for (k = 0; k < channel_cfg->reset_count; k++) {
-                    struct scmi_reset *reset_cfg = channel_cfg->reset + k;
-
-                    dev[k].element_id =
-                        (fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_RESET_DOMAIN,
-                                                      reset_index);
-
-                    optee_reset_data[reset_index].rstctrl = reset_cfg->rstctrl;
-
-                    optee_reset_elt[reset_index].name = reset_cfg->name;
-                    optee_reset_elt[reset_index].data =
-                        (void *)(optee_reset_data + reset_index);
-
-                    reset_data[reset_index] = (struct mod_reset_domain_dev_config){
-                        .driver_id =
-                            (fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_OPTEE_RESET,
-                                                          reset_index),
-                        .driver_api_id =
-                            (fwk_id_t)FWK_ID_API_INIT(FWK_MODULE_IDX_OPTEE_RESET, 0),
-                        .modes = MOD_RESET_DOMAIN_AUTO_RESET |
-                                 MOD_RESET_DOMAIN_MODE_EXPLICIT_ASSERT |
-                                 MOD_RESET_DOMAIN_MODE_EXPLICIT_DEASSERT,
-                    };
+	    if (channel_cfg->reset_count) {
+		struct mod_scmi_reset_domain_device *dev = NULL;
+		size_t reset_index = scpfw_resource_counter.reset_index;
+
+		/* Set SCMI reset domains array for the SCMI agent */
+		dev = fwk_mm_calloc(channel_cfg->reset_count, sizeof(*dev));
+
+		fwk_assert(!scmi_reset_agent_tbl[agent_index].device_table);
+		scmi_reset_agent_tbl[agent_index].agent_domain_count = channel_cfg->reset_count;
+		scmi_reset_agent_tbl[agent_index].device_table = dev;
+
+		/* Set reset_domain and optee/reset elements and config data */
+		for (k = 0; k < channel_cfg->reset_count; k++) {
+		    struct scmi_reset *reset_cfg = channel_cfg->reset + k;
+
+		    dev[k].element_id =
+			(fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_RESET_DOMAIN,
+						      reset_index);
+
+		    optee_reset_data[reset_index].rstctrl = reset_cfg->rstctrl;
+
+		    optee_reset_elt[reset_index].name = reset_cfg->name;
+		    optee_reset_elt[reset_index].data =
+			(void *)(optee_reset_data + reset_index);
+
+		    reset_data[reset_index] = (struct mod_reset_domain_dev_config){
+			.driver_id =
+			    (fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_OPTEE_RESET,
+							  reset_index),
+			.driver_api_id =
+			    (fwk_id_t)FWK_ID_API_INIT(FWK_MODULE_IDX_OPTEE_RESET, 0),
+			.modes = MOD_RESET_DOMAIN_AUTO_RESET |
+				 MOD_RESET_DOMAIN_MODE_EXPLICIT_ASSERT |
+				 MOD_RESET_DOMAIN_MODE_EXPLICIT_DEASSERT,
+		    };
 
-                    reset_elt[reset_index].name = reset_cfg->name;
-                    reset_elt[reset_index].data = (void *)(reset_data + reset_index);
+		    reset_elt[reset_index].name = reset_cfg->name;
+		    reset_elt[reset_index].data = (void *)(reset_data + reset_index);
 
-                    reset_index++;
-                }
+		    reset_index++;
+		}
 
-                scpfw_resource_counter.reset_index = reset_index;
-            }
+		scpfw_resource_counter.reset_index = reset_index;
+	    }
 #endif
 
 #ifdef CFG_SCPFW_MOD_VOLTAGE_DOMAIN
-            if (channel_cfg->voltd_count) {
-                size_t regu_index = scpfw_resource_counter.regu_index;
-                struct mod_scmi_voltd_device *dev = NULL;
-
-                /* Set SCMI voltage domains array for the SCMI agent */
-                dev = fwk_mm_calloc(channel_cfg->voltd_count,
-                                    sizeof(struct mod_scmi_voltd_device));
-
-                fwk_assert(!scmi_voltd_agent_tbl[agent_index].device_table);
-                scmi_voltd_agent_tbl[agent_index].domain_count = channel_cfg->voltd_count;
-                scmi_voltd_agent_tbl[agent_index].device_table = dev;
-
-                /* Set voltage_domain and optee/regu elements and config data */
-                for (k = 0; k < channel_cfg->voltd_count; k++) {
-                    struct scmi_voltd *voltd_cfg = channel_cfg->voltd + k;
-                    static const char reserved[] = "reserved";
-                    const char *name = NULL;
-
-                    if (voltd_cfg->rdev) {
-                        name = voltd_cfg->rdev->reg_name;
-                    } else {
-                        name = reserved;
-                    }
-
-                    dev[regu_index].element_id =
-                       (fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_VOLTAGE_DOMAIN, k);
-
-                    optee_regu_elt[regu_index].name = name;
-                    optee_regu_elt[regu_index].data = (void *)(optee_regu_data + regu_index);
-                    optee_regu_data[regu_index].rdev = voltd_cfg->rdev;
-                    optee_regu_data[regu_index].default_enabled = voltd_cfg->enabled;
-
-                    voltd_data[regu_index].driver_id =
-                        (fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_STM32_REGU_CONSUMER,
-                                                      regu_index);
-                    voltd_data[regu_index].api_id =
-                        (fwk_id_t)FWK_ID_API_INIT(FWK_MODULE_IDX_STM32_REGU_CONSUMER, 0);
-
-                    voltd_elt[regu_index].name = name;
-                    voltd_elt[regu_index].data = (void *)(voltd_data + regu_index);
-                    regu_index++;
-                }
+	    if (channel_cfg->voltd_count) {
+		size_t regu_index = scpfw_resource_counter.regu_index;
+		struct mod_scmi_voltd_device *dev = NULL;
+
+		/* Set SCMI voltage domains array for the SCMI agent */
+		dev = fwk_mm_calloc(channel_cfg->voltd_count,
+				    sizeof(struct mod_scmi_voltd_device));
+
+		fwk_assert(!scmi_voltd_agent_tbl[agent_index].device_table);
+		scmi_voltd_agent_tbl[agent_index].domain_count = channel_cfg->voltd_count;
+		scmi_voltd_agent_tbl[agent_index].device_table = dev;
+
+		/* Set voltage_domain and optee/regu elements and config data */
+		for (k = 0; k < channel_cfg->voltd_count; k++) {
+		    struct scmi_voltd *voltd_cfg = channel_cfg->voltd + k;
+		    static const char reserved[] = "reserved";
+		    const char *name = NULL;
+
+		    if (voltd_cfg->rdev) {
+			name = voltd_cfg->rdev->reg_name;
+		    } else {
+			name = reserved;
+		    }
+
+		    dev[regu_index].element_id =
+		       (fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_VOLTAGE_DOMAIN, k);
+
+		    optee_regu_elt[regu_index].name = name;
+		    optee_regu_elt[regu_index].data = (void *)(optee_regu_data + regu_index);
+		    optee_regu_data[regu_index].rdev = voltd_cfg->rdev;
+		    optee_regu_data[regu_index].default_enabled = voltd_cfg->enabled;
+
+		    voltd_data[regu_index].driver_id =
+			(fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_STM32_REGU_CONSUMER,
+						      regu_index);
+		    voltd_data[regu_index].api_id =
+			(fwk_id_t)FWK_ID_API_INIT(FWK_MODULE_IDX_STM32_REGU_CONSUMER, 0);
+
+		    voltd_elt[regu_index].name = name;
+		    voltd_elt[regu_index].data = (void *)(voltd_data + regu_index);
+		    regu_index++;
+		}
 
-                scpfw_resource_counter.regu_index = regu_index;
-            }
+		scpfw_resource_counter.regu_index = regu_index;
+	    }
 #endif
 
 #ifdef CFG_SCPFW_MOD_DVFS
-            if (channel_cfg->perfd_count) {
-                size_t clock_index = scpfw_resource_counter.clock_index;
-                size_t psu_index = scpfw_resource_counter.psu_index;
-                size_t dvfs_index = scpfw_resource_counter.dvfs_index;
-
-                for (k = 0; k < channel_cfg->perfd_count; k++) {
-                    struct mod_scmi_perf_config *scmi_perf_data = NULL;
-                    struct scmi_perfd *perfd_cfg = channel_cfg->perfd + k;
-
-                    /*
-                     * DVFS with SCMI performance management domains
-                     * 1 initial scmi_perf instance defines the number of DVFS's
-                     * For each DVFS instance:
-                     * - 1 instance (elt/config) of dvfs, psu, optee/psu, clock, optee/clock
-                     * Clocks and optee/clocks are already allocated but not yet set.
-                     */
-
-                    /* scmi_perf: data defines the DVFS domains indices */
-                    scmi_perf_domain_data[dvfs_index] = (struct mod_scmi_perf_domain_config){ };
-
-                    scmi_perf_data = (void *)config_scmi_perf.data;
-
-                    scmi_perf_data[dvfs_index].domains = (void *)scmi_perf_domain_data;
-                    scmi_perf_data[dvfs_index].perf_doms_count = scpfw_resource_counter.dvfs_count;
-                    scmi_perf_data[dvfs_index].fast_channels_alarm_id = (fwk_id_t)FWK_ID_NONE_INIT;
+	    if (channel_cfg->perfd_count) {
+		size_t clock_index = scpfw_resource_counter.clock_index;
+		size_t psu_index = scpfw_resource_counter.psu_index;
+		size_t dvfs_index = scpfw_resource_counter.dvfs_index;
+
+		for (k = 0; k < channel_cfg->perfd_count; k++) {
+		    struct mod_scmi_perf_config *scmi_perf_data = NULL;
+		    struct scmi_perfd *perfd_cfg = channel_cfg->perfd + k;
+
+		    /*
+		     * DVFS with SCMI performance management domains
+		     * 1 initial scmi_perf instance defines the number of DVFS's
+		     * For each DVFS instance:
+		     * - 1 instance (elt/config) of dvfs, psu, optee/psu, clock, optee/clock
+		     * Clocks and optee/clocks are already allocated but not yet set.
+		     */
+
+		    /* scmi_perf: data defines the DVFS domains indices */
+		    scmi_perf_domain_data[dvfs_index] = (struct mod_scmi_perf_domain_config){ };
+
+		    scmi_perf_data = (void *)config_scmi_perf.data;
+
+		    scmi_perf_data[dvfs_index].domains = (void *)scmi_perf_domain_data;
+		    scmi_perf_data[dvfs_index].perf_doms_count = scpfw_resource_counter.dvfs_count;
+		    scmi_perf_data[dvfs_index].fast_channels_alarm_id = (fwk_id_t)FWK_ID_NONE_INIT;
 #ifdef BUILD_HAS_MOD_STATISTICS
-                    scmi_perf_data[dvfs_index].stats_enabled = true;
+		    scmi_perf_data[dvfs_index].stats_enabled = true;
 #endif
 
-                    /* dvfs instances: 1 instance per expose DVFS service */
-                    dvfs_data[dvfs_index].psu_id =
-                        (fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_PSU, psu_index);
-                    dvfs_data[dvfs_index].clock_id =
-                        (fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_CLOCK, clock_index);
-                    dvfs_data[dvfs_index].latency = 0; /* not set, used for async access */
-                    dvfs_data[dvfs_index].sustained_idx = perfd_cfg->initial_opp;
-                    dvfs_data[dvfs_index].opps =
-                        fwk_mm_calloc(perfd_cfg->dvfs_opp_count,
-                                      sizeof(struct mod_dvfs_opp));
-
-                    for (l = 0; l < perfd_cfg->dvfs_opp_count; l++) {
-                        uint64_t power = 0;
-
-                        /* Quick estimation of power consumpsion */
-                        power = perfd_cfg->dvfs_opp_khz[l];
-                        power *= perfd_cfg->dvfs_opp_mv[l];
-                        power *= perfd_cfg->dvfs_opp_mv[l];
-                        power /= 100 * 1000 * 1000;
-
-                        dvfs_data[dvfs_index].opps[l] = (struct mod_dvfs_opp){
-                            .level = perfd_cfg->dvfs_opp_khz[l] * 1000UL,
-                            .frequency = perfd_cfg->dvfs_opp_khz[l],
-                            .voltage = perfd_cfg->dvfs_opp_mv[l],
-                            .power = (uint32_t)power,
-                        };
-                    }
-
-                    dvfs_elt[dvfs_index].name = perfd_cfg->name;
-                    dvfs_elt[dvfs_index].data = (void *)(dvfs_data + dvfs_index);
-
-                    /* Module psu_optee module (elements and  configuration data) */
-                    psu_optee_regu_elt[psu_index].name = perfd_cfg->rdev->reg_name;
-                    psu_optee_regu_elt[psu_index].sub_element_count = scpfw_resource_counter.psu_count;
-                    psu_optee_regu_elt[psu_index].data = (void *)(psu_optee_regu_data + psu_index);
-
-                    psu_optee_regu_data[psu_index].rdev = perfd_cfg->rdev;
-
-                    /* Module psu (elements and  configuration data) */
-                    psu_elt[psu_index].name = perfd_cfg->rdev->reg_name;
-                    psu_elt[psu_index].data = (void *)(psu_data + psu_index);
-                    psu_data[psu_index].driver_id =
-                        (fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_PSU_OPTEE_REGULATOR, psu_index);
-                    psu_data[psu_index].driver_api_id =
-                        (fwk_id_t)FWK_ID_API_INIT(FWK_MODULE_IDX_PSU_OPTEE_REGULATOR, 0);
-
-                    /* Module clock and optee_clock */
-                    fwk_assert(!clock_elt[clock_index].data);
-
-                    clock_elt[clock_index].name = perfd_cfg->clk->name;
-                    clock_elt[clock_index].data = (void *)(clock_data + clock_index);
-
-                    clock_data[clock_index] = (struct mod_clock_dev_config){
-                        .driver_id = (fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_OPTEE_CLOCK,
-                                                                   clock_index),
-                        .api_id = (fwk_id_t)FWK_ID_API_INIT(FWK_MODULE_IDX_OPTEE_CLOCK,
-                                                            0),
-                        .pd_source_id = FWK_ID_NONE,
-                    };
-
-                    optee_clock_cfg[clock_index].clk = perfd_cfg->clk;
-                    optee_clock_cfg[clock_index].default_enabled = false;
-
-                    optee_clock_elt[clock_index].name = perfd_cfg->clk->name;
-                    optee_clock_elt[clock_index].data = (void *)(optee_clock_cfg + clock_index);
-
-                    clock_index++;
-                    psu_index++;
-                    dvfs_index++;
-                }
-
-                scpfw_resource_counter.clock_index = clock_index;
-                scpfw_resource_counter.psu_index = psu_index;
-                scpfw_resource_counter.dvfs_index = dvfs_index;
-            }
+		    /* dvfs instances: 1 instance per expose DVFS service */
+		    dvfs_data[dvfs_index].psu_id =
+			(fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_PSU, psu_index);
+		    dvfs_data[dvfs_index].clock_id =
+			(fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_CLOCK, clock_index);
+		    dvfs_data[dvfs_index].latency = 0; /* not set, used for async access */
+		    dvfs_data[dvfs_index].sustained_idx = perfd_cfg->initial_opp;
+		    dvfs_data[dvfs_index].opps =
+			fwk_mm_calloc(perfd_cfg->dvfs_opp_count,
+				      sizeof(struct mod_dvfs_opp));
+
+		    for (l = 0; l < perfd_cfg->dvfs_opp_count; l++) {
+			uint64_t power = 0;
+
+			/* Quick estimation of power consumpsion */
+			power = perfd_cfg->dvfs_opp_khz[l];
+			power *= perfd_cfg->dvfs_opp_mv[l];
+			power *= perfd_cfg->dvfs_opp_mv[l];
+			power /= 100 * 1000 * 1000;
+
+			dvfs_data[dvfs_index].opps[l] = (struct mod_dvfs_opp){
+			    .level = perfd_cfg->dvfs_opp_khz[l] * 1000UL,
+			    .frequency = perfd_cfg->dvfs_opp_khz[l],
+			    .voltage = perfd_cfg->dvfs_opp_mv[l],
+			    .power = (uint32_t)power,
+			};
+		    }
+
+		    dvfs_elt[dvfs_index].name = perfd_cfg->name;
+		    dvfs_elt[dvfs_index].data = (void *)(dvfs_data + dvfs_index);
+
+		    /* Module psu_optee module (elements and  configuration data) */
+		    psu_optee_regu_elt[psu_index].name = perfd_cfg->rdev->reg_name;
+		    psu_optee_regu_elt[psu_index].sub_element_count = scpfw_resource_counter.psu_count;
+		    psu_optee_regu_elt[psu_index].data = (void *)(psu_optee_regu_data + psu_index);
+
+		    psu_optee_regu_data[psu_index].rdev = perfd_cfg->rdev;
+
+		    /* Module psu (elements and  configuration data) */
+		    psu_elt[psu_index].name = perfd_cfg->rdev->reg_name;
+		    psu_elt[psu_index].data = (void *)(psu_data + psu_index);
+		    psu_data[psu_index].driver_id =
+			(fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_PSU_OPTEE_REGULATOR, psu_index);
+		    psu_data[psu_index].driver_api_id =
+			(fwk_id_t)FWK_ID_API_INIT(FWK_MODULE_IDX_PSU_OPTEE_REGULATOR, 0);
+
+		    /* Module clock and optee_clock */
+		    fwk_assert(!clock_elt[clock_index].data);
+
+		    clock_elt[clock_index].name = perfd_cfg->clk->name;
+		    clock_elt[clock_index].data = (void *)(clock_data + clock_index);
+
+		    clock_data[clock_index] = (struct mod_clock_dev_config){
+			.driver_id = (fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_OPTEE_CLOCK,
+								   clock_index),
+			.api_id = (fwk_id_t)FWK_ID_API_INIT(FWK_MODULE_IDX_OPTEE_CLOCK,
+							    0),
+			.pd_source_id = FWK_ID_NONE,
+		    };
+
+		    optee_clock_cfg[clock_index].clk = perfd_cfg->clk;
+		    optee_clock_cfg[clock_index].default_enabled = false;
+
+		    optee_clock_elt[clock_index].name = perfd_cfg->clk->name;
+		    optee_clock_elt[clock_index].data = (void *)(optee_clock_cfg + clock_index);
+
+		    clock_index++;
+		    psu_index++;
+		    dvfs_index++;
+		}
+
+		scpfw_resource_counter.clock_index = clock_index;
+		scpfw_resource_counter.psu_index = psu_index;
+		scpfw_resource_counter.dvfs_index = dvfs_index;
+	    }
 #endif
 
 #ifdef CFG_SCPFW_MOD_POWER_DOMAIN
-            if (channel_cfg->pd_count) {
-                /* System domain */
-                size_t system_index = channel_cfg->pd_count; /* Last element */
-                struct fwk_element *pd_elt_sys = pd_elt + system_index;
-                struct mod_power_domain_element_config *pd_elt_data_sys =
-                    fwk_mm_calloc(1, sizeof(*pd_elt_data_sys));
-                struct fwk_element *stm32_pd_elt_sys = stm32_pd_elt + system_index;
-
-                /* Data are unused but framework doesn't accept NULL data */
-                stm32_pd_elt_sys->name = "system";
-                stm32_pd_elt_sys->data = (void *)1;
-
-                pd_elt_sys->name = stm32_pd_elt_sys->name;
-
-                pd_elt_data_sys->parent_idx = PD_STATIC_DEV_IDX_NONE;
-                pd_elt_data_sys->driver_id =
-                    (fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_STM32_PD, system_index);
-                pd_elt_data_sys->api_id =
-                    (fwk_id_t)FWK_ID_API_INIT(FWK_MODULE_IDX_STM32_PD, 0);
-                pd_elt_data_sys->attributes.pd_type = MOD_PD_TYPE_DEVICE;
-                pd_elt_data_sys->allowed_state_mask_table =
-                    pd_allowed_state_mask_table;
-                pd_elt_data_sys->allowed_state_mask_table_size =
-                    FWK_ARRAY_SIZE(pd_allowed_state_mask_table);
-                pd_elt_sys->data = pd_elt_data_sys;
-
-                /* Power domains */
-                for (k = 0; k < channel_cfg->pd_count; k++) {
-                    struct fwk_element *pd_elt_k = pd_elt + k;
-                    struct mod_power_domain_element_config *pd_elt_data_k =
-                        fwk_mm_calloc(1, sizeof(*pd_elt_data_k));
-                    struct fwk_element *stm32_pd_elt_k =
-                        stm32_pd_elt + k;
-                    struct scmi_pd *scmi_pd = channel_cfg->pd + k;
-
-                    stm32_pd_elt_k->name = scmi_pd->name;
-                    stm32_pd_elt_k->data = (void *)scmi_pd;
-
-                    pd_elt_k->name = scmi_pd->name;
-
-                    pd_elt_data_k->parent_idx = system_index;
-                    pd_elt_data_k->driver_id =
-                        (fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_STM32_PD, k);
-                    pd_elt_data_k->api_id =
-                        (fwk_id_t)FWK_ID_API_INIT(FWK_MODULE_IDX_STM32_PD, 0);
-                    pd_elt_data_k->attributes.pd_type = MOD_PD_TYPE_DEVICE;
-                    pd_elt_data_k->allowed_state_mask_table =
-                        pd_allowed_state_mask_table;
-                    pd_elt_data_k->allowed_state_mask_table_size =
-                        FWK_ARRAY_SIZE(pd_allowed_state_mask_table);
-                    pd_elt_k->data = pd_elt_data_k;
-                }
-            }
+	    if (channel_cfg->pd_count) {
+		/* System domain */
+		size_t system_index = channel_cfg->pd_count; /* Last element */
+		struct fwk_element *pd_elt_sys = pd_elt + system_index;
+		struct mod_power_domain_element_config *pd_elt_data_sys =
+		    fwk_mm_calloc(1, sizeof(*pd_elt_data_sys));
+		struct fwk_element *stm32_pd_elt_sys = stm32_pd_elt + system_index;
+
+		/* Data are unused but framework doesn't accept NULL data */
+		stm32_pd_elt_sys->name = "system";
+		stm32_pd_elt_sys->data = (void *)1;
+
+		pd_elt_sys->name = stm32_pd_elt_sys->name;
+
+		pd_elt_data_sys->parent_idx = PD_STATIC_DEV_IDX_NONE;
+		pd_elt_data_sys->driver_id =
+		    (fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_STM32_PD, system_index);
+		pd_elt_data_sys->api_id =
+		    (fwk_id_t)FWK_ID_API_INIT(FWK_MODULE_IDX_STM32_PD, 0);
+		pd_elt_data_sys->attributes.pd_type = MOD_PD_TYPE_DEVICE;
+		pd_elt_data_sys->allowed_state_mask_table =
+		    pd_allowed_state_mask_table;
+		pd_elt_data_sys->allowed_state_mask_table_size =
+		    FWK_ARRAY_SIZE(pd_allowed_state_mask_table);
+		pd_elt_sys->data = pd_elt_data_sys;
+
+		/* Power domains */
+		for (k = 0; k < channel_cfg->pd_count; k++) {
+		    struct fwk_element *pd_elt_k = pd_elt + k;
+		    struct mod_power_domain_element_config *pd_elt_data_k =
+			fwk_mm_calloc(1, sizeof(*pd_elt_data_k));
+		    struct fwk_element *stm32_pd_elt_k =
+			stm32_pd_elt + k;
+		    struct scmi_pd *scmi_pd = channel_cfg->pd + k;
+
+		    stm32_pd_elt_k->name = scmi_pd->name;
+		    stm32_pd_elt_k->data = (void *)scmi_pd;
+
+		    pd_elt_k->name = scmi_pd->name;
+
+		    pd_elt_data_k->parent_idx = system_index;
+		    pd_elt_data_k->driver_id =
+			(fwk_id_t)FWK_ID_ELEMENT_INIT(FWK_MODULE_IDX_STM32_PD, k);
+		    pd_elt_data_k->api_id =
+			(fwk_id_t)FWK_ID_API_INIT(FWK_MODULE_IDX_STM32_PD, 0);
+		    pd_elt_data_k->attributes.pd_type = MOD_PD_TYPE_DEVICE;
+		    pd_elt_data_k->allowed_state_mask_table =
+			pd_allowed_state_mask_table;
+		    pd_elt_data_k->allowed_state_mask_table_size =
+			FWK_ARRAY_SIZE(pd_allowed_state_mask_table);
+		    pd_elt_k->data = pd_elt_data_k;
+		}
+	    }
 #endif
-        }
+	}
     }
 }
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp2/module/psu_optee_regulator/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp2/module/psu_optee_regulator/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp2/module/psu_optee_regulator/CMakeLists.txt
@@ -8,8 +8,8 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include"
-                                  "${SCP_OPTEE_DIR}/core/arch/arm/plat-stm32mp2")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include"
+				  "${SCP_OPTEE_DIR}/core/arch/arm/plat-stm32mp2")
 
 target_sources(
     ${SCP_MODULE_TARGET} PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_psu_optee_regulator.c")
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp2/module/psu_optee_regulator/src/mod_psu_optee_regulator.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp2/module/psu_optee_regulator/src/mod_psu_optee_regulator.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp2/module/psu_optee_regulator/src/mod_psu_optee_regulator.c
@@ -35,7 +35,7 @@ static struct psu_optee_regulator_ctx mo
 static char __maybe_unused *regulator_name(struct rdev *rdev)
 {
     if (rdev)
-        return (char *)rdev->reg_name;
+	return (char *)rdev->reg_name;
 
     return NULL;
 }
@@ -46,7 +46,7 @@ static struct rdev *get_regulator(fwk_id
 
     elt_index = fwk_id_get_element_idx(id);
     if (elt_index >= module_ctx.dev_count)
-        return NULL;
+	return NULL;
 
     return module_ctx.config[elt_index].rdev;
 }
@@ -61,22 +61,22 @@ static int psu_optee_regulator_set_enabl
 
     regulator = get_regulator(id);
     if (!regulator) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     DEBUG_MSG("PSU set %s %s", regulator_name(regulator),
-          enabled ? "ON" : "OFF");
+	  enabled ? "ON" : "OFF");
 
     if (enabled) {
-        res = regulator_enable(regulator);
+	res = regulator_enable(regulator);
     } else {
-        res = regulator_disable(regulator);
+	res = regulator_disable(regulator);
     }
 
     if (res) {
-        return FWK_E_HANDLER;
+	return FWK_E_HANDLER;
     } else {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 }
 
@@ -86,12 +86,12 @@ static int psu_optee_regulator_get_enabl
 
    regulator = get_regulator(id);
    if (!regulator || (enabled == NULL)) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     *enabled = regulator_is_enabled((const struct rdev *)regulator);
     DEBUG_MSG("PSU get %s state: %s", regulator_name(regulator),
-              enabled ? "ON" : "OFF");
+	      enabled ? "ON" : "OFF");
 
     return FWK_SUCCESS;
 }
@@ -104,19 +104,19 @@ static int psu_optee_regulator_set_volta
 
     regulator = get_regulator(id);
     if (!regulator) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     DEBUG_MSG("PSU set regulator %s level: %"PRIu32"mV",
-              regulator_name(regulator), (uint32_t)level_mv);
+	      regulator_name(regulator), (uint32_t)level_mv);
 
     res = regulator_set_voltage(regulator, level_mv);
 
     if (res) {
-        return FWK_E_HANDLER;
+	return FWK_E_HANDLER;
     } else {
 
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 }
 
@@ -128,16 +128,16 @@ static int psu_optee_regulator_get_volta
 
     regulator = get_regulator(id);
     if (!regulator || (voltage == NULL)) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     res = regulator_get_voltage(regulator, &level_mv);
 
     DEBUG_MSG("PSU get regulator %s level: %"PRIu32"mV (res %#"PRIx32")",
-              regulator_name(regulator), (uint32_t)level_mv, res);
+	      regulator_name(regulator), (uint32_t)level_mv, res);
 
     if (res) {
-        return FWK_E_HANDLER;
+	return FWK_E_HANDLER;
     }
 
     *voltage = level_mv;
@@ -153,15 +153,15 @@ static struct mod_psu_driver_api psu_dri
 };
 
 static int psu_optee_regulator_init(fwk_id_t module_id,
-                                    unsigned int element_count,
-                                    const void *data)
+				    unsigned int element_count,
+				    const void *data)
 {
     return FWK_SUCCESS;
 }
 
 static int psu_optee_regulator_element_init(fwk_id_t element_id,
-                                            unsigned int sub_element_count,
-                                            const void *data)
+					    unsigned int sub_element_count,
+					    const void *data)
 {
     fwk_assert(data != NULL);
 
@@ -172,9 +172,9 @@ static int psu_optee_regulator_element_i
 }
 
 static int psu_optee_regulator_process_bind_request(fwk_id_t source_id,
-                                                    fwk_id_t target_id,
-                                                    fwk_id_t api_id,
-                                                    const void **api)
+						    fwk_id_t target_id,
+						    fwk_id_t api_id,
+						    const void **api)
 {
     *api = &psu_driver_api;
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp2/module/stm32_pd/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp2/module/stm32_pd/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp2/module/stm32_pd/CMakeLists.txt
@@ -8,8 +8,8 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include"
-                                  "${SCP_OPTEE_DIR}/core/arch/arm/plat-stm32mp2")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include"
+				  "${SCP_OPTEE_DIR}/core/arch/arm/plat-stm32mp2")
 
 target_sources(
     ${SCP_MODULE_TARGET} PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_stm32_pd.c")
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp2/module/stm32_pd/src/mod_stm32_pd.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp2/module/stm32_pd/src/mod_stm32_pd.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp2/module/stm32_pd/src/mod_stm32_pd.c
@@ -41,7 +41,7 @@ static int stm32_pd_set_state(fwk_id_t p
 static int stm32_pd_reset(fwk_id_t pd_id)
 {
     FWK_LOG_DEBUG("Power domain %s reset not supported",
-                  fwk_module_get_element_name(pd_id));
+		  fwk_module_get_element_name(pd_id));
     return FWK_E_SUPPORT;
 }
 
@@ -57,42 +57,42 @@ static int stm32_pd_set_state_gpu(fwk_id
     fwk_assert(fwk_id_get_element_idx(pd_id) == PD_SCMI_GPU);
 
     switch (state) {
-        case MOD_PD_STATE_ON:
-            if (regu != NULL && regulator_enable(regu) != TEE_SUCCESS) {
-                return FWK_E_DEVICE;
-            }
-            if (clk != NULL && clk_enable(clk) != TEE_SUCCESS) {
-                if (regu != NULL)
-                    regulator_disable(regu);
-                return FWK_E_DEVICE;
-            }
-            break;
-        case MOD_PD_STATE_OFF:
-            if (clk != NULL) {
-                clk_disable(clk);
-            }
-            if (regu != NULL && regulator_disable(regu) != TEE_SUCCESS) {
-                if (clk != NULL) {
-                    clk_enable(clk);
-                }
-                return FWK_E_DEVICE;
-            }
-            break;
-        default:
-            FWK_LOG_ERR("State %d not supported on power domain %s",
-                        state,
-                        fwk_module_get_element_name(pd_id));
-            return FWK_E_SUPPORT;
+	case MOD_PD_STATE_ON:
+	    if (regu != NULL && regulator_enable(regu) != TEE_SUCCESS) {
+		return FWK_E_DEVICE;
+	    }
+	    if (clk != NULL && clk_enable(clk) != TEE_SUCCESS) {
+		if (regu != NULL)
+		    regulator_disable(regu);
+		return FWK_E_DEVICE;
+	    }
+	    break;
+	case MOD_PD_STATE_OFF:
+	    if (clk != NULL) {
+		clk_disable(clk);
+	    }
+	    if (regu != NULL && regulator_disable(regu) != TEE_SUCCESS) {
+		if (clk != NULL) {
+		    clk_enable(clk);
+		}
+		return FWK_E_DEVICE;
+	    }
+	    break;
+	default:
+	    FWK_LOG_ERR("State %d not supported on power domain %s",
+			state,
+			fwk_module_get_element_name(pd_id));
+	    return FWK_E_SUPPORT;
     }
 
     status = ctx->pd_driver_input_api->report_power_state_transition(
-        ctx->requester_id,
-        state);
+	ctx->requester_id,
+	state);
     assert(status == FWK_SUCCESS);
 
     FWK_LOG_DEBUG("Power domain %s change state to %s",
-                  fwk_module_get_element_name(pd_id),
-                  state == MOD_PD_STATE_ON ? "ON" : "OFF");
+		  fwk_module_get_element_name(pd_id),
+		  state == MOD_PD_STATE_ON ? "ON" : "OFF");
 
     ctx->current_state = state;
     return FWK_SUCCESS;
@@ -104,48 +104,48 @@ static int stm32_pd_bind(fwk_id_t id, un
 
     /* Nothing to do for the module */
     if (fwk_module_is_valid_module_id(id)) {
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     }
 
     /* Element id is not valid */
     if (!fwk_module_is_valid_element_id(id)) {
-        FWK_LOG_ERR("Cannot bind %s, element id is needed",
-                    fwk_module_get_element_name(id));
-        return FWK_E_SUPPORT;
+	FWK_LOG_ERR("Cannot bind %s, element id is needed",
+		    fwk_module_get_element_name(id));
+	return FWK_E_SUPPORT;
     }
 
     ctx = mod_stm32_pd_ctx + fwk_id_get_element_idx(id);
 
     /* stm32_pd_process_bind_request probably isn't been called right now */
     if (!fwk_module_is_valid_element_id(ctx->requester_id)) {
-        FWK_LOG_ERR("stm32_pd_process_bind_request hasn't been called yet");
-        return FWK_E_ACCESS;
+	FWK_LOG_ERR("stm32_pd_process_bind_request hasn't been called yet");
+	return FWK_E_ACCESS;
     }
 
     /* Only get an input API from power domain framework */
     if (fwk_id_get_module_idx(ctx->requester_id) !=
-            FWK_MODULE_IDX_POWER_DOMAIN) {
-        FWK_LOG_ERR("Requester is not power domain module");
-        return FWK_E_SUPPORT;
+	    FWK_MODULE_IDX_POWER_DOMAIN) {
+	FWK_LOG_ERR("Requester is not power domain module");
+	return FWK_E_SUPPORT;
     }
 
     return fwk_module_bind(ctx->requester_id,
-                           mod_pd_api_id_driver_input,
-                           &ctx->pd_driver_input_api);
+			   mod_pd_api_id_driver_input,
+			   &ctx->pd_driver_input_api);
 }
 
 static int stm32_pd_element_init(fwk_id_t element_id,
-                                 unsigned int unused,
-                                 const void *data)
+				 unsigned int unused,
+				 const void *data)
 {
     struct mod_stm32_pd *ctx =
-                        mod_stm32_pd_ctx + fwk_id_get_element_idx(element_id);
+			mod_stm32_pd_ctx + fwk_id_get_element_idx(element_id);
 
     ctx->config = (const struct scmi_pd *)data;
 
     if (fwk_id_get_element_idx(element_id) == PD_SCMI_GPU) {
-        ctx->current_state = MOD_PD_STATE_OFF;
-        return FWK_SUCCESS;
+	ctx->current_state = MOD_PD_STATE_OFF;
+	return FWK_SUCCESS;
     }
 
     ctx->current_state = MOD_PD_STATE_ON;
@@ -165,9 +165,9 @@ static const struct mod_pd_driver_api st
 };
 
 static int stm32_pd_process_bind_request(fwk_id_t requester_id,
-                                         fwk_id_t target_id,
-                                         fwk_id_t api_type,
-                                         const void **api)
+					 fwk_id_t target_id,
+					 fwk_id_t api_type,
+					 const void **api)
 {
     struct mod_stm32_pd *ctx = NULL;
 
@@ -175,8 +175,8 @@ static int stm32_pd_process_bind_request
     ctx->requester_id = requester_id;
 
     if (fwk_id_get_element_idx(target_id) == PD_SCMI_GPU) {
-        *api = &stm32_pd_api_gpu;
-        return FWK_SUCCESS;
+	*api = &stm32_pd_api_gpu;
+	return FWK_SUCCESS;
     }
 
     *api = &stm32_pd_api;
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp2/module/stm32_regu_consumer/CMakeLists.txt
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp2/module/stm32_regu_consumer/CMakeLists.txt
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp2/module/stm32_regu_consumer/CMakeLists.txt
@@ -8,8 +8,8 @@
 add_library(${SCP_MODULE_TARGET} SCP_MODULE)
 
 target_include_directories(${SCP_MODULE_TARGET}
-                           PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include"
-                                  "${SCP_OPTEE_DIR}/core/arch/arm/plat-stm32mp2")
+			   PUBLIC "${CMAKE_CURRENT_SOURCE_DIR}/include"
+				  "${SCP_OPTEE_DIR}/core/arch/arm/plat-stm32mp2")
 
 target_sources(
     ${SCP_MODULE_TARGET} PRIVATE "${CMAKE_CURRENT_SOURCE_DIR}/src/mod_stm32_regu_consumer.c")
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp2/module/stm32_regu_consumer/include/mod_stm32_regu_consumer.h
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp2/module/stm32_regu_consumer/include/mod_stm32_regu_consumer.h
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp2/module/stm32_regu_consumer/include/mod_stm32_regu_consumer.h
@@ -32,6 +32,6 @@ struct mod_stm32_regu_consumer_dev_confi
  * @plat_count - Number of elements in platform data
  */
 void scmi_server_build_optee_regu_config(struct scmi_server_regu_channel *plat,
-                                         size_t plat_count);
+					 size_t plat_count);
 
 #endif /* MOD_STM32_REGU_CONSUMER_H */
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp2/module/stm32_regu_consumer/src/mod_stm32_regu_consumer.c
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp2/module/stm32_regu_consumer/src/mod_stm32_regu_consumer.c
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/product/optee-stm32mp2/module/stm32_regu_consumer/src/mod_stm32_regu_consumer.c
@@ -40,23 +40,23 @@ static struct stm32_regu_consumer_ctx mo
 static char *regu_name(struct rdev *rdev)
 {
     if (rdev)
-        return (char *)rdev->reg_name;
+	return (char *)rdev->reg_name;
 
     return NULL;
 }
 
 static int find_ctx(fwk_id_t dev_id,
-                    struct stm32_regu_consumer_dev_ctx **out_ctx)
+		    struct stm32_regu_consumer_dev_ctx **out_ctx)
 {
     struct stm32_regu_consumer_dev_ctx *ctx;
 
     ctx = module_ctx.dev_ctx_table + fwk_id_get_element_idx(dev_id);
 
     if (!fwk_module_is_valid_element_id(dev_id))
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
 
     if (!ctx->rdev)
-        return FWK_E_ACCESS;
+	return FWK_E_ACCESS;
 
     *out_ctx = ctx;
 
@@ -64,69 +64,69 @@ static int find_ctx(fwk_id_t dev_id,
 }
 
 static int stm32_regu_consumer_get_config(fwk_id_t dev_id, uint8_t *mode_type,
-                                          uint8_t *mode_id)
+					  uint8_t *mode_id)
 {
     struct stm32_regu_consumer_dev_ctx *ctx;
     int ret;
 
     ret = find_ctx(dev_id, &ctx);
     if (ret)
-        return ret;
+	return ret;
 
     *mode_type = MOD_VOLTD_MODE_TYPE_ARCH;
 
     if (ctx->enabled)
-        *mode_id = MOD_VOLTD_MODE_ID_ON;
+	*mode_id = MOD_VOLTD_MODE_ID_ON;
     else
-        *mode_id = MOD_VOLTD_MODE_ID_OFF;
+	*mode_id = MOD_VOLTD_MODE_ID_OFF;
 
     DEBUG_MSG("SCMI voltd %u: get config PMIC %s = %#"PRIx8", %#"PRIx8,
-              fwk_id_get_element_idx(dev_id), regu_name(ctx->rdev), *mode_type,
-              *mode_id);
+	      fwk_id_get_element_idx(dev_id), regu_name(ctx->rdev), *mode_type,
+	      *mode_id);
 
     return FWK_SUCCESS;
 }
 
 static int stm32_regu_consumer_set_config(fwk_id_t dev_id, uint8_t mode_type,
-                                          uint8_t mode_id)
+					  uint8_t mode_id)
 {
     struct stm32_regu_consumer_dev_ctx *ctx;
     int ret;
 
     ret = find_ctx(dev_id, &ctx);
     if (ret)
-        return ret;
+	return ret;
 
     if (mode_type != MOD_VOLTD_MODE_TYPE_ARCH) {
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     switch (mode_id) {
     case MOD_VOLTD_MODE_ID_ON:
-        if (!ctx->enabled) {
-            if (regulator_enable(ctx->rdev))
-                return FWK_E_DEVICE;
-
-            ctx->enabled = true;
-        }
-        break;
+	if (!ctx->enabled) {
+	    if (regulator_enable(ctx->rdev))
+		return FWK_E_DEVICE;
+
+	    ctx->enabled = true;
+	}
+	break;
 
     case MOD_VOLTD_MODE_ID_OFF:
-        if (ctx->enabled) {
-            if (regulator_disable(ctx->rdev))
-                return FWK_E_DEVICE;
-
-            ctx->enabled = false;
-        }
-        break;
+	if (ctx->enabled) {
+	    if (regulator_disable(ctx->rdev))
+		return FWK_E_DEVICE;
+
+	    ctx->enabled = false;
+	}
+	break;
 
     default:
-        return FWK_E_PARAM;
+	return FWK_E_PARAM;
     }
 
     DEBUG_MSG("SCMI voltd %u: set config PMIC %s to type %#"PRIx8" / mode %#"PRIx8,
-              fwk_id_get_element_idx(dev_id), regu_name(ctx->rdev), mode_type,
-              mode_id);
+	      fwk_id_get_element_idx(dev_id), regu_name(ctx->rdev), mode_type,
+	      mode_id);
 
     return FWK_SUCCESS;
 }
@@ -139,15 +139,15 @@ static int stm32_regu_consumer_get_level
 
     ret = find_ctx(dev_id, &ctx);
     if (ret)
-        return ret;
+	return ret;
 
     if (regulator_get_voltage(ctx->rdev, &level_mv))
-        return FWK_E_PANIC;
+	return FWK_E_PANIC;
 
     *level_uv = (int)level_mv * 1000;
 
     DEBUG_MSG("SCMI voltd %u: get level PMIC %s = %d",
-              fwk_id_get_element_idx(dev_id), regu_name(ctx->rdev), *level_uv);
+	      fwk_id_get_element_idx(dev_id), regu_name(ctx->rdev), *level_uv);
 
     return FWK_SUCCESS;
 }
@@ -158,25 +158,25 @@ static int stm32_regu_consumer_set_level
     int ret = FWK_E_PANIC;
 
     if (level_uv / 1000 > UINT16_MAX) {
-        FWK_LOG_ERR("Volatge level too high (mV shall fit in 16 bits)");
-        return FWK_E_PARAM;
+	FWK_LOG_ERR("Volatge level too high (mV shall fit in 16 bits)");
+	return FWK_E_PARAM;
     }
 
     ret = find_ctx(dev_id, &ctx);
     if (ret)
-        return ret;
+	return ret;
 
     if (regulator_set_voltage(ctx->rdev, level_uv / 1000))
-        return FWK_E_DEVICE;
+	return FWK_E_DEVICE;
 
     DEBUG_MSG("SCMI voltd %u: set level PMIC %s to %d",
-              fwk_id_get_element_idx(dev_id), regu_name(ctx->rdev), level_uv);
+	      fwk_id_get_element_idx(dev_id), regu_name(ctx->rdev), level_uv);
 
     return FWK_SUCCESS;
 }
 
 static void find_bound_uv(const uint16_t *levels, size_t count,
-                          int32_t *min, int32_t *max)
+			  int32_t *min, int32_t *max)
 {
     size_t n = 0;
 
@@ -184,12 +184,12 @@ static void find_bound_uv(const uint16_t
     *max = INT32_MIN;
 
     for (n = 0; n < count; n++) {
-        if (*min > levels[n]) {
-            *min = levels[n];
-        }
-        if (*max < levels[n]) {
-            *max = levels[n];
-        }
+	if (*min > levels[n]) {
+	    *min = levels[n];
+	}
+	if (*max < levels[n]) {
+	    *max = levels[n];
+	}
     }
 
     /* Convert from mV to uV */
@@ -198,7 +198,7 @@ static void find_bound_uv(const uint16_t
 }
 
 static int stm32_regu_consumer_get_info(fwk_id_t dev_id,
-                                        struct mod_voltd_info *info)
+					struct mod_voltd_info *info)
 {
     struct stm32_regu_consumer_dev_ctx *ctx;
     uint16_t *levels;
@@ -207,22 +207,22 @@ static int stm32_regu_consumer_get_info(
 
     ret = find_ctx(dev_id, &ctx);
     if (ret == FWK_E_ACCESS) {
-        static const char reserved[] = "reserved";
+	static const char reserved[] = "reserved";
 
-        memset(info, 0, sizeof(*info));
-        info->name = reserved;
-        info->level_range.level_type = MOD_VOLTD_VOLTAGE_LEVEL_DISCRETE;
-        info->level_range.level_count = 1;
-        info->level_range.min_uv = 0;
-        info->level_range.max_uv = 0;
+	memset(info, 0, sizeof(*info));
+	info->name = reserved;
+	info->level_range.level_type = MOD_VOLTD_VOLTAGE_LEVEL_DISCRETE;
+	info->level_range.level_count = 1;
+	info->level_range.min_uv = 0;
+	info->level_range.max_uv = 0;
 
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     } else if (ret != FWK_SUCCESS) {
-        return ret;
+	return ret;
     }
 
     if (regulator_list_voltages(ctx->rdev, &levels, &full_count)) {
-        return FWK_E_SUPPORT;
+	return FWK_E_SUPPORT;
     }
 
     memset(info, 0, sizeof(*info));
@@ -230,18 +230,18 @@ static int stm32_regu_consumer_get_info(
     info->level_range.level_type = MOD_VOLTD_VOLTAGE_LEVEL_DISCRETE;
     info->level_range.level_count = full_count;
     find_bound_uv(levels, full_count,
-                  &info->level_range.min_uv, &info->level_range.max_uv);
+		  &info->level_range.min_uv, &info->level_range.max_uv);
 
     DEBUG_MSG("SCMI voltd %u: get_info PMIC %s, range [%d %d]",
-              fwk_id_get_element_idx(dev_id), regu_name(ctx->rdev),
-              info->level_range.min_uv, info->level_range.max_uv);
+	      fwk_id_get_element_idx(dev_id), regu_name(ctx->rdev),
+	      info->level_range.min_uv, info->level_range.max_uv);
 
     return FWK_SUCCESS;
 }
 
 static int stm32_regu_consumer_level_from_index(fwk_id_t dev_id,
-                                                unsigned int index,
-                                                int32_t *level_uv)
+						unsigned int index,
+						int32_t *level_uv)
 {
     struct stm32_regu_consumer_dev_ctx *ctx;
     uint16_t *levels;
@@ -250,27 +250,27 @@ static int stm32_regu_consumer_level_fro
 
     ret = find_ctx(dev_id, &ctx);
     if (ret == FWK_E_ACCESS) {
-        if (index > 0) {
-            return FWK_E_RANGE;
-        }
-        *level_uv = 0;
+	if (index > 0) {
+	    return FWK_E_RANGE;
+	}
+	*level_uv = 0;
 
-        return FWK_SUCCESS;
+	return FWK_SUCCESS;
     } else if (ret != FWK_SUCCESS) {
-        return ret;
+	return ret;
     }
 
     if (regulator_list_voltages(ctx->rdev, &levels, &full_count)) {
-        return FWK_E_SUPPORT;
+	return FWK_E_SUPPORT;
     }
 
     if (index >= full_count)
-        return FWK_E_RANGE;
+	return FWK_E_RANGE;
 
     *level_uv = (int32_t)levels[index] * 1000;
 
     DEBUG_MSG("SCMI voltd %u: get level PMIC %s = %"PRId32,
-              fwk_id_get_element_idx(dev_id), regu_name(ctx->rdev), *level_uv);
+	      fwk_id_get_element_idx(dev_id), regu_name(ctx->rdev), *level_uv);
 
     return FWK_SUCCESS;
 }
@@ -289,27 +289,27 @@ static const struct mod_voltd_drv_api ap
  */
 
 static int stm32_regu_consumer_init(fwk_id_t module_id,
-                                    unsigned int element_count,
-                                    const void *data)
+				    unsigned int element_count,
+				    const void *data)
 {
     module_ctx.dev_count = element_count;
 
     if (element_count)
-        module_ctx.dev_ctx_table =
-            fwk_mm_calloc(element_count, sizeof(*module_ctx.dev_ctx_table));
+	module_ctx.dev_ctx_table =
+	    fwk_mm_calloc(element_count, sizeof(*module_ctx.dev_ctx_table));
 
     return FWK_SUCCESS;
 }
 
 static int stm32_regu_consumer_element_init(fwk_id_t element_id,
-                                            unsigned int unused,
-                                            const void *data)
+					    unsigned int unused,
+					    const void *data)
 {
     const struct mod_stm32_regu_consumer_dev_config *dev_config = data;
     struct stm32_regu_consumer_dev_ctx *ctx;
 
     if (!fwk_module_is_valid_element_id(element_id))
-        return FWK_E_PANIC;
+	return FWK_E_PANIC;
 
     ctx = module_ctx.dev_ctx_table + fwk_id_get_element_idx(element_id);
 
@@ -317,19 +317,19 @@ static int stm32_regu_consumer_element_i
     ctx->enabled = dev_config->default_enabled;
 
     if (ctx->enabled) {
-        if (!ctx->rdev)
-            return FWK_E_PARAM;
-        if (regulator_enable(ctx->rdev))
-            return FWK_E_DEVICE;
+	if (!ctx->rdev)
+	    return FWK_E_PARAM;
+	if (regulator_enable(ctx->rdev))
+	    return FWK_E_DEVICE;
     }
 
     return FWK_SUCCESS;
 }
 
 static int stm32_regu_consumer_process_bind_request(fwk_id_t requester_id,
-                                                    fwk_id_t target_id,
-                                                    fwk_id_t api_type,
-                                                    const void **api)
+						    fwk_id_t target_id,
+						    fwk_id_t api_type,
+						    const void **api)
 {
     *api = &api_optee_regu;
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/tools/build_string.py
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/tools/build_string.py
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/tools/build_string.py
@@ -13,14 +13,14 @@ from datetime import datetime
 def main():
     # Check if git is available and that the source is within a git repository
     try:
-        cmd = 'git describe --tags --dirty --always'
-        output = subprocess.check_output(cmd.split())
-        git_string = output.decode('utf-8').strip()
+	cmd = 'git describe --tags --dirty --always'
+	output = subprocess.check_output(cmd.split())
+	git_string = output.decode('utf-8').strip()
     except BaseException:
-        git_string = 'Unknown'
+	git_string = 'Unknown'
 
     print("{}-{}".format(datetime.now().strftime('%Y-%m-%d_%H-%M-%S'),
-          git_string))
+	  git_string))
 
 
 if __name__ == '__main__':
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/tools/check_api.py
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/tools/check_api.py
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/tools/check_api.py
@@ -62,8 +62,8 @@ FILE_TYPES = [
 
 def is_valid_type(filename):
     for file_type in FILE_TYPES:
-        if fnmatch.fnmatch(filename, file_type):
-            return True
+	if fnmatch.fnmatch(filename, file_type):
+	    return True
 
     return False
 
@@ -74,52 +74,52 @@ def main(argv=[], prog_name=''):
     illegal_use = 0
 
     with open(BANNED_LIST) as file:
-        for fname in file:
-            if fname[0] == '#':
-                continue
-            BANNED_API.append(fname.rstrip())
+	for fname in file:
+	    if fname[0] == '#':
+		continue
+	    BANNED_API.append(fname.rstrip())
 
-        print("\tBanned API list: {}".format(BANNED_API))
+	print("\tBanned API list: {}".format(BANNED_API))
 
     cwd = os.getcwd()
 
     for i, directory in enumerate(EXCLUDE_DIRECTORIES):
-        EXCLUDE_DIRECTORIES[i] = os.path.abspath(directory)
-        print("\tAdding to the exclude list: {}"
-              .format(EXCLUDE_DIRECTORIES[i]))
+	EXCLUDE_DIRECTORIES[i] = os.path.abspath(directory)
+	print("\tAdding to the exclude list: {}"
+	      .format(EXCLUDE_DIRECTORIES[i]))
 
     for root, dirs, files in os.walk(cwd, topdown=True):
-        #
-        # Exclude directories based on the EXCLUDE_DIRECTORIES pattern list
-        #
-        dirs[:] = [d for d in dirs
-                   if os.path.join(root, d) not in EXCLUDE_DIRECTORIES]
-
-        #
-        # Exclude files based on the EXCLUDE pattern list
-        #
-        matches = list()
-        for filename in files:
-            for file_pattern in EXCLUDE:
-                if fnmatch.fnmatch(filename, file_pattern):
-                    matches.append(filename)
-                    break
-
-        for match in matches:
-            files.remove(match)
-
-        #
-        # Check files
-        #
-        for filename in files:
-            path = os.path.join(root, filename)
-            content = ''
-            with open(path, encoding="utf-8") as file:
-                for line, string in enumerate(file):
-                    for fname in BANNED_API:
-                        if fname in string:
-                            print("{}:{}:{}".format(path, line + 1, fname),
-                                  file=sys.stderr)
+	#
+	# Exclude directories based on the EXCLUDE_DIRECTORIES pattern list
+	#
+	dirs[:] = [d for d in dirs
+		   if os.path.join(root, d) not in EXCLUDE_DIRECTORIES]
+
+	#
+	# Exclude files based on the EXCLUDE pattern list
+	#
+	matches = list()
+	for filename in files:
+	    for file_pattern in EXCLUDE:
+		if fnmatch.fnmatch(filename, file_pattern):
+		    matches.append(filename)
+		    break
+
+	for match in matches:
+	    files.remove(match)
+
+	#
+	# Check files
+	#
+	for filename in files:
+	    path = os.path.join(root, filename)
+	    content = ''
+	    with open(path, encoding="utf-8") as file:
+		for line, string in enumerate(file):
+		    for fname in BANNED_API:
+			if fname in string:
+			    print("{}:{}:{}".format(path, line + 1, fname),
+				  file=sys.stderr)
 
 
 if __name__ == '__main__':
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/tools/check_copyright.py
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/tools/check_copyright.py
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/tools/check_copyright.py
@@ -113,39 +113,39 @@ class ErrorYearNotCurrent(Exception):
 
 def is_valid_directory(filename):
     for dir in EXCLUDE_DIRECTORIES:
-        if filename.startswith(dir):
-            return False
+	if filename.startswith(dir):
+	    return False
     return True
 
 
 def is_valid_file_type(filename):
     for file_type in FILE_TYPES:
-        if fnmatch.fnmatch(filename, file_type):
-            return True
+	if fnmatch.fnmatch(filename, file_type):
+	    return True
     return False
 
 
 def check_copyright(pattern, filename):
     with open(filename, encoding="utf-8") as file:
-        # Read just the first HEAD_LINE_COUNT lines of a file
-        head_lines = islice(file, HEAD_LINE_COUNT)
-        head = ''
-        for line in head_lines:
-            head += line
-
-        match = pattern.search(head)
-        if not match:
-            raise ErrorCopyright
-
-        years = match.group('years').split('-')
-        if len(years) > 1:
-            if years[0] > years[1]:
-                raise ErrorYear
-
-        now = datetime.datetime.now()
-        final_year = len(years) - 1
-        if int(years[final_year]) != now.year:
-            raise ErrorYearNotCurrent
+	# Read just the first HEAD_LINE_COUNT lines of a file
+	head_lines = islice(file, HEAD_LINE_COUNT)
+	head = ''
+	for line in head_lines:
+	    head += line
+
+	match = pattern.search(head)
+	if not match:
+	    raise ErrorCopyright
+
+	years = match.group('years').split('-')
+	if len(years) > 1:
+	    if years[0] > years[1]:
+		raise ErrorYear
+
+	now = datetime.datetime.now()
+	final_year = len(years) - 1
+	if int(years[final_year]) != now.year:
+	    raise ErrorYearNotCurrent
 
 
 def main():
@@ -160,47 +160,47 @@ def main():
     print("Executing from {}".format(cwd))
 
     try:
-        result = subprocess.Popen(
-            GIT_CMD,
-            shell=True,
-            stdout=subprocess.PIPE,
-            stderr=subprocess.PIPE)
+	result = subprocess.Popen(
+	    GIT_CMD,
+	    shell=True,
+	    stdout=subprocess.PIPE,
+	    stderr=subprocess.PIPE)
     except subprocess.CalledProcessError as e:
-        print("ERROR " + e.returncode + ": Failed to get last changed files")
-        return 1
+	print("ERROR " + e.returncode + ": Failed to get last changed files")
+	return 1
 
     for line in result.stdout:
-        filename = line.decode("utf-8").strip('\n')
+	filename = line.decode("utf-8").strip('\n')
 
-        if is_valid_file_type(filename) and is_valid_directory(filename):
-            try:
-                check_copyright(pattern, filename)
-            except ErrorYear:
-                print("{}: Invalid year format.".format(filename))
-                error_year_count += 1
-
-            except ErrorCopyright:
-                print("{}: Invalid copyright header.".format(filename))
-                error_copyright_count += 1
-
-            except ErrorYearNotCurrent:
-                print("{}: Outdated copyright year range.".
-                      format(filename))
-                error_incorrect_year_count += 1
+	if is_valid_file_type(filename) and is_valid_directory(filename):
+	    try:
+		check_copyright(pattern, filename)
+	    except ErrorYear:
+		print("{}: Invalid year format.".format(filename))
+		error_year_count += 1
+
+	    except ErrorCopyright:
+		print("{}: Invalid copyright header.".format(filename))
+		error_copyright_count += 1
+
+	    except ErrorYearNotCurrent:
+		print("{}: Outdated copyright year range.".
+		      format(filename))
+		error_incorrect_year_count += 1
 
     if error_year_count != 0 or error_copyright_count != 0 or \
        error_incorrect_year_count != 0:
-        print("\t{} files with invalid year(s) format."
-              .format(error_year_count))
-        print("\t{} files with invalid copyright."
-              .format(error_copyright_count))
-        print("\t{} files with incorrect year ranges."
-              .format(error_incorrect_year_count))
+	print("\t{} files with invalid year(s) format."
+	      .format(error_year_count))
+	print("\t{} files with invalid copyright."
+	      .format(error_copyright_count))
+	print("\t{} files with incorrect year ranges."
+	      .format(error_incorrect_year_count))
 
-        return 1
+	return 1
     else:
-        print("Check copyright - No errors found.")
-        return 0
+	print("Check copyright - No errors found.")
+	return 0
 
 
 if __name__ == "__main__":
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/tools/check_doc.py
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/tools/check_doc.py
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/tools/check_doc.py
@@ -20,16 +20,16 @@ def main():
     print("Checking for undocumented code...")
 
     result = subprocess.Popen(
-        "make -f Makefile.cmake doc",
-        shell=True,
-        stdout=subprocess.DEVNULL,
-        stderr=subprocess.PIPE)
+	"make -f Makefile.cmake doc",
+	shell=True,
+	stdout=subprocess.DEVNULL,
+	stderr=subprocess.PIPE)
 
     (stdout, stderr) = result.communicate()
 
     if stderr:
-        print(stderr.decode())
-        return 1
+	print(stderr.decode())
+	return 1
 
     print("The codebase is fully documented.")
     return 0
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/tools/check_framework.py
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/tools/check_framework.py
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/tools/check_framework.py
@@ -25,18 +25,18 @@ def main():
     banner("Build and run framework tests")
 
     result = subprocess.Popen(
-        "CC=gcc make -f Makefile.cmake fwk_test",
-        shell=True,
-        stdout=subprocess.PIPE,
-        stderr=subprocess.PIPE)
+	"CC=gcc make -f Makefile.cmake fwk_test",
+	shell=True,
+	stdout=subprocess.PIPE,
+	stderr=subprocess.PIPE)
 
     (stdout, stderr) = result.communicate()
 
     print(stdout.decode())
 
     if stderr:
-        print(stderr.decode())
-        return 1
+	print(stderr.decode())
+	return 1
 
     return 0
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/tools/check_module_utest.py
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/tools/check_module_utest.py
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/tools/check_module_utest.py
@@ -25,18 +25,18 @@ def main():
     banner("Build and run modules' unit tests")
 
     result = subprocess.Popen(
-        "CC=gcc make -f Makefile.cmake mod_test",
-        shell=True,
-        stdout=subprocess.PIPE,
-        stderr=subprocess.PIPE)
+	"CC=gcc make -f Makefile.cmake mod_test",
+	shell=True,
+	stdout=subprocess.PIPE,
+	stderr=subprocess.PIPE)
 
     (stdout, stderr) = result.communicate()
 
     print(stdout.decode())
 
     if stderr:
-        print(stderr.decode())
-        return 1
+	print(stderr.decode())
+	return 1
 
     return 0
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/tools/check_pycodestyle.py
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/tools/check_pycodestyle.py
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/tools/check_pycodestyle.py
@@ -26,19 +26,19 @@ def main():
     banner("Build and run PyCodeStyle tests")
 
     result = subprocess.Popen(
-        "python -m pycodestyle {}".format(INCLUDE_DIRECTORIES),
-        shell=True,
-        stdout=subprocess.PIPE,
-        stderr=subprocess.PIPE)
+	"python -m pycodestyle {}".format(INCLUDE_DIRECTORIES),
+	shell=True,
+	stdout=subprocess.PIPE,
+	stderr=subprocess.PIPE)
 
     (stdout, stderr) = result.communicate()
 
     print(stdout.decode())
 
     if result.returncode != 0:
-        print(stderr.decode())
-        print('FAILED')
-        return 1
+	print(stderr.decode())
+	print('FAILED')
+	return 1
     print('SUCCESS')
     return 0
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/tools/check_spacing.py
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/tools/check_spacing.py
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/tools/check_spacing.py
@@ -73,28 +73,28 @@ FILE_TYPES = [
 
 def is_valid_type(filename):
     for file_type in FILE_TYPES:
-        if fnmatch.fnmatch(filename, file_type):
-            return True
+	if fnmatch.fnmatch(filename, file_type):
+	    return True
     return False
 
 
 def main(argv=[], prog_name=''):
     parser = argparse.ArgumentParser(prog=prog_name)
     parser.add_argument('-t', '--trim',
-                        help='Remove trailing spaces.',
-                        action='store_true',
-                        default=False)
+			help='Remove trailing spaces.',
+			action='store_true',
+			default=False)
     parser.add_argument('-c', '--correct',
-                        help='Correct spaces after keywords.',
-                        action='store_true',
-                        default=False)
+			help='Correct spaces after keywords.',
+			action='store_true',
+			default=False)
     args = parser.parse_args(argv)
 
     print('Checking for incorrect spacing in the code...')
     if args.trim:
-        print("Trim mode is enabled.")
+	print("Trim mode is enabled.")
     if args.correct:
-        print("Correct mode is enabled.")
+	print("Correct mode is enabled.")
 
     regex_patterns = dict.fromkeys(KEYWORDS, 0)
 
@@ -109,153 +109,153 @@ def main(argv=[], prog_name=''):
     print("Executing from {}".format(cwd))
 
     for i, directory in enumerate(EXCLUDE_DIRECTORIES):
-        EXCLUDE_DIRECTORIES[i] = os.path.abspath(directory)
-        print("\tAdding to the exclude list: {}"
-              .format(EXCLUDE_DIRECTORIES[i]))
+	EXCLUDE_DIRECTORIES[i] = os.path.abspath(directory)
+	print("\tAdding to the exclude list: {}"
+	      .format(EXCLUDE_DIRECTORIES[i]))
 
     for root, dirs, files in os.walk(cwd, topdown=True):
-        #
-        # Exclude directories based on the EXCLUDE_DIRECTORIES pattern list
-        #
-        dirs[:] = [d for d in dirs
-                   if os.path.join(root, d) not in EXCLUDE_DIRECTORIES]
-
-        #
-        # Exclude files based on the EXCLUDE pattern list
-        #
-        matches = list()
-        for filename in files:
-            for file_pattern in EXCLUDE:
-                if fnmatch.fnmatch(filename, file_pattern):
-                    matches.append(filename)
-                    break
-        for match in matches:
-            files.remove(match)
-
-        for keyword in KEYWORDS:
-            regex_patterns[keyword] = re.compile(
-                '(.*\\W)(%s)(\\s*)(\\(.*)' % keyword)
-        #
-        # Check files
-        #
-        for filename in files:
-            path = os.path.join(root, filename)
-            content = ''
-            trailing_spaces = 0
-            trailing_lines = 0
-            incorrect_spaces = 0
-
-            with open(path, encoding="utf-8") as file:
-                lines = file.readlines()
-                last_line_number = len(lines)-1
-                for line, string in enumerate(lines):
-                    if line == last_line_number and string[-1] != '\n':
-                        print('{} is missing a new line at the end of file'.
-                              format(path))
-                        missing_new_lines_files += 1
-
-                    # Note that all newlines are converted to '\n',
-                    # so the following will work regardless of
-                    # what the underlying file format is using to
-                    # represent a line break.
-                    if string.endswith(' \n'):
-                        print('{}:{} has trailing space'.format(line, path))
-                        trailing_spaces += 1
-                        if args.trim:
-                            string = string.rstrip()+'\n'
-                    if not is_valid_type(os.path.basename(path)):
-                        content += string
-                        continue
-                    for keyword in KEYWORDS:
-                        key_index = string.find(keyword)
-                        if key_index == -1:
-                            continue
-                        m = regex_patterns[keyword].search(string)
-                        if m and m.group(3) != ' ':
-                            incorrect_spaces += 1
-                            print('Abnormal spacing. "{}", {}:{} --> {}'
-                                  .format(keyword, path, line,
-                                          string.rstrip()))
-                            if args.correct:
-                                string = m.group(1) + m.group(2) + ' ' + \
-                                         m.group(4) + '\n'
-                    content += string
-
-                if content.endswith('\n\n'):
-                    print('Blank line at the end of file --> {}'.format(path))
-                    c_len = len(content)
-                    if args.trim:
-                        content = content.rstrip()+'\n'
-                    trailing_lines += c_len - len(content.rstrip() + '\n')
-                #
-                # If file.newlines has been set it is either a string with
-                # the determined line ending or a tuple with all the line
-                # endings we have encountered
-                #
-                if file.newlines:
-                    if isinstance(file.newlines, tuple):
-                        print('{} has mixed line endings'.format(path))
-                        non_unix_eol_files += 1
-                    elif file.newlines != '\n':
-                        print('{} has non-UNIX line endings'.format(path))
-                        non_unix_eol_files += 1
-
-            #
-            # Trim and/or correct file, depending on the provided arguments
-            #
-            write_file = False
-            if args.trim and (trailing_spaces or trailing_lines) != 0:
-                print("Trimming {}...".format(path))
-                write_file = True
-            if args.correct and incorrect_spaces != 0:
-                print("Correcting {}...".format(path))
-                write_file = True
-            if write_file:
-                modified_files += 1
-                with open(path, 'w') as file:
-                    file.write(content)
-
-            trailing_spaces_count += trailing_spaces
-            trailing_lines_count += trailing_lines
-            correct_spaces_count += incorrect_spaces
+	#
+	# Exclude directories based on the EXCLUDE_DIRECTORIES pattern list
+	#
+	dirs[:] = [d for d in dirs
+		   if os.path.join(root, d) not in EXCLUDE_DIRECTORIES]
+
+	#
+	# Exclude files based on the EXCLUDE pattern list
+	#
+	matches = list()
+	for filename in files:
+	    for file_pattern in EXCLUDE:
+		if fnmatch.fnmatch(filename, file_pattern):
+		    matches.append(filename)
+		    break
+	for match in matches:
+	    files.remove(match)
+
+	for keyword in KEYWORDS:
+	    regex_patterns[keyword] = re.compile(
+		'(.*\\W)(%s)(\\s*)(\\(.*)' % keyword)
+	#
+	# Check files
+	#
+	for filename in files:
+	    path = os.path.join(root, filename)
+	    content = ''
+	    trailing_spaces = 0
+	    trailing_lines = 0
+	    incorrect_spaces = 0
+
+	    with open(path, encoding="utf-8") as file:
+		lines = file.readlines()
+		last_line_number = len(lines)-1
+		for line, string in enumerate(lines):
+		    if line == last_line_number and string[-1] != '\n':
+			print('{} is missing a new line at the end of file'.
+			      format(path))
+			missing_new_lines_files += 1
+
+		    # Note that all newlines are converted to '\n',
+		    # so the following will work regardless of
+		    # what the underlying file format is using to
+		    # represent a line break.
+		    if string.endswith(' \n'):
+			print('{}:{} has trailing space'.format(line, path))
+			trailing_spaces += 1
+			if args.trim:
+			    string = string.rstrip()+'\n'
+		    if not is_valid_type(os.path.basename(path)):
+			content += string
+			continue
+		    for keyword in KEYWORDS:
+			key_index = string.find(keyword)
+			if key_index == -1:
+			    continue
+			m = regex_patterns[keyword].search(string)
+			if m and m.group(3) != ' ':
+			    incorrect_spaces += 1
+			    print('Abnormal spacing. "{}", {}:{} --> {}'
+				  .format(keyword, path, line,
+					  string.rstrip()))
+			    if args.correct:
+				string = m.group(1) + m.group(2) + ' ' + \
+					 m.group(4) + '\n'
+		    content += string
+
+		if content.endswith('\n\n'):
+		    print('Blank line at the end of file --> {}'.format(path))
+		    c_len = len(content)
+		    if args.trim:
+			content = content.rstrip()+'\n'
+		    trailing_lines += c_len - len(content.rstrip() + '\n')
+		#
+		# If file.newlines has been set it is either a string with
+		# the determined line ending or a tuple with all the line
+		# endings we have encountered
+		#
+		if file.newlines:
+		    if isinstance(file.newlines, tuple):
+			print('{} has mixed line endings'.format(path))
+			non_unix_eol_files += 1
+		    elif file.newlines != '\n':
+			print('{} has non-UNIX line endings'.format(path))
+			non_unix_eol_files += 1
+
+	    #
+	    # Trim and/or correct file, depending on the provided arguments
+	    #
+	    write_file = False
+	    if args.trim and (trailing_spaces or trailing_lines) != 0:
+		print("Trimming {}...".format(path))
+		write_file = True
+	    if args.correct and incorrect_spaces != 0:
+		print("Correcting {}...".format(path))
+		write_file = True
+	    if write_file:
+		modified_files += 1
+		with open(path, 'w') as file:
+		    file.write(content)
+
+	    trailing_spaces_count += trailing_spaces
+	    trailing_lines_count += trailing_lines
+	    correct_spaces_count += incorrect_spaces
 
     if trailing_spaces_count == 0:
-        print("No trailing spaces found")
+	print("No trailing spaces found")
     else:
-        print('{} trailing spaces found.'.format(trailing_spaces_count))
+	print('{} trailing spaces found.'.format(trailing_spaces_count))
 
     if trailing_lines_count == 0:
-        print("No trailing lines found")
+	print("No trailing lines found")
     else:
-        print('{} trailing lines found.'.format(trailing_lines_count))
+	print('{} trailing lines found.'.format(trailing_lines_count))
 
     if correct_spaces_count == 0:
-        print("No abnormal spaces found")
+	print("No abnormal spaces found")
     else:
-        print('Abnormal spaces found on {} lines.'
-              .format(correct_spaces_count))
+	print('Abnormal spaces found on {} lines.'
+	      .format(correct_spaces_count))
 
     if (args.trim or args.correct) and modified_files:
-        print("{} files modified".format(modified_files))
+	print("{} files modified".format(modified_files))
 
     if non_unix_eol_files == 0:
-        print("No files with non-UNIX or mixed line endings found")
+	print("No files with non-UNIX or mixed line endings found")
     else:
-        print("{} files have non-UNIX or mixed line endings"
-              .format(non_unix_eol_files))
+	print("{} files have non-UNIX or mixed line endings"
+	      .format(non_unix_eol_files))
 
     if missing_new_lines_files == 0:
-        print("No files with missing newlines at EOF found")
+	print("No files with missing newlines at EOF found")
     else:
-        print("{} text files are missing newlines at EOF"
-              .format(missing_new_lines_files))
+	print("{} text files are missing newlines at EOF"
+	      .format(missing_new_lines_files))
 
     if (trailing_spaces_count or
-            trailing_lines_count or
-            correct_spaces_count or
-            non_unix_eol_files or
-            missing_new_lines_files):
-        return 1
+	    trailing_lines_count or
+	    correct_spaces_count or
+	    non_unix_eol_files or
+	    missing_new_lines_files):
+	return 1
     return 0
 
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/tools/check_style.py
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/tools/check_style.py
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/tools/check_style.py
@@ -76,9 +76,9 @@ def check_file(checkpatch_params, filena
     cmd = '{} {}'.format(checkpatch_params, filename)
 
     try:
-        subprocess.check_call(cmd, shell=True, stdin=0)
+	subprocess.check_call(cmd, shell=True, stdin=0)
     except subprocess.CalledProcessError:
-        error_count += 1
+	error_count += 1
 
 
 def main(argv=[], prog_name=''):
@@ -90,79 +90,79 @@ def main(argv=[], prog_name=''):
 
     # Optional parameters
     parser.add_argument('-s', '--spacing', action='store_true',
-                        help='Check for correct use of spaces',
-                        required=False)
+			help='Check for correct use of spaces',
+			required=False)
 
     parser.add_argument('-l', '--line-length', action='store_true',
-                        dest='length',
-                        help='Check for lines longer than 80 characters',
-                        required=False)
+			dest='length',
+			help='Check for lines longer than 80 characters',
+			required=False)
 
     parser.add_argument('-i', '--initializers', action='store_true',
-                        help='Check for redundant variable initialization',
-                        required=False)
+			help='Check for redundant variable initialization',
+			required=False)
 
     parser.add_argument('-m', '--input-mode', choices=input_mode_list,
-                        help='Input mode for the content to be checked. '
-                             'Default: %(default)s',
-                        required=False, default=input_mode_list[0])
+			help='Input mode for the content to be checked. '
+			     'Default: %(default)s',
+			required=False, default=input_mode_list[0])
 
     parser.add_argument('-p', '--path', action='store', dest='path',
-                        help='Path to checkpatch.pl file. If not specified, '
-                             'the script will be found on the environment '
-                             'path.',
-                        required=False)
+			help='Path to checkpatch.pl file. If not specified, '
+			     'the script will be found on the environment '
+			     'path.',
+			required=False)
 
     args = parser.parse_args(argv)
 
     # Override path to checkpatch.pl if necessary
     if args.path:
-        script_path = args.path
+	script_path = args.path
 
     # Print the path to checkpatch.pl as confirmation
     print('checkpatch.pl path:', script_path, '\n')
 
     # Enable optional tests
     if not args.spacing:
-        IGNORED_TYPES.extend(['SPACING', 'MISSING_SPACE', 'BRACKET_SPACE'])
+	IGNORED_TYPES.extend(['SPACING', 'MISSING_SPACE', 'BRACKET_SPACE'])
 
     if not args.length:
-        IGNORED_TYPES.extend(['LONG_LINE', 'LONG_LINE_COMMENT',
-                              'LONG_LINE_STRING'])
+	IGNORED_TYPES.extend(['LONG_LINE', 'LONG_LINE_COMMENT',
+			      'LONG_LINE_STRING'])
     if not args.initializers:
-        IGNORED_TYPES.extend(['GLOBAL_INITIALISERS', 'INITIALISED_STATIC'])
+	IGNORED_TYPES.extend(['GLOBAL_INITIALISERS', 'INITIALISED_STATIC'])
 
     ignore_list = '--ignore ' + (','.join(map(str, IGNORED_TYPES)))
 
     checkpatch_params = '{} --show-types --no-tree --no-summary {}'.format(
-        script_path,
-        ignore_list,
+	script_path,
+	ignore_list,
     )
 
     if args.input_mode == 'project':
-        print("Checking the coding style of the whole project...")
-        checkpatch_params += ' --terse --file'
-        for directory in DIRECTORIES:
-            for root, dirs, files in os.walk(directory):
-                for file in files:
-                    filename = os.path.join(root, file)
-                    if is_valid_file_type(file):
-                        check_file(checkpatch_params, filename)
-        if error_count > 0:
-            print('{} files contained coding style errors.'.
-                  format(error_count))
+	print("Checking the coding style of the whole project...")
+	checkpatch_params += ' --terse --file'
+	for directory in DIRECTORIES:
+	    for root, dirs, files in os.walk(directory):
+		for file in files:
+		    filename = os.path.join(root, file)
+		    if is_valid_file_type(file):
+			check_file(checkpatch_params, filename)
+	if error_count > 0:
+	    print('{} files contained coding style errors.'.
+		  format(error_count))
 
     elif args.input_mode == 'stdin':
-        print("Checking content via standard input...")
-        check_file(checkpatch_params, '-')
+	print("Checking content via standard input...")
+	check_file(checkpatch_params, '-')
 
     else:
-        print('FAILED: Invalid input mode')
-        return 1
+	print('FAILED: Invalid input mode')
+	return 1
 
     if error_count > 0:
-        print('FAILED: One or more files contained coding style errors.')
-        return 1
+	print('FAILED: One or more files contained coding style errors.')
+	return 1
 
     print('PASSED: No files contained coding style errors.')
     return 0
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/tools/check_tabs.py
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/tools/check_tabs.py
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/tools/check_tabs.py
@@ -59,30 +59,30 @@ EXCLUDE = [
 def convert(path):
     print("\tConverting all tabs in %s into spaces..." % path)
     try:
-        file, temp_file = tempfile.mkstemp(prefix='tabs_to_spaces_')
-        print("Using %s" % temp_file)
-        subprocess.check_call('expand -t4 %s > %s' % (path, temp_file),
-                              shell=True)
-        shutil.copyfile(temp_file, path)
+	file, temp_file = tempfile.mkstemp(prefix='tabs_to_spaces_')
+	print("Using %s" % temp_file)
+	subprocess.check_call('expand -t4 %s > %s' % (path, temp_file),
+			      shell=True)
+	shutil.copyfile(temp_file, path)
     except Exception as e:
-        print("Error: Failed to convert file %s with %s" % (path, e))
-        sys.exit(1)
+	print("Error: Failed to convert file %s with %s" % (path, e))
+	sys.exit(1)
     finally:
-        if os.path.exists(temp_file):
-            os.remove(temp_file)
+	if os.path.exists(temp_file):
+	    os.remove(temp_file)
 
 
 def main(argv=[], prog_name=''):
     parser = argparse.ArgumentParser(prog=prog_name)
     parser.add_argument('-c', '--convert',
-                        help='Convert tabs to 4 spaces.',
-                        action='store_true',
-                        default=False)
+			help='Convert tabs to 4 spaces.',
+			action='store_true',
+			default=False)
     args = parser.parse_args(argv)
 
     print('Checking the presence of tabs in the code...')
     if args.convert:
-        print("Conversion mode is enabled.")
+	print("Conversion mode is enabled.")
 
     tabs_found_count = 0
 
@@ -97,54 +97,54 @@ def main(argv=[], prog_name=''):
     print("Executing from %s" % cwd)
 
     for i, directory in enumerate(EXCLUDE_DIRECTORIES):
-        EXCLUDE_DIRECTORIES[i] = os.path.abspath(directory)
-        print("\tAdding to the exclude list: %s" % EXCLUDE_DIRECTORIES[i])
+	EXCLUDE_DIRECTORIES[i] = os.path.abspath(directory)
+	print("\tAdding to the exclude list: %s" % EXCLUDE_DIRECTORIES[i])
 
     for root, dirs, files in os.walk(cwd, topdown=True):
-        #
-        # Exclude directories based on the EXCLUDE_DIRECTORIES pattern list
-        #
-        dirs[:] = [d for d in dirs
-                   if os.path.join(root, d) not in EXCLUDE_DIRECTORIES]
-
-        #
-        # Exclude files based on the EXCLUDE pattern list and the files
-        # Git ignores.
-        #
-        matches = list()
-
-        files = [f for f in files
-                 if os.path.join(root, f) not in git_ignores]
-
-        for filename in files:
-            for file_pattern in (EXCLUDE + git_ignores):
-                if fnmatch.fnmatch(filename, file_pattern):
-                    matches.append(filename)
-                    break
-        for match in matches:
-            files.remove(match)
-
-        #
-        # Check files
-        #
-        for filename in files:
-            path = os.path.join(root, filename)
-            print("processing %s" % filename)
-            with open(path, encoding="utf-8") as file:
-                for line, string in enumerate(file):
-                    if '\t' in string:
-                        print('%d:%s has tab' % (line, path))
-                        tabs_found_count += 1
-                        if args.convert:
-                            convert(path)
-                            break
+	#
+	# Exclude directories based on the EXCLUDE_DIRECTORIES pattern list
+	#
+	dirs[:] = [d for d in dirs
+		   if os.path.join(root, d) not in EXCLUDE_DIRECTORIES]
+
+	#
+	# Exclude files based on the EXCLUDE pattern list and the files
+	# Git ignores.
+	#
+	matches = list()
+
+	files = [f for f in files
+		 if os.path.join(root, f) not in git_ignores]
+
+	for filename in files:
+	    for file_pattern in (EXCLUDE + git_ignores):
+		if fnmatch.fnmatch(filename, file_pattern):
+		    matches.append(filename)
+		    break
+	for match in matches:
+	    files.remove(match)
+
+	#
+	# Check files
+	#
+	for filename in files:
+	    path = os.path.join(root, filename)
+	    print("processing %s" % filename)
+	    with open(path, encoding="utf-8") as file:
+		for line, string in enumerate(file):
+		    if '\t' in string:
+			print('%d:%s has tab' % (line, path))
+			tabs_found_count += 1
+			if args.convert:
+			    convert(path)
+			    break
 
     if tabs_found_count == 0:
-        print("No tabs found")
-        return 0
+	print("No tabs found")
+	return 0
     else:
-        print('%d tab(s) found.' % tabs_found_count)
-        return 1
+	print('%d tab(s) found.' % tabs_found_count)
+	return 1
 
 
 if __name__ == '__main__':
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/tools/ci_cmake.py
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/tools/ci_cmake.py
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/tools/ci_cmake.py
@@ -50,7 +50,7 @@ products = [
     Product('tc1'),
     Product('rcar', toolchains=[Parameter('GNU')]),
     Product('rdn2', variants=[
-        Parameter('0'), Parameter('1'), Parameter('2'), Parameter('3')]),
+	Parameter('0'), Parameter('1'), Parameter('2'), Parameter('3')]),
     Product('rdfremont'),
     Product('tc2', variants=[Parameter('0'), Parameter('1')]),
 ]
@@ -68,15 +68,15 @@ def dockerize(client):
     env = {"ARMLMD_LICENSE_FILE": os.environ["ARMLMD_LICENSE_FILE"]}
 
     volumes = {
-        os.getcwd(): {
-            "bind": "/scp-firmware",
-            "mode": "rw",
-        }
+	os.getcwd(): {
+	    "bind": "/scp-firmware",
+	    "mode": "rw",
+	}
     }
 
     container = client.containers.run(
-        image.id, __file__, environment=env, volumes=volumes, detach=True,
-        tty=True
+	image.id, __file__, environment=env, volumes=volumes, detach=True,
+	tty=True
     )
 
     sigint_handler = signal.getsignal(signal.SIGINT)
@@ -84,7 +84,7 @@ def dockerize(client):
 
     logs = container.attach(stdout=True, stderr=True, stream=True, logs=True)
     for log in logs:
-        sys.stdout.buffer.write(log)
+	sys.stdout.buffer.write(log)
 
     signal.signal(signal.SIGINT, sigint_handler)
 
@@ -93,39 +93,39 @@ def code_validation(checks: list) -> Lis
     banner('Code validation')
     results: List[Tuple[str, int]] = []
     for check in checks:
-        result = check.main()
-        test_name = check.__name__.split('_')[-1]
-        results.append(('Check {}'.format(test_name), result))
+	result = check.main()
+	test_name = check.__name__.split('_')[-1]
+	results.append(('Check {}'.format(test_name), result))
     return results
 
 
 def do_build(build_info: List[Build], output_path: str) -> \
-        List[Tuple[Build, subprocess.Popen]]:
+	List[Tuple[Build, subprocess.Popen]]:
     build_status: List[Tuple[Build, subprocess.Popen]] = []
     results: List[Tuple[str, int]] = []
 
     if not os.path.exists(output_path):
-        os.makedirs(output_path)
+	os.makedirs(output_path)
 
     files = []
     for build in build_info:
-        file_path = os.path.join(output_path, build.file_name())
-        files.append(open(file_path, "w", encoding="utf-8"))
+	file_path = os.path.join(output_path, build.file_name())
+	files.append(open(file_path, "w", encoding="utf-8"))
 
-        build_id = subprocess.Popen(
-                                build.command(),
-                                shell=True,
-                                stdout=files[-1],
-                                stderr=subprocess.STDOUT)
-
-        build_status.append((build, build_id))
-        print('Test building [{}]'.format(build.tag()))
-        print('[CMD] {}'.format(build.command()))
+	build_id = subprocess.Popen(
+				build.command(),
+				shell=True,
+				stdout=files[-1],
+				stderr=subprocess.STDOUT)
+
+	build_status.append((build, build_id))
+	print('Test building [{}]'.format(build.tag()))
+	print('[CMD] {}'.format(build.command()))
 
     for i, (build, build_id) in enumerate(build_status):
-        build_id.communicate()
-        results.append((build.tag(), build_id.returncode))
-        files[i].close()
+	build_id.communicate()
+	results.append((build.tag(), build_id.returncode))
+	files[i].close()
     return results
 
 
@@ -133,12 +133,12 @@ def print_results(results: List[Tuple[st
     banner('Tests summary')
     total_success = 0
     for result in results:
-        if result[1] == 0:
-            total_success += 1
-            verbose_result = "Success"
-        else:
-            verbose_result = "Failed"
-        print("{}: {}".format(result[0], verbose_result))
+	if result[1] == 0:
+	    total_success += 1
+	    verbose_result = "Success"
+	else:
+	    verbose_result = "Failed"
+	print("{}: {}".format(result[0], verbose_result))
 
     assert total_success <= len(results)
     return (total_success, len(results))
@@ -146,83 +146,83 @@ def print_results(results: List[Tuple[st
 
 def analyze_results(success: int, total: int) -> int:
     print("{} / {} passed ({}% pass rate)".format(success, total,
-                                                  int(success * 100 / total)))
+						  int(success * 100 / total)))
     return 1 if success < total else 0
 
 
 def check_errors(ignore_errors: bool, results: List[Tuple[str, int]]) -> bool:
     return not ignore_errors and len(list(filter(lambda x: x[1] != 0,
-                                                 results)))
+						 results)))
 
 
 def main(ignore_errors: bool, skip_container: bool, log_level: str,
-         output_path: str):
+	 output_path: str):
     # This code is only applicable if there is valid docker instance
     # On CI there is no docker instance at the moment
     if not skip_container:
-        try:
-            client = docker.from_env()
-            banner("Spawning container")
-
-            try:
-                return dockerize(client)
-            except Exception as ex:
-                print(ex)
-                return 1
+	try:
+	    client = docker.from_env()
+	    banner("Spawning container")
+
+	    try:
+		return dockerize(client)
+	    except Exception as ex:
+		print(ex)
+		return 1
 
-        except DockerException:
-            pass
+	except DockerException:
+	    pass
     else:
-        banner("Skipping spawning container")
+	banner("Skipping spawning container")
 
     results = []
 
     results.extend(code_validation(code_validations))
     if check_errors(ignore_errors, results):
-        print('Errors detected! Excecution stopped')
-        return analyze_results(*print_results(results))
+	print('Errors detected! Excecution stopped')
+	return analyze_results(*print_results(results))
 
     banner('Test building products')
 
     if output_path == "":
-        output_path = os.path.join("/tmp", "scp")
+	output_path = os.path.join("/tmp", "scp")
     output_path = os.path.join(output_path, "build-output")
 
     for product in products:
-        if log_level != "":
-            product.log_level = Parameter(log_level)
-        results.extend(do_build(product.builds, output_path))
-        if check_errors(ignore_errors, results):
-            print('Errors detected! Excecution stopped')
-            return analyze_results(*print_results(results))
+	if log_level != "":
+	    product.log_level = Parameter(log_level)
+	results.extend(do_build(product.builds, output_path))
+	if check_errors(ignore_errors, results):
+	    print('Errors detected! Excecution stopped')
+	    return analyze_results(*print_results(results))
 
     return analyze_results(*print_results(results))
 
 
 def parse_args():
     parser = argparse.ArgumentParser(
-        description='Perform basic checks to SCP-Firmware and build for all \
-                     supported platforms, modes and compilers.')
+	description='Perform basic checks to SCP-Firmware and build for all \
+		     supported platforms, modes and compilers.')
 
     parser.add_argument('-i', '--ignore-errors', dest='ignore_errors',
-                        required=False, default=False, action='store_true',
-                        help='Ignore errors and continue testing.')
+			required=False, default=False, action='store_true',
+			help='Ignore errors and continue testing.')
 
     parser.add_argument('-ll', '--log-level', dest='log_level',
-                        required=False, default="", type=str,
-                        action='store', help='Build every product with the \
-                        specified log level.')
+			required=False, default="", type=str,
+			action='store', help='Build every product with the \
+			specified log level.')
 
     parser.add_argument('-bod', '--build-output-dir', dest='output_path',
-                        required=False, default="", type=str, action='store',
-                        help='Parent directory of the "build-output" directory\
-                        , the one were the build logs will be stored in.\n \
-                        If bod is not given, the default location is /tmp/scp/\
-                        build-output')
+			required=False, default="", type=str, action='store',
+			help='Parent directory of the "build-output" directory\
+			, the one were the build logs will be stored in.\n \
+			If bod is not given, the default location is /tmp/scp/\
+			build-output')
 
     parser.add_argument('-sc', '--skip-container', dest='skip_container',
-                        required=False, default=False, action='store_true',
-                        help='Skip container execution.')
+			required=False, default=False, action='store_true',
+			help='Skip container execution.')
 
     return parser.parse_args()
 
@@ -230,4 +230,4 @@ def parse_args():
 if __name__ == "__main__":
     args = parse_args()
     sys.exit(main(args.ignore_errors, args.skip_container, args.log_level,
-                  args.output_path))
+		  args.output_path))
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/tools/cppcheck_wrapper.py
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/tools/cppcheck_wrapper.py
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/tools/cppcheck_wrapper.py
@@ -43,7 +43,7 @@ tool_name = "CPPCHECK"
 # Helper function
 def print_msg(message, output):
     if output:
-        print("[{}]  {}".format(tool_name, message))
+	print("[{}]  {}".format(tool_name, message))
 
 
 def main():
@@ -55,56 +55,56 @@ def main():
 
     # Check if output of the tool (e.g. cppcheck) is to be displayed
     if re.search("verbose", tool_args):
-        verbose = True
+	verbose = True
     else:
-        verbose = False
+	verbose = False
 
     # If yes, then remove verbose string from argument list.
     if verbose:
-        tool_args.replace("verbose", "")
+	tool_args.replace("verbose", "")
 
     # Check version
     r = subprocess.Popen(tool_path + " --version",
-                         shell=True,
-                         stdout=subprocess.PIPE,
-                         stderr=subprocess.PIPE)
+			 shell=True,
+			 stdout=subprocess.PIPE,
+			 stderr=subprocess.PIPE)
 
     out, err = r.communicate()
 
     tool_version = out.decode("utf-8", "ignore")
 
     if out:
-        print_msg("------>", output=verbose)
-        print_msg("cppcheck {}"
-                  " version {} ".format(tool_path, tool_version),
-                  output=verbose)
-
-        if not re.search(required_tool_version, tool_version):
-            print_msg("Warning: recommended"
-                      " {} version is {}"
-                      " but found {}".format(tool_name,
-                                             required_tool_version,
-                                             tool_version),
-                      output=True)
+	print_msg("------>", output=verbose)
+	print_msg("cppcheck {}"
+		  " version {} ".format(tool_path, tool_version),
+		  output=verbose)
+
+	if not re.search(required_tool_version, tool_version):
+	    print_msg("Warning: recommended"
+		      " {} version is {}"
+		      " but found {}".format(tool_name,
+					     required_tool_version,
+					     tool_version),
+		      output=True)
     if err:
-        print_msg("Error: failed to find"
-                  " binary at {}".format(tool_path),
-                  output=True)
-        return 1
+	print_msg("Error: failed to find"
+		  " binary at {}".format(tool_path),
+		  output=True)
+	return 1
 
     print_msg(cmd, output=verbose)
 
     r = subprocess.Popen(cmd,
-                         shell=True,
-                         stdout=subprocess.PIPE,
-                         stderr=subprocess.PIPE)
+			 shell=True,
+			 stdout=subprocess.PIPE,
+			 stderr=subprocess.PIPE)
 
     out, err = r.communicate()
 
     if out:
-        print_msg("" + out.decode("utf-8", "ignore"), output=verbose)
+	print_msg("" + out.decode("utf-8", "ignore"), output=verbose)
     if err:
-        print_msg("" + err.decode("utf-8", "ignore"), output=True)
+	print_msg("" + err.decode("utf-8", "ignore"), output=True)
 
     print_msg("<------", output=verbose)
     return r.returncode
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/tools/gen_module_code.py
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/tools/gen_module_code.py
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/tools/gen_module_code.py
@@ -23,63 +23,63 @@ DEFAULT_PATH = 'build/'
 
 FILENAME_H = "fwk_module_idx.h"
 TEMPLATE_H = "/* This file was auto generated using {} */\n" \
-             "#ifndef FWK_MODULE_IDX_H\n" \
-             "#define FWK_MODULE_IDX_H\n" \
-             "\n" \
-             "#include <fwk_id.h>\n" \
-             "\n" \
-             "enum fwk_module_idx {{\n" \
-             "{}" \
-             "}};\n" \
-             "\n" \
-             "{}" \
-             "\n" \
-             "#endif /* FWK_MODULE_IDX_H */\n"
+	     "#ifndef FWK_MODULE_IDX_H\n" \
+	     "#define FWK_MODULE_IDX_H\n" \
+	     "\n" \
+	     "#include <fwk_id.h>\n" \
+	     "\n" \
+	     "enum fwk_module_idx {{\n" \
+	     "{}" \
+	     "}};\n" \
+	     "\n" \
+	     "{}" \
+	     "\n" \
+	     "#endif /* FWK_MODULE_IDX_H */\n"
 
 FILENAME_C = "fwk_module_list.c"
 TEMPLATE_C = "/* This file was auto generated using {} */\n" \
-             "#include <stddef.h>\n" \
-             "#include <fwk_module.h>\n" \
-             "\n" \
-             "{}" \
-             "\n" \
-             "const struct fwk_module *module_table[] = {{\n" \
-             "{}" \
-             "}};\n" \
-             "\n" \
-             "const struct fwk_module_config *module_config_table[] = {{\n" \
-             "{}" \
-             "}};\n"
+	     "#include <stddef.h>\n" \
+	     "#include <fwk_module.h>\n" \
+	     "\n" \
+	     "{}" \
+	     "\n" \
+	     "const struct fwk_module *module_table[] = {{\n" \
+	     "{}" \
+	     "}};\n" \
+	     "\n" \
+	     "const struct fwk_module_config *module_config_table[] = {{\n" \
+	     "{}" \
+	     "}};\n"
 
 
 def generate_file(path, filename, content):
     full_filename = os.path.join(path, filename)
 
     try:
-        with open(full_filename) as f:
-            rewrite = f.read() != content
+	with open(full_filename) as f:
+	    rewrite = f.read() != content
     except FileNotFoundError:
-        rewrite = True
+	rewrite = True
 
     if rewrite:
-        with tempfile.NamedTemporaryFile(prefix="gen-module-code",
-                                         dir=path,
-                                         delete=False,
-                                         mode="wt") as f:
-            print("[GEN] {}...".format(full_filename))
-            f.write(content)
-        os.replace(f.name, full_filename)
+	with tempfile.NamedTemporaryFile(prefix="gen-module-code",
+					 dir=path,
+					 delete=False,
+					 mode="wt") as f:
+	    print("[GEN] {}...".format(full_filename))
+	    f.write(content)
+	os.replace(f.name, full_filename)
 
 
 def generate_header(path, modules):
     enum = ""
     const = ""
     for idx, module in enumerate(modules):
-        enum += "    FWK_MODULE_IDX_{} = {},\n".format(module.upper(), idx)
-        const += "static const fwk_id_t fwk_module_id_{} = " \
-                 "FWK_ID_MODULE_INIT(FWK_MODULE_IDX_{});\n".format(module,
-                                                                   module
-                                                                   .upper())
+	enum += "    FWK_MODULE_IDX_{} = {},\n".format(module.upper(), idx)
+	const += "static const fwk_id_t fwk_module_id_{} = " \
+		 "FWK_ID_MODULE_INIT(FWK_MODULE_IDX_{});\n".format(module,
+								   module
+								   .upper())
 
     enum += "    FWK_MODULE_IDX_COUNT = {},\n".format(idx + 1)
 
@@ -92,34 +92,34 @@ def generate_c(path, modules):
     config_entry = ""
     extern_entry = ""
     for module in modules:
-        extern_entry += "extern const struct fwk_module module_{};\n"\
-            .format(module.lower())
-        extern_entry += "extern const struct fwk_module_config config_{};\n"\
-            .format(module.lower())
-        module_entry += "    &module_{},\n".format(module.lower())
-        config_entry += "    &config_{},\n".format(module.lower())
+	extern_entry += "extern const struct fwk_module module_{};\n"\
+	    .format(module.lower())
+	extern_entry += "extern const struct fwk_module_config config_{};\n"\
+	    .format(module.lower())
+	module_entry += "    &module_{},\n".format(module.lower())
+	config_entry += "    &config_{},\n".format(module.lower())
 
     content = TEMPLATE_C.format(sys.argv[0], extern_entry, module_entry,
-                                config_entry)
+				config_entry)
     generate_file(path, FILENAME_C, content)
 
 
 def main():
     parser = argparse.ArgumentParser(description="Generates a header file and \
-        source file enumerating the modules that are included in a firmware.")
+	source file enumerating the modules that are included in a firmware.")
 
     parser.add_argument('modules',
-                        metavar='module',
-                        type=str,
-                        nargs='*',
-                        help='A list of module names that are included in a \
-                            firmware.')
+			metavar='module',
+			type=str,
+			nargs='*',
+			help='A list of module names that are included in a \
+			    firmware.')
 
     parser.add_argument('-p', '--path',
-                        help='Path to the location where generated files are \
-                        written. If the files exist then they will be \
-                        overwritten.',
-                        default=DEFAULT_PATH)
+			help='Path to the location where generated files are \
+			written. If the files exist then they will be \
+			overwritten.',
+			default=DEFAULT_PATH)
 
     args = parser.parse_args()
 
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/tools/product.py
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/tools/product.py
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/tools/product.py
@@ -31,46 +31,46 @@ class Build:
     variant: Parameter = None
 
     def tag(self):
-        build_str = 'Product: {} - Toolchain: {} - Mode: {}'.format(
-                                                        self.name,
-                                                        self.toolchain.name,
-                                                        self.build_type.name)
-        if self.variant:
-            build_str += ' - Variant {}'.format(self.variant.name)
-        if self.log_level:
-            build_str += ' - Log Level {}'.format(self.log_level.name)
-        return build_str
+	build_str = 'Product: {} - Toolchain: {} - Mode: {}'.format(
+							self.name,
+							self.toolchain.name,
+							self.build_type.name)
+	if self.variant:
+	    build_str += ' - Variant {}'.format(self.variant.name)
+	if self.log_level:
+	    build_str += ' - Log Level {}'.format(self.log_level.name)
+	return build_str
 
     def file_name(self):
-        filename = self.name + "_" + self.toolchain.name + "_" + \
-            self.build_type.name[0]
-        if self.log_level:
-            filename += "_" + self.log_level.name
-        if self.variant:
-            filename += "_" + self.variant.name[0]
-        filename += ".txt"
-        return filename
+	filename = self.name + "_" + self.toolchain.name + "_" + \
+	    self.build_type.name[0]
+	if self.log_level:
+	    filename += "_" + self.log_level.name
+	if self.variant:
+	    filename += "_" + self.variant.name[0]
+	filename += ".txt"
+	return filename
 
     def command(self):
-        cmd = 'make -f Makefile.cmake '
-        cmd += 'PRODUCT={} TOOLCHAIN={} MODE={} '.format(self.name,
-                                                         self.toolchain.name,
-                                                         self.build_type.name)
-        if self.log_level:
-            cmd += 'LOG_LEVEL={} '.format(self.log_level.name)
-        if self.variant:
-            cmd += 'PLATFORM_VARIANT=\"{}\" '.format(self.variant.name)
-            for extra in self.variant.arguments:
-                cmd += Build.__extra_arg__(extra)
-        for extra in self.toolchain.arguments:
-            cmd += Build.__extra_arg__(extra)
-        for extra in self.build_type.arguments:
-            cmd += Build.__extra_arg__(extra)
-        return cmd
+	cmd = 'make -f Makefile.cmake '
+	cmd += 'PRODUCT={} TOOLCHAIN={} MODE={} '.format(self.name,
+							 self.toolchain.name,
+							 self.build_type.name)
+	if self.log_level:
+	    cmd += 'LOG_LEVEL={} '.format(self.log_level.name)
+	if self.variant:
+	    cmd += 'PLATFORM_VARIANT=\"{}\" '.format(self.variant.name)
+	    for extra in self.variant.arguments:
+		cmd += Build.__extra_arg__(extra)
+	for extra in self.toolchain.arguments:
+	    cmd += Build.__extra_arg__(extra)
+	for extra in self.build_type.arguments:
+	    cmd += Build.__extra_arg__(extra)
+	return cmd
 
     @classmethod
     def __extra_arg__(cls, parameter: str) -> str:
-        return 'EXTRA_CONFIG_ARGS+={} '.format(parameter)
+	return 'EXTRA_CONFIG_ARGS+={} '.format(parameter)
 
 
 @dataclass
@@ -81,27 +81,27 @@ class Product:
     '''
     name: str
     toolchains: List[Parameter] = field(default_factory=lambda: [
-        Parameter('GNU'),
-        Parameter('ArmClang'),
-        Parameter('Clang', ('-DSCP_LLVM_SYSROOT_CC=arm-none-eabi-gcc',)),
-        ])
+	Parameter('GNU'),
+	Parameter('ArmClang'),
+	Parameter('Clang', ('-DSCP_LLVM_SYSROOT_CC=arm-none-eabi-gcc',)),
+	])
     build_types: List[Parameter] = field(default_factory=lambda: [
-        Parameter('debug'),
-        Parameter('release'),
-        ])
+	Parameter('debug'),
+	Parameter('release'),
+	])
     variants: List[Parameter] = field(default_factory=lambda: [None])
     log_level: Parameter = None
 
     @property
     def builds(self) -> List[Build]:
-        builds = []
-        for toolchain in self.toolchains:
-            for build_type in self.build_types:
-                for variant in self.variants:
-                    builds.append(Build(
-                        self.name,
-                        toolchain,
-                        build_type,
-                        self.log_level,
-                        variant))
-        return builds
+	builds = []
+	for toolchain in self.toolchains:
+	    for build_type in self.build_types:
+		for variant in self.variants:
+		    builds.append(Build(
+			self.name,
+			toolchain,
+			build_type,
+			self.log_level,
+			variant))
+	return builds
Index: optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/tools/yaml-format.py
===================================================================
--- optee-os-qsmp-2550.orig/core/lib/scmi-server/SCP-firmware/tools/yaml-format.py
+++ optee-os-qsmp-2550/core/lib/scmi-server/SCP-firmware/tools/yaml-format.py
@@ -20,14 +20,14 @@ from io import StringIO
 
 def colorize(diff):
     for line in diff:
-        if line.startswith("@"):
-            yield colorama.Fore.BLUE + line + colorama.Fore.RESET
-        elif line.startswith("+"):
-            yield colorama.Fore.GREEN + line + colorama.Fore.RESET
-        elif line.startswith("-"):
-            yield colorama.Fore.RED + line + colorama.Fore.RESET
-        else:
-            yield line
+	if line.startswith("@"):
+	    yield colorama.Fore.BLUE + line + colorama.Fore.RESET
+	elif line.startswith("+"):
+	    yield colorama.Fore.GREEN + line + colorama.Fore.RESET
+	elif line.startswith("-"):
+	    yield colorama.Fore.RED + line + colorama.Fore.RESET
+	else:
+	    yield line
 
 
 def main():
@@ -37,39 +37,39 @@ def main():
 
     parser_common = argparse.ArgumentParser(add_help=False)
     parser_common.add_argument("sources",
-                               help="list of source files to format or check",
-                               nargs="*")
+			       help="list of source files to format or check",
+			       nargs="*")
     parser_common.add_argument("-q", "--quiet", action="store_true",
-                               help="suppress output intended for humans")
+			       help="suppress output intended for humans")
 
     subparsers = parser.add_subparsers(dest="command")
 
     subparser_diff = subparsers.add_parser("diff",
-                                           help=("generate a unified diff " +
-                                                 "of required changes"),
-                                           parents=[parser_common])
+					   help=("generate a unified diff " +
+						 "of required changes"),
+					   parents=[parser_common])
 
     subparser_diff.add_argument("--check",
-                                action="store_true",
-                                help=("exit with an error status code " +
-                                      "if changes are required"))
+				action="store_true",
+				help=("exit with an error status code " +
+				      "if changes are required"))
 
     subparser_diff.add_argument("-o", "--output",
-                                help="file to write the unified diff file to")
+				help="file to write the unified diff file to")
 
     subparser_format = subparsers.add_parser("format",
-                                             help=("automatically format " +
-                                                   "source files"),
-                                             parents=[parser_common])
+					     help=("automatically format " +
+						   "source files"),
+					     parents=[parser_common])
 
     subparser_format_output_group = \
-        subparser_format.add_mutually_exclusive_group()
+	subparser_format.add_mutually_exclusive_group()
     subparser_format_output_group.add_argument("-i", "--in-place",
-                                               action="store_true",
-                                               help="format the file in-place")
+					       action="store_true",
+					       help="format the file in-place")
     subparser_format_output_group.add_argument("-o", "--output",
-                                               help=("file to write " +
-                                                     "output data to"))
+					       help=("file to write " +
+						     "output data to"))
 
     # fmt: on
 
@@ -80,47 +80,47 @@ def main():
     result = os.EX_OK
 
     for source in args.sources:
-        source = os.path.relpath(source)
+	source = os.path.relpath(source)
 
-        with open(source, "r") as istream:
-            idata = istream.read()
+	with open(source, "r") as istream:
+	    idata = istream.read()
 
-        data = yaml.round_trip_load(idata)
+	data = yaml.round_trip_load(idata)
 
-        with StringIO() as odata:
-            yaml.round_trip_dump(data, odata)
-
-            odata = odata.getvalue()
-
-        if ("in_place" in args) and args.in_place:
-            ostream = open(source, "w")
-        elif ("output" in args) and args.output:
-            ostream = open(args.output, "w")
-        else:
-            ostream = sys.stdout
-
-        if args.command == "diff":
-            if idata != odata:
-                if not args.quiet:
-                    print("Style violations found: " + source, file=sys.stderr)
-
-                if args.check:
-                    result = os.EX_DATAERR
-
-                diff = difflib.unified_diff(
-                    idata.splitlines(keepends=True),
-                    odata.splitlines(keepends=True),
-                    fromfile=source + " (original)",
-                    tofile=source + " (reformatted)",
-                )
-
-                for line in colorize(diff):
-                    ostream.write(line)
-        elif args.command == "format":
-            ostream.write(odata)
+	with StringIO() as odata:
+	    yaml.round_trip_dump(data, odata)
+
+	    odata = odata.getvalue()
+
+	if ("in_place" in args) and args.in_place:
+	    ostream = open(source, "w")
+	elif ("output" in args) and args.output:
+	    ostream = open(args.output, "w")
+	else:
+	    ostream = sys.stdout
+
+	if args.command == "diff":
+	    if idata != odata:
+		if not args.quiet:
+		    print("Style violations found: " + source, file=sys.stderr)
+
+		if args.check:
+		    result = os.EX_DATAERR
+
+		diff = difflib.unified_diff(
+		    idata.splitlines(keepends=True),
+		    odata.splitlines(keepends=True),
+		    fromfile=source + " (original)",
+		    tofile=source + " (reformatted)",
+		)
+
+		for line in colorize(diff):
+		    ostream.write(line)
+	elif args.command == "format":
+	    ostream.write(odata)
 
-        if ostream != sys.stdout:
-            ostream.close()
+	if ostream != sys.stdout:
+	    ostream.close()
 
     return result
 
