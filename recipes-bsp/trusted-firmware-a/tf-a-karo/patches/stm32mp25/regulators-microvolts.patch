Index: tf-a-txmp-2550/drivers/st/pmic/stpmic1.c
===================================================================
--- tf-a-txmp-2550.orig/drivers/st/pmic/stpmic1.c
+++ tf-a-txmp-2550/drivers/st/pmic/stpmic1.c
@@ -14,16 +14,16 @@
 
 struct regul_struct {
 	const char *dt_node_name;
-	const uint16_t *voltage_table;
+	const uint32_t *voltage_table;
+	uint16_t control_reg;
+	uint16_t low_power_reg;
+	uint16_t pull_down_reg;
+	uint16_t mask_reset_reg;
+	uint16_t icc_reg;
 	uint8_t voltage_table_size;
-	uint8_t control_reg;
 	uint8_t enable_mask;
-	uint8_t low_power_reg;
-	uint8_t pull_down_reg;
 	uint8_t pull_down;
-	uint8_t mask_reset_reg;
 	uint8_t mask_reset;
-	uint8_t icc_reg;
 	uint8_t icc_mask;
 };
 
@@ -35,404 +35,404 @@ static uint16_t pmic_i2c_addr;
  */
 static bool ldo3_special_mode;
 
-/* Voltage tables in mV */
-static const uint16_t buck1_voltage_table[] = {
-	725,
-	725,
-	725,
-	725,
-	725,
-	725,
-	750,
-	775,
-	800,
-	825,
-	850,
-	875,
-	900,
-	925,
-	950,
-	975,
-	1000,
-	1025,
-	1050,
-	1075,
-	1100,
-	1125,
-	1150,
-	1175,
-	1200,
-	1225,
-	1250,
-	1275,
-	1300,
-	1325,
-	1350,
-	1375,
-	1400,
-	1425,
-	1450,
-	1475,
-	1500,
-	1500,
-	1500,
-	1500,
-	1500,
-	1500,
-	1500,
-	1500,
-	1500,
-	1500,
-	1500,
-	1500,
-	1500,
-	1500,
-	1500,
-	1500,
-	1500,
-	1500,
-	1500,
-	1500,
-	1500,
-	1500,
-	1500,
-	1500,
-	1500,
-	1500,
-	1500,
-	1500,
-};
-
-static const uint16_t buck2_voltage_table[] = {
-	1000,
-	1000,
-	1000,
-	1000,
-	1000,
-	1000,
-	1000,
-	1000,
-	1000,
-	1000,
-	1000,
-	1000,
-	1000,
-	1000,
-	1000,
-	1000,
-	1000,
-	1000,
-	1050,
-	1050,
-	1100,
-	1100,
-	1150,
-	1150,
-	1200,
-	1200,
-	1250,
-	1250,
-	1300,
-	1300,
-	1350,
-	1350,
-	1400,
-	1400,
-	1450,
-	1450,
-	1500,
-};
-
-static const uint16_t buck3_voltage_table[] = {
-	1000,
-	1000,
-	1000,
-	1000,
-	1000,
-	1000,
-	1000,
-	1000,
-	1000,
-	1000,
-	1000,
-	1000,
-	1000,
-	1000,
-	1000,
-	1000,
-	1000,
-	1000,
-	1000,
-	1000,
-	1100,
-	1100,
-	1100,
-	1100,
-	1200,
-	1200,
-	1200,
-	1200,
-	1300,
-	1300,
-	1300,
-	1300,
-	1400,
-	1400,
-	1400,
-	1400,
-	1500,
-	1600,
-	1700,
-	1800,
-	1900,
-	2000,
-	2100,
-	2200,
-	2300,
-	2400,
-	2500,
-	2600,
-	2700,
-	2800,
-	2900,
-	3000,
-	3100,
-	3200,
-	3300,
-	3400,
-};
-
-static const uint16_t buck4_voltage_table[] = {
-	600,
-	625,
-	650,
-	675,
-	700,
-	725,
-	750,
-	775,
-	800,
-	825,
-	850,
-	875,
-	900,
-	925,
-	950,
-	975,
-	1000,
-	1025,
-	1050,
-	1075,
-	1100,
-	1125,
-	1150,
-	1175,
-	1200,
-	1225,
-	1250,
-	1275,
-	1300,
-	1300,
-	1350,
-	1350,
-	1400,
-	1400,
-	1450,
-	1450,
-	1500,
-	1600,
-	1700,
-	1800,
-	1900,
-	2000,
-	2100,
-	2200,
-	2300,
-	2400,
-	2500,
-	2600,
-	2700,
-	2800,
-	2900,
-	3000,
-	3100,
-	3200,
-	3300,
-	3400,
-	3500,
-	3600,
-	3700,
-	3800,
-	3900,
-};
-
-static const uint16_t ldo1_voltage_table[] = {
-	1700,
-	1700,
-	1700,
-	1700,
-	1700,
-	1700,
-	1700,
-	1700,
-	1700,
-	1800,
-	1900,
-	2000,
-	2100,
-	2200,
-	2300,
-	2400,
-	2500,
-	2600,
-	2700,
-	2800,
-	2900,
-	3000,
-	3100,
-	3200,
-	3300,
-};
-
-static const uint16_t ldo2_voltage_table[] = {
-	1700,
-	1700,
-	1700,
-	1700,
-	1700,
-	1700,
-	1700,
-	1700,
-	1700,
-	1800,
-	1900,
-	2000,
-	2100,
-	2200,
-	2300,
-	2400,
-	2500,
-	2600,
-	2700,
-	2800,
-	2900,
-	3000,
-	3100,
-	3200,
-	3300,
-};
-
-static const uint16_t ldo3_voltage_table[] = {
-	1700,
-	1700,
-	1700,
-	1700,
-	1700,
-	1700,
-	1700,
-	1700,
-	1700,
-	1800,
-	1900,
-	2000,
-	2100,
-	2200,
-	2300,
-	2400,
-	2500,
-	2600,
-	2700,
-	2800,
-	2900,
-	3000,
-	3100,
-	3200,
-	3300,
-	3300,
-	3300,
-	3300,
-	3300,
-	3300,
-	3300,
+/* Voltage tables in uv */
+static const uint32_t buck1_voltage_table[] = {
+	725000,
+	725000,
+	725000,
+	725000,
+	725000,
+	725000,
+	750000,
+	775000,
+	800000,
+	825000,
+	850000,
+	875000,
+	900000,
+	925000,
+	950000,
+	975000,
+	1000000,
+	1025000,
+	1050000,
+	1075000,
+	1100000,
+	1125000,
+	1150000,
+	1175000,
+	1200000,
+	1225000,
+	1250000,
+	1275000,
+	1300000,
+	1325000,
+	1350000,
+	1375000,
+	1400000,
+	1425000,
+	1450000,
+	1475000,
+	1500000,
+	1500000,
+	1500000,
+	1500000,
+	1500000,
+	1500000,
+	1500000,
+	1500000,
+	1500000,
+	1500000,
+	1500000,
+	1500000,
+	1500000,
+	1500000,
+	1500000,
+	1500000,
+	1500000,
+	1500000,
+	1500000,
+	1500000,
+	1500000,
+	1500000,
+	1500000,
+	1500000,
+	1500000,
+	1500000,
+	1500000,
+	1500000,
+};
+
+static const uint32_t buck2_voltage_table[] = {
+	1000000,
+	1000000,
+	1000000,
+	1000000,
+	1000000,
+	1000000,
+	1000000,
+	1000000,
+	1000000,
+	1000000,
+	1000000,
+	1000000,
+	1000000,
+	1000000,
+	1000000,
+	1000000,
+	1000000,
+	1000000,
+	1050000,
+	1050000,
+	1100000,
+	1100000,
+	1150000,
+	1150000,
+	1200000,
+	1200000,
+	1250000,
+	1250000,
+	1300000,
+	1300000,
+	1350000,
+	1350000,
+	1400000,
+	1400000,
+	1450000,
+	1450000,
+	1500000,
+};
+
+static const uint32_t buck3_voltage_table[] = {
+	1000000,
+	1000000,
+	1000000,
+	1000000,
+	1000000,
+	1000000,
+	1000000,
+	1000000,
+	1000000,
+	1000000,
+	1000000,
+	1000000,
+	1000000,
+	1000000,
+	1000000,
+	1000000,
+	1000000,
+	1000000,
+	1000000,
+	1000000,
+	1100000,
+	1100000,
+	1100000,
+	1100000,
+	1200000,
+	1200000,
+	1200000,
+	1200000,
+	1300000,
+	1300000,
+	1300000,
+	1300000,
+	1400000,
+	1400000,
+	1400000,
+	1400000,
+	1500000,
+	1600000,
+	1700000,
+	1800000,
+	1900000,
+	2000000,
+	2100000,
+	2200000,
+	2300000,
+	2400000,
+	2500000,
+	2600000,
+	2700000,
+	2800000,
+	2900000,
+	3000000,
+	3100000,
+	3200000,
+	3300000,
+	3400000,
+};
+
+static const uint32_t buck4_voltage_table[] = {
+	600000,
+	625000,
+	650000,
+	675000,
+	700000,
+	725000,
+	750000,
+	775000,
+	800000,
+	825000,
+	850000,
+	875000,
+	900000,
+	925000,
+	950000,
+	975000,
+	1000000,
+	1025000,
+	1050000,
+	1075000,
+	1100000,
+	1125000,
+	1150000,
+	1175000,
+	1200000,
+	1225000,
+	1250000,
+	1275000,
+	1300000,
+	1300000,
+	1350000,
+	1350000,
+	1400000,
+	1400000,
+	1450000,
+	1450000,
+	1500000,
+	1600000,
+	1700000,
+	1800000,
+	1900000,
+	2000000,
+	2100000,
+	2200000,
+	2300000,
+	2400000,
+	2500000,
+	2600000,
+	2700000,
+	2800000,
+	2900000,
+	3000000,
+	3100000,
+	3200000,
+	3300000,
+	3400000,
+	3500000,
+	3600000,
+	3700000,
+	3800000,
+	3900000,
+};
+
+static const uint32_t ldo1_voltage_table[] = {
+	1700000,
+	1700000,
+	1700000,
+	1700000,
+	1700000,
+	1700000,
+	1700000,
+	1700000,
+	1700000,
+	1800000,
+	1900000,
+	2000000,
+	2100000,
+	2200000,
+	2300000,
+	2400000,
+	2500000,
+	2600000,
+	2700000,
+	2800000,
+	2900000,
+	3000000,
+	3100000,
+	3200000,
+	3300000,
+};
+
+static const uint32_t ldo2_voltage_table[] = {
+	1700000,
+	1700000,
+	1700000,
+	1700000,
+	1700000,
+	1700000,
+	1700000,
+	1700000,
+	1700000,
+	1800000,
+	1900000,
+	2000000,
+	2100000,
+	2200000,
+	2300000,
+	2400000,
+	2500000,
+	2600000,
+	2700000,
+	2800000,
+	2900000,
+	3000000,
+	3100000,
+	3200000,
+	3300000,
+};
+
+static const uint32_t ldo3_voltage_table[] = {
+	1700000,
+	1700000,
+	1700000,
+	1700000,
+	1700000,
+	1700000,
+	1700000,
+	1700000,
+	1700000,
+	1800000,
+	1900000,
+	2000000,
+	2100000,
+	2200000,
+	2300000,
+	2400000,
+	2500000,
+	2600000,
+	2700000,
+	2800000,
+	2900000,
+	3000000,
+	3100000,
+	3200000,
+	3300000,
+	3300000,
+	3300000,
+	3300000,
+	3300000,
+	3300000,
+	3300000,
 };
 
 /* Special mode table is used for sink source OR bypass mode */
-static const uint16_t ldo3_special_mode_table[] = {
-	0,
+static const uint32_t ldo3_special_mode_table[] = {
+	0000,
 };
 
-static const uint16_t ldo5_voltage_table[] = {
-	1700,
-	1700,
-	1700,
-	1700,
-	1700,
-	1700,
-	1700,
-	1700,
-	1700,
-	1800,
-	1900,
-	2000,
-	2100,
-	2200,
-	2300,
-	2400,
-	2500,
-	2600,
-	2700,
-	2800,
-	2900,
-	3000,
-	3100,
-	3200,
-	3300,
-	3400,
-	3500,
-	3600,
-	3700,
-	3800,
-	3900,
-};
-
-static const uint16_t ldo6_voltage_table[] = {
-	900,
-	1000,
-	1100,
-	1200,
-	1300,
-	1400,
-	1500,
-	1600,
-	1700,
-	1800,
-	1900,
-	2000,
-	2100,
-	2200,
-	2300,
-	2400,
-	2500,
-	2600,
-	2700,
-	2800,
-	2900,
-	3000,
-	3100,
-	3200,
-	3300,
-};
-
-static const uint16_t ldo4_voltage_table[] = {
-	3300,
+static const uint32_t ldo5_voltage_table[] = {
+	1700000,
+	1700000,
+	1700000,
+	1700000,
+	1700000,
+	1700000,
+	1700000,
+	1700000,
+	1700000,
+	1800000,
+	1900000,
+	2000000,
+	2100000,
+	2200000,
+	2300000,
+	2400000,
+	2500000,
+	2600000,
+	2700000,
+	2800000,
+	2900000,
+	3000000,
+	3100000,
+	3200000,
+	3300000,
+	3400000,
+	3500000,
+	3600000,
+	3700000,
+	3800000,
+	3900000,
+};
+
+static const uint32_t ldo6_voltage_table[] = {
+	900000,
+	1000000,
+	1100000,
+	1200000,
+	1300000,
+	1400000,
+	1500000,
+	1600000,
+	1700000,
+	1800000,
+	1900000,
+	2000000,
+	2100000,
+	2200000,
+	2300000,
+	2400000,
+	2500000,
+	2600000,
+	2700000,
+	2800000,
+	2900000,
+	3000000,
+	3100000,
+	3200000,
+	3300000,
+};
+
+static const uint32_t ldo4_voltage_table[] = {
+	3300000,
 };
 
-static const uint16_t vref_ddr_voltage_table[] = {
-	3300,
+static const uint32_t vref_ddr_voltage_table[] = {
+	3300000,
 };
 
-static const uint16_t fixed_5v_voltage_table[] = {
-	5000,
+static const uint32_t fixed_5v_voltage_table[] = {
+	5000000,
 };
 
 /* Table of Regulators in PMIC SoC */
@@ -622,13 +622,13 @@ static const struct regul_struct *get_re
 	return NULL;
 }
 
-static uint8_t voltage_to_index(const char *name, uint16_t millivolts)
+static uint8_t voltage_to_index(const char *name, uint32_t microvolts)
 {
 	const struct regul_struct *regul = get_regulator_data(name);
 	uint8_t i;
 
 	for (i = 0 ; i < regul->voltage_table_size ; i++) {
-		if (regul->voltage_table[i] == millivolts) {
+		if (regul->voltage_table[i] == microvolts) {
 			return i;
 		}
 	}
@@ -679,9 +679,9 @@ bool stpmic1_is_regulator_enabled(const
 	return (val & regul->enable_mask) == regul->enable_mask;
 }
 
-int stpmic1_regulator_voltage_set(const char *name, uint16_t millivolts)
+int stpmic1_regulator_voltage_set(const char *name, uint32_t microvolts)
 {
-	uint8_t voltage_index = voltage_to_index(name, millivolts);
+	uint8_t voltage_index = voltage_to_index(name, microvolts);
 	const struct regul_struct *regul = get_regulator_data(name);
 	uint8_t mask;
 
@@ -796,7 +796,7 @@ int stpmic1_active_discharge_mode_set(co
 	return -EPERM;
 }
 
-int stpmic1_regulator_levels_mv(const char *name, const uint16_t **levels,
+int stpmic1_regulator_levels_uv(const char *name, const uint32_t **levels,
 				size_t *levels_count)
 {
 	const struct regul_struct *regul = get_regulator_data(name);
@@ -847,20 +847,20 @@ int stpmic1_regulator_voltage_get(const
 	return (int)regul->voltage_table[value];
 }
 
-int stpmic1_register_read(uint8_t register_id,  uint8_t *value)
+int stpmic1_register_read(uint16_t register_id, uint8_t *value)
 {
 	return stm32_i2c_mem_read(pmic_i2c_handle, pmic_i2c_addr,
-				  (uint16_t)register_id,
+				  register_id,
 				  I2C_MEMADD_SIZE_8BIT, value,
 				  1, I2C_TIMEOUT_MS);
 }
 
-int stpmic1_register_write(uint8_t register_id, uint8_t value)
+int stpmic1_register_write(uint16_t register_id, uint8_t value)
 {
 	int status;
 
 	status = stm32_i2c_mem_write(pmic_i2c_handle, pmic_i2c_addr,
-				     (uint16_t)register_id,
+				     register_id,
 				     I2C_MEMADD_SIZE_8BIT, &value,
 				     1, I2C_TIMEOUT_MS);
 
@@ -886,7 +886,7 @@ int stpmic1_register_write(uint8_t regis
 	return status;
 }
 
-int stpmic1_register_update(uint8_t register_id, uint8_t value, uint8_t mask)
+int stpmic1_register_update(uint16_t register_id, uint8_t value, uint8_t mask)
 {
 	int status;
 	uint8_t val;
@@ -914,7 +914,7 @@ void stpmic1_dump_regulators(void)
 	for (i = 0U; i < MAX_REGUL; i++) {
 		const char *name __unused = regulators_table[i].dt_node_name;
 
-		VERBOSE("PMIC regul %s: %sable, %dmV",
+		VERBOSE("PMIC regul %s: %sable, %duV",
 			name,
 			stpmic1_is_regulator_enabled(name) ? "en" : "dis",
 			stpmic1_regulator_voltage_get(name));
Index: tf-a-txmp-2550/drivers/st/pmic/stpmic2.c
===================================================================
--- tf-a-txmp-2550.orig/drivers/st/pmic/stpmic2.c
+++ tf-a-txmp-2550/drivers/st/pmic/stpmic2.c
@@ -22,69 +22,69 @@
 
 struct regul_struct {
 	const char *name;
-	const uint16_t *volt_table;
+	const uint32_t *volt_table;
+	uint16_t volt_cr;
+	uint16_t en_cr;
+	uint16_t alt_en_cr;
+	uint16_t msrt_reg;
+	uint16_t pd_reg;
+	uint16_t ocp_reg;
 	uint8_t volt_table_size;
-	uint8_t volt_cr;
 	uint8_t volt_shift;
-	uint8_t en_cr;
-	uint8_t alt_en_cr;
-	uint8_t msrt_reg;
 	uint8_t msrt_mask;
-	uint8_t pd_reg;
 	uint8_t pd_val;
-	uint8_t ocp_reg;
 	uint8_t ocp_mask;
 };
 
-/* Voltage tables in mV */
-static const uint16_t buck_low_volt_table[] = {
-	500U, 510U, 520U, 530U, 540U, 550U, 560U, 570U, 580U, 590U,
-	600U, 610U, 620U, 630U, 640U, 650U, 660U, 670U, 680U, 690U,
-	700U, 710U, 720U, 730U, 740U, 750U, 760U, 770U, 780U, 790U,
-	800U, 810U, 820U, 830U, 840U, 850U, 860U, 870U, 880U, 890U,
-	900U, 910U, 920U, 930U, 940U, 950U, 960U, 970U, 980U, 990U,
-	1000U, 1010U, 1020U, 1030U, 1040U, 1050U, 1060U, 1070U, 1080U, 1090U,
-	1100U, 1110U, 1120U, 1130U, 1140U, 1150U, 1160U, 1170U, 1180U, 1190U,
-	1200U, 1210U, 1220U, 1230U, 1240U, 1250U, 1260U, 1270U, 1280U, 1290U,
-	1300U, 1310U, 1320U, 1330U, 1340U, 1350U, 1360U, 1370U, 1380U, 1390U,
-	1400U, 1410U, 1420U, 1430U, 1440U, 1450U, 1460U, 1470U, 1480U, 1490U,
-	1500U, 1500U, 1500U, 1500U, 1500U, 1500U, 1500U, 1500U, 1500U, 1500U,
-	1500U, 1500U, 1500U, 1500U, 1500U, 1500U, 1500U, 1500U, 1500U, 1500U,
-	1500U, 1500U, 1500U, 1500U, 1500U, 1500U, 1500U, 1500U
+/* Voltage tables in uv */
+static const uint32_t buck_low_volt_table[] = {
+	500000U, 510000U, 520000U, 530000U, 540000U, 550000U, 560000U, 570000U, 580000U, 590000U,
+	600000U, 610000U, 620000U, 630000U, 640000U, 650000U, 660000U, 670000U, 680000U, 690000U,
+	700000U, 710000U, 720000U, 730000U, 740000U, 750000U, 760000U, 770000U, 780000U, 790000U,
+	800000U, 810000U, 820000U, 830000U, 840000U, 850000U, 860000U, 870000U, 880000U, 890000U,
+	900000U, 910000U, 920000U, 930000U, 940000U, 950000U, 960000U, 970000U, 980000U, 990000U,
+	1000000U, 1010000U, 1020000U, 1030000U, 1040000U, 1050000U, 1060000U, 1070000U, 1080000U, 1090000U,
+	1100000U, 1110000U, 1120000U, 1130000U, 1140000U, 1150000U, 1160000U, 1170000U, 1180000U, 1190000U,
+	1200000U, 1210000U, 1220000U, 1230000U, 1240000U, 1250000U, 1260000U, 1270000U, 1280000U, 1290000U,
+	1300000U, 1310000U, 1320000U, 1330000U, 1340000U, 1350000U, 1360000U, 1370000U, 1380000U, 1390000U,
+	1400000U, 1410000U, 1420000U, 1430000U, 1440000U, 1450000U, 1460000U, 1470000U, 1480000U, 1490000U,
+	1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U,
+	1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U,
+	1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U,
 };
 
-static const uint16_t buck_high_volt_table[] = {
-	1500U, 1500U, 1500U, 1500U, 1500U, 1500U, 1500U, 1500U, 1500U, 1500U,
-	1500U, 1500U, 1500U, 1500U, 1500U, 1500U, 1500U, 1500U, 1500U, 1500U,
-	1500U, 1500U, 1500U, 1500U, 1500U, 1500U, 1500U, 1500U, 1500U, 1500U,
-	1500U, 1500U, 1500U, 1500U, 1500U, 1500U, 1500U, 1500U, 1500U, 1500U,
-	1500U, 1500U, 1500U, 1500U, 1500U, 1500U, 1500U, 1500U, 1500U, 1500U,
-	1500U, 1500U, 1500U, 1500U, 1500U, 1500U, 1500U, 1500U, 1500U, 1500U,
-	1500U, 1500U, 1500U, 1500U, 1500U, 1500U, 1500U, 1500U, 1500U, 1500U,
-	1500U, 1500U, 1500U, 1500U, 1500U, 1500U, 1500U, 1500U, 1500U, 1500U,
-	1500U, 1500U, 1500U, 1500U, 1500U, 1500U, 1500U, 1500U, 1500U, 1500U,
-	1500U, 1500U, 1500U, 1500U, 1500U, 1500U, 1500U, 1500U, 1500U, 1500U,
-	1500U, 1600U, 1700U, 1800U, 1900U, 2000U, 2100U, 2200U, 2300U, 2400U,
-	2500U, 2600U, 2700U, 2800U, 2900U, 3000U, 3100U, 3200U, 3300U, 3400U,
-	3500U, 3600U, 3700U, 3800U, 3900U, 4000U, 4100U, 4200U
+static const uint32_t buck_high_volt_table[] = {
+	1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U,
+	1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U,
+	1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U,
+	1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U,
+	1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U,
+	1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U,
+	1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U,
+	1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U,
+	1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U,
+	1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U, 1500000U,
+	1500000U, 1600000U, 1700000U, 1800000U, 1900000U, 2000000U, 2100000U, 2200000U, 2300000U, 2400000U,
+	2500000U, 2600000U, 2700000U, 2800000U, 2900000U, 3000000U, 3100000U, 3200000U, 3300000U, 3400000U,
+	3500000U, 3600000U, 3700000U, 3800000U, 3900000U, 4000000U, 4100000U, 4200000U,
 };
 
-static const uint16_t ldo235678_volt_table[] = {
-	900U, 1000U, 1100U, 1200U, 1300U, 1400U, 1500U, 1600U, 1700U, 1800U,
-	1900U, 2000U, 2100U, 2200U, 2300U, 2400U, 2500U, 2600U, 2700U, 2800U,
-	2900U, 3000U, 3100U, 3200U, 3300U, 3400U, 3500U, 3600U, 3700U, 3800U,
-	3900U, 4000U
+static const uint32_t ldo235678_volt_table[] = {
+	900000U, 1000000U, 1100000U, 1200000U, 1300000U, 1400000U, 1500000U, 1600000U, 1700000U, 1800000U,
+	1900000U, 2000000U, 2100000U, 2200000U, 2300000U, 2400000U, 2500000U, 2600000U, 2700000U, 2800000U,
+	2900000U, 3000000U, 3100000U, 3200000U, 3300000U, 3400000U, 3500000U, 3600000U, 3700000U, 3800000U,
+	3900000U, 4000000U,
 };
 
-static const uint16_t ldo1_volt_table[] = {
-	1800U,
+static const uint32_t ldo1_volt_table[] = {
+	1800000U,
 };
 
-static const uint16_t ldo4_volt_table[] = {
-	3300U,
+static const uint32_t ldo4_volt_table[] = {
+	3300000U,
 };
 
-static const uint16_t refddr_volt_table[] = {
+static const uint32_t refddr_volt_table[] = {
 	0,
 };
 
@@ -181,14 +181,14 @@ static const struct regul_struct regul_t
 };
 
 int stpmic2_register_read(struct pmic_handle_s *pmic,
-			  uint8_t register_id, uint8_t *value)
+			  uint16_t register_id, uint8_t *value)
 {
 	int ret = stm32_i2c_mem_read(pmic->i2c_handle,
 				     pmic->i2c_addr,
-				     (uint16_t)register_id,
+				     register_id,
 				     I2C_MEMADD_SIZE_8BIT, value,
 				     1, I2C_TIMEOUT_MS);
-	if (ret != 0) {
+	if (ret) {
 		ERROR("Failed to read reg:0x%x\n", register_id);
 	}
 
@@ -196,15 +196,15 @@ int stpmic2_register_read(struct pmic_ha
 }
 
 int stpmic2_register_write(struct pmic_handle_s *pmic,
-			   uint8_t register_id, uint8_t value)
+			   uint16_t register_id, uint8_t value)
 {
 	uint8_t val = value;
 	int ret = stm32_i2c_mem_write(pmic->i2c_handle,
 				      pmic->i2c_addr,
-				      (uint16_t)register_id,
+				      register_id,
 				      I2C_MEMADD_SIZE_8BIT, &val,
 				      1, I2C_TIMEOUT_MS);
-	if (ret != 0) {
+	if (ret) {
 		ERROR("Failed to write reg:0x%x\n", register_id);
 	}
 
@@ -212,17 +212,16 @@ int stpmic2_register_write(struct pmic_h
 }
 
 int stpmic2_register_update(struct pmic_handle_s *pmic,
-			    uint8_t register_id, uint8_t value, uint8_t mask)
+			    uint16_t register_id, uint8_t value, uint8_t mask)
 {
 	int status;
-	uint8_t val = 0U;
+	uint8_t val;
 
 	status = stpmic2_register_read(pmic, register_id, &val);
-	if (status != 0) {
+	if (status)
 		return status;
-	}
 
-	val = (val & ((uint8_t)~mask)) | (value & mask);
+	val = (val & ~mask) | (value & mask);
 
 	VERBOSE("REG:0x%x v=0x%x mask=0x%x -> 0x%x\n",
 		register_id, value, mask, val);
@@ -235,11 +234,10 @@ int stpmic2_regulator_set_state(struct p
 {
 	const struct regul_struct *regul = &regul_table[id];
 
-	if (enable) {
+	if (enable)
 		return stpmic2_register_update(pmic, regul->en_cr, 1U, 1U);
-	} else {
+	else
 		return stpmic2_register_update(pmic, regul->en_cr, 0, 1U);
-	}
 }
 
 int stpmic2_regulator_get_state(struct pmic_handle_s *pmic,
@@ -248,7 +246,7 @@ int stpmic2_regulator_get_state(struct p
 	const struct regul_struct *regul = &regul_table[id];
 	uint8_t val;
 
-	if (stpmic2_register_read(pmic, regul->en_cr, &val) != 0) {
+	if (stpmic2_register_read(pmic, regul->en_cr, &val)) {
 		return RET_ERROR_GENERIC;
 	}
 
@@ -257,8 +255,8 @@ int stpmic2_regulator_get_state(struct p
 	return RET_SUCCESS;
 }
 
-int stpmic2_regulator_levels_mv(struct pmic_handle_s *pmic,
-				uint8_t id, const uint16_t **levels,
+int stpmic2_regulator_levels_uv(struct pmic_handle_s *pmic,
+				uint8_t id, const uint32_t **levels,
 				size_t *levels_count)
 {
 	const struct regul_struct *regul = &regul_table[id];
@@ -278,7 +276,7 @@ int stpmic2_regulator_levels_mv(struct p
 }
 
 int stpmic2_regulator_get_voltage(struct pmic_handle_s *pmic,
-				  uint8_t id, uint16_t *val)
+				  uint8_t id, uint32_t *val)
 {
 	const struct regul_struct *regul = &regul_table[id];
 	uint8_t value = 0U;
@@ -289,8 +287,8 @@ int stpmic2_regulator_get_voltage(struct
 	}
 
 	mask = regul->volt_table_size - 1U;
-	if (mask != 0U) {
-		if (stpmic2_register_read(pmic, regul->volt_cr, &value) != 0) {
+	if (mask) {
+		if (stpmic2_register_read(pmic, regul->volt_cr, &value)) {
 			return RET_ERROR_GENERIC;
 		}
 
@@ -307,34 +305,32 @@ int stpmic2_regulator_get_voltage(struct
 }
 
 static size_t voltage_to_index(const struct regul_struct *regul,
-			       uint16_t millivolts)
+			       uint32_t microvolts)
 {
 	unsigned int i;
 
 	assert(regul->volt_table);
 	for (i = 0U; i < regul->volt_table_size; i++) {
-		if (regul->volt_table[i] == millivolts) {
+		if (regul->volt_table[i] == microvolts)
 			return i;
-		}
 	}
 
 	return VOLTAGE_INDEX_INVALID;
 }
 
 int stpmic2_regulator_set_voltage(struct pmic_handle_s *pmic,
-				  uint8_t id, uint16_t millivolts)
+				  uint8_t id, uint32_t microvolts)
 {
 	const struct regul_struct *regul = &regul_table[id];
 	size_t index;
 	uint8_t mask;
 
-	if (!regul->volt_table_size) {
+	if (!regul->volt_table_size)
 		return RET_SUCCESS;
-	}
 
 	mask = regul->volt_table_size - 1U;
 
-	index = voltage_to_index(regul, millivolts);
+	index = voltage_to_index(regul, microvolts);
 	if (index == VOLTAGE_INDEX_INVALID) {
 		return RET_ERROR_GENERIC;
 	}
@@ -350,13 +346,11 @@ static int stpmic2_update_en_crs(struct
 {
 	const struct regul_struct *regul = &regul_table[id];
 
-	if (stpmic2_register_update(pmic, regul->en_cr, value, mask) != 0) {
+	if (stpmic2_register_update(pmic, regul->en_cr, value, mask))
 		return RET_ERROR_GENERIC;
-	}
 
-	if (stpmic2_register_update(pmic, regul->alt_en_cr, value, mask) != 0) {
+	if (stpmic2_register_update(pmic, regul->alt_en_cr, value, mask))
 		return RET_ERROR_GENERIC;
-	}
 
 	return RET_SUCCESS;
 }
@@ -371,27 +365,20 @@ int stpmic2_regulator_get_prop(struct pm
 
 	switch (prop) {
 	case STPMIC2_BYPASS:
-		if ((id <= STPMIC2_BUCK7) || (id == STPMIC2_LDO1) ||
-		    (id == STPMIC2_LDO4) || (id == STPMIC2_REFDDR) ||
-		    (pmic->ref_id != PMIC_REF_ID_STPMIC25)) {
+		if (id <= STPMIC2_BUCK7 || id == STPMIC2_LDO1 ||
+		    id == STPMIC2_LDO4 || id == STPMIC2_REFDDR ||
+		    pmic->ref_id != PMIC_REF_ID_STPMIC25)
 			return 0;
-		}
 
-		if (stpmic2_register_read(pmic, regul->en_cr, &val) != 0) {
+		if (stpmic2_register_read(pmic, regul->en_cr, &val))
 			return -EIO;
-		}
 
-		if ((val & LDO_BYPASS) != 0) {
-			return 1;
-		}
+		return !!(val & LDO_BYPASS);
 
-		break;
 	default:
 		ERROR("Invalid prop %u\n", prop);
 		panic();
 	}
-
-	return 0;
 }
 
 int stpmic2_regulator_set_prop(struct pmic_handle_s *pmic, uint8_t id,
@@ -407,30 +394,28 @@ int stpmic2_regulator_set_prop(struct pm
 					       regul->pd_val,
 					       regul->pd_val);
 	case STPMIC2_MASK_RESET:
-		if (!regul->msrt_mask) {
+		if (!regul->msrt_mask)
 			return RET_ERROR_NOT_SUPPORTED;
-		}
+
 		/* enable mask reset */
 		return stpmic2_register_update(pmic, regul->msrt_reg,
 					       regul->msrt_mask,
 					       regul->msrt_mask);
 	case STPMIC2_BYPASS:
-		if ((id <= STPMIC2_BUCK7) || (id == STPMIC2_LDO1) ||
-		    (id == STPMIC2_LDO4) || (id == STPMIC2_REFDDR) ||
-		    (pmic->ref_id != PMIC_REF_ID_STPMIC25)) {
+		if (id <= STPMIC2_BUCK7 || id == STPMIC2_LDO1 ||
+		    id == STPMIC2_LDO4 || id == STPMIC2_REFDDR ||
+		    pmic->ref_id != PMIC_REF_ID_STPMIC25)
 			return RET_ERROR_NOT_SUPPORTED;
-		}
 
 		/* clear sink source mode */
-		if ((id == STPMIC2_LDO3) && (arg != 0U)) {
-			if (stpmic2_update_en_crs(pmic, id, 0, LDO3_SNK_SRC) != 0) {
+		if (id == STPMIC2_LDO3 && arg) {
+			if (stpmic2_update_en_crs(pmic, id, 0, LDO3_SNK_SRC))
 				return RET_ERROR_GENERIC;
-			}
 		}
 
 		/* enable bypass mode */
 		return stpmic2_update_en_crs(pmic, id,
-					     (arg != 0U) ? LDO_BYPASS : 0,
+					     arg ? LDO_BYPASS : 0,
 					     LDO_BYPASS);
 	case STPMIC2_SINK_SOURCE:
 		if (id != STPMIC2_LDO3) {
@@ -438,7 +423,7 @@ int stpmic2_regulator_set_prop(struct pm
 		}
 
 		/* clear bypass mode */
-		if (stpmic2_update_en_crs(pmic, id, 0, LDO_BYPASS) != 0) {
+		if (stpmic2_update_en_crs(pmic, id, 0, LDO_BYPASS)) {
 			return RET_ERROR_GENERIC;
 		}
 
@@ -468,7 +453,7 @@ void stpmic2_dump_regulators(struct pmic
 	char const *name;
 
 	for (i = 0U; i < ARRAY_SIZE(regul_table); i++) {
-		uint16_t val;
+		uint32_t val;
 		bool state;
 
 		if (!regul_table[i].volt_cr) {
@@ -480,7 +465,7 @@ void stpmic2_dump_regulators(struct pmic
 
 		name = regul_table[i].name;
 
-		VERBOSE("PMIC regul %s: %s, %dmV\n",
+		VERBOSE("PMIC regul %s: %s, %duV\n",
 			name, state ? "EN" : "DIS", val);
 	}
 }
Index: tf-a-txmp-2550/drivers/st/regulator/regulator_core.c
===================================================================
--- tf-a-txmp-2550.orig/drivers/st/regulator/regulator_core.c
+++ tf-a-txmp-2550/drivers/st/regulator/regulator_core.c
@@ -204,23 +204,23 @@ int regulator_is_enabled(const struct rd
  * Set regulator voltage
  *
  * @rdev - pointer to rdev struct
- * @mvolt - Target voltage level in millivolt
+ * @uvolt - Target voltage level in microvolt
  * Return 0 if succeed, non 0 else.
  */
-int regulator_set_voltage(struct rdev *rdev, uint16_t mvolt)
+int regulator_set_voltage(struct rdev *rdev, uint32_t uvolt)
 {
 	int ret;
 
 	assert(rdev != NULL);
 
-	VERBOSE("%s: set mvolt\n", rdev->desc->node_name);
+	VERBOSE("%s: set uvolt\n", rdev->desc->node_name);
 
-	if ((mvolt < rdev->min_mv) || (mvolt > rdev->max_mv)) {
+	if ((uvolt < rdev->min_uv) || (uvolt > rdev->max_uv)) {
 		return -EPERM;
 	}
 
-	if (regulator_get_voltage(rdev) == mvolt) {
-		return 0U;
+	if (regulator_get_voltage(rdev) == (int)uvolt) {
+		return 0;
 	}
 
 	if (rdev->desc->ops->set_voltage == NULL) {
@@ -229,10 +229,10 @@ int regulator_set_voltage(struct rdev *r
 
 	lock_driver(rdev);
 
-	ret = rdev->desc->ops->set_voltage(rdev->desc, mvolt);
+	ret = rdev->desc->ops->set_voltage(rdev->desc, uvolt);
 	if (ret < 0) {
-		ERROR("regul %s set volt failed: err:%d\n",
-		      rdev->desc->node_name, ret);
+		ERROR("regul %s failed to set voltage to %uuV: err: %d\n",
+		      rdev->desc->node_name, uvolt, ret);
 	}
 
 	unlock_driver(rdev);
@@ -248,14 +248,14 @@ int regulator_set_voltage(struct rdev *r
  */
 int regulator_set_min_voltage(struct rdev *rdev)
 {
-	return regulator_set_voltage(rdev, rdev->min_mv);
+	return regulator_set_voltage(rdev, rdev->min_uv);
 }
 
 /*
  * Get regulator voltage
  *
  * @rdev - pointer to rdev struct
- * Return milli volts if succeed, <0 else.
+ * Return micro volts if succeed, <0 else.
  */
 int regulator_get_voltage(const struct rdev *rdev)
 {
@@ -266,7 +266,7 @@ int regulator_get_voltage(const struct r
 	VERBOSE("%s: get volt\n", rdev->desc->node_name);
 
 	if (rdev->desc->ops->get_voltage == NULL) {
-		return rdev->min_mv;
+		return rdev->min_uv;
 	}
 
 	lock_driver(rdev);
@@ -286,14 +286,15 @@ int regulator_get_voltage(const struct r
  * List regulator voltages
  *
  * @rdev - pointer to rdev struct
- * @levels - out: array of supported millitvolt levels from min to max value
- * @count - out: number of possible millivolt values
+ * @levels - out: array of supported microtvolt levels from min to max value
+ * @count - out: number of possible microvolt values
  * Return 0 if succeed, non 0 else.
  */
-int regulator_list_voltages(const struct rdev *rdev, const uint16_t **levels, size_t *count)
+int regulator_list_voltages(const struct rdev *rdev, const uint32_t **levels, size_t *count)
 {
 	int ret;
-	size_t n;
+	ssize_t n;
+	const uint32_t *volt_table = *levels;
 
 	assert(rdev != NULL);
 	assert(levels != NULL);
@@ -301,9 +302,8 @@ int regulator_list_voltages(const struct
 
 	VERBOSE("%s: list volt\n", rdev->desc->node_name);
 
-	if (rdev->desc->ops->list_voltages == NULL) {
+	if (rdev->desc->ops->list_voltages == NULL)
 		return -ENODEV;
-	}
 
 	lock_driver(rdev);
 
@@ -321,19 +321,18 @@ int regulator_list_voltages(const struct
 	 * Reduce the possible values depending on min and max from device-tree
 	 */
 	n = *count;
-	while ((n > 1U) && ((*levels)[n - 1U] > rdev->max_mv)) {
-		n--;
-	}
+	while (--n && volt_table[n] > rdev->max_uv)
+		;
 
 	/* Verify that max val is a valid value */
-	if (rdev->max_mv != (*levels)[n - 1]) {
+	if (n < 0 || rdev->max_uv != volt_table[n]) {
 		ERROR("regul %s: max value %u is invalid\n",
-		      rdev->desc->node_name, rdev->max_mv);
+		      rdev->desc->node_name, rdev->max_uv);
 		return -EINVAL;
 	}
-
-	while ((n > 1U) && ((*levels[0U]) < rdev->min_mv)) {
-		(*levels)++;
+	n++;
+	while (n > 1U && volt_table[0] < rdev->min_uv) {
+		volt_table++;
 		n--;
 	}
 
@@ -345,15 +344,16 @@ int regulator_list_voltages(const struct
 	}
 
 	/* Verify that min val is a valid vlue */
-	if (rdev->min_mv != (*levels)[0U]) {
+	if (rdev->min_uv != volt_table[0U]) {
 		ERROR("regul %s: min value %u is invalid\n",
-		      rdev->desc->node_name, rdev->min_mv);
+		      rdev->desc->node_name, rdev->min_uv);
 		return -EINVAL;
 	}
 
 	*count = n;
+	*levels = volt_table;
 
-	VERBOSE("rdev->min_mv=%u rdev->max_mv=%u\n", rdev->min_mv, rdev->max_mv);
+	VERBOSE("rdev->min_uv=%u rdev->max_uv=%u\n", rdev->min_uv, rdev->max_uv);
 
 	return 0;
 }
@@ -362,20 +362,19 @@ int regulator_list_voltages(const struct
  * Get regulator voltages range
  *
  * @rdev - pointer to rdev struct
- * @min_mv - out: min possible millivolt value
- * @max_mv - out: max possible millivolt value
+ * @min_uv - out: min possible microvolt value
+ * @max_uv - out: max possible microvolt value
  * Return 0 if succeed, non 0 else.
  */
-void regulator_get_range(const struct rdev *rdev, uint16_t *min_mv, uint16_t *max_mv)
+void regulator_get_range(const struct rdev *rdev, uint32_t *min_uv, uint32_t *max_uv)
 {
 	assert(rdev != NULL);
 
-	if (min_mv != NULL) {
-		*min_mv = rdev->min_mv;
-	}
-	if (max_mv != NULL) {
-		*max_mv = rdev->max_mv;
-	}
+	if (min_uv != NULL)
+		*min_uv = rdev->min_uv;
+
+	if (max_uv != NULL)
+		*max_uv = rdev->max_uv;
 }
 
 /*
@@ -461,7 +460,7 @@ static int parse_dt(struct rdev *rdev, i
 {
 	void *fdt;
 	const fdt32_t *cuint;
-	const uint16_t *levels;
+	const uint32_t *levels;
 	size_t size;
 	int ret;
 
@@ -475,37 +474,37 @@ static int parse_dt(struct rdev *rdev, i
 
 	cuint = fdt_getprop(fdt, node, "regulator-min-microvolt", NULL);
 	if (cuint != NULL) {
-		uint16_t min_mv;
+		uint32_t min_uv;
 
-		min_mv = (uint16_t)(fdt32_to_cpu(*cuint) / 1000U);
-		VERBOSE("%s: min_mv=%d\n", rdev->desc->node_name, (int)min_mv);
-		if (min_mv <= rdev->max_mv) {
-			rdev->min_mv = min_mv;
+		min_uv = fdt32_to_cpu(*cuint);
+		VERBOSE("%s: min_uv=%u\n", rdev->desc->node_name, min_uv);
+		if (min_uv <= rdev->max_uv) {
+			rdev->min_uv = min_uv;
 		} else {
-			ERROR("%s: min_mv=%d is too high\n",
-			      rdev->desc->node_name, (int)min_mv);
+			ERROR("%s: min_uv: %u is higher than max_uv: %u\n",
+			      rdev->desc->node_name, min_uv, rdev->max_uv);
 			return -EINVAL;
 		}
 	}
 
 	cuint = fdt_getprop(fdt, node, "regulator-max-microvolt", NULL);
 	if (cuint != NULL) {
-		uint16_t max_mv;
+		uint32_t max_uv;
 
-		max_mv = (uint16_t)(fdt32_to_cpu(*cuint) / 1000U);
-		VERBOSE("%s: max_mv=%d\n", rdev->desc->node_name, (int)max_mv);
-		if (max_mv >= rdev->min_mv) {
-			rdev->max_mv = max_mv;
+		max_uv = fdt32_to_cpu(*cuint);
+		VERBOSE("%s: max_uv=%u\n", rdev->desc->node_name, max_uv);
+		if (max_uv >= rdev->min_uv) {
+			rdev->max_uv = max_uv;
 		} else {
-			ERROR("%s: max_mv=%d is too low\n",
-			      rdev->desc->node_name, (int)max_mv);
+			ERROR("%s: max_uv: %u is lower than min_uv: %u\n",
+			      rdev->desc->node_name, max_uv, rdev->min_uv);
 			return -EINVAL;
 		}
 	}
 
 	/* validate that min and max values can be used */
 	ret = regulator_list_voltages(rdev, &levels, &size);
-	if ((ret != 0) && (ret != -ENODEV)) {
+	if (ret && ret != -ENODEV) {
 		return ret;
 	}
 
@@ -549,7 +548,7 @@ int regulator_register(const struct regu
 
 	if (rdev->desc->ops->list_voltages != NULL) {
 		int ret;
-		const uint16_t *levels;
+		const uint32_t *levels;
 		size_t count;
 
 		lock_driver(rdev);
@@ -564,10 +563,10 @@ int regulator_register(const struct regu
 			return ret;
 		}
 
-		rdev->min_mv = levels[0];
-		rdev->max_mv = levels[count - 1U];
+		rdev->min_uv = levels[0];
+		rdev->max_uv = levels[count - 1U];
 	} else {
-		rdev->max_mv = UINT16_MAX;
+		rdev->max_uv = UINT32_MAX;
 	}
 
 	return parse_dt(rdev, node);
Index: tf-a-txmp-2550/include/drivers/st/regulator.h
===================================================================
--- tf-a-txmp-2550.orig/include/drivers/st/regulator.h
+++ tf-a-txmp-2550/include/drivers/st/regulator.h
@@ -49,12 +49,12 @@ int regulator_enable(struct rdev *rdev);
 int regulator_disable(struct rdev *rdev);
 int regulator_is_enabled(const struct rdev *rdev);
 
-int regulator_set_voltage(struct rdev *rdev, uint16_t volt);
+int regulator_set_voltage(struct rdev *rdev, uint32_t volt);
 int regulator_set_min_voltage(struct rdev *rdev);
 int regulator_get_voltage(const struct rdev *rdev);
 
-int regulator_list_voltages(const struct rdev *rdev, const uint16_t **levels, size_t *count);
-void regulator_get_range(const struct rdev *rdev, uint16_t *min_mv, uint16_t *max_mv);
+int regulator_list_voltages(const struct rdev *rdev, const uint32_t **levels, size_t *count);
+void regulator_get_range(const struct rdev *rdev, uint32_t *min_uv, uint32_t *max_uv);
 int regulator_set_flag(struct rdev *rdev, uint16_t flag);
 
 /*
@@ -76,10 +76,10 @@ struct regul_description {
 struct regul_ops {
 	int (*set_state)(const struct regul_description *desc, bool state);
 	int (*get_state)(const struct regul_description *desc);
-	int (*set_voltage)(const struct regul_description *desc, uint16_t mv);
+	int (*set_voltage)(const struct regul_description *desc, uint32_t uv);
 	int (*get_voltage)(const struct regul_description *desc);
 	int (*list_voltages)(const struct regul_description *desc,
-			     const uint16_t **levels, size_t *count);
+			     const uint32_t **levels, size_t *count);
 	int (*set_flag)(const struct regul_description *desc, uint16_t flag);
 	void (*lock)(const struct regul_description *desc);
 	void (*unlock)(const struct regul_description *desc);
@@ -97,12 +97,12 @@ struct rdev {
 
 	int32_t phandle;
 
-	uint16_t min_mv;
-	uint16_t max_mv;
-
-	uint16_t flags;
+	uint32_t min_uv;
+	uint32_t max_uv;
 
 	uint32_t enable_ramp_delay;
+
+	uint16_t flags;
 };
 
 #endif /* REGULATOR_H */
Index: tf-a-txmp-2550/include/drivers/st/stpmic1.h
===================================================================
--- tf-a-txmp-2550.orig/include/drivers/st/stpmic1.h
+++ tf-a-txmp-2550/include/drivers/st/stpmic1.h
@@ -174,14 +174,14 @@
 
 int stpmic1_powerctrl_on(void);
 int stpmic1_switch_off(void);
-int stpmic1_register_read(uint8_t register_id, uint8_t *value);
-int stpmic1_register_write(uint8_t register_id, uint8_t value);
-int stpmic1_register_update(uint8_t register_id, uint8_t value, uint8_t mask);
+int stpmic1_register_read(uint16_t register_id, uint8_t *value);
+int stpmic1_register_write(uint16_t register_id, uint8_t value);
+int stpmic1_register_update(uint16_t register_id, uint8_t value, uint8_t mask);
 int stpmic1_regulator_enable(const char *name);
 int stpmic1_regulator_disable(const char *name);
 bool stpmic1_is_regulator_enabled(const char *name);
-int stpmic1_regulator_voltage_set(const char *name, uint16_t millivolts);
-int stpmic1_regulator_levels_mv(const char *name, const uint16_t **levels,
+int stpmic1_regulator_voltage_set(const char *name, uint32_t microvolts);
+int stpmic1_regulator_levels_uv(const char *name, const uint32_t **levels,
 				size_t *levels_count);
 int stpmic1_regulator_voltage_get(const char *name);
 int stpmic1_regulator_pull_down_set(const char *name);
Index: tf-a-txmp-2550/include/drivers/st/stpmic2.h
===================================================================
--- tf-a-txmp-2550.orig/include/drivers/st/stpmic2.h
+++ tf-a-txmp-2550/include/drivers/st/stpmic2.h
@@ -322,24 +322,24 @@ struct pmic_handle_s {
 };
 
 int stpmic2_register_read(struct pmic_handle_s *pmic,
-			  uint8_t register_id, uint8_t *value);
+			  uint16_t register_id, uint8_t *value);
 int stpmic2_register_write(struct pmic_handle_s *pmic,
-			   uint8_t register_id, uint8_t value);
+			   uint16_t register_id, uint8_t value);
 int stpmic2_register_update(struct pmic_handle_s *pmic,
-			    uint8_t register_id, uint8_t value, uint8_t mask);
+			    uint16_t register_id, uint8_t value, uint8_t mask);
 
 int stpmic2_regulator_set_state(struct pmic_handle_s *pmic,
 				uint8_t id, bool enable);
 int stpmic2_regulator_get_state(struct pmic_handle_s *pmic,
 				uint8_t id, bool *enabled);
 
-int stpmic2_regulator_levels_mv(struct pmic_handle_s *pmic,
-				uint8_t id, const uint16_t **levels,
+int stpmic2_regulator_levels_uv(struct pmic_handle_s *pmic,
+				uint8_t id, const uint32_t **levels,
 				size_t *levels_count);
 int stpmic2_regulator_get_voltage(struct pmic_handle_s *pmic,
-				  uint8_t id, uint16_t *val);
+				  uint8_t id, uint32_t *val);
 int stpmic2_regulator_set_voltage(struct pmic_handle_s *pmic,
-				  uint8_t id, uint16_t millivolts);
+				  uint8_t id, uint32_t microvolts);
 
 #if EVENT_LOG_LEVEL == LOG_LEVEL_VERBOSE
 void stpmic2_dump_regulators(struct pmic_handle_s *pmic);
Index: tf-a-txmp-2550/plat/st/common/stm32mp_dt.c
===================================================================
--- tf-a-txmp-2550.orig/plat/st/common/stm32mp_dt.c
+++ tf-a-txmp-2550/plat/st/common/stm32mp_dt.c
@@ -258,7 +258,7 @@ size_t dt_get_ddr_size(void)
 uint32_t dt_get_pwr_vdd_voltage(void)
 {
 	struct rdev *regul = dt_get_vdd_regulator();
-	uint16_t min;
+	uint32_t min;
 
 	if (regul == NULL) {
 		return 0;
@@ -266,7 +266,7 @@ uint32_t dt_get_pwr_vdd_voltage(void)
 
 	regulator_get_range(regul, &min, NULL);
 
-	return (uint32_t)min * 1000U;
+	return min;
 }
 
 /*******************************************************************************
Index: tf-a-txmp-2550/drivers/st/pmic/stm32mp_pmic2.c
===================================================================
--- tf-a-txmp-2550.orig/drivers/st/pmic/stm32mp_pmic2.c
+++ tf-a-txmp-2550/drivers/st/pmic/stm32mp_pmic2.c
@@ -24,7 +24,7 @@
 
 struct regul_handle_s {
 	const uint32_t id;
-	uint16_t bypass_mv;
+	uint32_t bypass_uv;
 };
 
 static struct pmic_handle_s pmic2_handle;
@@ -228,89 +228,83 @@ static int pmic2_get_state(const struct
 static int pmic2_get_voltage(const struct regul_description *desc)
 {
 	struct regul_handle_s *regul = (struct regul_handle_s *)desc->driver_data;
-	uint16_t mv;
+	uint32_t uv;
 
 	VERBOSE("%s: get volt\n", desc->node_name);
 
-	if (regul->bypass_mv != 0U) {
+	if (regul->bypass_uv != 0U) {
 		int ret;
 
 		/* If the regul is in bypass mode, return bypass value */
 		ret = stpmic2_regulator_get_prop(pmic2, regul->id, STPMIC2_BYPASS);
-		if (ret < 0) {
+		if (ret < 0)
 			return ret;
-		}
 
-		if (ret == 1) {
-			return regul->bypass_mv;
-		}
-	};
+		if (ret == 1)
+			return regul->bypass_uv;
+	}
 
-	if (stpmic2_regulator_get_voltage(pmic2, regul->id, &mv) < 0) {
+	if (stpmic2_regulator_get_voltage(pmic2, regul->id, &uv) < 0)
 		panic();
-	}
 
-	return mv;
+	return uv;
 }
 
-static int pmic2_set_voltage(const struct regul_description *desc, uint16_t mv)
+static int pmic2_set_voltage(const struct regul_description *desc, uint32_t uv)
 {
 	struct regul_handle_s *regul = (struct regul_handle_s *)desc->driver_data;
 
 	VERBOSE("%s: set volt\n", desc->node_name);
 
-	if (regul->bypass_mv != 0U) {
+	if (regul->bypass_uv != 0U) {
 		int ret;
 
-		/* If the regul is in bypass mode, authorize bypass mV */
+		/* If the regul is in bypass mode, authorize bypass µV */
 		ret = stpmic2_regulator_get_prop(pmic2, regul->id, STPMIC2_BYPASS);
-		if (ret < 0) {
+		if (ret < 0)
 			return ret;
-		}
 
-		if ((ret == 1) && (mv != regul->bypass_mv)) {
+		if (ret == 1 && uv != regul->bypass_uv)
 			return -EPERM;
-		}
-	};
+	}
 
-	return stpmic2_regulator_set_voltage(pmic2, regul->id, mv);
+	return stpmic2_regulator_set_voltage(pmic2, regul->id, uv);
 }
 
 static int pmic2_list_voltages(const struct regul_description *desc,
-			       const uint16_t **levels, size_t *count)
+			       const uint32_t **levels, size_t *count)
 {
 	struct regul_handle_s *regul = (struct regul_handle_s *)desc->driver_data;
 
 	VERBOSE("%s: list volt\n", desc->node_name);
 
-	if (regul->bypass_mv != 0U) {
+	if (regul->bypass_uv != 0U) {
 		int ret;
 
 		ret = stpmic2_regulator_get_prop(pmic2, regul->id, STPMIC2_BYPASS);
-		if (ret < 0) {
+		if (ret < 0)
 			return ret;
-		}
 
-		/* bypass is enabled, return a list with only bypass mV */
+		/* bypass is enabled, return a list with only bypass µV */
 		if (ret == 1) {
-			if (count != NULL) {
+			if (count != NULL)
 				*count = 1U;
-			}
-			if (levels != NULL) {
-				*levels = &regul->bypass_mv;
-			}
+
+			if (levels != NULL)
+				*levels = &regul->bypass_uv;
+
 			return 0;
 		}
-	};
+	}
 
-	return stpmic2_regulator_levels_mv(pmic2, regul->id, levels, count);
+	return stpmic2_regulator_levels_uv(pmic2, regul->id, levels, count);
 }
 
 static int pmic2_set_flag(const struct regul_description *desc, uint16_t flag)
 {
 	struct regul_handle_s *regul = (struct regul_handle_s *)desc->driver_data;
 	uint32_t id = regul->id;
-	int ret = -EPERM;
+	int ret;
 
 	VERBOSE("%s: set_flag 0x%x\n", desc->node_name, flag);
 
@@ -335,11 +329,7 @@ static int pmic2_set_flag(const struct r
 		panic();
 	}
 
-	if (ret != 0) {
-		return -EPERM;
-	}
-
-	return 0;
+	return ret;
 }
 
 int stpmic2_set_prop(const struct regul_description *desc, uint16_t prop, uint32_t value)
@@ -502,9 +492,9 @@ static int register_pmic2(void)
 		if (cuint != NULL) {
 			struct regul_handle_s *regul = (struct regul_handle_s *)desc->driver_data;
 
-			regul->bypass_mv = (uint16_t)(fdt32_to_cpu(*cuint) / 1000U);
-			VERBOSE("%s: bypass voltage=%umV\n", desc->node_name,
-				regul->bypass_mv);
+			regul->bypass_uv = fdt32_to_cpu(*cuint);
+			VERBOSE("%s: bypass voltage=%uV\n", desc->node_name,
+				regul->bypass_uv);
 		}
 
 		if (fdt_getprop(fdt, subnode, "st,mask-reset", NULL)  != NULL) {
Index: tf-a-txmp-2550/plat/st/stm32mp2/stm32mp2_pwr.c
===================================================================
--- tf-a-txmp-2550.orig/plat/st/stm32mp2/stm32mp2_pwr.c
+++ tf-a-txmp-2550/plat/st/stm32mp2/stm32mp2_pwr.c
@@ -13,7 +13,7 @@
 #include <platform_def.h>
 #include <stm32mp2_private.h>
 
-#define IO_VOLTAGE_THRESHOLD_MV 2700
+#define IO_VOLTAGE_THRESHOLD_UV 2700000
 
 struct pwr_regu {
 	const char *node_name;
@@ -93,7 +93,7 @@ static bool stm32mp2_pwr_domain_is_enabl
 static int stm32mp2_pwr_handle_vrsel(void *fdt, int node, const struct pwr_regu *regu)
 {
 	struct rdev *supply;
-	int mv;
+	int uv;
 
 	VERBOSE("Check VRSEL for %s\n", regu->node_name);
 
@@ -104,12 +104,12 @@ static int stm32mp2_pwr_handle_vrsel(voi
 		return 0;
 	}
 
-	mv = regulator_get_voltage(supply);
-	if (mv < 0) {
-		return mv;
+	uv = regulator_get_voltage(supply);
+	if (uv < 0) {
+		return uv;
 	}
 
-	if (mv < IO_VOLTAGE_THRESHOLD_MV) {
+	if (uv < IO_VOLTAGE_THRESHOLD_UV) {
 		VERBOSE("Enable VRSEL for %s\n", regu->node_name);
 
 		mmio_setbits_32(stm32mp_pwr_base() + regu->enable_reg, regu->vrsel_mask);
Index: tf-a-txmp-2550/drivers/st/pmic/stm32mp_pmic.c
===================================================================
--- tf-a-txmp-2550.orig/drivers/st/pmic/stm32mp_pmic.c
+++ tf-a-txmp-2550/drivers/st/pmic/stm32mp_pmic.c
@@ -306,19 +306,19 @@ static int pmic_get_voltage(const struct
 	return stpmic1_regulator_voltage_get(desc->node_name);
 }
 
-static int pmic_set_voltage(const struct regul_description *desc, uint16_t mv)
+static int pmic_set_voltage(const struct regul_description *desc, uint32_t uv)
 {
 	VERBOSE("%s: get volt\n", desc->node_name);
 
-	return stpmic1_regulator_voltage_set(desc->node_name, mv);
+	return stpmic1_regulator_voltage_set(desc->node_name, uv);
 }
 
 static int pmic_list_voltages(const struct regul_description *desc,
-			      const uint16_t **levels, size_t *count)
+			      const uint32_t **levels, size_t *count)
 {
 	VERBOSE("%s: list volt\n", desc->node_name);
 
-	return stpmic1_regulator_levels_mv(desc->node_name, levels, count);
+	return stpmic1_regulator_levels_uv(desc->node_name, levels, count);
 }
 
 static int pmic_set_flag(const struct regul_description *desc, uint16_t flag)
